{
 "description": "What changed in memory since a given ISO date: new facts learned, beliefs revised, facts retracted. The 'what have you learned about X since Monday?' query.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "since": {
    "description": "ISO date/datetime to diff from",
    "type": "string"
   },
   "subject": {
    "description": "Limit to one subject",
    "type": "string"
   }
  },
  "required": [
   "since"
  ],
  "type": "object"
 },
 "name": "diff"
}
