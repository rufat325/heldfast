{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "What one institutional manager NEW-ed, EXITED, INCREASED or DECREASED between two 13F quarters \u2014 the newsletter primitive: a portfolio snapshot is not news, a change is. Give the manager name; optionally the two report quarters to compare (YYYY-MM-DD, e.g. \"2026-03-31\"), defaulting to the two most recent quarters loaded for that manager. Returns four ranked lists (new positions, exits, adds, trims) each with issuer, CUSIP, value in both quarters and the dollar delta.",
 "inputSchema": {
  "examples": [
   {
    "limit": 25,
    "manager": "BERKSHIRE HATHAWAY"
   }
  ],
  "properties": {
   "compare_to": {
    "description": "The earlier (\"from\") quarter. Defaults to the manager's next-most-recent loaded quarter.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows per bucket (new/exited/increased/decreased), 1-100 (default 25).",
    "type": "number"
   },
   "manager": {
    "description": "Filing manager name or a distinctive fragment of it.",
    "type": "string"
   },
   "quarter": {
    "description": "The later (\"to\") quarter, e.g. \"2026-03-31\". Defaults to the manager's most recent loaded quarter.",
    "type": "string"
   }
  },
  "required": [
   "manager"
  ],
  "type": "object"
 },
 "name": "manager_changes",
 "title": "Manager Changes",
 "version": "v1.61d53b2f"
}
