{
 "description": "Write or update a text file in the workspace. Returns bytes written and content hash. When PROVENANCE_ENABLED=true, emits a signed provenance receipt after completion (fail-open).",
 "inputSchema": {
  "properties": {
   "content": {
    "description": "Full file content (UTF-8 text)",
    "type": "string"
   },
   "operation": {
    "description": "create | update | auto (default auto \u2014 detect from existing file)",
    "enum": [
     "create",
     "update",
     "auto"
    ],
    "type": "string"
   },
   "path": {
    "description": "Relative or absolute file path to write",
    "type": "string"
   }
  },
  "required": [
   "path",
   "content"
  ],
  "type": "object"
 },
 "name": "write_or_modify_file"
}
