{
 "annotations": {
  "destructiveHint": true
 },
 "description": "Pull changes from the connected source-control repository (requires the Source Control feature configured on the instance). force=true permanently discards local changes on the instance \u2014 cannot be undone.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "force": {
    "default": false,
    "description": "Discard local changes and force-pull",
    "type": "boolean"
   },
   "variables": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "Values for variables defined in the repository",
    "type": "object"
   }
  },
  "type": "object"
 },
 "name": "pull_source_control"
}
