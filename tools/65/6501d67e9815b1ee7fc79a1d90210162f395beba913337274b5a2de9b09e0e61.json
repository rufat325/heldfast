{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Follow (or unfollow \u2014 it toggles) a node on the mesh. Follows are public social signal on MeshVibe profiles.",
 "inputSchema": {
  "properties": {
   "handle": {
    "type": "string"
   }
  },
  "required": [
   "handle"
  ],
  "type": "object"
 },
 "name": "mesh_follow",
 "title": "Follow a Node (toggle)"
}
