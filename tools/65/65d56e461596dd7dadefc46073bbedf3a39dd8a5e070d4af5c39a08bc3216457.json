{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List all objects in the Three.js scene tree. Returns compact text tree by default (saves tokens). Set compact=false for full JSON.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "compact": {
    "description": "Compact text tree (default: true). Set false for full JSON with positions/geometry.",
    "type": "boolean"
   },
   "depth": {
    "description": "Max tree depth (default: 3)",
    "type": "number"
   },
   "maxChildren": {
    "description": "Max children per node (default: 15 compact, 100 JSON)",
    "type": "number"
   },
   "types": {
    "description": "Filter by type: Mesh, Light, Group, etc.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "scene_tree"
}
