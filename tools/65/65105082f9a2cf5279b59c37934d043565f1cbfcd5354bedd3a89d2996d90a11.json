{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Foreign exchange rates against the Malaysian Ringgit (MYR) from Bank Negara Malaysia. Latest rates for all currencies (default), one currency, or one currency on a specific historical date. Rates are published per session (snapshot time) and quote basis. Returns buying/selling/middle rates per currency unit.",
 "inputSchema": {
  "examples": [
   {
    "currency": "USD"
   },
   {
    "currency": "SGD",
    "date": "2026-05-28",
    "quote": "rm",
    "session": "1130"
   }
  ],
  "properties": {
   "currency": {
    "description": "ISO currency code, e.g. \"USD\", \"SGD\", \"EUR\". Omit to get all currencies. Required if \"date\" is given.",
    "type": "string"
   },
   "date": {
    "description": "Historical date YYYY-MM-DD, e.g. \"2026-05-28\". Only valid together with a \"currency\". Omit for latest.",
    "type": "string"
   },
   "quote": {
    "description": "Quote basis: \"rm\" (units of MYR per 1 foreign) or \"fx\" (units of foreign per 1 MYR).",
    "enum": [
     "rm",
     "fx"
    ],
    "type": "string"
   },
   "session": {
    "description": "Rate snapshot session time (24h). Defaults to BNM default if omitted.",
    "enum": [
     "0900",
     "1130",
     "1200",
     "1700"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "bnm_exchange_rates",
 "title": "Bnm Exchange Rates",
 "version": "v1.99c1f909"
}
