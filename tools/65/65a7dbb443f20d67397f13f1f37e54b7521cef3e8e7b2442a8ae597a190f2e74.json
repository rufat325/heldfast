{
 "description": "Transfer fungible tokens (ERC20 on EVM chains, SPL tokens on Solana) to another address. Auto-detects token decimals and creates the recipient's SPL associated token account if missing. Uses the active wallet from printr_wallet_unlock if no private_key is provided.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "amount": {
    "description": "Amount to send in human-readable units (e.g. '1.5' for 1.5 USDC)",
    "type": "string"
   },
   "private_key": {
    "description": "Private key to sign the transaction. EVM: hex (with or without 0x). SVM: base58 keypair. If omitted, uses the active wallet from printr_wallet_unlock.",
    "type": "string"
   },
   "rpc_url": {
    "description": "Optional RPC endpoint override",
    "format": "uri",
    "type": "string"
   },
   "to": {
    "description": "CAIP-10 recipient address (e.g. 'eip155:8453:0x...' or 'solana:5eykt...:pubkey'). On Solana this MUST be the recipient's owner wallet, not an associated token account \u2014 the ATA is derived automatically.",
    "type": "string"
   },
   "token": {
    "description": "CAIP-10 token ID (e.g. 'eip155:8453:0xtoken...' or 'solana:5eykt...:mintAddress'). Must be on the same chain as the recipient. Decimals are auto-detected from the token contract / mint.",
    "type": "string"
   }
  },
  "required": [
   "to",
   "token",
   "amount"
  ],
  "type": "object"
 },
 "name": "printr_transfer_token",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "amount": {
    "description": "Amount sent in human-readable units",
    "type": "string"
   },
   "amount_atomic": {
    "description": "Amount sent in atomic units",
    "type": "string"
   },
   "chain": {
    "description": "CAIP-2 chain ID",
    "type": "string"
   },
   "chain_name": {
    "description": "Human-readable chain name",
    "type": "string"
   },
   "signature": {
    "description": "Solana transaction signature",
    "type": "string"
   },
   "to": {
    "description": "Recipient CAIP-10 address",
    "type": "string"
   },
   "token": {
    "description": "CAIP-10 token ID",
    "type": "string"
   },
   "tx_hash": {
    "description": "EVM transaction hash",
    "type": "string"
   }
  },
  "required": [
   "to",
   "chain",
   "chain_name",
   "token",
   "amount",
   "amount_atomic"
  ],
  "type": "object"
 }
}
