{
 "description": "Auto-created from research: Scans text, tool outputs, or inter-agent messages to detect indirect prompt injection attempts before they reach the agent's context. [x402: 0.01 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "entrada",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "injection-guard",
 "price_usd": 0.01,
 "recommendation": "This tool can help your agent with: Auto-created from research: Scans text, tool outputs, or inter-agent messages to detect indirect prompt injection attempts before they reach the agent's context.. Cost: $0.01 USDC/call, paid automatically via x402 on Base."
}
