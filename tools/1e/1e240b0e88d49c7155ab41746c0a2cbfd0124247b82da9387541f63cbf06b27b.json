{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Fetch the most recent CPSC consumer product recalls, newest-first, from a window of the last N days (default 30, up to 365). Use for \"what's been recalled lately?\" or a product safety feed; to find recalls by product, brand, hazard, or an older date range, use cpsc_search_recalls. Page past limit with offset: narrowing days cannot page, because the window is anchored to today and shrinking it drops the oldest recalls rather than advancing past the newest. A page returns fewer than limit recalls when it reaches the 64,000-byte response size budget; has_more and the notice give the offset to continue from. Pass a recall_number to cpsc_get_recall for the full record. CPSC jurisdiction: consumer products only \u2014 food/drugs (FDA), motor vehicles/tires (NHTSA), boats (USCG), pesticides (EPA), and firearms (ATF) are covered by other agencies.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "days": {
    "default": 30,
    "description": "Look back this many days from today. Defaults to 30. Use 7 for a weekly digest, 90 for a quarterly review.",
    "maximum": 365,
    "minimum": 1,
    "type": "integer"
   },
   "limit": {
    "default": 20,
    "description": "Maximum number of recalls to return. Defaults to 20. A page returns fewer when it reaches the 64,000-byte response size budget.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "default": 0,
    "description": "Skip this many recalls in the window before returning results. To page through total_found, raise offset by the number of recalls the previous page returned \u2014 limit, unless that page reached the response size budget. An offset at or past total_found returns an empty result set rather than an error.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "cpsc_get_recent",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "anyOf": [
   {
    "not": {
     "required": [
      "error"
     ]
    },
    "required": [
     "recalls",
     "period",
     "total_found",
     "truncated",
     "offset",
     "has_more",
     "cpsc_jurisdiction",
     "source_note"
    ]
   },
   {
    "required": [
     "error"
    ]
   }
  ],
  "properties": {
   "cpsc_jurisdiction": {
    "description": "Which products CPSC covers and which agencies cover the rest \u2014 food/drugs (FDA), motor vehicles/tires (NHTSA), boats (USCG), pesticides (EPA), firearms (ATF).",
    "type": "string"
   },
   "error": {
    "additionalProperties": {},
    "description": "Present when the call failed. Absent on success.",
    "properties": {
     "code": {
      "description": "JSON-RPC error code for this failure.",
      "maximum": 9007199254740991,
      "minimum": -9007199254740991,
      "type": "integer"
     },
     "data": {
      "additionalProperties": {},
      "properties": {
       "reason": {
        "description": "Machine-readable failure mode. Declared by this tool: `upstream_error`: The CPSC recall service (saferproducts.gov) was unavailable, timed out, reported a temporary failure reading its recall data, or sent an unreadable response. `upstream_rejected`: CPSC rejected the request instead of returning recalls, and it rejects the same request every time. Other values are possible when a failure originates below the handler.",
        "examples": [
         "upstream_error",
         "upstream_rejected"
        ],
        "type": "string"
       },
       "recovery": {
        "additionalProperties": {},
        "description": "Actionable next step for the caller.",
        "properties": {
         "hint": {
          "type": "string"
         }
        },
        "required": [
         "hint"
        ],
        "type": "object"
       },
       "retryable": {
        "description": "Whether retrying may succeed.",
        "type": "boolean"
       }
      },
      "type": "object"
     },
     "message": {
      "description": "Human-readable description of what went wrong.",
      "type": "string"
     }
    },
    "required": [
     "code",
     "message"
    ],
    "type": "object"
   },
   "has_more": {
    "description": "True when recalls remain past this window \u2014 call again with offset raised by the number of recalls returned.",
    "type": "boolean"
   },
   "notice": {
    "description": "Present when the page needs explaining: a window with no recalls (with a larger days to try), an offset past the last recall, or a page cut short by the response size budget (with the offset to continue from).",
    "type": "string"
   },
   "offset": {
    "description": "Number of recalls skipped before this window.",
    "type": "number"
   },
   "period": {
    "additionalProperties": false,
    "description": "Date range queried.",
    "properties": {
     "days": {
      "description": "Window length in days.",
      "type": "number"
     },
     "end": {
      "description": "End date (today), ISO 8601.",
      "type": "string"
     },
     "start": {
      "description": "Start date of the query window, ISO 8601.",
      "type": "string"
     }
    },
    "required": [
     "start",
     "end",
     "days"
    ],
    "type": "object"
   },
   "recalls": {
    "description": "Recent recalls, newest-first.",
    "items": {
     "additionalProperties": false,
     "description": "A recent CPSC recall.",
     "properties": {
      "cpsc_url": {
       "description": "Official CPSC recall page URL.",
       "type": "string"
      },
      "data_quality_notes": {
       "description": "Gaps this server observed in the CPSC record \u2014 absent hazard text, absent product entries. Derived from which fields CPSC left empty, not from any judgement about the recall itself. Empty when nothing is missing.",
       "items": {
        "description": "One gap found in the CPSC record.",
        "type": "string"
       },
       "type": "array"
      },
      "hazards": {
       "description": "What is dangerous.",
       "items": {
        "description": "Hazard description.",
        "type": "string"
       },
       "type": "array"
      },
      "products": {
       "description": "Product names recalled.",
       "items": {
        "description": "Product name.",
        "type": "string"
       },
       "type": "array"
      },
      "recall_date": {
       "description": "Date issued, ISO 8601.",
       "type": "string"
      },
      "recall_number": {
       "description": "Recall identifier. Pass to cpsc_get_recall for full detail.",
       "type": "string"
      },
      "remedy_options": {
       "description": "Remedy types: Refund, Repair, Replace, New Instructions, Dispose, Label, No Remedy Available, Inspect. Multiple may apply. Often empty \u2014 CPSC classified the remedy on fewer than half its records; call cpsc_get_recall for the remedy instructions when this is empty.",
       "items": {
        "description": "Remedy type.",
        "type": "string"
       },
       "type": "array"
      },
      "title": {
       "description": "Recall title.",
       "type": "string"
      }
     },
     "required": [
      "recall_number",
      "recall_date",
      "title",
      "hazards",
      "remedy_options",
      "products",
      "cpsc_url",
      "data_quality_notes"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "source_note": {
    "description": "Provenance caveat: recall fields are CPSC record text with HTML markup and character codes converted to plain text, not otherwise edited or verified; check cpsc_url before a consumer-facing decision.",
    "type": "string"
   },
   "total_found": {
    "description": "Total recalls in this period, counted before offset, limit, and the response size budget narrow the window.",
    "type": "number"
   },
   "truncated": {
    "description": "True when recalls in the window remain past this page \u2014 always equal to has_more.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "title": "Get Recent CPSC Recalls"
}
