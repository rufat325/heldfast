{
 "description": "Manage Terraform workspaces. Supports:\n- list: List all workspaces and show which is active\n- show: Show the current workspace name\n- select: Switch to a workspace\n- new: Create a new workspace",
 "inputSchema": {
  "properties": {
   "name": {
    "description": "Workspace name for select or new operations",
    "type": "string"
   },
   "operation": {
    "description": "Workspace operation to perform",
    "enum": [
     "list",
     "show",
     "select",
     "new"
    ],
    "type": "string"
   },
   "workdir": {
    "description": "Absolute or relative path to the directory containing .tf files",
    "type": "string"
   }
  },
  "required": [
   "workdir",
   "operation"
  ],
  "type": "object"
 },
 "name": "tf_workspace"
}
