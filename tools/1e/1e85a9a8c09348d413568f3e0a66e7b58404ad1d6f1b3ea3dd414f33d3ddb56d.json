{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "CanonCannon corpus size and the canon bar CURRENTLY in force \u2014 entry, episode, idea and person counts, the tracked-podcast count, and the independent-podcast threshold an idea must clear. Call this to state the standard you are quoting: the bar is ceil(5% of tracked podcasts), so it rises as the corpus grows. Requires a CanonCannon API key via _apiKey. Example: canon_corpus_stats({}).",
 "inputSchema": {
  "examples": [
   {}
  ],
  "properties": {
   "_apiKey": {
    "description": "CanonCannon API key (cc_live_...). Every /api/v1 endpoint refuses without it. Keys are issued by CanonCannon \u2014 https://canoncannon.com",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "canon_corpus_stats",
 "title": "Canon Corpus Stats",
 "version": "v1.1b00e199"
}
