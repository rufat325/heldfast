{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Mint a NARROWER credential from the one you are holding, and hand it to a subtask. This is how you avoid giving a helper agent the whole account: an hour of life, one deployment, no spending. The child can never exceed you on any axis \u2014 scopes, lifetime, budget, or which resources it may touch \u2014 and asking for more is refused, not silently trimmed. Returns the token ONCE. Revoking this credential revokes everything it minted. Use it whenever you are about to delegate: the narrowest credential that can finish the job is the one to hand over.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "label": {
    "description": "What this credential is for. Say the task, e.g. \"redeploy blog after CI\".",
    "type": "string"
   },
   "resources": {
    "description": "Confine it, e.g. {\"deployment\":[\"dpl_abc\"]}. Kinds: deployment, service, domain. A kind you do NOT name is a kind the child cannot touch at all.",
    "type": "object"
   },
   "scopes": {
    "description": "A subset of [\"read\",\"deploy\",\"manage\"]. Naming a higher scope than you hold is refused.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "spend_cap_cents": {
    "description": "Daily purchase ceiling in cents. Default 0 = cannot spend. Needs the manage scope and cannot exceed what you have left today.",
    "type": "number"
   },
   "ttl_seconds": {
    "description": "How long it lives (60 to 2592000, default 3600). Clamped to your own remaining lifetime.",
    "type": "number"
   }
  },
  "required": [
   "scopes"
  ],
  "type": "object"
 },
 "name": "impreza_mint_subcredential"
}
