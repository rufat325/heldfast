{
 "description": "Update an existing block's content or HTML. Propagates live to any open editor via Hocuspocus stateless broadcast \u2014 no reload needed. Preserves block ID, version history, comments, and aiMessages.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "blockId": {
    "description": "The block ID to update",
    "type": "string"
   },
   "content": {
    "description": "New Tiptap JSON content (for text blocks) or table data (for table blocks)"
   },
   "documentId": {
    "description": "The document ID",
    "type": "string"
   },
   "html": {
    "description": "New HTML content (for interactive/html blocks)",
    "type": "string"
   }
  },
  "required": [
   "documentId",
   "blockId"
  ],
  "type": "object"
 },
 "name": "update_block"
}
