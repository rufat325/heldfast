{
 "description": "Test WHERE clause bypass via OR 1=1 variants. Sends multiple payloads (OR 1=1--, OR '1'='1, OR 1=1/*, etc.) against the target parameter and compares response lengths to the baseline. Returns baseline_length and results array. Side effects: None (read-only GET requests). Sends 7 requests total.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "parameter": {
    "description": "Vulnerable query parameter name, e.g. 'category'",
    "type": "string"
   },
   "url": {
    "description": "Full URL with query parameter, e.g. https://target/filter?category=Gifts",
    "type": "string"
   },
   "value": {
    "description": "Legitimate parameter value to base the injection on, e.g. 'Gifts'",
    "type": "string"
   }
  },
  "required": [
   "url",
   "parameter",
   "value"
  ],
  "type": "object"
 },
 "name": "sqli_where_bypass"
}
