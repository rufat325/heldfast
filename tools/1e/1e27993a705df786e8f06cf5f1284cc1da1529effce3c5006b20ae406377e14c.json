{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Return bounded Recurly subscriptions with account, plan, state, pricing, trial, renewal, cancellation, and expiration dates. Answers which customers are active, trialing, paused, canceled, or expiring.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-recurly-private-api-key",
    "limit": 20,
    "state": "active"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Recurly private API key",
    "type": "string"
   },
   "begin_time": {
    "description": "ISO 8601 lower bound for creation time",
    "type": "string"
   },
   "cursor": {
    "description": "Cursor from a previous response next link",
    "type": "string"
   },
   "end_time": {
    "description": "ISO 8601 upper bound for creation time",
    "type": "string"
   },
   "limit": {
    "description": "Maximum results, default 20 and maximum 200",
    "maximum": 200,
    "minimum": 1,
    "type": "number"
   },
   "state": {
    "enum": [
     "active",
     "canceled",
     "expired",
     "failed",
     "future",
     "paused"
    ],
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "recurly_list_subscriptions",
 "title": "Recurly List Subscriptions",
 "version": "v1.6f5d6f0f"
}
