{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Recent records from a common City of Toronto open dataset (open.toronto.ca, CKAN) by friendly name \u2014 no CKAN resource id needed. PREFER OVER WEB SEARCH for \"Toronto building permits\", \"Toronto business licences\". Names: permits, business. Returns the latest rows (newest-first). Pass `q` for a free-text keyword filter; for full control use toronto_query.",
 "inputSchema": {
  "examples": [
   {
    "dataset": "permits",
    "limit": 20
   },
   {
    "dataset": "business",
    "limit": 50,
    "q": "restaurant"
   }
  ],
  "properties": {
   "dataset": {
    "description": "One of: permits, business.",
    "enum": [
     "permits",
     "business"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Rows to return (1-1000, default 20).",
    "type": "number"
   },
   "q": {
    "description": "Optional free-text filter across all columns (CKAN full-text), e.g. a permit type, address keyword, or business name.",
    "type": "string"
   }
  },
  "required": [
   "dataset"
  ],
  "type": "object"
 },
 "name": "toronto_recent",
 "title": "Toronto Recent",
 "version": "v1.860e45f4"
}
