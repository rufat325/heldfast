{
 "description": "List all entities (tags, triggers, variables) within a specific GTM folder. Automatically follows pagination to return all entities.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "type": "string"
   },
   "containerId": {
    "type": "string"
   },
   "folderId": {
    "type": "string"
   },
   "maxPages": {
    "description": "Maximum number of API pages to fetch (default 50). GTM returns one nextPageToken for tags, triggers and variables together, so this bounds the whole walk rather than each collection.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "pageToken": {
    "description": "Continuation token to resume listing from a previous truncated result.",
    "type": "string"
   },
   "workspaceId": {
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "containerId",
   "workspaceId",
   "folderId"
  ],
  "type": "object"
 },
 "name": "folders_entities"
}
