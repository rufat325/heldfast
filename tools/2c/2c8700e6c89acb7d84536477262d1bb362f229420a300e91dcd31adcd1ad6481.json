{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "create_mysql_lease"
 },
 "description": "Mint a short-lived MySQL/MariaDB credential against a target and return a ready-to-use connection URL. The password is generated on THIS machine and only its mysql_native_password hash is sent to the API \u2014 the plaintext never reaches seekrit or MySQL at rest. The user auto-expires; revoke early with revoke_mysql_lease.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "org": {
    "type": "string"
   },
   "target": {
    "description": "target id or name",
    "type": "string"
   },
   "ttlSeconds": {
    "description": "lifetime (default 3600)",
    "maximum": 604800,
    "minimum": 60,
    "type": "integer"
   },
   "user": {
    "description": "user name to create (default: random tmp_ name)",
    "type": "string"
   }
  },
  "required": [
   "target"
  ],
  "type": "object"
 },
 "name": "create_mysql_lease"
}
