{
 "description": "Edit audio tags. Change the title, artist and album. The audio itself is copied untouched, so nothing is lost and it takes no time. Accepts audio/*, .m4a, .opus, .aac; up to 200 MB per file.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "album": {
    "description": "Album",
    "maxLength": 200,
    "type": "string"
   },
   "artist": {
    "description": "Artist",
    "maxLength": 200,
    "type": "string"
   },
   "clear": {
    "default": false,
    "description": "Wipe every existing tag first",
    "type": "boolean"
   },
   "outputDir": {
    "description": "Where to put the results. Defaults to the folder each source file came from. Existing files are never overwritten.",
    "type": "string"
   },
   "paths": {
    "description": "Paths to the files to process, absolute or relative to the working directory.",
    "items": {
     "type": "string"
    },
    "maxItems": 25,
    "minItems": 1,
    "type": "array"
   },
   "title": {
    "description": "Title",
    "maxLength": 200,
    "type": "string"
   },
   "year": {
    "description": "Year",
    "maximum": 2200,
    "minimum": 1877,
    "type": "integer"
   }
  },
  "required": [
   "paths"
  ],
  "type": "object"
 },
 "name": "audio_tags"
}
