{
 "description": "One post with its markdown body and SEO fields. A post outside the key owner's portfolio 404s exactly like a nonexistent one. (GET /api/v1/content/posts/{postId}; needs the read:content scope)",
 "inputSchema": {
  "properties": {
   "postId": {
    "description": "Post id.",
    "type": "string"
   }
  },
  "required": [
   "postId"
  ],
  "type": "object"
 },
 "name": "get_post"
}
