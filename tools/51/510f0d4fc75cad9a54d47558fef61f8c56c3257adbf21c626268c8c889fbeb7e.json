{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "OKX crypto exchange recent trade tape for an instrument. Returns price, size, side, timestamp. Use for tick-level execution analysis on OKX.",
 "inputSchema": {
  "examples": [
   {
    "instId": "BTC-USDT"
   },
   {
    "instId": "SOL-USDT",
    "limit": 50
   }
  ],
  "properties": {
   "instId": {
    "type": "string"
   },
   "limit": {
    "type": "number"
   }
  },
  "required": [
   "instId"
  ],
  "type": "object"
 },
 "name": "trades",
 "outputSchema": {
  "description": "Response from OKX trades endpoint",
  "type": "object"
 },
 "title": "Trades",
 "version": "v1.0daffacb"
}
