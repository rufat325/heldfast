{
 "_meta": {
  "dfx.example": {
   "arguments": {
    "kind": "absorbed",
    "min_advisors": 5,
    "since": "2025-09-15"
   },
   "goal": "RIAs absorbed whole by an acquirer in the last twelve months"
  }
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "RIA acquisitions and exits as filed facts"
 },
 "description": "RIA M&A from three factual sources, each labelled: successions the acquirer swore on Form ADV Item 4 (succession); firms whose advisors re-registered whole at one other firm (absorbed, a rule over registration dates); and firms that left the SEC adviser roster (roster_exit; the reason is not stated). Each row carries the target and, where there is one, the acquirer as dfx:ria: ids, the date, headline, advisor count or RAUM as magnitude, and the source. Filter by kind, acquirer or target CRD, firm id, date window or minimum advisors. No press, no rumours, no predictions.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "acquirer_crd": {
    "type": "string"
   },
   "cursor": {
    "description": "next_cursor from a previous page of this tool, unchanged.",
    "type": "string"
   },
   "firm_dfx_id": {
    "description": "A dfx:ria: firm id or CRD on either side of the event.",
    "type": "string"
   },
   "kind": {
    "default": "all",
    "enum": [
     "succession",
     "absorbed",
     "roster_exit",
     "all"
    ],
    "type": "string"
   },
   "limit": {
    "default": 25,
    "maximum": 25,
    "minimum": 1,
    "type": "integer"
   },
   "min_advisors": {
    "description": "Absorbed only: at least this many advisors moved.",
    "minimum": 1,
    "type": "integer"
   },
   "since": {
    "description": "ISO date.",
    "type": "string"
   },
   "target_crd": {
    "type": "string"
   },
   "until": {
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "search_ria_ma",
 "title": "RIA acquisitions and exits as filed facts"
}
