{
 "description": "Full-text search over the tgden catalog (1.5M+ channels, 366k+ group chats, 225k+ bots) with subscriber/member counts, categories and trust scores. Needs no Telegram account \u2014 this is the public catalog, so it finds communities the user has NOT joined. Use type to restrict to one entity kind.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max results (default 10)",
    "maximum": 25,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Search query, any language",
    "type": "string"
   },
   "type": {
    "description": "Entity kind filter (default all)",
    "enum": [
     "all",
     "channel",
     "chat",
     "bot"
    ],
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_telegram",
 "title": "Search Telegram channels, chats and bots"
}
