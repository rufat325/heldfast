{
 "description": "Mark a performance alert as resolved, optionally with a resolution note.",
 "inputSchema": {
  "properties": {
   "id": {
    "type": "string"
   },
   "resolution": {
    "description": "Optional resolution note (max 1000 chars)",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "resolve_alert"
}
