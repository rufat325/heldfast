{
 "description": "Verify the outcome of a previously executed action (act-\u2026 record id).",
 "inputSchema": {
  "properties": {
   "record_id": {
    "title": "Record Id",
    "type": "string"
   }
  },
  "required": [
   "record_id"
  ],
  "title": "verify_actionArguments",
  "type": "object"
 },
 "name": "verify_action",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "verify_actionOutput",
  "type": "object"
 }
}
