{
 "description": "Read the authoritative execution state, latest current-wave identity, and aggregate progress funnel.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "executionId": {
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "executionId"
  ],
  "type": "object"
 },
 "name": "trackly_get_apply_execution"
}
