{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Enqueue a JSON envelope onto a FIFO queue in a memory space. Use to send work/events to other agents sharing the space.",
 "inputSchema": {
  "properties": {
   "envelope": {
    "description": "JSON envelope to enqueue (opaque to the server)",
    "type": "object"
   },
   "queue": {
    "description": "Queue name (lowercase alphanumeric plus . _ : -, max 64 chars)",
    "type": "string"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   }
  },
  "required": [
   "spaceId",
   "queue",
   "envelope"
  ],
  "type": "object"
 },
 "name": "queue_push",
 "outputSchema": {
  "properties": {
   "length": {
    "description": "Queue length after the push",
    "type": "number"
   },
   "queue": {
    "description": "Queue name",
    "type": "string"
   },
   "queued": {
    "description": "True when the envelope was enqueued",
    "type": "boolean"
   }
  },
  "required": [
   "queued",
   "queue",
   "length"
  ],
  "type": "object"
 }
}
