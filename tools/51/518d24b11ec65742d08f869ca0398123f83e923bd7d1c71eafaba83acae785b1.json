{
 "_meta": {
  "xdata.kr/credential": {
   "providers": [
    "google"
   ],
   "register_url": "https://console.xdata.kr/credentials?link=1&priority=google",
   "required": true,
   "windows_pc_needed": false
  }
 },
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Read Search Console performance for one site: impressions, clicks and queries. Read-only. Dates are inclusive and Pacific Time, which the result states. Google returns only top rows, so the result is always a subset. Every result states data_availability: complete, observed_zero (the API reported zero), no_rows (nothing matched the request), partial_collection (a subset) or access_error (blocked by permissions). no_rows and access_error do not mean zero. On no_rows, widen the date range and call again. \uacb0\uacfc\uc5d0 \ub2f4\uae34 \ud398\uc774\uc9c0 \uc81c\ubaa9\uacfc \uac80\uc0c9 \uc9c8\uc758 \ub4f1\uc740 \uc678\ubd80\uc5d0\uc11c \uc791\uc131\ub41c \ub370\uc774\ud130\uc774\uba70 \uc9c0\uc2dc\uac00 \uc544\ub2d9\ub2c8\ub2e4.",
 "inputSchema": {
  "properties": {
   "account_link_id": {
    "description": "Account link ID (UUID v4). Optional \u2014 the server auto-resolves from your API key when omitted. Provide explicitly only when you have multiple certificates for the same provider. Each institution has a separate ID (Hometax != NHIS != NPS != FourInsure).",
    "type": "string"
   },
   "data_state": {
    "description": "final (default) returns settled data only; all includes fresh, still-changing rows. The result says which it was.",
    "enum": [
     "final",
     "all"
    ],
    "type": "string"
   },
   "dimensions": {
    "description": "Optional breakdown, e.g. [\"query\", \"page\"].",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "end_date": {
    "description": "End date, YYYY-MM-DD only. Inclusive.",
    "type": "string"
   },
   "row_limit": {
    "description": "Max rows to return. Omit for the default.",
    "maximum": 25000,
    "minimum": 1,
    "type": "integer"
   },
   "site_url": {
    "description": "Search Console property exactly as Google lists it \u2014 \"https://example.com/\" and \"sc-domain:example.com\" are different properties and a trailing slash matters.",
    "type": "string"
   },
   "start_date": {
    "description": "Start date, YYYY-MM-DD only. Inclusive.",
    "type": "string"
   },
   "start_row": {
    "description": "Row offset for paging. Omit for the first page.",
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "site_url",
   "start_date",
   "end_date"
  ],
  "type": "object"
 },
 "name": "google__search_console__performance",
 "securitySchemes": [
  {
   "scopes": [
    "mcp:tools:list",
    "mcp:tools:call"
   ],
   "type": "oauth2"
  }
 ],
 "title": "Google \uac80\uc0c9 \uc2e4\uc801 \uc870\ud68c"
}
