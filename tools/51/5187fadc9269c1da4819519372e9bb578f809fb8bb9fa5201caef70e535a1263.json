{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Look up a specific indicator of compromise (IP, domain, URL, hash, etc.). Returns matching IOCs with malware family, confidence, threat-type, first/last seen, tags, references.",
 "inputSchema": {
  "examples": [
   {
    "indicator": "105.72.55.52:8080"
   },
   {
    "indicator": "ladifo.workers.dev"
   }
  ],
  "properties": {
   "exact_match": {
    "description": "Require exact match (default true)",
    "type": "boolean"
   },
   "indicator": {
    "description": "IP / domain / URL / hash to look up",
    "type": "string"
   }
  },
  "required": [
   "indicator"
  ],
  "type": "object"
 },
 "name": "search_ioc",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of results returned",
    "type": "integer"
   },
   "query": {
    "description": "The query type sent (search_ioc)",
    "type": "string"
   },
   "results": {
    "description": "Matching IOC records with malware family, confidence, threat-type, dates, tags, references",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "status": {
    "description": "Query status (ok, no_result, or null)",
    "type": [
     "string",
     "null"
    ]
   }
  },
  "required": [
   "query",
   "status",
   "count",
   "results"
  ],
  "type": "object"
 },
 "title": "Search Ioc",
 "version": "v1.40f39dba"
}
