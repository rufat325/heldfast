{
 "description": "Clear one explicit user deferment previously listed or created in this session. An exact same-session retry remains idempotent.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "defermentId": {
    "minimum": 1,
    "type": "integer"
   },
   "idempotencyKey": {
    "maxLength": 200,
    "minLength": 16,
    "pattern": "^[\\x20-\\x7e]+$",
    "type": "string"
   }
  },
  "required": [
   "defermentId",
   "idempotencyKey"
  ],
  "type": "object"
 },
 "name": "trackly_clear_apply_access_deferment"
}
