{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "FREE \u2014 fetch the public receipt for an attestation: content hash, model, timestamps, Ed25519 signature, and Merkle anchor proof. Raw prompt/response are never stored, so receipts contain proof material only. No wallet needed.",
 "inputSchema": {
  "properties": {
   "attestation_id": {
    "description": "Attestation id (att_\u2026)",
    "type": "string"
   }
  },
  "required": [
   "attestation_id"
  ],
  "type": "object"
 },
 "name": "get_receipt"
}
