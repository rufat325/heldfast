{
 "_meta": {
  "skill": "souldinals"
 },
 "description": "Inscribe a soul.md as a child inscription - STEP 1: Broadcast commit transaction.\n\nCreates a child inscription linked to a parent genesis inscription, establishing on-chain provenance per the Ordinals provenance spec. Content type is always text/markdown.\n\nYou must own the parent inscription (it must be at your wallet's Taproot address).\n\nThis broadcasts the commit tx and returns immediately. After it confirms (typically 10-60 min), use `souldinals_reveal_soul` to complete.\n\nReturns: commitTxid, revealAmount, contentBase64 (save all for souldinals_reveal_soul)\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "feeRate": {
    "anyOf": [
     {
      "enum": [
       "fast",
       "medium",
       "slow"
      ],
      "type": "string"
     },
     {
      "exclusiveMinimum": 0,
      "type": "number"
     }
    ],
    "description": "Fee rate: 'fast' (~10 min), 'medium' (~30 min), 'slow' (~1 hr), or number in sat/vB (default: medium)"
   },
   "parentInscriptionId": {
    "description": "Genesis parent inscription ID (format: {txid}i{index}). You must own this inscription.",
    "type": "string"
   },
   "soulContent": {
    "description": "The soul.md content as a string (Markdown text). Will be inscribed as text/markdown.",
    "type": "string"
   }
  },
  "required": [
   "parentInscriptionId",
   "soulContent"
  ],
  "type": "object"
 },
 "name": "souldinals_inscribe_soul"
}
