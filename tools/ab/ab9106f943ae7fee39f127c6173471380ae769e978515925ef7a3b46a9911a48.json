{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get Artifacts"
 },
 "description": "List artifacts linked to a parent conversation. Pairs with save_artifact.",
 "inputSchema": {
  "properties": {
   "artifact_type": {
    "description": "Optional filter: research|code|table|framework|spec|diagram|other",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (default 20, max 100)",
    "type": "number"
   },
   "parent_conversation_id": {
    "description": "Parent conversation id that artifacts were saved against",
    "type": "string"
   }
  },
  "required": [
   "parent_conversation_id"
  ],
  "type": "object"
 },
 "name": "get_artifacts"
}
