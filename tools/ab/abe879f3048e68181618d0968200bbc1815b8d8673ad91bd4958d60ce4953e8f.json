{
 "description": "Bind this MCP session to a workspace. Required when cwd contains multiple workspaces. Lives in process memory; lost on MCP restart.",
 "inputSchema": {
  "properties": {
   "workspaceId": {
    "description": "Workspace ID (kebab-case)",
    "type": "string"
   }
  },
  "required": [
   "workspaceId"
  ],
  "type": "object"
 },
 "name": "set_workspace"
}
