{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Browse Investor Events"
 },
 "description": "List a company's recent investor events \u2014 earnings calls AND the other events it webcasts (conferences, investor/analyst days, shareholder meetings) \u2014 newest first. Each row gives the event id, type, the UTC start (time shown when one was reported), the event title, fiscal period (earnings calls only), status, and whether a transcript, audio and slide deck are on file. Conferences have no fiscal quarter, so use the event id with GetInvestorEventTranscript to read one rather than GetEarningsCallEvent (which is keyed by fiscal quarter and earnings-only).",
 "inputSchema": {
  "properties": {
   "eventType": {
    "default": null,
    "description": "Optional event type to filter on: EarningsCall, CapitalMarketsDay, InvestorUpdate, AGM, Conference, FiresideChat, or MAndA (default: all types)",
    "type": "string"
   },
   "limit": {
    "default": 25,
    "description": "Maximum number of events to return (default 25, max 100; values outside 1-100 are clamped)",
    "type": "integer"
   },
   "offset": {
    "default": 0,
    "description": "Number of matching events to skip before returning rows (default 0)",
    "type": "integer"
   },
   "ticker": {
    "description": "Company ticker symbol (e.g., AAPL, MSFT)",
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "ListInvestorEvents",
 "title": "Browse Investor Events"
}
