{
 "description": "Scan a file or directory for classical (quantum-vulnerable) asymmetric cryptography using quantakrypto. Returns a readiness summary and findings.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "format": {
    "description": "Output format: 'summary' (default) for readable text, 'json' for the raw ScanResult.",
    "enum": [
     "summary",
     "json"
    ],
    "type": "string"
   },
   "path": {
    "description": "Absolute or relative path to a file or directory to scan.",
    "type": "string"
   }
  },
  "required": [
   "path"
  ],
  "type": "object"
 },
 "name": "scan_path"
}
