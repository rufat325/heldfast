{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-team-admin-channel"
 },
 "description": "Retrieve the properties and relationships of a channel. This method supports federation. Only a user who is a member of the shared channel can retrieve channel information.\n\nTIP: Gets a specific channel by ID. Returns displayName, description, membershipType, createdDateTime, email, webUrl, tenantId.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "channelId": {
    "description": "Path parameter: channelId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "teamId": {
    "description": "Path parameter: teamId",
    "type": "string"
   }
  },
  "required": [
   "teamId",
   "channelId"
  ],
  "type": "object"
 },
 "name": "get-team-admin-channel"
}
