{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Browse or keyword-search ISTAT datasets (dataflows). Each result has an `id` (the dataflowId you pass to get_data / dataflow_structure) and an English name. ISTAT publishes ~4,800 datasets, so always pass `query` to filter unless you really want the whole list. Example: list_dataflows({ query: \"unemployment\" }) or list_dataflows({ query: \"GDP\" }).",
 "inputSchema": {
  "examples": [
   {
    "query": "unemployment"
   },
   {
    "limit": 20,
    "query": "GDP"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max results to return (default 50).",
    "type": "number"
   },
   "query": {
    "description": "Case-insensitive substring filter on dataset id or name, e.g. \"population\", \"inflation\", \"GDP\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_dataflows",
 "title": "List Dataflows",
 "version": "v1.3430fe80"
}
