{
 "description": "[engram-compat] Hybrid search: FTS5 across all layers plus semantic ranking for memories.",
 "inputSchema": {
  "properties": {
   "limit": {
    "default": 10,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "type": "string"
   },
   "scope": {
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "mem_search"
}
