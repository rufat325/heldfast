{
 "description": "Search across assembly for types, methods, fields, or string literals.",
 "inputSchema": {
  "properties": {
   "assembly": {
    "description": "Assembly key",
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 100)",
    "type": "number"
   },
   "pattern": {
    "description": "Search pattern",
    "type": "string"
   },
   "scope": {
    "description": "Search scope: all, types, methods, fields, strings (default: all)",
    "type": "string"
   }
  },
  "required": [
   "assembly",
   "pattern"
  ],
  "type": "object"
 },
 "name": "search_code"
}
