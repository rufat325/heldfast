{
 "description": "Get Kubernetes system events for a Code Engine build run (via the CE Kubernetes API proxy). Useful for diagnosing why a build failed \u2014 e.g. source clone errors, push permission failures, resource limits. Events are retained for roughly 60 minutes.",
 "inputSchema": {
  "properties": {
   "build_run_name": {
    "description": "Build run name",
    "type": "string"
   },
   "namespace": {
    "description": "Optional: project namespace (subdomain) override, if it cannot be auto-derived.",
    "type": "string"
   },
   "project_id": {
    "description": "Project ID or name",
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "build_run_name"
  ],
  "type": "object"
 },
 "name": "ce_get_build_run_events"
}
