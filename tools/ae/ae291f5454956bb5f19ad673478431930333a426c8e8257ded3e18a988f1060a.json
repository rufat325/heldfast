{
 "_meta": {
  "skill": "stacking"
 },
 "description": "List PoX-5 signer managers in the signer set for a reward cycle (default: next cycle), with the STX delegated to each. A signer manager is what stack_stx stakes with. Only signers with at least the signer-set minimum delegated appear here; other registered managers can still be staked with by contract id. Set withPayoutInfo to see how each manager pays stakers (slower).\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "rewardCycle": {
    "description": "Reward cycle to list. Defaults to the next cycle (the one a new stake joins).",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "withPayoutInfo": {
    "default": false,
    "description": "Also read each manager's claim path (one extra request per signer).",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "list_stacking_signers"
}
