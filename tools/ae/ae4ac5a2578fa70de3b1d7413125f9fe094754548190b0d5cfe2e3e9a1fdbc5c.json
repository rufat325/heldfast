{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Return the raw bytes of one entry in the file's blob table. Vector geometry, glyph outlines and similar bulk payloads are stored there and referenced by index from fields such as `vectorData.vectorNetworkBlob` and `Path.commandsBlob` (fig_node reports these as `vector.networkBlob` / `vector.fillBlobs`). This server does not decode the vector-network format \u2014 you get the bytes, base64 or hex, truncated to maxBytes with a flag when longer.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "encoding": {
    "description": "Default \"base64\".",
    "enum": [
     "base64",
     "hex"
    ],
    "type": "string"
   },
   "file": {
    "description": "Path to the .fig / .figma file (absolute, or relative to the server CWD).",
    "type": "string"
   },
   "index": {
    "description": "Blob index, as reported by fig_node `vector.*`.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "maxBytes": {
    "description": "Bytes to return before truncating (default 65536).",
    "maximum": 262144,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "file",
   "index"
  ],
  "type": "object"
 },
 "name": "fig_blob",
 "title": "Raw blob bytes"
}
