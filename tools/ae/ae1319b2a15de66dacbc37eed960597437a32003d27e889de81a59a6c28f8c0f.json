{
 "annotations": {
  "readOnlyHint": true,
  "title": "Verify Signature"
 },
 "description": "Verify an ML-DSA-65 + HMAC-SHA-512 signature from the sign tool. Reports validity of both the PQC signature and HMAC independently.",
 "inputSchema": {
  "properties": {
   "data": {
    "description": "Original text that was signed",
    "type": "string"
   },
   "hmac_signature": {
    "description": "Base64-encoded HMAC-SHA-512 signature",
    "type": "string"
   },
   "public_key": {
    "description": "Base64-encoded public key from the sign result",
    "type": "string"
   },
   "signature": {
    "description": "Base64-encoded ML-DSA-65 signature",
    "type": "string"
   }
  },
  "required": [
   "data",
   "signature",
   "hmac_signature",
   "public_key"
  ],
  "type": "object"
 },
 "name": "verify_signature"
}
