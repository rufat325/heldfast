{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Recent SEC EDGAR filings for a US public company by ticker or CIK, optionally filtered by form (10-K, 10-Q, 8-K, ...). Returns form, filing/report dates, accession number and a direct document URL. Free public source, $0.01. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "cik": {
    "description": "SEC CIK (numeric; zero-padding optional)",
    "type": "string"
   },
   "form": {
    "description": "Optional form filter, e.g. 10-K, 10-Q, 8-K",
    "type": "string"
   },
   "limit": {
    "description": "Max filings to return (1-50, default 20)",
    "type": "integer"
   },
   "ticker": {
    "description": "US equity ticker, e.g. AAPL",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "agentbit.sec_filings",
 "title": "SEC Filings (EDGAR)"
}
