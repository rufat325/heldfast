{
 "description": "Check an invoice against the chain: settled (paid on-chain, field-for-field) / overdue / issued. If settled, returns the receipt for the registry.",
 "inputSchema": {
  "properties": {
   "dueDateMs": {
    "type": "number"
   },
   "lookbackBlocks": {
    "description": "default 43200 (~1 day on Base); invoices are slower than tills",
    "type": "number"
   },
   "merchantName": {
    "type": "string"
   },
   "number": {
    "type": "string"
   },
   "to": {
    "type": "string"
   },
   "totalMicro": {
    "description": "the invoice totalMicro",
    "type": "string"
   }
  },
  "required": [
   "to",
   "totalMicro"
  ],
  "type": "object"
 },
 "name": "till_check_invoice"
}
