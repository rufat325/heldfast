{
 "description": "Dashboard bridge ONLY: send a conversational reply to the dashboard chat stream identified by chat_id. Use this \u2014 and ONLY this \u2014 to respond to a <channel source=\"gossipcat\" chat_id=\"...\"> dashboard message; your transcript output never reaches the dashboard. Pass the chat_id through verbatim. This is NOT gossip_relay: it does not close native-subagent tasks. Returns an error (no side effect) when no matching dashboard bridge stream is open.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "chat_id": {
    "description": "The chat_id verbatim from the <channel ... chat_id=\"...\"> dashboard message.",
    "type": "string"
   },
   "text": {
    "description": "The reply text to stream to the dashboard.",
    "type": "string"
   }
  },
  "required": [
   "chat_id",
   "text"
  ],
  "type": "object"
 },
 "name": "reply"
}
