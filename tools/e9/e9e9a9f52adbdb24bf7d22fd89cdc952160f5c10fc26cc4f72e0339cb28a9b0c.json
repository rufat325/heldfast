{
 "description": "The project's sequences with their status, step count, whether each is personalised (Bob writes every email per prospect) and how many prospects are enrolled. (GET /api/v1/outreach/sequences; needs the read:outreach scope)",
 "inputSchema": {
  "properties": {
   "project": {
    "description": "Project id or slug. Resolved inside the key owner's portfolio; an unowned value matches nothing rather than 404ing.",
    "type": "string"
   }
  },
  "required": [
   "project"
  ],
  "type": "object"
 },
 "name": "list_sequences"
}
