{
 "description": "Create a new backing image. With sourceType \"download\", Longhorn fetches `parameters.url` server-side from its own network position \u2014 only use with a URL the user has explicitly provided and trusts. Destructive. Do not call this unless the user has explicitly authorized it in this conversation \u2014 never proactively, speculatively, or as a side effect of another task. Longhorn docs: https://longhorn.io/docs/1.12.1/advanced-resources/backing-image/",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "extra": {
    "additionalProperties": {},
    "description": "Additional Longhorn fields, merged verbatim into the request body.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "name": {
    "type": "string"
   },
   "parameters": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "Source-type-specific parameters (e.g. { url } for \"download\").",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "sourceType": {
    "description": "e.g. \"download\", \"upload\", \"export-from-volume\", \"restore\", \"clone\".",
    "type": "string"
   }
  },
  "required": [
   "name",
   "sourceType",
   "parameters"
  ],
  "type": "object"
 },
 "name": "backingimage_create",
 "title": "Create backing image"
}
