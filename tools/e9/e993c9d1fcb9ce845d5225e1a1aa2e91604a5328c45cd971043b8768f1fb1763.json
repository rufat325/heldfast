{
 "description": "Capture the rendered terminal screen with ANSI colors \u2014 what a human would see. Useful for full-screen programs (top, htop, vim) where plain text capture loses layout. Includes pane status (running/idle) and current command.",
 "inputSchema": {
  "properties": {
   "pane": {
    "description": "Target pane as 'window.pane' (e.g. '0.0'). Defaults to '0.0'.",
    "type": "string"
   },
   "scrollback": {
    "description": "Number of scrollback lines to include (default: 200).",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "screen_snapshot"
}
