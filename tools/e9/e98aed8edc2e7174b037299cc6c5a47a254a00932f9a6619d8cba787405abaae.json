{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Manage Focus alerts with the following actions:\n\n- **create**: Create Alerts. (data: payload*{config*, rule_type*, project_id, recipient_group_ids, recipient_roles, recipient_user_ids})\n- **delete**: Delete Alerts. (data: alert_id*)\n- **get**: Get Alerts. (data: alert_id*)\n- **list**: List Alerts. (data: rule_type, project_id, scope)\n- **partial-update**: Partial Update Alerts. (data: alert_id*, payload*{config, recipient_group_ids, recipient_roles, recipient_user_ids, rule_type})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "action": {
    "description": "One of: create, delete, get, list, partial-update",
    "enum": [
     "create",
     "delete",
     "get",
     "list",
     "partial-update"
    ],
    "type": "string"
   },
   "confirm_token": {
    "description": "Mutation confirmation token from previous call",
    "type": "string"
   },
   "data": {
    "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
   },
   "dry_run": {
    "type": "boolean"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "alerts",
 "title": "Alerts"
}
