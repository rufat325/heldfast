{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the catalog of Workers AI models available to the account.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "account_id": {
    "description": "Cloudflare account ID. Falls back to CLOUDFLARE_ACCOUNT_ID env var when omitted.",
    "type": "string"
   },
   "search": {
    "description": "Filter models by name/task substring.",
    "type": "string"
   },
   "task": {
    "description": "Filter by task, e.g. 'Text Generation'.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_ai_models"
}
