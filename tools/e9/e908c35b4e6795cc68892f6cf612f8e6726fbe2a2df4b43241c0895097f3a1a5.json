{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List variants"
 },
 "description": "List variant configurations on the authenticated organization. These are org-configured delivery \"index\" values (e.g. hq, lq, preview, waveform) used in play sessions and signed URLs \u2014 distinct from the underlying variant type. See adn_list_variant_types for the type catalog.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "limit": {
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "minimum": 0,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "adn_list_variants"
}
