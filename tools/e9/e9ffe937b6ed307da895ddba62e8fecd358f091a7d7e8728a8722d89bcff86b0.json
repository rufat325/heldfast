{
 "description": "Export HTTP objects (files) from a PCAP to a directory. Returns exported_count, output_dir, files list, and tshark_output. Creates files in the output directory.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "output_dir": {
    "description": "Directory to export HTTP objects to",
    "type": "string"
   },
   "pcap_path": {
    "description": "Path to the PCAP file",
    "type": "string"
   }
  },
  "required": [
   "pcap_path",
   "output_dir"
  ],
  "type": "object"
 },
 "name": "pcap_http_objects"
}
