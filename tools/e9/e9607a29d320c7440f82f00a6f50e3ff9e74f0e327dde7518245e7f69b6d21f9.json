{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List TestFlight beta testers. Filter by app, beta group, or email.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "appId": {
    "description": "Filter to testers with access to a specific app",
    "type": "string"
   },
   "auto_paginate": {
    "description": "Follow links.next across pages until the limit is reached (default false).",
    "type": "boolean"
   },
   "betaGroupId": {
    "description": "Filter to testers in a specific beta group",
    "type": "string"
   },
   "email": {
    "description": "Exact email match",
    "type": "string"
   },
   "limit": {
    "description": "Max testers (default 100). With auto_paginate this is the total across pages.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_beta_testers"
}
