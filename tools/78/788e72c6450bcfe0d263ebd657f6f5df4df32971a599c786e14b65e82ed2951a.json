{
 "description": "Read the current account profile and platform role. `user.id` is the shared-account ID. Organizations are not listed here: they live in the shared account, which does not list them inside products yet \u2014 open a workspace by its ID with `GET /api/organizations/{organization_id}`.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {},
  "required": [],
  "type": "object"
 },
 "name": "commsharbor_me"
}
