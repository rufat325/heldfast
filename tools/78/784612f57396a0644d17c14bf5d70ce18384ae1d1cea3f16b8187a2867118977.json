{
 "annotations": {
  "readOnlyHint": true,
  "title": "Assess file modification risk"
 },
 "description": "Assess modification risk for files using Kage's code graph plus local git history: dependents, impact surface, churn, ownership, co-change partners, and test gaps. Use before editing hotspot or shared files.",
 "inputSchema": {
  "properties": {
   "changed_files": {
    "description": "Optional PR/branch changed files. If targets is omitted, these are assessed.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "project_dir": {
    "description": "Absolute path to the repository root.",
    "type": "string"
   },
   "targets": {
    "description": "File paths to assess",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "project_dir"
  ],
  "type": "object"
 },
 "name": "kage_risk"
}
