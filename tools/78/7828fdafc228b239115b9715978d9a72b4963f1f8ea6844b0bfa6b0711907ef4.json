{
 "annotations": {
  "readOnlyHint": false,
  "title": "Execute Proxy Request"
 },
 "description": "Execute an operation through an integration proxy. The credential is automatically unsealed from the vault, the operation is performed, and the response is encrypted. Only operations in allowed_operations are permitted (default-deny policy).",
 "inputSchema": {
  "properties": {
   "integration_id": {
    "description": "UUID of the integration",
    "type": "string"
   },
   "operation": {
    "description": "Operation to perform (must be listed in the integration's allowed_operations)",
    "type": "string"
   },
   "payload": {
    "additionalProperties": true,
    "description": "Operation-specific payload",
    "type": "object"
   }
  },
  "required": [
   "integration_id",
   "operation",
   "payload"
  ],
  "type": "object"
 },
 "name": "proxy_request"
}
