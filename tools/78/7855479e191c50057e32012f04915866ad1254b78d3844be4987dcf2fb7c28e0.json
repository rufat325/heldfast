{
 "annotations": {
  "cost": {
   "all": 2
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Verify an email address is properly formatted, has valid DNS records, and isn't disposable or an alias. Returns validation status, a 0-100 confidence score and the signals behind it. Pass your own Disify key as _apiKey \u2014 the keyless anonymous quota is shared and usually spent.",
 "inputSchema": {
  "examples": [
   {
    "email": "user@example.com"
   },
   {
    "email": "john.doe@company.org"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional but recommended: your own Disify API key (BYO \u2014 Pipeworx does not supply one). Without it the call uses the anonymous tier, whose daily quota is shared across all Pipeworx callers and is usually exhausted. Get one at disify.com.",
    "type": "string"
   },
   "email": {
    "description": "The email address to validate.",
    "type": "string"
   }
  },
  "required": [
   "email"
  ],
  "type": "object"
 },
 "name": "validate_email",
 "outputSchema": {
  "properties": {
   "alias": {
    "description": "Whether the email is an alias",
    "type": [
     "boolean",
     "null"
    ]
   },
   "disposable": {
    "description": "Whether the email is from a disposable service",
    "type": "boolean"
   },
   "dns_valid": {
    "description": "Whether DNS records exist for the domain",
    "type": "boolean"
   },
   "email": {
    "description": "The email address that was validated",
    "type": "string"
   },
   "format_valid": {
    "description": "Whether the email format is valid",
    "type": "boolean"
   },
   "whitelisted": {
    "description": "Whether the email domain is whitelisted",
    "type": [
     "boolean",
     "null"
    ]
   }
  },
  "required": [
   "email",
   "format_valid",
   "dns_valid",
   "disposable",
   "alias",
   "whitelisted"
  ],
  "type": "object"
 },
 "title": "Validate Email",
 "version": "v1.2ee718db"
}
