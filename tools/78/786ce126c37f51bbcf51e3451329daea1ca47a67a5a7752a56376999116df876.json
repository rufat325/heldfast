{
 "description": "Rotate a site's FraseCMS delivery token: invalidates every previously-issued token for the site and returns the new one.",
 "inputSchema": {
  "properties": {
   "site_id": {
    "description": "The site whose delivery token to rotate",
    "type": "string"
   }
  },
  "required": [
   "site_id"
  ],
  "type": "object"
 },
 "name": "rotate_delivery_token"
}
