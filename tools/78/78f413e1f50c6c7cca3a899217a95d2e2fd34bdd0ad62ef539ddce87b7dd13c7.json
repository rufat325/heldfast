{
 "description": "Fetch and parse a single RSS or Atom feed and return its recent items (title, link, date, snippet).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 10,
    "description": "Maximum number of items to return.",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "url": {
    "description": "The feed URL (RSS or Atom).",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "fetch_feed",
 "title": "Fetch a feed"
}
