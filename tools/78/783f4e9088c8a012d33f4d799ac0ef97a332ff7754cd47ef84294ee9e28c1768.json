{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List jobs"
 },
 "description": "List recent jobs with optional status and type filters.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "description": "Items per page (default 20)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "page": {
    "description": "Page number (default 1)",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "status": {
    "description": "Filter by job status",
    "enum": [
     "pending",
     "processing",
     "completed",
     "failed"
    ],
    "type": "string"
   },
   "type": {
    "description": "Filter by job type (scrape, rewrite, publish, enrich, etc.)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_jobs"
}
