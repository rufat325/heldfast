{
 "description": "Recommend a canonical Bay Run specialist (guard, sentiment, support tickets, document reranking) or a warm direct job (toxicity, embeddings, multilingual sentiment) \u2014 the same table find_runnable uses. Returns the exact Pin id or direct call, honest weight/byte status, and no decoder-only VRAM estimate.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "commercial": {
    "type": "boolean"
   },
   "limit": {
    "maximum": 4,
    "minimum": 1,
    "type": "number"
   },
   "task": {
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "task"
  ],
  "type": "object"
 },
 "name": "recommend_specialist"
}
