{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List Supported Chains"
 },
 "description": "Find which blockchains support wallet syncing by name, Tokenbooks chain slug, or numeric EVM network ID; results identify EVM networks whose shared 0x address can be added together with add_wallet.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "evmOnly": {
    "description": "When true, return only EVM-compatible chains",
    "type": "boolean"
   },
   "query": {
    "description": "Optional chain name, slug, or EVM chain id (e.g. \"Optimism\", \"optimism-mainnet\", \"10\")",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_chains",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "data": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "chainRef": {
       "type": "string"
      },
      "description": {
       "type": "string"
      },
      "evmChainId": {
       "type": "string"
      },
      "isEvm": {
       "type": "boolean"
      },
      "logoUrl": {
       "type": "string"
      },
      "name": {
       "type": "string"
      }
     },
     "required": [
      "chainRef",
      "name",
      "isEvm"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "data"
  ],
  "type": "object"
 },
 "title": "List Supported Chains"
}
