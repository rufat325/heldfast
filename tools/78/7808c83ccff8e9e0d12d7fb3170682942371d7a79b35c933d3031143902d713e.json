{
 "description": "Ask Claude about a task using the user's own Anthropic API key. Requires ANTHROPIC_API_KEY in environment.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "context": {
    "description": "Task text, section name, or other relevant context",
    "type": "string"
   },
   "question": {
    "type": "string"
   }
  },
  "required": [
   "question"
  ],
  "type": "object"
 },
 "name": "ask_claude"
}
