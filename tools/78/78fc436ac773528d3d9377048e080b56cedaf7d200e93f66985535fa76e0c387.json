{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true
 },
 "description": "Retry publishing a post that previously failed. Only posts in 'failed' or 'partial' status can be retried -- re-enqueues only the failed platform deliveries. Consumes 1 post credit.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "post_id": {
    "description": "Failed post ID to retry",
    "type": "string"
   }
  },
  "required": [
   "post_id"
  ],
  "type": "object"
 },
 "name": "social_api_retry_post",
 "title": "Retry Post"
}
