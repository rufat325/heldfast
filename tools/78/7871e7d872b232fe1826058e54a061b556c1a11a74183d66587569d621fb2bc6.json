{
 "description": "Create a ZIP. Put several files into one archive so you can send them as a single attachment. Packed in the browser \u2014 the files never reach a server. Accepts */*; up to 100 MB per file.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "level": {
    "default": "normal",
    "description": "How hard to compress",
    "enum": [
     "fast",
     "normal",
     "max"
    ],
    "type": "string"
   },
   "outputDir": {
    "description": "Where to put the results. Defaults to the folder each source file came from. Existing files are never overwritten.",
    "type": "string"
   },
   "paths": {
    "description": "Paths to the files to process, absolute or relative to the working directory.",
    "items": {
     "type": "string"
    },
    "maxItems": 25,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "paths"
  ],
  "type": "object"
 },
 "name": "archive_zip"
}
