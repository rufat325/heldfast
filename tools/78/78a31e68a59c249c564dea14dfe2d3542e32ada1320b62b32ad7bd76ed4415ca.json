{
 "description": "Create a new namespace in IBM Container Registry. Required before pushing images for the first time. Uses the ICR REST API with your IBM Cloud API key.",
 "inputSchema": {
  "properties": {
   "namespace": {
    "description": "Namespace name to create (lowercase, alphanumeric and hyphens)",
    "type": "string"
   },
   "region": {
    "description": "ICR host (default: us.icr.io)",
    "type": "string"
   }
  },
  "required": [
   "namespace"
  ],
  "type": "object"
 },
 "name": "icr_create_namespace"
}
