{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "The signed-in user's adherence rate, current streak, and per-supplement breakdown over the last N days (default 30). Requires the user to be signed in (`supstack login`).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "days": {
    "default": 30,
    "maximum": 365,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "supstack_track_adherence"
}
