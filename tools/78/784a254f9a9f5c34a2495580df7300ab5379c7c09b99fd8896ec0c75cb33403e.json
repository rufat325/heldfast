{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-auth-flows-policy"
 },
 "description": "Read the properties and relationships of an authenticationFlowsPolicy object.\n\nTIP: Gets the authentication flows policy. Returns selfServiceSignUp configuration (isEnabled) which controls whether external users can sign up via user flows.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get-auth-flows-policy"
}
