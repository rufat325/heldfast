{
 "annotations": {
  "readOnlyHint": true,
  "title": "list_memories"
 },
 "description": "List what the assistant currently remembers about this farm (the current value per key). Use before saving a memory to avoid duplicate or conflicting keys.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_memories"
}
