{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Cancel a meeting as its organizer, optionally sending a cancellation message to all attendees. The event moves to the Deleted Items folder. Prefer this over delete_event for meetings with attendees so they are notified.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "comment": {
    "description": "Cancellation message sent to attendees (e.g. \"Cancelling \u2014 rescheduling to next week\")",
    "type": "string"
   },
   "id": {
    "description": "Event ID",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "cancel_event"
}
