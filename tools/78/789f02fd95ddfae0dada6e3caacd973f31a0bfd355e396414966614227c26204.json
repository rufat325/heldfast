{
 "description": "Start/read/stop a live log stream. Use action=\"start\" to begin, \"read\" to get the buffer, \"stop\" to end. Great for watching app behavior in real-time.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "\"start\" begins streaming, \"read\" returns current buffer, \"stop\" ends the stream",
    "enum": [
     "start",
     "read",
     "stop"
    ],
    "type": "string"
   },
   "bufferSize": {
    "description": "Max lines to keep in buffer (default: 200)",
    "type": "number"
   },
   "deviceId": {
    "description": "Device (default: booted)",
    "type": "string"
   },
   "level": {
    "description": "Minimum log level",
    "enum": [
     "debug",
     "info",
     "default",
     "error",
     "fault"
    ],
    "type": "string"
   },
   "process": {
    "description": "Filter by process name",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "simulator_stream_logs"
}
