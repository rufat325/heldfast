{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List destinations"
 },
 "description": "Every city where Luxury Lodging has directly bookable homes, with the state or region, the country, how many homes and the largest group each can sleep. Use it when a guest has not named a place, or to check whether a destination is covered before searching.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {},
  "type": "object"
 },
 "name": "list_markets",
 "outputSchema": {
  "properties": {
   "homes_count": {
    "type": "integer"
   },
   "markets": {
    "items": {
     "properties": {
      "city": {
       "type": "string"
      },
      "collection_url": {
       "description": "The public page for this market, when it has enough homes to have one. Safe to cite; it lists the homes and their neighbourhoods.",
       "type": "string"
      },
      "country": {
       "description": "Not every destination is in the United States; two cities can share a name across countries.",
       "type": [
        "string",
        "null"
       ]
      },
      "homes": {
       "type": "integer"
      },
      "sleeps_max": {
       "description": "The largest group any single home in this market sleeps.",
       "type": "integer"
      },
      "state": {
       "description": "State, province or region as the provider files it.",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "required": [
      "city",
      "homes",
      "sleeps_max"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "markets_count": {
    "type": "integer"
   },
   "note": {
    "type": "string"
   }
  },
  "required": [
   "markets_count",
   "homes_count",
   "markets",
   "note"
  ],
  "type": "object"
 },
 "title": "List destinations"
}
