{
 "description": "Configure the phone call-control transport for this agent. This stores provider credentials and webhook settings; it does not start a call. Pick ONE provider \u2014 46elks or twilio \u2014 and supply that provider's credentials. For 46elks pass username + password; for twilio pass accountSid + authToken (or the generic username + password \u2014 for twilio username is the account SID and password is the auth token).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "accountSid": {
    "description": "Twilio only: the account SID (alias for username when provider is \"twilio\").",
    "type": "string"
   },
   "apiUrl": {
    "description": "Optional provider API base URL override (46elks or Twilio REST root)",
    "type": "string"
   },
   "authToken": {
    "description": "Twilio only: the account auth token (alias for password when provider is \"twilio\").",
    "type": "string"
   },
   "capabilities": {
    "description": "Transport capabilities, e.g. [\"call_control\"] or [\"call_control\",\"realtime_media\"]",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "password": {
    "description": "46elks API password. For twilio this is the auth token \u2014 prefer the authToken param for clarity.",
    "type": "string"
   },
   "phoneNumber": {
    "description": "Owned caller phone number in E.164 format, e.g. +43123456789",
    "type": "string"
   },
   "provider": {
    "description": "Phone provider: \"46elks\" (default) or \"twilio\". Both support call-control missions and realtime voice.",
    "enum": [
     "46elks",
     "twilio"
    ],
    "type": "string"
   },
   "supportedRegions": {
    "description": "Supported region scopes: AT, DE, EU, WORLD",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "username": {
    "description": "46elks API username. For twilio this is the account SID \u2014 prefer the accountSid param for clarity.",
    "type": "string"
   },
   "webhookBaseUrl": {
    "description": "Public HTTPS base URL for AgenticMail phone webhooks",
    "type": "string"
   },
   "webhookSecret": {
    "description": "Shared secret included on provider webhook URLs (at least 24 characters)",
    "type": "string"
   }
  },
  "required": [
   "phoneNumber",
   "webhookBaseUrl",
   "webhookSecret"
  ],
  "type": "object"
 },
 "name": "phone_transport_setup"
}
