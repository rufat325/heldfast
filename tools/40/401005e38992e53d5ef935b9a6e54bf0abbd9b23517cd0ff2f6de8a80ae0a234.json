{
 "description": "Search Marketplace by keyword + city \u2014 filters, isLocal/shipsOutsideRadius, opaque cursor (2 credits). Costs 2 credits. Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "availability": {
    "description": "available | sold | all.",
    "type": "string"
   },
   "cache": {
    "description": "Set true to serve from the 24h response cache (0 credits on hit). Default false \u2014 always fetch fresh. Envelope includes cached + cachedAt on hits.",
    "type": "boolean"
   },
   "category": {
    "description": "Top-level category slug, e.g. electronics.",
    "type": "string"
   },
   "condition": {
    "description": "new, like_new, good, fair (comma-separated ok).",
    "type": "string"
   },
   "cursor": {
    "description": "Opaque pagination cursor from a previous nextCursor.",
    "type": "string"
   },
   "daysSinceListed": {
    "description": "1 (24h), 7, or 30.",
    "type": "string"
   },
   "deliveryMethod": {
    "description": "local_pickup | shipping | all. Shipped listings can appear nationwide outside radiusMiles \u2014 use local_pickup for nearby-only; rows expose isLocal / shipsOutsideRadius.",
    "type": "string"
   },
   "details": {
    "description": "When true, adds description/condition/coordinates/full photo gallery/seller/distanceMiles \u2014 billed as 2 + 2 credits per listing. Default false \u2192 flat 2 credits; cover photo is still in image.",
    "type": "boolean"
   },
   "limit": {
    "description": "How many listings to return (1\u2013200). Flat 2 credits when details=false; details=true billed as 2 + 2 per listing.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "location": {
    "description": "Search-origin city or place name, e.g. 'Austin, TX' (query echo \u2014 not each listing's city).",
    "type": "string"
   },
   "maxPrice": {
    "description": "Maximum price in local currency units.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "minPrice": {
    "description": "Minimum price in local currency units.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "q": {
    "description": "Product or keyword to search Facebook Marketplace for.",
    "type": "string"
   },
   "radiusMiles": {
    "description": "Radius in miles: 1,2,5,10,20,40,60,80,100,250,500. Does not exclude nationwide shipped inventory.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "sortBy": {
    "description": "suggested | distance | creation_time | price_ascend | price_descend.",
    "type": "string"
   }
  },
  "required": [
   "q",
   "location"
  ],
  "type": "object"
 },
 "name": "facebook_marketplace_search",
 "title": "Facebook Marketplace Search"
}
