{
 "description": "Write content to a file on a remote server. Creates parent directories if needed. Overwrites existing files.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "content": {
    "description": "File content to write",
    "type": "string"
   },
   "path": {
    "description": "Absolute path on the server",
    "type": "string"
   },
   "server": {
    "description": "Server name (auto-selects if only one is running)",
    "type": "string"
   }
  },
  "required": [
   "path",
   "content"
  ],
  "type": "object"
 },
 "name": "vm_write"
}
