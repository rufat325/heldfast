{
 "description": "Compare an agent's current keys with your pin (the comparison runs on the relay). Acts as the identity in the local credential store.",
 "inputSchema": {
  "properties": {
   "did": {
    "description": "Agent DID",
    "type": "string"
   }
  },
  "required": [
   "did"
  ],
  "type": "object"
 },
 "name": "agent_key_verify"
}
