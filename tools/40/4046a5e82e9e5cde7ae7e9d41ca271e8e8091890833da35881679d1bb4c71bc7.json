{
 "description": "Find a single contact by exact email, phone, or name. Returns the best match or not_found. For fuzzy searches use list-contacts with filter_with: 'ilike' and % wildcards.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "by": {
    "description": "Which finder to use.",
    "enum": [
     "email",
     "phone",
     "name"
    ],
    "type": "string"
   },
   "value": {
    "description": "The email address, phone number, or full name to look up.",
    "type": "string"
   }
  },
  "required": [
   "by",
   "value"
  ],
  "type": "object"
 },
 "name": "find-contact"
}
