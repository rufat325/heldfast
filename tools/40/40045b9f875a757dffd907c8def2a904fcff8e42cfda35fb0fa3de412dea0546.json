{
 "description": "Promote a recurring pattern across memories into a trait (Soul Mirror). Increments strength on repeat.",
 "inputSchema": {
  "properties": {
   "evidence_memory_ids": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "name": {
    "type": "string"
   },
   "polarity": {
    "default": "neutral",
    "enum": [
     "shadow",
     "light",
     "neutral"
    ],
    "type": "string"
   },
   "scope": {
    "type": "string"
   }
  },
  "required": [
   "scope",
   "name",
   "evidence_memory_ids"
  ],
  "type": "object"
 },
 "name": "crystallize"
}
