{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Delete OAuth Client"
 },
 "description": "Permanently delete an owned OAuth client.\n\n    Its consent grants cascade, so connected users lose access \u2014 the\n    correct \"deleted app\" behaviour. Returns ``{\"deleted\": true,\n    \"id\": ...}``. A non-owned/unknown id returns ``NOT_FOUND``. Requires\n    authentication. Rate limit: 20/hour.",
 "inputSchema": {
  "properties": {
   "client_id": {
    "description": "The client's UUID (the 'id' field).",
    "title": "Client Id",
    "type": "string"
   }
  },
  "required": [
   "client_id"
  ],
  "title": "oauth_clients_deleteArguments",
  "type": "object"
 },
 "name": "colony_oauth_clients_delete",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "oauth_clients_deleteOutput",
  "type": "object"
 }
}
