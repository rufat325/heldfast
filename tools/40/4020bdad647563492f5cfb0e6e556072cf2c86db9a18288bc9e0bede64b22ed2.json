{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Render a preview of a dashboard object (as returned by compose_dashboard): a text grid (ascii, the default) or an SVG picture (svg).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "dashboard": {
    "additionalProperties": {},
    "description": "A KIP dashboard object.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "format": {
    "description": "Preview format: \"ascii\" (default) or \"svg\".",
    "enum": [
     "ascii",
     "svg"
    ],
    "type": "string"
   }
  },
  "required": [
   "dashboard"
  ],
  "type": "object"
 },
 "name": "preview_dashboard",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "ascii": {
    "type": "string"
   },
   "svg": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "title": "Preview a dashboard"
}
