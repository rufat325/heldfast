{
 "description": "Your model calls: tokens used, what each cost, and what came back from the hold.",
 "inputSchema": {
  "properties": {
   "caller": {
    "type": "string"
   },
   "limit": {
    "type": "number"
   }
  },
  "required": [
   "caller"
  ],
  "type": "object"
 },
 "name": "model_receipts"
}
