{
 "description": "Open the Notifications panel for the user (handy after `get_notifications` indicates unread items the user should see).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {},
  "type": "object"
 },
 "name": "open_notifications"
}
