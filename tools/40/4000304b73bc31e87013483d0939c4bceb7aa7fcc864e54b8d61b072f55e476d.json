{
 "description": "Install a CLI plugin",
 "inputSchema": {
  "properties": {
   "force": {
    "description": "Force reinstall if exists",
    "type": "boolean"
   },
   "plugin": {
    "description": "Plugin name",
    "type": "string"
   }
  },
  "required": [
   "plugin"
  ],
  "type": "object"
 },
 "name": "ibmcloud_plugin_install"
}
