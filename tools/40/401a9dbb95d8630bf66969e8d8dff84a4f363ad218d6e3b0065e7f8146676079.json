{
 "description": "Full day overview with AI context: all events, busy/free time analysis, first/last event times. Great for daily planning.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "calendarId": {
    "description": "Calendar ID (default: primary)",
    "maxLength": 500,
    "minLength": 1,
    "type": "string"
   },
   "date": {
    "description": "Date (YYYY-MM-DD). Default: today",
    "maxLength": 20,
    "minLength": 1,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "calendar_daily_summary"
}
