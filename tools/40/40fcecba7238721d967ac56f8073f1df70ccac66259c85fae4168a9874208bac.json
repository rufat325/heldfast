{
 "description": "Lists the retained versions of a workspace, newest first. Use this before restoring after a bad edit or merge. The current version plus the latest historical versions are retained.",
 "inputSchema": {
  "properties": {
   "url": {
    "description": "Encrypted workspace URL with #w= or #r=",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "vnsh_workspace_history"
}
