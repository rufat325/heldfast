{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List projects on the instance (requires the Projects feature, available on licensed plans). Project IDs are used by transfer_workflow / transfer_credential and as filters.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "maxItems": {
    "default": 200,
    "description": "Max projects to return",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_projects"
}
