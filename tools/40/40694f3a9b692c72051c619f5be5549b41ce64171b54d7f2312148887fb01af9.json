{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch live engagement metrics for a published post from each target platform. Returns per-platform metrics including like_count, comments_count, shares_count, saves_count, impressions, reach, and platform-specific extra_metrics. Triggers a real-time refresh from platform APIs.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "post_id": {
    "description": "Post ID to fetch live metrics for",
    "type": "string"
   }
  },
  "required": [
   "post_id"
  ],
  "type": "object"
 },
 "name": "social_api_get_post_metrics",
 "title": "Get Post Metrics"
}
