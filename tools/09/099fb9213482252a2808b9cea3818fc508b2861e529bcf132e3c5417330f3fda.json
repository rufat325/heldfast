{
 "description": "Export task details, reusable lessons, and App-admitted prototype inputs. The App is the sole execution-admission authority; this tool never guesses current or latest prototype versions.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "assetMode": {
    "default": "metadata",
    "description": "Use file to request short-lived decrypted package paths.",
    "enum": [
     "metadata",
     "file"
    ],
    "type": "string"
   },
   "fetchMissing": {
    "default": false,
    "description": "Allow the App to fetch a missing local package before export.",
    "type": "boolean"
   },
   "includePrototypes": {
    "default": true,
    "description": "Include structured prototype admission and admitted assets.",
    "type": "boolean"
   },
   "taskId": {
    "description": "Granoflow task id.",
    "minLength": 1,
    "type": "string"
   },
   "ttlSeconds": {
    "default": 600,
    "description": "Temporary prototype asset lifetime, capped at 600 seconds.",
    "maximum": 600,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "taskId"
  ],
  "type": "object"
 },
 "name": "granoflow_task_export"
}
