{
 "description": "Search notes by meaning using local embeddings (all-MiniLM-L6-v2, runs offline after a one-time model download). Finds related notes even with no shared keywords. `hybrid` fuses this with keyword search for best results.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "hybrid": {
    "description": "Fuse semantic + keyword ranking (default false)",
    "type": "boolean"
   },
   "limit": {
    "description": "Max results (default 10)",
    "exclusiveMinimum": 0,
    "maximum": 100,
    "type": "integer"
   },
   "query": {
    "description": "Natural-language query",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "semantic_search",
 "title": "Semantic search"
}
