{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Detect whether a SQL script comes from MySQL/MariaDB, SQLite, SQL Server or Oracle and translate it to PostgreSQL, explaining every change with its line number. Changes that are NOT exact equivalents are flagged as such.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dialect": {
    "default": "auto",
    "description": "Source engine; \"auto\" detects it from markers in the SQL itself",
    "enum": [
     "auto",
     "mysql",
     "sqlite",
     "mssql",
     "oracle"
    ],
    "type": "string"
   },
   "sql": {
    "description": "The source SQL script",
    "type": "string"
   }
  },
  "required": [
   "sql"
  ],
  "type": "object"
 },
 "name": "db_translate_sql",
 "title": "Translate SQL from another engine to PostgreSQL"
}
