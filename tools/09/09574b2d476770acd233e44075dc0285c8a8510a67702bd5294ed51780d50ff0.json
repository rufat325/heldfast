{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Crypto perpetual futures snapshot in one call: funding rate, open interest, taker buy/sell volume (CVD) and long/short account ratio for a perpetual swap. Accepts exchange-style symbols such as 'MASKUSDT', 'MASK' or 'MASK-USDT-SWAP'. Figures come from the OKX venue and the response states that venue, so a symbol quoted on Binance or Bybit is answered with OKX's own contract for that coin, labelled as such.",
 "inputSchema": {
  "examples": [
   {
    "buckets": 6,
    "instId": "MASKUSDT"
   },
   {
    "buckets": 24,
    "instId": "BTC-USDT-SWAP",
    "period": "1H"
   }
  ],
  "properties": {
   "buckets": {
    "description": "How many recent buckets of taker flow to sum for CVD. Default 24, max 100.",
    "type": "number"
   },
   "instId": {
    "description": "Perpetual symbol \u2014 'MASKUSDT', 'MASK', 'MASK-USDT' and 'MASK-USDT-SWAP' all resolve to the same OKX contract.",
    "type": "string"
   },
   "period": {
    "description": "Bucket size for taker flow and long/short ratio: 5m, 15m, 30m, 1H, 2H, 4H, 6H, 12H, 1D. Default 5m.",
    "type": "string"
   }
  },
  "required": [
   "instId"
  ],
  "type": "object"
 },
 "name": "perp_metrics",
 "title": "Perp Metrics",
 "version": "v1.4d0eb598"
}
