{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run a raw SoQL query against any Edmonton open-data resource (data.edmonton.ca) by its Socrata id (8-char like \"ukww-xkmj\"). Full SoQL: where/select/group/order/limit/offset. Use edmonton_datasets to find a resource id, or edmonton_recent for the common ones.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-data-edmonton-api-key",
    "resource_id": "ukww-xkmj",
    "where": "incident_year=2025"
   },
   {
    "_apiKey": "your-data-edmonton-api-key",
    "group": "incident_category",
    "limit": 10,
    "order": "count(*) DESC",
    "resource_id": "ukww-xkmj",
    "select": "incident_category, count(*)"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Socrata app token for higher rate limits. Omit to use the keyless endpoint.",
    "type": "string"
   },
   "group": {
    "description": "SoQL $group (e.g. \"incident_category\").",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (default 100, max 5000).",
    "type": "number"
   },
   "offset": {
    "description": "Row offset for paging.",
    "type": "number"
   },
   "order": {
    "description": "SoQL $order (e.g. \"incident_datetime DESC\").",
    "type": "string"
   },
   "resource_id": {
    "description": "Socrata resource id, e.g. \"ukww-xkmj\" (police incidents).",
    "type": "string"
   },
   "select": {
    "description": "SoQL $select (e.g. \"incident_category, count(*)\").",
    "type": "string"
   },
   "where": {
    "description": "SoQL $where filter (e.g. \"incident_year=2025\").",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "edmonton_query",
 "title": "Edmonton Query",
 "version": "v1.abee44b4"
}
