{
 "description": "Calculate the exact geodesic distance between two points on Earth's surface. Uses the WGS84 ellipsoid model for high accuracy. No dataset needed. Returns distance in both meters and miles.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "lat1": {
    "description": "Latitude of first point",
    "type": "number"
   },
   "lat2": {
    "description": "Latitude of second point",
    "type": "number"
   },
   "lng1": {
    "description": "Longitude of first point",
    "type": "number"
   },
   "lng2": {
    "description": "Longitude of second point",
    "type": "number"
   }
  },
  "required": [
   "lat1",
   "lng1",
   "lat2",
   "lng2"
  ],
  "type": "object"
 },
 "name": "calculate_distance"
}
