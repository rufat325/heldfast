{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch NOAA SPC preliminary storm reports CSV for a given date (YYMMDD, default today UTC) filtered by kind: tornado, wind, hail, or filtered (combined). Returns raw CSV with time, location, magnitude, and remarks.",
 "inputSchema": {
  "examples": [
   {
    "date": "250115",
    "kind": "tornado"
   },
   {
    "kind": "hail"
   }
  ],
  "properties": {
   "date": {
    "description": "YYMMDD (default today UTC)",
    "type": "string"
   },
   "kind": {
    "description": "tornado | wind | hail | filtered (default)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "storm_reports",
 "outputSchema": {
  "properties": {
   "body": {
    "description": "CSV data of preliminary storm reports",
    "type": "string"
   },
   "format": {
    "description": "Response format",
    "enum": [
     "csv"
    ],
    "type": "string"
   }
  },
  "required": [
   "format",
   "body"
  ],
  "type": "object"
 },
 "title": "Storm Reports",
 "version": "v1.2a470fde"
}
