{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Continue the user's most recent Palim thread. Call first in a new chat, no args. Ignore the result if it is unrelated. Project filter is case-insensitive and returns the most recently updated match.",
 "inputSchema": {
  "properties": {
   "include_messages": {
    "description": "Optional: append the tail of the transcript when the thread has stored messages. Default false.",
    "type": "boolean"
   },
   "project": {
    "description": "Optional: only consider threads belonging to this project (case-insensitive; Logisoft \u2261 logisoft).",
    "type": "string"
   },
   "tool_source": {
    "description": "Optional: only consider threads from one client (cursor, claude, codex, chatgpt, ...).",
    "type": "string"
   },
   "within_hours": {
    "description": "Optional: ignore threads older than this many hours. Omit to always return the latest thread.",
    "type": "number"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "palim_resume",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "message": {
    "description": "Human-readable tool result text.",
    "type": "string"
   }
  },
  "required": [
   "message"
  ],
  "type": "object"
 }
}
