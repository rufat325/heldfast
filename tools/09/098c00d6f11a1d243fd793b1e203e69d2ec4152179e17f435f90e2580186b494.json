{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Deterministically verify a built stake transaction only delegates to Blueprint with your wallet as both stake and withdraw authority. The fully trustless path is the @solentic/verify npm package run client-side before signing.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "transaction": {
    "description": "base64 unsigned transaction",
    "type": "string"
   },
   "walletAddress": {
    "description": "Your wallet address",
    "maxLength": 50,
    "type": "string"
   }
  },
  "required": [
   "transaction",
   "walletAddress"
  ],
  "type": "object"
 },
 "name": "verify_stake_transaction",
 "title": "Verify Stake Transaction"
}
