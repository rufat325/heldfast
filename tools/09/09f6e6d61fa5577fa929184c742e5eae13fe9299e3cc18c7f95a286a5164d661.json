{
 "description": "Retrieve memory entries relevant to a natural-language query for the active Solana project. Ranked by BM25 over entity fields with a recency boost.",
 "inputSchema": {
  "properties": {
   "kinds": {
    "description": "Optional filter to specific entity kinds",
    "items": {
     "enum": [
      "Program",
      "Account",
      "Instruction",
      "Decision",
      "Finding",
      "Integration",
      "Contract"
     ],
     "type": "string"
    },
    "type": "array"
   },
   "limit": {
    "default": 10,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Natural-language description of what you want to recall",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "gmem.recall"
}
