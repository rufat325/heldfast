{
 "description": "Delete a message by id (sender or recipient only). Acts as the identity in the local credential store.",
 "inputSchema": {
  "properties": {
   "message_id": {
    "description": "Message id",
    "type": "string"
   }
  },
  "required": [
   "message_id"
  ],
  "type": "object"
 },
 "name": "agent_delete_message"
}
