{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Plain-text of a whole act (recitals + enacting terms), paged. Use for short acts or to read recitals/definitions; for a specific Article prefer get_article. Returns up to max_chars from offset.",
 "inputSchema": {
  "examples": [
   {
    "celex": "GDPR"
   },
   {
    "celex": "32022R0868",
    "max_chars": 16000,
    "offset": 0
   }
  ],
  "properties": {
   "celex": {
    "description": "CELEX id or a known alias.",
    "type": "string"
   },
   "lang": {
    "description": "ISO 639-2/B language code (default \"eng\").",
    "type": "string"
   },
   "max_chars": {
    "description": "Max characters to return (default 8000, max 40000).",
    "type": "number"
   },
   "offset": {
    "description": "Character offset to start from, for paging (default 0).",
    "type": "number"
   }
  },
  "required": [
   "celex"
  ],
  "type": "object"
 },
 "name": "get_document",
 "title": "Get Document",
 "version": "v1.bcf08a4b"
}
