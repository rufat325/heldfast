{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Retrieve Pilot Reports (PIREPs) near a given ICAO station within a configurable radius and time window. PIREPs are real-time in-flight observations filed by pilots capturing actual icing severity, turbulence intensity, visibility, cloud bases and tops, and conditions at flight level. Returns decoded fields: flight level, icing and turbulence extents, and raw PIREP text. Useful for pre-flight planning, route analysis, and verifying forecast accuracy. Source: NOAA Aviation Weather Center \u2014 US Government public domain, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "age": {
    "description": "How many hours back to search for pilot reports (1\u201324, default 2).",
    "maximum": 24,
    "minimum": 1,
    "type": "integer"
   },
   "distance": {
    "description": "Search radius in nautical miles around the station (10\u2013200, default 100). 100 nm covers the typical terminal area plus en-route corridor.",
    "maximum": 200,
    "minimum": 10,
    "type": "integer"
   },
   "id": {
    "description": "ICAO station identifier to use as the geographic center of the search (e.g. \"KJFK\", \"KORD\"). Pilot reports within `distance` nautical miles of this station and within the last `age` hours are returned.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "aviationweather.observation.pirep",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Aviation Weather \u2014 Pilot Reports (PIREPs)"
}
