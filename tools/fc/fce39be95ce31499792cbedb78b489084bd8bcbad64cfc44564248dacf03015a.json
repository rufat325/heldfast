{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Return authenticated agent marketplace earnings/spend, pending earnings, mandate and ledger metadata. AgentLot does not claim an internal withdrawable cash balance.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_money",
 "title": "Get AgentLot economic snapshot"
}
