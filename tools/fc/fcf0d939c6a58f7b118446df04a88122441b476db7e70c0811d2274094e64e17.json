{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Query any City of Boston datastore resource (data.boston.gov, CKAN) by its resource id (a UUID). Supports a free-text `q`, exact-match `filters` (field\u2192value), `sort` (\"field desc\"), limit and offset. Use boston_datasets to find a resource id, or boston_recent for the common ones.",
 "inputSchema": {
  "examples": [
   {
    "filters": {
     "district": "B2"
    },
    "limit": 100,
    "resource_id": "b973d8cb-eeb2-4e7e-99da-c92938efc9c0",
    "sort": "OCCURRED_ON_DATE desc"
   },
   {
    "limit": 50,
    "q": "pothole",
    "resource_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
   }
  ],
  "properties": {
   "filters": {
    "description": "Exact-match filters as a JSON object, e.g. {\"district\":\"B2\"}.",
    "type": "object"
   },
   "limit": {
    "description": "Max rows (default 100, max 1000).",
    "type": "number"
   },
   "offset": {
    "description": "Row offset for paging.",
    "type": "number"
   },
   "q": {
    "description": "Free-text search across columns.",
    "type": "string"
   },
   "resource_id": {
    "description": "CKAN datastore resource id (UUID), e.g. \"b973d8cb-eeb2-4e7e-99da-c92938efc9c0\".",
    "type": "string"
   },
   "sort": {
    "description": "Sort clause, e.g. \"OCCURRED_ON_DATE desc\".",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "boston_query",
 "title": "Boston Query",
 "version": "v1.8e83b36c"
}
