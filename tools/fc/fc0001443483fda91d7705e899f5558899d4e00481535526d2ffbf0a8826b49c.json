{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch monthly values for a CBS price index by its numeric code (from index_catalog, e.g. 120010 = CPI General). Returns per-month entries with the index value, the monthly percent change (percent) and the year-over-year change (percentYear) \u2014 i.e. Israeli inflation. Optionally filter by start_period / end_period (YYYY-MM or YYYY).",
 "inputSchema": {
  "examples": [
   {
    "code": 120010
   },
   {
    "code": 120010,
    "end_period": "2026-06",
    "start_period": "2025-01"
   }
  ],
  "properties": {
   "code": {
    "description": "Index code, e.g. 120010 (Consumer Price Index - General).",
    "type": "integer"
   },
   "end_period": {
    "description": "Latest period, e.g. \"2026-06\".",
    "type": "string"
   },
   "lang": {
    "description": "Content language. Default \"en\".",
    "enum": [
     "en",
     "he"
    ],
    "type": "string"
   },
   "start_period": {
    "description": "Earliest period, e.g. \"2025-01\".",
    "type": "string"
   }
  },
  "required": [
   "code"
  ],
  "type": "object"
 },
 "name": "get_index_data",
 "title": "Get Index Data",
 "version": "v1.7f0cac5f"
}
