{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "The free half of check_vat_list: check the shape of up to 20 VAT numbers locally, no register, no account, no cost (`not_covered`, never `invalid`); a well-formed number is NOT a registered one. Free, no token.",
 "inputSchema": {
  "properties": {
   "rows": {
    "description": "The list: 1 to 20 rows per call.",
    "items": {
     "properties": {
      "reference": {
       "description": "Your own label for the row \u2014 a customer number, a name. It is echoed back, never interpreted.",
       "type": "string"
      },
      "vatId": {
       "description": "The VAT number as written, WITH its two-letter country prefix, e.g. \"BE0400378485\" or \"NL 8558.76.323.B01\".",
       "type": "string"
      }
     },
     "required": [
      "vatId"
     ],
     "type": "object"
    },
    "maxItems": 200,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "rows"
  ],
  "type": "object"
 },
 "name": "check_vat_list_format",
 "outputSchema": {
  "properties": {
   "data": {
    "type": "object"
   },
   "kind": {
    "type": "string"
   }
  },
  "required": [
   "kind",
   "data"
  ],
  "type": "object"
 }
}
