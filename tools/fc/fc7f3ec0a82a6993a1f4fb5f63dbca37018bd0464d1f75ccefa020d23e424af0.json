{
 "annotations": {
  "cost": {
   "all": 2
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch the current live top-ranked Hacker News stories from the Firebase API. Returns up to `count` stories (default 10) with title, URL, score, author, comment count, and Unix timestamp.",
 "inputSchema": {
  "examples": [
   {
    "count": 10
   },
   {
    "count": 30
   }
  ],
  "properties": {
   "count": {
    "description": "Number of top stories to return (default: 10)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "get_top_stories",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of top stories returned",
    "type": "number"
   },
   "stories": {
    "description": "Array of top stories",
    "items": {
     "properties": {
      "by": {
       "description": "Author username",
       "type": [
        "string",
        "null"
       ]
      },
      "descendants": {
       "description": "Number of comments",
       "type": [
        "number",
        "null"
       ]
      },
      "id": {
       "description": "HN item ID",
       "type": "number"
      },
      "score": {
       "description": "Story score/points",
       "type": [
        "number",
        "null"
       ]
      },
      "time": {
       "description": "Unix timestamp of story posting",
       "type": [
        "number",
        "null"
       ]
      },
      "title": {
       "description": "Story title",
       "type": [
        "string",
        "null"
       ]
      },
      "url": {
       "description": "Story URL",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "required": [
      "id"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "count",
   "stories"
  ],
  "type": "object"
 },
 "title": "Get Top Stories",
 "version": "v1.2d1556d9"
}
