{
 "description": "All IPL 2026 cross-fixture trends, optionally filtered by kind: conditional, momentum, venue, toss, anomaly. Returns id + kind + hook + canonicalUrl per row.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "kind": {
    "description": "Filter by kind: conditional / momentum / venue / toss / anomaly",
    "type": "string"
   },
   "limit": {
    "description": "Default 30, max 100",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list_trends"
}
