{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Aggregate execution health over a recent sample: per-workflow success/error/waiting counts, success rate and average duration. The queue-mode dashboard in one call \u2014 spot failing or slow workflows.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "sample": {
    "default": 200,
    "description": "How many recent executions to aggregate",
    "maximum": 1000,
    "minimum": 10,
    "type": "integer"
   },
   "workflowId": {
    "description": "Restrict stats to one workflow",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_execution_stats"
}
