{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Places a PNG, JPG or GIF, passed as base64. With nodeId it becomes that layer\u2019s fill, which is how a placeholder gets its picture; without one it arrives as a new rectangle at its own pixel size.\n\nBase64 rather than a URL on purpose: the plugin\u2019s manifest allows only the relay and the local daemon as network destinations, so the plugin cannot fetch an image from anywhere a designer keeps one. Read the file yourself and send the bytes. The cap is 700KB of base64, which is roughly a 500KB image.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "data": {
    "description": "The image as base64. A data: URL prefix is accepted and ignored.",
    "type": "string"
   },
   "height": {
    "description": "Default: the image\u2019s own height.",
    "minimum": 0.01,
    "type": "number"
   },
   "name": {
    "type": "string"
   },
   "nodeId": {
    "description": "Fill this layer instead of making a new one.",
    "type": "string"
   },
   "parentId": {
    "description": "Where a new rectangle goes. Default: the current page.",
    "type": "string"
   },
   "scaleMode": {
    "description": "How it sits in the frame. Default FILL.",
    "enum": [
     "FILL",
     "FIT",
     "CROP",
     "TILE"
    ],
    "type": "string"
   },
   "width": {
    "description": "Default: the image\u2019s own width.",
    "minimum": 0.01,
    "type": "number"
   },
   "x": {
    "type": "number"
   },
   "y": {
    "type": "number"
   }
  },
  "required": [
   "data"
  ],
  "type": "object"
 },
 "name": "figma_insert_image",
 "title": "Put an image on the canvas"
}
