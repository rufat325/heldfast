{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Give the person insight statements to test with their body on their own time. Adds one to three items to the optional reflection feed under Settings, returns immediately, never notifies the phone. Use when they ask to queue reflections, or when get_vibecheck_status says the feed needs items. Never use when they ask for a vibecheck now. One can become database-current for Home; the rest stay Up next without extra pushes. This does not verify phone display or delivery. Never call this to add follow-up asks after request_vibecheck. Call get_vibecheck_status first; propositions_needed is the available maximum, not a quota. Submit only genuinely useful statements and never invent extras to fill capacity. A still-active scheduled authorization does not require repeated consent each run.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "statements": {
    "description": "Distinct personalized statements for this person to test, ordered most useful first.",
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 3,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "statements"
  ],
  "type": "object"
 },
 "name": "post_vibecheck_statement",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "propositions": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "presentation_state": {
       "enum": [
        "presented",
        "queued"
       ],
       "type": "string"
      },
      "request_id": {
       "type": "string"
      },
      "stale_at": {
       "type": [
        "string",
        "null"
       ]
      }
     },
     "required": [
      "request_id",
      "presentation_state",
      "stale_at"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "propositions"
  ],
  "type": "object"
 },
 "title": "Stock reflections for later"
}
