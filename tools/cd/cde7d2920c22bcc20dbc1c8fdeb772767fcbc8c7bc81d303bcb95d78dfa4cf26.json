{
 "description": "Full-text search of message bodies via Spotlight (Tier 2 \u2014 requires Full Disk Access; refuses with instructions when absent rather than returning silently-empty results). Returns headers; fetch bodies with mail_read.\n[governed: scope=Mail mode=read undo=none]",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 15,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "text": {
    "description": "Text to find in message bodies",
    "minLength": 2,
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "mail_search_fulltext"
}
