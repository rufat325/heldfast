{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "Report a gap in the catalog"
 },
 "description": "FREE, no account \u2014 tell the catalog what you looked for and could not find. The consumer door (roadmap#166): a gap report is how the catalog learns what to add next, and a person reads the queue. One required field.",
 "inputSchema": {
  "properties": {
   "contact": {
    "description": "Optional email if you want to hear when the catalog can answer this.",
    "type": "string"
   },
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "found": {
    "enum": [
     "nothing",
     "wrong thing",
     "listed but unusable"
    ],
    "type": "string"
   },
   "looked_for": {
    "description": "What you were looking for, in your own words.",
    "type": "string"
   },
   "needed_for": {
    "description": "Optional: the task you needed it for.",
    "type": "string"
   },
   "provider": {
    "description": "Optional slug, when the gap is about an existing listing.",
    "type": "string"
   }
  },
  "required": [
   "looked_for"
  ],
  "type": "object"
 },
 "name": "report_gap"
}
