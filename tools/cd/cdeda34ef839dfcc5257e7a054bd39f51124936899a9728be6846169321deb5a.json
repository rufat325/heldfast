{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Copies a node, with everything inside it, into the same parent or one you name. The cheapest way to make a second row, card or list item that matches the first exactly.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "nodeId": {
    "description": "The node to copy.",
    "type": "string"
   },
   "parentId": {
    "description": "Where the copy goes. Default: beside the original.",
    "type": "string"
   },
   "x": {
    "type": "number"
   },
   "y": {
    "type": "number"
   }
  },
  "required": [
   "nodeId"
  ],
  "type": "object"
 },
 "name": "figma_clone_node",
 "title": "Duplicate a layer"
}
