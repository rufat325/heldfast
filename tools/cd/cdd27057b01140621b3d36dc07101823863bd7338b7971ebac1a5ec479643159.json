{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Check US agricultural imports by commodity and origin country. Returns import volumes, values, and source country details. Use fas_commodity_codes to find commodity codes (e.g., \"coffee\", \"cocoa\").",
 "inputSchema": {
  "examples": [
   {
    "commodity": "coffee",
    "country": "BR",
    "start_year": "2022"
   },
   {
    "commodity": "cocoa"
   }
  ],
  "properties": {
   "commodity": {
    "description": "Commodity name (e.g., \"coffee\", \"cocoa\", \"sugar\", \"beef\") or commodity code",
    "type": "string"
   },
   "country": {
    "description": "Origin country code (e.g., \"BR\" for Brazil, \"CO\" for Colombia). Optional \u2014 omit for all origins.",
    "type": "string"
   },
   "end_year": {
    "description": "End year (optional)",
    "type": "string"
   },
   "start_year": {
    "description": "Start year (optional)",
    "type": "string"
   }
  },
  "required": [
   "commodity"
  ],
  "type": "object"
 },
 "name": "fas_imports",
 "outputSchema": {
  "properties": {
   "commodity": {
    "description": "Commodity name provided in request",
    "type": "string"
   },
   "commodity_code": {
    "description": "Standardized commodity code",
    "type": "string"
   },
   "count": {
    "description": "Number of records returned",
    "type": "integer"
   },
   "data": {
    "description": "Import records (max 100 returned)",
    "items": {
     "properties": {
      "month": {
       "description": "Import month",
       "type": [
        "integer",
        "null"
       ]
      },
      "partner": {
       "description": "Origin country/partner name",
       "type": [
        "string",
        "null"
       ]
      },
      "partner_code": {
       "description": "Origin country code",
       "type": [
        "string",
        "null"
       ]
      },
      "quantity": {
       "description": "Import quantity",
       "type": [
        "number",
        "null"
       ]
      },
      "unit": {
       "description": "Unit of measurement",
       "type": [
        "string",
        "null"
       ]
      },
      "value": {
       "description": "Import value",
       "type": [
        "number",
        "null"
       ]
      },
      "year": {
       "description": "Import year",
       "type": [
        "integer",
        "null"
       ]
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "direction": {
    "description": "Trade direction",
    "enum": [
     "imports"
    ],
    "type": "string"
   },
   "truncated": {
    "description": "Whether results were truncated to 100 records",
    "type": "boolean"
   }
  },
  "required": [
   "commodity",
   "commodity_code",
   "direction",
   "count",
   "data",
   "truncated"
  ],
  "type": "object"
 },
 "title": "Fas Imports",
 "version": "v1.878fd207"
}
