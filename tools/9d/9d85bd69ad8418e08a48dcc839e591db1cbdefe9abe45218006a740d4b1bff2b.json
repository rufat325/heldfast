{
 "description": "Resolve a brand or @handle to one public Instagram profile \u2014 stable id, bio, links, and stats (not niche discovery). Costs 1 credit. Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cache": {
    "description": "Serve from the 24h shared cache when available (0 credits on hit). Default true \u2014 set false to always fetch fresh.",
    "type": "boolean"
   },
   "q": {
    "description": "Account name, @handle, or profile URL to resolve (min 2 characters). Not a niche keyword search.",
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "instagram_profile_search",
 "title": "Instagram Profile Search"
}
