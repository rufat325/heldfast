{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": true,
  "title": "Remind get messages"
 },
 "description": "Read messages in one or more chat streams, newest-last. Items are typed: MessageItem (a real message with sender, body and attachments), SystemMessageItem (joins, stream creation) or GapItem (a paging gap of `size` unloaded messages).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "limit": {
    "default": 25,
    "description": "Max non-gap messages per stream.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "uuids": {
    "description": "Chat stream uuids, from remind_list_chats.",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "uuids"
  ],
  "type": "object"
 },
 "name": "remind_get_messages"
}
