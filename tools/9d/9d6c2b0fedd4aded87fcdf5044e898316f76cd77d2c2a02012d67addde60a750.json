{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Run an organization's sales or attendees report \u2014 the aggregated analytics behind its events, optionally windowed by date.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "continuation": {
    "description": "Pagination continuation token from a previous response",
    "type": "string"
   },
   "end_date": {
    "description": "ISO date upper bound (YYYY-MM-DD)",
    "type": "string"
   },
   "event_status": {
    "enum": [
     "live",
     "started",
     "ended",
     "completed",
     "canceled"
    ],
    "type": "string"
   },
   "group_by": {
    "description": "Grouping dimension, e.g. 'event', 'day', 'ticket_class'",
    "type": "string"
   },
   "kind": {
    "description": "Which report to run",
    "enum": [
     "sales",
     "attendees"
    ],
    "type": "string"
   },
   "org_id": {
    "description": "Organization id (from eb_my_organizations)",
    "type": "string"
   },
   "start_date": {
    "description": "ISO date lower bound (YYYY-MM-DD)",
    "type": "string"
   },
   "view": {
    "description": "Response shape: \"compact\" (default) drops fields the response already carries elsewhere; \"full\" returns every field this server understands. compact strips image/avatar URLs from the response; \"full\" returns Eventbrite's payload untouched. No field projection on this tool: this server has no verified record of which Eventbrite fields matter here, and inventing one would risk dropping a field a caller needs.",
    "enum": [
     "compact",
     "full"
    ],
    "type": "string"
   }
  },
  "required": [
   "org_id",
   "kind"
  ],
  "type": "object"
 },
 "name": "eb_org_report"
}
