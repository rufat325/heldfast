{
 "description": "Publish an app to Qumge on the developer's behalf (needs the developer's Qumge key in the Authorization header). Give it a name, the app's HTTPS base_url, and \u2014 if you can read them off the app \u2014 its price book (events with a unit price) and routes (with a per-call ceiling). It returns a DRAFT app plus the signing secret and usage key (shown once). Nothing goes live here: the developer submits for review after that.",
 "inputSchema": {
  "properties": {
   "balance_json_path": {
    "description": "Only for balance_delta: where in that response.",
    "type": "string"
   },
   "balance_path": {
    "description": "Only for balance_delta: where we read the balance.",
    "type": "string"
   },
   "base_url": {
    "description": "Where the app lives. Must be https.",
    "type": "string"
   },
   "events": {
    "description": "Price book events. unit_price_usd is in US dollars, e.g. 0.10.",
    "items": {
     "properties": {
      "key": {
       "type": "string"
      },
      "label": {
       "type": "string"
      },
      "unit_price_usd": {
       "type": "number"
      }
     },
     "required": [
      "key",
      "unit_price_usd"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "metering": {
    "description": "reported (the app tells us what to charge) or balance_delta (we read its balance).",
    "enum": [
     "reported",
     "balance_delta"
    ],
    "type": "string"
   },
   "name": {
    "description": "Human name shown in the store.",
    "type": "string"
   },
   "routes": {
    "description": "HTTP routes. Billable routes need hold_usd (the most one call may cost).",
    "items": {
     "properties": {
      "billable": {
       "type": "boolean"
      },
      "hold_usd": {
       "type": "number"
      },
      "method": {
       "type": "string"
      },
      "name": {
       "type": "string"
      },
      "pattern": {
       "type": "string"
      },
      "settle": {
       "enum": [
        "on_response",
        "on_final"
       ],
       "type": "string"
      }
     },
     "required": [
      "pattern"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "slug": {
    "description": "Optional URL slug. Derived from the name when omitted.",
    "type": "string"
   },
   "summary": {
    "description": "One line describing what it does.",
    "type": "string"
   },
   "unit_name": {
    "description": "What users buy, e.g. credit.",
    "type": "string"
   }
  },
  "required": [
   "name",
   "base_url"
  ],
  "type": "object"
 },
 "name": "publish_app"
}
