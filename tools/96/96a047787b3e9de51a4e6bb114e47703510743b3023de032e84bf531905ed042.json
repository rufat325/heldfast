{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Run an owned/adopted asset in the locked-down Docker sandbox, or through an explicitly configured secure deployment hook.",
 "inputSchema": {
  "properties": {
   "asset_id": {
    "type": "string"
   },
   "mode": {
    "enum": [
     "sandbox",
     "external_hook"
    ],
    "type": "string"
   },
   "timeout_seconds": {
    "maximum": 300,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "asset_id"
  ],
  "type": "object"
 },
 "name": "deploy_code_asset",
 "title": "Deploy code in an isolated runner"
}
