{
 "description": "Upload and preview a consent-declared CSV. Nothing is created by this call. The consent declaration is mandatory: an import that cannot say why these people may be emailed is an import that does not happen.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "basis": {
    "description": "Consent basis",
    "enum": [
     "explicit",
     "documented_relationship"
    ],
    "type": "string"
   },
   "captured_at": {
    "description": "ISO-8601 capture time",
    "type": "string"
   },
   "csv": {
    "description": "CSV, maximum 5 MB and 10,000 rows",
    "type": "string"
   },
   "mapping": {
    "description": "Which CSV column feeds which contact field, e.g. `{ \"email\": \"Email\" }`.",
    "type": "object"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "source": {
    "description": "Consent source",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "csv",
   "mapping",
   "basis",
   "source",
   "captured_at"
  ],
  "type": "object"
 },
 "name": "commsharbor_contact_import_preview"
}
