{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Revoke Affiliate Access"
 },
 "description": "Remove an affiliate from a live event \u2014 disables their promo codes (keeps redemption history) and revokes access. Requires event_id + affiliate_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "affiliate_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "affiliate_id"
  ],
  "type": "object"
 },
 "name": "revoke_affiliate_access",
 "title": "Revoke Affiliate Access"
}
