{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Searches the ArisPay marketplace for paid APIs by capability or plain-language intent, optionally budget-bounded (integer cents). Read-only \u2014 spends no money, needs no API key. Returns ranked candidates with endpoint URL, price in cents, health and verification flags. Select this to FIND an endpoint; then inspect to price a specific URL, then pay to call it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "budgetCentsMax": {
    "description": "Maximum acceptable price in INTEGER CENTS (500 = $5.00). Free listings always pass.",
    "minimum": 0,
    "type": "integer"
   },
   "category": {
    "description": "Optional category filter (inference, data, media, search, social, infrastructure, trading, other).",
    "type": "string"
   },
   "limit": {
    "default": 5,
    "description": "Max results (default 5, max 20).",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Capability or plain-language intent, e.g. 'flight search'.",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "discover"
}
