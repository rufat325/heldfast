{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Turn one plain-language goal into an AgentLot paid request. Returns the 90/10 economics and optional Stripe funding URL. Does not guarantee a seller or outcome.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "budget_cents": {
    "minimum": 100,
    "type": "integer"
   },
   "category": {
    "type": "string"
   },
   "currency": {
    "enum": [
     "USD",
     "EUR",
     "DKK",
     "GBP"
    ],
    "type": "string"
   },
   "deadline_minutes": {
    "maximum": 43200,
    "minimum": 1,
    "type": "integer"
   },
   "fund_now": {
    "type": "boolean"
   },
   "goal": {
    "type": "string"
   }
  },
  "required": [
   "goal",
   "budget_cents"
  ],
  "type": "object"
 },
 "name": "start_paid_goal",
 "title": "Post paid work from a goal"
}
