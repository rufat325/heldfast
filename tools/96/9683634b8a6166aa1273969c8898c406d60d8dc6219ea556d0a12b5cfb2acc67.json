{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Search Knowledge Base"
 },
 "description": "TF-IDF search across the AMZScout knowledge base (Amazon-seller tutorials, brand reference, glossary). Returns the top-K relevant chunks with title, source URL and text. Use this to ground answers in factual material.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "query": {
    "description": "Search phrase. 2-300 chars.",
    "maxLength": 300,
    "minLength": 2,
    "type": "string"
   },
   "topK": {
    "default": 5,
    "description": "How many knowledge chunks to return (1\u201320)",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "amzscout_search_knowledge"
}
