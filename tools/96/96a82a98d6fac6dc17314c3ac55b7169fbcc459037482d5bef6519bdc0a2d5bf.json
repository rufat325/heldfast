{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "\"Old ICD-9 code for [diagnosis]\" / \"pre-2015 diagnosis codes\" \u2014 search legacy ICD-9-CM diagnostic codes. Use ONLY when working with pre-2015 US clinical records; new claims must use icd10cm. Same shape as icd10cm.",
 "inputSchema": {
  "examples": [
   {
    "count": 3,
    "terms": "asthma"
   }
  ],
  "properties": {
   "count": {
    "default": 7,
    "description": "Maximum matches to return. Default 7, max 500. Use 1\u20133 for typeahead UX, 20\u201350 for browsing.",
    "maximum": 500,
    "minimum": 1,
    "type": "number"
   },
   "df": {
    "description": "Comma-separated display fields to use in the `displays` array. Default varies per table; usually the canonical name.",
    "type": "string"
   },
   "ef": {
    "description": "Comma-separated extra fields to include per match. Field names vary per table; check NLM docs at clinicaltables.nlm.nih.gov.",
    "type": "string"
   },
   "terms": {
    "description": "Search query \u2014 prefix/contains match against canonical names. Whitespace-split into AND tokens. Example: \"asthma\".",
    "type": "string"
   }
  },
  "required": [
   "terms"
  ],
  "type": "object"
 },
 "name": "icd9cm",
 "title": "Icd9cm",
 "version": "v1.c1674df0"
}
