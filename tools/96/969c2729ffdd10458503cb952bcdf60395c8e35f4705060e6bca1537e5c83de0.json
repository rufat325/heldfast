{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get full metadata for one CZSO dataset (dataset_id from czso.dataset_list) \u2014 description, update frequency, temporal coverage, tags, and the resource list (download URL + format) needed before calling czso.dataset_data. Data: vdb.czso.cz (Czech Statistical Office open-data catalog), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataset_id": {
    "description": "CZSO dataset id, from czso.dataset_list (e.g. \"130141r25\" for 2024 population movement, \"010022\" for consumer price indices).",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "dataset_id"
  ],
  "type": "object"
 },
 "name": "czso.reference.dataset_metadata",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "CZSO Dataset Metadata"
}
