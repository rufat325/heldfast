{
 "description": "Get logs URL for a workflow run. GitHub returns a redirect to a zip file.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "owner": {
    "description": "Repository owner (username or org)",
    "type": "string"
   },
   "repo": {
    "description": "Repository name",
    "type": "string"
   },
   "run_id": {
    "description": "Workflow run ID",
    "type": "number"
   }
  },
  "required": [
   "owner",
   "repo",
   "run_id"
  ],
  "type": "object"
 },
 "name": "get_run_logs"
}
