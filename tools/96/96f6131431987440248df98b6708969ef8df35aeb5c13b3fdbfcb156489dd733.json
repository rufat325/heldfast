{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Escape hatch: a direct call to any method of the Yango Delivery claims B2B API \u2014 for endpoints without a dedicated tool (tariffs, delivery-methods, claims/proof-of-delivery/info, claims/edit, claims/apply-changes, claims/return, claims/bulk_info, claims/journal, \u2026). Paths are relative to the API root, e.g. \"b2b/cargo/integration/v2/tariffs\". query becomes the query string, body is sent as JSON. CAUTION: this tool can perform state-changing operations; 5xx/network errors are retried only for GETs.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "body": {
    "additionalProperties": {},
    "description": "JSON request body.",
    "type": "object"
   },
   "method": {
    "description": "HTTP method; defaults to POST.",
    "enum": [
     "GET",
     "POST"
    ],
    "type": "string"
   },
   "path": {
    "description": "Relative API path, e.g. \"b2b/cargo/integration/v2/claims/bulk_info\".",
    "minLength": 1,
    "type": "string"
   },
   "query": {
    "additionalProperties": {
     "type": [
      "string",
      "number",
      "boolean"
     ]
    },
    "description": "Query-string parameters, e.g. {\"claim_id\": \"...\"}.",
    "type": "object"
   }
  },
  "required": [
   "path"
  ],
  "type": "object"
 },
 "name": "raw_request",
 "title": "Raw Yango Delivery API call"
}
