{
 "description": "List Google Ads links for a GA4 property (linked customer ID, personalized-advertising and auto-tagging flags). Read-only.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "maxPages": {
    "description": "Maximum number of API pages to fetch (default 50). If more pages remain, the result includes truncated:true and a nextPageToken.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "pageToken": {
    "description": "Continuation token to resume listing from a previous truncated result.",
    "type": "string"
   },
   "property": {
    "description": "GA4 property ID, e.g. \"123456789\" or \"properties/123456789\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "property"
  ],
  "type": "object"
 },
 "name": "ga4_google_ads_links_list"
}
