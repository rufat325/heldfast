{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Drive the persistent MindForge Playwright/Chromium daemon (the same one behind /mindforge:browse): check status, navigate, click, type, screenshot, or assert on the current page. The daemon binds to 127.0.0.1 only (ADR-024) and must already be running \u2014 start it with `/mindforge:browse --start` first; this tool never spawns it. Arbitrary JS evaluation and native-browser cookie import are intentionally NOT exposed here.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "Browser action to perform",
    "enum": [
     "status",
     "navigate",
     "click",
     "type",
     "screenshot",
     "assert"
    ],
    "type": "string"
   },
   "assertType": {
    "description": "Assertion kind (action=assert)",
    "enum": [
     "visible",
     "url",
     "title"
    ],
    "type": "string"
   },
   "expectedText": {
    "description": "Expected value for the assertion (action=assert)",
    "type": "string"
   },
   "selector": {
    "description": "CSS selector (action=click|type|assert)",
    "type": "string"
   },
   "session": {
    "description": "Named browser session/context (default \"default\")",
    "type": "string"
   },
   "text": {
    "description": "Text to type, or fallback click-by-text (action=click|type)",
    "type": "string"
   },
   "url": {
    "description": "URL to navigate to (action=navigate)",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "mindforge_browse",
 "title": "Control the MindForge browser daemon"
}
