{
 "description": "List the agents in the workspace. Returns SENDABLE agents by default (status=live); archived agents cannot send, so they are excluded unless you ask for them. Pass status=\"all\" to see archived ones too.",
 "inputSchema": {
  "properties": {
   "cursor": {
    "type": "string"
   },
   "limit": {
    "type": "number"
   },
   "status": {
    "description": "Default \"live\" = everything except archived.",
    "enum": [
     "live",
     "all",
     "active",
     "paused",
     "archived"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "mails.list_agents"
}
