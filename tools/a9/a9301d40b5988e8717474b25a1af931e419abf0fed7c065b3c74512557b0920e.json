{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Create folder"
 },
 "description": "Create a folder to organise QR codes, optionally nested under a parent. Creating a folder that already exists (same name, same parent) returns the existing one, so it's safe to call repeatedly.",
 "inputSchema": {
  "properties": {
   "name": {
    "description": "Folder name.",
    "type": "string"
   },
   "parent": {
    "description": "Parent folder id or slug to nest under (optional).",
    "type": "string"
   },
   "workspace": {
    "description": "Workspace id. Optional when the account has one workspace; use list_workspaces to resolve otherwise.",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "create_folder",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "code_count": {
    "type": "integer"
   },
   "created": {
    "type": "string"
   },
   "error": {
    "additionalProperties": true,
    "description": "Present only when the tool failed (the result is also flagged isError).",
    "properties": {
     "cause": {
      "description": "Machine-readable failure category: invalid_argument, not_found, forbidden, plan_limit, rate_limited, or server_error.",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "retryable": {
      "description": "Whether repeating the same call could succeed.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "id": {
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "parent": {
    "description": "Parent folder id, null at the top level.",
    "type": [
     "string",
     "null"
    ]
   },
   "path": {
    "description": "Full path, e.g. Campaigns / Spring 2026.",
    "type": "string"
   },
   "slug": {
    "type": "string"
   }
  },
  "type": "object"
 }
}
