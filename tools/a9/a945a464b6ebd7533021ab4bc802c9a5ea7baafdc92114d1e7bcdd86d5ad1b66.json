{
 "description": "Unwrap a previously wrapped stdio MCP server, restoring its original command. Requires restarting Claude Code to take effect.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "mcp_name": {
    "description": "The name of the MCP server to unwrap",
    "type": "string"
   }
  },
  "required": [
   "mcp_name"
  ],
  "type": "object"
 },
 "name": "unwrap_mcp_stdio"
}
