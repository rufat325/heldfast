{
 "description": "Create an audience or saved segment. Only allowlisted filter fields are accepted \u2014 a saved segment cannot be turned into an arbitrary query over the CRM.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "filter": {
    "description": "The saved filter, for `saved_segment`. Allowlisted fields only.",
    "type": "object"
   },
   "kind": {
    "description": "static or saved_segment",
    "enum": [
     "static",
     "saved_segment"
    ],
    "type": "string"
   },
   "name": {
    "description": "Audience name",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "name",
   "kind"
  ],
  "type": "object"
 },
 "name": "commsharbor_audience_create"
}
