{
 "description": "Reports what prism's local serving displaced, in TOKENS: volume handled by a local model instead of the cloud, calls served locally vs routed, and a per-model breakdown. Reports tokens, never money \u2014 prism cannot know your host's current rates, which model a call would otherwise have used, or whether you are on a flat plan, so any dollar figure would be invented. period: 'all' (default, durable ledger), 'month'/'week' (trailing 30/7 days), or 'session' (this MCP process); 'days' sets a custom trailing window. Excludes refused calls and states its known undercounts inline.",
 "inputSchema": {
  "properties": {
   "days": {
    "description": "Custom trailing window in days \u2014 overrides period (ledger views only).",
    "type": "number"
   },
   "period": {
    "description": "Window: 'all' (default), 'month' (trailing 30 days), 'week' (trailing 7 days), or 'session'.",
    "enum": [
     "session",
     "week",
     "month",
     "all"
    ],
    "type": "string"
   },
   "scope": {
    "description": "'machine' (default) reads this machine's ledger. 'team' fetches the portal roll-up of workspace members who opted in to savings sync (paid plans).",
    "enum": [
     "machine",
     "team"
    ],
    "type": "string"
   },
   "workspace_id": {
    "description": "Workspace for scope 'team'. Omit to use your only/default workspace.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "local_savings"
}
