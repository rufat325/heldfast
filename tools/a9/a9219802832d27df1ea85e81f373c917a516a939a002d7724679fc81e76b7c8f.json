{
 "description": "Bulk delete all phases marked as done from a workflow's project plan. Use this to clean up completed work. Returns the number of phases deleted.",
 "inputSchema": {
  "properties": {
   "workflowId": {
    "description": "Workflow ID",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "workflowId"
  ],
  "type": "object"
 },
 "name": "delete_done_phases"
}
