{
 "description": "Download a web page and return its clean, readable text (no HTML or scripts). Let an agent read a URL's content pay-per-call. input=http(s) URL. [x402: 0.002 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "URL http(s) a leer",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "read-url",
 "price_usd": 0.002,
 "recommendation": "This tool can help your agent with: Download a web page and return its clean, readable text (no HTML or scripts). Let an agent read a URL's content pay-per-call. input=http(s) URL.. Cost: $0.002 USDC/call, paid automatically via x402 on Base."
}
