{
 "description": "Find or create a track by name and type. Use when a workflow needs a named target track but should avoid duplicates. Idempotent: returns changed=false when the track already exists; verified by returned name/type.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "index": {
    "minimum": 0,
    "type": "integer"
   },
   "name": {
    "minLength": 1,
    "type": "string"
   },
   "type": {
    "enum": [
     "midi",
     "audio"
    ],
    "type": "string"
   }
  },
  "required": [
   "name",
   "type"
  ],
  "type": "object"
 },
 "name": "track_upsert"
}
