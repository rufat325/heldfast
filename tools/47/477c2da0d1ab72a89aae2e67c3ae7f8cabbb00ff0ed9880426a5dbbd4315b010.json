{
 "_meta": {
  "skill": "defi"
 },
 "description": "Execute a token swap on ALEX DEX.\n\nSwaps tokenX for tokenY using the ALEX AMM.\nUse full contract IDs for tokens.\n\nNote: ALEX DEX is only available on mainnet.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "amountIn": {
    "description": "Amount of tokenX to swap (in smallest units)",
    "type": "string"
   },
   "minAmountOut": {
    "default": "0",
    "description": "Minimum acceptable output amount (slippage protection)",
    "type": "string"
   },
   "tokenX": {
    "description": "Input token contract ID",
    "type": "string"
   },
   "tokenY": {
    "description": "Output token contract ID",
    "type": "string"
   }
  },
  "required": [
   "tokenX",
   "tokenY",
   "amountIn"
  ],
  "type": "object"
 },
 "name": "alex_swap"
}
