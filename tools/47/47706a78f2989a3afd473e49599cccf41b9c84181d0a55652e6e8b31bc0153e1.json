{
 "description": "Record permission-based marketing consent. This NEVER restores a previous unsubscribe. If the contact opted out, they stay out and `marketing_enabled` remains false \u2014 recording consent after the fact does not undo their decision.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "basis": {
    "description": "explicit or documented_relationship",
    "enum": [
     "explicit",
     "documented_relationship"
    ],
    "type": "string"
   },
   "captured_at": {
    "description": "ISO-8601 capture time",
    "type": "string"
   },
   "contact_id": {
    "description": "CRM contact ID",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "source": {
    "description": "Consent source",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "contact_id",
   "basis",
   "source",
   "captured_at"
  ],
  "type": "object"
 },
 "name": "commsharbor_contact_marketing_put"
}
