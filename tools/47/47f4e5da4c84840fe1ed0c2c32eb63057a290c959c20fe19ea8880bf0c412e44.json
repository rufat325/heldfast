{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List email templates"
 },
 "description": "Call this to discover available email templates before creating a campaign. Returns templates for the active tenant: id, name, tags, and whether they use MJML or HTML. Use the returned template id in nevent_create_campaign. Call nevent_get_template to inspect the full HTML/MJML source of a specific template.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "content_nature": {
    "description": "Filter by AI-assigned content nature classification (e.g. \"promotional\", \"transactional\", \"newsletter\", \"event_reminder\"). Omit to return templates of all natures.",
    "type": "string"
   },
   "limit": {
    "default": 50,
    "description": "Maximum number of templates to return (default 50, max 200)",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "sort": {
    "default": "modifiedAt",
    "description": "Sort field: createdAt | modifiedAt | name (default modifiedAt)",
    "enum": [
     "createdAt",
     "modifiedAt",
     "name"
    ],
    "type": "string"
   },
   "sort_order": {
    "default": "desc",
    "description": "Sort direction: asc | desc (default desc)",
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   },
   "tags": {
    "description": "Filter templates by tags. Only templates that have ALL specified tags are returned. Omit to return templates regardless of tags.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "nevent_list_templates"
}
