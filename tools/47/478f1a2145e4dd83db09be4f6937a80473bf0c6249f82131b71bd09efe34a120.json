{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Free quote before paying: the exchange rate and exactly how much lands in the bank (or wallet) NET of every provider fee \u2014 that net figure is the number to decide on. Executable estimate, not a locked rate. side=SELL (USDC->fiat) or BUY (fiat->USDC).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cryptoAmount": {
    "description": "USDC amount (required for SELL)",
    "type": "number"
   },
   "fiatAmount": {
    "description": "Fiat amount (for BUY)",
    "type": "number"
   },
   "fiatCurrency": {
    "description": "e.g. EUR, default EUR",
    "type": "string"
   },
   "network": {
    "description": "USDC network, default base",
    "type": "string"
   },
   "side": {
    "default": "SELL",
    "description": "SELL = USDC to fiat, BUY = fiat to USDC",
    "enum": [
     "SELL",
     "BUY"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_quote",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "asOf": {
    "description": "ISO timestamp of the quote",
    "type": "string"
   },
   "fiatCurrency": {
    "description": "Fiat currency of the quote",
    "type": "string"
   },
   "network": {
    "description": "USDC network the quote assumes",
    "type": "string"
   },
   "note": {
    "description": "Caveats \u2014 youReceive is net of provider fees; the x402 session fee is separate",
    "type": "string"
   },
   "providerFixedFee": {
    "description": "Provider-reported fixed fee component",
    "type": "number"
   },
   "providerNetworkFee": {
    "description": "Provider-reported network delivery fee (0 on Base)",
    "type": "number"
   },
   "rate": {
    "description": "Exchange rate used (fiat per USDC)",
    "type": "number"
   },
   "side": {
    "description": "Quote direction",
    "enum": [
     "SELL",
     "BUY"
    ],
    "type": "string"
   },
   "source": {
    "description": "Where the price came from (the provider's own conversion API)",
    "type": "string"
   },
   "youReceive": {
    "description": "Amount received NET of all provider fees, e.g. '87.78 EUR' \u2014 this is the number to decide on",
    "type": "string"
   },
   "youSend": {
    "description": "Amount the sender pays, e.g. '100 USDC'",
    "type": "string"
   }
  },
  "required": [
   "side",
   "rate",
   "youSend",
   "youReceive"
  ],
  "type": "object"
 },
 "title": "Get a free quote"
}
