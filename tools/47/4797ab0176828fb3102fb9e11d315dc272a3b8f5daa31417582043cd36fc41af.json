{
 "description": "List credit notes. Supports where-clause filtering by Type, Status, Contact.ContactID, Date.",
 "inputSchema": {
  "properties": {
   "order": {
    "description": "Ordering, e.g. 'Date DESC'.",
    "type": "string"
   },
   "page": {
    "description": "Page number (100 per page).",
    "type": "number"
   },
   "where": {
    "description": "Xero where clause, e.g. 'Type==\"ACCRECCREDIT\" AND Status==\"AUTHORISED\"'.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_credit_notes"
}
