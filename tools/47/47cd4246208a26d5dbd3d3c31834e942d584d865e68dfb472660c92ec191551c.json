{
 "description": "Get the status and result of a specific agent runner job. Poll this after dispatch_agent_job to check completion.",
 "inputSchema": {
  "properties": {
   "jobId": {
    "description": "Job ID returned by dispatch_agent_job.",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your active workspace.",
    "type": "string"
   }
  },
  "required": [
   "jobId"
  ],
  "type": "object"
 },
 "name": "get_agent_job"
}
