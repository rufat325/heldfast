{
 "description": "Compare an earlier FixMap JSON report with a current report without rescanning. Returns files that entered, left, moved, changed confidence, or stayed unchanged.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "current": {
    "anyOf": [
     {
      "type": "object"
     },
     {
      "type": "string"
     }
    ],
    "description": "Current FixMap JSON report object or path to a local JSON report file"
   },
   "format": {
    "description": "Output format: markdown (default) or json, case-insensitive",
    "type": "string"
   },
   "previous": {
    "anyOf": [
     {
      "type": "object"
     },
     {
      "type": "string"
     }
    ],
    "description": "Earlier FixMap JSON report object or path to a local JSON report file"
   }
  },
  "required": [
   "previous",
   "current"
  ],
  "type": "object"
 },
 "name": "fixmap_compare",
 "title": "FixMap compare"
}
