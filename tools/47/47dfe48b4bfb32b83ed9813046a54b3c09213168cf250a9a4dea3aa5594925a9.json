{
 "description": "Every project the key owner has, with health status, stage and settings. Start here: most other capabilities take a project id or slug. (GET /api/v1/projects; needs the read:projects scope)",
 "inputSchema": {
  "properties": {
   "include_archived": {
    "description": "true to include archived projects (default: exclude).",
    "type": "boolean"
   },
   "limit": {
    "description": "1\u2013200, default 100.",
    "type": "number"
   },
   "updated_since": {
    "description": "ISO timestamp; only projects changed at or after it.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_projects"
}
