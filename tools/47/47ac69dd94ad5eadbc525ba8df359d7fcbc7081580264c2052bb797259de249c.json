{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Delete a DNS record from an Impreza-managed domain. Identify it by `type` + `host` + `value`.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "domain": {
    "description": "The domain name, e.g. example.com.",
    "type": "string"
   },
   "host": {
    "description": "Record name/host to delete.",
    "type": "string"
   },
   "type": {
    "description": "Record type to delete.",
    "type": "string"
   },
   "value": {
    "description": "Value of the record to delete.",
    "type": "string"
   }
  },
  "required": [
   "domain",
   "type",
   "host",
   "value"
  ],
  "type": "object"
 },
 "name": "impreza_delete_dns_record"
}
