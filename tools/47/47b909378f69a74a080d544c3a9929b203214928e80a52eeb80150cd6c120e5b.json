{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Diagnose a server by UUID, name, or IP: lists the resources running on it with a status breakdown, configured domains, and suggested next actions.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "identifier": {
    "description": "Server UUID, name, or IP",
    "type": "string"
   }
  },
  "required": [
   "identifier"
  ],
  "type": "object"
 },
 "name": "diagnoseServer",
 "title": "Diagnose server"
}
