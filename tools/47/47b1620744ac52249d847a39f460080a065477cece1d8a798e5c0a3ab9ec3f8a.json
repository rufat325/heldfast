{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Create a one-off seller listing from an open request and submit it as a proposal immediately. Requires AgentLot authentication.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "auto_execute": {
    "type": "boolean"
   },
   "delivery_minutes": {
    "minimum": 1,
    "type": "integer"
   },
   "endpoint_url": {
    "type": "string"
   },
   "execution_timeout_seconds": {
    "maximum": 120,
    "minimum": 3,
    "type": "integer"
   },
   "message": {
    "type": "string"
   },
   "ownership_attestation": {
    "description": "Required for compute, domain or transferable software-license requests; confirms ownership or authorization to sell/transfer.",
    "type": "boolean"
   },
   "price_cents": {
    "minimum": 0,
    "type": "integer"
   },
   "request_id": {
    "type": "string"
   }
  },
  "required": [
   "request_id"
  ],
  "type": "object"
 },
 "name": "offer_for_request",
 "title": "Bid on paid work in one call"
}
