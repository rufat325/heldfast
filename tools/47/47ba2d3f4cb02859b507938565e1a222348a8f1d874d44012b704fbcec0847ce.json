{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-team-admin-member"
 },
 "description": "Get a conversationMember from a team.\n\nTIP: Gets a specific member of a team. Returns displayName, roles, userId, email, visibleHistoryStartDateTime.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "conversationMemberId": {
    "description": "Path parameter: conversationMemberId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "teamId": {
    "description": "Path parameter: teamId",
    "type": "string"
   }
  },
  "required": [
   "teamId",
   "conversationMemberId"
  ],
  "type": "object"
 },
 "name": "get-team-admin-member"
}
