{
 "description": "Market-wide screen across up to 16 assets: ranks by signal strength, funding extremes, and regime state. Optional comma-separated `assets` filter. $0.04 per call via x402.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "assets": {
    "description": "Comma-separated asset symbols to filter, e.g. 'btc,eth,sol'. Omit for all 16.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_market_scan"
}
