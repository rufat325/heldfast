{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true
 },
 "description": "Moderate a comment by hiding, unhiding, or deleting it. The interaction_id must be a comment ID starting with sapi_cmt_. Actions: 'hide' removes from public view (reversible), 'unhide' restores a hidden comment, 'delete' permanently removes it (irreversible). Returns 501 for platforms without moderation support.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "account_id": {
    "description": "Connected account ID",
    "type": "string"
   },
   "action": {
    "description": "Moderation action: hide, unhide, or delete",
    "enum": [
     "hide",
     "unhide",
     "delete"
    ],
    "type": "string"
   },
   "interaction_id": {
    "description": "Comment ID (must start with sapi_cmt_)",
    "type": "string"
   }
  },
  "required": [
   "account_id",
   "interaction_id",
   "action"
  ],
  "type": "object"
 },
 "name": "social_api_moderate_comment",
 "title": "Moderate Comment"
}
