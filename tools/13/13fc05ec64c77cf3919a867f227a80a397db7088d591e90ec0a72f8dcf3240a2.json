{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": true,
  "title": "Remind get classes"
 },
 "description": "Get full detail for one or more classes by uuid: name, join code/url, member and message counts, owner count, history and messaging flags, and what this account may edit.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "uuids": {
    "description": "Class uuids, from remind_list_entities.",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "uuids"
  ],
  "type": "object"
 },
 "name": "remind_get_classes"
}
