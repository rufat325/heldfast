{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get Paleobiology Database (PBDB) fossil collections matched to a Macrostrat stratigraphic column or rock unit, looked up by col_id or unit_id (from macrostrat.columns_search / macrostrat.units_search), optionally narrowed by chronostratigraphic interval name. Returns collection names, ages, and PBDB occurrence counts. Data: macrostrat.org/api/v2, no auth required, CC BY 4.0.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "col_id": {
    "description": "Macrostrat column ID (from macrostrat.columns_search) to find fossil collections in",
    "type": "integer"
   },
   "interval_name": {
    "description": "Optional chronostratigraphic interval name to further narrow results within col_id/unit_id",
    "type": "string"
   },
   "unit_id": {
    "description": "Macrostrat unit ID (from macrostrat.units_search) to find fossil collections in",
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "macrostrat.geology.fossils_search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Macrostrat Fossil Collection Search"
}
