{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Enable or disable delete protection on a primary IP to guard against accidental destruction.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "delete": {
    "description": "If true, prevents the primary IP from being deleted",
    "type": "boolean"
   },
   "id": {
    "description": "Primary IP ID",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "hetzner_change_primary_ip_protection",
 "title": "Change Primary IP Protection"
}
