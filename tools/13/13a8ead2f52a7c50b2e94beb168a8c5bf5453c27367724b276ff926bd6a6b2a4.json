{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List build variants of a module by inspecting its Gradle assemble tasks (runs './gradlew <module>:tasks').",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "module": {
    "description": "Module name, e.g. 'app' (defaults to session)",
    "type": "string"
   },
   "projectDir": {
    "description": "Project root (defaults to session)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_variants",
 "title": "List build variants"
}
