{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get a date-ranged time series of readings (date, value, quality, completeness) for one EA hydrology data series, identified by the measure_id from ea-hydrology.station_measures. Requires min_date and max_date (YYYY-MM-DD, inclusive). Data: environment.data.gov.uk/hydrology, UK Open Government Licence v3.0, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max readings to return (default 500, max 2000).",
    "maximum": 2000,
    "minimum": 1,
    "type": "integer"
   },
   "max_date": {
    "description": "End date (inclusive), format YYYY-MM-DD.",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "measure_id": {
    "description": "Measure identifier returned by ea-hydrology.station_measures.",
    "maxLength": 300,
    "minLength": 1,
    "type": "string"
   },
   "min_date": {
    "description": "Start date (inclusive), format YYYY-MM-DD.",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   }
  },
  "required": [
   "measure_id",
   "min_date",
   "max_date"
  ],
  "type": "object"
 },
 "name": "ea-hydrology.readings.range",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "EA Hydrology Readings Range"
}
