{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "What matters now: the published signal cards"
 },
 "description": "The governed signal layer the site ranks by (actionable now, public or sellable only): each card names its family, the subject with its dfx id, what changed, why it matters, why now, the falsifier, the dates, the exposure in dollars with its basis, a rank, and the audience (who is affected and how) with their dfx ids. Reachability is a count only: how many named people are on the card and, for each organisation, whether a verified contact route exists and how many. Filter by family, domain, subject id, since date or minimum exposure. Not a prediction: every card names the observation it rests on.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "affected_dfx_id": {
    "description": "A dfx id on the AUDIENCE side: signals where this entity is affected (a lender, sponsor, firm).",
    "type": "string"
   },
   "cursor": {
    "description": "next_cursor from a previous page of this tool, unchanged.",
    "type": "string"
   },
   "domain": {
    "enum": [
     "private_credit",
     "real_estate",
     "ria_wealth",
     "private_equity",
     "allocators"
    ],
    "type": "string"
   },
   "family": {
    "description": "A signal family key, e.g. credit.maturity_approaching_12m, credit.sponsor_lender_relationship, credit.tranche_markdown_5pt, credit.tranche_markdown_intensified, credit.pik_added_or_increased, debt.maturity_window_12m.",
    "type": "string"
   },
   "include_audience": {
    "default": true,
    "type": "boolean"
   },
   "limit": {
    "default": 20,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "min_exposure_usd": {
    "type": "number"
   },
   "query": {
    "description": "Subject name contains.",
    "type": "string"
   },
   "since": {
    "description": "ISO date; signals that occurred on or after.",
    "type": "string"
   },
   "subject_dfx_id": {
    "description": "A DFX id (dfx:<graph>:<uuid>) or a bare real estate UUID.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "search_signals",
 "title": "What matters now: the published signal cards"
}
