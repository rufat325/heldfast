{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "Remove a label from a thread"
 },
 "description": "Remove a shared label from an email thread (or task).",
 "inputSchema": {
  "properties": {
   "boardId": {
    "description": "The board ID the thread belongs to",
    "type": "number"
   },
   "labelId": {
    "description": "The label ID to remove",
    "type": "number"
   },
   "threadId": {
    "description": "The thread or task ID to remove the label from",
    "type": "string"
   }
  },
  "required": [
   "threadId",
   "labelId",
   "boardId"
  ],
  "type": "object"
 },
 "name": "remove_label_from_thread",
 "title": "Remove a label from a thread"
}
