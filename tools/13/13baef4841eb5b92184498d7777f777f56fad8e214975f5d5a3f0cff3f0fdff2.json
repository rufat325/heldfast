{
 "description": "Deploy a new Longhorn engine image. `image` is not validated against a known-good list. Destructive. Do not call this unless the user has explicitly authorized it in this conversation \u2014 never proactively, speculatively, or as a side effect of another task. Longhorn docs: https://longhorn.io/docs/1.12.1/deploy/upgrade/",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "image": {
    "type": "string"
   }
  },
  "required": [
   "image"
  ],
  "type": "object"
 },
 "name": "engineimage_create",
 "title": "Deploy engine image"
}
