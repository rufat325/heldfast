{
 "description": "Create signed preference and unsubscribe URLs. The capability is scoped to one organization and one contact, it expires, and it embeds no email address. Put `marketing_headers` in the message and the unsubscribe works without a login.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "contact_id": {
    "description": "CRM contact ID",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "contact_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_preference_token_create"
}
