{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the components (SYMBOL nodes) defined in the file: guid, name, description, the component-set it belongs to when it is a variant, its property definitions with defaults, and how many instances of it exist. Filter with `query` (case-insensitive substring of the name). Sorted by instance count, most-used first, so the load-bearing parts of the design system come back before one-off symbols. Follow up with fig_instance on a specific INSTANCE guid.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "cursor": {
    "description": "Opaque nextCursor from a previous truncated response; resumes where it stopped.",
    "type": "string"
   },
   "file": {
    "description": "Path to the .fig / .figma file (absolute, or relative to the server CWD).",
    "type": "string"
   },
   "limit": {
    "description": "Default 50.",
    "maximum": 300,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Case-insensitive substring of the component name.",
    "type": "string"
   }
  },
  "required": [
   "file"
  ],
  "type": "object"
 },
 "name": "fig_components",
 "title": "Component catalogue"
}
