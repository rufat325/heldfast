{
 "description": "Get monthly CPI history for one category (headline \"all-items\" goes back to 1913). Returns index level and YoY/MoM percent per month, most recent first. Paginated by period range: if meta.next_end is not null, call again with end = meta.next_end for older data. Note: October 2025 was never published by the BLS (federal shutdown) and is absent.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "adjusted": {
    "description": "false (default) = not seasonally adjusted (NSA, the year-over-year convention the press quotes); true = seasonally adjusted (SA, the month-over-month convention).",
    "type": "boolean"
   },
   "category": {
    "description": "CPI category. \"all-items\" is headline inflation; \"core\" excludes food and energy.",
    "enum": [
     "all-items",
     "core",
     "energy",
     "food",
     "housing",
     "apparel",
     "transportation",
     "medical",
     "recreation",
     "education-communication",
     "other"
    ],
    "type": "string"
   },
   "end": {
    "description": "Inclusive end month (YYYY-MM). Pass meta.next_end here to get the next page.",
    "pattern": "^\\d{4}-(0[1-9]|1[0-2])$",
    "type": "string"
   },
   "limit": {
    "description": "Max points per page (1-100, default 30).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "start": {
    "description": "Inclusive start month (YYYY-MM), e.g. \"2020-01\".",
    "pattern": "^\\d{4}-(0[1-9]|1[0-2])$",
    "type": "string"
   }
  },
  "required": [
   "category"
  ],
  "type": "object"
 },
 "name": "get_us_inflation_history",
 "title": "US inflation monthly history"
}
