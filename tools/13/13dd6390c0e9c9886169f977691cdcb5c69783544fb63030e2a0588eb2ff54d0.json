{
 "description": "[Merchant] Format JSON payload with deterministic key sorting for consistent SHA-256 calculation and payware API compliance",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "calculateSHA256": {
    "default": true,
    "description": "Calculate SHA-256 hash for JWT contentSha256 usage",
    "type": "boolean"
   },
   "payload": {
    "description": "JSON payload to format with deterministic serialization",
    "type": "object"
   },
   "showComparison": {
    "default": true,
    "description": "Show comparison between standard and deterministic JSON",
    "type": "boolean"
   },
   "validateConsistency": {
    "default": false,
    "description": "Validate that multiple serializations produce the same result",
    "type": "boolean"
   }
  },
  "required": [
   "payload"
  ],
  "type": "object"
 },
 "name": "payware_utils_format_json_deterministic"
}
