{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List Promo Codes"
 },
 "description": "List the promo codes on a live event \u2014 code, discount, usage vs cap, schedule, classes, and whether each is an affiliate/profile code. Requires event_id; you must be a host. Read-only.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "enabled_only": {
    "type": "boolean"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id"
  ],
  "type": "object"
 },
 "name": "list_promo_codes",
 "title": "List Promo Codes"
}
