{
 "description": "Open a Gretl-supported dataset file, return summary statistics, and open the same workflow in Gretl GUI by default.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "datasetPath": {
    "description": "Path to a CSV, gdt, Excel, Stata, SPSS, or other Gretl-supported dataset.",
    "minLength": 1,
    "type": "string"
   },
   "displayInGretl": {
    "description": "Open a visible Gretl GUI script window for this workflow. Defaults to true outside CI.",
    "type": "boolean"
   },
   "gretlCliPath": {
    "type": "string"
   },
   "gretlGuiPath": {
    "type": "string"
   },
   "guiNewInstance": {
    "default": true,
    "type": "boolean"
   },
   "requireGui": {
    "description": "Require the visible Gretl GUI to open for this workflow. Defaults to true outside CI.",
    "type": "boolean"
   },
   "safeMode": {
    "default": false,
    "type": "boolean"
   },
   "timeoutSeconds": {
    "exclusiveMinimum": 0,
    "maximum": 300,
    "type": "integer"
   }
  },
  "required": [
   "datasetPath"
  ],
  "type": "object"
 },
 "name": "gretl_dataset_summary"
}
