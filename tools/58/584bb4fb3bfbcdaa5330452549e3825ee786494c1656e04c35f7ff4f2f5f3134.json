{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Full detail for a single posting by its id, including the description and the application link.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "format": {
    "enum": [
     "markdown",
     "json"
    ],
    "type": "string"
   },
   "id": {
    "description": "Posting id returned by search_jobs.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_job",
 "title": "Fetch one posting"
}
