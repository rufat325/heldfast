{
 "description": "Control Face ID / Touch ID: enroll, unenroll, trigger matching (success), or trigger failure. Test biometric authentication flows end-to-end.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "\"enroll\" to enable biometrics, \"unenroll\" to disable, \"match\" to simulate successful auth, \"fail\" to simulate failed auth",
    "enum": [
     "enroll",
     "unenroll",
     "match",
     "fail"
    ],
    "type": "string"
   },
   "deviceId": {
    "description": "Device (default: booted)",
    "type": "string"
   },
   "enrolled": {
    "description": "(Deprecated: use action instead) Whether Face ID / Touch ID is enrolled",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "simulator_biometric"
}
