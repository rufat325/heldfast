{
 "description": "Fill placeholders in a markdown/.docx template with parameter values (deterministic; no LLM). Returns the filled document on stdout.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "params": {
    "additionalProperties": true,
    "description": "Parameter values (snake_case keys), passed as JSON.",
    "type": "object"
   },
   "template": {
    "description": "Path to the template.",
    "type": "string"
   }
  },
  "required": [
   "template"
  ],
  "type": "object"
 },
 "name": "fill_template"
}
