{
 "description": "Open a real TCP connection and TLS handshake to the relay from THIS machine's network and report what happened. Sends no credentials and no mail. This is the one question documentation cannot answer: whether the firewall in front of the asker permits outbound 587.",
 "inputSchema": {
  "properties": {
   "host": {
    "description": "Host, defaults to mx.msgwing.com.",
    "type": "string"
   },
   "port": {
    "description": "Port, 587 (default) or 465.",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "check_relay_reachable"
}
