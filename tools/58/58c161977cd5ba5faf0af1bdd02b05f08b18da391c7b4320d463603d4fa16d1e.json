{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get historical daily mean streamflow (discharge) values for a USGS gauge station over any date range. Returns one measurement per day (midnight UTC) in cubic feet per second (ft3/s). Also supports gage height (ft), water temperature (\u00b0C), specific conductance, and turbidity via parameter_cd. Ideal for trend analysis, drought monitoring, flood season review, and hydrology research. Coverage: 1,500+ active US streamflow stations, records from 1900s to present. Use water.sites to look up station numbers by state or bounding box. No auth \u2014 USGS public domain, unlimited calls.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "end_date": {
    "description": "End date in YYYY-MM-DD format (e.g. \"2026-06-30\"). Defaults to today if omitted. Max range: 1 year.",
    "type": "string"
   },
   "parameter_cd": {
    "description": "USGS parameter code. \"00060\" = discharge/streamflow (ft3/s, default), \"00065\" = gage height (ft), \"00010\" = water temperature (\u00b0C), \"00095\" = specific conductance (\u00b5S/cm), \"63680\" = turbidity (FNU).",
    "enum": [
     "00060",
     "00065",
     "00010",
     "00095",
     "63680"
    ],
    "type": "string"
   },
   "site_no": {
    "description": "USGS gauge station number, 8\u201315 digits (e.g. \"01646500\" for Potomac River near DC). Use water.sites to find station numbers.",
    "type": "string"
   },
   "start_date": {
    "description": "Start date in YYYY-MM-DD format (e.g. \"2026-01-01\"). Defaults to 30 days ago if omitted.",
    "type": "string"
   }
  },
  "required": [
   "site_no"
  ],
  "type": "object"
 },
 "name": "nwis.streamflow.daily_values",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "USGS NWIS \u2014 Daily Streamflow Values"
}
