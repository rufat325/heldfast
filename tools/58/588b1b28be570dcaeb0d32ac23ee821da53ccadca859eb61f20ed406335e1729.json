{
 "description": "Grade up to 50 quotes in one round-trip. Falls back to a category-inferred FMV (flagged category_inferred:true) when a service has no observations yet.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "items": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "amount_usd": {
       "minimum": 0,
       "type": "number"
      },
      "slug": {
       "minLength": 1,
       "type": "string"
      },
      "unit": {
       "type": "string"
      }
     },
     "required": [
      "slug",
      "amount_usd"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "items"
  ],
  "type": "object"
 },
 "name": "is_fair_price_batch",
 "title": "Batch fair-price verdict"
}
