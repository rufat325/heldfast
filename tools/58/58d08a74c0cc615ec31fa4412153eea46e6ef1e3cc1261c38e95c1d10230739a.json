{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Generic fallback to any data.stortinget.no/eksport resource. Use for endpoints without a dedicated tool, e.g. resource \"moter\" (meetings), \"komiteer\" (committees) with params {sesjonid}. ?format=json is added automatically.",
 "inputSchema": {
  "examples": [
   {
    "params": {
     "sesjonid": "2023-2024"
    },
    "resource": "moter"
   },
   {
    "resource": "komiteer"
   }
  ],
  "properties": {
   "params": {
    "description": "Query params, e.g. {\"sesjonid\": \"2023-2024\"}. Do not include format.",
    "type": "object"
   },
   "resource": {
    "description": "Endpoint name under /eksport, e.g. \"moter\", \"komiteer\", \"sesjoner\".",
    "type": "string"
   }
  },
  "required": [
   "resource"
  ],
  "type": "object"
 },
 "name": "export",
 "title": "Export",
 "version": "v1.fea2169f"
}
