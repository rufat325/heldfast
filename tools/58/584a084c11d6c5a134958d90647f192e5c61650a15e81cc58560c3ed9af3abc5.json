{
 "description": "Create a payout (disbursement) to a beneficiary. Rapyd supports bank transfer, wallet top-up, and cash pickup (OXXO Pay, 7-Eleven, etc). Use list_payout_methods_by_country first to find the right payout_method_type for the destination country.",
 "inputSchema": {
  "properties": {
   "beneficiary": {
    "description": "Beneficiary object. Shape depends on beneficiary country and payout_method_type \u2014 typically includes name, account_number or card_number, identification fields, and address fields.",
    "type": "object"
   },
   "beneficiary_country": {
    "description": "ISO-3166 alpha-2 country code of the beneficiary",
    "type": "string"
   },
   "description": {
    "description": "Purpose / memo shown to the beneficiary where supported",
    "type": "string"
   },
   "ewallet": {
    "description": "Rapyd ewallet id to debit as the payout source",
    "type": "string"
   },
   "merchant_reference_id": {
    "description": "Merchant-side payout reference",
    "type": "string"
   },
   "metadata": {
    "description": "Arbitrary key-value metadata",
    "type": "object"
   },
   "payout_amount": {
    "description": "Payout amount in major units of payout_currency",
    "type": "number"
   },
   "payout_currency": {
    "description": "ISO-4217 currency code of the payout",
    "type": "string"
   },
   "payout_method_type": {
    "description": "Rapyd payout method type (e.g. 'mx_cash_oxxopay_payout', 'in_hdfc_bank', 'ph_gcash_ewallet'). Discover via list_payout_methods_by_country.",
    "type": "string"
   },
   "sender": {
    "description": "Sender object. Shape depends on sender country \u2014 typically includes first_name, last_name, company_name (for business senders), identification_type, identification_value, address fields.",
    "type": "object"
   },
   "sender_country": {
    "description": "ISO-3166 alpha-2 country code of the sending account (e.g. US)",
    "type": "string"
   },
   "sender_currency": {
    "description": "ISO-4217 currency code of the sending account",
    "type": "string"
   },
   "statement_descriptor": {
    "description": "Descriptor shown on the beneficiary's statement where supported",
    "type": "string"
   }
  },
  "required": [
   "sender_country",
   "sender_currency",
   "beneficiary_country",
   "payout_currency",
   "payout_amount",
   "payout_method_type",
   "sender",
   "beneficiary"
  ],
  "type": "object"
 },
 "name": "create_payout"
}
