{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false
 },
 "description": "Set a pane's display label (the tab name). Also what `app` filters match in get_logs/diagnose. (Cockpit only.)",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Id of the pane to label (from pane_list).",
    "type": "string"
   },
   "label": {
    "description": "New display label (e.g. the project name).",
    "type": "string"
   }
  },
  "required": [
   "id",
   "label"
  ],
  "type": "object"
 },
 "name": "pane_set_label"
}
