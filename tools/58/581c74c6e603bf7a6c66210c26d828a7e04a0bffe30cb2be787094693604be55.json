{
 "_meta": {
  "ui": {
   "resourceUri": "ui://sonar-connections/storefront-card.416685fd4034.html"
  }
 },
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "CALL THIS for what a shop sells, its products, its prices, a gift idea, or browsing or shopping a store served here. The SHOP CATALOG for a business served through this connector, read from that shop's OWN published catalog: product titles, the brand or maker, the product type, a price, whether it is in stock, and one image each. makecollectives is Make Collectives, a boutique at 430 E. 1st St in Long Beach. Pass `query` to narrow it to what somebody asked for (\"rings\", \"books\"); it only ever REMOVES products and never reorders them, because nothing in this connector is paid placement. The catalog is PAGED: sixty products come back per call and the reply carries `page`, `pageCount` and `total`, so a shop with 340 products is six pages and not sixty products. Pass `page` (1-based) to read the rest; never tell somebody sixty is all this shop has, because it is not. `query` searches the WHOLE catalog and not just the first page, so \"candles\" finds her candles wherever they sit in it. A `page` past the end answers with no products and the real `pageCount`, which is the number to ask again with. BUYING HAPPENS ON THE SHOP'S OWN SITE: each purchasable variant carries a `cartUrl` that is the shop's own cart link, and opening it lands on THEIR checkout. Give that url exactly as returned and never a link to any other host. No payment passes through Sonar Connections, this tool has nowhere to put a card, and no fee is added to the shopper. This connector does NOT hold this shop's stock levels, its opening hours or its order book: say what the catalog says and send people to the shop for anything else. A business that publishes no catalog we read says so honestly with its own website attached. The business may be given by NAME or by id: \"Long Beach Brunch Bakehouse\" and \"longbeachbrunch\" both reach the bakery, and a common short form (\"the bakehouse\", \"the dentist\") works too. A name we cannot place is refused with the list of businesses we do serve, never answered about a different one.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "business": {
    "description": "Which business, BY NAME OR BY ID. Optional, defaults to velvetandclay. Available: Velvet and Clay (velvetandclay), Brightpoint Dental (brightpointdental), Long Beach Brunch Bakehouse (longbeachbrunch), Make Collectives (makecollectives). A common short form works too (\"the bakehouse\", \"the dentist\"). A name that does not clearly match one of these is REFUSED with the list, never silently answered about a different business.",
    "type": "string"
   },
   "businessId": {
    "description": "Alias for `business`, accepted because every reply carries this name. Takes a NAME OR AN ID, exactly like `business`. Ignored when `business` is also given.",
    "type": "string"
   },
   "limit": {
    "description": "How many products to return, 1 to 60. Optional, defaults to 60. A larger number is clamped rather than refused.",
    "maximum": 9007199254740991,
    "minimum": -9007199254740991,
    "type": "integer"
   },
   "page": {
    "description": "Which page of the catalog, 1-based. Optional, defaults to 1. Each page holds up to 60 products; the reply carries pageCount and total so you can say which page you are reading and ask for the next one. A page past the end answers with no products and the true pageCount rather than an error.",
    "maximum": 9007199254740991,
    "minimum": -9007199254740991,
    "type": "integer"
   },
   "query": {
    "description": "Free text to narrow the catalog to, matched against the product title, the brand, the product type and the handle. Optional; omit it for the whole shop. It only ever removes products and never reorders them.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "biz_storefront",
 "title": "Shop catalog, prices and availability"
}
