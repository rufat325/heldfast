{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List all customers with pagination. Returns customer IDs, names, emails, billing addresses, and creation dates.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-chargebee-api-key",
    "_site": "mycompany",
    "limit": 50
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Chargebee API key",
    "type": "string"
   },
   "_site": {
    "description": "Chargebee site name",
    "type": "string"
   },
   "limit": {
    "description": "Number of results to return (default 10, max 100)",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset from a previous response",
    "type": "string"
   }
  },
  "required": [
   "_apiKey",
   "_site"
  ],
  "type": "object"
 },
 "name": "chargebee_list_customers",
 "outputSchema": {
  "properties": {
   "list": {
    "description": "Array of customer objects",
    "items": {
     "properties": {
      "customer": {
       "description": "Customer details",
       "type": "object"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "next_offset": {
    "description": "Pagination offset for next page of results",
    "type": "string"
   }
  },
  "required": [
   "list"
  ],
  "type": "object"
 },
 "title": "Chargebee List Customers",
 "version": "v1.a132ef46"
}
