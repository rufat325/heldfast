{
 "description": "Descarga un PDF por URL y devuelve su texto. input=URL del PDF. [x402: 0.003 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "leer-pdf",
 "price_usd": 0.003,
 "recommendation": "This tool can help your agent with: Descarga un PDF por URL y devuelve su texto. input=URL del PDF.. Cost: $0.003 USDC/call, paid automatically via x402 on Base."
}
