{
 "description": "Get fused signals for multiple assets at once. Pass comma-separated symbols or omit for all supported assets.",
 "inputSchema": {
  "properties": {
   "credit_token": {
    "description": "Optional credit token for bulk-prepaid access",
    "type": "string"
   },
   "symbols": {
    "description": "Comma-separated tickers, e.g. BTC,ETH,SOL. Omit for all.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_signal_batch"
}
