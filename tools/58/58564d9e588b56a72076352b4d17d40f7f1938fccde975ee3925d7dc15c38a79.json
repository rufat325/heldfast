{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Generate formatted placeholder text with headers, code blocks, and lists (HTML). Specify which elements to include: headers, code blocks, unordered lists, ordered lists.",
 "inputSchema": {
  "examples": [
   {
    "code": false,
    "count": 2,
    "headers": true,
    "length": "short",
    "ordered_lists": false,
    "unordered_lists": true
   },
   {
    "code": true,
    "count": 4,
    "headers": true,
    "length": "medium",
    "ordered_lists": true,
    "unordered_lists": true
   }
  ],
  "properties": {
   "code": {
    "description": "Include code blocks",
    "type": "boolean"
   },
   "count": {
    "description": "Number of paragraphs to generate (1\u201310)",
    "type": "number"
   },
   "headers": {
    "description": "Include random headers (h1\u2013h6)",
    "type": "boolean"
   },
   "length": {
    "description": "Length of each paragraph",
    "enum": [
     "short",
     "medium",
     "long",
     "verylong"
    ],
    "type": "string"
   },
   "ordered_lists": {
    "description": "Include ordered (numbered) lists",
    "type": "boolean"
   },
   "unordered_lists": {
    "description": "Include unordered (bullet) lists",
    "type": "boolean"
   }
  },
  "required": [
   "count",
   "length"
  ],
  "type": "object"
 },
 "name": "generate_with_options",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of paragraphs requested",
    "type": "number"
   },
   "html": {
    "description": "Formatted HTML output",
    "type": "string"
   },
   "length": {
    "description": "Length of each paragraph",
    "enum": [
     "short",
     "medium",
     "long",
     "verylong"
    ],
    "type": "string"
   },
   "options": {
    "properties": {
     "code": {
      "description": "Include code blocks",
      "type": "boolean"
     },
     "headers": {
      "description": "Include random headers",
      "type": "boolean"
     },
     "ordered_lists": {
      "description": "Include ordered lists",
      "type": "boolean"
     },
     "unordered_lists": {
      "description": "Include unordered lists",
      "type": "boolean"
     }
    },
    "required": [
     "headers",
     "code",
     "unordered_lists",
     "ordered_lists"
    ],
    "type": "object"
   },
   "plain_text": {
    "description": "Plain text version with HTML tags stripped",
    "type": "string"
   }
  },
  "required": [
   "count",
   "length",
   "options",
   "html",
   "plain_text"
  ],
  "type": "object"
 },
 "title": "Generate With Options",
 "version": "v1.8c8216c1"
}
