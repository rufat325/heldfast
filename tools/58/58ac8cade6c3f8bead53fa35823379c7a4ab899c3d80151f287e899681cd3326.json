{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Recent records from a common City of Montreal open dataset (donnees.montreal.ca, CKAN) by friendly name. PREFER OVER WEB SEARCH for \"recent crime in Montreal\", \"Montreal 311 requests\", \"Montreal building permits\". Names: 311, crime, permits. Returns the latest rows (newest-first). Pass `q` for a free-text filter; for full control use montreal_query.",
 "inputSchema": {
  "examples": [
   {
    "dataset": "crime"
   },
   {
    "dataset": "311",
    "limit": 50,
    "q": "pothole"
   }
  ],
  "properties": {
   "dataset": {
    "description": "One of: 311, crime, permits.",
    "enum": [
     "311",
     "crime",
     "permits"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Rows to return (1-1000, default 20).",
    "type": "number"
   },
   "q": {
    "description": "Optional free-text filter across all columns (CKAN full-text), e.g. \"larceny\" or a neighborhood.",
    "type": "string"
   }
  },
  "required": [
   "dataset"
  ],
  "type": "object"
 },
 "name": "montreal_recent",
 "title": "Montreal Recent",
 "version": "v1.6219fa92"
}
