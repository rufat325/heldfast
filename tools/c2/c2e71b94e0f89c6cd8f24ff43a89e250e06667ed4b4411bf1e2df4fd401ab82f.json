{
 "description": "Download AND auto-unzip the HTML report (HTML + screenshots + logs) for an upload. Returns the local extraction directory and an inventory of files, with failure screenshots highlighted at the top \u2014 those are typically the highest-signal artifact for debugging.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "outputDir": {
    "description": "Parent directory for the extracted report. Defaults to /tmp.",
    "type": "string"
   },
   "uploadId": {
    "type": "string"
   }
  },
  "required": [
   "uploadId"
  ],
  "type": "object"
 },
 "name": "get_html_report"
}
