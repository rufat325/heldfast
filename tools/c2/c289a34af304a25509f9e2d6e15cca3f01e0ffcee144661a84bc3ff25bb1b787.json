{
 "description": "Fetch everything Tidebase knows about one run: the run record, checkpointed steps (with resume contracts and failure classification), live state, versioned state streams, parent/child run edges, gates, recovery attempts, channel deliveries, usage records, and the append-only event log. Use to answer \"where did this run stop and is it safe to rerun?\"",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "runId": {
    "description": "Run id, e.g. run_abc123",
    "type": "string"
   }
  },
  "required": [
   "runId"
  ],
  "type": "object"
 },
 "name": "tidebase_get_run",
 "title": "Get full run detail"
}
