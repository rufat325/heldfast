{
 "description": "X/Twitter watchlist \u2014 2-50 handles in one call, their combined recent tweets newest first, deduped, per-handle counts and a since-cursor for only-what's-new next time. No from:-OR-splitting. Costs $0.05 per call, paid from clink's shop credits (get a key with buy_credits or at /buy/credits). A failed or empty call is free.",
 "inputSchema": {
  "properties": {
   "handles": {
    "description": "comma or space separated handles, e.g. 'elonmusk,naval,balajis'",
    "type": "string"
   },
   "limit": {
    "description": "1-100, default 40",
    "type": "string"
   },
   "since": {
    "description": "optional ISO timestamp or tweet id cursor",
    "type": "string"
   }
  },
  "required": [
   "handles"
  ],
  "type": "object"
 },
 "name": "x_list"
}
