{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Returns a TypeScript snippet for a requester to accept a provider quote and lock USDC in escrow (QUOTED \u2192 COMMITTED). Requires txId and quotedPrice (agreed USDC amount to lock). Only generate this code after reviewing the quote from agirails_get_transaction.",
 "inputSchema": {
  "properties": {
   "network": {
    "default": "testnet",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   },
   "quotedPrice": {
    "description": "The quoted price in USDC to accept (e.g. \"3.00\")",
    "type": "string"
   },
   "txId": {
    "description": "Transaction ID returned by agirails_request_service or agirails_list_transactions",
    "type": "string"
   }
  },
  "required": [
   "txId",
   "quotedPrice"
  ],
  "type": "object"
 },
 "name": "agirails_accept_quote"
}
