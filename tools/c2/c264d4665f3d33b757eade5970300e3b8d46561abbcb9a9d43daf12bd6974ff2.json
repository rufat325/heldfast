{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find places / points of interest (restaurants, shops, landmarks, gas stations, etc.) near a location via HERE \u2014 \"coffee near Times Square\", \"pharmacies near 48.85,2.35\", \"hardware stores in Austin\". Returns name, category, address, coordinates, distance, and contact info. The `near` location can be a place name (geocoded automatically) or \"lat,lng\".",
 "inputSchema": {
  "examples": [
   {
    "near": "Times Square, New York",
    "query": "coffee"
   },
   {
    "limit": 15,
    "near": "48.85,2.35",
    "query": "pharmacy"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max results (default 10, max 50).",
    "type": "number"
   },
   "near": {
    "description": "Place name (e.g. \"Times Square, New York\") or \"lat,lng\" (e.g. \"40.758,-73.985\") to search around.",
    "type": "string"
   },
   "query": {
    "description": "What to search for, e.g. \"coffee\", \"pharmacy\", \"hardware store\".",
    "type": "string"
   },
   "radius_km": {
    "description": "Only return places within this many km of `near` (default 100, max 1000). HERE ranks by relevance, so without this a query that matches a business NAME can return that business on another continent.",
    "type": "number"
   }
  },
  "required": [
   "query",
   "near"
  ],
  "type": "object"
 },
 "name": "here_discover",
 "title": "Here Discover",
 "version": "v1.b5d673fa"
}
