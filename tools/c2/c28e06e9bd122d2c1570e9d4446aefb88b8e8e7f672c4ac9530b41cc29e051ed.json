{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Find a component for a UI need"
 },
 "description": "Takes a UI need in plain language and returns ranked components, including an explicit answer when this catalog deliberately does not ship something and the OS's own control is the right choice. Useful before building a UI control by hand.",
 "inputSchema": {
  "properties": {
   "need": {
    "description": "e.g. \"a progress bar for an upload\"",
    "type": "string"
   }
  },
  "required": [
   "need"
  ],
  "type": "object"
 },
 "name": "find_component",
 "title": "Find a component for a UI need"
}
