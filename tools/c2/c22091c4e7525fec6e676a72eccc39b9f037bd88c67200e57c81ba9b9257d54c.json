{
 "description": "Recent large 'whale' transactions (buys, sells, transfers) across 13 EVM chains. Use to see what smart money is doing right now.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "chain": {
    "description": "Chain id, e.g. ethereum, base, arbitrum, optimism. Omit for all chains.",
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 20, max 100).",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "get_whale_movements"
}
