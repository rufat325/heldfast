{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Persist a partial patch to the canonical Delx Wellness profile. Requires explicit_user_intent=true after the user confirms they want to save. Rejects secret-like fields (oauth, token, api_key, password, cookie, refresh, session). Strava's GPS-redaction default is unaffected by profile changes.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "explicit_user_intent": {
    "description": "Set to true ONLY after the user has explicitly confirmed they want to save this. Otherwise the tool refuses to write.",
    "type": "boolean"
   },
   "patch": {
    "additionalProperties": {},
    "description": "Partial WellnessProfileDocument patch. Top-level keys: profile, goals, devices, training, nutrition, preferences, safety, notes.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "response_format": {
    "default": "markdown",
    "enum": [
     "markdown",
     "json"
    ],
    "type": "string"
   }
  },
  "required": [
   "patch"
  ],
  "type": "object"
 },
 "name": "strava_profile_update",
 "title": "Update Shared Wellness Profile"
}
