{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "R\u00e9cup\u00e8re une commune par son code INSEE. Retourne un objet `LookupResult` discrimin\u00e9 par `found`. `found: true` \u2192 champs commune \u00e0 plat (nom, codesPostaux, centre\u2026). `found: false` \u2192 `{ found: false, key, lookupStatus: 'not_found', message }` orientant vers `autocomplete_commune` pour disambiguer.\n\nAlias accept\u00e9s : `code_insee`/`codeInsee`/`insee` \u2192 `code`.",
 "inputSchema": {
  "properties": {
   "code": {
    "description": "Code INSEE 5 caract\u00e8res. Ex: \"75056\" Paris, \"59009\" Villeneuve-d'Ascq, \"2A004\" Ajaccio.",
    "type": "string"
   }
  },
  "required": [
   "code"
  ],
  "type": "object"
 },
 "name": "get_commune_by_code",
 "outputSchema": {
  "description": "LookupResult discrimin\u00e9 par `found`. true \u2192 payload m\u00e9tier \u00e0 plat (siren, nomComplet, etc.). false \u2192 `lookupStatus`/`key`/`message` actionnables. `lookupStatus` est toujours pr\u00e9sent runtime (valid\u00e9 par tests).",
  "properties": {
   "found": {
    "type": "boolean"
   },
   "key": {
    "description": "Cl\u00e9 recherch\u00e9e (SIREN, num_finess, code INSEE, \u2026).",
    "type": "string"
   },
   "lookupStatus": {
    "enum": [
     "found",
     "not_found",
     "ambiguous"
    ],
    "type": "string"
   },
   "message": {
    "description": "Explication actionnable quand `found=false` (cause probable + rem\u00e9diation).",
    "type": "string"
   }
  },
  "required": [
   "found",
   "lookupStatus"
  ],
  "type": "object"
 }
}
