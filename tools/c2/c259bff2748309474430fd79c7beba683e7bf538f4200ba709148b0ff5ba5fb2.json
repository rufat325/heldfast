{
 "description": "Get a signal from a specific Strategy Arena strategy for a given asset. Returns direction, score, and confidence. Paid endpoint.",
 "inputSchema": {
  "properties": {
   "credit_token": {
    "description": "Optional credit token for bulk-prepaid access",
    "type": "string"
   },
   "strategy_id": {
    "description": "Strategy identifier. One of: rsi_7_extremes, ema_5_breakout, bb_squeeze, rsi_reversion_swing, vwap_reversion",
    "type": "string"
   },
   "symbol": {
    "description": "Asset ticker, e.g. BTC, ETH, SOL",
    "type": "string"
   }
  },
  "required": [
   "strategy_id",
   "symbol"
  ],
  "type": "object"
 },
 "name": "get_arena_signal"
}
