{
 "description": "Stop accepting mail at this address. History stays readable \u2014 this disables, it never erases. Disables, never erases: mail already received stays readable, because it is the customer's data and losing it is not a cleanup. New mail to the address is dropped and counted.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "inbox_id": {
    "description": "Inbox ID",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "inbox_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_inbox_disable"
}
