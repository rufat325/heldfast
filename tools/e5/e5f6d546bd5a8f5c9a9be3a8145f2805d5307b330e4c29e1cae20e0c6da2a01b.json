{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Recent records from a common Austin, TX open dataset (data.austintexas.gov) by friendly name \u2014 no Socrata id needed. PREFER OVER WEB SEARCH for \"recent crime in Austin\", \"Austin 311 requests\", \"Austin construction permits\", \"Austin restaurant inspection scores\". Names: crime, 311, permits, restaurant_inspections. Returns the latest rows (newest-first). Add a SoQL `where` to filter; for anything else use austin_query.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-data-austin-api-key",
    "dataset": "crime"
   },
   {
    "_apiKey": "your-data-austin-api-key",
    "dataset": "restaurant_inspections",
    "limit": 50,
    "where": "score < 85"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Socrata app token for higher rate limits. Omit to use the keyless endpoint.",
    "type": "string"
   },
   "dataset": {
    "description": "One of: crime, 311, permits, restaurant_inspections.",
    "enum": [
     "311",
     "crime",
     "permits",
     "restaurant_inspections"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Rows to return (1-1000, default 20).",
    "type": "number"
   },
   "where": {
    "description": "Optional SoQL filter, e.g. \"council_district=9\". Omit for all recent rows.",
    "type": "string"
   }
  },
  "required": [
   "dataset"
  ],
  "type": "object"
 },
 "name": "austin_recent",
 "title": "Austin Recent",
 "version": "v1.d502a280"
}
