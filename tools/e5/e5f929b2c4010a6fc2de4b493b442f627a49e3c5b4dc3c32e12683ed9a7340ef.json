{
 "description": "Batch transcript fetch (max 10 videos, 3 at a time). Per-video failures don't kill the batch \u2014 failed entries carry an error field. Each transcript is truncated to fit; use get_transcript with offsetChars for full text of long ones.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "language": {
    "description": "Preferred caption language code (default 'en')",
    "type": "string"
   },
   "videos": {
    "description": "Video IDs or URLs",
    "items": {
     "type": "string"
    },
    "maxItems": 10,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "videos"
  ],
  "type": "object"
 },
 "name": "get_transcripts",
 "title": "Get transcripts for multiple videos"
}
