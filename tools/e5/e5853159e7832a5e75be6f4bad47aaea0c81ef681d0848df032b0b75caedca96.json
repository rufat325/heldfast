{
 "description": "Delete a persisted login session. The next open_app call for this (url, email) pair will return needsLogin: true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "email": {
    "description": "Account email of the session to forget.",
    "type": "string"
   },
   "url": {
    "description": "Origin URL of the session to forget.",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "url",
   "email"
  ],
  "type": "object"
 },
 "name": "forget_session"
}
