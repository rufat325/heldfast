{
 "description": "Check normalized iCalendar events for floating time, boundary, DATE, sequence, and recurrence hazards. Costs $0.05 USDC on Base mainnet.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "events": {
    "items": {
     "additionalProperties": true,
     "required": [
      "uid",
      "dtstart",
      "dtend"
     ],
     "type": "object"
    },
    "maxItems": 1000,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "events"
  ],
  "type": "object"
 },
 "name": "justhandled_calendar_invite_timezone_preflight"
}
