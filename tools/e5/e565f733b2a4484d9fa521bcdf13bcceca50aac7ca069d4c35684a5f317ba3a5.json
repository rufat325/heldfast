{
 "_meta": {
  "skill": "stacking"
 },
 "description": "Lock STX in PoX-5 with a signer manager to earn sBTC rewards.\n\nThe lock starts next reward cycle and lasts numCycles cycles (1-96). Pick a signer manager with list_stacking_signers. Refused during the prepare phase and if the address is already staking (use extend_stacking). Locking is not a transfer: the transaction carries a staking post-condition for exactly the locked amount.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "amount": {
    "description": "Amount to lock, in micro-STX",
    "type": "string"
   },
   "btcRewardAddress": {
    "description": "Optional Bitcoin address to receive rewards. Encoded as `{ pox-addr, max-fee }` signer calldata, the shape reference signer managers (e.g. Xverse, Fast Pool) use to pay rewards to BTC via an sBTC withdrawal. Some managers require it (PlanBetter), some ignore it (native-pool). Omit to receive sBTC where the manager supports that.",
    "type": "string"
   },
   "maxWithdrawalFeeSats": {
    "description": "Max sBTC withdrawal fee (sats) per payout when btcRewardAddress is set. Default 3000.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "numCycles": {
    "description": "Reward cycles to lock (1-96; one cycle is about two weeks)",
    "maximum": 96,
    "minimum": 1,
    "type": "integer"
   },
   "signerCalldataHex": {
    "description": "Advanced: raw signer calldata (hex, max 500 bytes) for managers with a custom format. Cannot be combined with btcRewardAddress.",
    "type": "string"
   },
   "signerManager": {
    "description": "Signer manager contract id, e.g. SP8HK160YD5GHXP69VGA0TC7AQJ1X4CDW3XVERSE.xverse-signer-manager-2",
    "type": "string"
   }
  },
  "required": [
   "signerManager",
   "amount",
   "numCycles"
  ],
  "type": "object"
 },
 "name": "stack_stx"
}
