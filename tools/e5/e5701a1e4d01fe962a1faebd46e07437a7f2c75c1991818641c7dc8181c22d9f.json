{
 "description": "Press a special key (return, escape, delete, tab, arrows, etc.) with optional modifiers (command, shift, option, control).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "deviceId": {
    "description": "Device UDID, name, or \"booted\" (default: booted)",
    "type": "string"
   },
   "key": {
    "description": "Key name: return, escape, delete, tab, space, up, down, left, right, home, end, pageup, pagedown, f1-f12",
    "type": "string"
   },
   "modifiers": {
    "description": "Modifier keys: command, shift, option, control",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "key"
  ],
  "type": "object"
 },
 "name": "simulator_press_key"
}
