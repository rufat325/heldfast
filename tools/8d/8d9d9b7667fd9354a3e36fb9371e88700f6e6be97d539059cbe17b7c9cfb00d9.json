{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Earnings Brief"
 },
 "description": "Get the AI 'Earnings Brief' for a company's recent earnings calls \u2014 a verifier-approved TL;DR, bullish and bearish points, and verbatim pull-quotes, plus a deterministic narrative shift against the immediately older available approved brief. When available, it also shows company guidance issued at the call, how that range changed from management's prior update, and the reported quarter versus the operative company guidance that preceded it. This is company guidance, not analyst consensus; actual comparisons use filed GAAP XBRL facts, never estimates. The shift is derived from approved bullets and is not separately verifier-approved. Newest quarter first. Only calls with an approved brief appear, so quarters can be missing from the sequence.",
 "inputSchema": {
  "properties": {
   "fiscalQuarter": {
    "default": null,
    "description": "Company fiscal quarter, 1-4. Quarter alone filters that quarter across fiscal years; both fields select an exact period.",
    "type": [
     "integer",
     "null"
    ]
   },
   "fiscalYear": {
    "default": null,
    "description": "Company fiscal year. Omit both period fields for newest briefs; year alone filters that fiscal year.",
    "type": [
     "integer",
     "null"
    ]
   },
   "limit": {
    "default": 2,
    "description": "Maximum number of quarterly briefs to return, newest first (default 2, max 8; values outside 1-8 are clamped)",
    "type": "integer"
   },
   "ticker": {
    "description": "Company ticker symbol (e.g., AAPL, MSFT)",
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "GetEarningsBrief",
 "title": "Earnings Brief"
}
