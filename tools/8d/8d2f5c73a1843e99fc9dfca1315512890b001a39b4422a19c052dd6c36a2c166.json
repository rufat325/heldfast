{
 "description": "Fetch an AnchorID (canonical entity) by UUID. Optionally include linked source records via the include parameter (links, source_records, or both).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "entity_id": {
    "description": "UUID of the AnchorID to retrieve",
    "type": "string"
   },
   "include": {
    "description": "Comma-separated relations to include: \"links\", \"source_records\", or both",
    "type": "string"
   }
  },
  "required": [
   "entity_id"
  ],
  "type": "object"
 },
 "name": "get_entity"
}
