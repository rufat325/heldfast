{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get collection"
 },
 "description": "Get a collection with its member artifacts and tags. The collection's `url` is its canonical shareable link; each artifact's `url` points at that file within the collection page - hand these over rather than ids. When the collection is under review, `review_requested` is true and `open_threads` / `resolved_threads` count its block-review threads, so you can tell whether the review is finished (open_threads = 0) without reading every block.",
 "icons": [
  {
   "mimeType": "image/svg+xml",
   "sizes": [
    "any"
   ],
   "src": "https://kythene.com/favicon.svg"
  }
 ],
 "inputSchema": {
  "properties": {
   "id": {
    "description": "id is the collection's id (from list/timeline, or a recall hit's ref).",
    "type": "string"
   },
   "space": {
    "description": "space optionally selects which of your spaces to act in (an id or name\n from list_spaces). Omit it when you belong to a single space.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_collection",
 "outputSchema": {
  "properties": {
   "collection": {
    "properties": {
     "artifacts": {
      "items": {
       "properties": {
        "author": {
         "description": "author names the human who created it (created_by, resolved).",
         "type": "string"
        },
        "createdAt": {
         "format": "date-time",
         "type": "string"
        },
        "createdBy": {
         "type": "string"
        },
        "id": {
         "type": "string"
        },
        "latestSize": {
         "format": "int64",
         "type": "string"
        },
        "latestVersion": {
         "type": "integer"
        },
        "mime": {
         "type": "string"
        },
        "name": {
         "type": "string"
        },
        "producer": {
         "type": "string"
        },
        "updatedAt": {
         "format": "date-time",
         "type": "string"
        },
        "url": {
         "description": "url is a canonical link an assistant can hand to a person - the containing\n collection's page anchored to this file (.../c/<collection>#a-<id>). Set only\n when the artifact is returned as part of a collection; empty on a standalone\n get_artifact, because an artifact has no page of its own independent of a\n collection (it can belong to several).",
         "type": "string"
        }
       },
       "type": "object"
      },
      "type": "array"
     },
     "author": {
      "description": "author names the human who published it (created_by, resolved).",
      "type": "string"
     },
     "createdAt": {
      "format": "date-time",
      "type": "string"
     },
     "createdBy": {
      "type": "string"
     },
     "id": {
      "type": "string"
     },
     "isExternal": {
      "description": "is_external is true when the origin space differs from the caller's\n viewing space (the collection was shared in from elsewhere).",
      "type": "boolean"
     },
     "openThreads": {
      "description": "open_threads and resolved_threads count the block-review threads on this\n collection: open = a status still owed (needs review / needs work / reject),\n resolved = done or approved. Populated by get_collection so an instance can\n tell whether a review is finished without reading the whole thing; 0 on\n responses from other calls.",
      "type": "integer"
     },
     "originSpace": {
      "description": "origin_space names the workspace it was published from, when you belong to\n that workspace; empty otherwise, and is_external still says it came from\n elsewhere.",
      "type": "string"
     },
     "originSpaceId": {
      "description": "origin_space_id is the space the collection was published from.",
      "type": "string"
     },
     "producer": {
      "type": "string"
     },
     "resolvedThreads": {
      "type": "integer"
     },
     "reviewRequested": {
      "description": "review_requested is true while the collection is under review (the\n approve/reject flow is on). Read it to tell whether you are looking at a\n collection that still expects a review decision.",
      "type": "boolean"
     },
     "revision": {
      "type": "integer"
     },
     "tags": {
      "items": {
       "properties": {
        "hasPassword": {
         "type": "boolean"
        },
        "id": {
         "type": "string"
        },
        "isInternal": {
         "type": "boolean"
        },
        "name": {
         "type": "string"
        }
       },
       "type": "object"
      },
      "type": "array"
     },
     "title": {
      "description": "title is what the collection is called.",
      "type": "string"
     },
     "url": {
      "description": "url is the canonical link to this collection (.../c/<id>) - the thing to hand\n to a person or paste into a message. It redirects through sign-in to the\n right space, so it works for anyone with access, from any device.",
      "type": "string"
     }
    },
    "type": "object"
   }
  },
  "type": "object"
 },
 "title": "Get collection"
}
