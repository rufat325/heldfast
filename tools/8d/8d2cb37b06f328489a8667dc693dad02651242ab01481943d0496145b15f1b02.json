{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Inspect variables visible in a stack frame. Returns: each scope (locals, members, globals) with its variables, values and types. Use the frame_id from debug_stack_trace. Only meaningful while execution is paused.",
 "inputSchema": {
  "properties": {
   "frame_id": {
    "description": "Frame id from debug_stack_trace",
    "type": "integer"
   }
  },
  "required": [
   "frame_id"
  ],
  "type": "object"
 },
 "name": "debug_inspect"
}
