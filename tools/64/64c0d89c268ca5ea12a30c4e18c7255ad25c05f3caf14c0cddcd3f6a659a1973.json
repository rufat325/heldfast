{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Top up a GPT55 prepaid balance account with x402 exact USDC before using balance-metered GPT-5.5 chat. Pay $0.005 per request with x402 exact USDC on Base or Solana when advertised. Canonical live prices: https://gpt55.558686.xyz/x402/live-prices.json.",
 "inputSchema": {
  "additionalProperties": true,
  "anyOf": [
   {
    "required": [
     "account_id"
    ]
   },
   {
    "required": [
     "accountId"
    ]
   }
  ],
  "properties": {
   "accountId": {
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   },
   "account_id": {
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   },
   "requestId": {
    "maxLength": 160,
    "type": "string"
   },
   "request_id": {
    "maxLength": 160,
    "type": "string"
   },
   "topupUsd": {
    "minimum": 0,
    "type": "number"
   },
   "topup_usd": {
    "minimum": 0,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "gpt55_balance_topup",
 "title": "GPT55 prepaid balance top-up"
}
