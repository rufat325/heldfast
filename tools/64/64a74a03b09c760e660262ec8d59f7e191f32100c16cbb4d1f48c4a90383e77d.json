{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Read a WHOLE site from one URL \u2014 its sitemap, or the links on that page \u2014 and give each page found its own DRAFT page here, built from this site's tokens. Not a clone. Dry run returns the page list before anything is created.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "depth": {
    "description": "No sitemap: link depth, default 1",
    "maximum": 3,
    "minimum": 0,
    "type": "integer"
   },
   "dry_run": {
    "type": "boolean"
   },
   "exclude": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "homepage": {
    "description": "Entry into this site's home page, default true",
    "type": "boolean"
   },
   "include": {
    "description": "Path substrings to keep",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "max_images": {
    "description": "Default 24, whole import",
    "maximum": 200,
    "minimum": 0,
    "type": "integer"
   },
   "max_nodes": {
    "description": "Per page. No cap by default",
    "minimum": 1,
    "type": "integer"
   },
   "max_pages": {
    "description": "Default 12",
    "maximum": 60,
    "minimum": 1,
    "type": "integer"
   },
   "nav": {
    "description": "Shared header linking the new pages, default true",
    "type": "boolean"
   },
   "nav_timeout_ms": {
    "description": "How long to wait for a page to answer at all. Default 30000; raise it for a slow origin",
    "maximum": 120000,
    "minimum": 5000,
    "type": "integer"
   },
   "site_id": {
    "type": "string"
   },
   "theme": {
    "description": "Patch this site's theme from the entry page's colours and type scale, default true \u2014 SITE-WIDE, affecting pages outside this import too",
    "type": "boolean"
   },
   "upload_images": {
    "type": "boolean"
   },
   "url": {
    "description": "Any page of the site",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "sb_import_site"
}
