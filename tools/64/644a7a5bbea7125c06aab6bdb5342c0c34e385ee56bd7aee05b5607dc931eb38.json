{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Cancels a claim (including an already accepted one). Get the terms with get_cancel_info first and pass its cancel_state: with paid the cancellation fee is charged. version comes from get_claim. 409 errors: old_version, inappropriate_status, free_cancel_is_unavailable.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cancel_state": {
    "description": "Cancellation mode from get_cancel_info: free \u2014 no charge, paid \u2014 a fee applies.",
    "enum": [
     "free",
     "paid"
    ],
    "type": "string"
   },
   "claim_id": {
    "description": "Claim id (claim_id, 32\u201364 characters) from the create_claim or search_claims response.",
    "minLength": 1,
    "type": "string"
   },
   "version": {
    "description": "Claim version from the latest get_claim response.",
    "type": "integer"
   }
  },
  "required": [
   "claim_id",
   "version",
   "cancel_state"
  ],
  "type": "object"
 },
 "name": "cancel_claim",
 "title": "Cancel a claim"
}
