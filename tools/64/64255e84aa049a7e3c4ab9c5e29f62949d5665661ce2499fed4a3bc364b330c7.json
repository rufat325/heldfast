{
 "annotations": {
  "readOnlyHint": true,
  "title": "list_animals"
 },
 "description": "List current animals on the farm, optionally paginated. Select an inventory status or ALL to discover historical profiles.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "inventory_status": {
    "description": "Defaults to CURRENT; ALL includes every nondeleted profile.",
    "enum": [
     "CURRENT",
     "UNKNOWN",
     "SOLD",
     "DECEASED",
     "ALL"
    ],
    "type": "string"
   },
   "skip": {
    "description": "Records to skip (pagination).",
    "minimum": 0,
    "type": "integer"
   },
   "take": {
    "description": "Max records to return.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_animals"
}
