{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "kms_create_key"
 },
 "description": "Create an org-scoped managed key. Material is generated locally and wrapped to each grantee (self plus any listed users/tokens); the server never sees it. Use the CLI for app/group-scoped keys.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "grantTokens": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "grantUsers": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "name": {
    "type": "string"
   },
   "org": {
    "type": "string"
   },
   "purpose": {
    "enum": [
     "encrypt",
     "sign"
    ],
    "type": "string"
   }
  },
  "required": [
   "name",
   "purpose"
  ],
  "type": "object"
 },
 "name": "kms_create_key"
}
