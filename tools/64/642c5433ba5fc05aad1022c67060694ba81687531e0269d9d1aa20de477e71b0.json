{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Report codebase changes since the last sync so VibeMap can flag spec drift. Workflow: 1) call vibemap_get_code_map and read anchor.commitSha; 2) run `git diff --name-only <commitSha>..HEAD` (plus untracked files from `git status --porcelain`); 3) call this tool with the changed paths and your current HEAD sha. The response lists affected map units and features \u2014 update the stale specs with vibemap_update_feature / vibemap_update_user_story / vibemap_update_acceptance_criterion (all changeset-audited), then re-submit the code map with vibemap_submit_code_map to clear the drift.",
 "inputSchema": {
  "properties": {
   "changedFiles": {
    "description": "Repo-relative paths changed since anchor.commitSha (max 2000)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "headSha": {
    "description": "Current HEAD commit sha (git rev-parse HEAD)",
    "type": "string"
   },
   "projectId": {
    "description": "VibeMap project id",
    "type": "string"
   }
  },
  "required": [
   "projectId",
   "changedFiles"
  ],
  "type": "object"
 },
 "name": "vibemap_sync_changes"
}
