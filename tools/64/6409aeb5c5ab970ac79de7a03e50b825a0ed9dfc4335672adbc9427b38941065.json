{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Set Up Automate Tests"
 },
 "description": "Set up and run automated web-based tests on BrowserStack using the BrowserStack SDK. Use this tool for functional or integration test setup on BrowserStack only. For any visual testing or Percy integration, use the dedicated Percy setup tool. Example prompts: run this test on browserstack; set up this project for browserstack.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "detectedBrowserAutomationFramework": {
    "enum": [
     "playwright",
     "selenium",
     "cypress",
     "webdriverio"
    ],
    "type": "string"
   },
   "detectedLanguage": {
    "enum": [
     "nodejs",
     "python",
     "java",
     "csharp",
     "ruby"
    ],
    "type": "string"
   },
   "detectedTestingFramework": {
    "enum": [
     "jest",
     "codeceptjs",
     "playwright",
     "pytest",
     "robot",
     "behave",
     "cucumber",
     "nightwatch",
     "mocha",
     "junit4",
     "junit5",
     "testng",
     "cypress",
     "nunit",
     "mstest",
     "xunit",
     "specflow",
     "reqnroll",
     "rspec",
     "serenity"
    ],
    "type": "string"
   },
   "devices": {
    "default": [],
    "description": "Device objects array. Use the object format directly - no transformation needed. Add only when user explicitly requests devices. Examples: [{ platform: 'windows', osVersion: '11', browser: 'chrome', browserVersion: 'latest' }] or [{ platform: 'android', deviceName: 'Samsung Galaxy S24', osVersion: '14', browser: 'chrome' }].",
    "items": {
     "properties": {
      "browser": {
       "description": "Browser name, e.g. 'chrome', 'safari', 'edge', 'firefox'",
       "type": "string"
      },
      "browserVersion": {
       "description": "Browser version for desktop platforms only (windows, macos), e.g. '132', 'latest', 'oldest'. Not used for mobile devices (android, ios).",
       "type": "string"
      },
      "deviceName": {
       "description": "Device name for mobile platforms, e.g. 'iPhone 15', 'Samsung Galaxy S24'",
       "type": "string"
      },
      "osVersion": {
       "description": "OS version, e.g. '11', 'Sequoia', '14', '17', 'latest'",
       "type": "string"
      },
      "platform": {
       "description": "Platform name, e.g. 'windows', 'macos', 'android', 'ios'",
       "enum": [
        "windows",
        "macos",
        "android",
        "ios"
       ],
       "type": "string"
      }
     },
     "required": [
      "platform",
      "osVersion"
     ],
     "type": "object"
    },
    "maxItems": 3,
    "type": "array"
   },
   "projectName": {
    "description": "A single name for your project to organize all your tests.",
    "type": "string"
   }
  },
  "required": [
   "projectName",
   "detectedLanguage",
   "detectedBrowserAutomationFramework",
   "detectedTestingFramework"
  ],
  "type": "object"
 },
 "name": "setupBrowserStackAutomateTests"
}
