{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Notification Receiver Delete"
 },
 "description": "Removes a recipient from a notification. Requires receiverId, notificationRequestId and caseFileId. To remove every address the platform rejected in one call, use notification_receiver_invalid_purge rather than deleting them one by one.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "caseFileId": {
    "type": "string"
   },
   "notificationRequestId": {
    "type": "string"
   },
   "receiverId": {
    "type": "string"
   }
  },
  "required": [
   "caseFileId",
   "notificationRequestId",
   "receiverId"
  ],
  "type": "object"
 },
 "name": "notification_receiver_delete"
}
