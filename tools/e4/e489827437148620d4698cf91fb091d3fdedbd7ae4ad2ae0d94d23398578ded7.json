{
 "description": "The preferred pre-invocation trust check for an unknown external agent. Read-only, requires no AgentTrust API key or account. Looks up an agent by its exact invocation URL among AgentTrust's already-observed public agents and returns its status, endpoint-ownership verification, agent reliability score (when available), and a machine-readable trustDecision (recommended, confidence, reasons). Does NOT contact endpointUrl itself during this check \u2014 it only reads AgentTrust's own existing monitoring history. An unregistered URL returns { matched: false }, never an error.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "endpointUrl": {
    "description": "The exact invocation URL of the agent you're about to call. Used only to look up AgentTrust's own already-observed data for it \u2014 this endpoint is never contacted during the check.",
    "maxLength": 2048,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "endpointUrl"
  ],
  "type": "object"
 },
 "name": "check_agent_trust",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "matched": {
    "type": "boolean"
   },
   "name": {
    "type": "string"
   },
   "reliabilityScore": {
    "type": [
     "number",
     "null"
    ]
   },
   "reliabilityScoreStatus": {
    "enum": [
     "none",
     "fresh",
     "stale"
    ],
    "type": "string"
   },
   "slug": {
    "type": "string"
   },
   "status": {
    "enum": [
     "unknown",
     "healthy",
     "degraded",
     "down"
    ],
    "type": "string"
   },
   "trustDecision": {
    "additionalProperties": false,
    "properties": {
     "confidence": {
      "enum": [
       "high",
       "medium",
       "low",
       "insufficient_data"
      ],
      "type": "string"
     },
     "reasons": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "recommended": {
      "type": "boolean"
     }
    },
    "required": [
     "recommended",
     "confidence",
     "reasons"
    ],
    "type": "object"
   },
   "verified": {
    "type": "boolean"
   }
  },
  "required": [
   "matched"
  ],
  "type": "object"
 },
 "title": "Check Agent Trust"
}
