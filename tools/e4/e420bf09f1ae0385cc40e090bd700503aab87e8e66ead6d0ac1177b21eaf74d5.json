{
 "description": "List App Store customer reviews with ratings, text, and your responses. Useful for monitoring user feedback.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "appId": {
    "description": "App ID",
    "type": "string"
   },
   "limit": {
    "default": 20,
    "description": "Number of reviews to fetch",
    "maximum": 200,
    "minimum": 1,
    "type": "number"
   },
   "sort": {
    "default": "-createdDate",
    "description": "Sort order",
    "enum": [
     "-createdDate",
     "createdDate",
     "-rating",
     "rating"
    ],
    "type": "string"
   }
  },
  "required": [
   "appId"
  ],
  "type": "object"
 },
 "name": "asc_list_customer_reviews"
}
