{
 "description": "Send a message through the full GSEP pipeline. Runs C3 prompt injection scan, enhanced LLM call with evolved genes, C4 behavioral immune check, C5 action firewall, fitness tracking, and autonomous evolution. Returns the protected response.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "api_key": {
    "description": "Your LLM API key (ANTHROPIC_API_KEY or OPENAI_API_KEY). Required if not set as server env var.",
    "type": "string"
   },
   "genome_id": {
    "description": "Unique identifier for this agent genome (e.g. \"my-assistant\")",
    "type": "string"
   },
   "llm_provider": {
    "description": "LLM provider to use. Auto-detected from api_key if omitted.",
    "enum": [
     "anthropic",
     "openai",
     "ollama"
    ],
    "type": "string"
   },
   "message": {
    "description": "User message to send through the full GSEP pipeline",
    "type": "string"
   },
   "task_type": {
    "description": "Task type hint (e.g. \"support\", \"coding\", \"general\")",
    "type": "string"
   },
   "user_id": {
    "description": "User identifier for personalization and per-user epigenomes",
    "type": "string"
   }
  },
  "required": [
   "genome_id",
   "message"
  ],
  "type": "object"
 },
 "name": "gsep_chat"
}
