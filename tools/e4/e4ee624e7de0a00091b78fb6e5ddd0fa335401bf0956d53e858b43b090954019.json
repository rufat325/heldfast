{
 "description": "Restrict a hosted drop so only viewers with a given email domain can open it (e.g. 'acme.com'); pass 'off' to remove.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "domain": {
    "description": "Allowed email domain, or 'off'.",
    "type": "string"
   },
   "slug": {
    "type": "string"
   }
  },
  "required": [
   "slug",
   "domain"
  ],
  "type": "object"
 },
 "name": "set_email_gate"
}
