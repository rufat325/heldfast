{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Register a new domain on the account. Check availability first with `impreza_domain_check`. This raises an invoice \u2014 settle it with `impreza_pay_invoice` (top up first via `impreza_topup` if the balance is short). Nameservers default to Impreza DNS, so records can be managed right away with `impreza_add_dns_record`; only pass `nameservers` to delegate the domain elsewhere.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "domain": {
    "description": "The domain to register, e.g. example.com.",
    "type": "string"
   },
   "nameservers": {
    "description": "Optional. Custom nameservers. Omit to keep Impreza DNS \u2014 passing an empty list would leave the domain unresolvable.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "years": {
    "description": "Registration period in years (1 or more).",
    "type": "number"
   }
  },
  "required": [
   "domain",
   "years"
  ],
  "type": "object"
 },
 "name": "impreza_register_domain"
}
