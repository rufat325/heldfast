{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the AbuseIPDB blacklist of the most-reported IP addresses. Returns IPs with their abuse confidence scores. Useful for building blocklists.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-abuseipdb-api-key",
    "limit": 100
   },
   {
    "_apiKey": "your-abuseipdb-api-key",
    "limit": 1000
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "AbuseIPDB API key",
    "type": "string"
   },
   "limit": {
    "description": "Number of IPs to return (default 100, max 10000)",
    "type": "number"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "get_blacklist",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of IPs returned in the blacklist",
    "type": "number"
   },
   "ips": {
    "description": "List of blacklisted IPs with abuse scores",
    "items": {
     "properties": {
      "abuse_confidence_score": {
       "description": "Abuse confidence score for this IP",
       "type": "number"
      },
      "ip": {
       "description": "IP address on the blacklist",
       "type": "string"
      }
     },
     "required": [
      "ip",
      "abuse_confidence_score"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "count",
   "ips"
  ],
  "type": "object"
 },
 "title": "Get Blacklist",
 "version": "v1.a830a1c3"
}
