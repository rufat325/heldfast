{
 "description": "Admin-only. Operator-verifies an agent (DNS, KYC, or platform check). +100 bonus to trust score.",
 "inputSchema": {
  "properties": {
   "agent_id": {
    "type": "string"
   },
   "verification_type": {
    "type": "string"
   }
  },
  "required": [
   "agent_id",
   "verification_type"
  ],
  "type": "object"
 },
 "name": "verify_agent"
}
