{
 "description": "Upload an Excel (.xlsx) file to the WaterfallBridge app. The file must contain a Data worksheet with valid Data Type headers.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "auto_run": {
    "description": "If true (default), automatically click 'Build Bridge' or 'Run Analysis' after import if the app prompts.",
    "type": "boolean"
   },
   "file_path": {
    "description": "Absolute path to a Bridge-formatted .xlsx file on the local filesystem.",
    "type": "string"
   }
  },
  "required": [
   "file_path"
  ],
  "type": "object"
 },
 "name": "upload_file"
}
