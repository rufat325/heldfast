{
 "description": "Register a non-canonical artifact (snapshot, journal entry, finding, etc.) on a feature. Canonical artifacts (design.md, plan.md, \u2026) inside the feature folder are auto-discovered and rejected here. Stores in feature.json artifacts[]; dedups on (type, path); appends an audit event (best-effort).",
 "inputSchema": {
  "properties": {
   "artifact_type": {
    "description": "e.g. \"journal\", \"snapshot\", \"finding\", \"report-supplement\", \"link\", \"external\"",
    "type": "string"
   },
   "feature_code": {
    "type": "string"
   },
   "force": {
    "description": "Overwrite an existing entry with the same (type, path)",
    "type": "boolean"
   },
   "idempotency_key": {
    "type": "string"
   },
   "path": {
    "description": "Repo-relative path. Must exist; cannot contain \"..\" after normalization.",
    "type": "string"
   },
   "status": {
    "enum": [
     "current",
     "superseded",
     "historical"
    ],
    "type": "string"
   }
  },
  "required": [
   "feature_code",
   "artifact_type",
   "path"
  ],
  "type": "object"
 },
 "name": "link_artifact"
}
