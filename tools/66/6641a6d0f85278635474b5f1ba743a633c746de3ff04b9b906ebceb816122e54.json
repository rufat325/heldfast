{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Other railway stations within a radius of one station, nearest first. Useful when a place has no service of its own and the traveller needs to know what is reachable \u2014 the planner already does this internally, so reach for it only when somebody asks the question directly.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "default": 10,
    "maximum": 40,
    "minimum": 1,
    "type": "integer"
   },
   "radius_km": {
    "default": 40,
    "maximum": 150,
    "minimum": 1,
    "type": "integer"
   },
   "station_code": {
    "maxLength": 12,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "station_code"
  ],
  "type": "object"
 },
 "name": "nearby_stations",
 "title": "Stations near a station"
}
