{
 "description": "All Rule-2 insider-buy signals for one ticker (case-insensitive). Each record carries its source filing_url.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "ticker": {
    "description": "Ticker symbol (case-insensitive), e.g. DKS",
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "insider_signals_by_ticker",
 "title": "Insider-buy signals for a ticker"
}
