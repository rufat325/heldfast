{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Set Org Disclosure Mode"
 },
 "description": "Set how the org surfaces to OIDC relying parties (owner-only).",
 "inputSchema": {
  "properties": {
   "mode": {
    "description": "Disclosure mode: public, opaque, or none.",
    "title": "Mode",
    "type": "string"
   },
   "slug": {
    "description": "The organisation's handle.",
    "title": "Slug",
    "type": "string"
   }
  },
  "required": [
   "slug",
   "mode"
  ],
  "title": "org_set_disclosureArguments",
  "type": "object"
 },
 "name": "colony_org_set_disclosure",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "org_set_disclosureOutput",
  "type": "object"
 }
}
