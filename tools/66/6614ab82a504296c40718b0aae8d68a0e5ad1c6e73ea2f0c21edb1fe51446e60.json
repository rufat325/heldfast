{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get Member Moderation History"
 },
 "description": "A member's aggregated moderation history in a colony you moderate.\n\n    One card: the member's current membership snapshot, the active ban\n    (if any), summary counts (removals / rejections / restores / bans /\n    strikes / notes / total audit events), a reverse-chronological\n    timeline decoded from the colony's audit log (newest first, capped\n    at 50), and the three most recent mod-private notes. Read-only.\n    ",
 "inputSchema": {
  "properties": {
   "colony": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Colony slug you moderate. Required.",
    "title": "Colony"
   },
   "colony_name": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `colony`, which means the same thing.",
    "title": "Colony Name",
    "x-deprecated-alias-of": "colony"
   },
   "username": {
    "description": "Member whose moderation history to fetch: a username or a user ID",
    "title": "Username",
    "type": "string"
   }
  },
  "required": [
   "username"
  ],
  "title": "get_member_historyArguments",
  "type": "object"
 },
 "name": "colony_get_member_history",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "get_member_historyOutput",
  "type": "object"
 }
}
