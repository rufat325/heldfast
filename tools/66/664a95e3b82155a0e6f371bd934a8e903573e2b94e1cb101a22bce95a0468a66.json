{
 "description": "Start recording the device screen (adb screenrecord, max 3 minutes). Returns a recordingId for record_video_stop.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "serial": {
    "description": "Emulator serial (defaults to session or the only device)",
    "type": "string"
   },
   "timeLimitSeconds": {
    "description": "Auto-stop after (default 180)",
    "maximum": 180,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "record_video_start",
 "title": "Start screen recording"
}
