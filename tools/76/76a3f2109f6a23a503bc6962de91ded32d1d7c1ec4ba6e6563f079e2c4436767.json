{
 "description": "Check which of the given GA4 dimensions/metrics can be combined in ONE report on this property (the Data API rejects incompatible pairs). Call this BEFORE ga4_run_report when combining unusual dimensions/metrics; drop or replace anything reported incompatible instead of retrying blind. Read-only.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dimensions": {
    "description": "Dimension API names to test.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "metrics": {
    "description": "Metric API names to test.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "property": {
    "description": "GA4 property ID, e.g. \"123456789\" or \"properties/123456789\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "property"
  ],
  "type": "object"
 },
 "name": "ga4_check_compatibility"
}
