{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Prediction for a match (model win/draw/away probabilities, when available on your plan). Requires a match ID. Example: highlightly_predictions({ sport: \"football\", matchId: 123456, _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-highlightly-api-key",
    "matchId": 123456,
    "sport": "football"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Highlightly API key (free 100/day at highlightly.net)",
    "type": "string"
   },
   "matchId": {
    "description": "Numeric match ID to fetch the prediction for (required)",
    "type": [
     "string",
     "number"
    ]
   },
   "sport": {
    "description": "Sport slug. Default \"football\" (soccer). \"soccer\" is normalized to \"football\".",
    "type": "string"
   }
  },
  "required": [
   "matchId",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "highlightly_predictions",
 "title": "Highlightly Predictions",
 "version": "v1.395fa623"
}
