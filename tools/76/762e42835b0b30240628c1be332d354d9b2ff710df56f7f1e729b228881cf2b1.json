{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Replaces a node\u2019s strokes with one solid colour, and optionally sets the stroke weight. Pass remove: true to take the stroke off entirely. Borders in Figma are strokes, not fills \u2014 a row outline or an icon drawn as an outline needs this rather than figma_set_fill.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "color": {
    "description": "Hex colour, like \"#1e88e5\". Omit only with remove.",
    "type": "string"
   },
   "nodeId": {
    "description": "The node to stroke.",
    "type": "string"
   },
   "opacity": {
    "description": "Stroke opacity, 0 to 1. Default 1.",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "remove": {
    "description": "Remove every stroke instead of setting one.",
    "type": "boolean"
   },
   "weight": {
    "description": "Stroke weight in pixels. Left alone when omitted.",
    "minimum": 0,
    "type": "number"
   }
  },
  "required": [
   "nodeId"
  ],
  "type": "object"
 },
 "name": "figma_set_stroke",
 "title": "Set or clear a stroke"
}
