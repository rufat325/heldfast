{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-intune-conditional-access-settings"
 },
 "description": "The Exchange on premises conditional access settings. On premises conditional access will require devices to be both enrolled and compliant for mail access\n\nTIP: Gets Intune on-premises conditional access settings. Returns enabled status and which devices are included/excluded from conditional access to on-premises Exchange.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get-intune-conditional-access-settings"
}
