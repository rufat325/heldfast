{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "DeFi protocol fundamentals in one call: current TVL, 1-day and 7-day change, category, chains and website for any protocol \u2014 search by name, or screen the whole universe by category and chain ranked by TVL. Open DefiLlama data, refreshed server-side every 15 minutes. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "category": {
    "description": "Filter by category, e.g. lending, dexes, bridge",
    "type": "string"
   },
   "chain": {
    "description": "Filter to protocols deployed on this chain, e.g. base",
    "type": "string"
   },
   "limit": {
    "description": "Max results (1-50, default 10)",
    "type": "integer"
   },
   "protocol": {
    "description": "Protocol name or slug fragment, e.g. aave",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "agentbit.protocol_tvl",
 "title": "Protocol TVL & Fundamentals"
}
