{
 "description": "Get AI visibility insights - actionable recommendations based on your AI search data.",
 "inputSchema": {
  "properties": {
   "page": {
    "description": "Page number (default: 1)",
    "type": "number"
   },
   "page_size": {
    "description": "Items per page, max 100 (default: 20)",
    "type": "number"
   },
   "status": {
    "description": "Filter by status (idle, scraping, analyzing, ready, applied, failed)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_insights"
}
