{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Call this when the user asks about Bitcoin hashrate, difficulty or block fees (our node runs blocksonly, so there is no mempool series). Returns the recorded daily series and latest values measured on ByKaranteli's own node.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "days": {
    "description": "Window in days, 1-730 (default 365).",
    "maximum": 730,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "get_network_health",
 "title": "Bitcoin network health from our own node"
}
