{
 "annotations": {
  "readOnlyHint": false,
  "title": "Download the layer files"
 },
 "description": "Prepare a private, short-lived download link for your own layers project: a ZIP holding every original transparent PNG plus a manifest of names, order, boxes and visibility, ready for Photoshop or Figma. Each call rebuilds the ZIP (one stored copy per project, overwritten).",
 "inputSchema": {
  "properties": {
   "layer_set_id": {
    "description": "The layers project id.",
    "type": "string"
   }
  },
  "required": [
   "layer_set_id"
  ],
  "type": "object"
 },
 "name": "export_layer_set",
 "title": "Download the layer files"
}
