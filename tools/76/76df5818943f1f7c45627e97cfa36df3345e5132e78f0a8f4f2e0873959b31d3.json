{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get Guest Details"
 },
 "description": "Get the full record for one guest on a live event \u2014 identity, tier, plus-ones, check-in status + time, staff role, tags, note, activity log, and plus-ones. Requires event_id + guest_id; you must be a host. Read-only.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "guest_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "guest_id"
  ],
  "type": "object"
 },
 "name": "get_guest_details",
 "title": "Get Guest Details"
}
