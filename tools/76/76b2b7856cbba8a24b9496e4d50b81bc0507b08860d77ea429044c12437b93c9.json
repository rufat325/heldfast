{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the cheapest cached flight tickets for a route (one per destination, cheapest first). Uses IATA city/airport codes. Example: travelpayouts_cheap_prices({ origin: \"MOW\", destination: \"BCN\", currency: \"usd\", _apiKey: \"token:marker\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "token:your-travelpayouts-api-key",
    "currency": "usd",
    "destination": "BCN",
    "origin": "MOW"
   },
   {
    "_apiKey": "token:your-travelpayouts-api-key",
    "currency": "usd",
    "depart_date": "2026-08-15",
    "origin": "JFK",
    "return_date": "2026-08-22"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Travelpayouts key as \"token:marker\" (marker optional)",
    "type": "string"
   },
   "currency": {
    "description": "Price currency code, e.g. \"usd\", \"eur\", \"rub\" (default rub)",
    "type": "string"
   },
   "depart_date": {
    "description": "Departure date, format YYYY-MM or YYYY-MM-DD (optional)",
    "type": "string"
   },
   "destination": {
    "description": "Arrival IATA city/airport code, e.g. \"BCN\", \"HKT\". Optional \u2014 omit for cheapest from origin to anywhere.",
    "type": "string"
   },
   "origin": {
    "description": "Departure IATA city/airport code, e.g. \"MOW\", \"LON\", \"JFK\"",
    "type": "string"
   },
   "return_date": {
    "description": "Return date, format YYYY-MM or YYYY-MM-DD (optional, for round trips)",
    "type": "string"
   }
  },
  "required": [
   "origin",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "travelpayouts_cheap_prices",
 "title": "Travelpayouts Cheap Prices",
 "version": "v1.d4ce1369"
}
