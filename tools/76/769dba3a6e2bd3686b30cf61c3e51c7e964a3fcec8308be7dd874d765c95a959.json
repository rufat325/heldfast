{
 "description": "Reorder all slides in a presentation. Pass every slide ID in the desired order.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "orderedIds": {
    "description": "All slide IDs in the desired order",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "presentationId": {
    "description": "The presentation ID",
    "type": "string"
   }
  },
  "required": [
   "presentationId",
   "orderedIds"
  ],
  "type": "object"
 },
 "name": "reorder_slides"
}
