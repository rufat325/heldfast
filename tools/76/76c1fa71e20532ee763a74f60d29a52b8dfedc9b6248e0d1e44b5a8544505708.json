{
 "annotations": {
  "cost": {
   "all": 15
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run custom GAQL queries against Google Ads data. Use for advanced analysis\u2014filter by keywords, matching types, or aggregate metrics by custom dimensions.",
 "inputSchema": {
  "examples": [
   {
    "customer_id": "1234567890",
    "query": "SELECT campaign.name, metrics.clicks FROM campaign WHERE segments.date DURING LAST_7_DAYS"
   },
   {
    "customer_id": "1234567890",
    "query": "SELECT ad_group.name, metrics.impressions, metrics.cost_micros FROM ad_group WHERE campaign.id = 9876543210"
   }
  ],
  "properties": {
   "customer_id": {
    "description": "Google Ads customer ID",
    "type": "string"
   },
   "query": {
    "description": "GAQL query string (e.g., \"SELECT campaign.name, metrics.clicks FROM campaign WHERE segments.date DURING LAST_7_DAYS\")",
    "type": "string"
   }
  },
  "required": [
   "customer_id",
   "query"
  ],
  "type": "object"
 },
 "name": "gads_search",
 "outputSchema": {
  "properties": {
   "results": {
    "description": "GAQL query results",
    "items": {
     "description": "Result row matching the GAQL query selection",
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "title": "Gads Search",
 "version": "v1.e7a14e79"
}
