{
 "description": "Resolve a free-text address into ranked candidates (formatted, placeId, location, components). Use before creating a project for a real listing \u2014 pass the chosen candidate as the project `address` so listing facts (price/beds/baths) auto-populate. Prefer candidates with isStreetLevel=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "maximum": 10,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "minLength": 3,
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "resolve_address",
 "title": "Geocode a free-text address"
}
