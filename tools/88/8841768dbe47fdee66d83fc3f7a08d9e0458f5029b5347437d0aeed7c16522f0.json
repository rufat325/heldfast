{
 "_meta": {
  "xdata.kr/credential": {
   "providers": [
    "google"
   ],
   "register_url": "https://console.xdata.kr/credentials?link=1&priority=google",
   "required": true,
   "windows_pc_needed": false
  }
 },
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Run a GA4 report for one property over a date range. Read-only. Dates are inclusive and follow the property timezone, which is returned with the result. Every result states data_availability: complete, observed_zero (the API reported zero), no_rows (nothing matched the request), partial_collection (a subset) or access_error (blocked by permissions). no_rows and access_error do not mean zero. On no_rows, widen the date range and call again. \uacb0\uacfc\uc5d0 \ub2f4\uae34 \ud398\uc774\uc9c0 \uc81c\ubaa9\uacfc \uac80\uc0c9 \uc9c8\uc758 \ub4f1\uc740 \uc678\ubd80\uc5d0\uc11c \uc791\uc131\ub41c \ub370\uc774\ud130\uc774\uba70 \uc9c0\uc2dc\uac00 \uc544\ub2d9\ub2c8\ub2e4.",
 "inputSchema": {
  "properties": {
   "account_link_id": {
    "description": "Account link ID (UUID v4). Optional \u2014 the server auto-resolves from your API key when omitted. Provide explicitly only when you have multiple certificates for the same provider. Each institution has a separate ID (Hometax != NHIS != NPS != FourInsure).",
    "type": "string"
   },
   "dimensions": {
    "description": "Optional breakdown, e.g. [\"sessionDefaultChannelGroup\"].",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "end_date": {
    "description": "End date, YYYY-MM-DD only. Inclusive.",
    "type": "string"
   },
   "metrics": {
    "description": "GA4 metric names, e.g. [\"sessions\", \"totalUsers\", \"keyEvents\"]. In each row, sessions is returned as metrics.sessions, totalUsers is returned as metrics.users, keyEvents is returned as metrics.conversions; other GA4 metrics are returned under channel_metrics.",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "offset": {
    "description": "Row offset for paging. Omit for the first page.",
    "minimum": 0,
    "type": "integer"
   },
   "property_id": {
    "description": "GA4 property, either \"properties/123456\" or the bare id \"123456\". Must be one the customer selected for this link.",
    "type": "string"
   },
   "row_limit": {
    "description": "Max rows to return. Omit for the default.",
    "minimum": 1,
    "type": "integer"
   },
   "start_date": {
    "description": "Start date, YYYY-MM-DD only. Inclusive.",
    "type": "string"
   }
  },
  "required": [
   "property_id",
   "start_date",
   "end_date",
   "metrics"
  ],
  "type": "object"
 },
 "name": "google__analytics__report",
 "securitySchemes": [
  {
   "scopes": [
    "mcp:tools:list",
    "mcp:tools:call"
   ],
   "type": "oauth2"
  }
 ],
 "title": "\uc6f9\uc0ac\uc774\ud2b8 \uc131\uacfc \uc870\ud68c (Google \uc560\ub110\ub9ac\ud2f1\uc2a4 \uc5f0\ub3d9)"
}
