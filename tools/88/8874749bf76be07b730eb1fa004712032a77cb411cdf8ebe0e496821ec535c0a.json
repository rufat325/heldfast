{
 "_meta": {
  "skill": "bounty-scanner"
 },
 "description": "List bounties on aibtc.com/api/bounties.\n\nFilters:\n- status: \"open\" | \"judging\" | \"winner-announced\" | \"paid\" | \"abandoned\" | \"cancelled\" | \"active\" (default: \"active\" \u2014 non-terminal states only)\n- poster: filter by poster BTC address\n- submitter: filter by submitter BTC address (bounties this address has submitted to)\n- tag: filter by single tag\n- limit / offset: pagination (max limit 100)\n\nEach bounty record includes a derived 'status' field computed from its timestamps.\n\nNo authentication required.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "description": "Max results (default 20, max 100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Pagination offset",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "poster": {
    "description": "Filter by poster BTC address",
    "type": "string"
   },
   "status": {
    "description": "Filter by computed status. Default: 'active' (excludes terminal states).",
    "enum": [
     "open",
     "judging",
     "winner-announced",
     "paid",
     "abandoned",
     "cancelled",
     "active"
    ],
    "type": "string"
   },
   "submitter": {
    "description": "Filter by submitter BTC address (bounties this address has submitted to)",
    "type": "string"
   },
   "tag": {
    "description": "Filter by a single tag",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "bounty_list"
}
