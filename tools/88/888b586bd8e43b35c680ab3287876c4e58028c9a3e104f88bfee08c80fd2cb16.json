{
 "description": "Get one phone mission by id, or list recent phone missions when id is omitted.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "id": {
    "description": "Phone mission id",
    "type": "string"
   },
   "limit": {
    "description": "Max missions when listing (default: 20, max: 100)",
    "type": "number"
   },
   "offset": {
    "description": "Skip missions when listing",
    "type": "number"
   },
   "status": {
    "description": "Optional status filter when listing missions",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "call_status"
}
