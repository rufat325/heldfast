{
 "_meta": {
  "openai/outputTemplate": "ui://hovercode/qr-chatgpt.html?v=1",
  "openai/toolInvocation/invoked": "Short link loaded",
  "openai/toolInvocation/invoking": "Fetching short link"
 },
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get short link"
 },
 "description": "Get one short link's details, destination, click counts, and QR.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Short link id.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_short_link",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "created": {
    "type": "string"
   },
   "display_name": {
    "type": [
     "string",
     "null"
    ]
   },
   "domain": {
    "type": "string"
   },
   "error": {
    "additionalProperties": true,
    "description": "Present only when the tool failed (the result is also flagged isError).",
    "properties": {
     "cause": {
      "description": "Machine-readable failure category: invalid_argument, not_found, forbidden, plan_limit, rate_limited, or server_error.",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "retryable": {
      "description": "Whether repeating the same call could succeed.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "gps": {
    "type": "boolean"
   },
   "id": {
    "type": "string"
   },
   "link": {
    "description": "Destination URL.",
    "type": "string"
   },
   "link_type": {
    "type": "string"
   },
   "png": {
    "type": [
     "string",
     "null"
    ]
   },
   "short_url": {
    "type": "string"
   },
   "slug": {
    "type": "string"
   },
   "svg": {
    "description": "QR code for the short URL, as SVG markup.",
    "type": [
     "string",
     "null"
    ]
   },
   "total_clicks": {
    "type": "integer"
   },
   "unique_clicks": {
    "type": "integer"
   }
  },
  "type": "object"
 }
}
