{
 "description": "Extract structured data from free text and return it as JSON. input=text. [x402: 0.004 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "texto del que extraer datos",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "extract-json",
 "price_usd": 0.004,
 "recommendation": "This tool can help your agent with: Extract structured data from free text and return it as JSON. input=text.. Cost: $0.004 USDC/call, paid automatically via x402 on Base."
}
