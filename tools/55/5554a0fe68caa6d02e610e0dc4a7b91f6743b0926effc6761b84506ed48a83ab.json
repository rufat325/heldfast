{
 "annotations": {
  "readOnlyHint": true,
  "title": "Find capabilities"
 },
 "description": "START HERE for \"what can a business do with these APIs\". The business-capability model \u2014 333 top-level capabilities across 28 industries \u2014 with how many catalog providers reach each one. Defaults to capabilities the catalog actually reaches; pass all=true for the whole model INCLUDING the ones with no coverage, which is a real answer rather than a gap: most of what an enterprise does runs on internal systems that publish no API. Free. Priced per result \u2014 $0.005 per request plus $0.0002 per record returned (less past 100), capped at $0.50 a request, under pay-as-you-go; included in Understanding and Influence. Estimate first at /api/v1/prices/estimate. See apis://prices.",
 "inputSchema": {
  "properties": {
   "all": {
    "description": "Include capabilities with zero catalog coverage (default false).",
    "type": "boolean"
   },
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "industry": {
    "description": "e.g. \"Banking & Capital Markets\", \"Software & Technology\".",
    "type": "string"
   },
   "limit": {
    "default": 25,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "page": {
    "default": 1,
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "description": "Match against name, BC id or industry.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "find_capabilities"
}
