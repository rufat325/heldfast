{
 "description": "People: one entry per person assembled from every app that serves their OpenProfile.md (podcasters from p0dcasters, public profiles from OutreachGraph). Search by name, headline or anything in the document; newest change first. Each answer carries the page, the openprofile.md URL, accounts, topics, Broadcast shows and the Guest section.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Default 30, max 200",
    "type": "integer"
   },
   "q": {
    "description": "Text to match against the name, headline and document (optional)",
    "type": "string"
   },
   "since": {
    "description": "ISO time: only profiles changed after it (optional)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_profiles"
}
