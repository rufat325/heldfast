{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Ask HandInLoop to propose the fields worth extracting from a document \u2014 the \"what should I extract?\" assist.\n\nGiven a document (and an optional goal), the model suggests a field set you can then pass as 'fields' to handinloop_submit_extraction_job for a custom, human-verified extraction. Synchronous: returns immediately, no job, no human review.\n\nArgs:\n  - document_url OR document_base64: the document (PDF/image). Exactly one required.\n  - file_name (optional)\n  - goal (optional): focus, e.g. \"accounts payable\"\n\nReturns: { fields: [{ name, description }] }",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "document_base64": {
    "description": "Base64-encoded document content (alternative to document_url)",
    "type": "string"
   },
   "document_url": {
    "description": "Publicly fetchable URL of the document (PDF or image)",
    "format": "uri",
    "type": "string"
   },
   "file_name": {
    "description": "Original filename, e.g. 'do-123.pdf'",
    "type": "string"
   },
   "goal": {
    "description": "Optional focus, e.g. 'accounts payable'",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "handinloop_propose_fields",
 "title": "Propose Extraction Fields"
}
