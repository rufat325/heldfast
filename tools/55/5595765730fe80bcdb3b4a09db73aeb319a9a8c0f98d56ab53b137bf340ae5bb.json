{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run a Socrata SoQL query against a Connecticut Open Data dataset by resource_id (e.g. \"28fr-iqnx\"). Filter with where/select/group/order (SoQL clauses, without the leading $) plus limit/offset. Returns matching rows as JSON.",
 "inputSchema": {
  "examples": [
   {
    "limit": 20,
    "resource_id": "28fr-iqnx"
   },
   {
    "limit": 20,
    "resource_id": "28fr-iqnx",
    "select": "lastupdatedate"
   }
  ],
  "properties": {
   "group": {
    "description": "SoQL $group column(s).",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (default Socrata 1000).",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset.",
    "type": "number"
   },
   "order": {
    "description": "SoQL $order, e.g. \"date DESC\".",
    "type": "string"
   },
   "resource_id": {
    "description": "Dataset id, e.g. \"28fr-iqnx\" (from datasets).",
    "type": "string"
   },
   "select": {
    "description": "SoQL $select, e.g. \"name, count(*) AS n\".",
    "type": "string"
   },
   "where": {
    "description": "SoQL $where filter, e.g. \"year >= 2020 AND status = 'Active'\".",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "query",
 "title": "Query",
 "version": "v1.1be84657"
}
