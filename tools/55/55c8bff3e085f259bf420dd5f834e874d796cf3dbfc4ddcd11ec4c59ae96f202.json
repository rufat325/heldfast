{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch a single known URL through Site Spy. Do NOT use this for discovery \u2014 use web_search to find URLs first, then fetch_url on each result. Do NOT fetch google.com / bing.com directly (they always block \u2014 wastes the cascade budget). For known static sites prefer fetch_backend: 'html_requests' (\u226410s). For SPAs use 'html_webdriver'. 'auto' starts cheap and escalates only when the site blocks us \u2014 plain HTTP, then a browser-grade TLS fingerprint, then a browser, then the anti-bot solvers \u2014 capped at 50s total (FETCH_URL_AUTO_TOTAL_TIMEOUT_SECONDS). On failure, response includes attempts[] and cascade_summary so you can see what was tried. This does not create a watch or save history.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "body": {
    "description": "Optional request body string.",
    "type": "string"
   },
   "fetch_backend": {
    "description": "Fetch strategy. Default 'auto' escalates on a block: html_requests -> impersonate (Chrome TLS, no browser) -> html_webdriver -> flaresolverr -> stealth_browser, with a 50s total cap. Prefer 'html_requests' explicitly for fast static sites; name a later stage only to test one.",
    "enum": [
     "auto",
     "html_requests",
     "impersonate",
     "html_webdriver",
     "flaresolverr",
     "stealth_browser"
    ],
    "type": "string"
   },
   "headers": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "Optional request headers.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "ignore_status_codes": {
    "description": "Return content for non-2xx responses instead of failing. Defaults to true.",
    "type": "boolean"
   },
   "include_filters": {
    "description": "Optional selectors used by auto mode to decide if browser rendering is needed.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "js": {
    "description": "JavaScript to execute before extraction when a browser backend is used.",
    "type": "string"
   },
   "max_content_chars": {
    "description": "Maximum returned characters for content fields.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "method": {
    "description": "HTTP method. Browser fetchers use GET-like navigation.",
    "enum": [
     "GET",
     "POST",
     "PUT",
     "PATCH",
     "DELETE",
     "HEAD",
     "OPTIONS"
    ],
    "type": "string"
   },
   "output": {
    "description": "Response content format. Defaults to text.",
    "enum": [
     "text",
     "html",
     "both",
     "metadata"
    ],
    "type": "string"
   },
   "screenshot": {
    "description": "Return a base64 screenshot when the browser backend captures one.",
    "type": "boolean"
   },
   "screenshot_format": {
    "description": "Screenshot format. Defaults to PNG.",
    "enum": [
     "PNG",
     "JPEG"
    ],
    "type": "string"
   },
   "timeout": {
    "description": "Per-backend timeout in seconds (1-120). 'auto' mode also has a separate 30s total wall-clock cap (env FETCH_URL_AUTO_TOTAL_TIMEOUT_SECONDS) that bounds the whole cascade.",
    "maximum": 120,
    "minimum": 1,
    "type": "integer"
   },
   "url": {
    "description": "URL to fetch",
    "type": "string"
   },
   "wait_seconds": {
    "description": "Extra browser wait before extracting content.",
    "maximum": 30,
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "fetch_url",
 "title": "Fetch a URL"
}
