{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "The fully resolved dependency graph of one published release \u2014 every transitive package it pulls in. Same requirement as `version`: the version must be one this package actually shipped, so call `package` first if you are not certain.",
 "inputSchema": {
  "examples": [
   {
    "name": "webpack",
    "system_": "NPM",
    "version": "5.88.0"
   },
   {
    "name": "org.springframework:spring-core",
    "system_": "MAVEN",
    "version": "6.0.0"
   }
  ],
  "properties": {
   "name": {
    "description": "Package name as the registry spells it.",
    "type": "string"
   },
   "system_": {
    "description": "NPM | PYPI | GO | MAVEN | NUGET | CARGO | RUBYGEMS",
    "type": "string"
   },
   "version": {
    "description": "An exact version string THIS package published \u2014 from the `package` tool, not assumed.",
    "type": "string"
   }
  },
  "required": [
   "system_",
   "name",
   "version"
  ],
  "type": "object"
 },
 "name": "dependencies",
 "outputSchema": {
  "description": "Resolved dependency graph for a package version",
  "properties": {},
  "type": "object"
 },
 "title": "Dependencies",
 "version": "v1.eb2edcc1"
}
