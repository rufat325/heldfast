{
 "description": "Semantic search for companies by name, domain, or keywords.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 10)",
    "maximum": 50,
    "type": "number"
   },
   "query": {
    "description": "Search query",
    "maxLength": 500,
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "trackly_search_companies"
}
