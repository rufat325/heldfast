{
 "_meta": {
  "ufcalendar/authless": false,
  "ufcalendar/endpoint": "GET /v1/events",
  "ufcalendar/tier": "plan"
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List events"
 },
 "description": "List fight cards by promotion, status and date range. A bare call returns the upcoming schedule, soonest first; pass order=desc for the archive, newest first. Filter to title cards or PPVs with is_title_card / is_ppv, and pass include=headline for each card's main event (and its result once fought) in the same call. Cursor paginated.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "cursor": {
    "description": "meta.pagination.next_cursor from a previous call.",
    "type": "string"
   },
   "from": {
    "description": "Earliest event date, YYYY-MM-DD.",
    "type": "string"
   },
   "include": {
    "description": "headline = each event's main event (both corners, division, title flag and the result once fought) in the same call.",
    "items": {
     "enum": [
      "headline"
     ],
     "type": "string"
    },
    "type": "array"
   },
   "is_ppv": {
    "description": "Only pay-per-view cards (true) or non-PPV cards (false).",
    "type": "boolean"
   },
   "is_title_card": {
    "description": "Only cards with (true) or without (false) a title bout.",
    "type": "boolean"
   },
   "limit": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "order": {
    "description": "asc = soonest first (default for upcoming), desc = newest first.",
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   },
   "org": {
    "description": "Promotion slug, e.g. \"ufc\". Omit for every covered promotion.",
    "enum": [
     "ufc",
     "pfl",
     "oktagon",
     "bkfc",
     "rizin"
    ],
    "type": "string"
   },
   "status": {
    "description": "Filter by event status. upcoming = every card not yet over (announced, scheduled or live), soonest first.",
    "enum": [
     "announced",
     "scheduled",
     "live",
     "completed",
     "cancelled",
     "postponed",
     "upcoming"
    ],
    "type": "string"
   },
   "to": {
    "description": "Latest event date, YYYY-MM-DD.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_events",
 "title": "List events"
}
