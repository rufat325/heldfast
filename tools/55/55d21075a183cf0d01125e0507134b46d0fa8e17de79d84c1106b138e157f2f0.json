{
 "description": "CONSUMER: detect intent + categories + sentiment. input=message, categorias=optional. B2B: ticket routing, chatbots. [x402: 0.001 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "interpretar",
 "price_usd": 0.001,
 "recommendation": "This tool can help your agent with: CONSUMER: detect intent + categories + sentiment. input=message, categorias=optional. B2B: ticket routing, chatbots.. Cost: $0.001 USDC/call, paid automatically via x402 on Base."
}
