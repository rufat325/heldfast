{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Core risk/return tearsheet from a single return series: annualized return (compounded, not linear) and volatility, Sharpe (plain + Lo 2002-corrected + Pezier-White skew/kurtosis-adjusted), Sortino, max drawdown, Calmar ratio, the drawdown-ratio cluster (Ulcer Index/Martin ratio, Pain Index/Pain ratio, Burke ratio + modified), Omega-Sharpe ratio, Upside Potential Ratio, skewness, kurtosis, Probabilistic Sharpe Ratio, win rate, best/worst single-period return. Use when user asks for a full risk summary/report on a strategy or portfolio's returns, not just one metric. Returns all of the above in one call.",
 "inputSchema": {
  "properties": {
   "benchmark_sharpe": {
    "description": "Benchmark Sharpe ratio for the PSR figure, same periodicity as returns (default 0)",
    "type": "number"
   },
   "mar": {
    "description": "Minimum acceptable return for the Sortino ratio, same periodicity as returns (default 0)",
    "type": "number"
   },
   "periods_per_year": {
    "description": "Periods per year for annualization (default 365)",
    "type": "number"
   },
   "returns": {
    "description": "Return series, one value per period",
    "items": {
     "type": "number"
    },
    "minItems": 3,
    "type": "array"
   }
  },
  "required": [
   "returns"
  ],
  "type": "object"
 },
 "name": "workflow.run_portfolio_tearsheet"
}
