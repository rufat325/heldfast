{
 "description": "Gets details and status for a specific quote by its quote key (GUID).",
 "execution": {
  "taskSupport": "optional"
 },
 "inputSchema": {
  "properties": {
   "quoteKey": {
    "description": "The quote key (GUID)",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "quoteKey"
  ],
  "type": "object"
 },
 "name": "get_quote_details"
}
