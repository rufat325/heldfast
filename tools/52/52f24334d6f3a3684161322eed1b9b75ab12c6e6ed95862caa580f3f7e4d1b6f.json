{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch the ISS's live orbital coordinates and return a number computed from them. No inputs required. Returns a single integer that changes with the ISS position \u2014 useful as a space-themed novelty value, not a stable seed.",
 "inputSchema": {
  "examples": [
   {},
   {}
  ],
  "properties": {},
  "required": [],
  "type": "object"
 },
 "name": "iss_number_generate",
 "outputSchema": {
  "properties": {
   "latitude": {
    "description": "ISS latitude coordinate",
    "type": "number"
   },
   "longitude": {
    "description": "ISS longitude coordinate",
    "type": "number"
   },
   "number": {
    "description": "Computed integer derived from ISS orbital coordinates",
    "type": "integer"
   }
  },
  "type": "object"
 },
 "title": "Iss Number Generate",
 "version": "v1.a9130b53"
}
