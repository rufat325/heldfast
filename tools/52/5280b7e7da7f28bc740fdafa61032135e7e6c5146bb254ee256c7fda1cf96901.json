{
 "description": "Modify a property on a Resource that is currently held by a node (or by another resource attached to that node). Use this to tweak shape radii, material colors, gradient stops, etc., WITHOUT recreating the resource. resource_path walks from the node down to the resource using \"/\"-separated property names, e.g. \"shape\", \"material\", or \"material/next_pass\". After the change, saves the scene.",
 "inputSchema": {
  "properties": {
   "node_path": {
    "description": "Path to the node owning the resource",
    "type": "string"
   },
   "property_name": {
    "description": "Property on the target resource to set (e.g. \"radius\", \"albedo_color\")",
    "type": "string"
   },
   "resource_path": {
    "description": "Path from node to the target resource via property names. Examples: \"shape\", \"material\", \"material/next_pass\"",
    "type": "string"
   },
   "scene_path": {
    "description": "Path to the .tscn scene file",
    "type": "string"
   },
   "value": {
    "description": "New value (same shape as modify_node_property.value)"
   }
  },
  "required": [
   "scene_path",
   "resource_path",
   "property_name",
   "value"
  ],
  "type": "object"
 },
 "name": "set_resource_property"
}
