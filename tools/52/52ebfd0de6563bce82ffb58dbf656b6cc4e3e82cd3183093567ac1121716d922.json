{
 "description": "Run an aggregation pipeline. $out/$merge and server-side JS are rejected.",
 "inputSchema": {
  "properties": {
   "collection": {
    "type": "string"
   },
   "pipeline": {
    "type": "array"
   }
  },
  "required": [
   "collection",
   "pipeline"
  ],
  "type": "object"
 },
 "name": "mongo_aggregate"
}
