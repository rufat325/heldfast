{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search UK earthquakes by area (lat/lng + radius), year, and minimum magnitude. Set period to \"modern\" (default, instrument-recorded since 1970) or \"historical\" (pre-1970 UK Historical Earthquakes catalogue). Returns event date/time, depth, local magnitude (ML), and intensity. Data: British Geological Survey (ogcapi.bgs.ac.uk), Open Government Licence, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lat": {
    "description": "Decimal degree latitude (WGS84) \u2014 must be supplied together with lng. Omit both for full Great Britain coverage.",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "limit": {
    "description": "Maximum earthquakes to return (default 20, max 100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "lng": {
    "description": "Decimal degree longitude (WGS84) \u2014 must be supplied together with lat.",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "min_magnitude": {
    "description": "Minimum local magnitude (ML) to include",
    "type": "number"
   },
   "period": {
    "description": "\"modern\" = instrument-recorded earthquakes since 1970 (default), \"historical\" = UK earthquakes before 1970",
    "enum": [
     "modern",
     "historical"
    ],
    "type": "string"
   },
   "radius_km": {
    "description": "Search radius in kilometres around lat/lng (default 50, max 500)",
    "maximum": 500,
    "minimum": 0.5,
    "type": "number"
   },
   "year": {
    "description": "Filter to a single 4-digit year, e.g. \"2020\"",
    "pattern": "^\\d{4}$",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "bgs-ogcapi.hazards.earthquake_search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "BGS UK Earthquake Search"
}
