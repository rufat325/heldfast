{
 "description": "Query account balance for a specific asset (chainId, assetChainId, assetId, address)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "Nerve address",
    "type": "string"
   },
   "assetChainId": {
    "description": "Asset chain ID",
    "type": "integer"
   },
   "assetId": {
    "description": "Asset ID",
    "type": "integer"
   },
   "chainId": {
    "description": "Nerve chain ID (e.g. 9 for mainnet)",
    "exclusiveMinimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "chainId",
   "assetChainId",
   "assetId",
   "address"
  ],
  "type": "object"
 },
 "name": "nerve_get_account_balance"
}
