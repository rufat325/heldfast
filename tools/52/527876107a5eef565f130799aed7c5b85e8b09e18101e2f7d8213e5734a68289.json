{
 "_meta": {},
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List subscription group subscriptions"
 },
 "description": "List all auto-renewable subscriptions in a subscription group (productId, name, state, prices, etc.). Use to verify products like premium_monthly_v2 and premium_annual_v2 in a group.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "filter.name": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by subscription name(s)"
   },
   "filter.productId": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by product ID(s), e.g. premium_monthly_v2"
   },
   "filter.state": {
    "anyOf": [
     {
      "enum": [
       "MISSING_METADATA",
       "READY_TO_SUBMIT",
       "WAITING_FOR_REVIEW",
       "IN_REVIEW",
       "DEVELOPER_ACTION_NEEDED",
       "PENDING_BINARY_APPROVAL",
       "APPROVED",
       "DEVELOPER_REMOVED_FROM_SALE",
       "REMOVED_FROM_SALE",
       "REJECTED"
      ],
      "type": "string"
     },
     {
      "items": {
       "enum": [
        "MISSING_METADATA",
        "READY_TO_SUBMIT",
        "WAITING_FOR_REVIEW",
        "IN_REVIEW",
        "DEVELOPER_ACTION_NEEDED",
        "PENDING_BINARY_APPROVAL",
        "APPROVED",
        "DEVELOPER_REMOVED_FROM_SALE",
        "REMOVED_FROM_SALE",
        "REJECTED"
       ],
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by state (e.g. APPROVED)"
   },
   "limit": {
    "default": 50,
    "description": "Maximum number of subscriptions to return (default 50, max 200)",
    "type": "number"
   },
   "sort": {
    "description": "Sort by name (use -name for descending)",
    "enum": [
     "name",
     "-name"
    ],
    "type": "string"
   },
   "subscriptionGroupId": {
    "description": "Subscription group ID (e.g. Premium group ID). Get from app \u2192 subscription groups.",
    "type": "string"
   }
  },
  "required": [
   "subscriptionGroupId"
  ],
  "type": "object"
 },
 "name": "list-subscription-group-subscriptions",
 "title": "List subscription group subscriptions"
}
