{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search 1M+ objects in the Victoria and Albert Museum collection by keyword, object type, date range, and display status. Returns object type, title, maker, date, place of origin, thumbnail URL, and IIIF image base for each result. Filter by on_display=true to find objects currently on display in London. Source: V&A public API (no auth required).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "object_type": {
    "description": "Filter by object type (e.g. \"Painting\", \"Vase\", \"Tile\", \"Photograph\", \"Drawing\", \"Print\", \"Textile\"). Case-sensitive; use exact V&A category names.",
    "type": "string"
   },
   "on_display": {
    "description": "When true, return only objects currently on display in V&A galleries. When false or omitted, include objects in storage too.",
    "type": "boolean"
   },
   "page": {
    "description": "Page number for paginating through results (default 1).",
    "minimum": 1,
    "type": "integer"
   },
   "page_size": {
    "description": "Number of results per page (1-30, default 10).",
    "maximum": 30,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Keyword search across titles, descriptions, maker names, and materials (e.g. \"teapot\", \"William Morris\", \"silk embroidery\"). Leave empty to browse all objects.",
    "type": "string"
   },
   "year_from": {
    "description": "Earliest production year filter (e.g. 1750).",
    "maximum": 2100,
    "minimum": 0,
    "type": "integer"
   },
   "year_to": {
    "description": "Latest production year filter (e.g. 1850).",
    "maximum": 2100,
    "minimum": 0,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "vam.collections.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "V&A Collection Search"
}
