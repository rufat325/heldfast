{
 "description": "Restart the running instances of a Code Engine application by terminating its current pods via the CE Kubernetes API proxy. Code Engine automatically recreates the instances, picking up the latest configuration and image. No effect if the app is scaled to zero.",
 "inputSchema": {
  "properties": {
   "app_name": {
    "description": "Application name",
    "type": "string"
   },
   "project_id": {
    "description": "Project ID or name",
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "app_name"
  ],
  "type": "object"
 },
 "name": "ce_restart_application"
}
