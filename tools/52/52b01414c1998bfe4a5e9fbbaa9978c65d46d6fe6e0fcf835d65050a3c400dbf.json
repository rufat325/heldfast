{
 "description": "Query recent tool calls made by THIS MCP session (in-process diagnostics, volatile, completed calls only). For the durable, hash-chained provenance trail pass durable:true \u2014 that queries the backend audit chain.",
 "inputSchema": {
  "properties": {
   "after": {
    "description": "ISO timestamp \u2014 only entries after this time",
    "type": "string"
   },
   "before": {
    "description": "ISO timestamp \u2014 only entries before this time",
    "type": "string"
   },
   "durable": {
    "description": "Query the backend hash-chained audit trail instead of this session buffer",
    "type": "boolean"
   },
   "limit": {
    "description": "Max entries to return (default 20)",
    "type": "number"
   },
   "tool_name": {
    "description": "Filter by tool name",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "velixar_audit_log"
}
