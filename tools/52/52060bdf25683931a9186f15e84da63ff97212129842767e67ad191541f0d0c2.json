{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search NASA Common Metadata Repository (CMR) for Earth science satellite datasets and collections. Filter by keyword, data provider (ORNL_DAAC, GES_DISC, LPDAAC_ECS, PODAAC, NSIDC_ECS), temporal range, spatial bounding box, and processing level. Returns concept IDs, titles, summaries, data centers, temporal coverage, and access flags. CMR indexes 45,000+ collections covering climate, land, atmosphere, ocean, cryosphere, and biosphere from 60+ NASA data archives. Concept IDs from results feed into nasa-cmr.datasets.detail and nasa-cmr.granules.search. Source: NASA CMR \u2014 US Gov public domain, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bbox": {
    "description": "Bounding box spatial filter as \"west,south,east,north\" decimal degrees (e.g. \"-180,-90,180,90\" for global, \"-125,24,-66,50\" for continental US). Returns collections with spatial coverage intersecting the box.",
    "type": "string"
   },
   "keyword": {
    "description": "Free-text keyword to search collection titles, summaries, and metadata (e.g. \"sea surface temperature\", \"MODIS land cover\", \"global precipitation\").",
    "type": "string"
   },
   "page_size": {
    "description": "Number of collections to return per page (1\u201320, default 10).",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "processing_level": {
    "description": "NASA data processing level filter (e.g. \"1B\", \"2\", \"3\", \"4\"). Level 1 = raw/calibrated; Level 2 = geophysical retrievals; Level 3 = gridded; Level 4 = model output.",
    "type": "string"
   },
   "provider": {
    "description": "Filter by NASA data center / archive provider ID (e.g. \"GES_DISC\", \"ORNL_DAAC\", \"LPDAAC_ECS\", \"PODAAC\", \"NSIDC_ECS\"). Use nasa-cmr.providers.list to discover available provider IDs.",
    "type": "string"
   },
   "short_name": {
    "description": "Exact collection short name identifier (e.g. \"MOD13A2\", \"GPM_3IMERGHHE\"). Use when you know the specific dataset short name.",
    "type": "string"
   },
   "sort_key": {
    "description": "Sort order for results: \"-score\" (relevance, default), \"entry_title\" / \"-entry_title\" (alphabetical), \"start_date\" / \"-start_date\" (temporal coverage start date).",
    "enum": [
     "-score",
     "entry_title",
     "-entry_title",
     "start_date",
     "-start_date"
    ],
    "type": "string"
   },
   "temporal_end": {
    "description": "End of temporal coverage filter in ISO 8601 UTC format (e.g. \"2023-12-31T23:59:59Z\"). Returns collections whose temporal extent overlaps this range.",
    "type": "string"
   },
   "temporal_start": {
    "description": "Start of temporal coverage filter in ISO 8601 UTC format (e.g. \"2020-01-01T00:00:00Z\"). Returns collections whose temporal extent overlaps this range.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "nasa-cmr.datasets.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "NASA CMR \u2014 Search Earth Science Datasets"
}
