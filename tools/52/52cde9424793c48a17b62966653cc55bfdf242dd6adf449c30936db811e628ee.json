{
 "description": "Report how far behind chainhead a subgraph is, by reading its _meta block. Worth calling before trusting any 'current' or 'latest' number, because a lagging subgraph returns stale data rather than an error \u2014 it succeeds, it is just answering about the past.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "subgraph": {
    "description": "Which subgraph to check",
    "enum": [
     "main",
     "beefy_pnl",
     "slimmed_pnl",
     "activity",
     "orderbook",
     "open_interest",
     "resolution",
     "traders",
     "v2_orderbook",
     "v2_pnl",
     "v2_main"
    ],
    "type": "string"
   }
  },
  "required": [
   "subgraph"
  ],
  "type": "object"
 },
 "name": "check_subgraph_freshness"
}
