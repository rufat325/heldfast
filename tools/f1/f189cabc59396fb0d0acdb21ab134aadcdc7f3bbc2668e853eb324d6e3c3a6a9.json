{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run a raw SoQL query against any Montgomery County, MD open-data resource (data.montgomerycountymd.gov) by its Socrata id (8-char like \"icn6-v9z3\"). Full SoQL: where/select/group/order/limit/offset. Use montgomery_datasets to find a resource id, or montgomery_recent for the common ones.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-data-montgomery-api-key",
    "limit": 100,
    "resource_id": "icn6-v9z3",
    "where": "status = 'Open'"
   },
   {
    "_apiKey": "your-data-montgomery-api-key",
    "group": "permit_type",
    "order": "count DESC",
    "resource_id": "p2c7-bxz5",
    "select": "permit_type, count(*) as count"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Socrata app token for higher rate limits. Omit to use the keyless endpoint.",
    "type": "string"
   },
   "group": {
    "description": "SoQL $group.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (default 100, max 5000).",
    "type": "number"
   },
   "offset": {
    "description": "Row offset for paging.",
    "type": "number"
   },
   "order": {
    "description": "SoQL $order.",
    "type": "string"
   },
   "resource_id": {
    "description": "Socrata resource id, e.g. \"icn6-v9z3\".",
    "type": "string"
   },
   "select": {
    "description": "SoQL $select.",
    "type": "string"
   },
   "where": {
    "description": "SoQL $where filter.",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "montgomery_query",
 "title": "Montgomery Query",
 "version": "v1.5814ee72"
}
