{
 "description": "Get transaction details by digest.",
 "inputSchema": {
  "properties": {
   "digest": {
    "description": "Transaction digest",
    "type": "string"
   },
   "showEffects": {
    "description": "Show effects (default: true)",
    "type": "boolean"
   },
   "showEvents": {
    "description": "Show events (default: true)",
    "type": "boolean"
   },
   "showInput": {
    "description": "Show transaction input (default: true)",
    "type": "boolean"
   }
  },
  "required": [
   "digest"
  ],
  "type": "object"
 },
 "name": "get_transaction"
}
