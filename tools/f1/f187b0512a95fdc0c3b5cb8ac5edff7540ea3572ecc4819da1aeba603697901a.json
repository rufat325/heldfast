{
 "description": "Stop the active execution without changing saved-job state or submitting any application.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "executionId": {
    "minimum": 1,
    "type": "integer"
   },
   "expectedRevision": {
    "minimum": 1,
    "type": "integer"
   },
   "idempotencyKey": {
    "maxLength": 200,
    "minLength": 16,
    "pattern": "^[\\x20-\\x7e]+$",
    "type": "string"
   },
   "reasonCode": {
    "enum": [
     "user_requested",
     "target_changed",
     "session_ended",
     "execution_restarted",
     "operator_stop"
    ],
    "type": "string"
   }
  },
  "required": [
   "executionId",
   "expectedRevision",
   "idempotencyKey"
  ],
  "type": "object"
 },
 "name": "trackly_stop_apply_execution"
}
