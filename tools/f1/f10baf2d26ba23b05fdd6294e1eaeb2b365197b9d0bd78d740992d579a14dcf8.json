{
 "description": "Append one event to a Team Task timeline (delegation, reply, status, approval_needed, result, error, done, lead_assigned). Log from your OWN identity only; never include secrets in summary or body.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action_id": {
    "description": "DashClaw action id this event corresponds to",
    "type": "string"
   },
   "body": {
    "description": "Optional detail",
    "type": "string"
   },
   "from_agent": {
    "description": "Sender agent id, e.g. claude | openclaw | moltfire (lowercase slug, required)",
    "type": "string"
   },
   "summary": {
    "description": "One-line summary (required)",
    "type": "string"
   },
   "task_id": {
    "description": "Team task id (required)",
    "type": "string"
   },
   "to_agent": {
    "description": "Recipient agent id, e.g. claude | openclaw | wes | forge (lowercase slug, required)",
    "type": "string"
   },
   "ts": {
    "description": "ISO 8601 timestamp (defaults to server time)",
    "type": "string"
   },
   "type": {
    "description": "Event type (required)",
    "type": "string"
   }
  },
  "required": [
   "task_id",
   "from_agent",
   "to_agent",
   "type",
   "summary"
  ],
  "type": "object"
 },
 "name": "dashclaw_task_event"
}
