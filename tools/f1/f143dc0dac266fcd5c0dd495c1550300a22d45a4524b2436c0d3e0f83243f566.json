{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search City of Tucson GIS open geospatial datasets (zoning, parcels, public safety, transport & city services) by keyword. Returns each dataset's name, summary, record_count, owner/org, and its Feature Service `url` \u2014 pass that url to query_layer / layer_info.",
 "inputSchema": {
  "examples": [
   {
    "query": "parcels"
   },
   {
    "limit": 10,
    "query": "crime public safety"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max datasets (1-50, default 20).",
    "type": "number"
   },
   "org_id": {
    "description": "Optional ArcGIS orgId to override the default (City of Tucson GIS).",
    "type": "string"
   },
   "query": {
    "description": "Keyword(s), e.g. \"parcels\", \"crime\", \"flood zones\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_datasets",
 "title": "Search Datasets",
 "version": "v1.cded02ce"
}
