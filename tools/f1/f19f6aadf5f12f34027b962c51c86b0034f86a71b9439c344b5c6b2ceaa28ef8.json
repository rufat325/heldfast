{
 "description": "Create a named group this tenant owns, for host.tool_share(visibility: \"group\").",
 "inputSchema": {
  "properties": {
   "name": {
    "description": "Group name.",
    "type": "string"
   },
   "tenant_key": {
    "description": "The key `signup` returned. Required only when this connection carries no Authorization: Bearer header -- when both are present, the header wins.",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "host.group.create"
}
