{
 "description": "Check for rootkits via syscall table tampering and hidden kernel modules. Runs linux_check_syscall and linux_hidden_modules plugins. Returns syscall_check, hidden_modules, rootkit_indicators, and likely_compromised. Read-only analysis.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dump_path": {
    "description": "Path to the Linux memory dump file",
    "type": "string"
   },
   "profile": {
    "description": "Volatility 2 profile name",
    "type": "string"
   }
  },
  "required": [
   "dump_path",
   "profile"
  ],
  "type": "object"
 },
 "name": "memory_detect_rootkit"
}
