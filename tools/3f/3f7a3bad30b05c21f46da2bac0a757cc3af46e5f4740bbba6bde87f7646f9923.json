{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Render a local .html or .htm file beneath the configured allowed root and return accessibility findings and corrections. Read-only.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "filePath": {
    "description": "Absolute path, or path relative to the server working directory, to an HTML file.",
    "minLength": 1,
    "type": "string"
   },
   "height": {
    "default": 900,
    "description": "Viewport height in CSS pixels.",
    "maximum": 4320,
    "minimum": 240,
    "type": "integer"
   },
   "maxIssues": {
    "default": 50,
    "description": "Maximum violation groups returned; summary totals still cover the full run.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "maxNodesPerIssue": {
    "default": 10,
    "description": "Maximum affected elements returned per rule.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "standard": {
    "default": "wcag22aa",
    "description": "WCAG ruleset to test. Use best-practice to include additional axe guidance.",
    "enum": [
     "wcag2a",
     "wcag2aa",
     "wcag2aaa",
     "wcag21aa",
     "wcag21aaa",
     "wcag22aa",
     "wcag22aaa",
     "best-practice"
    ],
    "type": "string"
   },
   "width": {
    "default": 1440,
    "description": "Viewport width in CSS pixels.",
    "maximum": 7680,
    "minimum": 320,
    "type": "integer"
   }
  },
  "required": [
   "filePath"
  ],
  "type": "object"
 },
 "name": "audit_file",
 "title": "Audit a local HTML file"
}
