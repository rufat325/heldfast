{
 "description": "Retry a failed or stopped execution by ID. Requires a recent n8n version exposing retry in the public API \u2014 if unavailable (404), re-trigger the workflow via run_webhook instead.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Execution ID to retry",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "retry_execution"
}
