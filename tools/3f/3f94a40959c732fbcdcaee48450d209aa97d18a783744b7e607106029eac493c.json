{
 "description": "Owner-only: undo host.channel.freeze; the next post succeeds with the next seq.",
 "inputSchema": {
  "properties": {
   "channel_id": {
    "description": "The group channel's id.",
    "type": "string"
   },
   "tenant_key": {
    "description": "The key `signup` returned. Required only when this connection carries no Authorization: Bearer header -- when both are present, the header wins.",
    "type": "string"
   }
  },
  "required": [
   "channel_id"
  ],
  "type": "object"
 },
 "name": "host.channel.unfreeze"
}
