{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": false
 },
 "description": "Export a portable evidence pack (JSON) plus a human-readable Markdown summary for review or later hosted promotion. Includes facts, provenance, generated outputs, and high-level score metadata \u2014 never prompts, weights, traces, or raw source. Validates against the local schema.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "graph_html": {
    "description": "Also write a self-contained offline evidence-graph explorer (metadata only, no network).",
    "type": "boolean"
   },
   "include_generated_bodies": {
    "description": "Embed generated test bodies in the pack. Default false: bodies stay in the local workspace so the exported pack is metadata-only. Raw source snippets never cross the boundary regardless of this flag.",
    "type": "boolean"
   },
   "output_path": {
    "description": "Output JSON path. Default 'orangepro-evidence-pack.json'.",
    "type": "string"
   },
   "workspace": {
    "description": "Local workspace root. Defaults to the current directory.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "orangepro_export_evidence_pack",
 "title": "Export evidence pack"
}
