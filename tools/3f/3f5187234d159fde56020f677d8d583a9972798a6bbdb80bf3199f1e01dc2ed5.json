{
 "description": "Save the user\u2019s qhs sync code on this machine so version history and restore work for shares created elsewhere. The code is stored locally in ~/.qhs/shares.json and only ever reaches the server as a hash. Use when the user says 'here is my sync code', 'connect my shares', or hits a tool that reports no credential.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "syncCode": {
    "pattern": "^qhsk_[A-Za-z0-9_-]{43}$",
    "type": "string"
   }
  },
  "required": [
   "syncCode"
  ],
  "type": "object"
 },
 "name": "qhs_set_sync_code"
}
