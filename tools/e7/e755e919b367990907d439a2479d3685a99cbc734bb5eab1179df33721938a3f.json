{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List messages in a DM conversation, sorted newest first. The conversation_id comes from social_api_list_conversations. Returns message objects with id, direction (incoming/outgoing), text, sender info, and timestamp. Supports pagination via cursor. Max 200 results (default 50).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "conversation_id": {
    "description": "Conversation ID (from social_api_list_conversations)",
    "type": "string"
   },
   "cursor": {
    "description": "Pagination cursor from previous response",
    "type": "string"
   },
   "limit": {
    "description": "Max results (1-200, default 50)",
    "type": "string"
   }
  },
  "required": [
   "conversation_id"
  ],
  "type": "object"
 },
 "name": "social_api_list_messages",
 "title": "List Messages"
}
