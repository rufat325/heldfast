{
 "description": "Search for products on Kroger by name, brand, or category. Returns product details with prices.",
 "inputSchema": {
  "properties": {
   "brand": {
    "description": "Optional brand filter",
    "type": "string"
   },
   "category": {
    "description": "Optional category filter",
    "type": "string"
   },
   "onSale": {
    "description": "Filter to only show sale items",
    "type": "boolean"
   },
   "query": {
    "description": "Search query for products",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "kroger_search_products"
}
