{
 "description": "Detecta outliers (IQR) en una columna de un CSV. input='<url_o_csv>||<columna>'. [x402: 0.02 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "detectar-outliers",
 "price_usd": 0.02,
 "recommendation": "This tool can help your agent with: Detecta outliers (IQR) en una columna de un CSV. input='<url_o_csv>||<columna>'.. Cost: $0.02 USDC/call, paid automatically via x402 on Base."
}
