{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Set Host Visibility"
 },
 "description": "Show or hide a host on a live event's public page without changing their permissions (visible=false hides, true shows). host_id defaults to you. Requires event_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "host_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "visible": {
    "type": "boolean"
   }
  },
  "required": [
   "event_id",
   "visible"
  ],
  "type": "object"
 },
 "name": "set_host_visibility",
 "title": "Set Host Visibility"
}
