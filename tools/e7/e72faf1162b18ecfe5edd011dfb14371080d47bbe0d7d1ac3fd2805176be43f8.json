{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Schedule a post"
 },
 "description": "Schedule a post. Sets or moves the publish time of a draft, failed or scheduled post. The scheduler publishes it within a minute of that time. Same rules as scheduling on create. post.scheduled fires.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Numeric id.",
    "type": "integer"
   },
   "scheduled_at": {
    "format": "date-time",
    "type": "string"
   }
  },
  "required": [
   "id",
   "scheduled_at"
  ],
  "type": "object"
 },
 "name": "schedule_post"
}
