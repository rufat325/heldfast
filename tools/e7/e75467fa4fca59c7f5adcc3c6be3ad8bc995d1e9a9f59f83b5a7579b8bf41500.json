{
 "_meta": {
  "skill": "signing"
 },
 "description": "Verify a Bitcoin message signature (BIP-137 or BIP-322) and recover or confirm the signer. Auto-detects BIP-137 (65-byte compact) vs BIP-322 (witness-serialized) format. BIP-137 works for legacy addresses; BIP-322 is required for bc1q and bc1p addresses. Takes the original message and signature (hex or base64). Compatible with signatures from most Bitcoin wallets.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "address": {
    "description": "The Bitcoin address that allegedly signed the message. Required for BIP-322 verification (bc1q for P2WPKH, bc1p for P2TR). Also used for BIP-137 to confirm the recovered address matches.",
    "type": "string"
   },
   "expectedSigner": {
    "description": "Optional: alias for address (backward compatibility). If address is not provided, this is used instead.",
    "type": "string"
   },
   "message": {
    "description": "The original plain text message that was signed.",
    "type": "string"
   },
   "signature": {
    "description": "The signature in hex or base64. BIP-137: 65 bytes (130 hex / 88 base64). BIP-322: variable-length witness-serialized (base64).",
    "type": "string"
   }
  },
  "required": [
   "message",
   "signature"
  ],
  "type": "object"
 },
 "name": "btc_verify_message"
}
