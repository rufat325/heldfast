{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Stop an automatic backup schedule on a Proxmox VPS. Get the schedule_id from `impreza_vps_list_backup_schedules`. This stops FUTURE backups; the ones already taken stay. Tell the customer that after this nothing is backing the machine up automatically.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "schedule_id": {
    "description": "Schedule id from impreza_vps_list_backup_schedules.",
    "type": "number"
   },
   "service_id": {
    "description": "Proxmox VPS service id.",
    "type": "number"
   }
  },
  "required": [
   "service_id",
   "schedule_id"
  ],
  "type": "object"
 },
 "name": "impreza_vps_delete_backup_schedule"
}
