{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Consensus Holdings Across Institutions"
 },
 "description": "Combine 2-25 institutions' 13F portfolios on their latest common report date. Ranks stocks by holder count, then combined value. Published values normally use report-date closing prices, may fall back to filer values, and can be zero when unavailable. Set minInstitutions to 2 or more for positions shared by multiple filers.",
 "inputSchema": {
  "properties": {
   "institutionNames": {
    "description": "Institution names or CIKs (2-25). Unique partial names and verified aliases resolve; ambiguous partials return candidate CIKs.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "maxResults": {
    "default": 30,
    "description": "Maximum number of stocks to return (default: 30, clamped to 1-500)",
    "type": "integer"
   },
   "minInstitutions": {
    "default": 1,
    "description": "Minimum number of institutions that must hold a stock (default: 1; set 2 or more for shared positions)",
    "type": "integer"
   },
   "reportDate": {
    "default": null,
    "description": "Quarter-end 13F report date in YYYY-MM-DD format (defaults to the latest common quarter; an off-quarter date snaps to the nearest common report on or before it)",
    "type": "string"
   }
  },
  "required": [
   "institutionNames"
  ],
  "type": "object"
 },
 "name": "GetInstitutionConsensusHoldings",
 "title": "Consensus Holdings Across Institutions"
}
