{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find yield opportunities across DeFi pools filtered by project name, minimum TVL, or minimum APY. Returns APY, TVL, and project details.",
 "inputSchema": {
  "examples": [
   {
    "search": "curve"
   },
   {
    "min_apy": 5,
    "min_tvl": 1000000
   }
  ],
  "properties": {
   "min_apy": {
    "description": "Minimum APY percentage (e.g., 5 for 5%)",
    "type": "number"
   },
   "min_tvl": {
    "description": "Minimum TVL in USD (e.g., 1000000 for $1M)",
    "type": "number"
   },
   "search": {
    "description": "Filter by project name (case-insensitive substring match)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "defi_yields",
 "outputSchema": {
  "properties": {
   "pools": {
    "items": {
     "properties": {
      "apy": {
       "description": "Annual percentage yield",
       "type": "number"
      },
      "apy_base": {
       "description": "Base APY from swap fees",
       "type": [
        "number",
        "null"
       ]
      },
      "apy_reward": {
       "description": "Additional reward APY",
       "type": [
        "number",
        "null"
       ]
      },
      "chain": {
       "description": "Blockchain where pool is deployed",
       "type": "string"
      },
      "exposure": {
       "description": "Pool exposure type",
       "type": [
        "string",
        "null"
       ]
      },
      "il_risk": {
       "description": "Impermanent loss risk level",
       "type": [
        "string",
        "null"
       ]
      },
      "pool_id": {
       "description": "Unique pool identifier",
       "type": "string"
      },
      "project": {
       "description": "Project/protocol name",
       "type": "string"
      },
      "stablecoin": {
       "description": "Whether pool is stablecoin pair",
       "type": "boolean"
      },
      "symbol": {
       "description": "Pool token symbol",
       "type": "string"
      },
      "tvl_usd": {
       "description": "Total value locked in USD",
       "type": "number"
      }
     },
     "required": [
      "pool_id",
      "project",
      "chain",
      "symbol",
      "tvl_usd",
      "apy"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "showing": {
    "description": "Number of pools returned (max 50)",
    "type": "integer"
   },
   "total_matched": {
    "description": "Total pools matching filters",
    "type": "integer"
   }
  },
  "required": [
   "total_matched",
   "showing",
   "pools"
  ],
  "type": "object"
 },
 "title": "Defi Yields",
 "version": "v1.c6142790"
}
