{
 "description": "Open a URL in the simulator using xcrun simctl openurl. Use to verify deep links, universal links, onboarding routes, and state restoration.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "udid": {
    "description": "Simulator UDID. Use simulator_list first when unsure.",
    "format": "uuid",
    "type": "string"
   },
   "url": {
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "udid",
   "url"
  ],
  "type": "object"
 },
 "name": "open_deep_link",
 "title": "Open a deep link"
}
