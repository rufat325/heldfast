{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Query records from a Nantes M\u00e9tropole Open Data dataset with ODSQL. Filter (where), aggregate (group_by/select), sort (order_by), paginate (limit/offset).",
 "inputSchema": {
  "examples": [
   {
    "dataset_id": "bike_stations",
    "limit": 20
   },
   {
    "dataset_id": "air_quality",
    "group_by": "station_name",
    "order_by": "avg_pm25 desc",
    "select": "count(*) as measurements, avg(pm25) as avg_pm25",
    "where": "date >= 2023-01-01"
   }
  ],
  "properties": {
   "dataset_id": {
    "description": "Dataset id from search_datasets.",
    "type": "string"
   },
   "group_by": {
    "description": "ODSQL group_by field(s).",
    "type": "string"
   },
   "limit": {
    "description": "Max records (1-100, default 20).",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset (default 0).",
    "type": "number"
   },
   "order_by": {
    "description": "Sort, e.g. `date desc`.",
    "type": "string"
   },
   "query": {
    "description": "Free-text keyword across all fields (optional).",
    "type": "string"
   },
   "select": {
    "description": "ODSQL select/aggregation, e.g. `count(*) as n, sum(amount)`.",
    "type": "string"
   },
   "where": {
    "description": "ODSQL filter, e.g. `year >= 2020 AND city = \"Paris\"` (overrides query).",
    "type": "string"
   }
  },
  "required": [
   "dataset_id"
  ],
  "type": "object"
 },
 "name": "query",
 "title": "Query",
 "version": "v1.1269b164"
}
