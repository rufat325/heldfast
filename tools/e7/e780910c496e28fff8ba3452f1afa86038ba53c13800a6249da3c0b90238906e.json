{
 "description": "Actively probe ONE live TLS/SSH endpoint YOU OWN for post-quantum readiness (PQC-hybrid key exchange X25519MLKEM768, classical certificate posture). REQUIRES an ownership attestation: set i_own_this=true to confirm you are authorized to test the target. Refuses CIDR ranges / wildcards / lists \u2014 one host at a time. Performs only a benign, unauthenticated handshake and never modifies the endpoint. NOTE: this is the ONLY quantakrypto MCP tool that opens a network connection; the server is otherwise offline. Over HTTP it is disabled unless the operator sets QUANTAKRYPTO_MCP_ALLOW_NETWORK=1.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "i_own_this": {
    "description": "Attestation that you are authorized to probe this endpoint. Must be true; the probe is refused otherwise.",
    "type": "boolean"
   },
   "mode": {
    "description": "Probe mode (default: auto \u2014 SSH on :22, TLS otherwise).",
    "enum": [
     "tls",
     "ssh",
     "auto"
    ],
    "type": "string"
   },
   "target": {
    "description": "A single host or host:port you own (no ranges/CIDRs/wildcards).",
    "type": "string"
   },
   "timeout_ms": {
    "description": "Per-connection timeout in ms (default 8000).",
    "type": "number"
   }
  },
  "required": [
   "target",
   "i_own_this"
  ],
  "type": "object"
 },
 "name": "probe_endpoint"
}
