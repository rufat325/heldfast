{
 "description": "Get the lifecycle state of a feature: current phase, phase history, artifacts, warnings.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Item ID (UUID) or slug",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_feature_lifecycle"
}
