{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "UPDATE a Robinhood Chain copy-trade rule (PATCH \u2014 this writes). The response carries source_wallets_tracked / source_wallets_untracked + warnings: only tracked KOL wallets (/rhc/kol/wallets) can ever fire, so surface any untracked_source_wallets warning. Send only the fields you want changed; is_active:false pauses a rule without deleting it. source_wallets is a whole-list REPLACE and is re-checked against the tier wallet cap, so a PRO rule cannot be PATCHed past 5 wallets. Pass null for name or webhook_url to clear them. There is no market-cap band to set on RHC. Tier: PRO+.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "delivery_mode": {
    "description": "Where signals are delivered",
    "enum": [
     "webhook",
     "websocket",
     "both"
    ],
    "type": "string"
   },
   "id": {
    "description": "Copy-trade rule id (positive integer)",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "is_active": {
    "description": "false pauses the rule without deleting it",
    "type": "boolean"
   },
   "min_trade_eth": {
    "description": "Minimum source trade size in ETH",
    "minimum": 0,
    "type": "number"
   },
   "name": {
    "anyOf": [
     {
      "maxLength": 64,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "New label, or null to clear"
   },
   "only_action": {
    "description": "Which side to mirror",
    "enum": [
     "buy",
     "sell",
     "both"
    ],
    "type": "string"
   },
   "sizing_amount": {
    "description": "Amount in ETH",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "sizing_mode": {
    "description": "How sizing_amount is interpreted",
    "enum": [
     "fixed",
     "proportional",
     "percent_source"
    ],
    "type": "string"
   },
   "source_wallets": {
    "description": "Replacement wallet list (0x, 40 hex) \u2014 re-checked against the tier cap",
    "items": {
     "type": "string"
    },
    "maxItems": 250,
    "minItems": 1,
    "type": "array"
   },
   "webhook_url": {
    "anyOf": [
     {
      "format": "uri",
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "New HTTPS webhook URL, or null to clear"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "rhc_copytrade_update"
}
