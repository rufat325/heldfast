{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": false
 },
 "description": "Archive a Bear note. Archived notes are hidden from the main list but not deleted. Use 'undo' to unarchive.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Note ID (uniqueIdentifier)",
    "type": "string"
   },
   "undo": {
    "description": "Unarchive the note instead of archiving",
    "type": "boolean"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "bear_archive_note"
}
