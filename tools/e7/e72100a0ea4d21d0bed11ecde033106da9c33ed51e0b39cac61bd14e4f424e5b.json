{
 "annotations": {
  "readOnlyHint": true,
  "title": "Who introduced whom"
 },
 "description": "Who introduced the people in the user's graph, aggregated per introducer, so a skill can see which relationships actually produce work rather than which are warm. Returns each introducer with the people they introduced and how many of those became clients. Use for 'which relationships turn into work', 'who sends me business', 'what do my best clients have in common'. NOT a route to one stranger, which is find_paths, and not a warmth ranking, which is who has gone quiet.",
 "inputSchema": {
  "properties": {
   "clients_only": {
    "description": "Only count introductions that became clients, judged on a billed or worked_on edge. Default false, which counts all.",
    "type": "boolean"
   },
   "limit": {
    "description": "Max introducers (default 20, max 100).",
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "referral_paths",
 "title": "Who introduced whom"
}
