{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Report the fmsg address this server acts as (from the authenticated connection), the fmsg Web API URL (null when the server does not publish it), when the current access token expires (it is renewed automatically; no action needed), and the address-resolution defaults. Call this first if unsure who you are sending as.",
 "inputSchema": {
  "properties": {},
  "type": "object"
 },
 "name": "whoami",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "address": {
    "type": "string"
   },
   "api_url": {
    "type": [
     "string",
     "null"
    ]
   },
   "default_domain": {
    "type": [
     "string",
     "null"
    ]
   },
   "directory_names": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "token_expires_at": {
    "type": [
     "string",
     "null"
    ]
   },
   "transport": {
    "enum": [
     "stdio",
     "http"
    ],
    "type": "string"
   }
  },
  "required": [
   "address",
   "api_url",
   "token_expires_at",
   "transport",
   "default_domain",
   "directory_names"
  ],
  "type": "object"
 },
 "title": "Show fmsg identity"
}
