{
 "annotations": {
  "readOnlyHint": false,
  "title": "Encrypt Data"
 },
 "description": "Encrypt text using ML-KEM-768 + AES-256-GCM (FIPS 203). Returns a HybridCryptoEnvelope with ciphertext and ML-DSA-65 signature. Store the full envelope to decrypt later.",
 "inputSchema": {
  "properties": {
   "algorithm": {
    "description": "KEM algorithm (default: ML-KEM-768)",
    "enum": [
     "ML-KEM-768",
     "ML-KEM-1024"
    ],
    "type": "string"
   },
   "plaintext": {
    "description": "Text to encrypt",
    "type": "string"
   }
  },
  "required": [
   "plaintext"
  ],
  "type": "object"
 },
 "name": "encrypt"
}
