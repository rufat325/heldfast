{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "For one product slug: every cited competitor row its comparison page shows, each with the price as the vendor publishes it, the monthly figure in integer cents where there is one, the vendor page it was read on and the date it was read. Returns an empty list and the reason when the product has no cited rows.",
 "inputSchema": {
  "properties": {
   "slug": {
    "description": "The product slug, e.g. \"jobora\".",
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "compare",
 "title": "Compare with the subscriptions"
}
