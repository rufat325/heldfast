{
 "description": "Cancel an order. Optionally restock inventory, refund payment, notify customer.",
 "inputSchema": {
  "properties": {
   "email": {
    "description": "Send cancellation email to customer",
    "type": "boolean"
   },
   "id": {
    "description": "Shopify order ID",
    "type": "string"
   },
   "reason": {
    "description": "Cancellation reason",
    "enum": [
     "customer",
     "fraud",
     "inventory",
     "declined",
     "other"
    ],
    "type": "string"
   },
   "refund": {
    "description": "Refund object to issue with the cancellation",
    "type": "object"
   },
   "restock": {
    "description": "Restock the line items (deprecated in newer versions; use refund)",
    "type": "boolean"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "cancel_order"
}
