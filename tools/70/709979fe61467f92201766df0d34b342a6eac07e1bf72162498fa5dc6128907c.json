{
 "description": "Report Code Engine apps that may be incurring avoidable cost \u2014 those with scale_min_instances > 0 (they never scale to zero). Optionally include per-app revision counts to spot cleanup candidates. Read-only; suggests actions but does not change anything.",
 "inputSchema": {
  "properties": {
   "include_revisions": {
    "description": "Also count revisions per app (extra API calls). Default false.",
    "type": "boolean"
   },
   "project_id": {
    "type": "string"
   }
  },
  "required": [
   "project_id"
  ],
  "type": "object"
 },
 "name": "ce_find_idle_apps"
}
