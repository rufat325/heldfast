{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List all Airtable bases the provided personal access token can access. Returns base IDs, names, and workspace info \u2014 use to discover baseId values needed by other airtable tools.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-airtable-api-key"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Airtable personal access token",
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "airtable_list_bases",
 "outputSchema": {
  "properties": {
   "bases": {
    "description": "List of accessible Airtable bases",
    "items": {
     "properties": {
      "id": {
       "description": "Base ID",
       "type": "string"
      },
      "name": {
       "description": "Base name",
       "type": "string"
      },
      "workspaceId": {
       "description": "Workspace ID this base belongs to",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "bases"
  ],
  "type": "object"
 },
 "title": "Airtable List Bases",
 "version": "v1.a32561a9"
}
