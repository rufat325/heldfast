{
 "description": "Retrieves the identity provider for the given Id or all the identity providers if the Id is null.",
 "inputSchema": {
  "properties": {
   "identityProviderId": {
    "description": "The identity provider Id.",
    "type": "string"
   }
  },
  "required": [
   "identityProviderId"
  ],
  "type": "object"
 },
 "name": "retrieveIdentityProviderWithId"
}
