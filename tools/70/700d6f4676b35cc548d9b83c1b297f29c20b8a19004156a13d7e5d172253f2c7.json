{
 "description": "Restore real secret values into an AI response that contains $CHRON_* tokens. Pass the token_map returned by scan_prompt.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "text": {
    "description": "AI response text containing $CHRON_* tokens",
    "type": "string"
   },
   "token_map": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "token_map from scan_prompt output",
    "type": "object"
   }
  },
  "required": [
   "text",
   "token_map"
  ],
  "type": "object"
 },
 "name": "rehydrate_response"
}
