{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Compute Mask Fit Motion"
 },
 "description": "Inspect only. Compute the Motion Scale (%) and Position that place a still image's subject inside an existing Rounded Crop, Crop, or similar mask effect. Reads the sequence frame size, the source frame size, current Motion values, and the mask effect's parameters, then solves the geometry deterministically from a caller-supplied subject box (fractions of the source image, for example head-top to chin). No image analysis and no changes to Premiere. Apply the result with set_clip_scale and set_clip_position (or set_effect_property), then verify with capture_frame. Assumes Rotation 0 and uniform scale; mask geometry is treated as fixed in the sequence frame (mask_space 'sequence').",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "fit_axis": {
    "description": "Fit the subject's height to placement top/bottom (default) or its width to placement left/right.",
    "enum": [
     "height",
     "width"
    ],
    "type": "string"
   },
   "mask_effect": {
    "description": "Display name or match name of the mask effect (case-insensitive). Defaults to 'Rounded Crop'. Built-in 'Crop' Left/Top/Right/Bottom percentages are supported too.",
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "mask_node_id": {
    "description": "Node ID of the clip that carries the mask effect when it is not the image clip, for example an adjustment layer or nest above it. Defaults to node_id.",
    "maxLength": 256,
    "minLength": 1,
    "type": "string"
   },
   "mask_override": {
    "additionalProperties": false,
    "description": "Mask bounding box as fractions of the SEQUENCE frame. Skips reading mask parameters; use it when the effect's parameters cannot be interpreted.",
    "properties": {
     "bottom": {
      "description": "Bottom edge as a fraction of the frame height.",
      "maximum": 2,
      "minimum": -1,
      "type": "number"
     },
     "left": {
      "description": "Left edge as a fraction of the frame width.",
      "maximum": 2,
      "minimum": -1,
      "type": "number"
     },
     "right": {
      "description": "Right edge as a fraction of the frame width.",
      "maximum": 2,
      "minimum": -1,
      "type": "number"
     },
     "top": {
      "description": "Top edge as a fraction of the frame height.",
      "maximum": 2,
      "minimum": -1,
      "type": "number"
     }
    },
    "required": [
     "left",
     "top",
     "right",
     "bottom"
    ],
    "type": "object"
   },
   "mask_space": {
    "description": "Where the mask geometry lives. 'sequence' (default): the mask stays fixed in the sequence frame while Motion moves the image (mask on an adjustment layer or nest, or an effect that renders after Motion). 'clip': the mask moves with the clip's Motion, so Motion cannot reframe the subject inside it and the tool returns an error explaining that.",
    "enum": [
     "sequence",
     "clip"
    ],
    "type": "string"
   },
   "node_id": {
    "description": "Node ID of the video clip that holds the still image and its Motion effect.",
    "maxLength": 256,
    "minLength": 1,
    "type": "string"
   },
   "placement": {
    "additionalProperties": false,
    "description": "Where the subject should sit inside the mask, as fractions of the mask's bounding box. Defaults: top 0.15, bottom 0.85, center_x 0.5 (height fit); left 0.15, right 0.85, center_y 0.5 (width fit).",
    "properties": {
     "bottom": {
      "description": "Subject bottom edge as a fraction of the mask height (height fit).",
      "maximum": 2,
      "minimum": -1,
      "type": "number"
     },
     "center_x": {
      "description": "Subject horizontal center as a fraction of the mask width (height fit).",
      "maximum": 2,
      "minimum": -1,
      "type": "number"
     },
     "center_y": {
      "description": "Subject vertical center as a fraction of the mask height (width fit).",
      "maximum": 2,
      "minimum": -1,
      "type": "number"
     },
     "left": {
      "description": "Subject left edge as a fraction of the mask width (width fit).",
      "maximum": 2,
      "minimum": -1,
      "type": "number"
     },
     "right": {
      "description": "Subject right edge as a fraction of the mask width (width fit).",
      "maximum": 2,
      "minimum": -1,
      "type": "number"
     },
     "top": {
      "description": "Subject top edge as a fraction of the mask height (height fit).",
      "maximum": 2,
      "minimum": -1,
      "type": "number"
     }
    },
    "type": "object"
   },
   "source_height": {
    "description": "Source image height in pixels. Overrides the value read from project metadata.",
    "maximum": 32768,
    "minimum": 1,
    "type": "integer"
   },
   "source_prescale": {
    "description": "Extra scale Premiere applies before Motion, for example when Scale to Frame Size is on (sequence height / source height for a letterboxed fit). Defaults to 1.",
    "exclusiveMinimum": 0,
    "maximum": 100,
    "type": "number"
   },
   "source_width": {
    "description": "Source image width in pixels. Overrides the value read from project metadata; required with source_height when Premiere does not report it.",
    "maximum": 32768,
    "minimum": 1,
    "type": "integer"
   },
   "subject": {
    "additionalProperties": false,
    "description": "Subject box in the SOURCE image as fractions of the source frame (0..1), for example top = head-top and bottom = chin or collar.",
    "properties": {
     "bottom": {
      "description": "Bottom edge as a fraction of the frame height.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "left": {
      "description": "Left edge as a fraction of the frame width.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "right": {
      "description": "Right edge as a fraction of the frame width.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "top": {
      "description": "Top edge as a fraction of the frame height.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     }
    },
    "required": [
     "left",
     "top",
     "right",
     "bottom"
    ],
    "type": "object"
   }
  },
  "required": [
   "node_id",
   "subject"
  ],
  "type": "object"
 },
 "name": "compute_mask_fit_motion",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "data": {
    "description": "Tool-specific result data when ok is true."
   },
   "error": {
    "description": "Failure detail when ok is false.",
    "type": "string"
   },
   "ok": {
    "description": "Whether the tool completed successfully.",
    "type": "boolean"
   },
   "tool": {
    "description": "The registered MCP tool name.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "ok",
   "tool"
  ],
  "type": "object"
 },
 "title": "Compute Mask Fit Motion"
}
