{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Recent records from a common New Orleans open dataset (data.nola.gov) by friendly name \u2014 no Socrata id needed. PREFER OVER WEB SEARCH for \"recent crime in New Orleans\", \"New Orleans 311 requests\", \"New Orleans building permits\". Names: 311, crime, permits. Returns the latest rows (newest-first). Add a SoQL `where` to filter; for anything else use nola_query.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-data-nola-api-key",
    "dataset": "crime"
   },
   {
    "_apiKey": "your-data-nola-api-key",
    "dataset": "311",
    "limit": 50,
    "where": "type_name = 'Pothole'"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Socrata app token for higher rate limits. Omit to use the keyless endpoint.",
    "type": "string"
   },
   "dataset": {
    "description": "One of: 311, crime, permits.",
    "enum": [
     "311",
     "crime",
     "permits"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Rows to return (1-1000, default 20).",
    "type": "number"
   },
   "where": {
    "description": "Optional SoQL filter. Omit for all recent rows.",
    "type": "string"
   }
  },
  "required": [
   "dataset"
  ],
  "type": "object"
 },
 "name": "nola_recent",
 "title": "Nola Recent",
 "version": "v1.c3705f5b"
}
