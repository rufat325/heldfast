{
 "description": "Create an X-Matrix element, or update one when element_id is given. Quadrants: north = 3-5 year vision, east = annual objectives, south = initiatives, west = KPIs. A west element should carry indicator_type so the completeness score can judge the leading/lagging balance.",
 "inputSchema": {
  "properties": {
   "current_value": {
    "description": "Where it stands today, e.g. \"31\"",
    "type": "string"
   },
   "description": {
    "description": "Longer description, rationale, scope",
    "type": "string"
   },
   "element_id": {
    "description": "UUID of an existing element to update. Omit to create a new one.",
    "type": "string"
   },
   "external_ref": {
    "description": "URL or key of the project implementing this in your execution tool (Linear, Jira, GitHub)",
    "type": "string"
   },
   "indicator_type": {
    "description": "For west elements only: result = lagging (measures outcomes), performance = leading (measures the process producing them). Every objective should be tracked by at least one of each.",
    "enum": [
     "result",
     "performance"
    ],
    "type": "string"
   },
   "matrix_id": {
    "description": "UUID of the matrix (default: the organization default matrix)",
    "type": "string"
   },
   "quadrant": {
    "description": "Quadrant of the element (required when creating)",
    "enum": [
     "north",
     "east",
     "south",
     "west"
    ],
    "type": "string"
   },
   "status": {
    "description": "Status of the element (default: active)",
    "enum": [
     "draft",
     "active",
     "completed",
     "cancelled"
    ],
    "type": "string"
   },
   "target_value": {
    "description": "Target to reach, e.g. \"38\"",
    "type": "string"
   },
   "title": {
    "description": "Title of the element (required when creating)",
    "type": "string"
   },
   "unit": {
    "description": "Unit of the values, e.g. \"%\", \"k\u20ac\", \"days\"",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "upsert_element"
}
