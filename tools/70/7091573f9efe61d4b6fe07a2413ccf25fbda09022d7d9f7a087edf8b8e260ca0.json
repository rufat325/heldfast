{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Read multiple known node ids in one call. Prefer over repeated get_node. Large output may be spilled.",
 "inputSchema": {
  "properties": {
   "channel": {
    "description": "Optional. Target a specific connected file by its channel id...",
    "type": "string"
   },
   "depth": {
    "description": "How many levels deep to traverse per node (default 50,...",
    "type": "number"
   },
   "nodeIds": {
    "description": "List of node IDs in colon format e.g. ['4029:12345',...",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "origin": {
    "description": "Origin: orchestrator/self=wolfgang; workers use assigned name....",
    "enum": [
     "wolfgang",
     "grace",
     "theo",
     "sunho",
     "zoe",
     "taewon",
     "emma",
     "alex",
     "rick"
    ],
    "type": "string"
   },
   "skipInvisibleInstanceChildren": {
    "description": "Optional. true = skip hidden instances' children during...",
    "type": "boolean"
   }
  },
  "required": [
   "nodeIds",
   "origin"
  ],
  "type": "object"
 },
 "name": "get_nodes_info"
}
