{
 "description": "Rasterize a local PDF to PNG or JPEG images, one file per page, written to a directory.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dpi": {
    "description": "Dots per inch (default 150)",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "format": {
    "enum": [
     "png",
     "jpeg"
    ],
    "type": "string"
   },
   "input": {
    "type": "string"
   },
   "outputDir": {
    "description": "Directory to write the images into",
    "type": "string"
   },
   "pages": {
    "description": "1-based page numbers; omit to render all",
    "items": {
     "type": "integer"
    },
    "type": "array"
   }
  },
  "required": [
   "input",
   "outputDir"
  ],
  "type": "object"
 },
 "name": "pdf_to_image",
 "title": "Render PDF pages to images (local, private)"
}
