{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the full Eurostat table of contents (~10,000 datasets and tables) by keyword. Returns dataset codes, titles, last-update dates, and the time range covered; pass a returned code to get_dataset for the numbers. Example: search_datasets({ query: \"unemployment rate\" })",
 "inputSchema": {
  "examples": [
   {
    "query": "unemployment rate"
   },
   {
    "query": "inflation"
   }
  ],
  "properties": {
   "query": {
    "description": "Search keyword, e.g. \"gdp\", \"unemployment\", \"inflation\", \"population\"",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_datasets",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of datasets found (up to 30)",
    "type": "number"
   },
   "datasets": {
    "description": "List of matching datasets",
    "items": {
     "properties": {
      "code": {
       "description": "Dataset code identifier",
       "type": "string"
      },
      "description": {
       "description": "Short description (truncated to 200 chars)",
       "type": [
        "string",
        "null"
       ]
      },
      "last_update": {
       "description": "Last update date",
       "type": [
        "string",
        "null"
       ]
      },
      "title": {
       "description": "Dataset title",
       "type": "string"
      }
     },
     "required": [
      "code",
      "title"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "count",
   "datasets"
  ],
  "type": "object"
 },
 "title": "Search Datasets",
 "version": "v1.f5216e5c"
}
