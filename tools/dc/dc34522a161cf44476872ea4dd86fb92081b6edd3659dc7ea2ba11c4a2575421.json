{
 "annotations": {
  "audience": [
   "agent",
   "user"
  ],
  "destructiveHint": false,
  "priority": 0.75,
  "readOnlyHint": true
 },
 "description": "Diagnose SRIFT on this machine: connectivity (DNS, proxy, HTTPS/TLS, WebSocket relay, UDP/P2P, peer discovery, clock), the local runtime (loopback, background processes, daemon + version, disk) and the environment (sandbox/CI/container detection, curl). Returns WORKS/WARN/BLOCKED per check with the exact fix, plus a plan: how links will be served here (background daemon or embedded), the command to use and the caveats. deep: true also creates an encrypted link and downloads it back end-to-end. Read-only apart from that temporary self-test link. Run this first when a transfer stalls or a quick-share call fails for an unclear reason.",
 "inputSchema": {
  "properties": {
   "deep": {
    "description": "Also run an end-to-end self-test: create an encrypted single-use link and download it back through the relay (\u22482 MB, a few seconds).",
    "type": "boolean"
   },
   "fresh": {
    "description": "Force a fresh probe instead of returning a cached result (slower, more accurate).",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "srift_net_diagnose",
 "outputSchema": {
  "properties": {
   "base": {
    "description": "The SRIFT server that was probed",
    "type": "string"
   },
   "checkedAt": {
    "description": "When the probe ran (ISO 8601); cached results show an older time",
    "type": "string"
   },
   "context": {
    "description": "runtime (mcp, cli, npx, binary, pypi\u2026), cliVersion, serverVersion, daemonVersion",
    "type": "object"
   },
   "env": {
    "description": "Relevant environment (proxy vars, sandbox indicators, Node version, etc.)",
    "type": "object"
   },
   "ok": {
    "description": "True if the diagnostic ran successfully (independent of what it found)",
    "type": "boolean"
   },
   "plan": {
    "description": "serveFrom (background-daemon | embedded | unavailable), transport, shareCommand, downloadCommand, advice[] \u2014 what to do in this environment",
    "type": "object"
   },
   "recommendedMode": {
    "description": "\"relay\" when links can be served from here, otherwise \"none\"",
    "type": "string"
   },
   "rungs": {
    "description": "Each transport rung checked, in order, with status and a fix hint if blocked",
    "items": {
     "properties": {
      "detail": {
       "type": "string"
      },
      "fix": {
       "type": "string"
      },
      "group": {
       "description": "\"connectivity\", \"runtime\" or \"environment\"",
       "type": "string"
      },
      "id": {
       "type": "string"
      },
      "name": {
       "type": "string"
      },
      "status": {
       "description": "\"WORKS\", \"WARN\" (works with a caveat), \"BLOCKED\", or \"SKIPPED\"",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "selfTest": {
    "description": "deep only: { ok, ms, bytes, mbps, via, error? }",
    "type": "object"
   },
   "summary": {
    "description": "One-line conclusion",
    "type": "string"
   },
   "verdict": {
    "description": "\"ok\", \"degraded\", or \"blocked\"",
    "type": "string"
   }
  },
  "type": "object"
 },
 "title": "Diagnose Network Transports"
}
