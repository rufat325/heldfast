{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Answer a yes-or-no question about a hotel from what its guests said, such as \"is it quiet at night?\" or \"is the breakfast good?\". Ask one thing per call, and turn an open question into a yes-or-no one (\"what is the breakfast like?\" becomes \"is the breakfast good?\"). Pass the hotel id from search_hotels results. Returns how many guest review summaries say yes, no or mixed, with a few of them. Report it as what guests found, with the counts. When enough_evidence is false, say few guests mentioned it rather than drawing a conclusion. It knows only what guests reported, not the hotel's facilities or policies.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "context": {
    "description": "Why are you calling this tool? Describe the user's underlying goal in one short sentence \u2014 not the tool name. Never include names, emails, phones, or prices. Always pass this. Used for analytics only; never shown to the user.",
    "maxLength": 200,
    "type": "string"
   },
   "id": {
    "description": "The hotel id from search_hotels results",
    "type": "string"
   },
   "question": {
    "description": "One yes-or-no question about the hotel, such as \"is it quiet at night?\"",
    "maxLength": 200,
    "pattern": "\\S",
    "type": "string"
   }
  },
  "required": [
   "id",
   "question"
  ],
  "type": "object"
 },
 "name": "ask_about_hotel",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "enough_evidence": {
    "description": "false when too few guests mentioned it to draw a conclusion",
    "type": "boolean"
   },
   "mixed": {
    "properties": {
     "count": {
      "type": "integer"
     },
     "sentiments": {
      "items": {
       "type": "string"
      },
      "maxItems": 5,
      "type": "array"
     }
    },
    "required": [
     "count",
     "sentiments"
    ],
    "type": "object"
   },
   "name": {
    "type": "string"
   },
   "no": {
    "properties": {
     "count": {
      "type": "integer"
     },
     "sentiments": {
      "items": {
       "type": "string"
      },
      "maxItems": 5,
      "type": "array"
     }
    },
    "required": [
     "count",
     "sentiments"
    ],
    "type": "object"
   },
   "question": {
    "type": "string"
   },
   "summaries_read": {
    "description": "Guest review summaries read to answer",
    "type": "integer"
   },
   "yes": {
    "properties": {
     "count": {
      "type": "integer"
     },
     "sentiments": {
      "items": {
       "type": "string"
      },
      "maxItems": 5,
      "type": "array"
     }
    },
    "required": [
     "count",
     "sentiments"
    ],
    "type": "object"
   }
  },
  "required": [
   "name",
   "question",
   "summaries_read",
   "yes",
   "no",
   "mixed",
   "enough_evidence"
  ],
  "type": "object"
 },
 "title": "Ask About Hotel"
}
