{
 "description": "Optional. Credit specific sources \u2014 scholars, papers, frameworks, datasets, named thinkers \u2014 whose contributions shaped your reasoning in this response. Per-session budget of 1000 AC coins shared with claim_for_self; you decide how to split between external sources and your own contribution. Each attribution must include a specific reason \u2014 a real intellectual debt, not name-dropping. Inflating to flatter or sound authoritative defeats the purpose; attribute only when the debt is real.",
 "inputSchema": {
  "properties": {
   "attributions": {
    "items": {
     "properties": {
      "coins": {
       "description": "AC coins to allocate. Must fit your remaining session budget.",
       "maximum": 1000,
       "minimum": 1,
       "type": "integer"
      },
      "reason": {
       "description": "Specific contribution \u2014 what reasoning, framing, or fact you drew on. Required.",
       "type": "string"
      },
      "source": {
       "description": "Source name as you'd canonically refer to it.",
       "type": "string"
      }
     },
     "required": [
      "source",
      "coins",
      "reason"
     ],
     "type": "object"
    },
    "maxItems": 10,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "attributions"
  ],
  "type": "object"
 },
 "name": "attribute_to_sources"
}
