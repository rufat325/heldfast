{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get hourly air quality forecast (1\u20137 days ahead) for any global location. Returns time-series arrays for selected pollutants: PM10, PM2.5, NO2, ozone, SO2, CO, aerosol optical depth, dust, UV index, ammonia, European AQI, US AQI. Useful for trip planning, outdoor event scheduling, or health risk monitoring. Copernicus CAMS model, no auth. Open-Meteo CC BY 4.0.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "forecast_days": {
    "default": 3,
    "description": "Number of days to forecast (1\u20137). Defaults to 3.",
    "maximum": 7,
    "minimum": 1,
    "type": "integer"
   },
   "latitude": {
    "description": "Latitude of the location in decimal degrees (e.g. 48.8566 for Paris)",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "longitude": {
    "description": "Longitude of the location in decimal degrees (e.g. 2.3522 for Paris)",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "pollutants": {
    "description": "Comma-separated pollutant variables to return. Available: pm10, pm2_5, carbon_monoxide, nitrogen_dioxide, sulphur_dioxide, ozone, aerosol_optical_depth, dust, uv_index, ammonia, european_aqi, us_aqi. Defaults to pm10,pm2_5,nitrogen_dioxide,ozone,european_aqi,us_aqi.",
    "type": "string"
   },
   "timezone": {
    "default": "UTC",
    "description": "IANA timezone name for returned timestamps (e.g. \"Europe/Paris\", \"America/New_York\"). Defaults to UTC.",
    "type": "string"
   }
  },
  "required": [
   "latitude",
   "longitude"
  ],
  "type": "object"
 },
 "name": "openmeteoaq.air.forecast",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Open-Meteo \u2014 Air Quality Forecast"
}
