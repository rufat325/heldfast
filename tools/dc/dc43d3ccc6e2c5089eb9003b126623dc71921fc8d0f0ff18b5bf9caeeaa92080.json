{
 "_meta": {
  "xdata.kr/credential": {
   "providers": [
    "hometax"
   ],
   "register_url": "https://console.xdata.kr/credentials?link=1&priority=hometax",
   "required": true,
   "windows_pc_needed": true
  }
 },
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "REQUIRES prior hometax__session__login call with same account_link_id. Search e-tax invoices by date range. Use from_date/to_date (YYYYMMDD or YYYY-MM-DD). prh_sls_cl_cd: '01'=issued(\ub9e4\ucd9c), '02'=received(\ub9e4\uc785). \uad6d\uc138\uccad rejects long ranges with '6\uac1c\uc6d4\uc774\uc0c1\uc740 \uc870\ud68c\ud560 \uc218 \uc5c6\uc2b5\ub2c8\ub2e4.' \u2014 keep the window under 6 months and split longer periods into several calls.",
 "inputSchema": {
  "properties": {
   "account_link_id": {
    "description": "Account link ID (UUID v4). Optional \u2014 the server auto-resolves from your API key when omitted. Provide explicitly only when you have multiple certificates for the same provider. Each institution has a separate ID (Hometax != NHIS != NPS != FourInsure).",
    "type": "string"
   },
   "from_date": {
    "description": "Start date YYYYMMDD or YYYY-MM-DD (e.g. 20260101)",
    "type": "string"
   },
   "page_num": {
    "description": "Page number (default 1)",
    "type": "integer"
   },
   "page_size": {
    "description": "Page size (default 10)",
    "type": "integer"
   },
   "prh_sls_cl_cd": {
    "description": "Invoice direction: '01'=issued(\ub9e4\ucd9c), '02'=received(\ub9e4\uc785). Default '01'.",
    "enum": [
     "01",
     "02"
    ],
    "type": "string"
   },
   "to_date": {
    "description": "End date YYYYMMDD or YYYY-MM-DD (e.g. 20260131)",
    "type": "string"
   }
  },
  "required": [
   "from_date",
   "to_date"
  ],
  "type": "object"
 },
 "name": "hometax__etax__invoice__search_list",
 "securitySchemes": [
  {
   "scopes": [
    "mcp:tools:list",
    "mcp:tools:call"
   ],
   "type": "oauth2"
  }
 ],
 "title": "\uc804\uc790\uc138\uae08\uacc4\uc0b0\uc11c \ubaa9\ub85d \uc870\ud68c"
}
