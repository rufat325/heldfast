{
 "description": "\u26a0 COSTS LLM CREDITS \u2014 same path as POST /api/chat. Router V5 classifies the message: Ask Clarity (persisted), Create Agent (spawns Aurora research), or single-tool fulfill (queued prompt job). Use this for chat turns; use create_agent only when you explicitly want agent spawn without single-tool fulfill. Aurora chooses the complete route adaptively.",
 "inputSchema": {
  "properties": {
   "attachment_ids": {
    "description": "READY chat-attachment ids from upload_chat_attachment. Bound onto the last user message as fileAttachments.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "conversationId": {
    "description": "Optional NexusGenAI conversation id",
    "type": "string"
   },
   "messages": {
    "description": "Full or partial ChatMessage array, e.g. [{\"sender\":\"User\",\"content\":\"...\"}]",
    "items": {
     "properties": {
      "content": {
       "type": "string"
      },
      "data": {
       "type": "object"
      },
      "sender": {
       "type": "string"
      },
      "timestamp": {
       "type": "string"
      }
     },
     "required": [
      "sender",
      "content"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "session_depth": {
    "description": "Deprecated compatibility field. Both values use Aurora's adaptive routing policy.",
    "enum": [
     "smart",
     "deep"
    ],
    "type": "string"
   }
  },
  "required": [
   "messages"
  ],
  "type": "object"
 },
 "name": "send_chat_message"
}
