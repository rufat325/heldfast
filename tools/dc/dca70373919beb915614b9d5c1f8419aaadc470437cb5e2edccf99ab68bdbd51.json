{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Checks the status of a stored transaction request. Returns one of: pending (user hasn't approved yet), approved (with tx hash), rejected, or expired.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "request_id": {
    "description": "The stored-request ID returned by a write tool.",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "request_id"
  ],
  "type": "object"
 },
 "name": "poll_request",
 "title": "Poll a pending approval request"
}
