{
 "annotations": {
  "destructiveHint": false
 },
 "description": "Open the connected app's system settings page using React Native Linking.openSettings().",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "bundleId": {
    "description": "Bundle ID (iOS) or package name (Android). If supplied, it must match the connected app.",
    "type": "string"
   },
   "platform": {
    "default": "auto",
    "description": "Target platform",
    "enum": [
     "ios",
     "android",
     "auto"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "open_app_settings"
}
