{
 "description": "Apply network throttling: bandwidth limits, latency, and packet loss. Presets: 3G, LTE, Edge, WiFi, WiFi-lossy, 100%-loss (offline). Simulator shares host network stack, so conditioning affects all simulator traffic.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "\"set\" to apply conditioning, \"clear\" to remove, \"list-presets\" to show available presets",
    "enum": [
     "set",
     "clear",
     "list-presets"
    ],
    "type": "string"
   },
   "bandwidth": {
    "description": "Custom bandwidth (e.g., \"1Mbit/s\", \"500Kbit/s\"). Required when preset is \"custom\"",
    "type": "string"
   },
   "delay": {
    "description": "Custom latency in milliseconds. Required when preset is \"custom\"",
    "type": "number"
   },
   "deviceId": {
    "description": "Device (default: booted)",
    "type": "string"
   },
   "packetLossRate": {
    "description": "Custom packet loss rate 0.0-1.0 (e.g., 0.05 = 5%). Required when preset is \"custom\"",
    "type": "number"
   },
   "preset": {
    "description": "Network preset name, or \"custom\" to specify parameters manually",
    "enum": [
     "3G",
     "LTE",
     "WiFi-lossy",
     "100%-loss",
     "Edge",
     "WiFi",
     "custom"
    ],
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "simulator_network_condition"
}
