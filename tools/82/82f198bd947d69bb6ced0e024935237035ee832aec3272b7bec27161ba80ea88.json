{
 "description": "Create a Splitwise expense. Use split_equally:true to split evenly among group members, or provide a users array for custom per-person splits (paid_share and owed_share as decimal strings like \"25.00\"). cost must be a decimal string. Asks the user to confirm first: a confirmation prompt where the client supports one; otherwise the first call returns a preview and a confirmToken, and only a repeat call with that token proceeds (see MCP_CONFIRM_MODE).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "category_id": {
    "description": "Category id from sw_get_categories",
    "type": "number"
   },
   "confirmToken": {
    "description": "ONLY for the two-step confirmation fallback (a client without MCP elicitation). The confirmToken from this same tool's phase-1 \"confirmation-required\" response, passed back ONLY after the user has seen that preview and explicitly approved it in chat \u2014 never on the first call, never invented, never reused. Call again with the same arguments. Ignored when the client supports elicitation.",
    "type": "string"
   },
   "cost": {
    "description": "Total cost as decimal string, e.g. \"25.00\"",
    "type": "string"
   },
   "currency_code": {
    "description": "Currency code, e.g. \"USD\". Defaults to group/user default.",
    "type": "string"
   },
   "date": {
    "description": "ISO 8601 datetime",
    "type": "string"
   },
   "description": {
    "description": "Short description of the expense",
    "type": "string"
   },
   "details": {
    "description": "Notes",
    "type": "string"
   },
   "group_id": {
    "description": "Group to add expense to (use 0 for no group)",
    "type": "number"
   },
   "split_equally": {
    "description": "Split equally among group members (mutually exclusive with users)",
    "type": "boolean"
   },
   "users": {
    "description": "Custom split (mutually exclusive with split_equally). Full list of participants required.",
    "items": {
     "properties": {
      "owed_share": {
       "description": "Amount this user owes, e.g. \"12.50\"",
       "type": "string"
      },
      "paid_share": {
       "description": "Amount this user paid, e.g. \"25.00\"",
       "type": "string"
      },
      "user_id": {
       "type": "number"
      }
     },
     "required": [
      "user_id",
      "paid_share",
      "owed_share"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "group_id",
   "description",
   "cost"
  ],
  "type": "object"
 },
 "name": "sw_create_expense"
}
