{
 "description": "Track one obligation's lifecycle (status / owner / recurrence / reminder days). Writes to the register.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "deal": {
    "description": "Deal id.",
    "type": "string"
   },
   "id": {
    "description": "Obligation id.",
    "type": "string"
   },
   "owner": {
    "description": "Assign an owner.",
    "type": "string"
   },
   "recurrence": {
    "description": "Recurrence.",
    "enum": [
     "none",
     "weekly",
     "monthly",
     "quarterly",
     "semiannual",
     "annual"
    ],
    "type": "string"
   },
   "reminders": {
    "description": "Reminder lead days, e.g. 30.",
    "type": "string"
   },
   "status": {
    "description": "New status.",
    "enum": [
     "open",
     "done",
     "waived"
    ],
    "type": "string"
   }
  },
  "required": [
   "deal",
   "id"
  ],
  "type": "object"
 },
 "name": "contract_vault_obligation"
}
