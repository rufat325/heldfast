{
 "annotations": {
  "readOnlyHint": true,
  "title": "list_records"
 },
 "description": "List records (events applied to animals or groups, e.g. feed/health/movement) on the farm.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "skip": {
    "description": "Records to skip (pagination).",
    "minimum": 0,
    "type": "integer"
   },
   "take": {
    "description": "Max records to return.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "type": {
    "description": "Filter by record type.",
    "enum": [
     "FEED",
     "GENETIC",
     "HEALTH",
     "MOVEMENT",
     "OTHER"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_records"
}
