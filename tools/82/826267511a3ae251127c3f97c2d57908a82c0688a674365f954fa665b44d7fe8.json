{
 "description": "Create a new site to track. A site represents a domain you want to manage content and SEO for.",
 "inputSchema": {
  "properties": {
   "description": {
    "type": "string"
   },
   "domain": {
    "description": "e.g. example.com (protocol and trailing slashes are stripped)",
    "type": "string"
   },
   "name": {
    "description": "Display name",
    "type": "string"
   }
  },
  "required": [
   "name",
   "domain"
  ],
  "type": "object"
 },
 "name": "create_site"
}
