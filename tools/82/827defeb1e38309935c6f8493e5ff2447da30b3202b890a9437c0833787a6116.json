{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Find books commonly co-shelved with a specific book (Shelfology)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "book_id": {
    "description": "The book ID to find pairings for",
    "type": "string"
   },
   "limit": {
    "default": 10,
    "description": "Maximum results (default 10)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   }
  },
  "required": [
   "book_id"
  ],
  "type": "object"
 },
 "name": "get_book_pairings"
}
