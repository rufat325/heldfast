{
 "annotations": {
  "cost": {
   "all": 2
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Full-text search Hacker News via Algolia. Pass query plus optional tags filter (story, comment, ask_hn, show_hn; default: story). Returns title, URL, score, author, comment count, and timestamp.",
 "inputSchema": {
  "examples": [
   {
    "query": "artificial intelligence"
   },
   {
    "per_page": 20,
    "query": "startup funding",
    "tags": "story"
   }
  ],
  "properties": {
   "per_page": {
    "description": "Number of results to return (default: 10)",
    "type": "number"
   },
   "query": {
    "description": "Search query string",
    "type": "string"
   },
   "tags": {
    "description": "Content type filter: story, comment, ask_hn, or show_hn (default: story)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_hn",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of results returned",
    "type": "number"
   },
   "query": {
    "description": "The search query string used",
    "type": "string"
   },
   "results": {
    "description": "Array of search result items",
    "items": {
     "properties": {
      "author": {
       "description": "Author username",
       "type": [
        "string",
        "null"
       ]
      },
      "created_at": {
       "description": "Creation timestamp",
       "type": [
        "string",
        "null"
       ]
      },
      "num_comments": {
       "description": "Number of comments",
       "type": [
        "number",
        "null"
       ]
      },
      "objectID": {
       "description": "Unique identifier for the HN item",
       "type": "string"
      },
      "points": {
       "description": "Story points/score",
       "type": [
        "number",
        "null"
       ]
      },
      "title": {
       "description": "Story title",
       "type": [
        "string",
        "null"
       ]
      },
      "url": {
       "description": "Story URL",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "required": [
      "objectID"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "tags": {
    "description": "Content type filter applied",
    "type": "string"
   }
  },
  "required": [
   "query",
   "tags",
   "count",
   "results"
  ],
  "type": "object"
 },
 "title": "Search Hn",
 "version": "v1.5e1a9b24"
}
