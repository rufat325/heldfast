{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Create segment"
 },
 "description": "Create and persist a new audience segment from a filter definition. SEMANTICS: criteria in the same stanza are OR-combined \u2014 a fan matches the stanza if ANY criterion matches. Stanzas are AND-combined \u2014 a fan must match EVERY stanza. To require A AND B, put them in separate stanzas; to accept A OR B, put both in the same stanza. PREREQUISITE: call nevent_segmentation_criteria first to discover valid criterion_ids and operators. MANDATORY RULES: (1) ENTITY operators (is/is_not) accept a single string OR an array of strings \u2014 e.g. value: \"EVENT_ID\" or value: [\"E1\",\"E2\"]. (2) Omit modifiers unless the user explicitly requests frequency/recency filtering; if included, time_range.value MUST be > 0. (3) Criteria fields: only criterion_id, operator, value \u2014 omit id, timeframe, type. (4) KNOWN LIMITATION: do NOT mix attendance criteria (attended_event, ticket_type) with spending criteria (total_spent, ticket_spent, cashless_recharge_amount) in the same stanza \u2014 put them in separate stanzas. Separate stanzas are AND-combined, so a fan must match both. After creation, call nevent_segment_preview to count the audience, then nevent_create_campaign to send to this segment.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "definition": {
    "additionalProperties": false,
    "description": "Segment DSL. Criteria in the same stanza are OR-combined: a fan matches the stanza if ANY criterion matches. Stanzas are AND-combined: a fan must match EVERY stanza. To require A AND B, put them in separate stanzas; to accept A OR B, put both in the same stanza. Must have at least one stanza with at least one criterion. Use nevent_segment_preview first to validate the definition.",
    "properties": {
     "stanzas": {
      "description": "Array of stanzas. Stanzas are AND-combined: a fan must match EVERY stanza. Criteria inside one stanza are OR-combined, so a fan matching ANY of them matches that stanza.",
      "items": {
       "additionalProperties": false,
       "properties": {
        "criteria": {
         "description": "Criteria in the same stanza are OR-combined: a fan matches the stanza if ANY criterion matches. Stanzas are AND-combined, so a fan must match EVERY stanza.",
         "items": {
          "additionalProperties": false,
          "properties": {
           "criterion_id": {
            "description": "Criterion identifier from nevent_segmentation_criteria. Examples: total_spent, attended_event, user_gender, user_age, campaign_opened, nevent_temperature",
            "type": "string"
           },
           "filters": {
            "additionalProperties": {},
            "description": "Only needed for user_custom_field criterion. Pass { \"property_name\": \"field_name\" } to specify which custom field.",
            "type": "object"
           },
           "id": {
            "description": "Optional stable ID for UI/cross-request correlation. If omitted, the MCP client auto-generates one before sending to the API.",
            "type": "string"
           },
           "modifiers": {
            "additionalProperties": false,
            "description": "ADVANCED: Usually OMIT this field entirely. Only include when the user explicitly asks for:\n- \"at least X times\" \u2192 { frequency: { count: X, operator: \"gte\" } }\n- \"in the last X days\" \u2192 { time_range: { value: X, unit: \"days\" } } (value MUST be > 0; unit is one of days, weeks, months, years)\nIf not asked for frequency or recency, DO NOT include modifiers.",
            "properties": {
             "frequency": {
              "additionalProperties": false,
              "properties": {
               "count": {
                "type": "number"
               },
               "operator": {
                "type": "string"
               }
              },
              "required": [
               "count",
               "operator"
              ],
              "type": "object"
             },
             "time_range": {
              "additionalProperties": false,
              "properties": {
               "unit": {
                "type": "string"
               },
               "value": {
                "minimum": 1,
                "type": "number"
               }
              },
              "required": [
               "value",
               "unit"
              ],
              "type": "object"
             }
            },
            "type": "object"
           },
           "operator": {
            "description": "Comparison operator. Rules by data type:\n- ENTITY: \"is\" and \"is_not\" accept a single string ID OR an array of string IDs (valueType=array per API). \"is_set\", \"is_not_set\" also supported.\n- TEXT: \"eq\", \"neq\", \"contains\", \"starts_with\", \"ends_with\", \"is_set\", \"is_not_set\"\n- NUMBER/CURRENCY: \"eq\", \"neq\", \"gt\", \"gte\", \"lt\", \"lte\", \"is_set\", \"is_not_set\"\n- BOOLEAN: \"is_true\", \"is_false\". NEVER use \"eq\" with \"true\"/\"false\" strings.\n- DATE: \"before\", \"after\", \"between\", \"eq\", \"is_set\", \"is_not_set\"",
            "type": "string"
           },
           "value": {
            "description": "Value to match. IMPORTANT rules:\n- ENTITY operators (is/is_not): accepts a single string ID OR an array of string IDs (e.g. \"event_123\" or [\"event_1\", \"event_2\"]).\n- NUMBER: must be a number (not string)\n- BOOLEAN operators (is_true/is_false): no value needed, omit this field.\n- DATE \"between\": array of 2 ISO date strings [start, end]"
           }
          },
          "required": [
           "criterion_id",
           "operator"
          ],
          "type": "object"
         },
         "minItems": 1,
         "type": "array"
        },
        "id": {
         "description": "Optional stable ID for the stanza. If omitted, the MCP client auto-generates one before sending to the API. Provide your own only if you need stable references across requests.",
         "type": "string"
        }
       },
       "required": [
        "criteria"
       ],
       "type": "object"
      },
      "minItems": 1,
      "type": "array"
     }
    },
    "required": [
     "stanzas"
    ],
    "type": "object"
   },
   "description": {
    "description": "Optional description of the segment's purpose",
    "type": "string"
   },
   "name": {
    "description": "Human-readable name for the segment, e.g. \"VIP Attendees 2025\"",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "name",
   "definition"
  ],
  "type": "object"
 },
 "name": "nevent_create_segment"
}
