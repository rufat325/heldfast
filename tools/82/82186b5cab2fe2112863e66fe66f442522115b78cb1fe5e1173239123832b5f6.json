{
 "_meta": {
  "fastmcp": {
   "tags": []
  }
 },
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "title": "Invoke MTProto"
 },
 "description": "Low-level Telegram API (MTProto) invoke for methods not wrapped by other tools. Dangerous methods require allow_dangerous=true. Success: API result dict or normalized error. PII and credential-shaped fields (phone, access_hash) are dropped from a successful result by default; pass include_sensitive=true for the raw payload. A bare message id needs a chat binding: requests with no peer field (messages.GetMessages, messages.DeleteMessages) are refused, because a bare id resolves against an arbitrary dialog. Use channels.GetMessages or messages.GetHistory, which carry the binding. messages.GetHistory cannot address a forum topic (no thread_id/top_msg_id in the schema, and channels.GetHistory does not exist) -- use messages.Search with top_msg_id, or the high-level get_messages with reply_to_id.  Full documentation: https://github.com/leshchenko1979/fast-mcp-telegram/blob/main/docs/Tools-Reference.md",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "allow_dangerous": {
    "default": false,
    "description": "If false, destructive methods (e.g. deletes) are blocked. Set true only when intended.",
    "type": "boolean"
   },
   "include_sensitive": {
    "default": false,
    "description": "If true, return the raw result including PII and credential-shaped fields (phone, access_hash). Default false drops them.",
    "type": "boolean"
   },
   "method_full_name": {
    "description": "Telegram API method, e.g. \"messages.GetHistory\" or \"users.GetFullUser\" (normalization applied).",
    "type": "string"
   },
   "params_json": {
    "description": "JSON object string of TL parameters as in Telegram API docs; nested TL uses \"_\": \"typeName\" discriminator.",
    "type": "string"
   },
   "resolve": {
    "default": true,
    "description": "If true, resolve string/int peer-like fields to TL Input* entities before invoke.",
    "type": "boolean"
   }
  },
  "required": [
   "method_full_name",
   "params_json"
  ],
  "type": "object"
 },
 "name": "invoke_mtproto",
 "outputSchema": {
  "description": "Return type for ``invoke_mtproto``.\n\nThe success payload is a JSON-safe dict whose shape depends on the\nTelegram API method invoked.  Common top-level fields are listed here;\neverything else passes through as-is.",
  "properties": {
   "_": {
    "type": "string"
   },
   "action": {
    "type": "string"
   },
   "chats": {
    "items": {
     "additionalProperties": true,
     "type": "object"
    },
    "type": "array"
   },
   "code": {
    "type": "integer"
   },
   "date": {
    "type": "integer"
   },
   "error": {
    "type": "string"
   },
   "error_code": {
    "type": "string"
   },
   "exception": {
    "additionalProperties": true,
    "type": "object"
   },
   "id": {
    "type": "integer"
   },
   "messages": {
    "items": {
     "additionalProperties": true,
     "type": "object"
    },
    "type": "array"
   },
   "ok": {
    "type": "boolean"
   },
   "operation": {
    "type": "string"
   },
   "params": {
    "additionalProperties": true,
    "type": "object"
   },
   "result": {},
   "users": {
    "items": {
     "additionalProperties": true,
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "title": "Invoke MTProto"
}
