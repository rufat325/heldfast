{
 "description": "Search the web using Tavily via SignalFuse gateway. Paid endpoint (x402). Returns Tavily search results.",
 "inputSchema": {
  "properties": {
   "credit_token": {
    "description": "Optional credit token for bulk-prepaid access",
    "type": "string"
   },
   "max_results": {
    "description": "Maximum number of results to return",
    "type": "number"
   },
   "query": {
    "description": "Search query string",
    "type": "string"
   },
   "search_depth": {
    "description": "Search depth: \"basic\" or \"advanced\"",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_tavily"
}
