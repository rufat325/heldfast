{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List Form Responses"
 },
 "description": "Read the responses submitted to a form \u2014 each submission's answers and when it arrived, newest first. Requires event_id + form_id; you must be a host. Read-only.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "form_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "form_id"
  ],
  "type": "object"
 },
 "name": "list_form_responses",
 "title": "List Form Responses"
}
