{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get real-time water level (m) and discharge (m3/s) readings for a Canadian river or lake by hydrometric station_number, sorted most-recent-first. Readings are typically reported every 5 minutes for stations with real-time telemetry. Data: api.weather.gc.ca (MSC GeoMet OGC API - Features, sourced from HYDAT/WSC), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max records to return (1-288). Defaults to 20.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "station_number": {
    "description": "Hydrometric station number, e.g. \"01AD003\" (St. Francis River, NB). Readings are reported roughly every 5 minutes for stations with real-time telemetry.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "station_number"
  ],
  "type": "object"
 },
 "name": "msc-geomet.hydrometric.realtime",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Canadian Real-Time River and Lake Levels"
}
