{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List the caller's tracked jobs (viewed/saved/applied) with their stage and note. Filter narrows the set.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "filter": {
    "default": "all",
    "description": "Which tracked jobs to list.",
    "enum": [
     "all",
     "viewed",
     "saved",
     "applied"
    ],
    "type": "string"
   },
   "limit": {
    "default": 20,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "default": 0,
    "minimum": 0,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "my"
}
