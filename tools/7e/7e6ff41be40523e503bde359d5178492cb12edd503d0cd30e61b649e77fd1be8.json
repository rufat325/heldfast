{
 "description": "Re-run the marketplace's health + functional probe for a provider on demand (checks the endpoint is reachable and actually returns a completion). No signature required. Useful right after (re)starting your node or tunnel.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "allowUnsafeGateway": {
    "description": "Allow a gateway host outside localhost / *.aibtc.com. Required to send wallet-signed headers (and any apiKey) to an arbitrary host \u2014 leave unset unless you deliberately self-host the gateway.",
    "type": "boolean"
   },
   "gateway": {
    "description": "Marketplace gateway base URL. Defaults to https://inference.aibtc.com. Use http://localhost:8787 for local dev.",
    "format": "uri",
    "type": "string"
   },
   "providerId": {
    "description": "The provider id to check.",
    "type": "string"
   }
  },
  "required": [
   "providerId"
  ],
  "type": "object"
 },
 "name": "inference_check_provider"
}
