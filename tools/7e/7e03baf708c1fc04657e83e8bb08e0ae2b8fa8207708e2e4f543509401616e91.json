{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Get a workflow by ID, including full nodes and connections JSON. Use mode='summary' to get only metadata and the node list (cheaper for inspection).",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Workflow ID",
    "type": "string"
   },
   "mode": {
    "default": "full",
    "description": "full = complete JSON; summary = metadata + node names/types only",
    "enum": [
     "full",
     "summary"
    ],
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_workflow"
}
