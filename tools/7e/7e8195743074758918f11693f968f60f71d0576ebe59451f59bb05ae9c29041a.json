{
 "description": "PAYWALLED. Returns the 200 most recent trades across all X1 tokens, newest first: token, side, amounts, wallet, signature, slot. 200 is the hard cap of this feed, not a sample of something larger. Returns x402 payment instructions to call /api/x402/firehose.",
 "inputSchema": {
  "type": "object"
 },
 "name": "firehose"
}
