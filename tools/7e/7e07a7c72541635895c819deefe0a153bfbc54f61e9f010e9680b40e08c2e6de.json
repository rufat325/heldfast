{
 "description": "[WRITE] Move a Tag ID out of a container into a new container. Requires GTM_MCP_ENABLE_WRITES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "description": "The GTM account ID.",
    "type": "string"
   },
   "allowUserPermissionFeatureUpdate": {
    "type": "boolean"
   },
   "confirm": {
    "description": "Must be true to confirm this write operation.",
    "type": "boolean"
   },
   "containerId": {
    "description": "The source container ID.",
    "type": "string"
   },
   "copySettings": {
    "description": "Whether to copy container settings.",
    "type": "boolean"
   },
   "copyTermsOfService": {
    "description": "Whether to copy terms of service.",
    "type": "boolean"
   },
   "copyUsers": {
    "description": "Whether to copy users.",
    "type": "boolean"
   },
   "tagId": {
    "description": "The Tag ID to move.",
    "type": "string"
   },
   "tagName": {
    "description": "Name for the new tag created in the destination container.",
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "containerId",
   "tagId",
   "confirm"
  ],
  "type": "object"
 },
 "name": "containers_move_tag_id"
}
