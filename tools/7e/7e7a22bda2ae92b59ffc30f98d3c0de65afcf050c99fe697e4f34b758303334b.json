{
 "description": "Start a competitive page analysis (content gaps, missing topics, optimization opportunities). Async: poll get_competitive_analysis with the returned id for results.",
 "inputSchema": {
  "properties": {
   "competitor_urls": {
    "description": "Up to 3 competitor URLs for direct comparison",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "industry": {
    "description": "Industry context, e.g. 'SaaS', 'E-commerce'",
    "type": "string"
   },
   "location_name": {
    "description": "Exact location for localized SERP, e.g. 'London,England,United Kingdom'. Use search_locations for valid values.",
    "type": "string"
   },
   "site_id": {
    "description": "Site ID if the page belongs to a tracked site",
    "type": "string"
   },
   "url": {
    "description": "Page URL to analyze",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "start_competitive_analysis"
}
