{
 "description": "Fund the Lightning wallet from the user's L1 BTC wallet. Sends BTC to the Spark deposit address and returns the L1 txid. After the transaction confirms on-chain, the deposit must be claimed to credit the Lightning balance (separate follow-up step; not automated in this PR). Requires the main wallet to be unlocked AND the Lightning wallet to be unlocked (to fetch the deposit address).\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "amountSats": {
    "description": "Amount to deposit in satoshis (1 BTC = 100,000,000 satoshis)",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "feeRate": {
    "anyOf": [
     {
      "enum": [
       "fast",
       "medium",
       "slow"
      ],
      "type": "string"
     },
     {
      "exclusiveMinimum": 0,
      "maximum": 9007199254740991,
      "type": "integer"
     }
    ],
    "default": "medium",
    "description": "Fee rate: 'fast' (~10 min), 'medium' (~30 min), 'slow' (~1 hr), or number in sat/vB"
   }
  },
  "required": [
   "amountSats"
  ],
  "type": "object"
 },
 "name": "lightning_fund_from_btc"
}
