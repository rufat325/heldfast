{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Free. Check full live coverage and <= 5 s feed freshness for one to five pump.fun mints before paying. Returns availability and exact prices. Use it right before a paid check; it returns no risk findings.",
 "inputSchema": {
  "properties": {
   "mints": {
    "description": "One to five distinct pump.fun mints with full live coverage, each a mint or a pump.fun/gmgn/solscan/birdeye link",
    "items": {
     "type": "string"
    },
    "maxItems": 5,
    "minItems": 1,
    "title": "Mints",
    "type": "array"
   }
  },
  "required": [
   "mints"
  ],
  "title": "check_coverageArguments",
  "type": "object"
 },
 "name": "check_coverage",
 "title": "Check mint coverage (free)"
}
