{
 "description": "List tasks in the shared queue. Defaults to open tasks in priority order; filter by status or assignee.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "all": {
    "description": "Include closed tasks too.",
    "type": "boolean"
   },
   "assignee": {
    "description": "Filter by assignee agent name.",
    "type": "string"
   },
   "status": {
    "description": "Filter by status (e.g. todo, in_progress, blocked, done).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "task_list"
}
