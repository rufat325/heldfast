{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List UNESCO UIS geographic units (countries/regions) with their ISO3 code (id), name and type. Use the id as `geoUnit` in get_data. Optional case-insensitive name substring filter.",
 "inputSchema": {
  "examples": [
   {
    "search": "brazil"
   },
   {
    "limit": 50
   }
  ],
  "properties": {
   "limit": {
    "description": "Max results to return (default 300).",
    "type": "number"
   },
   "search": {
    "description": "Case-insensitive substring matched against country/region name, e.g. \"brazil\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_geounits",
 "title": "List Geounits",
 "version": "v1.cc121a12"
}
