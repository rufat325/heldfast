{
 "description": "Open data breakdown of news coverage by category, country, and keyword for a given period.",
 "inputSchema": {
  "properties": {
   "location": {
    "description": "Country, region, or city (optional)",
    "type": "string"
   },
   "period": {
    "description": "Time window (default: week)",
    "enum": [
     "today",
     "week",
     "month"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_content_analytics"
}
