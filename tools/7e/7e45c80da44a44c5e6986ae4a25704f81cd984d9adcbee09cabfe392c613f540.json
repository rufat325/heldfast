{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Execute multiple searches in parallel and return grouped results. Never mutate or import data; use batch_records for write operations. Provide queries array (1\u201310 items recommended) and resource_type. If queries fail, retry individually using search_records.",
 "inputSchema": {
  "additionalProperties": false,
  "examples": [
   {
    "limit": 10,
    "queries": [
     "Acme",
     "Globex"
    ],
    "resource_type": "companies"
   }
  ],
  "properties": {
   "limit": {
    "description": "Maximum number of results per query (default: 20)",
    "maximum": 100,
    "minimum": 1,
    "type": "number"
   },
   "offset": {
    "description": "Number of results to skip per query (default: 0)",
    "minimum": 0,
    "type": "number"
   },
   "queries": {
    "description": "Array of search query strings",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "resource_type": {
    "description": "Resource type to search (companies, people, records, tasks, deals)",
    "enum": [
     "companies",
     "people",
     "lists",
     "records",
     "tasks",
     "deals",
     "notes"
    ],
    "type": "string"
   }
  },
  "required": [
   "resource_type",
   "queries"
  ],
  "type": "object"
 },
 "name": "batch_search_records"
}
