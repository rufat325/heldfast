{
 "description": "Remove one image from the user's Uploads library by id (from upload_image or list_uploads). Needs an API key with design:write. Saved designs keep their own copies, so this never breaks a design; it only frees a library slot. Prefer deleting images this session uploaded over ones the user added themselves.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "uploadId": {
    "description": "The id (or upload:<id> ref) to remove",
    "type": "string"
   }
  },
  "required": [
   "uploadId"
  ],
  "type": "object"
 },
 "name": "delete_upload"
}
