{
 "description": "Execute initial settlement setup in one command. Registers agent, approves USDC, and routes the first payment. Referral attribution remains a separate explicit step.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "agent_address": {
    "description": "Ethereum address of the AI agent",
    "pattern": "^0x[0-9a-fA-F]{40}$",
    "type": "string"
   },
   "amount": {
    "description": "First payment amount in USDC (e.g. \"100\")",
    "type": "string"
   }
  },
  "required": [
   "amount",
   "agent_address"
  ],
  "type": "object"
 },
 "name": "clicks_quick_start"
}
