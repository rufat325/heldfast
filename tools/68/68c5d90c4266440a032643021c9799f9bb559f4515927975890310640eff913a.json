{
 "description": "Detailed SEO analysis: title, meta description, headings, keywords, Open Graph tags, and improvement suggestions",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "url": {
    "description": "The URL to analyze for SEO",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "check_seo"
}
