{
 "description": "One top-level domain: type, registry, RDAP server, when it entered or left the root, every registrar price (register, renew, transfer, restore, promotions, privacy, restrictions) and its change history.",
 "inputSchema": {
  "properties": {
   "tld": {
    "description": "The label, with or without the dot; Unicode or xn-- both work",
    "type": "string"
   }
  },
  "required": [
   "tld"
  ],
  "type": "object"
 },
 "name": "get_tld"
}
