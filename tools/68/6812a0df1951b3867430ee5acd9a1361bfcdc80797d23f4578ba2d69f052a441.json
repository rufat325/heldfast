{
 "description": "Aurora agent surface (no LLM cost \u2014 pure DB write). Only relevant when managing Aurora agents. Restore an archived agent so it appears in default agent listings again. Address by agentId OR exact title (XOR).",
 "inputSchema": {
  "properties": {
   "agentId": {
    "description": "MongoDB agent _id to unarchive (XOR with title)",
    "type": "string"
   },
   "title": {
    "description": "Exact case-insensitive user-assigned title (XOR with agentId)",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "unarchive_agent"
}
