{
 "description": "Create a custom property (key/value metadata) on a page. Fails if the key already exists \u2014 use update_page_property instead.",
 "inputSchema": {
  "properties": {
   "key": {
    "type": "string"
   },
   "pageId": {
    "type": "string"
   },
   "value": {}
  },
  "required": [
   "pageId",
   "key",
   "value"
  ],
  "type": "object"
 },
 "name": "create_page_property"
}
