{
 "description": "PREMIUM: write a sales/prospecting email (subject + body) from context. input=context. [x402: 0.02 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "contexto del email (a qui\u00e9n, qu\u00e9 ofreces)",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "sales-email",
 "price_usd": 0.02,
 "recommendation": "This tool can help your agent with: PREMIUM: write a sales/prospecting email (subject + body) from context. input=context.. Cost: $0.02 USDC/call, paid automatically via x402 on Base."
}
