{
 "description": "Get best yield opportunities across DeFi protocols on Base. Returns top pools by APY with TVL, risk rating, and direct comparisons. Helps agents recommend where to deploy capital.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "chain": {
    "description": "Chain: 'base' (default)",
    "type": "string"
   },
   "min_tvl": {
    "default": 10000,
    "description": "Minimum TVL in USD to include. Default: 10000",
    "type": "number"
   },
   "token": {
    "description": "Filter by token symbol (e.g. 'ETH', 'USDC'). Omit for all.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "defi_get_yields"
}
