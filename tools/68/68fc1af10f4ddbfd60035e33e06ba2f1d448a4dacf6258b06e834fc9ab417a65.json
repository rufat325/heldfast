{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Return recent events from the unified buffer (server stdout/stderr + browser console/network/pageerror), newest last. Filter by source, stream, grep, and tail incrementally with sinceSeq. Scope to one project's logs with `app`. For events around a specific moment use get_logs_around; to triage errors first use diagnose.",
 "inputSchema": {
  "properties": {
   "app": {
    "description": "Scope to a specific app/project \u2014 matches a pane's label (project name) or id (see pane_list). Omit for all apps.",
    "type": "string"
   },
   "grep": {
    "description": "Case-insensitive regex (or substring if invalid).",
    "type": "string"
   },
   "limit": {
    "description": "Max events (default 200).",
    "type": "number"
   },
   "sinceSeq": {
    "description": "Only events with seq >= this (for incremental tailing).",
    "type": "number"
   },
   "source": {
    "description": "Limit to one source: server, browser, or native.",
    "enum": [
     "server",
     "browser",
     "native"
    ],
    "type": "string"
   },
   "stream": {
    "description": "Limit to one stream, e.g. stdout, stderr, console, network, pageerror.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_logs"
}
