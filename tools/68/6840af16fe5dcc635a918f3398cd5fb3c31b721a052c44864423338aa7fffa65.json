{
 "description": "Delete a snapshot of a volume. Destructive. Do not call this unless the user has explicitly authorized it in this conversation \u2014 never proactively, speculatively, or as a side effect of another task. Longhorn docs: https://longhorn.io/docs/1.12.1/snapshots-and-backups/setup-a-snapshot/",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "name": {
    "type": "string"
   },
   "snapshotName": {
    "type": "string"
   }
  },
  "required": [
   "name",
   "snapshotName"
  ],
  "type": "object"
 },
 "name": "volume_snapshot_delete",
 "title": "Delete volume snapshot"
}
