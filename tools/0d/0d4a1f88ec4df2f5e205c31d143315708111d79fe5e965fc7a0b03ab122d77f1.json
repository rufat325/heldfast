{
 "description": "What a raise is actually worth after tax. Runs the real brackets before and after, so bracket crossings are handled \u2014 not a flat marginal rate applied to the raise.",
 "inputSchema": {
  "properties": {
   "currentSalary": {
    "description": "Current annual salary",
    "type": "number"
   },
   "filingStatus": {
    "default": "single",
    "description": "Tax filing status",
    "enum": [
     "single",
     "married",
     "head"
    ],
    "type": "string"
   },
   "preTaxDeductions": {
    "description": "Pre-tax deductions (401k, HSA), held constant",
    "type": "number"
   },
   "raiseAmount": {
    "description": "Raise as a dollar amount \u2014 takes priority over raisePct",
    "type": "number"
   },
   "raisePct": {
    "description": "Raise as a percentage, e.g. 5 (negative for a cut)",
    "type": "number"
   },
   "state": {
    "default": "",
    "description": "US state abbreviation (e.g. CA, NY, TX)",
    "type": "string"
   }
  },
  "required": [
   "currentSalary"
  ],
  "type": "object"
 },
 "name": "calculate_pay_raise"
}
