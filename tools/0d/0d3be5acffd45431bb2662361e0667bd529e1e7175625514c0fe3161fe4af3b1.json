{
 "description": "Delete a Confluence page. Omit status to trash a current page; to permanently purge an already-trashed page, pass status=\"trashed\" (matching its current status \u2014 confirmed live, passing \"current\" here does nothing, it just re-trashes an already-trashed page).",
 "inputSchema": {
  "properties": {
   "pageId": {
    "type": "string"
   },
   "status": {
    "type": "string"
   }
  },
  "required": [
   "pageId"
  ],
  "type": "object"
 },
 "name": "delete_page"
}
