{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Find code by meaning when you don\u2019t know the name: symptoms, relationships, what-breaks-if. For a known name or literal string use search_symbols.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "budget": {
    "description": "context-block token budget in detailed mode (default 2000)",
    "type": "number"
   },
   "limit": {
    "description": "ranked matches to return (default 5)",
    "type": "number"
   },
   "offset": {
    "type": "number"
   },
   "query": {
    "maxLength": 300,
    "type": "string"
   },
   "question": {
    "maxLength": 300,
    "type": "string"
   },
   "response_format": {
    "enum": [
     "concise",
     "detailed"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "query_graph"
}
