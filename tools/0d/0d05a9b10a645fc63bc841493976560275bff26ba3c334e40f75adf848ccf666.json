{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Report UI action outcome (telemetry)"
 },
 "description": "Report one UI action outcome (invoke/set_value/focus). Feeds per-app recipes in get_app_knowledge. Metadata only \u2014 never screen content or typed text. Report FAILURES too (ok=false plus reason): they become common_failures and stop the next agent from repeating the same dead end.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Action performed (invoke, set_value, focus)",
    "type": "string"
   },
   "app": {
    "description": "App where action occurred",
    "type": "string"
   },
   "element_name": {
    "description": "Element name (e.g. \"Save\")",
    "type": "string"
   },
   "element_role": {
    "description": "Element role (Button, Edit, ...)",
    "type": "string"
   },
   "ms": {
    "description": "Action duration in ms",
    "type": "number"
   },
   "ok": {
    "description": "Action succeeded?",
    "type": "boolean"
   },
   "reason": {
    "description": "Only when ok=false. Closed vocabulary: elemento_nao_encontrado, elemento_nao_suporta_invoke, elemento_nao_suporta_set_value, campo_somente_leitura, janela_nao_encontrada, ui_timeout, acao_bloqueada, erro_uia, outro. Anything else is stored as \"outro\" \u2014 free text never reaches disk.",
    "type": "string"
   }
  },
  "required": [
   "app",
   "action"
  ],
  "type": "object"
 },
 "name": "report_action_outcome"
}
