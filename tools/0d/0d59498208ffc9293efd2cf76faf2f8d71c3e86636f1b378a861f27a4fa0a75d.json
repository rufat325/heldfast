{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Search Pipeworx packs by keyword or a plain question across pack names, descriptions and tool descriptions (e.g. \"weather\", \"translate\", \"Colombia procurement contracts awarded to a supplier\"). Returns matching packs ranked best-first with each one's matched terms, cost, auth and reliability. Use to find which pack covers a capability before connecting to it; question filler and proper names are ignored, so the subject words are what match.",
 "inputSchema": {
  "examples": [
   {
    "query": "weather"
   },
   {
    "query": "translate"
   },
   {
    "limit": 3,
    "query": "Which public contracts has the supplier Constructora Conconcreto been awarded recently in Colombia SECOP procurement system"
   }
  ],
  "properties": {
   "limit": {
    "description": "Maximum packs to return, best match first (1-200, default 25).",
    "type": "number"
   },
   "query": {
    "description": "Keywords or a question naming the data you want. The subject words match (country, system, dataset, topic); names of specific companies or people do not, since no pack describes them.",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_packs",
 "outputSchema": {
  "properties": {
   "hint": {
    "description": "Present only when no pack matched: how to rephrase, or use ask_pipeworx",
    "type": "string"
   },
   "packs": {
    "description": "Matching packs",
    "items": {
     "properties": {
      "category": {
       "description": "Pack category",
       "type": "string"
      },
      "description": {
       "description": "Pack description",
       "type": "string"
      },
      "gateway_url": {
       "description": "MCP gateway URL for the pack",
       "type": "string"
      },
      "matched_terms": {
       "description": "Query words found in this pack's text, best match first",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "name": {
       "description": "Pack display name",
       "type": "string"
      },
      "next_step": {
       "description": "The exact next request to make for the first named tool, in the shape of the surface this call arrived on (REST /v1/tools/{name}, or MCP tools/call)",
       "type": "string"
      },
      "slug": {
       "description": "Pack identifier slug",
       "type": "string"
      },
      "tools": {
       "description": "Up to 3 tools in this pack that best match the query, by the EXPOSED name \u2014 the one GET/POST /v1/tools/{name} resolves, namespaced when several packs export the same tool name. Not the pack's full list; call get_pack_tools for that.",
       "items": {
        "properties": {
         "name": {
          "description": "Exact tool name to call, e.g. fred_get_series",
          "type": "string"
         },
         "summary": {
          "description": "One line on what the tool returns",
          "type": "string"
         }
        },
        "required": [
         "name",
         "summary"
        ],
        "type": "object"
       },
       "type": "array"
      }
     },
     "required": [
      "slug",
      "name",
      "description",
      "category",
      "gateway_url"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "query": {
    "description": "Search query used",
    "type": "string"
   },
   "results": {
    "description": "Number of matching packs",
    "type": "number"
   },
   "showing": {
    "description": "Packs returned when the match list was capped by limit",
    "type": "number"
   },
   "truncated": {
    "description": "True when more packs matched than were returned",
    "type": "boolean"
   }
  },
  "required": [
   "query",
   "results",
   "packs"
  ],
  "type": "object"
 },
 "title": "Search Packs",
 "version": "v1.3cdd4440"
}
