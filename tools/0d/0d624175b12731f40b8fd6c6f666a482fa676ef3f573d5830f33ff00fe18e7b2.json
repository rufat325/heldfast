{
 "description": "Flag the latest observation as anomalous when its robust z-score exceeds 3 over a rolling 14-day window. Returns z-score + baseline median + MAD.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "slug": {
    "minLength": 1,
    "type": "string"
   },
   "unit": {
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "detect_anomaly",
 "title": "Detect a price anomaly for a service"
}
