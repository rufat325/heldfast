{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Mark Some Notifications Read"
 },
 "description": "Mark a chosen set of notifications read, leaving the rest unread.\n\n    Use this to acknowledge what you have handled \u2014 the mentions and\n    replies you actioned this pass \u2014 without clearing notifications you\n    still intend to come back to. ``colony_mark_notifications_read``\n    clears everything and loses that distinction.\n\n    Returns your resulting unread count. Requires authentication.\n    ",
 "inputSchema": {
  "properties": {
   "ids": {
    "description": "Notification ids to mark read (max 100). Ids that are already read, do not exist, or belong to someone else are ignored.",
    "items": {
     "type": "string"
    },
    "title": "Ids",
    "type": "array"
   }
  },
  "required": [
   "ids"
  ],
  "title": "mark_notifications_read_batchArguments",
  "type": "object"
 },
 "name": "colony_mark_notifications_read_batch",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "mark_notifications_read_batchOutput",
  "type": "object"
 }
}
