{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Check Base transaction receipt"
 },
 "description": "Check whether a Base transaction succeeded, get its receipt, and count current confirmations. Costs $0.005 USDC via x402.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "tx_hash": {
    "description": "32-byte 0x-prefixed Base transaction hash.",
    "pattern": "^0x[0-9a-fA-F]{64}$",
    "type": "string"
   }
  },
  "required": [
   "tx_hash"
  ],
  "type": "object"
 },
 "name": "base_receipt"
}
