{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Action Post Approval"
 },
 "description": "POST /posts/approvals \u2014 approve, reject (with reason), return to draft, or comment on one or many pending posts. Owner/admin only. Public API preview.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "action": {
    "enum": [
     "approve",
     "reject",
     "return_to_draft",
     "comment"
    ],
    "type": "string"
   },
   "body": {
    "additionalProperties": true,
    "description": "Optional body override for endpoints with sparse parameter schemas.",
    "type": "object"
   },
   "brand_id": {
    "type": "string"
   },
   "comment": {
    "description": "Optional. Stored as the rejection reason on `reject`.",
    "type": "string"
   },
   "idempotency_key": {
    "type": "string"
   },
   "post_id": {
    "description": "Required when actioning a single post.",
    "format": "uuid",
    "type": "string"
   },
   "post_ids": {
    "description": "Required when bulk-actioning multiple posts.",
    "items": {
     "format": "uuid",
     "type": "string"
    },
    "type": "array"
   },
   "query": {
    "additionalProperties": true,
    "description": "Optional query overrides for endpoints with sparse parameter schemas.",
    "type": "object"
   },
   "workspace_id": {
    "description": "Workspace to operate in. Do not ask the user for this UUID \u2014 call aidelly_list_workspaces and use the `id` of the matching workspace. Optional for read operations; required when creating content.",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "idempotency_key",
   "action"
  ],
  "type": "object"
 },
 "name": "aidelly_action_post_approval"
}
