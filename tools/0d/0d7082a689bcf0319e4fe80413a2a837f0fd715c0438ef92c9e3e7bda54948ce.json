{
 "description": "Get HICP inflation data from the ECB \u2014 the official eurozone inflation measure.\n\nReturns the Harmonised Index of Consumer Prices (HICP) as published by Eurostat via the ECB.\nAvailable as annual rate of change (%) or index level, for the euro area or individual countries.\n\nExamples of questions this tool answers:\n- \"What is the current eurozone inflation rate?\"\n- \"Show me Germany's inflation over the past year\"\n- \"Compare inflation in France vs Italy\"",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "country": {
    "description": "Country code: \"U2\" for euro area (default), \"DE\", \"FR\", \"IT\", \"ES\", etc.",
    "type": "string"
   },
   "endPeriod": {
    "description": "End date (YYYY-MM, or YYYY)",
    "type": "string"
   },
   "lastNObservations": {
    "description": "Return only the last N data points",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "measure": {
    "description": "annual_rate = year-over-year % change (default), index = price index level",
    "enum": [
     "annual_rate",
     "index"
    ],
    "type": "string"
   },
   "startPeriod": {
    "description": "Start date (YYYY-MM, or YYYY)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_inflation"
}
