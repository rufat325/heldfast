{
 "description": "List recent reply events (reply.received) for an agent.",
 "inputSchema": {
  "properties": {
   "agent": {
    "type": "string"
   },
   "limit": {
    "type": "number"
   },
   "since": {
    "type": "string"
   }
  },
  "required": [
   "agent"
  ],
  "type": "object"
 },
 "name": "mails.list_replies"
}
