{
 "description": "Mark an import request PROCESSING, COMPLETED, or FAILED, with an optional summary of what was loaded (admin only).",
 "inputSchema": {
  "properties": {
   "import_request_id": {
    "description": "The ID of the import request",
    "type": "string"
   },
   "status": {
    "description": "The new status",
    "enum": [
     "PROCESSING",
     "COMPLETED",
     "FAILED"
    ],
    "type": "string"
   },
   "summary": {
    "description": "What was loaded or why it failed (shown to the customer)",
    "type": "string"
   }
  },
  "required": [
   "import_request_id",
   "status"
  ],
  "type": "object"
 },
 "name": "update_import_request_status"
}
