{
 "description": "Change when a single timeline element starts and/or how long it lasts (any track). Use to nudge a floating audio/overlay element to a new point. To resequence all clips, use reorder_timeline. Element ids come from get_project (timeline.tracks[].elements[].id). Blocked in read-only mode.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "duration": {
    "description": "New duration in seconds.",
    "minimum": 0.5,
    "type": "number"
   },
   "elementId": {
    "type": "string"
   },
   "projectId": {
    "type": "string"
   },
   "startTime": {
    "description": "New start, seconds from the movie start.",
    "minimum": 0,
    "type": "number"
   }
  },
  "required": [
   "projectId",
   "elementId"
  ],
  "type": "object"
 },
 "name": "move_timeline_element",
 "title": "Retime a timeline element"
}
