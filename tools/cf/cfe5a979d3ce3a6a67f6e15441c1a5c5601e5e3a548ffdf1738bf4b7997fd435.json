{
 "description": "Follower cards {id, login, type, url, avatar} \u2014 ~0.1/row; large accounts are expensive to page fully. Costs ~3 credits (0.1/result). Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cache": {
    "description": "Set true to serve from the 24h response cache (0 credits on hit). Default false \u2014 always fetch fresh. Envelope includes cached + cachedAt on hits.",
    "type": "boolean"
   },
   "cursor": {
    "description": "Opaque cursor from a previous nextCursor (GitHub Link page=). Not a bare page number.",
    "type": "string"
   },
   "limit": {
    "description": "Max items to return (default 30, max 100). Billed per result.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "username": {
    "description": "GitHub username or profile URL, e.g. getify.",
    "type": "string"
   }
  },
  "required": [
   "username"
  ],
  "type": "object"
 },
 "name": "github_followers",
 "title": "GitHub Followers"
}
