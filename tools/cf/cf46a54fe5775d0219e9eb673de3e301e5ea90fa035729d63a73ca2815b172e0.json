{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List apps in your App Store Connect account. Supports optional filters by bundleId and name.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "auto_paginate": {
    "description": "Follow links.next across pages until the limit is reached (default false). Check the pagination block in the result for has_more.",
    "type": "boolean"
   },
   "bundleId": {
    "description": "Exact bundle ID filter (e.g. com.example.MyApp)",
    "type": "string"
   },
   "limit": {
    "description": "Max apps to return (default 50). Without auto_paginate this is capped at one API page (200); with auto_paginate it is the total across pages.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "name": {
    "description": "Exact app name filter",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_apps"
}
