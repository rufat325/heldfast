{
 "description": "Safely delete a Granoflow milestone. Defaults to dry-run and requires confirmTitle for writes.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "allowLinkedTasks": {
    "default": false,
    "type": "boolean"
   },
   "confirmTitle": {
    "minLength": 1,
    "type": "string"
   },
   "dryRun": {
    "default": true,
    "type": "boolean"
   },
   "milestoneId": {
    "description": "Granoflow milestone id.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "milestoneId"
  ],
  "type": "object"
 },
 "name": "granoflow_milestone_delete"
}
