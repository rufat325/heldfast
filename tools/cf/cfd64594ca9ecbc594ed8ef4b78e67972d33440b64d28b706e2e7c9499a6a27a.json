{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find the hostname for an IP address via reverse DNS lookup. Returns the PTR record if available.",
 "inputSchema": {
  "examples": [
   {
    "ip": "8.8.8.8"
   }
  ],
  "properties": {
   "ip": {
    "description": "IPv4 address to reverse-lookup (e.g., \"8.8.8.8\")",
    "type": "string"
   }
  },
  "required": [
   "ip"
  ],
  "type": "object"
 },
 "name": "reverse_dns",
 "outputSchema": {
  "properties": {
   "hostnames": {
    "description": "List of hostnames found via PTR record",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "ip": {
    "description": "IPv4 address that was queried",
    "type": "string"
   },
   "primary_hostname": {
    "description": "Primary hostname from first PTR record or null",
    "type": [
     "string",
     "null"
    ]
   },
   "reverse_name": {
    "description": "Reverse DNS name in .in-addr.arpa format",
    "type": "string"
   },
   "status": {
    "description": "DNS status code name (NOERROR, NXDOMAIN, etc.)",
    "type": "string"
   }
  },
  "required": [
   "ip",
   "reverse_name",
   "status",
   "hostnames",
   "primary_hostname"
  ],
  "type": "object"
 },
 "title": "Reverse Dns",
 "version": "v1.8c943aef"
}
