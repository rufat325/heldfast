{
 "description": "Soft-delete a sticky by id (marks it dismissed; it is no longer read back).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "id": {
    "description": "The sticky id to dismiss.",
    "type": "string"
   },
   "reason": {
    "description": "Optional reason for dismissal.",
    "type": [
     "string",
     "null"
    ]
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "stickies_dismiss",
 "title": "Dismiss a sticky"
}
