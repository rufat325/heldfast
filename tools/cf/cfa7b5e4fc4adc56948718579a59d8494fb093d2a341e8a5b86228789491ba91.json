{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Delete a saved place card (business_contacts) by place_id / business_contact_id. Returns the deleted card. Idempotent \u2014 a second delete returns not_found. ($0.10; API key required)",
 "destructiveHint": true,
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "business_contact_id": {
    "type": "string"
   },
   "id": {
    "type": "string"
   },
   "idempotency_key": {
    "type": "string"
   },
   "place_id": {
    "type": "string"
   },
   "request_id": {
    "description": "Alias for idempotency_key (preferred by some clients).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "delete_place",
 "openWorldHint": false,
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "business_contact_id": {
    "type": "string"
   },
   "deleted": {
    "additionalProperties": true,
    "description": "Deleted saved place card row.",
    "type": "object"
   },
   "place_id": {
    "type": "string"
   }
  },
  "required": [
   "place_id",
   "business_contact_id",
   "deleted"
  ],
  "type": "object"
 },
 "readOnlyHint": false,
 "securitySchemes": [
  {
   "scopes": [
    "context.write",
    "context.delete"
   ],
   "type": "oauth2"
  }
 ],
 "title": "Delete Place Card"
}
