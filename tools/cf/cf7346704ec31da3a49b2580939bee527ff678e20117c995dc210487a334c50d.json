{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Personalized supplement recommendations computed from the signed-in user's saved goals and cloud stack, each with a composite score, evidence, the best-matching goal, and any safety warnings. Requires the user to be signed in (`supstack login`).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "limit": {
    "default": 10,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "supstack_recommend"
}
