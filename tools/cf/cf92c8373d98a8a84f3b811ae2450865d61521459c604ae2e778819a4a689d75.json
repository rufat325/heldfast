{
 "description": "Fetch recent tweets from a public @username timeline. Returns compact summaries.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max results (default 20)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "username": {
    "description": "Handle without @",
    "type": "string"
   }
  },
  "required": [
   "username"
  ],
  "type": "object"
 },
 "name": "xflux_get_user_tweets"
}
