{
 "description": "Apply a saved preset of constant edits.",
 "inputSchema": {
  "properties": {
   "preset_name": {
    "description": "Preset name to apply",
    "type": "string"
   },
   "reverse": {
    "description": "Reverse the edits (default: false)",
    "type": "boolean"
   },
   "session_id": {
    "description": "Session ID",
    "type": "string"
   }
  },
  "required": [
   "session_id",
   "preset_name"
  ],
  "type": "object"
 },
 "name": "jar_preset_apply"
}
