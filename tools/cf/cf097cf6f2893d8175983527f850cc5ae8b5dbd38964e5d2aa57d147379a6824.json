{
 "_meta": {
  "fastmcp": {
   "tags": []
  }
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "The film library, one ranked page at a time. Films sit on job\nshelves; pass job and the human's goal and hire from the top of the\npage. Each film says what it is, when to hire it, its length and\nshapes, fits_this_brand (with what is missing) and made_recently\n(this brand already got it: prefer another film on the shelf unless\nthe human asked for that one). Page on with next_page. Want it all at\nonce? all=true, or fetch the catalog file (https://api.mysiren.ai/api/v1/films.md,\nevery film by shelf, also .txt/.json) and read it in steps. search=\nfinds films by word. Then describe_film for the body. Video renders need Pro or\nabove. Every film here is 25 credits. Platinum films (50 credits) are\nnot in these pages: the answer's platinum section flags them, and\nlist_platinum_films has the shelf.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "all": {
    "default": false,
    "description": "true = every film that passes the filters on one page (about 10k tokens for the whole library). Prefer job + page, or the catalog.md file.",
    "type": "boolean"
   },
   "aspect": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Only films that render this shape: wide-desktop, square-ig, story-reel, vertical-ig-feed."
   },
   "detail": {
    "default": "short",
    "description": "\"short\" (default) or \"full\" to add the long description per film.",
    "type": "string"
   },
   "goal": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "The human's own words for the outcome, e.g. \"launch video for our new AI agent\" or \"show our 10k users\". Ranks the films by fit."
   },
   "job": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "The shelf: launch, feature (one feature or update), explain (what the product does), brand (story, manifesto, mood), ai (AI or agent product), proof (a number, testimonials), app (mobile app), shop (products, food, property), people (team or mascot). Omit to rank the whole library."
   },
   "max_seconds": {
    "anyOf": [
     {
      "type": "number"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Only films this long or shorter, e.g. 15 for a short feed post."
   },
   "narrated": {
    "anyOf": [
     {
      "type": "boolean"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "true = only films with a voice-over part; false = only music-and-motion films."
   },
   "page": {
    "default": 1,
    "description": "Page number, from 1. Each page is page_size films; next_page in the answer says what to call next.",
    "type": "integer"
   },
   "page_size": {
    "default": 8,
    "description": "Films per page, 1 to 25.",
    "type": "integer"
   },
   "search": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Find films by word across ids, names and descriptions, e.g. \"phone\", \"chat\", \"mascot\", \"halftone\". Every word must match."
   }
  },
  "type": "object"
 },
 "name": "list_films",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 }
}
