{
 "description": "Read the entire content of a file. Returns {\"content\":\"...\"} (empty string if the file does not exist).",
 "inputSchema": {
  "properties": {
   "file": {
    "description": "Plain filename (with extension) inside the agent directory. No path separators or '..' allowed.",
    "type": "string"
   }
  },
  "required": [
   "file"
  ],
  "type": "object"
 },
 "name": "readNotes"
}
