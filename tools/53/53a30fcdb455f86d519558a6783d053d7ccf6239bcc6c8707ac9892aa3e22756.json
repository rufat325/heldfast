{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Add a note to a specific quote in Xero. Notes appear in the entity's history/audit trail. Notes are immutable once created.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "note": {
    "description": "The note text to add. This is immutable and cannot be edited or deleted after creation.",
    "type": "string"
   },
   "quoteID": {
    "description": "The Xero quote ID.",
    "type": "string"
   }
  },
  "required": [
   "quoteID",
   "note"
  ],
  "type": "object"
 },
 "name": "add-quote-note"
}
