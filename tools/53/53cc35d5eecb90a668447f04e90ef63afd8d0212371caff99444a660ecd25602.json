{
 "description": "Replace the tags of a workflow. Accepts tag NAMES \u2014 resolves them to IDs automatically and, with createMissing=true, creates tags that don't exist yet. Pass an empty list to remove all tags.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "createMissing": {
    "default": true,
    "description": "Create tags that don't exist yet",
    "type": "boolean"
   },
   "tagNames": {
    "description": "Full desired set of tag names (replaces current tags)",
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "type": "array"
   },
   "workflowId": {
    "description": "Workflow ID",
    "type": "string"
   }
  },
  "required": [
   "workflowId",
   "tagNames"
  ],
  "type": "object"
 },
 "name": "set_workflow_tags"
}
