{
 "description": "Import an AI-agent task result into Granoflow. Use dryRun first unless the user explicitly asks to write.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dryRun": {
    "default": true,
    "description": "When true, previews the request without writing.",
    "type": "boolean"
   },
   "input": {
    "additionalProperties": {},
    "description": "JSON object sent to the Granoflow Local HTTP API.",
    "type": "object"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "granoflow_task_import"
}
