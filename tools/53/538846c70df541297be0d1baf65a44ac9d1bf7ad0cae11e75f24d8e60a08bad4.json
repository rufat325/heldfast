{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Delete Screenshot"
 },
 "description": "Delete one screen from a screenshot set. A set keeps at least one screen \u2014 deleting the last one is rejected. Requires an authorized Sonar account or a write-scope API key.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "screenshot_id": {
    "description": "Screen id to delete.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "screenshot_id"
  ],
  "type": "object"
 },
 "name": "sonar_delete_screenshot",
 "title": "Delete Screenshot"
}
