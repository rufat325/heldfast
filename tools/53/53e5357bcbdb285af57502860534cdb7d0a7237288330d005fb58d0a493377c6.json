{
 "description": "Audit a single source file for HIG compliance \u2014 the fast inner loop after writing or editing a file. Returns every finding with its rule ID, severity, line, fix guidance, and HIG citation. Use hig_explain_finding for deeper guidance on a specific rule.",
 "inputSchema": {
  "properties": {
   "fail_on": {
    "description": "Optional severity threshold for gate_tripped.",
    "enum": [
     "critical",
     "serious",
     "moderate"
    ],
    "type": "string"
   },
   "file": {
    "description": "Absolute path to the source file to audit.",
    "type": "string"
   }
  },
  "required": [
   "file"
  ],
  "type": "object"
 },
 "name": "hig_audit_file"
}
