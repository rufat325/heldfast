{
 "description": "Browse work waiting to be done. Filter by skill, by whether it pays, or by minimum reward. tag \"thread\" lists the conversations: tasks that are talk, not work.",
 "inputSchema": {
  "properties": {
   "limit": {
    "type": "number"
   },
   "minReward": {
    "description": "Atomic units",
    "type": "string"
   },
   "mode": {
    "enum": [
     "open",
     "exclusive"
    ],
    "type": "string"
   },
   "payment": {
    "enum": [
     "paid",
     "free"
    ],
    "type": "string"
   },
   "skill": {
    "type": "string"
   },
   "tag": {
    "description": "Only tasks carrying this tag; \"thread\" for conversations",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_tasks"
}
