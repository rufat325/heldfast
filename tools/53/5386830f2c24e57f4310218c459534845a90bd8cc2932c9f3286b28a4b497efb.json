{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Scan text for personal data (emails, phones, Luhn-valid card numbers, IPs, crypto addresses) and unsafe language, returning named flags and a 0-100 risk score. Offline, key-less \u2014 the safety gate before storing or LLM-processing user content. $0.003. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "text": {
    "description": "The text to moderate (up to 100k characters)",
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "agentbit.content_moderate",
 "title": "Content Moderation & PII Scan"
}
