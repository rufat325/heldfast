{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get land surface elevation for up to 100 lat/lon coordinates in a single request. All points share the same dataset (srtm90m default, srtm30m, or aster30m). Returns an array of results, each with elevation_m, lat, lon, and dataset. Ideal for elevation profiling along a route, contour-line computation, or bulk terrain analysis. MIT licence, no auth, no upstream cost.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataset": {
    "description": "Elevation dataset applied to all locations: srtm90m (default), srtm30m, aster30m",
    "enum": [
     "srtm90m",
     "srtm30m",
     "aster30m"
    ],
    "type": "string"
   },
   "interpolation": {
    "description": "Interpolation method: bilinear (default) or nearest",
    "enum": [
     "bilinear",
     "nearest"
    ],
    "type": "string"
   },
   "locations": {
    "description": "Array of lat/lon objects to query (1\u2013100 locations per request)",
    "items": {
     "additionalProperties": false,
     "properties": {
      "lat": {
       "description": "Latitude (-90 to 90)",
       "maximum": 90,
       "minimum": -90,
       "type": "number"
      },
      "lon": {
       "description": "Longitude (-180 to 180)",
       "maximum": 180,
       "minimum": -180,
       "type": "number"
      }
     },
     "required": [
      "lat",
      "lon"
     ],
     "type": "object"
    },
    "maxItems": 100,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "locations"
  ],
  "type": "object"
 },
 "name": "opentopodata.elevation.batch",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Open Topo Data \u2014 Batch Elevation Query"
}
