{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Browse EU legislative acts by document type (regulation, directive, or decision) from EUR-Lex Cellar. Returns acts with CELEX identifier, publication date, English title, in-force status, Cellar URI, and direct EUR-Lex URL, ordered by most recent date first. Regulations are directly applicable in all EU member states without national implementation; directives set binding objectives but require member state transposition; decisions are binding on specific addressees (states, companies, or individuals). Use from_year to restrict to acts published from that year onwards (e.g. 2020 for recent acts). Data source: EU Publications Office EUR-Lex Cellar \u2014 official EU law repository, no auth required, open access.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "doc_type": {
    "description": "Type of EU legislative act: \"regulation\" (directly applicable in all EU member states), \"directive\" (binding on objectives, member states choose implementation means), \"decision\" (binding on specific addressees)",
    "enum": [
     "regulation",
     "directive",
     "decision"
    ],
    "type": "string"
   },
   "from_year": {
    "description": "Only return legislation from this year onwards (e.g. 2020). Omit to retrieve the most recent across all years.",
    "maximum": 2030,
    "minimum": 1950,
    "type": "integer"
   },
   "limit": {
    "description": "Maximum number of results to return (1\u201320, default 10)",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "doc_type"
  ],
  "type": "object"
 },
 "name": "eurlex.legislation.by_type",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Browse EU Legislation by Type"
}
