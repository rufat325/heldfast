{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "For one ABS dataflow, return its ordered dimensions and the valid codes for each. Use this to build a dataKey for get_data: the key has one dot-separated position per dimension, in the order returned here. Call this before get_data.",
 "inputSchema": {
  "examples": [
   {
    "dataflowId": "CPI"
   },
   {
    "dataflowId": "ABS_REGIONAL_LGA2021",
    "maxCodesPerDimension": 30
   }
  ],
  "properties": {
   "dataflowId": {
    "description": "Dataflow id from list_dataflows, e.g. \"CPI\".",
    "type": "string"
   },
   "maxCodesPerDimension": {
    "description": "Cap codes listed per dimension to keep output small (default 50).",
    "type": "number"
   }
  },
  "required": [
   "dataflowId"
  ],
  "type": "object"
 },
 "name": "dataflow_structure",
 "title": "Dataflow Structure",
 "version": "v1.0ef7b064"
}
