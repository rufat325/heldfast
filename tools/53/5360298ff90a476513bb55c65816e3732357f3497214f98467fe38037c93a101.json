{
 "description": "Adjust the available inventory for a specific inventory_item at a specific location by a delta (positive to increase, negative to decrease).",
 "inputSchema": {
  "properties": {
   "available_adjustment": {
    "description": "Delta to apply to available quantity (e.g. -3 to decrement by 3, +10 to increment by 10)",
    "type": "number"
   },
   "inventory_item_id": {
    "description": "Shopify inventory_item_id (numeric) \u2014 found on variant.inventory_item_id",
    "type": "number"
   },
   "location_id": {
    "description": "Shopify location_id (numeric)",
    "type": "number"
   }
  },
  "required": [
   "inventory_item_id",
   "location_id",
   "available_adjustment"
  ],
  "type": "object"
 },
 "name": "adjust_inventory"
}
