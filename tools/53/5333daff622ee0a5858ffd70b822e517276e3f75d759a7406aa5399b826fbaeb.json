{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Return the full text of an entire act (all scenes within it). Use this to read a full structural unit at once. Returned object contains nested scenes with their speeches and lines.",
 "inputSchema": {
  "examples": [
   {
    "act": 3,
    "author_id": "william-shakespeare",
    "work_slug": "hamlet"
   },
   {
    "act": 3,
    "author_id": "oscar-wilde",
    "work_slug": "an-ideal-husband"
   }
  ],
  "properties": {
   "act": {
    "description": "Act number.",
    "type": "number"
   },
   "author_id": {
    "description": "Author id (e.g., \"william-shakespeare\").",
    "type": "string"
   },
   "work_slug": {
    "description": "Work slug (e.g., \"hamlet\").",
    "type": "string"
   }
  },
  "required": [
   "author_id",
   "work_slug",
   "act"
  ],
  "type": "object"
 },
 "name": "get_act",
 "outputSchema": {
  "oneOf": [
   {
    "properties": {
     "error": {
      "description": "Error message when act not found",
      "type": "string"
     }
    },
    "required": [
     "error"
    ],
    "type": "object"
   },
   {
    "properties": {
     "act": {
      "description": "Act number",
      "type": "number"
     },
     "author_id": {
      "description": "Author id of the work",
      "type": "string"
     },
     "scene_count": {
      "description": "Number of scenes in the act",
      "type": "number"
     },
     "scenes": {
      "description": "Array of all scenes within the act",
      "type": "array"
     },
     "work_slug": {
      "description": "Work slug identifier",
      "type": "string"
     },
     "work_title": {
      "description": "Full title of the work",
      "type": "string"
     }
    },
    "required": [
     "author_id",
     "work_slug",
     "act",
     "scene_count",
     "scenes"
    ],
    "type": "object"
   }
  ],
  "type": "object"
 },
 "title": "Get Act",
 "version": "v1.b35704ad"
}
