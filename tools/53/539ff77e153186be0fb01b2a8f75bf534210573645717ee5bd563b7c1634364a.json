{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "DESTRUCTIVE: wipe and reinstall a Proxmox VPS from an OS template \u2014 ALL DATA ON THE VPS IS ERASED. Pick a `template_id` via `impreza_vps_list_templates` and set a new root password (min 8 chars). Always confirm with the customer before calling.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "password": {
    "description": "New root / administrator password (min 8 chars).",
    "type": "string"
   },
   "service_id": {
    "description": "VPS service id (numeric; from impreza_list_services).",
    "type": "string"
   },
   "template_id": {
    "description": "OS template id (from impreza_vps_list_templates).",
    "type": "number"
   }
  },
  "required": [
   "service_id",
   "template_id",
   "password"
  ],
  "type": "object"
 },
 "name": "impreza_vps_reinstall"
}
