{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Delete Promo Profile"
 },
 "description": "Delete a discount profile and resolve its codes \u2014 'detach' (default) keeps each code standalone; 'delete_codes' deletes unused codes but disables any with redemptions. Requires event_id + profile_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "action": {
    "enum": [
     "detach",
     "delete_codes"
    ],
    "type": "string"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "profile_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "profile_id"
  ],
  "type": "object"
 },
 "name": "delete_promo_profile",
 "title": "Delete Promo Profile"
}
