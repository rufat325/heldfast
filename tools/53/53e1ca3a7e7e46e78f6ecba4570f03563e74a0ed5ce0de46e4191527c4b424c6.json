{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the Vimeo video library through the Vimeo API (api.vimeo.com/videos) with a free-text query, plus page, per_page, sort, direction, and filter parameters, authenticated with a Vimeo access token. Returns matching Vimeo videos with title, description, duration, link and embed URLs, owner, and play statistics. Answers which videos hosted on Vimeo match a subject, title, or creator name.",
 "inputSchema": {
  "examples": [
   {
    "query": "javascript tutorial"
   },
   {
    "page": 1,
    "per_page": 10,
    "query": "motion design"
   }
  ],
  "properties": {
   "direction": {
    "type": "string"
   },
   "filter": {
    "type": "string"
   },
   "page": {
    "type": "number"
   },
   "per_page": {
    "type": "number"
   },
   "query": {
    "type": "string"
   },
   "sort": {
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "videos_search",
 "outputSchema": {
  "properties": {
   "data": {
    "description": "Array of matching videos",
    "items": {
     "properties": {
      "created_time": {
       "description": "Creation timestamp",
       "type": "string"
      },
      "description": {
       "description": "Video description",
       "type": [
        "string",
        "null"
       ]
      },
      "duration": {
       "description": "Duration in seconds",
       "type": "number"
      },
      "height": {
       "description": "Video height",
       "type": "number"
      },
      "name": {
       "description": "Video title",
       "type": "string"
      },
      "pictures": {
       "description": "Picture/thumbnail data",
       "type": "object"
      },
      "uri": {
       "description": "Video URI",
       "type": "string"
      },
      "user": {
       "description": "Uploader user object",
       "type": "object"
      },
      "width": {
       "description": "Video width",
       "type": "number"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "page": {
    "description": "Current page",
    "type": "number"
   },
   "paging": {
    "description": "Pagination links",
    "type": "object"
   },
   "per_page": {
    "description": "Items per page",
    "type": "number"
   },
   "total": {
    "description": "Total matching videos",
    "type": "number"
   }
  },
  "type": "object"
 },
 "title": "Videos Search",
 "version": "v1.375af5fe"
}
