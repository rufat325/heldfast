{
 "description": "Call any Nerve JSON-RPC method by name with params array. Use for methods not covered by dedicated tools (e.g. getRandomSeedByCount, getProposalInfo, swap/farm APIs).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "method": {
    "description": "JSON-RPC method name (e.g. info, getTx, getSwapPairInfo)",
    "type": "string"
   },
   "params": {
    "default": [],
    "description": "Array of positional parameters",
    "items": {},
    "type": "array"
   }
  },
  "required": [
   "method"
  ],
  "type": "object"
 },
 "name": "nerve_jsonrpc"
}
