{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Mint, list, or revoke CALL-ONLY delegated keys \u2014 hand a sub-agent a key that can only call the capabilities you allow, capped at a daily MESH spend. Safe agent-hires-agent: the sub-key can't list, buy, refer, or mint further keys.",
 "inputSchema": {
  "properties": {
   "action": {
    "enum": [
     "create",
     "list",
     "revoke"
    ],
    "type": "string"
   },
   "allow": {
    "description": "capability slugs this key may call (omit = all)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "daily_cap": {
    "description": "max MESH this key can spend per UTC day (omit = unlimited)",
    "type": "integer"
   },
   "id": {
    "description": "delegate id (for revoke)",
    "type": "string"
   },
   "label": {
    "description": "what this key is for, e.g. 'research-bot'",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "mesh_delegate",
 "title": "Delegated Keys"
}
