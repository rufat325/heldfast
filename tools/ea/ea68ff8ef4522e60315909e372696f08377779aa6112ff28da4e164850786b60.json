{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Assert how many elements match, polling until the count satisfies the predicate or timeoutMs elapses. Target EITHER by selector (counts querySelectorAll; visible:true counts visible only, visible:false counts hidden only) OR by accessibility role/name filters (role, name_contains, name_exact, visible, enabled, interactive). Provide at least one of equals, min, max. Returns: { ok, session_id, matched, actual }. Errors: EXPECTATION_FAILED (count predicate not met within timeoutMs \u2014 details carry expected + actual; retryable), TRANSPORT_UNSUPPORTED, NOT_RUNNING, BAD_ARGUMENT (no count predicate, or selector mixed with role filters).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "enabled": {
    "description": "Restrict to enabled (or disabled) matches (role mode).",
    "type": "boolean"
   },
   "equals": {
    "description": "The match count must equal this.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "interactive": {
    "description": "Restrict to interactive matches (role mode).",
    "type": "boolean"
   },
   "max": {
    "description": "The match count must be <= this.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "min": {
    "description": "The match count must be >= this.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "name_contains": {
    "description": "Substring the accessible name must contain (role mode).",
    "type": "string"
   },
   "name_exact": {
    "description": "Exact accessible name to match (role mode).",
    "type": "string"
   },
   "role": {
    "description": "Accessibility role to match (role mode).",
    "type": "string"
   },
   "selector": {
    "description": "CSS selector to count. Omit to count by role/name.",
    "minLength": 1,
    "type": "string"
   },
   "sessionId": {
    "description": "Target session id. Omit when a single session is running.",
    "type": "string"
   },
   "timeoutMs": {
    "description": "Max poll time in ms before EXPECTATION_FAILED (default 5000, clamped to 60000). 0 = check once.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "visible": {
    "description": "Count only visible (or hidden) matches.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "electron_expect_count",
 "title": "Expect a match count"
}
