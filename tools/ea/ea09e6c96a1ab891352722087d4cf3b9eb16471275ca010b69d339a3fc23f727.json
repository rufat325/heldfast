{
 "description": "Creates a new link ad. Mirrors the web \"add link\" flow. Returns the new link's id. Requires a paid membership (Bronze/Silver/Gold); returns an error for free accounts.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "title": {
    "description": "Link ad title.",
    "type": "string"
   },
   "url": {
    "description": "HTTPS destination URL for the link ad.",
    "type": "string"
   }
  },
  "required": [
   "url",
   "title"
  ],
  "type": "object"
 },
 "name": "create_link",
 "title": "Create a link ad"
}
