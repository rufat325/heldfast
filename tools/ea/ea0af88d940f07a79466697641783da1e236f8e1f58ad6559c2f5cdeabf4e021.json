{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Read the signed-in user's health profile (age, biological sex, weight, conditions, medications, goals, lifestyle). Returns null if no profile has been set. Requires the user to be signed in (`supstack login`).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {},
  "type": "object"
 },
 "name": "supstack_profile_get"
}
