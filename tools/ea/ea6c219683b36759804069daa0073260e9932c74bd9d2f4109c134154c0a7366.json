{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Submit a task ID and wait for it to complete. Polls automatically. Returns the final task result including output URLs when successful.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "poll_interval": {
    "description": "Seconds between polls. Default: 15. Min: 5.",
    "type": "number"
   },
   "task_id": {
    "description": "Task ID from any generate_* tool.",
    "type": "string"
   },
   "timeout": {
    "description": "Max seconds to wait. Default: 300 (5 min). Max: 600.",
    "type": "number"
   }
  },
  "required": [
   "task_id"
  ],
  "type": "object"
 },
 "name": "wait_for_runway_task"
}
