{
 "description": "Create a custom property on a space. Fails if the key already exists \u2014 use update_space_property instead.",
 "inputSchema": {
  "properties": {
   "key": {
    "type": "string"
   },
   "spaceKey": {
    "type": "string"
   },
   "value": {}
  },
  "required": [
   "spaceKey",
   "key",
   "value"
  ],
  "type": "object"
 },
 "name": "create_space_property"
}
