{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "grant_env"
 },
 "description": "Grant a member (by email) or service token (by id) access to an environment's data key. Re-wraps the DEK to the grantee \u2014 the caller must already hold the key.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "app": {
    "description": "application slug or id (or set via configure_project)",
    "type": "string"
   },
   "env": {
    "description": "environment slug or id (a service token infers its own)",
    "type": "string"
   },
   "group": {
    "description": "target a group environment instead of an app",
    "type": "string"
   },
   "org": {
    "description": "organization slug or id (defaults to a lone org)",
    "type": "string"
   },
   "token": {
    "description": "service token id (skt_\u2026)",
    "type": "string"
   },
   "user": {
    "description": "org member email",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "grant_env"
}
