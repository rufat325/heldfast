{
 "description": "Generate multiple assets in sequence with full pipeline (generate \u2192 bg remove \u2192 save). Each asset failure does not stop others. Includes 300ms rate limit delay between assets.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "assets": {
    "description": "Array of assets to generate",
    "items": {
     "properties": {
      "model": {
       "description": "Model to use",
       "type": "string"
      },
      "name": {
       "description": "Asset filename (without extension)",
       "type": "string"
      },
      "negative_prompt": {
       "description": "What to exclude",
       "type": "string"
      },
      "output_dir": {
       "description": "Directory to save the asset",
       "type": "string"
      },
      "prompt": {
       "description": "Image description",
       "type": "string"
      },
      "remove_bg": {
       "description": "Remove background (default: true)",
       "type": "boolean"
      },
      "size": {
       "description": "Image size",
       "type": "string"
      },
      "style": {
       "description": "Broad style family. One of: any, digital_illustration, icon, realistic_image, vector_illustration, logo_raster. A specific look (e.g. pixel_art, kawaii, b_and_w) belongs in `substyle`, but passing one here is accepted and routed to the right field.",
       "type": "string"
      },
      "substyle": {
       "description": "Specific look within the family \u2014 104 values, validated strictly. Common ones: 2d_art_poster, 3d, 80s, glow, grain, hand_drawn, infantile_sketch, kawaii, pixel_art, psychedelic, seamless, voxel. An invalid value returns the full list.",
       "type": "string"
      }
     },
     "required": [
      "name",
      "prompt",
      "output_dir"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "assets"
  ],
  "type": "object"
 },
 "name": "recraft_batch_generate"
}
