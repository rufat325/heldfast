{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "\"Find protein [name]\" / \"look up [gene] in UniProt\" / \"BRCA1 / TP53 / insulin protein info\" / \"all proteins for [organism]\" \u2014 UniProtKB search via Lucene-style queries (e.g. gene:BRCA1 AND organism_id:9606 for human BRCA1). UniProt is the authoritative protein-sequence-and-function database \u2014 use for protein characterization, function annotation, sequence retrieval, and cross-references to PDB/GO/PubMed.",
 "inputSchema": {
  "examples": [
   {
    "query": "gene:BRCA1 AND organism_id:9606"
   },
   {
    "format": "fasta",
    "query": "protein_name:hemoglobin AND organism_id:9606",
    "size": 50
   }
  ],
  "properties": {
   "fields": {
    "description": "Comma-sep returned fields (json/tsv only).",
    "type": "string"
   },
   "format": {
    "description": "json (default) | tsv | fasta | xml",
    "type": "string"
   },
   "query": {
    "description": "Lucene-style expression, passed bare \u2014 do NOT wrap the whole thing in quotes. Fielded terms combine with AND, e.g. gene:BRCA1 AND organism_id:9606. Quotes are only for a multi-word value, e.g. organism_name:\"Homo sapiens\". Plain-English questions match poorly because every token must appear; prefer keywords.",
    "type": "string"
   },
   "size": {
    "description": "1-500 (default 25)",
    "type": "number"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search",
 "outputSchema": {
  "description": "UniProtKB search results",
  "properties": {
   "facets": {
    "description": "Search facets for filtering",
    "type": "array"
   },
   "results": {
    "description": "Array of search result entries",
    "items": {
     "description": "Protein entry result",
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "title": "Search",
 "version": "v1.cadb2b16"
}
