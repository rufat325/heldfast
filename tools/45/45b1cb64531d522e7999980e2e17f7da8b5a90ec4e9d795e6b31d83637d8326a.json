{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Robinhood Chain KOL activity leaderboard \u2014 KOLs ranked by trade count, then net ETH flow, over the chosen window. net_eth is buy\u2212sell flow (not realized PnL). Tier: BASIC.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "default": 50,
    "description": "Number of KOLs to return (1-100)",
    "maximum": 100,
    "minimum": 1,
    "type": "number"
   },
   "period": {
    "default": "24h",
    "description": "Rolling window",
    "enum": [
     "24h",
     "7d",
     "30d"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "rhc_kol_leaderboard"
}
