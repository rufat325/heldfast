{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Press several keys in order (same names as key_press).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "keys": {
    "description": "Keys in press order",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "serial": {
    "description": "Device serial (defaults to session or the only device)",
    "type": "string"
   }
  },
  "required": [
   "keys"
  ],
  "type": "object"
 },
 "name": "key_sequence",
 "title": "Press key sequence"
}
