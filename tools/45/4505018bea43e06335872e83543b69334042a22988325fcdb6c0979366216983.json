{
 "description": "Stop tracking a keyword and drop its mentions. Prefer update_keyword with is_active=false if the user may want the history back.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "delete_keyword"
}
