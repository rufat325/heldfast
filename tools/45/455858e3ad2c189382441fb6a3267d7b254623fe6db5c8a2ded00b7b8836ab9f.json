{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Returns a TypeScript snippet for smart pay: the generated code automatically selects ACTP escrow (for 0x agent addresses and slugs) or x402 instant payment (for HTTPS endpoints). Use for direct payments without negotiation.",
 "inputSchema": {
  "properties": {
   "amount": {
    "description": "USDC amount to send (e.g. \"1\", \"0.50\")",
    "type": "string"
   },
   "network": {
    "default": "testnet",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   },
   "service": {
    "type": "string"
   },
   "target": {
    "description": "Agent address (0x...), HTTPS endpoint, or agent slug. Smart pay selects ACTP vs x402 automatically.",
    "type": "string"
   }
  },
  "required": [
   "target",
   "amount"
  ],
  "type": "object"
 },
 "name": "agirails_pay"
}
