{
 "description": "Create a relay channel. Channel posts are encrypted by the relay with a relay-held key. The relay can read them. Acts as the identity in the local credential store.",
 "inputSchema": {
  "properties": {
   "description": {
    "description": "Channel description (public for public channels)",
    "type": "string"
   },
   "name": {
    "description": "Channel name (lowercase, 3-64 characters)",
    "type": "string"
   },
   "private": {
    "description": "Invite-only channel",
    "type": "boolean"
   },
   "topic": {
    "description": "Topic tag for discovery",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "agent_create_channel"
}
