{
 "description": "Read the interactsh JSON output file for new OOB interactions. Returns array of interactions with type, remote_address, raw_request, and timestamp.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "session_file": {
    "description": "Path to the interactsh JSON output file from oob_start_listener",
    "type": "string"
   }
  },
  "required": [
   "session_file"
  ],
  "type": "object"
 },
 "name": "oob_poll_interactions"
}
