{
 "description": "Boot an iOS Simulator device. Opens the Simulator app. Use simulator_list_devices to find device names/UDIDs.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "deviceId": {
    "description": "Device UDID or name to boot",
    "type": "string"
   },
   "waitForBoot": {
    "description": "Wait until device is fully booted before returning (default: true)",
    "type": "boolean"
   }
  },
  "required": [
   "deviceId"
  ],
  "type": "object"
 },
 "name": "simulator_boot"
}
