{
 "description": "Monthly fund flows from N-PORT Part B: sales, reinvestments, redemptions and net flow, in US dollars. Redemptions are positive outflow magnitudes; netFlowUsd already accounts for the sign.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "since": {
    "type": "string"
   },
   "ticker": {
    "type": "string"
   },
   "until": {
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "get_flows"
}
