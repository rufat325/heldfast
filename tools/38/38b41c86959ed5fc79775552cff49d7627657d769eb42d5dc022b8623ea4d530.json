{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Full-text search across Wikipedia article titles and content. Returns matching page titles, highlighted excerpts, short descriptions, and thumbnails, ranked by relevance. Select the language edition with lang (default en). Data: MediaWiki REST API (wikipedia.org/w/rest.php/v1), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lang": {
    "description": "Wikipedia language edition subdomain code (default en, e.g. de, fr, ja, zh-yue)",
    "pattern": "^[a-z]{2,3}(-[a-z0-9]{1,8})?$",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of results to return, 1-50 (default 10)",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Full-text search query (e.g. \"einstein relativity\")",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "wikimedia-rest.pages.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Wikipedia Full-Text Search"
}
