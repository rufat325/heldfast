{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List/search contacts (subscribers) in an ActiveCampaign account. Filter by exact email or a free-text search across name/email. Returns contacts with IDs, emails, names, and meta.total.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-activecampaign-api-key",
    "account": "myaccount",
    "email": "john.doe@example.com"
   },
   {
    "_apiKey": "your-activecampaign-api-key",
    "account": "myaccount",
    "limit": 50,
    "search": "john"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "ActiveCampaign API key (UI: Settings \u2192 Developer).",
    "type": "string"
   },
   "account": {
    "description": "Your ActiveCampaign account URL or subdomain, e.g. \"https://youraccount.api-us1.com\" or just \"youraccount\".",
    "type": "string"
   },
   "email": {
    "description": "Filter to an exact email address.",
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 20, max 100).",
    "type": "number"
   },
   "search": {
    "description": "Free-text search across name and email.",
    "type": "string"
   }
  },
  "required": [
   "_apiKey",
   "account"
  ],
  "type": "object"
 },
 "name": "list_contacts",
 "title": "List Contacts",
 "version": "v1.6cde4240"
}
