{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the full option chain for an underlying stock/ETF/index, with strike, bid/ask/mid/last, volume, open interest, IV and greeks (delta/gamma/theta/vega). Filter by expiration, days-to-expiration, delta, side, and liquidity. Example: marketdata_option_chain({ underlyingSymbol: \"AAPL\", expiration: \"2026-01-16\", side: \"call\", minOpenInterest: 100, _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-marketdata-app-api-key",
    "expiration": "2026-01-16",
    "minOpenInterest": 100,
    "side": "call",
    "underlyingSymbol": "AAPL"
   },
   {
    "_apiKey": "your-marketdata-app-api-key",
    "delta": "0.30",
    "dte": 30,
    "strikeLimit": 10,
    "underlyingSymbol": "SPY"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Market Data App API token. Get one at https://www.marketdata.app/pricing/ (auth docs: https://www.marketdata.app/docs/api/authentication)",
    "type": "string"
   },
   "date": {
    "description": "Historical snapshot date (YYYY-MM-DD) for a past chain. Optional (defaults to live).",
    "type": "string"
   },
   "delta": {
    "description": "Delta-based strike selection, e.g. \"0.30\" or a range \".20-.40\". Optional.",
    "type": "string"
   },
   "dte": {
    "description": "Filter to contracts with this many days to expiration (nearest match). Optional.",
    "type": "number"
   },
   "expiration": {
    "description": "Filter to a specific expiration date (YYYY-MM-DD) or \"all\" for every expiration. Optional.",
    "type": "string"
   },
   "minOpenInterest": {
    "description": "Only return contracts with at least this open interest. Optional.",
    "type": "number"
   },
   "side": {
    "description": "Contract side: \"call\" or \"put\". Optional (returns both if omitted).",
    "enum": [
     "call",
     "put"
    ],
    "type": "string"
   },
   "strikeLimit": {
    "description": "Limit the number of strikes returned (nearest to the money). Optional.",
    "type": "number"
   },
   "underlyingSymbol": {
    "description": "Underlying ticker symbol, e.g. \"AAPL\", \"SPY\", \"TSLA\"",
    "type": "string"
   }
  },
  "required": [
   "underlyingSymbol",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "marketdata_option_chain",
 "title": "Marketdata Option Chain",
 "version": "v1.02fd6de2"
}
