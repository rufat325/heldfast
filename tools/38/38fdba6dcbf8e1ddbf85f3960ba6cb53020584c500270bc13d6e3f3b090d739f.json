{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Inspect image metadata"
 },
 "description": "Inspect an image without converting it: dimensions, format, colour space, alpha, distinct colour count and run density \u2014 and which vectorize mode suits it, including whether bit-exact output is achievable. Call this first when choosing between `pixel` and `trace`.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "Path to the image to inspect",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "image_info"
}
