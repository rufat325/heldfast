{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search aggregated human genetic-variant annotations on MyVariant.info. Accepts an rsID (\"rs58991260\") or a fielded query (\"dbnsfp.genename:CDK2\", \"clinvar.rcv.clinical_significance:pathogenic\"). A bare HGVS id does NOT match here \u2014 look one up with the variant tool instead. Each hit merges dbSNP, ClinVar clinical significance, CADD/dbNSFP deleteriousness scores, and gnomAD population allele frequencies. Returns { total, hits, assembly }; hit._id is the HGVS id you can pass to the variant tool, and its coordinates are in the assembly you asked for \u2014 hg19 unless you say otherwise.",
 "inputSchema": {
  "examples": [
   {
    "query": "rs58991260"
   },
   {
    "fields": "dbsnp,clinvar,cadd.phred,gnomad_genome.af",
    "query": "dbnsfp.genename:CDK2",
    "size": 20
   }
  ],
  "properties": {
   "assembly": {
    "description": "Genome build for the coordinates you pass AND the ids you get back: \"hg19\" (GRCh37, the default) or \"hg38\" (GRCh38). gnomAD and Ensembl coordinates are GRCh38.",
    "type": "string"
   },
   "fields": {
    "description": "Comma-separated return fields (default: all). e.g. \"dbsnp,clinvar,cadd.phred,gnomad_genome.af\".",
    "type": "string"
   },
   "query": {
    "description": "rsID, HGVS id, or fielded query. e.g. \"rs58991260\", \"chr1:g.218631822G>A\", \"dbnsfp.genename:CDK2\".",
    "type": "string"
   },
   "size": {
    "description": "Max hits to return, 1-1000 (default 10).",
    "type": "number"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "query",
 "title": "Query",
 "version": "v1.7595e90f"
}
