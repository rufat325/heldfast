{
 "description": "Checks a counterparty agent's stated security posture against the MASSAT governance framework's actual requirements \u2014 flags claims that don't hold up. (x402: $0.25 USDC per call)",
 "inputSchema": {
  "properties": {
   "task": {
    "description": "Task/query text for this SKU",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "security.massat-conformance"
}
