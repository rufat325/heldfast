{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Real-time predicted arrivals at a stop. Pass the naptanId from stop_search (e.g. \"940GZZLUOXC\"). Results are sorted by timeToStation (seconds away), nearest first.",
 "inputSchema": {
  "examples": [
   {
    "naptanId": "940GZZLUOXC"
   }
  ],
  "properties": {
   "naptanId": {
    "description": "Stop NaPTAN id, obtained from stop_search.",
    "type": "string"
   }
  },
  "required": [
   "naptanId"
  ],
  "type": "object"
 },
 "name": "arrivals",
 "title": "Arrivals",
 "version": "v1.49dcd1a4"
}
