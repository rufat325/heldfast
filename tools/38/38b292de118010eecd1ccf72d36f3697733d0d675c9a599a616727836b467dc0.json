{
 "description": "Generate a new signing secret for a webhook. The old secret will immediately stop working. Returns the new secret (shown only once).",
 "inputSchema": {
  "properties": {
   "webhook_id": {
    "description": "Webhook ID to rotate secret for",
    "type": "number"
   }
  },
  "required": [
   "webhook_id"
  ],
  "type": "object"
 },
 "name": "barevalue_webhook_rotate_secret"
}
