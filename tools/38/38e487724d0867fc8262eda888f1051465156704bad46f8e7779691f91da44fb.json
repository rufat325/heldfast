{
 "description": "Resolve or reopen a single comment by ID. Hidden AI comments (visibility gate) return not-found.",
 "inputSchema": {
  "properties": {
   "comment_id": {
    "type": "string"
   },
   "status": {
    "enum": [
     "open",
     "resolved"
    ],
    "type": "string"
   }
  },
  "required": [
   "comment_id",
   "status"
  ],
  "type": "object"
 },
 "name": "set_comment_status"
}
