{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Get the status and result of an analytics export. Returns status (pending, processing, completed, failed), progress (total_videos, processed_videos), and for completed exports: a presigned Excel download URL (valid 15 minutes) and a shareable report page URL. Poll this after social_api_create_export to check completion.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "export_id": {
    "description": "Export ID (from social_api_create_export or social_api_list_exports)",
    "type": "string"
   }
  },
  "required": [
   "export_id"
  ],
  "type": "object"
 },
 "name": "social_api_get_export",
 "title": "Get Export"
}
