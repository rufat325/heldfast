{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Return the live schema for one BatchOpCatalog op. Use before composing unfamiliar batch ops.",
 "inputSchema": {
  "properties": {
   "includeExamples": {
    "description": "Include example op payloads when available. Default false.",
    "type": "boolean"
   },
   "op": {
    "description": "Batch op name, e.g. create_frame, set_fills, rename_node, map.",
    "type": "string"
   }
  },
  "required": [
   "op"
  ],
  "type": "object"
 },
 "name": "get_batch_op_spec",
 "outputSchema": {
  "properties": {
   "category": {
    "type": "string"
   },
   "description": {
    "type": "string"
   },
   "inputSchema": {
    "type": "object"
   },
   "mutates": {
    "type": "boolean"
   },
   "name": {
    "type": "string"
   },
   "paramKeys": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "readOnly": {
    "type": "boolean"
   }
  },
  "type": "object"
 }
}
