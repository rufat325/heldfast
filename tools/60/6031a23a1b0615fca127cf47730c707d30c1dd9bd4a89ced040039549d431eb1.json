{
 "description": "Create a new minimal Jetpack Compose app project (single module, version catalog, Gradle wrapper included) ready to build with build_app.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "appName": {
    "description": "App name, e.g. 'My Notes'",
    "pattern": "^[A-Za-z][\\w -]*$",
    "type": "string"
   },
   "directory": {
    "description": "Directory to create the project in (must not exist or be empty)",
    "type": "string"
   },
   "packageName": {
    "description": "Application id, e.g. 'com.example.mynotes'",
    "pattern": "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)+$",
    "type": "string"
   }
  },
  "required": [
   "directory",
   "appName",
   "packageName"
  ],
  "type": "object"
 },
 "name": "scaffold_android_project",
 "title": "Scaffold Android project"
}
