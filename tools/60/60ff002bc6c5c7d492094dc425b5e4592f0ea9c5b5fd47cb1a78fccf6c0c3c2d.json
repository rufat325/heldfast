{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Update Production Item"
 },
 "description": "Update a production item in ANY area of a live event (entity: milestones, timeline, schedule_items, tasks, projects, vendors, talent, roles, inventory, programming, meals, places, modes, transactions, invoices, teams, receipts) \u2014 change status, rename, edit notes, archive/restore (active), or set details via a `fields` object. Requires event_id + entity + item_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "active": {
    "type": "boolean"
   },
   "entity": {
    "enum": [
     "vendors",
     "talent",
     "tasks",
     "projects",
     "schedule",
     "receipts",
     "roles",
     "inventory",
     "programming",
     "meals",
     "places",
     "milestones",
     "timeline",
     "schedule_items",
     "modes",
     "transactions",
     "invoices",
     "teams",
     "buildUnits",
     "scholarships",
     "invitations"
    ],
    "type": "string"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "fields": {
    "additionalProperties": {},
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "item_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "name": {
    "maxLength": 300,
    "minLength": 1,
    "type": "string"
   },
   "notes": {
    "maxLength": 4000,
    "type": "string"
   },
   "status": {
    "maxLength": 60,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "entity",
   "item_id"
  ],
  "type": "object"
 },
 "name": "update_production_item",
 "title": "Update Production Item"
}
