{
 "description": "List recent agent runner jobs for the workspace \u2014 shows status (pending, running, done, failed), intent, and result summary.",
 "inputSchema": {
  "properties": {
   "workspaceId": {
    "description": "Workspace ID. Defaults to your active workspace.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_agent_jobs"
}
