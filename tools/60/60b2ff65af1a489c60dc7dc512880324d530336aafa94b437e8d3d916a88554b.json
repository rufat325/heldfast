{
 "annotations": {
  "readOnlyHint": true,
  "title": "Find tags"
 },
 "description": "Paginated, sortable list of tags in the taxonomy. Filter by `axis` to separate MARKETS (a business someone is in) from CAPABILITIES (the name of a resource in somebody's OpenAPI) \u2014 the single most useful distinction in the vocabulary, and a coin flip without it. A tag can carry more than one axis, so axis=market means market is ONE of its axes. Use apis_io_search first for a cross-type overview.",
 "inputSchema": {
  "properties": {
   "axis": {
    "description": "market | capability | persona | posture. Comma-separated for more than one.",
    "type": "string"
   },
   "band": {
    "description": "canonical | established | emerging | niche | stale | posture.",
    "type": "string"
   },
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "fields": {
    "description": "Return exactly these top-level keys (overrides view).",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "limit": {
    "default": 25,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "min_api_count": {
    "description": "Only tags applied to at least this many APIs.",
    "minimum": 0,
    "type": "integer"
   },
   "page": {
    "default": 1,
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "description": "Free text over name + description.",
    "type": "string"
   },
   "sort": {
    "description": "Order results. Default: relevance with a query, composite (quality) when browsing. `demand` = Fortune-1000 adoption, scoped to the query.",
    "enum": [
     "relevance",
     "composite",
     "name",
     "created",
     "modified",
     "api_count",
     "demand"
    ],
    "type": "string"
   },
   "view": {
    "default": "summary",
    "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity).",
    "enum": [
     "summary",
     "full"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "find_tags"
}
