{
 "description": "Auto-generate article topic suggestions from query analytics \u2014 surfaces content gaps, low-confidence queries, and trending topics that don't have good coverage.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "days": {
    "default": 7,
    "description": "Look back N days for query analytics (default: 7)",
    "maximum": 90,
    "minimum": 1,
    "type": "number"
   },
   "limit": {
    "default": 5,
    "description": "Maximum suggestions to return (default: 5)",
    "maximum": 20,
    "minimum": 1,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "suggest_ideas"
}
