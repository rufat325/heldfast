{
 "description": "Retrieve one artwork in full: technical specification, availability, price, the physics measured from its master, and, when available, the curatorial capsule. Accepts the identifier or the title \u2014 and when a title belongs to more than one work, it returns them all rather than guessing.",
 "inputSchema": {
  "properties": {
   "name": {
    "description": "Artwork identifier, or its name. An ambiguous name returns every candidate.",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "get_artwork"
}
