{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the FBI Wanted API for wanted persons, fugitives, and missing/victim listings. Filter by name, FBI field office, sex, race, or person classification. Returns title, charges, physical description, reward, field offices, and photos for each match (api.fbi.gov, public US Government data)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "field_office": {
    "description": "Filter by FBI field office slug handling the case (e.g. \"seattle\", \"miami\", \"losangeles\").",
    "type": "string"
   },
   "page": {
    "description": "Page number for pagination (default 1).",
    "minimum": 1,
    "type": "integer"
   },
   "page_size": {
    "description": "Results per page (default 20, max 50).",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "person_classification": {
    "description": "Filter by classification, e.g. \"Main\" (the wanted subject) or \"Victim\".",
    "type": "string"
   },
   "race": {
    "description": "Filter by race, lowercase (e.g. \"white\", \"black\", \"hispanic\", \"asian\", \"native\").",
    "type": "string"
   },
   "sex": {
    "description": "Filter by sex (lowercase values only).",
    "enum": [
     "male",
     "female"
    ],
    "type": "string"
   },
   "title": {
    "description": "Filter by name/title, partial case-insensitive match (e.g. \"smith\").",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "fbiwanted.persons.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Search FBI Wanted Persons"
}
