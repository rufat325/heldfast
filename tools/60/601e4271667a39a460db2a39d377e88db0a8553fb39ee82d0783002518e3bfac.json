{
 "description": "Get internal link structure analysis for a specific page \u2014 inbound/outbound links, link depth, and authority metrics. Requires a page_id \u2014 get it from the pages list in `get_audit`.",
 "inputSchema": {
  "properties": {
   "page_id": {
    "description": "From get_audit pages list",
    "type": "string"
   }
  },
  "required": [
   "page_id"
  ],
  "type": "object"
 },
 "name": "get_link_graph"
}
