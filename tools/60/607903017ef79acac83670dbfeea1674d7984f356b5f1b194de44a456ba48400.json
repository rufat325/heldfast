{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Get one database by id, including its name, type and connection details.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "databaseId": {
    "description": "Database id, as returned by ws_db_list.",
    "minLength": 1,
    "type": "string"
   },
   "hostingId": {
    "description": "Hosting id or uuid the database belongs to.",
    "minLength": 1,
    "type": "string"
   },
   "userId": {
    "default": "self",
    "description": "Account id, or the literal \"self\" for the authenticated account. Defaults to \"self\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "hostingId",
   "databaseId"
  ],
  "type": "object"
 },
 "name": "ws_db_get",
 "title": "Get database"
}
