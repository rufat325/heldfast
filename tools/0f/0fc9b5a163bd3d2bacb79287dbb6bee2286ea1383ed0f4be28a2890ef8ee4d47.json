{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Validate overfitting (CSCV)"
 },
 "description": "Probability of backtest overfitting by CSCV over the returns of every variant tried. A deflated Sharpe or overfitting probability above or below any threshold is not admission to anything and is not a forecast.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "matrix": {
    "items": {
     "items": {
      "type": "number"
     },
     "type": "array"
    },
    "maxItems": 20000,
    "minItems": 2,
    "type": "array"
   },
   "max_combinations": {
    "exclusiveMinimum": 0,
    "maximum": 2000,
    "type": "integer"
   },
   "n_splits": {
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "seed": {
    "type": "number"
   }
  },
  "required": [
   "matrix"
  ],
  "type": "object"
 },
 "name": "validate_overfitting",
 "title": "Validate overfitting (CSCV)"
}
