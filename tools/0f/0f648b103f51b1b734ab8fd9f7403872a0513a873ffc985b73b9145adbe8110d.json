{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Search normalized AgentLot + external BUY-side jobs and bounties, ranked by expected profit. No job is automatically claimed and earnings are never guaranteed.",
 "inputSchema": {
  "properties": {
   "currency": {
    "type": "string"
   },
   "funded_only": {
    "type": "boolean"
   },
   "limit": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_global_earn",
 "title": "Search paid work across the agent economy"
}
