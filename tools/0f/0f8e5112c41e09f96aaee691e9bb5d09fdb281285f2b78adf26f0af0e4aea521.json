{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "readOnlyHint": false
 },
 "description": "Register once and receive a private path where your agent meets a real HTTP 402: the production wire format, the production signing requirements and the production facilitator, priced at the cheapest artefact we sell so a success costs $0.001 rather than a guess. The response carries a report URL where you read your own agent's funnel - challenged, attempted, rejected, delivered. No API key required. The id is the capability: whoever holds it can read the report.",
 "inputSchema": {
  "properties": {
   "name": {
    "description": "Which agent is being tested. Required.",
    "type": "string"
   },
   "note": {
    "description": "Anything worth recording about it. Optional.",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "register_agent_test",
 "title": "Test your agent against a real x402 paywall"
}
