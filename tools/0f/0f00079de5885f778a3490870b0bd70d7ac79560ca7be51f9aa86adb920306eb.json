{
 "_meta": {
  "com.oxinsider/docs-group": "large_trades"
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Replay Public Events"
 },
 "description": "Replay durable public large-trade events strictly after an opaque cursor, in commit order (a page never reaches past the oldest open write transaction, so a late-committing lower id is delivered later instead of skipped). Filters narrow the stream in place and are bound to the cursor: a cursor issued under other filters answers error.reason cursor_expired, so restart without a cursor. expand=trade adds the public trade read to every event, so a page needs no per-event get_whale_trade call.\n\nArgs:\n  - cursor (string, optional): Opaque event replay cursor\n  - limit (number, 1-100, default 50)\n  - trader (string, optional): wallet address, trd_ id or username\n  - condition_id (string, optional): raw condition_id or mkt_ id\n  - min_grade (string, optional): S, A, B, C, D or F\n  - min_size (number, optional): minimum size in USD\n  - expand (string, optional): \"trade\"\n\nReturns: Event replay window with retention and completeness metadata; each event carries trade when expanded.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "condition_id": {
    "description": "Only this market's trades: raw condition_id or mkt_ id (bound to the cursor)",
    "type": "string"
   },
   "cursor": {
    "description": "Opaque event replay cursor; bound to the filters the walk ran with",
    "type": "string"
   },
   "expand": {
    "description": "trade: add the public trade read to every event, so no per-event detail call is needed",
    "enum": [
     "trade"
    ],
    "type": "string"
   },
   "limit": {
    "default": 50,
    "description": "Max events (1-100, default 50)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "min_grade": {
    "description": "Only wallets at this grade or better (bound to the cursor)",
    "enum": [
     "S",
     "A",
     "B",
     "C",
     "D",
     "F"
    ],
    "type": "string"
   },
   "min_size": {
    "description": "Only trades of at least this size in USD (bound to the cursor)",
    "minimum": 0,
    "type": "number"
   },
   "trader": {
    "description": "Only this wallet's trades: address, trd_ id or username (bound to the cursor)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_event_replay_since",
 "title": "Replay Public Events"
}
