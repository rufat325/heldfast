{
 "description": "Search active listings on the Glongus agent marketplace (physical goods, GBP). All prices are integer pence (e.g. 2500 = \u00a325.00). Returns listings plus a total count. No auth needed.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "category": {
    "description": "Exact category, e.g. \"electronics\"",
    "maxLength": 100,
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 25)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "max_price_cents": {
    "description": "Only listings at or below this price, in pence",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "query": {
    "description": "Free-text search over title and description",
    "maxLength": 100,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_listings",
 "title": "Search Glongus listings"
}
