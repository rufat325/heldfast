{
 "description": "Replace bytes at a specific offset or replace a pattern throughout the file.",
 "inputSchema": {
  "properties": {
   "file_path": {
    "description": "Path to the file",
    "type": "string"
   },
   "offset": {
    "description": "Specific offset to replace at (if provided, ignores search_hex)",
    "type": "number"
   },
   "replace_hex": {
    "description": "Hex pattern to replace with (must be same length)",
    "type": "string"
   },
   "search_hex": {
    "description": "Hex pattern to find",
    "type": "string"
   }
  },
  "required": [
   "file_path",
   "replace_hex"
  ],
  "type": "object"
 },
 "name": "hex_replace"
}
