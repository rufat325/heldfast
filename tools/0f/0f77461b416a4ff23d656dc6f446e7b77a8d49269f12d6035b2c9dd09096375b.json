{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Look up a URL in the URLhaus malware database to check if it is known to host or distribute malware. Returns threat category, status, blacklist status, and tags.",
 "inputSchema": {
  "examples": [
   {
    "url": "http://malicious-site.com/payload.exe"
   },
   {
    "url": "https://phishing-domain.net/fake-login.html"
   }
  ],
  "properties": {
   "url": {
    "description": "The full URL to look up (e.g. \"http://example.com/malware.exe\").",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "lookup_url",
 "outputSchema": {
  "properties": {
   "blacklists": {
    "description": "Blacklist detection status by various services",
    "type": [
     "object",
     "null"
    ]
   },
   "date_added": {
    "description": "Date when URL was added to database",
    "type": [
     "string",
     "null"
    ]
   },
   "id": {
    "description": "URLhaus ID for this URL",
    "type": [
     "string",
     "null"
    ]
   },
   "query_status": {
    "description": "Status of the query (e.g. 'ok', 'no_results')",
    "type": "string"
   },
   "tags": {
    "description": "Associated tags for the URL",
    "items": {
     "type": "string"
    },
    "type": [
     "array",
     "null"
    ]
   },
   "threat": {
    "description": "Threat category (e.g. 'malware', 'phishing')",
    "type": [
     "string",
     "null"
    ]
   },
   "url_status": {
    "description": "Status of the URL (e.g. 'online', 'offline')",
    "type": [
     "string",
     "null"
    ]
   }
  },
  "required": [
   "query_status",
   "id",
   "url_status",
   "date_added",
   "threat",
   "tags",
   "blacklists"
  ],
  "type": "object"
 },
 "title": "Lookup Url",
 "version": "v1.bb0a47ce"
}
