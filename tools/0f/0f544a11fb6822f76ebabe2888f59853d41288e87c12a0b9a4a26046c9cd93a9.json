{
 "description": "Lint one or more @mostajs/orm EntitySchemas with the built-in conceptual validator (24 rules). Pass schemas as JSON. (sourceRoot cross-file rules are only available in local/stdio mode.)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "schemas": {
    "description": "Array of EntitySchema objects (JSON)",
    "type": "array"
   },
   "sourceRoot": {
    "description": "Project src root for cross-file rules \u2014 IGNORED on the hosted server",
    "type": "string"
   }
  },
  "required": [
   "schemas"
  ],
  "type": "object"
 },
 "name": "mostajs_validate"
}
