{
 "description": "Search the memory system. Returns scored memories ranked by relevance, importance, recency, and decay.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "description": "Max results (default 5, max 50)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   },
   "memory_types": {
    "description": "Filter by memory type",
    "items": {
     "enum": [
      "episodic",
      "semantic",
      "procedural",
      "self_model",
      "introspective"
     ],
     "type": "string"
    },
    "type": "array"
   },
   "min_decay": {
    "description": "Minimum decay factor (filters out faded memories)",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "min_importance": {
    "description": "Minimum importance threshold",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "query": {
    "description": "Text to search against memory summaries and content",
    "type": "string"
   },
   "related_user": {
    "description": "Filter by related user/agent ID",
    "type": "string"
   },
   "related_wallet": {
    "description": "Filter by associated Solana wallet address",
    "type": "string"
   },
   "skip_expansion": {
    "description": "Skip LLM query expansion for faster recall (saves ~500-800ms)",
    "type": "boolean"
   },
   "tags": {
    "description": "Tags to filter by (matches any)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "track_access": {
    "description": "Whether to update access timestamps (default true)",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "recall_memories"
}
