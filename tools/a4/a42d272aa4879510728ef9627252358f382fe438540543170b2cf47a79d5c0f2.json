{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": false,
  "title": "Arrange layers"
 },
 "description": "Arrange your own layers project: show or hide a layer, move or resize it (box as [left, top, right, bottom] in the base picture's pixels), change its stacking order, or reset it back to where the split put it. Saves immediately through the same checks the Switch workspace uses, so the app shows the same thing.",
 "inputSchema": {
  "properties": {
   "changes": {
    "description": "One entry per layer you are changing.",
    "items": {
     "properties": {
      "box": {
       "description": "Move and resize: [left, top, right, bottom] in base-image pixels.",
       "items": {
        "type": "number"
       },
       "type": "array"
      },
      "layer_id": {
       "description": "The layer id from get_layer_set.",
       "type": "string"
      },
      "order": {
       "description": "Stacking order; higher sits on top. The base always stays at the bottom.",
       "type": "integer"
      },
      "reset": {
       "description": "Put this layer back exactly where the split placed it.",
       "type": "boolean"
      },
      "visible": {
       "description": "Show or hide this layer.",
       "type": "boolean"
      }
     },
     "required": [
      "layer_id"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "layer_set_id": {
    "description": "The layers project id.",
    "type": "string"
   }
  },
  "required": [
   "layer_set_id",
   "changes"
  ],
  "type": "object"
 },
 "name": "update_layer_set",
 "title": "Arrange layers"
}
