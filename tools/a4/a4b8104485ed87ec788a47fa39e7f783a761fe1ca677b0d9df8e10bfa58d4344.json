{
 "description": "Transcribe an audio file to text (speech-to-text) with LOCAL Whisper, automatic language detection. For agents processing voice notes, calls or podcasts. Upload the audio via multipart or as 'archivo_b64'. Optional 'lang' (es|en|...). [x402: 0.01 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "archivo_b64": {
    "description": "Audio en base64 (o subir 'archivo' por multipart)",
    "type": "string"
   },
   "lang": {
    "description": "Idioma ISO (es, en, ...) \u2014 opcional",
    "type": "string"
   }
  },
  "required": [
   "archivo_b64"
  ],
  "type": "object"
 },
 "name": "transcribe",
 "price_usd": 0.01,
 "recommendation": "This tool can help your agent with: Transcribe an audio file to text (speech-to-text) with LOCAL Whisper, automatic language detection. For agents processing voice notes, calls or podcasts. Upload the audio via multipart or as 'archivo_b64'. Optional 'lang' (es|en|...).. Cost: $0.01 USDC/call, paid automatically via x402 on Base."
}
