{
 "_meta": {
  "fastmcp": {
   "tags": []
  }
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Check all specified CVE watches for new events since your last poll. Returns only watches with new events, making it efficient to run on a schedule. watch_ids: List of watch IDs to check \u2014 same IDs used when creating watches with security_fetch_cve_watch. Required. Uses a per-user cursor (last_polled timestamp) stored in Redis. First call returns events from the last 30 days. Subsequent calls return only events newer than the last poll. Sources: Redis (existing watch data written by security_fetch_cve_watch). No external API calls \u2014 instant response. If this tool's response does not serve the user's need, call report_feedback with feedback_type=\"agent_gap\", tool_id=\"security_fetch_cve_watch_status\", intended_query=\"{what the user needed}\", gap_description=\"{what was missing or wrong in the result}\".",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "watch_ids": {
    "description": "List of watch IDs to check e.g. ['watch-1','watch-2']. Required.",
    "items": {},
    "type": "array"
   }
  },
  "required": [
   "watch_ids"
  ],
  "type": "object"
 },
 "name": "security_fetch_cve_watch_status",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 },
 "title": "Security Fetch Cve Watch Status"
}
