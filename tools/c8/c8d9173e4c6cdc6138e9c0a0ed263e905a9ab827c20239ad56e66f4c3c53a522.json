{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Registered buildings in Dubai from the Dubai Land Department \u2014 building number, floors, rooms, flats/shops/offices counts, actual area, area/district, zone, project and creation date. No date range needed; filter by zone, freehold, leasehold or off-plan status.",
 "inputSchema": {
  "examples": [
   {
    "limit": 3,
    "zone_id": "1"
   }
  ],
  "properties": {
   "free_hold": {
    "description": "\"1\" freehold only, \"0\" non-freehold only",
    "enum": [
     "0",
     "1"
    ],
    "type": "string"
   },
   "include_arabic": {
    "description": "Keep the Arabic (`*_AR`) twin of each bilingual column. Default false.",
    "type": "boolean"
   },
   "lease_hold": {
    "description": "\"1\" leasehold only, \"0\" non-leasehold only",
    "enum": [
     "0",
     "1"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Rows to return, 1-200 (default 25)",
    "type": "number"
   },
   "off_plan": {
    "description": "\"1\" off-plan only, \"0\" ready only",
    "enum": [
     "0",
     "1"
    ],
    "type": "string"
   },
   "offset": {
    "description": "Rows to skip, for paging through `total` (default 0)",
    "type": "number"
   },
   "sort": {
    "description": "Sort as \"<COLUMN>_ASC\" or \"<COLUMN>_DESC\" using a column name from the rows, e.g. \"TRANS_VALUE_DESC\". Omit for the upstream default order.",
    "type": "string"
   },
   "zone_id": {
    "description": "Zone: \"1\" Deira, \"2\" Dubai",
    "enum": [
     "1",
     "2"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "dld_search_buildings",
 "title": "Dld Search Buildings",
 "version": "v1.cbf5b11f"
}
