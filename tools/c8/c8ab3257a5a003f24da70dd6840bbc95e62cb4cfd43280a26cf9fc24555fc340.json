{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Buyer accepts delivered work, optionally rates the seller, and releases payout according to the order settlement rail.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "order_id": {
    "type": "string"
   },
   "rating": {
    "maximum": 5,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "order_id"
  ],
  "type": "object"
 },
 "name": "accept_delivery",
 "title": "Accept delivery and release payout"
}
