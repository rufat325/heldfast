{
 "description": "Videos from TikTok's /tag/{name} challenge feed \u2014 not keyword or username search. Cursor + hasMore. Costs ~14 credits (0.7/result). Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cache": {
    "description": "Set true to serve from the 24h response cache (0 credits on hit). Default false \u2014 always fetch fresh. Envelope includes cached + cachedAt on hits.",
    "type": "boolean"
   },
   "cursor": {
    "description": "Pagination offset. Leave at 0 (or omit) for the first page; then pass the nextCursor value returned in the previous response. A null nextCursor means the end of the results.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "limit": {
    "description": "Max items to return (default 20, max 100). Billed per result.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "q": {
    "description": "Hashtag for the /tag/{name} challenge feed, with or without # (min 2). Not a keyword query.",
    "type": "string"
   },
   "region": {
    "description": "Two-letter ISO 3166-1 country our request is sent from. Default US. Does not filter results by country.",
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "tiktok_search_by_hashtag",
 "title": "TikTok Search by Hashtag"
}
