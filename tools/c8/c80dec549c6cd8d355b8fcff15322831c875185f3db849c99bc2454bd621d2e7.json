{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the authenticated user's Dayze Contacts (private CRM; table people). Each contact includes avatar_url, photo_count, and has_photos; use get_person_photos(person_id) for the full gallery. Notes omitted by default (has_notes flag); pass include_notes=true for progressive fetch (credential/secret spans are redacted server-side). Supports limit (default 100, max 500) and offset; returns total and truncated. Distinct from public notable People at /people. Advertised name get_people is stable; tools/call also accepts get_contacts / list_contacts. Requires OAuth or a supported scoped credential. ($0.10; API key required)",
 "destructiveHint": false,
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "cursor": {
    "type": "string"
   },
   "include_notes": {
    "description": "When true, include notes (server-redacted). Default false.",
    "type": "boolean"
   },
   "limit": {
    "description": "Page size (default 100, max 500)",
    "type": "number"
   },
   "offset": {
    "description": "Skip N rows (default 0)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "get_people",
 "openWorldHint": false,
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "expires_at": {
    "type": "string"
   },
   "has_more": {
    "type": "boolean"
   },
   "include_notes": {
    "type": "boolean"
   },
   "limit": {
    "type": "number"
   },
   "next_cursor": {
    "type": [
     "string",
     "null"
    ]
   },
   "offset": {
    "type": "number"
   },
   "ordering": {
    "type": "string"
   },
   "people": {
    "items": {
     "additionalProperties": true,
     "description": "A Dayze person or contact record.",
     "type": "object"
    },
    "type": "array"
   },
   "server_time": {
    "type": "string"
   },
   "snapshot_at": {
    "type": "string"
   },
   "snapshot_id": {
    "type": "string"
   },
   "total": {
    "type": "number"
   },
   "truncated": {
    "type": "boolean"
   }
  },
  "required": [
   "people",
   "total",
   "limit",
   "offset",
   "truncated"
  ],
  "type": "object"
 },
 "readOnlyHint": true,
 "securitySchemes": [
  {
   "scopes": [
    "context.read"
   ],
   "type": "oauth2"
  }
 ],
 "title": "Dayze Contacts"
}
