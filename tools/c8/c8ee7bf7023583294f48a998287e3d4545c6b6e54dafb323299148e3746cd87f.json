{
 "description": "Query Salvage Signal's bounded Chicago demolition-permit adapter and compare current matches with an optional prior snapshot. Costs $0.25 USDC on Base mainnet.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "previous": {
    "additionalProperties": false,
    "properties": {
     "match_ids": {
      "items": {
       "type": "string"
      },
      "maxItems": 100,
      "type": "array"
     },
     "source_generated_at": {
      "format": "date-time",
      "type": "string"
     }
    },
    "type": "object"
   },
   "query": {
    "description": "One Chicago permit number or street address.",
    "maxLength": 80,
    "minLength": 1,
    "type": "string"
   },
   "records": {
    "description": "Optional caller-supplied records for deterministic integration testing; omit for the maintained adapter.",
    "items": {
     "additionalProperties": true,
     "required": [
      "id"
     ],
     "type": "object"
    },
    "maxItems": 100,
    "type": "array"
   },
   "records_generated_at": {
    "format": "date-time",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "justhandled_chicago_demolition_permit_change_packet"
}
