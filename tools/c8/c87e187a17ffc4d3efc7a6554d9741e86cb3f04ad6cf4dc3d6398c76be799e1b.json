{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Search Modifications by Classified Rim"
 },
 "description": "Find aftermarket rim products compatible with a specific vehicle make/model/generation. Required: bolt_pattern, rim_diameter, rim_width, rim_offset, make, model, generation. Optional: cb, fd, fitment tolerances, diameter_range, sort, limit, offset.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bolt_pattern": {
    "description": "Bolt pattern (PCD) as stud-count \u00d7 circle-diameter in mm (e.g. \"5x112\", \"4x100\"). Required. Use list-rim-bolt-patterns to enumerate valid values.",
    "type": "string"
   },
   "bs_push": {
    "description": "Backspace push tolerance in mm (default 2). 0 = exact or safer only, no inward encroachment. Positive values allow more clearance margin.",
    "minimum": 0,
    "type": "integer"
   },
   "cb": {
    "description": "Centre bore diameter in mm (e.g. 66.5). Must be positive. Narrows results to matching bore size.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "diameter_range": {
    "description": "Widen rim diameter search by \u00b1N inches, min 0 (default 0 = exact match only). E.g. diameterRange=1 on an 18-inch search also returns 17 and 19-inch options.",
    "minimum": 0,
    "type": "integer"
   },
   "fd": {
    "description": "Fastener thread diameter in mm. Must be positive. Filters by lug-bolt thread size.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "fs_poke": {
    "description": "Frontspace poke tolerance in mm (default 2). 0 = exact or safer only, no stick-out past the fender. Positive values allow increasing amounts of wheel poke.",
    "minimum": 0,
    "type": "integer"
   },
   "generation": {
    "description": "Generation slug (e.g. \"b9\"). Get slugs from list-generations.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum results to return (1-100).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "make": {
    "description": "Manufacturer slug (e.g. \"audi\"). Get slugs from list-makes.",
    "type": "string"
   },
   "model": {
    "description": "Model slug (e.g. \"a4\"). Get slugs from list-models.",
    "type": "string"
   },
   "od_tolerance": {
    "description": "Overall diameter tolerance as a decimal fraction (0-0.05, default 0.01 = 1%). Controls how closely the product OD must match the vehicle spec.",
    "maximum": 0.05,
    "minimum": 0,
    "type": "number"
   },
   "offset": {
    "description": "Number of results to skip for pagination.",
    "minimum": 0,
    "type": "integer"
   },
   "ow_tolerance": {
    "description": "Overall width tolerance as a decimal fraction (0-0.03, default 0).",
    "maximum": 0.03,
    "minimum": 0,
    "type": "number"
   },
   "rim_diameter": {
    "description": "Rim diameter in inches (e.g. 18). Required. Must be positive.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "rim_offset": {
    "description": "Rim offset (ET) in mm, range -100 to 100 (e.g. 35). Distance from mounting face to rim centre-line. Required.",
    "maximum": 100,
    "minimum": -100,
    "type": "number"
   },
   "rim_width": {
    "description": "Rim width in inches from bead seat to bead seat (e.g. 8.0). Required. Must be positive.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "sort": {
    "description": "Sort order: \"name\" (A-Z by make/model), \"fitment\" (closest frontspace delta), \"load\" (heaviest first).",
    "type": "string"
   }
  },
  "required": [
   "bolt_pattern",
   "rim_diameter",
   "rim_width",
   "rim_offset",
   "make",
   "model",
   "generation"
  ],
  "type": "object"
 },
 "name": "search-modifications-by-classified-rim"
}
