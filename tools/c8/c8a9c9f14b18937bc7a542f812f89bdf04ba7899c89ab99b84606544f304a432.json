{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find NWS weather observation stations in a US state (e.g., \"CA\", \"NY\", \"TX\"). Returns station IDs (use with get_observation), names, time zones, and coordinates. US only.",
 "inputSchema": {
  "examples": [
   {
    "state": "NY"
   },
   {
    "limit": 10,
    "state": "CA"
   }
  ],
  "properties": {
   "limit": {
    "description": "Maximum stations to return (1-500, default 20).",
    "type": "number"
   },
   "state": {
    "description": "Two-letter US state code (e.g. \"CA\", \"NY\").",
    "type": "string"
   }
  },
  "required": [
   "state"
  ],
  "type": "object"
 },
 "name": "get_stations",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of weather stations returned",
    "type": "number"
   },
   "state": {
    "description": "Two-letter US state code",
    "type": "string"
   },
   "stations": {
    "items": {
     "properties": {
      "coordinates": {
       "description": "Station geographic coordinates",
       "properties": {
        "lat": {
         "description": "Latitude",
         "type": "number"
        },
        "lon": {
         "description": "Longitude",
         "type": "number"
        }
       },
       "required": [
        "lon",
        "lat"
       ],
       "type": [
        "object",
        "null"
       ]
      },
      "id": {
       "description": "Station identifier",
       "type": "string"
      },
      "name": {
       "description": "Station name",
       "type": "string"
      },
      "time_zone": {
       "description": "Station time zone",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "required": [
      "id",
      "name",
      "time_zone",
      "coordinates"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "state",
   "count",
   "stations"
  ],
  "type": "object"
 },
 "title": "Get Stations",
 "version": "v1.4234f21f"
}
