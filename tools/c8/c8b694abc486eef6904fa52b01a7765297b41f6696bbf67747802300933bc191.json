{
 "description": "Run a read-only SELECT (only SELECT/WITH/VALUES). Use positional params. Results are row/cell-capped.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "connection": {
    "description": "Configured connection name (see list_connections)",
    "type": "string"
   },
   "params": {
    "description": "Positional bind parameters",
    "items": {},
    "type": "array"
   },
   "sql": {
    "type": "string"
   }
  },
  "required": [
   "connection",
   "sql"
  ],
  "type": "object"
 },
 "name": "query",
 "title": "Run query"
}
