{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "One-command local setup for a repo or PR: analyze sources, auto-apply weak AI candidate links and candidate flows when a real BYOK provider is configured, write behavior-coverage.html + rtm.md, summarize changed/gap targets, and return agent next actions. AI lanes stay separate and never affect Proven coverage.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "ai_all": {
    "description": "Run AI weak-linking over all deterministic behavior nodes instead of gaps-only.",
    "type": "boolean"
   },
   "base_ref": {
    "description": "Optional PR/diff base ref. Default auto-detects main/master when possible.",
    "type": "string"
   },
   "coverage_timeout_ms": {
    "description": "Per-command coverage-generation timeout.",
    "maximum": 600000,
    "minimum": 1000,
    "type": "integer"
   },
   "generate_coverage": {
    "description": "Run safe local coverage generation before analyze where supported. Default false.",
    "type": "boolean"
   },
   "include_markdown": {
    "description": "Enrich requirements-like Markdown docs. Default true.",
    "type": "boolean"
   },
   "model": {
    "description": "Model name override.",
    "type": "string"
   },
   "no_ai": {
    "description": "Disable automatic AI weak-link and candidate-flow passes even if a provider key is configured.",
    "type": "boolean"
   },
   "no_ai_flows": {
    "description": "Disable automatic AI candidate-flow discovery while keeping weak AI links enabled.",
    "type": "boolean"
   },
   "prompt_version": {
    "description": "Opt-in generation strategy for the auto-prove generation lane. Default v2/deterministic; v5 uses batched two-phase generation. The prove/mint gate is unchanged either way.",
    "enum": [
     "v2",
     "v5"
    ],
    "type": "string"
   },
   "provider": {
    "description": "BYOK provider override for the AI passes.",
    "enum": [
     "openai",
     "ollama",
     "anthropic"
    ],
    "type": "string"
   },
   "source": {
    "description": "Source path to analyze. Defaults to workspace.",
    "type": "string"
   },
   "workspace": {
    "description": "Local workspace root. Defaults to the current directory.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "orangepro_start",
 "title": "Start OrangePro"
}
