{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Return bounded Recurly payment transactions with account, invoice, type, status, amount, currency, gateway response, and timestamps. Answers which charges, refunds, and verifications succeeded or failed.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-recurly-private-api-key",
    "limit": 20,
    "success": true,
    "type": "refund"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Recurly private API key",
    "type": "string"
   },
   "begin_time": {
    "description": "ISO 8601 lower bound for creation time",
    "type": "string"
   },
   "cursor": {
    "description": "Cursor from a previous response next link",
    "type": "string"
   },
   "end_time": {
    "description": "ISO 8601 upper bound for creation time",
    "type": "string"
   },
   "limit": {
    "description": "Maximum results, default 20 and maximum 200",
    "maximum": 200,
    "minimum": 1,
    "type": "number"
   },
   "success": {
    "type": "boolean"
   },
   "type": {
    "enum": [
     "authorization",
     "capture",
     "purchase",
     "refund",
     "verify"
    ],
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "recurly_list_transactions",
 "title": "Recurly List Transactions",
 "version": "v1.c44462f6"
}
