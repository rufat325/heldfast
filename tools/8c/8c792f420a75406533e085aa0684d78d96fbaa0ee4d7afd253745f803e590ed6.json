{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch and render any web page through ScrapingBee (headless browser + proxy rotation), returning the resulting HTML. Handles JS-heavy sites, geolocation, and ad-blocking. Example: scrapingbee_scrape({ url: \"https://example.com\", render_js: true, _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-scrapingbee-api-key",
    "render_js": true,
    "url": "https://example.com/page"
   },
   {
    "_apiKey": "your-scrapingbee-api-key",
    "block_ads": true,
    "country_code": "us",
    "premium_proxy": true,
    "render_js": true,
    "url": "https://news.example.com/article",
    "wait_for": ".article-content"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Your ScrapingBee API key (get one free at scrapingbee.com).",
    "type": "string"
   },
   "block_ads": {
    "description": "Block advertisements while rendering (default false).",
    "type": "boolean"
   },
   "country_code": {
    "description": "ISO 3166-1 alpha-2 country code for proxy geolocation, e.g. \"us\", \"de\", \"br\".",
    "type": "string"
   },
   "premium_proxy": {
    "description": "Route through residential premium proxies (higher credit cost). Use for hard-to-scrape or geo-blocked sites.",
    "type": "boolean"
   },
   "render_js": {
    "description": "Execute JavaScript via a headless browser (default true). Set false for faster/cheaper static fetches.",
    "type": "boolean"
   },
   "stealth_proxy": {
    "description": "Use the stealth proxy pool for the most heavily protected sites (highest credit cost).",
    "type": "boolean"
   },
   "url": {
    "description": "Full target URL including protocol, e.g. \"https://example.com/page\"",
    "type": "string"
   },
   "wait_for": {
    "description": "CSS/XPath selector to wait for before capturing the page, e.g. \"#content\" or \".results\".",
    "type": "string"
   }
  },
  "required": [
   "url",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "scrapingbee_scrape",
 "title": "Scrapingbee Scrape",
 "version": "v1.d0cad847"
}
