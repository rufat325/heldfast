{
 "description": "Best-effort heartbeat that bumps last_seen_at on the install row. Never blocks the agent and never collects identifying data.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "install_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "install_id"
  ],
  "type": "object"
 },
 "name": "mcp_health_ping",
 "title": "Heartbeat from a running MCP session"
}
