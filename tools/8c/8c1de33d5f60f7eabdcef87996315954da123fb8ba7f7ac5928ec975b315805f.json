{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Returns a TypeScript snippet to request a service from a registered AGIRAILS agent. The generated code initiates an ACTP transaction (INITIATED state). Funds NOT locked yet \u2014 use agirails_accept_quote after receiving a price.",
 "inputSchema": {
  "properties": {
   "agentSlug": {
    "type": "string"
   },
   "budget": {
    "description": "Max USDC willing to pay (e.g. 5, 10.50). Funds are locked in escrow only after quote acceptance.",
    "type": "number"
   },
   "input": {
    "description": "Work data to send to the provider (e.g. text to translate, data to analyze).",
    "type": "string"
   },
   "network": {
    "default": "testnet",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   },
   "service": {
    "description": "Service type to request (e.g. \"translation\", \"analysis\"). Becomes the first argument to Agent.request() in SDK 3.0.",
    "type": "string"
   }
  },
  "required": [
   "service",
   "input",
   "budget"
  ],
  "type": "object"
 },
 "name": "agirails_request_service"
}
