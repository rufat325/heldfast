{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Render a node \u2014 frame, component, instance, group, shape, text, or a whole page \u2014 to a PNG the model can look at, or to SVG. Fully offline: geometry, text outlines and images all come from the file. Rendering is best-effort: read `approximated` and `unsupported` in the report before trusting fine details; exact values remain available from fig_node / fig_style / fig_text. Default output is PNG at 2x, capped to 1568 px on the longest edge; use `savePath` to write a file instead of inlining it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "background": {
    "description": "Default \"transparent\". \"page\" fills with the page background colour.",
    "enum": [
     "transparent",
     "page"
    ],
    "type": "string"
   },
   "file": {
    "description": "Path to the .fig / .figma file (absolute, or relative to the server CWD).",
    "type": "string"
   },
   "format": {
    "description": "Default \"png\". Falls back to SVG text when the optional rasterizer is not installed.",
    "enum": [
     "png",
     "svg"
    ],
    "type": "string"
   },
   "guid": {
    "description": "Node to render, e.g. \"2:1339\"; a Figma node-id or link works too. A page guid renders the whole page, downscaled to fit maxSize.",
    "type": "string"
   },
   "maxNodes": {
    "description": "Refuse subtrees larger than this. Default 20000.",
    "maximum": 60000,
    "minimum": 1,
    "type": "integer"
   },
   "maxSize": {
    "description": "Longest edge in pixels. Default 1568.",
    "maximum": 4096,
    "minimum": 64,
    "type": "integer"
   },
   "savePath": {
    "description": "Write the PNG/SVG to this path (directories are created) instead of inlining it. Required when the PNG exceeds 2097152 bytes or the SVG exceeds 50000 characters.",
    "type": "string"
   },
   "scale": {
    "description": "Device scale factor. Default 2. Lowered automatically to respect maxSize.",
    "maximum": 4,
    "minimum": 0.1,
    "type": "number"
   }
  },
  "required": [
   "file",
   "guid"
  ],
  "type": "object"
 },
 "name": "fig_render",
 "title": "Render a node to an image"
}
