{
 "_meta": {
  "openai/outputTemplate": "ui://nowline/preview-v1",
  "ui": {
   "resourceUri": "ui://nowline/preview-v1"
  },
  "ui/resourceUri": "ui://nowline/preview-v1"
 },
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Render Roadmap"
 },
 "description": "Validate then render a .nowline roadmap to SVG or PNG (combined validate+render). Returns structured diagnostics on error-severity input instead of a raw kernel error. On MCP Apps hosts the in-chat preview is view-only (no download/export button); use `export` for files and the `share` tool for an openable link.\n\nExample:\nnowline v1\n\nroadmap r \"Title\" start:2026-01-05 scale:2w\n\nswimlane eng \"Engineering\"\n  item build \"Build\" duration:3w\n\nSource is `.nowline` DSL (not JSON/YAML); call the `reference`/`examples` tools for full syntax.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "format": {
    "description": "Output format. Defaults to svg.",
    "enum": [
     "svg",
     "png"
    ],
    "type": "string"
   },
   "now": {
    "description": "Now-line date as YYYY-MM-DD (UTC). Omit to suppress.",
    "type": "string"
   },
   "output": {
    "description": "Real local filesystem path to write the output file (e.g. /Users/name/Desktop/roadmap.svg). Never pass a virtual or sandbox path such as /mnt/user-data/\u2026 \u2014 omit this parameter to receive output inline instead.",
    "type": "string"
   },
   "path": {
    "description": "Real local filesystem path to the .nowline file (e.g. /Users/name/Desktop/foo.nowline). Never pass a virtual or sandbox path such as /mnt/user-data/\u2026 \u2014 those do not exist on the host filesystem. Pass `source` instead.",
    "type": "string"
   },
   "preview": {
    "description": "When true, force the in-chat MCP Apps preview. On MCP Apps hosts the preview auto-renders via _meta.ui without this flag.",
    "type": "boolean"
   },
   "review": {
    "description": "When true, also attach a downscaled PNG so a multimodal model can visually check layout (label overflow, lane crowding, off-range now-line). Off by default.",
    "type": "boolean"
   },
   "scale": {
    "description": "PNG pixel-density multiplier. Defaults to 2.",
    "type": "number"
   },
   "source": {
    "description": "Inline .nowline source text.",
    "type": "string"
   },
   "theme": {
    "description": "Color theme. Defaults to light.",
    "enum": [
     "light",
     "dark",
     "grayscale"
    ],
    "type": "string"
   },
   "width": {
    "description": "Canvas width in px.",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "render",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bytes": {
    "type": "number"
   },
   "format": {
    "type": "string"
   },
   "insights": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "code": {
       "type": "string"
      },
      "entityId": {
       "type": "string"
      },
      "message": {
       "type": "string"
      },
      "severity": {
       "enum": [
        "info",
        "warning"
       ],
       "type": "string"
      }
     },
     "required": [
      "severity",
      "code",
      "message"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "path": {
    "type": "string"
   }
  },
  "required": [
   "format"
  ],
  "type": "object"
 }
}
