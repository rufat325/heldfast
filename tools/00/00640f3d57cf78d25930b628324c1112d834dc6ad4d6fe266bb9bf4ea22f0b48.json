{
 "_meta": {
  "skill": "styx"
 },
 "description": "Full headless BTC\u2192sBTC deposit via the Styx protocol. Flow: reserve pool liquidity \u2192 build PSBT locally \u2192 sign with wallet keys \u2192 broadcast to mempool.space \u2192 update deposit status. Requires an unlocked wallet with sufficient BTC balance. On mainnet, ordinal UTXOs are automatically filtered out to protect inscriptions. Minimum deposit: 10,000 sats. Pool limits: main=300k sats, aibtc=1M sats.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "amount": {
    "description": "Amount to deposit in satoshis (min 10000). Example: 50000",
    "maximum": 9007199254740991,
    "minimum": 10000,
    "type": "integer"
   },
   "btcSender": {
    "description": "BTC address sending funds. Must match the active wallet's BTC address. Uses active wallet if omitted.",
    "type": "string"
   },
   "fee": {
    "description": "Fee priority: \"low\", \"medium\", or \"high\". Defaults to \"medium\".",
    "enum": [
     "low",
     "medium",
     "high"
    ],
    "type": "string"
   },
   "pool": {
    "description": "Pool ID: \"main\" or \"aibtc\". Defaults to \"main\".",
    "type": "string"
   },
   "stxReceiver": {
    "description": "Stacks address to receive sBTC. Uses active wallet address if omitted.",
    "type": "string"
   }
  },
  "required": [
   "amount"
  ],
  "type": "object"
 },
 "name": "styx_deposit"
}
