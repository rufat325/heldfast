{
 "description": "Fetch the live input schema + description for one actor so you can construct a correct call. Provide either the actor slug or its tool name.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "slug": {
    "description": "Actor slug, e.g. crbr-beneficial-owners-scraper",
    "type": "string"
   },
   "tool": {
    "description": "Tool name, e.g. regdata_crbr_beneficial_owners",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "regdata_describe"
}
