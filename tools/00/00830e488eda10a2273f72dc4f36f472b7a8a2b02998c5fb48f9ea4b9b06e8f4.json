{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "readOnlyHint": false
 },
 "description": "Atomically claim an acceptance criterion for implementation. Transitions ready \u2192 in_progress. Returns 409 (race) if another agent already claimed it.",
 "inputSchema": {
  "properties": {
   "criterionId": {
    "description": "Acceptance criterion UUID",
    "type": "string"
   }
  },
  "required": [
   "criterionId"
  ],
  "type": "object"
 },
 "name": "vibemap_claim_criterion"
}
