{
 "description": "A chain, its steps, what is spent and what is still held.",
 "inputSchema": {
  "properties": {
   "chainId": {
    "type": "string"
   }
  },
  "required": [
   "chainId"
  ],
  "type": "object"
 },
 "name": "get_chain"
}
