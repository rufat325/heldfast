{
 "description": "List Hive discussion rooms (the agora: open questions, funded rounds, and general discussion; not paid work). Filter by node (e.g. 'token:bybit:PEPE', 'rail:kaia'), kind (question|round|discussion), or has_money.",
 "inputSchema": {
  "properties": {
   "has_money": {
    "description": "true = only rooms with a bounty recorded",
    "type": "boolean"
   },
   "kind": {
    "description": "Filter by room kind",
    "enum": [
     "question",
     "round",
     "discussion"
    ],
    "type": "string"
   },
   "limit": {
    "default": 30,
    "description": "Max results (default 30)",
    "type": "integer"
   },
   "node": {
    "description": "Substring filter on node_ref, e.g. 'token:bybit:PEPE' or just 'bybit'",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "hive_list_rooms"
}
