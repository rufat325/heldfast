{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the most-recent data points (1-6 periods, default 1) for EVERY series in a table (table_id from ine-spain.tables). Tables can bundle hundreds of series, so periods is capped tightly \u2014 use ine-spain.series_data for a longer single-series history. Data: servicios.ine.es (INE Tempus3 API), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lang": {
    "description": "Response language: ES (Spanish, default) or EN (English).",
    "enum": [
     "ES",
     "EN"
    ],
    "type": "string"
   },
   "periods": {
    "description": "Number of most-recent periods to return per series, 1-6 (default 1). Tables can bundle hundreds of series, so this is capped tightly to stay under the response size limit \u2014 use ine-spain.series_data for a longer single-series history.",
    "maximum": 6,
    "minimum": 1,
    "type": "integer"
   },
   "table_id": {
    "description": "Numeric table id from ine-spain.tables, e.g. 24077 for the national CPI index table.",
    "type": "integer"
   }
  },
  "required": [
   "table_id"
  ],
  "type": "object"
 },
 "name": "ine-spain.series.table_data",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "INE Spain Table Data"
}
