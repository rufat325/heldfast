{
 "description": "Get the aggregated slowlog across ALL nodes in the cluster. This is the primary tool for finding slow commands in cluster mode \u2014 per-node slowlogs are incomplete. Returns an error message if not in cluster mode.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "instanceId": {
    "description": "Optional instance ID override",
    "type": "string"
   },
   "limit": {
    "description": "Max entries to return (default 100)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "get_cluster_slowlog"
}
