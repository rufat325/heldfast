{
 "description": "Translate text \u2014 up to 2,000 chars, any language pair, source auto-detected when omitted. Returns clean JSON: detected source language + full translation. Costs $0.01 per call, paid from clink's shop credits (get a key with buy_credits or at /buy/credits). A failed or empty call is free.",
 "inputSchema": {
  "properties": {
   "source_lang": {
    "description": "optional; auto-detected when omitted",
    "type": "string"
   },
   "target_lang": {
    "description": "language to translate into, e.g. 'English', 'es'",
    "type": "string"
   },
   "text": {
    "description": "text to translate, up to 2,000 chars",
    "type": "string"
   }
  },
  "required": [
   "text",
   "target_lang"
  ],
  "type": "object"
 },
 "name": "translate_text"
}
