{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get HTTP traffic analytics for a zone over a time range (via GraphQL).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "since": {
    "description": "Hour-aligned ISO 8601 UTC start, e.g. 2024-01-01T00:00:00Z. httpRequests1hGroups buckets are hourly.",
    "type": "string"
   },
   "until": {
    "description": "Hour-aligned ISO 8601 UTC end, e.g. 2024-01-02T00:00:00Z.",
    "type": "string"
   },
   "zone_id": {
    "description": "The Cloudflare zone ID (32-char hex). Get it from list_zones.",
    "type": "string"
   }
  },
  "required": [
   "zone_id",
   "since",
   "until"
  ],
  "type": "object"
 },
 "name": "get_zone_analytics"
}
