{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Review authoring project"
 },
 "description": "Review an authoring project against the deterministic authoring-coach rubric and get skill-building feedback. Returns a score, per-check evidence across grounding / process / lifecycle, strengths, and the single highest-leverage next improvement. Advisory only \u2014 it never blocks publishing. The deterministic report needs no LLM key; set coach=true (with an Anthropic key) to add an LLM-synthesised coaching narrative on top of the computed checks.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "anthropic_key": {
    "description": "An Anthropic API key the user explicitly provided for this call. [sensitive \u2014 do not echo or log] Deprecated: the raw value is written verbatim to the host's session transcript. Never fill this from the environment.",
    "type": "string"
   },
   "anthropic_key_env": {
    "description": "Optional. Only honoured when it equals the env var the user configured via AETHIS_ANTHROPIC_KEY_ENV in this MCP server's config; that configured key is used automatically, so this can be omitted. Do not guess a variable name: the server refuses any name the user did not configure.",
    "type": "string"
   },
   "anthropic_key_keychain": {
    "description": "macOS keychain reference the user created for Aethis: either 'service:account' or just 'account' (service defaults to 'aethis-anthropic-key'). The server reads it via the `security` command at call time.",
    "type": "string"
   },
   "coach": {
    "description": "Add an opt-in LLM-synthesised coaching narrative on top of the deterministic rubric. Requires an Anthropic key the user configured (AETHIS_ANTHROPIC_KEY_ENV, anthropic_key_keychain, or anthropic_key). Off by default \u2014 the deterministic report needs no key.",
    "type": "boolean"
   },
   "openai_key": {
    "description": "Retired and refused: Aethis LLM tools use Anthropic models only.",
    "type": "string"
   },
   "project_id": {
    "description": "The project ID to review",
    "type": "string"
   }
  },
  "required": [
   "project_id"
  ],
  "type": "object"
 },
 "name": "aethis_review_project"
}
