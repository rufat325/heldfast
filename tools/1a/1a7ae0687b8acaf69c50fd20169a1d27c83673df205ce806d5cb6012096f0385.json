{
 "description": "List today's trending assets (symbol, display name, market, selection date) \u2014 the assets LDBD's daily trending bot surfaced from social/news buzz. Public, no API key needed. Data only: no direction, score, or buzz signal is returned. Empty list when today's queue is empty.",
 "inputSchema": {
  "properties": {
   "limit": {
    "default": 10,
    "description": "Max assets to return (default 10)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "ldbd_get_trending_assets"
}
