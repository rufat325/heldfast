{
 "description": "Send a follow-up instruction to a running build. Cost: 0 credits.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "execution_id": {
    "description": "The execution_id returned by build_company.",
    "type": "string"
   },
   "prompt": {
    "description": "Follow-up instruction for the running build agent.",
    "type": "string"
   }
  },
  "required": [
   "execution_id",
   "prompt"
  ],
  "type": "object"
 },
 "name": "message_build"
}
