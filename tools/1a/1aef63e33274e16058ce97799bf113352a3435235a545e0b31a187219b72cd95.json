{
 "_meta": {
  "skill": "signing"
 },
 "description": "Verify a SIP-018 signature and recover the signer's address. Takes the verification hash (from sip018_sign or sip018_hash 'verification' field) and the signature, then recovers the public key and derives the signer's Stacks address.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "expectedSigner": {
    "description": "Optional: expected signer address to verify against. If provided, returns whether the signature is valid for this signer.",
    "type": "string"
   },
   "messageHash": {
    "description": "The SIP-018 verification hash (from sip018_sign/sip018_hash 'verification' field). This is sha256(prefix || domainHash || messageHash).",
    "type": "string"
   },
   "signature": {
    "description": "The signature in RSV format (65 bytes hex from sip018_sign)",
    "type": "string"
   }
  },
  "required": [
   "messageHash",
   "signature"
  ],
  "type": "object"
 },
 "name": "sip018_verify"
}
