{
 "description": "List reusable hosted checkouts, newest first. Cursor pagination via starting_after / ending_before.",
 "inputSchema": {
  "properties": {
   "ending_before": {
    "description": "Cursor: return results before this checkout id",
    "type": "string"
   },
   "limit": {
    "description": "Max results per page (default 25, max 100)",
    "type": "number"
   },
   "order": {
    "description": "Sort order by created_at. Defaults to desc.",
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   },
   "starting_after": {
    "description": "Cursor: return results after this checkout id",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_checkouts"
}
