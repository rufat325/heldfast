{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Plan SAP Runtime Repair"
 },
 "description": "Free helper that returns the pinned latest-release repair command for hosted SAP MCP plus local sap_payments bridge setup. Use this before asking users to manually edit runtime config.\n\nSAP MCP execution guidance: Intent: agent bootstrap, routing, skills, or repair guidance. Pricing: free; call directly without x402. Routing: free hosted call; call directly and keep it small/exact when possible. Signer boundary: hosted reads/builders never receive keypair bytes; value-moving results must be finalized locally when signing is required.",
 "inputSchema": {
  "description": "Plan SAP Runtime Repair input schema. Intent: agent bootstrap, routing, skills, or repair guidance. Pricing: free; call directly without x402. Routing: free hosted call; call directly and keep it small/exact when possible. Signer boundary: hosted reads/builders never receive keypair bytes; value-moving results must be finalized locally when signing is required. Use exact field names from this schema; do not guess aliases or include private key material.",
  "properties": {
   "agent": {
    "description": "Optional runtime to focus repair instructions on.",
    "enum": [
     "claude",
     "codex",
     "hermes",
     "openclaw",
     "clawpump",
     "custom"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "sap_runtime_repair_plan",
 "outputSchema": {
  "properties": {
   "action": {
    "description": "Plan type.",
    "type": "string"
   },
   "agentInstructions": {
    "description": "Instructions agents should follow without guessing.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "commands": {
    "description": "OS-specific repair command aliases.",
    "type": "object"
   },
   "expectedAfterRestart": {
    "description": "Tool namespaces and bridge tools expected after restarting the runtime.",
    "type": "object"
   },
   "hostedEndpoint": {
    "description": "Hosted SAP MCP endpoint.",
    "type": "string"
   },
   "latestVersion": {
    "description": "Pinned SAP MCP package version for repair.",
    "type": "string"
   },
   "package": {
    "description": "Pinned npm package spec.",
    "type": "string"
   },
   "repairCommand": {
    "description": "Primary local command to repair hosted MCP and sap_payments bridge config.",
    "type": "string"
   },
   "serverMode": {
    "description": "Current server mode.",
    "type": "string"
   },
   "success": {
    "description": "Whether the repair plan was generated.",
    "type": "boolean"
   },
   "targetRuntime": {
    "description": "Runtime selected for repair guidance.",
    "type": "string"
   },
   "whatRepairDoes": {
    "description": "Concrete operations performed by repair.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "wizardCommand": {
    "description": "Full local wizard command for profile creation or full setup.",
    "type": "string"
   }
  },
  "required": [
   "success",
   "action",
   "latestVersion",
   "package",
   "hostedEndpoint",
   "serverMode",
   "targetRuntime",
   "repairCommand",
   "wizardCommand",
   "commands",
   "whatRepairDoes",
   "expectedAfterRestart",
   "agentInstructions"
  ],
  "type": "object"
 },
 "title": "Plan SAP Runtime Repair"
}
