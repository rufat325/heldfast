{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Static inventory of memory domains, privacy modes and recommended first calls. No live value reads.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "privacy_mode": {
    "default": "structured",
    "description": "summary = keys/meta without full values; structured/raw = full entries (local store parity).",
    "enum": [
     "summary",
     "structured",
     "raw"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "memory_data_inventory"
}
