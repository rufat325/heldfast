{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Get the cached video data from a completed analytics export. Returns the full video array with per-video analytics (views, likes, comments, shares, duration, transcripts, vision analysis). The export must be in 'completed' status. Use social_api_get_export to check status first.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "export_id": {
    "description": "Export ID of a completed export",
    "type": "string"
   }
  },
  "required": [
   "export_id"
  ],
  "type": "object"
 },
 "name": "social_api_get_export_videos",
 "title": "Get Export Videos"
}
