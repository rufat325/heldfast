{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Converts an amount between currencies using a manually supplied rate: no live data fetch. Prefer workflow.run_forex_currency_converter_live instead to have the rate fetched automatically. Use when user already knows the exact rate they want applied. Returns: converted.",
 "inputSchema": {
  "properties": {
   "amount": {
    "description": "Amount in the source currency",
    "type": "number"
   },
   "rate": {
    "description": "Exchange rate to apply (1 unit of source currency = rate units of target currency)",
    "type": "number"
   }
  },
  "required": [
   "amount",
   "rate"
  ],
  "type": "object"
 },
 "name": "workflow.run_forex_currency_converter"
}
