{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get Formula 1 World Drivers Championship standings for any season. Returns each driver ranked by points, with their constructor, nationality, wins count, and total championship points as of the latest completed round. Omit the season parameter to get current live standings. Historical seasons available back to 1950. Data source: Jolpica F1 (Ergast-compatible API) \u2014 free, unlimited, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Maximum number of results to return (1\u2013100, default 30).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Pagination offset \u2014 number of results to skip (default 0).",
    "minimum": 0,
    "type": "integer"
   },
   "season": {
    "description": "Season year (e.g. 2026, 2024, 2023). Omit for the current season.",
    "type": [
     "string",
     "number"
    ]
   }
  },
  "type": "object"
 },
 "name": "f1.standings.drivers",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "F1 Driver Championship Standings"
}
