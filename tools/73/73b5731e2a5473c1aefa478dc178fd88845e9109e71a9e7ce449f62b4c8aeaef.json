{
 "description": "SMB: classify a support ticket + draft the reply. input=ticket, tono=optional. [x402: 1.0 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "customer-reply",
 "price_usd": 1.0,
 "recommendation": "This tool can help your agent with: SMB: classify a support ticket + draft the reply. input=ticket, tono=optional.. Cost: $1.0 USDC/call, paid automatically via x402 on Base."
}
