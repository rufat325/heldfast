{
 "description": "Check which cloud providers are used in a Terraform workspace and verify authentication status for each. Call this before plan or apply to catch credential issues early.",
 "inputSchema": {
  "properties": {
   "workdir": {
    "description": "Absolute or relative path to the directory containing .tf files",
    "type": "string"
   }
  },
  "required": [
   "workdir"
  ],
  "type": "object"
 },
 "name": "tf_preflight"
}
