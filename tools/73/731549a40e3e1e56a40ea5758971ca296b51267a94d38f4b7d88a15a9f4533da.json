{
 "description": "Publish or re-publish. THE propagation trigger: it snapshots the body, assigns the OG image and fires the content webhook that pushes the post to mirrored sites. Nothing else takes a post live. (POST /api/v1/content/posts/{postId}/publish; needs the write:content scope)",
 "inputSchema": {
  "properties": {
   "postId": {
    "description": "Post id.",
    "type": "string"
   }
  },
  "required": [
   "postId"
  ],
  "type": "object"
 },
 "name": "publish_post"
}
