{
 "description": "Register the host that will serve this organization's message links. One per organization; registering never activates it. One per organization, and the host is unique across the platform \u2014 two tenants claiming the same name would make link routing ambiguous. Registering does not activate: publish the CNAME, then call `verify`.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "domain": {
    "description": "Tracking host, e.g. links.example.com",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "domain"
  ],
  "type": "object"
 },
 "name": "commsharbor_tracking_domain_create"
}
