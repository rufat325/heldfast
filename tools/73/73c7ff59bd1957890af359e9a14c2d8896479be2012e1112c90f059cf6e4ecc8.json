{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List recent builds, sorted by upload date (newest first). Filter by app, processing state, or version.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "appId": {
    "description": "Filter to builds for a single app ID",
    "type": "string"
   },
   "auto_paginate": {
    "description": "Follow links.next across pages until the limit is reached (default false).",
    "type": "boolean"
   },
   "limit": {
    "description": "Max builds (default 25). With auto_paginate this is the total across pages.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "processingState": {
    "description": "Filter by processing state",
    "enum": [
     "PROCESSING",
     "FAILED",
     "INVALID",
     "VALID"
    ],
    "type": "string"
   },
   "version": {
    "description": "Filter by build version (e.g. \"42\")",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_builds"
}
