{
 "description": "Search the catalogue of 296 moving-image works by C\u00e9sar Yag\u00fce. Filter by collection, orientation (including ultrawide, 21:9 and wider), resolution, availability, price, free text \u2014 or by dominant colour, which is measured from every frame of the master rather than described.",
 "inputSchema": {
  "properties": {
   "availability": {
    "enum": [
     "available",
     "any"
    ],
    "type": "string"
   },
   "collection": {
    "description": "Collection name",
    "type": "string"
   },
   "colour": {
    "description": "Dominant colour by name (gold, teal, crimson\u2026) or hex. Measured from every frame.",
    "type": "string"
   },
   "limit": {
    "type": "integer"
   },
   "max_price": {
    "type": "number"
   },
   "min_price": {
    "type": "number"
   },
   "order": {
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   },
   "orientation": {
    "description": "ultrawide = at least twice as wide as tall (21:9 and wider); landscape includes it",
    "enum": [
     "portrait",
     "landscape",
     "square",
     "ultrawide"
    ],
    "type": "string"
   },
   "query": {
    "description": "Free text across titles, descriptions and curatorial notes",
    "type": "string"
   },
   "resolution": {
    "description": "HD, 4K, 5K, 8K, 10K, 12K or 16K",
    "type": "string"
   },
   "sort_by": {
    "enum": [
     "price",
     "name"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_artworks"
}
