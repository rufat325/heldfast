{
 "annotations": {
  "readOnlyHint": true,
  "title": "List saved templates"
 },
 "description": "List the templates the user has saved from earlier drafts, newest first: the saved_template_id, name, kind and which manifest template it was made from. kind narrows it to one kind. Pass a saved_template_id to a generator as saved_template_id to generate from it again.",
 "inputSchema": {
  "properties": {
   "kind": {
    "enum": [
     "og_image",
     "email",
     "pdf",
     "substack_post",
     "markdown"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_templates",
 "title": "List saved templates"
}
