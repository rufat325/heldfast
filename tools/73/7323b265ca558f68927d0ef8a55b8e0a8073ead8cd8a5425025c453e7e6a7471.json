{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-site-default-drive"
 },
 "description": "The default drive (document library) for this site.\n\nTIP: Gets the default document library (drive) for a SharePoint site. Returns id, name, driveType, webUrl, quota.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "siteId": {
    "description": "Path parameter: siteId",
    "type": "string"
   }
  },
  "required": [
   "siteId"
  ],
  "type": "object"
 },
 "name": "get-site-default-drive"
}
