{
 "description": "List tax rates configured in the Xero tenant. Use the returned TaxType codes on invoice/bill line items. Supports where clause like 'Status==\"ACTIVE\"'.",
 "inputSchema": {
  "properties": {
   "order": {
    "description": "Ordering, e.g. 'Name ASC'.",
    "type": "string"
   },
   "where": {
    "description": "Xero where clause, e.g. 'Status==\"ACTIVE\"'.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_tax_rates"
}
