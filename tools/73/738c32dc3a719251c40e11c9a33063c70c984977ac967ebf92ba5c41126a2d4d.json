{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Solana DEX quote from Jupiter for any SPL pair: output amount, price impact %, the route taken and the slippage assumed. The real executable price on Solana, not an index price. Costs 0.005 USDC per call (x402, USDC on Solana or Base).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "amount": {
    "description": "amount in raw base units of input mint",
    "type": "string"
   },
   "input_mint": {
    "description": "input mint (base58)",
    "type": "string"
   },
   "output_mint": {
    "description": "output mint (base58)",
    "type": "string"
   }
  },
  "required": [
   "input_mint",
   "output_mint",
   "amount"
  ],
  "type": "object"
 },
 "name": "get_dex_quote",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "data": {
    "additionalProperties": {},
    "description": "the get_dex_quote payload; a real captured example is free at https://x402.ochinimus.app/api/sample/get_dex_quote",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "tool": {
    "const": "get_dex_quote",
    "description": "the tool that produced this payload",
    "type": "string"
   }
  },
  "required": [
   "tool",
   "data"
  ],
  "type": "object"
 },
 "title": "Jupiter DEX quote"
}
