{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Diagnose an application by UUID, name, or domain. Aggregates current status, the latest deployments, a log tail from the most recent failed deployment, recent runtime logs, and suggested next actions in one call.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "identifier": {
    "description": "Application UUID, name, or domain (fqdn substring)",
    "type": "string"
   },
   "log_lines": {
    "description": "Runtime log lines to include (default 60)",
    "maximum": 500,
    "minimum": 10,
    "type": "integer"
   }
  },
  "required": [
   "identifier"
  ],
  "type": "object"
 },
 "name": "diagnoseApp",
 "title": "Diagnose application"
}
