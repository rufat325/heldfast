{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List pending user invitations on your team.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "auto_paginate": {
    "description": "Follow links.next across pages until the limit is reached (default false).",
    "type": "boolean"
   },
   "email": {
    "description": "Exact email filter",
    "type": "string"
   },
   "limit": {
    "description": "Max invitations (default 100). With auto_paginate this is the total across pages.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_user_invitations"
}
