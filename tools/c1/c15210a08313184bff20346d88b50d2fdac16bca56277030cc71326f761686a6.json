{
 "description": "One-call helper that returns the cheapest indexed service in a category plus a short list of alternates.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "category": {
    "minLength": 1,
    "type": "string"
   },
   "limit": {
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "unit": {
    "type": "string"
   }
  },
  "required": [
   "category"
  ],
  "type": "object"
 },
 "name": "smart_route",
 "title": "Route to the cheapest peer in a category"
}
