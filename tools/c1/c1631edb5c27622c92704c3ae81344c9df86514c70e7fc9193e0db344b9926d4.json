{
 "description": "A listing's prose as its reader sees it: the phrases that address the reading agent named, and the description rewritten without them, nothing added. Charged per call.",
 "inputSchema": {
  "properties": {
   "caller": {
    "type": "string"
   },
   "idempotencyKey": {
    "description": "your key for this request; send it again on a retry and the charge is made once",
    "type": "string"
   },
   "text": {
    "type": "string"
   }
  },
  "required": [
   "caller",
   "text"
  ],
  "type": "object"
 },
 "name": "sapphire_card_read"
}
