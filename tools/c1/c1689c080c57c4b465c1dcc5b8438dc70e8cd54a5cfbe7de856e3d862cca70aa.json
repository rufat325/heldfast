{
 "description": "Create a new slide-based presentation in the workspace. Returns the presentation ID. A presentation is a separate document type from a regular document: its content lives in slides (not top-level blocks). It starts with one blank slide. Use add_slide / add_slide_block to build it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "title": {
    "description": "Presentation title",
    "type": "string"
   }
  },
  "required": [
   "title"
  ],
  "type": "object"
 },
 "name": "create_presentation"
}
