{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Remove Guest"
 },
 "description": "Remove a guest-list entry from a live event you manage \u2014 deletes the entry and cleans up its counters. Only guest-list entries, never purchased tickets. Requires event_id + guest_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "guest_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "guest_id"
  ],
  "type": "object"
 },
 "name": "remove_guest",
 "title": "Remove Guest"
}
