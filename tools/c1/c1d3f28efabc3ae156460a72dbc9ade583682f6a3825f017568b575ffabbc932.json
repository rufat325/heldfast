{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true
 },
 "description": "Scroll an element into view (pass selector), or scroll the window to coordinates (pass x and y). Provide a selector OR x+y, not both.",
 "inputSchema": {
  "properties": {
   "selector": {
    "description": "CSS selector to scroll into view. Omit to scroll the window instead.",
    "type": "string"
   },
   "x": {
    "description": "Window scroll-x in pixels (used when no selector).",
    "type": "number"
   },
   "y": {
    "description": "Window scroll-y in pixels (used when no selector).",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "browser_scroll"
}
