{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true
 },
 "description": "Press a single key (e.g. Enter, Escape, Tab, ArrowDown) on the page. For typing text use browser_type.",
 "inputSchema": {
  "properties": {
   "key": {
    "description": "Key name to press, e.g. Enter, Escape, Tab, ArrowDown.",
    "type": "string"
   },
   "selector": {
    "description": "Optional CSS selector to focus before pressing the key.",
    "type": "string"
   }
  },
  "required": [
   "key"
  ],
  "type": "object"
 },
 "name": "browser_press"
}
