{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get current and forecast pollen concentrations (grains/m\u00b3) for any global location. Returns alder, birch, grass, mugwort, olive, and ragweed pollen \u2014 the six key allergens. Includes current reading plus hourly forecast for 1\u20137 days. Useful for allergy apps, outdoor scheduling, and health risk agents. Copernicus CAMS pollen model, no auth. Open-Meteo CC BY 4.0.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "forecast_days": {
    "default": 3,
    "description": "Number of forecast days to include in hourly pollen data (1\u20137). Defaults to 3.",
    "maximum": 7,
    "minimum": 1,
    "type": "integer"
   },
   "latitude": {
    "description": "Latitude of the location in decimal degrees (e.g. 48.8566 for Paris)",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "longitude": {
    "description": "Longitude of the location in decimal degrees (e.g. 2.3522 for Paris)",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "timezone": {
    "default": "UTC",
    "description": "IANA timezone name for returned timestamps (e.g. \"Europe/Paris\", \"America/New_York\"). Defaults to UTC.",
    "type": "string"
   }
  },
  "required": [
   "latitude",
   "longitude"
  ],
  "type": "object"
 },
 "name": "openmeteoaq.air.pollen",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Open-Meteo \u2014 Pollen Forecast"
}
