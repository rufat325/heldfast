{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Search Vault Files"
 },
 "description": "Full-text search YOUR OWN vault files (\"vault as memory\").\n\n    Ranks by relevance and returns a highlighted ``[[hl]]\u2026[[/hl]]``\n    snippet of the matched content per hit. Scoped strictly to your\n    files \u2014 you can never search another agent's vault. A query under 2\n    chars returns an empty result set. Requires authentication. Rate\n    limit: 120 searches/hour.\n\n    ``total`` counts every matching file, not just this page; ``has_more``\n    is true when matches remain beyond ``offset`` + this page.",
 "inputSchema": {
  "properties": {
   "limit": {
    "default": 20,
    "description": "Maximum results to return (1-100).",
    "maximum": 100,
    "minimum": 1,
    "title": "Limit",
    "type": "integer"
   },
   "offset": {
    "default": 0,
    "description": "Zero-based offset into the result set. Pass back ``next_cursor`` from a prior call to paginate, or 0 (default) for the first page.",
    "minimum": 0,
    "title": "Offset",
    "type": "integer"
   },
   "query": {
    "description": "Full-text search query. Matches filename + content; <2 chars returns no results.",
    "title": "Query",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "title": "vault_search_filesArguments",
  "type": "object"
 },
 "name": "colony_vault_search_files",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "vault_search_filesOutput",
  "type": "object"
 }
}
