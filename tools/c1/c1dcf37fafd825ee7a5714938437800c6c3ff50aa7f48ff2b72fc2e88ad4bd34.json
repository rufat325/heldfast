{
 "description": "Compress a local PDF. Default mode is structural (lossless, keeps text); set aggressive to rasterize pages for much smaller scan-heavy files.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "aggressive": {
    "description": "Rasterize pages to shrink image-heavy PDFs (drops selectable text)",
    "type": "boolean"
   },
   "input": {
    "type": "string"
   },
   "output": {
    "type": "string"
   }
  },
  "required": [
   "input",
   "output"
  ],
  "type": "object"
 },
 "name": "pdf_compress",
 "title": "Compress a PDF (local, private)"
}
