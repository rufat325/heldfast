{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": false,
  "title": "Supersede a memory packet with a newer one"
 },
 "description": "Replace one repo-local memory packet with a newer one that corrects or obsoletes it. Marks the old packet superseded, links it to the replacement, and writes bidirectional lineage edges so the history stays traceable. Use this instead of deleting when new knowledge updates an old fact, or to resolve a contradiction surfaced by kage_conflicts. Mutates both packets on disk: the superseded packet is withheld from recall but kept for lineage. Returns ids, paths, and titles for confirmation, not the full packet bodies.",
 "inputSchema": {
  "properties": {
   "packet_id": {
    "description": "Id of the existing packet to retire (the one being replaced).",
    "type": "string"
   },
   "project_dir": {
    "description": "Absolute path to the repository root.",
    "type": "string"
   },
   "reason": {
    "description": "Optional human note recorded on the lineage edge explaining why it was superseded.",
    "type": "string"
   },
   "replacement_packet_id": {
    "description": "Id of the newer packet that wins and stays active.",
    "type": "string"
   }
  },
  "required": [
   "project_dir",
   "packet_id",
   "replacement_packet_id"
  ],
  "type": "object"
 },
 "name": "kage_supersede"
}
