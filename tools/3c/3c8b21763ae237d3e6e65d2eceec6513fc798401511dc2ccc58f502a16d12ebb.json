{
 "description": "Audit a single page URL; returns E-E-A-T, GEO, and SEO scores with issues. Auto-routes: audit_page_id re-audits in-place; no existing site audit starts one async (poll audit_id); else synchronous.",
 "inputSchema": {
  "properties": {
   "audit_page_id": {
    "description": "Re-audit this page in-place within its site audit instead of a standalone audit.",
    "type": "string"
   },
   "confirm_target_keyword_change": {
    "description": "Set true after explicit user confirmation.",
    "type": "boolean"
   },
   "keyword": {
    "description": "Target keyword for SEO scoring context",
    "type": "string"
   },
   "site_audit_id": {
    "description": "Site audit the page belongs to; use with audit_page_id.",
    "type": "string"
   },
   "site_id": {
    "description": "Force routing to this site instead of auto-detecting by domain.",
    "type": "string"
   },
   "url": {
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "audit_page"
}
