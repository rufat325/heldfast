{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get analytics"
 },
 "description": "Get analytics snapshots for a post, an account, or a run. scope must be post, account, or run; id is the matching identifier.",
 "inputSchema": {
  "properties": {
   "id": {
    "type": "string"
   },
   "scope": {
    "enum": [
     "post",
     "account",
     "run"
    ],
    "type": "string"
   },
   "window": {
    "description": "Optional analytics window, e.g. 7d.",
    "type": "string"
   }
  },
  "required": [
   "scope",
   "id"
  ],
  "type": "object"
 },
 "name": "get_analytics",
 "title": "Get analytics"
}
