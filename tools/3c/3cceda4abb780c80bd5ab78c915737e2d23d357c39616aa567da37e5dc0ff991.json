{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List running processes"
 },
 "description": "List running processes sorted by CPU. Use \"top\" to limit (default 25, max 200). Prefer check_process when searching for one process. Command lines truncated unless full_command_line=true.",
 "inputSchema": {
  "properties": {
   "full_command_line": {
    "description": "true for full command lines",
    "type": "boolean"
   },
   "top": {
    "description": "How many processes to return (default 25)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list_processes"
}
