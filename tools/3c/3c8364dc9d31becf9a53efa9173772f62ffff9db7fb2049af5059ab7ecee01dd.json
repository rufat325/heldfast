{
 "annotations": {
  "readOnlyHint": false,
  "title": "Cancel a scheduled text"
 },
 "description": "Cancel a text that was scheduled with send_text_to_user and has not been sent yet. Takes the task_id from the scheduling response or from list_scheduled_texts.",
 "inputSchema": {
  "properties": {
   "task_id": {
    "description": "The scheduled text to cancel.",
    "type": "integer"
   }
  },
  "required": [
   "task_id"
  ],
  "type": "object"
 },
 "name": "cancel_scheduled_text",
 "title": "Cancel a scheduled text"
}
