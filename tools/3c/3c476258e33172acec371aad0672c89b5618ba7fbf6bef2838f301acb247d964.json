{
 "description": "Show virtual memory map for a running app. Displays memory regions, sizes, and permissions. Useful for diagnosing memory issues.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bundleId": {
    "description": "Bundle ID of the running app to inspect virtual memory",
    "type": "string"
   },
   "deviceId": {
    "description": "Device (default: booted)",
    "type": "string"
   },
   "summary": {
    "description": "Show summary only instead of full map (default: true)",
    "type": "boolean"
   }
  },
  "required": [
   "bundleId"
  ],
  "type": "object"
 },
 "name": "simulator_vmmap"
}
