{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Add or replace a screening answer in the answer book ('What is your notice period?' \u2192 '1 month'). Applications already waiting on that fact are completed and sent in the background. Only save what the person actually stated.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "answer": {
    "maxLength": 5000,
    "type": "string"
   },
   "kind": {
    "default": "text",
    "enum": [
     "text",
     "boolean",
     "select",
     "number"
    ],
    "type": "string"
   },
   "options": {
    "description": "For select questions: the choices the form offers.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "question": {
    "maxLength": 500,
    "type": "string"
   }
  },
  "required": [
   "question",
   "answer"
  ],
  "type": "object"
 },
 "name": "save_answer",
 "title": "Teach an answer once, for every application"
}
