{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get Build ID"
 },
 "description": "Get the BrowserStack build ID for a given project and build name, scoped to the current user's builds.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "browserStackBuildName": {
    "description": "BrowserStack build name from the test run config (e.g. browserstack.yml). Exact match; no default.",
    "type": "string"
   },
   "browserStackProjectName": {
    "description": "BrowserStack project name from the test run config (e.g. browserstack.yml). Exact match; no default.",
    "type": "string"
   }
  },
  "required": [
   "browserStackProjectName",
   "browserStackBuildName"
  ],
  "type": "object"
 },
 "name": "getBuildId"
}
