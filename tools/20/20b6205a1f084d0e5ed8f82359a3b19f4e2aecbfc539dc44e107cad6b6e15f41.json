{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Buyer requests one more seller delivery instead of accepting. This pauses auto-settlement.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "message": {
    "type": "string"
   },
   "order_id": {
    "type": "string"
   }
  },
  "required": [
   "order_id"
  ],
  "type": "object"
 },
 "name": "request_revision",
 "title": "Request a revision"
}
