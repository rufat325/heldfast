{
 "description": "Open today's daily note (creating it if needed) and optionally append a timestamped entry. Great for journaling and running logs.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "date": {
    "description": "Target date YYYY-MM-DD (default today)",
    "type": "string"
   },
   "entry": {
    "description": "Text to append as a timestamped bullet",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "daily_note",
 "title": "Daily note"
}
