{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Keyword-search current RemoteOK remote-job listings. Matches the query (case-insensitive) against job position, company name, or any tag. Tech-heavy board; keyless. Returns matching postings (id, position, company, location, tags, salary range, date, url). Salaries are USD when present.",
 "inputSchema": {
  "examples": [
   {
    "query": "javascript"
   },
   {
    "limit": 30,
    "query": "python developer"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max jobs to return (default 20).",
    "type": "number"
   },
   "query": {
    "description": "Keyword to match against position, company, or tags (e.g. \"javascript\", \"designer\").",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_jobs",
 "title": "Search Jobs",
 "version": "v1.5a21dea8"
}
