{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Add a subnet to an existing network.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "id": {
    "description": "Resource ID",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "ip_range": {
    "description": "IP range of the subnet",
    "type": "string"
   },
   "network_zone": {
    "description": "Name of the network zone, e.g. \"eu-central\"",
    "type": "string"
   },
   "type": {
    "description": "Type of subnet",
    "enum": [
     "cloud",
     "server",
     "vswitch"
    ],
    "type": "string"
   },
   "vswitch_id": {
    "description": "ID of the vSwitch (required for vswitch type)",
    "maximum": 9007199254740991,
    "minimum": -9007199254740991,
    "type": "integer"
   }
  },
  "required": [
   "id",
   "type",
   "network_zone"
  ],
  "type": "object"
 },
 "name": "hetzner_add_subnet",
 "title": "Add Subnet to Network"
}
