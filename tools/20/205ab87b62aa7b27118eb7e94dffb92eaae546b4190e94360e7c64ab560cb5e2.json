{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Query T100 airline segment statistics from BTS giving passenger counts, departures, seats, load factor, and payload by US airport. Filter by IATA airport code (e.g. ATL, JFK, LAX, ORD) and year/period (annual \"2025\" or quarterly \"2026M1-3\"). Data covers all US commercial service airports through the most recent quarter. Source: BTS T100 Segment Summary By Origin Airport (Socrata r495-tyji). No auth \u2014 US Gov public domain.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "airport_code": {
    "description": "IATA airport code to filter by (e.g. \"ATL\" for Atlanta, \"JFK\" for New York JFK, \"LAX\" for Los Angeles). Returns top airports if omitted.",
    "maxLength": 3,
    "minLength": 3,
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of airport traffic records to return (1\u2013500, default 20).",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "year": {
    "description": "Year or period to filter by. Annual values use format \"YYYY\" (e.g. \"2025\"). Quarterly use \"YYYYQn\" (e.g. \"2026M1-3\" for Q1 2026). Latest available is 2026M1-3.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "bts.aviation.traffic",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "BTS \u2014 US Aviation Traffic by Airport (T100)"
}
