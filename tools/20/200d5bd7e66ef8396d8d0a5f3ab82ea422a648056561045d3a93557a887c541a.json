{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Fetches a transaction receipt by hash. Returns status, gas used, block number, and the explorer URL.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "network": {
    "description": "Monad network \u2014 \"mainnet\" or \"testnet\". Defaults to the server's configured network.",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   },
   "tx_hash": {
    "description": "Transaction hash to look up.",
    "pattern": "^0x[a-fA-F0-9]{64}$",
    "type": "string"
   }
  },
  "required": [
   "tx_hash"
  ],
  "type": "object"
 },
 "name": "get_tx_receipt",
 "title": "Get transaction receipt"
}
