{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Create an account-balance top-up invoice payable in crypto (BTC, XMR, USDT-TRC20, TRX) \u2014 Impreza is no-KYC and privacy-first. Returns an `invoice_id`, the amount, and a `payment_url` the customer opens to pay; the balance auto-credits once the payment confirms. Poll `impreza_topup_status` for the state.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "amount": {
    "description": "Top-up amount in the account currency (1.00\u201310000.00).",
    "type": "number"
   },
   "method": {
    "description": "Optional preferred crypto rail; the payment page still lets the customer switch.",
    "enum": [
     "btc",
     "xmr",
     "trx",
     "usdt",
     "usdt_trc20"
    ],
    "type": "string"
   }
  },
  "required": [
   "amount"
  ],
  "type": "object"
 },
 "name": "impreza_topup"
}
