{
 "description": "Ranked list of which Captapi routes your key calls most (0 credits). Costs 0 credits. Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "days": {
    "description": "Number of days to include (default 30, max 365).",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "limit": {
    "description": "Max rows to return (default 20, max 100). Free \u2014 does not consume credits.",
    "exclusiveMinimum": 0,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "account_most_used_routes",
 "title": "Most Used Routes"
}
