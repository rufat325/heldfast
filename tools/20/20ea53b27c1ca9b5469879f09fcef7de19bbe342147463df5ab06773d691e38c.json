{
 "description": "Updates one deal in the tenant CRM; only the fields you send change. Needs crm:write on the organization. A foreign or missing id answers 404. Money is in minor units: `value_minor: 10000` with `currency: \"USD\"` is $100.00, not $10,000.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "currency": {
    "description": "currency",
    "type": "string"
   },
   "deal_id": {
    "description": "deal_id",
    "type": "string"
   },
   "organization_id": {
    "description": "organization_id",
    "type": "string"
   },
   "pipeline_id": {
    "description": "pipeline id",
    "type": "string"
   },
   "stage_id": {
    "description": "stage id",
    "type": "string"
   },
   "title": {
    "description": "title",
    "type": "string"
   },
   "value_minor": {
    "description": "value_minor",
    "type": "integer"
   }
  },
  "required": [
   "organization_id",
   "deal_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_crm_deals_update"
}
