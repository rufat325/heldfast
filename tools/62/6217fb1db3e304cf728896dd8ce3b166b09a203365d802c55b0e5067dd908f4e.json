{
 "description": "Content-aware removal of a region in an image using a mask. White mask areas are erased and filled naturally.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "file_path": {
    "description": "Local path to source image (max 4MP)",
    "type": "string"
   },
   "mask_path": {
    "description": "Local path to mask (white=erase, black=preserve, same dimensions)",
    "type": "string"
   },
   "response_format": {
    "default": "url",
    "description": "Response format",
    "enum": [
     "url",
     "b64_json"
    ],
    "type": "string"
   }
  },
  "required": [
   "file_path",
   "mask_path"
  ],
  "type": "object"
 },
 "name": "recraft_erase_region"
}
