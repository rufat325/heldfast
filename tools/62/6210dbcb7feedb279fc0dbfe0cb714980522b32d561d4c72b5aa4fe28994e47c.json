{
 "description": "Fuzzy search for specific tools/actions across all Katzilla data agents by query terms.",
 "inputSchema": {
  "properties": {
   "agent": {
    "description": "Optional: Filter results to specific agent handle (comma-separated)",
    "type": "string"
   },
   "query": {
    "description": "Search term (e.g., 'earthquake', 'sec filing', 'weather')",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_tools",
 "title": "Search Tools"
}
