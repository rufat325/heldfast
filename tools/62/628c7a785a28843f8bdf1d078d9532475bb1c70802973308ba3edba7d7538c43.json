{
 "description": "[GA4 WRITE] Grant a user access to a GA4 account (needs analytics.manage.users). Requires GA4_MCP_ENABLE_WRITES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "description": "GA4 account ID, e.g. \"123456\" or \"accounts/123456\".",
    "minLength": 1,
    "type": "string"
   },
   "confirm": {
    "description": "Must be true to apply the change.",
    "type": "boolean"
   },
   "roles": {
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "user": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "user",
   "roles",
   "confirm"
  ],
  "type": "object"
 },
 "name": "ga4_create_account_access_binding"
}
