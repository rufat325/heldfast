{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Decode a categorical column's codes into German + English display names (e.g. \"STAATS-9\" -> \"Insgesamt\" / \"Total\"). Requires dataset_id and dimension_code (format \"C-{NAME}-{N}\", from statistik-austria.dataset_metadata's category_dimensions list). Data: data.statistik.gv.at (Statistik Austria), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataset_id": {
    "description": "Statistik Austria dataset id, from statistik-austria.dataset_search (e.g. \"OGD_veste309_Veste309_1\").",
    "minLength": 1,
    "type": "string"
   },
   "dimension_code": {
    "description": "Categorical column code to decode, from statistik-austria.dataset_metadata's category_dimensions list (format \"C-{NAME}-{N}\", e.g. \"C-STAATS-0\" for citizenship, \"C-VEBDL-0\" for federal state). Columns starting with \"F-\" are numeric measures with no code lookup.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "dataset_id",
   "dimension_code"
  ],
  "type": "object"
 },
 "name": "statistik-austria.reference.category_codes",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Statistik Austria Category Codes"
}
