{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "PAID ($0.001 USDC via x402). The native ETH balance of any address on Base, in wei and ETH. Without payment this returns the 402 challenge; the fiatdock-mcp npm package pays automatically. An invalid address returns 400 and is NOT charged; on any RPC failure it returns 5xx and is NOT charged.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "A 40-hex EVM address (0x\u2026) to read the ETH balance of",
    "type": "string"
   },
   "payment": {
    "description": "Base64 of a single x402 v2 PaymentPayload (EIP-3009 transferWithAuthorization on Base USDC). OMIT it on the first call: the 402 you get back carries `howToPay.payloadTemplate` \u2014 the exact envelope to fill in \u2014 plus the price and the EIP-712 domain. Send it on the second call to complete the purchase; it is forwarded as the PAYMENT-SIGNATURE header, never as body data. Nothing is charged for the 402 itself, and a call that fails after payment settles nothing.",
    "type": "string"
   }
  },
  "required": [
   "address"
  ],
  "type": "object"
 },
 "name": "eth_balance",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "The queried address",
    "type": "string"
   },
   "asOf": {
    "description": "ISO 8601 read time",
    "type": "string"
   },
   "eth": {
    "description": "ETH balance as a decimal string (18 dp, trimmed)",
    "type": "string"
   },
   "network": {
    "description": "Chain read (always base)",
    "type": "string"
   },
   "wei": {
    "description": "ETH balance in wei (string; may exceed Number range)",
    "type": "string"
   }
  },
  "required": [
   "network",
   "address",
   "wei",
   "eth",
   "asOf"
  ],
  "type": "object"
 },
 "title": "ETH balance on Base ($0.001)"
}
