{
 "description": "Record-only dry run to verify selectors/timing (narration optional). With updateGolden, writes golden/probe.json (the regression baseline); with golden, deep-compares against it and returns match + field-level diffs. Returns a jobId immediately \u2014 poll job_status. Rejects if another job is running.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "capture": {
    "enum": [
     "playwright",
     "native",
     "obs"
    ],
    "type": "string"
   },
   "cookies": {
    "description": "cookies to seed before the first action (name, value, domain[, path, secure, httpOnly, sameSite, expires]). Also storyboard setup.cookies.",
    "items": {
     "additionalProperties": false,
     "properties": {
      "domain": {
       "type": "string"
      },
      "expires": {
       "type": "number"
      },
      "httpOnly": {
       "type": "boolean"
      },
      "name": {
       "type": "string"
      },
      "path": {
       "type": "string"
      },
      "sameSite": {
       "enum": [
        "Strict",
        "Lax",
        "None"
       ],
       "type": "string"
      },
      "secure": {
       "type": "boolean"
      },
      "url": {
       "type": "string"
      },
      "value": {
       "type": "string"
      }
     },
     "required": [
      "name",
      "value"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "dir": {
    "description": "demo project directory \u2014 pass an absolute path",
    "type": "string"
   },
   "fresh": {
    "description": "run against a WIPED throwaway profile \u2014 use for any demo whose story starts at a first-run gate, onboarding, an empty state or a one-shot flow, since carried-over cookies/localStorage silently record the wrong story. Not for logged-in demos (a fresh profile has no login).",
    "type": "boolean"
   },
   "fromScene": {
    "description": "resume: keep the previous take's scenes before this scene id (footage + timeline, verified unchanged by hash) and record from it \u2014 after a late-scene failure or a change to the tail of the storyboard",
    "type": "string"
   },
   "golden": {
    "description": "compare against golden/probe.json; returns match + diffs (CI guard)",
    "type": "boolean"
   },
   "headless": {
    "description": "run Chrome headless (default false \u2014 headed, like the CLI)",
    "type": "boolean"
   },
   "params": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "storyboard template params (name \u2192 value); each must be declared in the storyboard's params block. Substitutes {{name}} across all stages.",
    "type": "object"
   },
   "profile": {
    "description": "Chrome user-data dir (logged-in profile)",
    "type": "string"
   },
   "profileSeeded": {
    "description": "the profile is seeded on purpose (login / cookie gate): silence the carried-over-state warning. Implied by storageState/cookies.",
    "type": "boolean"
   },
   "storageState": {
    "description": "absolute path to a Playwright storageState JSON (cookies + per-origin localStorage) to seed into the profile before the first action \u2014 for cookie-gated sites. Also available as storyboard setup.storageState.",
    "type": "string"
   },
   "updateGolden": {
    "description": "write golden/probe.json from this probe (commit it as the baseline)",
    "type": "boolean"
   }
  },
  "required": [
   "dir"
  ],
  "type": "object"
 },
 "name": "probe",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "demoDir": {
    "description": "resolved absolute demo directory",
    "type": "string"
   },
   "jobId": {
    "type": "string"
   },
   "kind": {
    "type": "string"
   },
   "logFile": {
    "type": "string"
   },
   "status": {
    "type": "string"
   }
  },
  "required": [
   "jobId",
   "kind",
   "demoDir",
   "status",
   "logFile"
  ],
  "type": "object"
 }
}
