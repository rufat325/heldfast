{
 "description": "Get a network brief for a specific job. Returns company signal, recommended motion, top contact, and suggested actions.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "jobId": {
    "description": "Job posting ID",
    "type": "number"
   }
  },
  "required": [
   "jobId"
  ],
  "type": "object"
 },
 "name": "trackly_get_job_brief"
}
