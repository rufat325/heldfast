{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Shipments refused entry to the US by FDA at the border, with the refusal charges (the reason), product code and date. A leading indicator of supply risk for a foreign manufacturer. Search by firm, country, product or date range. Source: FDA Data Dashboard (OII).",
 "inputSchema": {
  "examples": [
   {
    "country": "India",
    "limit": 25
   },
   {
    "firm": "Intas",
    "limit": 50,
    "since": "2024-01-01"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "FDA Data Dashboard credential as \"email:key\" \u2014 the e-mail the credential was issued to, then the FDA-generated key, joined by a colon.",
    "type": "string"
   },
   "country": {
    "description": "Country of origin, e.g. \"India\".",
    "type": "string"
   },
   "firm": {
    "description": "Firm name substring (this resource keys on FirmName).",
    "type": "string"
   },
   "limit": {
    "description": "Rows to return (default 50, max 5000).",
    "type": "number"
   },
   "product": {
    "description": "Product-code description substring, e.g. \"Antibiotic\".",
    "type": "string"
   },
   "since": {
    "description": "Earliest refusal date, YYYY-MM-DD.",
    "type": "string"
   },
   "state": {
    "description": "US state, as a postal code (\"NJ\") or full name (\"New Jersey\") \u2014 both work. Foreign sites carry no state; filter those by country.",
    "type": "string"
   },
   "until": {
    "description": "Latest refusal date, YYYY-MM-DD.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "fda_import_refusals",
 "title": "Fda Import Refusals",
 "version": "v1.e6ca21c0"
}
