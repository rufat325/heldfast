{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "List the charts on a sheet \u2014 id, kind, title, the ranges each series reads, and where the chart sits. Call this before updating or deleting one.",
 "inputSchema": {
  "properties": {
   "sheetIdx": {
    "description": "Zero-based sheet index.",
    "type": "integer"
   }
  },
  "required": [
   "sheetIdx"
  ],
  "type": "object"
 },
 "name": "chart_list"
}
