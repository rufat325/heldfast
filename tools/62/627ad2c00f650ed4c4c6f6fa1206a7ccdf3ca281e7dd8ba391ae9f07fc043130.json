{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get price history (OHLC + volume) for a listed security",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "end_date": {
    "description": "End date (YYYY-MM-DD)",
    "type": "string"
   },
   "exchange_code": {
    "description": "Exchange code",
    "type": "string"
   },
   "limit": {
    "default": 365,
    "description": "Max data points",
    "maximum": 5000,
    "minimum": 1,
    "type": "integer"
   },
   "start_date": {
    "description": "Start date (YYYY-MM-DD)",
    "type": "string"
   },
   "symbol": {
    "description": "Ticker symbol",
    "type": "string"
   }
  },
  "required": [
   "exchange_code",
   "symbol"
  ],
  "type": "object"
 },
 "name": "get_ticker_history"
}
