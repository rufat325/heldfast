{
 "description": "Enable clean status bar for screenshots (fixed clock, full battery, full signal) or disable it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "clock": {
    "description": "Clock as hhmm, e.g. '1200' (default 1200)",
    "type": "string"
   },
   "enabled": {
    "description": "true to enter demo mode, false to exit",
    "type": "boolean"
   },
   "serial": {
    "description": "Emulator serial (defaults to session or the only device)",
    "type": "string"
   }
  },
  "required": [
   "enabled"
  ],
  "type": "object"
 },
 "name": "statusbar_demo_mode",
 "title": "Status bar demo mode"
}
