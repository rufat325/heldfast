{
 "description": "Persist the current workbook so the user can keep it.\n\nWaterfallBridge stores no user data on the server, and the Agent-controlled Chromium has its own IndexedDB profile that is isolated from the user's daily browser. By default this tool therefore does BOTH of the following:\n  1. Save the Univer snapshot to the Agent Chromium's IndexedDB (equivalent to Ctrl+S inside that window).\n  2. Export an .xlsx copy to <UserHome>/Downloads so the user can import it from their regular browser (or open it in Excel).\n\nDisable either side via `to_browser=false` / `export_to_disk=false`. Returns the exported file path so the AI can tell the user where their result was saved.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "export_path": {
    "description": "Optional absolute path for the exported .xlsx. Defaults to <UserHome>/Downloads/WaterfallBridge-AI-<timestamp>.xlsx. Ignored when export_to_disk=false.",
    "type": "string"
   },
   "export_to_disk": {
    "description": "When true (default), also export an .xlsx copy of the workbook to the user's Downloads folder so the user can later import it from their regular browser. This is what makes the AI's work user-visible in WaterfallBridge's no-server-data architecture. Set to false to suppress the file export.",
    "type": "boolean"
   },
   "to_browser": {
    "description": "When true (default), persist the workbook to the Agent-controlled Chromium's IndexedDB \u2014 equivalent to clicking Save / pressing Ctrl+S inside that window. NOTE: this storage is local to the Agent's Chromium profile and is NOT visible to the user's regular browser. Set to false to skip this step.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "save_workbook"
}
