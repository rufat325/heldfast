{
 "description": "Release a leased account so others can use it. Idempotent: releasing an already-released, expired, or unknown token returns released:false rather than erroring.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lease_token": {
    "description": "The lease_token returned by lease_account.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "lease_token"
  ],
  "type": "object"
 },
 "name": "release_account"
}
