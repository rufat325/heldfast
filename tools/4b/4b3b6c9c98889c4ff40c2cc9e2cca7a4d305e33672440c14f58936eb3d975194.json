{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Queries the LiFi aggregator for the cheapest/fastest route from another chain into Monad (chain id 143). Returns the chosen route + transaction request you can submit on the source chain. If Monad isn't yet listed by LiFi, returns a clear error.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "amount": {
    "description": "Amount in source token, decimal e.g. '10'.",
    "pattern": "^\\d+(\\.\\d+)?$",
    "type": "string"
   },
   "from_chain_id": {
    "description": "Source chain ID (e.g. 1=Ethereum, 10=Optimism, 42161=Arbitrum).",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "from_decimals": {
    "default": 18,
    "maximum": 36,
    "minimum": 0,
    "type": "integer"
   },
   "from_token": {
    "description": "Source token: 0x address, or '0x0000\u20260000' for native.",
    "type": "string"
   },
   "recipient": {
    "description": "Address on Monad to receive bridged tokens. Defaults to the user's wallet.",
    "pattern": "^0x[a-fA-F0-9]{40}$",
    "type": "string"
   },
   "slippage_bps": {
    "default": 50,
    "description": "Default 50 bps = 0.5%.",
    "maximum": 5000,
    "minimum": 0,
    "type": "integer"
   },
   "to_token": {
    "description": "Destination token on Monad (0x address) or 'MON' for native.",
    "type": "string"
   }
  },
  "required": [
   "from_chain_id",
   "from_token",
   "to_token",
   "amount"
  ],
  "type": "object"
 },
 "name": "bridge_quote",
 "title": "Get a bridge quote into Monad (LiFi)"
}
