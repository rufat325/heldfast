{
 "description": "Record review readiness or a user-confirmed outcome. Before handoff use literal outcome=review_ready and verify awaiting_manual_submit. Mark submitted with literal outcome=submitted only after a success page or explicit user confirmation.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "batchId": {
    "minimum": 1,
    "type": "integer"
   },
   "confirmation": {
    "enum": [
     "user_confirmation",
     "success_page"
    ],
    "type": "string"
   },
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "idempotencyKey": {
    "maxLength": 200,
    "minLength": 16,
    "pattern": "^[\\x20-\\x7e]+$",
    "type": "string"
   },
   "inspectionEpoch": {
    "minimum": 0,
    "type": "integer"
   },
   "leaseToken": {
    "maxLength": 1024,
    "minLength": 1,
    "type": "string"
   },
   "memberId": {
    "minimum": 1,
    "type": "integer"
   },
   "outcome": {
    "enum": [
     "review_ready",
     "submitted",
     "failed",
     "blocked"
    ],
    "type": "string"
   },
   "runId": {
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "runId",
   "idempotencyKey",
   "outcome"
  ],
  "type": "object"
 },
 "name": "trackly_record_application_outcome"
}
