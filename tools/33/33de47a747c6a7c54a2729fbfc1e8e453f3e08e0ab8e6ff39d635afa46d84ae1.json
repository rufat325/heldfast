{
 "description": "Too many MCP tools makes agents pick the wrong one. Given your tool list and usage, get keep / merge / remove recommendations to get under the 10\u201315 tool guideline.",
 "inputSchema": {
  "properties": {
   "targetToolCount": {
    "description": "Target number of tools to keep (default 12)",
    "type": "number"
   },
   "tools": {
    "description": "JSON array of {name, category?, callsPerDay?, description?}. Accepts a JSON array, or a JSON string containing one.",
    "type": [
     "string",
     "array",
     "object"
    ]
   }
  },
  "required": [
   "tools"
  ],
  "type": "object"
 },
 "name": "advise_tool_portfolio"
}
