{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Get one invoice by id, including line items, totals and payment status. Use ws_invoice_pdf for the document itself.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "invoiceId": {
    "description": "Invoice id, as returned by ws_invoice_list.",
    "minLength": 1,
    "type": "string"
   },
   "userId": {
    "default": "self",
    "description": "Account id, or the literal \"self\" for the authenticated account. Defaults to \"self\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "invoiceId"
  ],
  "type": "object"
 },
 "name": "ws_invoice_get",
 "title": "Get invoice"
}
