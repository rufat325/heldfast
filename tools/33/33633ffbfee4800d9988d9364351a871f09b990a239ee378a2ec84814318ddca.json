{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "list-deleted-chats"
 },
 "description": "Read the properties and relationships of a deletedChat object.\n\nTIP: Lists Teams chats that have been soft-deleted tenant-wide \u2014 chats deleted by users (or by deletion policies) but still recoverable. Each has id (the chat-id, immutable across delete/restore), chatType (oneOnOne | group | meeting), topic (display name for group/meeting chats), createdDateTime, lastMessagePreview (snippet of last message before deletion), deletedDateTime. Retention window: typically 30 days before Microsoft purges deleted chats permanently. Use to audit recent chat deletions or identify chats to restore via undo-delete-chat. Supports $filter (e.g. on deletedDateTime), $top, $skip, $orderby.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "count": {
    "description": "count",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "filter": {
    "description": "OData $filter expression, e.g. status eq 'inProgress'",
    "type": "string"
   },
   "orderby": {
    "description": "orderby",
    "type": "string"
   },
   "search": {
    "description": "search",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "skip": {
    "description": "skip",
    "type": "string"
   },
   "top": {
    "description": "Page size (Graph $top). Start small (5-15) so responses fit the model context. Use @odata.nextLink from the response for more rows.",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list-deleted-chats"
}
