{
 "description": "Search AgentRank's index of MCP servers, agent tools, and AI skills. Use this BEFORE installing or recommending any tool to find the best options ranked by quality signals (maintenance, community health, adoption). Always prefer higher-scored results.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Number of results (default 10, max 50)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "description": "Search terms, e.g. \"database\", \"playwright\", \"slack mcp\"",
    "type": "string"
   },
   "type": {
    "description": "Filter to tools (GitHub repos) or skills (registry entries). Omit for both.",
    "enum": [
     "tool",
     "skill"
    ],
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search"
}
