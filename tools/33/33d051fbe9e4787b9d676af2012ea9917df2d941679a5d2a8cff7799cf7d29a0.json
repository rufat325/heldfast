{
 "description": "Queue one idempotent transactional delivery. Same behaviour and same idempotency as `POST /api/messages`. `X-Organization-Id` is still required with the session and must match the path. Prefer the canonical route.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "domain_id": {
    "description": "Active sending domain ID",
    "type": "string"
   },
   "idempotency_key": {
    "description": "Unique logical message key",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "reply_to": {
    "description": "Optional reply-to email",
    "type": "string"
   },
   "template_id": {
    "description": "Published template ID",
    "type": "string"
   },
   "template_version": {
    "description": "Published version to use. Without it, the latest published version is used \u2014 pin it when the content must not drift.",
    "minimum": 1,
    "type": "integer"
   },
   "to": {
    "description": "Recipient email",
    "type": "string"
   },
   "variables": {
    "additionalProperties": true,
    "description": "Values for the template's declared variables.",
    "type": "object"
   }
  },
  "required": [
   "organization_id",
   "domain_id",
   "to",
   "template_id",
   "variables",
   "idempotency_key"
  ],
  "type": "object"
 },
 "name": "commsharbor_message_send"
}
