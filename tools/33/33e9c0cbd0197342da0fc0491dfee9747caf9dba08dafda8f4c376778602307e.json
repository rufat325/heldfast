{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Save a named custom field set as a reusable task type (#103 Phase B).\n\nOnce saved, submit against it by passing its returned id as 'task_type' to handinloop_submit_extraction_job \u2014 no need to re-send the fields each time. Saved types also appear in handinloop_list_task_types. Saving an existing id updates it.\n\nArgs:\n  - fields: [{ name, description? }] \u2014 the field set (required, at least one).\n  - name (optional): human name; the id is derived from it if 'id' is omitted.\n  - id (optional): stable handle (lowercase letter then letters/digits/underscores). Must not collide with a built-in.\n  - description (optional).\n\nReturns the saved TaskType { id, name, description, output_fields }.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "description": {
    "description": "Optional description of the type",
    "type": "string"
   },
   "fields": {
    "description": "The field set to save (at least one)",
    "items": {
     "additionalProperties": false,
     "properties": {
      "description": {
       "description": "What this field is",
       "type": "string"
      },
      "name": {
       "description": "Field name, e.g. 'do_number'",
       "type": "string"
      }
     },
     "required": [
      "name"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "id": {
    "description": "Stable handle; derived from name if omitted",
    "type": "string"
   },
   "name": {
    "description": "Human name, e.g. 'Delivery Order'",
    "type": "string"
   }
  },
  "required": [
   "fields"
  ],
  "type": "object"
 },
 "name": "handinloop_save_task_type",
 "title": "Save a Reusable Task Type"
}
