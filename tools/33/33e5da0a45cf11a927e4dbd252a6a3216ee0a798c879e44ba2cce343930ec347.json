{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch items from an existing Apify dataset (e.g. the output of a prior Actor run). Example: apify_dataset_items({ datasetId: \"abc123\", limit: 50, _apiKey: \"your-token\" }).",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-apify-api-key",
    "datasetId": "abc123xyz"
   },
   {
    "_apiKey": "your-apify-api-key",
    "clean": true,
    "datasetId": "apify~my-dataset",
    "limit": 50,
    "offset": 0
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Your Apify API token (get one at https://console.apify.com/sign-up).",
    "type": "string"
   },
   "clean": {
    "description": "If true (default), return only non-empty items and skip hidden fields.",
    "type": "boolean"
   },
   "datasetId": {
    "description": "The dataset ID (or username~dataset-name). Returned as `defaultDatasetId` from an Actor run.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of items to return. Default 100 (Apify itself has no default limit; we cap for readability).",
    "type": "number"
   },
   "offset": {
    "description": "Number of items to skip from the start. Default 0.",
    "type": "number"
   }
  },
  "required": [
   "datasetId",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "apify_dataset_items",
 "title": "Apify Dataset Items",
 "version": "v1.e691e882"
}
