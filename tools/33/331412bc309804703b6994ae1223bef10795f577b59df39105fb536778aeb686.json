{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List page block types"
 },
 "description": "List the page BLOCK TYPES available to build_page, each with its content JSON Schema. Call this first when composing a rich page so you know the vocabulary (header, link, text, image, button, video, gallery, faq, countdown, socials, divider, form, \u2026).",
 "inputSchema": {
  "properties": {},
  "type": "object"
 },
 "name": "list_block_types",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "block_types": {
    "items": {
     "additionalProperties": true,
     "properties": {
      "label": {
       "type": "string"
      },
      "schema": {
       "description": "JSON Schema for the block's `content`.",
       "type": "object"
      },
      "type": {
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "error": {
    "additionalProperties": true,
    "description": "Present only when the tool failed (the result is also flagged isError).",
    "properties": {
     "cause": {
      "description": "Machine-readable failure category: invalid_argument, not_found, forbidden, plan_limit, rate_limited, or server_error.",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "retryable": {
      "description": "Whether repeating the same call could succeed.",
      "type": "boolean"
     }
    },
    "type": "object"
   }
  },
  "type": "object"
 }
}
