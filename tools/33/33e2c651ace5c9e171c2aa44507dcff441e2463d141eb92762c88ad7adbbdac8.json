{
 "description": "Update the status of a task. Omit workflowId to update a task by id regardless of its workflow (the server resolves it). Updates the DB task record; for legacy file-backed workflows it instead swaps the status tag on the underlying phase file. Use in_progress when starting work, done when complete, dismissed when no longer relevant.",
 "inputSchema": {
  "properties": {
   "phaseFileId": {
    "description": "Legacy file-backed workflows only: link or update the phase file ID for this task. Pass null to clear the link.",
    "type": "string"
   },
   "priority": {
    "description": "Update the task priority. Optional \u2014 only set when changing priority.",
    "enum": [
     "high",
     "medium",
     "low"
    ],
    "type": "string"
   },
   "status": {
    "description": "New status for the task.",
    "enum": [
     "not_started",
     "in_progress",
     "done",
     "dismissed"
    ],
    "type": "string"
   },
   "taskId": {
    "description": "Task ID to update.",
    "type": "string"
   },
   "workflowId": {
    "description": "Workflow ID that owns the task. Omit to resolve the task by id regardless of its workflow.",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "taskId",
   "status"
  ],
  "type": "object"
 },
 "name": "update_task_status"
}
