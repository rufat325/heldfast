{
 "description": "Check whether a Bitcoin payment from buy_quota has landed. Pass wait_seconds (around 120) once the user says they have paid, to wait for the credits in one call. Credit usually arrives within seconds of broadcast, well before confirmation.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "Payment address returned by buy_quota",
    "type": "string"
   },
   "poll_interval_seconds": {
    "default": 5,
    "description": "Seconds between checks (default: 5)",
    "type": "number"
   },
   "wait_seconds": {
    "default": 0,
    "description": "Seconds to keep polling for credit (default: 0, check once)",
    "type": "number"
   }
  },
  "required": [
   "address"
  ],
  "type": "object"
 },
 "name": "check_payment"
}
