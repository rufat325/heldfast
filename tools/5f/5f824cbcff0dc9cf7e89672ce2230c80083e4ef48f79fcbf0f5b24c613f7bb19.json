{
 "description": "Write a project context YAML attachment through app-owned safety gates. project_rules.yaml requires confirmation; secret/privacy risks fail closed.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "attachment": {
    "default": "snapshot",
    "enum": [
     "snapshot",
     "rules",
     "project_snapshot.yaml",
     "project_rules.yaml"
    ],
    "type": "string"
   },
   "confirmed": {
    "default": false,
    "description": "Required for project_rules.yaml rule, wording, or decision-note changes.",
    "type": "boolean"
   },
   "content": {
    "minLength": 1,
    "type": "string"
   },
   "dryRun": {
    "default": false,
    "description": "When true, previews the HTTP request without calling the app.",
    "type": "boolean"
   },
   "projectId": {
    "description": "Granoflow project id.",
    "minLength": 1,
    "type": "string"
   },
   "section": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "projectId",
   "content"
  ],
  "type": "object"
 },
 "name": "granoflow_project_context_attachment_write"
}
