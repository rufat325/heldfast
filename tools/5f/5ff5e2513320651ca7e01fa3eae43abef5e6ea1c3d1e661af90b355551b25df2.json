{
 "description": "Download an image from an http(s) URL and return it so you can SEE it. Use this when the user gives a LINK to a food photo instead of uploading the image. After viewing it, identify each dish, propose qty + unit per item, confirm portions with the user, then call log_meal (photo portions are approximate).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "url": {
    "description": "Direct http(s) URL of the food image.",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "fetch_image",
 "title": "Fetch a food image from a URL"
}
