{
 "description": "[engram-compat] Recent session context. Returns latest N episodes (any scope or filtered).",
 "inputSchema": {
  "properties": {
   "limit": {
    "default": 10,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "scope": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "mem_context"
}
