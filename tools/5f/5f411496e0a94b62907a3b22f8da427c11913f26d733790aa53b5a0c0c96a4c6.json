{
 "description": "Publish a matrix as a public read-only web page and return the tokenized link \u2014 or list/revoke existing share links.\n\nPublication is a deliberate act: anyone with the link can view the matrix (title, elements, correlations, completeness score) until the link is revoked or expires. The page is never indexed by search engines. Use it to share the X-Matrix with people OUTSIDE the organization (board members, investors, partners) without creating accounts. For sharing inside a claude.ai Team organization, prefer the matrix_artifact prompt instead.\n\nNot available in local JSON mode (sovereign mode has no cloud page by design \u2014 export a snapshot artifact instead).",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "create (default): publish and return the link. list: existing shares with status. revoke: kill a link (requires share_id).",
    "enum": [
     "create",
     "list",
     "revoke"
    ],
    "type": "string"
   },
   "expires_in_days": {
    "description": "Optional auto-expiry in days (default: no expiry, revocable manually)",
    "maximum": 365,
    "minimum": 1,
    "type": "integer"
   },
   "label": {
    "description": "Optional label to remember who this link was created for (e.g. \"board Q3\")",
    "type": "string"
   },
   "matrix_id": {
    "description": "UUID of the matrix (optional, uses the first active matrix if not provided)",
    "type": "string"
   },
   "share_id": {
    "description": "UUID of the share to revoke (required for action=revoke, from action=list)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "share_matrix"
}
