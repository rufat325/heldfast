{
 "_meta": {
  "access": "public",
  "endpointPath": "/v1/cost-estimate",
  "method": "GET",
  "paidExecutionAuthorized": false,
  "source": "cost_estimate"
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Public/free planning tool that calls GET /v1/cost-estimate for workflow-level budgeting only. It does not authorize paid execution, payment, x402, wallet use, or paid endpoint calls.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "max_budget_stc": {
    "description": "Optional local STC planning budget. Not sent to the API.",
    "minimum": 0,
    "type": "number"
   },
   "max_budget_usd": {
    "description": "Optional local USD planning budget. Not sent to the API.",
    "minimum": 0,
    "type": "number"
   },
   "quota_remaining": {
    "description": "Optional caller-supplied illustrative subscription quota remaining.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "rail_preference": {
    "description": "Optional planning rail preference. Defaults to auto.",
    "enum": [
     "subscription",
     "x402",
     "mpp",
     "auto"
    ],
    "type": "string"
   },
   "workflow_id": {
    "description": "Confirmed Stock Trends workflow id to estimate.",
    "enum": [
     "regime_analysis",
     "symbol_decision",
     "stim_forecast_review",
     "portfolio_build",
     "portfolio_compare_review"
    ],
    "type": "string"
   }
  },
  "required": [
   "workflow_id"
  ],
  "type": "object"
 },
 "name": "stocktrends_estimate_workflow_cost",
 "title": "Estimate Stock Trends Workflow Cost"
}
