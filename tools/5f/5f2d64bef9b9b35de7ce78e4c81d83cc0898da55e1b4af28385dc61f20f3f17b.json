{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the authenticated user's quote requests with current status (open / closed / cancelled / expired) and quote-response counts. Read-only \u2014 no state changes. Requires authentication.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "maximum": 1000,
    "minimum": 0,
    "type": "integer"
   },
   "sort_by": {
    "enum": [
     "createdAt",
     "expiresAt",
     "quotesReceived"
    ],
    "type": "string"
   },
   "sort_order": {
    "enum": [
     "ASC",
     "DESC"
    ],
    "type": "string"
   },
   "status": {
    "enum": [
     "open",
     "closed",
     "cancelled",
     "expired"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_my_requests",
 "title": "List my quote requests"
}
