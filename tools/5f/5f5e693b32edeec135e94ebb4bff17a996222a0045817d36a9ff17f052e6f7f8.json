{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "\"What if\" access diff: compares two versions of the same scenario (e.g. before/after removing a role assignment, or offboarding a principal) against a list of (principal, action, scope) checks, and reports who gains access and who loses it. A check that flips to/from \"indeterminate\" counts as neither a gain nor a loss \u2014 only a genuine allowed<->denied flip does.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "checks": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "action": {
       "type": "string"
      },
      "plane": {
       "enum": [
        "control",
        "data"
       ],
       "type": "string"
      },
      "principalId": {
       "type": "string"
      },
      "scopeId": {
       "type": "string"
      }
     },
     "required": [
      "principalId",
      "action",
      "scopeId"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "provider": {
    "enum": [
     "azure",
     "aws",
     "gcp",
     "k8s"
    ],
    "type": "string"
   },
   "worldAfter": {
    "$ref": "#/properties/worldBefore"
   },
   "worldBefore": {
    "additionalProperties": {},
    "description": "Scenario data. Shape depends on \"provider\" \u2014 azure: {ambitos, pertenencias?, roles?, asignaciones?, denegaciones?}; aws: {ambitos, cuentasGestion?, identidades?, recursos?, scps?, boundaries?}; gcp: {ambitos, pertenencias?, roles?, bindings?, denyPolicies?}; k8s: {ambitos, pertenencias?, roles?, bindings?}. See access.md for the exact field shapes, worked examples and the official-docs citation behind every rule.",
    "type": "object"
   }
  },
  "required": [
   "provider",
   "worldBefore",
   "worldAfter",
   "checks"
  ],
  "type": "object"
 },
 "name": "access_diff"
}
