{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Set Form Active"
 },
 "description": "Activate or deactivate a form on a live event (activating makes it the event's live form, deactivating any other). Requires event_id + form_id + active; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "active": {
    "type": "boolean"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "form_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "form_id",
   "active"
  ],
  "type": "object"
 },
 "name": "set_form_active",
 "title": "Set Form Active"
}
