{
 "description": "Update a job application status (apply, save, or dismiss).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "Status action",
    "enum": [
     "applied",
     "saved",
     "dismissed"
    ],
    "type": "string"
   },
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "id": {
    "description": "Job posting ID",
    "type": "number"
   }
  },
  "required": [
   "id",
   "action"
  ],
  "type": "object"
 },
 "name": "trackly_update_status"
}
