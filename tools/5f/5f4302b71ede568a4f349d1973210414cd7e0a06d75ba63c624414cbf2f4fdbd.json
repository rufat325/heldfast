{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Convenience search over Sag (cases/bills): finds cases whose Danish title (titel) contains a substring. Sorted by most recently updated. Use this to look up legislation/matters by keyword; for full control use query_entity.",
 "inputSchema": {
  "examples": [
   {
    "query": "klima",
    "top": 20
   },
   {
    "query": "skat",
    "skip": 0,
    "top": 10
   }
  ],
  "properties": {
   "query": {
    "description": "Substring to match in the Danish case title (titel), e.g. \"klima\", \"skat\", \"sundhed\".",
    "type": "string"
   },
   "skip": {
    "description": "Rows to skip for paging.",
    "type": "number"
   },
   "top": {
    "description": "Max rows to return (default 20).",
    "type": "number"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_cases",
 "title": "Search Cases",
 "version": "v1.0b7cdc2f"
}
