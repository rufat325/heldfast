{
 "description": "Per-URL metrics (visitors, pageviews, bounce_rate, avg_duration) with optional filtering. Use for 'top pages under /blog', 'find the homepage's stats', 'which checkout pages have the worst bounce rate'. Paginated via limit and offset.",
 "inputSchema": {
  "properties": {
   "contains": {
    "description": "Match URLs that contain this substring anywhere. Use this when the user gives you a path or a keyword without specifying the full URL (e.g. user says 'checkout pages' \u2192 contains='/checkout').",
    "type": "string"
   },
   "exact": {
    "description": "Match the URL that equals this string exactly. Like `prefix`, this is a full URL: scheme + host + path.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows. Default 50, max 500.",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Pagination offset. Default 0.",
    "maximum": 10000,
    "minimum": 0,
    "type": "integer"
   },
   "prefix": {
    "description": "Match URLs whose value starts with this string. URLs are stored as full URLs (scheme + host + path), so pass a full prefix like 'https://yoursite.com/blog/', NOT just '/blog/'. If you only have a path the user mentioned, use `contains` instead (matches the path as a substring anywhere in the URL).",
    "type": "string"
   },
   "range": {
    "description": "Date range. Accepts natural strings ('today', 'yesterday', 'last 7 days', 'last 30 days', 'this week', 'last week', 'this month', 'last month') or an explicit 'YYYY-MM-DD..YYYY-MM-DD' range. UTC. If you have separate from/to dates, use the explicit form.",
    "type": "string"
   },
   "site_id": {
    "description": "Site ID. Get it from list_sites if you don't know it.",
    "type": "string"
   }
  },
  "required": [
   "site_id",
   "range"
  ],
  "type": "object"
 },
 "name": "get_top_urls"
}
