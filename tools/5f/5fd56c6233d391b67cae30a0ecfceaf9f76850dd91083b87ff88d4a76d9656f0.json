{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get QR code scan activity"
 },
 "description": "List recent scan events for a dynamic QR code (time, location, device). Use this to answer questions like where or when a code was scanned.",
 "inputSchema": {
  "properties": {
   "end": {
    "description": "Range end, ISO date/datetime. Defaults to now when only `start` is given.",
    "type": "string"
   },
   "id": {
    "description": "QR code id.",
    "type": "string"
   },
   "page": {
    "description": "1-based page number.",
    "type": "integer"
   },
   "page_size": {
    "description": "Events per page (max 200, default 25).",
    "type": "integer"
   },
   "start": {
    "description": "Range start, ISO date or datetime (e.g. 2026-08-03 or 2026-08-03T14:00:00Z). Use start/end together for a fixed window instead of the rolling `days` look-back.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_qr_activity",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "count": {
    "description": "Total matching rows across all pages.",
    "type": "integer"
   },
   "error": {
    "additionalProperties": true,
    "description": "Present only when the tool failed (the result is also flagged isError).",
    "properties": {
     "cause": {
      "description": "Machine-readable failure category: invalid_argument, not_found, forbidden, plan_limit, rate_limited, or server_error.",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "retryable": {
      "description": "Whether repeating the same call could succeed.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "next": {
    "description": "URL of the next page, or null on the last page.",
    "type": [
     "string",
     "null"
    ]
   },
   "previous": {
    "type": [
     "string",
     "null"
    ]
   },
   "results": {
    "items": {
     "additionalProperties": true,
     "properties": {
      "device": {
       "type": [
        "string",
        "null"
       ]
      },
      "id": {
       "type": "string"
      },
      "location": {
       "type": [
        "string",
        "null"
       ]
      },
      "qr_code_id": {
       "type": [
        "string",
        "null"
       ]
      },
      "scanner_id": {
       "type": [
        "string",
        "null"
       ]
      },
      "time_timezone_aware": {
       "type": [
        "string",
        "null"
       ]
      },
      "time_utc": {
       "type": [
        "string",
        "null"
       ]
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 }
}
