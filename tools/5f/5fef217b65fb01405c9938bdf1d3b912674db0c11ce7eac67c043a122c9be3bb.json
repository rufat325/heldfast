{
 "description": "Initialize a Terraform working directory. Downloads providers and modules. Run this before plan or apply on a fresh workspace.",
 "inputSchema": {
  "properties": {
   "reconfigure": {
    "description": "Pass -reconfigure to reinitialize backend even if already configured",
    "type": "boolean"
   },
   "upgrade": {
    "description": "Pass -upgrade to update providers/modules to latest allowed versions",
    "type": "boolean"
   },
   "workdir": {
    "description": "Absolute or relative path to the directory containing .tf files",
    "type": "string"
   }
  },
  "required": [
   "workdir"
  ],
  "type": "object"
 },
 "name": "tf_init"
}
