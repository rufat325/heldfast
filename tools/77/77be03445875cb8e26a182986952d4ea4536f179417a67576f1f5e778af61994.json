{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List flows"
 },
 "description": "List flows. Automation flows with lifetime stats. The canvas definition is not exposed.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 25,
    "description": "Page size, 1 to 100. Default 25.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "starting_after": {
    "description": "The next_cursor from the previous page.",
    "type": "string"
   },
   "status": {
    "description": "Flow status.",
    "enum": [
     "draft",
     "active",
     "paused"
    ],
    "type": "string"
   },
   "trigger_type": {
    "description": "What starts the flow.",
    "enum": [
     "keyword",
     "first_message",
     "comment_trigger",
     "button_tap"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_flows"
}
