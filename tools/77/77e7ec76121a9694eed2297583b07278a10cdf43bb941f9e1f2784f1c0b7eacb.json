{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "MHRA documents added or revised since a date, newest first \u2014 the weekly-refresh feed. Pass an ISO date such as \"2026-09-01\". Returns the same rows as mhra_search plus the cutoff that was applied, so replaying the same `since` gives the same answer. NOTE what the register can and cannot tell you: a new or revised DOCUMENT is what is recorded here. A withdrawal is not published as an event, so this cannot report withdrawals \u2014 treat it as \"new and changed\", not \"new, changed and withdrawn\".",
 "inputSchema": {
  "examples": [
   {
    "since": "2026-09-01"
   },
   {
    "doc_type": "Spc",
    "limit": 10,
    "since": "2026-09-01"
   }
  ],
  "properties": {
   "doc_type": {
    "description": "Restrict to \"Spc\", \"Pil\" or \"Par\".",
    "type": "string"
   },
   "limit": {
    "description": "Maximum documents to return, 1-200 (default 50).",
    "type": "number"
   },
   "since": {
    "description": "ISO date or datetime, e.g. \"2026-09-01\" or \"2026-09-01T00:00:00Z\".",
    "type": "string"
   }
  },
  "required": [
   "since"
  ],
  "type": "object"
 },
 "name": "mhra_recent_changes",
 "title": "Mhra Recent Changes",
 "version": "v1.039599e6"
}
