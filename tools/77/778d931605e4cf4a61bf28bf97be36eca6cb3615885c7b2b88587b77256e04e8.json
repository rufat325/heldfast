{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Pull data for a cube as JSON-stat. Provide one selection per dimension, in the dimension order returned by dataset_dimensions. Each selection is either a single value code, a comma-separated list of codes (e.g. \"SEX_M,SEX_F\"), or the literal \"all\" for every value of that dimension. The number of selections must equal the cube's dimension count. Output is JSON-stat: `value` is the flat data array, `dimension`/`size`/`id` describe its shape.",
 "inputSchema": {
  "examples": [
   {
    "cube": "as1001rs",
    "selections": [
     "2024",
     "UKAZ01",
     "all"
    ]
   },
   {
    "cube": "as1001rs",
    "selections": [
     "2023,2024",
     "UKAZ01,UKAZ02",
     "SEX_M"
    ]
   }
  ],
  "properties": {
   "cube": {
    "description": "Cube code, e.g. \"as1001rs\" (from list_datasets).",
    "type": "string"
   },
   "lang": {
    "description": "Label language (default \"en\").",
    "enum": [
     "en",
     "sk"
    ],
    "type": "string"
   },
   "selections": {
    "description": "One entry per dimension, in dataset_dimensions order. Each is a value code, a comma-separated list of codes, or \"all\". Example for as1001rs: [\"2024\", \"UKAZ01\", \"all\"].",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "cube",
   "selections"
  ],
  "type": "object"
 },
 "name": "get_data",
 "title": "Get Data",
 "version": "v1.f718b662"
}
