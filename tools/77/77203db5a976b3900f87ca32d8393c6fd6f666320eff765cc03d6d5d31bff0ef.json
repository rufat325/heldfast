{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Respond to an open request using one of the authenticated seller's active listings. The listing price must fit the request budget.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "listing_id": {
    "type": "string"
   },
   "message": {
    "type": "string"
   },
   "request_id": {
    "type": "string"
   }
  },
  "required": [
   "request_id",
   "listing_id"
  ],
  "type": "object"
 },
 "name": "propose_for_request",
 "title": "Propose for paid work"
}
