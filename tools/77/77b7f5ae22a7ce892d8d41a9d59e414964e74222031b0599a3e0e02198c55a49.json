{
 "description": "Save research findings to sources/research/<slug>.md. Appends if file exists; otherwise creates. Always appends a log entry.",
 "inputSchema": {
  "properties": {
   "body": {
    "description": "Markdown body",
    "type": "string"
   },
   "projectId": {
    "description": "Project id; defaults to the current project (config.json)",
    "type": "string"
   },
   "sources": {
    "description": "URLs or citations",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "topic": {
    "description": "Human-readable topic name; slugified for filename",
    "type": "string"
   }
  },
  "required": [
   "topic",
   "body"
  ],
  "type": "object"
 },
 "name": "mindbase_research_save"
}
