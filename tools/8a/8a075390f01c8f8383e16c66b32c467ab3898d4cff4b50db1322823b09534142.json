{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false,
  "title": "Move threads in bulk"
 },
 "description": "Move multiple email threads at once to a different column or board. Use this for batch triage operations.",
 "inputSchema": {
  "properties": {
   "newBoardId": {
    "description": "Target board ID",
    "type": "number"
   },
   "newColumnId": {
    "description": "Target column ID",
    "type": "string"
   },
   "threadIds": {
    "description": "Array of thread IDs to move",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "threadIds",
   "newBoardId",
   "newColumnId"
  ],
  "type": "object"
 },
 "name": "move_threads_bulk",
 "title": "Move threads in bulk"
}
