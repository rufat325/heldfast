{
 "description": "Rule-based project briefing from README, CLAUDE.md, package.json, and indexed stats (no LLM). Results are cached in SQLite until the index fingerprint changes.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "project_name": {
    "description": "Project name",
    "type": "string"
   },
   "regenerate": {
    "description": "If true, rebuild briefing and refresh cache",
    "type": "boolean"
   }
  },
  "required": [
   "project_name"
  ],
  "type": "object"
 },
 "name": "project_briefing"
}
