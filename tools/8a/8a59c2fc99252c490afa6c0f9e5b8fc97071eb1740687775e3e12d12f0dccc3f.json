{
 "description": "Cancel/stop a Code Engine job run. This deletes the job run resource, terminating any still-running instances.",
 "inputSchema": {
  "properties": {
   "job_run_name": {
    "description": "Name of the job run to cancel",
    "type": "string"
   },
   "project_id": {
    "description": "Project ID or name",
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "job_run_name"
  ],
  "type": "object"
 },
 "name": "ce_cancel_job_run"
}
