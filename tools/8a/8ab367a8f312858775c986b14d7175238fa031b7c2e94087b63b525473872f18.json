{
 "description": "Edit an existing pending proposal and approve it in one step. Provide exactly one edit field matching the proposal type: new_threshold for threshold_adjust, new_ttl_seconds for tool_ttl_adjust. Invalidate proposals are not editable.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "actor": {
    "description": "Optional actor identity stamped into the audit trail",
    "minLength": 1,
    "type": "string"
   },
   "new_threshold": {
    "description": "For threshold_adjust proposals",
    "maximum": 2,
    "minimum": 0,
    "type": "number"
   },
   "new_ttl_seconds": {
    "description": "For tool_ttl_adjust proposals",
    "maximum": 86400,
    "minimum": 10,
    "type": "integer"
   },
   "proposal_id": {
    "description": "Proposal id",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "proposal_id"
  ],
  "type": "object"
 },
 "name": "cache_edit_and_approve_proposal"
}
