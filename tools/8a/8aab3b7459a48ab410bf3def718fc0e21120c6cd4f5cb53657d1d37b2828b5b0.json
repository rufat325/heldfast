{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "DAY Pass Activate"
 },
 "contract": {
  "auth_scope": "public",
  "idempotent": false,
  "output_schema": {
   "access_key": "string",
   "activated": "boolean",
   "already_active": "boolean?",
   "expires_at": "string",
   "how_to_use": "object",
   "x711_credits": "number"
  },
  "price": {
   "free_tier": "none",
   "paid": "$1.49 USDC on Base via x402: one 24-hour Day Pass, then send X-Zambo-Key",
   "per_24h_pass": "1.49 USD"
  },
  "side_effects": true
 },
 "description": "Agent-native access activation endpoint. Accepts an optional payment envelope or existing access key and returns a payment challenge or verified activation result. Activation results include receipt URL, run ID, access key, expiration, usage guidance, and a spend receipt. Activation occurs only after a verified transfer.",
 "destructiveHint": true,
 "inputSchema": {
  "examples": [
   {
    "payment_method": "x402"
   }
  ],
  "properties": {
   "x_payment": {
    "description": "Base64 JSON x402 payment envelope signed from the live challenge, sent as X-Payment.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "day_pass_activate",
 "openWorldHint": true,
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "access_key": {
    "type": "string"
   },
   "activated": {
    "type": "boolean"
   },
   "already_active": {
    "type": "boolean"
   },
   "expires_at": {
    "type": "string"
   },
   "how_to_use": {
    "additionalProperties": true,
    "type": "object"
   },
   "x711_credits": {
    "type": "number"
   }
  },
  "type": "object"
 },
 "readOnlyHint": false,
 "title": "DAY Pass Activate"
}
