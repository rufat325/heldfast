{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-exchange-mailbox-folder"
 },
 "description": "Read the properties and relationships of a mailboxFolder object.\n\nTIP: Gets a specific mailbox folder by ID (admin). Returns displayName, totalItemCount, unreadItemCount, childFolderCount.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "mailboxFolderId": {
    "description": "Path parameter: mailboxFolderId",
    "type": "string"
   },
   "mailboxId": {
    "description": "Path parameter: mailboxId",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "required": [
   "mailboxId",
   "mailboxFolderId"
  ],
  "type": "object"
 },
 "name": "get-exchange-mailbox-folder"
}
