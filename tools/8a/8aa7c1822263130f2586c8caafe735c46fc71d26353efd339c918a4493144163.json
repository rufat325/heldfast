{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Get props, state, and hooks for the first exact component-name match, with traversal completeness metadata.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "maxDepth": {
    "default": 200,
    "description": "Maximum fiber depth to traverse (default 200, maximum 600)",
    "maximum": 600,
    "minimum": 0,
    "type": "integer"
   },
   "maxNodes": {
    "default": 1200,
    "description": "Maximum fibers to scan (default 1200)",
    "maximum": 5000,
    "minimum": 1,
    "type": "integer"
   },
   "name": {
    "description": "Exact component name to inspect",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "inspect_component"
}
