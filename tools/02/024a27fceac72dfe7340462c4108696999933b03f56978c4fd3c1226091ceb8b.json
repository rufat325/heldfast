{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List captured network requests (the full ring \u2014 every request, independent of DEVLOOP_NET_THRESHOLD). Each row has method/url/status/timing/headers (bodies for the curated subset). Unlike get_logs (curated timeline only); for an importable HAR file use export_har.",
 "inputSchema": {
  "properties": {
   "app": {
    "description": "Scope to one app/project (pane label or id).",
    "type": "string"
   },
   "grep": {
    "description": "Substring match on the request line (status/method/url).",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (most recent), default 200.",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "get_network"
}
