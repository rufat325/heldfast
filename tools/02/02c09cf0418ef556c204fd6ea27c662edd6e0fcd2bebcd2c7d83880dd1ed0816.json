{
 "annotations": {
  "readOnlyHint": true,
  "title": "List WhatsApp templates"
 },
 "description": "List the pre-approved WhatsApp message templates available on a WhatsApp board. Returns each template's name, language, status, category, body text, and how many {{n}} variables it expects. Only APPROVED templates can be sent.",
 "inputSchema": {
  "properties": {
   "boardId": {
    "description": "The WhatsApp board ID",
    "type": "number"
   },
   "limit": {
    "description": "Maximum templates to return (default: 50)",
    "type": "number"
   }
  },
  "required": [
   "boardId"
  ],
  "type": "object"
 },
 "name": "list_whatsapp_templates",
 "title": "List WhatsApp templates"
}
