{
 "_meta": {
  "anthropic/alwaysLoad": true
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Full-text search across all indexed files. Supports regex, glob file patterns, language filter. Use for finding strings, comments, TODOs, config values, error messages \u2014 anything not captured as a symbol. Skips .env files: secret values are never searched or returned, use get_env_vars for env keys. For symbol search (functions, classes) use search instead. Read-only. Returns JSON: { files: [{ file, language, hits: [{ line, column, match, context }] }], total_matches } \u2014 hits grouped per file, so a long path is paid once. Pass `grouping: \"flat\"` for the ungrouped matches[] shape.",
 "inputSchema": {
  "properties": {
   "case_sensitive": {
    "description": "Case-sensitive search (default false)",
    "type": "boolean"
   },
   "context_lines": {
    "description": "Lines of context before/after each match (default 0 \u2014 set higher if you need surrounding code)",
    "maximum": 10,
    "minimum": 0,
    "type": "integer"
   },
   "file_pattern": {
    "description": "Glob filter, e.g. \"src/**/*.ts\"",
    "maxLength": 512,
    "type": "string"
   },
   "grouping": {
    "default": "by_file",
    "description": "Payload shape. \"by_file\" (default) groups hits per file, so a long path is paid once; \"flat\" is a single matches[] array.",
    "enum": [
     "flat",
     "by_file"
    ],
    "type": "string"
   },
   "is_regex": {
    "description": "Treat query as regex (default false)",
    "type": "boolean"
   },
   "language": {
    "description": "Filter by language (e.g. \"typescript\", \"python\")",
    "maxLength": 64,
    "type": "string"
   },
   "max_results": {
    "description": "Max matches to return (default 50)",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Search string or regex pattern",
    "maxLength": 1000,
    "minLength": 1,
    "type": "string"
   },
   "timeout_ms": {
    "description": "Wall-clock budget in ms \u2014 caps a catastrophic-backtracking regex. Default 2000; 0 disables.",
    "maximum": 30000,
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_text"
}
