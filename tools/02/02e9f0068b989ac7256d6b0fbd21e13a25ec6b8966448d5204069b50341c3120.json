{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get collection articles"
 },
 "description": "Get recent articles from a saved collection. Runs the collection across its sources (all current collections are multi-source) and does not modify it. Costs credits per source.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "collection_id": {
    "description": "Collection id (from list_collections)",
    "minLength": 1,
    "type": "string"
   },
   "limit": {
    "default": 25,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "collection_id"
  ],
  "type": "object"
 },
 "name": "get_collection_articles",
 "title": "Get collection articles"
}
