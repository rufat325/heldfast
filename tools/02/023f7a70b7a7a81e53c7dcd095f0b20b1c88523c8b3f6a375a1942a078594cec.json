{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-threat-intel-whois-record"
 },
 "description": "Get the specified whoisRecord resource.  Specify the desired whoisRecord in one of the following two ways:\n- Identify a host and get its current whoisRecord. \n- Specify an id value to get the corresponding whoisRecord.\n\nTIP: Gets a specific WHOIS record by ID. Returns full registrant and registrar details, nameservers, registration and expiration dates.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "whoisRecordId": {
    "description": "Path parameter: whoisRecordId",
    "type": "string"
   }
  },
  "required": [
   "whoisRecordId"
  ],
  "type": "object"
 },
 "name": "get-threat-intel-whois-record"
}
