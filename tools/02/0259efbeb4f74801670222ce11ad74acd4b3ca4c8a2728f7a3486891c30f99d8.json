{
 "description": "Set a space to use the instance default color scheme (\"global\") or its own (\"custom\").",
 "inputSchema": {
  "properties": {
   "spaceKey": {
    "type": "string"
   },
   "type": {
    "type": "string"
   }
  },
  "required": [
   "spaceKey",
   "type"
  ],
  "type": "object"
 },
 "name": "update_space_color_scheme_type"
}
