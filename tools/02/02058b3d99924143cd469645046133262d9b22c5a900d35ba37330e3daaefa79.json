{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "View pull requests. Methods: list, get_story_prs",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max results (for: list)",
    "type": "number"
   },
   "method": {
    "description": "Operation to perform",
    "enum": [
     "list",
     "get_story_prs"
    ],
    "type": "string"
   },
   "project_id": {
    "description": "Project ID (for: list)",
    "type": "string"
   },
   "status": {
    "description": "PR state filter (for: list)",
    "enum": [
     "open",
     "merged",
     "closed"
    ],
    "type": "string"
   },
   "story_id": {
    "description": "Story ID or ticket ID like VP-42 (required for: get_story_prs)",
    "type": "string"
   }
  },
  "required": [
   "method"
  ],
  "type": "object"
 },
 "name": "pull_requests"
}
