{
 "annotations": {
  "readOnlyHint": true,
  "title": "list_rations"
 },
 "description": "List feed rations on the farm: named recipes in per-head-per-day amounts, with their ingredients and group assignments.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "include_inactive": {
    "description": "Include retired rations.",
    "type": "boolean"
   },
   "skip": {
    "description": "Records to skip (pagination).",
    "minimum": 0,
    "type": "integer"
   },
   "take": {
    "description": "Max records to return.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_rations"
}
