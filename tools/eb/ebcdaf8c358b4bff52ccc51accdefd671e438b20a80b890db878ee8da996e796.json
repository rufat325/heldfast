{
 "_meta": {
  "xdata.kr/credential": {
   "providers": [
    "hometax"
   ],
   "register_url": "https://console.xdata.kr/credentials?link=1&priority=hometax",
   "required": true,
   "windows_pc_needed": true
  }
 },
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "REQUIRES prior hometax__session__login call with same account_link_id. Download an issued certificate as PDF (returned base64-encoded). Get cva_id/rcat_no from the certificate application result (hometax__certificate__apply_auto or apply_with_template).",
 "inputSchema": {
  "properties": {
   "account_link_id": {
    "description": "Account link ID (UUID v4). Optional \u2014 the server auto-resolves from your API key when omitted. Provide explicitly only when you have multiple certificates for the same provider. Each institution has a separate ID (Hometax != NHIS != NPS != FourInsure).",
    "type": "string"
   },
   "confirm": {
    "description": "Set true ONLY after showing the preview to the user and receiving their explicit approval. Omit it on the first call to obtain the preview. Printing consumes one issued copy and cannot be undone.",
    "type": "boolean"
   },
   "cva_id": {
    "description": "Certificate ID (\uc99d\uba85\uc11c ID, cvaId)",
    "type": "string"
   },
   "rcat_no": {
    "description": "Receipt number (\uc811\uc218\ubc88\ud638, rcatNo)",
    "type": "string"
   },
   "rpt_pkg_pth": {
    "description": "Report package path (rptPkgPth). Optional \u2014 looked up from the certificate list when omitted.",
    "type": "string"
   }
  },
  "required": [
   "cva_id",
   "rcat_no"
  ],
  "type": "object"
 },
 "name": "hometax__certificate__download",
 "securitySchemes": [
  {
   "scopes": [
    "mcp:tools:list",
    "mcp:tools:call"
   ],
   "type": "oauth2"
  }
 ],
 "title": "\uc99d\uba85\uc11c \ub2e4\uc6b4\ub85c\ub4dc"
}
