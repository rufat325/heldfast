{
 "description": "Power tool: search Polymarket markets then auto-enrich each result with live CLOB prices and on-chain resolution status. Combines Gamma API + CLOB API + The Graph in one call \u2014 no need to chain tools manually.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "active": {
    "description": "Filter: only active markets",
    "type": "boolean"
   },
   "closed": {
    "description": "Filter: only closed/resolved markets",
    "type": "boolean"
   },
   "limit": {
    "default": 5,
    "description": "Number of results (1-20, kept small for enrichment speed)",
    "maximum": 20,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "description": "Search text (e.g. 'Trump', 'Bitcoin', 'World Cup')",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_markets_enriched"
}
