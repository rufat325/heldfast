{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Look up the member values (e.g. specific states, counties, industries, or years) available for a dimension level within a Data USA cube, optionally filtered by name \u2014 use this to find the exact key to pass into datausa.data.query's include filter",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cube": {
    "description": "Data cube (dataset) name, e.g. \"acs_yg_total_population_1\".",
    "minLength": 1,
    "type": "string"
   },
   "level": {
    "description": "Dimension level name to list members for, e.g. \"State\" or \"County\".",
    "minLength": 1,
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of members to return.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "search": {
    "description": "Filter members by name, case-insensitive substring match (e.g. \"cali\").",
    "type": "string"
   }
  },
  "required": [
   "cube",
   "level"
  ],
  "type": "object"
 },
 "name": "datausa.reference.members",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Look Up Data USA Dimension Members"
}
