{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "ADQL TAP query against the SIMBAD database (sync). Returns JSON by default.",
 "inputSchema": {
  "examples": [
   {
    "query": "SELECT TOP 5 main_id FROM basic WHERE otype = 'G'"
   },
   {
    "format": "csv",
    "query": "SELECT main_id, ra, dec FROM basic WHERE otype = 'SN'"
   }
  ],
  "properties": {
   "format": {
    "description": "json (default) | votable | csv | tsv",
    "type": "string"
   },
   "query": {
    "description": "ADQL, e.g. \"SELECT TOP 5 main_id FROM basic WHERE otype = 'G'\"",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "tap",
 "outputSchema": {
  "oneOf": [
   {
    "description": "Parsed JSON response from TAP query",
    "type": "object"
   },
   {
    "properties": {
     "body": {
      "description": "Raw text response body when JSON parsing fails",
      "type": "string"
     },
     "format": {
      "description": "Response format (json, votable, csv, tsv)",
      "type": "string"
     }
    },
    "required": [
     "format",
     "body"
    ],
    "type": "object"
   }
  ]
 },
 "title": "Tap",
 "version": "v1.1e89469c"
}
