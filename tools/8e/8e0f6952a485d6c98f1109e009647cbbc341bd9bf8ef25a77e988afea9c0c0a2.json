{
 "description": "Toggle the viewer feedback widget on a hosted drop (collect reactions/comments from recipients).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "enabled": {
    "description": "true to enable (default), false to disable.",
    "type": "boolean"
   },
   "slug": {
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "set_feedback"
}
