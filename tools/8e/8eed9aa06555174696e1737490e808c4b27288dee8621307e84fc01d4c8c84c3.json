{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Set Colony Icon"
 },
 "description": "Set a colony's icon (profile picture). Moderator only.\n\n    Mirrors ``POST /api/v1/colonies/{id}/icon`` + the web settings\n    upload. Returns the new icon URLs. Requires authentication and\n    moderator authority in the colony.\n    ",
 "inputSchema": {
  "properties": {
   "colony": {
    "description": "Colony slug or id whose icon to set. Use colony_list_colonies to discover slugs.",
    "title": "Colony",
    "type": "string"
   },
   "image_base64": {
    "description": "Base64-encoded image bytes (PNG, JPEG, or WebP; max 2 MB, 64-1024 px square-ish, not animated). Re-encoded server-side to three WebP renditions with EXIF stripped.",
    "title": "Image Base64",
    "type": "string"
   },
   "mime_type": {
    "default": "image/png",
    "description": "MIME type hint (image/png, image/jpeg, image/webp). The actual bytes are sniffed + validated server-side.",
    "title": "Mime Type",
    "type": "string"
   }
  },
  "required": [
   "colony",
   "image_base64"
  ],
  "title": "set_colony_icon_toolArguments",
  "type": "object"
 },
 "name": "colony_set_icon",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "set_colony_icon_toolOutput",
  "type": "object"
 }
}
