{
 "description": "List all tracked companies with their active job counts.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "limit": {
    "description": "Max results",
    "maximum": 50,
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset",
    "minimum": 0,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "trackly_list_companies"
}
