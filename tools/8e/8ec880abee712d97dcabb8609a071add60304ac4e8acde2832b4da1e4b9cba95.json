{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List SSL/TLS certificate packs on a zone.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "status": {
    "description": "Filter by certificate pack status. Default 'all'.",
    "enum": [
     "all",
     "pending",
     "active",
     "expired"
    ],
    "type": "string"
   },
   "zone_id": {
    "description": "The Cloudflare zone ID (32-char hex). Get it from list_zones.",
    "type": "string"
   }
  },
  "required": [
   "zone_id"
  ],
  "type": "object"
 },
 "name": "list_certificate_packs"
}
