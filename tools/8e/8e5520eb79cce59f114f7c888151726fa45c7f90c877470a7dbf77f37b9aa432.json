{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the finishing results for a specific Formula 1 race: final classified positions, driver name, constructor, grid position, laps completed, finish status (Finished/DNF/DSQ/+N Laps), points scored, finish time, and fastest lap time and rank. Defaults to the most recently completed race (round=last) of the current season. Specify season + round to fetch any race back to 1950. Data source: Jolpica F1 (Ergast-compatible API) \u2014 free, unlimited, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Maximum number of results to return (1\u2013100, default 30).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Pagination offset \u2014 number of results to skip (default 0).",
    "minimum": 0,
    "type": "integer"
   },
   "round": {
    "description": "Race round number within the season (1-based), or 'last' for the most recent completed race. Omit for last race.",
    "type": [
     "string",
     "number"
    ]
   },
   "season": {
    "description": "Season year (e.g. 2026, 2024, 2023). Omit for the current season.",
    "type": [
     "string",
     "number"
    ]
   }
  },
  "type": "object"
 },
 "name": "f1.races.results",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "F1 Race Results"
}
