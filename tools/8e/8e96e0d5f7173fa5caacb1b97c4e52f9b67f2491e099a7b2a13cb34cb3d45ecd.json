{
 "description": "Reveal or rotate your provider's shared key \u2014 the gateway\u2194endpoint credential the gateway presents when it calls your endpoint (NOT an ownership token; ownership is your wallet). The gateway stores it write-only, so a wallet signature is the only way to read it back. Pass rotate=true to issue a new one. Requires an unlocked wallet.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "allowUnsafeGateway": {
    "description": "Allow a gateway host outside localhost / *.aibtc.com. Required to send wallet-signed headers (and any apiKey) to an arbitrary host \u2014 leave unset unless you deliberately self-host the gateway.",
    "type": "boolean"
   },
   "gateway": {
    "description": "Marketplace gateway base URL. Defaults to https://inference.aibtc.com. Use http://localhost:8787 for local dev.",
    "format": "uri",
    "type": "string"
   },
   "providerId": {
    "description": "The provider id (from inference_list_providers).",
    "type": "string"
   },
   "rotate": {
    "description": "true = generate and return a NEW key (invalidates the old one). false/omitted = reveal the current key.",
    "type": "boolean"
   }
  },
  "required": [
   "providerId"
  ],
  "type": "object"
 },
 "name": "inference_reveal_key"
}
