{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the keys held in a namespace with their metadata: alg, size, timestamps. Never returns ciphertext \u2014 use vault_retrieve for that. Each item's updated_at is the version to pass back as if_match on a conditional store. Use this when you have stored secrets and need to know what is there. Costs $0.005 in USDC on Base, paid via the x402 protocol, or from a credit token \u2014 call credits_trial for free credit if you have neither.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "credit_token": {
    "description": "Optional. A credit token from credits_trial or /credits/buy. Supplying it pays for this call from that balance, so no x402 payment or wallet is needed.",
    "type": "string"
   },
   "namespace": {
    "description": "Isolation scope",
    "type": "string"
   },
   "namespace_token": {
    "description": "One-time token issued by the first call that claimed this namespace. Required for every later call.",
    "type": "string"
   }
  },
  "required": [
   "namespace",
   "namespace_token"
  ],
  "type": "object"
 },
 "name": "vault_list",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of entries",
    "type": "number"
   },
   "items": {
    "description": "Metadata only \u2014 ciphertext is never included here",
    "items": {
     "properties": {
      "alg": {
       "description": "Algorithm label",
       "type": "string"
      },
      "created_at": {
       "description": "ISO-8601 creation time",
       "type": "string"
      },
      "expires_at": {
       "description": "Expiry, or null",
       "type": [
        "string",
        "null"
       ]
      },
      "key": {
       "description": "Entry key",
       "type": "string"
      },
      "size_bytes": {
       "description": "Stored size",
       "type": "number"
      },
      "updated_at": {
       "description": "ISO-8601 last write",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "status": {
    "description": "'listed'",
    "type": "string"
   }
  },
  "required": [
   "status",
   "count",
   "items"
  ],
  "type": "object"
 },
 "title": "List the keys in a vault namespace"
}
