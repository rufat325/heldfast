{
 "description": "[GA4 WRITE] Acknowledge that the property owner has the required user privacy disclosures/rights. Some operations (e.g. creating certain links or MP secrets) require this once. Requires GA4_MCP_ENABLE_WRITES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "confirm": {
    "type": "boolean"
   },
   "property": {
    "description": "GA4 property ID, e.g. \"123456789\" or \"properties/123456789\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "property",
   "confirm"
  ],
  "type": "object"
 },
 "name": "ga4_acknowledge_user_data_collection"
}
