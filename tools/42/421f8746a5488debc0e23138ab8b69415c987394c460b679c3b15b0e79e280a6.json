{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Read a Belgian company's financials from a connected register. With none connected it returns 503 and charges nothing, never an estimate. 3 credits (EUR 0.03) per successful call; needs a bearer token.",
 "inputSchema": {
  "properties": {
   "vatNumber": {
    "description": "Belgian VAT or KBO/BCE enterprise number, e.g. BE0403170701 or 0403.170.701.",
    "maxLength": 32,
    "type": "string"
   }
  },
  "required": [
   "vatNumber"
  ],
  "type": "object"
 },
 "name": "analyze_kbo_financials",
 "outputSchema": {
  "properties": {
   "billing": {
    "type": "object"
   },
   "data": {
    "type": "object"
   },
   "kind": {
    "type": "string"
   }
  },
  "required": [
   "kind",
   "data",
   "billing"
  ],
  "type": "object"
 }
}
