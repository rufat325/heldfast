{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Get the JSON schema of a credential type \u2014 which fields it needs (e.g. 'slackApi' \u2192 accessToken). Use before create_credential.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "credentialTypeName": {
    "description": "Credential type name, e.g. 'githubApi'",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "credentialTypeName"
  ],
  "type": "object"
 },
 "name": "get_credential_schema"
}
