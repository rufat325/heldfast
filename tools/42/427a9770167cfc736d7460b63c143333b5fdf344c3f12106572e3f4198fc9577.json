{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Converts an amount between currencies using a live FX rate: TrueFX for its 10 quoted majors (genuinely live tick data), frankfurter.app daily ECB reference rate as the fallback for every other currency pair (30 total). Use when user asks \"what's $X worth in EUR?\" or any currency conversion where the rate itself isn't already known. Returns: converted, rate, source (truefx/frankfurter/identity), asOf, available (false + error if no rate could be found for that pair).",
 "inputSchema": {
  "properties": {
   "amount": {
    "description": "Amount in the source currency",
    "type": "number"
   },
   "from": {
    "description": "3-letter source currency code, e.g. \"USD\"",
    "type": "string"
   },
   "to": {
    "description": "3-letter target currency code, e.g. \"EUR\"",
    "type": "string"
   }
  },
  "required": [
   "amount",
   "from",
   "to"
  ],
  "type": "object"
 },
 "name": "workflow.run_forex_currency_converter_live"
}
