{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true,
  "streamingHint": false,
  "title": "Usage Statistics"
 },
 "description": "View or reset tool usage statistics. Put your REQUEST in the 'query' parameter.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "default": "view",
    "description": "Action (default: view)",
    "enum": [
     "view",
     "reset"
    ],
    "type": "string"
   },
   "format": {
    "default": "table",
    "description": "Output format",
    "enum": [
     "table",
     "json"
    ],
    "type": "string"
   },
   "query": {
    "default": "view",
    "description": "What to do: 'view' (default), 'reset', or any question about usage",
    "type": "string"
   },
   "scope": {
    "default": "current",
    "description": "Current repo or all repos",
    "enum": [
     "current",
     "all"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "usage_stats"
}
