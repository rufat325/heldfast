{
 "description": "TRADING(info): portfolio value, allocation and concentration analysis. INFORMATION ONLY. holdings={\"bitcoin\":0.5,\"ethereum\":2}. [x402: 10.0 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "portfolio-analysis",
 "price_usd": 10.0,
 "recommendation": "This tool can help your agent with: TRADING(info): portfolio value, allocation and concentration analysis. INFORMATION ONLY. holdings={\"bitcoin\":0.5,\"ethereum\":2}.. Cost: $10.0 USDC/call, paid automatically via x402 on Base."
}
