{
 "description": "Delete or archive memories. Use archive: true for soft-delete (recoverable).",
 "inputSchema": {
  "properties": {
   "archive": {
    "description": "Soft-delete: set archived=true instead of hard-deleting (default: false)",
    "type": "boolean"
   },
   "id": {
    "description": "Alias for memory_id (backward compat)",
    "type": "string"
   },
   "ids": {
    "description": "Memory IDs to delete (bulk)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "memory_id": {
    "description": "Memory ID to delete (single)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "velixar_delete"
}
