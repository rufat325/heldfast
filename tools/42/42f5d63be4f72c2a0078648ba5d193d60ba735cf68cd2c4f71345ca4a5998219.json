{
 "description": "Retire a legacy fixed Apply batch after the user explicitly chooses to start fresh. This preserves submitted work, Check Later jobs, and browser tabs; it never submits an application.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "batchId": {
    "minimum": 1,
    "type": "integer"
   },
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "expectedRevision": {
    "minimum": 1,
    "type": "integer"
   },
   "idempotencyKey": {
    "maxLength": 200,
    "minLength": 16,
    "pattern": "^[\\x20-\\x7e]+$",
    "type": "string"
   },
   "reasonCode": {
    "enum": [
     "user_requested_restart"
    ],
    "type": "string"
   }
  },
  "required": [
   "batchId",
   "expectedRevision",
   "reasonCode",
   "idempotencyKey"
  ],
  "type": "object"
 },
 "name": "trackly_cancel_apply_batch"
}
