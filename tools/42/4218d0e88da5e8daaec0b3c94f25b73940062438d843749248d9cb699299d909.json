{
 "description": "A picture of the running game: the whole screen or one viewport, as an image. Needs the game running with a window. Operations: screenshot: the screen; viewport: one viewport's texture. Default screenshot.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "height": {
    "description": "Scale the image to this height. Given alone, the width keeps the picture's proportions.",
    "minimum": 1,
    "type": "integer"
   },
   "op": {
    "description": "What to do.",
    "enum": [
     "screenshot",
     "viewport"
    ],
    "type": "string"
   },
   "outputPath": {
    "description": "Also save the picture as a PNG at this absolute path, for somebody to open later; the answer says where. Refused unless it ends in .png, its directory exists, it is outside the project, and no file is there already.",
    "type": "string"
   },
   "pid": {
    "description": "Which game, when several are running from one project, such as a bench and its workers: its process id, as editor_status lists under runtimes. Without it the game this server started or plays is the one asked when it is among them, and the answer says so under answeredBy; with one game per project it is not needed at all.",
    "type": "number"
   },
   "projectPath": {
    "description": "Which game, when more than one is running: the project directory it was started from. The pick without it, in order: the game this server started or is playing when it is among those running, named under answeredBy where there was a choice; else the only game there is; else a refusal naming every game and which argument tells them apart, pid when they are all one project and projectPath when they are not. Not needed with one game, nor for a call to this server's own.",
    "type": "string"
   },
   "viewportPath": {
    "description": "viewport: the Viewport node. Default the root viewport.",
    "type": "string"
   },
   "width": {
    "description": "Scale the image to this width. Given alone, the height keeps the picture's proportions.",
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "runtime_capture"
}
