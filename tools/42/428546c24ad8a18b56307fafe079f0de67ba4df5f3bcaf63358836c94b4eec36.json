{
 "description": "Recent fills routed by one Polymarket CLOB V2 builder code, with that builder's aggregate totals. Use get_builder_leaderboard first to find a builder id. V2-era only.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "builderId": {
    "description": "The builder code (bytes32 hex, as returned by get_builder_leaderboard)",
    "type": "string"
   },
   "limit": {
    "description": "How many recent fills (default 20)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "builderId"
  ],
  "type": "object"
 },
 "name": "get_builder_activity"
}
