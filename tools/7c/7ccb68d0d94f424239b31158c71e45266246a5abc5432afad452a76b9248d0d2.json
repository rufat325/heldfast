{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List a participant's change events\n\nThe participant's typed change events, newest-first, cursor-paginated.",
 "inputSchema": {
  "properties": {
   "cursor": {
    "description": "Opaque pagination cursor returned as `next_cursor` by the previous page.",
    "type": "string"
   },
   "id": {
    "description": "Canonical `scheme::value` Peppol identifier.",
    "type": "string"
   },
   "limit": {
    "default": 50,
    "description": "Page size, clamped to [1, 200]. Defaults to 50.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "list_participant_events"
}
