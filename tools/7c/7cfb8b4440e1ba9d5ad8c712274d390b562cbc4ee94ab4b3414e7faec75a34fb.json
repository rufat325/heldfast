{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true
 },
 "description": "Start one containerized instance of an application version, placed near the players you specify. Returns immediately with a request_id; the server is still starting and has no connection details yet. Follow this call with edgegap_wait_for_deployment to get the address players connect to. Always stop deployments you started for testing.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "application": {
    "description": "Application name.",
    "type": "string"
   },
   "cpu_units": {
    "description": "Override the version CPU.",
    "maximum": 9007199254740991,
    "minimum": 256,
    "type": "integer"
   },
   "env": {
    "description": "Environment variables for this deployment only.",
    "items": {
     "properties": {
      "key": {
       "type": "string"
      },
      "value": {
       "type": "string"
      }
     },
     "required": [
      "key",
      "value"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "memory_mb": {
    "description": "Override the version memory.",
    "maximum": 9007199254740991,
    "minimum": 256,
    "type": "integer"
   },
   "tags": {
    "description": "Tags for filtering later, e.g. [\"agent-test\"]. Recommended.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "users": {
    "description": "Where the players are. Exactly one of these two is required.",
    "properties": {
     "geo_coordinates": {
      "description": "Latitude/longitude pairs, as an alternative to IP addresses.",
      "items": {
       "properties": {
        "latitude": {
         "type": "number"
        },
        "longitude": {
         "type": "number"
        }
       },
       "required": [
        "latitude",
        "longitude"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "ip_addresses": {
      "description": "Public IPv4/IPv6 addresses of the players. Edgegap places the server near them.",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "type": "object"
   },
   "version": {
    "description": "Version name within the application.",
    "type": "string"
   }
  },
  "required": [
   "application",
   "version",
   "users"
  ],
  "type": "object"
 },
 "name": "edgegap_deploy",
 "title": "Deploy a game server"
}
