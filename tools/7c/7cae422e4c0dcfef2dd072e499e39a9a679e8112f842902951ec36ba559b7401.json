{
 "description": "Sign a counter-round with amendments \u2014 either from a JSON amendments file you provide, or deterministic --auto with a stance. Signs a negotiating commitment.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "amendments_file": {
    "description": "Path to a JSON amendments file (drafted by you).",
    "type": "string"
   },
   "as": {
    "description": "Which side you're countering as.",
    "enum": [
     "a",
     "b"
    ],
    "type": "string"
   },
   "auto": {
    "description": "Use the deterministic auto-amender instead of an amendments file.",
    "type": "boolean"
   },
   "out": {
    "description": "Optional output path for the round.",
    "type": "string"
   },
   "stance": {
    "description": "Deterministic stance when using auto.",
    "enum": [
     "compromising",
     "conservative",
     "middleground"
    ],
    "type": "string"
   },
   "state": {
    "description": "Path to the negotiation state file.",
    "type": "string"
   }
  },
  "required": [
   "state"
  ],
  "type": "object"
 },
 "name": "negotiate_counter"
}
