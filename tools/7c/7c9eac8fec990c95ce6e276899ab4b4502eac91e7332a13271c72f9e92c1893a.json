{
 "description": "Verify a post-quantum signature over a message against a public key \u2014 auto-detects ML-DSA-65 / Dilithium-3 (verified offline) or Dilithium-2 (node). The floor primitive ($0.01) for any proof pipeline; returns a post-quantum-signed verdict receipt. Price $0.01 USDC on Base.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "message": {
    "description": "The UTF-8 message that was signed. Provide message OR message_b64.",
    "type": "string"
   },
   "message_b64": {
    "description": "The exact base64 bytes that were signed (use for /sign outputs).",
    "type": "string"
   },
   "payment": {
    "description": "Optional base64 x402 X-PAYMENT (EIP-3009 exact-scheme payload, or {txHash}). x402-aware wallet clients pass this automatically via _meta[\"x402/payment\"] \u2014 set it explicitly only if your client does not plumb _meta. Omit on the first call to receive the payment challenge.",
    "type": "string"
   },
   "public_key": {
    "description": "Base64 public key to verify against (1952B ML-DSA-65 or 1312B Dilithium-2).",
    "type": "string"
   },
   "signature": {
    "description": "Base64 signature to verify (ML-DSA-65 3309B or Dilithium-2 2420B).",
    "type": "string"
   }
  },
  "required": [
   "signature",
   "public_key"
  ],
  "type": "object"
 },
 "name": "verify_proof",
 "title": "Verify a post-quantum signature (floor primitive)"
}
