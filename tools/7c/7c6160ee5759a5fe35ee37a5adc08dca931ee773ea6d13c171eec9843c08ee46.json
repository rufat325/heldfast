{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Pull rows from a tabular Netherlands Open Data resource (CKAN DataStore) by resource_id (from a dataset's resources). Optional full-text q, limit, offset. Note: only resources with the DataStore enabled are queryable.",
 "inputSchema": {
  "examples": [
   {
    "resource_id": "abc123def456"
   },
   {
    "limit": 50,
    "q": "Amsterdam",
    "resource_id": "xyz789uvw012"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max rows (default 100).",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset.",
    "type": "number"
   },
   "q": {
    "description": "Optional full-text filter over the rows.",
    "type": "string"
   },
   "resource_id": {
    "description": "A resource_id from dataset/search_datasets.",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "query_resource",
 "title": "Query Resource",
 "version": "v1.b5e0891b"
}
