{
 "description": "Re-verify a portable signing receipt bundle, fully offline. Read-only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "bundle": {
    "description": "Path to the receipt bundle directory.",
    "type": "string"
   }
  },
  "required": [
   "bundle"
  ],
  "type": "object"
 },
 "name": "verify_receipt"
}
