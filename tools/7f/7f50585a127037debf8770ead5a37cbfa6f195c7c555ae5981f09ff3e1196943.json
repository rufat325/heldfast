{
 "annotations": {
  "title": "Click Element"
 },
 "description": "Click an element on a one-off page: loads `url` in a fresh browser context (stateless \u2014 no cookies unless passed, no shared state with other calls), waits `wait_secs` after load before clicking, then fires a DOM click on the first CSS-selector match. The click may trigger navigation (link, form submit) \u2014 the response `url` and `text_after` are read after that navigation lands. Returns `clicked:false` when the selector matches nothing. For multi-step interaction on a shared page use session_click instead.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "selector": {
    "description": "CSS selector of element to click",
    "type": "string"
   },
   "url": {
    "description": "The URL to load",
    "type": "string"
   },
   "wait_secs": {
    "default": null,
    "description": "Seconds to wait for the page to settle after load, before clicking",
    "format": "uint64",
    "minimum": 0,
    "type": [
     "integer",
     "null"
    ]
   }
  },
  "required": [
   "url",
   "selector"
  ],
  "type": "object"
 },
 "name": "click"
}
