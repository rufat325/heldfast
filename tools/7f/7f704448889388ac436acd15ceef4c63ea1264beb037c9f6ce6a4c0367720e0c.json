{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Comment"
 },
 "description": "Comment on a collection or artifact; the comment pins to its current revision/version.",
 "icons": [
  {
   "mimeType": "image/svg+xml",
   "sizes": [
    "any"
   ],
   "src": "https://kythene.com/favicon.svg"
  }
 ],
 "inputSchema": {
  "properties": {
   "artifactId": {
    "description": "artifact_id comments on a single artifact (from get_artifact); use instead\n of collection_id.",
    "type": "string"
   },
   "body": {
    "description": "body is the comment text (markdown welcome).",
    "type": "string"
   },
   "collectionId": {
    "description": "Exactly one of collection_id / artifact_id.",
    "type": "string"
   },
   "space": {
    "description": "space optionally selects which of your spaces to act in (an id or name\n from list_spaces). Omit it when you belong to a single space.",
    "type": "string"
   }
  },
  "required": [
   "body"
  ],
  "type": "object"
 },
 "name": "comment",
 "outputSchema": {
  "properties": {
   "id": {
    "type": "string"
   },
   "pinnedRevision": {
    "type": "integer"
   },
   "pinnedVersion": {
    "type": "integer"
   }
  },
  "type": "object"
 },
 "title": "Comment"
}
