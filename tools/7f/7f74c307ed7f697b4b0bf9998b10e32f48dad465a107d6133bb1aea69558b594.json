{
 "description": "QC-008 Simon collision: certify a presented collision for a hidden mask. x and y are equal-length bit strings with x != y and x XOR y == mask, and f(x) == f(y) (fx and fy are the presented function values). WIN when all three hold, MISS naming the failing relation. The mask is presented, never recovered. Floats refused.",
 "inputSchema": {
  "properties": {
   "fx": {
    "description": "presented f(x), decimal or bit string",
    "type": "string"
   },
   "fy": {
    "description": "presented f(y); must equal fx",
    "type": "string"
   },
   "mask": {
    "description": "hidden mask as a bit string, e.g. 11",
    "type": "string"
   },
   "x": {
    "description": "first input bit string, same length as mask",
    "type": "string"
   },
   "y": {
    "description": "second input bit string; x XOR y must equal mask",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "verify_simon"
}
