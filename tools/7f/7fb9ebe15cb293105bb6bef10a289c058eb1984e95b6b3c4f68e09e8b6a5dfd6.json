{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Propose a LOG PROFILE from a real log: infers the format, the own-code prefix (read from the stack traces, which is where it is demonstrated) and a starting set of rules covering the most frequent errors and whatever floods the file. What it does NOT propose is the server time zone: that is not in the log, and inventing it shifts the entire timeline without anyone noticing. Accepts a file path.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "format": {
    "description": "Force the format instead of detecting it",
    "enum": [
     "json",
     "logfmt",
     "syslog5424",
     "syslog3164",
     "clf",
     "combined",
     "plain"
    ],
    "type": "string"
   },
   "from": {
    "default": "end",
    "description": "Which end to read if the file is over 32 MB",
    "enum": [
     "start",
     "end"
    ],
    "type": "string"
   },
   "name": {
    "description": "Name for the profile",
    "type": "string"
   },
   "path": {
    "description": "Path to the log file. Use this for big logs: they do not fit in a tool call.",
    "type": "string"
   },
   "text": {
    "description": "The log as text. Alternative to `path`.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "log_profile_suggest"
}
