{
 "description": "List all GTM zones in a workspace. Automatically follows pagination.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "description": "The GTM account ID.",
    "type": "string"
   },
   "containerId": {
    "description": "The GTM container ID.",
    "type": "string"
   },
   "maxPages": {
    "description": "Maximum number of API pages to fetch (default 50). If more pages remain, the result includes truncated:true and a nextPageToken.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "pageToken": {
    "description": "Continuation token to resume listing from a previous truncated result.",
    "type": "string"
   },
   "workspaceId": {
    "description": "The GTM workspace ID.",
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "containerId",
   "workspaceId"
  ],
  "type": "object"
 },
 "name": "zones_list"
}
