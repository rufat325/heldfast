{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true
 },
 "description": "Navigate the active pane to a URL (full page load). Returns the resolved URL and HTTP status. Use browser_back / browser_forward to move through history, browser_reload to refresh the same URL.",
 "inputSchema": {
  "properties": {
   "url": {
    "description": "Absolute URL to load (e.g. http://localhost:3000/path).",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "browser_navigate"
}
