{
 "description": "Validate presence and correctness of common web resources (robots.txt, sitemap.xml, openapi.json, agent.json) for any domain. Returns availability status for the requested resource.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "resource": {
    "description": "The web resource to validate",
    "enum": [
     "robots.txt",
     "sitemap.xml",
     "openapi.json",
     "agent.json"
    ],
    "type": "string"
   },
   "url": {
    "description": "The website URL to validate resources for (e.g. https://example.com)",
    "type": "string"
   }
  },
  "required": [
   "url",
   "resource"
  ],
  "type": "object"
 },
 "name": "web_resource_validator"
}
