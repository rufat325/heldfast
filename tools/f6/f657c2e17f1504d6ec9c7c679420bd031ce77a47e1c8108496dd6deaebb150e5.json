{
 "description": "Get the current status and step results of a playbook run. Requires both playbook_id and run_id because the API uses nested routing (/playbooks/:id/runs/:runId).",
 "inputSchema": {
  "properties": {
   "playbook_id": {
    "description": "The playbook ID (required)",
    "type": "string"
   },
   "run_id": {
    "description": "The run ID to check (required)",
    "type": "string"
   }
  },
  "required": [
   "playbook_id",
   "run_id"
  ],
  "type": "object"
 },
 "name": "get_playbook_run_status"
}
