{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Reads the current ClassDojo roster and a local XLSX workbook, then reports exact count, missing, and unexpected student-name differences without changing ClassDojo data.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "mappings": {
    "items": {
     "properties": {
      "classdojoClassName": {
       "pattern": "^\\d{3}$",
       "type": "string"
      },
      "sourceClassName": {
       "minLength": 1,
       "type": "string"
      }
     },
     "required": [
      "classdojoClassName",
      "sourceClassName"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "sheetNames": {
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "studentNameFormat": {
    "enum": [
     "seat_number_dot_name",
     "name_only"
    ],
    "type": "string"
   },
   "workbookPath": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "workbookPath",
   "sheetNames",
   "studentNameFormat",
   "mappings"
  ],
  "type": "object"
 },
 "name": "classdojo_verify_roster_against_workbook",
 "title": "Verify a ClassDojo roster against an XLSX workbook"
}
