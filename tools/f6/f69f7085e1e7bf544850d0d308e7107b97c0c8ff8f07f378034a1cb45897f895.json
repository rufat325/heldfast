{
 "description": "Search for assets by name (case-insensitive).",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max results (default 100)",
    "type": "number"
   },
   "query": {
    "description": "Search query",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_assets"
}
