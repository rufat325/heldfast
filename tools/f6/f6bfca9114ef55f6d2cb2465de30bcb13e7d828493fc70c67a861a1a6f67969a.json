{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the UniProt protein sequence database for proteins by name, gene, or organism. UniProt is the world's leading repository for protein sequences and functional annotations, covering 250M+ sequences from Swiss-Prot (reviewed) and TrEMBL (unreviewed). Returns accession, protein name, organism, gene names, sequence length, review status, and annotation score. Supports field-specific queries (gene:TP53, organism:9606) and free-text search. Use reviewed:true to restrict to Swiss-Prot's manually curated, high-quality entries. CC BY 4.0 \u2014 Universal Protein Resource (UniProt Consortium)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "gene": {
    "description": "Gene name filter (exact or partial). E.g. \"TP53\", \"EGFR\", \"BRAC1\". Combined with query using AND logic",
    "type": "string"
   },
   "limit": {
    "description": "Number of results to return (1\u201325, default 5)",
    "maximum": 25,
    "minimum": 1,
    "type": "integer"
   },
   "organism_id": {
    "description": "NCBI taxonomy ID to filter by organism. Common IDs: 9606 (human), 10090 (mouse), 10116 (rat), 7227 (fruit fly), 6239 (C. elegans), 3702 (A. thaliana)",
    "type": "integer"
   },
   "query": {
    "description": "Protein or gene search term \u2014 e.g. \"insulin\", \"hemoglobin\", \"BRCA1\". Supports UniProt query syntax: field:value pairs (e.g. \"protein_name:kinase\")",
    "minLength": 1,
    "type": "string"
   },
   "reviewed": {
    "description": "If true, restrict to Swiss-Prot (manually reviewed) entries only. Swiss-Prot has higher quality annotations than TrEMBL (unreviewed)",
    "type": "boolean"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "science.uniprot.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "UniProt \u2014 Search Proteins"
}
