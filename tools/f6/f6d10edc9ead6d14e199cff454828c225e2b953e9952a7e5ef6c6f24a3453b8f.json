{
 "description": "Search ASTGL articles about MCP servers, local AI, and AI automation. Returns relevant article sections with source URLs for citation.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "content_type": {
    "description": "Filter by content type: article, tutorial, faq, comparison, guide, newsletter, project",
    "type": "string"
   },
   "limit": {
    "default": 5,
    "description": "Maximum number of results to return (default: 5)",
    "maximum": 20,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "description": "Search query (e.g., 'how to build an MCP server')",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_articles"
}
