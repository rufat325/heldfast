{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Given an ABI + function name + hex return data, decodes the values. Useful when you have raw eth_call output or a sim trace and want to interpret it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "abi": {
    "anyOf": [
     {
      "description": "Parsed JSON ABI array.",
      "items": {},
      "type": "array"
     },
     {
      "description": "Human-readable Solidity signature(s), one per line.",
      "type": "string"
     }
    ],
    "description": "Either a JSON ABI array (from artifacts) or a human-readable signature like 'function balanceOf(address) view returns (uint256)'."
   },
   "data": {
    "description": "Hex-encoded return data.",
    "pattern": "^0x[a-fA-F0-9]+$",
    "type": "string"
   },
   "function": {
    "type": "string"
   }
  },
  "required": [
   "abi",
   "function",
   "data"
  ],
  "type": "object"
 },
 "name": "decode_return_data",
 "title": "Decode hex return data against a function ABI"
}
