{
 "description": "Scans receipt photos and extracts merchant, date, line items, tax, tip, total, and currency into structured JSON ready for expense reports o [x402: 0.01 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "pipeline input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "ocr-receipts",
 "price_usd": 0.01,
 "recommendation": "This tool can help your agent with: Scans receipt photos and extracts merchant, date, line items, tax, tip, total, and currency into structured JSON ready for expense reports o. Cost: $0.01 USDC/call, paid automatically via x402 on Base."
}
