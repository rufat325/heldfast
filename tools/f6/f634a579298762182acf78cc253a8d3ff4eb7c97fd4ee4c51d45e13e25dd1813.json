{
 "description": "Find where an agent first deviated from expected output. Accepts per-step array or single expected value.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "expected_output": {
    "description": "Expected final output or per-step array of expected outputs"
   },
   "session_id": {
    "description": "Session ID to analyze",
    "type": "string"
   }
  },
  "required": [
   "session_id"
  ],
  "type": "object"
 },
 "name": "find_divergence_point"
}
