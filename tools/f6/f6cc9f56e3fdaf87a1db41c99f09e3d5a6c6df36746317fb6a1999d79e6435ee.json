{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search crates.io for Rust packages by keyword. Returns crate name, description, downloads, latest version, and repo URL.",
 "inputSchema": {
  "examples": [
   {
    "query": "web framework"
   },
   {
    "limit": 20,
    "query": "async runtime"
   }
  ],
  "properties": {
   "limit": {
    "description": "Number of results to return (default 10, max 100)",
    "type": "number"
   },
   "query": {
    "description": "Search query string",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_crates",
 "outputSchema": {
  "properties": {
   "crates": {
    "items": {
     "properties": {
      "description": {
       "description": "Short description of the crate",
       "type": [
        "string",
        "null"
       ]
      },
      "downloads": {
       "description": "Total download count",
       "type": "number"
      },
      "homepage": {
       "description": "Homepage URL",
       "type": [
        "string",
        "null"
       ]
      },
      "max_stable_version": {
       "description": "Latest stable version",
       "type": [
        "string",
        "null"
       ]
      },
      "name": {
       "description": "Crate name",
       "type": "string"
      },
      "newest_version": {
       "description": "Latest published version",
       "type": "string"
      },
      "repository": {
       "description": "Repository URL",
       "type": [
        "string",
        "null"
       ]
      },
      "updated_at": {
       "description": "Last update timestamp",
       "type": "string"
      }
     },
     "required": [
      "name",
      "description",
      "downloads",
      "newest_version",
      "max_stable_version",
      "repository",
      "homepage",
      "updated_at"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "total": {
    "description": "Total number of matching crates",
    "type": "number"
   }
  },
  "required": [
   "total",
   "crates"
  ],
  "type": "object"
 },
 "title": "Search Crates",
 "version": "v1.a6fd5115"
}
