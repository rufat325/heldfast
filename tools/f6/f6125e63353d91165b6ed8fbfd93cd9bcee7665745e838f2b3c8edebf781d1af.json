{
 "_meta": {
  "openai/outputTemplate": "ui://hovercode/qr-chatgpt.html?v=1",
  "openai/toolInvocation/invoked": "Form updated",
  "openai/toolInvocation/invoking": "Updating form"
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Update form"
 },
 "description": "Change an existing form without recreating it: edit its fields, settings (redirect, thank-you, title), or branding (theme/colors/logo), or close/reopen it. If you pass `fields`, provide the COMPLETE desired list \u2014 include each existing field's `id` (from get_form) to keep it; omit a field to remove it; leave `id` off a new field. If the form is already published, edits are re-published so they go live.",
 "inputSchema": {
  "properties": {
   "bg_color": {
    "description": "Hex background color.",
    "type": "string"
   },
   "button_color": {
    "description": "Hex submit-button color.",
    "type": "string"
   },
   "button_text_color": {
    "description": "Hex button-text color.",
    "type": "string"
   },
   "description": {
    "description": "New description.",
    "type": "string"
   },
   "fields": {
    "description": "Complete desired field list (see note); create_form shape plus an optional `id`.",
    "items": {
     "properties": {
      "id": {
       "description": "Existing field id to keep/update; omit if new.",
       "type": "string"
      },
      "label": {
       "description": "The field's question/label.",
       "type": "string"
      },
      "max_label": {
       "description": "scale only: label under the top value",
       "type": "string"
      },
      "min_label": {
       "description": "scale only: label under the lowest value",
       "type": "string"
      },
      "nps": {
       "description": "Net Promoter Score preset (0\u201310, standard labels).",
       "type": "boolean"
      },
      "options": {
       "description": "Choices for select/radio/checkbox/ranking; {label, image} objects for image_choice.",
       "items": {
        "type": [
         "string",
         "object"
        ]
       },
       "type": "array"
      },
      "required": {
       "description": "Whether an answer is required.",
       "type": "boolean"
      },
      "show_when": {
       "description": "Conditional logic: show this field only while its conditions match EARLIER answers (e.g. a follow-up question, or a statement recommending a product). Omit to always show.",
       "properties": {
        "action": {
         "description": "'show' (default): shown only while the rules match. 'require': always shown, required only while they match. On a page_break field the rules skip the whole page.",
         "enum": [
          "show",
          "require"
         ],
         "type": "string"
        },
        "match": {
         "description": "Default 'all'.",
         "enum": [
          "all",
          "any"
         ],
         "type": "string"
        },
        "rules": {
         "items": {
          "properties": {
           "field": {
            "description": "The question this condition looks at: its label, its id, or its 0-based position.",
            "type": "string"
           },
           "op": {
            "description": "eq / neq (is, is not), contains / not_contains (text or checkbox), gt / lt (numbers, ratings, dates), is_empty / not_empty (no value needed).",
            "enum": [
             "eq",
             "neq",
             "contains",
             "not_contains",
             "gt",
             "lt",
             "is_empty",
             "not_empty"
            ],
            "type": "string"
           },
           "value": {
            "description": "Value to compare with (an option's value for choice questions).",
            "type": "string"
           }
          },
          "required": [
           "field",
           "op"
          ],
          "type": "object"
         },
         "type": "array"
        }
       },
       "required": [
        "rules"
       ],
       "type": "object"
      },
      "type": {
       "description": "Field type.",
       "enum": [
        "text",
        "email",
        "phone",
        "number",
        "textarea",
        "select",
        "radio",
        "checkbox",
        "date",
        "time",
        "url",
        "rating",
        "scale",
        "yes_no",
        "image_choice",
        "ranking",
        "statement",
        "hidden",
        "page_break",
        "image"
       ],
       "type": "string"
      }
     },
     "required": [
      "label"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "id": {
    "description": "Form id.",
    "type": "string"
   },
   "logo_url": {
    "description": "URL of a logo image (JPEG/PNG/GIF/WebP) to show on the form.",
    "type": "string"
   },
   "outcomes": {
    "description": "Pathways / branching endings. After submit the first outcome whose rules match decides the ending: its own thank-you title/message and/or a redirect_url (e.g. send people to a different product page per answer). If none match, the default thank-you page is shown.",
    "items": {
     "properties": {
      "end_early": {
       "description": "Screening: end the form as soon as this matches (checked on every page change), not just at submit.",
       "type": "boolean"
      },
      "match": {
       "description": "Default 'all'.",
       "enum": [
        "all",
        "any"
       ],
       "type": "string"
      },
      "name": {
       "description": "Short name, recorded on each response (e.g. 'Recommend Zero Sugar').",
       "type": "string"
      },
      "redirect_url": {
       "description": "Optional page to send the respondent to.",
       "type": "string"
      },
      "rules": {
       "items": {
        "properties": {
         "field": {
          "description": "The question this condition looks at: its label, its id, or its 0-based position.",
          "type": "string"
         },
         "op": {
          "description": "eq / neq (is, is not), contains / not_contains (text or checkbox), gt / lt (numbers, ratings, dates), is_empty / not_empty (no value needed).",
          "enum": [
           "eq",
           "neq",
           "contains",
           "not_contains",
           "gt",
           "lt",
           "is_empty",
           "not_empty"
          ],
          "type": "string"
         },
         "value": {
          "description": "Value to compare with (an option's value for choice questions).",
          "type": "string"
         }
        },
        "required": [
         "field",
         "op"
        ],
        "type": "object"
       },
       "type": "array"
      },
      "thank_you_message": {
       "type": "string"
      },
      "thank_you_title": {
       "type": "string"
      }
     },
     "required": [
      "name",
      "rules"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "redirect_url": {
    "description": "URL to send respondents to after they submit.",
    "type": "string"
   },
   "status": {
    "description": "Close (stop responses) or reopen. When set, other edits are ignored.",
    "enum": [
     "close",
     "reopen"
    ],
    "type": "string"
   },
   "text_color": {
    "description": "Hex text color.",
    "type": "string"
   },
   "thank_you_message": {
    "description": "Shown after submitting (if no redirect).",
    "type": "string"
   },
   "theme": {
    "description": "Named color preset. Explicit colors below override it.",
    "enum": [
     "light",
     "dark",
     "minimal",
     "bold"
    ],
    "type": "string"
   },
   "title": {
    "description": "New title.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "update_form",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "allow_multiple_submissions": {
    "type": "boolean"
   },
   "bg_color": {
    "type": [
     "string",
     "null"
    ]
   },
   "button_color": {
    "type": [
     "string",
     "null"
    ]
   },
   "button_text_color": {
    "type": [
     "string",
     "null"
    ]
   },
   "created": {
    "type": "string"
   },
   "dashboard_url": {
    "description": "Where the owner manages the form on hovercode.com.",
    "type": "string"
   },
   "description": {
    "type": [
     "string",
     "null"
    ]
   },
   "error": {
    "additionalProperties": true,
    "description": "Present only when the tool failed (the result is also flagged isError).",
    "properties": {
     "cause": {
      "description": "Machine-readable failure category: invalid_argument, not_found, forbidden, plan_limit, rate_limited, or server_error.",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "retryable": {
      "description": "Whether repeating the same call could succeed.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "fields": {
    "items": {
     "additionalProperties": true,
     "properties": {
      "field_type": {
       "type": "string"
      },
      "help_text": {
       "type": [
        "string",
        "null"
       ]
      },
      "id": {
       "type": "string"
      },
      "label": {
       "type": "string"
      },
      "logic": {
       "type": [
        "object",
        "null"
       ]
      },
      "max_length": {
       "type": [
        "integer",
        "null"
       ]
      },
      "max_value": {
       "type": [
        "number",
        "null"
       ]
      },
      "min_length": {
       "type": [
        "integer",
        "null"
       ]
      },
      "min_value": {
       "type": [
        "number",
        "null"
       ]
      },
      "options": {
       "items": {
        "additionalProperties": true,
        "properties": {
         "label": {
          "type": "string"
         },
         "value": {
          "type": "string"
         }
        },
        "type": "object"
       },
       "type": "array"
      },
      "order": {
       "type": "integer"
      },
      "pattern": {
       "type": [
        "string",
        "null"
       ]
      },
      "placeholder": {
       "type": [
        "string",
        "null"
       ]
      },
      "required": {
       "type": "boolean"
      },
      "settings": {
       "type": "object"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "has_unpublished_changes": {
    "type": "boolean"
   },
   "id": {
    "type": "string"
   },
   "logo": {
    "type": [
     "string",
     "null"
    ]
   },
   "modified": {
    "type": "string"
   },
   "notification_recipients": {
    "type": "array"
   },
   "outcomes": {
    "type": "array"
   },
   "qr_code_svg": {
    "description": "QR code for the share URL, as SVG markup.",
    "type": [
     "string",
     "null"
    ]
   },
   "qr_id": {
    "description": "Id of the form's own QR code (published forms). Use with download_qr.",
    "type": [
     "string",
     "null"
    ]
   },
   "redirect_delay": {
    "type": [
     "integer",
     "null"
    ]
   },
   "redirect_url": {
    "type": [
     "string",
     "null"
    ]
   },
   "response_count": {
    "type": "integer"
   },
   "share_url": {
    "description": "Public URL respondents open (null until published).",
    "type": [
     "string",
     "null"
    ]
   },
   "shortlink_domain": {
    "type": [
     "string",
     "null"
    ]
   },
   "shortlink_slug": {
    "type": [
     "string",
     "null"
    ]
   },
   "shortlink_url": {
    "type": [
     "string",
     "null"
    ]
   },
   "show_powered_by": {
    "type": "boolean"
   },
   "show_progress": {
    "type": "boolean"
   },
   "status": {
    "description": "Draft, Published, or Closed.",
    "type": "string"
   },
   "text_color": {
    "type": [
     "string",
     "null"
    ]
   },
   "thank_you_message": {
    "type": [
     "string",
     "null"
    ]
   },
   "thank_you_title": {
    "type": [
     "string",
     "null"
    ]
   },
   "title": {
    "type": "string"
   },
   "workspace_id": {
    "type": "string"
   }
  },
  "type": "object"
 }
}
