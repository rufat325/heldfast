{
 "description": "Rank 18 LLM models by the real monthly cost of a given workload (input/output tokens per call \u00d7 calls per month). Reports the cheapest and most expensive option, the cost spread, and which models can actually hold your prompt in their context window.",
 "inputSchema": {
  "properties": {
   "callsPerMonth": {
    "description": "Number of API calls per month",
    "type": "number"
   },
   "inputTokensPerCall": {
    "description": "Average input (prompt) tokens per call",
    "type": "number"
   },
   "minContextWindow": {
    "description": "Only include models whose context window is at least this many tokens",
    "type": "number"
   },
   "outputTokensPerCall": {
    "description": "Average output (completion) tokens per call",
    "type": "number"
   },
   "provider": {
    "description": "Filter to one provider: openai, anthropic, google, deepseek or meta",
    "type": "string"
   },
   "topN": {
    "description": "How many models to return, cheapest first (default 8)",
    "type": "number"
   }
  },
  "required": [
   "callsPerMonth"
  ],
  "type": "object"
 },
 "name": "compare_llm_api_costs"
}
