{
 "description": "Perform comprehensive CORS headers audit across multiple endpoints",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "baseUrl": {
    "description": "Base URL or domain to analyze (e.g. https://example.com)",
    "type": "string"
   },
   "endpoints": {
    "description": "Optional comma-separated list of endpoint paths to analyze (e.g. /api,/auth)",
    "type": "string"
   }
  },
  "required": [
   "baseUrl"
  ],
  "type": "object"
 },
 "name": "cross_origin_resource_policymapper"
}
