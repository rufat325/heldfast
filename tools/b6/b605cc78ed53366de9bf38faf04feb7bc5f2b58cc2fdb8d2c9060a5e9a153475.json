{
 "_meta": {
  "skill": "btc"
 },
 "description": "Get confirmation status and details for a Bitcoin transaction by txid. Returns whether the transaction is confirmed, block height, fee, size, and I/O summary. Works for both confirmed and unconfirmed (mempool) transactions.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "txid": {
    "description": "Bitcoin transaction ID (64 hex characters)",
    "type": "string"
   }
  },
  "required": [
   "txid"
  ],
  "type": "object"
 },
 "name": "get_btc_transaction_status"
}
