{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Triage what's broken right now: group repeated errors (browser console/page errors + server errors) with counts, list failed/4xx-5xx network requests, and return a one-line summary. Start here before digging through get_logs.",
 "inputSchema": {
  "properties": {
   "app": {
    "description": "Scope to one app/project (pane label or id).",
    "type": "string"
   },
   "windowMs": {
    "description": "Only consider events from the last N ms (default: all).",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "diagnose"
}
