{
 "description": "Charge a stored OneClick Mall card across one or more mall merchant codes. Each details entry is a separate child charge with its own commerce_code, buy_order, amount, and installments_number.",
 "inputSchema": {
  "properties": {
   "buy_order": {
    "description": "Parent (mall) buy_order (max 26 chars)",
    "type": "string"
   },
   "details": {
    "description": "Child charges, one per mall seller",
    "items": {
     "properties": {
      "amount": {
       "description": "Child amount in CLP (integer)",
       "type": "number"
      },
      "buy_order": {
       "description": "Child buy_order (unique per detail, max 26 chars)",
       "type": "string"
      },
      "commerce_code": {
       "description": "Child-merchant commerce code",
       "type": "string"
      },
      "installments_number": {
       "description": "Installments (1 for single payment)",
       "type": "number"
      }
     },
     "required": [
      "commerce_code",
      "buy_order",
      "amount",
      "installments_number"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "tbk_user": {
    "description": "Stored-card token",
    "type": "string"
   },
   "username": {
    "description": "Merchant-side username associated with the tbk_user",
    "type": "string"
   }
  },
  "required": [
   "username",
   "tbk_user",
   "buy_order",
   "details"
  ],
  "type": "object"
 },
 "name": "oneclick_authorize"
}
