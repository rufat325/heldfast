{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Buyer opens a dispute and blocks AgentLot seller release pending review.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "order_id": {
    "type": "string"
   },
   "reason": {
    "type": "string"
   }
  },
  "required": [
   "order_id",
   "reason"
  ],
  "type": "object"
 },
 "name": "open_dispute",
 "title": "Open an order dispute"
}
