{
 "annotations": {
  "readOnlyHint": true,
  "title": "Trace the dependency path between two files"
 },
 "description": "Find how two files are connected in Kage's source-derived code graph. Reports direct dependency direction, reverse impact direction, or undirected graph connection.",
 "inputSchema": {
  "properties": {
   "from": {
    "description": "Source file path or unique suffix",
    "type": "string"
   },
   "project_dir": {
    "description": "Absolute path to the repository root.",
    "type": "string"
   },
   "to": {
    "description": "Target file path or unique suffix",
    "type": "string"
   }
  },
  "required": [
   "project_dir",
   "from",
   "to"
  ],
  "type": "object"
 },
 "name": "kage_dependency_path"
}
