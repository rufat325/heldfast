{
 "description": "Run a coverage Gradle task (Kover or Jacoco) and summarize line coverage per package. Tries koverXmlReport<Variant> by default; pass 'task' for a custom one (e.g. jacocoTestReport). The project must apply a coverage plugin.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "module": {
    "description": "Module (defaults to session)",
    "type": "string"
   },
   "projectDir": {
    "description": "Project root (defaults to session)",
    "type": "string"
   },
   "skipRun": {
    "description": "Only parse an existing report without running Gradle",
    "type": "boolean"
   },
   "task": {
    "description": "Coverage task to run, e.g. 'koverXmlReportDebug'",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_coverage_report",
 "title": "Get coverage report"
}
