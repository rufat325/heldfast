{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Trades by Congress Member"
 },
 "description": "Get a congress member's disclosed securities transactions (newest first, last year by default). Shows tickers, transaction and filing dates, disclosed amount ranges, and the filed Asset identifying the instrument (such as stock, option, or bond). Use SearchCongressMembers to find member names, and GetCongressionalTrades for all members' transactions in one ticker.",
 "inputSchema": {
  "properties": {
   "endDate": {
    "default": null,
    "description": "End date in YYYY-MM-DD format (defaults to today)",
    "type": "string"
   },
   "maxResults": {
    "default": 50,
    "description": "Maximum number of trades to return (default: 50, max: 500, newest first)",
    "type": "integer"
   },
   "memberName": {
    "description": "Congress member name, case-insensitive (e.g., 'Nancy Pelosi', 'Dan Crenshaw'); use SearchCongressMembers to find the exact name",
    "type": "string"
   },
   "offset": {
    "default": 0,
    "description": "Number of trades to skip before returning rows \u2014 pass the previous call's shown count to page past the maxResults cap (default: 0)",
    "type": "integer"
   },
   "startDate": {
    "default": null,
    "description": "Start date in YYYY-MM-DD format (defaults to 1 year ago)",
    "type": "string"
   },
   "ticker": {
    "default": null,
    "description": "Optional stock ticker to combine with the member filter (e.g., AAPL)",
    "type": "string"
   },
   "transactionType": {
    "default": null,
    "description": "Filter by transaction type: Purchase or Sale; the synonyms Buy/Sell are accepted (defaults to all)",
    "type": "string"
   }
  },
  "required": [
   "memberName"
  ],
  "type": "object"
 },
 "name": "GetMemberTrades",
 "title": "Trades by Congress Member"
}
