{
 "description": "List contributor + research files modified since the last build (or since a given ISO date). Returns paths + sizes + mtimes. Used by /mb:build to know what to synthesize.",
 "inputSchema": {
  "properties": {
   "projectId": {
    "description": "Project id; defaults to the current project (config.json)",
    "type": "string"
   },
   "since": {
    "description": "ISO date (YYYY-MM-DD); defaults to last context.md mtime",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "mindbase_gather_sources"
}
