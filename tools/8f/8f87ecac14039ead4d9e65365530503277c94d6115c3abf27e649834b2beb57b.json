{
 "description": "Compile and render a safe template preview. `warnings` tells you what was stripped and what would not render \u2014 read it before publishing rather than after sending.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "blocks": {
    "description": "Canonical content blocks. This is the stored form \u2014 HTML is converted into it, never kept raw.",
    "items": {
     "additionalProperties": true,
     "type": "object"
    },
    "type": "array"
   },
   "message_type": {
    "description": "What the template may be used for. A marketing campaign will not accept a transactional template.",
    "enum": [
     "transactional",
     "marketing"
    ],
    "type": "string"
   },
   "name": {
    "description": "Template name",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "subject": {
    "description": "Subject with declared {{variables}}",
    "type": "string"
   },
   "values": {
    "additionalProperties": true,
    "description": "Values to substitute into the declared variables for this preview.",
    "type": "object"
   },
   "variables": {
    "description": "Typed variables the template declares: `name`, `type` and whether it is `required`.",
    "items": {
     "additionalProperties": false,
     "properties": {
      "name": {
       "description": "Variable name",
       "type": "string"
      },
      "required": {
       "type": "boolean"
      },
      "type": {
       "enum": [
        "string",
        "number",
        "date"
       ],
       "type": "string"
      }
     },
     "required": [
      "name",
      "type"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "organization_id",
   "subject",
   "variables",
   "blocks"
  ],
  "type": "object"
 },
 "name": "commsharbor_template_preview"
}
