{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List open-box units"
 },
 "description": "List open-box / ex-display units (Romanian: \"resigilat\") \u2014 returned or opened stock sold cheaper, one entry per physical unit, each with its own price, condition note and product page. Quantities are small and often one-of-a-kind, so treat availability as immediate-only. These units are NOT part of the normal catalog listings.",
 "inputSchema": {
  "properties": {
   "brand": {
    "description": "Restrict by brand name, partial match.",
    "type": "string"
   },
   "category": {
    "description": "Restrict by category name, partial match.",
    "type": "string"
   },
   "category_id": {
    "description": "Restrict to one category id.",
    "type": "integer"
   },
   "language": {
    "description": "Locale for text + pricing (ro=RON, hu=HUF, de/en=EUR). Default ro.",
    "enum": [
     "ro",
     "hu",
     "de",
     "en"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 20).",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "max_price": {
    "description": "Highest acceptable price, response currency.",
    "type": "number"
   },
   "offset": {
    "maximum": 500,
    "minimum": 0,
    "type": "integer"
   },
   "query": {
    "description": "Optional text match on product or brand name.",
    "type": "string"
   },
   "sort": {
    "description": "Default discount (biggest reduction first).",
    "enum": [
     "discount",
     "price_asc",
     "price_desc",
     "newest"
    ],
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "list_open_box",
 "title": "List open-box units"
}
