{
 "description": "MCP subregistry (GET /v0.1/servers). Only live kind=mcp. Only `kind=mcp` and only `live`. It is the route a generic MCP client knows how to read without knowing this product. `GET /v0/servers` is an alias of the same resource, kept for whoever already pointed there \u2014 this is the canonical URL.",
 "inputSchema": {
  "properties": {
   "api_pass": {
    "description": "MAT-only private pass: mat_<32 random hex>_<64 random hex>. Generate and save before buying.",
    "type": "string"
   },
   "cursor": {
    "description": "Opaque cursor of the next page, from `metadata.next_cursor`.",
    "type": "string"
   },
   "limit": {
    "default": 30,
    "description": "Servers per page.",
    "type": "number"
   },
   "retry_key": {
    "description": "Optional retry key, up to 80 ASCII characters; same read for five minutes.",
    "type": "string"
   },
   "search": {
    "description": "Free text over the server's name and description.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_mcp_servers"
}
