{
 "description": "Revoke a single signing certificate by its ASC certificate ID. Use when a Mac's keychain ACL is corrupted and the local private key is unusable \u2014 Apple's portal still holds the cert and Xcode refuses to create a new one until you revoke. CAUTION: revoking a Distribution cert in active use breaks new uploads.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "certificateId": {
    "description": "Certificate ID from asc_list_certificates (e.g. \"4Z59G7STQA\")",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "certificateId"
  ],
  "type": "object"
 },
 "name": "asc_revoke_certificate"
}
