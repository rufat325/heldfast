{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Build Company Universe"
 },
 "description": "Build a list of listed companies from filters rather than from identifiers you already hold. Filter by exchange, country, region, sector, security type, public float band, fiscal year end month, reporting cadence, operating status and foreign private issuer status. Returns 44 fields per company, the same shape resolve_company returns. At least one filter is required, so a bare call cannot pull the whole universe. This tool does NOT accept company identifiers: use resolve_company, qualify_company or get_reporting_timing for specific companies. SET limit EXPLICITLY. It defaults to 1000 and you are charged per company row returned, so an unbounded call is an expensive call. Truncation is always reported, never silent. Requires an APIFY_TOKEN and consumes Apify credits. Read only.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cadences": {
    "description": "quarterly, semiannual, annual, unknown.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "country_codes": {
    "description": "ISO 3166-1 alpha-2, e.g. US, GB, FR.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "exchange_codes": {
    "description": "ISO 10383 MICs. 18 venues are covered.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "exclude_blank_checks": {
    "description": "Drop pre deal SPACs. Separate from the operating company filter: a blank check shell is flagged as an operating company and passes every ordinary firmographic filter.",
    "enum": [
     "false",
     "true"
    ],
    "type": "string"
   },
   "exclude_december_fiscal_year_end": {
    "description": "Keep only companies whose fiscal year ends in a month other than December, the accounts whose budget cycle is out of phase with a calendar quarter.",
    "enum": [
     "false",
     "true"
    ],
    "type": "string"
   },
   "exclude_name_only_matches": {
    "description": "Drop rows whose identity link rests on a name and country agreeing rather than on an identifier. Use this wherever a wrong identity link matters.",
    "enum": [
     "false",
     "true"
    ],
    "type": "string"
   },
   "exclude_share_alike": {
    "description": "Drop rows derived from CC BY-SA sources, whose share alike condition may not suit a closed product.",
    "enum": [
     "false",
     "true"
    ],
    "type": "string"
   },
   "fiscal_year_end_months": {
    "description": "Integers 1 to 12. Fiscal year end is effectively a United States field in this dataset.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "foreign_private_issuer": {
    "description": "Filter on foreign private issuer status.",
    "enum": [
     "any",
     "only",
     "exclude"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Maximum rows for universe and season. Truncation is always reported, never silent.",
    "type": "string"
   },
   "min_provenance_confidence": {
    "description": "Set to high to exclude rows whose source terms were never read.",
    "enum": [
     "any",
     "high"
    ],
    "type": "string"
   },
   "operating_companies_only": {
    "description": "Drop funds, trusts and other non operating entities. Sent as a string for Clay compatibility.",
    "enum": [
     "false",
     "true"
    ],
    "type": "string"
   },
   "public_float_bands": {
    "description": "micro, small, mid, large, mega, unknown. Size runs on public float because market capitalization is not populated anywhere in this dataset.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "regions": {
    "description": "Shorthand for a set of venues and countries: us, uk, eu. Widens an explicit exchange or country filter rather than replacing it.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "sectors": {
    "description": "SEC SIC descriptions, e.g. Pharmaceutical Preparations. Populated on roughly 65 percent of the publishable universe.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "security_types": {
    "description": "ordinary_shares, depositary_receipt, preferred_shares.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "us_registrant_only": {
    "description": "Keep only companies carrying an SEC CIK.",
    "enum": [
     "false",
     "true"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "build_company_universe",
 "title": "Build Company Universe"
}
