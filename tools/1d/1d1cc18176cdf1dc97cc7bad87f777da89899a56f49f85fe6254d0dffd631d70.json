{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Execute collection"
 },
 "description": "Run a collection now to fetch fresh articles across all its sources. Costs credits per source. Does not modify the collection.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "collection_id": {
    "description": "Collection id (from list_collections)",
    "minLength": 1,
    "type": "string"
   },
   "max_articles": {
    "default": 50,
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "collection_id"
  ],
  "type": "object"
 },
 "name": "execute_collection",
 "title": "Execute collection"
}
