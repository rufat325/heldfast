{
 "description": "Download raw run artifacts (logs, screenshots, videos) as a ZIP. Use when the HTML report doesn't contain enough detail (e.g. need full logcat, video recording, or non-failure screenshots).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "outputDir": {
    "description": "Directory for the zip. Defaults to /tmp.",
    "type": "string"
   },
   "results": {
    "default": "FAILED",
    "description": "Whether to download only failed flows' artifacts or all.",
    "enum": [
     "FAILED",
     "ALL"
    ],
    "type": "string"
   },
   "uploadId": {
    "type": "string"
   }
  },
  "required": [
   "uploadId"
  ],
  "type": "object"
 },
 "name": "download_artifacts"
}
