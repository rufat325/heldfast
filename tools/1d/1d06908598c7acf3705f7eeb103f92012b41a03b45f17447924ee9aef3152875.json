{
 "description": "List all payment transactions for an order (authorizations, captures, sales, refunds, voids) including gateway and amount.",
 "inputSchema": {
  "properties": {
   "in_shop_currency": {
    "description": "Show amounts in the shop's default currency rather than transaction currency",
    "type": "boolean"
   },
   "order_id": {
    "description": "Shopify order ID",
    "type": "string"
   },
   "since_id": {
    "description": "Return only transactions with IDs greater than this value",
    "type": "string"
   }
  },
  "required": [
   "order_id"
  ],
  "type": "object"
 },
 "name": "list_transactions"
}
