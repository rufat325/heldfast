{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the complete stop schedule for a specific Belgian train \u2014 returns all stations the train calls at with scheduled times, actual delays, platform numbers, occupancy levels, and whether the train has already departed each stop. Train IDs are returned by the liveboard and connections tools. No auth required. Data: iRail SNCB/NMBS Belgium.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "date": {
    "description": "Date in DDMMYY format (e.g. \"290626\" for 29 June 2026). Defaults to today.",
    "pattern": "^\\d{6}$",
    "type": "string"
   },
   "id": {
    "description": "Vehicle/train ID in iRail format (e.g. \"BE.NMBS.IC1810\", \"BE.NMBS.S22308\"). Obtain from liveboard or connections response.",
    "minLength": 3,
    "type": "string"
   },
   "lang": {
    "description": "Language for station names. Default: en.",
    "enum": [
     "en",
     "nl",
     "fr",
     "de"
    ],
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "irail.transit.vehicle",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "iRail Train Vehicle Schedule"
}
