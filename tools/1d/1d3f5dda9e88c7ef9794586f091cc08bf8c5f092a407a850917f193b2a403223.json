{
 "description": "A tool's JSON Schema as compact English for an agent's context: identifiers, types, required flags, enums and constraints verbatim, the punctuation gone. Charged per call.",
 "inputSchema": {
  "properties": {
   "caller": {
    "type": "string"
   },
   "idempotencyKey": {
    "description": "your key for this request; send it again on a retry and the charge is made once",
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "schema": {
    "description": "the JSON Schema of the tool's arguments",
    "type": "object"
   }
  },
  "required": [
   "caller",
   "schema"
  ],
  "type": "object"
 },
 "name": "sapphire_schema_to_english"
}
