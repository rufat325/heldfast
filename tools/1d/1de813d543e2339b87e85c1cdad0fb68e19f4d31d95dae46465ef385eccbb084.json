{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Capability Search"
 },
 "contract": {
  "auth_scope": "public",
  "idempotent": true,
  "output_schema": {
   "count": "number",
   "matched": "string[]",
   "tools": "object[]"
  },
  "price": {
   "free_tier": "unlimited",
   "paid": "included in Zambo Pass",
   "per_call": "free"
  },
  "side_effects": false
 },
 "description": "Search across the supported Zambo tool catalog for a use case. Returns relevant tools with relevance scores, descriptions, taglines, and callable API endpoints.",
 "destructiveHint": false,
 "inputSchema": {
  "anyOf": [
   {
    "required": [
     "q"
    ]
   },
   {
    "required": [
     "query"
    ]
   },
   {
    "required": [
     "keyword"
    ]
   },
   {
    "required": [
     "search"
    ]
   }
  ],
  "examples": [
   {
    "q": "code audit"
   }
  ],
  "properties": {
   "keyword": {
    "description": "Natural-language alias for q.",
    "type": "string"
   },
   "q": {
    "description": "Search keyword or phrase. Also accepts the natural aliases query, keyword, or search. Example: 'code audit', 'prompt injection defense', 'wallet scoring', 'lead generation', 'trust verification'",
    "type": "string"
   },
   "query": {
    "description": "Alias for q. Use q when possible.",
    "type": "string"
   },
   "search": {
    "description": "Natural-language alias for q.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "capability_search",
 "openWorldHint": true,
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "count": {
    "type": "number"
   },
   "matched": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "tools": {
    "items": {
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "readOnlyHint": true,
 "title": "Capability Search"
}
