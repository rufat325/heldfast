{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "list_secret_versions"
 },
 "description": "List a secret's version history: who wrote each version, when, and which ones were restores. Never returns values \u2014 pair it with restore_secret to roll back, or get_secret(version, reveal:true) to inspect one.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "app": {
    "description": "application slug or id (or set via configure_project)",
    "type": "string"
   },
   "env": {
    "description": "environment slug or id (a service token infers its own)",
    "type": "string"
   },
   "group": {
    "description": "target a group environment instead of an app",
    "type": "string"
   },
   "limit": {
    "description": "default 20",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "name": {
    "type": "string"
   },
   "org": {
    "description": "organization slug or id (defaults to a lone org)",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "list_secret_versions"
}
