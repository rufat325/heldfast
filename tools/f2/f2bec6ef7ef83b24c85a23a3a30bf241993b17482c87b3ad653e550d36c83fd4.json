{
 "description": "Return an LLM (Kimi/DeepSeek) answer to your query. General-purpose, pay-per-call inference for when an agent needs a model response. input=your query as text. [x402: 0.005 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "Consulta/prompt para el modelo",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "inference",
 "price_usd": 0.005,
 "recommendation": "This tool can help your agent with: Return an LLM (Kimi/DeepSeek) answer to your query. General-purpose, pay-per-call inference for when an agent needs a model response. input=your query as text.. Cost: $0.005 USDC/call, paid automatically via x402 on Base."
}
