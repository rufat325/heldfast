{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Everything to vet a domain in one call: DNS records + email posture, RDAP registration/age/registrar, IP network/ASN/geo (from the resolved A record) and public threat-intel on the site. Four AgentBIT tools, merged. $0.015. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "domain": {
    "description": "Bare registrable domain, e.g. example.com (no scheme, no path)",
    "type": "string"
   }
  },
  "required": [
   "domain"
  ],
  "type": "object"
 },
 "name": "agentbit.domain_dossier",
 "title": "Domain Dossier"
}
