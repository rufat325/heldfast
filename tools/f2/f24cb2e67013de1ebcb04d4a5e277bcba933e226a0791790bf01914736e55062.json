{
 "_meta": {
  "securitySchemes": [
   {
    "scopes": [
     "openid",
     "email",
     "profile"
    ],
    "type": "oauth2"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the models, defaults, limits, and credit costs available for one generation kind before queueing it. Video duration is an enum per model (durations_seconds), not a min-max range.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "kind": {
    "description": "Generation kind to describe.",
    "enum": [
     "image",
     "video",
     "voiceover",
     "music",
     "sound",
     "faceless"
    ],
    "type": "string"
   }
  },
  "required": [
   "kind"
  ],
  "type": "object"
 },
 "name": "generation_options_list",
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "options": {
    "type": "object"
   }
  },
  "required": [
   "options"
  ],
  "type": "object"
 },
 "title": "List Generation Options"
}
