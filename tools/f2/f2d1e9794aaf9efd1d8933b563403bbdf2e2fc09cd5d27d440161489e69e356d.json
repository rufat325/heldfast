{
 "description": "Scan only the added lines of a unified diff, reporting findings against the new file line numbers. Use this on a patch before committing so untouched code does not drown the result. Fully local. Runs fully locally: the input never leaves this machine.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "diff": {
    "description": "A unified diff.",
    "type": "string"
   },
   "language": {
    "description": "python, javascript, typescript. Auto-detected if omitted.",
    "type": "string"
   }
  },
  "required": [
   "diff"
  ],
  "type": "object"
 },
 "name": "scan_diff"
}
