{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Set Event URL"
 },
 "description": "Set a custom vanity URL for a live event you manage (e.g. socialloop.ai/go/sundown-sessions). 3\u201330 lowercase letters, numbers, and hyphens; must be unique. Requires event_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "slug": {
    "maxLength": 30,
    "minLength": 3,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "slug"
  ],
  "type": "object"
 },
 "name": "set_event_url",
 "title": "Set Event URL"
}
