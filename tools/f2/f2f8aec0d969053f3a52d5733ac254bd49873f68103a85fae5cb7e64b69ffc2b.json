{
 "description": "List available phone-call skill playbooks, optionally filtered by category (e.g. \"negotiation\", \"reservations\", \"debt-collection\") or tag. Returns summaries (id, name, description, tags) \u2014 call `skill_load` with the id to get the full playbook.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "category": {
    "description": "Filter by category. Valid: negotiation, customer-service, reservations, medical-admin, legal-admin, finance-admin, real-estate, travel, subscription, home-services, social, civic, employment, debt-collection, other.",
    "type": "string"
   },
   "tag": {
    "description": "Filter by a single tag (case-insensitive).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "skill_list"
}
