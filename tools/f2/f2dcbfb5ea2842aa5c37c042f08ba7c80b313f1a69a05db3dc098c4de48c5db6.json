{
 "description": "Synthesize what the user's wiki collectively knows about a topic. Returns structured threads with row-level citations, contradictions, and gaps. Cached; pass force_refresh: true to regenerate.",
 "inputSchema": {
  "properties": {
   "force_refresh": {
    "type": "boolean"
   },
   "topic": {
    "description": "Topic to synthesize (slug or free text)",
    "type": "string"
   }
  },
  "required": [
   "topic"
  ],
  "type": "object"
 },
 "name": "synthesize_topic"
}
