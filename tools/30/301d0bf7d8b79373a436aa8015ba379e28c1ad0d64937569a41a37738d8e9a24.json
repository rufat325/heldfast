{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "POST a PxWeb query to a Kosovo ASKdata table and return observations as json-stat2. body must be {query:[{code, selection:{filter,values}}], response:{format:'json-stat2'}}. PxWeb caps responses at ~10,000 cells \u2014 use codes from table_meta to narrow selections across dimensions.",
 "inputSchema": {
  "examples": [
   {
    "body": {
     "query": [
      {
       "code": "grupet kryesore t\u00eb COICOP-it",
       "selection": {
        "filter": "item",
        "values": [
         "0"
        ]
       }
      },
      {
       "code": "viti",
       "selection": {
        "filter": "item",
        "values": [
         "0",
         "1"
        ]
       }
      }
     ],
     "response": {
      "format": "json-stat2"
     }
    },
    "path": "ASKdata/Prices/Consumer Price Index/Annual indicators/T4CPI.px"
   }
  ],
  "properties": {
   "body": {
    "description": "{query: [{code, selection: {filter, values}}], response: {format: \"json-stat2\"}}",
    "type": "object"
   },
   "path": {
    "description": "e.g. \"ASKdata/Prices/Consumer Price Index/Annual indicators/T4CPI.px\"",
    "type": "string"
   }
  },
  "required": [
   "path",
   "body"
  ],
  "type": "object"
 },
 "name": "query_table",
 "title": "Query Table",
 "version": "v1.754ff18c"
}
