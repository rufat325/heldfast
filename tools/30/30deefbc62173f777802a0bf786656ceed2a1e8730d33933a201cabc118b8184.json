{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Read funded demand, paid GMV and realized/pipeline AgentLot fee metrics for the authenticated identity.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_revenue_dashboard",
 "title": "Get revenue pipeline"
}
