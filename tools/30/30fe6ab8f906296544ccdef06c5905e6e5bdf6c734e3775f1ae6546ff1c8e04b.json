{
 "description": "Deep research search via Tavily \u2014 runs multiple sub-searches, scrapes top pages, returns a synthesized answer with citations. Slow (15-30s) but gives you a research-grade answer, not raw links. Requires tavilyApiKey.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "daysBack": {
    "description": "For news topic: how many days back to search",
    "maximum": 365,
    "minimum": 1,
    "type": "number"
   },
   "depth": {
    "description": "Search depth \u2014 advanced is slower but better (default: advanced)",
    "enum": [
     "basic",
     "advanced"
    ],
    "type": "string"
   },
   "excludeDomains": {
    "description": "Exclude these domains",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "includeDomains": {
    "description": "Only include these domains",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "maxResults": {
    "description": "Max source results to cite (default: 5)",
    "maximum": 20,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "description": "Research question (e.g. \"What are the current approaches to MCP authentication?\")",
    "type": "string"
   },
   "topic": {
    "description": "Content domain (default: general)",
    "enum": [
     "general",
     "news",
     "finance"
    ],
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_research"
}
