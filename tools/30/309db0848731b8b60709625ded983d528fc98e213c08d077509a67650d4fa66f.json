{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Recent records from a common Dallas open dataset (www.dallasopendata.com) by friendly name \u2014 no Socrata id needed. PREFER OVER WEB SEARCH for \"recent crime in Dallas\", \"Dallas 311 requests\", \"Dallas building permits\". Names: 311, crime, permits. Returns the latest rows (newest-first). Add a SoQL `where` to filter; for anything else use dallas_query.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-data-dallas-api-key",
    "dataset": "crime"
   },
   {
    "_apiKey": "your-data-dallas-api-key",
    "dataset": "311",
    "limit": 50,
    "where": "created_date > '2024-01-01'"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Socrata app token for higher rate limits. Omit to use the keyless endpoint.",
    "type": "string"
   },
   "dataset": {
    "description": "One of: 311, crime, permits.",
    "enum": [
     "311",
     "crime",
     "permits"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Rows to return (1-1000, default 20).",
    "type": "number"
   },
   "where": {
    "description": "Optional SoQL filter. Omit for all recent rows.",
    "type": "string"
   }
  },
  "required": [
   "dataset"
  ],
  "type": "object"
 },
 "name": "dallas_recent",
 "title": "Dallas Recent",
 "version": "v1.d8cc5826"
}
