{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-direct-routing-calls"
 },
 "description": "Get a log of direct routing calls as a collection of directRoutingLogRow entries.\n\nTIP: Gets Direct Routing call records for a date range. Parameters are ISO 8601 dates. Returns callId, correlationId, userId, callerNumber, calleeNumber, duration, startDateTime, failureDateTime, finalSipCode, mediaPathLocation.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "count": {
    "description": "count",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "filter": {
    "description": "OData $filter expression, e.g. status eq 'inProgress'",
    "type": "string"
   },
   "fromDateTime": {
    "description": "Path parameter: fromDateTime",
    "type": "string"
   },
   "search": {
    "description": "search",
    "type": "string"
   },
   "skip": {
    "description": "skip",
    "type": "string"
   },
   "toDateTime": {
    "description": "Path parameter: toDateTime",
    "type": "string"
   },
   "top": {
    "description": "Page size (Graph $top). Start small (5-15) so responses fit the model context. Use @odata.nextLink from the response for more rows.",
    "type": "number"
   }
  },
  "required": [
   "fromDateTime",
   "toDateTime"
  ],
  "type": "object"
 },
 "name": "get-direct-routing-calls"
}
