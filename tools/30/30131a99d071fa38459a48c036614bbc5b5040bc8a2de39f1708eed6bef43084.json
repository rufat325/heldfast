{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Invoke a child tool"
 },
 "description": "Call one tool of an enabled service through the hub: service, tool name and its arguments (see datailo_tool_schema). Returns the service's own result.",
 "inputSchema": {
  "properties": {
   "args": {
    "anyOf": [
     {
      "additionalProperties": true,
      "type": "object"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "title": "Args"
   },
   "service": {
    "title": "Service",
    "type": "string"
   },
   "session": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "The `session` value datailo_connect_verify returned. Pass it on every Datailo call in this conversation; omit it when the user connected with Sign in (OAuth) or a key.",
    "title": "Session"
   },
   "tool": {
    "title": "Tool",
    "type": "string"
   }
  },
  "required": [
   "service",
   "tool"
  ],
  "title": "datailo_invokeArguments",
  "type": "object"
 },
 "name": "datailo_invoke",
 "title": "Invoke a child tool"
}
