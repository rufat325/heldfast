{
 "description": "Delete a single phase from a project plan by its phase ID. Use this to remove a specific work item. To bulk-remove completed work, use delete_done_phases instead.",
 "inputSchema": {
  "properties": {
   "phaseId": {
    "description": "Phase ID (from list_phases or get_project)",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "phaseId"
  ],
  "type": "object"
 },
 "name": "delete_phase"
}
