{
 "description": "Enable or disable Content Guard monitoring for an article. Enabling needs a live page: the stored URL, or one the server recovers (CMS inventory; FraseCMS re-derivation when a published post + domain exist). Quota-checked per plan. Needs write:content scope.",
 "inputSchema": {
  "properties": {
   "content_id": {
    "type": "string"
   },
   "enable": {
    "description": "true to start, false to stop monitoring",
    "type": "boolean"
   }
  },
  "required": [
   "content_id",
   "enable"
  ],
  "type": "object"
 },
 "name": "toggle_content_monitoring"
}
