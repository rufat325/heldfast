{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Batch-match a list of manufacturer part numbers in one call \u2014 ideal for BOM (bill of materials) enrichment. Each query resolves to the best-matching part(s) with distributor offers. Example: octopart_multi_match({ mpns: [\"SY89832UMG\", \"SY89874UMG\"], limit: 1, _apiKey: \"client_id:client_secret\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-octopart-client_id:your-octopart-client_secret",
    "limit": 1,
    "mpns": [
     "SY89832UMG",
     "GRM188R71C104KA01D"
    ]
   },
   {
    "_apiKey": "your-octopart-client_id:your-octopart-client_secret",
    "country": "US",
    "currency": "USD",
    "limit": 2,
    "mpns": [
     "LM317T",
     "ATMEGA328P-PU",
     "TL072CP"
    ]
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Nexar OAuth2 credentials as \"client_id:client_secret\". Get one at https://portal.nexar.com",
    "type": "string"
   },
   "country": {
    "description": "ISO country code for pricing/availability context, e.g. \"US\" (optional)",
    "type": "string"
   },
   "currency": {
    "description": "ISO currency code for converted prices, e.g. \"USD\" (optional)",
    "type": "string"
   },
   "limit": {
    "description": "Max parts to return per MPN (default 1)",
    "type": "number"
   },
   "mpns": {
    "description": "List of manufacturer part numbers to match (exact-match), e.g. [\"SY89832UMG\", \"GRM188R71C104KA01D\"]",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "mpns",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "octopart_multi_match",
 "title": "Octopart Multi Match",
 "version": "v1.607ec636"
}
