{
 "description": "List, create, or delete email signatures",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "action": {
    "description": "Action to perform",
    "enum": [
     "list",
     "create",
     "delete"
    ],
    "type": "string"
   },
   "id": {
    "description": "Signature ID (for delete)",
    "type": "string"
   },
   "isDefault": {
    "description": "Set as default signature (for create)",
    "type": "boolean"
   },
   "name": {
    "description": "Signature name (for create)",
    "type": "string"
   },
   "text": {
    "description": "Signature text content (for create)",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "manage_signatures"
}
