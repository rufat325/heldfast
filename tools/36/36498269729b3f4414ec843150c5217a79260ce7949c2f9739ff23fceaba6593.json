{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get a single scheduled event by its UUID (the last path segment of an event URI). Returns full event details including name, times, status, location, and event membership.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-calendly-api-key",
    "uuid": "XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Calendly personal access token (sent as Bearer token)",
    "type": "string"
   },
   "uuid": {
    "description": "Scheduled event UUID (last segment of the event URI).",
    "type": "string"
   }
  },
  "required": [
   "_apiKey",
   "uuid"
  ],
  "type": "object"
 },
 "name": "get_event",
 "title": "Get Event",
 "version": "v1.43134377"
}
