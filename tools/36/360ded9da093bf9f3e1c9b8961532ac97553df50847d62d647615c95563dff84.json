{
 "annotations": {
  "cost": {
   "all": 2
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "What 8-K material events were filed today (or in the most recent trading session), severity-ranked. Drops noise/procedural filings by default. Use for \"what mattered in the market today?\" / \"any breaking corporate news?\". Defaults to high+medium severity only.",
 "inputSchema": {
  "examples": [
   {},
   {
    "severity": "high"
   }
  ],
  "properties": {
   "limit": {
    "default": 30,
    "description": "Max filings. Default 30, max 100.",
    "maximum": 100,
    "minimum": 1,
    "type": "number"
   },
   "severity": {
    "default": "high,medium",
    "description": "Comma-separated severities to include. Default \"high,medium\". Allowed: high, medium, low, noise.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "sec_8k_today",
 "title": "Sec 8k Today",
 "version": "v1.e21e4b92"
}
