{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Store client-side encrypted data. This service holds no key that could decrypt it and never sees plaintext; note that the item key, namespace, alg label and size ARE stored in the clear. The first store claims the namespace and returns a namespace_token shown only once \u2014 store it immediately, because it is required by every later call and cannot be recovered. Pass if_match with an item's updated_at for a compare-and-swap write, or if_absent to create only; either returns status 'precondition_failed' rather than silently clobbering a concurrent write. Costs $0.02 in USDC on Base, paid via the x402 protocol, or from a credit token \u2014 call credits_trial for free credit if you have neither.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "alg": {
    "description": "Algorithm label recorded alongside the item (default aes-256-gcm)",
    "type": "string"
   },
   "ciphertext": {
    "description": "Encrypt before sending. Plaintext here would be a mistake.",
    "type": "string"
   },
   "credit_token": {
    "description": "Optional. A credit token from credits_trial or /credits/buy. Supplying it pays for this call from that balance, so no x402 payment or wallet is needed.",
    "type": "string"
   },
   "if_absent": {
    "description": "Only write if the key does not already exist. Fails with status 'precondition_failed' if it does.",
    "type": "boolean"
   },
   "if_match": {
    "description": "Only write if the item's current updated_at equals this. Use it whenever you are updating a value you read earlier, or a concurrent writer's change is lost silently.",
    "type": "string"
   },
   "key": {
    "description": "Item key",
    "type": "string"
   },
   "namespace": {
    "description": "Isolation scope",
    "type": "string"
   },
   "namespace_token": {
    "description": "One-time token issued by the first call that claimed this namespace. Required for every later call.",
    "type": "string"
   },
   "ttl": {
    "description": "Item lifetime in seconds",
    "type": "integer"
   }
  },
  "required": [
   "namespace",
   "key",
   "ciphertext"
  ],
  "type": "object"
 },
 "name": "vault_store",
 "outputSchema": {
  "properties": {
   "alg": {
    "description": "Algorithm label you supplied; the service never decrypts",
    "type": "string"
   },
   "created_at": {
    "description": "ISO-8601 creation time",
    "type": "string"
   },
   "expires_at": {
    "description": "Expiry, or null if it never expires",
    "type": [
     "string",
     "null"
    ]
   },
   "key": {
    "description": "The key that was written",
    "type": "string"
   },
   "namespace": {
    "description": "Isolation scope",
    "type": "string"
   },
   "namespace_token": {
    "description": "Issued only on the first store into a namespace, shown exactly once",
    "type": "string"
   },
   "receipt": {
    "description": "Payment receipt",
    "type": "string"
   },
   "size_bytes": {
    "description": "Stored ciphertext size",
    "type": "number"
   },
   "status": {
    "description": "'stored'",
    "type": "string"
   },
   "updated_at": {
    "description": "ISO-8601 last write",
    "type": "string"
   }
  },
  "required": [
   "status",
   "key"
  ],
  "type": "object"
 },
 "title": "Store an encrypted secret"
}
