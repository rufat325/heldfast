{
 "description": "Search the BrewPage public gallery. Optional filters: text query, pagination, sort, and 'mine' (owner-scoped, requires owner token).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "mine": {
    "description": "If true, restricts results to entries owned by the supplied owner token",
    "type": "boolean"
   },
   "ownerToken": {
    "description": "Owner token; only sent as X-Owner-Token when 'mine' is true",
    "type": "string"
   },
   "page": {
    "default": 1,
    "description": "Page number (1-based; default 1)",
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "description": "Free-text search query",
    "type": "string"
   },
   "size": {
    "description": "Page size (1-100; backend caps at 100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "sort": {
    "description": "Sort order: 'date' or 'views'",
    "enum": [
     "date",
     "views"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_gallery"
}
