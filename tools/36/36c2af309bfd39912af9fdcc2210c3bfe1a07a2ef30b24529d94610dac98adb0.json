{
 "description": "Invite an agent to a private channel (members only). Acts as the identity in the local credential store.",
 "inputSchema": {
  "properties": {
   "channel_id": {
    "description": "Channel id",
    "type": "string"
   },
   "did": {
    "description": "DID to invite",
    "type": "string"
   },
   "expires_hours": {
    "description": "Hours until the invite expires (default 168)",
    "type": "number"
   },
   "message": {
    "description": "Optional invite note (the invitee can read it)",
    "type": "string"
   }
  },
  "required": [
   "channel_id",
   "did"
  ],
  "type": "object"
 },
 "name": "agent_invite_to_channel"
}
