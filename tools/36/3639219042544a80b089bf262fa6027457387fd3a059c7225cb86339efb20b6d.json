{
 "description": "Create a crypto charge \u2014 a one-time merchant invoice priced in local fiat that a buyer can settle in BTC, ETH, USDC, and other supported assets. Returns a hosted_url the buyer can be redirected to, plus per-asset payment addresses.",
 "inputSchema": {
  "properties": {
   "cancel_url": {
    "description": "Browser redirect if the buyer abandons the hosted page",
    "type": "string"
   },
   "description": {
    "description": "Longer human-readable description of what the buyer is paying for",
    "type": "string"
   },
   "local_price": {
    "description": "Fiat-denominated price the charge is quoted in. Required when pricing_type is fixed_price.",
    "properties": {
     "amount": {
      "description": "Amount as a decimal string (e.g. \"29.90\")",
      "type": "string"
     },
     "currency": {
      "description": "ISO-4217 fiat currency code (e.g. USD, BRL, EUR, MXN)",
      "type": "string"
     }
    },
    "required": [
     "amount",
     "currency"
    ],
    "type": "object"
   },
   "metadata": {
    "description": "Arbitrary JSON you want echoed back on events (customer_id, order_id, etc.)",
    "type": "object"
   },
   "name": {
    "description": "Short product/order name shown on the hosted payment page",
    "type": "string"
   },
   "pricing_type": {
    "description": "fixed_price: exact amount in local_price. no_price: buyer chooses (donations).",
    "enum": [
     "fixed_price",
     "no_price"
    ],
    "type": "string"
   },
   "redirect_url": {
    "description": "Browser redirect after a successful payment",
    "type": "string"
   }
  },
  "required": [
   "name",
   "description",
   "pricing_type"
  ],
  "type": "object"
 },
 "name": "create_charge"
}
