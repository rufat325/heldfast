{
 "description": "Submit a version for App Review. Accepts either versionId directly OR appId+versionString to auto-resolve. Runs pre-flight checks before submitting.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "appId": {
    "description": "App ID (use with versionString instead of versionId)",
    "type": "string"
   },
   "platform": {
    "default": "IOS",
    "description": "Platform (when using versionString)",
    "enum": [
     "IOS",
     "MAC_OS",
     "TV_OS",
     "VISION_OS"
    ],
    "type": "string"
   },
   "versionId": {
    "description": "Version ID (provide this OR appId+versionString)",
    "type": "string"
   },
   "versionString": {
    "description": "Version string like \"1.6.0\" (use with appId instead of versionId)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "asc_submit_for_review"
}
