{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Browse / search the DATAcube collection of Slovak statistical cubes. Filter by case-insensitive substring of the English label. Returns each matching cube's code (use it as `cube` in the other tools), label, last update date, and its ordered list of dimension codes.",
 "inputSchema": {
  "examples": [
   {
    "query": "population"
   },
   {
    "lang": "en",
    "limit": 20,
    "query": "unemployment"
   }
  ],
  "properties": {
   "lang": {
    "description": "Label language (default \"en\").",
    "enum": [
     "en",
     "sk"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Max results to return (default 50, max 200).",
    "type": "number"
   },
   "query": {
    "description": "Case-insensitive substring to match against dataset labels (e.g. \"population\", \"unemployment\"). Omit to list everything.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_datasets",
 "title": "List Datasets",
 "version": "v1.1c8f97b6"
}
