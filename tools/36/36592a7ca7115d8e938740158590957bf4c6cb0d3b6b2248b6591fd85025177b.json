{
 "description": "QC-018 Bell measurement: certify that the presented Bell class bits equal the presented two-qubit pair. ab is the two measured bits (e.g. 00, 11) and class is the two class bits the caller claims; WIN when they agree, MISS naming both when they do not. A presented witness is verified; no state is prepared or sampled. Bit strings only; floats refused.",
 "inputSchema": {
  "properties": {
   "ab": {
    "description": "two measured bits, e.g. 00 or 11",
    "type": "string"
   },
   "class": {
    "description": "the two Bell-class bits claimed for the pair, e.g. 00",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "verify_bell_measurement"
}
