{
 "annotations": {
  "readOnlyHint": true,
  "title": "List connected playlists"
 },
 "description": "List your playlists from a connected streaming service (Spotify or Apple Music) so you can import songs from them. Requires that source to be connected (see vibo_get_me).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "limit": {
    "description": "Max items to return (default 20).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "description": "Filter playlists by name.",
    "type": "string"
   },
   "skip": {
    "description": "Number of items to skip, for paging (default 0).",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "source": {
    "description": "Streaming source \u2014 must be connected to your Vibo account.",
    "enum": [
     "spotify",
     "appleMusic"
    ],
    "type": "string"
   }
  },
  "required": [
   "source"
  ],
  "type": "object"
 },
 "name": "vibo_get_playlists"
}
