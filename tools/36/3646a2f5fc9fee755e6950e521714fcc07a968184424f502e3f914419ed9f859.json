{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List comparison invites the user has sent and each one's status: pending, clicked, started or completed. Read-only; costs 1 unit.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 20,
    "description": "Maximum rows to return (1-50, default 20).",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "status": {
    "default": "all",
    "description": "Filter by status.",
    "enum": [
     "all",
     "pending",
     "clicked",
     "started",
     "completed"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_invites",
 "title": "List invites"
}
