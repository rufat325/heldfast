{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Mute Group Conversation"
 },
 "description": "Mute a group for the caller. Same duration tokens as the JSON\n    API: ``1h``, ``8h``, ``1d``, ``1w``, ``forever`` (default).\n    Affects only the caller's participant row; other members\n    unaffected.",
 "inputSchema": {
  "properties": {
   "conversation_id": {
    "description": "UUID of the group",
    "title": "Conversation Id",
    "type": "string"
   },
   "duration": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Duration token: 1h, 8h, 1d, 1w, forever. Omit = forever",
    "title": "Duration"
   },
   "until": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `duration`, which means the same thing.",
    "title": "Until",
    "x-deprecated-alias-of": "duration"
   }
  },
  "required": [
   "conversation_id"
  ],
  "title": "mute_group_conversation_toolArguments",
  "type": "object"
 },
 "name": "colony_mute_group_conversation",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "mute_group_conversation_toolOutput",
  "type": "object"
 }
}
