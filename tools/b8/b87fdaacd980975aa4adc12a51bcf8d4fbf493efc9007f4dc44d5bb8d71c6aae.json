{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Daily figures for the components of the Hong Kong Monetary Base: Certificates of Indebtedness, government notes & coins in circulation, aggregate balance (before/after discount window), and outstanding Exchange Fund Bills & Notes. One record per business day. Use from/to to window the dates.",
 "inputSchema": {
  "examples": [
   {
    "from": "2024-01-01",
    "to": "2024-12-31"
   }
  ],
  "properties": {
   "fields": {
    "description": "Comma-separated subset of columns to return, e.g. \"end_of_date,hibor_overnight\".",
    "type": "string"
   },
   "from": {
    "description": "Start of date window, YYYY-MM-DD (or YYYY-MM for monthly series), e.g. \"2026-01-01\".",
    "type": "string"
   },
   "lang": {
    "description": "Language of text fields: \"en\" (default), \"tc\", or \"sc\". Required by some datasets (e.g. coin-cart-schedule).",
    "type": "string"
   },
   "offset": {
    "description": "Zero-based record offset for pagination.",
    "type": "integer"
   },
   "pagesize": {
    "description": "Records per page (default ~100). Max varies by dataset.",
    "type": "integer"
   },
   "sortby": {
    "description": "Column to sort by, e.g. \"end_of_date\".",
    "type": "string"
   },
   "sortorder": {
    "description": "Sort direction: \"asc\" or \"desc\".",
    "type": "string"
   },
   "to": {
    "description": "End of date window, YYYY-MM-DD (or YYYY-MM for monthly series), e.g. \"2026-01-31\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "hkma_monetary_base",
 "title": "Hkma Monetary Base",
 "version": "v1.54eaf208"
}
