{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch a URL and return a structured LLM brief: summary, key points, entities, topics, sentiment, notable facts \u2014 one call instead of fetch + reason. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "focus": {
    "description": "Optional angle to emphasize (e.g. \"pricing\", \"security\").",
    "type": "string"
   },
   "url": {
    "description": "The page URL to analyze (required).",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "agentbit.web_brief",
 "title": "Web Brief (page intelligence)"
}
