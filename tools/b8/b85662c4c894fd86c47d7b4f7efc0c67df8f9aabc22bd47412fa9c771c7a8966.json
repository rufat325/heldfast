{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Generate a custom avatar SVG. Specify a style (e.g., 'avataaars', 'pixel-art', 'lorelei') and seed (e.g., username). Returns the SVG URL ready to display.",
 "inputSchema": {
  "examples": [
   {
    "seed": "john_doe",
    "style": "avataaars"
   },
   {
    "seed": "user123",
    "style": "pixel-art"
   }
  ],
  "properties": {
   "seed": {
    "description": "A seed string that determines the avatar appearance. Same seed + style always produces the same avatar.",
    "type": "string"
   },
   "style": {
    "description": "The avatar style to use. Available styles: adventurer, avataaars, bottts, fun-emoji, identicon, initials, lorelei, micah, miniavs, notionists, open-peeps, personas, pixel-art, thumbs.",
    "type": "string"
   }
  },
  "required": [
   "style",
   "seed"
  ],
  "type": "object"
 },
 "name": "generate_avatar",
 "outputSchema": {
  "properties": {
   "format": {
    "description": "The format of the avatar (always SVG)",
    "enum": [
     "svg"
    ],
    "type": "string"
   },
   "seed": {
    "description": "The seed string that determines avatar appearance",
    "type": "string"
   },
   "style": {
    "description": "The avatar style that was used",
    "type": "string"
   },
   "url": {
    "description": "The SVG URL for the generated avatar",
    "type": "string"
   }
  },
  "required": [
   "style",
   "seed",
   "url",
   "format"
  ],
  "type": "object"
 },
 "title": "Generate Avatar",
 "version": "v1.df878bf0"
}
