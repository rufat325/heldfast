{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "What Statistics Sweden (SCB) is SCHEDULED to publish and when \u2014 the official publishing calendar (publiceringskalender), returning each upcoming release with its statistical product, product code, reference period, publishing form and publishing date. Answers \"what is SCB releasing next week\", \"n\u00e4r publicerar SCB KPI\", \"which Swedish statistics come out in September\". Also reads backwards over a date range you name, to list what was released between two dates. For the text of releases already out, use scb_se_statistical_news. Read from the SCB website calendar listing, so the field shapes depend on SCB markup rather than a published API contract.",
 "inputSchema": {
  "examples": [
   {
    "form": "statistical news",
    "from": "2026-08-07",
    "to": "2026-08-10"
   },
   {
    "limit": 3,
    "period": "Forward1Month"
   }
  ],
  "properties": {
   "form": {
    "description": "Publishing form: 0 all (default), 1 Database, 2 Publication, 3 Statistical news, 4 Tables and graphs. Words work too, e.g. \"statistical news\".",
    "type": "string"
   },
   "from": {
    "description": "Start date as YYYY-MM-DD. Supplying from/to selects the Custom period automatically; date ranges are honoured only in that mode.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum releases to return. Default 40, max 200.",
    "type": "number"
   },
   "period": {
    "description": "Forward1Week (default), Forward1Month, Forward3Months, Forward6Months, Forward1Year, Previous1Week, or Custom. Plain phrasings like \"next month\" are understood.",
    "type": "string"
   },
   "sort_field": {
    "description": "1 product name, 2 publishing date (default), 3 publisher.",
    "type": "string"
   },
   "sort_order": {
    "description": "0 ascending (default), 1 descending.",
    "type": "string"
   },
   "subject_areas": {
    "description": "Comma-separated two-letter SCB subject-area codes to restrict to, e.g. \"AM\" (labour market), \"PR\" (prices), \"BE\" (population), \"NR\" (national accounts).",
    "type": "string"
   },
   "to": {
    "description": "End date as YYYY-MM-DD. Required together with \"from\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "scb_se_publishing_calendar",
 "title": "Scb Se Publishing Calendar",
 "version": "v1.53612927"
}
