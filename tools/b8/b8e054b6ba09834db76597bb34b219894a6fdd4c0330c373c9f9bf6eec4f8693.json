{
 "description": "Import one validated .granoprototype package through the App-owned task Prototype/Version/Link aggregate and return exact SHA-256 readback.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dryRun": {
    "default": true,
    "type": "boolean"
   },
   "filePath": {
    "description": "Absolute path to a local .granoprototype package.",
    "minLength": 1,
    "type": "string"
   },
   "idempotencyKey": {
    "minLength": 1,
    "type": "string"
   },
   "prototypeId": {
    "minLength": 1,
    "type": "string"
   },
   "taskId": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "taskId",
   "filePath",
   "idempotencyKey"
  ],
  "type": "object"
 },
 "name": "granoflow_task_prototype_import"
}
