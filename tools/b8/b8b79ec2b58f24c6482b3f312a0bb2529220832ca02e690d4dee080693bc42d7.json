{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch deps.dev project metadata for a GitHub, GitLab, or Bitbucket repository (e.g. project_key=\"facebook/react\"), returning scorecard scores, version count, and linked package references.",
 "inputSchema": {
  "examples": [
   {
    "project_key": "facebook/react",
    "project_type": "github"
   },
   {
    "project_key": "gitlab-org/gitlab",
    "project_type": "gitlab"
   }
  ],
  "properties": {
   "project_key": {
    "description": "Plain \"owner/repo\", e.g. \"facebook/react\" \u2014 do not URL-encode it.",
    "type": "string"
   },
   "project_type": {
    "description": "github | gitlab | bitbucket",
    "type": "string"
   }
  },
  "required": [
   "project_type",
   "project_key"
  ],
  "type": "object"
 },
 "name": "project",
 "outputSchema": {
  "description": "Project metadata from deps.dev API",
  "properties": {},
  "type": "object"
 },
 "title": "Project",
 "version": "v1.3a0b8fca"
}
