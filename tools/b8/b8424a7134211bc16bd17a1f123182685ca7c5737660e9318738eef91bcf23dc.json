{
 "description": "Open or create the daily note for today (or a specific date). Daily notes follow the slug pattern `daily-YYYY-MM-DD` and are auto-linked to yesterday/tomorrow. Returns the existing page if already created, marking `created: false`.",
 "inputSchema": {
  "properties": {
   "date": {
    "description": "ISO date YYYY-MM-DD (default: today server-local)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "create_daily_note"
}
