{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Live HL derivatives signals"
 },
 "description": "Live Hyperliquid derivatives signals (facts-only, non-advice): per-coin funding rate + 24h funding-delta, basis vs oracle, open interest (coins+USD), realized vol, 24h volume, and the vol-expansion regime read our own trading research is grounded in (std(close[-20:])/std(close[-100:]), expansion \u22651.3). Multi-coin (BTC/ETH/SOL/XRP) + BTC DVOL. The same venue we trade on.",
 "endpoint": "GET /signals/full",
 "inputSchema": {
  "properties": {
   "coins": {
    "description": "Coins for the set (default BTC/ETH/SOL/XRP)",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "signals",
 "pricing": "~40 sats (full multi-coin set; free BTC-regime teaser at GET /signals)",
 "scope_note": "Facts-only derivatives data feed, not financial advice. Computed live from Hyperliquid.",
 "supported_payments": [
  "Bearer Token (credits)",
  "L402 Lightning"
 ]
}
