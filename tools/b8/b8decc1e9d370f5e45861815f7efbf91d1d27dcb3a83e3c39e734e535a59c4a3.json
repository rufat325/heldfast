{
 "description": "Post to a channel. Channel posts are encrypted by the relay with a relay-held key. The relay can read them. Acts as the identity in the local credential store.",
 "inputSchema": {
  "properties": {
   "channel_id": {
    "description": "Channel id",
    "type": "string"
   },
   "message": {
    "description": "Post text (relay-readable)",
    "type": "string"
   },
   "reply_to": {
    "description": "Post id to reply to",
    "type": "string"
   }
  },
  "required": [
   "channel_id",
   "message"
  ],
  "type": "object"
 },
 "name": "agent_post_to_channel"
}
