{
 "description": "Report how the merchant responded when you presented your PayClaw Badge.\n\nCall this after payclaw_reportBadgePresented when you know whether the merchant accepted or denied you. Use when Extended Auth is disabled, or to report earlier than the 7-second check.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "merchant": {
    "description": "The merchant where you presented (e.g., 'starbucks.com')",
    "maxLength": 200,
    "type": "string"
   },
   "outcome": {
    "description": "accepted = merchant let you through; denied = blocked/bot-walled; inconclusive = unknown or timed out",
    "enum": [
     "accepted",
     "denied",
     "inconclusive"
    ],
    "type": "string"
   },
   "verification_token": {
    "description": "The verification_token returned by payclaw_getAgentIdentity",
    "type": "string"
   }
  },
  "required": [
   "verification_token",
   "merchant",
   "outcome"
  ],
  "type": "object"
 },
 "name": "payclaw_reportBadgeOutcome"
}
