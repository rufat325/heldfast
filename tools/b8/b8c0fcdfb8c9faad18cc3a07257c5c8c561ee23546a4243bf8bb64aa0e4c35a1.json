{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List messages in a conversation"
 },
 "description": "List messages in a conversation. The transcript, newest first. Purged bodies appear with text null and body_purged true.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Numeric id.",
    "type": "integer"
   },
   "limit": {
    "default": 50,
    "description": "1 to 100, default 50.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "starting_after": {
    "description": "The next_cursor from the previous page.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "list_messages"
}
