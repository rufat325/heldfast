{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Query records from a Qatar government open data dataset with ODSQL. Filter (where), aggregate (group_by/select), sort (order_by), paginate (limit/offset). Field labels come back as label_en/label_ar \u2014 dataset_ids and field names stay in their native form.",
 "inputSchema": {
  "examples": [
   {
    "dataset_id": "percentage-distribution-of-population-by-sex-and-municipality-census-2015",
    "limit": 50,
    "where": "gender=\"Males\""
   },
   {
    "dataset_id": "percentage-distribution-of-population-by-sex-and-municipality-census-2015",
    "group_by": "gender",
    "select": "sum(number_of_population_dd_lskn) as total"
   }
  ],
  "properties": {
   "dataset_id": {
    "description": "Dataset id from search_datasets.",
    "type": "string"
   },
   "group_by": {
    "description": "ODSQL group_by field(s).",
    "type": "string"
   },
   "limit": {
    "description": "Max records (1-100, default 20).",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset (default 0).",
    "type": "number"
   },
   "order_by": {
    "description": "Sort, e.g. `year desc`.",
    "type": "string"
   },
   "query": {
    "description": "Free-text keyword across all fields (optional).",
    "type": "string"
   },
   "select": {
    "description": "ODSQL select/aggregation, e.g. `count(*) as n, sum(amount)`.",
    "type": "string"
   },
   "where": {
    "description": "ODSQL filter, e.g. `year >= 2020` (overrides query).",
    "type": "string"
   }
  },
  "required": [
   "dataset_id"
  ],
  "type": "object"
 },
 "name": "query",
 "title": "Query",
 "version": "v1.1269b164"
}
