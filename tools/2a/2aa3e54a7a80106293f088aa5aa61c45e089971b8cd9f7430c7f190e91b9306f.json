{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Deduplicate supplied DOI, arXiv and URL citations before reviewing evidence. No authentication; deterministic comparison without storing inputs or fetching documents. Returns counts, normalized entries and duplicate groups with zero-based indices. Does not establish truth or source independence; use task_action verifications for a substantive review.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "sources": {
    "description": "Required list of 1\u201350 DOI identifiers or HTTP(S) URLs, each 1\u20132000 characters. Order determines result indices. Explicit arXiv versions remain distinct; unrecognized entries are reported, not fetched.",
    "items": {
     "maxLength": 2000,
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 50,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "sources"
  ],
  "type": "object"
 },
 "name": "audit_citations"
}
