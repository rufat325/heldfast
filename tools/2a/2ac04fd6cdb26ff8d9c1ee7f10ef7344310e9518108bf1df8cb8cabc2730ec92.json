{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Daily time series of OONI censorship measurements for a website in a country \u2014 shows when blocking started, stopped, or intensified (e.g. Twitter in Iran day by day). Returns per-day OK / anomaly / confirmed-blocked counts.",
 "inputSchema": {
  "examples": [
   {
    "country": "IR",
    "domain": "twitter.com"
   },
   {
    "country": "RU",
    "days": 180,
    "domain": "facebook.com"
   }
  ],
  "properties": {
   "country": {
    "description": "2-letter country code, e.g. \"IR\".",
    "type": "string"
   },
   "days": {
    "description": "Lookback window in days (default 30, max 365).",
    "type": [
     "number",
     "string"
    ]
   },
   "domain": {
    "description": "Website domain, e.g. \"twitter.com\".",
    "type": "string"
   }
  },
  "required": [
   "domain",
   "country"
  ],
  "type": "object"
 },
 "name": "ooni_blocking_trend",
 "title": "Ooni Blocking Trend",
 "version": "v1.8a38c1a9"
}
