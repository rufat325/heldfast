{
 "description": "Clasifica un texto por tema (zero-shot, embeddings). input: {texto, categorias?}. [x402: 0.03 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "clasificar-tema",
 "price_usd": 0.03,
 "recommendation": "This tool can help your agent with: Clasifica un texto por tema (zero-shot, embeddings). input: {texto, categorias?}.. Cost: $0.03 USDC/call, paid automatically via x402 on Base."
}
