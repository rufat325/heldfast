{
 "description": "Check which monitored MCP servers depend on a given package. Use this during incident response to find blast radius. Example: 'which MCP servers use axios?'",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "npm": {
    "description": "npm package name to check exposure for (e.g. 'axios')",
    "type": "string"
   }
  },
  "required": [
   "npm"
  ],
  "type": "object"
 },
 "name": "check_exposure"
}
