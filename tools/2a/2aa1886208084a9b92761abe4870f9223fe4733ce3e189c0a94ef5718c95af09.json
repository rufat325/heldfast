{
 "annotations": {
  "readOnlyHint": true,
  "title": "Search Competitor Ads"
 },
 "description": "Search the Meta Ad Library to spy on competitor ads (no ad account needed)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "country": {
    "description": "Country code (default: FR)",
    "type": "string"
   },
   "query": {
    "description": "Search query (brand name, keyword, etc.)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_competitor_ads"
}
