{
 "_meta": {
  "apiVersion": "1.0.0",
  "examples": [
   {
    "input": {
     "affectedPackages": [
      "zipnative",
      "zipnative-mcp"
     ],
     "duplicateSearchPerformed": true,
     "expectedBehavior": "The entry is read and its CRC verified.",
     "issueType": "bug",
     "reproduction": {
      "command": "scan_zip_forward on an archive written with compression.method:'store' and stream:true",
      "result": "ZIP_STREAM_TRUNCATED"
     },
     "summary": "A stored entry followed by a signed data descriptor is reported as truncated by the forward reader.",
     "targetRepo": "zipnative",
     "title": "iterateZipEntries mis-detects the descriptor of a stored entry"
    },
    "title": "Draft an engine bug report (never submitted)"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Produce a LOCAL GitHub issue draft + compliance report for zipnative / zipnative-mcp and NEVER submit it \u2014 the agent drafts, a human reviews and files it under their own identity (Human-In-The-Loop contract, .github/AGENT_RULES.md). No GitHub write path and no network path exist in this server. The draft is checked against the zero-dependency + reproduction policy; a violation (new runtime dependency, missing reproduction, duplicateSearchPerformed:false) throws GOVERNANCE_VIOLATION. targetRepo 'zipnative' for engine behaviour (parsing, writing, limits, codecs), 'zipnative-mcp' for wrapper behaviour (schemas, sandbox, transport). Returns markdown inline (outputMode:'file' also writes a .md inside the sandbox). Use it only after reproducing a defect \u2014 for questions read describe_engine / AGENTS.md instead. Present the draft AND the report to the user, then STOP.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "actualBehavior": {
    "description": "What actually happened (defaults to the reproduction result when omitted).",
    "type": "string"
   },
   "affectedPackages": {
    "default": [
     "zipnative-mcp"
    ],
    "description": "Packages impacted by this issue (e.g. [\"zipnative-mcp\"], [\"zipnative\"]).",
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 16,
    "type": "array"
   },
   "duplicateSearchPerformed": {
    "description": "MUST be true: confirms you searched open AND closed issues/PRs for duplicates before drafting.",
    "type": "boolean"
   },
   "expectedBehavior": {
    "description": "What you expected to happen instead.",
    "minLength": 4,
    "type": "string"
   },
   "issueType": {
    "description": "Issue category: 'bug' | 'feature' | 'security' | 'docs' | 'performance'.",
    "enum": [
     "bug",
     "feature",
     "security",
     "docs",
     "performance"
    ],
    "type": "string"
   },
   "outputMode": {
    "default": "inline",
    "description": "'inline' (default) returns the draft markdown in the response. 'file' additionally writes it to the sandbox (requires ZIPNATIVE_MCP_OUTPUT_DIR); outputPath must be a relative .md path.",
    "enum": [
     "inline",
     "file"
    ],
    "type": "string"
   },
   "outputPath": {
    "description": "Relative .md path inside the sandbox (only when outputMode='file').",
    "type": "string"
   },
   "reproduction": {
    "additionalProperties": false,
    "description": "The minimal, locally-executed reproduction that justifies this issue.",
    "properties": {
     "command": {
      "description": "The exact command or MCP tool call you ran.",
      "minLength": 1,
      "type": "string"
     },
     "result": {
      "description": "The observed failure, error, or regression.",
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "command",
     "result"
    ],
    "type": "object"
   },
   "summary": {
    "description": "One or two paragraphs describing the problem or proposal.",
    "minLength": 16,
    "type": "string"
   },
   "targetRepo": {
    "default": "zipnative-mcp",
    "description": "Destination repository label for the draft (documentation only \u2014 the server never contacts it). 'zipnative-mcp' (wrapper: schemas, sandbox, transport) or 'zipnative' (engine: parser, writer, limits, codecs).",
    "maxLength": 100,
    "minLength": 1,
    "type": "string"
   },
   "title": {
    "description": "Concise issue title (imperative, no trailing period).",
    "maxLength": 160,
    "minLength": 8,
    "type": "string"
   }
  },
  "required": [
   "title",
   "summary",
   "issueType",
   "reproduction",
   "expectedBehavior",
   "duplicateSearchPerformed"
  ],
  "type": "object"
 },
 "name": "draft_governance_issue",
 "outputSchema": {
  "additionalProperties": false,
  "description": "A local, unsubmitted issue draft plus its compliance report. The server NEVER opens the issue \u2014 a human must review and submit it.",
  "properties": {
   "compliance": {
    "additionalProperties": false,
    "properties": {
     "affectedPackages": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "duplicateSearchPerformed": {
      "type": "boolean"
     },
     "environment": {
      "additionalProperties": false,
      "properties": {
       "node": {
        "type": "string"
       },
       "os": {
        "type": "string"
       },
       "zipnativeMcp": {
        "type": "string"
       }
      },
      "required": [
       "node",
       "os",
       "zipnativeMcp"
      ],
      "type": "object"
     },
     "humanGate": {
      "type": "string"
     },
     "identityReminderShown": {
      "type": "boolean"
     },
     "reproductionCommand": {
      "type": "string"
     },
     "reproductionResult": {
      "type": "string"
     },
     "zeroDependencyConfirmed": {
      "type": "boolean"
     }
    },
    "required": [
     "zeroDependencyConfirmed",
     "reproductionCommand",
     "reproductionResult",
     "duplicateSearchPerformed",
     "affectedPackages",
     "identityReminderShown",
     "humanGate",
     "environment"
    ],
    "type": "object"
   },
   "draftMarkdown": {
    "description": "The full draft, ready for a human to review and submit.",
    "type": "string"
   },
   "filePath": {
    "description": "Sandboxed absolute path (when outputMode='file').",
    "type": "string"
   },
   "issueType": {
    "enum": [
     "bug",
     "feature",
     "security",
     "docs",
     "performance"
    ],
    "type": "string"
   },
   "outputMode": {
    "enum": [
     "inline",
     "file"
    ],
    "type": "string"
   },
   "sizeBytes": {
    "minimum": 0,
    "type": "integer"
   },
   "targetRepo": {
    "type": "string"
   },
   "title": {
    "type": "string"
   },
   "warnings": {
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "title",
   "issueType",
   "targetRepo",
   "outputMode",
   "sizeBytes",
   "draftMarkdown",
   "warnings",
   "compliance"
  ],
  "type": "object"
 },
 "title": "Draft a governance-compliant GitHub issue (HITL)"
}
