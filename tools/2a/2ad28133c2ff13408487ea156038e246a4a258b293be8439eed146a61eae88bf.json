{
 "description": "Delete a folder/notebook from Joplin (requires confirmation)",
 "inputSchema": {
  "properties": {
   "confirm": {
    "description": "Confirmation flag",
    "type": "boolean"
   },
   "folder_id": {
    "description": "ID of the folder to delete",
    "type": "string"
   },
   "force": {
    "description": "Force delete even if folder has contents",
    "type": "boolean"
   }
  },
  "required": [
   "folder_id"
  ],
  "type": "object"
 },
 "name": "delete_folder"
}
