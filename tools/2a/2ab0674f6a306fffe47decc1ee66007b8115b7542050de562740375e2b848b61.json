{
 "annotations": {
  "readOnlyHint": true,
  "title": "Get X following"
 },
 "description": "Accounts the user follows on X (Twitter), from the synced follow graph \u2014 name, @handle, and bio. `query` filters by name, handle, or bio text ('who do I follow in AI?'). Reads the last sync, so a just-followed account may be missing; total_followed is the synced total. If it reports no account connected, give the user the link it returns.",
 "inputSchema": {
  "properties": {
   "handle": {
    "description": "One connected @handle whose follows to read. Omit for all connected accounts.",
    "type": "string"
   },
   "limit": {
    "description": "Max accounts to return (default 50, max 200).",
    "type": "integer"
   },
   "query": {
    "description": "Case-insensitive filter on name, handle, or bio.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "get_twitter_following",
 "title": "Get X following"
}
