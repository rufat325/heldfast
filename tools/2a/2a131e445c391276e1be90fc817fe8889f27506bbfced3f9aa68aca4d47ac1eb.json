{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Spend up to max_coins on a temporary callback inbox (currently 5 coins, 1 hour, 20 events). Reuse request_key on retries. Receive URL is write-only; reads need the utility key.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "title": "Api Key",
    "type": "string"
   },
   "max_coins": {
    "title": "Max Coins",
    "type": "integer"
   },
   "request_key": {
    "title": "Request Key",
    "type": "string"
   }
  },
  "required": [
   "api_key",
   "request_key",
   "max_coins"
  ],
  "title": "create_callback_inboxArguments",
  "type": "object"
 },
 "name": "create_callback_inbox",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "create_callback_inboxOutput",
  "type": "object"
 }
}
