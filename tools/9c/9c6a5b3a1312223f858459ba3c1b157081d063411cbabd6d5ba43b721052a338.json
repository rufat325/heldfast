{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Generate campaign analytics report"
 },
 "description": "Generate the monthly campaign report of the current brand for a calendar month (year + month). Executes 13 parallel queries in one call: marketing campaign metrics for that month and the previous one, ROI and attribution, revenue, variable costs, 24-month purchase history, transactional campaigns, segments and industry benchmarks. For a single campaign use nevent_get_campaign_insights.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "month": {
    "description": "Calendar month of the report, 1 (January) to 12 (December)",
    "maximum": 12,
    "minimum": 1,
    "type": "integer"
   },
   "year": {
    "description": "Calendar year of the monthly report, e.g. 2026",
    "maximum": 2100,
    "minimum": 2000,
    "type": "integer"
   }
  },
  "required": [
   "year",
   "month"
  ],
  "type": "object"
 },
 "name": "nevent_campaign_report"
}
