{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Dry-run edits on the workbook's temp branch and report what they would do. Nothing is committed \u2014 the branch is discarded, so this is the safe way to explore a model instead of changing it and putting it back.\n\nTwo shapes. `changes` runs one hypothetical and returns every cell that would move, direct writes and cascaded recalculations alike. `scenarios` runs several, each on its own branch, and returns one result per scenario in order \u2014 that is a sensitivity table or a scenario comparison in a single call.\n\nAdd `watch` to get just the cells you care about instead of the whole cascade. A grid of sixteen scenarios over a model that cascades into 26 cells is 416 rows of diff to answer sixteen questions; `watch` makes it sixteen numbers. Name a cell semantically (`{block, row_key, field}`) or by coordinate (`{sheet_idx, row, col}`).",
 "inputSchema": {
  "properties": {
   "changes": {
    "description": "One hypothetical. Use `scenarios` for more than one.",
    "items": {
     "properties": {
      "block": {
       "type": "string"
      },
      "field": {
       "type": "string"
      },
      "row_key": {
       "description": "Value of the block's key column.",
       "type": "string"
      },
      "value": {
       "description": "Cell value. Prefix with \"=\" to write a formula. null clears.",
       "type": [
        "string",
        "number",
        "boolean",
        "null"
       ]
      }
     },
     "required": [
      "block",
      "row_key",
      "field",
      "value"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "scenarios": {
    "description": "Several hypotheticals, each evaluated independently. Results come back in this order.",
    "items": {
     "properties": {
      "changes": {
       "items": {
        "properties": {
         "block": {
          "type": "string"
         },
         "field": {
          "type": "string"
         },
         "row_key": {
          "description": "Value of the block's key column.",
          "type": "string"
         },
         "value": {
          "description": "Cell value. Prefix with \"=\" to write a formula. null clears.",
          "type": [
           "string",
           "number",
           "boolean",
           "null"
          ]
         }
        },
        "required": [
         "block",
         "row_key",
         "field",
         "value"
        ],
        "type": "object"
       },
       "minItems": 1,
       "type": "array"
      },
      "label": {
       "description": "Echoed back on the result, so a grid is easy to line up.",
       "type": "string"
      }
     },
     "required": [
      "changes"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "watch": {
    "description": "Report only these cells' values instead of the full diff. Name each one semantically or by coordinate.",
    "items": {
     "properties": {
      "block": {
       "type": "string"
      },
      "col": {
       "type": "integer"
      },
      "field": {
       "type": "string"
      },
      "row": {
       "type": "integer"
      },
      "row_key": {
       "type": "string"
      },
      "sheet_idx": {
       "default": 0,
       "type": "integer"
      }
     },
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "preview_changes"
}
