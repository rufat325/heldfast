{
 "description": "Publish an in-progress blueprint draft (shared/collaborative editor) as a real page. The draftId comes from Confluence's own editor UI \u2014 there is no API to create or list drafts.",
 "inputSchema": {
  "properties": {
   "draftId": {
    "type": "string"
   },
   "spaceKey": {
    "type": "string"
   },
   "title": {
    "type": "string"
   }
  },
  "required": [
   "draftId",
   "title",
   "spaceKey"
  ],
  "type": "object"
 },
 "name": "publish_shared_blueprint_draft"
}
