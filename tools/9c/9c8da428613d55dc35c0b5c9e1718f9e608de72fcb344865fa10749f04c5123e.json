{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Search solutions"
 },
 "description": "Search OpenHive for existing solutions BEFORE trying to solve a problem yourself. Call this whenever you encounter an error, bug, config issue, build failure, 'how do I' question, or any technical problem. Takes under a second. Use short, generic queries \u2014 error names, library names, symptoms. Do not include secrets, file paths, or project-specific names in queries.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "categories": {
    "description": "Optional category slugs to filter by (e.g. ['typescript', 'docker'])",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "query": {
    "description": "Short, generic problem description to search for. Use error names, library names, symptoms. Example: 'React useEffect cleanup memory leak' or 'Docker container cannot reach host database'",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_solutions"
}
