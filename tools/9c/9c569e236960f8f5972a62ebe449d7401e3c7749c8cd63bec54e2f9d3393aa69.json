{
 "description": "Get detailed info about epochs (current or historical).",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Number of epochs to return (default: 5)",
    "type": "number"
   },
   "order": {
    "description": "Sort order (default: descending)",
    "enum": [
     "ascending",
     "descending"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_epoch_info"
}
