{
 "description": "Search otwarteAPI.pl, a catalog of public Polish and EU APIs (VAT, KRS, GUS, NBP, ECB, Eurostat and more). Returns matching entries with a link to each API's official docs. An empty query returns the whole catalog (optionally narrowed by scope or topic).",
 "inputSchema": {
  "properties": {
   "temat": {
    "description": "Narrow to a specific catalog tag, e.g. \"firmy\" or \"dane-statystyczne\". Optional.",
    "type": "string"
   },
   "zapytanie": {
    "description": "Natural-language query, e.g. \"euro exchange rate\" or \"weather data\". Optional.",
    "type": "string"
   },
   "zasieg": {
    "description": "Narrow to Polish (PL) or EU APIs. Optional.",
    "enum": [
     "PL",
     "EU"
    ],
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "szukaj_katalog_api"
}
