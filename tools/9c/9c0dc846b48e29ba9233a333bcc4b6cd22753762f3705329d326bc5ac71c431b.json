{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true,
  "title": "Read a task's full activity timeline"
 },
 "description": "Read the merged, newest-first activity timeline for a task: progress notes, comments, status changes, handoffs, artifacts and escalations. Use this when get_task's `activity` window (30 entries) isn't enough, or to filter to a single kind of entry. This is the shared surface teammates write to with add_progress_note and add_comment.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "description": "Entries per page. Default 50.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Skip this many entries for paging.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "task_id": {
    "description": "Task id, or a pasted Tango task URL.",
    "type": "string"
   },
   "types": {
    "description": "Optional event types to include, e.g. ['progress_note','comment','status_changed'].",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "task_id"
  ],
  "type": "object"
 },
 "name": "get_task_activity",
 "title": "Read a task's full activity timeline"
}
