{
 "description": "\"accept\" writes a supersede link from candidateId to againstId (the same effect as \"nexusmem mark-stale\") -- the ranker down-weights candidateId from then on but never deletes it. \"dismiss\" silences the suggestion without changing ranking, so it stops resurfacing on future stale/sync runs.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "action": {
    "description": "Whether to write the supersede link or silence the suggestion",
    "enum": [
     "accept",
     "dismiss"
    ],
    "type": "string"
   },
   "againstId": {
    "description": "Id of the superseding node. Required for \"accept\", ignored for \"dismiss\".",
    "type": "string"
   },
   "candidateId": {
    "description": "Id of the stale/superseded node",
    "type": "string"
   },
   "projectRoot": {
    "description": "Absolute path to the repository root",
    "type": "string"
   }
  },
  "required": [
   "projectRoot",
   "candidateId",
   "action"
  ],
  "type": "object"
 },
 "name": "resolve_stale_suggestion",
 "title": "Accept or dismiss a contradiction suggestion"
}
