{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search/list COVID-19 tracked locations (countries, states/provinces, counties, localities) by free-text name, ISO country code, and/or administrative aggregation level. Returns each location's key, names, and level \u2014 use the returned location_key with global-health-covid.latest_snapshot or global-health-covid.location_history. Data: Google \"COVID-19 Open Data\" (storage.googleapis.com/covid19-open-data), no auth required. HISTORICAL DATA ONLY \u2014 frozen since 2022-09-15.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "aggregation_level": {
    "description": "Filter by administrative level: \"0\"=country, \"1\"=state/province, \"2\"=county, \"3\"=locality/city.",
    "enum": [
     "0",
     "1",
     "2",
     "3"
    ],
    "type": "string"
   },
   "country_code": {
    "description": "Filter to a single 2-letter ISO country code (e.g. \"US\", \"DE\").",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   },
   "query": {
    "description": "Free-text match against country, subregion, or locality name (e.g. \"California\", \"Germany\"). Case-insensitive substring match. Omit to browse by country_code/aggregation_level only.",
    "maxLength": 80,
    "minLength": 1,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "global-health-covid.epidemiology.location_search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "COVID-19 Location Search"
}
