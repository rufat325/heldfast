{
 "description": "List the addresses this organization receives on. Up to 100, newest first. A disabled address stays listed: its history is still readable.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_inboxes"
}
