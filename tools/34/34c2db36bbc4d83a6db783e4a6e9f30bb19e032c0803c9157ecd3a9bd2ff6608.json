{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Cheap generic EVM reads on Base for AI agents: native balance, nonce, contract code, ERC-20 token balance, eth_call, gas price, latest block, and transaction/receipt lookups. One call, read-only, $0.001. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "address": {
    "description": "EVM address (0x + 40 hex) \u2014 for balance/nonce/code/token_balance (the holder)",
    "type": "string"
   },
   "data": {
    "description": "0x-prefixed hex calldata for method=call",
    "type": "string"
   },
   "hash": {
    "description": "Transaction hash (0x + 64 hex) for method=tx/receipt",
    "type": "string"
   },
   "method": {
    "description": "Which read to perform",
    "enum": [
     "balance",
     "nonce",
     "code",
     "block_number",
     "gas_price",
     "call",
     "token_balance",
     "tx",
     "receipt"
    ],
    "type": "string"
   },
   "to": {
    "description": "Contract address for method=call",
    "type": "string"
   },
   "token": {
    "description": "ERC-20 contract address for method=token_balance",
    "type": "string"
   }
  },
  "required": [
   "method"
  ],
  "type": "object"
 },
 "name": "agentbit.onchain_read",
 "title": "Onchain Read (EVM / Base)"
}
