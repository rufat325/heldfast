{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Domain registration record from RDAP \u2014 registrar, registration/expiry dates, nameservers, status; a \"domain not found\" answer means the name is UNREGISTERED (available to register, subject to registrar checks). Keyless and authoritative (the registry's own service; the modern replacement for WHOIS). Covers gTLDs and ccTLDs including .io, .me, .de, .us, .ch, .sh and .ac, which IANA's bootstrap file omits. PREFER for \"who is the registrar for <domain>\", \"when does <domain> expire\", \"who owns <domain>\", \"WHOIS for <domain>\", \"is <domain>.com available/registered\".",
 "inputSchema": {
  "examples": [
   {
    "name": "example.com"
   },
   {
    "name": "google.org"
   },
   {
    "domain": "pipeworx.io"
   },
   {
    "base": "https://rdap.nic.ch",
    "domain": "nic.ch"
   }
  ],
  "properties": {
   "base": {
    "description": "Optional registry RDAP base URL, e.g. \"https://rdap.verisign.com/com/v1\". Skips IANA bootstrap lookup \u2014 use it when a TLD comes back as tld_not_in_iana_bootstrap and you know the registry endpoint.",
    "type": "string"
   },
   "domain": {
    "description": "Alias for `name`.",
    "type": "string"
   },
   "name": {
    "description": "Domain name, e.g. \"openai.com\". Also accepted as `domain`.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "domain",
 "outputSchema": {
  "description": "RDAP domain registration record",
  "type": "object"
 },
 "title": "Domain",
 "version": "v1.afd1c187"
}
