{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "Restore an archived worker"
 },
 "description": "Reverse archive_worker and put the worker back into active service. Subject to your organization's worker quota \u2014 if restoring it would exceed the plan limit the call fails and nothing changes.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "worker_id": {
    "format": "uuid",
    "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
    "type": "string"
   }
  },
  "required": [
   "worker_id"
  ],
  "type": "object"
 },
 "name": "unarchive_worker",
 "title": "Restore an archived worker"
}
