{
 "description": "Create a backup of a backing image. Longhorn does not publish a dedicated input schema for this action in /v1/schemas \u2014 use longhorn_describe_resource_type('backingImage') to check the deployed version's exact body shape, or pass fields via extra. Longhorn docs: https://longhorn.io/docs/1.12.1/advanced-resources/backing-image/",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "extra": {
    "additionalProperties": {},
    "description": "Additional Longhorn fields, merged verbatim into the request body.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "name": {
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "backingimage_backup_create",
 "title": "Back up a backing image"
}
