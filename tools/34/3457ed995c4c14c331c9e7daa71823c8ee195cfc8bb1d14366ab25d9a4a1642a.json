{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Reorder a block by moving one row to a new position, addressing both the row and its destination by key. Moves to the end when neither after_key nor before_key is given (same default as add_block_rows). Row order inside a block is presentation only \u2014 formulas address rows by key, so reordering never changes a single computed value. Use it to match a reading order someone expects, not to change the model.",
 "inputSchema": {
  "properties": {
   "after_key": {
    "description": "Land the row directly after the row with this key. Mutually exclusive with before_key.",
    "type": "string"
   },
   "before_key": {
    "description": "Land the row directly before the row with this key. Pass the first key to move it to the top. Mutually exclusive with after_key.",
    "type": "string"
   },
   "block": {
    "description": "Block ref name.",
    "type": "string"
   },
   "key": {
    "description": "Row key of the row to move.",
    "type": "string"
   }
  },
  "required": [
   "block",
   "key"
  ],
  "type": "object"
 },
 "name": "move_block_row"
}
