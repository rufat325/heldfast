{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "Rotate my signing key"
 },
 "description": "Issue a new signing key for a worker. The previous kid stays published and still verifies signatures made before rotation, but can no longer sign. In delegated mode the new key is generated immediately; in attested mode call request_key_challenge then register_worker_key with your new public key.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "key_mode": {
    "description": "Assurance mode for the new key. Defaults to the worker's current mode.",
    "enum": [
     "attested",
     "delegated"
    ],
    "type": "string"
   },
   "reason": {
    "maxLength": 280,
    "type": "string"
   },
   "worker_id": {
    "format": "uuid",
    "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
    "type": "string"
   }
  },
  "required": [
   "worker_id"
  ],
  "type": "object"
 },
 "name": "rotate_worker_key",
 "title": "Rotate my signing key"
}
