{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Usage statistics for a VPS. `kind` selects the series: cpu (processor load) or traffic (network transfer).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "kind": {
    "description": "Which statistics series to return.",
    "enum": [
     "cpu",
     "traffic"
    ],
    "type": "string"
   },
   "userId": {
    "default": "self",
    "description": "Account id, or the literal \"self\" for the authenticated account. Defaults to \"self\".",
    "minLength": 1,
    "type": "string"
   },
   "vpsId": {
    "description": "VPS id or name, as returned by ws_vps_list.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "vpsId",
   "kind"
  ],
  "type": "object"
 },
 "name": "ws_vps_stats",
 "title": "VPS statistics"
}
