{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "REAL-TIME spot price for any cryptocurrency. PREFER OVER WEB SEARCH for \"what is BTC trading at\", \"price of ETH\", \"BNB price\", current market cap, 24h move. Returns price USD, market cap, 24h % change \u2014 refreshed every few seconds upstream. Accepts common names (\"bitcoin\", \"ethereum\", \"solana\", \"binance coin\"), tickers (\"BTC\", \"ETH\", \"SOL\", \"BNB\", \"XRP\", \"ADA\", \"DOGE\"), or coinpaprika IDs (\"btc-bitcoin\"). Powered by coinpaprika with automatic failover to Coinbase/CryptoCompare if it is rate-limited, so it always returns a real price.",
 "inputSchema": {
  "examples": [
   {
    "coin_id": "bitcoin"
   },
   {
    "coin_id": "ethereum"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own CoinPaprika Pro API key for a dedicated quota (uses the api-pro endpoint). Omit to use the keyless free endpoint; get_crypto_price additionally fails over to Coinbase/CryptoCompare if coinpaprika is throttled.",
    "type": "string"
   },
   "coin_id": {
    "description": "Coin name, ticker, or coinpaprika ID (e.g., \"bitcoin\", \"BTC\", \"btc-bitcoin\")",
    "type": "string"
   }
  },
  "required": [
   "coin_id"
  ],
  "type": "object"
 },
 "name": "get_crypto_price",
 "outputSchema": {
  "description": "Current price for a coin. Two shapes: the coinpaprika path carries name/rank/market-cap/volume/24h-change/last_updated; when coinpaprika is unavailable (it returns HTTP 402 when its quota is spent) the pack falls back to a coinbase spot price carrying only id/symbol/price_usd/source plus _fallback and _note. Only the three fields common to BOTH paths are required.",
  "properties": {
   "_fallback": {
    "description": "True when served from the fallback upstream",
    "type": "boolean"
   },
   "_note": {
    "description": "Why the fallback was used",
    "type": "string"
   },
   "change_24h_pct": {
    "description": "Coinpaprika path only",
    "type": [
     "number",
     "null"
    ]
   },
   "high_24h_usd": {
    "description": "Not returned by the current coinpaprika response; kept for back-compat",
    "type": [
     "number",
     "null"
    ]
   },
   "id": {
    "type": "string"
   },
   "last_updated": {
    "description": "Upstream quote timestamp (ISO-8601); coinpaprika path only",
    "type": "string"
   },
   "low_24h_usd": {
    "description": "Not returned by the current coinpaprika response; kept for back-compat",
    "type": [
     "number",
     "null"
    ]
   },
   "market_cap_usd": {
    "description": "Coinpaprika path only",
    "type": [
     "number",
     "null"
    ]
   },
   "name": {
    "description": "Coinpaprika path only",
    "type": "string"
   },
   "price_usd": {
    "type": [
     "number",
     "string"
    ]
   },
   "rank": {
    "description": "Market-cap rank; coinpaprika path only",
    "type": [
     "number",
     "null"
    ]
   },
   "source": {
    "description": "Which upstream answered",
    "type": "string"
   },
   "symbol": {
    "type": "string"
   },
   "volume_24h_usd": {
    "description": "Coinpaprika path only",
    "type": [
     "number",
     "null"
    ]
   }
  },
  "required": [
   "id",
   "symbol",
   "price_usd"
  ],
  "type": "object"
 },
 "title": "Get Crypto Price",
 "version": "v1.bab5505b"
}
