{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "List kanban transition events for a project, newest first. Use `since` to fetch only events after a timestamp (for reconnect-backfill).",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max events to return (default 200, max 1000)",
    "type": "number"
   },
   "projectId": {
    "type": "string"
   },
   "since": {
    "description": "ISO timestamp; only events strictly after this are returned",
    "type": "string"
   }
  },
  "required": [
   "projectId"
  ],
  "type": "object"
 },
 "name": "vibemap_list_kanban_events"
}
