{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false,
  "title": "Send a WhatsApp template"
 },
 "description": "Send a pre-approved WhatsApp template message into a conversation. Use this to reach a contact outside the 24-hour window. Call list_whatsapp_templates first to get the exact template name and language, and to see how many {{n}} variables it needs. Requires an existing conversation card \u2014 starting a conversation with a number that has never messaged the business is not supported. Confirm with the user before sending when intent is unclear.",
 "inputSchema": {
  "properties": {
   "boardId": {
    "description": "Board ID. Auto-resolved from the card when omitted.",
    "type": "number"
   },
   "bodyParameters": {
    "description": "Values to fill the template's {{1}}, {{2}}\u2026 BODY variables, in order. Required when the template has variables.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "cardId": {
    "description": "The WhatsApp card ID (whatsappCardId) of the conversation to send to.",
    "type": "string"
   },
   "columnId": {
    "description": "Column ID the card is in. Auto-resolved from the card when omitted.",
    "type": "string"
   },
   "components": {
    "description": "Advanced: a fully-formed WhatsApp components array (for templates with media headers or buttons). Overrides bodyParameters when provided.",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "languageCode": {
    "description": "Template language code (e.g. 'fr', 'en_US'). Required only when a board has multiple templates sharing the same name; otherwise auto-resolved.",
    "type": "string"
   },
   "templateName": {
    "description": "The exact template name (e.g. 'french'). Get it from list_whatsapp_templates.",
    "type": "string"
   }
  },
  "required": [
   "cardId",
   "templateName"
  ],
  "type": "object"
 },
 "name": "send_whatsapp_template",
 "title": "Send a WhatsApp template"
}
