{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch a URL and report what its HTML says about Cloudflare Turnstile: whether a widget is present, its sitekey, whether it renders implicitly or via turnstile.render(), the widget's data-* configuration, whether the sitekey is one of Cloudflare's documented test keys, and whether the page is a full-page WAF challenge rather than a widget. Free, no API key required. Use this before solving when the sitekey is unknown. It CANNOT determine the widget's mode (managed / non-interactive / invisible) \u2014 that is stored in the Cloudflare account that owns the sitekey and is not in the page at all.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "url": {
    "description": "The page URL to inspect, e.g. https://example.com/login",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "inspect_page",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "finalUrl": {
    "type": "string"
   },
   "httpStatus": {
    "type": "number"
   },
   "otherCaptcha": {
    "description": "Other CAPTCHA products on the page \u2014 we do not solve these",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "present": {
    "description": "Turnstile was found on the page",
    "type": "boolean"
   },
   "rendering": {
    "anyOf": [
     {
      "enum": [
       "implicit",
       "explicit"
      ],
      "type": "string"
     },
     {
      "type": "null"
     }
    ]
   },
   "responseFieldPresent": {
    "type": "boolean"
   },
   "sitekeys": {
    "description": "Sitekeys found, in document order",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "summary": {
    "description": "One line a human can read",
    "type": "string"
   },
   "wafChallenge": {
    "description": "The response is a Cloudflare WAF challenge interstitial",
    "type": "boolean"
   }
  },
  "required": [
   "present",
   "wafChallenge",
   "sitekeys",
   "rendering",
   "responseFieldPresent",
   "otherCaptcha",
   "finalUrl",
   "httpStatus",
   "summary"
  ],
  "type": "object"
 },
 "title": "Inspect a page for Cloudflare Turnstile"
}
