{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Discover active USGS gauge sites in a US state that are currently reporting a given parameter (real-time). Returns sites with their latest reading; useful for finding which gauges are live in a region. Can match many sites, so results are capped. Keyless.",
 "inputSchema": {
  "examples": [
   {
    "state": "co"
   },
   {
    "limit": 50,
    "parameter_code": "00010",
    "state": "ca"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max sites to return (default 30, max 60).",
    "type": "number"
   },
   "parameter_code": {
    "description": "USGS parameter code (default \"00060\" = discharge/streamflow).",
    "type": "string"
   },
   "state": {
    "description": "2-letter US state code, e.g. \"co\", \"ca\", \"tx\".",
    "type": "string"
   }
  },
  "required": [
   "state"
  ],
  "type": "object"
 },
 "name": "find_sites",
 "title": "Find Sites",
 "version": "v1.1de3a786"
}
