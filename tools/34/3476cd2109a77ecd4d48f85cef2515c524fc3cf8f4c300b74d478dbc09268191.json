{
 "description": "Add a file from disk into the JAR.",
 "inputSchema": {
  "properties": {
   "jar_path": {
    "description": "Destination path within JAR",
    "type": "string"
   },
   "overwrite": {
    "description": "Overwrite if exists (default: true)",
    "type": "boolean"
   },
   "session_id": {
    "description": "Session ID",
    "type": "string"
   },
   "source_path": {
    "description": "Source file on disk",
    "type": "string"
   }
  },
  "required": [
   "session_id",
   "jar_path",
   "source_path"
  ],
  "type": "object"
 },
 "name": "jar_add_file"
}
