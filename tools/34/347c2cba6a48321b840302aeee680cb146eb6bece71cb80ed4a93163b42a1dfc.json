{
 "description": "Return the default field values Odoo would pre-fill when clicking New. Pass action_id to include the action's context (e.g. default_partner_id). Pass context dict directly for wizard models.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action_id": {
    "type": "integer"
   },
   "context": {
    "additionalProperties": {},
    "type": "object"
   },
   "model": {
    "type": "string"
   }
  },
  "required": [
   "model"
  ],
  "type": "object"
 },
 "name": "get_defaults"
}
