{
 "description": "Delete a specific backup belonging to a backup volume. Destructive. Do not call this unless the user has explicitly authorized it in this conversation \u2014 never proactively, speculatively, or as a side effect of another task. Longhorn docs: https://longhorn.io/docs/1.12.1/snapshots-and-backups/backup-and-restore/",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "backupName": {
    "type": "string"
   },
   "name": {
    "type": "string"
   }
  },
  "required": [
   "name",
   "backupName"
  ],
  "type": "object"
 },
 "name": "backupvolume_backup_delete",
 "title": "Delete backup"
}
