{
 "description": "List local containers (running by default, or all with all=true) \u2014 useful after test_container_locally",
 "inputSchema": {
  "properties": {
   "all": {
    "description": "Include stopped containers (default: false \u2014 running only)",
    "type": "boolean"
   },
   "runtime": {
    "enum": [
     "docker",
     "podman"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_local_containers"
}
