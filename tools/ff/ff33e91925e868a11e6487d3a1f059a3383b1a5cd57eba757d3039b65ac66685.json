{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the data.gov.uk catalogue (CKAN package_search) across 57k+ UK government open datasets. Returns matching datasets with titles, descriptions, publishing organization, and resources. Content is English. Pass an empty query to browse everything by relevance/sort.",
 "inputSchema": {
  "examples": [
   {
    "query": "air quality"
   },
   {
    "fq": "organization:office-for-national-statistics",
    "query": "house prices",
    "rows": 50,
    "sort": "metadata_modified desc"
   }
  ],
  "properties": {
   "fq": {
    "description": "Solr filter query, e.g. \"organization:office-for-national-statistics\", \"res_format:CSV\", or \"tags:gtfs\".",
    "type": "string"
   },
   "query": {
    "description": "Search terms, e.g. \"air quality\", \"house prices\", \"covid\". Pass \"\" to list all datasets.",
    "type": "string"
   },
   "rows": {
    "description": "Max results, 1-1000 (default 25).",
    "type": "number"
   },
   "sort": {
    "description": "Sort spec, e.g. \"metadata_modified desc\" or \"score desc\".",
    "type": "string"
   },
   "start": {
    "description": "0-based offset for paging.",
    "type": "number"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_datasets",
 "title": "Search Datasets",
 "version": "v1.6ee0d62d"
}
