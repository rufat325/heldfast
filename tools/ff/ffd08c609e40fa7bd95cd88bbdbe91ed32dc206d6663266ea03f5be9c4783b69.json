{
 "description": "Compare two or more models or providers side-by-side on cost, tokens, efficiency, and usage metrics. Use this when the user wants to know which model or provider is cheaper, more efficient, or more heavily used.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "compare_type": {
    "description": "What to compare: models, providers, or projects",
    "enum": [
     "models",
     "providers",
     "projects"
    ],
    "type": "string"
   },
   "names": {
    "description": "Specific names to compare (if omitted, compares top entries)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "project": {
    "description": "Filter by project name/path substring",
    "type": "string"
   },
   "scope": {
    "description": "Time scope: today, week (last 7d), month (calendar month), or all (default)",
    "enum": [
     "today",
     "week",
     "month",
     "all"
    ],
    "type": "string"
   }
  },
  "required": [
   "compare_type"
  ],
  "type": "object"
 },
 "name": "drishti_compare"
}
