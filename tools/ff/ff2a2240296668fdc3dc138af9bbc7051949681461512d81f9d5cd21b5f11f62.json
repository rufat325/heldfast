{
 "description": "Search past saved chat conversations by content. Returns matching chats with previews.",
 "inputSchema": {
  "properties": {
   "limit": {
    "type": "number"
   },
   "query": {
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "recall_chat"
}
