{
 "annotations": {
  "readOnlyHint": false
 },
 "description": "Encodes and sends a transaction to any Monad contract. Auto-executes under a session grant if covered; otherwise returns an approval URL. Use `read_contract` for view functions \u2014 this rejects view/pure to avoid wasting a tx.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "abi": {
    "anyOf": [
     {
      "description": "Parsed JSON ABI array.",
      "items": {},
      "type": "array"
     },
     {
      "description": "Human-readable Solidity signature(s), one per line.",
      "type": "string"
     }
    ],
    "description": "Either a JSON ABI array (from artifacts) or a human-readable signature like 'function balanceOf(address) view returns (uint256)'."
   },
   "address": {
    "description": "Contract address.",
    "pattern": "^0x[a-fA-F0-9]{40}$",
    "type": "string"
   },
   "args": {
    "default": [],
    "items": {},
    "type": "array"
   },
   "function": {
    "description": "Function name to call (nonpayable or payable).",
    "type": "string"
   },
   "network": {
    "description": "Monad network \u2014 \"mainnet\" or \"testnet\". Defaults to the server's configured network.",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   },
   "ttl_seconds": {
    "default": 300,
    "exclusiveMinimum": 0,
    "maximum": 3600,
    "type": "integer"
   },
   "value_mon": {
    "description": "Native MON to attach (decimal string). Required for payable functions; rejected otherwise.",
    "pattern": "^\\d+(\\.\\d+)?$",
    "type": "string"
   }
  },
  "required": [
   "address",
   "abi",
   "function"
  ],
  "type": "object"
 },
 "name": "write_contract",
 "title": "Send a transaction to any contract"
}
