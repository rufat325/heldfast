{
 "description": "Abort the current iteration loop early.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Item ID or semanticId",
    "type": "string"
   },
   "reason": {
    "description": "Why the loop was aborted",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "abort_iteration_loop"
}
