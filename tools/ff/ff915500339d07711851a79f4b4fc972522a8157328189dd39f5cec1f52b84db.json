{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-access-review-instance"
 },
 "description": "Read the properties and relationships of an accessReviewInstance object.\n\nTIP: Gets a specific access review instance. Returns status, startDateTime, endDateTime, scope, and reviewers for this particular execution of the review.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "accessReviewInstanceId": {
    "description": "Path parameter: accessReviewInstanceId",
    "type": "string"
   },
   "accessReviewScheduleDefinitionId": {
    "description": "Path parameter: accessReviewScheduleDefinitionId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "required": [
   "accessReviewScheduleDefinitionId",
   "accessReviewInstanceId"
  ],
  "type": "object"
 },
 "name": "get-access-review-instance"
}
