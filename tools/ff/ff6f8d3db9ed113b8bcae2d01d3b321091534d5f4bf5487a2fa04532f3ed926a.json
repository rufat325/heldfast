{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List gift cards"
 },
 "description": "List gift cards on the account \u2014 balances and status, with each code masked to its last 4 characters. Pass revealCodes: true only when the user needs a full code.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "revealCodes": {
    "description": "Return full gift-card codes instead of masked ones. A code is redeemable money; default false.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "mhlb_list_gift_cards"
}
