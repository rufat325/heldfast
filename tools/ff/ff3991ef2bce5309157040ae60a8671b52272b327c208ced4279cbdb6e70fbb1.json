{
 "description": "Create an exact-member recovery execution from one confirmed source snapshot. The asserted candidate set is immutable and replacements are forbidden.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "candidateIds": {
    "items": {
     "minimum": 1,
     "type": "integer"
    },
    "maxItems": 20,
    "minItems": 1,
    "type": "array"
   },
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "explicitExactSetConfirmation": {
    "const": true,
    "type": "boolean"
   },
   "idempotencyKey": {
    "maxLength": 200,
    "minLength": 16,
    "pattern": "^[\\x20-\\x7e]+$",
    "type": "string"
   },
   "sourceExecutionId": {
    "minimum": 1,
    "type": "integer"
   },
   "sourceSnapshotHash": {
    "pattern": "^[a-f0-9]{64}$",
    "type": "string"
   }
  },
  "required": [
   "sourceExecutionId",
   "sourceSnapshotHash",
   "candidateIds",
   "explicitExactSetConfirmation",
   "idempotencyKey"
  ],
  "type": "object"
 },
 "name": "trackly_recover_exact_apply_members"
}
