{
 "description": "Detect unusual spending patterns and anomalies in token usage. Identifies days or sessions with cost spikes, sudden model switches, abnormally large requests, and deviation from historical averages. Use this to catch runaway costs or unexpected usage.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "project": {
    "description": "Filter by project name/path substring",
    "type": "string"
   },
   "providers": {
    "description": "Filter to specific providers (e.g. ['claude-code','cursor'])",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "scope": {
    "description": "Time scope: today, week (last 7d), month (calendar month), or all (default)",
    "enum": [
     "today",
     "week",
     "month",
     "all"
    ],
    "type": "string"
   },
   "sensitivity": {
    "description": "Anomaly detection sensitivity (default: medium)",
    "enum": [
     "low",
     "medium",
     "high"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "drishti_anomaly"
}
