{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Look up a licensed NYC dog by name (partial match), optionally scoped to a ZIP code. Returns license records from the NYC Dog Licensing Dataset.",
 "inputSchema": {
  "examples": [
   {
    "name": "Paige"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Socrata app token for higher rate limits. Omit to use the keyless endpoint.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (1-200, default 20).",
    "type": "number"
   },
   "name": {
    "description": "Dog name (partial match, case-insensitive), e.g. \"Paige\".",
    "type": "string"
   },
   "zipcode": {
    "description": "Optional ZIP code filter.",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "nyc_search_animal",
 "title": "Nyc Search Animal",
 "version": "v1.0ba582fd"
}
