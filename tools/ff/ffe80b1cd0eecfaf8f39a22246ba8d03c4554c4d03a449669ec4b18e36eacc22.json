{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Check for a key without returning its ciphertext. Requires the namespace_token. Costs $0.005 in USDC on Base, paid via the x402 protocol, or from a credit token \u2014 call credits_trial for free credit if you have neither.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "credit_token": {
    "description": "Optional. A credit token from credits_trial or /credits/buy. Supplying it pays for this call from that balance, so no x402 payment or wallet is needed.",
    "type": "string"
   },
   "key": {
    "description": "Item key",
    "type": "string"
   },
   "namespace": {
    "description": "Isolation scope",
    "type": "string"
   },
   "namespace_token": {
    "description": "One-time token issued by the first call that claimed this namespace. Required for every later call.",
    "type": "string"
   }
  },
  "required": [
   "namespace",
   "key",
   "namespace_token"
  ],
  "type": "object"
 },
 "name": "vault_exists",
 "outputSchema": {
  "properties": {
   "exists": {
    "description": "Whether the key is present. Cheaper than a retrieve.",
    "type": "boolean"
   },
   "key": {
    "description": "The key that was checked",
    "type": "string"
   },
   "namespace": {
    "description": "Isolation scope",
    "type": "string"
   }
  },
  "required": [
   "exists",
   "key"
  ],
  "type": "object"
 },
 "title": "Test whether a key exists"
}
