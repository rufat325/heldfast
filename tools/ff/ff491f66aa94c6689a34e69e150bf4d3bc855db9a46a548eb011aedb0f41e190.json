{
 "description": "Get the project plan attached to a workflow, including all its phases (open and completed). Returns phase titles, statuses, assigned agents, and sort order. Use this to understand what work is planned or in progress for a workflow.",
 "inputSchema": {
  "properties": {
   "workflowId": {
    "description": "Workflow ID",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "workflowId"
  ],
  "type": "object"
 },
 "name": "get_project"
}
