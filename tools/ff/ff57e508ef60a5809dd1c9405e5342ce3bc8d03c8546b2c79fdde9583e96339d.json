{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search/browse AniList's animation/production studios by name, when you want to see multiple candidates rather than take the closest match \u2014 get_studio's own `name` param already does this same fuzzy search directly and resolves a partial name on its own. Returns AniList IDs to use with get_studio.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "page": {
    "default": 1,
    "description": "Page number for pagination. AniList's paginated connections (search results, a title's characters/staff/reviews, activity feeds, forum comments) reject a page whose page \u00d7 perPage exceeds ~5000 entries with an upstream error, so paging that deep fails rather than returning more results.",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "perPage": {
    "default": 10,
    "description": "Results per page (max 50, AniList's own ceiling \u2014 it silently clamps anything higher).",
    "exclusiveMinimum": 0,
    "maximum": 50,
    "type": "integer"
   },
   "term": {
    "description": "Studio name (or part of it) to search for.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "term"
  ],
  "type": "object"
 },
 "name": "search_studio",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "results": {
    "additionalProperties": {},
    "properties": {
     "pageInfo": {
      "additionalProperties": {},
      "properties": {
       "currentPage": {
        "anyOf": [
         {
          "maximum": 9007199254740991,
          "minimum": -9007199254740991,
          "type": "integer"
         },
         {
          "type": "null"
         }
        ]
       },
       "hasNextPage": {
        "type": [
         "boolean",
         "null"
        ]
       },
       "lastPage": {
        "anyOf": [
         {
          "maximum": 9007199254740991,
          "minimum": -9007199254740991,
          "type": "integer"
         },
         {
          "type": "null"
         }
        ],
        "description": "Not currently accurate (a known AniList performance limitation) \u2014 use `hasNextPage` instead to decide whether to fetch another page."
       },
       "perPage": {
        "anyOf": [
         {
          "maximum": 9007199254740991,
          "minimum": -9007199254740991,
          "type": "integer"
         },
         {
          "type": "null"
         }
        ]
       },
       "total": {
        "anyOf": [
         {
          "maximum": 9007199254740991,
          "minimum": -9007199254740991,
          "type": "integer"
         },
         {
          "type": "null"
         }
        ],
        "description": "Not currently accurate (a known AniList performance limitation) \u2014 don't rely on it to decide whether to fetch more pages."
       }
      },
      "type": "object"
     },
     "studios": {
      "items": {
       "additionalProperties": {},
       "description": "Only `id` is guaranteed here \u2014 the real object has more fields, but their shape depends on which variant of an underlying union type this is (e.g. which activity type), so only the field common to every variant is declared.",
       "properties": {
        "id": {
         "exclusiveMinimum": 0,
         "maximum": 2147483647,
         "type": "integer"
        }
       },
       "required": [
        "id"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "type": "object"
   }
  },
  "required": [
   "results"
  ],
  "type": "object"
 },
 "title": "Search studios"
}
