{
 "description": "Extract a template from a brief or article. Saves it when outline sections exist; otherwise returns it unsaved.",
 "inputSchema": {
  "properties": {
   "source_id": {
    "description": "The brief ID or content ID to extract from (required)",
    "type": "string"
   },
   "source_type": {
    "description": "Source type: \"brief\" or \"content\" (required)",
    "enum": [
     "brief",
     "content"
    ],
    "type": "string"
   }
  },
  "required": [
   "source_type",
   "source_id"
  ],
  "type": "object"
 },
 "name": "extract_template"
}
