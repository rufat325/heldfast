{
 "description": "Get Kubernetes system events for a Code Engine job run (via the CE Kubernetes API proxy). Useful for diagnosing why a job run failed \u2014 e.g. image pull errors, OOMKilled, quota limits. Events are retained for roughly 60 minutes.",
 "inputSchema": {
  "properties": {
   "job_run_name": {
    "description": "Job run name",
    "type": "string"
   },
   "namespace": {
    "description": "Optional: project namespace (subdomain) override, if it cannot be auto-derived.",
    "type": "string"
   },
   "project_id": {
    "description": "Project ID or name",
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "job_run_name"
  ],
  "type": "object"
 },
 "name": "ce_get_job_run_events"
}
