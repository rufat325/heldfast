{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Persist per-repo guidelines extracted from PR review feedback. Overwrites any existing guidelines for the repo. Content is capped at 8 KB. Requires Gist persistence \u2014 fails with GUIDELINES_NOT_AVAILABLE in local mode.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "content": {
    "description": "Markdown content extracted from past PR review feedback",
    "maxLength": 8192,
    "minLength": 1,
    "type": "string"
   },
   "repo": {
    "pattern": "^[^/]+\\/[^/]+$",
    "type": "string"
   }
  },
  "required": [
   "repo",
   "content"
  ],
  "type": "object"
 },
 "name": "guidelines-store"
}
