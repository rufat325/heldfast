{
 "description": "Configure Gmail/Outlook relay for sending real internet email (requires master API key). BEGINNER-FRIENDLY: Just needs a Gmail/Outlook email + app password. Agents send as user+agentname@gmail.com. Automatically creates a default agent (secretary) unless skipped. Best for: quick setup, personal use, no domain needed.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "agentName": {
    "description": "Name for the default agent (default: secretary). This becomes the email sub-address, e.g., user+secretary@gmail.com",
    "type": "string"
   },
   "agentRole": {
    "description": "Role for the default agent (default: secretary)",
    "enum": [
     "secretary",
     "assistant",
     "researcher",
     "writer",
     "custom"
    ],
    "type": "string"
   },
   "email": {
    "description": "Your real email address (e.g., user@gmail.com)",
    "type": "string"
   },
   "imapHost": {
    "description": "IMAP host (auto-filled for gmail/outlook)",
    "type": "string"
   },
   "imapPort": {
    "description": "IMAP port (auto-filled for gmail/outlook)",
    "type": "number"
   },
   "password": {
    "description": "App password (not your regular password)",
    "type": "string"
   },
   "provider": {
    "description": "Email provider (gmail, outlook, or custom)",
    "enum": [
     "gmail",
     "outlook",
     "custom"
    ],
    "type": "string"
   },
   "skipDefaultAgent": {
    "description": "Skip creating the default agent (default: false)",
    "type": "boolean"
   },
   "smtpHost": {
    "description": "SMTP host (auto-filled for gmail/outlook)",
    "type": "string"
   },
   "smtpPort": {
    "description": "SMTP port (auto-filled for gmail/outlook)",
    "type": "number"
   }
  },
  "required": [
   "provider",
   "email",
   "password"
  ],
  "type": "object"
 },
 "name": "setup_email_relay"
}
