{
 "description": "Reputation for an IP address: Tor exit node, Spamhaus listing, ASN and owner, reverse DNS, datacenter or residential. Needs the network. Private and loopback addresses are answered locally. Network: needs Team Cymru DNS, Spamhaus ZEN, check.torproject.org.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "ip": {
    "description": "IPv4 or IPv6 address.",
    "type": "string"
   }
  },
  "required": [
   "ip"
  ],
  "type": "object"
 },
 "name": "check_ip"
}
