{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Change where a subscription posts, which events it wants, its label, or switch it off without deleting it. Pause with active:false while an endpoint is being repaired \u2014 that keeps the secret alive, where deleting and recreating would force a new one into the customer's deployment. Only the fields you pass change.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "active": {
    "description": "false pauses delivery and keeps the subscription and its secret; true resumes.",
    "type": "boolean"
   },
   "description": {
    "description": "New label. Max 200 characters.",
    "type": "string"
   },
   "events": {
    "description": "Replaces the whole list, it does not add to it. Same vocabulary as impreza_create_webhook.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "url": {
    "description": "New endpoint. Same rules as on create: public https, max 500 characters.",
    "type": "string"
   },
   "webhook_id": {
    "description": "The id from impreza_list_webhooks.",
    "type": "number"
   }
  },
  "required": [
   "webhook_id"
  ],
  "type": "object"
 },
 "name": "impreza_update_webhook"
}
