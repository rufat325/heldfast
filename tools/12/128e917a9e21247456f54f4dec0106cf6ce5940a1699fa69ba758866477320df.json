{
 "description": "List releases in a Sentry organization. Returns version, name, dateReleased, dateCreated, newGroups, and authors for each release.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cursor": {
    "description": "Pagination cursor. Omit for first page.",
    "type": "string"
   },
   "limit": {
    "default": 25,
    "description": "Maximum releases to return (default 25)",
    "type": "number"
   },
   "org": {
    "description": "Organization slug (e.g. 'acme')",
    "type": "string"
   },
   "query": {
    "description": "Search query (e.g. 'v1.' to find all 1.x releases)",
    "type": "string"
   }
  },
  "required": [
   "org"
  ],
  "type": "object"
 },
 "name": "list_releases"
}
