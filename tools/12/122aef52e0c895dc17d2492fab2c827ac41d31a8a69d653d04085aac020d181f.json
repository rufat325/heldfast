{
 "description": "Get the Code Engine project resource quotas showing Used-vs-Limit for CPU, memory, ephemeral storage, apps, instances, secrets, jobs, builds, and more (via the CE Kubernetes API proxy resourcequotas). Use this to check whether the project is near a limit.",
 "inputSchema": {
  "properties": {
   "namespace": {
    "description": "Optional: project namespace (subdomain) override, if it cannot be auto-derived.",
    "type": "string"
   },
   "project_id": {
    "description": "Project ID or name",
    "type": "string"
   }
  },
  "required": [
   "project_id"
  ],
  "type": "object"
 },
 "name": "ce_get_project_quotas"
}
