{
 "description": "Update an existing order (tags, note, email, shipping_address, metafields, etc).",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Shopify order ID",
    "type": "string"
   },
   "order": {
    "description": "Partial order object with fields to update. The 'id' field is injected automatically.",
    "type": "object"
   }
  },
  "required": [
   "id",
   "order"
  ],
  "type": "object"
 },
 "name": "update_order"
}
