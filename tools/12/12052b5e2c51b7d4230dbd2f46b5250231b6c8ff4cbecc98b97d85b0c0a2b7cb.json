{
 "annotations": {
  "readOnlyHint": true,
  "title": "List track licenses"
 },
 "description": "List a track's licenses (e.g. cover/mechanical or sample clearances), paginated. Pass `license_id` to retrieve one license instead.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "license_id": {
    "description": "Retrieve one license by id instead of listing.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "page": {
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "per_page": {
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "track_id": {
    "description": "The track id.",
    "exclusiveMinimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "track_id"
  ],
  "type": "object"
 },
 "name": "list_track_licenses",
 "title": "List track licenses"
}
