{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List active deployments, optionally filtered. Use this to find deployments left running from earlier sessions before starting new ones \u2014 orphaned servers cost money.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "filter": {
    "description": "Edgegap filter expression, e.g. by tag. Omit for all deployments.",
    "type": "string"
   },
   "limit": {
    "description": "Default 50.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "edgegap_list_deployments",
 "title": "List running deployments"
}
