{
 "description": "Search code across GitHub (e.g. 'addEventListener repo:owner/name').",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "exclusiveMinimum": 0,
    "maximum": 100,
    "type": "integer"
   },
   "query": {
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_code",
 "title": "Search code"
}
