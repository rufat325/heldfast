{
 "description": "Log in to a container registry (e.g. IBM Container Registry) so images can be pushed/pulled. Uses IBM Cloud IAM token for ICR or username/password for other registries.",
 "inputSchema": {
  "properties": {
   "password": {
    "description": "Password or API key. For ICR leave blank to use IBMCLOUD_API_KEY env var.",
    "type": "string"
   },
   "registry": {
    "description": "Registry hostname (default: us.icr.io for ICR)",
    "type": "string"
   },
   "runtime": {
    "enum": [
     "docker",
     "podman"
    ],
    "type": "string"
   },
   "username": {
    "description": "Username \u2014 use \"iamapikey\" for ICR with an IBM Cloud API key",
    "type": "string"
   }
  },
  "required": [
   "registry"
  ],
  "type": "object"
 },
 "name": "login_to_registry"
}
