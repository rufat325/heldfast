{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "readOnlyHint": false
 },
 "description": "Submit completed work for review. Transitions in_progress \u2192 in_review. Requires a git SHA and a diff URL as evidence.",
 "inputSchema": {
  "properties": {
   "criterionId": {
    "type": "string"
   },
   "diffUrl": {
    "description": "URL to view the diff (PR link or compare URL)",
    "type": "string"
   },
   "gitSha": {
    "description": "7+ char commit SHA",
    "type": "string"
   },
   "notes": {
    "description": "Optional notes for the reviewer (max 2000 chars)",
    "type": "string"
   }
  },
  "required": [
   "criterionId",
   "gitSha",
   "diffUrl"
  ],
  "type": "object"
 },
 "name": "vibemap_submit_for_review"
}
