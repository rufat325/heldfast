{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Return time-bucketed Customer.io delivery, bounce, open, click, unsubscribe, and conversion metrics for one campaign. Answers how a lifecycle campaign performed and when engagement changed.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-customer-io-app-api-key",
    "_region": "us",
    "campaign_id": 42,
    "resolution": "daily"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Customer.io App API key",
    "type": "string"
   },
   "_region": {
    "description": "Workspace region; defaults to us",
    "enum": [
     "us",
     "eu"
    ],
    "type": "string"
   },
   "campaign_id": {
    "description": "Customer.io campaign ID",
    "type": "number"
   },
   "end": {
    "description": "End as Unix seconds",
    "type": "number"
   },
   "message_type": {
    "enum": [
     "email",
     "webhook",
     "twilio",
     "whatsapp",
     "slack",
     "push",
     "in_app",
     "live_notification"
    ],
    "type": "string"
   },
   "resolution": {
    "description": "Metric bucket; defaults to daily",
    "enum": [
     "hourly",
     "daily",
     "weekly",
     "monthly"
    ],
    "type": "string"
   },
   "start": {
    "description": "Start as Unix seconds",
    "type": "number"
   },
   "timezone": {
    "description": "IANA timezone such as America/Los_Angeles",
    "type": "string"
   }
  },
  "required": [
   "_apiKey",
   "campaign_id"
  ],
  "type": "object"
 },
 "name": "cio_campaign_metrics",
 "title": "Cio Campaign Metrics",
 "version": "v1.009c1ad2"
}
