{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search SGD (Saccharomyces Genome Database, the authoritative budding-yeast / S. cerevisiae genetics resource) for genes, loci, alleles, and other entities by free-text query. Returns matching hits with their name, category, and href. Keyless.",
 "inputSchema": {
  "examples": [
   {
    "query": "ADE2"
   },
   {
    "limit": 10,
    "query": "DNA repair"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max results (default 15).",
    "type": "number"
   },
   "query": {
    "description": "Free-text search, e.g. a gene name like ADE2.",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_genes",
 "title": "Search Genes",
 "version": "v1.a5ba95a3"
}
