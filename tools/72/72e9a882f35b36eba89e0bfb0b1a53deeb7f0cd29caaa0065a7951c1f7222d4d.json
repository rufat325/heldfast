{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List experts cited in the Streaming Radar newsletter with optional filters by tier or country. Free access \u2014 no API key required.",
 "inputSchema": {
  "properties": {
   "country": {
    "description": "ISO 3166-1 alpha-2 country code (e.g. \"NG\", \"KE\", \"ZA\")",
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 20, max 50)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   },
   "tier": {
    "description": "Expert tier (1 = top, 2 = mid, 3 = emerging)",
    "enum": [
     1,
     2,
     3
    ],
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "sr_get_experts",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Tool-specific result object. Inspect the `content` array for human-readable text and structured data.",
  "type": "object"
 }
}
