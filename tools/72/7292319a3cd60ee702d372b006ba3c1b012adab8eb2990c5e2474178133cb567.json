{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Call this when the user asks what new perpetuals were listed, which exchange listed a coin first, or about delistings. Returns listings and delistings across every exchange the hourly scan covers.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "days": {
    "description": "Window in days, 1-30 (default 30). Longer listing history is the listings dataset at bykaranteli.com/data.",
    "maximum": 30,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "get_new_listings",
 "title": "New and delisted perpetual contracts"
}
