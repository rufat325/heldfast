{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Query any Baltimore ArcGIS layer by service path + layer id. Full ArcGIS query: where, out_fields, order_by, limit. Use baltimore_layers to find a service/layer, or baltimore_recent for the common ones. Epoch dates are converted to ISO.",
 "inputSchema": {
  "examples": [
   {
    "layer": 0,
    "limit": 100,
    "order_by": "CreatedDate DESC",
    "service": "crime",
    "where": "OFFENSE='HOMICIDE'"
   },
   {
    "layer": 39,
    "limit": 200,
    "out_fields": "RequestID,CreatedDate,ServiceCode",
    "service": "311_Customer_Service_Requests_current/FeatureServer"
   }
  ],
  "properties": {
   "layer": {
    "description": "Layer id within the service (from baltimore_layers), e.g. 39.",
    "type": "number"
   },
   "limit": {
    "description": "Max rows (default 100, max 2000).",
    "type": "number"
   },
   "order_by": {
    "description": "Sort clause, e.g. \"CreatedDate DESC\".",
    "type": "string"
   },
   "out_fields": {
    "description": "Comma-separated fields, or \"*\" (default).",
    "type": "string"
   },
   "service": {
    "description": "ArcGIS service path, e.g. \"311_Customer_Service_Requests_current/FeatureServer\" (or a short name: crime|service_requests|permits).",
    "type": "string"
   },
   "where": {
    "description": "ArcGIS SQL where (default \"1=1\").",
    "type": "string"
   }
  },
  "required": [
   "service",
   "layer"
  ],
  "type": "object"
 },
 "name": "baltimore_query",
 "title": "Baltimore Query",
 "version": "v1.e00891c9"
}
