{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Change member role"
 },
 "description": "Update board member role/title. Requires ADMIN/OWNER on that board.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "board_id": {
    "description": "Board UUID",
    "format": "uuid",
    "type": "string"
   },
   "board_slug": {
    "description": "Board slug (alternative to board_id)",
    "type": "string"
   },
   "role": {
    "description": "New role",
    "enum": [
     "ADMIN",
     "MEMBER",
     "VIEWER"
    ],
    "type": "string"
   },
   "target_user_id": {
    "description": "Target member user UUID",
    "format": "uuid",
    "type": "string"
   },
   "title": {
    "description": "New title (or null to clear)",
    "type": [
     "string",
     "null"
    ]
   }
  },
  "required": [
   "target_user_id"
  ],
  "type": "object"
 },
 "name": "update_board_member",
 "title": "Change member role"
}
