{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Returns the current USD price for a token on Monad, sourced from the deepest-liquidity pair found on DexScreener. Mainnet only \u2014 testnet has no price feed.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "network": {
    "description": "Monad network \u2014 \"mainnet\" or \"testnet\". Defaults to the server's configured network.",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   },
   "token": {
    "description": "ERC-20 token address (or use resolve_token to look it up).",
    "pattern": "^0x[a-fA-F0-9]{40}$",
    "type": "string"
   }
  },
  "required": [
   "token"
  ],
  "type": "object"
 },
 "name": "get_token_price",
 "title": "Get token USD price (DexScreener)"
}
