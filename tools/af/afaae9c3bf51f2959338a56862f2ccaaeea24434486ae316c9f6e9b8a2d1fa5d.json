{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Reduce text to fit a token budget, preserving whole sentences and reporting before/after token estimates. Costs $0.010 in USDC on Base, paid via the x402 protocol, or from a credit token \u2014 call credits_trial for free credit if you have neither.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "credit_token": {
    "description": "Optional. A credit token from credits_trial or /credits/buy. Supplying it pays for this call from that balance, so no x402 payment or wallet is needed.",
    "type": "string"
   },
   "strategy": {
    "description": "Compression strategy (default extractive)",
    "enum": [
     "extractive",
     "truncate"
    ],
    "type": "string"
   },
   "target_tokens": {
    "description": "Approximate token budget",
    "type": "integer"
   },
   "text": {
    "description": "Text to compress",
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "compress",
 "outputSchema": {
  "properties": {
   "compressed_length": {
    "description": "Output length in characters",
    "type": "number"
   },
   "original_length": {
    "description": "Input length in characters",
    "type": "number"
   },
   "ratio": {
    "description": "compressed_length / original_length. Lower is more aggressive.",
    "type": "number"
   },
   "strategy": {
    "description": "Strategy applied, echoing your request",
    "type": "string"
   },
   "text": {
    "description": "The compressed text",
    "type": "string"
   }
  },
  "required": [
   "original_length",
   "compressed_length",
   "ratio",
   "text"
  ],
  "type": "object"
 },
 "title": "Compress text to a token budget"
}
