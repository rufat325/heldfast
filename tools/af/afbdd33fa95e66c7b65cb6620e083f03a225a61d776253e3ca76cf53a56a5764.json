{
 "description": "Export the register as csv | md | json (for spreadsheets / reports). Returns the export; writes no file. Read-only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "expiring_before": {
    "description": "Only rows expiring before this date.",
    "type": "string"
   },
   "format": {
    "description": "Export format.",
    "enum": [
     "csv",
     "md",
     "json"
    ],
    "type": "string"
   },
   "needs_review": {
    "description": "Only rows needing review.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "contract_vault_export"
}
