{
 "description": "Refresh domain readiness from real SES and DNS observations. This is the ONLY operation that can move a domain to `active`, and only because it actually looked.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "domain_id": {
    "description": "Sending domain ID",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "domain_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_domain_verify"
}
