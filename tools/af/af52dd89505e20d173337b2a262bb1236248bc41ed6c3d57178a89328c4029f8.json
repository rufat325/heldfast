{
 "annotations": {
  "readOnlyHint": true,
  "title": "Get closed-thread activity"
 },
 "description": "List individual thread-close events for a board over a date range. Each event gives openedAt, closedAt, intervalTime in milliseconds, and who closed it. Returns raw events, not per-day counts.",
 "inputSchema": {
  "properties": {
   "boardId": {
    "description": "The board ID",
    "type": "number"
   },
   "dateFrom": {
    "description": "Start date for the period (ISO-8601)",
    "type": "string"
   },
   "dateTo": {
    "description": "End date for the period (ISO-8601)",
    "type": "string"
   }
  },
  "required": [
   "boardId",
   "dateFrom",
   "dateTo"
  ],
  "type": "object"
 },
 "name": "get_closed_activity",
 "title": "Get closed-thread activity"
}
