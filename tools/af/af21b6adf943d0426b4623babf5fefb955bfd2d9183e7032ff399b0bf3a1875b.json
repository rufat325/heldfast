{
 "description": "Search Google Images with the documented /google-search-images endpoint.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "async": {
    "description": "Deprecated compatibility flag. Prefer execution_mode.",
    "type": "boolean"
   },
   "execution_mode": {
    "description": "Execution strategy. Use auto to let the MCP server choose between sync and async.",
    "enum": [
     "auto",
     "sync",
     "async"
    ],
    "type": "string"
   },
   "fields": {
    "description": "Specific fields to return.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "language": {
    "description": "Language code.",
    "type": "string"
   },
   "limit": {
    "description": "Items per query limit.",
    "maximum": 9007199254740991,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "One or more Google image search queries.",
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "region": {
    "description": "Region code.",
    "type": "string"
   },
   "uule": {
    "description": "Optional Google UULE location string.",
    "type": "string"
   },
   "webhook": {
    "description": "Optional webhook URL for async completion.",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "google_search_images",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "async": {
    "additionalProperties": false,
    "properties": {
     "id": {
      "type": "string"
     },
     "is_async_submission": {
      "type": "boolean"
     },
     "next_step": {
      "type": "string"
     },
     "results_location": {
      "type": "string"
     },
     "status": {
      "type": "string"
     }
    },
    "required": [
     "is_async_submission"
    ],
    "type": "object"
   },
   "data": {},
   "meta": {
    "additionalProperties": false,
    "properties": {
     "operation": {
      "type": "string"
     },
     "service": {
      "type": "string"
     }
    },
    "required": [
     "service",
     "operation"
    ],
    "type": "object"
   }
  },
  "required": [
   "data",
   "meta"
  ],
  "type": "object"
 },
 "title": "Google Search Images"
}
