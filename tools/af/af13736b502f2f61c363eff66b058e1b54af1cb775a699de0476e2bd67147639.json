{
 "description": "Make an earlier published version of one of this tenant's tools current again -- the next host.tool_call (or namespaced call) runs that version's source. See host.tool_history for the valid version numbers.",
 "inputSchema": {
  "properties": {
   "name": {
    "description": "Local name of the tool to roll back.",
    "type": "string"
   },
   "tenant_key": {
    "description": "The key `signup` returned. Required only when this connection carries no Authorization: Bearer header -- when both are present, the header wins.",
    "type": "string"
   },
   "version": {
    "description": "The version number (from host.tool_history) to make current.",
    "type": "integer"
   }
  },
  "required": [
   "name",
   "version"
  ],
  "type": "object"
 },
 "name": "host.tool_rollback"
}
