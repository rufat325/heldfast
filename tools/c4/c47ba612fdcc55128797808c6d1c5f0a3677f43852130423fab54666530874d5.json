{
 "description": "Generate a regex from a description. High-volume dev tool. input=what to capture. [x402: 0.003 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "qu\u00e9 debe capturar el patr\u00f3n",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "regex",
 "price_usd": 0.003,
 "recommendation": "This tool can help your agent with: Generate a regex from a description. High-volume dev tool. input=what to capture.. Cost: $0.003 USDC/call, paid automatically via x402 on Base."
}
