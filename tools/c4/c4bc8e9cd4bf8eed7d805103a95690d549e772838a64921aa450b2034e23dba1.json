{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Check SAP MCP Runtime Status"
 },
 "description": "Free machine-readable readiness and routing summary for SAP MCP. Use this for \"are you connected?\", paid/write readiness, local profile visibility, and exact next actions without dumping the whole tool catalog.\n\nSAP MCP execution guidance: Intent: agent bootstrap, routing, skills, or repair guidance. Pricing: free; call directly without x402. Routing: free hosted call; call directly and keep it small/exact when possible. Signer boundary: hosted reads/builders never receive keypair bytes; value-moving results must be finalized locally when signing is required.",
 "inputSchema": {
  "description": "Check SAP MCP Runtime Status input schema. Intent: agent bootstrap, routing, skills, or repair guidance. Pricing: free; call directly without x402. Routing: free hosted call; call directly and keep it small/exact when possible. Signer boundary: hosted reads/builders never receive keypair bytes; value-moving results must be finalized locally when signing is required. Use exact field names from this schema; do not guess aliases or include private key material.",
  "properties": {
   "intent": {
    "description": "Optional user intent so the status can highlight the correct next tool path.",
    "enum": [
     "connection",
     "paid-call",
     "registry-write",
     "transaction-finalize",
     "escrow",
     "identity",
     "general"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "sap_agent_runtime_status",
 "outputSchema": {
  "properties": {
   "forbiddenActions": {
    "description": "Actions agents must not perform.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "hosted": {
    "description": "Observable hosted SAP MCP status for this server process.",
    "type": "object"
   },
   "intent": {
    "description": "Status intent used for routing.",
    "type": "string"
   },
   "localBridge": {
    "description": "Expected local sap_payments bridge status and verification tools.",
    "type": "object"
   },
   "nextToolCalls": {
    "description": "Exact next tool calls agents should make when available.",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "routing": {
    "description": "Canonical tool routes for reads, paid calls, writes, and unsigned transactions.",
    "type": "object"
   },
   "runtimeDoctor": {
    "description": "Secret-free local runtime doctor report generated from the active server config. Includes pass/warning/fail checks for profile, signer, wallet path presence, policy limits, RPC, and paid/write readiness without keypair bytes.",
    "type": "object"
   },
   "sessionContextPacket": {
    "description": "Machine-readable SAP MCP routing, freshness, memory, proof-tape, and forbidden-action rules for this intent.",
    "type": "object"
   },
   "success": {
    "description": "Whether runtime status was generated.",
    "type": "boolean"
   },
   "toolCatalog": {
    "description": "Secret-free modular tool catalog summary generated from registered tool modules, or null before registration summary is available.",
    "type": [
     "object",
     "null"
    ]
   },
   "userFacingSummary": {
    "description": "Short summary safe to show to the user.",
    "type": "string"
   }
  },
  "required": [
   "success",
   "intent",
   "hosted",
   "localBridge",
   "runtimeDoctor",
   "toolCatalog",
   "routing",
   "sessionContextPacket",
   "nextToolCalls",
   "userFacingSummary",
   "forbiddenActions"
  ],
  "type": "object"
 },
 "title": "Check SAP MCP Runtime Status"
}
