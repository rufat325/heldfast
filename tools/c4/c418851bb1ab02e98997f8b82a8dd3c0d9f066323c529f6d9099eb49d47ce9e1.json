{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Materialize backlog stage"
 },
 "description": "Create (or fetch, if it already exists) the special backlog stage for a board \u2014 holds tasks that aren't part of any active/planned stage. Requires ADMIN/OWNER role on that board.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "board_id": {
    "description": "Board UUID",
    "format": "uuid",
    "type": "string"
   },
   "board_slug": {
    "description": "Board slug (alternative to board_id)",
    "type": "string"
   },
   "name": {
    "description": "Backlog stage name",
    "type": "string"
   },
   "status": {
    "description": "Optional initial stage status",
    "enum": [
     "PLANNED",
     "ACTIVE"
    ],
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "materialize_board_backlog_stage",
 "title": "Materialize backlog stage"
}
