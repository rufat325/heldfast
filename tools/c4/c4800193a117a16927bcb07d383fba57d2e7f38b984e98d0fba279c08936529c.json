{
 "description": "Computed supply-chain risk score (0-100) for one npm package version: install-script analysis, typosquat detection, publish anomalies, adoption and provenance signals. Cheap per-call; built to run on every dependency an agent touches.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "packageName": {
    "description": "Exact npm package name, e.g. express",
    "type": "string"
   },
   "version": {
    "description": "Exact version to analyze; omit for latest",
    "type": "string"
   }
  },
  "required": [
   "packageName"
  ],
  "type": "object"
 },
 "name": "package_risk",
 "title": "Supply-chain risk score"
}
