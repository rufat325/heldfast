{
 "description": "Mint a portable sanity attestation: a signed token binding an agent_id to a statement's sanity-check results. Checks shape (presence, length, injection indicators), not truth \u2014 it proves the statement was screened, not that it is correct. Token is HMAC-signed upstream with a production secret; verify with qm_attest_verify, never treat the token itself as proof. Free, anonymous. Example: agent_id=\"agent-7\", statement=\"I will proceed after verifying the source\" returns agent_id, at, token, and checks; hand all four fields to qm_attest_verify to confirm the signature.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "agent_id": {
    "description": "Identifier of the attesting agent.",
    "type": "string"
   },
   "statement": {
    "description": "Statement being attested (max 20000 chars).",
    "maxLength": 20000,
    "type": "string"
   }
  },
  "required": [
   "agent_id",
   "statement"
  ],
  "type": "object"
 },
 "name": "qm_attest"
}
