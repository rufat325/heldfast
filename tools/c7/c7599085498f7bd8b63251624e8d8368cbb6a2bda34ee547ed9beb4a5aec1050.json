{
 "_meta": {
  "ufcalendar/authless": false,
  "ufcalendar/endpoint": "POST /v1/webhook-endpoints/{id}/rotate-secret",
  "ufcalendar/tier": "pro"
 },
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Rotate a webhook signing secret"
 },
 "description": "Replace the signing secret of one webhook endpoint. The old secret stops verifying immediately, so update the receiver first. The new secret is returned once.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "endpoint_id": {
    "anyOf": [
     {
      "maximum": 9007199254740991,
      "minimum": -9007199254740991,
      "type": "integer"
     },
     {
      "pattern": "^\\d+$",
      "type": "string"
     }
    ],
    "description": "Endpoint id from list_webhook_endpoints. Integer or numeric string."
   }
  },
  "required": [
   "endpoint_id"
  ],
  "type": "object"
 },
 "name": "rotate_webhook_secret",
 "title": "Rotate a webhook signing secret"
}
