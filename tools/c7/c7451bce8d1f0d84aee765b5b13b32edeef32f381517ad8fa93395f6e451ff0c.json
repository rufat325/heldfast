{
 "annotations": {
  "readOnlyHint": true,
  "title": "Unseal Credential"
 },
 "description": "Unseal (decrypt and retrieve) a credential. Verifies ML-DSA-65 signature and HMAC-SHA-512 before any decryption. Every call is logged in the tamper-evident audit trail.",
 "inputSchema": {
  "properties": {
   "entry_id": {
    "description": "UUID of the vault entry to unseal",
    "type": "string"
   }
  },
  "required": [
   "entry_id"
  ],
  "type": "object"
 },
 "name": "vault_unseal"
}
