{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Save Snapshot Draft"
 },
 "description": "Persist your synthesized snapshot content as a draft. Step 3 of the MCP snapshot path (ADR-032 Amendment A).\n\nCall this after synthesizing from snapshot_sources(). Backend derives evidence_tier from cited_ids \u2014 not caller-controlled. Runs claim verification on your content.",
 "inputSchema": {
  "properties": {
   "cited_ids": {
    "description": "Memory IDs cited \u2014 use the cited_memory_ids from snapshot_sources().",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "content": {
    "description": "Your synthesized snapshot content (markdown). Min 100 chars.",
    "minLength": 100,
    "type": "string"
   },
   "force": {
    "default": false,
    "description": "Skip event-driven regeneration gate. Default false.",
    "type": "boolean"
   },
   "topic": {
    "description": "Topic this snapshot covers.",
    "maxLength": 200,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "topic",
   "content",
   "cited_ids"
  ],
  "type": "object"
 },
 "name": "save_snapshot"
}
