{
 "description": "Resume an execution's events after a drop. Pass the last event id you actually processed.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "execution_id": {
    "minLength": 1,
    "type": "string"
   },
   "last_event_id": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "execution_id"
  ],
  "type": "object"
 },
 "name": "dreamlayer_events"
}
