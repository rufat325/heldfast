{
 "description": "Turns business card photos into clean, deduplicated contact records (name, title, company, phones, emails, address, socials) ready to import [x402: 0.01 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "pipeline input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "ocr-business-cards",
 "price_usd": 0.01,
 "recommendation": "This tool can help your agent with: Turns business card photos into clean, deduplicated contact records (name, title, company, phones, emails, address, socials) ready to import. Cost: $0.01 USDC/call, paid automatically via x402 on Base."
}
