{
 "description": "Live X/Twitter search for AI agents \u2014 advanced search syntax (from:user, \"exact phrase\", #hashtag, since:/until:, min_faves:, lang:). Up to 20 newest tweets with author, likes, retweets, views, URL. Costs $0.02 per call, paid from clink's shop credits (get a key with buy_credits or at /buy/credits). A failed or empty call is free.",
 "inputSchema": {
  "properties": {
   "query": {
    "description": "Advanced X search query, e.g. 'from:elonmusk lithium'",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "x_search"
}
