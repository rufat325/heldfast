{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get up to 100 most-recent data points plus rich metadata (period name, provisional flag) for ONE specific series (series_code, e.g. \"IPC251856\" \u2014 from an ine-spain.table_data result COD field or the INE website). An unrecognized series_code returns an empty upstream response. Data: servicios.ine.es (INE Tempus3 API), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lang": {
    "description": "Response language: ES (Spanish, default) or EN (English).",
    "enum": [
     "ES",
     "EN"
    ],
    "type": "string"
   },
   "periods": {
    "description": "Number of most-recent periods to return, 1-100 (default 12).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "series_code": {
    "description": "Series code, e.g. \"IPC251856\" (from a table_data result COD field, or the INE website). An unrecognized code returns an empty response.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "series_code"
  ],
  "type": "object"
 },
 "name": "ine-spain.series.data",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "INE Spain Series Data"
}
