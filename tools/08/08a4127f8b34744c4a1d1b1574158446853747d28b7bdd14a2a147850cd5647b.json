{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "\"Find research software for [task]\" / \"academic / open-source tools for [field]\" \u2014 search research software registrations in OpenAIRE. Use for finding citable scientific software tied to published research.",
 "inputSchema": {
  "examples": [
   {
    "query": "bioinformatics alignment tool"
   }
  ],
  "properties": {
   "page": {
    "description": "1-based page (default 1)",
    "type": "number"
   },
   "query": {
    "description": "Free-text query (matches title/abstract/keywords)",
    "type": "string"
   },
   "size": {
    "description": "Page size, 1-100 (default 20)",
    "type": "number"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_software",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of results in this response",
    "type": "number"
   },
   "page": {
    "description": "Current page number (1-based)",
    "type": [
     "number",
     "null"
    ]
   },
   "page_size": {
    "description": "Number of results per page",
    "type": [
     "number",
     "null"
    ]
   },
   "query": {
    "description": "The search query that was executed",
    "type": "string"
   },
   "results": {
    "description": "Array of software objects",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "total": {
    "description": "Total number of matching software items",
    "type": [
     "number",
     "null"
    ]
   }
  },
  "required": [
   "query",
   "total",
   "page",
   "page_size",
   "count",
   "results"
  ],
  "type": "object"
 },
 "title": "Search Software",
 "version": "v1.3b341894"
}
