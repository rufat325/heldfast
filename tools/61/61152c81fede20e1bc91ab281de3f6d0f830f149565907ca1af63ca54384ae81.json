{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get full metadata for a specific Data.gov dataset by its ID or name. Returns title, description, resources (download links), organization, tags, and update frequency.",
 "inputSchema": {
  "examples": [
   {
    "id": "noaa-global-monitoring-laboratory-carbon-cycle-greenhouse-gases"
   }
  ],
  "properties": {
   "id": {
    "description": "Dataset ID or name slug from Data.gov",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_dataset",
 "outputSchema": {
  "properties": {
   "description": {
    "description": "Full dataset description",
    "type": [
     "string",
     "null"
    ]
   },
   "id": {
    "description": "Dataset unique identifier",
    "type": [
     "string",
     "null"
    ]
   },
   "issued": {
    "description": "DCAT issued date \u2014 when the DATA was published. Replaces CKAN metadata_created, which recorded when the catalog RECORD was made; a different fact, so renamed rather than silently repurposed.",
    "type": [
     "string",
     "null"
    ]
   },
   "modified": {
    "description": "ISO timestamp of last metadata modification",
    "type": [
     "string",
     "null"
    ]
   },
   "name": {
    "description": "Dataset name slug",
    "type": [
     "string",
     "null"
    ]
   },
   "organization": {
    "description": "Publishing organization name",
    "type": [
     "string",
     "null"
    ]
   },
   "resources": {
    "items": {
     "properties": {
      "description": {
       "description": "Resource description (truncated to 200 chars)",
       "type": "string"
      },
      "format": {
       "description": "File format (e.g., CSV, JSON, XML)",
       "type": "string"
      },
      "id": {
       "description": "Resource unique identifier",
       "type": "string"
      },
      "name": {
       "description": "Resource name",
       "type": "string"
      },
      "url": {
       "description": "Download URL for the resource",
       "type": "string"
      }
     },
     "required": [
      "id",
      "name",
      "format",
      "url",
      "description"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "tags": {
    "description": "List of tag names associated with the dataset",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "title": {
    "description": "Human-readable dataset title",
    "type": [
     "string",
     "null"
    ]
   }
  },
  "required": [
   "id",
   "name",
   "title",
   "description",
   "organization",
   "tags",
   "resources",
   "modified"
  ],
  "type": "object"
 },
 "title": "Get Dataset",
 "version": "v1.fad95482"
}
