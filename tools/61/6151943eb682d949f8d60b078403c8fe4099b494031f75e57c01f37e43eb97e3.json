{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search or list ILOSTAT labor-statistics dataflows \u2014 ~1200 official series from the International Labour Organization covering employment, unemployment, wages, working time, labour force participation, informality, and child labour. Filter by name/id with query, or omit to list all (capped to 200 results). Use the returned dataflow_id with ilostat.structure and ilostat.data. Data: sdmx.ilo.org (ILOSTAT SDMX public REST API), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "query": {
    "description": "Filter dataflows by name or id substring, case-insensitive (e.g. \"unemployment\", \"wages\", \"child labour\"). Omit to list all ~1200 available dataflows (capped to 200 results).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ilostat.reference.dataflows",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "ILOSTAT Dataflows"
}
