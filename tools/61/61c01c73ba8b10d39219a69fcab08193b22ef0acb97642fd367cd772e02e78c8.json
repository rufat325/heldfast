{
 "description": "Retrieves a single webhook attempt log for the given Id.",
 "inputSchema": {
  "properties": {
   "webhookAttemptLogId": {
    "description": "The Id of the webhook attempt log to retrieve.",
    "type": "string"
   }
  },
  "required": [
   "webhookAttemptLogId"
  ],
  "type": "object"
 },
 "name": "retrieveWebhookAttemptLogWithId"
}
