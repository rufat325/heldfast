{
 "_meta": {
  "skill": "reputation"
 },
 "description": "Get the approved feedback index limit for a specific client of an agent. Returns the maximum index up to which the client is approved to submit feedback. No wallet required.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "agentId": {
    "description": "Agent ID",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "client": {
    "description": "Stacks address of the client (SP... mainnet or ST... testnet)",
    "pattern": "^S[PT][0-9A-Z]{38,39}$",
    "type": "string"
   }
  },
  "required": [
   "agentId",
   "client"
  ],
  "type": "object"
 },
 "name": "reputation_get_approved_limit"
}
