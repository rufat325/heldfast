{
 "_meta": {},
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Create subscription price"
 },
 "description": "Attach a price to a subscription (by price point and optionally territory). Set subscription availability first with create-subscription-availability for the territory, or you may get 409 ENTITY_ERROR.RELATIONSHIP.INVALID. Use price point IDs from list-subscription-price-points; when you used filter.territory there, pass that same territoryId here.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "preserveCurrentPrice": {
    "description": "If true, existing subscribers keep their current price when you add a new price",
    "type": "boolean"
   },
   "startDate": {
    "description": "ISO 8601 date when this price becomes effective. Omit for immediate.",
    "type": "string"
   },
   "subscriptionId": {
    "description": "Subscription ID (from create-subscription or list-subscription-group-subscriptions)",
    "type": "string"
   },
   "subscriptionPricePointId": {
    "description": "Price point ID from list-subscription-price-points for this subscription.",
    "type": "string"
   },
   "territoryId": {
    "description": "Territory ID (e.g. USA). When the price point came from list-subscription-price-points with filter.territory, pass that same territory here. Required by API for territory-specific price points; omit only if applying to all.",
    "type": "string"
   }
  },
  "required": [
   "subscriptionId",
   "subscriptionPricePointId"
  ],
  "type": "object"
 },
 "name": "create-subscription-price",
 "title": "Create subscription price"
}
