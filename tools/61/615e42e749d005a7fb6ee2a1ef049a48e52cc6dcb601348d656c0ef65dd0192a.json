{
 "description": "Migrate one or all projects from legacy wiki/ layout to v2 layout. Snapshots to archive/<projectId>-<unixTs>/ first; idempotent on re-run.",
 "inputSchema": {
  "properties": {
   "all": {
    "type": "boolean"
   },
   "dryRun": {
    "description": "If true, report what would happen without writing",
    "type": "boolean"
   },
   "projectId": {
    "description": "Specific project to migrate; omit + set all=true to migrate every project",
    "type": "string"
   },
   "user": {
    "description": "Contributor username for routing legacy notes; defaults to os.userInfo()",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "mindbase_migrate"
}
