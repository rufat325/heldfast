{
 "description": "Identify a Microsoft 365 SMTP AUTH error string and say what it means, what caused it, and whether an administrator can still undo the cause. Paste the line the server or the mail library printed.",
 "inputSchema": {
  "properties": {
   "text": {
    "description": "The error string, as printed.",
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "lookup_smtp_error"
}
