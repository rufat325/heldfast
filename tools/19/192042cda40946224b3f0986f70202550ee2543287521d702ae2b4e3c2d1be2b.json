{
 "description": "Read a local .env file and apply its key/value pairs to a Code Engine app as literal env variables (merge by default), OR store them as a generic secret. Skips comments and blank lines. Never logs secret values.",
 "inputSchema": {
  "properties": {
   "app_name": {
    "description": "Target app (required when target=app)",
    "type": "string"
   },
   "dotenv_path": {
    "description": "Path to the .env file (default: .env in the current working directory)",
    "type": "string"
   },
   "project_id": {
    "type": "string"
   },
   "replace": {
    "description": "When target=app, replace all env vars instead of merging (default: false = merge)",
    "type": "boolean"
   },
   "secret_name": {
    "description": "Secret name when target=secret",
    "type": "string"
   },
   "target": {
    "description": "Apply as app env vars or create/replace a generic secret (default: app)",
    "enum": [
     "app",
     "secret"
    ],
    "type": "string"
   }
  },
  "required": [
   "project_id"
  ],
  "type": "object"
 },
 "name": "ce_sync_env_from_dotenv"
}
