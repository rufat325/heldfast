{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run a raw SoQL query against any Kansas City, MO open-data resource (data.kcmo.org) by its Socrata id (8-char like \"vsgj-uufz\"). Full SoQL: where/select/group/order/limit/offset. Use kcmo_datasets to find a resource id, or kcmo_recent for the common ones.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-data-kcmo-api-key",
    "limit": 100,
    "resource_id": "vsgj-uufz",
    "select": "report_date, offense_type"
   },
   {
    "_apiKey": "your-data-kcmo-api-key",
    "limit": 250,
    "order": "created_date DESC",
    "resource_id": "xyz12345",
    "where": "created_date > '2024-01-01'"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Socrata app token for higher rate limits. Omit to use the keyless endpoint.",
    "type": "string"
   },
   "group": {
    "description": "SoQL $group.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (default 100, max 5000).",
    "type": "number"
   },
   "offset": {
    "description": "Row offset for paging.",
    "type": "number"
   },
   "order": {
    "description": "SoQL $order.",
    "type": "string"
   },
   "resource_id": {
    "description": "Socrata resource id, e.g. \"vsgj-uufz\".",
    "type": "string"
   },
   "select": {
    "description": "SoQL $select.",
    "type": "string"
   },
   "where": {
    "description": "SoQL $where filter.",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "kcmo_query",
 "title": "Kcmo Query",
 "version": "v1.112ad701"
}
