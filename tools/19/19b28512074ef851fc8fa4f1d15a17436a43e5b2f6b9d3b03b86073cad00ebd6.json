{
 "description": "Screenshot the screen or one app. Password fields and password-manager windows are blacked out BEFORE the image is written, so they never reach the model. Set redact=false only if you know the screen holds no secrets.",
 "inputSchema": {
  "properties": {
   "app": {
    "description": "Bundle ID or app name. Omit for the whole screen.",
    "type": "string"
   },
   "maxWidth": {
    "description": "Scale down to this width in pixels. Default 1400.",
    "type": "number"
   },
   "redact": {
    "description": "Default true. Blacks out secure fields.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "computer_screenshot"
}
