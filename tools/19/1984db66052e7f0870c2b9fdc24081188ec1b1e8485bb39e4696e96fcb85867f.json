{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Convert a Bluesky handle to its DID (decentralized identifier). Returns the DID for programmatic account lookups.",
 "inputSchema": {
  "examples": [
   {
    "handle": "alice.bsky.social"
   },
   {
    "handle": "jack.bsky.social"
   }
  ],
  "properties": {
   "handle": {
    "description": "Bluesky handle to resolve",
    "type": "string"
   }
  },
  "required": [
   "handle"
  ],
  "type": "object"
 },
 "name": "resolve_handle",
 "outputSchema": {
  "properties": {
   "did": {
    "description": "The decentralized identifier for the handle",
    "type": "string"
   },
   "handle": {
    "description": "The Bluesky handle",
    "type": "string"
   }
  },
  "required": [
   "handle",
   "did"
  ],
  "type": "object"
 },
 "title": "Resolve Handle",
 "version": "v1.42063465"
}
