{
 "description": "Search Google Maps places through Outscraper.\n\nBest for:\n- ad hoc place discovery from one or more Google Maps queries\n- cases where the user thinks in Google Maps terms rather than /businesses filters\n- retrieving place results directly from the Google Maps search pipeline\n\nPrefer this tool when:\n- the user gives Google Maps-style queries such as \"restaurants brooklyn usa\"\n- you want place discovery without building structured business filters first\n- you want async submissions for larger query batches or enriched Google Maps searches\n\nUse businesses_search instead when:\n- you want normalized businesses filters or cursor pagination\n- you want to combine strict filters with natural-language business search\n\nUse execution_mode=\"auto\" when:\n- there are multiple queries\n- the limit is high\n- enrichments are requested",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "async": {
    "description": "Deprecated compatibility flag. Prefer execution_mode.",
    "type": "boolean"
   },
   "coordinates": {
    "description": "Optional latitude,longitude coordinates bias supported by Outscraper.",
    "type": "string"
   },
   "drop_duplicates": {
    "description": "Drop duplicate places across results.",
    "type": "boolean"
   },
   "enrichment": {
    "description": "Optional enrichment names supported by Outscraper.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "execution_mode": {
    "description": "Execution strategy. Use auto to let the MCP server choose between sync and async.",
    "enum": [
     "auto",
     "sync",
     "async"
    ],
    "type": "string"
   },
   "fields": {
    "description": "Specific place fields to return.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "language": {
    "description": "Language code, for example en.",
    "type": "string"
   },
   "limit": {
    "description": "Organizations per query limit.",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "One or more Google Maps queries or place ids.",
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "region": {
    "description": "Region code, for example us.",
    "type": "string"
   },
   "skip": {
    "description": "Skip places count.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "webhook": {
    "description": "Optional webhook URL for async completion.",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "google_maps_search",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "async": {
    "additionalProperties": false,
    "properties": {
     "id": {
      "type": "string"
     },
     "is_async_submission": {
      "type": "boolean"
     },
     "next_step": {
      "type": "string"
     },
     "results_location": {
      "type": "string"
     },
     "status": {
      "type": "string"
     }
    },
    "required": [
     "is_async_submission"
    ],
    "type": "object"
   },
   "data": {},
   "meta": {
    "additionalProperties": false,
    "properties": {
     "operation": {
      "type": "string"
     },
     "service": {
      "type": "string"
     }
    },
    "required": [
     "service",
     "operation"
    ],
    "type": "object"
   }
  },
  "required": [
   "data",
   "meta"
  ],
  "type": "object"
 },
 "title": "Google Maps Search"
}
