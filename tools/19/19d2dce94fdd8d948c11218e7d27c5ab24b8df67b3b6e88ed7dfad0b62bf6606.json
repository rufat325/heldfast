{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch annual macroeconomic time-series data from the ADB Key Indicators Database for any combination of Asian Development Bank member economies and SDMX indicators. Returns structured observations with values, units, data sources, and footnotes. Covers GDP, population, inflation, exchange rates, trade, energy, SDGs, and 700+ other indicators for 50 Asia-Pacific economies (China=PRC, India=IND, Japan=JPN, Indonesia=INO, etc.) from 1960s onward. Workflow: (1) adbkidb.dataflows_list to pick a topic, (2) adbkidb.indicators to find indicator codes, (3) this tool to fetch values. Example: dataflow=EO_NA, indicator_codes=[NGDP_XDC], economy_codes=[PRC,IND], start_year=2015 returns GDP at current prices for China and India from 2015 to present. Source: kidb.adb.org SDMX v3.0 API \u2014 ADB open data, no auth, 20 req/min.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataflow": {
    "description": "KIDB dataflow code (e.g. \"EO_NA\" for National Accounts, \"PPL_POP\" for Population, \"MFP_XR\" for Exchange Rates). Use adbkidb.dataflows_list to discover codes, then adbkidb.indicators to find indicator codes within the dataflow.",
    "minLength": 1,
    "type": "string"
   },
   "economy_codes": {
    "description": "One or more ADB economy codes (e.g. [\"PRC\"] for China, [\"IND\"] for India, [\"JPN\"] for Japan, [\"PRC\",\"IND\",\"INO\"] for multiple). Use adbkidb.economies to list all 50 codes. Maximum 10.",
    "items": {
     "maxLength": 3,
     "minLength": 2,
     "type": "string"
    },
    "maxItems": 10,
    "minItems": 1,
    "type": "array"
   },
   "end_year": {
    "description": "End year for the data range (e.g. 2024). Defaults to current year. Annual frequency only (A).",
    "maximum": 2030,
    "minimum": 1960,
    "type": "integer"
   },
   "indicator_codes": {
    "description": "One or more indicator codes within the dataflow (e.g. [\"NGDP_XDC\"] for GDP at current prices in EO_NA, [\"LP_PE_NUM_MOP\"] for population in PPL_POP). Use adbkidb.indicators to discover codes. Maximum 10.",
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 10,
    "minItems": 1,
    "type": "array"
   },
   "start_year": {
    "description": "Start year for the data range (e.g. 2010). Data availability varies by indicator; most series begin 1990\u20132000. Defaults to 2000.",
    "maximum": 2030,
    "minimum": 1960,
    "type": "integer"
   }
  },
  "required": [
   "dataflow",
   "indicator_codes",
   "economy_codes"
  ],
  "type": "object"
 },
 "name": "adbkidb.data.query",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "ADB KIDB \u2014 Query Economic Data"
}
