{
 "description": "Update an existing Code Engine job definition (PATCH)",
 "inputSchema": {
  "properties": {
   "env_vars": {
    "description": "Key/value environment variables",
    "type": "object"
   },
   "image": {
    "description": "New container image reference",
    "type": "string"
   },
   "image_secret": {
    "type": "string"
   },
   "job_name": {
    "type": "string"
   },
   "project_id": {
    "type": "string"
   },
   "scale_array_spec": {
    "description": "Array indices to run (e.g. \"0-9\")",
    "type": "string"
   },
   "scale_cpu_limit": {
    "type": "string"
   },
   "scale_memory_limit": {
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "job_name"
  ],
  "type": "object"
 },
 "name": "ce_update_job"
}
