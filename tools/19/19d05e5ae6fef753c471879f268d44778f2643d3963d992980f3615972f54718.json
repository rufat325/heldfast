{
 "description": "Search tracked creators by name or handle \u2014 exact \u2192 prefix \u2192 substring \u2192 typo-tolerant fuzzy. Returns { query, count, results } where each result includes the creator's name, slug, handle, niche, and whether they are breaking out right now. Use the returned `slug` with the `creator` tool for the full profile.",
 "inputSchema": {
  "properties": {
   "q": {
    "description": "The name or handle to search for.",
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "search_creators"
}
