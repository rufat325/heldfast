{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Save an article as an unpublished draft.\n\nNothing becomes visible to readers: use this whenever the work still needs review, and publish_article only when it should go live. The draft can be edited afterwards with update_article.\n\nEach call creates a NEW draft \u2014 not idempotent, so calling twice leaves two drafts. Requires an API key. Returns the draft with an editor_url for finishing it in the browser.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "body_markdown": {
    "description": "Article body so far, as Markdown.",
    "minLength": 1,
    "type": "string"
   },
   "tags": {
    "description": "Topic tags to carry through to publication.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "title": {
    "description": "Working headline. Can be changed before publishing.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "title",
   "body_markdown"
  ],
  "type": "object"
 },
 "name": "create_draft",
 "title": "Create a draft"
}
