{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch the public bookshelves for a Google account (by numeric user_id). Omit shelf to list all public shelves; provide a numeric shelf id to list the volumes on that specific shelf.",
 "inputSchema": {
  "examples": [
   {
    "user_id": "118212570460635920423"
   },
   {
    "shelf": "1001",
    "user_id": "118212570460635920423"
   }
  ],
  "properties": {
   "shelf": {
    "description": "Shelf id (numeric) \u2014 omit to list all public shelves",
    "type": "string"
   },
   "user_id": {
    "description": "Google account numeric user id",
    "type": "string"
   }
  },
  "required": [
   "user_id"
  ],
  "type": "object"
 },
 "name": "list_bookshelf",
 "outputSchema": {
  "oneOf": [
   {
    "properties": {
     "kind": {
      "description": "Resource kind (books#bookshelves)",
      "type": "string"
     },
     "shelves": {
      "description": "List of public bookshelves",
      "items": {
       "properties": {
        "created": {
         "description": "Shelf creation timestamp",
         "type": "string"
        },
        "description": {
         "description": "Shelf description",
         "type": "string"
        },
        "id": {
         "description": "Shelf numeric id",
         "type": "number"
        },
        "selfLink": {
         "description": "Link to shelf resource",
         "type": "string"
        },
        "title": {
         "description": "Shelf display title",
         "type": "string"
        },
        "updated": {
         "description": "Shelf last update timestamp",
         "type": "string"
        },
        "volumeCount": {
         "description": "Number of volumes in shelf",
         "type": "number"
        }
       },
       "type": "object"
      },
      "type": "array"
     }
    },
    "type": "object"
   },
   {
    "properties": {
     "items": {
      "description": "Array of volumes in specified shelf",
      "items": {
       "properties": {
        "etag": {
         "description": "Entity tag for caching",
         "type": "string"
        },
        "id": {
         "description": "Volume id",
         "type": "string"
        },
        "kind": {
         "description": "Resource kind (books#volume)",
         "type": "string"
        },
        "selfLink": {
         "description": "Link to volume resource",
         "type": "string"
        },
        "volumeInfo": {
         "description": "Volume metadata",
         "type": "object"
        }
       },
       "type": "object"
      },
      "type": "array"
     },
     "kind": {
      "description": "Resource kind (books#volumes)",
      "type": "string"
     },
     "totalItems": {
      "description": "Total volumes in shelf",
      "type": "number"
     }
    },
    "type": "object"
   }
  ]
 },
 "title": "List Bookshelf",
 "version": "v1.315444cd"
}
