{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-teams-catalog-app"
 },
 "description": "Get teamsApps from appCatalogs\n\nTIP: Gets a specific app from the Teams app catalog. Use $expand=appDefinitions to get version details.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "teamsAppId": {
    "description": "Path parameter: teamsAppId",
    "type": "string"
   }
  },
  "required": [
   "teamsAppId"
  ],
  "type": "object"
 },
 "name": "get-teams-catalog-app"
}
