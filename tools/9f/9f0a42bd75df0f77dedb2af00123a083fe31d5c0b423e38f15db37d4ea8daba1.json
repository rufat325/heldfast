{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "Rotate Vault Key"
 },
 "description": "Rotate encryption keys for a vault entry. The credential is decrypted with current keys and immediately re-encrypted with fresh keys. Old entry is deactivated. Returns old and new entry IDs.",
 "inputSchema": {
  "properties": {
   "entry_id": {
    "description": "UUID of the vault entry to rotate",
    "type": "string"
   }
  },
  "required": [
   "entry_id"
  ],
  "type": "object"
 },
 "name": "vault_rotate"
}
