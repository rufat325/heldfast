{
 "description": "Validate a model. errors.spec = schema/semantic problems (fix these). errors.compile = spec-valid features compiler 0.1 cannot build yet (redesign around them; see capability card). Run after EVERY edit.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "modelYaml": {
    "description": "Full app.acm.yaml content",
    "type": "string"
   }
  },
  "required": [
   "modelYaml"
  ],
  "type": "object"
 },
 "name": "validate"
}
