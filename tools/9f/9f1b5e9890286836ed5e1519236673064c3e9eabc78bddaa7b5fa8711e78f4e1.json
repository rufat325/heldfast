{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Browse the e-Stat high-level data catalog (dataset/table groupings) with an optional free-text filter. Returns catalog entries with dataset IDs, names, and organization metadata. This endpoint is slow at e-Stat and often exceeds its 35s budget, in which case it returns {found:false, reason:\"upstream_timeout\"}; search_stats answers \"which Japanese statistics table covers X\" faster. Requires _apiKey.",
 "inputSchema": {
  "examples": [
   {
    "query": "\u52b4\u50cd"
   },
   {
    "lang": "E",
    "limit": 15
   }
  ],
  "properties": {
   "data_type": {
    "description": "Restrict to one file format: XLS | CSV | PDF | XML | XLS_REP | DB",
    "type": "string"
   },
   "lang": {
    "description": "J (Japanese, default) | E (English)",
    "type": "string"
   },
   "limit": {
    "description": "Page size, 1-100. Defaults to 20 when omitted, so a bare call is already bounded.",
    "type": "number"
   },
   "query": {
    "description": "Optional free-text filter, Japanese or English",
    "type": "string"
   },
   "start_position": {
    "description": "1-based offset (default 1)",
    "type": "number"
   },
   "stats_code": {
    "description": "Government statistics code, e.g. \"00200521\" (population census)",
    "type": "string"
   },
   "stats_field": {
    "description": "Statistical field code, 2 digits (major) or 4 digits (minor)",
    "type": "string"
   },
   "survey_years": {
    "description": "Survey period: yyyy, yyyymm, or yyyymm-yyyymm",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_data_catalog",
 "outputSchema": {
  "description": "Response from e-Stat getDataCatalog endpoint containing table groupings",
  "type": "object"
 },
 "title": "List Data Catalog",
 "version": "v1.3579a238"
}
