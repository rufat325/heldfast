{
 "description": "Create a Webpay Plus transaction. Returns { token, url } \u2014 redirect the user to url?token_ws=<token> to complete payment. After the user returns to return_url, call webpay_commit_transaction.",
 "inputSchema": {
  "properties": {
   "amount": {
    "description": "Amount in CLP (integer, no decimals)",
    "type": "number"
   },
   "buy_order": {
    "description": "Merchant-side order id (max 26 chars)",
    "type": "string"
   },
   "return_url": {
    "description": "URL Transbank redirects the user back to after payment",
    "type": "string"
   },
   "session_id": {
    "description": "Merchant-side session id (max 61 chars)",
    "type": "string"
   }
  },
  "required": [
   "buy_order",
   "session_id",
   "amount",
   "return_url"
  ],
  "type": "object"
 },
 "name": "webpay_create_transaction"
}
