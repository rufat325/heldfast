{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch observations from a table (TypedDataSet). Supports OData v3 query params. Use $select to pick columns (from table_dimensions keys) and $filter to subset, e.g. filter=\"Periods eq '2020JJ00'\". Always set a $top to avoid huge responses.",
 "inputSchema": {
  "examples": [
   {
    "select": "Periods,TotalPopulation_1",
    "table": "37296eng",
    "top": 100
   },
   {
    "filter": "Periods eq '2023JJ00'",
    "select": "Periods,TotalPopulation_1",
    "table": "37296eng",
    "top": 50
   }
  ],
  "properties": {
   "filter": {
    "description": "OData $filter, e.g. \"Periods eq '2020JJ00'\" or \"substringof('2020',Periods)\".",
    "type": "string"
   },
   "select": {
    "description": "OData $select, comma-separated dimension/topic keys, e.g. \"Periods,TotalPopulation_1\".",
    "type": "string"
   },
   "skip": {
    "description": "OData $skip, rows to skip for paging.",
    "type": "number"
   },
   "table": {
    "description": "CBS table id, e.g. \"37296eng\".",
    "type": "string"
   },
   "top": {
    "description": "OData $top, max rows to return (default 50).",
    "type": "number"
   }
  },
  "required": [
   "table"
  ],
  "type": "object"
 },
 "name": "get_data",
 "title": "Get Data",
 "version": "v1.99aca0da"
}
