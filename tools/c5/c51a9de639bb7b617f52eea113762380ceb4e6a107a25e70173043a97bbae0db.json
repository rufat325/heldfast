{
 "description": "Bulk-revoke ALL certificates of a given type. Distribution types are blocked at the helper level for safety. Default is DEVELOPMENT \u2014 the most common \"blow it all away and let Xcode rebuild\" recovery path when local keychain ACLs are broken.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "certificateType": {
    "default": "DEVELOPMENT",
    "description": "Type of certificates to revoke (Distribution types are blocked)",
    "enum": [
     "IOS_DEVELOPMENT",
     "MAC_APP_DEVELOPMENT",
     "DEVELOPMENT"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "asc_revoke_certificates_by_type"
}
