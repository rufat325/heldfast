{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Convenience: list the most recent HM Land Registry property sales for a single UK postcode (England & Wales). Returns sale price (\u00a3GBP), date, address, property type, tenure and new-build flag, newest sale first. Postcode must be UPPERCASE with a space, e.g. \"SW1A 1AA\".",
 "inputSchema": {
  "examples": [
   {
    "postcode": "WA2 8SN"
   },
   {
    "limit": 50,
    "postcode": "M1 1AD"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max records to return, 1-500. Default 25.",
    "type": "number"
   },
   "postcode": {
    "description": "UK postcode, UPPERCASE with a space, e.g. \"WA2 8SN\".",
    "type": "string"
   }
  },
  "required": [
   "postcode"
  ],
  "type": "object"
 },
 "name": "lookup_postcode",
 "title": "Lookup Postcode",
 "version": "v1.27e2e727"
}
