{
 "description": "Answer \"can I work remotely from <country>?\" \u2014 returns how many live jobs accept applicants in that country and how many are location-independent (no country restriction). This is eligibility, not a hiring guarantee.",
 "inputSchema": {
  "properties": {
   "country": {
    "description": "ISO-2 country code, e.g. \"PT\", \"US\", \"DE\"",
    "type": "string"
   }
  },
  "required": [
   "country"
  ],
  "type": "object"
 },
 "name": "work_anywhere_scope"
}
