{
 "description": "Render HTML (and optional CSS) into a PNG/JPEG/WebP image using codetoimage.app. Instead of raw HTML you can pass `template_id` (a template saved in the dashboard) plus `variables` to fill its {{placeholders}}, and `preset` for named platform sizes (og, instagram-story, ...). Returns the image inline so the conversation can display it directly. Use this when you need to *see* or hand back a finished image (cards, OG previews, charts, badges). If the caller needs a URL to embed elsewhere (Instagram Graph API, OpenGraph meta, Slack unfurl), use `render_html_to_url` instead.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "background": {
    "description": "Background color, e.g. '#ffffff' or 'rgb(10,10,10)'.",
    "maxLength": 100,
    "type": "string"
   },
   "css": {
    "description": "Optional CSS injected as a <style> block.",
    "maxLength": 500000,
    "type": "string"
   },
   "format": {
    "description": "Output format. Default: png.",
    "enum": [
     "png",
     "jpeg",
     "webp"
    ],
    "type": "string"
   },
   "height": {
    "description": "Viewport height in pixels (default 600).",
    "maximum": 2048,
    "minimum": 1,
    "type": "integer"
   },
   "html": {
    "description": "Full HTML (typically including <body>\u2026</body>). Inline styles work; for larger CSS use the `css` field. Required unless `template_id` is provided.",
    "maxLength": 1000000,
    "minLength": 1,
    "type": "string"
   },
   "preset": {
    "description": "Named viewport size: og 1200\u00d7630, twitter-card 1200\u00d7628, facebook-post 1200\u00d7630, linkedin-post 1200\u00d7627, instagram-square 1080\u00d71080, instagram-portrait 1080\u00d71350, instagram-story 1080\u00d71920, youtube-thumbnail 1280\u00d7720, pinterest-pin 1000\u00d71500. One template, many sizes. Explicit width/height override the preset.",
    "enum": [
     "og",
     "twitter-card",
     "facebook-post",
     "linkedin-post",
     "instagram-square",
     "instagram-portrait",
     "instagram-story",
     "youtube-thumbnail",
     "pinterest-pin"
    ],
    "type": "string"
   },
   "quality": {
    "description": "Encoder quality (JPEG/WebP only, 1-100).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "template_id": {
    "description": "ID of a template saved in the codetoimage dashboard (Templates page). Use INSTEAD of `html` \u2013 the saved HTML/CSS renders with {{placeholders}} filled from `variables`.",
    "format": "uuid",
    "type": "string"
   },
   "transparent": {
    "description": "Transparent background (PNG/WebP only; paid plans).",
    "type": "boolean"
   },
   "variables": {
    "additionalProperties": {
     "maxLength": 10000,
     "type": "string"
    },
    "description": "Values for {{placeholders}} in the template (or in `html` if provided). Values are HTML-escaped server-side. Missing placeholders render as empty.",
    "type": "object"
   },
   "width": {
    "description": "Viewport width in pixels (default 800).",
    "maximum": 2048,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "render_html_to_image"
}
