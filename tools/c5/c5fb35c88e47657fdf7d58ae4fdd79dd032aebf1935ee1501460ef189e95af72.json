{
 "description": "Search an agent's archived knowledge files, or manage the memory sidecar index.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "action": {
    "default": "search",
    "description": "Action: \"search\" (default) to query knowledge; \"rebuild_index\" for a full BM25 sidecar rebuild; \"rebuild_md\" to regenerate MEMORY.md from the index.",
    "enum": [
     "search",
     "rebuild_index",
     "rebuild_md"
    ],
    "type": "string"
   },
   "agent_id": {
    "description": "Agent ID to search knowledge for",
    "type": "string"
   },
   "max_results": {
    "default": 3,
    "description": "Max results (default 3, max 10)",
    "maximum": 10,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Search query (max 500 chars)",
    "maxLength": 500,
    "type": "string"
   }
  },
  "required": [
   "agent_id",
   "query"
  ],
  "type": "object"
 },
 "name": "gossip_remember"
}
