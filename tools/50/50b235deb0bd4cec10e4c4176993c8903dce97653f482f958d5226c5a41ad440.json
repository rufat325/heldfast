{
 "description": "Publish or unschedule content on a connected CMS. Check connections first with list_cms_connections.",
 "inputSchema": {
  "properties": {
   "content_id": {
    "description": "The content ID to publish",
    "type": "string"
   },
   "destination": {
    "description": "Target CMS platform",
    "enum": [
     "frase_cms",
     "wordpress",
     "webflow",
     "sanity",
     "wix"
    ],
    "type": "string"
   },
   "force": {
    "description": "Override a warning-only rules refusal.",
    "type": "boolean"
   },
   "scheduled_at": {
    "description": "UTC (Z) ISO 8601 datetime for scheduled publishing",
    "type": "string"
   },
   "site_id": {
    "description": "Site ID with the CMS connection (see list_sites).",
    "type": "string"
   },
   "status": {
    "description": "default: publish",
    "enum": [
     "publish",
     "draft"
    ],
    "type": "string"
   },
   "unschedule": {
    "description": "true = clear the scheduled publish instead.",
    "type": "boolean"
   }
  },
  "required": [
   "content_id"
  ],
  "type": "object"
 },
 "name": "publish_content"
}
