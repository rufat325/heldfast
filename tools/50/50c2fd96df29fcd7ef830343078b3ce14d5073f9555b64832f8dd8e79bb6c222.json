{
 "annotations": {
  "readOnlyHint": true,
  "title": "Get the distribution queue"
 },
 "description": "List your account's distribution queue, paginated \u2014 one entry per (release, outlet) delivery with its status (e.g. pending review, processing, scheduled, complete, error). Filter by `release_id`, `outlet_id`, or `status`.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "outlet_id": {
    "description": "Filter to one outlet/store.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "page": {
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "per_page": {
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "release_id": {
    "description": "Filter to one release.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "response_format": {
    "description": "'concise' (default) keeps high-signal fields and ids; 'detailed' is the verbatim response.",
    "enum": [
     "concise",
     "detailed"
    ],
    "type": "string"
   },
   "status": {
    "description": "Filter by delivery status.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_delivery_queue",
 "title": "Get the distribution queue"
}
