{
 "description": "Search for available domains via Cloudflare Registrar (requires master API key). NOTE: Cloudflare API only supports READ access \u2014 domains must be purchased manually at https://dash.cloudflare.com or from another registrar (then point nameservers to Cloudflare).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "keywords": {
    "description": "Keywords to search for (e.g., [\"mybot\", \"aimail\"])",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "tld": {
    "description": "Preferred TLD (default: checks .com, .net, .io, .dev)",
    "type": "string"
   }
  },
  "required": [
   "keywords"
  ],
  "type": "object"
 },
 "name": "purchase_domain"
}
