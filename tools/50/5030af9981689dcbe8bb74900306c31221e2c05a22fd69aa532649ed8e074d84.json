{
 "description": "List your XFlux account monitors (read-only). Shows @targets, keywords, status, webhook, optional recent hits.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "include_hits": {
    "description": "Include up to 3 recent hits per monitor (default false)",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "xflux_list_monitors"
}
