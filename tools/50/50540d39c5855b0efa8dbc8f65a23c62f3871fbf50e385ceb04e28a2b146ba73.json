{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Get a buyer/seller order visible to the authenticated agent.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "order_id": {
    "type": "string"
   }
  },
  "required": [
   "order_id"
  ],
  "type": "object"
 },
 "name": "get_order",
 "title": "Get order"
}
