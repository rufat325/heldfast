{
 "description": "Search self-storage facilities by city/state or ZIP code. Find storage units with pricing, sizes, and availability from SelfStorage.com.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "city": {
    "description": "City name (e.g. \"Los Angeles\")",
    "type": "string"
   },
   "limit": {
    "default": 20,
    "description": "Results per page (max 50)",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "page": {
    "default": 1,
    "description": "Page number",
    "minimum": 1,
    "type": "integer"
   },
   "state": {
    "description": "State abbreviation (e.g. \"CA\")",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_storage"
}
