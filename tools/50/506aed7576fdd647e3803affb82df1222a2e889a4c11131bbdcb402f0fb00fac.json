{
 "annotations": {
  "title": "Upload a catalog image"
 },
 "description": "Upload a label image (logo, dark-mode logo, or background) or an artist photo from a local image file, per `target`.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "file_path": {
    "description": "Local path to the image file.",
    "type": "string"
   },
   "id": {
    "description": "The label id (label_*) or artist id (artist_photo).",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "target": {
    "description": "Which image asset to upload.",
    "enum": [
     "label_logo",
     "label_logo_dark",
     "label_background",
     "artist_photo"
    ],
    "type": "string"
   }
  },
  "required": [
   "target",
   "id",
   "file_path"
  ],
  "type": "object"
 },
 "name": "upload_image",
 "title": "Upload a catalog image"
}
