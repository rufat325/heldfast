{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Add a new record to an Airtable table with specified field values. Returns the created record ID and full record data.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-airtable-api-key",
    "baseId": "appXXXXXXXXXXXX",
    "fields": {
     "Name": "New Project",
     "Owner": "John Doe",
     "Status": "In Progress"
    },
    "tableIdOrName": "Tasks"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Airtable personal access token",
    "type": "string"
   },
   "baseId": {
    "description": "Airtable base ID",
    "type": "string"
   },
   "fields": {
    "description": "Object of field name/value pairs to set on the new record",
    "type": "object"
   },
   "tableIdOrName": {
    "description": "Table ID or name",
    "type": "string"
   }
  },
  "required": [
   "_apiKey",
   "baseId",
   "tableIdOrName",
   "fields"
  ],
  "type": "object"
 },
 "name": "airtable_create_record",
 "outputSchema": {
  "properties": {
   "createdTime": {
    "description": "ISO timestamp when record was created",
    "type": "string"
   },
   "fields": {
    "description": "Field values of the created record",
    "type": "object"
   },
   "id": {
    "description": "ID of the newly created record",
    "type": "string"
   }
  },
  "required": [
   "id",
   "fields",
   "createdTime"
  ],
  "type": "object"
 },
 "title": "Airtable Create Record",
 "version": "v1.428bea08"
}
