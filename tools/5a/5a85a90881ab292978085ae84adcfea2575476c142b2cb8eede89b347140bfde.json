{
 "description": "Fetch the current status and latest result payload for a wan task.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "Asynchronous endpoint the task was created on.",
    "enum": [
     "animate",
     "edit_video",
     "image_to_video",
     "speech_to_video",
     "text_to_image",
     "text_to_video"
    ],
    "type": "string"
   },
   "task_id": {
    "description": "Task id returned when the task was created.",
    "type": "string"
   }
  },
  "required": [
   "task_id",
   "action"
  ],
  "type": "object"
 },
 "name": "get_task"
}
