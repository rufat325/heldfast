{
 "description": "Scan a path and return a deterministic, prioritized post-quantum migration plan: findings grouped by algorithm, ordered harvest-now-decrypt-later first, each with its PQC target and the readiness-score impact. Reads the filesystem, so it is gated like scan_path over HTTP.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "path": {
    "description": "Absolute or relative path to a file or directory to plan a migration for.",
    "type": "string"
   }
  },
  "required": [
   "path"
  ],
  "type": "object"
 },
 "name": "plan_migration"
}
