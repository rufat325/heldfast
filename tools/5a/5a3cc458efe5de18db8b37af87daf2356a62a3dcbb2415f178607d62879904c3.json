{
 "description": "Annotate a declared table or one of its columns -- the next describe merges this back in (an annotation's role wins over the inferred one; unit/description are added; hidden marks a column to omit from a summary). key must be one of role, unit, description, hidden.",
 "inputSchema": {
  "properties": {
   "column": {
    "description": "Column name to annotate; omit for a table-level annotation.",
    "type": "string"
   },
   "key": {
    "description": "One of role, unit, description, hidden.",
    "type": "string"
   },
   "table": {
    "description": "Name of the declared table to annotate.",
    "type": "string"
   },
   "tenant_key": {
    "description": "The key `signup` returned. Required only when this connection carries no Authorization: Bearer header -- when both are present, the header wins.",
    "type": "string"
   },
   "value": {
    "description": "The annotation's value."
   }
  },
  "required": [
   "table",
   "key",
   "value"
  ],
  "type": "object"
 },
 "name": "host.table.model_set"
}
