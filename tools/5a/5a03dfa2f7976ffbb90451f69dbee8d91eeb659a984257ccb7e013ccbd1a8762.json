{
 "description": "[PUBLISH] Publish a specific GTM container version to live. Requires GTM_MCP_ENABLE_PUBLISH=true and confirm=true. \u26a0\ufe0f  This immediately makes the version live in production.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "type": "string"
   },
   "confirm": {
    "description": "Must be true to confirm this publish operation.",
    "type": "boolean"
   },
   "containerId": {
    "type": "string"
   },
   "containerVersionId": {
    "description": "The version ID to publish.",
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "containerId",
   "containerVersionId",
   "confirm"
  ],
  "type": "object"
 },
 "name": "versions_publish"
}
