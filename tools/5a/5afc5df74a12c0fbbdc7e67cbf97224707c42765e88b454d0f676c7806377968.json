{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find upcoming electronic/dance events, concerts and festivals for a region. Specify a region either by latitude+longitude (returns events within ~75 miles) or by location_ids. Optionally filter by date window, name, and festivals-only.",
 "inputSchema": {
  "examples": [
   {
    "latitude": 37.77,
    "limit": 50,
    "longitude": -122.42,
    "state": "CA"
   },
   {
    "end_date": "2024-08-31",
    "festivals_only": true,
    "location_ids": "nyc,la",
    "start_date": "2024-06-01"
   }
  ],
  "properties": {
   "end_date": {
    "description": "Latest event date, YYYY-MM-DD.",
    "type": "string"
   },
   "event_name": {
    "description": "Filter by event/festival name keyword.",
    "type": "string"
   },
   "festivals_only": {
    "description": "If true, return only festivals.",
    "type": "boolean"
   },
   "latitude": {
    "description": "Region center latitude (pair with longitude), e.g. 37.77 for SF.",
    "type": "number"
   },
   "limit": {
    "description": "Max events to return (1-200, default 50).",
    "type": "number"
   },
   "location_ids": {
    "description": "Comma-separated EDMtrain location ids (from the locations tool). Alternative to lat/long.",
    "type": "string"
   },
   "longitude": {
    "description": "Region center longitude (pair with latitude), e.g. -122.42 for SF.",
    "type": "number"
   },
   "start_date": {
    "description": "Earliest event date, YYYY-MM-DD.",
    "type": "string"
   },
   "state": {
    "description": "Optional 2-letter state filter when using lat/long, e.g. \"CA\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "events",
 "title": "Events",
 "version": "v1.41775058"
}
