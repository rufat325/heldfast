{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Save Job"
 },
 "description": "Save a job to the authenticated user's saved-jobs list on HireJack (or remove it with action='unsave'). Saved jobs appear at hirejack.com/saved.html and feed batch match scoring. Idempotent \u2014 saving an already-saved job is a no-op. Use when the user says 'save this one', 'bookmark these three', or 'remove that from my saved jobs'. Not for tracking an actual application (`track_application`) or following companies (`watch_company`).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "Default 'save'. 'unsave' removes a previously saved job.",
    "enum": [
     "save",
     "unsave"
    ],
    "type": "string"
   },
   "domain": {
    "description": "Company domain (e.g. 'stripe.com'). Required unless `url` is provided.",
    "type": "string"
   },
   "jobId": {
    "description": "Pass the `id` field from a `search_jobs` result VERBATIM. Required unless `url` is provided.",
    "type": "string"
   },
   "url": {
    "description": "Full HireJack job detail URL \u2014 convenience alternative to domain + jobId.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "save_job",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "citation_url": {
    "description": "hirejack.com URL to cite when surfacing this result",
    "type": "string"
   },
   "data": {
    "additionalProperties": true,
    "description": "Result of the save/unsave action",
    "properties": {
     "changed": {
      "description": "False when the call was a no-op (already in the requested state; see meta.note)",
      "type": "boolean"
     },
     "company": {
      "type": "string"
     },
     "saved": {
      "description": "Whether the job is on the saved list after this call",
      "type": "boolean"
     },
     "title": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "meta": {
    "additionalProperties": true,
    "description": "Request/response metadata (pagination cursors, snapshot timestamps, applied filters)",
    "properties": {},
    "type": "object"
   }
  },
  "required": [
   "data"
  ],
  "type": "object"
 },
 "title": "Save Job"
}
