{
 "description": "Pull the most relevant remembered context for the current coding task \u2014 call this at the START of a session to reload what the agent knew.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max memories (default 8)",
    "type": "number"
   },
   "task": {
    "description": "The task or file you're working on",
    "type": "string"
   }
  },
  "required": [
   "task"
  ],
  "type": "object"
 },
 "name": "opencode_recall_context"
}
