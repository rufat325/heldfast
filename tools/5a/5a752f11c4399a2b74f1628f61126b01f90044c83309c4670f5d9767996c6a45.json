{
 "description": "Batch-check multiple URLs against the same policy. Returns per-URL decisions plus an allowed/denied summary. Useful for vetting a list of pending tool fetches.",
 "inputSchema": {
  "properties": {
   "method": {
    "description": "HTTP method applied to all URLs.",
    "type": "string"
   },
   "policy": {
    "description": "A simplified network policy. Use { allow: [\"api.openai.com\", \"*.example.com\"] }. Optional: deny: string[], methods: string[].",
    "properties": {
     "allow": {
      "description": "Host patterns to allow. Supports exact and *.suffix wildcards.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "deny": {
      "description": "Host patterns to deny. Wins over allow.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "methods": {
      "description": "HTTP methods to permit (e.g. [\"GET\", \"POST\"]). Default any.",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "type": "object"
   },
   "urls": {
    "description": "URLs to check.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "urls",
   "policy"
  ],
  "type": "object"
 },
 "name": "check_urls_batch"
}
