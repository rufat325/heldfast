{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run a SoQL query against a specific dataset's actual data rows on a Socrata portal \u2014 supports $select, $where, $order, $group, $limit, and $offset clauses (see socrata.dataset_metadata for column names). Returns the matching rows as JSON. Use socrata.dataset_search first to find a domain + dataset_id. Data: per-portal Socrata resource API (e.g. data.cityofnewyork.us), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataset_id": {
    "description": "Socrata dataset 4x4 identifier, e.g. \"erm2-nwe9\" (from socrata.dataset_search results)",
    "pattern": "^[a-z0-9]{4}-[a-z0-9]{4}$",
    "type": "string"
   },
   "domain": {
    "description": "Socrata portal hostname, e.g. \"data.cityofnewyork.us\" (from socrata.dataset_search results)",
    "maxLength": 253,
    "minLength": 3,
    "type": "string"
   },
   "group": {
    "description": "SoQL $group clause, e.g. \"complaint_type\"",
    "maxLength": 200,
    "minLength": 1,
    "type": "string"
   },
   "limit": {
    "description": "Max rows to return, 1-1000 (default 50)",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Pagination offset (default 0)",
    "minimum": 0,
    "type": "integer"
   },
   "order": {
    "description": "SoQL $order clause, e.g. \"created_date DESC\"",
    "maxLength": 200,
    "minLength": 1,
    "type": "string"
   },
   "select": {
    "description": "SoQL $select clause, e.g. \"complaint_type,count(*)\" (see socrata.dataset_metadata for column names)",
    "maxLength": 500,
    "minLength": 1,
    "type": "string"
   },
   "where": {
    "description": "SoQL $where clause, e.g. \"borough='BROOKLYN'\" or \"created_date>'2026-01-01'\"",
    "maxLength": 500,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "domain",
   "dataset_id"
  ],
  "type": "object"
 },
 "name": "socrata.datasets.query",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Socrata Dataset Query"
}
