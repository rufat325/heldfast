{
 "description": "Return the last N messages from a session for full context injection. Defaults to 10.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "last_n": {
    "description": "Number of most recent messages to return (default 10).",
    "type": "number"
   },
   "session_id": {
    "description": "Session id to read.",
    "type": "string"
   }
  },
  "required": [
   "session_id"
  ],
  "type": "object"
 },
 "name": "get_session_context"
}
