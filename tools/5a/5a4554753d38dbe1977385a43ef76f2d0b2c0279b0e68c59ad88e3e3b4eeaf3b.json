{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "List FTP accounts on a hosting service. Passwords are never returned \u2014 the API response schema has no password field.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "descending": {
    "description": "Reverse the sort order.",
    "type": "boolean"
   },
   "page": {
    "description": "1-based page number.",
    "maximum": 9007199254740991,
    "minimum": 1,
    "type": "integer"
   },
   "rowsPerPage": {
    "description": "Records per page.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "service": {
    "description": "Service identifier the zone belongs to. See ws_service_list for the account's services.",
    "minLength": 1,
    "type": "string"
   },
   "sortBy": {
    "description": "Field name to sort by.",
    "type": "string"
   }
  },
  "required": [
   "service"
  ],
  "type": "object"
 },
 "name": "ws_ftp_account_list",
 "title": "List FTP accounts"
}
