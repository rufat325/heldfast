{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the Library of Congress catalogue: books, photographs, maps, recordings, films, manuscripts, sheet music and newspaper titles. 'media_type' is required, because the Library keeps a separate catalogue for each kind of thing. This matches titles, creators and catalogue descriptions. It does not read the text inside a scan; use search_newspapers for a phrase printed on a newspaper page. Filters take the words the Library itself uses: 'subject' and 'location' as the rows here spell them, 'language' written in English, 'collection' as list_collections reports it under 'collection_filter'. A filter matching nothing is set aside and the search asked again without it; the answer names what was dropped, and the count it reports is then the unfiltered search's. By default only material with a digitised copy is returned; set 'online_only' to false to take in records the Library holds on a shelf alone. A row carries an 'identifier' get_item takes when it names a record. A row that is a collection the Library gathered names no record: 'is_collection' is true there, 'identifier' is null, and 'source_url' opens the collection.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "collection": {
    "description": "A collection, named exactly as list_collections reports it under 'collection_filter'.",
    "maxLength": 160,
    "type": "string"
   },
   "language": {
    "description": "A language, written in English: 'english'.",
    "maxLength": 120,
    "type": "string"
   },
   "limit": {
    "default": 10,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "location": {
    "description": "A place, such as 'oklahoma'.",
    "maxLength": 120,
    "type": "string"
   },
   "media_type": {
    "description": "Which catalogue to search: books, photos, maps, audio, film-and-videos, manuscripts, notated-music or newspapers. 'newspapers' holds the papers themselves; a phrase printed on a page belongs in search_newspapers.",
    "enum": [
     "books",
     "photos",
     "maps",
     "audio",
     "film-and-videos",
     "manuscripts",
     "notated-music",
     "newspapers"
    ],
    "type": "string"
   },
   "online_only": {
    "default": true,
    "description": "Keep to material with a digitised copy. Set false to include shelf-only records.",
    "type": "boolean"
   },
   "page": {
    "default": 1,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Words to look for in titles, creators and catalogue descriptions. The catalogue index holds no word of a single letter, so at least one word has to run to two letters or more, unless it is written in a script where one character is a word.",
    "maxLength": 300,
    "minLength": 1,
    "type": "string"
   },
   "sort": {
    "default": "relevance",
    "description": "Order of the results.",
    "enum": [
     "relevance",
     "newest",
     "oldest",
     "title"
    ],
    "type": "string"
   },
   "subject": {
    "description": "A subject heading, such as 'crime'.",
    "maxLength": 120,
    "type": "string"
   },
   "year_from": {
    "description": "Earliest year, inclusive.",
    "maximum": 9999,
    "minimum": 1000,
    "type": "integer"
   },
   "year_to": {
    "description": "Latest year, inclusive.",
    "maximum": 9999,
    "minimum": 1000,
    "type": "integer"
   }
  },
  "required": [
   "query",
   "media_type"
  ],
  "type": "object"
 },
 "name": "search_items",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "items": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "creator": {
       "description": "Whoever the Library credits for the work.",
       "type": [
        "string",
        "null"
       ]
      },
      "date": {
       "description": "The date as published, which is often a range.",
       "type": [
        "string",
        "null"
       ]
      },
      "date_code": {
       "description": "The cataloguing code the Library files the row under in place of a date, such as 'uuuu' or '18??'. It stands for digits the Library has not established, so 'date' and 'year' are null beside it. Null wherever the filed value is a date.",
       "type": [
        "string",
        "null"
       ]
      },
      "format": {
       "description": "What the thing is: book, photo, map, newspaper.",
       "type": [
        "string",
        "null"
       ]
      },
      "identifier": {
       "description": "Pass this to get_item to read the full record. Null when the row names no address a record can be read at.",
       "type": [
        "string",
        "null"
       ]
      },
      "is_collection": {
       "description": "True when the row is a collection a curator gathered and named rather than a record of the catalogue searched. The item route holds nothing at a collection's address, so 'identifier' is null and 'source_url' is where it opens.",
       "type": "boolean"
      },
      "location": {
       "description": "Places the record is catalogued under.",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "online": {
       "description": "Whether a digitised copy can be read online.",
       "type": "boolean"
      },
      "source_url": {
       "description": "Public page. Show this when citing the record.",
       "type": "string"
      },
      "subjects": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "title": {
       "type": [
        "string",
        "null"
       ]
      },
      "year": {
       "anyOf": [
        {
         "maximum": 9007199254740991,
         "minimum": -9007199254740991,
         "type": "integer"
        },
        {
         "type": "null"
        }
       ],
       "description": "Read off 'date' when that date names a year."
      }
     },
     "required": [
      "identifier",
      "is_collection",
      "title",
      "creator",
      "year",
      "date",
      "date_code",
      "format",
      "location",
      "subjects",
      "online",
      "source_url"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "notes": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "page": {
    "maximum": 9007199254740991,
    "minimum": -9007199254740991,
    "type": "integer"
   },
   "query": {
    "type": "string"
   },
   "total": {
    "description": "Records matching across this catalogue for the search the rows come from, not the number returned. A filter that matched nothing is set aside, and this then counts the search without it, which the notes name.",
    "maximum": 9007199254740991,
    "minimum": -9007199254740991,
    "type": "integer"
   }
  },
  "required": [
   "query",
   "total",
   "page",
   "items",
   "notes"
  ],
  "type": "object"
 },
 "title": "Search the catalogue"
}
