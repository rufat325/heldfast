{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "A one-time sign-in link to the Titan webmail / mail admin panel for a domain, so the customer can read mail or add mailboxes without hunting for a password. Treat the returned URL as a credential: hand it to the account owner, do not post it anywhere shared.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "domain": {
    "description": "The domain the mail service belongs to.",
    "type": "string"
   }
  },
  "required": [
   "domain"
  ],
  "type": "object"
 },
 "name": "impreza_titan_webmail"
}
