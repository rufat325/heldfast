{
 "description": "What a fund bought, sold, added to, and trimmed between two snapshots. Defaults to the latest snapshot versus the one before it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "from": {
    "description": "Baseline snapshot date (YYYY-MM-DD).",
    "type": "string"
   },
   "ticker": {
    "type": "string"
   },
   "to": {
    "description": "Comparison snapshot date (YYYY-MM-DD).",
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "diff_holdings"
}
