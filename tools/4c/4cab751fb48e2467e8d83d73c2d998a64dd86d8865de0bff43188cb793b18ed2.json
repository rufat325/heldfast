{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Change targeting rules; send only the keys to change (remote_mode any|remote|hybrid|onsite, geo_scope worldwide|region|country, geo_countries [ISO-2], locations, include_keywords, exclude_keywords, title_allow, title_block, seniority, salary_min, currency, languages, company_block, max_apps_per_day, max_apps_per_company, cover_letter_enabled, cv_tone, tailor_instructions, years_experience, salary_expectation, notice_period, \u2026). The feed re-matches in the background.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "patch": {
    "description": "Targeting fields to change, by name.",
    "type": "object"
   }
  },
  "required": [
   "patch"
  ],
  "type": "object"
 },
 "name": "update_targeting",
 "title": "Change my job targeting"
}
