{
 "description": "List public URBIS WMS layers for the vector or raster service.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 25,
    "description": "Maximum number of layers to return",
    "maximum": 100,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "description": "Optional text filter on layer name or title",
    "type": "string"
   },
   "service": {
    "description": "Which public URBIS WMS service to inspect",
    "enum": [
     "vector",
     "raster"
    ],
    "type": "string"
   }
  },
  "required": [
   "service"
  ],
  "type": "object"
 },
 "name": "urbis_list_layers"
}
