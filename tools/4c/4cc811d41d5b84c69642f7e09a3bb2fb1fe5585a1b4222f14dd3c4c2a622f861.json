{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the ICD-11 disease classification for diagnosis codes by clinical term. Returns matching ICD-11 codes with titles and code type (stem = primary diagnosis, extension = modifier/qualifier). Covers all 55,000+ ICD-11 codes including diseases, injuries, external causes, and functional assessments. Use for clinical coding, medical record classification, disease surveillance, and health informatics. No auth \u2014 US National Library of Medicine public domain, unlimited free.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "max_results": {
    "default": 10,
    "description": "Maximum number of results to return (1-25). Default: 10",
    "maximum": 25,
    "minimum": 1,
    "type": "integer"
   },
   "terms": {
    "description": "Clinical search term \u2014 disease name, symptom, or condition (e.g. \"diabetes mellitus\", \"pneumonia\", \"hypertension\", \"fracture\")",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "terms"
  ],
  "type": "object"
 },
 "name": "icd11.disease.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "NLM ICD-11 \u2014 Search Disease Codes"
}
