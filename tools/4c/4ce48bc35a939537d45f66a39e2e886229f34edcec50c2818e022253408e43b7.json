{
 "description": "Image converter. Convert images between JPG, PNG, WebP, AVIF and HEIC. Resize and strip EXIF along the way. Accepts image/*, .heic, .heif, .avif; up to 50 MB per file.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "format": {
    "description": "Target format",
    "enum": [
     "jpeg",
     "png",
     "webp",
     "avif",
     "tiff"
    ],
    "type": "string"
   },
   "outputDir": {
    "description": "Where to put the results. Defaults to the folder each source file came from. Existing files are never overwritten.",
    "type": "string"
   },
   "paths": {
    "description": "Paths to the files to process, absolute or relative to the working directory.",
    "items": {
     "type": "string"
    },
    "maxItems": 25,
    "minItems": 1,
    "type": "array"
   },
   "quality": {
    "default": 82,
    "description": "Quality, 1\u2013100. Ignored for PNG \u2014 that one is lossless",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "resize": {
    "additionalProperties": false,
    "description": "Resize",
    "properties": {
     "fit": {
      "default": "inside",
      "description": "How to fit: without enlarging, cropping, or with padding",
      "enum": [
       "inside",
       "cover",
       "contain"
      ],
      "type": "string"
     },
     "height": {
      "description": "Height in pixels",
      "maximum": 20000,
      "minimum": 1,
      "type": "integer"
     },
     "width": {
      "description": "Width in pixels",
      "maximum": 20000,
      "minimum": 1,
      "type": "integer"
     }
    },
    "required": [
     "fit"
    ],
    "type": "object"
   },
   "stripMetadata": {
    "default": true,
    "description": "Strip EXIF and location data",
    "type": "boolean"
   }
  },
  "required": [
   "paths",
   "format"
  ],
  "type": "object"
 },
 "name": "image_convert"
}
