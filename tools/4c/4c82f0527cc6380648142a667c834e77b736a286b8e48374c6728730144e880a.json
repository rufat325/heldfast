{
 "description": "Create an invoice \u2014 a directed bill sent to a specific named recipient. Unlike a charge, an invoice captures who it was issued to and has its own draft / viewed / paid lifecycle.",
 "inputSchema": {
  "properties": {
   "business_name": {
    "description": "Your business name shown on the invoice",
    "type": "string"
   },
   "customer_email": {
    "description": "Email of the invoice recipient",
    "type": "string"
   },
   "customer_name": {
    "description": "Display name of the invoice recipient",
    "type": "string"
   },
   "local_price": {
    "description": "Fiat-denominated amount the invoice is quoted in",
    "properties": {
     "amount": {
      "description": "Amount as decimal string",
      "type": "string"
     },
     "currency": {
      "description": "ISO-4217 fiat currency code",
      "type": "string"
     }
    },
    "required": [
     "amount",
     "currency"
    ],
    "type": "object"
   },
   "memo": {
    "description": "Free-form note to the recipient (appears on invoice)",
    "type": "string"
   }
  },
  "required": [
   "business_name",
   "customer_email",
   "customer_name",
   "local_price"
  ],
  "type": "object"
 },
 "name": "create_invoice"
}
