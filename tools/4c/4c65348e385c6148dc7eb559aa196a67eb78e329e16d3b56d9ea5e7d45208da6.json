{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get SSL/TLS certificate verification status for a zone.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "zone_id": {
    "description": "The Cloudflare zone ID (32-char hex). Get it from list_zones.",
    "type": "string"
   }
  },
  "required": [
   "zone_id"
  ],
  "type": "object"
 },
 "name": "get_ssl_verification"
}
