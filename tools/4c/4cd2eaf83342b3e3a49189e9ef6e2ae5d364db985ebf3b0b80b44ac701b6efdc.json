{
 "description": "List organization suppressions. Global, organization and SES tenant suppressions are all checked BEFORE quota and before any queue work.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "cursor": {
    "description": "Opaque pagination cursor",
    "type": "string"
   },
   "limit": {
    "default": 50,
    "description": "Page size, maximum 100",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_suppressions"
}
