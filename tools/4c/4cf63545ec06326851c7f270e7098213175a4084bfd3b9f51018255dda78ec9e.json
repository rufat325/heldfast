{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false
 },
 "description": "Emulate a device/viewport on the active page. Pass a `device` preset, or custom `width`+`height`. `reset: true` restores a desktop viewport. For network conditions use browser_throttle instead.",
 "inputSchema": {
  "properties": {
   "device": {
    "description": "Device preset to emulate. Use this OR width+height.",
    "enum": [
     "iphone",
     "ipad",
     "pixel"
    ],
    "type": "string"
   },
   "deviceScaleFactor": {
    "description": "Device pixel ratio (e.g. 2 for retina). Default 1.",
    "type": "number"
   },
   "height": {
    "description": "Custom viewport height in CSS pixels (with width).",
    "type": "number"
   },
   "mobile": {
    "description": "Emulate a mobile device (touch + mobile UA hints). Default false.",
    "type": "boolean"
   },
   "reset": {
    "description": "Restore the default desktop viewport and clear emulation.",
    "type": "boolean"
   },
   "userAgent": {
    "description": "Override the User-Agent string.",
    "type": "string"
   },
   "width": {
    "description": "Custom viewport width in CSS pixels (with height).",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "browser_emulate"
}
