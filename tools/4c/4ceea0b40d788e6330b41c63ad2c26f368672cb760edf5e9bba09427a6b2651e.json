{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Review CSS/markup against the Japanese UI standard (typography, colour, font performance) and return a pass/needs_work/fail verdict with violations, JLReq/JIS citations, and fixes. The front-end correctness enforcer \u2014 run on any UI (including Western-built) before shipping to Japan; call transform_for_japan to auto-apply form/copy fixes.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "context": {
    "description": "Design context for the recommended font stack.",
    "enum": [
     "corporate",
     "editorial",
     "casual",
     "luxury"
    ],
    "title": "Context",
    "type": "string"
   },
   "css": {
    "description": "CSS content to review against the Japanese UI standard.",
    "title": "CSS",
    "type": "string"
   },
   "markup": {
    "description": "Optional HTML/JSX markup for context (e.g. text-on-photo detection).",
    "title": "Markup",
    "type": "string"
   }
  },
  "required": [
   "css"
  ],
  "type": "object"
 },
 "name": "review_jp_ui",
 "title": "Review JP UI"
}
