{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Daily OHLC + volume + market cap for a coin. Defaults to the last 30 days when no date range is supplied. Free tier: up to 1 year back.",
 "inputSchema": {
  "examples": [
   {
    "coin_id": "btc-bitcoin"
   },
   {
    "coin_id": "eth-ethereum",
    "end": "2024-12-31",
    "limit": 365,
    "quote": "usd",
    "start": "2024-01-01"
   }
  ],
  "properties": {
   "coin_id": {
    "description": "coinpaprika coin ID (e.g. \"btc-bitcoin\", \"eth-ethereum\")",
    "type": "string"
   },
   "end": {
    "description": "YYYY-MM-DD or unix timestamp (optional, defaults to now).",
    "type": "string"
   },
   "limit": {
    "description": "1-365 daily candles (default 30).",
    "type": "number"
   },
   "quote": {
    "description": "usd | btc (default usd)",
    "type": "string"
   },
   "start": {
    "description": "YYYY-MM-DD or unix timestamp. Optional \u2014 defaults to 30 days ago.",
    "type": "string"
   }
  },
  "required": [
   "coin_id"
  ],
  "type": "object"
 },
 "name": "historical_ohlc",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of items returned.",
    "type": "integer"
   },
   "items": {
    "description": "Daily OHLC candles with volume and market cap",
    "items": {
     "properties": {
      "close": {
       "description": "Closing price",
       "type": "number"
      },
      "high": {
       "description": "Highest price in period",
       "type": "number"
      },
      "low": {
       "description": "Lowest price in period",
       "type": "number"
      },
      "market_cap": {
       "description": "Market cap at close",
       "type": "number"
      },
      "open": {
       "description": "Opening price",
       "type": "number"
      },
      "time_close": {
       "description": "Unix timestamp for period close",
       "type": "number"
      },
      "time_open": {
       "description": "Unix timestamp for period open",
       "type": "number"
      },
      "volume": {
       "description": "Trading volume",
       "type": "number"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "items",
   "count"
  ],
  "type": "object"
 },
 "title": "Historical Ohlc",
 "version": "v1.673483dd"
}
