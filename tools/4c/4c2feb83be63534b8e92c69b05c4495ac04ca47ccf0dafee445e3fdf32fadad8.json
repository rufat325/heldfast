{
 "description": "Find gaps in the wiki's coverage of a topic. Returns LLM-suggested missing pieces (e.g., \"you mention X but never document Y\").",
 "inputSchema": {
  "properties": {
   "topic": {
    "type": "string"
   }
  },
  "required": [
   "topic"
  ],
  "type": "object"
 },
 "name": "find_gaps"
}
