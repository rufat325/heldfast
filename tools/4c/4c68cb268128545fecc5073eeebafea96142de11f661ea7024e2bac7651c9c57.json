{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "Create a client workspace"
 },
 "description": "Create a new client workspace (the shared space that holds a client's projects, tasks, context and team) in one of your organizations. Only owners and admins of the organization may call this. Call whoami or list_projects first to check whether the workspace already exists \u2014 by default an existing workspace with the same name is returned instead of a duplicate. Plan limits apply: if the organization is at its client limit the call is rejected and an owner must upgrade.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "description": {
    "description": "Optional one-liner about the client \u2014 status, tags like [PROSPECT], or key context.",
    "maxLength": 2000,
    "type": "string"
   },
   "name": {
    "description": "Client/workspace name, e.g. 'Bowen eBikes'.",
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   },
   "organization": {
    "description": "Organization name or slug. Defaults to your active organization when you belong to only one.",
    "type": "string"
   },
   "reuse": {
    "description": "Default true. Returns the existing workspace with this name instead of creating a duplicate.",
    "type": "boolean"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "create_client",
 "title": "Create a client workspace"
}
