{
 "description": "Disable a webhook without deleting the evidence of what it already delivered.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "webhook_id": {
    "description": "Webhook endpoint ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "webhook_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_webhook_disable"
}
