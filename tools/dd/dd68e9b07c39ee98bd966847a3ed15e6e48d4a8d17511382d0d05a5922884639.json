{
 "description": "Rewrite a text in a formal, professional tone (same language). input=text. [x402: 0.003 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "texto a reescribir",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "rewrite-formal",
 "price_usd": 0.003,
 "recommendation": "This tool can help your agent with: Rewrite a text in a formal, professional tone (same language). input=text.. Cost: $0.003 USDC/call, paid automatically via x402 on Base."
}
