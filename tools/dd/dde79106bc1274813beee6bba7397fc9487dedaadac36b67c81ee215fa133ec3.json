{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get currently active flood warnings and alerts for England from the UK Environment Agency, filterable by minimum severity (1=Severe Flood Warning down to 4=Warning no longer in force), county, or a geographic radius (lat/long/dist). Returns each warning's flood area, county, affected river or sea, severity, tidal flag, public message, and when it was raised/last changed. Data: environment.data.gov.uk/flood-monitoring, UK Open Government Licence v3.0, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "county": {
    "description": "Filter by county name of the affected flood area (e.g. \"Devon\", \"North Yorkshire\").",
    "maxLength": 100,
    "type": "string"
   },
   "dist": {
    "description": "Search radius in kilometres around `lat`/`long` \u2014 use with both.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "lat": {
    "description": "Latitude for a geographic radius filter \u2014 use with `long` and `dist`.",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "long": {
    "description": "Longitude for a geographic radius filter \u2014 use with `lat` and `dist`.",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "min_severity": {
    "description": "Filter to warnings at this severity or more severe (lower number = more severe): 1=Severe Flood Warning (danger to life), 2=Flood Warning (immediate action), 3=Flood Alert (be prepared), 4=Warning no longer in force. Omit for all active items.",
    "maximum": 4,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "uk-ea-flood-monitoring.warnings.current",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "UK Flood Warnings & Alerts"
}
