{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Non-destructive lease (at-least-once): take the oldest envelope with a visibility timeout. Returns {item, leaseToken, deadline}. Ack with queue_ack, requeue with queue_nack.",
 "inputSchema": {
  "properties": {
   "queue": {
    "description": "Queue name",
    "type": "string"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   },
   "visibilitySeconds": {
    "description": "Visibility timeout before auto-requeue",
    "type": "number"
   },
   "wait": {
    "description": "Long-poll seconds, 0-25 (default 0)",
    "type": "number"
   }
  },
  "required": [
   "spaceId",
   "queue"
  ],
  "type": "object"
 },
 "name": "queue_lease",
 "outputSchema": {
  "properties": {},
  "type": "object"
 }
}
