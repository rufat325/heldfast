{
 "annotations": {
  "destructiveHint": true
 },
 "description": "Update an existing OurFamilyWizard calendar event. Fetches the event, applies the given changes, and writes the merged result back (OFW has no partial update). A change to an event the co-parent can see (shared before or after the change) is confirmed first; the confirmation is bound to the event exactly as read, so if it changes on OFW in between (say the co-parent edited it) the update is refused instead of overwriting their edit. Asks the user to confirm first: a confirmation prompt where the client supports one; otherwise the first call performs NO write and returns a preview of exactly what would happen plus a confirmToken, and only a repeat call with that token proceeds (see MCP_CONFIRM_MODE).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "allDay": {
    "type": "boolean"
   },
   "children": {
    "description": "Child userIds to tag; pass [] to remove all child tags (omit to keep current tags)",
    "items": {
     "type": "number"
    },
    "type": "array"
   },
   "confirmToken": {
    "description": "ONLY for the two-step confirmation fallback (a client without MCP elicitation). The confirmToken from this same tool's phase-1 \"confirmation-required\" response, passed back ONLY after the user has seen that preview and explicitly approved it in chat \u2014 never on the first call, never invented, never reused. Call again with the same arguments. Ignored when the client supports elicitation.",
    "type": "string"
   },
   "dropOffParentId": {
    "description": "userId of the drop-off parent",
    "type": "number"
   },
   "endDate": {
    "description": "End date YYYY-MM-DD (default: startDate)",
    "type": "string"
   },
   "endTime": {
    "description": "End time HH:mm, 24-hour (required unless allDay)",
    "type": "string"
   },
   "eventId": {
    "description": "Event id \u2014 the `id` from ofw_list_events / eventRecurrenceId from ofw_create_event",
    "type": "string"
   },
   "eventParentId": {
    "description": "userId of the parent the event is 'for'",
    "type": "number"
   },
   "location": {
    "type": "string"
   },
   "notes": {
    "type": "string"
   },
   "pickUpParentId": {
    "description": "userId of the pick-up parent",
    "type": "number"
   },
   "privateEvent": {
    "description": "true = visible only to you; default false = shared with co-parent",
    "type": "boolean"
   },
   "reminderMinutes": {
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "startDate": {
    "description": "Start date YYYY-MM-DD",
    "type": "string"
   },
   "startTime": {
    "description": "Start time HH:mm, 24-hour (required unless allDay)",
    "type": "string"
   },
   "title": {
    "type": "string"
   }
  },
  "required": [
   "eventId"
  ],
  "type": "object"
 },
 "name": "ofw_update_event"
}
