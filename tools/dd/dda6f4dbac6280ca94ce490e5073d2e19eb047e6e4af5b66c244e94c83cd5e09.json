{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Choose a MACH Base capability"
 },
 "description": "Free decision layer: map an agent goal to the narrowest paid MACH Base capability, exact price, expected result fields and exact next request. Returns no live chain data and has no side effects.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "goal": {
    "description": "The job or outcome the agent needs from Base mainnet.",
    "maxLength": 500,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "goal"
  ],
  "type": "object"
 },
 "name": "mach_choose"
}
