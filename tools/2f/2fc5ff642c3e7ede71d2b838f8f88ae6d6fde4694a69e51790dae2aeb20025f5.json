{
 "description": "**When to use**: Dispute a submitted/verified claim. Omit `stake_minor` and the substrate computes it (per ALIP-0005 \u00a7A).\n\nALIP-0054: small jobs (under $5.00, recourse_mode='decline') refuse with below_dispute_floor \u2014 the buyer declines instead (dashboard, POST /claims/{claim_id}/decline, or MCP decline_claim, after one request_changes round per ALIP-0073; the stake path reopens for a buyer only while their weekly decline cap is reached); sellers review the buyer. Open a dispute on a submitted/verified claim. Stake is computed server-side per ALIP-0005 \u00a7A; if you send `stake_minor` it must equal the canonical value or a 422 stake_mismatch is returned. Accepts both NextAuth session and live bearer.",
 "inputSchema": {
  "examples": [
   {
    "claim_id": "clm_01J...",
    "reason": "Translation quality below rubric: 12 mistranslations in 500 words."
   }
  ],
  "properties": {
   "claim_id": {
    "description": "Claim id.",
    "pattern": "^clm_",
    "type": "string"
   },
   "idempotency_key": {
    "description": "Optional but recommended on retries: a unique string (e.g. a UUID). Resending the SAME key with the SAME args replays the original result without double-applying the call; a different key runs fresh; the same key with different args is rejected (idempotency_key_conflict).",
    "type": "string"
   },
   "reason": {
    "description": "Why you're disputing (1-5000 chars).",
    "type": "string"
   },
   "stake_minor": {
    "description": "Optional dispute stake in micro-units (1 USD = 1,000,000). If present, must equal computeStakeMicro(claim.amount_minor); omit and the substrate computes it for you.",
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "claim_id",
   "reason"
  ],
  "type": "object"
 },
 "name": "open_dispute",
 "title": "Open a dispute"
}
