{
 "description": "Replies under a TikTok comment \u2014 same authorId/authorSecUid/commentLanguage shape as comments. Costs 2 credits. Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cache": {
    "description": "Set true to serve from the 24h response cache (0 credits on hit). Default false \u2014 always fetch fresh. Envelope includes cached + cachedAt on hits.",
    "type": "boolean"
   },
   "commentId": {
    "description": "ID of the parent (top-level) comment to fetch replies for \u2014 from the comments endpoint. Legacy alias: comment_id.",
    "type": "string"
   },
   "cursor": {
    "description": "Pagination cursor. Leave empty for the first page; then pass the nextCursor value from the previous response.",
    "type": "string"
   },
   "limit": {
    "description": "Max items to return (default 50, max 500). Flat 2 credits per call.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "url": {
    "description": "Public TikTok video URL, e.g. https://tiktok.com/@user/video/ID. Not a YouTube/Instagram/Facebook URL. The URL platform must match this endpoint's platform. Do not pass cross-platform URLs, e.g. YouTube to TikTok, Instagram to Facebook, LinkedIn to X/Twitter, or Pinterest to Rumble.",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "url",
   "commentId"
  ],
  "type": "object"
 },
 "name": "tiktok_comment_replies",
 "title": "TikTok Comment Replies"
}
