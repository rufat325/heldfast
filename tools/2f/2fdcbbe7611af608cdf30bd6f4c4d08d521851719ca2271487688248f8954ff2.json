{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Read the comment thread on one article, oldest first, paginated.\n\nUse it to review reader feedback or summarise a discussion. It reads comments only; there is no tool here for posting or moderating a reply.\n\nReads only. Requires an API key and counts against the plan's request quota. Page through with limit and offset \u2014 the default returns the first 20. Returns the comments with their authors and timestamps; an empty list simply means no comments yet, which is not an error.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "article_id": {
    "description": "UUID of the article, as returned in the `id` field by article tools. Not the slug.",
    "format": "uuid",
    "type": "string"
   },
   "limit": {
    "default": 20,
    "description": "Comments per page, 1-100. Defaults to 20.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "default": 0,
    "description": "Comments to skip before this page. Defaults to 0.",
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "article_id"
  ],
  "type": "object"
 },
 "name": "list_comments",
 "title": "List article comments"
}
