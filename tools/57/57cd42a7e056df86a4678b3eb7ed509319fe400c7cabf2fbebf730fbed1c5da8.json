{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Tap element #N from the most recent screenshot { annotate: true } capture \u2014 no coordinate math needed. The mapping is exact even if your client displays the image scaled. Re-capture after the UI changes: element numbers go stale.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "element": {
    "description": "Element number from the annotated screenshot",
    "minimum": 0,
    "type": "integer"
   },
   "serial": {
    "description": "Device serial (defaults to session or the only device)",
    "type": "string"
   }
  },
  "required": [
   "element"
  ],
  "type": "object"
 },
 "name": "tap_element",
 "title": "Tap annotated element"
}
