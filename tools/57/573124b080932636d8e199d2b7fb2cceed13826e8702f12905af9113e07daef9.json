{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "tier": "free",
  "title": "Cite Image"
 },
 "description": "Generate canonical attribution blocks for one or more MemeStack images. Returns markdown, HTML, and plain-text citation strings ready to paste into your output, plus a combined block for citing multiple images at once.",
 "inputSchema": {
  "properties": {
   "image_ids": {
    "description": "Up to 50 image UUIDs",
    "items": {
     "type": "string"
    },
    "maxItems": 50,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "image_ids"
  ],
  "type": "object"
 },
 "name": "cite_image",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Per-image and combined citations for the requested image IDs.",
  "properties": {
   "citations": {
    "description": "One citation per resolved image ID.",
    "items": {
     "additionalProperties": true,
     "properties": {
      "controversial": {
       "description": "True when MemeStack classifies the image as controversial (likely to offend or divide a general audience). The list tools exclude such images unless include_controversial is true; by-id tools always answer and carry this flag. Absent when the API answer predates the flag.",
       "type": "boolean"
      },
      "html": {
       "description": "Attribution as HTML.",
       "type": "string"
      },
      "id": {
       "description": "Image UUID.",
       "type": "string"
      },
      "markdown": {
       "description": "Attribution as Markdown.",
       "type": "string"
      },
      "plain": {
       "description": "Attribution as plain text.",
       "type": "string"
      }
     },
     "required": [
      "id",
      "markdown",
      "html",
      "plain"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "combined": {
    "additionalProperties": false,
    "description": "Combined attribution for all images in this response, in three formats.",
    "properties": {
     "html": {
      "description": "Numbered attribution list as HTML.",
      "type": "string"
     },
     "markdown": {
      "description": "Numbered attribution list as Markdown.",
      "type": "string"
     },
     "plain": {
      "description": "Numbered attribution list as plain text.",
      "type": "string"
     }
    },
    "required": [
     "markdown",
     "html",
     "plain"
    ],
    "type": "object"
   },
   "missing_ids": {
    "description": "Requested IDs that could not be resolved.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "citations",
   "combined",
   "missing_ids"
  ],
  "type": "object"
 }
}
