{
 "description": "Show one FramingUI screen recipe as JSON (anatomy, hierarchy, capabilities, states, responsive rules, accessibility requirements, acceptance criteria). Runs the skill's recipe script: `recipe show <id> --json`. Use framingui_catalog with kind 'recipes' to list available recipe ids. Recipes ship with the free scope of FramingUI.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Recipe id, e.g. 'dashboard-overview' or 'conversation-thread'.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "framingui_recipe_show",
 "title": "Show FramingUI recipe"
}
