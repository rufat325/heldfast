{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Check upcoming and recent economic data releases. Returns release dates, names, and which series they update.",
 "inputSchema": {
  "examples": [
   {
    "limit": 20
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "FRED API key",
    "type": "string"
   },
   "limit": {
    "description": "Max results (1-1000, default 20)",
    "type": "number"
   },
   "offset": {
    "description": "Result offset for pagination (default 0)",
    "type": "number"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "fred_releases",
 "outputSchema": {
  "properties": {
   "note": {
    "description": "Present when the release list was truncated.",
    "type": "string"
   },
   "releases": {
    "description": "List of economic data releases",
    "items": {
     "properties": {
      "id": {
       "description": "Release ID",
       "type": "number"
      },
      "link": {
       "description": "URL link to release details",
       "type": [
        "string",
        "null"
       ]
      },
      "name": {
       "description": "Name of the release",
       "type": "string"
      },
      "notes": {
       "description": "Additional notes about the release",
       "type": [
        "string",
        "null"
       ]
      },
      "press_release": {
       "description": "Whether this is a press release",
       "type": "boolean"
      }
     },
     "required": [
      "id",
      "name",
      "press_release",
      "link",
      "notes"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "returned": {
    "description": "How many releases are in `releases` (capped by `limit`, default 20).",
    "type": "number"
   },
   "total_releases": {
    "description": "Total number of releases",
    "type": "number"
   },
   "truncated": {
    "description": "True when `returned` is less than `total_releases`.",
    "type": "boolean"
   }
  },
  "required": [
   "total_releases",
   "releases"
  ],
  "type": "object"
 },
 "title": "Fred Releases",
 "version": "v1.8ab7ad5e"
}
