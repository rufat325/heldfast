{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Security advisories, license and dependency metadata for ONE specific published release of a package. Requires an exact version that this package actually shipped \u2014 if you do not already have one, call `package` first to list them; a version guessed or carried over from a different package will not resolve.",
 "inputSchema": {
  "examples": [
   {
    "name": "express",
    "system_": "NPM",
    "version": "4.18.2"
   },
   {
    "name": "serde",
    "system_": "CARGO",
    "version": "1.0.163"
   }
  ],
  "properties": {
   "name": {
    "description": "Package name as the registry spells it \u2014 scoped npm names keep the \"@scope/\" prefix, Go modules are the full import path, Maven is \"groupId:artifactId\".",
    "type": "string"
   },
   "system_": {
    "description": "NPM | PYPI | GO | MAVEN | NUGET | CARGO | RUBYGEMS",
    "type": "string"
   },
   "version": {
    "description": "An exact version string THIS package published. Get it from the `package` tool rather than assuming; version numbers are not transferable between packages.",
    "type": "string"
   }
  },
  "required": [
   "system_",
   "name",
   "version"
  ],
  "type": "object"
 },
 "name": "version",
 "outputSchema": {
  "description": "Package version metadata including dependencies, advisories, and license",
  "properties": {},
  "type": "object"
 },
 "title": "Version",
 "version": "v1.28b073fa"
}
