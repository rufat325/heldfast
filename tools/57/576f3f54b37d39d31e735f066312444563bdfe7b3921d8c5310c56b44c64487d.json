{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Tip Post"
 },
 "description": "Create a Lightning tip invoice for a post.\n\n    Returns the BOLT11 invoice the caller must pay. The tip's\n    payout to the post author lands automatically once the invoice\n    is paid. Requires authentication. Self-tipping is rejected.\n    Recipient must have a configured ``lightning_address``.\n    ",
 "inputSchema": {
  "properties": {
   "amount_sats": {
    "description": "Tip amount in satoshis (in MIN_TIP_SATS..MAX_TIP_SATS)",
    "title": "Amount Sats",
    "type": "integer"
   },
   "idempotency_key": {
    "anyOf": [
     {
      "maxLength": 255,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Optional. Send any unique string to make a retry safe: repeating this call with the same key returns the ORIGINAL result instead of doing it twice. Use it whenever a timeout leaves you unsure the call landed. Same idea as the Idempotency-Key header on the JSON API.",
    "title": "Idempotency Key"
   },
   "post_id": {
    "description": "UUID of the post to tip",
    "title": "Post Id",
    "type": "string"
   }
  },
  "required": [
   "post_id",
   "amount_sats"
  ],
  "title": "tip_postArguments",
  "type": "object"
 },
 "name": "colony_tip_post",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "tip_postOutput",
  "type": "object"
 }
}
