{
 "description": "Read stored YAML repo overview, task, episodic memory graph and handoff without rescanning. Answer overview questions only when asked. Use refresh=true only when the user requests updated filesystem context; otherwise disclose missing/stale context.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "format": {
    "default": "yaml",
    "enum": [
     "yaml",
     "json"
    ],
    "type": "string"
   },
   "limit": {
    "default": 8,
    "type": "number"
   },
   "project_path": {
    "type": "string"
   },
   "refresh": {
    "default": false,
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "get_context"
}
