{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "NFL player-prop edges \u2014 the PredictionMarketsPicks projection vs the Kalshi prop line for passing yards, rushing yards, receiving yards, receptions, and anytime touchdown. Every Kalshi prop is an \"X or more\" contract; each row returns the contract (e.g. \"3+ receptions\"), the call (YES / NO), our odds and the Kalshi price for the called side, the model projection, the edge, and the raw over/under inputs. Live in-season (opens NFL Week 1). Pro key required. Use for \"NFL player prop edges\", \"best NFL props today\", \"passing yards over under\", \"receiving yards prop value\".",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "limit": {
    "default": 10,
    "description": "Max rows (default 10).",
    "maximum": 25,
    "minimum": 1,
    "type": "integer"
   },
   "minEdge": {
    "anyOf": [
     {
      "type": "number"
     },
     {
      "type": "string"
     }
    ],
    "description": "Min absolute edge in pp (default 5). Accepts a number or a numeric string (\"3\", \"3pp\", \"3%\")."
   },
   "propType": {
    "description": "Optional filter by prop type: pass_yds, pass_tds, rush_yds, rec_yds, receptions, anytime_td.",
    "enum": [
     "pass_yds",
     "pass_tds",
     "rush_yds",
     "rec_yds",
     "receptions",
     "anytime_td"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "nfl_prop_edge",
 "title": "NFL Player Prop Edge (model vs Kalshi)"
}
