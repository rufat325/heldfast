{
 "description": "Minify an SVG without changing what it draws: round coordinates, drop default attributes, strip the prolog. Render-preserving by construction, so it is safe on any SVG, not only ours.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "Path to the SVG",
    "type": "string"
   },
   "output": {
    "description": "Destination path; overwrites the input when omitted",
    "type": "string"
   },
   "precision": {
    "description": "Decimal places kept in path coordinates (0\u20138)",
    "type": "number"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "optimize"
}
