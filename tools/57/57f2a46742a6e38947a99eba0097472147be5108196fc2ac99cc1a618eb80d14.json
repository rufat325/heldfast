{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "LLM-quality summary of long text as a paragraph, bullets or a one-liner. Reads for meaning (nuance, tone) \u2014 unlike the free extractive summarizer. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "max_words": {
    "description": "Optional soft word limit (max 400).",
    "type": "integer"
   },
   "style": {
    "description": "Output shape (default paragraph).",
    "enum": [
     "paragraph",
     "bullets",
     "tweet"
    ],
    "type": "string"
   },
   "text": {
    "description": "Text to summarize (required, up to ~40 KB).",
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "agentbit.ai_summarize",
 "title": "AI Summarize (LLM)"
}
