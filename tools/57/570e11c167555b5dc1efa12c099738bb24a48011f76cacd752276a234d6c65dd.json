{
 "description": "Validate Terraform configuration files for syntax and internal consistency. Does not access remote state or APIs.",
 "inputSchema": {
  "properties": {
   "workdir": {
    "description": "Absolute or relative path to the directory containing .tf files",
    "type": "string"
   }
  },
  "required": [
   "workdir"
  ],
  "type": "object"
 },
 "name": "tf_validate"
}
