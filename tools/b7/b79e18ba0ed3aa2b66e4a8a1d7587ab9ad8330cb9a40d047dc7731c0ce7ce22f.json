{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Backlinks pointing at `<domain>` \u2014 the actual list of linking pages with source domain, anchor text, dofollow flag, and first/last seen dates, via DataForSEO. \"Who links to this site\". Example: seo_backlinks_list({ target: \"nike.com\", limit: 50, _apiKey: \"your-base64-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-seo-backlinks-api-key",
    "limit": 50,
    "target": "nike.com"
   },
   {
    "_apiKey": "your-seo-backlinks-api-key",
    "backlinks_status_type": "live",
    "limit": 100,
    "mode": "one_per_domain",
    "target": "example.com"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "DataForSEO API key = base64(\"login:password\")",
    "type": "string"
   },
   "backlinks_status_type": {
    "description": "\"live\" (default), \"all\", or \"lost\"",
    "type": "string"
   },
   "limit": {
    "description": "Max backlinks to return (default 50, max 200)",
    "type": "integer"
   },
   "mode": {
    "description": "\"as_is\" (default), \"one_per_domain\", or \"one_per_anchor\"",
    "type": "string"
   },
   "target": {
    "description": "Domain, subdomain, or URL, e.g. \"nike.com\"",
    "type": "string"
   }
  },
  "required": [
   "target",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "seo_backlinks_list",
 "title": "Seo Backlinks List",
 "version": "v1.9ce74cda"
}
