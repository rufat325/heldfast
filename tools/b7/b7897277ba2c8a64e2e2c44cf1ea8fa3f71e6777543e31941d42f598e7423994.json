{
 "description": "X/Twitter trend digest \u2014 live tweet search PLUS an AI-written summary: sentiment, key themes, driving accounts, notable posts. Same query syntax as x_search. Costs $0.05 per call, paid from clink's shop credits (get a key with buy_credits or at /buy/credits). A failed or empty call is free.",
 "inputSchema": {
  "properties": {
   "query": {
    "description": "Advanced X search query, e.g. '#bitcoin min_faves:50'",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "x_digest"
}
