{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Return events, visitors, and conversion rate grouped by configured Plausible goal. Answers which signup, activation, purchase, or other goals converted during a period.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-plausible-stats-api-key",
    "date_range": "month",
    "limit": 20,
    "site_id": "example.com"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Plausible Stats API key",
    "type": "string"
   },
   "date_range": {
    "anyOf": [
     {
      "enum": [
       "day",
       "24h",
       "7d",
       "28d",
       "30d",
       "91d",
       "month",
       "6mo",
       "12mo",
       "year",
       "all"
      ],
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "maxItems": 2,
      "minItems": 2,
      "type": "array"
     }
    ],
    "description": "Plausible date range preset or a two-item ISO date array"
   },
   "limit": {
    "description": "Maximum goals, default 20 and maximum 100",
    "maximum": 100,
    "minimum": 1,
    "type": "number"
   },
   "site_id": {
    "description": "Plausible site ID, usually the configured domain such as example.com",
    "type": "string"
   }
  },
  "required": [
   "_apiKey",
   "site_id",
   "date_range"
  ],
  "type": "object"
 },
 "name": "plausible_goal_performance",
 "title": "Plausible Goal Performance",
 "version": "v1.51a0989e"
}
