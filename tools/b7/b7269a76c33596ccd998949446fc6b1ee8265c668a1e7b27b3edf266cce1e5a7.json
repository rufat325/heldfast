{
 "description": "Capture a snapshot of network activity: active TCP connections, DNS resolution, host interfaces, and any active network conditioning.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bundleId": {
    "description": "App bundle ID to filter network activity for (optional \u2014 shows all if omitted)",
    "type": "string"
   },
   "deviceId": {
    "description": "Device (default: booted)",
    "type": "string"
   },
   "duration": {
    "description": "Capture duration in seconds (default: 3, max: 10)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "simulator_network_capture"
}
