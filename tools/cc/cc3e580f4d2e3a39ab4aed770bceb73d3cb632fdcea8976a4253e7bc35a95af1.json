{
 "description": "Mark an email as unread (removes \\Seen IMAP flag). Get UIDs from email_list.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "folder": {
    "description": "Folder (default: INBOX)",
    "type": "string"
   },
   "uid": {
    "description": "Email UID to mark as unread",
    "type": "number"
   }
  },
  "required": [
   "uid"
  ],
  "type": "object"
 },
 "name": "email_mark_unread"
}
