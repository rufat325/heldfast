{
 "description": "Apply approved control evidence. Verified status remains App-owned and requires readback evidence.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "idempotencyKey": {
    "minLength": 1,
    "type": "string"
   },
   "operationId": {
    "minLength": 1,
    "type": "string"
   },
   "previewHash": {
    "minLength": 1,
    "type": "string"
   },
   "previewToken": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "previewToken",
   "previewHash",
   "operationId",
   "idempotencyKey"
  ],
  "type": "object"
 },
 "name": "granoflow_knowledge_control_apply"
}
