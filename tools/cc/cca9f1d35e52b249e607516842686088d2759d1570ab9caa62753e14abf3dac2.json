{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "High-level stats about the local memory store. Safe to call first on every session to gauge whether the store is empty, small, or large.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "privacy_mode": {
    "default": "structured",
    "description": "summary = keys/meta without full values; structured/raw = full entries (local store parity).",
    "enum": [
     "summary",
     "structured",
     "raw"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "memory_stats"
}
