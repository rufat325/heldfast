{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "readOnlyHint": false
 },
 "description": "Add a tag to an existing Bear note. The tag is inserted into the note's markdown. Hierarchical tags like 'parent/child' also index every ancestor \u2014 so the note becomes discoverable under both #parent and #parent/child in Bear's sidebar.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Note ID (uniqueIdentifier)",
    "type": "string"
   },
   "tag": {
    "description": "Tag to add (without #)",
    "type": "string"
   }
  },
  "required": [
   "id",
   "tag"
  ],
  "type": "object"
 },
 "name": "bear_add_tag"
}
