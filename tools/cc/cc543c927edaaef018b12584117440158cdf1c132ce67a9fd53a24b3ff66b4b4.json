{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-attack-simulation"
 },
 "description": "Get an attack simulation campaign for a tenant.\n\nTIP: Gets a specific attack simulation by ID. Returns full details including report with simulationEventsContent (users who clicked, reported, etc.) and trainingsContent.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "simulationId": {
    "description": "Path parameter: simulationId",
    "type": "string"
   }
  },
  "required": [
   "simulationId"
  ],
  "type": "object"
 },
 "name": "get-attack-simulation"
}
