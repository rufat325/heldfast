{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get Test Plan"
 },
 "description": "Fetch a test plan by identifier (TP-*) from BrowserStack Test Management. Returns plan metadata, the full list of linked test runs, total test-case count across runs, and a status summary \u2014 suitable for generating test documentation or QA status reports. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns. Requires a test plan identifier (TP-*). Call listTestPlans first if you do not have it. Creating a test plan or sub-plan, and linking or unlinking test runs on one, are not available as tools here: call askBrowserStackAI with product \"tm\" and describe what you want. It asks you to confirm before changing anything.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "project_identifier": {
    "description": "Identifier of the project (starts with PR- followed by a number).",
    "type": "string"
   },
   "test_plan_identifier": {
    "description": "Identifier of the test plan (starts with TP- followed by a number).",
    "type": "string"
   }
  },
  "required": [
   "project_identifier",
   "test_plan_identifier"
  ],
  "type": "object"
 },
 "name": "getTestPlan"
}
