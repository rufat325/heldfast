{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Delete a VPS backup to free storage. Irreversible \u2014 if it is the only copy of a given date, that restore point is gone. Confirm with the customer first.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "backup_id": {
    "description": "Backup id from impreza_vps_list_backups.",
    "type": "number"
   },
   "service_id": {
    "description": "VPS service id.",
    "type": "number"
   }
  },
  "required": [
   "service_id",
   "backup_id"
  ],
  "type": "object"
 },
 "name": "impreza_vps_delete_backup"
}
