{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the historical time series for a single standardized XBRL financial concept (e.g. Assets, Liabilities, Revenues, NetIncomeLoss, StockholdersEquity) as reported by a company across its SEC filings \u2014 one value per fiscal period with the filing it came from. Sourced directly from structured XBRL data extracted from 10-K/10-Q filings, not free text. Requires a CIK from sec-edgar.company_lookup. Data: data.sec.gov (US Securities and Exchange Commission EDGAR system), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cik": {
    "description": "SEC Central Index Key for the company, e.g. \"320193\" or \"0000320193\". Obtained from sec-edgar.company_lookup.",
    "minLength": 1,
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of reported values to return, 1-100 (default 20, most recent first).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "tag": {
    "description": "XBRL concept tag name, e.g. \"Assets\", \"Liabilities\", \"Revenues\", \"NetIncomeLoss\", \"StockholdersEquity\", \"CashAndCashEquivalentsAtCarryingValue\", \"EarningsPerShareBasic\".",
    "minLength": 1,
    "type": "string"
   },
   "taxonomy": {
    "description": "XBRL taxonomy the tag belongs to \u2014 \"us-gaap\" for financial statement concepts (default), \"dei\" for document/entity metadata like EntityCommonStockSharesOutstanding.",
    "enum": [
     "us-gaap",
     "dei"
    ],
    "type": "string"
   },
   "unit": {
    "description": "Unit of measure to filter by, e.g. \"USD\" or \"shares\" (default: USD if available).",
    "type": "string"
   }
  },
  "required": [
   "cik",
   "tag"
  ],
  "type": "object"
 },
 "name": "sec-edgar.financials.concept",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Get SEC XBRL Financial Concept"
}
