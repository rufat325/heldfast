{
 "description": "Deletes the identity provider for the given Id.",
 "inputSchema": {
  "properties": {
   "identityProviderId": {
    "description": "The Id of the identity provider to delete.",
    "type": "string"
   }
  },
  "required": [
   "identityProviderId"
  ],
  "type": "object"
 },
 "name": "deleteIdentityProviderWithId"
}
