{
 "_meta": {
  "com.signaledi/conditionalScopes": [],
  "com.signaledi/productionScopes": [],
  "com.signaledi/requiredScopes": []
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Generate Test Document"
 },
 "description": "Render a synthetic X12 sample for 850, 810, 856, or 837 Professional (005010X222A1). controlNumber and the transaction's primary date apply to every fixture; poNumber applies only to 850 and 810. Local only; works in the docs profile. Local read-only operation.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "overrides": {
    "additionalProperties": false,
    "properties": {
     "controlNumber": {
      "maxLength": 9,
      "minLength": 9,
      "pattern": "^\\d{9}$",
      "type": "string"
     },
     "date": {
      "description": "YYYYMMDD primary transaction date: PO date (850), invoice date (810), shipment date (856), or claim creation/service date (837 Professional).",
      "pattern": "^\\d{8}$",
      "type": "string"
     },
     "poNumber": {
      "description": "Purchase-order reference for 850 and 810 only.",
      "maxLength": 40,
      "minLength": 1,
      "type": "string"
     }
    },
    "type": "object"
   },
   "type": {
    "description": "Transaction set to generate. 837 means Professional 005010X222A1 only.",
    "enum": [
     "850",
     "810",
     "856",
     "837"
    ],
    "type": "string"
   }
  },
  "required": [
   "type"
  ],
  "type": "object"
 },
 "name": "generate_test_document",
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "content": {
    "type": "string"
   },
   "type": {
    "enum": [
     "850",
     "810",
     "856",
     "837"
    ],
    "type": "string"
   }
  },
  "required": [
   "type",
   "content"
  ],
  "type": "object"
 },
 "title": "Generate Test Document"
}
