{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Load a knowledge pack \u2014 a markdown document with optional YAML frontmatter \u2014 into the persistent memory store. The pack is chunked by heading, each chunk embedded, and made available for semantic retrieval during subsequent optimize_prompt calls. Packs can come from a local file path, an HTTPS URL, or be passed inline as raw markdown. Bundled packs + authoring guide: https://github.com/LumabyteCo/clarifyprompt-mcp/tree/main/packs",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "name": {
    "description": "Override the pack name (else pulled from frontmatter).",
    "type": "string"
   },
   "scope": {
    "description": "Scope to load under (e.g. 'user', 'project:myapp'). Defaults to pack frontmatter or 'user'.",
    "type": "string"
   },
   "source": {
    "description": "Local file path, HTTPS URL, or inline markdown body (auto-detected).",
    "type": "string"
   },
   "source_type": {
    "default": "auto",
    "description": "Override source-type detection. `registry` marks a pack as community-sourced.",
    "enum": [
     "auto",
     "local",
     "url",
     "inline",
     "registry"
    ],
    "type": "string"
   },
   "version": {
    "description": "Override the pack version (else pulled from frontmatter or '0.0.0').",
    "type": "string"
   }
  },
  "required": [
   "source"
  ],
  "type": "object"
 },
 "name": "load_knowledge_pack",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true,
  "properties": {
   "chunks": {
    "type": "number"
   },
   "embedded": {
    "type": "number"
   },
   "message": {
    "type": "string"
   },
   "pack": {
    "additionalProperties": true,
    "properties": {},
    "type": "object"
   },
   "skipped": {
    "type": "number"
   },
   "success": {
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "title": "Load a knowledge pack"
}
