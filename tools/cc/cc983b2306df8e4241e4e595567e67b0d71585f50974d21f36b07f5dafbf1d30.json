{
 "description": "Discover ranked Smart Money X/Twitter accounts (macro, trading, crypto, geopolitics) that posted extractable forward-looking calls recently. Pass exclude to skip handles you already track. Prefer this over googling long analyst prompts.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "days": {
    "description": "Only accounts with a call in the last N days (default 14)",
    "maximum": 90,
    "minimum": 1,
    "type": "integer"
   },
   "exclude": {
    "description": "Comma-separated @handles to exclude (already tracked)",
    "type": "string"
   },
   "limit": {
    "description": "Max accounts to return (default 4)",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "niche": {
    "description": "Optional niche filter",
    "enum": [
     "MACRO",
     "TRADING",
     "CRYPTO",
     "GEOPOLITICS",
     "macro",
     "trading",
     "crypto",
     "geopolitics"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "xflux_smart_money_list"
}
