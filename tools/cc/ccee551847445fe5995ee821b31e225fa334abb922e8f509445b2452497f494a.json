{
 "description": "Delete a content rule permanently. Use get_rule_set to find rule IDs.",
 "inputSchema": {
  "properties": {
   "id": {
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "delete_rule"
}
