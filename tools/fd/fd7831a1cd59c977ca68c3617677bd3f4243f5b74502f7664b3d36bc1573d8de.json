{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Browse digitized items from US archives by subject or topic keyword using DPLA subject vocabulary. Subjects include broad categories (Women, Architecture, Photography) and historical topics (Civil War, Native Americans, Abolitionists). Optionally filter by media type. Returns paginated list of matching items with title, creator, date, type, thumbnail, and direct URL.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "page": {
    "description": "Page number for pagination, starting at 1 (default: 1)",
    "minimum": 1,
    "type": "integer"
   },
   "page_size": {
    "description": "Number of items per page, 1\u201350 (default: 10)",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "subject": {
    "description": "Subject or topic to browse (e.g. \"Photography\", \"Women\", \"Architecture\", \"Native Americans\"). Case-insensitive exact match against DPLA subject vocabulary.",
    "type": "string"
   },
   "type": {
    "description": "Optionally narrow results to a specific media type: image, text, sound, moving image, or physical object",
    "enum": [
     "image",
     "text",
     "sound",
     "moving image",
     "physical object"
    ],
    "type": "string"
   }
  },
  "required": [
   "subject"
  ],
  "type": "object"
 },
 "name": "dpla.archives.by_subject",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "DPLA \u2014 Browse Items by Subject"
}
