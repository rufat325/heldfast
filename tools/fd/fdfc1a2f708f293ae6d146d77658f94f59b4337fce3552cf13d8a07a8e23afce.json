{
 "description": "Resume a caption-video blueprint run that paused in status awaiting_review for transcript review. Pass the approved (optionally edited) SRT text; the run continues to render the captioned video. Poll with get_blueprint_run afterwards.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Blueprint run ID (must be in status awaiting_review)",
    "type": "string"
   },
   "srt_text": {
    "description": "The reviewed/edited SRT transcript to burn into the video",
    "type": "string"
   }
  },
  "required": [
   "id",
   "srt_text"
  ],
  "type": "object"
 },
 "name": "continue_blueprint_run",
 "title": "Continue Blueprint Run"
}
