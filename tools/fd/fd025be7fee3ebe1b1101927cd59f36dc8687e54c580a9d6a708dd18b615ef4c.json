{
 "description": "Move an email to a different mailbox. Use list_folders to see available mailbox names.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "dest_account": {
    "description": "Destination account name, e.g. \"iCloud\".",
    "type": "string"
   },
   "dest_mailbox": {
    "description": "Destination mailbox name, e.g. \"Finance & Accounts\". Must match exactly (case-sensitive).",
    "type": "string"
   },
   "message_ref": {
    "description": "Composite message reference from list_emails or search_emails.",
    "type": "string"
   }
  },
  "required": [
   "message_ref",
   "dest_account",
   "dest_mailbox"
  ],
  "type": "object"
 },
 "name": "move_email"
}
