{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetches a random cat image and returns it base64-encoded with the MIME type. Useful for testing image handling.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Parameters for fetching a random image.",
  "properties": {
   "trigger": {
    "default": true,
    "description": "A trigger to invoke the tool and fetch a new cat image.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "template_image_test",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Image tool response payload.",
  "properties": {
   "data": {
    "description": "Base64 encoded image data.",
    "type": "string"
   },
   "mimeType": {
    "description": "The MIME type of the image (e.g., 'image/jpeg').",
    "type": "string"
   }
  },
  "required": [
   "data",
   "mimeType"
  ],
  "type": "object"
 },
 "title": "Template Image Test"
}
