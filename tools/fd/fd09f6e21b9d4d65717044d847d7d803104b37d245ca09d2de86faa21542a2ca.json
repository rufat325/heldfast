{
 "description": "Start recording all actions for an agent session. Returns a session_id to use with log_action.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "agent_id": {
    "description": "Unique identifier for the agent being recorded",
    "type": "string"
   },
   "metadata": {
    "additionalProperties": {},
    "default": {},
    "description": "Optional metadata (task, model, environment)",
    "type": "object"
   }
  },
  "required": [
   "agent_id"
  ],
  "type": "object"
 },
 "name": "record_session"
}
