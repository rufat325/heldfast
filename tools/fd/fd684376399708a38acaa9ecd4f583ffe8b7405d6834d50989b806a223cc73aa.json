{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Model Lab: run one prompt across many AI models at once and compare their outputs, cost, and latency side by side. Create and start an experiment: run each prompt against each model (\u00d7 reps), collecting output, tokens, latency, and billed cost per cell. PREPAID: requires Pipeworx credit balance \u2265 max_spend_usd (top up via experiment_topup); bills actual provider cost \u00d7 1.5 with a $0.10 minimum per experiment. ASYNC: returns experiment_id immediately \u2014 execution starts within ~1 minute; poll experiment_status until complete, then call experiment_results. Do NOT wait synchronously. Set summary:false to skip the AI-written model-comparison summary. Example: experiment_create({ name: \"tone test\", prompts: [\"Rewrite formally: ...\"], models: [\"anthropic/claude-haiku-4.5\", \"openai/gpt-5-mini\"], reps: 2, max_spend_usd: 2 })",
 "inputSchema": {
  "examples": [
   {
    "max_spend_usd": 5,
    "models": [
     "anthropic/claude-sonnet-4.5",
     "openai/gpt-4o-mini"
    ],
    "name": "model comparison",
    "prompts": [
     "Explain quantum computing in simple terms."
    ]
   },
   {
    "max_spend_usd": 10,
    "models": [
     "anthropic/claude-haiku-4.5",
     "openai/gpt-4o-mini",
     "google/gemini-2.0-flash"
    ],
    "name": "tone test",
    "params": {
     "max_tokens": 256,
     "temperature": 0.8
    },
    "prompts": [
     "Rewrite formally: hey there!",
     "Rewrite casually: Good morning."
    ],
    "reps": 2,
    "summary": true
   }
  ],
  "properties": {
   "max_spend_usd": {
    "description": "REQUIRED hard spend cap in USD for this experiment (max 100). Execution stops when reached.",
    "type": "number"
   },
   "models": {
    "description": "Model ids from experiment_models (max 12)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "name": {
    "description": "Short experiment name",
    "type": "string"
   },
   "params": {
    "description": "Optional {system, temperature, max_tokens (default 512)}",
    "type": "object"
   },
   "prompts": {
    "description": "Prompts to test (max 20)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "reps": {
    "description": "Repetitions per prompt\u00d7model for variance, 1-5 (default 1)",
    "type": "number"
   },
   "summary": {
    "description": "AI-written comparison of the models' outputs when the run completes (default true)",
    "type": "boolean"
   }
  },
  "required": [
   "prompts",
   "models",
   "max_spend_usd"
  ],
  "type": "object"
 },
 "name": "experiment_create",
 "title": "Experiment Create",
 "version": "v1.ba3d08bf"
}
