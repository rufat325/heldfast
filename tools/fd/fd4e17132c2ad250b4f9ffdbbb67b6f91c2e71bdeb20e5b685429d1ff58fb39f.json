{
 "description": "Check whether a channel name or handle collides with returned YouTube channel results.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "query": {
    "description": "Channel name or handle idea, for example 'CozyPixel'.",
    "maxLength": 60,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "check_youtube_channel_name",
 "title": "Check YouTube channel name"
}
