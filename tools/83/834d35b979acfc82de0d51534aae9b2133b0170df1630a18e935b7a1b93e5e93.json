{
 "description": "Manage TODO status. Update a todo's status (pending \u2192 in_progress \u2192 done) or list todos by status. Essential for tracking work across sessions without external files.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Memory ID to update status (omit to list)",
    "type": "string"
   },
   "list_status": {
    "description": "Filter todos by status (omit to list all)",
    "enum": [
     "pending",
     "in_progress",
     "done"
    ],
    "type": "string"
   },
   "status": {
    "description": "New status to set",
    "enum": [
     "pending",
     "in_progress",
     "done"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "memory_status"
}
