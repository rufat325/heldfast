{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Type text as real per-character keystrokes (fires keydown/keypress/input/keyup per char), unlike electron_type which sets the value directly. Focuses ref/selector first when given; otherwise types into the active element. For a code editor (Monaco / EditContext), the reliable path is electron_type_into_editor (it clicks the editor content area, e.g. '.monaco-editor .view-lines', then types into the focused editor) \u2014 do NOT target the hidden textarea, which modern editors ignore. force:true focuses an offscreen/aria-hidden input but a swallowed keystroke returns TYPE_NO_EFFECT. Options: force, timeoutMs. Returns: { ok, session_id, typed }. Errors: SELECTOR_NO_MATCH / REF_NOT_FOUND (carries similar_refs), ELEMENT_NOT_VISIBLE (retryable), TYPE_NO_EFFECT (typing changed nothing \u2014 use electron_type_into_editor), NOT_RUNNING, BAD_ARGUMENT.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "force": {
    "description": "Bypass actionability checks (visibility/enabled/stable). Default false.",
    "type": "boolean"
   },
   "ref": {
    "description": "Element ref from a snapshot (resolves to [data-sw-ref=\"N\"]). Provide ref OR selector.",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "selector": {
    "description": "CSS selector. Provide ref OR selector, not both.",
    "minLength": 1,
    "type": "string"
   },
   "sessionId": {
    "description": "Target session id. Omit when a single session is running.",
    "type": "string"
   },
   "text": {
    "description": "The text to type, character by character.",
    "maxLength": 10000,
    "type": "string"
   },
   "timeoutMs": {
    "description": "Actionability budget in ms (default 5000, clamped to 30000).",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "electron_keyboard_type",
 "title": "Type text as real keystrokes"
}
