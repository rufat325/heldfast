{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "restore_group"
 },
 "description": "Restore a previously archived group back to the active list.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "group_id": {
    "description": "The UUID of the archived group to restore.",
    "type": "string"
   }
  },
  "required": [
   "group_id"
  ],
  "type": "object"
 },
 "name": "restore_group"
}
