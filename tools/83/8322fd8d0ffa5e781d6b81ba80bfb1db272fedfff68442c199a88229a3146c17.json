{
 "description": "Create a phase plan for a workflow. A workflow can have at most one plan. Returns the new plan ID to use with add_phase. This is an alias of create_project.",
 "inputSchema": {
  "properties": {
   "description": {
    "description": "Optional short description of the project.",
    "type": "string"
   },
   "name": {
    "description": "Project plan name (defaults to the workflow name if omitted).",
    "type": "string"
   },
   "spec": {
    "description": "Optional full project spec stored on the plan record.",
    "type": "string"
   },
   "workflowId": {
    "description": "Workflow ID to attach the plan to.",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "workflowId"
  ],
  "type": "object"
 },
 "name": "create_plan"
}
