{
 "description": "Search 27,000+ AgentiScript SVG icons by concept. Returns icon URLs and embed code for AI, blockchain, DeFi, finance concepts.",
 "inputSchema": {
  "properties": {
   "limit": {
    "default": 10,
    "description": "Max results (default 10)",
    "type": "number"
   },
   "query": {
    "description": "Concept to search (e.g. zero knowledge proof, defi, xrpl)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_icons"
}
