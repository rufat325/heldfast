{
 "description": "Compare recorded API responses with explicit status, content-type, field, type, and stable-value contracts. Costs $0.05 USDC on Base mainnet.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "contracts": {
    "items": {
     "additionalProperties": true,
     "required": [
      "id",
      "expected_status"
     ],
     "type": "object"
    },
    "maxItems": 1000,
    "minItems": 1,
    "type": "array"
   },
   "responses": {
    "items": {
     "additionalProperties": true,
     "required": [
      "id",
      "contract_id",
      "status",
      "content_type",
      "body"
     ],
     "type": "object"
    },
    "maxItems": 1000,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "contracts",
   "responses"
  ],
  "type": "object"
 },
 "name": "justhandled_api_response_contract_drift_auditor"
}
