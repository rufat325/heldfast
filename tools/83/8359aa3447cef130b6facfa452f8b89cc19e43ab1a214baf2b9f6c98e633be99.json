{
 "description": "Permanently delete multiple pages in one request. DESTRUCTIVE and irreversible for pages without descendants excluded. Strongly consider preview_bulk_delete or get_deletable_descendant_counts first to see what would be affected.",
 "inputSchema": {
  "properties": {
   "pages": {
    "items": {
     "properties": {
      "contentId": {
       "type": "string"
      },
      "deleteAllDescendants": {
       "type": "boolean"
      },
      "excludedDescendantIds": {
       "items": {
        "type": "string"
       },
       "type": "array"
      }
     },
     "required": [
      "contentId"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "pages"
  ],
  "type": "object"
 },
 "name": "bulk_delete_pages"
}
