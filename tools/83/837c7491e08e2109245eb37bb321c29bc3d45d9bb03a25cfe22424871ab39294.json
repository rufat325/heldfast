{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Discover source sections"
 },
 "description": "Discover the logical sections of source legislation for a domain. Provide the raw text of your source documents (legislation, guidance notes, form instructions). The service analyses the content and identifies which sections should be authored as separate rule rulesets. Run BEFORE creating projects \u2014 you need to know the sections before you can create one. Call aethis_refine_sections if sections are missing or incorrectly split.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "anthropic_key": {
    "description": "An Anthropic API key the user explicitly provided for this call. [sensitive \u2014 do not echo or log] Deprecated: the raw value is written verbatim to the host's session transcript. Never fill this from the environment.",
    "type": "string"
   },
   "anthropic_key_env": {
    "description": "Optional. Only honoured when it equals the env var the user configured via AETHIS_ANTHROPIC_KEY_ENV in this MCP server's config; that configured key is used automatically, so this can be omitted. Do not guess a variable name: the server refuses any name the user did not configure.",
    "type": "string"
   },
   "anthropic_key_keychain": {
    "description": "macOS keychain reference the user created for Aethis: either 'service:account' or just 'account' (service defaults to 'aethis-anthropic-key'). The server reads it via the `security` command at call time.",
    "type": "string"
   },
   "domain": {
    "description": "Domain identifier, e.g. 'uk_citizenship'",
    "type": "string"
   },
   "openai_key": {
    "description": "Retired and refused: Aethis LLM tools use Anthropic models only.",
    "type": "string"
   },
   "sources": {
    "description": "Source documents to analyse. Provide the actual text content.",
    "items": {
     "additionalProperties": false,
     "properties": {
      "content": {
       "description": "Raw source text (legislation, guidance, form instructions)",
       "type": "string"
      },
      "name": {
       "description": "Filename or short identifier, e.g. 'bna1981_schedule1.md'",
       "type": "string"
      }
     },
     "required": [
      "name",
      "content"
     ],
     "type": "object"
    },
    "maxItems": 10,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "domain",
   "sources"
  ],
  "type": "object"
 },
 "name": "aethis_discover_sections"
}
