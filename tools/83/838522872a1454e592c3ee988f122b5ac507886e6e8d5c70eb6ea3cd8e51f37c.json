{
 "description": "Get a project's schema as CREATE TABLE statements. Accepts a project id or name and an optional SQL dialect (postgresql, mysql, sqlite, mssql).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "dialect": {
    "description": "SQL dialect for the DDL (default: the project's database type)",
    "enum": [
     "postgresql",
     "mysql",
     "sqlite",
     "mssql"
    ],
    "type": "string"
   },
   "project": {
    "description": "Project id (UUID) or project name",
    "type": "string"
   }
  },
  "required": [
   "project"
  ],
  "type": "object"
 },
 "name": "get_ddl"
}
