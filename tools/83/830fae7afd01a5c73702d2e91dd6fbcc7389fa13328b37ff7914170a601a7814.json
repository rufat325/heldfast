{
 "description": "Replace one clause in a template with the same clause from another template. Writes a new version.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "clause": {
    "description": "Clause name to replace.",
    "type": "string"
   },
   "from_ref": {
    "description": "Template reference to take the clause from.",
    "type": "string"
   },
   "target": {
    "description": "Template reference to modify.",
    "type": "string"
   }
  },
  "required": [
   "target",
   "clause",
   "from_ref"
  ],
  "type": "object"
 },
 "name": "template_vault_swap"
}
