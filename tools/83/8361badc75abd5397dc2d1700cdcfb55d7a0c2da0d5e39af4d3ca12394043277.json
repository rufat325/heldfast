{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get IPC (Integrated Food Security Phase Classification) data \u2014 population counts by food-insecurity phase (1=minimal to 5=catastrophe), current and projected. Filter by location_code (ISO3), ipc_phase, admin1_name, and admin_level. Data: HDX Humanitarian API (hapi.humdata.org), run by UN OCHA Centre for Humanitarian Data, CC BY 4.0, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "admin1_name": {
    "description": "1st-level subnational admin area name to filter results, e.g. \"Kayes\"",
    "type": "string"
   },
   "admin_level": {
    "description": "Admin granularity of returned rows: 0=national, 1=admin1, 2=admin2",
    "enum": [
     "0",
     "1",
     "2"
    ],
    "type": "string"
   },
   "ipc_phase": {
    "description": "IPC phase classification: 1=minimal, 2=stressed, 3=crisis, 4=emergency, 5=catastrophe, 3+=crisis or worse, all=all phases",
    "enum": [
     "1",
     "2",
     "3",
     "4",
     "5",
     "3+",
     "all"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Maximum rows to return (1-1000, default 100)",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "location_code": {
    "description": "ISO3 country code to filter results, e.g. \"MLI\", \"AFG\", \"SOM\"",
    "maxLength": 3,
    "minLength": 3,
    "type": "string"
   },
   "location_name": {
    "description": "Country name to filter results (alternative to location_code), e.g. \"Mali\"",
    "type": "string"
   },
   "offset": {
    "description": "Number of rows to skip, for pagination (default 0)",
    "minimum": 0,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "hdx-hapi.food.security_ipc",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "HDX HAPI Food Security (IPC)"
}
