{
 "_meta": {
  "skill": "ordinals-p2p"
 },
 "description": "Sign one or more PSBT inputs with the active wallet. Supports buyer (P2WPKH) and taproot keys.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "finalizeSignedInputs": {
    "default": false,
    "description": "Finalize only the inputs signed in this call",
    "type": "boolean"
   },
   "psbtBase64": {
    "description": "PSBT in base64 format",
    "type": "string"
   },
   "signInputs": {
    "description": "Optional input indexes to sign. Signs all signable inputs if omitted.",
    "items": {
     "maximum": 9007199254740991,
     "minimum": 0,
     "type": "integer"
    },
    "type": "array"
   }
  },
  "required": [
   "psbtBase64"
  ],
  "type": "object"
 },
 "name": "psbt_sign"
}
