{
 "_meta": {
  "xdata.kr/credential": {
   "providers": [
    "kb_biz_card"
   ],
   "register_url": "https://console.xdata.kr/credentials?link=1&priority=kb_biz_card",
   "required": true,
   "windows_pc_needed": true
  }
 },
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "REQUIRES prior kb_biz_card__session__login call with same account_link_id. Search KB Biz Card approvals rows.",
 "inputSchema": {
  "properties": {
   "account_link_id": {
    "description": "Account link ID (UUID v4). Optional \u2014 the server auto-resolves from your API key when omitted. Provide explicitly only when you have multiple certificates for the same provider. Each institution has a separate ID (Hometax != NHIS != NPS != FourInsure).",
    "type": "string"
   },
   "date_from": {
    "description": "Start date YYYY-MM-DD or YYYYMMDD",
    "type": "string"
   },
   "date_to": {
    "description": "End date YYYY-MM-DD or YYYYMMDD",
    "type": "string"
   },
   "department_name": {
    "description": "Department name",
    "type": "string"
   },
   "department_sequence_no": {
    "description": "Department sequence number",
    "type": "string"
   },
   "max_pages": {
    "description": "Maximum pages to collect",
    "type": "integer"
   },
   "page": {
    "description": "Page number; default 1",
    "type": "integer"
   }
  },
  "required": [
   "date_from",
   "date_to"
  ],
  "type": "object"
 },
 "name": "kb_biz_card__approvals__search",
 "securitySchemes": [
  {
   "scopes": [
    "mcp:tools:list",
    "mcp:tools:call"
   ],
   "type": "oauth2"
  }
 ],
 "title": "KB\uad6d\ubbfc\uce74\ub4dc \uc2b9\uc778\ub0b4\uc5ed \uac80\uc0c9"
}
