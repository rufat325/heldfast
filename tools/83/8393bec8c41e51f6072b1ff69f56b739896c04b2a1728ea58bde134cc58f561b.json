{
 "description": "Generate a plain-English health summary from lab results. Produces jargon-free explanations of findings, what they mean for the patient, and actionable next steps. Available in patient-friendly or provider-clinical tone. Costs 20 credits per call.",
 "inputSchema": {
  "properties": {
   "audience_type": {
    "default": "patient",
    "description": "Tone and detail level: patient (plain English, no jargon), provider (clinical terminology), executive (concise business summary)",
    "enum": [
     "patient",
     "provider",
     "executive"
    ],
    "type": "string"
   },
   "upload_id": {
    "description": "UUID of the lab upload to narrate",
    "type": "string"
   }
  },
  "required": [
   "upload_id"
  ],
  "type": "object"
 },
 "name": "generate_health_narrative"
}
