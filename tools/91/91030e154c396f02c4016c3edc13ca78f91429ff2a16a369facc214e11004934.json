{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Delete Production Item"
 },
 "description": "Permanently delete a production item (any entity: milestones, timeline, schedule_items, tasks, projects, vendors, talent, roles, inventory, programming, meals, places, modes, transactions, invoices, teams, receipts). To merely hide it, use update_production_item active:false instead. Requires event_id + entity + item_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "entity": {
    "enum": [
     "vendors",
     "talent",
     "tasks",
     "projects",
     "schedule",
     "receipts",
     "roles",
     "inventory",
     "programming",
     "meals",
     "places",
     "milestones",
     "timeline",
     "schedule_items",
     "modes",
     "transactions",
     "invoices",
     "teams",
     "buildUnits",
     "scholarships",
     "invitations"
    ],
    "type": "string"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "item_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "entity",
   "item_id"
  ],
  "type": "object"
 },
 "name": "delete_production_item",
 "title": "Delete Production Item"
}
