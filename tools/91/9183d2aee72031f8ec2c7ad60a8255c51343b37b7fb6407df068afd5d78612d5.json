{
 "description": "Clear all data of an app (pm clear) \u2014 resets it to a fresh-install state.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "appId": {
    "description": "Application id (defaults to session)",
    "type": "string"
   },
   "serial": {
    "description": "Device serial (defaults to session or the only device)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "clear_app_data",
 "title": "Clear app data"
}
