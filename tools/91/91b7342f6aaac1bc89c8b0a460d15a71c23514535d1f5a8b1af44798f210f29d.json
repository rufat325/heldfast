{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the Standard Ebooks catalog of carefully typeset, public-domain ebooks by title, author, or subject keyword. Returns title, author, language, description, subjects, cover image URL, and direct download links (EPUB, advanced EPUB, Kobo Kepub, Kindle AZW3, XHTML). Sortable by newest, author name, reading ease, length, or popularity. Data: standardebooks.org OPDS 2.0 JSON feed, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "page": {
    "description": "Page number for pagination (default 1).",
    "minimum": 1,
    "type": "integer"
   },
   "per_page": {
    "description": "Results per page (default 12, max 60).",
    "maximum": 60,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Free-text search across title, author name, and subject (e.g. \"frankenstein\", \"jane austen\"). Required \u2014 an empty query returns zero results.",
    "minLength": 1,
    "type": "string"
   },
   "sort": {
    "description": "Sort order: \"newest\" (S.E. release date, default), \"author\" (author name A-Z), \"reading_ease\" (easiest first), \"length\" (shortest first), \"popularity\" (most downloaded first).",
    "enum": [
     "newest",
     "author",
     "reading_ease",
     "length",
     "popularity"
    ],
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "standard-ebooks.books.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Search Standard Ebooks"
}
