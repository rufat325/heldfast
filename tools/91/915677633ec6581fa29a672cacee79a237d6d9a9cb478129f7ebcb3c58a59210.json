{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Convert the most recent recording into a test file. Supports three formats: appium (WebdriverIO + Mocha), maestro (YAML), and detox (Jest). Call stop_test_recording first.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "bundleId": {
    "description": "iOS bundle ID or Android app package",
    "type": "string"
   },
   "format": {
    "description": "Output format",
    "enum": [
     "appium",
     "maestro",
     "detox"
    ],
    "type": "string"
   },
   "includeSetup": {
    "default": true,
    "description": "Include WDIO configuration usage comments (the runner owns setup and teardown)",
    "type": "boolean"
   },
   "platform": {
    "default": "ios",
    "description": "Target platform (appium only)",
    "enum": [
     "ios",
     "android",
     "both"
    ],
    "type": "string"
   },
   "testName": {
    "description": "Name for the test / describe block",
    "type": "string"
   }
  },
  "required": [
   "format"
  ],
  "type": "object"
 },
 "name": "generate_test_from_recording"
}
