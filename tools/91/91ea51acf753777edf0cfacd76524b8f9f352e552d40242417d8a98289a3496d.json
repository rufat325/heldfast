{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Release Booking"
 },
 "description": "Release a venue booking \u2014 frees the Space's slot. Community creator/admin/moderator releases any booking; the booking's own host withdraws their slot (listing kept). Terminal; a re-request is a new booking. Optional reason recorded for audit. Requires community_id + booking_id.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "booking_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "community_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "reason": {
    "maxLength": 500,
    "type": "string"
   }
  },
  "required": [
   "community_id",
   "booking_id"
  ],
  "type": "object"
 },
 "name": "release_booking",
 "title": "Release Booking"
}
