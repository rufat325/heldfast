{
 "description": "Upload an app preview video (MP4/MOV/M4V). Handles 3-step upload process.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "fileName": {
    "description": "File name (e.g., preview_home.mp4)",
    "type": "string"
   },
   "filePath": {
    "description": "Absolute path to the video file",
    "type": "string"
   },
   "mimeType": {
    "description": "MIME type (auto-detected from extension if not provided)",
    "type": "string"
   },
   "previewSetId": {
    "description": "Preview Set ID",
    "type": "string"
   }
  },
  "required": [
   "previewSetId",
   "filePath",
   "fileName"
  ],
  "type": "object"
 },
 "name": "asc_upload_app_preview"
}
