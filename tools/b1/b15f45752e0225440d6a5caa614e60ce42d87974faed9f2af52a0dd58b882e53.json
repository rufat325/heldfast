{
 "annotations": {
  "readOnlyHint": true,
  "title": "List Vibo events"
 },
 "description": "List the events you're part of (as host or guest). Defaults to upcoming events; pass past:true for events that have already happened. Optionally filter by a search query.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "limit": {
    "description": "Max items to return (default 20).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "past": {
    "description": "Return past events instead of upcoming (default false).",
    "type": "boolean"
   },
   "q": {
    "description": "Search events by title.",
    "type": "string"
   },
   "skip": {
    "description": "Number of items to skip, for paging (default 0).",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "vibo_list_events"
}
