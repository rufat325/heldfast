{
 "description": "Create a public share link for a file so someone outside the workspace can view it. Returns the full link ONCE. accessMode \"link\" = anyone with the link; \"restricted\" = only allowlisted emails/domains (verified by emailed code). permission \"view\" or \"download\".",
 "inputSchema": {
  "properties": {
   "accessMode": {
    "description": "link (anyone) | restricted (specific people). Default link.",
    "enum": [
     "link",
     "restricted"
    ],
    "type": "string"
   },
   "allowComments": {
    "description": "Allow viewers to comment (after verifying their email). Default false.",
    "type": "boolean"
   },
   "allowedDomains": {
    "description": "restricted mode: allowlisted domains, e.g. buddyboss.com",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "allowedEmails": {
    "description": "restricted mode: allowlisted emails",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "expiresInDays": {
    "description": "Optional. Link expires after N days (omit for no expiry).",
    "type": "number"
   },
   "fileId": {
    "description": "UUID of the file to share",
    "type": "string"
   },
   "permission": {
    "description": "view | download. Default view.",
    "enum": [
     "view",
     "download"
    ],
    "type": "string"
   }
  },
  "required": [
   "fileId"
  ],
  "type": "object"
 },
 "name": "share_create"
}
