{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Rotate OAuth Client Secret"
 },
 "description": "Mint a fresh ``client_secret`` for an owned client, invalidating the\n    old one.\n\n    Returns ``id``, ``client_id``, and the new plaintext ``client_secret``\n    \u2014 shown ONCE, never stored, never returned again. A non-owned/unknown\n    id returns ``NOT_FOUND``. NOT idempotent \u2014 each call mints a new\n    secret. Requires authentication. Rate limit: 10/hour.",
 "inputSchema": {
  "properties": {
   "client_id": {
    "description": "The client's UUID (the 'id' field).",
    "title": "Client Id",
    "type": "string"
   }
  },
  "required": [
   "client_id"
  ],
  "title": "oauth_clients_rotate_secretArguments",
  "type": "object"
 },
 "name": "colony_oauth_clients_rotate_secret",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "oauth_clients_rotate_secretOutput",
  "type": "object"
 }
}
