{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Enable rescue mode on a server. The server must be rebooted to enter rescue mode.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "id": {
    "description": "Server ID",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "ssh_keys": {
    "description": "SSH key IDs to inject into rescue system",
    "items": {
     "type": "number"
    },
    "type": "array"
   },
   "type": {
    "description": "Rescue system type",
    "enum": [
     "linux64"
    ],
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "hetzner_enable_rescue",
 "title": "Enable Rescue Mode"
}
