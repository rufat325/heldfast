{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List LLMs from the OpenRouter catalog (~340 models across all major providers) with normalized $/1M-token pricing, context windows, and modality. Filter by search, free-only, minimum context, or modality (e.g. \"image\" for vision models). Sorted cheapest-first by completion price. Keyless.",
 "inputSchema": {
  "examples": [
   {
    "limit": 10,
    "search": "claude"
   },
   {
    "free_only": true,
    "min_context": 100000,
    "modality": "image"
   }
  ],
  "properties": {
   "free_only": {
    "description": "If true, only return models whose prompt price is $0 (free tier).",
    "type": "boolean"
   },
   "limit": {
    "description": "Max models to return (default 25, max 60).",
    "type": "number"
   },
   "min_context": {
    "description": "Minimum context_length in tokens, e.g. 100000 or 200000.",
    "type": "number"
   },
   "modality": {
    "description": "Substring match on architecture.modality, e.g. \"image\" to find vision/multimodal models, \"text->text\" for text-only.",
    "type": "string"
   },
   "search": {
    "description": "Case-insensitive substring match against model id, name, and description. e.g. \"claude\", \"gpt-4o\", \"deepseek\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_models",
 "title": "List Models",
 "version": "v1.1b58a565"
}
