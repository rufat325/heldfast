{
 "description": "Remove the tracking domain; NEW messages go back to the platform origin. Links already sent keep pointing at the removed host. Links already sent keep pointing at the removed host: this stops NEW messages, not old ones.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_tracking_domain_remove"
}
