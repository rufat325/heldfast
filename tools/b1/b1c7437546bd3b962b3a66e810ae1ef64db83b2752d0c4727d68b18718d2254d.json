{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Terminal Aerodrome Forecast (TAF) for one or more ICAO airport codes (e.g. KSFO,EGLL); returns forecast periods covering wind, visibility, and cloud ceilings for the next 24\u201330 h.",
 "inputSchema": {
  "examples": [
   {
    "ids": "KSFO,EGLL"
   },
   {
    "format": "raw",
    "ids": "KDFW"
   }
  ],
  "properties": {
   "date": {
    "description": "Optional. ISO-8601 UTC instant (e.g. \"2026-08-20T12:00:00Z\") to retrieve the TAF that was valid then. Omit for the current forecast \u2014 the TAF already covers the next 24\u201330 h, so there is no lookback-hours argument.",
    "type": "string"
   },
   "format": {
    "description": "json (default) | raw",
    "type": "string"
   },
   "ids": {
    "description": "Comma-separated ICAO codes (4 letters, e.g. KSFO,EGLL) \u2014 not IATA codes.",
    "type": "string"
   }
  },
  "required": [
   "ids"
  ],
  "type": "object"
 },
 "name": "taf",
 "outputSchema": {
  "oneOf": [
   {
    "description": "Terminal Aerodrome Forecasts in JSON format",
    "type": "object"
   },
   {
    "properties": {
     "body": {
      "description": "Raw response body",
      "type": "string"
     },
     "format": {
      "description": "Response format (raw)",
      "type": "string"
     }
    },
    "required": [
     "format",
     "body"
    ],
    "type": "object"
   }
  ]
 },
 "title": "Taf",
 "version": "v1.a3b2d328"
}
