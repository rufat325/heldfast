{
 "_meta": {
  "ui": {
   "resourceUri": "ui://metro/components"
  },
  "ui/resourceUri": "ui://metro/components"
 },
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Get a paged flat React component tree. Follow nextCursor to complete the snapshot and check traversal.complete before treating an empty page as authoritative.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "cursor": {
    "description": "Opaque nextCursor returned by the previous page",
    "type": "string"
   },
   "maxDepth": {
    "default": 200,
    "description": "Maximum fiber depth to traverse (default 200, maximum 600)",
    "maximum": 600,
    "minimum": 0,
    "type": "integer"
   },
   "maxNodes": {
    "default": 1200,
    "description": "Maximum fibers to scan (default 1200)",
    "maximum": 5000,
    "minimum": 1,
    "type": "integer"
   },
   "pageSize": {
    "default": 100,
    "description": "Nodes per page (default 100, maximum 250)",
    "maximum": 250,
    "minimum": 1,
    "type": "integer"
   },
   "structureOnly": {
    "default": false,
    "description": "Return component names and selectors without props",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "get_component_tree"
}
