{
 "description": "Install a built .app bundle into a simulator using xcrun simctl install. Use after build_project or after locating a derived-data app bundle.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "appPath": {
    "description": "Path to a built .app bundle.",
    "type": "string"
   },
   "udid": {
    "description": "Simulator UDID. Use simulator_list first when unsure.",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "udid",
   "appPath"
  ],
  "type": "object"
 },
 "name": "install_app",
 "title": "Install an app bundle"
}
