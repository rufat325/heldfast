{
 "description": "Scan a game directory and detect what engine it uses (Unity, Unreal, Godot, Java, etc.). Returns engine type, runtime, and primary assembly path.",
 "inputSchema": {
  "properties": {
   "game_path": {
    "description": "Full path to the game directory",
    "type": "string"
   }
  },
  "required": [
   "game_path"
  ],
  "type": "object"
 },
 "name": "detect_engine"
}
