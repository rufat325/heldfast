{
 "annotations": {
  "readOnlyHint": true,
  "title": "Get cohort"
 },
 "description": "One cohort: its identity, its population, and the full member roster with each provider's Kin Score and agent readiness. Understanding plan. The analysis parts (stats/rankings/scores/capabilities) are Understanding too \u2014 they moved down from Influence on 2026-09-01. Priced per result \u2014 $0.005 per request plus $0.0002 per record returned (less past 100), capped at $0.50 a request, under pay-as-you-go; included in Understanding and Influence. Estimate first at /api/v1/prices/estimate. See apis://prices.",
 "inputSchema": {
  "properties": {
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "kind": {
    "enum": [
     "tag",
     "industry",
     "region",
     "area",
     "vc"
    ],
    "type": "string"
   },
   "slug": {
    "type": "string"
   }
  },
  "required": [
   "kind",
   "slug"
  ],
  "type": "object"
 },
 "name": "get_cohort"
}
