{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Update plan block"
 },
 "description": "Update fields of an existing day-planner block. Pass only the fields you want to change. Title may reference a board task with `@boardSlug/N`. Returns the updated block.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "end_time": {
    "description": "End time HH:mm, or null to clear",
    "type": [
     "string",
     "null"
    ]
   },
   "id": {
    "description": "Day-planner block UUID",
    "format": "uuid",
    "type": "string"
   },
   "recurrence": {
    "description": "New repeat rule. ONCE requires scheduled_day.",
    "enum": [
     "DAILY",
     "WEEKDAYS",
     "ONCE"
    ],
    "type": "string"
   },
   "scheduled_day": {
    "description": "Local date YYYY-MM-DD, or null to clear",
    "type": [
     "string",
     "null"
    ]
   },
   "scheduled_time": {
    "description": "Start time HH:mm, or null to clear",
    "type": [
     "string",
     "null"
    ]
   },
   "sort_order": {
    "description": "New manual sort position",
    "type": "number"
   },
   "title": {
    "description": "New title \u2014 use `@boardSlug/N` to link a board task",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "update_daily_plan_block",
 "title": "Update plan block"
}
