{
 "description": "Close a JAR session and clean up temp files.",
 "inputSchema": {
  "properties": {
   "cleanup": {
    "description": "Delete temp files (default: true)",
    "type": "boolean"
   },
   "session_id": {
    "description": "Session ID",
    "type": "string"
   }
  },
  "required": [
   "session_id"
  ],
  "type": "object"
 },
 "name": "jar_close"
}
