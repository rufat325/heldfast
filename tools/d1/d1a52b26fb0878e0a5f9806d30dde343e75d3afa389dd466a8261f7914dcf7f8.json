{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Inspect a pull request: read-only metadata lookup (repo, number, title). Despite the name, nothing is tracked or persisted \u2014 in v2, PRs are discovered automatically on each daily run. Use `daily` or `status` for ongoing monitoring.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "prUrl": {
    "description": "Full GitHub PR URL to fetch metadata for (e.g. https://github.com/owner/repo/pull/123)",
    "pattern": "^https:\\/\\/github\\.com\\/[^/]+\\/[^/]+\\/pull\\/\\d+$",
    "type": "string"
   }
  },
  "required": [
   "prUrl"
  ],
  "type": "object"
 },
 "name": "track"
}
