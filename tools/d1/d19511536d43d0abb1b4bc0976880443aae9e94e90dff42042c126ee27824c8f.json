{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Search SEC Filings"
 },
 "description": "Search SEC filings and earnings-call transcripts with hybrid keyword and semantic retrieval. Omit ticker to search every company, or provide one ticker to search only that company. Returns excerpts with document IDs for SearchDocument or ReadDocumentLines. Use excludeTickers and maxResultsPerCompany only for market-wide discovery; use ListFilings to browse filings newest first without a text query.",
 "inputSchema": {
  "properties": {
   "documentTypes": {
    "default": null,
    "description": "Optional document types. Accepts registered values such as TenK, TenQ, EightK, TwentyF, SixK, FortyF, or deployment-registered types such as EarningsCallTranscript. Display names such as 10-K are also accepted; an invalid value returns the full accepted list.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "endDate": {
    "default": null,
    "description": "Optional end date filter in YYYY-MM-DD format",
    "format": "date-time",
    "type": [
     "string",
     "null"
    ]
   },
   "excludeTickers": {
    "default": null,
    "description": "Optional tickers to exclude from a market-wide search (max 25). Cannot be combined with ticker.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "maxExcerptChars": {
    "default": 0,
    "description": "Maximum characters per excerpt (default: 0 = full excerpt). Set a small value (e.g. 400) for a compact scan across many results; truncated excerpts end with an explicit note.",
    "type": "integer"
   },
   "maxResults": {
    "default": 5,
    "description": "Maximum number of results to return (default: 5, max: 500)",
    "type": "integer"
   },
   "maxResultsPerCompany": {
    "default": 0,
    "description": "Maximum results from any single company (default: 0 = unlimited). Set a small value (e.g. 2) to spread results across more companies for discovery-style queries.",
    "type": "integer"
   },
   "query": {
    "description": "Search query \u2014 plain keywords or a short natural-language phrase. When too few excerpts match every word, the search automatically broadens to match any of the words; concise, filing-phrased terms (e.g. 'Data Center revenue') still rank best.",
    "type": "string"
   },
   "startDate": {
    "default": null,
    "description": "Optional start date filter in YYYY-MM-DD format",
    "format": "date-time",
    "type": [
     "string",
     "null"
    ]
   },
   "ticker": {
    "default": null,
    "description": "Optional company ticker. Omit to search across all companies.",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "SearchDocuments",
 "title": "Search SEC Filings"
}
