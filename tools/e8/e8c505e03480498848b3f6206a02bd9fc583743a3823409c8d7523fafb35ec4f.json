{
 "description": "Search the web using Brave Search via SignalFuse gateway. Paid endpoint (x402). Returns Brave search results.",
 "inputSchema": {
  "properties": {
   "count": {
    "description": "Number of results to return (default 10)",
    "type": "number"
   },
   "credit_token": {
    "description": "Optional credit token for bulk-prepaid access",
    "type": "string"
   },
   "query": {
    "description": "Search query string",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_brave"
}
