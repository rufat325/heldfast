{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "\u26a1 ACTION: Send a command to a physical device the calling agent owns via a linked vendor connection (e.g. turn a plug on/off, set an AC temperature). Numeric setpoints are bounds-checked against the device class's configured safety limits (config/device-classes.json); some device classes require confirm:true. Requires connection_id, device_id, command, value.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "command": {
    "description": "The device's data-point code, e.g. 'switch_1', 'temp_set'",
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "confirm": {
    "description": "Must be true for any device class flagged confirm_required in the safety layer (locks, valves, etc.)",
    "type": "boolean"
   },
   "connection_id": {
    "description": "Your device-connection id from /connect/device/connections",
    "format": "uuid",
    "type": "string"
   },
   "device_id": {
    "description": "The vendor's device id (from device.list)",
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "value": {
    "description": "The value to set for this command",
    "type": [
     "string",
     "number",
     "boolean"
    ]
   }
  },
  "required": [
   "connection_id",
   "device_id",
   "command",
   "value"
  ],
  "type": "object"
 },
 "name": "device.registry.command",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Send Device Command"
}
