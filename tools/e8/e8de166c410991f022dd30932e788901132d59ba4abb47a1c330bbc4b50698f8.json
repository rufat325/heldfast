{
 "_meta": {
  "xdata.kr/credential": {
   "providers": [
    "comwel"
   ],
   "register_url": "https://console.xdata.kr/credentials?link=1&priority=comwel",
   "required": true,
   "windows_pc_needed": true
  }
 },
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Call this FIRST before any other tool for this provider. Login to COMWEL via NPKI AnySign certificate.",
 "inputSchema": {
  "properties": {
   "account_link_id": {
    "description": "Account link ID (UUID v4). Optional \u2014 the server auto-resolves from your API key when omitted. Provide explicitly only when you have multiple certificates for the same provider. Each institution has a separate ID (Hometax != NHIS != NPS != FourInsure).",
    "type": "string"
   },
   "auth_provider": {
    "description": "Auth provider: npki_anysign (default: npki_anysign)",
    "type": "string"
   },
   "business_registration_no": {
    "description": "10-digit business registration number (e.g. 1234567890)",
    "type": "string"
   }
  },
  "required": [
   "business_registration_no"
  ],
  "type": "object"
 },
 "name": "comwel__auth__npki_session__login",
 "securitySchemes": [
  {
   "scopes": [
    "mcp:tools:list",
    "mcp:tools:call"
   ],
   "type": "oauth2"
  }
 ],
 "title": "\uadfc\ub85c\ubcf5\uc9c0\uacf5\ub2e8 \ub85c\uadf8\uc778"
}
