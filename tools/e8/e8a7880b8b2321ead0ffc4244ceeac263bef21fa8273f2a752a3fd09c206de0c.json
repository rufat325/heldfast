{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List zones (domains), optionally filtered by name or status.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "fetch_all": {
    "description": "When true, follow pagination and return all pages combined (ignores page/per_page).",
    "type": "boolean"
   },
   "name": {
    "description": "Filter by domain name (exact or substring).",
    "type": "string"
   },
   "page": {
    "type": "number"
   },
   "per_page": {
    "type": "number"
   },
   "status": {
    "description": "Filter by zone status.",
    "enum": [
     "active",
     "pending",
     "initializing",
     "moved",
     "deleted",
     "deactivated"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_zones"
}
