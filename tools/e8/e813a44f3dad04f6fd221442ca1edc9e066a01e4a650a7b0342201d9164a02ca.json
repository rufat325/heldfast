{
 "description": "Get labels commonly used together with a given label, globally (returns global labels only).",
 "inputSchema": {
  "properties": {
   "labelName": {
    "type": "string"
   }
  },
  "required": [
   "labelName"
  ],
  "type": "object"
 },
 "name": "get_related_labels"
}
