{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "readOnlyHint": false
 },
 "description": "Unblock a criterion. Transitions blocked \u2192 prior_status (recorded when block was set; defaults to ready).",
 "inputSchema": {
  "properties": {
   "criterionId": {
    "type": "string"
   },
   "resolution": {
    "description": "How the blocker was resolved (1-2000 chars)",
    "type": "string"
   }
  },
  "required": [
   "criterionId",
   "resolution"
  ],
  "type": "object"
 },
 "name": "vibemap_unblock_criterion"
}
