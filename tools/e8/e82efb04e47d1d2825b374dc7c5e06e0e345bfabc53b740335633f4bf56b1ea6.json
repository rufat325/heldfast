{
 "annotations": {
  "readOnlyHint": true,
  "title": "Match providers to a need"
 },
 "description": "UNDERSTANDING \u2014 The supply\u2194demand join: apis.io providers whose product appears in a company's adopted stack, ranked by rating. Priced B2 (cross-catalog synthesis) \u2014 $0.05 per call under pay-as-you-go; included in Understanding and Influence. See apis://prices.",
 "inputSchema": {
  "properties": {
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "slug": {
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "match_providers"
}
