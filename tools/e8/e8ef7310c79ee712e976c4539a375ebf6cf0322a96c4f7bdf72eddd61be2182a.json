{
 "description": "Fetch a current Jupiter route quote for Solana token mints and a base-unit amount. This does not sign or broadcast a transaction. Price: 0.03 USDC. Mode: live_source. Source: Jupiter Lite Quote API. Set auto_pay=true for a one-call purchase when the local buyer wallet is configured.",
 "inputSchema": {
  "properties": {
   "agent_token": {
    "description": "Optional pre-funded marketplace bearer token.",
    "type": "string"
   },
   "amount": {
    "description": "Amount of input token in its native decimals.",
    "type": "integer"
   },
   "auto_pay": {
    "description": "Automatically authorize and pay this request with the locally configured buyer wallet. Requires X402_AUTO_PAY=true and X402_EVM_PRIVATE_KEY.",
    "type": "boolean"
   },
   "input_mint": {
    "description": "Mint address of the token to sell.",
    "type": "string"
   },
   "output_mint": {
    "description": "Mint address of the token to buy.",
    "type": "string"
   },
   "payment_id": {
    "description": "payment_id returned by the x402 invoice.",
    "type": "string"
   },
   "payment_signature": {
    "description": "Base64 x402 v2 PAYMENT-SIGNATURE produced by a compatible buyer client.",
    "type": "string"
   },
   "preview": {
    "description": "Inspect a rate-limited live contract proof showing source, freshness, response shape, and required inputs. Decision-ready values remain in the paid result.",
    "type": "boolean"
   },
   "slippage_bps": {
    "description": "Maximum allowed slippage in basis points.",
    "type": "integer"
   },
   "tx_hash": {
    "description": "Base transaction hash after paying an x402 invoice.",
    "type": "string"
   }
  },
  "required": [
   "input_mint",
   "output_mint",
   "amount"
  ],
  "type": "object"
 },
 "name": "solana_route"
}
