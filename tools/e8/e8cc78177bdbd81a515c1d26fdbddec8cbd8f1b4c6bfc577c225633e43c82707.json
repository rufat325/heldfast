{
 "description": "Resolve an \"unconfirmed\" readiness dimension, in a local intent.md or in a connected workspace. Use this when check_intent_readiness reports that it READ your objective or outcomes but could not confirm them: the gate matches a fixed English vocabulary and misses valid phrasing, including other languages. confirm: state who is affected and what goes wrong for them, in your own words, and the dimension passes. waive: mark the dimension as not applicable to this change (a pure refactor has no product outcome). A waiver is a HUMAN decision, so set humanApproved only after the user has explicitly agreed. Waivers are local-mode only: a workspace will not accept one over an API key, which authenticates as a machine. The confirmation is bound to the exact text it was made against and dies automatically if that text is edited. You cannot confirm a dimension the gate read nothing for; write the field first, save, then confirm.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "action": {
    "description": "confirm: vouch that the text names what the gate could not find. waive: this dimension does not apply to this change.",
    "enum": [
     "confirm",
     "waive"
    ],
    "type": "string"
   },
   "actor": {
    "description": "confirm + objective: who is affected, in your words.",
    "type": "string"
   },
   "dimension": {
    "description": "Only these two are confirmable. Title and verification are never waivable.",
    "enum": [
     "objective",
     "outcomes"
    ],
    "type": "string"
   },
   "humanApproved": {
    "description": "waive only. Set ONLY after the user has explicitly approved the waiver. Recorded as a human decision that this environment cannot verify.",
    "type": "boolean"
   },
   "intentId": {
    "description": "Cloud mode: confirm on a specific saved intent. Defaults to the current intent.",
    "type": "string"
   },
   "observable": {
    "description": "confirm + outcomes: how you would see it.",
    "type": "string"
   },
   "outcome": {
    "description": "confirm + outcomes: the observable change.",
    "type": "string"
   },
   "path": {
    "description": "Local mode only: path to the intent file. Defaults to the one check_intent_readiness reads: intent.md at the root, else the single intent/**/intent.md.",
    "type": "string"
   },
   "problem": {
    "description": "confirm + objective: what goes wrong for them today.",
    "type": "string"
   },
   "reason": {
    "description": "waive: why this dimension does not apply.",
    "type": "string"
   }
  },
  "required": [
   "dimension",
   "action"
  ],
  "type": "object"
 },
 "name": "confirm_intent_dimension",
 "title": "Confirm or waive a readiness dimension"
}
