{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "One workflow"
 },
 "description": "One playbook's steps from the service itself, each annotated with the exact datailo_invoke call to run it through this connection. service='datailo' with a job name returns the whole job: services to enable, stages with their playbooks, checkpoints and the work-product template.",
 "inputSchema": {
  "properties": {
   "name": {
    "title": "Name",
    "type": "string"
   },
   "service": {
    "title": "Service",
    "type": "string"
   },
   "session": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "The `session` value datailo_connect_verify returned. Pass it on every Datailo call in this conversation; omit it when the user connected with Sign in (OAuth) or a key.",
    "title": "Session"
   }
  },
  "required": [
   "service",
   "name"
  ],
  "title": "datailo_workflowArguments",
  "type": "object"
 },
 "name": "datailo_workflow",
 "title": "One workflow"
}
