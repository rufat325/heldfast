{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Compare the current pricing plans of 2 to 5 SaaS products side by side. Call this when the user is choosing between named products and wants their prices in one view. Takes product slugs (find them with search_products).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "slugs": {
    "description": "2-5 product slugs",
    "items": {
     "maxLength": 255,
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 5,
    "minItems": 2,
    "type": "array"
   }
  },
  "required": [
   "slugs"
  ],
  "type": "object"
 },
 "name": "compare_products",
 "title": "Compare product pricing"
}
