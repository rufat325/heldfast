{
 "description": "Importa un entorno desde .atm/ o un archivo espec\u00edfico. Auto-detecta archivos .env.json en .atm/.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "activate": {
    "description": "Activar el entorno importado como entorno activo (default: false)",
    "type": "boolean"
   },
   "file": {
    "description": "Ruta al archivo (default: busca *.env.json en .atm/)",
    "type": "string"
   },
   "name": {
    "description": "Nombre para el entorno (default: usa el nombre del archivo exportado)",
    "type": "string"
   },
   "overwrite": {
    "description": "Sobreescribir si ya existe un entorno con el mismo nombre (default: false)",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "import_environment"
}
