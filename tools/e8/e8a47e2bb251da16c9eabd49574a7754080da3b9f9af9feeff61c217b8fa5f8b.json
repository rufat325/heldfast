{
 "_meta": {
  "skill": "ordinals-p2p"
 },
 "description": "Return a step-by-step guide for M-of-N Taproot multisig coordination between agents using OP_CHECKSIGADD (BIP-341/342). Read-only \u2014 no wallet needed.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "m": {
    "default": 2,
    "description": "Number of required signatures (default: 2).",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "n": {
    "default": 3,
    "description": "Total number of co-signers (default: 3).",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "taproot_multisig_guide"
}
