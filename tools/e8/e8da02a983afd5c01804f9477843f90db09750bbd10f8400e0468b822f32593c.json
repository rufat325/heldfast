{
 "description": "Analyse a video before editing it. Extracts frames at intervals and (when a whisper model is supplied) transcribes the audio, returning a structured timeline of what is shown and said. Requires ffmpeg; transcription additionally needs whisper.cpp + a model file.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "frameInterval": {
    "description": "Seconds between extracted frames. Default: 3.",
    "type": "number"
   },
   "input": {
    "description": "Absolute path to the video file.",
    "type": "string"
   },
   "maxFrames": {
    "description": "Maximum number of frames to extract. Default: 30.",
    "type": "number"
   },
   "whisperModel": {
    "description": "Absolute path to a whisper.cpp model file (.bin). When supplied, the audio is transcribed and merged into the timeline.",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "media_video_understand"
}
