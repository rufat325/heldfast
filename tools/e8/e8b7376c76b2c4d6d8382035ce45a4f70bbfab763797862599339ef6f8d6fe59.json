{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Free. One full risk card (the exact paid mint_risk_card shape) for a mint aged >= 300 s with >= 3 buys, delayed. Use it to see every field before paying; the server picks the mint, so it is not a check on yours. No arguments.",
 "inputSchema": {
  "properties": {},
  "title": "sample_mintArguments",
  "type": "object"
 },
 "name": "sample_mint",
 "title": "Sample risk card (free)"
}
