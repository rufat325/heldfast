{
 "description": "Every DexScreener trading pair matching a query: ticker, token name, contract/mint address, pair address, or a dexscreener.com URL. Returns chain, dex, price, 24h change, liquidity, volume, and the pair address (chartable at https://dexscreener.com/<chainId>/<pairAddress>).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max pairs to return (default 10)",
    "maximum": 30,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Ticker, name, CA/mint, pair address, or dexscreener.com URL",
    "minLength": 2,
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_pairs",
 "title": "DexScreener pair search"
}
