{
 "description": "Fetch several RSS/Atom feeds and return a merged, date-sorted list of recent items. Useful for building a digest.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "perFeed": {
    "default": 5,
    "description": "Maximum items to take from each feed.",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "urls": {
    "description": "List of feed URLs.",
    "items": {
     "format": "uri",
     "type": "string"
    },
    "maxItems": 20,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "urls"
  ],
  "type": "object"
 },
 "name": "fetch_many",
 "title": "Fetch multiple feeds"
}
