{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find OpenAQ air-quality monitoring stations by location: a city or place name, coordinates+radius (nearest first), a country (ISO 3166-1 alpha-2 code), or a bounding box. Returns station id, name, country, coordinates, and the pollutants each measures (with sensor ids), plus resolved_place echoing the coordinates a city name resolved to. This is the first step for a historical series: find the station here, then pass its sensor_id to get_measurements. At least one location argument is required \u2014 the tool will not return an unfiltered global list.",
 "inputSchema": {
  "examples": [
   {
    "city": "Delhi"
   },
   {
    "country": "US",
    "limit": 10
   },
   {
    "latitude": 51.5074,
    "longitude": -0.1278,
    "radius_km": 20
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "OpenAQ API key (optional; the gateway supplies one).",
    "type": "string"
   },
   "bbox": {
    "description": "Bounding box \"minLon,minLat,maxLon,maxLat\" to search within.",
    "type": "string"
   },
   "city": {
    "description": "City or place name, e.g. \"Delhi\", \"Los Angeles\", \"Krak\u00f3w\". Geocoded to coordinates, then searched by radius. Add the country for ambiguous names (\"Cambridge, UK\").",
    "type": "string"
   },
   "country": {
    "description": "ISO 3166-1 alpha-2 country code (e.g. \"US\", \"IN\", \"GB\").",
    "type": "string"
   },
   "latitude": {
    "description": "Latitude for a radius search (use with longitude).",
    "type": "number"
   },
   "limit": {
    "description": "Max stations to return (1-100, default 20).",
    "type": "number"
   },
   "longitude": {
    "description": "Longitude for a radius search (use with latitude).",
    "type": "number"
   },
   "radius_km": {
    "description": "Radius in km for a coordinate or city search (default 12, max 25).",
    "type": "number"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "find_stations",
 "title": "Find Stations",
 "version": "v1.a6156c11"
}
