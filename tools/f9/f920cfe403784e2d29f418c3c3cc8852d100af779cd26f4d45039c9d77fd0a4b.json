{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Check whether a messaging or circumvention app is blocked in a country using OONI app-specific tests: WhatsApp, Telegram, Signal, Facebook Messenger, or Tor. Returns measurement counts and a blocking assessment, e.g. \"is Telegram blocked in Iran\", \"is Tor reachable from Russia\".",
 "inputSchema": {
  "examples": [
   {
    "app": "telegram",
    "country": "IR"
   },
   {
    "app": "tor",
    "country": "CN",
    "days": 60
   }
  ],
  "properties": {
   "app": {
    "description": "One of: whatsapp | telegram | signal | facebook_messenger | tor.",
    "type": "string"
   },
   "country": {
    "description": "2-letter country code, e.g. \"IR\", \"RU\", \"CN\".",
    "type": "string"
   },
   "days": {
    "description": "Lookback window in days (default 30, max 365).",
    "type": [
     "number",
     "string"
    ]
   }
  },
  "required": [
   "app",
   "country"
  ],
  "type": "object"
 },
 "name": "ooni_app_blocking",
 "title": "Ooni App Blocking",
 "version": "v1.05ac6cb5"
}
