{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch an invoice as a PDF. `base64` returns the document encoded for a caller that will decode and save it. `binary` reports the size and type only \u2014 raw PDF bytes are never inlined into a tool result, because they would flood the conversation without being usable.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "format": {
    "default": "binary",
    "description": "base64 returns the encoded document; binary returns metadata and a byte count only.",
    "enum": [
     "base64",
     "binary"
    ],
    "type": "string"
   },
   "invoiceId": {
    "description": "Invoice id, as returned by ws_invoice_list.",
    "minLength": 1,
    "type": "string"
   },
   "userId": {
    "default": "self",
    "description": "Account id, or the literal \"self\" for the authenticated account. Defaults to \"self\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "invoiceId"
  ],
  "type": "object"
 },
 "name": "ws_invoice_pdf",
 "title": "Get invoice PDF"
}
