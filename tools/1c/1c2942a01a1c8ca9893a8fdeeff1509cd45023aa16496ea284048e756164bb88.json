{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "DOES NOT WORK and cannot be made to: formulae.brew.sh/api/formula.json no longer carries a `date_added` field on any of its 8,600 formulae (verified 2026-09-16), so there is nothing left to sort or filter by and this tool can only return an empty list with `empty_reason: \"upstream_field_removed\"`. Homebrew publishes no dated feed. For install popularity use analytics_install; for a single formula use formula.",
 "inputSchema": {
  "examples": [
   {
    "days": 7,
    "limit": 10
   }
  ],
  "properties": {
   "days": {
    "description": "1-365 (default 30).",
    "type": "number"
   },
   "limit": {
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "recent_formulae",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of recently-added formulae found",
    "type": "number"
   },
   "days": {
    "description": "Number of days looked back (1-365)",
    "type": "number"
   },
   "formulae": {
    "description": "Array of recently-added formulae",
    "items": {
     "properties": {
      "date_added": {
       "description": "ISO date when formula was added",
       "type": "string"
      },
      "name": {
       "description": "Formula name",
       "type": "string"
      },
      "ruby_source_path": {
       "description": "Path to Ruby source",
       "type": "string"
      },
      "tap": {
       "description": "Tap name",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "days",
   "count",
   "formulae"
  ],
  "type": "object"
 },
 "title": "Recent Formulae",
 "version": "v1.4ecfe173"
}
