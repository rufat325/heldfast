{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Usage statistics for a hosting service. `kind` selects the series: size (disk usage), domain (per-domain traffic), or ftp (FTP activity).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "hostingId": {
    "description": "Hosting id or uuid, as returned by ws_hosting_list.",
    "minLength": 1,
    "type": "string"
   },
   "kind": {
    "description": "Which statistics series to return.",
    "enum": [
     "size",
     "domain",
     "ftp"
    ],
    "type": "string"
   },
   "userId": {
    "default": "self",
    "description": "Account id, or the literal \"self\" for the authenticated account. Defaults to \"self\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "hostingId",
   "kind"
  ],
  "type": "object"
 },
 "name": "ws_hosting_stats",
 "title": "Hosting statistics"
}
