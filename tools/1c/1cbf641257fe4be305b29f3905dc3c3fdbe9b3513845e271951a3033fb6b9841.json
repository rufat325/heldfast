{
 "description": "Generate a NEW article from a brief with a complete outline. Always creates a new article and never overwrites the brief's existing content (use regenerate_content to rewrite one). Returns a content ID for get_job_status.",
 "inputSchema": {
  "properties": {
   "brief_id": {
    "type": "string"
   },
   "generate_images": {
    "description": "Generate a hero image (default false)",
    "type": "boolean"
   }
  },
  "required": [
   "brief_id"
  ],
  "type": "object"
 },
 "name": "generate_content"
}
