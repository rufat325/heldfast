{
 "description": "Update an existing BrewPage HTML/Markdown page. Requires owner token. Returns the updated page metadata.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "content": {
    "description": "New HTML or Markdown content",
    "type": "string"
   },
   "format": {
    "description": "Content format. Accepts (case-insensitive on html/markdown): 'html', 'markdown'/'md', or a code language \u2014 yaml, json, xml, csv, tsv, log, toml, ini, sql, sh, bat, env, javascript, typescript, tsx, jsx, css, properties, docker, txt. Omit to preserve the stored format.",
    "type": "string"
   },
   "id": {
    "description": "Page ID",
    "type": "string"
   },
   "namespace": {
    "description": "Page namespace",
    "type": "string"
   },
   "ownerToken": {
    "description": "Owner token received when the page was created",
    "type": "string"
   }
  },
  "required": [
   "namespace",
   "id",
   "content",
   "ownerToken"
  ],
  "type": "object"
 },
 "name": "update_html"
}
