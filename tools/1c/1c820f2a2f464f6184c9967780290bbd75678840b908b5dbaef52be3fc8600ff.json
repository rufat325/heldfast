{
 "description": "[GA4 WRITE] Create a data stream (web / Android / iOS). Requires GA4_MCP_ENABLE_WRITES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "body": {
    "additionalProperties": {},
    "description": "Raw request-body object (JSON) merged OVER the typed fields \u2014 use for nested/advanced fields not exposed as flat args.",
    "type": "object"
   },
   "bundleId": {
    "description": "For IOS streams: the app bundle id.",
    "type": "string"
   },
   "confirm": {
    "description": "Must be true to apply the change.",
    "type": "boolean"
   },
   "defaultUri": {
    "description": "For WEB streams: the site URL (sets webStreamData.defaultUri).",
    "type": "string"
   },
   "displayName": {
    "minLength": 1,
    "type": "string"
   },
   "packageName": {
    "description": "For ANDROID streams: the app package name.",
    "type": "string"
   },
   "property": {
    "description": "GA4 property ID, e.g. \"123456789\" or \"properties/123456789\".",
    "minLength": 1,
    "type": "string"
   },
   "type": {
    "description": "Stream type.",
    "enum": [
     "WEB_DATA_STREAM",
     "ANDROID_APP_DATA_STREAM",
     "IOS_APP_DATA_STREAM"
    ],
    "type": "string"
   }
  },
  "required": [
   "property",
   "type",
   "displayName",
   "confirm"
  ],
  "type": "object"
 },
 "name": "ga4_create_data_stream"
}
