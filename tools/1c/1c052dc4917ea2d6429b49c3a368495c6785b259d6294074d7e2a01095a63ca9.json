{
 "description": "Propose an agent-cache per-tool TTL change for review. Creates a pending proposal that requires human approval. Reasoning must be at least 20 characters.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "cache_name": {
    "description": "Name of the agent cache (e.g. 'betterdb_agentcache_prod')",
    "minLength": 1,
    "type": "string"
   },
   "instanceId": {
    "description": "Connection ID; defaults to the active instance",
    "pattern": "^[a-zA-Z0-9_-]+$",
    "type": "string"
   },
   "new_ttl_seconds": {
    "description": "Proposed TTL in seconds (10\u201386400)",
    "maximum": 86400,
    "minimum": 10,
    "type": "integer"
   },
   "reasoning": {
    "description": "Why the change is being proposed (\u226520 chars)",
    "minLength": 20,
    "type": "string"
   },
   "tool_name": {
    "description": "Tool whose TTL is being changed",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "cache_name",
   "tool_name",
   "new_ttl_seconds",
   "reasoning"
  ],
  "type": "object"
 },
 "name": "cache_propose_tool_ttl_adjust"
}
