{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Read a saved AlphaCouncil Agent run. Terminal text is the persisted user handoff. detail=compact (default) returns bounded polling state and artifact paths; detail=full returns the legacy complete evidence/report payload.",
 "inputSchema": {
  "properties": {
   "detail": {
    "default": "compact",
    "description": "compact returns status, a bounded decision, report quality, artifact paths, an event summary and user_response only. full preserves the legacy evidence/events/report bodies.",
    "enum": [
     "compact",
     "full"
    ],
    "type": "string"
   },
   "run_id": {
    "type": "string"
   }
  },
  "required": [
   "run_id"
  ],
  "type": "object"
 },
 "name": "read_run",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 },
 "title": "Check a Research Run"
}
