{
 "annotations": {
  "readOnlyHint": true,
  "title": "Read Slack thread"
 },
 "description": "The saved messages of one Slack thread, oldest first: the parent and every reply the brain holds. Only threads from channels the user switched on are stored; an empty result means the thread was not captured, and the fix is enabling that channel on the Slack integrations page.",
 "inputSchema": {
  "properties": {
   "channel_id": {
    "description": "The channel id (C\u2026/D\u2026).",
    "type": "string"
   },
   "thread_ts": {
    "description": "The thread's parent ts (from a search result).",
    "type": "string"
   }
  },
  "required": [
   "channel_id",
   "thread_ts"
  ],
  "type": "object"
 },
 "name": "get_slack_thread",
 "title": "Read Slack thread"
}
