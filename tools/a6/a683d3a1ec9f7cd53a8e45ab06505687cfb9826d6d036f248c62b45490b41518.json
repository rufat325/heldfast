{
 "description": "[GA4 WRITE] Create a new GA4 property under an account. Requires GA4_MCP_ENABLE_WRITES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "description": "Parent account ID (\"123456\" or \"accounts/123456\").",
    "minLength": 1,
    "type": "string"
   },
   "confirm": {
    "type": "boolean"
   },
   "currencyCode": {
    "description": "ISO 4217 currency, e.g. \"USD\".",
    "minLength": 1,
    "type": "string"
   },
   "displayName": {
    "minLength": 1,
    "type": "string"
   },
   "industryCategory": {
    "description": "e.g. \"TECHNOLOGY\", \"RETAIL\".",
    "type": "string"
   },
   "timeZone": {
    "description": "IANA time zone, e.g. \"America/New_York\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "displayName",
   "timeZone",
   "currencyCode",
   "confirm"
  ],
  "type": "object"
 },
 "name": "ga4_create_property"
}
