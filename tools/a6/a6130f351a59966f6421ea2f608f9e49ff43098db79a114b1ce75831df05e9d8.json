{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Add a note to a specific invoice in Xero. Notes appear in the entity's history/audit trail. Notes are immutable once created.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "invoiceID": {
    "description": "The Xero invoice ID.",
    "type": "string"
   },
   "note": {
    "description": "The note text to add. This is immutable and cannot be edited or deleted after creation.",
    "type": "string"
   }
  },
  "required": [
   "invoiceID",
   "note"
  ],
  "type": "object"
 },
 "name": "add-invoice-note"
}
