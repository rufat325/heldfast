{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Current Amazon deals \u2014 recent price drops across the marketplace, optionally filtered to a minimum % off. Example: keepa_deals({ domain: 1, min_percent_off: 20, _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-keepa-api-key"
   },
   {
    "_apiKey": "your-keepa-api-key",
    "domain": 1,
    "min_percent_off": 30,
    "page": 0
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Keepa API key",
    "type": "string"
   },
   "domain": {
    "description": "Keepa marketplace id (default 1=US). See keepa_product for the full list.",
    "type": "integer"
   },
   "min_percent_off": {
    "description": "Minimum price-drop percentage to include (default 20).",
    "type": "integer"
   },
   "page": {
    "description": "Result page (default 0; each page ~150 deals).",
    "type": "integer"
   },
   "selection": {
    "description": "Advanced: a full Keepa deals \"selection\" object to send verbatim (overrides the simple filters). See keepa.com API docs for its schema.",
    "type": "object"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "keepa_deals",
 "title": "Keepa Deals",
 "version": "v1.0dc22061"
}
