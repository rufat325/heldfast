{
 "description": "Lint structured tool descriptions for bounded purpose, inputs, outputs, side effects, failure modes, cost, and idempotency. Costs $0.05 USDC on Base mainnet.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "tools": {
    "items": {
     "additionalProperties": true,
     "required": [
      "name",
      "description"
     ],
     "type": "object"
    },
    "maxItems": 1000,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "tools"
  ],
  "type": "object"
 },
 "name": "justhandled_tool_description_contract_linter"
}
