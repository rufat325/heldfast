{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Create a Turnstile widget (CAPTCHA alternative) for given domains. Requires confirm:true (human-approval gate); without it returns a preview only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "account_id": {
    "description": "Cloudflare account ID. Falls back to CLOUDFLARE_ACCOUNT_ID env var when omitted.",
    "type": "string"
   },
   "confirm": {
    "description": "Human-approval gate: must be true to actually perform this mutating operation. Omit or set false to get a non-executing preview of what would happen (with secrets redacted). A human should approve before this is set to true.",
    "type": "boolean"
   },
   "domains": {
    "description": "Allowed domains for the widget, e.g. ['example.com'].",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "mode": {
    "description": "Widget mode. Default 'managed'.",
    "enum": [
     "managed",
     "non-interactive",
     "invisible"
    ],
    "type": "string"
   },
   "name": {
    "description": "Human-readable widget name.",
    "type": "string"
   }
  },
  "required": [
   "name",
   "domains"
  ],
  "type": "object"
 },
 "name": "create_turnstile_widget"
}
