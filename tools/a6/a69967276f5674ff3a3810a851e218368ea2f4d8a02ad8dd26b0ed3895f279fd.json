{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Add a target (server, label selector, or IP) to a load balancer.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "id": {
    "description": "Resource ID",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "ip": {
    "description": "IP target",
    "properties": {
     "ip": {
      "description": "IP address",
      "type": "string"
     }
    },
    "required": [
     "ip"
    ],
    "type": "object"
   },
   "label_selector": {
    "description": "Label selector target",
    "properties": {
     "selector": {
      "description": "Label selector",
      "type": "string"
     }
    },
    "required": [
     "selector"
    ],
    "type": "object"
   },
   "server": {
    "description": "Server target",
    "properties": {
     "id": {
      "description": "Server ID",
      "maximum": 9007199254740991,
      "minimum": -9007199254740991,
      "type": "integer"
     }
    },
    "required": [
     "id"
    ],
    "type": "object"
   },
   "type": {
    "description": "Target type",
    "enum": [
     "server",
     "label_selector",
     "ip"
    ],
    "type": "string"
   },
   "use_private_ip": {
    "description": "Use private IP for the target",
    "type": "boolean"
   }
  },
  "required": [
   "id",
   "type"
  ],
  "type": "object"
 },
 "name": "hetzner_add_lb_target",
 "title": "Add Load Balancer Target"
}
