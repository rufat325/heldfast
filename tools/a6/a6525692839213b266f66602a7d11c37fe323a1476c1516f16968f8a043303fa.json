{
 "description": "Owner-test-only paid search candidate. No payment, settlement, or provider execution is enabled in this no-value build.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "request": {
    "additionalProperties": false,
    "properties": {
     "idempotency_key": {
      "pattern": "^[A-Za-z0-9._:-]{1,128}$",
      "type": "string"
     },
     "page": {
      "type": "integer"
     },
     "page_size": {
      "maximum": 10,
      "minimum": 1,
      "type": "integer"
     },
     "q": {
      "maxLength": 200,
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "q"
    ],
    "type": "object"
   }
  },
  "required": [
   "request"
  ],
  "type": "object"
 },
 "name": "invoke_product_keyword_search_v0"
}
