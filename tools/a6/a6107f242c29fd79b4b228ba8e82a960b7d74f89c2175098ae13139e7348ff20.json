{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the variable dimensions and available filter values for any Swiss Federal Statistical Office (FSO/BFS) STAT-TAB dataset. Provide a database_id from catalog.list. Returns the table title, dimension codes, and all available value codes with German labels \u2014 needed to construct filters for table.query. Example: database_id \"px-x-0304010000_201\" returns year, region, industry, professional status, gender, and percentile dimensions for monthly gross wages. Data: Swiss OGD, no auth, commercial use permitted.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "database_id": {
    "description": "BFS database identifier (e.g. \"px-x-0304010000_201\" for monthly wages, \"px-x-0103010000_123\" for 2023 population by canton). Obtain from catalog.list.",
    "type": "string"
   }
  },
  "required": [
   "database_id"
  ],
  "type": "object"
 },
 "name": "swissfso.table.metadata",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Swiss FSO Table Dimensions"
}
