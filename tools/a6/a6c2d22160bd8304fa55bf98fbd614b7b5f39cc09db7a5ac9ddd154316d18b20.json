{
 "description": "Execute an authorized payment. Requires a valid authorization token.",
 "inputSchema": {
  "properties": {
   "authorizationId": {
    "description": "Authorization ID from authorize_payment",
    "type": "string"
   },
   "paymentMethodId": {
    "description": "Specific payment method ID to use",
    "type": "string"
   }
  },
  "required": [
   "authorizationId"
  ],
  "type": "object"
 },
 "name": "execute_payment"
}
