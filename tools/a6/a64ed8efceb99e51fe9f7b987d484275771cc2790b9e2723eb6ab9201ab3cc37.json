{
 "description": "Run a language-game context and return its state plus the cell's own self-evaluation of the rendered frame - integer score 0-1000 over occupancy, spread, contrast and edge quality, with the C4 span correction applied. Stateless: (kind, seed, t, n, at_seq) fully determine the result.",
 "inputSchema": {
  "properties": {
   "at_seq": {
    "description": "decimal string, pinned corpus epoch",
    "type": "string"
   },
   "h": {
    "description": "decimal string",
    "type": "string"
   },
   "kind": {
    "enum": [
     "atc",
     "proteins",
     "materials"
    ],
    "type": "string"
   },
   "max_iter": {
    "description": "decimal string, iteration cap, default 10",
    "type": "string"
   },
   "n": {
    "description": "decimal string, population",
    "type": "string"
   },
   "seed": {
    "description": "decimal string; JSON numbers still admitted",
    "type": "string"
   },
   "t": {
    "description": "decimal string, step index",
    "type": "string"
   },
   "target": {
    "description": "decimal string, refine until score >= this (0-1000, default 950)",
    "type": "string"
   },
   "w": {
    "description": "decimal string",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "game_frame_meta"
}
