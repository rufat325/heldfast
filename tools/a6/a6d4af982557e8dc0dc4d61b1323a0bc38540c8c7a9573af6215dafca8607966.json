{
 "_meta": {
  "skill": "ordinals-marketplace"
 },
 "description": "Buy a listed inscription from Magic Eden.\n\nRequests a buyer PSBT from the Magic Eden API, funded by the active wallet.\nReturns a PSBT that combines the seller's listing inputs with the buyer's payment\ninputs. The buyer signs the PSBT then broadcasts it to complete the purchase.\n\nRequires an unlocked wallet with Bitcoin keys and sufficient BTC balance.\n\nSteps:\n1. Call ordinals_get_listings to find an inscription and its price\n2. Call ordinals_buy with the inscriptionId and desired buyer address\n3. Sign the returned PSBT using psbt_sign\n4. Broadcast using psbt_broadcast\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "buyerAddress": {
    "description": "BTC address to receive the inscription (defaults to wallet's Taproot address)",
    "type": "string"
   },
   "buyerPaymentAddress": {
    "description": "BTC address to fund the purchase (defaults to wallet's SegWit address)",
    "type": "string"
   },
   "feeRate": {
    "description": "Fee rate in sat/vB (optional, uses network default if omitted)",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "inscriptionId": {
    "description": "Inscription ID to purchase, e.g. abc123...i0",
    "type": "string"
   }
  },
  "required": [
   "inscriptionId"
  ],
  "type": "object"
 },
 "name": "ordinals_buy"
}
