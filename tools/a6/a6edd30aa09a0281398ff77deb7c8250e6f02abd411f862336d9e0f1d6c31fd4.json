{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Diff a build against a reference and return concrete, ranked fix observations (size, color, shadow, spacing) with values measured from the reference. Selectors are only emitted when they resolve from real DOM; for screenshot-only input the fixes are region-anchored observations with capped confidence rather than guessed selectors. Inputs are image file paths.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "disable_ocr": {
    "description": "Skip OCR text extraction. OCR runs by default and roughly doubles latency; disable it when you don't need text content or typography (note: extract then returns empty fontSizes).",
    "type": "boolean"
   },
   "framework": {
    "description": "Output format: react or vanilla (default react).",
    "enum": [
     "react",
     "vanilla"
    ],
    "type": "string"
   },
   "implementation_path": {
    "description": "Path (absolute, or relative to the server's launch directory) to the current build screenshot.",
    "type": "string"
   },
   "reference_path": {
    "description": "Path (absolute, or relative to the server's launch directory) to the reference (design) screenshot.",
    "type": "string"
   },
   "region": {
    "description": "Limit fixes to a named semantic anchor from the reference.",
    "type": "string"
   },
   "styling": {
    "description": "Styling output: tailwind or css (default tailwind).",
    "enum": [
     "tailwind",
     "css"
    ],
    "type": "string"
   },
   "top": {
    "description": "Maximum number of fixes to return (default 20).",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   }
  },
  "required": [
   "reference_path",
   "implementation_path"
  ],
  "type": "object"
 },
 "name": "suggest_fixes",
 "title": "Get ranked fix suggestions from a diff"
}
