{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Enable or disable delete protection on a load balancer to guard against accidental destruction.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "delete": {
    "description": "If true, prevents the load balancer from being deleted",
    "type": "boolean"
   },
   "id": {
    "description": "Load Balancer ID",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "hetzner_change_load_balancer_protection",
 "title": "Change Load Balancer Protection"
}
