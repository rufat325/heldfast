{
 "description": "Lists the objects of the object layers with id, name, type, shape, position, size, rotation, gid and properties. Optionally filtered by layer, id, name or type.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "id": {
    "description": "Only the object with this id.",
    "maximum": 9007199254740991,
    "minimum": -9007199254740991,
    "type": "integer"
   },
   "layer": {
    "description": "Name of the layer; inside groups also 'group/layer'. Alternatively use layer_id.",
    "type": "string"
   },
   "layer_id": {
    "description": "Id of the layer. Unique, unlike the name. Alternatively use layer.",
    "maximum": 9007199254740991,
    "minimum": -9007199254740991,
    "type": "integer"
   },
   "limit": {
    "description": "Upper limit of objects shown. Default 200.",
    "maximum": 9007199254740991,
    "minimum": -9007199254740991,
    "type": "integer"
   },
   "name": {
    "description": "Only objects with this name.",
    "type": "string"
   },
   "path": {
    "description": "Path to ONE Tiled map file (.tmx or .tmj) \u2014 no directory, no wildcards. Please pass it absolute: a relative path is resolved against the working directory of the server process, and that is chosen by the MCP host, not by the project.",
    "minLength": 1,
    "type": "string"
   },
   "type": {
    "description": "Only objects with this class (attribute type).",
    "type": "string"
   }
  },
  "required": [
   "path"
  ],
  "type": "object"
 },
 "name": "objects_list",
 "title": "List objects"
}
