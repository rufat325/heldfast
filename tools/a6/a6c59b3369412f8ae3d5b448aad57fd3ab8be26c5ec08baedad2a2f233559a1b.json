{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "streamingHint": true,
  "title": "Grok Code Generation"
 },
 "description": "Code analysis. Put the CODE in the 'code' parameter, NOT in 'task'.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "code": {
    "description": "The actual source code to analyze (REQUIRED - put your code here)",
    "type": "string"
   },
   "files": {
    "description": "File paths to read as code context. Supports line ranges: 'src/foo.ts:100-200'. Model sees ACTUAL CODE.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "language": {
    "description": "Programming language (e.g., 'typescript', 'python')",
    "type": "string"
   },
   "requirements": {
    "description": "Specific requirements or focus areas",
    "type": "string"
   },
   "task": {
    "description": "Code task (e.g., analyze, optimize, debug, review, refactor)",
    "type": "string"
   }
  },
  "required": [
   "task",
   "code"
  ],
  "type": "object"
 },
 "name": "grok_code"
}
