{
 "description": "Observability for the pools: totals, how many are available vs leased, and per-account state (free / leased / expired). Credential values are NEVER returned.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "pool": {
    "description": "Limit to one pool. Omit for all pools.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "pool_status"
}
