{
 "annotations": {
  "cost": {
   "all": 3
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search FDA medical-device recall records by firm, product, product code, K number, status, or date. A recall record describes a correction/removal action and does not by itself establish patient harm.",
 "inputSchema": {
  "examples": [
   {
    "limit": 10,
    "query": "insulin pump"
   },
   {
    "from_date": "2026-07-12",
    "limit": 20
   },
   {
    "firm": "Medtronic",
    "from_date": "2025-01-01",
    "limit": 10
   }
  ],
  "properties": {
   "firm": {
    "description": "Recalling firm substring.",
    "type": "string"
   },
   "from_date": {
    "description": "Earliest posted date, YYYY-MM-DD.",
    "type": "string"
   },
   "k_number": {
    "description": "Exact related K number.",
    "type": "string"
   },
   "limit": {
    "description": "Results (1-100, default 20).",
    "type": "number"
   },
   "product_code": {
    "description": "Exact FDA product code.",
    "type": "string"
   },
   "query": {
    "description": "Product or reason text.",
    "type": "string"
   },
   "status": {
    "description": "Recall status, e.g. Ongoing or Terminated.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "fda_device_recalls",
 "outputSchema": {
  "properties": {
   "recalls": {
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "returned": {
    "type": "number"
   },
   "source": {
    "type": "string"
   },
   "total": {
    "type": "number"
   }
  },
  "required": [
   "total",
   "returned",
   "recalls",
   "source"
  ],
  "type": "object"
 },
 "title": "Fda Device Recalls",
 "version": "v1.0d61882d"
}
