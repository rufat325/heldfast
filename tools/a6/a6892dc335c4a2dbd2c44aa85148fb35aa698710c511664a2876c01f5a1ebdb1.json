{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search NASA Exoplanet Archive for confirmed exoplanets by name, host star, discovery method, year range, radius, or mass. Returns orbital period, radius, mass, equilibrium temperature, discovery year and facility, and system distance. 6,298+ confirmed planets from Transit, Radial Velocity, Imaging, and Microlensing surveys (NASA Exoplanet Archive).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "disc_year_max": {
    "description": "Maximum discovery year (e.g. 2020 to filter planets found before 2021)",
    "maximum": 2030,
    "minimum": 1988,
    "type": "integer"
   },
   "disc_year_min": {
    "description": "Minimum discovery year (e.g. 2010 to filter planets found in 2010 or later)",
    "maximum": 2030,
    "minimum": 1988,
    "type": "integer"
   },
   "discovery_method": {
    "description": "Discovery method filter (e.g. \"Transit\", \"Radial Velocity\", \"Imaging\")",
    "enum": [
     "Transit",
     "Radial Velocity",
     "Microlensing",
     "Imaging",
     "Astrometry",
     "Transit Timing Variations",
     "Orbital Brightness Modulation",
     "Pulsar Timing",
     "Pulsation Timing Variations",
     "Disk Kinematics",
     "Eclipse Timing Variations"
    ],
    "type": "string"
   },
   "host_name": {
    "description": "Host star name or partial name (e.g. \"51 Peg\", \"Kepler-442\")",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of results to return (default 20, max 100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "mass_max": {
    "description": "Maximum planet mass in Earth masses (e.g. 318 \u2248 1 Jupiter mass)",
    "minimum": 0,
    "type": "number"
   },
   "mass_min": {
    "description": "Minimum planet mass in Earth masses (e.g. 0.1 for sub-Earths)",
    "minimum": 0,
    "type": "number"
   },
   "planet_name": {
    "description": "Planet name or partial name to search (e.g. \"Kepler-22 b\", \"TOI-700\")",
    "type": "string"
   },
   "radius_max": {
    "description": "Maximum planet radius in Earth radii (e.g. 2.0 for super-Earths; 11 \u2248 Jupiter)",
    "minimum": 0,
    "type": "number"
   },
   "radius_min": {
    "description": "Minimum planet radius in Earth radii (e.g. 0.5 for sub-Earths and above)",
    "minimum": 0,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "nasaexoplanet.planets.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Search Exoplanets"
}
