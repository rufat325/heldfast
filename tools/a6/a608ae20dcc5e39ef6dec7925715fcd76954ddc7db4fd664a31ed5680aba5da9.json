{
 "description": "Install an APK on a device/emulator (adb install -r).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "apkPath": {
    "description": "Path to the APK (from build_app / get_apk_path)",
    "type": "string"
   },
   "serial": {
    "description": "Device serial (defaults to session or the only device)",
    "type": "string"
   }
  },
  "required": [
   "apkPath"
  ],
  "type": "object"
 },
 "name": "install_app",
 "title": "Install app"
}
