{
 "description": "Retrieve an invoice by code. Returns recipient details, status (DRAFT, OPEN, VIEWED, PAID, VOID), and the linked charge once payment begins.",
 "inputSchema": {
  "properties": {
   "code": {
    "description": "Invoice short code",
    "type": "string"
   }
  },
  "required": [
   "code"
  ],
  "type": "object"
 },
 "name": "retrieve_invoice"
}
