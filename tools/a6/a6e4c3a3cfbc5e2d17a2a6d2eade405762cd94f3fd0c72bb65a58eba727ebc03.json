{
 "description": "Schedule an activity on a record. activity_type: name of the activity type (e.g. \"To-Do\", \"Email\", \"Phone Call\"). deadline: ISO date string YYYY-MM-DD. summary: short title. note: longer description (optional). assigned_user_id: who to assign (default: current user). Returns {activity_id, activity_type, deadline} or {error}.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "activity_type": {
    "type": "string"
   },
   "assigned_user_id": {
    "type": "integer"
   },
   "deadline": {
    "type": "string"
   },
   "model": {
    "type": "string"
   },
   "note": {
    "type": "string"
   },
   "record_id": {
    "type": "integer"
   },
   "summary": {
    "type": "string"
   }
  },
  "required": [
   "model",
   "record_id",
   "activity_type",
   "deadline",
   "summary"
  ],
  "type": "object"
 },
 "name": "schedule_activity"
}
