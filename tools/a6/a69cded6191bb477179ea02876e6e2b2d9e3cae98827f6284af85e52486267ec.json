{
 "description": "Sapphire verifies a server now, outside the free verify's ration, and signs the answer: per-tool verdicts with prices, card instruction signals, schema drift, a receipt. Charged per call.",
 "inputSchema": {
  "properties": {
   "caller": {
    "type": "string"
   },
   "idempotencyKey": {
    "description": "your key for this request; send it again on a retry and the charge is made once",
    "type": "string"
   },
   "url": {
    "type": "string"
   }
  },
  "required": [
   "caller",
   "url"
  ],
  "type": "object"
 },
 "name": "sapphire_verdict"
}
