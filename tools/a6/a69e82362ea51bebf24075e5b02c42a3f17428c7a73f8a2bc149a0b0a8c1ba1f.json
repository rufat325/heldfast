{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Backlink profile summary for `<domain>` \u2014 total backlinks, referring domains, domain rank, dofollow/nofollow split, and spam score, via DataForSEO. The \"how strong is this domain's link profile\" overview. Example: seo_backlinks_summary({ target: \"nike.com\", _apiKey: \"your-base64-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-seo-backlinks-api-key",
    "target": "nike.com"
   },
   {
    "_apiKey": "your-seo-backlinks-api-key",
    "backlinks_status_type": "live",
    "include_subdomains": true,
    "target": "example.com"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "DataForSEO API key = base64(\"login:password\")",
    "type": "string"
   },
   "backlinks_status_type": {
    "description": "\"live\" (default), \"all\", or \"lost\"",
    "type": "string"
   },
   "include_subdomains": {
    "description": "Include subdomains (default true)",
    "type": "boolean"
   },
   "target": {
    "description": "Domain, subdomain, or URL to analyze (no protocol for a domain), e.g. \"nike.com\"",
    "type": "string"
   }
  },
  "required": [
   "target",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "seo_backlinks_summary",
 "title": "Seo Backlinks Summary",
 "version": "v1.f38b0702"
}
