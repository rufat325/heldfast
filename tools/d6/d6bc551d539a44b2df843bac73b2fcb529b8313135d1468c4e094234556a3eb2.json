{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Delete a single key from memory. Idempotent: returns existed=false if the key was not present. Requires explicit_user_intent: true.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "explicit_user_intent": {
    "const": true,
    "description": "Must be true. Set ONLY when the current user message explicitly asks the agent to modify memory (set/forget/export). Do not infer intent.",
    "type": "boolean"
   },
   "key": {
    "description": "Stable identifier for the memory entry. Free-form text. Treat as case-sensitive.",
    "maxLength": 512,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "key",
   "explicit_user_intent"
  ],
  "type": "object"
 },
 "name": "memory_forget"
}
