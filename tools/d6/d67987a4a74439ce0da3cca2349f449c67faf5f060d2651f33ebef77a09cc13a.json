{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Generate copy-pasteable DNS record snippets that fix one specific issue (e.g. `spf_missing` \u2192 suggested SPF record). Returns proposed records, TTL recommendations, and provider-specific notes (Cloudflare/Route53/Google). Read-only POST to /ai/fix \u2014 the API only suggests; it never modifies the user's zone. Provide `domain` and `issue` (enum); pass `context` from prior scan output for tailored output. Use after explain_issue or scan_domain identifies a problem; use lookup_dns afterwards to verify the user has applied the suggested record.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "context": {
    "additionalProperties": {},
    "description": "Optional issue context from scan output",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "domain": {
    "description": "Domain name only, e.g. example.com (no URL, path, or port)",
    "maxLength": 253,
    "type": "string"
   },
   "issue": {
    "enum": [
     "no_a_record",
     "no_mx_record",
     "mx_ptr_missing",
     "mx_fcrdns_missing",
     "single_ns",
     "dnssec_invalid",
     "nsec3_not_compliant",
     "rrsig_expiring",
     "ds_digest_weak",
     "dnskey_algo_weak",
     "rrsig_ttl_unsafe",
     "chain_incomplete",
     "no_ipv6",
     "no_ipv6_mail",
     "no_dnssec",
     "no_spf",
     "no_dmarc",
     "weak_dmarc",
     "no_dkim",
     "excessive_verification_records",
     "no_http3",
     "spf_missing",
     "spf_too_many_lookups",
     "spf_softfail",
     "dkim_missing",
     "dmarc_missing",
     "dmarc_none",
     "dmarc_quarantine",
     "no_caa",
     "dane_missing"
    ],
    "type": "string"
   }
  },
  "required": [
   "domain",
   "issue"
  ],
  "type": "object"
 },
 "name": "generate_dns_fix"
}
