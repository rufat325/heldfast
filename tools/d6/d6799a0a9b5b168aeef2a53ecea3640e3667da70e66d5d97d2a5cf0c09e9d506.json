{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the official Norwegian place name register (Kartverket Stedsnavn) by text, optionally filtered by county or municipality. Returns each match's place id (stedsnummer), type (city, lake, mountain, street, etc.), status, language, county, municipality, and coordinates. Call norway-kartverket-stedsnavn.get_place for full detail on a specific result. Data: Kartverket (Norwegian Mapping Authority), NLOD 2.0 open licence, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "county": {
    "description": "Filter results to a Norwegian county name (fylkesnavn), e.g. \"Viken\"",
    "type": "string"
   },
   "fuzzy": {
    "description": "Allow fuzzy/approximate text matching instead of exact substring search",
    "type": "boolean"
   },
   "limit": {
    "description": "Maximum results to return (default 10, max 30)",
    "maximum": 30,
    "minimum": 1,
    "type": "integer"
   },
   "municipality": {
    "description": "Filter results to a Norwegian municipality name (kommunenavn), e.g. \"Bergen\"",
    "type": "string"
   },
   "query": {
    "description": "Place name text to search for, e.g. \"Oslo\" or \"Bergen\"",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "norway-kartverket-stedsnavn.places.search_names",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Norwegian Place Name Search"
}
