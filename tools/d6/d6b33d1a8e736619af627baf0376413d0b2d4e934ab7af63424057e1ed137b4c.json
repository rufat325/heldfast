{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": false
 },
 "description": "Permanently delete a webhook endpoint. Future events will no longer be delivered to this URL. Any in-flight deliveries may still complete. This is irreversible. Use social_api_list_webhooks to find the webhook_id.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "webhook_id": {
    "description": "Webhook endpoint ID to delete",
    "type": "string"
   }
  },
  "required": [
   "webhook_id"
  ],
  "type": "object"
 },
 "name": "social_api_delete_webhook",
 "title": "Delete Webhook"
}
