{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Build Invoke SC Transaction"
 },
 "description": "Build an unsigned smart contract invocation transaction on the Klever blockchain. Calls a state-changing endpoint on a deployed contract. Returns the unsigned transaction for client-side signing. For read-only calls, use query_sc instead.",
 "inputSchema": {
  "properties": {
   "args": {
    "description": "Optional base64-encoded arguments.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "callValue": {
    "additionalProperties": {
     "type": "integer"
    },
    "description": "Optional token amounts to send with the call, as a map of token ID to amount (e.g. {\"KLV\": 1000000}). Required for payable endpoints.",
    "type": "object"
   },
   "funcName": {
    "description": "Endpoint function name to invoke.",
    "type": "string"
   },
   "network": {
    "description": "Network to use. Options: \"mainnet\", \"testnet\", \"devnet\", \"local\". Defaults to server default (mainnet).",
    "enum": [
     "mainnet",
     "testnet",
     "devnet",
     "local"
    ],
    "type": "string"
   },
   "scAddress": {
    "description": "Smart contract address (klv1... bech32 format).",
    "type": "string"
   },
   "sender": {
    "description": "Caller address (klv1... bech32 format).",
    "type": "string"
   }
  },
  "required": [
   "sender",
   "scAddress",
   "funcName"
  ],
  "type": "object"
 },
 "name": "invoke_sc"
}
