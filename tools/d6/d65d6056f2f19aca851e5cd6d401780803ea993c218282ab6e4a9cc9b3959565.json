{
 "description": "Update an existing Code Engine secret in-place (PATCH). Use ce_renew_tls_secret_from_pem for TLS cert renewal. For generic secrets supply a data object with the new key/value pairs.",
 "inputSchema": {
  "properties": {
   "data": {
    "description": "New key/value data to replace the secret contents",
    "type": "object"
   },
   "format": {
    "description": "Secret format if changing: generic, registry, tls, ssh_auth, basic_auth",
    "type": "string"
   },
   "project_id": {
    "type": "string"
   },
   "secret_name": {
    "description": "Name of the secret to update",
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "secret_name",
   "data"
  ],
  "type": "object"
 },
 "name": "ce_update_secret"
}
