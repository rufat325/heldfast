{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "CREATE a Robinhood Chain price alert (POST \u2014 this writes and consumes quota). Fires when the token's market cap falls drop_pct below the baseline captured AT CREATION TIME, and optionally again when it recovers recovery_pct off the dip low. TIMING: evaluation is event-driven off the chain trade feed, with price-table polls and a trade-tape replay as safety nets. Latency is a few seconds (the chain trade flush is about 2 s), so do NOT assume parity with Solana's sub-second alerts. The response's evaluation block says mode 'event_driven'. The token must already be tracked with a market cap on RHC or the call returns 400. Returns webhook_secret EXACTLY ONCE when delivery_mode includes 'webhook'. Active-alert quota is PER CHAIN (PRO 5, ULTRA 25, BUSINESS 125); exceeding it returns 409. Tier: PRO+.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "delivery_mode": {
    "description": "Where events are delivered (default 'webhook')",
    "enum": [
     "webhook",
     "websocket",
     "both"
    ],
    "type": "string"
   },
   "drop_pct": {
    "description": "Percent drop from the creation-time baseline MC that fires the dip (0.01-99.99)",
    "maximum": 99.99,
    "minimum": 0.01,
    "type": "number"
   },
   "name": {
    "description": "Optional human label (max 64 chars)",
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "recovery_pct": {
    "description": "Percent rebound off the dip low that fires a recovery event (0.01-1000). Omit for dip-only",
    "maximum": 1000,
    "minimum": 0.01,
    "type": "number"
   },
   "token_address": {
    "description": "RHC token address (0x, 40 hex). Must be a token we already price",
    "type": "string"
   },
   "webhook_url": {
    "description": "HTTPS URL \u2014 REQUIRED unless delivery_mode is 'websocket'",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "token_address",
   "drop_pct"
  ],
  "type": "object"
 },
 "name": "rhc_price_alerts_create"
}
