{
 "description": "List matter sub-statuses. Each belongs to a top-level status: pnc (potential new client), hired, or lost. Use the IDs with create-matter / update-matter.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "fetch_all": {
    "description": "Follow pagination and return every record (capped at 1,000). The response says complete=false if truncated.",
    "type": "boolean"
   },
   "fields": {
    "description": "Comma-separated attributes to return, or 'all' for everything. Add 'custom_fields' to include custom field values.",
    "type": "string"
   },
   "filter_by": {
    "description": "Attribute to filter on. IMPORTANT: the Lawmatics API supports only ONE filter per request. Association filters use the _id suffix, e.g. practice_area_id, stage_id, source_id, matter_id, contact_id.",
    "type": "string"
   },
   "filter_on": {
    "description": "Value to filter for. Required with filter_by unless filter_with is null/not_null.",
    "type": [
     "string",
     "number"
    ]
   },
   "filter_with": {
    "description": "Filter operator: =, !=, <, <=, >, >=, like, ilike (default: ilike for text \u2014 add % wildcards for fuzzy match, e.g. %smith%), or presence checks: null, not_null. Currency values are in cents.",
    "type": "string"
   },
   "page": {
    "description": "Page number (Lawmatics returns a fixed 25 records per page).",
    "minimum": 1,
    "type": "integer"
   },
   "sort_by": {
    "description": "Attribute to sort by, e.g. 'created_at', 'updated_at', 'id'. Default: id, newest first.",
    "type": "string"
   },
   "sort_order": {
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list-sub-statuses"
}
