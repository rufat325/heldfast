{
 "description": "Change the acting user's own password.",
 "inputSchema": {
  "properties": {
   "newPassword": {
    "type": "string"
   },
   "oldPassword": {
    "type": "string"
   }
  },
  "required": [
   "oldPassword",
   "newPassword"
  ],
  "type": "object"
 },
 "name": "change_current_user_password"
}
