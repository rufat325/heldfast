{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Add human names to coord-only places/visits via Google nearby search + reverse geocode. Never overwrites existing names, coordinates, or user-entered fields. Idempotent; resumable via cursor under a per-account monthly Places quota. ($0.10; API key required)",
 "destructiveHint": false,
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "cursor": {
    "description": "Resume token from a previous next_cursor.",
    "type": "string"
   },
   "dry_run": {
    "type": "boolean"
   },
   "idempotency_key": {
    "description": "Alias for request_id.",
    "type": "string"
   },
   "limit": {
    "description": "Max targets this run (1\u2013100, default 25).",
    "type": "number"
   },
   "request_id": {
    "description": "Client idempotency key (retries return original result).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "backfill_place_names",
 "openWorldHint": false,
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "done": {
    "type": "boolean"
   },
   "message": {
    "type": [
     "string",
     "null"
    ]
   },
   "named": {
    "type": "number"
   },
   "next_cursor": {
    "type": [
     "string",
     "null"
    ]
   },
   "quota": {
    "additionalProperties": true,
    "description": "Per-account monthly Places usage.",
    "type": "object"
   },
   "rate_limited": {
    "type": "boolean"
   },
   "skipped_named": {
    "type": "number"
   },
   "skipped_no_match": {
    "type": "number"
   }
  },
  "type": "object"
 },
 "readOnlyHint": false,
 "securitySchemes": [
  {
   "scopes": [
    "context.write"
   ],
   "type": "oauth2"
  }
 ],
 "title": "Backfill Place Names"
}
