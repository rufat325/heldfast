{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Domain overview (Semrush type=domain_ranks): high-level SEO/PPC summary for a domain. Returns parsed rows with columns: Db (database), Dn (domain), Rk (Semrush rank), Or (organic keywords count), Ot (organic traffic), Oc (organic traffic cost), Ad (adwords/paid keywords count), At (paid traffic), Ac (paid traffic cost). Consumes your Semrush API units.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-semrush-api-key",
    "domain": "apple.com"
   },
   {
    "_apiKey": "your-semrush-api-key",
    "database": "us",
    "display_limit": 10,
    "domain": "nike.com"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Your own Semrush API key (BYO). Passed to Semrush as the `key` query param. Consumes your Semrush API units.",
    "type": "string"
   },
   "database": {
    "description": "2-letter Semrush regional database (e.g. us, uk, de, fr, es, ca, au). Default us.",
    "type": "string"
   },
   "display_limit": {
    "description": "Max rows to return.",
    "type": "number"
   },
   "domain": {
    "description": "Domain to analyze, e.g. \"apple.com\" (no scheme).",
    "type": "string"
   }
  },
  "required": [
   "_apiKey",
   "domain"
  ],
  "type": "object"
 },
 "name": "domain_overview",
 "title": "Domain Overview",
 "version": "v1.9b154635"
}
