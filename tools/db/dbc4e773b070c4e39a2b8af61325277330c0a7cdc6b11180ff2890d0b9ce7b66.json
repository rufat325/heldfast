{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Post Analytics Insights"
 },
 "description": "POST /analytics/insights",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "body": {
    "additionalProperties": true,
    "description": "Optional body override for endpoints with sparse parameter schemas.",
    "type": "object"
   },
   "brand_id": {
    "type": "string"
   },
   "idempotency_key": {
    "type": "string"
   },
   "platforms": {
    "description": "Platforms to analyze for insights.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "query": {
    "additionalProperties": true,
    "description": "Optional query overrides for endpoints with sparse parameter schemas.",
    "type": "object"
   },
   "regenerate": {
    "description": "If true, force regeneration even if insights exist.",
    "type": "boolean"
   },
   "workspace_id": {
    "description": "Workspace to operate in. Do not ask the user for this UUID \u2014 call aidelly_list_workspaces and use the `id` of the matching workspace. Optional for read operations; required when creating content.",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "idempotency_key"
  ],
  "type": "object"
 },
 "name": "aidelly_post_analytics_insights"
}
