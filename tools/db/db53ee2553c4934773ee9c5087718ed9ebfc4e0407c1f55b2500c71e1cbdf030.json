{
 "description": "Convert text(s) into vector embeddings using NVIDIA NIM embedding models. Useful for semantic search, RAG, clustering, and similarity comparisons.",
 "inputSchema": {
  "properties": {
   "encoding_format": {
    "enum": [
     "float",
     "base64"
    ],
    "type": "string"
   },
   "input": {
    "description": "Text or list of texts to embed",
    "oneOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ]
   },
   "model": {
    "description": "Embedding model ID (e.g. nvidia/nv-embed-v1)",
    "type": "string"
   },
   "truncate": {
    "enum": [
     "NONE",
     "START",
     "END"
    ],
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "create_embeddings"
}
