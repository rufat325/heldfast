{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the dimensions and valid codes for an ILOSTAT dataflow (dataflow_id from ilostat.dataflows, e.g. \"DF_EMP_TEMP_SEX_AGE_NB\" for employment by sex and age) \u2014 reference area, frequency, measure, sex, and age dimensions each with their code list. Use this to interpret the dimension labels returned by ilostat.data, or to build a narrower key filter. Data: sdmx.ilo.org (ILOSTAT SDMX public REST API), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataflow_id": {
    "description": "ILOSTAT dataflow id, from ilostat.dataflows (e.g. \"DF_EMP_TEMP_SEX_AGE_NB\" for employment by sex and age). Returns the dimension list (e.g. REF_AREA, FREQ, MEASURE, SEX, AGE) with each dimension's valid codes \u2014 needed to interpret ilostat.data results and build a key filter.",
    "minLength": 1,
    "type": "string"
   },
   "version": {
    "description": "Dataflow version, from ilostat.dataflows (e.g. \"1.0\"). Omit to use the latest version.",
    "type": "string"
   }
  },
  "required": [
   "dataflow_id"
  ],
  "type": "object"
 },
 "name": "ilostat.reference.structure",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "ILOSTAT Dataflow Structure"
}
