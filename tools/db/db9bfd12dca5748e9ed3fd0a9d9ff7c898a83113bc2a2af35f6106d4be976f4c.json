{
 "description": "Write a typed entry to compose/docs/journal/ with auto-numbered global session and inserted index row. Idempotent on (date, slug) at storage level; optional caller idempotency_key for retry safety. Audit-log append is best-effort.",
 "inputSchema": {
  "properties": {
   "closing_line": {
    "description": "Optional final italicized one-liner.",
    "type": "string"
   },
   "date": {
    "description": "ISO date \"YYYY-MM-DD\".",
    "type": "string"
   },
   "feature_code": {
    "description": "Optional feature code stamped in entry frontmatter.",
    "type": "string"
   },
   "force": {
    "description": "If true and an entry with the same (date, slug) exists, overwrite in place.",
    "type": "boolean"
   },
   "idempotency_key": {
    "description": "Optional caller-supplied key. Same key replays return the cached result without re-mutating.",
    "type": "string"
   },
   "sections": {
    "additionalProperties": false,
    "properties": {
     "open_threads": {
      "type": "string"
     },
     "what_happened": {
      "type": "string"
     },
     "what_we_built": {
      "type": "string"
     },
     "what_we_learned": {
      "type": "string"
     }
    },
    "required": [
     "what_happened",
     "what_we_built",
     "what_we_learned",
     "open_threads"
    ],
    "type": "object"
   },
   "slug": {
    "description": "Kebab-case slug for the filename, e.g. \"mcp-journal-writer\".",
    "type": "string"
   },
   "summary_for_index": {
    "description": "Single-line summary for the README index row. No newlines, no \"|\".",
    "type": "string"
   }
  },
  "required": [
   "date",
   "slug",
   "sections",
   "summary_for_index"
  ],
  "type": "object"
 },
 "name": "write_journal_entry"
}
