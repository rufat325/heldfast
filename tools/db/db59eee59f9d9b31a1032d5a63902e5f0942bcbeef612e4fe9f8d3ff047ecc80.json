{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the global astronaut roster by name \u2014 returns astronaut name, active/retired status, and sponsoring space agency, optionally filtered by a free-text search. Data: ll.thespacedevs.com (The Space Devs), no auth required; free tier is throttled to 15 requests/hour shared across all APIbase agents, so results are cached for 24 hours.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max results to return, 1-20 (default 10)",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "search": {
    "description": "Free-text search across name/mission/agency fields, e.g. \"falcon\" or \"artemis\"",
    "maxLength": 100,
    "minLength": 1,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "launch-library-2.astronauts.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Launch Library 2 Astronaut Search"
}
