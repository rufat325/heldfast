{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Order a VPS and pay from the account credit balance \u2014 the balance must already cover the price (top up first with `impreza_topup`). For the VPS, choose `location`, `os`, `cpu_cores`, `memory_gb` and `disk_gb` from `impreza_vps_offer` (price that configuration there first) and leave `product_id` out. For a fixed plan such as Tor Hosting, pass its `product_id` from `impreza_list_products` instead (and `config_options` only if that plan has some). The VPS is born DEPLOYABLE: unless you pass `app`, the Impreza agent is auto-installed so it appears under `impreza_list_servers` within a few minutes. Returns immediately (202) \u2014 the order provisions in the background, so poll `impreza_list_servers` until the new agent is online.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "app": {
    "description": "Optional. '@agent' (default \u2014 deployable), '@agent-mcp', 'none' (bare OS), a '@panel-*' control panel, or a catalog app name from impreza_list_apps.",
    "type": "string"
   },
   "billing_cycle": {
    "description": "One of: monthly, quarterly, semiannually, annually, biennially, triennially.",
    "type": "string"
   },
   "config_options": {
    "description": "Fixed plans only: configurable options as {optionId: value}, from GET /v1/products/{id}. Each must belong to that plan.",
    "type": "object"
   },
   "cpu_cores": {
    "description": "VPS vCPU count, inside the range impreza_vps_offer lists.",
    "type": "number"
   },
   "disk_gb": {
    "description": "VPS SSD disk in GB, inside the range impreza_vps_offer lists.",
    "type": "number"
   },
   "domain": {
    "description": "Fixed plans only: domain for the order line.",
    "type": "string"
   },
   "hostname": {
    "description": "Optional server hostname (a DNS name such as \"web-1\"). Two identical VPS orders within a day are refused as a repeat unless their hostnames differ.",
    "type": "string"
   },
   "location": {
    "description": "VPS location: an id, slug or name from impreza_vps_offer, e.g. \"romania\".",
    "type": [
     "string",
     "number"
    ]
   },
   "memory_gb": {
    "description": "VPS memory in GB, inside the range impreza_vps_offer lists.",
    "type": "number"
   },
   "os": {
    "description": "VPS operating system: an id, slug or name from impreza_vps_offer, e.g. \"ubuntu-24.04\".",
    "type": [
     "string",
     "number"
    ]
   },
   "product_id": {
    "description": "Only for a fixed plan (e.g. Tor Hosting) from impreza_list_products. Leave it out for the VPS.",
    "type": "number"
   }
  },
  "required": [
   "billing_cycle"
  ],
  "type": "object"
 },
 "name": "impreza_order_vps"
}
