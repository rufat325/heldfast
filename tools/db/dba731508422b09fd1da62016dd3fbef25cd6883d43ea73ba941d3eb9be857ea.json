{
 "description": "Find and choose the best eligible provider, tool, or MCP route for a task when the provider is unknown. Use this for requests like 'find a provider', 'which tool should I use', 'find a fallback', 'use a cheaper, faster, or more reliable option', or when the current provider failed. Pass the task in natural language. If ATL should own execution, call atl_execute next using the returned decision_reference and outcome_correlation_token; atl_execute records the durable Outcome automatically.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "auth_available": {
    "description": "Whether the caller can authenticate to the selected provider.",
    "type": "boolean"
   },
   "constraints": {
    "description": "Optional hard requirements the selected provider must meet.",
    "type": "object"
   },
   "desired_outcome": {
    "description": "Optional result the caller wants from the task.",
    "type": "string"
   },
   "max_retries": {
    "description": "Optional maximum retries allowed for the decision request.",
    "maximum": 2,
    "minimum": 0,
    "type": "integer"
   },
   "preferences": {
    "description": "Optional trade-offs such as cost, latency, or reliability.",
    "type": "object"
   },
   "required_capability": {
    "description": "Capability the selected provider must support.",
    "type": "string"
   },
   "task": {
    "description": "Natural-language task. ATL infers the required capability when possible.",
    "type": "string"
   },
   "timeout_ms": {
    "description": "Optional time limit for the decision request in milliseconds.",
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "task"
  ],
  "type": "object"
 },
 "name": "atl_decide"
}
