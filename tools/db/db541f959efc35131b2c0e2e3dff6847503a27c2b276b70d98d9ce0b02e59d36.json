{
 "description": "Read the current terminal output from any pane without running a command. Use after execute_async to check if a command finished, or to monitor a running process.",
 "inputSchema": {
  "properties": {
   "pane": {
    "description": "Target pane as 'window.pane' (e.g. '0.0'). Defaults to '0.0'.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_pane_output"
}
