{
 "description": "[Merchant] Test JWT token creation process with detailed step-by-step breakdown, showing deterministic JSON serialization, SHA-256 hash calculation, and final JWT structure",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "partnerId": {
    "description": "Partner ID for JWT creation (defaults to environment)",
    "type": "string"
   },
   "requestPayload": {
    "description": "Sample request payload to demonstrate SHA-256 hash calculation",
    "type": "object"
   },
   "showPrivateKey": {
    "default": false,
    "description": "Whether to show private key details (for debugging)",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "payware_authentication_test_jwt"
}
