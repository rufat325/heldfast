{
 "_meta": {
  "securitySchemes": [
   {
    "scopes": [
     "email"
    ],
    "type": "oauth2"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Whether the apply agent can submit an application for this job on the user's behalf, and what that requires. Jobs on non-supported systems still return their direct applyUrl for the human to use. For whether THIS KEY may apply at all, call key_status \u2014 this tool answers about the job, not the key. Needs a key or a sign-in.",
 "inputSchema": {
  "properties": {
   "id": {
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "check_apply_support",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "agentReady": {
    "description": "True when the posting's hiring system is one the apply agent can submit to.",
    "type": "boolean"
   },
   "applyUrl": {
    "description": "The employer's own apply page. ABSENT when the board could not read the posting.",
    "type": "string"
   },
   "jobId": {
    "type": "string"
   },
   "requirements": {
    "description": "What applying through the agent needs \u2014 or, on a non-supported system, the one line saying the human applies at applyUrl.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "vendor": {
    "description": "The hiring-system prefix of the id, e.g. 'greenhouse'. Null when the id carries none.",
    "type": [
     "string",
     "null"
    ]
   }
  },
  "required": [
   "jobId",
   "agentReady",
   "vendor",
   "requirements"
  ],
  "type": "object"
 },
 "securitySchemes": [
  {
   "scopes": [
    "email"
   ],
   "type": "oauth2"
  }
 ],
 "title": "Check apply support"
}
