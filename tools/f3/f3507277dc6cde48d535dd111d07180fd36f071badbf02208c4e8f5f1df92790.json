{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List overdue plan blocks"
 },
 "description": "List the user's day-planner blocks that were due before the given local date and are still not done.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "local_date": {
    "description": "Local date YYYY-MM-DD \u2014 blocks overdue relative to this date",
    "type": "string"
   }
  },
  "required": [
   "local_date"
  ],
  "type": "object"
 },
 "name": "list_overdue_daily_plan_blocks",
 "title": "List overdue plan blocks"
}
