{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch statistical data from a Eurostat dataset by dataset code. Optionally filter by country (geo) and time period. Example: get_dataset({ dataset_code: \"nama_10_gdp\", geo: \"DE\", time: \"2023\" }). Common dataset codes: \"nama_10_gdp\" (GDP), \"prc_hicp_manr\" (inflation), \"une_rt_m\" (unemployment). Codes are lowercase, underscore-separated and must come from search_datasets or list_datasets \u2014 a guessed or descriptive code is not a Eurostat code.",
 "inputSchema": {
  "examples": [
   {
    "dataset_code": "nama_10_gdp",
    "geo": "DE",
    "time": "2023"
   },
   {
    "dataset_code": "prc_hicp_manr",
    "geo": "EU27_2020",
    "time": "2023-06"
   }
  ],
  "properties": {
   "dataset_code": {
    "description": "Eurostat dataset code from search_datasets or list_datasets, e.g. \"nama_10_gdp\", \"prc_hicp_manr\", \"une_rt_m\". Lowercase with underscores \u2014 not a title or a category word.",
    "type": "string"
   },
   "geo": {
    "description": "Country/region code filter, e.g. \"DE\" (Germany), \"FR\" (France), \"EU27_2020\" (EU aggregate)",
    "type": "string"
   },
   "time": {
    "description": "Time period filter, e.g. \"2023\", \"2023-01\" (Jan 2023), \"2023-Q1\" (Q1 2023)",
    "type": "string"
   }
  },
  "required": [
   "dataset_code"
  ],
  "type": "object"
 },
 "name": "get_dataset",
 "outputSchema": {
  "properties": {
   "dataset": {
    "description": "The dataset code requested",
    "type": "string"
   },
   "dimensions": {
    "additionalProperties": {
     "properties": {
      "categories": {
       "additionalProperties": {
        "type": "string"
       },
       "description": "Category index to label mappings",
       "type": "object"
      },
      "label": {
       "description": "Label for the dimension",
       "type": "string"
      }
     },
     "type": "object"
    },
    "description": "Dataset dimensions with labels and category mappings",
    "type": "object"
   },
   "label": {
    "description": "Human-readable label for the dataset",
    "type": "string"
   },
   "updated": {
    "description": "Last update timestamp of the dataset",
    "type": "string"
   },
   "value_count": {
    "description": "Number of data values in the dataset",
    "type": "number"
   },
   "values": {
    "additionalProperties": {
     "description": "Numeric value or null if missing",
     "type": [
      "number",
      "null"
     ]
    },
    "description": "Data values indexed by dimension combination",
    "type": "object"
   }
  },
  "required": [
   "dataset",
   "label",
   "updated",
   "dimensions",
   "value_count",
   "values"
  ],
  "type": "object"
 },
 "title": "Get Dataset",
 "version": "v1.241ceeac"
}
