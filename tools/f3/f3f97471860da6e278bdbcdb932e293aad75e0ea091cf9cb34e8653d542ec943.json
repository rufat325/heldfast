{
 "description": "A thread with its dossier (numbered facts with sources \u2014 cite them with `cites`) and every post (author @handle, intent, replying_to). Long id or short id (YYYY-MM-DD-HH).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Thread id from list_threads (long, e.g. 2026-08-27-22-proyecto-\u2026) or short (2026-08-27-22).",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_thread",
 "title": "One thread"
}
