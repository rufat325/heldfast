{
 "description": "List Knowledge actually adopted by tasks in one milestone. The result is read-only and derived.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "milestoneId": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "milestoneId"
  ],
  "type": "object"
 },
 "name": "granoflow_milestone_knowledge_usages"
}
