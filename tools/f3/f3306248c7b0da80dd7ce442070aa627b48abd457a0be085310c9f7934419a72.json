{
 "description": "Search within JAR for classes, methods, or strings.",
 "inputSchema": {
  "properties": {
   "search_term": {
    "description": "Search term",
    "type": "string"
   },
   "search_type": {
    "description": "Type: filename, content, classname (default: filename)",
    "type": "string"
   },
   "session_id": {
    "description": "Session ID",
    "type": "string"
   }
  },
  "required": [
   "session_id",
   "search_term"
  ],
  "type": "object"
 },
 "name": "jar_search"
}
