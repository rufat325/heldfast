{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Resolve one physical U.S. facility and return authoritative FRS environmental program/permit identifiers plus qualified jurisdiction and source-system provenance before deeper compliance research. This is a $0.10 USD MPP operation when commerce is active. It is currently jurisdiction-ready in 41 states. Ambiguous identity, medium-confidence identity, linked-record conflicts, missing routed program identity, and states with unresolved Tribal/federal carve-outs are rejected before payment. The result does not determine compliance, permit status, violations, enforcement, or penalties.",
 "inputSchema": {
  "additionalProperties": false,
  "anyOf": [
   {
    "required": [
     "name"
    ]
   },
   {
    "required": [
     "frsId"
    ]
   },
   {
    "required": [
     "programId"
    ]
   }
  ],
  "description": "Identity hints for one physical U.S. facility. Provide an exact EPA FRS ID or authoritative program ID when known; otherwise provide the facility name plus enough location context to resolve one site safely. FacilityProof fails closed on ambiguous identity.",
  "properties": {
   "address": {
    "description": "Street address for the physical facility when known. Used only for physical-site identity resolution; do not provide corporate headquarters unless that is the facility being checked.",
    "minLength": 1,
    "type": "string"
   },
   "city": {
    "description": "City for the physical facility. Used with name/address hints to distinguish facilities with similar names.",
    "minLength": 1,
    "type": "string"
   },
   "frsId": {
    "description": "Exact 12-digit EPA Facility Registry Service identifier when known. Prefer this over name matching because it anchors federal facility identity directly.",
    "pattern": "^\\d{12}$",
    "type": "string"
   },
   "name": {
    "description": "Facility or site name, including a distinguishing plant/site descriptor when available. For name-based resolution, also provide city/state, address, or ZIP whenever possible.",
    "minLength": 1,
    "type": "string"
   },
   "programId": {
    "description": "Exact authoritative environmental program identifier when known, such as an NPDES permit ID or RCRA handler ID. FacilityProof uses it to resolve the physical facility before paid work.",
    "minLength": 1,
    "type": "string"
   },
   "state": {
    "description": "Two-letter U.S. postal abbreviation for the physical facility. Full compliance evidence and permit identity have separate production-readiness rules.",
    "pattern": "^[A-Za-z]{2}$",
    "type": "string"
   },
   "zip": {
    "description": "U.S. ZIP or ZIP+4 for the physical facility. Use as an identity hint, not as a substitute for a site name or authoritative identifier.",
    "pattern": "^\\d{5}(?:-\\d{4})?$",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "facilityproof.check_permit_identity",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Resolved physical facility plus authoritative FRS environmental program identifiers and qualified state/federal source-system provenance. This is identity output, not a compliance conclusion.",
  "properties": {
   "conflicts": {
    "type": "array"
   },
   "facility": {
    "additionalProperties": true,
    "type": "object"
   },
   "freshness": {
    "description": "FRS retrieval time plus source-system cadence/provenance. Source-route entries do not pretend a facility-specific source record was captured.",
    "type": "array"
   },
   "generatedAt": {
    "type": "string"
   },
   "identity": {
    "additionalProperties": true,
    "type": "object"
   },
   "jurisdiction": {
    "additionalProperties": true,
    "type": "object"
   },
   "limitations": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "operation": {
    "const": "facilityproof.check_permit_identity"
   },
   "programs": {
    "type": "array"
   },
   "schemaVersion": {
    "const": "facility-permit-identity.v1"
   },
   "sourceProvenance": {
    "additionalProperties": true,
    "type": "object"
   },
   "unresolvedIdentifiers": {
    "type": "array"
   }
  },
  "required": [
   "schemaVersion",
   "operation",
   "generatedAt",
   "facility",
   "identity",
   "jurisdiction",
   "programs",
   "sourceProvenance",
   "unresolvedIdentifiers",
   "conflicts",
   "freshness",
   "limitations"
  ],
  "type": "object"
 },
 "title": "Check facility permit identity"
}
