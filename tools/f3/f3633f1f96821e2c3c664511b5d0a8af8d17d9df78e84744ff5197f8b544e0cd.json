{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Set up automatic backups for a Proxmox VPS \u2014 the answer to \"make sure this is backed up\". Check what already exists with `impreza_vps_list_backup_schedules` first: a second overlapping schedule just doubles the storage. Backups land on the Impreza backup store, not on the VPS disk, so they survive the machine. Basic Cloud VPS does not support infrastructure backups.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "day": {
    "description": "Day of week for a weekly schedule, e.g. \"sunday\".",
    "type": "string"
   },
   "frequency": {
    "description": "How often, e.g. \"daily\" or \"weekly\".",
    "type": "string"
   },
   "hour": {
    "description": "Hour of day to run (0-23), server time.",
    "type": "number"
   },
   "keep": {
    "description": "How many backups to keep before the oldest rotates out.",
    "type": "number"
   },
   "service_id": {
    "description": "Proxmox VPS service id.",
    "type": "number"
   }
  },
  "required": [
   "service_id"
  ],
  "type": "object"
 },
 "name": "impreza_vps_create_backup_schedule"
}
