{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "\"Find a hospital / clinic by name\" / \"look up NPI for [organization]\" / \"what's [Mayo Clinic]'s NPI\" / \"verify a healthcare facility\" \u2014 search the NPPES NPI registry for organizational providers (~1.8M entries) \u2014 hospitals, clinics, group practices. Same shape as npi_individual. terms can be org name, NPI, or city.",
 "inputSchema": {
  "examples": [
   {
    "count": 3,
    "terms": "mayo clinic"
   }
  ],
  "properties": {
   "count": {
    "default": 7,
    "description": "Maximum matches to return. Default 7, max 500. Use 1\u20133 for typeahead UX, 20\u201350 for browsing.",
    "maximum": 500,
    "minimum": 1,
    "type": "number"
   },
   "df": {
    "description": "Comma-separated display fields to use in the `displays` array. Default varies per table; usually the canonical name.",
    "type": "string"
   },
   "ef": {
    "description": "Comma-separated extra fields to include per match. Known fields: name.full,addr_practice.full,licenses.taxonomy.classification.",
    "type": "string"
   },
   "terms": {
    "description": "Search query \u2014 prefix/contains match against canonical names. Whitespace-split into AND tokens. Example: \"mayo clinic\".",
    "type": "string"
   }
  },
  "required": [
   "terms"
  ],
  "type": "object"
 },
 "name": "npi_organization",
 "title": "Npi Organization",
 "version": "v1.7d927777"
}
