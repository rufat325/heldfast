{
 "_meta": {
  "skill": "bitflow"
 },
 "description": "Create an automated swap order via Bitflow Keeper.\n\nCreates a pending order that will be executed by the Keeper service.\nNo API key required \u2014 uses public endpoints (500 req/min).\n\nNote: Bitflow is only available on mainnet.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "actionAmount": {
    "description": "Amount for the action",
    "type": "string"
   },
   "actionType": {
    "description": "Action type (e.g., 'SWAP_XYK_SWAP_HELPER')",
    "type": "string"
   },
   "autoAdjust": {
    "default": true,
    "description": "Auto-adjust minimum received based on market (default true)",
    "type": "boolean"
   },
   "contractIdentifier": {
    "description": "Keeper contract identifier",
    "type": "string"
   },
   "fundingTokens": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "Map of token IDs to amounts for funding",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "minReceivedAmount": {
    "description": "Minimum amount to receive (slippage protection)",
    "type": "string"
   }
  },
  "required": [
   "contractIdentifier",
   "actionType",
   "fundingTokens",
   "actionAmount"
  ],
  "type": "object"
 },
 "name": "bitflow_create_order"
}
