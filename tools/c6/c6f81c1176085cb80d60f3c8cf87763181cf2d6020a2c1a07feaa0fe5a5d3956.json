{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get full Wikidata entity by ID (e.g., \"Q42\" for Douglas Adams, \"Q5\" for human, \"Q1764\" for Budapest). Returns labels, descriptions, aliases, claims/statements (properties and values), sitelinks, and the revision the data came from \u2014 lastrevid plus the modified timestamp \u2014 so live statements can be pinned to a specific edit. Pass \"language\" (comma/pipe-joined for several) to get labels/descriptions in languages other than English, side by side. For checking several ids at once, use get_entities instead of looping this.",
 "inputSchema": {
  "examples": [
   {
    "id": "Q42"
   },
   {
    "id": "Q162887",
    "language": "ka,ru,en"
   }
  ],
  "properties": {
   "id": {
    "description": "Wikidata entity ID (e.g., \"Q42\", \"Q937\", \"P31\")",
    "type": "string"
   },
   "language": {
    "description": "Language code(s) for labels/descriptions/aliases \u2014 a single code (e.g. \"ka\") or a comma/pipe-joined list to compare several at once (e.g. \"ka,ru,en\"), which returns every requested language's label side by side in one call \u2014 the shape needed to decide whether two records in different languages describe the same entity. Default \"en\". Falls back to \"en\", then to Wikidata's language-neutral \"mul\" label, for any requested language the entity has no label in \u2014 Q42 (Douglas Adams) has no English label at all, only a \"mul\" one. When the name came from \"mul\" it appears under a \"mul\" key in the labels map, so you can tell where it came from.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_entity",
 "outputSchema": {
  "properties": {
   "aliases": {
    "description": "List of alternative names",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "claims": {
    "additionalProperties": {
     "items": {},
     "type": "array"
    },
    "description": "Property claims/statements (up to 30 properties, 5 values each)",
    "type": "object"
   },
   "description": {
    "description": "English description of the entity",
    "type": [
     "string",
     "null"
    ]
   },
   "id": {
    "description": "Wikidata entity ID",
    "type": "string"
   },
   "label": {
    "description": "English label for the entity",
    "type": [
     "string",
     "null"
    ]
   },
   "lastrevid": {
    "description": "Revision id these statements were read at, for pinning the answer to a specific edit",
    "type": [
     "number",
     "null"
    ]
   },
   "modified": {
    "description": "UTC timestamp of that revision (ISO 8601)",
    "type": [
     "string",
     "null"
    ]
   },
   "revision_url": {
    "description": "Permalink to that exact revision on wikidata.org",
    "type": [
     "string",
     "null"
    ]
   },
   "sitelinks_count": {
    "description": "Number of Wikipedia/sister project links",
    "type": "number"
   },
   "type": {
    "description": "Entity type (e.g., 'item', 'property')",
    "type": "string"
   },
   "wikipedia_en": {
    "description": "English Wikipedia article title if available",
    "type": [
     "string",
     "null"
    ]
   }
  },
  "required": [
   "id",
   "type",
   "label",
   "description",
   "aliases",
   "claims",
   "sitelinks_count",
   "wikipedia_en",
   "lastrevid",
   "modified",
   "revision_url"
  ],
  "type": "object"
 },
 "title": "Get Entity",
 "version": "v1.c622b975"
}
