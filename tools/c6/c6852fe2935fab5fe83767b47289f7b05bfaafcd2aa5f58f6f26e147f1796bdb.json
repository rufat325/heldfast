{
 "annotations": {
  "readOnlyHint": true,
  "title": "Get card details"
 },
 "description": "Get full details of a card (task or email thread) by ID. Returns title, status, assignees, due date, column, board, note, and timestamps.",
 "inputSchema": {
  "properties": {
   "boardId": {
    "description": "Optional board ID. Speeds up the lookup; not required.",
    "type": "number"
   },
   "cardId": {
    "description": "The card ID. Numeric task IDs and hex thread IDs both work.",
    "type": "string"
   }
  },
  "required": [
   "cardId"
  ],
  "type": "object"
 },
 "name": "get_card",
 "title": "Get card details"
}
