{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List all images and media files embedded in a Wikipedia article \u2014 returns file titles, type (image/video/audio), srcset URLs at multiple resolutions, and caption text. Useful for enriching responses with article visuals. No auth required. Data: Wikipedia contributors, CC BY-SA 4.0.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "language": {
    "description": "Two-letter Wikipedia language code (default \"en\"). Media availability varies across language editions.",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   },
   "title": {
    "description": "Wikipedia article title (e.g. \"Eiffel_Tower\", \"Milky_Way\"). Use underscores for spaces. Returns images and media files embedded in the article.",
    "type": "string"
   }
  },
  "required": [
   "title"
  ],
  "type": "object"
 },
 "name": "wikipedia.article.media",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Wikipedia Article Media"
}
