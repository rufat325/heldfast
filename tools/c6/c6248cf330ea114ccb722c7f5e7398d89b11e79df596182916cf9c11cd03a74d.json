{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Check proposed file content for errors WITHOUT writing it to disk. Returns: valid flag, errors and warnings. WHEN TO CALL: before writing an edit, so broken code never reaches the project. The LSP is restored to the on-disk content afterwards.",
 "inputSchema": {
  "properties": {
   "content": {
    "description": "Full proposed file content",
    "type": "string"
   },
   "file": {
    "description": "Path the content is intended for",
    "type": "string"
   }
  },
  "required": [
   "file",
   "content"
  ],
  "type": "object"
 },
 "name": "gdscript_validate"
}
