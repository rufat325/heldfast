{
 "description": "List bank transactions (spend/receive entries on bank accounts). Supports where-clause filtering by BankAccount.AccountID, Type, Date, etc.",
 "inputSchema": {
  "properties": {
   "order": {
    "description": "Ordering, e.g. 'Date DESC'.",
    "type": "string"
   },
   "page": {
    "description": "Page number (100 per page).",
    "type": "number"
   },
   "where": {
    "description": "Xero where clause, e.g. 'BankAccount.AccountID==guid(\"...\")'.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_bank_transactions"
}
