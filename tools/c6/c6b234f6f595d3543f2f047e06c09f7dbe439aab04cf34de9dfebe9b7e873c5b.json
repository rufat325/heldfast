{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "REQUIRES API KEY (sessions:read scope). Lists the authenticated user's remote sessions on Robotics & AI bookings \u2014 status, GPU allocation, total compute hours, cost so far. Use before starting a new session to check if one is already active.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "booking_id": {
    "format": "uuid",
    "type": "string"
   },
   "status": {
    "enum": [
     "provisioning",
     "active",
     "paused",
     "terminated",
     "failed"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "rigshare_list_my_sessions",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "sessions": {
    "items": {
     "additionalProperties": {},
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "sessions"
  ],
  "type": "object"
 },
 "title": "List my sessions"
}
