{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Place a Lighter order"
 },
 "description": "Sign and send one limit or market order. max_notional_usd is required and has no default. The order is priced against one book snapshot and refused above the ceiling before it is signed, so the order that goes out is the order that was checked. The server applies its own configured ceiling as well, and the lower of the two wins. A Lighter API key is full write authority over the account it is bound to, so run this server against a dedicated sub-account funded with only what the agent may lose.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "market_id": {
    "description": "Market id, for example 182 for H100. Use this or symbol.",
    "maximum": 4095,
    "minimum": 0,
    "type": "integer"
   },
   "max_notional_usd": {
    "description": "Required ceiling on order notional in USD. The order is refused when its worst-case notional exceeds this value. There is no default.",
    "exclusiveMinimum": 0,
    "maximum": 10000000,
    "type": "number"
   },
   "order_type": {
    "default": "limit",
    "description": "limit rests on the book. market is immediate-or-cancel with an average execution bound.",
    "enum": [
     "limit",
     "market"
    ],
    "type": "string"
   },
   "price": {
    "description": "Limit price in quote units. Required for a limit order.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "reduce_only": {
    "default": false,
    "description": "Refuse to open or increase a position. A reduce-only order can only shrink an existing position, so it is exempt from max_notional_usd and from the market size floor.",
    "type": "boolean"
   },
   "side": {
    "description": "Order side.",
    "enum": [
     "buy",
     "sell"
    ],
    "type": "string"
   },
   "size": {
    "description": "Order size in base units, in human decimals.",
    "exclusiveMinimum": 0,
    "maximum": 1000000000,
    "type": "number"
   },
   "slippage_bps": {
    "description": "Average execution bound for a market order, in basis points.",
    "exclusiveMinimum": 0,
    "maximum": 1000,
    "type": "number"
   },
   "symbol": {
    "description": "Market ticker, for example H100. Use this or market_id.",
    "maxLength": 32,
    "minLength": 1,
    "type": "string"
   },
   "time_in_force": {
    "description": "Resting behaviour for a limit order. Defaults to gtt.",
    "enum": [
     "gtt",
     "ioc",
     "post_only"
    ],
    "type": "string"
   }
  },
  "required": [
   "side",
   "size",
   "max_notional_usd"
  ],
  "type": "object"
 },
 "name": "lighter_place_order",
 "title": "Place a Lighter order"
}
