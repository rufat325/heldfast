{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Minute-by-minute timeline for one match: goals with scorer and assist, cards, and substitutions, each with the minute and which side it belongs to. Use for settling scorer, first-goal and card questions. COVERAGE IS PER-COMPETITION, not universal \u2014 a match the source has not covered returns found:false with a reason, not an error and not a wrong answer.",
 "inputSchema": {
  "examples": [
   {
    "event_id": "2594568"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional TheSportsDB key. The free tier serves this tool; a supporter key raises the rate limit.",
    "type": "string"
   },
   "event_id": {
    "description": "TheSportsDB idEvent, e.g. \"2594568\".",
    "type": "string"
   }
  },
  "required": [
   "event_id"
  ],
  "type": "object"
 },
 "name": "event_timeline",
 "title": "Event Timeline",
 "version": "v1.29a5fb75"
}
