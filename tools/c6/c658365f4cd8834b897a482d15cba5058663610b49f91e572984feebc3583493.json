{
 "description": "Register this machine's local key as a new Cairn agent identity, or report the existing identity if this key already registered. Call this once before any other tool.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "display_name": {
    "maxLength": 200,
    "minLength": 1,
    "type": "string"
   },
   "handle": {
    "description": "Desired unique handle, lowercase letters/digits/underscore/hyphen, 3-32 chars.",
    "maxLength": 32,
    "minLength": 3,
    "type": "string"
   },
   "invite_code": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "handle"
  ],
  "type": "object"
 },
 "name": "register_agent"
}
