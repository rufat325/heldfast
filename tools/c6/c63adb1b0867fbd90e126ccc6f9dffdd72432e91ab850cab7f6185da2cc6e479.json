{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find apps that integrate a given SDK. Pass one or more SDK ids via `sdk` (e.g. \"google-ads-admob\"). Useful for competitive/market-share analysis of ad networks, analytics, and other SDKs. Example: fortytwo_apps_by_sdk({ platform: \"android\", sdk: \"google-ads-admob\", limit: 20, _apiKey: \"your-token\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-42matters-api-key",
    "limit": 20,
    "platform": "android",
    "sdk": "google-ads-admob"
   },
   {
    "_apiKey": "your-42matters-api-key",
    "limit": 50,
    "page": 1,
    "platform": "ios",
    "sdk": "facebook-sdk,google-analytics"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "42matters API access token (get one at https://42matters.com/pricing).",
    "type": "string"
   },
   "limit": {
    "description": "Results per page (default 10).",
    "type": "number"
   },
   "page": {
    "description": "Page number for pagination (optional).",
    "type": "number"
   },
   "platform": {
    "description": "App store to query: \"android\" (Google Play) or \"ios\" (Apple App Store). Defaults to \"android\".",
    "enum": [
     "android",
     "ios"
    ],
    "type": "string"
   },
   "sdk": {
    "description": "SDK id, or comma-separated SDK ids, to match (apps embedding ANY of them). e.g. \"google-ads-admob\", \"facebook-sdk\".",
    "type": "string"
   }
  },
  "required": [
   "sdk",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "fortytwo_apps_by_sdk",
 "title": "Fortytwo Apps By Sdk",
 "version": "v1.6490fccb"
}
