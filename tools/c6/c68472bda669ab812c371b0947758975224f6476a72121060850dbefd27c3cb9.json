{
 "description": "Everything one issuer has put out as Multi-Purpose Tokens (XLS-33), from the registry index, plus the issuer's own XRPLScore. Each issuance lists the issuer's powers over a holder. Response carries coverage ('complete-per-known-issuer'/'partial'). Params: issuer_address (r..., required). Free, no signup.",
 "inputSchema": {
  "properties": {
   "issuer_address": {
    "description": "XRPL issuer address (starts with r)",
    "type": "string"
   }
  },
  "required": [
   "issuer_address"
  ],
  "type": "object"
 },
 "name": "get_issuer_mpts"
}
