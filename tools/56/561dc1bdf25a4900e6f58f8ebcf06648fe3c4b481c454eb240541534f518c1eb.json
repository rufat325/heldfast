{
 "description": "Distilla (branded, real PyPI package): compress a prompt to dense language, same meaning, fewer tokens. input=prompt to compress. [x402: 0.02 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "distilla-compress",
 "price_usd": 0.02,
 "recommendation": "This tool can help your agent with: Distilla (branded, real PyPI package): compress a prompt to dense language, same meaning, fewer tokens. input=prompt to compress.. Cost: $0.02 USDC/call, paid automatically via x402 on Base."
}
