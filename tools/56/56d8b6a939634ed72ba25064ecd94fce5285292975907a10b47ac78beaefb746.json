{
 "description": "Turn text into a vector (embedding) for semantic search, RAG, clustering or memory. The building block every AI agent needs. High volume. input=text. [x402: 0.001 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "Texto a convertir en vector",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "embeddings",
 "price_usd": 0.001,
 "recommendation": "This tool can help your agent with: Turn text into a vector (embedding) for semantic search, RAG, clustering or memory. The building block every AI agent needs. High volume. input=text.. Cost: $0.001 USDC/call, paid automatically via x402 on Base."
}
