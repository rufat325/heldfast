{
 "description": "Check a domain\u2019s ownership proof now. The proof is the record; no signature needed.",
 "inputSchema": {
  "properties": {
   "keyId": {
    "type": "string"
   },
   "origin": {
    "type": "string"
   }
  },
  "required": [
   "origin"
  ],
  "type": "object"
 },
 "name": "verify_domain"
}
