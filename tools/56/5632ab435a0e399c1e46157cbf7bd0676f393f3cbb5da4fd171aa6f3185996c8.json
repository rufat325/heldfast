{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Rename or move folder"
 },
 "description": "Rename a folder and/or move it under another folder. `parent` 'none' moves it to the top level. Codes inside keep their filing.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Folder id (from list_folders).",
    "type": "string"
   },
   "name": {
    "description": "New folder name.",
    "type": "string"
   },
   "parent": {
    "description": "New parent folder id or slug, or 'none' for top level.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "update_folder",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "code_count": {
    "type": "integer"
   },
   "created": {
    "type": "string"
   },
   "error": {
    "additionalProperties": true,
    "description": "Present only when the tool failed (the result is also flagged isError).",
    "properties": {
     "cause": {
      "description": "Machine-readable failure category: invalid_argument, not_found, forbidden, plan_limit, rate_limited, or server_error.",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "retryable": {
      "description": "Whether repeating the same call could succeed.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "id": {
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "parent": {
    "description": "Parent folder id, null at the top level.",
    "type": [
     "string",
     "null"
    ]
   },
   "path": {
    "description": "Full path, e.g. Campaigns / Spring 2026.",
    "type": "string"
   },
   "slug": {
    "type": "string"
   }
  },
  "type": "object"
 }
}
