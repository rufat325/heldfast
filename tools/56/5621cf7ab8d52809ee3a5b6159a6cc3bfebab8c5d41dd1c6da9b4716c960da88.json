{
 "description": "List spaced-repetition review cards. By default returns cards due now (due_only=true). Use this to conduct an interactive review session: list due cards, present each Q to the user, wait for their answer, call answer_card, repeat.",
 "inputSchema": {
  "properties": {
   "due_only": {
    "description": "If true, only cards due now (default true)",
    "type": "boolean"
   },
   "limit": {
    "description": "Max cards to return (default 20, max 100)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list_review_cards"
}
