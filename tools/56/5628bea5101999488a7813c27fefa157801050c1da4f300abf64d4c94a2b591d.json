{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get Invitation Status"
 },
 "description": "Check the status of an invitation send \u2014 state, recipient count, invites/emails sent, failures, % complete. Requires event_id + job_id; you must be a host. Read-only.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "job_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "job_id"
  ],
  "type": "object"
 },
 "name": "get_invitation_status",
 "title": "Get Invitation Status"
}
