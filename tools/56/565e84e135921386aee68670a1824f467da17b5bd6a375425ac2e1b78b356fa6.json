{
 "description": "List public contracts where a given CNPJ appears as the supplier (fornecedor). Useful for analyzing a competitor or a potential partner. Defaults to the last 365 days.",
 "inputSchema": {
  "properties": {
   "cnpj": {
    "description": "Supplier CNPJ (14 digits)",
    "type": "string"
   },
   "diasAtras": {
    "default": 365,
    "description": "How many days back to search.",
    "maximum": 3650,
    "minimum": 1,
    "type": "integer"
   },
   "pagina": {
    "default": 1,
    "minimum": 1,
    "type": "integer"
   },
   "tamanhoPagina": {
    "default": 50,
    "maximum": 50,
    "minimum": 10,
    "type": "integer"
   }
  },
  "required": [
   "cnpj"
  ],
  "type": "object"
 },
 "name": "get_fornecedor_contratos"
}
