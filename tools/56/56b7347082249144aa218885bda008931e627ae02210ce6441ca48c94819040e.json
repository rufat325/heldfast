{
 "_meta": {},
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List app encryption declarations"
 },
 "description": "List app encryption declarations (export compliance, encryption usage)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "filter.app": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by app ID(s)"
   },
   "filter.builds": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by build ID(s)"
   },
   "filter.platform": {
    "anyOf": [
     {
      "enum": [
       "IOS",
       "MAC_OS",
       "TV_OS",
       "VISION_OS"
      ],
      "type": "string"
     },
     {
      "items": {
       "enum": [
        "IOS",
        "MAC_OS",
        "TV_OS",
        "VISION_OS"
       ],
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by platform(s)"
   },
   "limit": {
    "default": 100,
    "description": "Maximum number of app encryption declarations to return (default 100, max 200)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list-app-encryption-declarations",
 "title": "List app encryption declarations"
}
