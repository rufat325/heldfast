{
 "description": "Multi-round deep search. Generates multiple search angles from the query, searches in parallel, deduplicates results, and returns a comprehensive combined result set.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "maxResultsPerRound": {
    "description": "Max results per search angle (default: 5)",
    "maximum": 20,
    "minimum": 1,
    "type": "number"
   },
   "maxRounds": {
    "description": "Number of search angles to explore (default: 4)",
    "maximum": 8,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "description": "The main research query",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_deep"
}
