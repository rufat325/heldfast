{
 "description": "Validate YAML syntax and structure. Returns parsed result on success or detailed error with line/column on failure. Useful for CI pipelines and config file validation.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "yaml": {
    "description": "The YAML string to validate",
    "type": "string"
   }
  },
  "required": [
   "yaml"
  ],
  "type": "object"
 },
 "name": "yaml_validator"
}
