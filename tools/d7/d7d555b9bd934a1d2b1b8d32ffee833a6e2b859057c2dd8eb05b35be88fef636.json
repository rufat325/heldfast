{
 "description": "Verify a signed PDF matches what was recorded for its request. (Read-only; signing itself stays human-gated behind sign-cli's own MCP.)",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "path": {
    "description": "Path to the signed PDF.",
    "type": "string"
   },
   "request_id": {
    "description": "The sign request id.",
    "type": "string"
   }
  },
  "required": [
   "request_id",
   "path"
  ],
  "type": "object"
 },
 "name": "verify_signature"
}
