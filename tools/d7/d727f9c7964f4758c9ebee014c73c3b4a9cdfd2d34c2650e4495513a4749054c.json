{
 "description": "GET https://ticks.bnm.farm/ccb-determinations \u2014 $0.05 USDC on Base to 0xf59621FC406D266e18f314Ae18eF0a33b8401004. Copyright Claims Board Final Determination text GET ?id= is one official text ($0.02). Newest chunk on a plain GET ($0.05); older chunk if they ask (?before, $0.05); newer than a watermark ?since= ($0.05). Unpaid returns HTTP 402. After a valid X-PAYMENT, the same URL returns JSON. Not a new SKU.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "before": {
    "description": "Official catalog id or YYYY-MM-DD. Next older chunk on the same URL, another $0.05. Omit for the newest chunk.",
    "type": "string"
   },
   "id": {
    "description": "Official catalog id. That one official text, $0.02. Same door, not a new SKU.",
    "type": "string"
   },
   "page": {
    "description": "1-based page. Page 1 is the newest chunk. Ignored when since/before is set.",
    "type": "string"
   },
   "since": {
    "description": "ISO timestamp or official catalog id. Official texts newer than this watermark, $0.05. Empty new set is 304 or recordCount 0.",
    "type": "string"
   },
   "x_payment": {
    "description": "Optional x402 X-PAYMENT value forwarded to the paid GET as the X-PAYMENT header.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ccb-determinations"
}
