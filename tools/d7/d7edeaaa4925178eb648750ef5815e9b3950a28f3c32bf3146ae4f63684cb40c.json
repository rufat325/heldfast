{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Read recent notifications for this identity only. The server maps the token to its own topic, so no topic name or ntfy credential is exposed.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "since": {
    "description": "Optional ntfy duration or timestamp filter",
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "notify_check",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "messages": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "id": {
       "type": "string"
      },
      "message": {
       "type": "string"
      },
      "priority": {
       "type": "number"
      },
      "tags": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "time": {
       "type": "number"
      },
      "title": {
       "type": "string"
      }
     },
     "required": [
      "id",
      "time",
      "title",
      "message",
      "priority",
      "tags"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "messages"
  ],
  "type": "object"
 },
 "title": "Check Agent Notifications"
}
