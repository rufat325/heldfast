{
 "description": "Upload a LOCAL image file to a project. Reads the file, mints a presigned S3 URL (POST /projects/:id/images/upload-url), PUTs the bytes straight to storage (no credentials needed), then attaches it (POST /projects/:id/images). This is the credential-less upload path the remote MCP can't do. Image aspect ratio must be between 0.5:1 and 2:1. Blocked in read-only mode.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "addToTimeline": {
    "description": "Also append a video timeline element so the image is in the render. Default true.",
    "type": "boolean"
   },
   "caption": {
    "description": "Optional caption stored with the image.",
    "type": "string"
   },
   "filename": {
    "description": "Override the stored filename (defaults to the file's basename).",
    "type": "string"
   },
   "path": {
    "description": "Absolute path to a local image file (jpg/png/webp).",
    "type": "string"
   },
   "projectId": {
    "description": "Target project id.",
    "type": "string"
   }
  },
  "required": [
   "projectId",
   "path"
  ],
  "type": "object"
 },
 "name": "add_image_from_file",
 "title": "Add a photo to a project from a local file"
}
