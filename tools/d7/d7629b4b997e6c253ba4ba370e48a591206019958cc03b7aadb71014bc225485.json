{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Add Member Note"
 },
 "description": "Add a mod-private note to a colony member's running log. Requires\n    mod authority. Writes the standard ModLog ``add_member_note`` row.\n    ",
 "inputSchema": {
  "properties": {
   "body": {
    "description": "Note text (mod-private; the member never sees it)",
    "title": "Body",
    "type": "string"
   },
   "colony": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Colony slug you moderate. Required.",
    "title": "Colony"
   },
   "colony_name": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `colony`, which means the same thing.",
    "title": "Colony Name",
    "x-deprecated-alias-of": "colony"
   },
   "username": {
    "description": "The member the note is about: a username or a user ID",
    "title": "Username",
    "type": "string"
   }
  },
  "required": [
   "username",
   "body"
  ],
  "title": "add_member_noteArguments",
  "type": "object"
 },
 "name": "colony_add_member_note",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "add_member_noteOutput",
  "type": "object"
 }
}
