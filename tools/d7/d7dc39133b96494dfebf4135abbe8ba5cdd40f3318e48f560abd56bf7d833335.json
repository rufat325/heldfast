{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the sanctions watchlists (OFAC SDN + Consolidated, EU, UK, UN) and GLEIF datasets currently loaded in the local mirror, each with its record count, source URL, license, and the mirror's readiness and as-of timestamp \u2014 for GLEIF, also whether its reporting exceptions are loaded and how many. Use this for provenance and freshness on any result \u2014 results are only as current as the last mirror refresh, and a not-ready mirror means screening cannot run yet. Attribution: UK data is under the Open Government Licence v3.0; all sources are cited here.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {},
  "type": "object"
 },
 "name": "sanctions_list_sources",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "anyOf": [
   {
    "not": {
     "required": [
      "error"
     ]
    },
    "required": [
     "sanctionsReady",
     "leiReady",
     "reportingExceptionsLoaded",
     "sources"
    ]
   },
   {
    "required": [
     "error"
    ]
   }
  ],
  "properties": {
   "error": {
    "additionalProperties": {},
    "description": "Present when the call failed. Absent on success.",
    "properties": {
     "code": {
      "description": "JSON-RPC error code for this failure.",
      "maximum": 9007199254740991,
      "minimum": -9007199254740991,
      "type": "integer"
     },
     "data": {
      "additionalProperties": {},
      "properties": {
       "reason": {
        "description": "Machine-readable failure mode.",
        "type": "string"
       },
       "recovery": {
        "additionalProperties": {},
        "description": "Actionable next step for the caller.",
        "properties": {
         "hint": {
          "type": "string"
         }
        },
        "required": [
         "hint"
        ],
        "type": "object"
       },
       "retryable": {
        "description": "Whether retrying may succeed.",
        "type": "boolean"
       }
      },
      "type": "object"
     },
     "message": {
      "description": "Human-readable description of what went wrong.",
      "type": "string"
     }
    },
    "required": [
     "code",
     "message"
    ],
    "type": "object"
   },
   "leiAsOf": {
    "description": "ISO 8601 timestamp of the last completed GLEIF sync, when available.",
    "type": "string"
   },
   "leiReady": {
    "description": "True once the GLEIF (LEI) mirror has completed at least one full sync.",
    "type": "boolean"
   },
   "reportingExceptionsLoaded": {
    "description": "Whether GLEIF reporting exceptions are loaded. When false, sanctions_trace_ownership reads a parent level with no published relationship as unknown.",
    "type": "boolean"
   },
   "sanctionsAsOf": {
    "description": "ISO 8601 timestamp of the last completed sanctions sync, when available.",
    "type": "string"
   },
   "sanctionsReady": {
    "description": "True once the sanctions mirror has completed at least one full sync.",
    "type": "boolean"
   },
   "sources": {
    "description": "All loaded sources, sanctions lists then the GLEIF dataset.",
    "items": {
     "additionalProperties": false,
     "description": "One loaded source with count, provenance, and license.",
     "properties": {
      "code": {
       "description": "Source code (ofac_sdn, eu, \u2026, or gleif).",
       "type": "string"
      },
      "label": {
       "description": "Human-readable source name.",
       "type": "string"
      },
      "license": {
       "description": "Redistribution license / terms for this source.",
       "type": "string"
      },
      "recordCount": {
       "description": "Records currently loaded for this source (Level 1 entities for gleif).",
       "type": "number"
      },
      "reportingExceptionCount": {
       "description": "GLEIF reporting-exception records loaded (gleif only). Absent when the dataset has never been loaded \u2014 never read as zero.",
       "type": "number"
      },
      "url": {
       "description": "Upstream source URL the mirror harvests from.",
       "type": "string"
      }
     },
     "required": [
      "code",
      "label",
      "recordCount",
      "url",
      "license"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "title": "sanctions-screening-mcp-server: list sources"
}
