{
 "description": "Revoke a share link so the server stops handing out the file. Note: anyone who already downloaded the file keeps it \u2014 revocation only stops future downloads.",
 "inputSchema": {
  "properties": {
   "share_id": {
    "description": "Share ID to revoke",
    "type": "string"
   }
  },
  "required": [
   "share_id"
  ],
  "type": "object"
 },
 "name": "bitatlas_revoke_share"
}
