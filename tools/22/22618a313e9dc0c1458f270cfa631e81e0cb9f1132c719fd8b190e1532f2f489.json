{
 "description": "The ranked Do Next feed across the whole portfolio \u2014 the same rules the Command Center runs, highest priority first, each with why it fired and where it leads. Deterministic: no model runs, so the answer is the same for the same data. (GET /api/v1/actions; needs the read:actions scope)",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "1\u201350, default 10.",
    "type": "number"
   },
   "project": {
    "description": "Project id or slug. Resolved inside the key owner's portfolio; an unowned value matches nothing rather than 404ing.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "next_actions"
}
