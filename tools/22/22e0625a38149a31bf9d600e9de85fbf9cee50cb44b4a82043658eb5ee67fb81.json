{
 "description": "Supported formats, samples, and service capabilities. No auth required.\nDelegates to package implementation. Single source of truth lives in\npkg/sunholo/ailang_parse/services/mcp/tools \u2014 since ailang_parse 0.39.x the\nresponse carries a runtime capability block (state, not intent), which is\nwhy this is no longer pure: it reads env and probes the runtime (Process).",
 "inputSchema": {
  "properties": {
   "_": {
    "type": "string"
   }
  },
  "required": [
   "_"
  ],
  "type": "object"
 },
 "name": "mcpFormats"
}
