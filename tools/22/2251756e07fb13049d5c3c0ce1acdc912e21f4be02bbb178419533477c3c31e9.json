{
 "description": "Encrypts a local file and uploads it to vnsh, returning a shareable URL. Use this for images, screenshots, PDFs, binaries, or any file that should not be loaded into context. More efficient than vnsh_share for large or binary files.",
 "inputSchema": {
  "properties": {
   "file_path": {
    "description": "Absolute path to the file to encrypt and share",
    "type": "string"
   },
   "host": {
    "description": "Override the vnsh host URL",
    "type": "string"
   },
   "ttl": {
    "description": "Time-to-live in whole hours (default: 24, max: 168)",
    "maximum": 168,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "file_path"
  ],
  "type": "object"
 },
 "name": "vnsh_share_file"
}
