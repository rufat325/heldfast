{
 "description": "Full-text search across all logged AI conversations. Returns matching sessions with excerpts. Uses SQLite FTS5 with porter stemming \u2014 supports phrases (\"exact match\"), boolean (term1 OR term2), and prefix (migrat*).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max sessions to return (default: 10)",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "query": {
    "description": "FTS5 search query, e.g. \"database migration\" or \"auth bug\" or \"deploy OR release\"",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_sessions"
}
