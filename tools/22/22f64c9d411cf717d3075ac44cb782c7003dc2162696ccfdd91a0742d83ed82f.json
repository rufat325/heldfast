{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Dequeue the oldest envelope from a FIFO queue (returns null if empty). Set wait (max 25s) to long-poll for the next envelope.",
 "inputSchema": {
  "properties": {
   "queue": {
    "description": "Queue name",
    "type": "string"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   },
   "wait": {
    "description": "Long-poll seconds, 0-25 (default: 0 = return immediately)",
    "type": "number"
   }
  },
  "required": [
   "spaceId",
   "queue"
  ],
  "type": "object"
 },
 "name": "queue_pop",
 "outputSchema": {
  "properties": {
   "empty": {
    "description": "True when the queue was empty",
    "type": "boolean"
   },
   "encrypted": {
    "description": "True if the space is end-to-end encrypted (item is ciphertext)",
    "type": "boolean"
   },
   "item": {
    "description": "The dequeued envelope, or null if the queue was empty",
    "type": [
     "object",
     "null"
    ]
   },
   "queue": {
    "description": "Queue name",
    "type": "string"
   }
  },
  "required": [
   "queue"
  ],
  "type": "object"
 }
}
