{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": false
 },
 "description": "Clear the active page's storage \u2014 cookies, localStorage, IndexedDB, cache, service workers \u2014 for the current origin. Set allOrigins to wipe the whole browser session. Use to log out or test a fresh-user flow; reload afterward.",
 "inputSchema": {
  "properties": {
   "allOrigins": {
    "description": "Wipe the entire session, not just the current origin.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "browser_clear_storage"
}
