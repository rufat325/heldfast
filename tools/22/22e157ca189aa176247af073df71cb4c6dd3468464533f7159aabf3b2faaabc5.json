{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "\"What is the range of [CIDR]\" / \"how many hosts in a /24\" / \"network and broadcast address for [X]\" / \"subnet calculator\" \u2014 subnet arithmetic via HackerTarget, returning network address, broadcast address, usable host range, netmask and host count for a CIDR block. A pure calculation over the input; it does not probe the network.",
 "inputSchema": {
  "examples": [
   {
    "target": "192.168.1.0/24"
   }
  ],
  "properties": {
   "target": {
    "description": "CIDR block or address with prefix, e.g. \"192.168.1.0/24\".",
    "type": "string"
   }
  },
  "required": [
   "target"
  ],
  "type": "object"
 },
 "name": "subnet_lookup",
 "title": "Subnet Lookup",
 "version": "v1.2fe07d02"
}
