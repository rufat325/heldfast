{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search Datafiniti's US property database (100M+ records) \u2014 returns address, square footage, bed/bath counts, property type, ownership (people), and tax/assessment data. Uses the Datafiniti field:value query language. Example: datafiniti_property_search({ query: \"country:US AND city:\\\"Austin\\\" AND numBedroom:3\", num_records: 5, _apiKey: \"your-token\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-datafiniti-api-key",
    "num_records": 10,
    "query": "country:US AND province:TX AND city:\"Austin\" AND numBedroom:3"
   },
   {
    "_apiKey": "your-datafiniti-api-key",
    "num_records": 5,
    "query": "country:US AND postalCode:90210 AND propertyType:\"Single Family\""
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Datafiniti API token (from https://portal.datafiniti.co/). Sent as Authorization: Bearer.",
    "type": "string"
   },
   "num_records": {
    "description": "Max records to return (default 10, set 0 to get only num_found at no credit cost).",
    "type": "number"
   },
   "query": {
    "description": "Datafiniti query, e.g. `country:US AND province:TX AND city:\"Austin\"`. Fields: country, province, city, postalCode, propertyType, numBedroom, numBathroom, address.",
    "type": "string"
   }
  },
  "required": [
   "query",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "datafiniti_property_search",
 "title": "Datafiniti Property Search",
 "version": "v1.fdd97bdf"
}
