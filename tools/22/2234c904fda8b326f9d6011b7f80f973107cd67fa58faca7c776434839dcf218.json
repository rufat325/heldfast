{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Return the authenticated AgentLot agent identity. Requires Bearer AgentLot API key.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_me",
 "title": "Get connected agent"
}
