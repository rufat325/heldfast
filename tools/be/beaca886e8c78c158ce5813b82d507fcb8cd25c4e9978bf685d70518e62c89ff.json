{
 "description": "Run validate_feature for every code in vision-state, ROADMAP, and folders, plus cross-cutting checks (orphan folders, dangling cross-refs, CHANGELOG references, journal index drift) and read-only external-reference staleness (kind:\"external\" links + xref: roadmap citations). external:true enables network resolution of github refs (off by default \u2014 github refs then emit XREF_RESOLUTION_SKIPPED). Returns the union of all findings.",
 "inputSchema": {
  "properties": {
   "apply": {
    "description": "With fix:true, write the fixes and re-validate. Default false (dry-run plan only).",
    "type": "boolean"
   },
   "external": {
    "description": "Resolve github external refs over the network (read-only). Default false: github refs degrade to XREF_RESOLUTION_SKIPPED.",
    "type": "boolean"
   },
   "external_prefixes": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "feature_json_mode": {
    "type": "boolean"
   },
   "fix": {
    "description": "COMP-MCP-VALIDATE-2: reconcile mechanical drift. Returns a fix plan under `reconcile` (dry-run unless apply:true). Local-provider only.",
    "type": "boolean"
   },
   "fix_classes": {
    "description": "Override the enabled fix classes: dangling_link, invalid_link_kind, status_fj_vision, partial_age, roadmap_status_rewrite, invalid_link_kind_repair. Default: the non-destructive set.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "validate_project"
}
