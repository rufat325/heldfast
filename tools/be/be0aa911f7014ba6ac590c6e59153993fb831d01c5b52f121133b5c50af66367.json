{
 "description": "Local helper \u2014 verify a Coinbase Commerce webhook payload using HMAC-SHA256. Pass the EXACT raw request body string (do not re-stringify the parsed JSON, byte-equivalence matters) and the X-CC-Webhook-Signature header value. The shared secret comes from COINBASE_COMMERCE_WEBHOOK_SECRET unless overridden. Returns { valid: boolean }.",
 "inputSchema": {
  "properties": {
   "raw_body": {
    "description": "Exact raw request body bytes as a string",
    "type": "string"
   },
   "secret": {
    "description": "Override webhook shared secret. Defaults to COINBASE_COMMERCE_WEBHOOK_SECRET env var.",
    "type": "string"
   },
   "signature": {
    "description": "X-CC-Webhook-Signature header value (hex)",
    "type": "string"
   }
  },
  "required": [
   "raw_body",
   "signature"
  ],
  "type": "object"
 },
 "name": "verify_webhook_signature"
}
