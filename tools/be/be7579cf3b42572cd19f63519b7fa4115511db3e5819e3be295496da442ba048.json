{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Cut a log down to a CHARACTER budget while keeping what matters: errors with their full stack trace, the window of what happened just before, and anything that appears only once or twice. Repeats are collapsed with their count ([xN]) and gaps are marked ([...N]). Use it to hand a big log to a model without spending the context on the same line repeated.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "context": {
    "default": 3,
    "description": "Events kept before each error",
    "maximum": 50,
    "minimum": 0,
    "type": "integer"
   },
   "format": {
    "description": "Force the format instead of detecting it",
    "enum": [
     "json",
     "logfmt",
     "syslog5424",
     "syslog3164",
     "clf",
     "combined",
     "plain"
    ],
    "type": "string"
   },
   "from": {
    "default": "end",
    "description": "Which end to read if the file is over 32 MB",
    "enum": [
     "start",
     "end"
    ],
    "type": "string"
   },
   "fullTraces": {
    "default": true,
    "description": "false collapses each stack trace to its one-line summary",
    "type": "boolean"
   },
   "maxChars": {
    "default": 12000,
    "description": "Character budget for the output",
    "maximum": 400000,
    "minimum": 200,
    "type": "integer"
   },
   "path": {
    "description": "Path to the log file. Use this for big logs: they do not fit in a tool call.",
    "type": "string"
   },
   "text": {
    "description": "The log as text. Alternative to `path`.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "log_reduce"
}
