{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List campaign recipients"
 },
 "description": "List the individual recipients of a campaign and what happened to each message. Use this to put names behind the aggregate numbers from nevent_get_campaign_metrics: who bounced, who clicked, who unsubscribed. Filter with status (SCHEDULED, DELIVERED, OPENED, CLICKED, BOUNCES, UNSUBSCRIBES), narrow to one audience with segment_id when the campaign targeted several segments, or find one person with search (matches name and email). Returns a paginated envelope \u2014 content (the recipient rows), page, size, totalElements, totalPages. Rows contain personal data: request the smallest page that answers the question, filter rather than paginate through everything, and do not dump full recipient lists into a summary.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "campaign_id": {
    "description": "Campaign ID. Get it from nevent_list_campaigns, or from the response of nevent_create_campaign.",
    "minLength": 1,
    "type": "string"
   },
   "page": {
    "description": "Zero-based page number. Default: 0 (first page).",
    "minimum": 0,
    "type": "integer"
   },
   "page_size": {
    "description": "Recipients per page (1-100). Default: 25. Keep this small \u2014 recipient rows contain personal data and large pages waste context.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "search": {
    "description": "Free-text search across recipient name and email address.",
    "type": "string"
   },
   "segment_id": {
    "description": "Restrict the listing to recipients that came from one segment. Useful on multi-segment campaigns to compare which segment engaged. Get segment IDs from nevent_list_segments.",
    "type": "string"
   },
   "status": {
    "description": "Filter recipients by delivery state. SCHEDULED = queued but not sent; DELIVERED = accepted by the receiving server; OPENED / CLICKED = engaged; BOUNCES = delivery failed; UNSUBSCRIBES = opted out from this send. Omit to return every recipient.",
    "enum": [
     "SCHEDULED",
     "DELIVERED",
     "OPENED",
     "CLICKED",
     "BOUNCES",
     "UNSUBSCRIBES"
    ],
    "type": "string"
   }
  },
  "required": [
   "campaign_id"
  ],
  "type": "object"
 },
 "name": "nevent_list_campaign_recipients"
}
