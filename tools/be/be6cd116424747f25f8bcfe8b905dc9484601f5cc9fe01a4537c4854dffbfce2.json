{
 "description": "Type text into the currently focused text field in the simulator. Make sure a text field is focused first (tap on it).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "deviceId": {
    "description": "Device UDID, name, or \"booted\" (default: booted)",
    "type": "string"
   },
   "text": {
    "description": "Text to type into the currently focused field",
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "simulator_type_text"
}
