{
 "description": "Google publishes spend and impression ranges only for election ads; commercial advertisers omit both. isActive is true when lastShown is within 7 UTC days. Costs 2 credits. Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "advertiser": {
    "description": "Advertiser name, domain (e.g. nike.com), or Google advertiser ID (AR\u2026). Prefer AR\u2026 from advertiser-search.",
    "type": "string"
   },
   "cache": {
    "description": "Set true to serve from the 24h response cache (0 credits on hit). Default false \u2014 always fetch fresh. Envelope includes cached + cachedAt on hits.",
    "type": "boolean"
   },
   "country": {
    "description": "Two-letter ISO country / region code (soft filter). Default US. Alias: region.",
    "type": "string"
   },
   "cursor": {
    "description": "Pagination cursor from nextCursor.",
    "type": "string"
   },
   "end_date": {
    "description": "YYYY-MM-DD \u2014 keep creatives whose shown window overlaps this end.",
    "type": "string"
   },
   "limit": {
    "description": "Max items to return (default 20, max 200). Flat 2 credits when results are returned; 0 credits when totalReturned is 0.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "region": {
    "description": "Alias for country.",
    "type": "string"
   },
   "sort": {
    "description": "Client-side sort: last_shown (recent activity first) or first_shown. Default is ATC order.",
    "type": "string"
   },
   "start_date": {
    "description": "YYYY-MM-DD \u2014 keep creatives whose shown window overlaps this start.",
    "type": "string"
   },
   "topic": {
    "description": "Only \"all\" is supported (commercial ATC). Google publishes spend and impression ranges only for election ads; commercial advertisers omit both. isActive is true when lastShown is within 7 UTC days.",
    "type": "string"
   }
  },
  "required": [
   "advertiser"
  ],
  "type": "object"
 },
 "name": "google_ad_library_company_ads",
 "title": "Google Company Ads"
}
