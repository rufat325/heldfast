{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List ads within an ad group (`ad_group_id` is required). Each ad holds a creative (title, body, target URL, image) and a `review_status` (in_review, approved, or rejected). Supports cursor pagination (`order`, `after`/`before`; the response includes `first_id`, `last_id`, and `has_more`).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "ad_group_id": {
    "description": "Parent ad group ID. Required \u2014 ads are listed within an ad group.",
    "minLength": 1,
    "type": "string"
   },
   "after": {
    "description": "Pagination cursor. Pass the `last_id` from the previous page to fetch the next page.",
    "type": "string"
   },
   "before": {
    "description": "Pagination cursor. Pass the `first_id` from the previous page to fetch the previous page.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of objects to return per page (1-500). Uses the API default if omitted.",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "order": {
    "description": "Sort by creation time: 'asc' for oldest-first, 'desc' for newest-first.",
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   }
  },
  "required": [
   "ad_group_id"
  ],
  "type": "object"
 },
 "name": "list_ads",
 "title": "List ads"
}
