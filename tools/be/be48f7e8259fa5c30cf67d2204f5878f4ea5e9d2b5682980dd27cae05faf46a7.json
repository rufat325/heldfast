{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Rename or change tier"
 },
 "description": "Atomically rename a roost's URL, change gallery listing, or bind a custom domain (Roost / Roost Audit after checkout grant custom_domain). Pass `hatchId` plus at least one of `newPreferredSlug`, `galleryListed`, or `customDomain`. Do NOT call `hatch` again to rename \u2014 that creates a second site.\n\nRoost and Roost Audit include 10 subscription Pins. `convert` with `newTier: forever` is allowed while those slots remain. After that, call `checkout` with grant `publish` (Pin, $4.99/yr). Do not use convert to collect payment.\n\nPins are hidden from the carousel by default; pass `galleryListed: true` to feature the site publicly.",
 "inputSchema": {
  "properties": {
   "customDomain": {
    "description": "Customer-owned hostname. Roost / Roost Audit only, after checkout grant custom_domain (+$29/mo). Pass an empty string to clear.",
    "type": "string"
   },
   "galleryListed": {
    "description": "Opt in (`true`) or out (`false`) of the public Barnyard carousel. Pins require `true` to appear; free is listed unless `false`. Paid Roost hatches (`tier: workspace`) are never listed.",
    "type": "boolean"
   },
   "hatchId": {
    "description": "Hatch id from the original hatch response.",
    "type": "string"
   },
   "newPreferredSlug": {
    "description": "New hostname label. The old slug is released atomically.",
    "type": "string"
   },
   "newTier": {
    "description": "Roost / Roost Audit may pass `forever` while subscription Pins remain (10). After the allotment, use `checkout` (grant: publish) for a paid Pin.",
    "enum": [
     "free",
     "forever"
    ],
    "type": "string"
   },
   "sessionToken": {
    "description": "Optional paired session token from poll_pairing (when Authorization headers are unavailable).",
    "type": "string"
   },
   "tenantId": {
    "description": "Legacy alias for `hatchId`.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "convert",
 "title": "Rename or change tier"
}
