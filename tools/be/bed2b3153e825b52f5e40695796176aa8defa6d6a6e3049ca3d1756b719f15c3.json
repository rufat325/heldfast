{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch any public URL and return clean, structured content: title, meta description, main text, markdown, headings, outbound links and page metadata. Removes scripts, navigation and boilerplate. Ideal for reading articles, docs and product pages before reasoning over them. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "include_links": {
    "type": "boolean"
   },
   "output_format": {
    "type": "string"
   },
   "render_fallback": {
    "type": "boolean"
   },
   "url": {
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "agentbit.web_extract",
 "title": "Web Extract"
}
