{
 "description": "Get the grouping hashes (fingerprint) for a specific issue. Useful to identify why issues are grouped together or to deduplicate across projects.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "issue_id": {
    "description": "Numeric issue ID",
    "type": "string"
   }
  },
  "required": [
   "issue_id"
  ],
  "type": "object"
 },
 "name": "get_issue_hashes"
}
