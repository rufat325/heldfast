{
 "description": "Resolve a template's outline, defaults, and boilerplate without modifying data. Use list_templates for IDs.",
 "inputSchema": {
  "properties": {
   "template_id": {
    "description": "The template ID to apply (required)",
    "type": "string"
   }
  },
  "required": [
   "template_id"
  ],
  "type": "object"
 },
 "name": "apply_template"
}
