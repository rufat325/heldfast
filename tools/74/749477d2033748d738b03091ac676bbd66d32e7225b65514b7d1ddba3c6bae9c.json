{
 "annotations": {
  "readOnlyHint": true,
  "title": "Compare providers"
 },
 "description": "UNDERSTANDING \u2014 Compare 2+ providers side by side: composite/band, per-facet leaders, and an artifact-coverage matrix. Priced B2 (cross-catalog synthesis) \u2014 $0.05 per call under pay-as-you-go; included in Understanding and Influence. See apis://prices.",
 "inputSchema": {
  "properties": {
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "providers": {
    "description": "Provider slugs to compare (2+).",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "providers"
  ],
  "type": "object"
 },
 "name": "compare_providers"
}
