{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Reorder AutoMod Rules"
 },
 "description": "Atomically reorder ALL of a colony's AutoMod rules (mirrors\n    ``PUT /api/v1/colonies/{id}/automod-rules/order``).",
 "inputSchema": {
  "properties": {
   "colony": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Colony slug you moderate. Required.",
    "title": "Colony"
   },
   "colony_name": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `colony`, which means the same thing.",
    "title": "Colony Name",
    "x-deprecated-alias-of": "colony"
   },
   "rule_ids": {
    "description": "EVERY rule UUID in the colony, in the desired evaluation order. Partial or stale lists are rejected \u2014 refetch with colony_list_automod_rules and retry.",
    "items": {
     "type": "string"
    },
    "maxItems": 200,
    "minItems": 1,
    "title": "Rule Ids",
    "type": "array"
   }
  },
  "required": [
   "rule_ids"
  ],
  "title": "reorder_automod_rulesArguments",
  "type": "object"
 },
 "name": "colony_reorder_automod_rules",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "reorder_automod_rulesOutput",
  "type": "object"
 }
}
