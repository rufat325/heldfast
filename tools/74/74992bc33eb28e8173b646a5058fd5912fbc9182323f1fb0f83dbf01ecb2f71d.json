{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "VAERS report counts and severity breakdown (died / life-threatening / hospitalized / disabled / ER visit) by vaccine, for the requested year range. Pass `vaccine` as the VAERS vax_type code (e.g. \"COVID19\", \"FLU4\", \"HPV9\", \"VARCEL\", \"MMR\") to get that vaccine's counts; omit it to browse the top vaccines ranked by report volume, which doubles as a way to discover valid codes. Optionally narrow to one `manufacturer` (substring match, e.g. \"PFIZER\", \"MODERNA\"). Answers \"how many VAERS reports mention the COVID-19 vaccine\", \"which vaccine has the most reported deaths in VAERS\", \"Pfizer vs Moderna VAERS report counts\". IMPORTANT: a report count is NOT evidence the vaccine caused the event \u2014 VAERS accepts any report from anyone, unverified. Every response carries the CDC/FDA disclaimer and `is_causal: false`. This never returns a single report's free text.",
 "inputSchema": {
  "examples": [
   {
    "vaccine": "COVID19",
    "year_from": 2021,
    "year_to": 2022
   },
   {
    "limit": 5
   }
  ],
  "properties": {
   "limit": {
    "description": "Max rows (1-200, default 20).",
    "type": [
     "number",
     "string"
    ]
   },
   "manufacturer": {
    "description": "Manufacturer substring, e.g. \"PFIZER\", \"MODERNA\", \"MERCK\". Optional.",
    "type": "string"
   },
   "vaccine": {
    "description": "VAERS vax_type code, e.g. \"COVID19\", \"FLU4\", \"HPV9\", \"MMR\", \"VARCEL\". Omit to see the top vaccines by report count.",
    "type": "string"
   },
   "year_from": {
    "description": "Earliest report-received year to include (data covers 1990-2026).",
    "type": [
     "number",
     "string"
    ]
   },
   "year_to": {
    "description": "Latest report-received year to include.",
    "type": [
     "number",
     "string"
    ]
   }
  },
  "type": "object"
 },
 "name": "vaers_events_by_vaccine",
 "title": "Vaers Events By Vaccine",
 "version": "v1.99696cf1"
}
