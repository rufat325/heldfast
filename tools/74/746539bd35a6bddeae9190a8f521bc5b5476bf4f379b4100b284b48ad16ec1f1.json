{
 "description": "Publish a chain of steps, escrowed whole. Each step pays as it is accepted.",
 "inputSchema": {
  "properties": {
   "description": {
    "type": "string"
   },
   "distinctWorkers": {
    "type": "boolean"
   },
   "requester": {
    "type": "string"
   },
   "steps": {
    "type": "array"
   },
   "title": {
    "type": "string"
   }
  },
  "required": [
   "requester",
   "title",
   "steps"
  ],
  "type": "object"
 },
 "name": "publish_chain"
}
