{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Add Portfolio Lot"
 },
 "description": "Record a user-confirmed purchase or sale in the caller's portfolio. Never infer or guess the instrument, quantity, price or date. For stock, pass ticker. For an option, pass both its underlying ticker and verified OCC symbol; unknown or expired contracts are rejected. quantity is signed: positive for long shares/contracts, negative for short stock or written options. costPerUnit is always the positive per-share price or option premium, never the per-contract total. Identical retries within the duplicate window return the existing lot instead of adding another.",
 "inputSchema": {
  "properties": {
   "acquiredDate": {
    "description": "The trade date, as yyyy-MM-dd. Cannot be in the future.",
    "type": "string"
   },
   "costPerUnit": {
    "description": "Price per share, always positive: paid for a long, received for a short. For an option, the premium per share (a $3.20 contract is 3.20, not 320).",
    "type": "number"
   },
   "note": {
    "default": null,
    "description": "Optional. A short note from the user about this lot, up to 256 characters.",
    "type": "string"
   },
   "optionContract": {
    "default": null,
    "description": "Optional. The OCC option symbol, e.g. O:AAPL260724C00110000. Provide it to record an option contract; omit it for shares.",
    "type": "string"
   },
   "portfolio": {
    "description": "The portfolio to add to, by name.",
    "type": "string"
   },
   "quantity": {
    "description": "Signed size: shares for a stock, contracts for an option. Negative for a short sale or a written option.",
    "type": "number"
   },
   "ticker": {
    "description": "Stock ticker. For an option, the UNDERLYING ticker, e.g. AAPL.",
    "type": "string"
   }
  },
  "required": [
   "portfolio",
   "ticker",
   "quantity",
   "costPerUnit",
   "acquiredDate"
  ],
  "type": "object"
 },
 "name": "AddPortfolioLot",
 "title": "Add Portfolio Lot"
}
