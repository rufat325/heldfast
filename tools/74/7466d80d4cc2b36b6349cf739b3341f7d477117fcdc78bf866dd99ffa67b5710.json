{
 "description": "Register an AI agent for Clicks settlement routing. Required before the first routed payment.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "agent_address": {
    "description": "Ethereum address of the AI agent to register",
    "pattern": "^0x[0-9a-fA-F]{40}$",
    "type": "string"
   }
  },
  "required": [
   "agent_address"
  ],
  "type": "object"
 },
 "name": "clicks_register_agent"
}
