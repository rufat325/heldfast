{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search for real-time job listings with freshness badges on every result \u2014 so you never apply to a role that closed months ago. Sources: Remotive + RemoteOK + The Muse + HN 'Who is Hiring'. Supports location filtering, remote-only mode, keyword spotting (e.g. FIFO), and max age filtering. Returns timestamped freshcontext sorted freshest first.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "keywords": {
    "default": [],
    "description": "Keywords to highlight in results e.g. ['FIFO', 'underground', 'contract']",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "location": {
    "description": "Country, city, or 'remote' / 'worldwide' e.g. 'South Africa', 'Australia', 'remote'",
    "type": "string"
   },
   "max_age_days": {
    "default": 60,
    "description": "Hide listings older than N days (default 60, use 7 for very fresh only)",
    "type": "number"
   },
   "max_length": {
    "default": 8000,
    "type": "number"
   },
   "query": {
    "description": "Job search query e.g. 'typescript', 'mining engineer', 'FIFO operator', 'data analyst'",
    "type": "string"
   },
   "remote_only": {
    "default": false,
    "description": "Only return remote-friendly listings",
    "type": "boolean"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_jobs"
}
