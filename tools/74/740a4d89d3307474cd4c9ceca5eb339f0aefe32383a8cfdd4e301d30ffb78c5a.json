{
 "description": "Record the result of a tool call as a first-class audit event (event_type=tool_result). Call immediately after receiving tool output. Links back to log_tool_call via tool_call_id.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "exit_code": {
    "description": "Exit code for shell tools (0 = success)",
    "type": "integer"
   },
   "output": {
    "description": "Tool output (stdout, file content, API response, etc.)",
    "type": "string"
   },
   "session_id": {
    "description": "Session ID",
    "type": "string"
   },
   "tool_call_id": {
    "description": "tool_call_id returned by log_tool_call",
    "type": "string"
   }
  },
  "required": [
   "session_id",
   "tool_call_id",
   "output"
  ],
  "type": "object"
 },
 "name": "log_tool_result"
}
