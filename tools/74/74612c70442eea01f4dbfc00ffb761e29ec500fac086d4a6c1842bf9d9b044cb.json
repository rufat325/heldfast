{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Blast radius ACROSS the federated workspace: impact in this repo plus which member repos consume it over confidence-scored bridges (workspace/package links). Every cross-repo hop carries confidence, evidence, and the member graph identity.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "max_members": {
    "description": "member repos to inspect (default 4)",
    "type": "number"
   },
   "name": {
    "type": "string"
   },
   "pick": {
    "description": "nth candidate if ambiguous",
    "type": "number"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "cross_impact_of"
}
