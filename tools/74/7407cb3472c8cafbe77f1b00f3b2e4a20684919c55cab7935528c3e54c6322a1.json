{
 "description": "Dismiss a broadcast notification so it no longer appears in the inbox.",
 "inputSchema": {
  "properties": {
   "broadcastId": {
    "description": "ID of the broadcast to dismiss.",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "broadcastId"
  ],
  "type": "object"
 },
 "name": "dismiss_broadcast"
}
