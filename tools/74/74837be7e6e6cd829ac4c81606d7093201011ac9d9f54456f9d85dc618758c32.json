{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Search Companies"
 },
 "description": "List HireJack's tracked companies with hiring volume. Filter by name/domain (`q`) or industry. Returns slim records with domain, name, industry, total open jobs, and engineering job count. Use this to find companies in a sector ('which fintech companies are hiring?') or to resolve a company name to its domain before calling get_company_profile. Not for deep per-company data (`get_company_profile`) or multi-axis segmentation by trend/skill/job-count (`find_companies`, Analyst).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "industry": {
    "description": "Industry substring filter (case-insensitive). Examples: 'fintech', 'health tech', 'gaming', 'defense', 'AI'.",
    "type": "string"
   },
   "limit": {
    "description": "Max companies to return (1-200, default 50)",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "description": "Company name or domain substring (case-insensitive). Use this to resolve a company name to its domain, e.g. q='stripe' \u2192 'stripe.com'. Combines with `industry` (AND).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_companies",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "citation_url": {
    "description": "hirejack.com URL to cite when surfacing this result",
    "type": "string"
   },
   "data": {
    "additionalProperties": true,
    "description": "Slim company records; meta.fetchedAt stamps the embedded snapshot",
    "properties": {
     "companies": {
      "description": "Sorted by total open jobs descending",
      "items": {
       "additionalProperties": true,
       "properties": {
        "domain": {
         "description": "Company domain \u2014 pass to get_company_profile / company_fit / watch_company",
         "type": "string"
        },
        "engineering_jobs": {
         "type": "number"
        },
        "engineering_pct": {
         "description": "Engineering share of open roles, 0-100",
         "type": "number"
        },
        "industry": {
         "type": "string"
        },
        "name": {
         "type": "string"
        },
        "profile_url": {
         "description": "hirejack.com company page for citation",
         "type": "string"
        },
        "total_jobs": {
         "description": "Total open postings tracked",
         "type": "number"
        }
       },
       "type": "object"
      },
      "type": "array"
     },
     "count": {
      "type": "number"
     }
    },
    "required": [
     "companies",
     "count"
    ],
    "type": "object"
   },
   "meta": {
    "additionalProperties": true,
    "description": "Request/response metadata (pagination cursors, snapshot timestamps, applied filters)",
    "properties": {},
    "type": "object"
   }
  },
  "required": [
   "data"
  ],
  "type": "object"
 },
 "title": "Search Companies"
}
