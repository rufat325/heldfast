{
 "description": "Execute a Move function call. Arguments are passed as an array of strings/numbers.",
 "inputSchema": {
  "properties": {
   "arguments": {
    "description": "Function arguments",
    "items": {},
    "type": "array"
   },
   "target": {
    "description": "Move call target: package::module::function",
    "type": "string"
   },
   "typeArguments": {
    "description": "Type arguments for generic functions",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "wallet": {
    "description": "Wallet name for signing",
    "type": "string"
   }
  },
  "required": [
   "wallet",
   "target"
  ],
  "type": "object"
 },
 "name": "move_call"
}
