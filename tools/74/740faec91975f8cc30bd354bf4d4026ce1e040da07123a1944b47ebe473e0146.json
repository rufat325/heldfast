{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the upcoming 15-day daily weather FORECAST for a location \u2014 per-day temp/min/max, humidity, precipitation chance, wind, and conditions. Example: forecast({ location: \"Berlin\" }).",
 "inputSchema": {
  "examples": [
   {
    "location": "Berlin"
   },
   {
    "location": "Sydney",
    "units": "metric"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Visual Crossing API key for higher limits; omit to use the shared Pipeworx key.",
    "type": "string"
   },
   "location": {
    "description": "City name (e.g. \"Berlin\") or \"lat,lon\".",
    "type": "string"
   },
   "units": {
    "description": "Unit group: \"metric\" (default), \"us\", or \"uk\".",
    "enum": [
     "metric",
     "us",
     "uk"
    ],
    "type": "string"
   }
  },
  "required": [
   "location"
  ],
  "type": "object"
 },
 "name": "forecast",
 "title": "Forecast",
 "version": "v1.353cf756"
}
