{
 "description": "Inspect an MCP server: list its tools/resources/prompts, call them, and report schema and protocol-compliance issues.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "headers": {
    "description": "Headers for remote transports, e.g. \"Authorization: Bearer x\".",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "target": {
    "description": "MCP server target: a launch command or an http(s) URL.",
    "type": "string"
   },
   "timeout": {
    "description": "Per-operation timeout in milliseconds (default 30000).",
    "type": "number"
   },
   "transport": {
    "description": "Force transport. Inferred from the target when omitted.",
    "enum": [
     "stdio",
     "sse",
     "http"
    ],
    "type": "string"
   }
  },
  "required": [
   "target"
  ],
  "type": "object"
 },
 "name": "probe_server",
 "title": "Probe MCP server"
}
