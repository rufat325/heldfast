{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the Copernicus Data Space Ecosystem STAC catalog for Sentinel satellite scenes covering an area and date range, with optional maximum cloud cover filter and sort by date or cloudiness. Returns scene IDs, acquisition time, cloud cover, and a quicklook thumbnail URL for each match. Data: ESA/EU Copernicus Sentinel program, public STAC catalog, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bbox": {
    "description": "Bounding box [min_lon, min_lat, max_lon, max_lat] in WGS84 decimal degrees. Provide this OR lat + lon.",
    "items": {
     "type": "number"
    },
    "maxItems": 4,
    "minItems": 4,
    "type": "array"
   },
   "collection": {
    "description": "STAC collection ID to search (default \"sentinel-2-l2a\"). Other common values: \"sentinel-1-grd\" (radar), \"sentinel-2-l1c\" (top-of-atmosphere optical), \"sentinel-3-olci-2-wfr-nrt\" (ocean color). See copernicus-sentinel.list_collections for the full list.",
    "minLength": 1,
    "type": "string"
   },
   "end_date": {
    "description": "End of the acquisition date range, format YYYY-MM-DD.",
    "type": "string"
   },
   "lat": {
    "description": "Latitude of a point of interest. Use with \"lon\" (and optional \"radius_km\") instead of bbox.",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "limit": {
    "description": "Maximum number of scenes to return (default 10, max 50).",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "lon": {
    "description": "Longitude of a point of interest. Use with \"lat\" (and optional \"radius_km\") instead of bbox.",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "max_cloud_cover": {
    "description": "Maximum acceptable cloud cover percentage (0-100). Applies to optical collections like Sentinel-2; omit for radar collections such as Sentinel-1.",
    "maximum": 100,
    "minimum": 0,
    "type": "number"
   },
   "radius_km": {
    "description": "Search radius in kilometers around lat/lon (default 20, max 500). Ignored if bbox is given.",
    "maximum": 500,
    "minimum": 1,
    "type": "number"
   },
   "sort": {
    "description": "Sort order: \"date_desc\" (newest first, default) or \"cloud_asc\" (least cloudy first).",
    "enum": [
     "date_desc",
     "cloud_asc"
    ],
    "type": "string"
   },
   "start_date": {
    "description": "Start of the acquisition date range, format YYYY-MM-DD.",
    "type": "string"
   }
  },
  "required": [
   "start_date",
   "end_date"
  ],
  "type": "object"
 },
 "name": "copernicus-sentinel.imagery.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Search Sentinel Satellite Scenes"
}
