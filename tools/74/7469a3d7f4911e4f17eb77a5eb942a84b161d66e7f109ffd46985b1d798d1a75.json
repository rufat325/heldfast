{
 "description": "Start a Founden build from a plain-language prompt. Cost: 1 credit.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "company_id": {
    "description": "Target an existing company you own. Omit to use (or create) your default company.",
    "type": "string"
   },
   "prompt": {
    "description": "What to build or change, in plain language.",
    "type": "string"
   }
  },
  "required": [
   "prompt"
  ],
  "type": "object"
 },
 "name": "build_company"
}
