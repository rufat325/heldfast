{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Update an existing DNS record (partial update). Requires confirm:true (human-approval gate); without it returns a preview only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "comment": {
    "type": "string"
   },
   "confirm": {
    "description": "Human-approval gate: must be true to actually perform this mutating operation. Omit or set false to get a non-executing preview of what would happen (with secrets redacted). A human should approve before this is set to true.",
    "type": "boolean"
   },
   "content": {
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "priority": {
    "type": "number"
   },
   "proxied": {
    "type": "boolean"
   },
   "record_id": {
    "description": "DNS record ID (from list_dns_records).",
    "type": "string"
   },
   "ttl": {
    "type": "number"
   },
   "type": {
    "description": "DNS record type.",
    "enum": [
     "A",
     "AAAA",
     "CNAME",
     "TXT",
     "MX",
     "NS",
     "SRV",
     "CAA",
     "PTR",
     "SPF"
    ],
    "type": "string"
   },
   "zone_id": {
    "description": "The Cloudflare zone ID (32-char hex). Get it from list_zones.",
    "type": "string"
   }
  },
  "required": [
   "zone_id",
   "record_id"
  ],
  "type": "object"
 },
 "name": "update_dns_record"
}
