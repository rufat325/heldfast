{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find georeferenced marine occurrence records (latitude, longitude, date, depth, country, locality, basis of record) for a scientific name from OBIS. Optionally filter by date range. Returns the total match count plus a sample of records.",
 "inputSchema": {
  "examples": [
   {
    "limit": 50,
    "scientificname": "Orcinus orca"
   },
   {
    "enddate": "2023-12-31",
    "limit": 100,
    "scientificname": "Calanus finmarchicus",
    "startdate": "2010-01-01"
   }
  ],
  "properties": {
   "enddate": {
    "description": "Latest event date, YYYY-MM-DD (optional).",
    "type": "string"
   },
   "limit": {
    "description": "Max records to return (default 20, max 100).",
    "type": "number"
   },
   "scientificname": {
    "description": "Scientific name to search occurrences for, e.g. \"Orcinus orca\".",
    "type": "string"
   },
   "startdate": {
    "description": "Earliest event date, YYYY-MM-DD (optional).",
    "type": "string"
   }
  },
  "required": [
   "scientificname"
  ],
  "type": "object"
 },
 "name": "find_occurrences",
 "title": "Find Occurrences",
 "version": "v1.888996db"
}
