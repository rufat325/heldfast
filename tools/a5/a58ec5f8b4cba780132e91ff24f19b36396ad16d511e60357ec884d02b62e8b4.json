{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Crawl a website and return all discovered URLs (up to 5,000). Optionally filter by a search term to rank discovered links by relevance. Returns {url, count, links[]}.",
 "inputSchema": {
  "examples": [
   {
    "url": "https://example.com"
   },
   {
    "limit": 50,
    "search": "api reference",
    "url": "https://docs.example.com"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Firecrawl API key for higher limits; omit to use the shared Pipeworx key.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of URLs to return (default 100, max 5000).",
    "type": "number"
   },
   "search": {
    "description": "Optional search term to filter discovered URLs by relevance.",
    "type": "string"
   },
   "url": {
    "description": "The website URL to map, e.g. \"https://example.com\"",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "map",
 "title": "Map",
 "version": "v1.4efaf9b3"
}
