{
 "description": "Read the organization's plan, limits and global entitlements. Read this before a bulk send: `state.active` says whether sending is allowed and `warning` says when the plan runs out. A new organization's trial starts with its first domain or send (`state.started`).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_billing"
}
