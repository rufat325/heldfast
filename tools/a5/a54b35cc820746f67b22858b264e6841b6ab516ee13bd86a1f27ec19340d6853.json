{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search UK Companies House by company name. Returns matches with company number, name, status (active/dissolved), type, registered office address, and incorporation date. Use the company_number to look up officers, filings, and PSCs.",
 "inputSchema": {
  "examples": [
   {
    "query": "Apple"
   },
   {
    "items_per_page": 10,
    "query": "Tesco PLC",
    "start_index": 0
   }
  ],
  "properties": {
   "items_per_page": {
    "description": "1-100 (default 20)",
    "type": "number"
   },
   "query": {
    "description": "Company name (full or partial)",
    "type": "string"
   },
   "start_index": {
    "description": "Pagination offset (default 0)",
    "type": "number"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_companies",
 "outputSchema": {
  "properties": {
   "companies": {
    "description": "List of matching companies",
    "items": {
     "properties": {
      "address": {
       "description": "Registered office address snippet",
       "type": [
        "string",
        "null"
       ]
      },
      "ch_url": {
       "description": "Link to company profile on Companies House website",
       "type": [
        "string",
        "null"
       ]
      },
      "company_number": {
       "description": "UK company number",
       "type": [
        "string",
        "null"
       ]
      },
      "incorporated_on": {
       "description": "Incorporation date (ISO format)",
       "type": [
        "string",
        "null"
       ]
      },
      "name": {
       "description": "Company name",
       "type": [
        "string",
        "null"
       ]
      },
      "status": {
       "description": "Company status (active, dissolved, etc.)",
       "type": [
        "string",
        "null"
       ]
      },
      "type": {
       "description": "Company type (private limited, public limited, etc.)",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "returned": {
    "description": "Number of companies returned in this response",
    "type": "number"
   },
   "total": {
    "description": "Total number of companies matching the search query",
    "type": "number"
   }
  },
  "required": [
   "total",
   "returned",
   "companies"
  ],
  "type": "object"
 },
 "title": "Search Companies",
 "version": "v1.01cf235a"
}
