{
 "description": "Aurora agent surface (no LLM cost \u2014 pure DB write). Only relevant when managing Aurora agents. Archive an agent (soft-delete). The agent remains in the database but is hidden from default listings. Address by agentId OR exact title (XOR).",
 "inputSchema": {
  "properties": {
   "agentId": {
    "description": "MongoDB agent _id to archive (XOR with title)",
    "type": "string"
   },
   "title": {
    "description": "Exact case-insensitive user-assigned title (XOR with agentId)",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "archive_agent"
}
