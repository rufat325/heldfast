{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List call history. Requires account linking (a proto-key with no linked user returns 401 \u2014 call get_account_link_url). Inbound history requires a reserved-number plan.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "direction": {
    "description": "Optional: inbound or outbound",
    "enum": [
     "inbound",
     "outbound"
    ],
    "type": "string"
   },
   "limit": {
    "default": 25,
    "description": "Max results, default 25, 1-200",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "since": {
    "description": "Optional ISO 8601 timestamp lower bound",
    "format": "date-time",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_calls"
}
