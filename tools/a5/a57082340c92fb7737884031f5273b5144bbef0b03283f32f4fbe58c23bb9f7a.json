{
 "description": "Return the most recent price observations for one service, oldest-newest by `observedAt`. Useful when an agent wants to spot intraday trends or audit the inputs to FMV.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "limit": {
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "since": {
    "format": "date-time",
    "type": "string"
   },
   "slug": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "recent_observations",
 "title": "Recent observations for a service"
}
