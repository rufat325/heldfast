{
 "description": "Execute JavaScript in the current page context and return the result. Use await_promise=true (default) for async expressions.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "await_promise": {
    "description": "Set to \"false\" to skip awaiting a returned Promise (default: true)",
    "type": "string"
   },
   "expression": {
    "description": "JavaScript expression to evaluate",
    "type": "string"
   }
  },
  "required": [
   "expression"
  ],
  "type": "object"
 },
 "name": "browser_evaluate"
}
