{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Cask install counts.",
 "inputSchema": {
  "examples": [
   {
    "cask": "docker",
    "days": 30
   }
  ],
  "properties": {
   "cask": {
    "type": "string"
   },
   "days": {
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "analytics_cask_install",
 "outputSchema": {
  "oneOf": [
   {
    "properties": {
     "cask": {
      "description": "Cask name queried",
      "type": "string"
     },
     "data": {
      "description": "Install count data for the cask or null if not found"
     }
    },
    "required": [
     "cask",
     "data"
    ],
    "type": "object"
   },
   {
    "description": "Top install counts across all casks for the period",
    "type": "object"
   }
  ]
 },
 "title": "Analytics Cask Install",
 "version": "v1.67978673"
}
