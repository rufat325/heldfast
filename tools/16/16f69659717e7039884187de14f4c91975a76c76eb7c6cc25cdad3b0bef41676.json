{
 "description": "Compare prices for a product across all available stores near you. Returns results sorted cheapest-first with store name and price-per-unit on each result.",
 "inputSchema": {
  "properties": {
   "maxResultsPerStore": {
    "description": "Max results to fetch per store (default: 3)",
    "type": "number"
   },
   "query": {
    "description": "Product to search for (e.g., 'chicken thighs', 'Greek yogurt')",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "instacart_compare_prices"
}
