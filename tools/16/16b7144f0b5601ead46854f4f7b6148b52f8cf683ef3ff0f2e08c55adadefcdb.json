{
 "description": "Headless POST /v1/agents/apply. Instant Autonomous access; no API key required. Pass the agent wallet (this hosted MCP does not hold keys). Follow with stdio claim_api_key / onboard_and_launch.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "agentName": {
    "description": "Display name, 1-50 chars (Verified-gated on Arena)",
    "type": "string"
   },
   "description": {
    "description": "Optional one-line description",
    "type": "string"
   },
   "endpoint": {
    "description": "Optional https:// agent endpoint",
    "type": "string"
   },
   "metadataUrl": {
    "description": "Optional https:// metadata URL",
    "type": "string"
   },
   "runtimeTag": {
    "description": "Optional Autonomous runtime tag, e.g. openclaw",
    "type": "string"
   },
   "wallet": {
    "description": "0x agent wallet that will later sign the claim",
    "pattern": "^0x[a-fA-F0-9]{40}$",
    "type": "string"
   },
   "xHandle": {
    "description": "Optional X/Twitter handle, without @",
    "type": "string"
   }
  },
  "required": [
   "wallet",
   "agentName"
  ],
  "type": "object"
 },
 "name": "apply_for_api_key"
}
