{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Run Percy Scan"
 },
 "description": "Run a Percy visual test scan. Example prompts : Run this Percy build/scan. Never run percy scan/build without this tool",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "integrationType": {
    "description": "Specifies whether to integrate with Percy Web or Percy Automate. If not explicitly provided, prompt the user to select the desired integration type.",
    "enum": [
     "web",
     "automate"
    ],
    "type": "string"
   },
   "percyRunCommand": {
    "description": "The test command to run with Percy. Optional \u2014 the LLM should try to infer it first from project context.",
    "type": "string"
   },
   "projectName": {
    "description": "The name of the project to run Percy on.",
    "type": "string"
   }
  },
  "required": [
   "projectName",
   "integrationType"
  ],
  "type": "object"
 },
 "name": "runPercyScan"
}
