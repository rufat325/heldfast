{
 "description": "Rebuild the search index (instance-wide, or one space if spaceKey is given). Resource-intensive and disrupts search while running. Requires confirm: true. See skill://instance-maintenance for real gotchas across this tool surface.",
 "inputSchema": {
  "properties": {
   "confirm": {
    "type": "boolean"
   },
   "spaceKey": {
    "type": "string"
   }
  },
  "required": [
   "confirm"
  ],
  "type": "object"
 },
 "name": "trigger_reindex"
}
