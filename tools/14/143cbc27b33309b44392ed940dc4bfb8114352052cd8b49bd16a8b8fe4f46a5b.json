{
 "description": "Alias for runShopifyQL \u2014 pass a raw ShopifyQL string in `query`. See runShopifyQL description for syntax.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "query": {
    "description": "Raw ShopifyQL query \u2014 bare dates, GROUP BY full keyword, verified datasets only",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "getCustomReport"
}
