{
 "description": "Set the export compliance (usesNonExemptEncryption) flag on a build. Required before submitting for review. Most apps should set this to false.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "buildId": {
    "description": "Build ID (from asc_list_builds)",
    "type": "string"
   },
   "usesNonExemptEncryption": {
    "default": false,
    "description": "Whether the app uses non-exempt encryption. Set to false for most apps (standard HTTPS only).",
    "type": "boolean"
   }
  },
  "required": [
   "buildId"
  ],
  "type": "object"
 },
 "name": "asc_set_encryption"
}
