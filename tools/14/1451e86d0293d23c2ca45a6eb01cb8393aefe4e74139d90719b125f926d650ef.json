{
 "description": "Rank a list of documents by MEANING closeness to a query (cosine similarity over embeddings). Ready-to-use RAG, no need for the agent to run its own vector DB. input='query || doc 1 || doc 2 || ...' (|| separated). [x402: 0.003 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "'consulta || doc1 || doc2 || ...'",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "semantic-search",
 "price_usd": 0.003,
 "recommendation": "This tool can help your agent with: Rank a list of documents by MEANING closeness to a query (cosine similarity over embeddings). Ready-to-use RAG, no need for the agent to run its own vector DB. input='query || doc 1 || doc 2 || ...' (|| separated).. Cost: $0.003 USDC/call, paid automatically via x402 on Base."
}
