{
 "annotations": {
  "destructiveHint": true,
  "title": "Generate Video"
 },
 "description": "Generate a video using AI (async job). Returns job ID to track progress. Costs credits.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "pipeline": {
    "description": "Pipeline type (e.g. kling_story, pixar_episode)",
    "type": "string"
   },
   "prompt": {
    "description": "Video description/script",
    "type": "string"
   }
  },
  "required": [
   "prompt"
  ],
  "type": "object"
 },
 "name": "generate_video"
}
