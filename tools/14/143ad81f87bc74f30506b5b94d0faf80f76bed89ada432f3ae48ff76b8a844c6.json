{
 "description": "\u8907\u6570\u306e\u30c6\u30b9\u30c8\u30d9\u30fc\u30b9\u6587\u66f8\u3092\u6a2a\u65ad\u5206\u6790\u3057\u3001\u8981\u4ef6ID\u4f53\u7cfb\u30fb\u6570\u91cf\u8868\u73fe\u306e\u77db\u76fe\u30fb\u5883\u754c\u5024\u5019\u88dc\u30fb\u7528\u8a9e\u306e\u8868\u8a18\u3086\u308c\u3092\u6c7a\u5b9a\u7684\u306b\u691c\u51fa\u3059\u308b\u3002\u54c1\u8cea\u7279\u6027\u30de\u30c3\u30d4\u30f3\u30b0\u3084\u30b9\u30c6\u30fc\u30af\u30db\u30eb\u30c0\u30fc\u5f71\u97ff\u30fb\u5909\u66f4\u5dee\u5206\u533a\u5206\u306f\u547c\u3073\u51fa\u3057\u5074LLM\u3078\u306e\u6307\u793a\u3068\u3057\u3066\u8fd4\u3059\u3002",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "additionalAmbiguousTerms": {
    "description": "Extra ambiguous/weak words to count in addition to the built-in list",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "alreadyAssured": {
    "description": "Areas already assured by other means",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "background": {
    "description": "Development background of the project",
    "type": "string"
   },
   "changeItems": {
    "description": "Change items to classify into the 4 change categories",
    "items": {
     "properties": {
      "category": {
       "description": "Change category; if omitted, the calling LLM should classify it",
       "enum": [
        "new",
        "modified",
        "existing-impacted",
        "existing-unaffected"
       ],
       "type": "string"
      },
      "description": {
       "description": "Change item description",
       "type": "string"
      },
      "note": {
       "description": "Additional note",
       "type": "string"
      }
     },
     "required": [
      "description"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "completedTools": {
    "description": "Already executed tools; entries without evidence are treated as not executed",
    "items": {
     "properties": {
      "evidence": {
       "description": "Evidence of execution: an output file path (e.g. docs/test-cases.md) or an output heading line starting with '#'",
       "type": "string"
      },
      "outputExcerpt": {
       "description": "Excerpt of the declared tool's actual output text. Must contain that tool's own H1 heading line; otherwise the declaration is reported as evidence-unverified and still counted as not executed",
       "type": "string"
      },
      "toolName": {
       "description": "Name of an already executed tool in this workflow",
       "type": "string"
      }
     },
     "required": [
      "toolName",
      "evidence"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "documents": {
    "description": "One or more test basis documents (requirements, specifications) to analyze",
    "items": {
     "properties": {
      "content": {
       "description": "Full text of the document (any format; caller converts binaries to text)",
       "type": "string"
      },
      "name": {
       "description": "Document name or file name of the requirements document",
       "type": "string"
      }
     },
     "required": [
      "name",
      "content"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "focusAreas": {
    "description": "Scope / focus areas to prioritize",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "idPatterns": {
    "description": "Extra regular expression sources for requirement/feature IDs, added to the default pattern. Capture group count decides how the ID is built: 1 group = group 1 is used as the whole ID as-is (no hyphen joining), 2 groups = reconstructed as `${group1}-${group2}` (default pattern behavior), 0 groups = the whole match is used. Use a 1-group pattern for numeric-only IDs (031), dot-separated IDs (3.1.2) and underscore IDs (REQ_001) so the reported ID matches the notation in the source document. If a given pattern matches nothing, a [high] finding is emitted in the input digest.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "outOfScope": {
    "description": "Explicitly out-of-scope areas",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "qualityCharacteristicIds": {
    "description": "If provided, restrict the quality characteristic mapping section to these characteristic ids",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "stakeholders": {
    "description": "Stakeholders whose impact should be analyzed",
    "items": {
     "properties": {
      "concerns": {
       "description": "Known concerns of this stakeholder",
       "type": "string"
      },
      "name": {
       "description": "Stakeholder name",
       "type": "string"
      },
      "role": {
       "description": "Stakeholder role",
       "type": "string"
      }
     },
     "required": [
      "role"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "verbose": {
    "description": "If false/omitted (default), section 2.6 shows a per-prefix count summary and lists source references only for requirement IDs flagged as duplicate or unresolved-reference, capped at a fixed row limit; section 2.1's duplicate-ID and unresolved-reference listings and section 3's findings table are also capped at fixed row limits (findings sorted by remediation-priority score descending), with every truncated place annotated with the total count, omitted count, and a note that `verbose: true` returns the full data. If true, lists every requirement-source reference in full and every finding in its original generation order, with no caps, no sorting, and no truncation notes (as in previous versions).",
    "type": "boolean"
   }
  },
  "required": [
   "documents"
  ],
  "type": "object"
 },
 "name": "analyze_requirements",
 "title": "Analyze Requirements"
}
