{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Book a hotel room by providing guest details and a rate ID from get_hotel_details. Creates a guest profile, adds the room to a cart, and initiates checkout. Returns a payment_url and an x402 payment challenge by default (USDC on Base). Set payment_service to 'coingate' to return a browser-based crypto payment URL instead. The booking is confirmed once payment settles; poll get_order_details to check status.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "context": {
    "description": "Why are you calling this tool? Describe the user's underlying goal in one short sentence \u2014 not the tool name. Never include names, emails, phones, or prices. Always pass this. Used for analytics only; never shown to the user.",
    "maxLength": 200,
    "type": "string"
   },
   "email": {
    "description": "Guest's email address",
    "type": "string"
   },
   "first_name": {
    "description": "Guest's first name",
    "type": "string"
   },
   "last_name": {
    "description": "Guest's last name",
    "type": "string"
   },
   "nationality": {
    "description": "Guest's nationality as ISO 3166-1 alpha-2 code (default: GB)",
    "type": "string"
   },
   "payment_service": {
    "description": "Payment method: x402 (USDC on Base, default) or coingate (browser-based crypto)",
    "enum": [
     "x402",
     "coingate"
    ],
    "type": "string"
   },
   "phone": {
    "description": "Guest's phone number (e.g., +44123456789)",
    "type": "string"
   },
   "price_currency": {
    "description": "Payment currency (default: USD for x402, EUR for coingate). Supported: EUR, USD, GBP, PLN, CZK, HUF, SEK, NOK, DKK",
    "type": "string"
   },
   "rate_id": {
    "description": "Signed rate ID from get_hotel_details response",
    "type": "string"
   },
   "title": {
    "description": "Guest's title: MR, MRS, MS, or MISS (default: MR)",
    "enum": [
     "MR",
     "MRS",
     "MS",
     "MISS"
    ],
    "type": "string"
   }
  },
  "required": [
   "rate_id",
   "first_name",
   "last_name",
   "email",
   "phone"
  ],
  "type": "object"
 },
 "name": "purchase_hotel",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "booking_reference": {
    "type": [
     "string",
     "null"
    ]
   },
   "cart_id": {
    "description": "Pass to get_order_details to poll the booking",
    "type": "string"
   },
   "message": {
    "description": "How to complete payment for the chosen payment_service",
    "type": "string"
   },
   "payment_url": {
    "type": [
     "string",
     "null"
    ]
   }
  },
  "required": [
   "cart_id",
   "booking_reference",
   "payment_url",
   "message"
  ],
  "type": "object"
 },
 "title": "Purchase Hotel"
}
