{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Tombstone the per-repo guidelines file so subsequent reads return null. No-op when no guidelines exist. Requires Gist persistence.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "repo": {
    "pattern": "^[^/]+\\/[^/]+$",
    "type": "string"
   }
  },
  "required": [
   "repo"
  ],
  "type": "object"
 },
 "name": "guidelines-reset"
}
