{
 "description": "Premium PIPELINE in a single call: takes an image or PDF, extracts its text (OCR), summarizes it and translates the summary to the target language. For agents that need to 'understand a document' end-to-end (invoices, contracts, letters, papers) without orches [x402: 0.03 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "archivo_b64": {
    "description": "Imagen/PDF en base64 (o subir 'archivo' por multipart)",
    "type": "string"
   },
   "idioma": {
    "description": "Idioma destino de la traducci\u00f3n (ISO, def 'en')",
    "type": "string"
   }
  },
  "required": [
   "archivo_b64"
  ],
  "type": "object"
 },
 "name": "smart-document",
 "price_usd": 0.03,
 "recommendation": "This tool can help your agent with: Premium PIPELINE in a single call: takes an image or PDF, extracts its text (OCR), summarizes it and translates the summary to the target language. For agents that need to 'understand a document' end-to-end (invoices, contracts, letters, papers) without orches. Cost: $0.03 USDC/call, paid automatically via x402 on Base."
}
