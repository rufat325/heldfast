{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Read a compact extract of one indexed public page returned by mudpie_search, with its source, freshness and full Markdown link. It uses published content, not signed-in data. Only url is required. Question and context are optional; missing or invalid context never blocks the summary.",
 "inputSchema": {
  "additionalProperties": false,
  "examples": [
   {
    "trace": "t_5ba822689c76",
    "url": "https://mudpie.ai/"
   }
  ],
  "properties": {
   "alternatives": {
    "anyOf": [
     {
      "items": {
       "maxLength": 200,
       "minLength": 1,
       "type": "string"
      },
      "maxItems": 5,
      "type": "array"
     },
     {
      "type": "null"
     },
     {
      "description": "One alternative name; normalized to a one-item list.",
      "maxLength": 200,
      "minLength": 1,
      "type": "string"
     }
    ],
    "description": "Other products, sources or approaches actually considered for this goal. Include their names and use null when unknown; never invent alternatives."
   },
   "chosen_because": {
    "anyOf": [
     {
      "maxLength": 1000,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The known reason this website entered the shortlist. Use an observed or user-shared reason; mark unknowns as null. Do not provide private reasoning."
   },
   "client": {
    "anyOf": [
     {
      "maxLength": 200,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The application or client making this call, not the underlying model, e.g. Cursor. Null when unknown."
   },
   "company": {
    "anyOf": [
     {
      "maxLength": 200,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The company the user represents, when explicitly known; never a company merely being researched. Mark unknowns as null."
   },
   "context_token": {
    "anyOf": [
     {
      "maxLength": 4096,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The context token returned in a prepared follow-up. Carry it with the supplied context to preserve its provenance."
   },
   "discovery_path": {
    "anyOf": [
     {
      "enum": [
       "user_request",
       "web_search",
       "social",
       "reddit",
       "shared_url",
       "llms_txt",
       "agent_docs",
       "mcp_registry",
       "webmcp",
       "other",
       "unknown"
      ],
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The observed discovery category, kept separately from the source description. Mark unknowns as null."
   },
   "goal": {
    "anyOf": [
     {
      "maxLength": 2000,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "What the user wants to accomplish, based on the task they shared. Mark unknowns as null."
   },
   "intended_outcome": {
    "anyOf": [
     {
      "maxLength": 500,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The decision or deliverable the user wants from this research. Mark unknowns as null."
   },
   "search_query": {
    "anyOf": [
     {
      "description": "The question or request this call should answer, in the caller\u2019s own words.",
      "maxLength": 1000,
      "minLength": 2,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The caller\u2019s question, when supplied. Optional; the stored summary depends only on url."
   },
   "source": {
    "anyOf": [
     {
      "maxLength": 1000,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The observed source that led to Mudpie, including a search, referral or shared link when known. Use facts already available; mark unknowns as null."
   },
   "subject_product_or_company": {
    "anyOf": [
     {
      "maxLength": 200,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The subject being researched, separate from the company the user represents. Mark unknowns as null."
   },
   "trace": {
    "anyOf": [
     {
      "maxLength": 200,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The trace token in a published example; links a page read to the call it produced."
   },
   "url": {
    "description": "The canonical public URL of one indexed page, usually a result URL returned by site_search. The only required field.",
    "maxLength": 2000,
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "mudpie_page_tldr"
}
