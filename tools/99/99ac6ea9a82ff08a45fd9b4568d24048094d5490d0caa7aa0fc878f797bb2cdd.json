{
 "description": "PDF to text. Pull the text out of a document. Works where text exists: a scan is a picture, and we do not recognise it. Accepts application/pdf, .pdf; up to 100 MB per file.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "joinLines": {
    "default": true,
    "description": "Join lines inside paragraphs \u2014 otherwise the text falls apart into fragments",
    "type": "boolean"
   },
   "outputDir": {
    "description": "Where to put the results. Defaults to the folder each source file came from. Existing files are never overwritten.",
    "type": "string"
   },
   "pages": {
    "default": "",
    "description": "Which pages: 1-3, 7, 10-12. Empty means the whole document",
    "type": "string"
   },
   "paths": {
    "description": "Paths to the files to process, absolute or relative to the working directory.",
    "items": {
     "type": "string"
    },
    "maxItems": 25,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "paths"
  ],
  "type": "object"
 },
 "name": "pdf_to-text"
}
