{
 "description": "Send an email. `agent` is OPTIONAL: omit it and the workspace's single agent is used, or one is created for you (named from `from`) \u2014 so a first send needs no setup. Pass `agent` only when the workspace has several and you must say which. The reputation firewall runs on every send: an agent auto-suspended for crossing a 0.3% complaint rate is blocked (422 agent_paused), suppressed recipients are skipped, and the response returns the server-chosen routing_pool + classifier_score so you can see how the send was graded.",
 "inputSchema": {
  "properties": {
   "agent": {
    "description": "Optional. The agent to send as; omit to use or create the workspace's default.",
    "type": "string"
   },
   "attachments": {
    "items": {
     "properties": {
      "content_base64": {
       "type": "string"
      },
      "content_type": {
       "type": "string"
      },
      "filename": {
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "bcc": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "body_html": {
    "type": "string"
   },
   "body_text": {
    "type": "string"
   },
   "cc": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "from": {
    "description": "Optional. The sender you want, as a handle or address \u2014 \"billing\", \"billing@acme.com\" and \"Acme <billing@acme.com>\" all mean the agent called \"billing\". It SELECTS that agent: resolved if it exists, created if it does not, and refused with 402 plan_limit_exceeded if the plan has no room. It is never resolved to a DIFFERENT agent, and it cannot spoof a sender. The address that actually sent comes back as `from` on the result.",
    "type": "string"
   },
   "in_reply_to_message_id": {
    "type": "string"
   },
   "metadata": {
    "additionalProperties": {
     "type": "string"
    },
    "type": "object"
   },
   "pool_hint": {
    "enum": [
     "clean",
     "mixed"
    ],
    "type": "string"
   },
   "references": {
    "description": "RFC Message-IDs to thread into",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "reply_to": {
    "type": "string"
   },
   "scheduled_at": {
    "description": "ISO 8601 future timestamp",
    "type": "string"
   },
   "subject": {
    "type": "string"
   },
   "tags": {
    "items": {
     "properties": {
      "name": {
       "type": "string"
      },
      "value": {
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "to": {
    "oneOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ]
   }
  },
  "required": [
   "to",
   "subject"
  ],
  "type": "object"
 },
 "name": "mails.send"
}
