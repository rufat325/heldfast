{
 "description": "Top-level domains added to or removed from IANA's root, newest first, with the IANA list version each happened in. Nobody else publishes this diff.",
 "inputSchema": {
  "properties": {
   "change": {
    "description": "added, removed or returned (optional)",
    "type": "string"
   },
   "limit": {
    "description": "Default 100, max 1000",
    "type": "integer"
   },
   "since": {
    "description": "ISO time: only changes after it (optional)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "tld_changes"
}
