{
 "description": "Find code examples, documentation, GitHub issues, StackOverflow threads via Exa. Best for \"how do I do X in library Y\" queries. Requires exaApiKey.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "maxResults": {
    "description": "Max results (default: 10)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "description": "Code/technical query (e.g. \"how to stream tool_use responses with anthropic SDK\")",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_code_context"
}
