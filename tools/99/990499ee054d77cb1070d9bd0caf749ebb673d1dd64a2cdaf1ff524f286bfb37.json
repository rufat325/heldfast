{
 "description": "Create a Team Task \u2014 one record per multi-agent /team run. Groups the inter-agent event timeline (delegations, replies, approvals) for governance visibility on the Team Tasks page. Call once per task, from the receiving agent.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "instruction": {
    "description": "The full task instruction (required)",
    "type": "string"
   },
   "lead_agent": {
    "description": "Lead agent id, e.g. claude | openclaw | moltfire (lowercase slug, required)",
    "type": "string"
   },
   "max_exchanges": {
    "description": "Round-trip cap (default 10)",
    "type": "integer"
   },
   "origin": {
    "description": "Origin surface: telegram | claude-code | company-loop (required)",
    "type": "string"
   },
   "status": {
    "description": "Initial status (default open)",
    "type": "string"
   },
   "stop_condition": {
    "description": "When the task is considered complete",
    "type": "string"
   },
   "task_id": {
    "description": "Ledger task id, e.g. team-20260710-0912-slug (required)",
    "type": "string"
   }
  },
  "required": [
   "task_id",
   "instruction",
   "origin",
   "lead_agent"
  ],
  "type": "object"
 },
 "name": "dashclaw_task_create"
}
