{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "NASA/JPL CNEOS \u2014 list near-Earth close approaches to Earth in a date range, with miss distance (AU), relative velocity (km/s), and absolute magnitude (size proxy). Keyless.",
 "inputSchema": {
  "examples": [
   {
    "end": "2024-12-31",
    "start": "2024-01-01"
   },
   {
    "end": "2025-06-30",
    "limit": 50,
    "max_distance": "10LD",
    "start": "2025-01-01"
   }
  ],
  "properties": {
   "end": {
    "description": "End date (inclusive), YYYY-MM-DD.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows, default 25, max 100.",
    "type": "number"
   },
   "max_distance": {
    "description": "Max miss distance, default \"0.05\" AU. Accepts lunar distances too, e.g. \"10LD\".",
    "type": "string"
   },
   "start": {
    "description": "Start date (inclusive), YYYY-MM-DD.",
    "type": "string"
   }
  },
  "required": [
   "start",
   "end"
  ],
  "type": "object"
 },
 "name": "close_approaches",
 "title": "Close Approaches",
 "version": "v1.1f67da28"
}
