{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the CrossRef member registry (17K+ registered publishers and societies) by name. Returns member ID, primary name, location, DOI prefixes, and DOI coverage counts. Useful for identifying a publisher's CrossRef member ID or checking publication volume. Data: api.crossref.org (CrossRef REST API), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "query": {
    "description": "Search query for publisher/member name (e.g. \"elsevier\", \"springer\")",
    "minLength": 1,
    "type": "string"
   },
   "rows": {
    "description": "Number of results to return (1-20, default 10)",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "crossref.members.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "CrossRef Member/Publisher Search"
}
