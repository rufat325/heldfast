{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "tier": "free",
  "title": "Get User Profile"
 },
 "description": "Get a user's public profile including display name, stats, and avatar info.",
 "inputSchema": {
  "properties": {
   "identifier": {
    "description": "User public key (hex) or UUID",
    "type": "string"
   }
  },
  "required": [
   "identifier"
  ],
  "type": "object"
 },
 "name": "get_user_profile",
 "outputSchema": {
  "additionalProperties": true,
  "description": "A public user profile (pubkey and role omitted).",
  "properties": {
   "approved_image_count": {
    "description": "Number of approved uploads.",
    "type": "number"
   },
   "created_at": {
    "description": "ISO 8601 account-creation timestamp.",
    "type": "string"
   },
   "display_name": {
    "description": "Display name.",
    "type": [
     "string",
     "null"
    ]
   },
   "has_avatar": {
    "description": "Whether the user has an avatar.",
    "type": "boolean"
   },
   "id": {
    "description": "User UUID.",
    "type": "string"
   }
  },
  "type": "object"
 }
}
