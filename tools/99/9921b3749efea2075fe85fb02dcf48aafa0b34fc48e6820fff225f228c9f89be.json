{
 "description": "Combine two or more PDFs into one, preserving page order. Outputs to a caller-specified path.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "outputPath": {
    "description": "Absolute path for the combined output PDF. Its parent directory will be created if missing.",
    "type": "string"
   },
   "paths": {
    "description": "Two or more absolute PDF paths to combine, in order. The result preserves pages in the order given.",
    "items": {
     "type": "string"
    },
    "minItems": 2,
    "type": "array"
   }
  },
  "required": [
   "paths",
   "outputPath"
  ],
  "type": "object"
 },
 "name": "merge",
 "title": "Merge PDFs"
}
