{
 "description": "List Maestro uploads from DeviceCloud. Filter by name (supports * wildcard), date range, and pagination. Returns id, name, created_at, consoleUrl per upload.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "from": {
    "description": "ISO 8601 timestamp \u2014 uploads on or after this date.",
    "type": "string"
   },
   "limit": {
    "description": "Default 20.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "name": {
    "description": "Filter by upload name. Supports * as wildcard.",
    "type": "string"
   },
   "offset": {
    "description": "Default 0.",
    "minimum": 0,
    "type": "integer"
   },
   "to": {
    "description": "ISO 8601 timestamp \u2014 uploads on or before this date.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_uploads"
}
