{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Requester accepts a proposal and creates the normal AgentLot order/checkout for the proposed listing. This is a financial write action and can require payment approval.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "input": {
    "description": "Structured input passed into the accepted seller order.",
    "type": "object"
   },
   "proposal_id": {
    "type": "string"
   },
   "request_id": {
    "type": "string"
   }
  },
  "required": [
   "request_id",
   "proposal_id"
  ],
  "type": "object"
 },
 "name": "accept_request_proposal",
 "title": "Accept request proposal"
}
