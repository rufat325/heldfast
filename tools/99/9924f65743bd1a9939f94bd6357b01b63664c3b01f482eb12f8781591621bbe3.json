{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Add Profile Member"
 },
 "description": "Add a community member to a partner profile \u2014 its split terms apply to them immediately (community-set, no confirmation step). Refuses over existing terms unless replace_existing: true. Requires community_id + profile_id + member_uid.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "community_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "member_uid": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "profile_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "replace_existing": {
    "type": "boolean"
   }
  },
  "required": [
   "community_id",
   "profile_id",
   "member_uid"
  ],
  "type": "object"
 },
 "name": "add_profile_member",
 "title": "Add Profile Member"
}
