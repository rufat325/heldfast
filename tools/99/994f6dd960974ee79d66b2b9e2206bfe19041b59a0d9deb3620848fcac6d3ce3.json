{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch the full text of one ECHR document by its itemid (from echr_search, e.g. \"001-249132\"). Returns the judgment/decision body converted to plain text, paged. Very recently published items (within a few days) sometimes have no converted text yet \u2014 the tool says so rather than returning nothing silently; echr_search's `conclusion` field is available immediately even then.",
 "inputSchema": {
  "examples": [
   {
    "itemid": "001-249132"
   }
  ],
  "properties": {
   "itemid": {
    "description": "HUDOC itemid from echr_search, e.g. \"001-249132\".",
    "type": "string"
   },
   "max_chars": {
    "description": "Max characters to return (default 12000, max 40000).",
    "type": "number"
   },
   "offset": {
    "description": "Character offset, for paging through a long judgment (default 0).",
    "type": "number"
   }
  },
  "required": [
   "itemid"
  ],
  "type": "object"
 },
 "name": "echr_judgment",
 "outputSchema": {
  "properties": {
   "citation_url": {
    "description": "Public hudoc.echr.coe.int URL for the document.",
    "type": "string"
   },
   "found": {
    "description": "Whether converted full text was available. False is a normal outcome for very recently published documents, not an error.",
    "type": "boolean"
   },
   "has_more": {
    "description": "Whether text remains beyond this slice (present when found).",
    "type": "boolean"
   },
   "itemid": {
    "description": "The HUDOC document id requested.",
    "type": "string"
   },
   "message": {
    "description": "Plain-English explanation of the miss and what to do instead (present only when found is false).",
    "type": "string"
   },
   "next_offset": {
    "description": "Offset to pass back to read the next slice, or null at the end of the document (present when found).",
    "type": [
     "number",
     "null"
    ]
   },
   "offset": {
    "description": "Character offset this slice starts at (present when found).",
    "type": "number"
   },
   "reason": {
    "description": "Why no text was returned (present only when found is false).",
    "enum": [
     "not_yet_converted",
     "empty_body"
    ],
    "type": "string"
   },
   "returned_chars": {
    "description": "Length of the text slice returned (present when found).",
    "type": "number"
   },
   "source": {
    "description": "Attribution string for the HUDOC database (present when found).",
    "type": "string"
   },
   "text": {
    "description": "Plain-text slice of the judgment, converted from HUDOC's DOCX-to-HTML endpoint (present when found).",
    "type": "string"
   },
   "total_chars": {
    "description": "Total length of the whole document in characters (present when found).",
    "type": "number"
   }
  },
  "required": [
   "itemid",
   "found",
   "citation_url"
  ],
  "type": "object"
 },
 "title": "Echr Judgment",
 "version": "v1.3e0d2787"
}
