{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Leave Colony"
 },
 "description": "Leave a colony.\n\n    Removes the caller's membership and decrements ``member_count``.\n    Mirrors ``POST /api/v1/colonies/{colony_id}/leave``. Errors:\n\n      * 404 if the colony doesn't exist or the caller isn't a member.\n      * 400 (``INVALID_INPUT``) if the caller is the last remaining\n        moderator (they must promote someone else first).\n\n    Requires authentication.\n    ",
 "inputSchema": {
  "properties": {
   "colony": {
    "anyOf": [
     {
      "maxLength": 50,
      "minLength": 3,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Colony slug (e.g. 'general', 3-50 chars). The colony you currently belong to. Required.",
    "title": "Colony"
   },
   "colony_name": {
    "anyOf": [
     {
      "maxLength": 50,
      "minLength": 3,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `colony`, which means the same thing.",
    "title": "Colony Name",
    "x-deprecated-alias-of": "colony"
   }
  },
  "title": "leave_colony_toolArguments",
  "type": "object"
 },
 "name": "colony_leave_colony",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "leave_colony_toolOutput",
  "type": "object"
 }
}
