{
 "annotations": {
  "cost": {
   "all": 2
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Retrieve PSC (beneficial owner) records for a UK company by company_number (from search_companies). Returns each PSC's name, kind (individual-person / corporate-entity / legal-person), natures of control (e.g. ownership-of-shares-75-to-100-percent), country of residence, nationality, notification date, and ceased date.",
 "inputSchema": {
  "examples": [
   {
    "company_number": "00006245"
   }
  ],
  "properties": {
   "company_number": {
    "description": "UK company number (e.g. \"07098618\") OR the company NAME (e.g. \"Ocado Group\") \u2014 a name is resolved here automatically and the match is echoed back as resolved_from_name. Pass whichever you have; do not guess a number from memory, since a plausible wrong one returns a clean empty result that reads as \"nothing filed\" for a company that may not exist.",
    "type": "string"
   },
   "items_per_page": {
    "description": "1-100 (default 25)",
    "type": "number"
   },
   "start_index": {
    "description": "Pagination offset (default 0)",
    "type": "number"
   }
  },
  "required": [
   "company_number"
  ],
  "type": "object"
 },
 "name": "get_persons_with_significant_control",
 "outputSchema": {
  "properties": {
   "company_number": {
    "description": "UK company number",
    "type": "string"
   },
   "persons": {
    "description": "List of beneficial owners with significant control",
    "items": {
     "properties": {
      "address": {
       "description": "Address details of PSC",
       "type": [
        "object",
        "null"
       ]
      },
      "ceased_on": {
       "description": "Date PSC ceased (ISO format) if applicable",
       "type": [
        "string",
        "null"
       ]
      },
      "country_of_residence": {
       "description": "Country of residence",
       "type": [
        "string",
        "null"
       ]
      },
      "identification": {
       "description": "Identification details",
       "type": [
        "object",
        "null"
       ]
      },
      "kind": {
       "description": "Kind of person (individual, corporate-entity, etc.)",
       "type": [
        "string",
        "null"
       ]
      },
      "name": {
       "description": "Name of person or entity with significant control",
       "type": [
        "string",
        "null"
       ]
      },
      "nationality": {
       "description": "Nationality",
       "type": [
        "string",
        "null"
       ]
      },
      "natures_of_control": {
       "description": "Nature(s) of control exercised",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "notified_on": {
       "description": "Date PSC entry was notified (ISO format)",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "resolved_from_name": {
    "description": "Present only when a NAME was passed instead of a number. Shows which company answered, so a caller can see the resolution rather than assume it.",
    "properties": {
     "company_name": {
      "description": "Official name of the matched company.",
      "type": "string"
     },
     "company_number": {
      "description": "The number it resolved to.",
      "type": "string"
     },
     "resolved_from": {
      "description": "The name the caller passed.",
      "type": "string"
     },
     "status": {
      "description": "Company status; an active match is preferred over a dissolved namesake.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "total": {
    "description": "Total number of persons with significant control",
    "type": "number"
   }
  },
  "required": [
   "company_number",
   "total",
   "persons"
  ],
  "type": "object"
 },
 "title": "Get Persons With Significant Control",
 "version": "v1.1ff207a8"
}
