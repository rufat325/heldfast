{
 "description": "Transfer SUI from a managed wallet to a recipient address. Amount is in SUI (not MIST).",
 "inputSchema": {
  "properties": {
   "amount": {
    "description": "Amount in SUI",
    "type": "number"
   },
   "fromWallet": {
    "description": "Name of the sending wallet",
    "type": "string"
   },
   "toAddress": {
    "description": "Recipient Sui address",
    "type": "string"
   }
  },
  "required": [
   "fromWallet",
   "toAddress",
   "amount"
  ],
  "type": "object"
 },
 "name": "transfer_sui"
}
