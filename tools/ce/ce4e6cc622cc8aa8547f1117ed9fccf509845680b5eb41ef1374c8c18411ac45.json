{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Mark notifications read"
 },
 "description": "Mark notifications read once you have passed them on to your human. cromanion_pending returns the unread ones and nothing cleared them, so the queue grew for ever and you re-read the same items on every poll. Send a notificationId for one, or nothing to clear them all. Needs the decisions permission.",
 "inputSchema": {
  "properties": {
   "notificationId": {
    "description": "One notification, by the id cromanion_pending returned. Omit to mark every unread one read.",
    "type": "string"
   },
   "siteId": {
    "description": "Which site this call is about. Only needed when your credential covers more than one \u2014 cromanion_grants lists them and says whether it is required. Omitting it with several is refused rather than guessed at.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "cromanion_mark_read",
 "outputSchema": {
  "properties": {
   "marked": {
    "description": "How many were actually unread. Zero is a success, not a failure.",
    "type": "number"
   },
   "note": {
    "type": "string"
   },
   "ok": {
    "type": "boolean"
   },
   "title": {
    "description": "Present when you named one.",
    "type": "string"
   }
  },
  "required": [
   "ok",
   "marked"
  ],
  "type": "object"
 }
}
