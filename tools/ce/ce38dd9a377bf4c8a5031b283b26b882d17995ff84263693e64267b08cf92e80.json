{
 "description": "Transcribe audio a texto (Whisper local). Sube el audio. [x402: 0.01 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "transcribir-audio",
 "price_usd": 0.01,
 "recommendation": "This tool can help your agent with: Transcribe audio a texto (Whisper local). Sube el audio.. Cost: $0.01 USDC/call, paid automatically via x402 on Base."
}
