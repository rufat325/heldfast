{
 "description": "Find vacancies similar to a given one. Useful for expanding a candidate's job search.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "page": {
    "default": 0,
    "description": "Page number (0-based)",
    "minimum": 0,
    "type": "integer"
   },
   "per_page": {
    "default": 20,
    "description": "Results per page",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "raw": {
    "description": "Return the full raw hh.ru JSON instead of the compact summary.",
    "type": "boolean"
   },
   "vacancy_id": {
    "description": "Vacancy ID to find similar vacancies for",
    "pattern": "^\\d+$",
    "type": "string"
   }
  },
  "required": [
   "vacancy_id"
  ],
  "type": "object"
 },
 "name": "get_similar_vacancies"
}
