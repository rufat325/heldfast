{
 "description": "Auto-created from real demand (75 requests) [x402: 0.02 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "entrada",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "codigo",
 "price_usd": 0.02,
 "recommendation": "This tool can help your agent with: Auto-created from real demand (75 requests). Cost: $0.02 USDC/call, paid automatically via x402 on Base."
}
