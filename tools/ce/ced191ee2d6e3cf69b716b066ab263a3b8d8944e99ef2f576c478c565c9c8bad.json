{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get metadata for an academic journal by its ISSN \u2014 title, publisher, subject areas, and DOI coverage counts (current, backfile, total). Useful for verifying a journal's publisher or gauging its publication volume. Data: api.crossref.org (CrossRef REST API), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "issn": {
    "description": "Journal ISSN in NNNN-NNNN format (e.g. 0028-0836 for Nature)",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "issn"
  ],
  "type": "object"
 },
 "name": "crossref.journals.lookup",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "CrossRef Journal Lookup"
}
