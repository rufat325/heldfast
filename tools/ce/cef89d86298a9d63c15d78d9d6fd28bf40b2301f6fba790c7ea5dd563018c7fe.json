{
 "description": "Search Instagram users by keyword. Pagination via rank_token is best effort. 10 credits.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "cursor": {
    "description": "rank_token from previous response.",
    "type": "string"
   },
   "keyword": {
    "description": "Search query.",
    "maxLength": 500,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "keyword"
  ],
  "type": "object"
 },
 "name": "search_instagram_users"
}
