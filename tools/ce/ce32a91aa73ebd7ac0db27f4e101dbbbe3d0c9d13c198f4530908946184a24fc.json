{
 "description": "Lists events from a Google Calendar within an optional time range. Returns event details including title, time, location, attendees, and description. Useful for checking upcoming meetings, finding events by search query, or reviewing a specific time period. By default, shows the next 10 events from the primary calendar.",
 "inputSchema": {
  "properties": {
   "calendar_id": {
    "description": "Calendar ID to list events from. Defaults to \"primary\" (user's primary calendar).",
    "type": "string"
   },
   "max_results": {
    "description": "Maximum number of events to return. Defaults to 10, maximum is 250.",
    "type": "number"
   },
   "order_by": {
    "description": "Order of events. \"startTime\" orders by start time (requires single_events=true), \"updated\" orders by last modification time.",
    "enum": [
     "startTime",
     "updated"
    ],
    "type": "string"
   },
   "query": {
    "description": "Free text search query to filter events.",
    "type": "string"
   },
   "single_events": {
    "description": "Whether to expand recurring events into instances. Defaults to true to show individual occurrences.",
    "type": "boolean"
   },
   "time_max": {
    "description": "Upper bound (exclusive) for event start time in RFC3339 format (e.g., \"2024-12-31T23:59:59Z\").",
    "type": "string"
   },
   "time_min": {
    "description": "Lower bound (inclusive) for event start time in RFC3339 format (e.g., \"2024-01-01T00:00:00Z\"). Defaults to current time if not specified.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_calendar_events"
}
