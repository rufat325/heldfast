{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Add Org Delegation Grant"
 },
 "description": "Authorise which resource/scopes/roles the org mints on-behalf-of tokens\n    for (admin+). ttl is clamped to the org-delegation ceiling.",
 "inputSchema": {
  "properties": {
   "max_ttl_seconds": {
    "anyOf": [
     {
      "type": "integer"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Max minted-token lifetime (clamped to the org ceiling).",
    "title": "Max Ttl Seconds"
   },
   "min_role": {
    "default": "admin",
    "description": "Minimum org role that may use the grant: member, admin, or owner.",
    "title": "Min Role",
    "type": "string"
   },
   "resource": {
    "description": "Target audience (a client id or URL) the grant applies to.",
    "title": "Resource",
    "type": "string"
   },
   "scopes": {
    "description": "Scopes the org will mint on-behalf-of tokens for.",
    "items": {
     "type": "string"
    },
    "title": "Scopes",
    "type": "array"
   },
   "slug": {
    "description": "The organisation's handle.",
    "title": "Slug",
    "type": "string"
   }
  },
  "required": [
   "slug",
   "resource",
   "scopes"
  ],
  "title": "org_delegation_addArguments",
  "type": "object"
 },
 "name": "colony_org_delegation_add",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "org_delegation_addOutput",
  "type": "object"
 }
}
