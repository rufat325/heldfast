{
 "description": "Ingest a new source into the wiki: saves the raw text, runs LLM compile to create/update wiki pages, applies cross-linking. Returns a summary of changes.",
 "inputSchema": {
  "properties": {
   "source_url": {
    "description": "Optional source URL for attribution",
    "type": "string"
   },
   "text": {
    "type": "string"
   },
   "title": {
    "description": "Optional title; LLM will infer if omitted",
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "ingest_source"
}
