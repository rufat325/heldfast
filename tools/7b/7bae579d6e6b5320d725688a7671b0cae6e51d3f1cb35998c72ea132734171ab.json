{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List agent memory keys"
 },
 "description": "List all stored memory keys under your authenticated Bearer key (agent_id is accepted but not currently used to partition storage \u2014 see memory_store's note). Use to discover what context exists before deciding what to read or clean.",
 "inputSchema": {
  "examples": [
   {
    "agent_id": "my-trading-bot"
   }
  ],
  "properties": {
   "agent_id": {
    "description": "Accepted but not currently used to partition storage \u2014 memory is namespaced by your Bearer key.",
    "type": "string"
   }
  },
  "required": [
   "agent_id"
  ],
  "type": "object"
 },
 "name": "memory_list",
 "pricing": "Free",
 "supported_payments": [
  "Bearer Token (credits)"
 ]
}
