{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Buyer Setup Help"
 },
 "description": "Diagnose missing local AntSeed buyer \u2014 registered instead of create_session when no buyer responded to /health at startup. Use this to tell the user how to install a buyer (CLI or AntStation Desktop). Returns the probed buyer URL and install instructions. For trading, install a buyer and restart the MCP server.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {},
  "type": "object"
 },
 "name": "buyer_setup",
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "message": {
    "type": "string"
   },
   "nextSteps": {
    "type": "string"
   },
   "options": {
    "items": {
     "properties": {
      "defaultPort": {
       "type": "integer"
      },
      "description": {
       "type": "string"
      },
      "envVar": {
       "type": "string"
      },
      "installHint": {
       "type": "string"
      },
      "name": {
       "type": "string"
      }
     },
     "required": [
      "name",
      "description",
      "defaultPort",
      "envVar",
      "installHint"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "probedAt": {
    "description": "URL the MCP probed for /health",
    "type": "string"
   },
   "status": {
    "enum": [
     "BUYER_NOT_DETECTED"
    ],
    "type": "string"
   },
   "strictMode": {
    "type": "boolean"
   }
  },
  "required": [
   "status",
   "probedAt",
   "strictMode",
   "message",
   "options",
   "nextSteps"
  ],
  "type": "object"
 }
}
