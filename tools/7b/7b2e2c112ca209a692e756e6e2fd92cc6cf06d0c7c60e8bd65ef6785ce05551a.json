{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Roll a Storage Box back to a snapshot. This overwrites current data with the snapshot contents.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "id": {
    "description": "Storage Box ID",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "snapshot": {
    "description": "Name of the snapshot to roll back to",
    "type": "string"
   }
  },
  "required": [
   "id",
   "snapshot"
  ],
  "type": "object"
 },
 "name": "hetzner_rollback_storage_box_snapshot",
 "title": "Rollback Storage Box Snapshot"
}
