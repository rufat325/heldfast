{
 "description": "Check a job started by asc_upload_build or asc_upload_ipa. Returns status (running/succeeded/failed) and the tail of the log.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "job_id": {
    "description": "Job ID returned by asc_upload_build or asc_upload_ipa",
    "type": "string"
   },
   "log_lines": {
    "default": 30,
    "description": "How many log lines to include",
    "maximum": 200,
    "minimum": 5,
    "type": "integer"
   }
  },
  "required": [
   "job_id"
  ],
  "type": "object"
 },
 "name": "asc_job_status"
}
