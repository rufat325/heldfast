{
 "description": "Semantic search over everything the agent has remembered. Returns the most relevant memories for a query.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max results (default 5)",
    "type": "number"
   },
   "query": {
    "description": "What to search for",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "opencode_search_memory"
}
