{
 "description": "List all caches (semantic_cache and agent_cache) registered for the active instance, with hit rate and total ops.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "instanceId": {
    "description": "Connection ID; defaults to the active instance",
    "pattern": "^[a-zA-Z0-9_-]+$",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "cache_list"
}
