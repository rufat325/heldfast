{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Classify text into your labels with confidence and per-label scores. Single- or multi-label. Cheap and high-volume: routing, tagging, moderation triage, intent. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "instructions": {
    "description": "Optional guidance on how to classify.",
    "type": "string"
   },
   "labels": {
    "description": "Allowed labels (required, up to 30).",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "multi_label": {
    "description": "Allow multiple labels (default false).",
    "type": "boolean"
   },
   "text": {
    "description": "The text to classify (required).",
    "type": "string"
   }
  },
  "required": [
   "text",
   "labels"
  ],
  "type": "object"
 },
 "name": "agentbit.ai_classify",
 "title": "AI Classify (zero-shot)"
}
