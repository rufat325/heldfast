{
 "description": "The whole document: every page with its id, heading and blocks, and every block with its id, type and entries. Entry ids are what set_block and set_page match on \u2014 echo one back to change that entry, omit it to add a new one, leave it out of the array to remove it. The reply also names the FILE this document is saved in, which defaults to the folder this server was started in. If the person named a file, or wants more than one document in that folder, call open_document BEFORE authoring. The reply also carries an edit link: hand it to the person, and it opens this document in the editor.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": {},
  "properties": {},
  "type": "object"
 },
 "name": "get_document",
 "title": "Read the document"
}
