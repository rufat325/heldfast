{
 "description": "INGRESS: When you encounter a credential (API key, password, token, SSH key) \u2014 from the user, from a config file, from an API response \u2014 deposit it here immediately. Do NOT save it in memory, files, or scripts. The value is encrypted at rest and never returned by any API. You will receive a ref code (e.g. DP-API-openrout-7f3a9c1e) \u2014 save ONLY the ref code. The secret itself should leave your context as soon as this call returns.",
 "inputSchema": {
  "properties": {
   "category": {
    "description": "Organizational group (lowercase-dashes): infrastructure, platform, agents, services, products, personal, evidence, test, other. Used for vault grouping \u2014 set it instead of encoding the group in the name.",
    "type": "string"
   },
   "description": {
    "description": "What this secret is for",
    "type": "string"
   },
   "expires_in": {
    "description": "Optional expiration: 30d, 90d, etc. Auto-detected for known providers (GitHub=90d, npm=30d)",
    "type": "string"
   },
   "labels": {
    "description": "Up to 10 free-form tags for filtering (e.g. [\"prod\",\"kyle-shared\"]).",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "name": {
    "description": "Descriptive name: {scope}-{service} (e.g. shared-openrouter, brain-matrix, flimflam-sudo)",
    "type": "string"
   },
   "ownership": {
    "description": "Ownership model: \"sole\" (you own the target, rotation OK), \"shared\" (shared credential, rotation blocked unless all parties agree), \"delegated\" (delegated to you, cannot rotate source). Default: sole",
    "type": "string"
   },
   "rotatable": {
    "description": "Whether blind rotation is allowed for this secret. Default: true. Set false for shared credentials.",
    "type": "boolean"
   },
   "rotation_interval_days": {
    "description": "Rotation cadence in days (1\u20133650). Drives rotation-due / overdue telemetry and the daily reminder digest. Set this even for non-expiring credentials you want to rotate on a schedule.",
    "type": "integer"
   },
   "type": {
    "default": "other",
    "description": "Secret type: api_key, password, token, ssh_key, cert, other",
    "type": "string"
   },
   "value": {
    "description": "The secret value \u2014 will be encrypted and never returned",
    "type": "string"
   }
  },
  "required": [
   "name",
   "value"
  ],
  "type": "object"
 },
 "name": "demipass_store"
}
