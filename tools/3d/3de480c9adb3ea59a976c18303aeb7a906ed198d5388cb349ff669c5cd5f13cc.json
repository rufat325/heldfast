{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List countries covered by Malaria Atlas Project admin boundaries with their ISO3/ISO2 codes and name, optionally filtered by a name substring. Use the returned iso code as the `country` filter for the other malaria-atlas tools. Data: Malaria Atlas Project GeoServer WFS (data.malariaatlas.org), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Maximum number of countries to return (1-250). Defaults to 50.",
    "maximum": 250,
    "minimum": 1,
    "type": "integer"
   },
   "name": {
    "description": "Case-insensitive substring filter on the country name (e.g. \"kenya\").",
    "maxLength": 60,
    "minLength": 1,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "malaria-atlas.reference.country_list",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Malaria Atlas Country List"
}
