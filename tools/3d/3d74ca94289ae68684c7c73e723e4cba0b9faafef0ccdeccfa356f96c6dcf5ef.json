{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": false
 },
 "description": "Add a Bear note to the context library by tagging it with #context. Optionally specify a subtag for grouping (e.g., subtag 'jira' \u2192 #context/jira). Triggers a sync after tagging.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Note ID (uniqueIdentifier)",
    "type": "string"
   },
   "subtag": {
    "description": "Optional sub-tag for grouping (e.g., 'architecture', 'jira')",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "bear_context_add"
}
