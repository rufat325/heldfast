{
 "description": "Launch an installed app in the simulator by bundle identifier. Optional arguments are passed after the bundle id and are useful for UI-test/debug launch flags.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "args": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "bundleId": {
    "type": "string"
   },
   "udid": {
    "description": "Simulator UDID. Use simulator_list first when unsure.",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "udid",
   "bundleId"
  ],
  "type": "object"
 },
 "name": "launch_app",
 "title": "Launch an installed app"
}
