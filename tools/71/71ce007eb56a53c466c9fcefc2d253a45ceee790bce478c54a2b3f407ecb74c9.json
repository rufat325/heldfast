{
 "description": "PREMIUM: returns SSML ready for your TTS engine. input=text. [x402: 3.0 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "voice-clone-tts",
 "price_usd": 3.0,
 "recommendation": "This tool can help your agent with: PREMIUM: returns SSML ready for your TTS engine. input=text.. Cost: $3.0 USDC/call, paid automatically via x402 on Base."
}
