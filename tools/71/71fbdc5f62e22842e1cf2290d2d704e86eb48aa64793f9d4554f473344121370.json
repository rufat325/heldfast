{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List all boards in your account. Returns board ID, name, state, and item count to discover available boards.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-monday-api-key"
   },
   {
    "_apiKey": "your-monday-api-key",
    "limit": 50
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Monday.com API token",
    "type": "string"
   },
   "limit": {
    "description": "Number of boards to return (default 20, max 50)",
    "type": "number"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "monday_list_boards",
 "outputSchema": {
  "properties": {
   "boards": {
    "description": "List of boards in the account",
    "items": {
     "properties": {
      "board_kind": {
       "description": "Kind of board",
       "type": "string"
      },
      "id": {
       "description": "Board ID",
       "type": "string"
      },
      "items_count": {
       "description": "Number of items in board",
       "type": "number"
      },
      "name": {
       "description": "Board name",
       "type": "string"
      },
      "state": {
       "description": "Board state",
       "type": "string"
      },
      "updated_at": {
       "description": "Last update timestamp",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "boards"
  ],
  "type": "object"
 },
 "title": "Monday List Boards",
 "version": "v1.736120de"
}
