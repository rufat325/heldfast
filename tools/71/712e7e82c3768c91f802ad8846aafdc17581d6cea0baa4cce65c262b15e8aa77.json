{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Create a custom voice from a text description of desired voice characteristics. MODELS: eleven_multilingual_ttv_v2 (default), eleven_ttv_v3 (newer). Check status with list_custom_voices. Once READY, use its ID with generate_speech.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "description": {
    "description": "Optional description for your reference. Max 512 characters.",
    "maxLength": 512,
    "type": "string"
   },
   "model": {
    "description": "Voice design model. Default: eleven_multilingual_ttv_v2.",
    "enum": [
     "eleven_multilingual_ttv_v2",
     "eleven_ttv_v3"
    ],
    "type": "string"
   },
   "name": {
    "description": "Name for the voice. Max 100 characters.",
    "maxLength": 100,
    "minLength": 1,
    "type": "string"
   },
   "prompt": {
    "description": "Text description of desired voice. Min 20, max 1000 characters.",
    "maxLength": 1000,
    "minLength": 20,
    "type": "string"
   }
  },
  "required": [
   "name",
   "prompt"
  ],
  "type": "object"
 },
 "name": "create_custom_voice"
}
