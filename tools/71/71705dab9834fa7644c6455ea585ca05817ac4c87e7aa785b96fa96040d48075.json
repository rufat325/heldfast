{
 "description": "Seal the provenance of a dataset / training run / AI-generated content: a post-quantum signature + a drand lower-bound timestamp (cannot be backdated) + an on-chain anchor. Call to prove PRIOR EXISTENCE and INTEGRITY of AI data your agent produced or consumed. Privacy-preserving (send a hash). Price $0.05 USDC on Base.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "content": {
    "description": "Raw content to seal; we seal sha256(content).",
    "type": "string"
   },
   "content_hash": {
    "description": "A sha256 hex you already computed; sealed as-is (content never leaves you). Provide content OR content_hash.",
    "type": "string"
   },
   "issuer": {
    "description": "Optional declaring party (recorded).",
    "type": "string"
   },
   "kind": {
    "description": "Provenance kind: \"dataset\" | \"training\" | \"content\".",
    "type": "string"
   },
   "payment": {
    "description": "Optional base64 x402 X-PAYMENT (EIP-3009 exact-scheme payload, or {txHash}). x402-aware wallet clients pass this automatically via _meta[\"x402/payment\"] \u2014 set it explicitly only if your client does not plumb _meta. Omit on the first call to receive the payment challenge.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "prove_provenance",
 "title": "Post-quantum provenance seal (dataset/content)"
}
