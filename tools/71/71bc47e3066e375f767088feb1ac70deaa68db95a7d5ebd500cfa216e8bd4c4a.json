{
 "description": "Surface recurring problem/solution motifs from memory. Omit topic to return all detected patterns. Supports dismiss action.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Action (default: list)",
    "enum": [
     "list",
     "dismiss"
    ],
    "type": "string"
   },
   "evidence": {
    "description": "Include supporting memory IDs (default false)",
    "type": "boolean"
   },
   "limit": {
    "description": "Max results (default 5)",
    "type": "number"
   },
   "min_confidence": {
    "description": "Minimum confidence threshold (0-1)",
    "type": "number"
   },
   "pattern_id": {
    "description": "Pattern ID to dismiss (required for dismiss action)",
    "type": "string"
   },
   "topic": {
    "description": "Topic to find patterns for (optional \u2014 omit for all patterns)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "velixar_patterns"
}
