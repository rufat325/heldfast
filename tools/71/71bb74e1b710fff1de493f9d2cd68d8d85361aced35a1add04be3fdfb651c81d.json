{
 "description": "Tabla pivote sobre un CSV. input='<url_o_csv>||<indice>||<valores>'. [x402: 0.03 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "pivotar-datos",
 "price_usd": 0.03,
 "recommendation": "This tool can help your agent with: Tabla pivote sobre un CSV. input='<url_o_csv>||<indice>||<valores>'.. Cost: $0.03 USDC/call, paid automatically via x402 on Base."
}
