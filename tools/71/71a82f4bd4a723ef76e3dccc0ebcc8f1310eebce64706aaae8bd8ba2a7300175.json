{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get upload session"
 },
 "description": "Get details about an existing upload session by ID (authorized by the session ID itself).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "upload_session_id": {
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "upload_session_id"
  ],
  "type": "object"
 },
 "name": "adn_get_upload_session"
}
