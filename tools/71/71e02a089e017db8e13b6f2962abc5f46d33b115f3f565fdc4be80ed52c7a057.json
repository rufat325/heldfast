{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Opt-in AI lane: propose weak MAY_RELATE_TO candidate links between existing deterministic behavior and CodeSymbol nodes. Generate writes .orangepro/ai/links.json only; apply=true explicitly merges survivors into candidate_edges. These links never affect proven coverage.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "all": {
    "description": "Link across all behavior nodes instead of the default gaps-only scope.",
    "type": "boolean"
   },
   "apply": {
    "description": "Apply staged links into candidate_edges. Default false stages links only.",
    "type": "boolean"
   },
   "max_behaviors": {
    "description": "Maximum behavior targets to process in this AI linking run.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "max_prompt_tokens": {
    "description": "Approximate per-batch prompt token ceiling.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "model": {
    "description": "Model name override.",
    "type": "string"
   },
   "provider": {
    "description": "BYOK provider override.",
    "enum": [
     "openai",
     "ollama",
     "anthropic"
    ],
    "type": "string"
   },
   "symbols_per_behavior": {
    "description": "Deterministic CodeSymbol shortlist size per behavior.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "workspace": {
    "description": "Local workspace root. Defaults to the current directory.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "orangepro_ai_links",
 "title": "Stage/apply AI candidate links"
}
