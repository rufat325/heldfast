{
 "_meta": {},
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List beta app review submissions"
 },
 "description": "List TestFlight (beta) app review submissions for a build",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "filter.betaReviewState": {
    "anyOf": [
     {
      "enum": [
       "WAITING_FOR_REVIEW",
       "IN_REVIEW",
       "REJECTED",
       "APPROVED"
      ],
      "type": "string"
     },
     {
      "items": {
       "enum": [
        "WAITING_FOR_REVIEW",
        "IN_REVIEW",
        "REJECTED",
        "APPROVED"
       ],
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by beta review state(s)"
   },
   "filter.build": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Build ID(s) (required). Beta app review submissions for this build."
   },
   "limit": {
    "default": 100,
    "description": "Maximum number of beta app review submissions to return (default 100, max 200)",
    "type": "number"
   }
  },
  "required": [
   "filter.build"
  ],
  "type": "object"
 },
 "name": "list-beta-app-review-submissions",
 "title": "List beta app review submissions"
}
