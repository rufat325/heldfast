{
 "description": "Execute code in a sandboxed E2B environment. Supports Python (default) and JavaScript. Max 60s timeout. Paid endpoint.",
 "inputSchema": {
  "properties": {
   "code": {
    "description": "Code to execute",
    "type": "string"
   },
   "credit_token": {
    "description": "Credit token for authentication (optional if using x402)",
    "type": "string"
   },
   "language": {
    "description": "Programming language (python or javascript)",
    "enum": [
     "python",
     "javascript"
    ],
    "type": "string"
   },
   "timeout": {
    "description": "Execution timeout in seconds (max 60)",
    "type": "number"
   }
  },
  "required": [
   "code"
  ],
  "type": "object"
 },
 "name": "execute_code"
}
