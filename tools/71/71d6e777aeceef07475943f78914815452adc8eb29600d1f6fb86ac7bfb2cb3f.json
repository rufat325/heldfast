{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Metrics \u00b7 Timeseries (multi-asset)"
 },
 "description": "Timeseries for ONE metric across MANY assets in a single call.\n\n        `assets` is comma-separated (e.g. \"BTC,ETH,SOL\"). Prefer this over\n        looping get_metric_timeseries \u2014 it is one round trip instead of N, and\n        the values are guaranteed to come from the same read.",
 "inputSchema": {
  "properties": {
   "assets": {
    "title": "Assets",
    "type": "string"
   },
   "from_date": {
    "default": "utc_now-30d",
    "title": "From Date",
    "type": "string"
   },
   "interval": {
    "default": "1d",
    "title": "Interval",
    "type": "string"
   },
   "metric": {
    "title": "Metric",
    "type": "string"
   },
   "to_date": {
    "default": "utc_now",
    "title": "To Date",
    "type": "string"
   }
  },
  "required": [
   "metric",
   "assets"
  ],
  "title": "get_metric_timeseries_multiArguments",
  "type": "object"
 },
 "name": "get_metric_timeseries_multi",
 "title": "Metrics \u00b7 Timeseries (multi-asset)"
}
