{
 "description": "List tasks. Omit workflowId to list ALL tasks across every workflow in the workspace. Provide workflowId to list a single workflow's tasks instead; these are DB tasks by default, or phase files tagged \"task\" for legacy file-backed workflows. Returns status, creator, priority, and due dates. Use sparingly \u2014 load_workflow already includes active workflow tasks.",
 "inputSchema": {
  "properties": {
   "includeDismissed": {
    "description": "Include dismissed tasks. Default: false.",
    "type": "boolean"
   },
   "mine": {
    "description": "Cross-workflow listing only: return just tasks assigned to you plus unassigned ones. Ignored when workflowId is set. Default: false (all tasks across the workspace).",
    "type": "boolean"
   },
   "priority": {
    "description": "Filter tasks by priority. Omit to return all priorities.",
    "enum": [
     "high",
     "medium",
     "low"
    ],
    "type": "string"
   },
   "status": {
    "description": "Optional comma-separated status filter: not_started,in_progress,done,dismissed",
    "type": "string"
   },
   "workflowId": {
    "description": "Workflow ID. Omit to list all tasks across every workflow in the workspace.",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your configured workspace.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "list_tasks"
}
