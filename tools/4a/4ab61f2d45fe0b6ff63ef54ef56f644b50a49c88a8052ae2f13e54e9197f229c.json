{
 "description": "Return the LIVE price of a cryptocurrency in USD and COP with its 24h change (real market data). High volume for agents that need prices. input=symbol or id (btc, eth, sol, usdc...). [x402: 0.005 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "S\u00edmbolo o id de la moneda (btc, eth, sol...)",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "crypto-price",
 "price_usd": 0.005,
 "recommendation": "This tool can help your agent with: Return the LIVE price of a cryptocurrency in USD and COP with its 24h change (real market data). High volume for agents that need prices. input=symbol or id (btc, eth, sol, usdc...).. Cost: $0.005 USDC/call, paid automatically via x402 on Base."
}
