{
 "description": "Detect file format from magic bytes. Works with PE, ELF, Mach-O, ZIP, Java class, and other formats.",
 "inputSchema": {
  "properties": {
   "file_path": {
    "description": "Path to the file",
    "type": "string"
   }
  },
  "required": [
   "file_path"
  ],
  "type": "object"
 },
 "name": "analyze_file_format"
}
