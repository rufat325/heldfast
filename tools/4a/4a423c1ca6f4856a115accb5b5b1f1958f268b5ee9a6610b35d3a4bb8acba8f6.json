{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Ghost Audit Status"
 },
 "contract": {
  "auth_scope": "public",
  "idempotent": true,
  "output_schema": {
   "audit_id": "string",
   "ready": "boolean",
   "status": "'complete'|'running'"
  },
  "price": {
   "free_tier": "unlimited",
   "paid": "included in Zambo Pass",
   "per_call": "free"
  },
  "side_effects": false
 },
 "description": "Status report for a Ghost Audit identified by audit_id. Returns whether the audit is running or complete, along with elapsed-time information and report availability.",
 "destructiveHint": false,
 "inputSchema": {
  "examples": [
   {
    "audit_id": "audit_123"
   }
  ],
  "properties": {
   "audit_id": {
    "description": "The audit_id returned by ghost_audit_site",
    "type": "string"
   }
  },
  "required": [
   "audit_id"
  ],
  "type": "object"
 },
 "name": "ghost_audit_status",
 "openWorldHint": true,
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "audit_id": {
    "type": "string"
   },
   "ready": {
    "type": "boolean"
   },
   "status": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "readOnlyHint": true,
 "title": "Ghost Audit Status"
}
