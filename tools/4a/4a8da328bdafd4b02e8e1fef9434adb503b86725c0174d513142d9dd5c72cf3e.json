{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Walk the full taxonomic lineage (root \u2192 tip) for a WoRMS AphiaID: returns a flat ordered array of { rank, name, aphia_id } from superdomain down to the taxon itself. Keyless.",
 "inputSchema": {
  "examples": [
   {
    "aphia_id": 137102
   }
  ],
  "properties": {
   "aphia_id": {
    "description": "WoRMS AphiaID, e.g. 137102.",
    "type": [
     "number",
     "string"
    ]
   }
  },
  "required": [
   "aphia_id"
  ],
  "type": "object"
 },
 "name": "get_classification",
 "title": "Get Classification",
 "version": "v1.dab16feb"
}
