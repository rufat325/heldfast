{
 "description": "Cold-outreach prospects for one project, newest first, with status, source and the facts enrichment read off their own website. (GET /api/v1/outreach/prospects; needs the read:outreach scope)",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "1\u2013200, default 50.",
    "type": "number"
   },
   "project": {
    "description": "Project id or slug. Resolved inside the key owner's portfolio; an unowned value matches nothing rather than 404ing.",
    "type": "string"
   },
   "search": {
    "description": "Substring match on email, company or name.",
    "type": "string"
   },
   "source": {
    "description": "Where the prospect came from, e.g. launch_venues, import, manual.",
    "type": "string"
   },
   "status": {
    "description": "new | contacted | replied | bounced | rejected. Default: everything but archived.",
    "type": "string"
   }
  },
  "required": [
   "project"
  ],
  "type": "object"
 },
 "name": "list_prospects"
}
