{
 "description": "DIAGNOSTICS: When an action is blocked, run this to find out why. Checks: does the secret exist? Is it active? Is there a matching context? Is your account suspended? Returns specific fixes for each failing check.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Action type that was denied (ssh_exec, http_header, etc.)",
    "type": "string"
   },
   "name": {
    "description": "Secret name that was denied",
    "type": "string"
   },
   "ref": {
    "description": "Ref code that was denied",
    "type": "string"
   },
   "target_host": {
    "description": "Target host that was denied",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "demipass_explain_denial"
}
