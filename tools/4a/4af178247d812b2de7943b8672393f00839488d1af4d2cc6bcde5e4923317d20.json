{
 "description": "Draft a reply to a message (email, chat, ticket). For support agents. input=message. [x402: 0.003 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "mensaje a responder",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "reply",
 "price_usd": 0.003,
 "recommendation": "This tool can help your agent with: Draft a reply to a message (email, chat, ticket). For support agents. input=message.. Cost: $0.003 USDC/call, paid automatically via x402 on Base."
}
