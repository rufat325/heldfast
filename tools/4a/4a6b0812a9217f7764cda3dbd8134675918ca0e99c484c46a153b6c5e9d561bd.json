{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "List TEXT nodes and copy in a scoped subtree. Large output may be spilled.",
 "inputSchema": {
  "properties": {
   "channel": {
    "description": "Optional. Target a specific connected file by its channel id...",
    "type": "string"
   },
   "nodeId": {
    "description": "Root node ID to scan from, colon format e.g. '4029:12345'",
    "type": "string"
   },
   "origin": {
    "description": "Origin: orchestrator/self=wolfgang; workers use assigned name....",
    "enum": [
     "wolfgang",
     "grace",
     "theo",
     "sunho",
     "zoe",
     "taewon",
     "emma",
     "alex",
     "rick"
    ],
    "type": "string"
   },
   "skipInvisibleInstanceChildren": {
    "description": "Optional. true = skip hidden instances' children during...",
    "type": "boolean"
   }
  },
  "required": [
   "nodeId",
   "origin"
  ],
  "type": "object"
 },
 "name": "scan_text_nodes"
}
