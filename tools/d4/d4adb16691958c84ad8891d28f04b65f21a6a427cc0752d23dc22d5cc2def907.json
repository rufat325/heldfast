{
 "_meta": {
  "skill": "bitflow"
 },
 "description": "Get a swap quote from Bitflow DEX.\n\nReturns the expected output amount and best route for swapping tokens.\nNo API key required \u2014 uses public endpoints (500 req/min).\n\nNote: Bitflow is only available on mainnet.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "amountIn": {
    "description": "Amount of input token. Default interpretation is human units (e.g. '100' = 100 LEO).",
    "type": "string"
   },
   "amountUnit": {
    "description": "Required. Amount units: 'human' (frontend-style decimal, e.g. '2' for 2 STX) or 'base' (smallest integer units, e.g. '2000000' for 2 STX).",
    "enum": [
     "human",
     "base"
    ],
    "type": "string"
   },
   "tokenX": {
    "description": "Input token ID (e.g. 'token-stx', 'token-sbtc')",
    "type": "string"
   },
   "tokenY": {
    "description": "Output token ID (e.g. 'token-sbtc', 'token-aeusdc')",
    "type": "string"
   }
  },
  "required": [
   "tokenX",
   "tokenY",
   "amountIn",
   "amountUnit"
  ],
  "type": "object"
 },
 "name": "bitflow_get_quote"
}
