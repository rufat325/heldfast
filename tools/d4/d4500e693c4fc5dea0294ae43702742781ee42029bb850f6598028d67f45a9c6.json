{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": false,
  "title": "Codify verified memory into agent skills"
 },
 "description": "Codify durable, verified repo memory (runbooks, workflows, actionable decisions) into git-native SKILL.md files under .claude/skills/ that every teammate's agent auto-loads. Only grounded, non-stale packets become skills. Pass dry_run to preview without writing. dir overrides the output directory.",
 "inputSchema": {
  "properties": {
   "dir": {
    "description": "Override the output directory (default .claude/skills/).",
    "type": "string"
   },
   "dry_run": {
    "description": "Preview which skills would be written without creating any files.",
    "type": "boolean"
   },
   "project_dir": {
    "description": "Absolute path to the repository root.",
    "type": "string"
   }
  },
  "required": [
   "project_dir"
  ],
  "type": "object"
 },
 "name": "kage_skills"
}
