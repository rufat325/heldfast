{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Earnings Call Transcript"
 },
 "description": "Get the speaker-labelled transcript of a company's earnings call for a fiscal quarter \u2014 every speaker turn in order, attributed to the real person (executive or sell-side analyst) with their role at the time. Identities appear only when the resolution is trusted (auto-resolved or human-reviewed); unverified voices show as a role label (e.g. Operator) or a neutral speaker number. Use GetEarningsCallEvent first to check a transcript exists.",
 "inputSchema": {
  "properties": {
   "fiscalQuarter": {
    "default": null,
    "description": "Company fiscal quarter, 1-4. Quarter alone selects the latest matching quarter across fiscal years; provide both fields for an exact period.",
    "type": [
     "integer",
     "null"
    ]
   },
   "fiscalYear": {
    "default": null,
    "description": "Company fiscal year. Omit both period fields for the latest call; year alone selects the latest call in that fiscal year.",
    "type": [
     "integer",
     "null"
    ]
   },
   "limit": {
    "default": 50,
    "description": "Maximum number of speaker turns to return (default 50, max 200; values outside 1-200 are clamped)",
    "type": "integer"
   },
   "offset": {
    "default": 0,
    "description": "Number of leading speaker turns to skip, for paging through calls longer than the 200-turn cap (default 0)",
    "type": "integer"
   },
   "ticker": {
    "description": "Company ticker symbol (e.g., AAPL, MSFT)",
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "GetEarningsCallTranscript",
 "title": "Earnings Call Transcript"
}
