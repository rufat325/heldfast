{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Databases"
 },
 "description": "Database schema, query, and bulk row operations.\n\nActions (required params -> optional):\n- create (parent_id -> title, properties, is_inline, icon, cover)\n- get (database_id)\n- query (database_id -> filters, sorts, limit, search)\n- create_page (database_id, pages[{properties}])\n- update_page (database_id, page_id, page_properties)\n- delete_page (database_id, page_ids)\n- create_data_source / update_data_source / update_database / list_templates\n\nUse `pages` instead for single page CRUD. Accepts both database_id (from URL) and data_source_id (from workspace search) -- auto-resolved.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Action to perform",
    "enum": [
     "create",
     "get",
     "query",
     "create_page",
     "update_page",
     "delete_page",
     "create_data_source",
     "update_data_source",
     "update_database",
     "list_templates"
    ],
    "type": "string"
   },
   "cover": {
    "description": "Cover image (for update_database): URL or built-in shorthand (gradient_1..11, solid_red/yellow/blue/beige, nasa_*, met_*, rijksmuseum_*, woodcuts_*)",
    "type": "string"
   },
   "data_source_id": {
    "description": "Data source ID (for update_data_source action)",
    "type": "string"
   },
   "database_id": {
    "description": "Database ID (from Notion URL) or data_source_id (from workspace search). Auto-resolved for query/create_page/list_templates.",
    "type": "string"
   },
   "description": {
    "description": "Description",
    "type": "string"
   },
   "filters": {
    "description": "Query filters (for query action)",
    "type": "object"
   },
   "icon": {
    "description": "Icon (for update_database): emoji (e.g. \"(icon)\"), external URL (https://...), or built-in shorthand (name:color, e.g. \"document:gray\")",
    "type": "string"
   },
   "is_inline": {
    "description": "Display as inline (for create/update_database)",
    "type": "boolean"
   },
   "limit": {
    "description": "Max query results",
    "type": "number"
   },
   "page_id": {
    "description": "Single page ID (for update_page)",
    "type": "string"
   },
   "page_ids": {
    "description": "Multiple page IDs (for delete_page)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "page_properties": {
    "description": "Page properties to update (for update_page)",
    "type": "object"
   },
   "pages": {
    "description": "Array of pages for bulk create/update",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "parent_id": {
    "description": "Parent page ID (for create/update_database)",
    "type": "string"
   },
   "properties": {
    "description": "Schema properties (for create/update data source)",
    "type": "object"
   },
   "search": {
    "description": "Smart search across text fields (for query)",
    "type": "string"
   },
   "sorts": {
    "description": "Query sorts",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "title": {
    "description": "Title (for database or data source)",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "databases",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 }
}
