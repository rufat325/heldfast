{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "Create a task"
 },
 "description": "Create a standalone task in DragApp. Tasks are lightweight to-do items that can be assigned, given a due date, and tracked on a board.",
 "inputSchema": {
  "properties": {
   "assignee": {
    "description": "Email address of the assignee",
    "type": "string"
   },
   "boardId": {
    "description": "The board to create the task on",
    "type": "number"
   },
   "columnId": {
    "description": "Column ID to place the task in, e.g. 'Label_1'",
    "type": "string"
   },
   "comment": {
    "description": "Initial comment attached to the task",
    "type": "string"
   },
   "dueDate": {
    "description": "Due date in ISO-8601 format (e.g. 2026-05-20T00:00:00.000Z). Saved via a separate call after the task is created.",
    "type": "string"
   },
   "note": {
    "description": "Initial note attached to the task",
    "type": "string"
   },
   "title": {
    "description": "Task title",
    "type": "string"
   }
  },
  "required": [
   "boardId",
   "columnId",
   "title"
  ],
  "type": "object"
 },
 "name": "create_task",
 "title": "Create a task"
}
