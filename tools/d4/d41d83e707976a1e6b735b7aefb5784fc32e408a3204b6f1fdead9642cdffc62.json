{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List all context keys and values in a space.",
 "inputSchema": {
  "properties": {
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   }
  },
  "required": [
   "spaceId"
  ],
  "type": "object"
 },
 "name": "context_list",
 "outputSchema": {
  "properties": {
   "context": {
    "description": "Map of every context key to its stored value",
    "type": "object"
   },
   "encrypted": {
    "description": "True if the space is end-to-end encrypted (values are ciphertext)",
    "type": "boolean"
   }
  },
  "required": [
   "context"
  ],
  "type": "object"
 }
}
