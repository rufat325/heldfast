{
 "description": "End-to-end game modding assistant. Detects engine, analyzes game structure, and returns a modding plan with available tools and next steps. Start here when modding a new game.",
 "inputSchema": {
  "properties": {
   "game_path": {
    "description": "Full path to the game directory",
    "type": "string"
   },
   "objective": {
    "description": "What you want to mod (e.g. \"infinite health\", \"speed hack\", \"item spawner\")",
    "type": "string"
   }
  },
  "required": [
   "game_path"
  ],
  "type": "object"
 },
 "name": "mod_this_game"
}
