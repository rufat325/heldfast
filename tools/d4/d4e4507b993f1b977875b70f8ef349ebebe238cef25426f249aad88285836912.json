{
 "description": "Instant deterministic HTML mockup of the model (no Android toolchain needed). Provide outPath to write the file, or omit it to get the HTML back.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "modelYaml": {
    "description": "Full app.acm.yaml content",
    "type": "string"
   },
   "outPath": {
    "description": "Optional path for the .html file",
    "type": "string"
   }
  },
  "required": [
   "modelYaml"
  ],
  "type": "object"
 },
 "name": "preview"
}
