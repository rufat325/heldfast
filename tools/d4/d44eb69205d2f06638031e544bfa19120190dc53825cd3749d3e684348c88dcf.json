{
 "description": "[Merchant] List all available report types with their configurations, descriptions, and visibility settings. Production only - not supported in sandbox.",
 "inputSchema": {
  "properties": {
   "partnerId": {
    "default": "heldfast-churn-placeholder",
    "description": "Partner ID from payware dashboard. Uses PAYWARE_PARTNER_ID env var as default.",
    "type": "string"
   },
   "privateKey": {
    "description": "RSA private key for authentication. Uses production private key as default (Data API is production-only).",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "payware_data_list_reports"
}
