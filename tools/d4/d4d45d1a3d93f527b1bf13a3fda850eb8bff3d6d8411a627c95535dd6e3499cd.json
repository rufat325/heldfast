{
 "annotations": {
  "readOnlyHint": true,
  "title": "List cards in a column"
 },
 "description": "List all cards in a specific column of a DragApp board. Supports pagination. Cards are email threads or tasks with titles, assignees, due dates, and custom fields.",
 "inputSchema": {
  "properties": {
   "boardId": {
    "description": "The board ID",
    "type": "number"
   },
   "columnId": {
    "description": "The column ID string (e.g. \"Label_1\") \u2014 use list_columns to find available IDs",
    "type": "string"
   },
   "limit": {
    "description": "Cards per page (default: 10)",
    "type": "number"
   },
   "page": {
    "description": "Page number (default: 1)",
    "type": "number"
   }
  },
  "required": [
   "boardId",
   "columnId"
  ],
  "type": "object"
 },
 "name": "list_cards_in_column",
 "title": "List cards in a column"
}
