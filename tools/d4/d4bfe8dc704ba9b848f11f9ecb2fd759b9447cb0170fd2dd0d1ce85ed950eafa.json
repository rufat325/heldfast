{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Poll the UI until an element containing the given text / content-desc / resource-id appears. Returns its tap coordinates.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "match": {
    "description": "Substring to look for in text, content-desc or resource-id",
    "type": "string"
   },
   "serial": {
    "description": "Device serial (defaults to session or the only device)",
    "type": "string"
   },
   "timeoutSeconds": {
    "description": "Give up after this long (default 15)",
    "type": "integer"
   }
  },
  "required": [
   "match"
  ],
  "type": "object"
 },
 "name": "wait_for_ui",
 "title": "Wait for UI"
}
