{
 "description": "Submit a preflight plan \u2014 an ordered list of intended steps \u2014 for one-card operator review BEFORE executing. Each step is dry-run through the guard pipeline server-side; approved steps become single-use execution authorities. Claim-aware paths such as dashclaw_invoke, governed SDK callbacks, and supported hooks can claim a matching step and consume it with one attempt. Bare dashclaw_guard/record/wait calls remain cooperative policy and audit and do not consume plan authority. Off-plan actions fall back to normal per-action governance.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "agent_id": {
    "description": "Fallback identity when no server-level agent id is configured (the configured id wins). With a configured id you may name a sub-agent under it as \"<configured id>/<name>\" (e.g. claude-desktop/nightly-seo) so this routine gets its own row on /decisions; anything else falls back to the configured id.",
    "type": "string"
   },
   "declared_goal": {
    "description": "The mission-level goal of the plan (required)",
    "type": "string"
   },
   "steps": {
    "description": "Ordered steps: [{ action_type, step_goal, act?, declared_paths?, declared_systems? }] (required)",
    "items": {
     "additionalProperties": {},
     "type": "object"
    },
    "type": "array"
   },
   "ttl_minutes": {
    "description": "Requested grant TTL after approval (server-clamped; default 60)",
    "type": "integer"
   }
  },
  "required": [
   "declared_goal",
   "steps"
  ],
  "type": "object"
 },
 "name": "dashclaw_plan_submit"
}
