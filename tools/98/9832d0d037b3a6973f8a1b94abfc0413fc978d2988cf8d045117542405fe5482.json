{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search 100M+ marine species occurrence records in the Ocean Biodiversity Information System (OBIS). Filter by scientific name, WoRMS AphiaID, geographic coordinates with radius, or year range. Each record includes GPS coordinates, observation year, depth, sea-surface temperature, dataset source, and full taxonomic classification (kingdom\u2192species). Covers all ocean environments: pelagic, benthic, coastal, deep-sea. Source: OBIS \u2014 free, CC BY 4.0, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lat": {
    "description": "Center latitude for geographic search (decimal degrees, -90 to 90). Requires lon and radius.",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "limit": {
    "description": "Number of occurrence records to return (1-100, default 10).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "lon": {
    "description": "Center longitude for geographic search (decimal degrees, -180 to 180). Requires lat and radius.",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "maxyear": {
    "description": "Latest observation year to include (e.g. 2024). Pairs with minyear.",
    "maximum": 2030,
    "minimum": 1600,
    "type": "integer"
   },
   "minyear": {
    "description": "Earliest observation year to include (e.g. 2000). Pairs with maxyear.",
    "maximum": 2030,
    "minimum": 1600,
    "type": "integer"
   },
   "radius": {
    "description": "Search radius in kilometers around the lat/lon center point. Requires lat and lon.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "scientificname": {
    "description": "Scientific name of the marine species to search (e.g. \"Abra alba\", \"Gadus morhua\", \"Thunnus thynnus\"). Supports partial matching.",
    "type": "string"
   },
   "taxonid": {
    "description": "WoRMS AphiaID (numeric taxon identifier). Use instead of or alongside scientificname for precise lookups.",
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "obis.marine.occurrence_search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "OBIS \u2014 Marine Species Occurrence Search"
}
