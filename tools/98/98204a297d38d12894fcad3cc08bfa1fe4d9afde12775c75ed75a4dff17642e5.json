{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get a receipt"
 },
 "description": "Fetch a stored verdict by its content-hash id (GET /api/v1/receipts/{id}), immutable and cacheable. The receipt is content-hashed and reproducible from the open-source core it names. It is not signed.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "pattern": "^[0-9a-f]{24}$",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_receipt",
 "title": "Get a receipt"
}
