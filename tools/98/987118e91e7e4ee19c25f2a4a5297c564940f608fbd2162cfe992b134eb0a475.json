{
 "annotations": {
  "readOnlyHint": true,
  "title": "Get tag group"
 },
 "description": "One tag group: its member tags and the full provider roster resolved from the catalog, each with Kin Score and agent readiness. Filter the roster by industry, region, country or area. Free; the DISTRIBUTION over the roster (`stats`) needs Understanding. Note `source_provider_count` vs `provider_count` \u2014 the first comes from an index that caps band membership at 1,000, the second is resolved live and is the complete number.",
 "inputSchema": {
  "properties": {
   "area": {
    "description": "Narrow the roster to one or more area slugs (comma-separated).",
    "type": "string"
   },
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "country": {
    "description": "Narrow the roster to one or more country slugs (comma-separated).",
    "type": "string"
   },
   "industry": {
    "description": "Narrow the roster to one or more industry slugs (comma-separated).",
    "type": "string"
   },
   "region": {
    "description": "Narrow the roster to one or more region slugs (comma-separated).",
    "type": "string"
   },
   "slug": {
    "description": "Tag group slug, e.g. cards, logistics, wealth-management.",
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "get_tag_group"
}
