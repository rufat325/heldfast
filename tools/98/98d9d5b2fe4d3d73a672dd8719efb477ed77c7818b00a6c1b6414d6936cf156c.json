{
 "description": "Check for or apply gossipcat updates. Detects install method (global npm, local dep, git clone) and fetches the latest version from the npm registry.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "check_only": {
    "default": false,
    "description": "Only check version, do not update",
    "type": "boolean"
   },
   "confirm": {
    "default": false,
    "description": "Set true to actually apply the update",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "gossip_update"
}
