{
 "description": "Read one stored sending-domain resource. This returns what was stored at the last observation. To look again, call `verify`.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "domain_id": {
    "description": "Sending domain ID",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "domain_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_domain_get"
}
