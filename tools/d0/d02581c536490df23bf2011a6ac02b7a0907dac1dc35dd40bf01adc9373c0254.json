{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search contacts by name, email, or custom properties. Use when you need to find specific people in your database.",
 "inputSchema": {
  "examples": [
   {
    "query": "john@example.com"
   },
   {
    "limit": 20,
    "query": "Sarah Smith"
   }
  ],
  "properties": {
   "limit": {
    "description": "Maximum number of results (default 10, max 100)",
    "type": "number"
   },
   "query": {
    "description": "Search query (e.g., name or email)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "hs_search_contacts",
 "outputSchema": {
  "properties": {
   "results": {
    "description": "Array of matching contact objects",
    "items": {
     "properties": {
      "id": {
       "description": "Contact ID",
       "type": "string"
      },
      "properties": {
       "description": "Contact properties",
       "type": "object"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "total": {
    "description": "Total number of matches",
    "type": "number"
   }
  },
  "type": "object"
 },
 "title": "Hs Search Contacts",
 "version": "v1.c108912f"
}
