{
 "description": "Apply one-click unsubscribe through a signed capability. This is the endpoint mail clients call from the `List-Unsubscribe-Post` header, which is why the body is form-encoded and fixed. Calling it twice is the same as calling it once.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "token": {
    "description": "Signed preference token",
    "type": "string"
   }
  },
  "required": [
   "token"
  ],
  "type": "object"
 },
 "name": "commsharbor_preference_unsubscribe"
}
