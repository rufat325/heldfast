{
 "description": "Generate context-appropriate XSS payloads with optional filter evasion. Returns a list of payloads tailored to the injection context and filter bypass requirements. Returns context, filter_bypass, payloads array, notes. Side effects: None. Pure payload generation, no network requests.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "callback_url": {
    "description": "Attacker-controlled URL for data exfiltration payloads",
    "type": "string"
   },
   "context": {
    "description": "Injection context: where the user input lands",
    "enum": [
     "html_body",
     "html_attribute",
     "javascript",
     "url",
     "css"
    ],
    "type": "string"
   },
   "filter_bypass": {
    "description": "Level of filter evasion needed",
    "enum": [
     "none",
     "tag_filter",
     "keyword_filter",
     "waf",
     "aggressive"
    ],
    "type": "string"
   }
  },
  "required": [
   "context"
  ],
  "type": "object"
 },
 "name": "xss_payload_generate"
}
