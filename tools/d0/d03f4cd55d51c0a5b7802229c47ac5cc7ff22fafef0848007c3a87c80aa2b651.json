{
 "description": "List Scope-verified vendors. Each result includes verified-reputation metrics (on-time %, budget variance, rework rate, completed matters, satisfaction) and credentialing summary. Vendor names are returned only for verified callers; anonymous callers get anonymized labels.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "category_slug": {
    "description": "Service category slug to filter by (e.g. 'court-reporting'). Omit for all.",
    "type": "string"
   },
   "jurisdiction": {
    "description": "Federal district or state code.",
    "type": "string"
   },
   "limit": {
    "default": 20,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "scope_list_vendors"
}
