{
 "description": "Close a DashClaw session and update its status. Call this when the task is complete or if the session needs to be marked as failed. Provides a clean lifecycle boundary for governance reporting in the Approvals inbox.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "session_id": {
    "description": "Session ID from dashclaw_session_start",
    "type": "string"
   },
   "status": {
    "description": "Final session status",
    "enum": [
     "completed",
     "failed",
     "cancelled"
    ],
    "type": "string"
   },
   "summary": {
    "description": "Brief description of what was accomplished",
    "type": "string"
   }
  },
  "required": [
   "session_id",
   "status"
  ],
  "type": "object"
 },
 "name": "dashclaw_session_end"
}
