{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get full detail for a single earthquake event from the EMSC real-time seismicity catalog by its event ID (unid), as returned by emsc.search_earthquakes. Optionally include all reported magnitude estimates and origin solutions, not just the preferred ones. Data: seismicportal.eu (EMSC), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "eventid": {
    "description": "EMSC event unique ID / unid (e.g. 20260827_0000123), as returned by emsc.search_earthquakes.",
    "minLength": 1,
    "type": "string"
   },
   "includeallmagnitudes": {
    "description": "Include all reported magnitude estimates for the event, not just the preferred one (default false).",
    "type": "boolean"
   },
   "includeallorigins": {
    "description": "Include all reported origin solutions for the event, not just the preferred one (default false).",
    "type": "boolean"
   }
  },
  "required": [
   "eventid"
  ],
  "type": "object"
 },
 "name": "emsc.seismic.event_detail",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Get EMSC Earthquake Event Detail"
}
