{
 "description": "Run local JVM unit tests (test<Variant>UnitTest) and return a parsed pass/fail summary with failure messages.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "module": {
    "description": "Gradle module, e.g. 'app' (defaults to session)",
    "type": "string"
   },
   "projectDir": {
    "description": "Project root (defaults to session)",
    "type": "string"
   },
   "tests": {
    "description": "Filter passed to --tests, e.g. 'com.foo.MyTest' or '*MyTest*'",
    "type": "string"
   },
   "variant": {
    "description": "Build variant, e.g. 'debug' (defaults to session)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "test_app",
 "title": "Run unit tests"
}
