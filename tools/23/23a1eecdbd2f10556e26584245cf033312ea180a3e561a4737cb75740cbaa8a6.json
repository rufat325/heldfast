{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List federal/provincial departments and agencies publishing on the Government of Canada open data portal, with dataset counts, optionally filtered by a name substring. Use the returned org slug as the `organization` filter for open-canada.dataset_search. Data: open.canada.ca CKAN Action API, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max organizations to return (1-50, default 20)",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Substring filter on organization title (e.g. \"natural resources\")",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "open-canada.reference.organization_list",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Canada Open Data Organization List"
}
