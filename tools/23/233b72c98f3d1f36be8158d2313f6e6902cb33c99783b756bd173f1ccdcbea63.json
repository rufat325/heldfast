{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Query any Detroit ArcGIS layer by service path + layer id. Full ArcGIS query: where, out_fields, order_by, limit. Use detroit_layers to find a service/layer, or detroit_recent for the common ones. Epoch dates are converted to ISO.",
 "inputSchema": {
  "examples": [
   {
    "layer": 0,
    "limit": 50,
    "service": "crime",
    "where": "OFFENSE='THEFT/OTHER'"
   },
   {
    "layer": 39,
    "limit": 100,
    "order_by": "incident_occurred_at DESC",
    "service": "RMS_Crime_Incidents/FeatureServer"
   }
  ],
  "properties": {
   "layer": {
    "description": "Layer id within the service (from detroit_layers), e.g. 39.",
    "type": "number"
   },
   "limit": {
    "description": "Max rows (default 100, max 2000).",
    "type": "number"
   },
   "order_by": {
    "description": "Sort clause, e.g. \"incident_occurred_at DESC\".",
    "type": "string"
   },
   "out_fields": {
    "description": "Comma-separated fields, or \"*\" (default).",
    "type": "string"
   },
   "service": {
    "description": "ArcGIS service path, e.g. \"RMS_Crime_Incidents/FeatureServer\" (or a short name: crime|service_requests|permits).",
    "type": "string"
   },
   "where": {
    "description": "ArcGIS SQL where (default \"1=1\").",
    "type": "string"
   }
  },
  "required": [
   "service",
   "layer"
  ],
  "type": "object"
 },
 "name": "detroit_query",
 "title": "Detroit Query",
 "version": "v1.1978a425"
}
