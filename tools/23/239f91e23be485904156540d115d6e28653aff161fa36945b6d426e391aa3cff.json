{
 "description": "NOAA NCEI monthly average temperature (GSOM) for one weather station, with station metadata and observation date. $0.001 per call via x402 (USDC on Base); response includes a provenance chain and an Ed25519 attestation.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "end": {
    "maxLength": 12,
    "type": "string"
   },
   "entity": {
    "maxLength": 24,
    "type": "string"
   },
   "payment": {
    "description": "Encoded x402 X-PAYMENT header value. Omit to receive the payment requirements (free quote).",
    "type": "string"
   },
   "start": {
    "maxLength": 12,
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "station_monthly_temp"
}
