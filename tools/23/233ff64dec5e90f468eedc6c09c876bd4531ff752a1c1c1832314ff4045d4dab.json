{
 "description": "Return the per-UTC-day median + sample count for one service over the last N days (default 30, max 180).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "days": {
    "maximum": 180,
    "minimum": 1,
    "type": "integer"
   },
   "slug": {
    "minLength": 1,
    "type": "string"
   },
   "unit": {
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "historical_fmv",
 "title": "Per-day FMV history for a service"
}
