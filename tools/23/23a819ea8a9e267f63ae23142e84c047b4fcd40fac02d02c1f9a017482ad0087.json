{
 "description": "Record user satisfaction feedback for a genome. Positive signals reinforce current gene configuration. Negative signals trigger evolution on the next cycle.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "genome_id": {
    "description": "Genome ID to record feedback for",
    "type": "string"
   },
   "note": {
    "description": "Optional feedback note",
    "type": "string"
   },
   "quality": {
    "description": "Quality score 0-1 (optional, derived from satisfied if omitted)",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "satisfied": {
    "description": "Whether the user was satisfied with the last response",
    "type": "boolean"
   },
   "user_id": {
    "default": "mcp-user",
    "description": "User identifier",
    "type": "string"
   }
  },
  "required": [
   "genome_id",
   "satisfied"
  ],
  "type": "object"
 },
 "name": "gsep_record_feedback"
}
