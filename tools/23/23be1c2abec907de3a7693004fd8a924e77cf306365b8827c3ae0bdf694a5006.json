{
 "description": "Return per-day macro totals over a date range, as a compact block for coaching. Feed this to the model so it can program diet around real intake. Specify start/end (YYYY-MM-DD) or days (default last 14).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "days": {
    "description": "Trailing days if no start.",
    "maximum": 365,
    "minimum": 1,
    "type": "integer"
   },
   "end": {
    "description": "End date YYYY-MM-DD (default today).",
    "type": "string"
   },
   "start": {
    "description": "Start date YYYY-MM-DD.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_history",
 "title": "Get nutrition history"
}
