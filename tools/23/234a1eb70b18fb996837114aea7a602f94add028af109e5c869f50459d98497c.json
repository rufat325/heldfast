{
 "description": "Soft-delete the link between an AnchorID and a source record. Idempotent \u2014 calling on an already-unlinked pair returns 200.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "entity_id": {
    "description": "UUID of the AnchorID to unlink from",
    "type": "string"
   },
   "source_record_id": {
    "description": "UUID of the source record to unlink",
    "type": "string"
   }
  },
  "required": [
   "entity_id",
   "source_record_id"
  ],
  "type": "object"
 },
 "name": "unlink_source_record"
}
