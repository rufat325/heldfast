{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Query Base event logs"
 },
 "description": "Query smart-contract event logs on Base by contract address, topics and bounded block range. Costs $0.005 USDC via x402.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "Smart-contract address on Base mainnet.",
    "pattern": "^0x[0-9a-fA-F]{40}$",
    "type": "string"
   },
   "from_block": {
    "description": "First Base block to include in the log query.",
    "minimum": 0,
    "type": "integer"
   },
   "to_block": {
    "description": "Last Base block to include in the log query.",
    "minimum": 0,
    "type": "integer"
   },
   "topics": {
    "description": "Up to four Ethereum log topics; null keeps a topic position as a wildcard.",
    "items": {
     "type": [
      "string",
      "null"
     ]
    },
    "maxItems": 4,
    "type": "array"
   }
  },
  "required": [
   "address"
  ],
  "type": "object"
 },
 "name": "base_logs"
}
