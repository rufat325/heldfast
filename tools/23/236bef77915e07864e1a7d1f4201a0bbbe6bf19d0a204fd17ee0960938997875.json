{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search California State Parks GIS open geospatial datasets (parks, trails, recreation & natural-resource layers) by keyword. Returns each dataset's name, summary, record_count, owner/org, and its Feature Service `url` \u2014 pass that url to query_layer / layer_info.",
 "inputSchema": {
  "examples": [
   {
    "query": "trails"
   },
   {
    "limit": 10,
    "query": "parks recreation"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max datasets (1-50, default 20).",
    "type": "number"
   },
   "org_id": {
    "description": "Optional ArcGIS orgId to override the default (California State Parks GIS).",
    "type": "string"
   },
   "query": {
    "description": "Keyword(s), e.g. \"parcels\", \"crime\", \"flood zones\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_datasets",
 "title": "Search Datasets",
 "version": "v1.c870308a"
}
