{
 "description": "Respond to a customer review on the App Store. Creates or updates your developer response.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "responseBody": {
    "description": "Your response text (visible on App Store)",
    "type": "string"
   },
   "reviewId": {
    "description": "Review ID (from asc_list_customer_reviews)",
    "type": "string"
   }
  },
  "required": [
   "reviewId",
   "responseBody"
  ],
  "type": "object"
 },
 "name": "asc_respond_to_review"
}
