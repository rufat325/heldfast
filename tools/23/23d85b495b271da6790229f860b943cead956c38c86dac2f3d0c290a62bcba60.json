{
 "description": "Register referral attribution as an explicit separate step. Requires an agent-signed approval and an authorized caller.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "agent_address": {
    "description": "Ethereum address of the agent being attributed",
    "pattern": "^0x[0-9a-fA-F]{40}$",
    "type": "string"
   },
   "deadline": {
    "description": "Approval expiry as unix timestamp in seconds",
    "type": "string"
   },
   "referrer_address": {
    "description": "Ethereum address of the referrer to attribute",
    "pattern": "^0x[0-9a-fA-F]{40}$",
    "type": "string"
   },
   "signature": {
    "description": "EIP-712 signature by the agent approving the referral attribution",
    "pattern": "^0x[0-9a-fA-F]+$",
    "type": "string"
   }
  },
  "required": [
   "agent_address",
   "referrer_address",
   "deadline",
   "signature"
  ],
  "type": "object"
 },
 "name": "clicks_register_referral"
}
