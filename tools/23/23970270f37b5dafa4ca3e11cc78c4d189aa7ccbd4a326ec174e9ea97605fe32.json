{
 "_meta": {
  "skill": "bounty-scanner"
 },
 "description": "Post a new bounty on aibtc.com. Requires Genesis-level (L2+) registration.\n\nFields:\n- title: short description (max 120 chars)\n- description: full task details (max 4000 chars; markdown allowed)\n- reward_sats: reward in satoshis (min 1)\n- expires_at: ISO 8601 deadline for new submissions. Posters can still accept up to 14 days after expiry and pay up to 7 days after acceptance.\n- tags: optional list (max 5 tags, max 24 chars each)\n\nSigns with BIP-322 over: \"AIBTC Bounty Create | {posterBtc} | {title} | {description} | {rewardSats} | {expiresAt} | {tagsCommaJoined} | {signedAt}\"\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "description": {
    "description": "Task description (max 4000 chars)",
    "maxLength": 4000,
    "minLength": 1,
    "type": "string"
   },
   "expires_at": {
    "description": "ISO 8601 expiry timestamp (e.g. '2026-06-01T00:00:00Z'). Min 1 hour, max 365 days from now.",
    "type": "string"
   },
   "reward_sats": {
    "description": "Reward in satoshis",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "tags": {
    "description": "Up to 5 tags",
    "items": {
     "maxLength": 24,
     "type": "string"
    },
    "maxItems": 5,
    "type": "array"
   },
   "title": {
    "description": "Bounty title (max 120 chars)",
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "title",
   "description",
   "reward_sats",
   "expires_at"
  ],
  "type": "object"
 },
 "name": "bounty_create"
}
