{
 "description": "Send multiple raw HTTP requests on a single TLS connection. Useful for testing connection-level attacks like request smuggling, pipeline confusion, and socket poisoning.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "requests": {
    "description": "Array of raw HTTP request strings to send sequentially on one connection",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "target_host": {
    "description": "Target hostname",
    "type": "string"
   },
   "target_port": {
    "default": 443,
    "description": "Target port (default 443)",
    "type": "number"
   }
  },
  "required": [
   "target_host",
   "requests"
  ],
  "type": "object"
 },
 "name": "raw_connection_reuse"
}
