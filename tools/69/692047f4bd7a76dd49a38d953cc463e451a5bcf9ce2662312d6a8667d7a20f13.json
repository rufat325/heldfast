{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Read actual rows from a HealthData.gov dataset by its Socrata 4x4 id. Optional SoQL `where` (e.g. \"state = 'CA'\") and `order` (e.g. \"date DESC\"). Use get_dataset first to see the column names. This returns the DATA, not a download link.",
 "inputSchema": {
  "examples": [
   {
    "id": "g62h-syeh",
    "limit": 5
   },
   {
    "id": "g62h-syeh",
    "limit": 10,
    "order": "date DESC",
    "where": "state = 'CA'"
   }
  ],
  "properties": {
   "id": {
    "description": "Socrata 4x4, e.g. \"g62h-syeh\".",
    "type": "string"
   },
   "limit": {
    "description": "Rows to return, 1-1000 (default 20).",
    "type": "number"
   },
   "offset": {
    "description": "Skip this many rows (default 0).",
    "type": "number"
   },
   "order": {
    "description": "SoQL ordering, e.g. \"date DESC\".",
    "type": "string"
   },
   "where": {
    "description": "SoQL filter, e.g. \"state = 'CA'\".",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_rows",
 "title": "Get Rows",
 "version": "v1.42337312"
}
