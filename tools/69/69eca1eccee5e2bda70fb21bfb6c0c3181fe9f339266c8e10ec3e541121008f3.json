{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search for cryptocurrencies by name or symbol. Returns matching coins with their IDs. Example: search_coins({ query: \"bitcoin\" })",
 "inputSchema": {
  "examples": [
   {
    "query": "bitcoin"
   },
   {
    "query": "ETH"
   }
  ],
  "properties": {
   "query": {
    "description": "Search query \u2014 coin name or symbol, e.g. \"bitcoin\", \"ETH\", \"sol\"",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_coins",
 "outputSchema": {
  "properties": {
   "coins": {
    "description": "List of matching coins (max 20)",
    "items": {
     "properties": {
      "id": {
       "description": "CoinGecko coin ID",
       "type": "string"
      },
      "market_cap_rank": {
       "description": "Rank by market cap or null if unranked",
       "type": [
        "number",
        "null"
       ]
      },
      "name": {
       "description": "Coin name",
       "type": "string"
      },
      "symbol": {
       "description": "Coin symbol",
       "type": "string"
      }
     },
     "required": [
      "id",
      "name",
      "symbol",
      "market_cap_rank"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "count": {
    "description": "Number of matching coins found",
    "type": "number"
   }
  },
  "required": [
   "count",
   "coins"
  ],
  "type": "object"
 },
 "title": "Search Coins",
 "version": "v1.28f01863"
}
