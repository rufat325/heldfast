{
 "description": "Send one controlled permission-based marketing smoke. Like the transactional smoke, the destination is a server-side secret. `contact_id` must be the CRM contact that matches it \u2014 you cannot point this at an arbitrary person.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "contact_id": {
    "description": "CRM contact matching the server-side QA recipient",
    "type": "string"
   },
   "domain_id": {
    "description": "Active sending domain ID",
    "type": "string"
   },
   "idempotency_key": {
    "description": "Unique send key",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "domain_id",
   "contact_id",
   "idempotency_key"
  ],
  "type": "object"
 },
 "name": "commsharbor_marketing_smoke"
}
