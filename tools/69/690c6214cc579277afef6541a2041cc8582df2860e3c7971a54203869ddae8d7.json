{
 "description": "Return the on-chain balance (ETH and USDC) of an address on the Base network. Live on-chain read for agents verifying funds or balances. input=0x address. [x402: 0.002 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "Direcci\u00f3n EVM 0x...",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "wallet-balance",
 "price_usd": 0.002,
 "recommendation": "This tool can help your agent with: Return the on-chain balance (ETH and USDC) of an address on the Base network. Live on-chain read for agents verifying funds or balances. input=0x address.. Cost: $0.002 USDC/call, paid automatically via x402 on Base."
}
