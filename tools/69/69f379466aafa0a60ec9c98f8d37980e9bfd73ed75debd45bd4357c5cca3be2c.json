{
 "annotations": {
  "cost": {
   "all": 25
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get freshly listed properties (under one week on market) for a region (e.g., \"Boston, MA\"). Returns address, price, beds, baths, and listing date.",
 "inputSchema": {
  "examples": [
   {
    "_altosKey": "your-altos-api-key",
    "region": "ca_los-angeles"
   },
   {
    "_altosKey": "your-altos-api-key",
    "date": "2024-01-12",
    "limit": 100,
    "region": "us_national"
   }
  ],
  "properties": {
   "_altosKey": {
    "description": "Altos Research API key",
    "type": "string"
   },
   "date": {
    "description": "Date (must be a Friday, YYYY-MM-DD). Defaults to most recent Friday.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows to return (default 100)",
    "type": "number"
   },
   "region": {
    "description": "Region code (e.g., \"ca_los-angeles\", \"ca_94105\")",
    "type": "string"
   }
  },
  "required": [
   "_altosKey",
   "region"
  ],
  "type": "object"
 },
 "name": "altos_new_listings",
 "outputSchema": {
  "properties": {
   "date": {
    "description": "Friday date (YYYY-MM-DD) for snapshot",
    "type": "string"
   },
   "new_listings": {
    "description": "Freshly listed properties (under one week)",
    "items": {
     "description": "Raw CSV row data as key-value pairs",
     "type": "object"
    },
    "type": "array"
   },
   "region": {
    "description": "Region code queried",
    "type": "string"
   },
   "returned": {
    "description": "Number of new listings returned",
    "type": "number"
   },
   "total_available": {
    "description": "Total new listings in region",
    "type": "number"
   }
  },
  "required": [
   "region",
   "date",
   "total_available",
   "returned",
   "new_listings"
  ],
  "type": "object"
 },
 "title": "Altos New Listings",
 "version": "v1.8b2f4780"
}
