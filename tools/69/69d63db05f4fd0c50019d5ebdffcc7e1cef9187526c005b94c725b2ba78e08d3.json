{
 "description": "Search TFT traits by name or description. Omit the query to list all traits in the current set.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 50,
    "description": "Max results to return, 1-100 (default: 50)",
    "maximum": 100,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "description": "Free-text search across trait name and description (uses FTS5)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_traits"
}
