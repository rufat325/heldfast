{
 "_meta": {
  "skill": "identity"
 },
 "description": "Register a new agent identity on-chain using ERC-8004 identity registry. Returns a transaction ID. Check the transaction result to get the assigned agent ID. Requires an unlocked wallet.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "fee": {
    "description": "Fee preset (\"low\", \"medium\", \"high\") or micro-STX amount. Optional.",
    "type": "string"
   },
   "metadata": {
    "description": "Array of metadata key-value pairs. Values must be hex-encoded buffers. Optional.",
    "items": {
     "properties": {
      "key": {
       "description": "Metadata key (max 128 chars)",
       "maxLength": 128,
       "type": "string"
      },
      "value": {
       "description": "Metadata value as hex string (max 512 bytes)",
       "type": "string"
      }
     },
     "required": [
      "key",
      "value"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "sponsored": {
    "default": false,
    "description": "Use sponsored transaction relay so a relay service pays fees instead of your wallet. Requires SPONSOR_API_KEY env var or wallet-level sponsorApiKey.",
    "type": "boolean"
   },
   "uri": {
    "description": "URI pointing to agent metadata (IPFS, HTTP, etc.). Optional.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "identity_register"
}
