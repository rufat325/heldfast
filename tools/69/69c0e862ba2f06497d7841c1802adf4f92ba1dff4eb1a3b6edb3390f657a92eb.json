{
 "description": "Update an existing resource in place by ID \u2014 edit a file's title, path, content, or status, or rename/reconfigure an agent, skill, workflow, or project. Only the fields you pass are changed; for files, a new version is created automatically so history is preserved. Set `type` to the resource kind and `id` to its UUID (from list/search/get).\n\nFile linking (type=file): to link to another file in the SAME workspace, put //<fileId> inline in the content \u2014 use the target's UUID (from search/list/get results), not its title. The link renders as the target's title and is re-indexed on save; backlinks are automatic. To add a backlink to file B, add //<B-id> to this file's content. Remove a link by deleting its //<id> token. Only link to files that exist. Use get({ type: \"file_links\", id }) to inspect links and backlinks.",
 "inputSchema": {
  "properties": {
   "agentIds": {
    "description": "Agent IDs (type=workflow)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "changeSummary": {
    "type": "string"
   },
   "content": {
    "type": "string"
   },
   "createVersion": {
    "description": "Create version history entry (default true)",
    "type": "boolean"
   },
   "customContext": {
    "description": "Custom context (type=agent)",
    "type": "string"
   },
   "description": {
    "description": "New description (type=agent, workflow, project)",
    "type": "string"
   },
   "folderId": {
    "type": "string"
   },
   "id": {
    "description": "Resource ID",
    "type": "string"
   },
   "isPrivate": {
    "description": "Workflow visibility (type=workflow). true = only you, the workspace owner/admin, and explicitly-shared members can see it. false = visible to the whole team.",
    "type": "boolean"
   },
   "name": {
    "description": "New name (type=agent, workflow, project)",
    "type": "string"
   },
   "path": {
    "type": "string"
   },
   "projectId": {
    "description": "Move file to different library (type=file)",
    "type": "string"
   },
   "projectIds": {
    "description": "Library IDs (type=workflow)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "skillIds": {
    "description": "Skill IDs (type=workflow)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "slug": {
    "description": "Slug (type=project)",
    "type": "string"
   },
   "status": {
    "enum": [
     "active",
     "archived"
    ],
    "type": "string"
   },
   "systemPrompt": {
    "description": "System prompt (type=agent)",
    "type": "string"
   },
   "tagIds": {
    "description": "Tag IDs (type=workflow)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "title": {
    "type": "string"
   },
   "type": {
    "description": "Resource type to update",
    "enum": [
     "file",
     "agent",
     "skill",
     "workflow",
     "project"
    ],
    "type": "string"
   },
   "workspaceId": {
    "type": "string"
   }
  },
  "required": [
   "type",
   "id"
  ],
  "type": "object"
 },
 "name": "update"
}
