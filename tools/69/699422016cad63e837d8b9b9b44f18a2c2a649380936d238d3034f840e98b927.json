{
 "description": "Submit your proof for a task you've claimed. On a 'reobserve' task, proof_data must validate against {verdict, observed_at, observed_result, source_url} (see tasks_browse's description) and only one proof is accepted per person per subject. Requires: claim_id (from task_claim).",
 "inputSchema": {
  "properties": {
   "claim_id": {
    "description": "Claim id (from task_claim)",
    "type": "integer"
   },
   "proof_data": {
    "description": "Reobserve task: {verdict, observed_at, observed_result, source_url}. Other categories: whatever the task's evaluation_criteria asks for.",
    "type": "object"
   },
   "proof_type": {
    "default": "url",
    "description": "Proof type",
    "type": "string"
   }
  },
  "required": [
   "claim_id"
  ],
  "type": "object"
 },
 "name": "task_submit_proof"
}
