{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Journal LOG"
 },
 "contract": {
  "auth_scope": "public",
  "idempotent": true,
  "output_schema": {
   "result": "string"
  },
  "price": {
   "free_tier": "unlimited",
   "paid": "included in Zambo Pass",
   "per_call": "free"
  },
  "side_effects": false
 },
 "description": "Log an action performed outside Zambo into an append-only job timeline. Zambo records the report and does not claim it ran or observed the action.",
 "destructiveHint": false,
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "completed_at": {
    "description": "RFC 3339 completion timestamp.",
    "type": "string"
   },
   "duration_ms": {
    "description": "Reported duration in milliseconds.",
    "type": "number"
   },
   "executor_identity": {
    "description": "Agent, client, or local executor that reported the action.",
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   },
   "external_executor": {
    "description": "External server or executor name when relevant.",
    "maxLength": 120,
    "type": "string"
   },
   "inputs": {
    "description": "Optional local input object. It is redacted and hashed, then discarded.",
    "type": "object"
   },
   "job_id": {
    "description": "Stable job or session identifier shared by the agent.",
    "maxLength": 160,
    "minLength": 1,
    "type": "string"
   },
   "metadata": {
    "description": "Optional redacted display metadata.",
    "type": "object"
   },
   "redacted_inputs_hash": {
    "description": "Hash of redacted inputs. Raw inputs are never stored.",
    "maxLength": 160,
    "type": "string"
   },
   "started_at": {
    "description": "RFC 3339 start timestamp.",
    "type": "string"
   },
   "tool_name": {
    "description": "Tool or action name reported by the agent.",
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "job_id",
   "tool_name",
   "executor_identity"
  ],
  "type": "object"
 },
 "name": "journal_log",
 "openWorldHint": true,
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "result": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "readOnlyHint": true,
 "title": "Journal LOG"
}
