{
 "description": "Get detailed activity log for a Jules session. Activities include plan generation, progress updates, messages, and completion events. Most recent activities appear first.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "default": 10,
    "description": "Number of activities to retrieve (default: 10)",
    "type": "number"
   },
   "sessionId": {
    "description": "Session ID to get activities for",
    "type": "string"
   }
  },
  "required": [
   "sessionId"
  ],
  "type": "object"
 },
 "name": "jules_list_activities",
 "title": "List Jules Session Activities"
}
