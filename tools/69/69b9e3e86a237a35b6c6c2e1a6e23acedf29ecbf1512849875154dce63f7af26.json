{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Personalize \u2014 Choose which concrete content option to show a specific user and how to frame it. Send profile, candidate content options, and a goal; the model may return a best fit or no-selection when nothing clearly matches. Use it to rank existing variants, not to invent a whole campaign from scratch. (4 MESH/call, a tool \u00b7 personalization)",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "Payload for personalize",
    "properties": {
     "content": {
      "description": "REQUIRED \u2014 the candidate content options (array of strings; the handler maps over it)",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "goal": {
      "description": "REQUIRED \u2014 what the personalization is for, e.g. maximize trial signup",
      "type": "string"
     },
     "profile": {
      "description": "REQUIRED \u2014 what you know about the user, e.g. {segment: pro, locale: en-US}",
      "type": "object"
     }
    },
    "required": [
     "profile",
     "content",
     "goal"
    ],
    "type": "object"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "personalize",
 "title": "Personalize"
}
