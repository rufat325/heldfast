{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Export Roadmap"
 },
 "description": "Export a .nowline roadmap to any of the eight canonical formats. Byte-identical to `nowline -f <format>` for the same source and inputs. For pdf/xlsx/png: omit `delivery` and omit `output` \u2014 the server auto-saves to the configured folder and returns the path. Never use `delivery:\"inline\"` for binary formats on Claude Desktop (inline binary cannot be displayed). For svg/html/mermaid: the text is returned inline \u2014 present it to the user as a downloadable artifact of the matching type.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "delivery": {
    "description": "\"file\" \u2014 write to disk and return the path (no inline bytes). \"inline\" \u2014 return bytes in the response (embedded resource for pdf/xlsx, image for png, text for other formats). \"both\" \u2014 write to disk and also attach inline bytes. Default is smart per format: pdf/xlsx/png write to disk when a root folder is configured, else return inline; text formats (svg/html/mermaid/msproj) always return inline. IMPORTANT \u2014 for pdf/xlsx/png on Claude Desktop, prefer omitting `delivery` entirely (smart default saves the file). Do NOT use delivery:\"inline\" for pdf/xlsx \u2014 Claude Desktop silently drops inline documents, so nothing appears to the user.",
    "enum": [
     "file",
     "inline",
     "both"
    ],
    "type": "string"
   },
   "format": {
    "description": "Export format: svg, pdf, html, mermaid, xlsx, msproj, or png.",
    "enum": [
     "svg",
     "pdf",
     "html",
     "mermaid",
     "xlsx",
     "msproj",
     "png"
    ],
    "type": "string"
   },
   "marginPt": {
    "description": "PDF margin in points.",
    "type": "number"
   },
   "msprojStart": {
    "description": "MS Project start date override (YYYY-MM-DD).",
    "type": "string"
   },
   "now": {
    "description": "Now-line date as YYYY-MM-DD (UTC).",
    "type": "string"
   },
   "orientation": {
    "description": "PDF orientation.",
    "enum": [
     "portrait",
     "landscape",
     "auto"
    ],
    "type": "string"
   },
   "output": {
    "description": "Optional path to write the output file. Omit for binary formats (pdf, xlsx, png) \u2014 the server auto-saves to the configured folder as <roadmap-id>.<ext>. To choose a filename, pass a bare filename (e.g. \"roadmap.pdf\") \u2014 it is saved into the configured folder. Never pass a virtual or sandbox path such as /home/claude/\u2026, /mnt/user-data/\u2026, or any path outside the configured output folder \u2014 those do not exist on the host filesystem.",
    "type": "string"
   },
   "pageSize": {
    "description": "PDF page size.",
    "type": "string"
   },
   "path": {
    "description": "Real local filesystem path to the .nowline file (e.g. /Users/name/Desktop/foo.nowline). Never pass a virtual or sandbox path such as /mnt/user-data/\u2026 \u2014 those do not exist on the host filesystem. Pass `source` instead.",
    "type": "string"
   },
   "scale": {
    "description": "PNG scale factor.",
    "type": "number"
   },
   "source": {
    "description": "Inline .nowline source text.",
    "type": "string"
   },
   "theme": {
    "enum": [
     "light",
     "dark",
     "grayscale"
    ],
    "type": "string"
   }
  },
  "required": [
   "format"
  ],
  "type": "object"
 },
 "name": "export",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bytes": {
    "type": "number"
   },
   "format": {
    "type": "string"
   },
   "path": {
    "type": "string"
   }
  },
  "required": [
   "format"
  ],
  "type": "object"
 }
}
