{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Preview the N-of-1 protocol for a supplement \u00d7 goal WITHOUT starting it: the dosing, schedule, and the baseline + check-in questions (with their ids, types, and options). Call this first so you know which questions to answer before `supstack_experiment_start`. Supplement is a slug, goal is a goal id (see supstack_goals). Requires the user to be signed in (`supstack login`).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "goal": {
    "minLength": 1,
    "type": "string"
   },
   "supplement": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "supplement",
   "goal"
  ],
  "type": "object"
 },
 "name": "supstack_experiment_protocol"
}
