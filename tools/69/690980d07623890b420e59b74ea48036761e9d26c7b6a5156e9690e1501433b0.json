{
 "description": "TLS Cert Lifetime Lint - SC-081v3 renewal check - sweep a folder and return every finding (licence; $29 once)",
 "inputSchema": {
  "properties": {
   "dir": {
    "type": "string"
   },
   "ext": {
    "description": "optional extension filter, e.g. .html",
    "type": "string"
   }
  },
  "required": [
   "dir"
  ],
  "type": "object"
 },
 "name": "check_dir"
}
