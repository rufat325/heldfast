{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Query US government statistical data (Census/ACS, BLS, IPEDS, and more) from Data USA. Group by one or more dimensions (e.g. State, Year, Industry) and retrieve one or more measures (e.g. Population, Median Household Income), optionally restricted to specific member keys. Returns dataset source/citation plus the resulting rows (Data USA, Deloitte/Datawheel/MIT Media Lab, US government open data)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cube": {
    "description": "Data cube (dataset) name to query, e.g. \"acs_yg_total_population_1\". Look up names via datausa.reference.cubes.",
    "minLength": 1,
    "type": "string"
   },
   "drilldowns": {
    "description": "Dimension level names to group the results by, e.g. [\"State\"] or [\"State\", \"Year\"]. Look up available levels for a cube via datausa.reference.cubes.",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "include": {
    "description": "Restrict results to specific member keys, formatted \"Level:key,key;Level2:key\" (e.g. \"State:04000US06\" for California only, or \"Year:2021,2022\"). Find member keys via datausa.reference.members.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of result rows to return (default 100, max 1000).",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "measures": {
    "description": "Measure (metric) names to retrieve, e.g. [\"Population\"]. Look up available measures for a cube via datausa.reference.cubes.",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "offset": {
    "description": "Number of result rows to skip, for pagination (default 0).",
    "minimum": 0,
    "type": "integer"
   },
   "sort": {
    "description": "Sort order as \"field\" or \"field.order\" where order is asc/desc, e.g. \"Population.desc\".",
    "type": "string"
   }
  },
  "required": [
   "cube",
   "drilldowns",
   "measures"
  ],
  "type": "object"
 },
 "name": "datausa.data.query",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Query US Public Data"
}
