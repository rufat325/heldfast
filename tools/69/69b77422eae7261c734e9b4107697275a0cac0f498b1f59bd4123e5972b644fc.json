{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Run one of the registrar's one-shot administrative actions on a domain, when a process is stuck waiting on it: resend the ICANN registrant verification mail (raa_verify \u2014 an unverified domain gets SUSPENDED after 15 days, so this is the fix for \"my domain stopped working after I registered it\"), resend the GDPR contact-disclosure mail, resend the transfer approval mail to the current owner, or activate Impreza's own DNS for the domain. None of them charge anything.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "raa_verify | gdpr_auth | transfer_approval | activate_dns.",
    "enum": [
     "raa_verify",
     "gdpr_auth",
     "transfer_approval",
     "activate_dns"
    ],
    "type": "string"
   },
   "domain": {
    "description": "The domain name.",
    "type": "string"
   }
  },
  "required": [
   "domain",
   "action"
  ],
  "type": "object"
 },
 "name": "impreza_domain_registrar_action"
}
