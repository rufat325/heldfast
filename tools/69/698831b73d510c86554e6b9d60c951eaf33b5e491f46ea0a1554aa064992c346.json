{
 "description": "Add a product to the Instacart cart. Searches for the product and adds the first matching result.",
 "inputSchema": {
  "properties": {
   "product": {
    "description": "Product name or search query",
    "type": "string"
   },
   "quantity": {
    "description": "Quantity to add (default: 1)",
    "type": "number"
   }
  },
  "required": [
   "product"
  ],
  "type": "object"
 },
 "name": "instacart_add_to_cart"
}
