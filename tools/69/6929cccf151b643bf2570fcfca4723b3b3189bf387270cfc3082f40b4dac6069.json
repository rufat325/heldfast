{
 "description": "Create a new Jules session and immediately wait/poll for its completion. Automatically tracks progress, emits progress notifications, and returns a unified payload with final state, summary, and PR details upon resolution. Bounded by a wait limit to prevent timeouts.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "autoApprove": {
    "default": true,
    "description": "Automatically approve the execution plan (default: true). Set false to manually approve with jules_approve_plan",
    "type": "boolean"
   },
   "autoCreatePR": {
    "default": false,
    "description": "Automatically create pull request when task completes (default: false)",
    "type": "boolean"
   },
   "branch": {
    "default": "main",
    "description": "Starting branch name (default: main)",
    "type": "string"
   },
   "includeActivities": {
    "default": 5,
    "description": "Number of recent activities to include in the output (default: 5)",
    "type": "number"
   },
   "maxWaitSeconds": {
    "default": 60,
    "description": "Maximum time to wait/poll in seconds (default: 60, max: 300)",
    "type": "number"
   },
   "prompt": {
    "description": "Detailed task description - be specific about what needs to be done",
    "type": "string"
   },
   "repoName": {
    "description": "GitHub repository name",
    "type": "string"
   },
   "repoOwner": {
    "description": "GitHub repository owner (username or organization)",
    "type": "string"
   },
   "title": {
    "description": "Optional custom title for the session",
    "type": "string"
   }
  },
  "required": [
   "repoOwner",
   "repoName",
   "prompt"
  ],
  "type": "object"
 },
 "name": "jules_execute_and_wait",
 "title": "Create and Wait for Jules Session"
}
