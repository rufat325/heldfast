{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Unarchive Ticket Tier"
 },
 "description": "Restore an archived ticket tier to active (back on sale). Requires event_id + tier_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "tier_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "tier_id"
  ],
  "type": "object"
 },
 "name": "unarchive_ticket_tier",
 "title": "Unarchive Ticket Tier"
}
