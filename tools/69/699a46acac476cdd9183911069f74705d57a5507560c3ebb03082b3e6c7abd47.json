{
 "description": "PAID $0.10 per call (https://api.6766587364.lol/v1/breadth). Market regime check: top-50 breadth, BTC dominance, XLM relative strength and a single risk-on / risk-off verdict. Returns a payment requirement until called with a `payment` header, a prepaid `bundle_token`, or a free-tier `api_key` (mint one with get_free_api_key).",
 "inputSchema": {
  "properties": {
   "api_key": {
    "default": null,
    "title": "Api Key",
    "type": "string"
   },
   "bundle_token": {
    "default": null,
    "title": "Bundle Token",
    "type": "string"
   },
   "payment": {
    "default": null,
    "title": "Payment",
    "type": "string"
   }
  },
  "title": "tool_get_market_breadthArguments",
  "type": "object"
 },
 "name": "get_market_breadth",
 "title": "$0.10 \u2014 get_market_breadth"
}
