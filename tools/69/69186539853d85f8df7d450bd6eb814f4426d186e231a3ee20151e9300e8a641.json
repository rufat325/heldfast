{
 "annotations": {
  "readOnlyHint": true,
  "title": "Find regions"
 },
 "description": "Browse geographic regions; sort by provider/API count. Returns the 18 regions themselves. The collection also holds region x industry crossover pages (nordics-healthcare and 1,097 more); those are excluded unless you ask for them with type.",
 "inputSchema": {
  "properties": {
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "limit": {
    "default": 25,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "page": {
    "default": 1,
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "type": "string"
   },
   "sort": {
    "type": "string"
   },
   "type": {
    "default": "region",
    "description": "Which record kind to list. `region` (default) is the dimension itself; `region-industry` is the crossover pages; `all` is both.",
    "enum": [
     "region",
     "region-industry",
     "all"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "find_regions"
}
