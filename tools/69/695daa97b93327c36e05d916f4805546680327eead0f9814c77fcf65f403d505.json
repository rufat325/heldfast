{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search HM Land Registry Price Paid records \u2014 individual residential property sales in England & Wales since 1995. Filter by postcode, town, district, county, price range and sale-date range; sort and page through results. Each record returns price paid (\u00a3GBP), sale date, full address, property type, tenure (freehold/leasehold) and new-build flag. Postcodes must be UPPERCASE with a space (e.g. \"SW1A 1AA\").",
 "inputSchema": {
  "examples": [
   {
    "maxPrice": 2000000,
    "minDate": "2020-01-01",
    "minPrice": 500000,
    "pageSize": 25,
    "postcode": "SW1A 1AA",
    "sort": "-transactionDate"
   },
   {
    "county": "GREATER MANCHESTER",
    "maxDate": "2024-12-31",
    "minDate": "2023-01-01",
    "page": 0,
    "sort": "-pricePaid",
    "town": "MANCHESTER"
   }
  ],
  "properties": {
   "county": {
    "description": "County, exact match, e.g. \"GREATER MANCHESTER\".",
    "type": "string"
   },
   "date": {
    "description": "Exact sale date, YYYY-MM-DD. Overrides minDate/maxDate.",
    "type": "string"
   },
   "district": {
    "description": "Local district, exact match, e.g. \"MANCHESTER\".",
    "type": "string"
   },
   "maxDate": {
    "description": "Latest sale date, YYYY-MM-DD (inclusive).",
    "type": "string"
   },
   "maxPrice": {
    "description": "Maximum price paid in whole GBP (inclusive).",
    "type": "number"
   },
   "minDate": {
    "description": "Earliest sale date, YYYY-MM-DD (inclusive). Data starts 1995.",
    "type": "string"
   },
   "minPrice": {
    "description": "Minimum price paid in whole GBP (inclusive).",
    "type": "number"
   },
   "page": {
    "description": "0-indexed page number. Default 0.",
    "type": "number"
   },
   "pageSize": {
    "description": "Results per page, 1-500. Default 25.",
    "type": "number"
   },
   "postcode": {
    "description": "UK postcode, UPPERCASE with a space, e.g. \"WA2 8SN\". Exact match.",
    "type": "string"
   },
   "sort": {
    "description": "Sort field. Prefix with \"-\" for descending. Common: \"-transactionDate\" (newest first), \"transactionDate\", \"-pricePaid\", \"pricePaid\". Default newest first.",
    "type": "string"
   },
   "town": {
    "description": "Post town, exact match (case-insensitive in practice), e.g. \"WARRINGTON\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_transactions",
 "title": "Search Transactions",
 "version": "v1.4af31e7a"
}
