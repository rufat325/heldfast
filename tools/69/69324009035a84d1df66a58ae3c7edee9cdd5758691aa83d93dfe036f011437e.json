{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Reference list of countries/territories UNHCR tracks. Use this to map a country name to the `code` you pass as coo/coa in the other tools. Each item has: code (UNHCR 3-letter, USE THIS for filtering), iso (true ISO3), iso2, name, region, majorArea. Paginated \u2014 large list, raise `limit` or page through (response.maxPages).",
 "inputSchema": {
  "examples": [
   {
    "limit": 50
   }
  ],
  "properties": {
   "limit": {
    "description": "Rows per page (default 25; raise to fetch more at once).",
    "type": "integer"
   },
   "page": {
    "description": "1-based page; see response.maxPages.",
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_countries",
 "title": "List Countries",
 "version": "v1.d01528cc"
}
