{
 "description": "List individual flow results for an upload. Each result includes id, test_file_name, status, fail_reason, duration_seconds, retry_of. Optional client-side status filter.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "status": {
    "description": "Filter results client-side by status.",
    "enum": [
     "PENDING",
     "QUEUED",
     "RUNNING",
     "PASSED",
     "FAILED",
     "CANCELLED"
    ],
    "type": "string"
   },
   "uploadId": {
    "type": "string"
   }
  },
  "required": [
   "uploadId"
  ],
  "type": "object"
 },
 "name": "get_results"
}
