{
 "description": "Make a content item (not a brief; already assigned via assign_to_cluster) the pillar of a topic cluster. Replaces any sitting pillar, which stays as a supporting piece.",
 "inputSchema": {
  "properties": {
   "cluster_id": {
    "type": "string"
   },
   "content_id": {
    "description": "Content already in the cluster to elect as its pillar",
    "type": "string"
   }
  },
  "required": [
   "cluster_id",
   "content_id"
  ],
  "type": "object"
 },
 "name": "set_cluster_pillar"
}
