{
 "description": "Return the graph around a person \u2014 all companies where they hold a role, and optionally (depth=2) other people connected through those companies. Use to map a person's full portfolio of Slovak companies.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "depth": {
    "default": 1,
    "description": "Graph traversal depth (1 or 2). Default 1.",
    "enum": [
     1,
     2
    ],
    "type": "integer"
   },
   "id": {
    "description": "Internal person ID.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_person_graph"
}
