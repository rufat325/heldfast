{
 "_meta": {
  "fastmcp": {
   "tags": []
  }
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Add a product demo, walkthrough or footage to Brand DNA. With only\nthe file, Gemini reads frames across the clip and writes what it\nshows, when to use it, tags and the best moments (scan_status\nscanning, then ready in about 15 seconds; poll list_brand_videos).\nFilms play it when a brief names it (\"launch film with our checkout\ndemo\"). Videos are for films; stills are painted from pictures.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "base64": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "The clip bytes as base64, if you have no URL."
   },
   "description": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "What the clip shows, in your words. When set, Siren files it without reading it."
   },
   "name": {
    "default": "",
    "description": "Human-readable name, e.g. \"checkout demo\".",
    "type": "string"
   },
   "tags": {
    "anyOf": [
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Short lowercase search words."
   },
   "url": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Public https URL of the clip (MP4, MOV or WebM, up to 200 MB and 5 minutes)."
   },
   "use_when": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "When a film should play it, e.g. \"any film about checkout\"."
   }
  },
  "type": "object"
 },
 "name": "upload_brand_video",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 }
}
