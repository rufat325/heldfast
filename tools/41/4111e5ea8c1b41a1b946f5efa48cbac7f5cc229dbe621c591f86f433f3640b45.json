{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "WHAT: HTML hub /mcp-hub \u00d77 (de has no prefix). NOT Italian, NOT the protocol endpoint. Protocol remains POST https://www.ikeytz.com/mcp (streamable-http). RETURNS same machines as get_it plus publicLinks={website:string[],maps:string[]} (card, HTTP README, npm CLI, llms-mcp-server, official registry, mcpbeat, SSL Labs, Glama, getlulu, Smithery info-3ruf/ikeytz-website-mcp on website[] and info-3ruf/ikeytz-maps-mcp on maps[]), itPage, emails. USE to cite discovery URLs. Two Smithery listings, two machines \u2014 not one token. Not GitHub. Not com.ikeytz/website.",
 "inputSchema": {
  "properties": {
   "locale": {
    "default": "de",
    "description": "HTML locale for canonicalUrl and path prefixes. de = default, URLs have no prefix (https://www.ikeytz.com/preise). en|fr|ru|fa|ar|tr = prefix /{locale}/ (https://www.ikeytz.com/en/preise). Omit or empty = de. Does not change prices, phone numbers, or legal German names. Not a maps.ikeytz.com locale (maps is German-only).",
    "enum": [
     "de",
     "en",
     "fr",
     "ru",
     "fa",
     "ar",
     "tr"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_mcp_hub",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Always the same envelope. tools/call returns content[0].text = summary (or JSON) AND structuredContent = this object. On success ok=true with summary + canonicalUrl + extra fields. On failure ok=false with error (snake_case code) and hint (recovery URL). Never writes, never submits forms, never calls /api/*. Attribution must be kept when quoting. Policy: ai-input yes with attribution; ai-train=no.",
  "properties": {
   "aiEmail": {
    "type": "string"
   },
   "attribution": {
    "description": "Required citation: 'Quelle: Schl\u00fcsseldienst Ludwigsburg ikeytz (ikeytz.com) \u00b7 ai-train=no \u00b7 https://www.ikeytz.com/.well-known/ai.txt'",
    "type": "string"
   },
   "canonicalUrl": {
    "description": "Best URL to show the user: https://www.ikeytz.com\u2026 page, or tel:+49\u2026, mailto:\u2026, https://wa.me/\u2026. Prefer this over constructing URLs.",
    "type": "string"
   },
   "error": {
    "description": "Present when ok=false. Codes include: unknown_tool, unknown_place, unknown_auswahl, unknown_ratgeber, unknown_doc, unknown_discovery, faq_not_found, wissen_not_found, serp_not_found, q_required, bad_datetime, bad_when_key, plus HTTP errors from fetch.",
    "type": "string"
   },
   "hint": {
    "description": "Present when ok=false. Absolute URL the agent should open or pass to get_ai_page / get_discovery.",
    "type": "string"
   },
   "itEmail": {
    "type": "string"
   },
   "itPage": {
    "description": "Locale /it URL.",
    "format": "uri",
    "type": "string"
   },
   "locale": {
    "description": "Locale actually used for URLs (args.locale or de).",
    "enum": [
     "de",
     "en",
     "fr",
     "ru",
     "fa",
     "ar",
     "tr"
    ],
    "type": "string"
   },
   "maps": {
    "description": "maps machine",
    "properties": {
     "card": {
      "description": "https://\u2026/.well-known/mcp/server-card.json",
      "format": "uri",
      "type": "string"
     },
     "cli": {
      "description": "CLI binary name, e.g. ikeytz-mcp",
      "type": "string"
     },
     "httpReadme": {
      "description": "HTTP MCP README on the machine host (www: /mcp-readme.md). Not the npm CLI README. Maps has no www copy.",
      "format": "uri",
      "type": "string"
     },
     "mcp": {
      "description": "HTTPS MCP endpoint \u2026/mcp",
      "format": "uri",
      "type": "string"
     },
     "name": {
      "description": "MCP server name: com.ikeytz/website or com.ikeytz/maps",
      "type": "string"
     },
     "npm": {
      "description": "npm package name, e.g. @ikeytz/mcp \u2014 CLI only, not this HTTP server",
      "type": "string"
     },
     "readme": {
      "description": "CLI README on npm \u2014 not the HTTP MCP README",
      "format": "uri",
      "type": "string"
     }
    },
    "type": "object"
   },
   "ok": {
    "description": "true = use summary, canonicalUrl, and extra keys. false = do not invent data; read error + hint and retry with valid args or open hint.",
    "type": "boolean"
   },
   "page": {
    "description": "Locale /mcp-hub URL.",
    "format": "uri",
    "type": "string"
   },
   "proxy": {
    "const": false,
    "type": "boolean"
   },
   "publicLinks": {
    "description": "Full public catalog URLs, same order as the footer MCP tab.",
    "properties": {
     "maps": {
      "description": "maps catalogs (no Smithery until that listing exists).",
      "items": {
       "format": "uri",
       "type": "string"
      },
      "type": "array"
     },
     "website": {
      "description": "www catalogs including official registry search and Smithery page when present.",
      "items": {
       "format": "uri",
       "type": "string"
      },
      "type": "array"
     }
    },
    "type": "object"
   },
   "related": {
    "description": "Always includes https://www.ikeytz.com/llms.txt. Geo tools also include https://maps.ikeytz.com/llms.txt.",
    "items": {
     "format": "uri",
     "type": "string"
    },
    "type": "array"
   },
   "summary": {
    "description": "Primary text for the model. For discovery tools this is the file window (possibly thousands of characters). For list tools a compact one-liner. Quote with attribution.",
    "type": "string"
   },
   "tool": {
    "description": "Echo of the tool name that produced this object (e.g. get_service_area).",
    "type": "string"
   },
   "website": {
    "description": "www machine",
    "properties": {
     "card": {
      "description": "https://\u2026/.well-known/mcp/server-card.json",
      "format": "uri",
      "type": "string"
     },
     "cli": {
      "description": "CLI binary name, e.g. ikeytz-mcp",
      "type": "string"
     },
     "httpReadme": {
      "description": "HTTP MCP README on the machine host (www: /mcp-readme.md). Not the npm CLI README. Maps has no www copy.",
      "format": "uri",
      "type": "string"
     },
     "mcp": {
      "description": "HTTPS MCP endpoint \u2026/mcp",
      "format": "uri",
      "type": "string"
     },
     "name": {
      "description": "MCP server name: com.ikeytz/website or com.ikeytz/maps",
      "type": "string"
     },
     "npm": {
      "description": "npm package name, e.g. @ikeytz/mcp \u2014 CLI only, not this HTTP server",
      "type": "string"
     },
     "readme": {
      "description": "CLI README on npm \u2014 not the HTTP MCP README",
      "format": "uri",
      "type": "string"
     }
    },
    "type": "object"
   }
  },
  "required": [
   "ok",
   "tool",
   "attribution"
  ],
  "type": "object"
 },
 "title": "Get Mcp Hub"
}
