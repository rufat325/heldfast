{
 "description": "Stop a running build. Cost: 0 credits.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "execution_id": {
    "description": "The execution_id returned by build_company.",
    "type": "string"
   }
  },
  "required": [
   "execution_id"
  ],
  "type": "object"
 },
 "name": "stop_build"
}
