{
 "annotations": {
  "readOnlyHint": false
 },
 "description": "Apply a batch of path-addressed edits to a tailored CV, atomically. Read cv_context and cv_get first \u2014 indices are counted over the document cv_get returned. THE HONEST WALL: editing with an API key edits as the tailoring agent, and the server enforces it \u2014 the candidate's own name, email, phone and links are refused, and anything stating what they DID needs evidence_id, the id of something they asserted themselves. One uncited op rejects the whole batch. For a missing_have requirement, reframe an existing bullet; for a missing_gap, ask the candidate before writing anything. A bad path is a 422 and the CV is untouched. Export with cv_render.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "The CV id, from cv_tailor or cv_list (or the tailoring workspace URL /tailor/<job>?cv=<id>). Opaque \u2014 never construct or guess one.",
    "minLength": 1,
    "type": "string"
   },
   "note": {
    "description": "Your own one-line reason for the change; shown to the candidate as the agent's words.",
    "type": "string"
   },
   "ops": {
    "description": "Every edit that belongs together, in one call.",
    "items": {
     "additionalProperties": false,
     "description": "One path-addressed operation.",
     "properties": {
      "evidence_id": {
       "description": "Id of the banked achievement backing this claim. Required for anything stating what the candidate did.",
       "type": "string"
      },
      "kind": {
       "description": "set replaces a node, insert adds one, remove deletes one, move reorders within its list.",
       "enum": [
        "set",
        "insert",
        "remove",
        "move"
       ],
       "type": "string"
      },
      "path": {
       "description": "Where to edit, 0-indexed over what cv_get returned: summary, experience[2].bullets[1], experience[0].stack[0], skills[0].items[3], education[1].degree, style.font_size.",
       "minLength": 1,
       "type": "string"
      },
      "to": {
       "description": "Destination index. Only for move.",
       "type": "integer"
      },
      "value": {
       "description": "The new content. Omit for remove and move."
      }
     },
     "required": [
      "kind",
      "path"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "id",
   "ops"
  ],
  "type": "object"
 },
 "name": "cv_edit"
}
