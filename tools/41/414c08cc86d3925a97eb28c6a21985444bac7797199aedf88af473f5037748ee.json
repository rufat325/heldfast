{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false,
  "title": "Delete Vault Entry"
 },
 "description": "Soft-delete a vault entry. Entry is deactivated and excluded from list results. Logged in the audit trail.",
 "inputSchema": {
  "properties": {
   "entry_id": {
    "description": "UUID of the vault entry to delete",
    "type": "string"
   }
  },
  "required": [
   "entry_id"
  ],
  "type": "object"
 },
 "name": "vault_delete"
}
