{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Compute the aspects from a transiting sky to a natal chart \u2014 how the planets\nat one moment relate to the planets at a birth moment.\n\nExamples:\n  - \"What's transiting my chart today, born 1990-03-15 15:45 Riga time?\" ->\n    { natal_datetime: \"1990-03-15T15:45\", natal_timezone: \"Europe/Riga\" }\n    (transit_datetime defaults to now)\n\nFor the exact date a specific transit perfects, use find_aspect_times instead.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "natal_datetime": {
    "description": "Birth date and time.",
    "minLength": 4,
    "type": "string"
   },
   "natal_timezone": {
    "description": "IANA timezone the datetime is local to, e.g. \"Europe/Riga\" or \"America/New_York\". Omit when the datetime already carries Z or a UTC offset.",
    "minLength": 1,
    "type": "string"
   },
   "transit_datetime": {
    "description": "Moment of the transiting sky. Defaults to now.",
    "minLength": 4,
    "type": "string"
   },
   "transit_timezone": {
    "description": "IANA timezone the datetime is local to, e.g. \"Europe/Riga\" or \"America/New_York\". Omit when the datetime already carries Z or a UTC offset.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "natal_datetime"
  ],
  "type": "object"
 },
 "name": "compute_transits",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "transits": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "angle": {
       "type": "number"
      },
      "aspect": {
       "type": "string"
      },
      "natal": {
       "type": "string"
      },
      "orb": {
       "type": "number"
      },
      "retrograde": {
       "type": "boolean"
      },
      "transiting": {
       "type": "string"
      }
     },
     "required": [
      "transiting",
      "natal",
      "aspect",
      "angle",
      "orb",
      "retrograde"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "transits"
  ],
  "type": "object"
 },
 "title": "Compute Transits"
}
