{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get articles published since a given date, ordered by publication date descending. Free access \u2014 no API key required.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max results (default 20, max 50)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   },
   "since_date": {
    "description": "ISO 8601 date or datetime (e.g. \"2026-02-01\" or \"2026-02-01T00:00:00Z\")",
    "type": "string"
   }
  },
  "required": [
   "since_date"
  ],
  "type": "object"
 },
 "name": "sr_get_new_since",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Tool-specific result object. Inspect the `content` array for human-readable text and structured data.",
  "type": "object"
 }
}
