{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Diagnose the mediamcp server setup: endpoint, API key presence and validity, default models, and output directory writability. Run this first when any other mediamcp tool fails.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "ping": {
    "default": true,
    "description": "Also verify the API key against the endpoint with a live request.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "check_config",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "api_key": {
    "description": "Masked key or 'not set'",
    "type": "string"
   },
   "api_key_source": {
    "type": "string"
   },
   "base_url": {
    "type": "string"
   },
   "image_model": {
    "type": "string"
   },
   "key_check": {
    "type": "string"
   },
   "ok": {
    "type": "boolean"
   },
   "output_dir": {
    "type": "string"
   },
   "output_dir_writable": {
    "type": "boolean"
   },
   "preview": {
    "type": "string"
   },
   "problems": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "schema_dialect": {
    "description": "JSON Schema dialect the tool schemas are advertised in",
    "type": "string"
   },
   "version": {
    "type": "string"
   },
   "video_model": {
    "type": "string"
   }
  },
  "required": [
   "ok",
   "version",
   "base_url",
   "image_model",
   "video_model",
   "output_dir",
   "output_dir_writable",
   "schema_dialect",
   "api_key",
   "preview",
   "problems"
  ],
  "type": "object"
 },
 "title": "Check mediamcp Configuration"
}
