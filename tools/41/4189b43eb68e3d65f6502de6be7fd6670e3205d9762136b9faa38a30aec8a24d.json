{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get a ranked top-chart list (top free, top paid, top grossing, etc.) for a country and category. Android uses `cat_key` (e.g. \"OVERALL\"); iOS uses `genre_id` (e.g. \"36\" for the overall/all-apps genre) plus optional `device_type`. Example: fortytwo_top_charts({ platform: \"android\", list_name: \"topselling_free\", cat_key: \"OVERALL\", country: \"US\", limit: 10, _apiKey: \"your-token\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-42matters-api-key",
    "cat_key": "OVERALL",
    "country": "US",
    "limit": 10,
    "list_name": "topselling_free",
    "platform": "android"
   },
   {
    "_apiKey": "your-42matters-api-key",
    "country": "US",
    "device_type": "iphone",
    "genre_id": "36",
    "limit": 25,
    "list_name": "topgrossing",
    "platform": "ios"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "42matters API access token (get one at https://42matters.com/pricing).",
    "type": "string"
   },
   "cat_key": {
    "description": "Android only \u2014 category key, e.g. \"OVERALL\" or a Google Play category id. Default \"OVERALL\".",
    "type": "string"
   },
   "country": {
    "description": "2-letter country code for the chart (default \"US\").",
    "type": "string"
   },
   "device_type": {
    "description": "iOS only \u2014 \"iphone\" or \"ipad\" (optional).",
    "type": "string"
   },
   "genre_id": {
    "description": "iOS only \u2014 App Store genre/category id (e.g. \"36\" for all apps).",
    "type": "string"
   },
   "limit": {
    "description": "Number of ranked results (1-100). Default 10.",
    "type": "number"
   },
   "list_name": {
    "description": "Chart type. Android: \"topselling_free\", \"topselling_paid\", \"topgrossing\", \"movers_shakers\", \"topselling_new_free\", \"topselling_new_paid\". iOS: \"topselling_free\", \"topselling_paid\", \"topgrossing\". Default \"topselling_free\".",
    "type": "string"
   },
   "page": {
    "description": "Page number for pagination (optional).",
    "type": "number"
   },
   "platform": {
    "description": "App store to query: \"android\" (Google Play) or \"ios\" (Apple App Store). Defaults to \"android\".",
    "enum": [
     "android",
     "ios"
    ],
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "fortytwo_top_charts",
 "title": "Fortytwo Top Charts",
 "version": "v1.38e314a2"
}
