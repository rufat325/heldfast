{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Alert Events Feed"
 },
 "description": "The detected alert events for your workspace, newest first \u2014 the same feed as the in-app Recent Alerts panel and the email digest: rank drops/gains, top-10 entries/exits, new rankings, rating drops, review spikes, and competitor changes, each with a type-specific payload (app/keyword names, old vs new values). Events only exist for alert types you've enabled rules for (sonar_set_alert); detection runs once daily. Poll with `since` to react to changes programmatically. Requires an Indie plan (trial counts).",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "app_id": {
    "description": "Filter to events about one tracked app (Sonar app UUID or store id).",
    "minLength": 1,
    "type": "string"
   },
   "limit": {
    "description": "Max events to return (1-200). Default 50.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "since": {
    "description": "Only events created at/after this ISO 8601 timestamp (e.g. \"2026-08-01T00:00:00Z\"). Use your last poll time to fetch only new events.",
    "minLength": 1,
    "type": "string"
   },
   "type": {
    "description": "Filter to one alert type. Omit for all types.",
    "enum": [
     "rank_drop",
     "rank_gain",
     "entered_top10",
     "left_top10",
     "new_ranking",
     "rating_drop",
     "review_spike",
     "competitor_change"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "sonar_alert_events",
 "title": "Alert Events Feed"
}
