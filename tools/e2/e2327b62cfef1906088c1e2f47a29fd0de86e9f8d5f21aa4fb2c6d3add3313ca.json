{
 "description": "Analyze a codebase for dead code \u2014 functions, classes, and modules that are defined but never referenced. Returns findings with file paths, line numbers, severity, and fix suggestions.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "include_suggestions": {
    "default": true,
    "description": "Whether to include fix suggestions",
    "type": "boolean"
   },
   "repo_path": {
    "description": "Path or URL to the repository to analyze",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "analyze_dead_code"
}
