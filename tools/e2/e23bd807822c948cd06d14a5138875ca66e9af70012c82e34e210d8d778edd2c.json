{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Trigger a deployment for every application in a project (optionally a single environment). Asks for confirmation on clients that support elicitation.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "environment": {
    "type": "string"
   },
   "force": {
    "description": "Force rebuild without cache",
    "type": "boolean"
   },
   "project_uuid": {
    "type": "string"
   }
  },
  "required": [
   "project_uuid"
  ],
  "type": "object"
 },
 "name": "redeployProject",
 "title": "Redeploy all project applications"
}
