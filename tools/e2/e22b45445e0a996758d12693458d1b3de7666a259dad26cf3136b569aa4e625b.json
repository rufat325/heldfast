{
 "description": "[GA4 WRITE] Create a Firebase link (no update; delete + recreate to change). Requires GA4_MCP_ENABLE_WRITES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "confirm": {
    "description": "Must be true to apply the change.",
    "type": "boolean"
   },
   "project": {
    "description": "Firebase project id or number (\"projects/{id}\").",
    "minLength": 1,
    "type": "string"
   },
   "property": {
    "description": "GA4 property ID, e.g. \"123456789\" or \"properties/123456789\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "property",
   "project",
   "confirm"
  ],
  "type": "object"
 },
 "name": "ga4_create_firebase_link"
}
