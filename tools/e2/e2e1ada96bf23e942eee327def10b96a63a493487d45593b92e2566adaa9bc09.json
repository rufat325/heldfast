{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "\"Resolve [domain]\" / \"what IP does [site] point at\" / \"A records for [X]\" \u2014 forward DNS lookup via HackerTarget, returning the A/AAAA/MX/NS/SOA records the resolver holds for a hostname. Use to check where a domain currently points, or to confirm a DNS change has propagated.",
 "inputSchema": {
  "examples": [
   {
    "target": "pipeworx.io"
   }
  ],
  "properties": {
   "target": {
    "description": "Hostname to resolve, e.g. \"pipeworx.io\". Not a URL \u2014 strip the scheme and path.",
    "type": "string"
   }
  },
  "required": [
   "target"
  ],
  "type": "object"
 },
 "name": "dns_lookup",
 "title": "Dns Lookup",
 "version": "v1.006e7d24"
}
