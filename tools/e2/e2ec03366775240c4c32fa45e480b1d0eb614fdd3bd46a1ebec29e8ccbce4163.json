{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List globally supported DWG types (not file-specific). Supports regex filtering and cursor pagination.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "cursor": {
    "description": "Opaque cursor from a previous response.",
    "type": "string"
   },
   "limit": {
    "default": 100,
    "description": "Maximum number of items to return.",
    "minimum": 1,
    "type": "integer"
   },
   "regex": {
    "description": "Optional regex over typeName, genericType, or aliases.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "dwg.list_types"
}
