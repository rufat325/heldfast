{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "list-application-owners"
 },
 "description": "Retrieve a list of owners for an application that are directoryObject types.\n\nTIP: Lists owners of an app registration. Owners can manage the app including adding credentials. Returns user objects. Audit this to ensure apps have appropriate owners and no orphaned apps exist.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "ConsistencyLevel": {
    "description": "ConsistencyLevel",
    "type": "string"
   },
   "applicationId": {
    "description": "Path parameter: applicationId",
    "type": "string"
   },
   "count": {
    "description": "count",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "filter": {
    "description": "OData $filter expression, e.g. status eq 'inProgress'",
    "type": "string"
   },
   "orderby": {
    "description": "orderby",
    "type": "string"
   },
   "search": {
    "description": "search",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "skip": {
    "description": "skip",
    "type": "string"
   },
   "top": {
    "description": "Page size (Graph $top). Start small (5-15) so responses fit the model context. Use @odata.nextLink from the response for more rows.",
    "type": "number"
   }
  },
  "required": [
   "applicationId"
  ],
  "type": "object"
 },
 "name": "list-application-owners"
}
