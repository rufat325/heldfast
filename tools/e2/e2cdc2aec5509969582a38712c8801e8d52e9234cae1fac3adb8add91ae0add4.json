{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Take a screenshot of the device/emulator screen and return it as an image. With annotate: true (needs the Google android CLI), UI elements get numbered bounding boxes \u2014 including text inside images/WebViews that snapshot_ui cannot see.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "annotate": {
    "description": "Draw labeled bounding boxes around UI elements (vision-based)",
    "type": "boolean"
   },
   "serial": {
    "description": "Device serial (defaults to session or the only device)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "screenshot",
 "title": "Screenshot"
}
