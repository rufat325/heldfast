{
 "description": "Extend (heartbeat) the current lease so long-running work is not reclaimed out from under you. If the lease already expired and was reclaimed (or the token is unknown), returns renewed:false \u2014 do NOT assume you still hold the account; call lease_account again.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lease_token": {
    "description": "The lease_token returned by lease_account.",
    "minLength": 1,
    "type": "string"
   },
   "ttl_seconds": {
    "description": "New lease lifetime in seconds from now. Defaults to the server default TTL.",
    "exclusiveMinimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "lease_token"
  ],
  "type": "object"
 },
 "name": "renew_lease"
}
