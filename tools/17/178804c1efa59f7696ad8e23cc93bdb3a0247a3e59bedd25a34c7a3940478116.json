{
 "description": "Run a read-only transaction simulation with eth_call and eth_estimateGas against a supported public EVM RPC without signing or broadcasting. Price: 0.005 USDC. Mode: live_source. Source: public EVM JSON-RPC. Set auto_pay=true for a one-call purchase when the local buyer wallet is configured.",
 "inputSchema": {
  "properties": {
   "agent_token": {
    "description": "Optional pre-funded marketplace bearer token.",
    "type": "string"
   },
   "auto_pay": {
    "description": "Automatically authorize and pay this request with the locally configured buyer wallet. Requires X402_AUTO_PAY=true and X402_EVM_PRIVATE_KEY.",
    "type": "boolean"
   },
   "data": {
    "description": "Hex-encoded transaction calldata.",
    "type": "string"
   },
   "from": {
    "description": "Sender address.",
    "type": "string"
   },
   "network": {
    "description": "Blockchain network identifier.",
    "type": "string"
   },
   "payment_id": {
    "description": "payment_id returned by the x402 invoice.",
    "type": "string"
   },
   "payment_signature": {
    "description": "Base64 x402 v2 PAYMENT-SIGNATURE produced by a compatible buyer client.",
    "type": "string"
   },
   "preview": {
    "description": "Inspect a rate-limited live contract proof showing source, freshness, response shape, and required inputs. Decision-ready values remain in the paid result.",
    "type": "boolean"
   },
   "to": {
    "description": "Destination contract address.",
    "type": "string"
   },
   "tx_hash": {
    "description": "Base transaction hash after paying an x402 invoice.",
    "type": "string"
   },
   "value": {
    "description": "Amount of native token sent in wei.",
    "type": "string"
   }
  },
  "required": [
   "network",
   "from",
   "to",
   "data"
  ],
  "type": "object"
 },
 "name": "tx_simulator"
}
