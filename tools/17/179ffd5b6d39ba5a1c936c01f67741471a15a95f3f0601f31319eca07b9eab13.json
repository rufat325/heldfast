{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search emails using Gmail query syntax (e.g., 'from:sender@example.com', 'subject:invoice', 'has:attachment', 'after:2024/01/01', 'is:unread'). Returns matching message IDs.",
 "inputSchema": {
  "examples": [
   {
    "query": "from:bob@example.com after:2024/01/01 has:attachment"
   },
   {
    "max_results": 50,
    "query": "is:unread subject:invoice"
   }
  ],
  "properties": {
   "max_results": {
    "description": "Maximum number of messages to return (default 10, max 100)",
    "type": "number"
   },
   "query": {
    "description": "Gmail search query (e.g., \"from:bob@example.com after:2024/01/01 has:attachment\")",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "gmail_search",
 "outputSchema": {
  "properties": {
   "error": {
    "description": "Error code if connection not authenticated",
    "type": "string"
   },
   "message": {
    "description": "Error message if connection not authenticated",
    "type": "string"
   },
   "messages": {
    "description": "Array of matching message summaries",
    "items": {
     "properties": {
      "id": {
       "description": "Message ID",
       "type": "string"
      },
      "labelIds": {
       "description": "Applied label IDs",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "threadId": {
       "description": "Thread ID",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "nextPageToken": {
    "description": "Token for fetching next page of results",
    "type": "string"
   },
   "resultSizeEstimate": {
    "description": "Estimated total number of matching messages",
    "type": "number"
   }
  },
  "type": "object"
 },
 "title": "Gmail Search",
 "version": "v1.793e0864"
}
