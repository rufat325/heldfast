{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "OSM Taginfo: search OpenStreetMap keys by text, ranked by total usage. Useful for discovering how features are tagged in OSM. Keyless.",
 "inputSchema": {
  "examples": [
   {
    "query": "cuisine"
   },
   {
    "limit": 30,
    "query": "restaurant"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max keys to return (default 20).",
    "type": "number"
   },
   "query": {
    "description": "Text to search keys for, e.g. \"cuisine\".",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_keys",
 "title": "Search Keys",
 "version": "v1.c83842ff"
}
