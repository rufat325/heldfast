{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List tags"
 },
 "description": "List tags across every board you can access. Tags belong to boards, so the same name on two boards is two different tags \u2014 each item carries its board. For one board use list_board_tags.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {},
  "type": "object"
 },
 "name": "list_tags",
 "title": "List tags"
}
