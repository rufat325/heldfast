{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get a mutual-fund scheme's NAV (net asset value) history from MFAPI \u2014 fund house, category, ISIN, latest NAV, and the most-recent NAV points (newest first). Use search_schemes to find a scheme_code first (e.g. 118550). Keyless.",
 "inputSchema": {
  "examples": [
   {
    "scheme_code": 118550
   },
   {
    "limit": 60,
    "scheme_code": 118550
   }
  ],
  "properties": {
   "limit": {
    "description": "Number of most-recent NAV points to return (default 30, max 120).",
    "type": "number"
   },
   "scheme_code": {
    "description": "AMFI scheme code, e.g. 118550. Get one from search_schemes.",
    "type": "number"
   }
  },
  "required": [
   "scheme_code"
  ],
  "type": "object"
 },
 "name": "get_nav_history",
 "title": "Get Nav History",
 "version": "v1.e212f8d3"
}
