{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List NSI Bulgaria open-data datasets with their numeric id and English (or Bulgarian) name. Each dataset id can be passed to get_dataset for the actual figures. Use the optional `filter` to substring-match dataset names (case-insensitive), e.g. 'population', 'export', 'inflation'.",
 "inputSchema": {
  "examples": [
   {
    "filter": "population"
   },
   {
    "filter": "inflation",
    "lang": "bg"
   }
  ],
  "properties": {
   "filter": {
    "description": "Case-insensitive substring to match against dataset names.",
    "type": "string"
   },
   "lang": {
    "description": "Language of dataset names. Default en.",
    "enum": [
     "en",
     "bg"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_datasets",
 "title": "List Datasets",
 "version": "v1.bac9cb9c"
}
