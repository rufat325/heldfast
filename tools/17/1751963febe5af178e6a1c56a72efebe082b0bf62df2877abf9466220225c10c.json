{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Export selected/provided frames as one PDF payload.",
 "inputSchema": {
  "properties": {
   "channel": {
    "description": "Optional. Target a specific connected file by its channel id...",
    "type": "string"
   },
   "nodeIds": {
    "description": "Ordered list of frame node IDs to export as PDF pages, colon...",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "origin": {
    "description": "Origin: orchestrator/self=wolfgang; workers use assigned name....",
    "enum": [
     "wolfgang",
     "grace",
     "theo",
     "sunho",
     "zoe",
     "taewon",
     "emma",
     "alex",
     "rick"
    ],
    "type": "string"
   },
   "outputPath": {
    "description": "File path to write the PDF to, must end in .pdf (relative to...",
    "type": "string"
   }
  },
  "required": [
   "nodeIds",
   "outputPath",
   "origin"
  ],
  "type": "object"
 },
 "name": "export_frames_to_pdf"
}
