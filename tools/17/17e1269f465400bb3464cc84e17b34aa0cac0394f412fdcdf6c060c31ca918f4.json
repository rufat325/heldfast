{
 "description": "Compare two inline OpenAPI 3.0, 3.1, or 3.2 JSON documents and return a deterministic machine-readable compatibility report with every detected breaking and non-breaking change. Price: 0.01 USDC. Mode: computed_service. Source: pb33f/openapi-changes 0.2.7 (Apache-2.0), executed locally without network access. Set auto_pay=true for a one-call purchase when the local buyer wallet is configured.",
 "inputSchema": {
  "properties": {
   "after": {
    "description": "Proposed inline OpenAPI JSON object, up to 500 KB. Local # references are allowed; external references are rejected.",
    "type": "object"
   },
   "agent_token": {
    "description": "Optional pre-funded marketplace bearer token.",
    "type": "string"
   },
   "auto_pay": {
    "description": "Automatically authorize and pay this request with the locally configured buyer wallet. Requires X402_AUTO_PAY=true and X402_EVM_PRIVATE_KEY.",
    "type": "boolean"
   },
   "before": {
    "description": "Original inline OpenAPI JSON object, up to 500 KB. Local # references are allowed; external references are rejected.",
    "type": "object"
   },
   "payment_id": {
    "description": "payment_id returned by the x402 invoice.",
    "type": "string"
   },
   "payment_signature": {
    "description": "Base64 x402 v2 PAYMENT-SIGNATURE produced by a compatible buyer client.",
    "type": "string"
   },
   "preview": {
    "description": "Inspect a rate-limited live contract proof showing source, freshness, response shape, and required inputs. Decision-ready values remain in the paid result.",
    "type": "boolean"
   },
   "tx_hash": {
    "description": "Base transaction hash after paying an x402 invoice.",
    "type": "string"
   }
  },
  "required": [
   "before",
   "after"
  ],
  "type": "object"
 },
 "name": "openapi_change_report"
}
