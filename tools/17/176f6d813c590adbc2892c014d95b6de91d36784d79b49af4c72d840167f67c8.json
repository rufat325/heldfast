{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": true,
  "title": "List Editor Projects"
 },
 "description": "List your persistent Switch Editor projects, newest first, with media, scene, composition and render counts. Optional search matches the project name. Each project carries its own media and its saved chat history for every brain; the same project opens in the /editor page.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Default 25, max 100.",
    "type": "integer"
   },
   "search": {
    "description": "Optional. Part of a project name.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_editor_projects",
 "title": "List Editor Projects"
}
