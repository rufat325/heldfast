{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": false
 },
 "description": "Build or refresh the local OrangePro evidence graph from a local checkout/path plus optional CSV/Markdown enrichers. Metadata-only by default \u2014 reads source in-process but never stores or uploads code. Optionally stages/applies AI candidate flows and refreshes the report when ai_flows=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "ai_flows": {
    "description": "Generate and apply AI-suggested candidate flows after analyze, then refresh the behavior report. Requires a BYOK provider; skips without failing analyze if none is configured.",
    "type": "boolean"
   },
   "include_markdown": {
    "description": "Enrich requirements-like Markdown docs. Default true.",
    "type": "boolean"
   },
   "include_source_snippets": {
    "description": "Deferred: source snippets are never persisted in the first slice.",
    "type": "boolean"
   },
   "mode": {
    "description": "Persistence privacy mode. Only metadata_only is supported.",
    "enum": [
     "metadata_only"
    ],
    "type": "string"
   },
   "model": {
    "description": "Model name override for ai_flows.",
    "type": "string"
   },
   "paths": {
    "description": "Extra template/doc files (.csv/.md) to enrich the graph.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "provider": {
    "description": "BYOK provider override for ai_flows.",
    "enum": [
     "openai",
     "ollama",
     "anthropic"
    ],
    "type": "string"
   },
   "workspace": {
    "description": "Local workspace root. Defaults to the current directory.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "orangepro_analyze_sources",
 "title": "Analyze local sources"
}
