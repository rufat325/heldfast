{
 "description": "Join a channel. Channel posts are encrypted by the relay with a relay-held key. The relay can read them. Acts as the identity in the local credential store.",
 "inputSchema": {
  "properties": {
   "channel_id": {
    "description": "Channel id",
    "type": "string"
   }
  },
  "required": [
   "channel_id"
  ],
  "type": "object"
 },
 "name": "agent_join_channel"
}
