{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Suggest an Equibles Tool Improvement"
 },
 "description": "Suggest the smallest actionable contract improvement to an existing Equibles tool you actually called when it worked as documented but lacked a useful operation, filter, parameter or output option. Answer the user first; this records a future improvement and does not change the current call. Describe the call, never the person or their question. Omit private or user-provided argument values or replace them with [redacted]. Mention briefly that you suggested it. Use ReportProblem for wrong data; do not request new tools, duplicate existing options, or report non-Equibles ideas.",
 "inputSchema": {
  "properties": {
   "arguments": {
    "default": null,
    "description": "Optional. The arguments you passed to the existing tool, as JSON or key=value pairs, so the limitation can be reproduced. Omit or redact any user-provided or private text.",
    "type": "string"
   },
   "limitation": {
    "description": "The concrete limitation encountered in that call. State what the current operation, filter, parameter, or output contract could not do. Describe the call only \u2014 never the user or their question. Do not submit placeholder-only text such as N/A.",
    "type": "string"
   },
   "suggestedChange": {
    "description": "The smallest actionable change you recommend, including the proposed operation, filter, parameter, or output behaviour and why it would resolve the limitation. Do not submit placeholder-only text such as N/A.",
    "type": "string"
   },
   "toolName": {
    "description": "The existing Equibles tool you actually called, e.g. GetCompanyKpis.",
    "type": "string"
   }
  },
  "required": [
   "toolName",
   "limitation",
   "suggestedChange"
  ],
  "type": "object"
 },
 "name": "SuggestToolImprovement",
 "title": "Suggest an Equibles Tool Improvement"
}
