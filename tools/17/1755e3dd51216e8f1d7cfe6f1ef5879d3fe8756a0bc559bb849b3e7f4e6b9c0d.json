{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true,
  "title": "List questions and answers on tasks"
 },
 "description": "List questions raised on tasks you can see, with any human answers. Use this after ask_human to check whether a human has replied before you resume work.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "include_answered": {
    "description": "Include already-answered questions. Default false.",
    "type": "boolean"
   },
   "limit": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "task_id": {
    "description": "Limit to one task id.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_open_questions",
 "title": "List questions and answers on tasks"
}
