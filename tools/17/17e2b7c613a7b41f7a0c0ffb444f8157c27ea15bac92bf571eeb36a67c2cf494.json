{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "MUTATES by removing one Food Diary row the authenticated user owns and its mirrored calendar event. Required: food_id (UUID). Prefer this over delete_event for meals logged via log_food. Unknown or other-user food_id returns an error. Rebuilds life_state. Requires API key or OAuth with scope context. Share tokens cannot write. ($0.10; API key required)",
 "destructiveHint": true,
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "food_id": {
    "description": "UUID of a Food Diary row the authenticated user owns",
    "type": "string"
   },
   "idempotency_key": {
    "description": "Alias for request_id.",
    "type": "string"
   },
   "request_id": {
    "description": "Client idempotency key (retries return original result).",
    "type": "string"
   }
  },
  "required": [
   "food_id"
  ],
  "type": "object"
 },
 "name": "delete_food",
 "openWorldHint": false,
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "deleted": {
    "additionalProperties": true,
    "description": "Deleted food diary row.",
    "type": "object"
   },
   "event_id": {
    "type": [
     "string",
     "null"
    ]
   },
   "food_id": {
    "type": "string"
   },
   "life_state_rebuilt": {
    "type": "boolean"
   }
  },
  "required": [
   "food_id",
   "deleted",
   "life_state_rebuilt"
  ],
  "type": "object"
 },
 "readOnlyHint": false,
 "securitySchemes": [
  {
   "scopes": [
    "context.write",
    "context.delete"
   ],
   "type": "oauth2"
  }
 ],
 "title": "Delete Food (Food Diary remove)"
}
