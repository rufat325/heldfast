{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List invoices filtered by status (e.g., 'paid', 'pending') and/or customer ID. Returns invoice numbers, amounts, dates, and payment status. Paginate with limit and offset.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-chargebee-api-key",
    "_site": "mycompany",
    "limit": 30,
    "status": "paid"
   },
   {
    "_apiKey": "your-chargebee-api-key",
    "_site": "mycompany",
    "customer_id": "cust_00123456789",
    "limit": 20,
    "status": "payment_due"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Chargebee API key",
    "type": "string"
   },
   "_site": {
    "description": "Chargebee site name",
    "type": "string"
   },
   "customer_id": {
    "description": "Filter invoices by customer ID",
    "type": "string"
   },
   "limit": {
    "description": "Number of results to return (default 10, max 100)",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset from a previous response",
    "type": "string"
   },
   "status": {
    "description": "Filter by invoice status: paid, posted, payment_due, not_paid, voided, pending",
    "type": "string"
   }
  },
  "required": [
   "_apiKey",
   "_site"
  ],
  "type": "object"
 },
 "name": "chargebee_list_invoices",
 "outputSchema": {
  "properties": {
   "list": {
    "description": "Array of invoice objects",
    "items": {
     "properties": {
      "invoice": {
       "description": "Invoice details",
       "type": "object"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "next_offset": {
    "description": "Pagination offset for next page of results",
    "type": "string"
   }
  },
  "required": [
   "list"
  ],
  "type": "object"
 },
 "title": "Chargebee List Invoices",
 "version": "v1.016aa9c9"
}
