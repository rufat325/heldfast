{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "Remove a task dependency"
 },
 "description": "Remove the prerequisite edge (task_id depends on depends_on_task_id). No-op if the edge does not exist.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "depends_on_task_id": {
    "description": "The blocker task to detach. Id or task URL.",
    "type": "string"
   },
   "task_id": {
    "description": "The dependent task. Id or task URL.",
    "type": "string"
   }
  },
  "required": [
   "task_id",
   "depends_on_task_id"
  ],
  "type": "object"
 },
 "name": "remove_dependency",
 "title": "Remove a task dependency"
}
