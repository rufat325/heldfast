{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Per-space webhook subscriptions: create, list, delete.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Operation",
    "enum": [
     "create",
     "list",
     "delete"
    ],
    "type": "string"
   },
   "events": {
    "description": "Event types to subscribe to (create)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "filter": {
    "description": "Event filter (create)",
    "type": "object"
   },
   "hookId": {
    "description": "Webhook ID (delete)",
    "type": "string"
   },
   "secret": {
    "description": "Signing secret (create)",
    "type": "string"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   },
   "url": {
    "description": "Webhook URL (create)",
    "type": "string"
   }
  },
  "required": [
   "spaceId",
   "action"
  ],
  "type": "object"
 },
 "name": "webhook",
 "outputSchema": {
  "properties": {},
  "type": "object"
 }
}
