{
 "description": "Review the latest negotiation round against your policy. Read-only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "as": {
    "description": "Which side's policy view.",
    "enum": [
     "a",
     "b"
    ],
    "type": "string"
   },
   "state": {
    "description": "Path to the negotiation state file.",
    "type": "string"
   }
  },
  "required": [
   "state"
  ],
  "type": "object"
 },
 "name": "negotiate_review"
}
