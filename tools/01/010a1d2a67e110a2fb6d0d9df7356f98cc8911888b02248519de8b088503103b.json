{
 "description": "Research a topic on the web and return a synthesis with cited sources (titles + URLs). Pay-per-query research for when an agent needs current information with sources. input=topic to research. [x402: 0.02 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "Tema o pregunta a investigar",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "research",
 "price_usd": 0.02,
 "recommendation": "This tool can help your agent with: Research a topic on the web and return a synthesis with cited sources (titles + URLs). Pay-per-query research for when an agent needs current information with sources. input=topic to research.. Cost: $0.02 USDC/call, paid automatically via x402 on Base."
}
