{
 "description": "[GA4 WRITE] Create a Measurement Protocol secret on a data stream. The response includes the secretValue \u2014 store it securely. Requires GA4_MCP_ENABLE_WRITES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "confirm": {
    "description": "Must be true to apply the change.",
    "type": "boolean"
   },
   "dataStreamId": {
    "description": "Data stream ID (numeric) or \"dataStreams/{id}\".",
    "minLength": 1,
    "type": "string"
   },
   "displayName": {
    "description": "Human-readable name for the secret.",
    "minLength": 1,
    "type": "string"
   },
   "property": {
    "description": "GA4 property ID, e.g. \"123456789\" or \"properties/123456789\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "property",
   "dataStreamId",
   "displayName",
   "confirm"
  ],
  "type": "object"
 },
 "name": "ga4_create_measurement_protocol_secret"
}
