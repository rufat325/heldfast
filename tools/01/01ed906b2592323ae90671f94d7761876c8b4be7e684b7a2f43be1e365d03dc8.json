{
 "annotations": {
  "cost": {
   "all": 2
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Is this domain widely visited, or nowhere? Returns a single domain's Cloudflare Radar popularity: an exact `rank` for domains inside the global top 100, otherwise only a `bucket` (e.g. \"200\" = top 200, \"100000\" = top 100k) \u2014 `rank` is null for everything outside the top 100, so do not build logic on it populating. Below Radar's tracked ceiling (bucket \">200000\") there is NO signal at all: Radar does not verify domain existence, so a real-but-obscure site and a domain that has never existed come back identically. Read `ranked` and `interpretation`, not just `bucket`, to tell those apart.",
 "inputSchema": {
  "examples": [
   {
    "domain": "reddit.com"
   },
   {
    "domain": "pipeworx.io"
   }
  ],
  "properties": {
   "date": {
    "description": "ISO date (YYYY-MM-DD) for a historical ranking snapshot. Omit for the latest.",
    "type": "string"
   },
   "domain": {
    "description": "Domain to look up, e.g. \"reddit.com\" (no scheme/path).",
    "type": "string"
   }
  },
  "required": [
   "domain"
  ],
  "type": "object"
 },
 "name": "radar_domain_rank",
 "title": "Radar Domain Rank",
 "version": "v1.e3b28e3f"
}
