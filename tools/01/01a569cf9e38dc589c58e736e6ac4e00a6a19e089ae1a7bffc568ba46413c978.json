{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Look up the IANA timezone name, current local time, and UTC offset (hours) for a given latitude and longitude via TimeAPI.io. Requires coordinates \u2014 does not accept city names.",
 "inputSchema": {
  "examples": [
   {
    "latitude": 40.7128,
    "longitude": -74.006
   },
   {
    "latitude": 51.5074,
    "longitude": -0.1278
   }
  ],
  "properties": {
   "latitude": {
    "description": "Latitude",
    "type": "number"
   },
   "longitude": {
    "description": "Longitude",
    "type": "number"
   }
  },
  "required": [
   "latitude",
   "longitude"
  ],
  "type": "object"
 },
 "name": "get_timezone",
 "outputSchema": {
  "properties": {
   "local_time": {
    "description": "Current local time in ISO format",
    "type": "string"
   },
   "timezone": {
    "description": "IANA timezone name",
    "type": "string"
   },
   "utc_offset_hours": {
    "description": "UTC offset in hours (may include decimal for minutes)",
    "type": [
     "number",
     "null"
    ]
   }
  },
  "required": [
   "timezone",
   "local_time",
   "utc_offset_hours"
  ],
  "type": "object"
 },
 "title": "Get Timezone",
 "version": "v1.0b6057c2"
}
