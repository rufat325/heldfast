{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Write a value to a key in a KV namespace, with optional TTL. Requires confirm:true (human-approval gate); without it returns a preview only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "account_id": {
    "description": "Cloudflare account ID. Falls back to CLOUDFLARE_ACCOUNT_ID env var when omitted.",
    "type": "string"
   },
   "confirm": {
    "description": "Human-approval gate: must be true to actually perform this mutating operation. Omit or set false to get a non-executing preview of what would happen (with secrets redacted). A human should approve before this is set to true.",
    "type": "boolean"
   },
   "expiration_ttl": {
    "description": "Seconds until the key expires.",
    "type": "number"
   },
   "key": {
    "description": "The key to write.",
    "type": "string"
   },
   "namespace_id": {
    "description": "KV namespace ID.",
    "type": "string"
   },
   "value": {
    "description": "The value to store.",
    "type": "string"
   }
  },
  "required": [
   "namespace_id",
   "key",
   "value"
  ],
  "type": "object"
 },
 "name": "kv_put"
}
