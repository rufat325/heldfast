{
 "description": "[GA4 WRITE] Create a calculated metric (calculatedMetricId is required). Requires GA4_MCP_ENABLE_WRITES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "body": {
    "additionalProperties": {},
    "description": "Raw request-body object (JSON) merged OVER the typed fields \u2014 use for nested/advanced fields not exposed as flat args.",
    "type": "object"
   },
   "calculatedMetricId": {
    "description": "The user-chosen id for the calculated metric (used in reporting).",
    "minLength": 1,
    "type": "string"
   },
   "confirm": {
    "description": "Must be true to apply the change.",
    "type": "boolean"
   },
   "displayName": {
    "type": "string"
   },
   "formula": {
    "description": "The calculation formula, e.g. \"{{purchase_revenue}} / {{sessions}}\".",
    "type": "string"
   },
   "metricUnit": {
    "type": "string"
   },
   "property": {
    "description": "GA4 property ID, e.g. \"123456789\" or \"properties/123456789\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "property",
   "calculatedMetricId",
   "confirm"
  ],
  "type": "object"
 },
 "name": "ga4_create_calculated_metric"
}
