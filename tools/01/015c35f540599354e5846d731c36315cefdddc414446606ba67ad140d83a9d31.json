{
 "description": "Read an existing frozen Apply batch by opaque server pagination. Do not reorder, replace, or rescore members.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "actionCursor": {
    "maxLength": 2048,
    "minLength": 1,
    "type": "string"
   },
   "actionLimit": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "batchId": {
    "minimum": 1,
    "type": "integer"
   },
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "cursor": {
    "maxLength": 2048,
    "minLength": 1,
    "type": "string"
   },
   "limit": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "batchId"
  ],
  "type": "object"
 },
 "name": "trackly_get_apply_batch"
}
