{
 "description": "Product performance via ShopifyQL. Default: top 25 products by total_sales over date range. Pass `query` to override with raw ShopifyQL (e.g. session/conversion-funnel queries).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "endDate": {
    "description": "End date (YYYY-MM-DD)",
    "type": "string"
   },
   "query": {
    "description": "Optional raw ShopifyQL query to override the default",
    "type": "string"
   },
   "startDate": {
    "description": "Start date (YYYY-MM-DD)",
    "type": "string"
   }
  },
  "required": [
   "startDate",
   "endDate"
  ],
  "type": "object"
 },
 "name": "getConversionReport"
}
