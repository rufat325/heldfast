{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "POST a PxWeb query to a Moldova National Bureau of Statistics (Statbank) table and return observations as json-stat2. body must be {query:[{code, selection:{filter,values}}], response:{format:'json-stat2'}}. PxWeb caps responses at ~10,000 cells \u2014 narrow each dimension's values using codes from table_meta.",
 "inputSchema": {
  "examples": [
   {
    "body": {
     "query": [
      {
       "code": "Ani",
       "selection": {
        "filter": "item",
        "values": [
         "2025",
         "2026"
        ]
       }
      },
      {
       "code": "Medii",
       "selection": {
        "filter": "item",
        "values": [
         "0"
        ]
       }
      },
      {
       "code": "Sexe",
       "selection": {
        "filter": "item",
        "values": [
         "0"
        ]
       }
      }
     ],
     "response": {
      "format": "json-stat2"
     }
    },
    "path": "20 Populatia si procesele demografice/POP010/POPro/POP010100rcl.px"
   }
  ],
  "properties": {
   "body": {
    "description": "{query: [{code, selection: {filter, values}}], response: {format: \"json-stat2\"}}",
    "type": "object"
   },
   "path": {
    "description": "Path ending in the \".px\" table id.",
    "type": "string"
   }
  },
  "required": [
   "path",
   "body"
  ],
  "type": "object"
 },
 "name": "query_table",
 "title": "Query Table",
 "version": "v1.95567cb1"
}
