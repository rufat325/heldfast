{
 "description": "[DELETE] Delete a GTM transformation. Requires GTM_MCP_ENABLE_DELETES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "description": "The GTM account ID.",
    "type": "string"
   },
   "confirm": {
    "description": "Must be true to confirm this delete operation.",
    "type": "boolean"
   },
   "containerId": {
    "description": "The GTM container ID.",
    "type": "string"
   },
   "transformationId": {
    "description": "The transformations ID to delete.",
    "type": "string"
   },
   "workspaceId": {
    "description": "The GTM workspace ID.",
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "containerId",
   "workspaceId",
   "transformationId",
   "confirm"
  ],
  "type": "object"
 },
 "name": "transformations_delete"
}
