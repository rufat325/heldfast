{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Look up npm and PyPI package metadata \u2014 version history, release cadence, last updated. Use to gauge ecosystem activity around a tool or dependency. Supports comma-separated list of packages.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "max_length": {
    "default": 5000,
    "type": "number"
   },
   "packages": {
    "description": "Package name(s) e.g. 'langchain' or 'npm:zod,pypi:fastapi'",
    "type": "string"
   }
  },
  "required": [
   "packages"
  ],
  "type": "object"
 },
 "name": "package_trends"
}
