{
 "description": "Add a data table block with columns and rows.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "content": {
    "description": "Table data: { columns: [{id, name}], rows: [{id, cells: {colId: value}}] }"
   },
   "documentId": {
    "description": "The document ID",
    "type": "string"
   },
   "sortOrder": {
    "description": "Position (0-based). Omit to append at the end.",
    "type": "number"
   }
  },
  "required": [
   "documentId"
  ],
  "type": "object"
 },
 "name": "add_table_block"
}
