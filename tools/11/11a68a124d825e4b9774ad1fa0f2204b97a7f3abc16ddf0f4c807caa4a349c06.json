{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get board activity"
 },
 "description": "Get the audit trail for a whole board: task/stage/member changes across the board, newest first. Requires ADMIN/OWNER role on that board.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "actor_id": {
    "description": "Filter by the user who performed the action",
    "format": "uuid",
    "type": "string"
   },
   "board_id": {
    "description": "Board UUID",
    "format": "uuid",
    "type": "string"
   },
   "board_slug": {
    "description": "Board slug (alternative to board_id)",
    "type": "string"
   },
   "from": {
    "description": "ISO date \u2014 only activity at or after this time",
    "type": "string"
   },
   "limit": {
    "description": "Max items",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "offset": {
    "description": "Pagination offset",
    "minimum": 0,
    "type": "integer"
   },
   "search": {
    "description": "Search in activity descriptions",
    "type": "string"
   },
   "to": {
    "description": "ISO date \u2014 only activity at or before this time",
    "type": "string"
   },
   "type": {
    "description": "Filter by activity type",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_board_activity",
 "title": "Get board activity"
}
