{
 "description": "Start an optimization analysis for a piece of content. Provide EXACTLY ONE of content_id (UUID) or content_name. If multiple docs share the name, the response lists candidates \u2014 retry with a content_id.",
 "inputSchema": {
  "properties": {
   "alert_id": {
    "description": "Alert that triggered this optimization",
    "type": "string"
   },
   "content_id": {
    "type": "string"
   },
   "content_name": {
    "description": "Content title; case-insensitive exact match. Returns needs_disambiguation candidates if multiple docs share it.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "start_optimization"
}
