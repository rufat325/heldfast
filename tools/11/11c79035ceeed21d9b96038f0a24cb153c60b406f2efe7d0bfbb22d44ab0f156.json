{
 "description": "Search for bytecode opcode patterns in .class files.",
 "inputSchema": {
  "properties": {
   "file_filter": {
    "description": "Optional file filter",
    "type": "string"
   },
   "pattern": {
    "description": "Opcode pattern to search for",
    "type": "string"
   },
   "session_id": {
    "description": "Session ID",
    "type": "string"
   }
  },
  "required": [
   "session_id",
   "pattern"
  ],
  "type": "object"
 },
 "name": "jar_search_opcodes"
}
