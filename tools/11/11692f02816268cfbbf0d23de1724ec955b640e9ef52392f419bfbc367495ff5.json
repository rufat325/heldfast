{
 "description": "Read one delivery and its SES MessageId without recipient data. `ses_message_id` is what correlates this record with AWS when you need to chase a message there.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "delivery_id": {
    "description": "Delivery ID",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "delivery_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_delivery_get"
}
