{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch a target x402 endpoint without paying it, inspect the live HTTP 402 quote, and return pay/skip/manual-review guidance for agents based on liveness, latency, amount, payTo, network, asset, scheme, and buyer spend caps. This deterministic endpoint never sends a payment header to the target endpoint and does not call an upstream model. Pay $0.004 per request with x402 exact USDC on Base or Solana when advertised. Canonical live prices: https://gpt55.558686.xyz/x402/live-prices.json.",
 "inputSchema": {
  "additionalProperties": false,
  "anyOf": [
   {
    "required": [
     "url"
    ]
   },
   {
    "required": [
     "endpoint"
    ]
   }
  ],
  "properties": {
   "body": {
    "additionalProperties": true,
    "type": "object"
   },
   "endpoint": {
    "maxLength": 2048,
    "minLength": 1,
    "type": "string"
   },
   "expectedAsset": {
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   },
   "expectedNetwork": {
    "maxLength": 80,
    "minLength": 1,
    "type": "string"
   },
   "expectedPayTo": {
    "maxLength": 80,
    "minLength": 1,
    "type": "string"
   },
   "expected_asset": {
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   },
   "expected_network": {
    "maxLength": 80,
    "minLength": 1,
    "type": "string"
   },
   "expected_pay_to": {
    "maxLength": 80,
    "minLength": 1,
    "type": "string"
   },
   "maxLatencyMs": {
    "maximum": 30000,
    "minimum": 100,
    "type": "integer"
   },
   "maxPriceUsd": {
    "minimum": 0,
    "type": "number"
   },
   "max_latency_ms": {
    "maximum": 30000,
    "minimum": 100,
    "type": "integer"
   },
   "max_price_usd": {
    "minimum": 0,
    "type": "number"
   },
   "method": {
    "enum": [
     "GET",
     "POST",
     "PUT",
     "PATCH",
     "DELETE"
    ],
    "type": "string"
   },
   "observedConsecutiveFailures": {
    "maximum": 100,
    "minimum": 0,
    "type": "integer"
   },
   "observed_consecutive_failures": {
    "maximum": 100,
    "minimum": 0,
    "type": "integer"
   },
   "timeout_ms": {
    "maximum": 12000,
    "minimum": 1000,
    "type": "integer"
   },
   "url": {
    "maxLength": 2048,
    "minLength": 1,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "gpt55_x402_prepay_trust_check",
 "title": "x402 prepay trust check"
}
