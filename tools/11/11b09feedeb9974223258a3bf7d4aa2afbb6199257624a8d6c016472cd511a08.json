{
 "description": "Turn an SVG into a React, Vue, Svelte or Solid component, ready to import. Emits typed TSX for React and Solid and an SFC for Vue and Svelte, and refuses rather than writing source that will not parse.",
 "inputSchema": {
  "properties": {
   "currentColor": {
    "description": "Replace solid fills with currentColor so CSS `color` drives it",
    "type": "boolean"
   },
   "framework": {
    "description": "Target framework (default react)",
    "enum": [
     "react",
     "vue",
     "svelte",
     "solid"
    ],
    "type": "string"
   },
   "input": {
    "description": "Path to the SVG",
    "type": "string"
   },
   "name": {
    "description": "Component name, PascalCase (default Icon)",
    "type": "string"
   },
   "output": {
    "description": "Destination path; defaults beside the input with the framework extension",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "component"
}
