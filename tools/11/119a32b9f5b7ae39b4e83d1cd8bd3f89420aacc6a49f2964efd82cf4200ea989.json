{
 "description": "Update App Store listing metadata (description, keywords, what's-new, promotional text) on the currently editable version via the ASC API. If no editable version exists, pass create_version to open a new one.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "create_version": {
    "description": "If no editable version exists, create one with this version string (e.g. '1.1.0')",
    "pattern": "^\\d+\\.\\d+(\\.\\d+)?$",
    "type": "string"
   },
   "description": {
    "type": "string"
   },
   "keywords": {
    "description": "Comma-separated, 100 chars max total",
    "type": "string"
   },
   "locale": {
    "default": "en-US",
    "type": "string"
   },
   "promotional_text": {
    "type": "string"
   },
   "whats_new": {
    "description": "Release notes shown in 'What's New'",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "asc_update_metadata"
}
