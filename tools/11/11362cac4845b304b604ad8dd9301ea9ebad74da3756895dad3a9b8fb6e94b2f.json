{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Accept Snapshot"
 },
 "description": "Promote a draft snapshot to canonical. Supersedes the prior canonical for this topic.\n\nIf gate blockers exist (conflicts detected, tier downgrade, or first canonical), returns them for review. Pass force: true to approve and promote anyway.",
 "inputSchema": {
  "properties": {
   "force": {
    "default": false,
    "description": "Override gate blockers. Default false.",
    "type": "boolean"
   },
   "snapshot_id": {
    "description": "UUID of the draft snapshot to promote.",
    "type": "string"
   }
  },
  "required": [
   "snapshot_id"
  ],
  "type": "object"
 },
 "name": "accept_snapshot"
}
