{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the NCBI Taxonomy database by organism scientific or common name (e.g. \"Panthera leo\" or \"African elephant\"). Returns matching NCBI TaxIDs plus the query translation NCBI actually ran. Use a returned tax_id with ncbi-eutils.taxonomy_summary or ncbi-eutils.taxonomy_lineage. Data: eutils.ncbi.nlm.nih.gov (US National Library of Medicine, public domain), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "query": {
    "description": "Organism name to search NCBI Taxonomy for \u2014 scientific (e.g. \"Panthera leo\") or common name (e.g. \"African elephant\")",
    "minLength": 1,
    "type": "string"
   },
   "retmax": {
    "description": "Maximum number of matching TaxIDs to return (1-50, default 20)",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "ncbi-eutils.taxonomy.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "NCBI Taxonomy Search"
}
