{
 "description": "Identify who deployed a token and profile that wallet: age, activity level, SOL balance, and whether they still hold a meaningful share of the supply. Heuristic, read-only, not financial advice.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "tokenAddress": {
    "description": "Base58 mint address of the token whose deployer to profile",
    "type": "string"
   }
  },
  "required": [
   "tokenAddress"
  ],
  "type": "object"
 },
 "name": "rigel_deployer_history",
 "title": "Deployer rap sheet"
}
