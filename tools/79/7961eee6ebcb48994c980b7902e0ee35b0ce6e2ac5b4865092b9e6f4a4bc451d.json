{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List every SLOT on a SignUpGenius sign-up \u2014 date, day of week, start/end time, title, location, capacity, how many spots are filled vs still available, and who has already signed up (with the quantity each entry consumes). Accepts a sign-up URL, slug, or numeric id. Requires NO auth and works for sheets the user did NOT create, which is exactly the case the Pro signupgenius_report_* tools cannot serve (they are key-only AND owner-scoped). This is the right tool for \"what is still open?\".",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "includeParticipants": {
    "description": "Fetch the name + quantity of everyone already signed up (default true). Costs one extra request per slot row; set false for a fast availability-only view.",
    "type": "boolean"
   },
   "onlyAvailable": {
    "description": "Return only slots that still have room. Default false (return all).",
    "type": "boolean"
   },
   "url": {
    "description": "A SignUpGenius sign-up URL, its slug (`<urlid>-<signupid>[-<vanity>]`), or just the numeric sign-up id.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "signupgenius_list_slots"
}
