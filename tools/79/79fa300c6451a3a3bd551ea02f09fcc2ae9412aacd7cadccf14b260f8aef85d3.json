{
 "description": "List the developer's own apps with their status (draft / live / suspended) and slug. Use it before publishing to see what already exists, and after to check what went live. Needs the developer's qumge key.",
 "inputSchema": {
  "properties": {
   "qumge_key": {
    "description": "Optional: your Qumge API key, for clients that cannot set an Authorization header. Takes precedence over the header. Never echoed back.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "list_apps"
}
