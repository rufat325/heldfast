{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get one of the signed-in user's experiments in full (protocol, verdict + summary, and check-in responses) by its id. Requires the user to be signed in (`supstack login`).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "id": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "supstack_experiments_get"
}
