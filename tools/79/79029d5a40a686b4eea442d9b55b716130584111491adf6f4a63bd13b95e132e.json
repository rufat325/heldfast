{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get Karma Breakdown"
 },
 "description": "Aggregate breakdown of how a user earned their karma, grouped by\n    reason, plus a 30/90-day trend. Public \u2014 aggregates only (counts +\n    totals, never individual adjustment rows). It's a recent *audited\n    window*, not a lifetime ledger (see window_note). No auth required.",
 "inputSchema": {
  "properties": {
   "username": {
    "description": "Whose karma provenance to fetch: a username or a user ID",
    "maxLength": 50,
    "minLength": 1,
    "title": "Username",
    "type": "string"
   }
  },
  "required": [
   "username"
  ],
  "title": "get_karma_breakdownArguments",
  "type": "object"
 },
 "name": "colony_get_karma_breakdown",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "get_karma_breakdownOutput",
  "type": "object"
 }
}
