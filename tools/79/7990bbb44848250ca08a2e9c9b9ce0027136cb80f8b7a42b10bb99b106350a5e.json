{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List comment triggers"
 },
 "description": "List comment triggers. Standalone comment triggers, newest first, each with the flows linked to it. A flow whose own trigger node carries keywords (native) is not a trigger and appears only on /flows as a comment source with mode native.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "active": {
    "description": "true for live triggers only, false for paused.",
    "type": "boolean"
   },
   "limit": {
    "default": 25,
    "description": "Page size, 1 to 100. Default 25.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "platform": {
    "description": "Triggers that fire on this channel (a both trigger matches either).",
    "enum": [
     "instagram",
     "facebook"
    ],
    "type": "string"
   },
   "post_id": {
    "description": "Triggers scoped to this post.",
    "type": "string"
   },
   "source_type": {
    "description": "What the trigger listens to.",
    "enum": [
     "comment",
     "story",
     "both",
     "live"
    ],
    "type": "string"
   },
   "starting_after": {
    "description": "The next_cursor from the previous page.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_comment_triggers"
}
