{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List workflows"
 },
 "description": "Cross-service jobs first (service 'datailo', kind 'job': whole work products that chain several services), then each service's ready-made playbooks, marked enabled or not. Open one with datailo_workflow.",
 "inputSchema": {
  "properties": {
   "q": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "title": "Q"
   },
   "service": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "title": "Service"
   },
   "session": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "The `session` value datailo_connect_verify returned. Pass it on every Datailo call in this conversation; omit it when the user connected with Sign in (OAuth) or a key.",
    "title": "Session"
   }
  },
  "title": "datailo_workflowsArguments",
  "type": "object"
 },
 "name": "datailo_workflows",
 "title": "List workflows"
}
