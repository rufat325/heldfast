{
 "description": "Get the notification inbox for the current user \u2014 both activity notifications and broadcasts.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max notifications to return (default 20).",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset (default 0).",
    "type": "number"
   },
   "unreadOnly": {
    "description": "Only return unread notifications.",
    "type": "boolean"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to configured workspace.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "get_notifications"
}
