{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch UNESCO UIS statistic values: education, literacy, school enrollment/completion, science (R&D) and culture indicators. Returns {records:[{indicatorId,geoUnit,year,value}]}. Indicator IDs come from list_indicators; geoUnit is an ISO3 country code (e.g. \"BRA\") from list_geounits. Both accept one or many (comma-separated) values; empty records means no data for that indicator/country/year combination.",
 "inputSchema": {
  "examples": [
   {
    "end": 2020,
    "geoUnit": "BRA",
    "indicator": "CR.1",
    "start": 2015
   },
   {
    "geoUnit": "BRA,ARG,CHL",
    "indicator": "10,11"
   }
  ],
  "properties": {
   "end": {
    "description": "Last year (inclusive), e.g. 2020.",
    "type": "number"
   },
   "geoUnit": {
    "description": "ISO3 country/region code(s) from list_geounits, e.g. \"BRA\". Multiple comma-separated: \"BRA,ARG\".",
    "type": "string"
   },
   "indicator": {
    "description": "Indicator ID(s) from list_indicators, e.g. \"CR.1\" or \"10\". Multiple comma-separated: \"CR.1,CR.2\".",
    "type": "string"
   },
   "start": {
    "description": "First year (inclusive), e.g. 2015.",
    "type": "number"
   }
  },
  "required": [
   "indicator",
   "geoUnit"
  ],
  "type": "object"
 },
 "name": "get_data",
 "title": "Get Data",
 "version": "v1.6dab6463"
}
