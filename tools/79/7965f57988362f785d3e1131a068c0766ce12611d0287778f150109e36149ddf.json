{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Read a full CSO table as JSON-stat 2.0 (dimensions + flat value array). Some tables are large (the CPI table is ~60k values); use dataset_metadata first to gauge size, or use query_dataset to fetch a filtered slice instead.",
 "inputSchema": {
  "examples": [
   {
    "matrix": "CPM01"
   }
  ],
  "properties": {
   "matrix": {
    "description": "Matrix code, e.g. \"CPM01\".",
    "type": "string"
   }
  },
  "required": [
   "matrix"
  ],
  "type": "object"
 },
 "name": "get_dataset",
 "title": "Get Dataset",
 "version": "v1.59cbc08d"
}
