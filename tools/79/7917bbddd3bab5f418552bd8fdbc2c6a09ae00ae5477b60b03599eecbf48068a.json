{
 "description": "List the most recent Claude Code sessions. Useful when the user doesn't remember specific keywords but knows it was recent.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "count": {
    "default": 10,
    "description": "Number of recent sessions to show (default: 10)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "project": {
    "description": "Filter to a specific project (partial match)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "nelly_recent"
}
