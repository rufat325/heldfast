{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List all memory spaces owned by this API key.",
 "inputSchema": {
  "properties": {},
  "type": "object"
 },
 "name": "spaces_list",
 "outputSchema": {
  "properties": {
   "spaces": {
    "description": "Every memory space owned by the caller",
    "items": {
     "properties": {
      "_id": {
       "description": "Space ID (use as spaceId in other tools)",
       "type": "string"
      },
      "config": {
       "description": "Space configuration (TTLs, limits, encoding)",
       "type": "object"
      },
      "createdAt": {
       "description": "ISO 8601 creation timestamp",
       "type": "string"
      },
      "name": {
       "description": "Human-readable space name",
       "type": "string"
      },
      "slug": {
       "description": "URL-safe slug",
       "type": "string"
      },
      "usage": {
       "description": "Usage counters (message count, etc.)",
       "type": "object"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "spaces"
  ],
  "type": "object"
 }
}
