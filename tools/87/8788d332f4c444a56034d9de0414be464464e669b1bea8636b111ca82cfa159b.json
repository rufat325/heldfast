{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Import Contacts To List"
 },
 "description": "Import contacts into one of your distribution lists (your network) \u2014 give a list_name (created if new) plus a contacts array ({name,email,phone}) and/or raw csv text (header row auto-mapped). Normalizes + dedupes, skips unreachable/duplicate, respects your plan cap. Requires a connected account.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "contacts": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "email": {
       "maxLength": 320,
       "type": "string"
      },
      "name": {
       "maxLength": 300,
       "type": "string"
      },
      "phone": {
       "maxLength": 60,
       "type": "string"
      }
     },
     "type": "object"
    },
    "maxItems": 10000,
    "type": "array"
   },
   "csv": {
    "maxLength": 8000000,
    "type": "string"
   },
   "list_name": {
    "maxLength": 160,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "list_name"
  ],
  "type": "object"
 },
 "name": "import_contacts_to_list",
 "title": "Import Contacts To List"
}
