{
 "description": "Pin a reference document to a section's ref shelf. Use GitHub blob URLs so links open on mobile.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "label": {
    "description": "Display label",
    "type": "string"
   },
   "section_id": {
    "type": "string"
   },
   "url": {
    "description": "https:// URL. GitHub blob URLs preferred.",
    "type": "string"
   }
  },
  "required": [
   "section_id",
   "label"
  ],
  "type": "object"
 },
 "name": "pin_ref"
}
