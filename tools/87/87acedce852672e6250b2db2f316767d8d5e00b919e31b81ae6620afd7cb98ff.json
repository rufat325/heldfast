{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get total pageview counts for a Wikimedia project (e.g. en.wikipedia, de.wikipedia, commons.wikimedia) over a date range, bucketed daily or monthly. Filter by access method (desktop, mobile-web, mobile-app, or all-access) and traffic agent (user, spider, automated, or all-agents). Data: Wikimedia Analytics REST API (wikimedia.org/api/rest_v1), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "access": {
    "description": "Access method filter (default all-access)",
    "enum": [
     "all-access",
     "desktop",
     "mobile-app",
     "mobile-web"
    ],
    "type": "string"
   },
   "agent": {
    "description": "Traffic agent filter (default all-agents)",
    "enum": [
     "all-agents",
     "user",
     "spider",
     "automated"
    ],
    "type": "string"
   },
   "end": {
    "description": "End date in YYYYMMDD format (e.g. 20260801)",
    "pattern": "^\\d{8}$",
    "type": "string"
   },
   "granularity": {
    "description": "Time bucket granularity (default daily)",
    "enum": [
     "daily",
     "monthly"
    ],
    "type": "string"
   },
   "project": {
    "description": "Wikimedia project domain (e.g. en.wikipedia, de.wikipedia, commons.wikimedia)",
    "minLength": 1,
    "type": "string"
   },
   "start": {
    "description": "Start date in YYYYMMDD format (e.g. 20260801)",
    "pattern": "^\\d{8}$",
    "type": "string"
   }
  },
  "required": [
   "project",
   "start",
   "end"
  ],
  "type": "object"
 },
 "name": "wikimedia-analytics.pageviews.aggregate",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Wikipedia Pageviews Aggregate"
}
