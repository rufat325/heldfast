{
 "description": "List all items/products in the Xero tenant. Supports where clause, e.g. 'IsSold==true'.",
 "inputSchema": {
  "properties": {
   "order": {
    "description": "Ordering, e.g. 'Code ASC'.",
    "type": "string"
   },
   "where": {
    "description": "Xero where clause, e.g. 'IsSold==true'.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_items"
}
