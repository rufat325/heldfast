{
 "description": "Classify the sentiment of a text (positive/negative/neutral) with a confidence score. input=text. [x402: 0.002 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "texto a analizar",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "sentiment",
 "price_usd": 0.002,
 "recommendation": "This tool can help your agent with: Classify the sentiment of a text (positive/negative/neutral) with a confidence score. input=text.. Cost: $0.002 USDC/call, paid automatically via x402 on Base."
}
