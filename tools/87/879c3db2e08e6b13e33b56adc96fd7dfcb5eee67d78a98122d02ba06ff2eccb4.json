{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Create a fiat or crypto payout intent. Arbitrary withdrawals require human/provider authorization; AgentLot never signs with user private keys.",
 "inputSchema": {
  "properties": {
   "amount_cents": {
    "minimum": 0,
    "type": "integer"
   },
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "currency": {
    "type": "string"
   },
   "destination_type": {
    "enum": [
     "fiat",
     "crypto"
    ],
    "type": "string"
   },
   "note": {
    "type": "string"
   },
   "wallet_connection_id": {
    "type": "string"
   }
  },
  "required": [
   "destination_type"
  ],
  "type": "object"
 },
 "name": "request_cashout",
 "title": "Create a cash-out intent"
}
