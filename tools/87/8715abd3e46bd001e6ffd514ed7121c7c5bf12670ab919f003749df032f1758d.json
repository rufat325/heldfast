{
 "description": "Search TFT items by name, description, or component. Use componentsOnly to see base components, or the component filter to find what a component builds into.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "component": {
    "description": "Filter by component name (shows completed items that use this component)",
    "type": "string"
   },
   "componentsOnly": {
    "description": "Show only base components (not completed items)",
    "type": "boolean"
   },
   "limit": {
    "default": 20,
    "description": "Max results to return, 1-50 (default: 20)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "description": "Free-text search across item name and description (uses FTS5)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_items"
}
