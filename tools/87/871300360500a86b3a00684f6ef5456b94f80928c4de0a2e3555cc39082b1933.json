{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search 50M+ digitized items from US libraries, archives, and museums via the Digital Public Library of America. Filter by media type (image, text, sound, moving image), date range, US state, or contributing institution. Results include title, creator, date, type, subject tags, rights, and direct links to the original item and thumbnail. Sources include the Smithsonian, Library of Congress, NYPL, state digitization programs, and 2,000+ contributing institutions. Returns facets for top subjects, providers, and types within the result set.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "date_begin": {
    "description": "Filter items created on or after this year (e.g. \"1850\"). Use with date_end for a range.",
    "type": "string"
   },
   "date_end": {
    "description": "Filter items created on or before this year (e.g. \"1900\"). Use with date_begin for a range.",
    "type": "string"
   },
   "page": {
    "description": "Page number for pagination, starting at 1 (default: 1)",
    "minimum": 1,
    "type": "integer"
   },
   "page_size": {
    "description": "Number of items per page, 1\u201350 (default: 10)",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "provider": {
    "description": "Filter by contributing institution name (e.g. \"Smithsonian Institution\", \"The New York Public Library\")",
    "type": "string"
   },
   "q": {
    "description": "Full-text search query across title, description, creator, and subject fields (e.g. \"civil war photographs\", \"Thomas Jefferson\")",
    "type": "string"
   },
   "state": {
    "description": "Filter by US state name where the item originated or is about (e.g. \"Massachusetts\", \"California\")",
    "type": "string"
   },
   "type": {
    "description": "Filter by media type: image (photographs, drawings), text (books, manuscripts), sound (audio recordings), moving image (film/video), physical object",
    "enum": [
     "image",
     "text",
     "sound",
     "moving image",
     "physical object"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "dpla.archives.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "DPLA \u2014 Search Digitized Items"
}
