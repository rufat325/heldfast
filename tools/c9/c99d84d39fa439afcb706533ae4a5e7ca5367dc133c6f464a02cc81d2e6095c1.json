{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List Test Cases"
 },
 "description": "List test cases in a project, optionally scoped to a specific folder. Omit folder_id to list all test cases in the project; provide folder_id (discoverable via listFolders) to list only that folder's cases. Supports filters: case_type, priority, pagination. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "case_type": {
    "description": "Comma-separated list of case types (e.g. functional,regression).",
    "type": "string"
   },
   "folder_id": {
    "description": "Optional. If provided, only return test cases in this folder. If omitted, returns all test cases in the project. Folder ids can be discovered via listFolders.",
    "type": "string"
   },
   "p": {
    "description": "Page number.",
    "type": "number"
   },
   "priority": {
    "description": "Comma-separated list of priorities (e.g. critical,medium,low).",
    "type": "string"
   },
   "project_identifier": {
    "description": "Identifier of the project to fetch test cases from. This id starts with a PR- and is followed by a number.",
    "type": "string"
   }
  },
  "required": [
   "project_identifier"
  ],
  "type": "object"
 },
 "name": "listTestCases"
}
