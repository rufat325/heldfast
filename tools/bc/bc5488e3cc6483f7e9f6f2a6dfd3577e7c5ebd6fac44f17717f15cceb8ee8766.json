{
 "description": "[GA4 WRITE] Change a user's roles on a property (needs analytics.manage.users). The roles you pass replace the binding's roles. This API has no updateMask: the supplied fields replace the whole resource. Requires GA4_MCP_ENABLE_WRITES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "confirm": {
    "description": "Must be true to apply the change.",
    "type": "boolean"
   },
   "name": {
    "description": "Full resource name of the property access binding to target, e.g. \"properties/123/\u2026/456\".",
    "minLength": 1,
    "type": "string"
   },
   "roles": {
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "name",
   "roles",
   "confirm"
  ],
  "type": "object"
 },
 "name": "ga4_update_property_access_binding"
}
