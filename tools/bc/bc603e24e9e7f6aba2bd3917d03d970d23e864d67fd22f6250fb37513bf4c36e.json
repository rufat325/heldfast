{
 "description": "Set a connection as the active default for BetterDB.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "instanceId": {
    "description": "The instance ID to set as default",
    "pattern": "^[a-zA-Z0-9_-]+$",
    "type": "string"
   }
  },
  "required": [
   "instanceId"
  ],
  "type": "object"
 },
 "name": "set_default_connection"
}
