{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search recent New Zealand earthquakes by minimum shaking intensity. Returns quakes that may have caused shaking greater than or equal to the given MMI (Modified Mercalli Intensity, -1 to 8) in the NZ region during the last 365 days (max 100 results), with time, location, depth, magnitude, and locality. Data: GeoNet (api.geonet.org.nz), run by GNS Science, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "mmi": {
    "description": "Minimum Modified Mercalli Intensity (MMI) threshold, -1 to 8. Returns quakes that may have caused shaking greater than or equal to this value in the New Zealand region during the last 365 days (max 100 results). -1 = quakes too small to calculate a stable MMI.",
    "maximum": 8,
    "minimum": -1,
    "type": "integer"
   }
  },
  "required": [
   "mmi"
  ],
  "type": "object"
 },
 "name": "geonet-nz.quakes.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "GeoNet NZ Quake Search"
}
