{
 "description": "Dismiss an ENTIRE optimization session with ALL its suggestions. This marks the whole session (not individual suggestions) as dismissed. Use this when you want to skip all suggestions for a piece of content.",
 "inputSchema": {
  "properties": {
   "id": {
    "type": "string"
   },
   "reason": {
    "description": "Optional, max 500 chars",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "dismiss_optimization"
}
