{
 "description": "Estimate MockHero agent-plan cost before generating data. No login or API key is required; when MOCKHERO_API_KEY is configured the estimate uses actual daily usage.\n\nAgent plan pricing: 500 free records per day, then $0.001 per 100 records\n(rounded up per request), billed monthly through Polar as Merchant of Record.\n\nSend the same tables/template/prompt you plan to generate. For prompt-based\nestimates you must also pass estimated_records because this tool does not run\nprompt-to-schema conversion.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "daily_used_before": {
    "description": "Optional assumption for records already used today when no API key is supplied.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "estimated_records": {
    "description": "Required for prompt estimates because this tool does not run prompt-to-schema conversion.",
    "maximum": 9007199254740991,
    "minimum": 1,
    "type": "integer"
   },
   "format": {
    "description": "Output format",
    "enum": [
     "json",
     "csv",
     "sql"
    ],
    "type": "string"
   },
   "locale": {
    "description": "Default locale such as en, de, fr, es, or ja.",
    "type": "string"
   },
   "prompt": {
    "description": "Plain-English data request. Requires estimated_records.",
    "type": "string"
   },
   "scale": {
    "description": "Multiplier for template record counts.",
    "type": "number"
   },
   "seed": {
    "description": "Seed for reproducible output.",
    "type": "number"
   },
   "sql_dialect": {
    "description": "SQL dialect (only when format=sql)",
    "enum": [
     "postgres",
     "mysql",
     "sqlite"
    ],
    "type": "string"
   },
   "tables": {
    "description": "Structured schema \u2014 array of table definitions",
    "items": {
     "properties": {
      "count": {
       "description": "Number of records to generate",
       "type": "number"
      },
      "fields": {
       "items": {
        "properties": {
         "name": {
          "description": "Field/column name",
          "type": "string"
         },
         "nullable": {
          "description": "If true, field can be null",
          "type": "boolean"
         },
         "params": {
          "additionalProperties": {},
          "description": "Type-specific parameters",
          "propertyNames": {
           "type": "string"
          },
          "type": "object"
         },
         "type": {
          "description": "Field type (uuid, first_name, email, ref, enum, etc.)",
          "type": "string"
         }
        },
        "required": [
         "name",
         "type"
        ],
        "type": "object"
       },
       "type": "array"
      },
      "name": {
       "description": "Table name",
       "type": "string"
      }
     },
     "required": [
      "name",
      "count",
      "fields"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "template": {
    "description": "Pre-built template name",
    "enum": [
     "ecommerce",
     "blog",
     "saas",
     "social"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "estimate_agent_usage"
}
