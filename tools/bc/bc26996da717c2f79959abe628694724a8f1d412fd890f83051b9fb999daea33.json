{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "cacheHint": {
  "cacheKey": "input_hash",
  "note": "cache by the JCS-canonical policy_parameters hash only; never by wall-clock or session",
  "ttlMs": 86400000
 },
 "description": "Independently verify a ChainGraph artifact (ChainGraph Standard v0.1 \u00a76). Recomputes SHA-256 over the canonical (sorted-key, whitespace-stripped) JSON of policy_parameters + output_payload and compares it to the claimed execution_hash. A match proves the artifact's stated inputs deterministically produce its stated outputs. Pass either a full artifact object, or policy_parameters + output_payload + claimed_hash. Inputs must be I-JSON (RFC 7493): integers beyond 2^53 must be passed as JSON strings, and NaN/Infinity are rejected. Values outside I-JSON have no stable canonical form, so this tool returns a structured -32602 error (error.data.reason \"ijson_violation\") instead of a hash. Pure client-safe compute -- no data is stored. Use this to verify artifacts from any vendor that conforms to the ChainGraph Standard.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "artifact": {
    "additionalProperties": {},
    "description": "A full ChainGraph artifact envelope (must contain policy_parameters, output_payload, and execution_hash).",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "claimed_hash": {
    "description": "The execution_hash to check against (if not passing a full artifact).",
    "type": "string"
   },
   "output_payload": {
    "additionalProperties": {},
    "description": "Artifact output_payload (if not passing a full artifact).",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "policy_parameters": {
    "additionalProperties": {},
    "description": "Artifact policy_parameters (if not passing a full artifact).",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   }
  },
  "type": "object"
 },
 "lifecycle_status": "Active",
 "name": "verify_execution_hash",
 "title": "Verify a ChainGraph execution hash"
}
