{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get sunrise/sunset/moon phase data for a date range of up to 365 days for any global coordinate. Returns an array of daily records each with: sunrise, sunset, solar noon, golden hour, day length, moon phase name, and moon illumination percentage. Useful for seasonal planning, photography planning, agriculture, and event scheduling. Specify date_start and date_end in YYYY-MM-DD format; max 365 days per call. No auth required; free commercial use. Source: SunriseSunset.io.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "date_end": {
    "description": "End date of range in ISO format YYYY-MM-DD (e.g. 2026-06-30). Maximum 365 days from start date.",
    "type": "string"
   },
   "date_start": {
    "description": "Start date of range in ISO format YYYY-MM-DD (e.g. 2026-06-01).",
    "type": "string"
   },
   "lat": {
    "description": "Latitude of the target location (-90 to 90). Positive = North, Negative = South.",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "lng": {
    "description": "Longitude of the target location (-180 to 180). Positive = East, Negative = West.",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "time_format": {
    "description": "Time output format: \"0\"=12-hour AM/PM (e.g. 5:26:02 AM), \"1\"=24-hour (e.g. 05:26:02), \"2\"=Unix timestamp seconds. Defaults to \"0\".",
    "enum": [
     "0",
     "1",
     "2"
    ],
    "type": "string"
   },
   "timezone": {
    "description": "IANA timezone name for output times (e.g. America/New_York, Europe/London, Asia/Tokyo). Auto-detected from lat/lng if omitted.",
    "type": "string"
   }
  },
  "required": [
   "lat",
   "lng",
   "date_start",
   "date_end"
  ],
  "type": "object"
 },
 "name": "sunrisesunset.astronomy.range",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "SunriseSunset \u2014 Date Range Sun & Moon Data"
}
