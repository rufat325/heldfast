{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Turn any public GLB URL into a one-tap \"View in your space\" experience. The returned link routes itself by device: Apple Quick Look on iPhone and iPad, Google Scene Viewer on Android, an interactive WebGL viewer on desktop. Use it right after generating or finding a model so the person can put it on their actual desk.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "glb_url": {
    "description": "Public https URL of a .glb or .gltf model.",
    "format": "uri",
    "type": "string"
   },
   "kind": {
    "description": "avatar marks a rigged character, which stays animated.",
    "enum": [
     "model",
     "avatar"
    ],
    "type": "string"
   },
   "title": {
    "description": "Name shown in the AR experience.",
    "maxLength": 120,
    "type": "string"
   },
   "user_agent": {
    "description": "If you know the viewer's User-Agent, the response names the viewer they will get.",
    "type": "string"
   }
  },
  "required": [
   "glb_url"
  ],
  "type": "object"
 },
 "name": "export_ar",
 "title": "Turn a model into a \"View in your space\" link"
}
