{
 "description": "Extract every table in a PDF as JSON rows: [{ page, rows: string[][] }]. The first row is the header.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "base64": {
    "description": "Raw PDF bytes, base64-encoded",
    "type": "string"
   },
   "lang": {
    "description": "Tesseract language(s) for OCR, e.g. 'eng', 'deu', 'eng+ara' (default 'eng')",
    "type": "string"
   },
   "ocr": {
    "description": "Escalate ink the text layer cannot explain to on-device OCR (default true)",
    "type": "boolean"
   },
   "path": {
    "description": "Path to a PDF on this machine",
    "type": "string"
   },
   "url": {
    "description": "URL of a PDF to fetch",
    "format": "uri",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "pdf_tables",
 "title": "PDF tables"
}
