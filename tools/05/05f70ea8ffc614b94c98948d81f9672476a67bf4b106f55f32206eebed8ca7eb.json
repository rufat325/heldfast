{
 "description": "Side-by-side input/output prices for several model ids on one date (today by default, or pass date as YYYY-MM-DD). Each row resolves independently; unknown ids are reported per row.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "date": {
    "description": "Optional date (YYYY-MM-DD). Omit for today.",
    "type": "string"
   },
   "models": {
    "description": "Model ids or aliases to compare, e.g. [\"claude-opus-4-8\", \"gpt-4o\", \"gemini-2.5-pro\"].",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "models"
  ],
  "type": "object"
 },
 "name": "compare",
 "title": "Compare models"
}
