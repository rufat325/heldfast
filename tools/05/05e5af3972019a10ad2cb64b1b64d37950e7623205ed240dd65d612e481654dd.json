{
 "description": "Archive a template and preserve its versions. Deliveries that referenced a version must keep resolving to it, so archiving hides the draft and keeps the history.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "template_id": {
    "description": "Template ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "template_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_template_archive"
}
