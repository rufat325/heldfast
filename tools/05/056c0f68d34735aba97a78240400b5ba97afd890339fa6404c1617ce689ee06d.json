{
 "description": "Add a task \u2014 autonomous by default. Just pass a title and the server will (a) auto-ROUTE the task to the workflow it best fits (matched from the task text against workflow names/descriptions), falling back to the workspace's \"General\" workflow when no workflow clearly relates, and (b) auto-ASSIGN it to the right team member(s) by role/default. Every task belongs to a workflow \u2014 there is no workspace-level task. The caller does NOT need to choose a workflow or assignee. Provide an explicit workflowId/workflowName and/or assigneeIds ONLY to override that decision (the user has already decided) \u2014 explicit values are used verbatim and skip auto-routing/auto-assignment for whatever was specified. Set autoRoute:false to skip AI routing and place the task directly in the \"General\" workflow. Tasks are stored as DB tasks under the chosen workflow by default; only legacy workflows that still have a file-backed project library store the task as a phase file tagged \"task\".",
 "inputSchema": {
  "properties": {
   "assignedTo": {
    "description": "User ID to assign the task to. Overrides the workflow default assignee when provided.",
    "type": "string"
   },
   "assigneeIds": {
    "description": "User IDs to assign this task to as a SHARED task that any one of them can complete (max 5). Only assign multiple people when the task genuinely needs more than one owner \u2014 do not blanket-assign. Prefer one assignee unless the task spans several roles.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "autoAssign": {
    "description": "When true (the default) and no explicit assignee is given, the task is auto-assigned to the team member(s) whose job role best fits it. Set false to leave it unassigned.",
    "type": "boolean"
   },
   "autoRoute": {
    "description": "When true (the default) and no workflow is given, the task is auto-routed to the best-fit workflow, falling back to the workspace's \"General\" workflow if none relates. Set false to skip AI routing and place it directly in the \"General\" workflow. Ignored when an explicit workflowId/workflowName is provided.",
    "type": "boolean"
   },
   "description": {
    "description": "Optional longer description or context for the task.",
    "type": "string"
   },
   "dueDate": {
    "description": "Optional due date \u2014 a calendar date \"YYYY-MM-DD\" or a full ISO 8601 datetime. Once set, the task surfaces in due-soon / overdue nudges.",
    "type": "string"
   },
   "phaseFileId": {
    "description": "Legacy: phase file ID to link this task to, for workflows still using a file-backed project library. New workflows use DB-backed phases (add_phase) and do not need this.",
    "type": "string"
   },
   "priority": {
    "description": "Task priority \u2014 high, medium, or low. AI agents should set this based on urgency and importance.",
    "enum": [
     "high",
     "medium",
     "low"
    ],
    "type": "string"
   },
   "title": {
    "description": "Task title \u2014 short, actionable description.",
    "type": "string"
   },
   "workflowId": {
    "description": "Exact workflow ID. Use this when you already know the ID.",
    "type": "string"
   },
   "workflowName": {
    "description": "Workflow name or partial name. Used to look up the workflow when you don't know the ID. If multiple workflows match, candidates are returned so the user can pick.",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "title"
  ],
  "type": "object"
 },
 "name": "add_task"
}
