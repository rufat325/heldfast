{
 "description": "Initiates a customer request to change the date/time of a job. If the job is Waiting For Helper it may be changed immediately. Returns the raw result.",
 "execution": {
  "taskSupport": "optional"
 },
 "inputSchema": {
  "properties": {
   "body": {
    "description": "The reschedule request body (desired new date/time)",
    "properties": {
     "note": {
      "type": "string"
     },
     "requestedArrivalWindow": {
      "type": "string"
     },
     "requestedDate": {
      "type": "string"
     }
    },
    "required": [
     "requestedDate",
     "requestedArrivalWindow",
     "note"
    ],
    "type": "object"
   },
   "jobId": {
    "description": "The Job ID to create the reschedule request for",
    "type": "integer"
   }
  },
  "required": [
   "jobId",
   "body"
  ],
  "type": "object"
 },
 "name": "create_job_reschedule_request"
}
