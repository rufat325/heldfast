{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Full Resync (DANGER \u2014 wipes & rebuilds history)"
 },
 "description": "DANGER: delete and rebuild the selected wallet transaction, journal, and cost-basis history from source data. Use only after the user explicitly requests this destructive reset and accepts the consequences; use incremental_sync for routine new activity.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "portfolioRef": {
    "description": "Portfolio exact name or slug",
    "type": "string"
   },
   "wallets": {
    "description": "Optional exact wallet pairs; omit to wipe and rebuild every wallet in the portfolio",
    "items": {
     "additionalProperties": false,
     "properties": {
      "chainId": {
       "description": "Exact chain ID for the wallet",
       "type": "string"
      },
      "walletAddress": {
       "description": "Exact case-sensitive wallet address",
       "type": "string"
      }
     },
     "required": [
      "chainId",
      "walletAddress"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "workspaceRef": {
    "description": "Workspace exact name or slug",
    "type": "string"
   }
  },
  "required": [
   "workspaceRef",
   "portfolioRef"
  ],
  "type": "object"
 },
 "name": "full_resync",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "portfolioSlug": {
    "type": "string"
   },
   "requestId": {
    "type": "string"
   }
  },
  "required": [
   "portfolioSlug",
   "requestId"
  ],
  "type": "object"
 },
 "title": "Full Resync (DANGER \u2014 wipes & rebuilds history)"
}
