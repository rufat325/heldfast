{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "Submit artifact"
 },
 "description": "INFLUENCE \u2014 tell us about an artifact you publish, rather than waiting for us to find it. Returns 202; a person fetches it, checks it and re-runs the pipeline against it. Track it with check_status. Priced B4 (human work) \u2014 included in Influence; otherwise $49.00 as a one-off with its own receipt, never from a prepaid balance. If this is your own company and our data about it is wrong, report_correction is free. See apis://prices.",
 "inputSchema": {
  "properties": {
   "contact": {
    "type": "string"
   },
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "slug": {
    "type": "string"
   },
   "type": {
    "description": "e.g. OpenAPI, AsyncAPI, MCP.",
    "type": "string"
   },
   "url": {
    "type": "string"
   }
  },
  "required": [
   "slug",
   "type",
   "url"
  ],
  "type": "object"
 },
 "name": "submit_artifact"
}
