{
 "description": "An institutional manager's reported 13F positions at a point in time. weightPercent is a share of their REPORTED 13F portfolio (US-listed equities and options), not total AUM.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "as_of": {
    "description": "Point-in-time date (YYYY-MM-DD). Returns the latest snapshot on or before this date. Omit for the most recent.",
    "type": "string"
   },
   "available_as_of": {
    "description": "Lookahead guard (YYYY-MM-DD): only consider filings actually published by this date. Use for backtests.",
    "type": "string"
   },
   "cik": {
    "description": "Manager CIK, e.g. 1067983.",
    "type": "string"
   },
   "limit": {
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "cik"
  ],
  "type": "object"
 },
 "name": "get_manager_holdings"
}
