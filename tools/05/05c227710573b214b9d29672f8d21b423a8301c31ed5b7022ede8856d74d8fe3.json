{
 "description": "Read preference through a signed capability. The token IS the credential. It reveals preference state and nothing else \u2014 no email address, no CRM record.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "token": {
    "description": "Signed preference token",
    "type": "string"
   }
  },
  "required": [
   "token"
  ],
  "type": "object"
 },
 "name": "commsharbor_preference_get"
}
