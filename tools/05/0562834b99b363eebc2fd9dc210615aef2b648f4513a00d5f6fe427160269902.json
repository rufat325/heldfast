{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Compatibility alias for parse_search. Use for clients that expect a generic search tool.",
 "inputSchema": {
  "properties": {
   "limit": {
    "maximum": 10,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search",
 "title": "Search"
}
