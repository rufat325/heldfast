{
 "description": "Get cart and checkout abandonment analytics",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "endDate": {
    "description": "End date (YYYY-MM-DD)",
    "type": "string"
   },
   "groupBy": {
    "enum": [
     "HOUR_OF_DAY",
     "DAY_OF_WEEK",
     "DEVICE_TYPE",
     "TRAFFIC_SOURCE"
    ],
    "type": "string"
   },
   "startDate": {
    "description": "Start date (YYYY-MM-DD)",
    "type": "string"
   }
  },
  "required": [
   "startDate",
   "endDate"
  ],
  "type": "object"
 },
 "name": "getAbandonmentReport"
}
