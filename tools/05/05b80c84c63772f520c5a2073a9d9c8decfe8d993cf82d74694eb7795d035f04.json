{
 "_meta": {
  "skill": "jingswap"
 },
 "description": "Settle the current auction cycle by first refreshing Pyth oracle prices with fresh VAAs. This is the recommended settlement method \u2014 stored prices are almost always stale. Costs ~2 \u00b5STX for the Pyth update. Automatically fetches fresh VAAs from the backend. Settlement distributes funds to all depositors so post conditions are in Allow mode. There is no guarantee settlement succeeds (e.g. if oracle update fails), but this is the most reliable path. Only works after deposits have been closed (buffer/settle phase).\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "market": {
    "description": "Market: \"sbtc-stx\" (default) or \"sbtc-usdcx\"",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "jingswap_settle_with_refresh"
}
