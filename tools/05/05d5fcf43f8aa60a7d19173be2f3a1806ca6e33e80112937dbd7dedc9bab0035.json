{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Estimate someone's age from their first name within a specific country (e.g., 'US', 'FR', 'JP'). Returns predicted age and regional confidence count.",
 "inputSchema": {
  "examples": [
   {
    "country_code": "US",
    "name": "John"
   },
   {
    "country_code": "DE",
    "name": "Hans"
   }
  ],
  "properties": {
   "country_code": {
    "description": "ISO 3166-1 alpha-2 country code (e.g. \"US\", \"GB\", \"DE\") to localize the prediction.",
    "type": "string"
   },
   "name": {
    "description": "First name to predict age for.",
    "type": "string"
   }
  },
  "required": [
   "name",
   "country_code"
  ],
  "type": "object"
 },
 "name": "predict_age_country",
 "outputSchema": {
  "properties": {
   "country": {
    "description": "ISO country code for the region used in prediction",
    "type": "string"
   },
   "name": {
    "description": "The name that was analyzed",
    "type": "string"
   },
   "predicted_age": {
    "description": "Predicted age based on country-specific statistics, or null if unavailable",
    "type": [
     "number",
     "null"
    ]
   },
   "sample_size": {
    "description": "Number of data points used for regional prediction (confidence measure)",
    "type": "number"
   }
  },
  "required": [
   "name",
   "predicted_age",
   "sample_size",
   "country"
  ],
  "type": "object"
 },
 "title": "Predict Age Country",
 "version": "v1.73dd553d"
}
