{
 "description": "Check whether a package is in quantakrypto's known quantum-vulnerable dependency database (the classical crypto it exposes). Provide 'name' and optional 'ecosystem' (default npm).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "ecosystem": {
    "description": "Package ecosystem. Default: npm.",
    "type": "string"
   },
   "name": {
    "description": "Package name to look up (e.g. 'node-forge', 'jsonwebtoken').",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "check_dependency"
}
