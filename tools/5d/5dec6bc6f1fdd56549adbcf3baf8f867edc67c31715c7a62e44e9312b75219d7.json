{
 "description": "File a GitHub issue on the gossipcat repo from an in-session bug report. Dedupes against open issues. Requires authenticated gh CLI.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "description": {
    "description": "Bug description \u2014 what went wrong, what you expected, what happened instead",
    "minLength": 1,
    "type": "string"
   },
   "task_id": {
    "description": "Optional task ID to attach for context",
    "type": "string"
   }
  },
  "required": [
   "description"
  ],
  "type": "object"
 },
 "name": "gossip_bug_feedback"
}
