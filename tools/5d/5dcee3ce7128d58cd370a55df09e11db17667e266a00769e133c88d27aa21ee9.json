{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "The published guides. With no slug: the list, newest first, each with its title, description, written date and address, optionally only those whose title, description or slug contain every word of q. With a slug: that guide in full, its body as markdown. A guide that is not published is not found.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "The list only: 1-200, default 50.",
    "type": "integer"
   },
   "q": {
    "description": "Optional. Words to filter the list by.",
    "type": "string"
   },
   "slug": {
    "description": "Optional. One guide's slug, to read it in full.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "guides",
 "title": "Read the guides"
}
