{
 "_meta": {},
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get phased release"
 },
 "description": "Get phased release for an App Store version (gradual rollout state). Returns 404 if version has no phased release.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "appStoreVersionId": {
    "description": "App Store version ID. Phased release is optional per version.",
    "type": "string"
   }
  },
  "required": [
   "appStoreVersionId"
  ],
  "type": "object"
 },
 "name": "get-phased-release",
 "title": "Get phased release"
}
