{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "WRITES \u2014 change the label on a tracked Robinhood Chain wallet. Pass label=null to clear it (unlike the add tool, null IS accepted here). Returns 404 if the wallet is not on your watchlist. Tier: PRO+.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "address": {
    "description": "Tracked wallet EVM address (0x, 40 hex)",
    "type": "string"
   },
   "label": {
    "anyOf": [
     {
      "maxLength": 64,
      "minLength": 1,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "New label (1-64 chars), or null to clear it"
   }
  },
  "required": [
   "address",
   "label"
  ],
  "type": "object"
 },
 "name": "rhc_wallet_tracker_relabel"
}
