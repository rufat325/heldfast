{
 "_meta": {
  "skill": "x402"
 },
 "description": "Execute an x402 API endpoint. Payment is handled automatically.\n\nSupported sources:\n- x402.biwas.xyz (default): Use path like \"/api/pools/trending\"\n- x402.aibtc.com (mainnet) / x402.aibtc.dev (testnet): Use apiUrl=\"https://x402.aibtc.com\" with path like \"/inference/openrouter/chat\"\n- stx402.com: Use apiUrl=\"https://stx402.com\" with path like \"/registry/list\"\n- aibtc.com (mainnet) / aibtc.dev (testnet): Use apiUrl=\"https://aibtc.com\" with path like \"/api/inbox/{address}\"\n- Any x402-compatible URL: Use url parameter with full endpoint URL\n\nUse list_x402_endpoints to discover available endpoints.\n\nFor aibtc.com inbox messages, use send_inbox_message_direct instead \u2014 it signs a standard sBTC transfer and settles directly through the x402 facilitator.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "apiUrl": {
    "description": "API base URL. Known sources: x402.biwas.xyz, x402.aibtc.com, stx402.com, aibtc.com. Defaults to configured API_URL.",
    "format": "uri",
    "type": "string"
   },
   "asset": {
    "description": "Which asset to pay with when the endpoint accepts several. Accepts a symbol (\"sBTC\", \"STX\") or a full contract identifier. Defaults to the first asset this client can pay. Only STX and sBTC can be signed; requesting any other asset returns an error listing what is accepted.",
    "type": "string"
   },
   "autoApprove": {
    "default": false,
    "description": "Skip cost probe and execute immediately. When false (default), probes first and returns cost info for paid endpoints. When true, executes atomically like before. Free endpoints always execute transparently.",
    "type": "boolean"
   },
   "data": {
    "additionalProperties": {},
    "description": "Request body for POST/PUT requests",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "method": {
    "default": "GET",
    "description": "HTTP method",
    "enum": [
     "GET",
     "POST",
     "PUT",
     "DELETE"
    ],
    "type": "string"
   },
   "params": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "Query parameters for GET requests",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "path": {
    "description": "API endpoint path (e.g., '/api/pools/trending'). Required if url is not provided.",
    "type": "string"
   },
   "url": {
    "description": "Full endpoint URL (e.g., 'https://x402.aibtc.com/hashing/sha256'). Takes precedence over path+apiUrl.",
    "format": "uri",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "execute_x402_endpoint"
}
