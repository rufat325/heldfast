{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "TVL for any DeFi protocol with 1d and 7d change, or a top-15 chains ranking. DefiLlama-backed; a protocol family is aggregated across its deployments and the change is TVL-weighted. Costs 0.005 USDC per call (x402, USDC on Solana or Base).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "target": {
    "description": "protocol slug/name e.g. jito, marinade \u2014 omit for top chains",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_tvl",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "data": {
    "additionalProperties": {},
    "description": "the get_tvl payload; a real captured example is free at https://x402.ochinimus.app/api/sample/get_tvl",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "tool": {
    "const": "get_tvl",
    "description": "the tool that produced this payload",
    "type": "string"
   }
  },
  "required": [
   "tool",
   "data"
  ],
  "type": "object"
 },
 "title": "Protocol TVL"
}
