{
 "description": "Reject a pending proposal. Optionally records a reason in the audit trail.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "actor": {
    "description": "Optional actor identity stamped into the audit trail",
    "minLength": 1,
    "type": "string"
   },
   "proposal_id": {
    "description": "Proposal id",
    "minLength": 1,
    "type": "string"
   },
   "reason": {
    "description": "Optional rejection reason recorded on the audit row",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "proposal_id"
  ],
  "type": "object"
 },
 "name": "cache_reject_proposal"
}
