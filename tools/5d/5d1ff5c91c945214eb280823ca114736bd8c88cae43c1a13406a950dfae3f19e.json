{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Claim (sign up for) a slot on a slot-based SignUpGenius sheet. WRITES DATA the organizer sees, so it asks the user to confirm first: a confirmation prompt where the client supports one; otherwise the first call writes nothing and returns the preview (sheet, slot, identity) and a confirmToken, and only a repeat call with the same arguments plus that token submits \u2014 show the preview to the user and get their approval first (MCP_CONFIRM_MODE). For Yes/No/Maybe headcount sheets use signupgenius_rsvp instead.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "comment": {
    "description": "Optional comment shown to the organizer.",
    "maxLength": 500,
    "type": "string"
   },
   "confirmToken": {
    "description": "ONLY for the two-step confirmation fallback (a client without MCP elicitation). The confirmToken from this same tool's phase-1 \"confirmation-required\" response, passed back ONLY after the user has seen that preview and explicitly approved it in chat \u2014 never on the first call, never invented, never reused. Call again with the same arguments. Ignored when the client supports elicitation.",
    "type": "string"
   },
   "customFields": {
    "description": "Answers to the sheet's required custom questions. Read the required set from signupgenius_get_public_signup.customFields \u2014 omitting a required field makes the server reject the claim.",
    "items": {
     "properties": {
      "fieldname": {
       "type": "string"
      },
      "fieldtype": {
       "minLength": 1,
       "type": "string"
      },
      "myvalue": {
       "type": "string"
      }
     },
     "required": [
      "fieldtype",
      "myvalue"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "email": {
    "format": "email",
    "pattern": "^(?:[A-Za-z0-9_'+\\-]+\\.)*[A-Za-z0-9_'+\\-]*[A-Za-z0-9_+-]@(?:[A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
    "type": "string"
   },
   "firstname": {
    "minLength": 1,
    "type": "string"
   },
   "lastname": {
    "minLength": 1,
    "type": "string"
   },
   "quantity": {
    "description": "Spots to take. Default 1.",
    "maximum": 99,
    "minimum": 1,
    "type": "integer"
   },
   "slotitemid": {
    "description": "The slot to claim. Get this from signupgenius_list_slots.",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "url": {
    "description": "Sign-up URL or slug.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "url",
   "slotitemid",
   "firstname",
   "lastname",
   "email"
  ],
  "type": "object"
 },
 "name": "signupgenius_claim_slot"
}
