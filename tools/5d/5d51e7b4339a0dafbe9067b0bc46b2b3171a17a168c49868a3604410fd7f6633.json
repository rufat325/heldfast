{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Your account"
 },
 "description": "The connected account's email, service links, and plan states. action='keys' lists your dat_ keys (prefix, origin, created, last used \u2014 never the key); action='revoke_key' with key_prefix revokes one.",
 "inputSchema": {
  "properties": {
   "action": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "title": "Action"
   },
   "key_prefix": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "title": "Key Prefix"
   },
   "session": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "The `session` value datailo_connect_verify returned. Pass it on every Datailo call in this conversation; omit it when the user connected with Sign in (OAuth) or a key.",
    "title": "Session"
   }
  },
  "title": "datailo_accountArguments",
  "type": "object"
 },
 "name": "datailo_account",
 "title": "Your account"
}
