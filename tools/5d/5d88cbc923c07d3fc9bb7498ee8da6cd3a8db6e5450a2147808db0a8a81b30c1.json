{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get Base wallet snapshot"
 },
 "description": "Take a Base wallet snapshot in one call: native ETH plus up to 10 supplied ERC-20 token balances. Costs $0.005 USDC via x402.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "Wallet or account address on Base mainnet.",
    "pattern": "^0x[0-9a-fA-F]{40}$",
    "type": "string"
   },
   "tokens": {
    "description": "Up to ten token contract addresses whose balances should be included.",
    "items": {
     "description": "ERC-20 or token contract address on Base mainnet.",
     "pattern": "^0x[0-9a-fA-F]{40}$",
     "type": "string"
    },
    "maxItems": 10,
    "type": "array"
   }
  },
  "required": [
   "address"
  ],
  "type": "object"
 },
 "name": "base_wallet_snapshot"
}
