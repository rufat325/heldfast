{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Flight Search"
 },
 "description": "Searches live flight offers between an origin and destination for given travel dates. Supports one-way (omit return_date) and round-trip searches with flexible passenger counts and cabin class. Use this when the user wants to compare available flights, airlines, prices, layovers, or booking links for a specific route. Do not use it for rental cars, hotels, trains, or general travel planning unless the user has flight-search intent. The tool queries external flight aggregator APIs in real time, returns price-ranked results grouped by number of stops, and includes affiliate booking links. Results and booking links are valid for approximately 15 minutes due to real-time airline pricing. It does not book flights, modify reservations, charge users, or store user data.",
 "inputSchema": {
  "properties": {
   "adults": {
    "default": 1,
    "description": "Number of adult passengers (1-9). Defaults to 1.",
    "maximum": 9,
    "minimum": 1,
    "title": "Adults",
    "type": "integer"
   },
   "children": {
    "default": 0,
    "description": "Number of children aged 2-11 (0-9). Defaults to 0.",
    "maximum": 9,
    "minimum": 0,
    "title": "Children",
    "type": "integer"
   },
   "currency": {
    "default": "EUR",
    "description": "Preferred ISO 4217 currency code for prices (e.g. EUR, USD, GBP). Defaults to EUR.",
    "title": "Currency",
    "type": "string"
   },
   "departure_date": {
    "description": "Departure date. Accepts common formats such as YYYY-MM-DD, DD.MM.YYYY, or natural-language dates.",
    "title": "Departure Date",
    "type": "string"
   },
   "destination": {
    "description": "Arrival city, airport name, or IATA code.",
    "title": "Destination",
    "type": "string"
   },
   "infants": {
    "default": 0,
    "description": "Number of infants under 2 (0-9). Defaults to 0.",
    "maximum": 9,
    "minimum": 0,
    "title": "Infants",
    "type": "integer"
   },
   "locale": {
    "default": "en",
    "description": "Preferred language code (e.g. en, de, fr). Defaults to en.",
    "title": "Locale",
    "type": "string"
   },
   "origin": {
    "description": "Departure city, airport name, or IATA code (e.g. 'Frankfurt', 'JFK', 'London Heathrow').",
    "title": "Origin",
    "type": "string"
   },
   "return_date": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Return date for round-trip. Omit for one-way flights.",
    "title": "Return Date"
   },
   "trip_class": {
    "default": "Y",
    "description": "Cabin class: Y for economy, C for business. Defaults to Y.",
    "title": "Trip Class",
    "type": "string"
   }
  },
  "required": [
   "origin",
   "destination",
   "departure_date"
  ],
  "title": "searchArguments",
  "type": "object"
 },
 "name": "search",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "searchOutput",
  "type": "object"
 }
}
