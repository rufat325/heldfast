{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Google web search via Serper: returns organic results (title, link, snippet, position), an answer box if present, and a knowledge graph summary. Accepts query, result count (max 20), and optional country code (gl) and location for geo-targeted results.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-serper-api-key",
    "num": 10,
    "query": "best espresso machine 2026"
   },
   {
    "_apiKey": "your-serper-api-key",
    "gl": "us",
    "location": "San Francisco, California, United States",
    "num": 15,
    "query": "python machine learning libraries"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Serper API key for higher limits; omit to use the shared Pipeworx key.",
    "type": "string"
   },
   "gl": {
    "description": "Country code for geolocation, e.g. \"us\", \"gb\", \"de\"",
    "type": "string"
   },
   "location": {
    "description": "Location string, e.g. \"San Francisco, California, United States\"",
    "type": "string"
   },
   "num": {
    "description": "Number of results (default 10, max 20)",
    "type": "number"
   },
   "query": {
    "description": "Search query, e.g. \"best espresso machine 2026\"",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search",
 "title": "Search",
 "version": "v1.d3182749"
}
