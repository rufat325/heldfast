{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Tell ClarifyPrompt whether an optimization's output was accepted, edited, or rejected. Feeds two loops: (1) the session ring buffer so accepted prior outputs are injected as few-shot examples into future similar prompts, and (2) the persistent memory layer via reflection \u2014 on accept/edit, ClarifyPrompt extracts atomic facts from the interaction and stores them; on reject, recent reflection facts from this session are invalidated. Reflection uses the same LLM you've configured; expect a 1\u20133s latency on local models.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "diff": {
    "description": "Optional: the user's edited version or a diff. Helps reflection extract better facts.",
    "type": "string"
   },
   "optimization_id": {
    "description": "The `id` returned from optimize_prompt",
    "type": "string"
   },
   "session_id": {
    "description": "The `sessionId` returned from optimize_prompt. Required so the outcome lands in the right session bucket.",
    "type": "string"
   },
   "skip_reflection": {
    "default": false,
    "description": "Skip the LLM-based fact extraction pass (faster, no facts learned)",
    "type": "boolean"
   },
   "verdict": {
    "description": "accepted = user used the output as-is; edited = user kept it with edits; rejected = user threw it away",
    "enum": [
     "accepted",
     "edited",
     "rejected"
    ],
    "type": "string"
   }
  },
  "required": [
   "optimization_id",
   "session_id",
   "verdict"
  ],
  "type": "object"
 },
 "name": "save_outcome",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true,
  "properties": {
   "message": {
    "type": "string"
   },
   "optimizationId": {
    "type": "string"
   },
   "reflection": {
    "additionalProperties": true,
    "properties": {},
    "type": "object"
   },
   "sessionId": {
    "type": "string"
   },
   "success": {
    "type": "boolean"
   },
   "verdict": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "title": "Record an outcome (accept / edit / reject)"
}
