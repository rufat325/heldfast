{
 "description": "Start the BetterDB monitor as a persistent background process. If already running, returns the existing URL. The monitor persists across MCP sessions and must be stopped explicitly with stop_monitor.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "port": {
    "default": 3001,
    "description": "Port for the monitor API (default 3001)",
    "maximum": 65535,
    "minimum": 1,
    "type": "integer"
   },
   "storage": {
    "default": "sqlite",
    "description": "Storage backend (default sqlite)",
    "enum": [
     "sqlite",
     "memory"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "start_monitor"
}
