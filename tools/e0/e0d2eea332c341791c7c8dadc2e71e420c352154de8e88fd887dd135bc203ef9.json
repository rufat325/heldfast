{
 "description": "The sub-cent ($0.002 x402) ROUTER: tell it a NEED in free text (e.g. 'ENS resolution', 'BTC spot price', 'web search') and get the best live x402 service(s) to actually CALL \u2014 ranked by relevance \u00d7 aicomglobal's measured reliability \u00d7 Coinbase 30-day demand, cheaper preferred, each with a verify-before-you-pay pointer. The decision over the 20,000+ live Bazaar services computed for you, so you never call a dead or overpriced endpoint. Browsing the full Index is FREE (aicom_x402_index); you pay only for the targeted need\u2192best-service answer. Returns the QUOTE only; to get the answer, GET /route for a single-use nonce then POST /route {nonce, need}. LIVE right now: of 6 x402 services aicomglobal is currently observing, 0 are failing or drifting \u2014 a dead endpoint eats your call fee and your task, so verify before you send one your money (recompute free at /api/x402).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "idempotency_key": {
    "description": "Optional retry-safety key: the same key on a retry returns the SAME answer, never a second debit.",
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 3)",
    "maximum": 10,
    "minimum": 1,
    "type": "integer"
   },
   "need": {
    "description": "The capability you need in free text, e.g. 'ENS resolution' (required when pay_with:'credits')",
    "type": "string"
   },
   "pay_with": {
    "description": "Set to 'credits' to SETTLE now from your prepaid balance (no wallet) and get the ranked answer in THIS call; omit to get the quote.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "aicom_x402_route",
 "title": "x402 router \u2014 the best service for your need (sub-cent, paid)"
}
