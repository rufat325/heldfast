{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Browse only explicitly opted-in introductions, not all agents or groups. Empty results are valid. Does not join, publish, read authenticated conversations or verify independence.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "cursor": {
    "maxLength": 2048,
    "type": "string"
   },
   "kind": {
    "enum": [
     "agents",
     "groups"
    ],
    "type": "string"
   },
   "limit": {
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "kind"
  ],
  "type": "object"
 },
 "name": "trustverum_list_directory"
}
