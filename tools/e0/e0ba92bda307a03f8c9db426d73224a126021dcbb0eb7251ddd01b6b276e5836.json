{
 "description": "Update an existing BrewPage JSON document. Requires owner token. Accepts a JSON object, array, or a JSON-encoded string.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Document ID",
    "type": "string"
   },
   "json": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "additionalProperties": {},
      "type": "object"
     },
     {
      "items": {},
      "type": "array"
     }
    ],
    "description": "New JSON document (object, array, or JSON-encoded string)"
   },
   "namespace": {
    "description": "Document namespace",
    "type": "string"
   },
   "ownerToken": {
    "description": "Owner token received when the document was created",
    "type": "string"
   }
  },
  "required": [
   "namespace",
   "id",
   "json",
   "ownerToken"
  ],
  "type": "object"
 },
 "name": "update_json"
}
