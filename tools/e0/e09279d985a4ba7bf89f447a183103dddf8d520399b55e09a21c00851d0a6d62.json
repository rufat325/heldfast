{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Find symbol definitions (functions, structs, classes, etc.) by name across all indexed files",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max results (default 50)",
    "type": "integer"
   },
   "name": {
    "description": "Symbol name to search for",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "find_symbol"
}
