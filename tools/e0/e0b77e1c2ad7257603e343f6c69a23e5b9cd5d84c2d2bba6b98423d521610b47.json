{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "create_ration"
 },
 "description": "Create a feed ration the user has already decided on: a named recipe in per-head-per-day pounds, optionally assigned to groups. STRUCTURE ONLY \u2014 never formulate, balance, or validate nutritional adequacy, and never invent amounts; if asked what or how much to feed, decline and refer the user to their nutritionist. Assignments are created INACTIVE: the user activates them in the app (Rations page) before they appear on the Feed Mode Today board.",
 "inputSchema": {
  "properties": {
   "assignments": {
    "description": "Group assignments; created inactive pending in-app activation.",
    "items": {
     "additionalProperties": false,
     "properties": {
      "feedings_per_day": {
       "description": "Feedings per day for this group. Defaults to 1.",
       "maximum": 12,
       "minimum": 1,
       "type": "integer"
      },
      "group_id": {
       "description": "UUID of the group this ration feeds.",
       "type": "string"
      },
      "label": {
       "description": "Optional display label like \"AM\" or \"PM\".",
       "type": "string"
      }
     },
     "required": [
      "group_id"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "ingredients": {
    "description": "Ingredients in load order (array order = default mixer load order).",
    "items": {
     "additionalProperties": false,
     "properties": {
      "name": {
       "description": "Free-text ingredient name, e.g. \"Alfalfa hay\".",
       "minLength": 1,
       "type": "string"
      },
      "per_head_lbs": {
       "description": "Pounds per head per DAY (the canonical basis), as the user stated.",
       "exclusiveMinimum": 0,
       "type": "number"
      }
     },
     "required": [
      "name",
      "per_head_lbs"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "name": {
    "description": "Ration name, e.g. \"Finishing lambs \u2014 winter\".",
    "minLength": 1,
    "type": "string"
   },
   "unit": {
    "description": "Weight unit label. Defaults to \"lb\".",
    "type": "string"
   }
  },
  "required": [
   "name",
   "ingredients"
  ],
  "type": "object"
 },
 "name": "create_ration"
}
