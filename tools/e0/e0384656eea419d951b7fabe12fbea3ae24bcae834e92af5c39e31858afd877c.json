{
 "description": "Restore Clinic diagnosis: describe symptoms or paste a transcript/context and receive a structured read of likely conditions encountered (prompt_injection_indicator, possible_contradiction, loop, context_bloat, secret_or_pii_exposure, duplicated_content), each with severity, evidence, and detail, plus a cleaned redacted context safe to carry forward and suggested next steps. Anonymous \u2014 only aggregate counters are kept; content is never stored. Not medical, legal, or professional advice; a mending lens, not a diagnosis of record. Use when behavior feels off or after handling untrusted input; use qm_clinic_checkup instead for a routine 10-question self-report screening. Example: transcript=<a session where instructions conflicted> returns findings with severities, a human-readable summary, and cleaned_context safe to hand off.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "transcript": {
    "description": "Symptoms or transcript to read (max 20000 chars).",
    "maxLength": 20000,
    "type": "string"
   }
  },
  "required": [
   "transcript"
  ],
  "type": "object"
 },
 "name": "qm_clinic_diagnose",
 "outputSchema": {
  "properties": {
   "at": {
    "description": "ISO timestamp of the diagnosis.",
    "type": "string"
   },
   "cleaned_context": {
    "description": "Redacted, deduplicated context safe to carry forward.",
    "type": "string"
   },
   "clinic": {
    "description": "Clinic engine version.",
    "type": "string"
   },
   "counts": {
    "properties": {
     "by_severity": {
      "properties": {
       "high": {
        "type": "integer"
       },
       "info": {
        "type": "integer"
       },
       "warn": {
        "type": "integer"
       }
      },
      "type": "object"
     },
     "by_type": {
      "description": "Finding counts keyed by condition type.",
      "type": "object"
     }
    },
    "type": "object"
   },
   "findings": {
    "items": {
     "properties": {
      "count": {
       "type": "integer"
      },
      "detail": {
       "type": "string"
      },
      "evidence": {
       "type": "string"
      },
      "kind": {
       "type": "string"
      },
      "severity": {
       "enum": [
        "high",
        "warn",
        "info"
       ],
       "type": "string"
      },
      "type": {
       "description": "Condition type, e.g. prompt_injection_indicator, possible_contradiction, loop, context_bloat, secret_or_pii_exposure, duplicated_content.",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "input": {
    "properties": {
     "chars": {
      "type": "integer"
     },
     "est_tokens": {
      "type": "integer"
     }
    },
    "type": "object"
   },
   "ok": {
    "type": "boolean"
   },
   "stats_note": {
    "type": "string"
   },
   "suggested_next_steps": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "summary": {
    "description": "Human-readable finding count by severity.",
    "type": "string"
   }
  },
  "type": "object"
 }
}
