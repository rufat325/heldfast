{
 "description": "Validate a Dockerfile for IBM Code Engine compatibility. Checks architecture (linux/amd64 required), port configuration (8080 required), nginx port sed patterns, base image known issues, and USER/root warnings. Returns a list of errors, warnings, and info messages.",
 "inputSchema": {
  "properties": {
   "context_path": {
    "description": "Build context directory (used to check for .dockerignore, etc.)",
    "type": "string"
   },
   "dockerfile_path": {
    "description": "Absolute or relative path to the Dockerfile to validate",
    "type": "string"
   },
   "expected_port": {
    "description": "Expected container port (default: 8080 for Code Engine)",
    "type": "number"
   }
  },
  "required": [
   "dockerfile_path"
  ],
  "type": "object"
 },
 "name": "ce_validate_dockerfile"
}
