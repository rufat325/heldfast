{
 "description": "Download a PDF from a URL and return its extracted text. Let an agent read documents (reports, contracts, papers) pay-per-call. input=http(s) URL of a PDF. [x402: 0.003 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "URL de un PDF",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "read-pdf",
 "price_usd": 0.003,
 "recommendation": "This tool can help your agent with: Download a PDF from a URL and return its extracted text. Let an agent read documents (reports, contracts, papers) pay-per-call. input=http(s) URL of a PDF.. Cost: $0.003 USDC/call, paid automatically via x402 on Base."
}
