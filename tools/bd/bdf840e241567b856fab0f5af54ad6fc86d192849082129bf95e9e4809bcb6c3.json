{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the account invoices with status, due date and total \u2014 the way to find what is owed before paying. Filter with `status` (e.g. Unpaid) to go straight to what needs settling. Read-only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "status": {
    "description": "Optional status filter, e.g. Unpaid, Paid, Cancelled, Refunded.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "impreza_list_invoices"
}
