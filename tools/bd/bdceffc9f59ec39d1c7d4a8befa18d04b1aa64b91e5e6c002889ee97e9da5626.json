{
 "description": "Search for coworking spaces by name, neighborhood, or city (text search)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "description": "Max results (default 10)"
   },
   "query": {
    "description": "Search text (e.g. 'Midtown Manhattan', 'WeWork', 'Soho')"
   },
   "skip": {
    "description": "Results to skip for pagination"
   }
  },
  "type": "object"
 },
 "name": "croissant_find_spaces"
}
