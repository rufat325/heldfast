{
 "description": "Claim one exact, unambiguous review-handoff group and classify every member before grouped submission reconciliation. Never use this tool to infer submission or replace success-page evidence or explicit user confirmation.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "handoffId": {
    "minimum": 1,
    "type": "integer"
   },
   "idempotencyKey": {
    "maxLength": 200,
    "minLength": 16,
    "pattern": "^[\\x20-\\x7e]+$",
    "type": "string"
   },
   "members": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "classification": {
       "enum": [
        "detected",
        "user_confirmed",
        "unresolved",
        "contradictory"
       ],
       "type": "string"
      },
      "memberId": {
       "minimum": 1,
       "type": "integer"
      }
     },
     "required": [
      "memberId",
      "classification"
     ],
     "type": "object"
    },
    "maxItems": 20,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "handoffId",
   "members",
   "idempotencyKey"
  ],
  "type": "object"
 },
 "name": "trackly_claim_apply_review_handoff"
}
