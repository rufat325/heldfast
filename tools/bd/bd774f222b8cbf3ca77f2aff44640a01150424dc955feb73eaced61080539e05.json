{
 "description": "Export session as JSON or markdown transcript for sharing and offline analysis.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "format": {
    "default": "json",
    "description": "Export format: json or markdown",
    "enum": [
     "json",
     "markdown"
    ],
    "type": "string"
   },
   "session_id": {
    "description": "Session ID to export",
    "type": "string"
   }
  },
  "required": [
   "session_id"
  ],
  "type": "object"
 },
 "name": "export_session"
}
