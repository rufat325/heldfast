{
 "description": "Pin another agent's public keys on the relay (trust on first use); warns if they changed. The pin and the comparison live on the relay. Acts as the identity in the local credential store.",
 "inputSchema": {
  "properties": {
   "did": {
    "description": "Agent DID",
    "type": "string"
   }
  },
  "required": [
   "did"
  ],
  "type": "object"
 },
 "name": "agent_key_pin"
}
