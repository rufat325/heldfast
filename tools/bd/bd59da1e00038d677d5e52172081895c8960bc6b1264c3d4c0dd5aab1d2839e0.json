{
 "_meta": {
  "skill": "signing"
 },
 "description": "Verify a Stacks message signature and recover the signer's address. Takes the original message and signature, applies the Stacks prefix, and verifies. Compatible with SIWS (Sign In With Stacks) authentication flows.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "expectedSigner": {
    "description": "Optional: expected signer address to verify against. If provided, returns whether the signature is valid for this signer.",
    "type": "string"
   },
   "message": {
    "description": "The original plain text message that was signed.",
    "type": "string"
   },
   "signature": {
    "description": "The signature in RSV format (65 bytes hex from stacks_sign_message or wallet signature). Accepts both '0x'-prefixed and bare hex formats.",
    "type": "string"
   }
  },
  "required": [
   "message",
   "signature"
  ],
  "type": "object"
 },
 "name": "stacks_verify_message"
}
