{
 "description": "Call BEFORE editing behavioral source files (API routes, ordering logic, billing, auth, migrations). Returns a domain-specific scenario you must answer to demonstrate understanding of the end-user impact. Example: editing a KDS route returns 'A cook has a 3-item ticket. One item is voided. What should the cook see?' Answer the scenario concretely before proceeding with the edit. If the MCP transport is unavailable, use the packaged `prism verify-behavior` CLI fallback; never fabricate a replacement scenario.",
 "inputSchema": {
  "properties": {
   "change_summary": {
    "description": "Brief description of the intended change.",
    "type": "string"
   },
   "file_path": {
    "description": "Path of the file about to be edited.",
    "type": "string"
   },
   "project": {
    "description": "Optional project identifier for workspace-scoped scenarios.",
    "type": "string"
   },
   "workspace_id": {
    "description": "Optional workspace ID for custom scenarios.",
    "type": "string"
   }
  },
  "required": [
   "file_path",
   "change_summary"
  ],
  "type": "object"
 },
 "name": "verify_behavior"
}
