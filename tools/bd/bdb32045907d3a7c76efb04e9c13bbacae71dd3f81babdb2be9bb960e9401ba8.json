{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false
 },
 "description": "Remove a saved project from the registry by name (irreversible; doesn't stop a running server). List names with project_list.",
 "inputSchema": {
  "properties": {
   "name": {
    "description": "Name of the saved project to remove (see project_list).",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "project_remove"
}
