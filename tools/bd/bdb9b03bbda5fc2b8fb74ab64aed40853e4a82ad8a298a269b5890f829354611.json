{
 "description": "Aggregated social-listening analytics: mention volume over time, per-platform split, sentiment distribution and trend, totals. Defaults to quality-filtered mentions.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "days": {
    "description": "1-30, default 7 (capped by mention retention).",
    "type": "number"
   },
   "include_low_relevancy": {
    "description": "Include low-relevancy mentions the feed normally hides.",
    "type": "boolean"
   },
   "keyword_id": {
    "description": "Restrict to one keyword (id from list_keywords).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_listening_analytics"
}
