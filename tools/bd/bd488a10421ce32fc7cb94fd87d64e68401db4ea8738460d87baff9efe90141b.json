{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the design tokens in the file: every variable collection (VARIABLE_SET) with its modes, and every variable with its value per mode \u2014 colours as hex, numbers and strings verbatim. Aliases are followed when the target variable lives in this file; a variable published from another library is returned as its opaque assetRef instead. Filter with `set` (collection name or guid) or `query` (substring of the variable name).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "cursor": {
    "description": "Opaque nextCursor from a previous truncated response; resumes where it stopped.",
    "type": "string"
   },
   "file": {
    "description": "Path to the .fig / .figma file (absolute, or relative to the server CWD).",
    "type": "string"
   },
   "includeSets": {
    "description": "Include the collection list with their modes (default true).",
    "type": "boolean"
   },
   "limit": {
    "description": "Default 100.",
    "maximum": 300,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Case-insensitive substring of the variable name.",
    "type": "string"
   },
   "set": {
    "description": "Only variables from this collection (name or guid).",
    "type": "string"
   }
  },
  "required": [
   "file"
  ],
  "type": "object"
 },
 "name": "fig_variables",
 "title": "Variables and modes"
}
