{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": false
 },
 "description": "Moderator: create a hand-curated job. URL is the dedup key \u2014 re-adding the same URL updates the posting. `description` is stored and rendered as HTML. Requires the moderator role (403 otherwise).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "company": {
    "description": "Company name. Required.",
    "minLength": 1,
    "type": "string"
   },
   "description": {
    "description": "Stored and rendered as HTML.",
    "type": "string"
   },
   "location": {
    "type": "string"
   },
   "posted_at": {
    "description": "RFC3339 timestamp.",
    "type": "string"
   },
   "remote": {
    "type": "boolean"
   },
   "source": {
    "description": "The posting's real origin (defaults to 'manual' server-side).",
    "type": "string"
   },
   "title": {
    "description": "Job title. Required.",
    "minLength": 1,
    "type": "string"
   },
   "url": {
    "description": "The posting URL \u2014 the dedup key. Required.",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "url",
   "title",
   "company"
  ],
  "type": "object"
 },
 "name": "jobs_add"
}
