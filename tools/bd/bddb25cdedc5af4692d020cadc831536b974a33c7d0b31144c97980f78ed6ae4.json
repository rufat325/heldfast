{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Render supplied HTML in an isolated headless browser and return WCAG findings plus evidence and suggested corrections. Use for generated UI before it is hosted. Read-only.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "baseUrl": {
    "description": "Optional http(s) base URL used to resolve relative assets.",
    "format": "uri",
    "type": "string"
   },
   "height": {
    "default": 900,
    "description": "Viewport height in CSS pixels.",
    "maximum": 4320,
    "minimum": 240,
    "type": "integer"
   },
   "html": {
    "description": "Complete HTML document or fragment to render and test.",
    "maxLength": 2000000,
    "minLength": 1,
    "type": "string"
   },
   "maxIssues": {
    "default": 50,
    "description": "Maximum violation groups returned; summary totals still cover the full run.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "maxNodesPerIssue": {
    "default": 10,
    "description": "Maximum affected elements returned per rule.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "standard": {
    "default": "wcag22aa",
    "description": "WCAG ruleset to test. Use best-practice to include additional axe guidance.",
    "enum": [
     "wcag2a",
     "wcag2aa",
     "wcag2aaa",
     "wcag21aa",
     "wcag21aaa",
     "wcag22aa",
     "wcag22aaa",
     "best-practice"
    ],
    "type": "string"
   },
   "width": {
    "default": 1440,
    "description": "Viewport width in CSS pixels.",
    "maximum": 7680,
    "minimum": 320,
    "type": "integer"
   }
  },
  "required": [
   "html"
  ],
  "type": "object"
 },
 "name": "audit_html",
 "title": "Audit an HTML document"
}
