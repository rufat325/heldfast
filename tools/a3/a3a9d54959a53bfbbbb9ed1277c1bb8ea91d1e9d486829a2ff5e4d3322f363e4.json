{
 "description": "List site audits (SEO, content-quality, and technical issues). Each audit is scoped to one site \u2014 evaluate independently, don't compare across domains.",
 "inputSchema": {
  "properties": {
   "page": {
    "description": "default 1",
    "type": "number"
   },
   "page_size": {
    "description": "default 20, max 100",
    "type": "number"
   },
   "site_id": {
    "type": "string"
   },
   "status": {
    "enum": [
     "pending",
     "crawling",
     "analyzing",
     "completed",
     "failed"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_audits"
}
