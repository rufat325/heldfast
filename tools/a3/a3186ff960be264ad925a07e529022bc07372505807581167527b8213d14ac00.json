{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Manage Percy Build Approval"
 },
 "description": "Approve or reject a Percy build",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "action": {
    "description": "The action to perform on the Percy build.",
    "enum": [
     "approve",
     "unapprove",
     "reject"
    ],
    "type": "string"
   },
   "buildId": {
    "description": "The ID of the Percy build to approve or reject.",
    "type": "string"
   }
  },
  "required": [
   "buildId",
   "action"
  ],
  "type": "object"
 },
 "name": "managePercyBuildApproval"
}
