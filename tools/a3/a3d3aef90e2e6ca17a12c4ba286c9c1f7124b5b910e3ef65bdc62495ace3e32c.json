{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Optimize a prompt for a specific AI platform. Context-aware: auto-gathers workspace signals (CLAUDE.md / AGENTS.md / .cursorrules / package.json), resolves intent + category + recommended mode in a single analysis step, shapes the system prompt to the target model's capabilities, and grounds the rewrite in a priority-ordered Grounding Context. Supports 58+ platforms across 7 categories, plus custom registered platforms. Category, platform, and mode are all optional \u2014 the engine chooses sane defaults from the analysis.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "category": {
    "description": "Prompt category. Auto-detected via the analyzer when omitted. When provided, the analyzer can still override if it's confident the hint is wrong.",
    "enum": [
     "chat",
     "image",
     "voice",
     "video",
     "music",
     "code",
     "document"
    ],
    "type": "string"
   },
   "cwd": {
    "description": "Working directory to scan for CLAUDE.md / AGENTS.md / .cursorrules / package.json. Defaults to server cwd.",
    "type": "string"
   },
   "enrich_context": {
    "default": false,
    "description": "Use web search for context enrichment (Tavily/Brave/Serper/SerpAPI/Exa/SearXNG). Results merge into the single Grounding Context block.",
    "type": "boolean"
   },
   "file_excerpt": {
    "description": "Short excerpt (\u22642 KB) of the active file to ground the rewrite",
    "type": "string"
   },
   "file_language": {
    "description": "Explicit language override for the active file",
    "type": "string"
   },
   "file_path": {
    "description": "Active file path \u2014 infers language and grounds the rewrite",
    "type": "string"
   },
   "include_bundle": {
    "default": false,
    "description": "Include the full resolved ContextBundle in the response (same shape as inspect_context returns)",
    "type": "boolean"
   },
   "mode": {
    "description": "Output mode. When omitted, the engine uses the analyzer's intent-derived recommendation (e.g. production-code \u2192 technical, quick-draft \u2192 concise). When passed, user choice wins.",
    "enum": [
     "concise",
     "detailed",
     "structured",
     "step-by-step",
     "bullet-points",
     "technical",
     "simple"
    ],
    "type": "string"
   },
   "platform": {
    "description": "Target platform ID (e.g. midjourney, dall-e, sora, suno, claude, cursor, or a custom platform ID). Uses category default when omitted.",
    "type": "string"
   },
   "prompt": {
    "description": "The prompt to optimize",
    "type": "string"
   },
   "session_id": {
    "description": "Session ID to stitch related optimizations so the engine can reuse accepted prior outputs as few-shot examples. Auto-generated when omitted.",
    "type": "string"
   },
   "skip_intent_resolution": {
    "default": false,
    "description": "Skip the analyzer LLM call (faster; loses intent/category/mode recommendations)",
    "type": "boolean"
   },
   "user_locale": {
    "description": "User locale hint (e.g. en-US, ar-EG)",
    "type": "string"
   },
   "user_pinned_instructions": {
    "description": "Pinned, always-applied user instructions (highest-priority grounding)",
    "type": "string"
   }
  },
  "required": [
   "prompt"
  ],
  "type": "object"
 },
 "name": "optimize_prompt",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true,
  "properties": {
   "analysis": {
    "additionalProperties": true,
    "properties": {
     "category": {
      "type": "string"
     },
     "confidence": {
      "type": "string"
     },
     "intent": {
      "type": "string"
     },
     "recommendedMode": {
      "type": "string"
     },
     "source": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "bundle": {},
   "category": {
    "type": "string"
   },
   "context": {
    "additionalProperties": true,
    "properties": {},
    "type": "object"
   },
   "id": {
    "type": "string"
   },
   "mode": {
    "type": "string"
   },
   "modeSource": {
    "type": "string"
   },
   "optimizedPrompt": {
    "type": "string"
   },
   "originalPrompt": {
    "type": "string"
   },
   "platform": {
    "type": "string"
   },
   "sessionId": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "title": "Optimize a prompt for a platform"
}
