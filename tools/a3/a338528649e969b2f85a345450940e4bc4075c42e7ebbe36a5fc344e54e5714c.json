{
 "description": "Search Reddit and optionally Nairaland for keyword mentions. Filters by approved subreddits and generates AI-drafted replies ready to post. Requires GROQ_API_KEY for drafts.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "generateReplies": {
    "default": true,
    "description": "Generate AI reply drafts for approved posts",
    "type": "boolean"
   },
   "includeNairaland": {
    "default": false,
    "description": "Also search Nairaland",
    "type": "boolean"
   },
   "keywords": {
    "description": "Keywords to monitor",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "maxPostAgeHours": {
    "default": 24,
    "description": "Only return posts newer than this many hours",
    "type": "number"
   },
   "productContext": {
    "description": "Describe your product/expertise for AI reply generation",
    "type": "string"
   }
  },
  "required": [
   "keywords",
   "productContext"
  ],
  "type": "object"
 },
 "name": "monitor_keywords"
}
