{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List tags used in the Streaming Radar newsletter, optionally filtered by category. Free access \u2014 no API key required.",
 "inputSchema": {
  "properties": {
   "category": {
    "description": "Tag category (e.g. \"company\", \"country\", \"topic\", \"format\")",
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 50)",
    "maximum": 200,
    "minimum": 1,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "sr_get_tags",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Tool-specific result object. Inspect the `content` array for human-readable text and structured data.",
  "type": "object"
 }
}
