{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-call-session-segment"
 },
 "description": "The list of segments involved in the session. Read-only. Nullable.\n\nTIP: Gets a specific segment of a call session. Returns detailed media stream quality metrics including jitter, packetLoss, roundTripTime for audio/video/screenSharing.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "callRecordId": {
    "description": "Path parameter: callRecordId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "segmentId": {
    "description": "Path parameter: segmentId",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "sessionId": {
    "description": "Path parameter: sessionId",
    "type": "string"
   }
  },
  "required": [
   "callRecordId",
   "sessionId",
   "segmentId"
  ],
  "type": "object"
 },
 "name": "get-call-session-segment"
}
