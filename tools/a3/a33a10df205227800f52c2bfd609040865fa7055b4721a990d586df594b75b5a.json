{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch the social graph edges for a Bluesky account \u2014 who follows them, or who they follow. Returns paginated actor profiles (handle, DID, displayName, bio, pronouns when set, and Bluesky verification status) plus a summary of the subject account. Follower, following, and post counts and the website are not on this view, for the listed accounts or the subject \u2014 bsky_get_profile returns them for one account. Accounts with large social graphs return only the first page; use cursor pagination to walk through the full list, or sort \"top\" to put the accounts Bluesky ranks most prominent first.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "actor": {
    "description": "Handle (e.g. \"alice.bsky.social\") or DID of the account to query. A leading \"@\" and the account's bsky.app page (\"https://bsky.app/profile/alice.bsky.social\") are accepted and read as the handle or DID they carry. A bare name without a dot is not a handle \u2014 use bsky_search_actors to resolve one.",
    "maxLength": 2048,
    "minLength": 1,
    "pattern": "^(?:(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\\.)+[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?|did:[a-z]+:[a-zA-Z0-9._:%-]*[a-zA-Z0-9._-]|@(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\\.)+[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?|https:\\/\\/bsky\\.app\\/profile\\/(?:(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?\\.)+[a-zA-Z](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?|did:[a-z]+:[a-zA-Z0-9._:%-]*[a-zA-Z0-9._-])\\/?(?:[?#].*)?)$",
    "type": "string"
   },
   "cursor": {
    "description": "Opaque pagination cursor from a previous response. Omit for the first page.",
    "maxLength": 2048,
    "type": "string"
   },
   "direction": {
    "description": "\"followers\" returns accounts that follow this actor. \"following\" returns accounts this actor follows.",
    "enum": [
     "followers",
     "following"
    ],
    "type": "string"
   },
   "limit": {
    "default": 25,
    "description": "Maximum number of actors to return per page (1\u2013100). Default 25.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "sort": {
    "description": "Order of the list. \"latest\" (Bluesky's default, and what omitting this gives) puts the most recent follows first; \"top\" is Bluesky's own ranking, which surfaces prominent accounts but is not a follower-count order. Pass a cursor back with the same sort it came from.",
    "enum": [
     "latest",
     "top"
    ],
    "type": "string"
   }
  },
  "required": [
   "actor",
   "direction"
  ],
  "type": "object"
 },
 "name": "bsky_get_follows",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "anyOf": [
   {
    "not": {
     "required": [
      "error"
     ]
    },
    "required": [
     "actors",
     "subject",
     "totalReturned"
    ]
   },
   {
    "required": [
     "error"
    ]
   }
  ],
  "properties": {
   "actors": {
    "description": "Actors in the requested direction of the social graph.",
    "items": {
     "additionalProperties": false,
     "description": "A Bluesky actor in the social graph.",
     "properties": {
      "avatar": {
       "description": "Avatar image URL.",
       "type": "string"
      },
      "description": {
       "description": "Biography / about text.",
       "type": "string"
      },
      "did": {
       "description": "Decentralized Identifier of the actor.",
       "type": "string"
      },
      "displayName": {
       "description": "Display name set by the user.",
       "type": "string"
      },
      "handle": {
       "description": "Human-readable handle, e.g. \"alice.bsky.social\".",
       "type": "string"
      },
      "labels": {
       "description": "Moderation labels.",
       "items": {
        "additionalProperties": false,
        "description": "A moderation label.",
        "properties": {
         "val": {
          "description": "Label value (content warning or moderation tag, e.g. \"porn\", \"spam\").",
          "type": "string"
         }
        },
        "required": [
         "val"
        ],
        "type": "object"
       },
       "type": "array"
      },
      "pronouns": {
       "description": "Free-form pronouns the account set, e.g. \"they/he\". Absent when it set none. Account-authored text bounded only by length, not a fixed vocabulary \u2014 read it as written rather than parsing it.",
       "type": "string"
      },
      "verification": {
       "additionalProperties": false,
       "description": "Bluesky verification of this account \u2014 what tells it from a look-alike handle. Absent when Bluesky sent none. Who issued it is on bsky_get_profile.",
       "properties": {
        "trustedVerifierStatus": {
         "description": "Whether this account is itself a trusted verifier \u2014 same values as verifiedStatus.",
         "type": "string"
        },
        "verifiedStatus": {
         "description": "Whether a trusted verifier verified this account: \"valid\", \"invalid\" (verified once, no longer holds), or \"none\". Passed through as Bluesky sends it, so another value may appear.",
         "type": "string"
        }
       },
       "required": [
        "verifiedStatus",
        "trustedVerifierStatus"
       ],
       "type": "object"
      }
     },
     "required": [
      "did",
      "handle"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "cap": {
    "description": "The limit applied to this page.",
    "type": "number"
   },
   "cursor": {
    "description": "Opaque cursor for the next page. Absent on the last page.",
    "type": "string"
   },
   "error": {
    "additionalProperties": {},
    "description": "Present when the call failed. Absent on success.",
    "properties": {
     "code": {
      "description": "JSON-RPC error code for this failure.",
      "maximum": 9007199254740991,
      "minimum": -9007199254740991,
      "type": "integer"
     },
     "data": {
      "additionalProperties": {},
      "properties": {
       "reason": {
        "description": "Machine-readable failure mode. Declared by this tool: `actor_not_found`: The actor handle or DID does not resolve to an existing account. Other values are possible when a failure originates below the handler.",
        "examples": [
         "actor_not_found"
        ],
        "type": "string"
       },
       "recovery": {
        "additionalProperties": {},
        "description": "Actionable next step for the caller.",
        "properties": {
         "hint": {
          "type": "string"
         }
        },
        "required": [
         "hint"
        ],
        "type": "object"
       },
       "retryable": {
        "description": "Whether retrying may succeed.",
        "type": "boolean"
       }
      },
      "type": "object"
     },
     "message": {
      "description": "Human-readable description of what went wrong.",
      "type": "string"
     }
    },
    "required": [
     "code",
     "message"
    ],
    "type": "object"
   },
   "notice": {
    "description": "Guidance when the result set is empty or constrained.",
    "type": "string"
   },
   "shown": {
    "description": "Number of actors returned on this page.",
    "type": "number"
   },
   "subject": {
    "additionalProperties": false,
    "description": "Profile summary of the queried actor.",
    "properties": {
     "did": {
      "description": "Permanent DID of the queried account.",
      "type": "string"
     },
     "displayName": {
      "description": "Subject display name.",
      "type": "string"
     },
     "handle": {
      "description": "Human-readable handle of the queried account.",
      "type": "string"
     },
     "pronouns": {
      "description": "Free-form pronouns the subject account set, e.g. \"they/he\". Absent when it set none.",
      "type": "string"
     },
     "verification": {
      "additionalProperties": false,
      "description": "Bluesky verification of this account \u2014 what tells it from a look-alike handle. Absent when Bluesky sent none. Who issued it is on bsky_get_profile.",
      "properties": {
       "trustedVerifierStatus": {
        "description": "Whether this account is itself a trusted verifier \u2014 same values as verifiedStatus.",
        "type": "string"
       },
       "verifiedStatus": {
        "description": "Whether a trusted verifier verified this account: \"valid\", \"invalid\" (verified once, no longer holds), or \"none\". Passed through as Bluesky sends it, so another value may appear.",
        "type": "string"
       }
      },
      "required": [
       "verifiedStatus",
       "trustedVerifierStatus"
      ],
      "type": "object"
     }
    },
    "required": [
     "did",
     "handle"
    ],
    "type": "object"
   },
   "totalReturned": {
    "description": "Number of actors in this response page.",
    "type": "number"
   },
   "truncated": {
    "description": "True when Bluesky returned a cursor, whatever this page held. A cursor is not proof more accounts exist: the next page can come back empty when the remaining accounts are unavailable.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "title": "Get Bluesky Social Graph"
}
