{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find a gene by its official gene symbol with an exact match for a specific species. Returns a single best-matching gene with full annotation: NCBI Entrez Gene ID, Ensembl gene ID, gene name, type, taxon, summary, UniProt accession, genomic coordinates, pathway memberships, and GO terms. Faster and more precise than full-text search when the official symbol is known. Case-insensitive (BRCA1, brca1, Brca1 all match). For human genes, use HGNC symbols (e.g. \"TP53\", \"EGFR\", \"KRAS\", \"VEGFA\"). Results can be used directly with mygene.genes.batch for comparative multi-gene analysis.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "fields": {
    "description": "Comma-separated fields to include in the response. Default includes: symbol, name, taxid, entrezgene, type_of_gene, summary, ensembl, uniprot, alias, genomic_pos, pathway. Use \"all\" for complete gene annotations.",
    "type": "string"
   },
   "species": {
    "default": "human",
    "description": "Target species for the symbol lookup. Common values: \"human\", \"mouse\", \"rat\", \"fruitfly\", \"zebrafish\", \"chicken\", \"pig\", \"dog\". Can also use NCBI taxid (e.g. \"9606\" for human). Defaults to \"human\".",
    "type": "string"
   },
   "symbol": {
    "description": "Official HGNC gene symbol (for human) or equivalent gene symbol in the target species. Case-insensitive (e.g. \"BRCA1\", \"TP53\", \"EGFR\", \"KRAS\"). Performs an exact symbol match and returns the single best-matching gene.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "symbol"
  ],
  "type": "object"
 },
 "name": "mygene.genes.symbol",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Look Up Gene by Official Symbol"
}
