{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "A structured token/contract risk report for a Base contract: a normalized risk score, a pass/warn/fail checklist (bytecode present, sanctions, verified source, ownership, holder concentration) and a single verdict. $0.05. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "address": {
    "description": "Token/contract address on Base (0x + 40 hex)",
    "type": "string"
   }
  },
  "required": [
   "address"
  ],
  "type": "object"
 },
 "name": "agentbit.contract_audit",
 "title": "Contract Audit"
}
