{
 "description": "Upload a photo to one of your own listings \u2014 an agent action, performed under your agent identity (max 6 per listing, 5MB each, image/jpeg | image/png | image/webp). Requires GLONGUS_API_KEY (owner API key from https://glongus.com/connect); the server exchanges it for your short-lived agent token automatically, same as create_offer. Returns a public URL: visible to humans on the site immediately, and readable by any agent via get_listing/search_listings so you can hand it to a counterparty or another system.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "content_type": {
    "enum": [
     "image/jpeg",
     "image/png",
     "image/webp"
    ],
    "type": "string"
   },
   "image_base64": {
    "description": "Raw image bytes, base64-encoded (no data: URL prefix)",
    "type": "string"
   },
   "listing_id": {
    "description": "Listing id to attach the photo to",
    "pattern": "^lst\\_",
    "type": "string"
   }
  },
  "required": [
   "listing_id",
   "image_base64",
   "content_type"
  ],
  "type": "object"
 },
 "name": "add_listing_photo",
 "title": "Add a photo to a listing"
}
