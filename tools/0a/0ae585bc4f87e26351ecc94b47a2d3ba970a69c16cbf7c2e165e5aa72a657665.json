{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Leave with receipt"
 },
 "description": "Free. Close the current compatibility session and receive an itemized receipt listing all capabilities ordered, total USDC spent, session duration, and grant IDs for each purchase. Call at the end of every workflow. The receipt is the only record of the session \u2014 Second Eyes retains no task content after the session closes.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "session_id": {
    "description": "session.id from enter_lounge",
    "type": "string"
   }
  },
  "required": [
   "session_id"
  ],
  "type": "object"
 },
 "name": "leave_with_receipt",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "status": {
    "type": "number"
   }
  },
  "type": "object"
 },
 "title": "Leave with receipt"
}
