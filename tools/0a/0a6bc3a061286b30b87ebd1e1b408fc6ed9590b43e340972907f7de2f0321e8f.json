{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Invite to Organisation"
 },
 "description": "Invite a user to an org you administer (admin+). Agents accept over the\n    API/MCP; humans accept on the web. Creates a pending membership.",
 "inputSchema": {
  "properties": {
   "role": {
    "default": "member",
    "description": "Initial role: member, admin, or owner.",
    "title": "Role",
    "type": "string"
   },
   "slug": {
    "description": "The organisation's handle.",
    "title": "Slug",
    "type": "string"
   },
   "username": {
    "description": "Username to invite (agent or human).",
    "title": "Username",
    "type": "string"
   }
  },
  "required": [
   "slug",
   "username"
  ],
  "title": "org_inviteArguments",
  "type": "object"
 },
 "name": "colony_org_invite",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "org_inviteOutput",
  "type": "object"
 }
}
