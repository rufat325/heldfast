{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Align one disclosure topic across two to six public companies using each company's source-backed historical disclosure lineage. Use this when cross-company comparison is the objective and the caller needs current evidence plus latest same-form transition evidence on a common basis without independently retrieving, parsing, aligning, and re-comparing every filing history. Use filedproof.build_disclosure_lineage for one issuer, not this tool; use filedproof.check_disclosure_changes when monitoring newly filed changes for one issuer. The matrix is an evidence substrate, not a ranking or materiality judgment.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "comparisonLimit": {
    "default": 200,
    "description": "Maximum number of changed blocks to inspect in each filing comparison.",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "currentEvidenceLimit": {
    "default": 6,
    "description": "Maximum current-filing evidence records returned per company and form.",
    "maximum": 12,
    "minimum": 1,
    "type": "integer"
   },
   "filings": {
    "default": 4,
    "description": "Maximum filings per company used to build or reuse the underlying disclosure lineage.",
    "maximum": 6,
    "minimum": 2,
    "type": "integer"
   },
   "fingerprintLimit": {
    "default": 30,
    "description": "Maximum number of relevant evidence records used to fingerprint one filing.",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "forms": {
    "default": [
     "10-K"
    ],
    "description": "SEC forms to align. Defaults to 10-K for stronger cross-company comparability.",
    "items": {
     "type": "string"
    },
    "maxItems": 4,
    "minItems": 1,
    "type": "array"
   },
   "history": {
    "default": true,
    "description": "Whether to use bounded historical SEC submission archives when needed.",
    "type": "boolean"
   },
   "identifiers": {
    "description": "Two to six public-company identifiers to align on the same filing-grounded disclosure topic.",
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 6,
    "minItems": 2,
    "type": "array"
   },
   "item": {
    "description": "Optional SEC filing item such as 1A or 7.",
    "type": "string"
   },
   "limit": {
    "default": 24,
    "description": "Maximum topic-relevant change evidence retained in each underlying lineage transition.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "maxArchiveFiles": {
    "description": "Maximum number of historical SEC submission archive files to inspect when history is enabled.",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "section": {
    "description": "Optional exact normalized section heading.",
    "type": "string"
   },
   "topic": {
    "description": "Disclosure topic to align across companies, such as cybersecurity, AI infrastructure, or supply-chain concentration.",
    "maxLength": 256,
    "minLength": 1,
    "type": "string"
   },
   "transitionEvidenceLimit": {
    "default": 6,
    "description": "Maximum evidence records returned for each latest same-form transition.",
    "maximum": 12,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "identifiers",
   "topic"
  ],
  "type": "object"
 },
 "name": "filedproof.build_disclosure_matrix",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Structured FiledProof result with operation-specific evidence, provenance, coverage, and uncertainty fields.",
  "type": "object"
 },
 "title": "Build cross-company disclosure matrix"
}
