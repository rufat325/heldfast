{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Retrieve hourly historical air quality data for a date range at any global location. Returns PM10, PM2.5, ozone, nitrogen dioxide, European AQI, US AQI and other pollutants. Historical data available from 2022-01-01. Maximum query range ~1 year per call. Copernicus CAMS reanalysis, no auth. Open-Meteo CC BY 4.0.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "end_date": {
    "description": "End date in YYYY-MM-DD format (e.g. \"2024-01-31\"). Maximum range is ~1 year per query.",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "latitude": {
    "description": "Latitude of the location in decimal degrees (e.g. 48.8566 for Paris)",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "longitude": {
    "description": "Longitude of the location in decimal degrees (e.g. 2.3522 for Paris)",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "pollutants": {
    "description": "Comma-separated pollutant variables to return. Available: pm10, pm2_5, carbon_monoxide, nitrogen_dioxide, sulphur_dioxide, ozone, aerosol_optical_depth, dust, uv_index, ammonia, european_aqi, us_aqi. Defaults to pm10,pm2_5,nitrogen_dioxide,ozone,european_aqi,us_aqi.",
    "type": "string"
   },
   "start_date": {
    "description": "Start date in YYYY-MM-DD format (e.g. \"2024-01-01\"). Historical data from 2022.",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "timezone": {
    "default": "UTC",
    "description": "IANA timezone name for returned timestamps (e.g. \"Europe/Paris\", \"America/New_York\"). Defaults to UTC.",
    "type": "string"
   }
  },
  "required": [
   "latitude",
   "longitude",
   "start_date",
   "end_date"
  ],
  "type": "object"
 },
 "name": "openmeteoaq.air.historical",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Open-Meteo \u2014 Historical Air Quality"
}
