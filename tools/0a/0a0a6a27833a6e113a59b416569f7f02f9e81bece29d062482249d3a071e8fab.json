{
 "description": "Run a comprehensive site audit covering accessibility (WCAG 2.1 AA / EAA), performance, SEO, design quality, and mobile responsiveness. Returns scores, grades, and prioritized fixes.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "format": {
    "default": "report",
    "description": "Output format: 'report' for readable markdown, 'json' for structured data",
    "enum": [
     "report",
     "json"
    ],
    "type": "string"
   },
   "url": {
    "description": "The URL to audit (e.g., https://example.com)",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "audit_site"
}
