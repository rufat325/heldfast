{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Normalize a Japanese address into clean prefecture/city/town/chome components, fixing fullwidth/halfwidth and old-form kanji, and resolving the postal code. Returns a 0-1 confidence score.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "A Japanese address string, any common notation.",
    "type": "string"
   }
  },
  "required": [
   "address"
  ],
  "type": "object"
 },
 "name": "address_normalize"
}
