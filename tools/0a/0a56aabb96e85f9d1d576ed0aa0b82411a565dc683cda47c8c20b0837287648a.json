{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "PAID ($0.002 USDC via x402). Name, symbol, decimals and total supply for any ERC-20 contract on Base \u2014 the identity fields an agent needs before pricing or safety-checking a token. Without payment this returns the 402 challenge; the fiatdock-mcp npm package pays automatically. A non-ERC-20 / bad address returns 4xx and is NOT charged; on any RPC failure it returns 5xx and is NOT charged.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "payment": {
    "description": "Base64 of a single x402 v2 PaymentPayload (EIP-3009 transferWithAuthorization on Base USDC). OMIT it on the first call: the 402 you get back carries `howToPay.payloadTemplate` \u2014 the exact envelope to fill in \u2014 plus the price and the EIP-712 domain. Send it on the second call to complete the purchase; it is forwarded as the PAYMENT-SIGNATURE header, never as body data. Nothing is charged for the 402 itself, and a call that fails after payment settles nothing.",
    "type": "string"
   },
   "token": {
    "description": "An ERC-20 contract address (0x\u2026, 40 hex) on Base",
    "type": "string"
   }
  },
  "required": [
   "token"
  ],
  "type": "object"
 },
 "name": "token_metadata",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "asOf": {
    "description": "ISO 8601 read time",
    "type": "string"
   },
   "contract": {
    "description": "The ERC-20 contract address queried",
    "type": "string"
   },
   "decimals": {
    "description": "Token decimals",
    "type": "number"
   },
   "name": {
    "description": "Token name (null if the contract omits name())",
    "type": [
     "string",
     "null"
    ]
   },
   "network": {
    "description": "Chain read (always base)",
    "type": "string"
   },
   "symbol": {
    "description": "Token symbol (null if the contract omits symbol())",
    "type": [
     "string",
     "null"
    ]
   },
   "totalSupply": {
    "description": "Total supply as a decimal string (null if unavailable)",
    "type": [
     "string",
     "null"
    ]
   },
   "totalSupplyAtomic": {
    "description": "Total supply in atomic units (string; null if unavailable)",
    "type": [
     "string",
     "null"
    ]
   }
  },
  "required": [
   "network",
   "contract",
   "name",
   "symbol",
   "decimals",
   "totalSupplyAtomic",
   "totalSupply",
   "asOf"
  ],
  "type": "object"
 },
 "title": "ERC-20 token metadata on Base ($0.002)"
}
