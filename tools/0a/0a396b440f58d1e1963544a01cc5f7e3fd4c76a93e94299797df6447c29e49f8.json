{
 "description": "Return a CLI's full machine-readable command/flag catalog (`<cli> --catalog json`). Use this to discover the long tail beyond the curated tools.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "cli": {
    "description": "Which suite CLI.",
    "enum": [
     "extract",
     "template-vault",
     "draft",
     "nda-review",
     "lint",
     "compare",
     "docx2pdf",
     "sign",
     "contract-vault"
    ],
    "type": "string"
   }
  },
  "required": [
   "cli"
  ],
  "type": "object"
 },
 "name": "catalog"
}
