{
 "description": "Run instrumented tests on a connected device/emulator (connected<Variant>AndroidTest) and return a parsed summary. Requires a booted device.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "module": {
    "description": "Gradle module, e.g. 'app' (defaults to session)",
    "type": "string"
   },
   "projectDir": {
    "description": "Project root (defaults to session)",
    "type": "string"
   },
   "variant": {
    "description": "Build variant, e.g. 'debug' (defaults to session)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "test_instrumented",
 "title": "Run instrumented tests"
}
