{
 "description": "Manage compact project memory across chats, agents, and IDEs: remember active work, complete and archive sessions, search history, or update durable rules.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "action": {
    "default": "resume",
    "enum": [
     "resume",
     "remember",
     "complete",
     "search",
     "ledger",
     "supersede"
    ],
    "type": "string"
   },
   "category": {
    "enum": [
     "decision",
     "rule",
     "quirk",
     "blocker"
    ],
    "type": "string"
   },
   "event_type": {
    "default": "note",
    "enum": [
     "note",
     "progress",
     "decision",
     "blocker",
     "index",
     "plan"
    ],
    "type": "string"
   },
   "key": {
    "type": "string"
   },
   "limit": {
    "default": 8,
    "type": "number"
   },
   "note": {
    "type": "string"
   },
   "project_path": {
    "type": "string"
   },
   "query": {
    "type": "string"
   },
   "task": {
    "type": "string"
   },
   "use_model": {
    "default": true,
    "type": "boolean"
   },
   "value": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "project_memory"
}
