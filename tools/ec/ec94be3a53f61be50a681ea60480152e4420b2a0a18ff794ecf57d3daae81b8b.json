{
 "description": "[PUBLISH] Create a new container version from a workspace and immediately publish it. Requires GTM_MCP_ENABLE_PUBLISH=true and confirm=true. \u26a0\ufe0f  This is the most powerful write operation \u2014 it goes live in production immediately.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "type": "string"
   },
   "confirm": {
    "description": "Must be true to confirm this publish operation.",
    "type": "boolean"
   },
   "containerId": {
    "type": "string"
   },
   "name": {
    "description": "Version name.",
    "type": "string"
   },
   "notes": {
    "description": "Version notes.",
    "type": "string"
   },
   "workspaceId": {
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "containerId",
   "workspaceId",
   "confirm"
  ],
  "type": "object"
 },
 "name": "workspace_create_version_and_publish"
}
