{
 "description": "SSH into a host using a DemiPass ref code. One call: ref + host + command \u2192 output. The password is injected server-side. You never see it. Self-healing: if no SSH context exists for this secret, one is auto-created. This is the primary way to access remote machines.",
 "inputSchema": {
  "properties": {
   "command": {
    "description": "Command to run on the remote host",
    "type": "string"
   },
   "ref": {
    "description": "Ref code for the SSH password (e.g. DP-PWD-sharedra-b08a108a)",
    "type": "string"
   },
   "target_host": {
    "description": "IP or hostname to SSH into",
    "type": "string"
   },
   "target_user": {
    "description": "SSH username (default: root)",
    "type": "string"
   }
  },
  "required": [
   "ref",
   "target_host",
   "command"
  ],
  "type": "object"
 },
 "name": "demipass_ssh"
}
