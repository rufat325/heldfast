{
 "description": "Update an existing Code Engine custom domain mapping (PATCH) \u2014 e.g. change the target app",
 "inputSchema": {
  "properties": {
   "app_name": {
    "description": "New application to route traffic to",
    "type": "string"
   },
   "domain_name": {
    "type": "string"
   },
   "project_id": {
    "type": "string"
   },
   "tls_secret": {
    "description": "New TLS secret name (optional)",
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "domain_name"
  ],
  "type": "object"
 },
 "name": "ce_update_domain_mapping"
}
