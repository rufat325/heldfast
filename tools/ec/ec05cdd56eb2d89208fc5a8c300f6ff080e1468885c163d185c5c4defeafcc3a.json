{
 "description": "Record a lesson learned \u2014 something that went wrong and how it was fixed \u2014 into the workspace Lessons Learned library, so the same mistake can be avoided on THIS and any OTHER project. Call this when you hit and resolve a non-obvious bug, a broken assumption, a footgun, or a fix that took real effort to find. Do NOT use it for routine notes or ideas (use capture_idea) or for reusable reference knowledge (use create_skill). Pass `project` so the lesson can be filtered back to its origin; omit it only for genuinely global lessons.",
 "inputSchema": {
  "properties": {
   "fix": {
    "description": "What actually resolved it, specific enough to act on next time.",
    "type": "string"
   },
   "problem": {
    "description": "What went wrong and, importantly, WHY \u2014 the root cause, not just the symptom.",
    "type": "string"
   },
   "project": {
    "description": "Project/product this came from \u2014 becomes a #project-<slug> tag for filtering. Omit only if the lesson is genuinely project-independent.",
    "type": "string"
   },
   "title": {
    "description": "Short, searchable summary of the lesson (e.g. \"Redis delete-then-set race made deleted libraries reappear\")",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your active workspace.",
    "type": "string"
   }
  },
  "required": [
   "title",
   "problem",
   "fix"
  ],
  "type": "object"
 },
 "name": "record_lesson"
}
