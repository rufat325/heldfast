{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Purge a zone's cache \u2014 everything, or by files/tags/hosts/prefixes. Requires confirm:true (human-approval gate); without it returns a preview only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "confirm": {
    "description": "Human-approval gate: must be true to actually perform this mutating operation. Omit or set false to get a non-executing preview of what would happen (with secrets redacted). A human should approve before this is set to true.",
    "type": "boolean"
   },
   "files": {
    "description": "Specific URLs to purge.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "hosts": {
    "description": "Hostnames to purge (Enterprise).",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "prefixes": {
    "description": "URL prefixes to purge (Enterprise).",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "purge_everything": {
    "description": "Purge the entire cache. Mutually exclusive with files/tags/hosts/prefixes.",
    "type": "boolean"
   },
   "tags": {
    "description": "Cache-Tags to purge (Enterprise).",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "zone_id": {
    "description": "The Cloudflare zone ID (32-char hex). Get it from list_zones.",
    "type": "string"
   }
  },
  "required": [
   "zone_id"
  ],
  "type": "object"
 },
 "name": "purge_cache"
}
