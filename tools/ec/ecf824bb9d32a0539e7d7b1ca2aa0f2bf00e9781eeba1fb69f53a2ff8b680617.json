{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Answer WHEN IT STARTED, which is what a log is really asked. Compares each pattern against ITSELF rather than against total volume \u2014 in a busy service everything rises at 9am and nothing has happened \u2014 and returns three different things: what SPIKED relative to its own normal and at what time; what is NEW, which did not exist before and is usually what a deploy left behind; and what STOPPED APPEARING, which raises no counter and is often the worse news.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bucketSeconds": {
    "description": "Width of the time bucket; by default chosen from the log's own range",
    "maximum": 86400,
    "minimum": 1,
    "type": "integer"
   },
   "format": {
    "description": "Force the format instead of detecting it",
    "enum": [
     "json",
     "logfmt",
     "syslog5424",
     "syslog3164",
     "clf",
     "combined",
     "plain"
    ],
    "type": "string"
   },
   "from": {
    "default": "end",
    "description": "Which end to read if the file is over 32 MB",
    "enum": [
     "start",
     "end"
    ],
    "type": "string"
   },
   "limit": {
    "default": 20,
    "description": "How many findings to return",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "minRatio": {
    "default": 4,
    "description": "How many times its own normal counts as a spike",
    "maximum": 1000,
    "minimum": 1.5,
    "type": "number"
   },
   "path": {
    "description": "Path to the log file. Use this for big logs: they do not fit in a tool call.",
    "type": "string"
   },
   "text": {
    "description": "The log as text. Alternative to `path`.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "log_trends"
}
