{
 "description": "Resize image. Change the dimensions of a photo or picture. Fits within your numbers without enlarging. Accepts image/*, .heic, .heif, .avif; up to 50 MB per file.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "fit": {
    "default": "inside",
    "description": "How to fit: without enlarging, cropping, or with padding",
    "enum": [
     "inside",
     "cover",
     "contain"
    ],
    "type": "string"
   },
   "format": {
    "default": "same",
    "description": "Output format. \u00abAs is\u00bb keeps the original",
    "enum": [
     "same",
     "jpeg",
     "png",
     "webp"
    ],
    "type": "string"
   },
   "height": {
    "description": "Height in pixels",
    "maximum": 20000,
    "minimum": 1,
    "type": "integer"
   },
   "outputDir": {
    "description": "Where to put the results. Defaults to the folder each source file came from. Existing files are never overwritten.",
    "type": "string"
   },
   "paths": {
    "description": "Paths to the files to process, absolute or relative to the working directory.",
    "items": {
     "type": "string"
    },
    "maxItems": 25,
    "minItems": 1,
    "type": "array"
   },
   "quality": {
    "default": 85,
    "description": "Quality, 1\u2013100",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "width": {
    "description": "Width in pixels",
    "maximum": 20000,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "paths"
  ],
  "type": "object"
 },
 "name": "image_resize"
}
