{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Add Codes To Profile"
 },
 "description": "Attach or auto-generate promo codes under a discount profile \u2014 each code inherits the profile's discount and becomes immediately redeemable. Give explicit codes and/or auto_generate a count (optional code_prefix); max 500/call, duplicates skipped with a suggestion. Requires event_id + profile_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "auto_generate": {
    "exclusiveMinimum": 0,
    "maximum": 500,
    "type": "integer"
   },
   "code_prefix": {
    "maxLength": 32,
    "type": "string"
   },
   "codes": {
    "items": {
     "maxLength": 64,
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 500,
    "type": "array"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "profile_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "profile_id"
  ],
  "type": "object"
 },
 "name": "add_codes_to_profile",
 "title": "Add Codes To Profile"
}
