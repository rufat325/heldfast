{
 "description": "Get the fleet's daily standup digest (activity, token burn, health). Pass window=7d for the weekly view.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "window": {
    "description": "Digest window: 1d (default) or 7d.",
    "enum": [
     "1d",
     "7d"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "digest_get"
}
