{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false
 },
 "description": "Save (or replace) a project in the registry so you can dev_start it by name. Replaces any existing project with the same name.",
 "inputSchema": {
  "properties": {
   "cmd": {
    "description": "Dev command (optional; auto-detected if omitted).",
    "type": "string"
   },
   "cwd": {
    "description": "Project directory.",
    "type": "string"
   },
   "name": {
    "description": "Unique project name (used to dev_start it later).",
    "type": "string"
   },
   "url": {
    "description": "Default URL to open in the browser pane (optional).",
    "type": "string"
   }
  },
  "required": [
   "name",
   "cwd"
  ],
  "type": "object"
 },
 "name": "project_add"
}
