{
 "annotations": {
  "destructiveHint": true,
  "title": "Publish to CMS"
 },
 "description": "Publish a FlowContent article to connected CMS (Ghost, Webflow, PrestaShop)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "article_id": {
    "description": "FlowContent article ID to publish",
    "type": "string"
   },
   "connection_id": {
    "description": "Specific CMS connection ID (publishes to all if omitted)",
    "type": "string"
   }
  },
  "required": [
   "article_id"
  ],
  "type": "object"
 },
 "name": "publish_to_cms"
}
