{
 "description": "Extract durable memories from session content. Use at natural memory-worthy breakpoints: task complete, decision made, bug solved, preference clarified. Detects duplicates (skips near-identical content) and flags active contradictions. Use preview: true to see extractions without storing.",
 "inputSchema": {
  "properties": {
   "content": {
    "description": "Session text to distill into durable memories",
    "type": "string"
   },
   "max_memories": {
    "description": "Maximum number of memories to extract",
    "type": "number"
   },
   "preview": {
    "description": "Preview mode: return extracted memories without storing (default: false)",
    "type": "boolean"
   },
   "source_ids": {
    "description": "Source memory IDs for provenance tracking",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "tags": {
    "description": "Tags to apply (auto-generated if absent)",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "content"
  ],
  "type": "object"
 },
 "name": "velixar_distill"
}
