{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List email addresses that have bounced. Includes bounce reason and timestamp. Example: get_bounces({ limit: 100, _apiKey: \"SG.xxx\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-sendgrid-api-key"
   },
   {
    "_apiKey": "your-sendgrid-api-key",
    "limit": 100
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "SendGrid API key (SG.xxx)",
    "type": "string"
   },
   "limit": {
    "description": "Max results to return (default 100, max 500)",
    "type": "number"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "get_bounces",
 "outputSchema": {
  "description": "List of bounced email addresses with reasons and timestamps",
  "type": "object"
 },
 "title": "Get Bounces",
 "version": "v1.675bcfce"
}
