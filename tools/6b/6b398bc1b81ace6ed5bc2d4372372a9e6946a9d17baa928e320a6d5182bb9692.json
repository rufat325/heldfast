{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Full report for a sign-up: every slot plus the participant who claimed it (with custom-question answers when present). Requires SIGNUPGENIUS_USER_KEY (Pro subscription) \u2014 session mode is not supported for reports \u2014 AND only covers sheets the key-holder created. For slot dates, times and availability on ANY sign-up (including sheets the user did not create) prefer signupgenius_list_slots, which needs no auth.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "signupId": {
    "description": "Sign-up ID (signupid).",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   }
  },
  "required": [
   "signupId"
  ],
  "type": "object"
 },
 "name": "signupgenius_report_all"
}
