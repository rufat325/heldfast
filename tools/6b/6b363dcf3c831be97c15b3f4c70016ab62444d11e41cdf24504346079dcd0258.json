{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Image search through your Google Programmable Search Engine (searchType=image). Works only when the engine has \"Image search\" enabled in its control panel \u2014 otherwise the API answers 400. Each item's url is the image file itself; image.context_url is the page hosting it, with width/height/byte_size and a thumbnail_url (plus its dimensions) for previews. Everything else matches the search tool: same filters (language/country, safe search, site include/exclude, date_restrict, file_type, rights \u2014 use rights for reusable images), same pagination (max 10 per call, 100 per query, paginate via next_start), same quota (one unit per call). Image-only filters: size (icon..huge), type (clipart/face/lineart/stock/photo/animated), color_type (mono/gray/color/trans) and dominant_color.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "color_type": {
    "description": "mono = black & white, trans = transparent background.",
    "enum": [
     "mono",
     "gray",
     "color",
     "trans"
    ],
    "type": "string"
   },
   "country": {
    "description": "Geolocation bias \u2014 rank results as if searching from this country, e.g. \"de\". Soft signal.",
    "pattern": "^[a-zA-Z]{2}$",
    "type": "string"
   },
   "country_restrict": {
    "description": "Hard filter \u2014 only documents Google attributes to this country, e.g. \"DE\".",
    "pattern": "^[a-zA-Z]{2}$",
    "type": "string"
   },
   "date_restrict": {
    "description": "Only results from the last N days/weeks/months/years: \"d7\", \"w2\", \"m6\", \"y1\", ...",
    "pattern": "^[dwmy][1-9]\\d*$",
    "type": "string"
   },
   "dominant_color": {
    "description": "Prefer images dominated by this color.",
    "enum": [
     "black",
     "blue",
     "brown",
     "gray",
     "green",
     "orange",
     "pink",
     "purple",
     "red",
     "teal",
     "white",
     "yellow"
    ],
    "type": "string"
   },
   "engine_id": {
    "description": "Programmable Search Engine id (cx) to search with, overriding the configured GOOGLE_CUSTOM_SEARCH_ENGINE_ID. Omit to use the configured engine.",
    "minLength": 1,
    "type": "string"
   },
   "exact_terms": {
    "description": "A phrase every result must contain.",
    "minLength": 1,
    "type": "string"
   },
   "exclude_terms": {
    "description": "A word or phrase no result may contain.",
    "minLength": 1,
    "type": "string"
   },
   "file_type": {
    "description": "Only files of this type, e.g. \"pdf\", \"xlsx\", \"svg\".",
    "pattern": "^[a-zA-Z0-9]{1,10}$",
    "type": "string"
   },
   "filter_duplicates": {
    "description": "Google's duplicate-content filter (default on). false returns near-duplicate results too.",
    "type": "boolean"
   },
   "include_pagemap": {
    "description": "Attach each result's raw pagemap (OpenGraph/metatags/thumbnails and other structured page data). Verbose \u2014 leave off unless you need page metadata.",
    "type": "boolean"
   },
   "interface_language": {
    "description": "Interface language used to interpret the query, e.g. \"de\". Improves non-English relevance.",
    "pattern": "^[a-zA-Z]{2}(-[a-zA-Z]{2,4})?$",
    "type": "string"
   },
   "language": {
    "description": "Only results in this language, e.g. \"en\", \"de\", \"zh-CN\".",
    "pattern": "^[a-zA-Z]{2}(-[a-zA-Z]{2,4})?$",
    "type": "string"
   },
   "num": {
    "description": "Results per page, 1..10 (API maximum 10; default 10).",
    "maximum": 10,
    "minimum": 1,
    "type": "integer"
   },
   "or_terms": {
    "description": "Space-separated words \u2014 each result must contain at least one of them.",
    "minLength": 1,
    "type": "string"
   },
   "query": {
    "description": "The search query, Google syntax included (quotes, site:, filetype:, OR, -exclusions).",
    "minLength": 1,
    "type": "string"
   },
   "rights": {
    "description": "License filter: cc_publicdomain, cc_attribute, cc_sharealike, cc_noncommercial, cc_nonderived (combine with |).",
    "minLength": 1,
    "type": "string"
   },
   "safe": {
    "description": "Safe search: active filters adult content, off (the API default) does not.",
    "enum": [
     "off",
     "active"
    ],
    "type": "string"
   },
   "site_search": {
    "description": "A site or domain to include or exclude, e.g. \"example.com\" \u2014 pair with site_search_filter.",
    "minLength": 1,
    "type": "string"
   },
   "site_search_filter": {
    "description": "include = only results from site_search, exclude = everything but it. Default include.",
    "enum": [
     "include",
     "exclude"
    ],
    "type": "string"
   },
   "size": {
    "description": "Image size class, from icon to huge.",
    "enum": [
     "icon",
     "small",
     "medium",
     "large",
     "xlarge",
     "xxlarge",
     "huge"
    ],
    "type": "string"
   },
   "sort": {
    "description": "Sort expression, e.g. \"date\" for newest-first. Omit for relevance (usually better).",
    "minLength": 1,
    "type": "string"
   },
   "start": {
    "description": "1-based index of the first result \u2014 use next_start/previous_start from the previous page. The API serves at most 100 results per query, so start + num - 1 must stay <= 100 (enforced: a wider window is rejected without spending quota).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "type": {
    "description": "Kind of image.",
    "enum": [
     "clipart",
     "face",
     "lineart",
     "stock",
     "photo",
     "animated"
    ],
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_images",
 "title": "Image search"
}
