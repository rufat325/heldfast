{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Return bounded Paddle subscriptions with customer, status, recurring items, billing cycle, next billing date, and scheduled changes. Answers which accounts are active, trialing, paused, past due, or canceled.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "pdl_sdbx_apikey_your-key",
    "_environment": "sandbox",
    "per_page": 20,
    "status": "past_due"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Paddle Billing API key with read permission for the resource",
    "type": "string"
   },
   "_environment": {
    "description": "Paddle environment; defaults to production",
    "enum": [
     "production",
     "sandbox"
    ],
    "type": "string"
   },
   "after": {
    "description": "Pagination cursor from meta.pagination.next",
    "type": "string"
   },
   "customer_id": {
    "description": "Paddle customer ID beginning ctm_",
    "type": "string"
   },
   "per_page": {
    "description": "Results per page, default 20 and maximum 30",
    "maximum": 30,
    "minimum": 1,
    "type": "number"
   },
   "status": {
    "enum": [
     "active",
     "canceled",
     "past_due",
     "paused",
     "trialing"
    ],
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "paddle_list_subscriptions",
 "title": "Paddle List Subscriptions",
 "version": "v1.54cb8a89"
}
