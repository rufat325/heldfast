{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Click a UI element"
 },
 "description": "Semantically CLICK a UI element via UI Automation (button, menu item, checkbox). Target by name_contains or element_index from ui_find. REQUIRES user-enabled action tier + app allowlist; otherwise returns action_denied. Confirm with the user before acting.",
 "inputSchema": {
  "properties": {
   "element_index": {
    "description": "Stable index from ui_find",
    "type": "number"
   },
   "hwnd": {
    "description": "Exact window handle",
    "type": "number"
   },
   "name_contains": {
    "description": "Element name to click (e.g. \"Save\")",
    "type": "string"
   },
   "title_contains": {
    "description": "Window title fragment",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ui_invoke"
}
