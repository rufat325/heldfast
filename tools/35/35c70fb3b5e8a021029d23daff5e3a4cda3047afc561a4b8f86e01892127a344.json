{
 "_meta": {},
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List actors"
 },
 "description": "List actors (users or API keys) by ID(s). Use when you have actor IDs from review submissions or version history.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "filter.id": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Actor ID(s) to fetch (required). Get IDs from submission/version responses."
   },
   "limit": {
    "default": 50,
    "type": "number"
   }
  },
  "required": [
   "filter.id"
  ],
  "type": "object"
 },
 "name": "list-actors",
 "title": "List actors"
}
