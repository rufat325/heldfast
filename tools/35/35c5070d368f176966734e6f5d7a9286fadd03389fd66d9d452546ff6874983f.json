{
 "description": "Initialize or resume an audit session. Returns session_id, message_count, first_message_at, and recent messages in a single call \u2014 use this instead of calling start_session + get_session_history separately.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "ai_tool": {
    "description": "AI tool name: \"claude\", \"cursor\", \"windsurf\", etc.",
    "type": "string"
   },
   "attach": {
    "description": "Set true when Chron is being started partway through a conversation that has already begun. Marks the session as partial evidence valid from the attach point forward. Do not estimate how many earlier messages there were \u2014 that is self-report, not evidence.",
    "type": "boolean"
   },
   "external_ref": {
    "description": "External ticket/PR reference, e.g. \"jira:ENG-123\" or \"github:org/repo#456\"",
    "type": "string"
   },
   "limit": {
    "description": "Number of recent messages to return (default: 10)",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "metadata": {
    "additionalProperties": false,
    "description": "Evidence provenance. `partial` is derived by Chron and cannot be set by the caller.",
    "properties": {
     "attach_reason": {
      "type": "string"
     },
     "client": {
      "type": "string"
     },
     "source": {
      "description": "How Chron obtained this session. Defaults to witnessed.",
      "enum": [
       "witnessed",
       "imported",
       "live_attach",
       "transcript_backfill"
      ],
      "type": "string"
     }
    },
    "type": "object"
   },
   "parent_session_id": {
    "description": "Parent session ID if this is a subagent session spawned by another session",
    "type": "string"
   },
   "resume_latest": {
    "description": "When true, resumes the most recently active session instead of matching by title. Use this to survive context compaction without breaking the audit chain.",
    "type": "boolean"
   },
   "title": {
    "description": "Descriptive session title, e.g. \"Contract review \u2014 2026-05-08\"",
    "type": "string"
   }
  },
  "required": [
   "title"
  ],
  "type": "object"
 },
 "name": "init_session"
}
