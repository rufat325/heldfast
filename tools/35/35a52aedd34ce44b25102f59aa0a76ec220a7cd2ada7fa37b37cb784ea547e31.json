{
 "description": "Generate a roundup blog post combining multiple related ASTGL articles on a topic.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 5,
    "description": "Number of articles to include (default: 5)",
    "maximum": 10,
    "minimum": 2,
    "type": "number"
   },
   "topic": {
    "description": "Topic to generate roundup for (e.g., 'local AI tools')",
    "type": "string"
   }
  },
  "required": [
   "topic"
  ],
  "type": "object"
 },
 "name": "export_topic_roundup"
}
