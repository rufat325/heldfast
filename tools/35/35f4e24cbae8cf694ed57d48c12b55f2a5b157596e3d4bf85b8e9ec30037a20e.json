{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get precise sunrise, sunset, solar noon, and solar midnight times for any global coordinate and date from the Norwegian Meteorological Institute Sunrise 3.0 API. For the Sun: returns sunrise time + azimuth, sunset time + azimuth, solar noon elevation and visibility. For the Moon: returns moonrise, moonset, high moon, low moon, and lunar phase (0\u2013360\u00b0). Supports UTC offset for local time output. Global coverage; handles polar day/night (midnight sun / polar night). CC BY 4.0 + NLOD 2.0, no auth, no upstream cost.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "body": {
    "description": "Celestial body: \"sun\" returns sunrise/sunset/solarnoon (default), \"moon\" returns moonrise/moonset/phase.",
    "enum": [
     "sun",
     "moon"
    ],
    "type": "string"
   },
   "date": {
    "description": "Date to compute times for in ISO 8601 format YYYY-MM-DD (e.g. \"2026-06-24\")",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "lat": {
    "description": "Latitude in decimal degrees (e.g. 59.91 for Oslo, 51.5 for London)",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "lon": {
    "description": "Longitude in decimal degrees (e.g. 10.75 for Oslo, -0.12 for London)",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "offset": {
    "description": "UTC offset for local time output in \u00b1HH:MM format (e.g. \"+02:00\" for CEST). Defaults to UTC.",
    "type": "string"
   }
  },
  "required": [
   "lat",
   "lon",
   "date"
  ],
  "type": "object"
 },
 "name": "metno.astronomy.sunrise",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "MET Norway \u2014 Sunrise, Sunset & Moon Times"
}
