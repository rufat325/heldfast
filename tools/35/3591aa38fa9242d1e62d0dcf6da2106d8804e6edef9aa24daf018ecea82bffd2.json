{
 "description": "Full database management system for agents. 28 actions: DDL (create/alter/drop/clone/rename tables & columns), DML (insert/upsert/query/aggregate/update/delete/truncate), indexing (create/list/drop/reindex), import/export (JSON/CSV, conflict handling), raw SQL, maintenance (stats/vacuum/analyze/explain), archiving. WHERE supports operators: $gt, $gte, $lt, $lte, $ne, $like, $ilike, $in, $not_in, $is_null, $between. Works on SQLite, Postgres, MySQL, Turso.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "action": {
    "description": "create_table, list_tables, describe_table, insert, upsert, query, aggregate, update, delete_rows, truncate, drop_table, clone_table, rename_table, rename_column, add_column, drop_column, create_index, list_indexes, drop_index, reindex, archive_table, unarchive_table, export, import, sql, stats, vacuum, analyze, explain",
    "type": "string"
   },
   "column": {
    "additionalProperties": {},
    "description": "For add_column: {name, type, ...}",
    "type": "object"
   },
   "columnName": {
    "description": "For drop_column",
    "type": "string"
   },
   "columns": {
    "description": "For create_table: [{name, type, required?, default?, unique?, primaryKey?, references?: {table, column, onDelete?}, check?}]",
    "items": {
     "additionalProperties": {},
     "type": "object"
    },
    "type": "array"
   },
   "conflictColumn": {
    "description": "For upsert/import",
    "type": "string"
   },
   "description": {
    "description": "For create_table: human-readable description",
    "type": "string"
   },
   "distinct": {
    "description": "SELECT DISTINCT",
    "type": "boolean"
   },
   "format": {
    "description": "For export: json|csv",
    "type": "string"
   },
   "groupBy": {
    "description": "GROUP BY clause",
    "type": "string"
   },
   "having": {
    "description": "HAVING clause",
    "type": "string"
   },
   "includeArchived": {
    "description": "For list_tables",
    "type": "boolean"
   },
   "includeData": {
    "description": "For clone_table",
    "type": "boolean"
   },
   "includeShared": {
    "description": "For list_tables",
    "type": "boolean"
   },
   "indexColumns": {
    "description": "For create_index",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "indexName": {
    "description": "For create/drop_index",
    "type": "string"
   },
   "indexUnique": {
    "description": "For create_index",
    "type": "boolean"
   },
   "indexWhere": {
    "description": "Partial index condition",
    "type": "string"
   },
   "indexes": {
    "description": "For create_table: [{columns, unique?, name?, where?}]",
    "items": {
     "additionalProperties": {},
     "type": "object"
    },
    "type": "array"
   },
   "limit": {
    "description": "Max rows",
    "type": "number"
   },
   "newName": {
    "description": "For rename_table/rename_column/clone_table",
    "type": "string"
   },
   "offset": {
    "description": "Skip rows",
    "type": "number"
   },
   "oldName": {
    "description": "For rename_column",
    "type": "string"
   },
   "onConflict": {
    "description": "For import: skip|replace|error",
    "type": "string"
   },
   "operations": {
    "description": "For aggregate: [{fn: count|sum|avg|min|max|count_distinct, column?, alias?}]",
    "items": {
     "additionalProperties": {},
     "type": "object"
    },
    "type": "array"
   },
   "orderBy": {
    "description": "ORDER BY clause",
    "type": "string"
   },
   "params": {
    "description": "For sql/explain: query params",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "rows": {
    "description": "For insert/upsert/import: row objects",
    "items": {
     "additionalProperties": {},
     "type": "object"
    },
    "type": "array"
   },
   "selectColumns": {
    "description": "Specific columns to select",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "set": {
    "additionalProperties": {},
    "description": "For update: {column: newValue}",
    "type": "object"
   },
   "shared": {
    "description": "For create_table: shared across agents",
    "type": "boolean"
   },
   "sql": {
    "description": "For sql/explain: raw SQL",
    "type": "string"
   },
   "table": {
    "description": "Table name",
    "type": "string"
   },
   "timestamps": {
    "description": "For create_table: auto-add created_at/updated_at (default: true)",
    "type": "boolean"
   },
   "where": {
    "additionalProperties": {},
    "description": "Filter conditions with operator support",
    "type": "object"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "storage"
}
