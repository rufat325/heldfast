{
 "description": "Write a batch of cells in the live workbook (values or formulas). After writing, marks filters stale and persists a workbook snapshot.\n\nWARNING about the 'Data' sheet layout: column A is the Bridge label column (A1='Data Type', A2='Header', A3='Data', A4+ empty) \u2014 do NOT overwrite A1/A2/A3 unless you specifically want to invalidate the Bridge data sheet. User data lives in columns B onwards: Row 1 = Data Type, Row 2 = Field Name, Row 3+ = values. For changing Data Types or Field Names prefer set_data_type / set_field_name (clearer intent + same-row validation).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "edits": {
    "description": "Batch of cell edits. Each entry needs either { a1 } or { row, col }.",
    "items": {
     "additionalProperties": false,
     "properties": {
      "a1": {
       "description": "Target cell in A1 notation (e.g. 'B3'). Mutually exclusive with row/col.",
       "type": "string"
      },
      "col": {
       "description": "0-based column index.",
       "minimum": 0,
       "type": "integer"
      },
      "formula": {
       "description": "Excel formula starting with '=' (or without \u2014 '=' will be added). Allowed operators: + - * / ( ).",
       "type": "string"
      },
      "row": {
       "description": "0-based row index.",
       "minimum": 0,
       "type": "integer"
      },
      "value": {
       "description": "Literal value to write. Ignored when `formula` is provided.",
       "type": [
        "string",
        "number",
        "boolean",
        "null"
       ]
      }
     },
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "sheet": {
    "description": "Sheet name.",
    "type": "string"
   }
  },
  "required": [
   "sheet",
   "edits"
  ],
  "type": "object"
 },
 "name": "write_sheet_cells"
}
