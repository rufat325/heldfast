{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "list-activity-timeout-policies"
 },
 "description": "Get a list of activityBasedTimeoutPolicy objects.\n\nTIP: Lists activity-based timeout policies. These enforce idle session timeout for web applications. Each has displayName, isOrganizationDefault, and definition (JSON with ActivityBasedTimeoutPolicy settings).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "count": {
    "description": "count",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "filter": {
    "description": "OData $filter expression, e.g. status eq 'inProgress'",
    "type": "string"
   },
   "orderby": {
    "description": "orderby",
    "type": "string"
   },
   "search": {
    "description": "search",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "skip": {
    "description": "skip",
    "type": "string"
   },
   "top": {
    "description": "Page size (Graph $top). Start small (5-15) so responses fit the model context. Use @odata.nextLink from the response for more rows.",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list-activity-timeout-policies"
}
