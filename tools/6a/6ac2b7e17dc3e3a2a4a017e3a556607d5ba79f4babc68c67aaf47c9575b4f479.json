{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Objects within a radius of (RA, Dec) decimal degrees.",
 "inputSchema": {
  "examples": [
   {
    "dec": 22.145,
    "ra": 83.633,
    "radius_deg": 0.5
   },
   {
    "dec": -60.8,
    "max": 50,
    "ra": 210.8,
    "radius_deg": 1
   }
  ],
  "properties": {
   "dec": {
    "type": "number"
   },
   "max": {
    "description": "Top-N rows to return (default 100).",
    "type": "number"
   },
   "ra": {
    "type": "number"
   },
   "radius_deg": {
    "type": "number"
   }
  },
  "required": [
   "ra",
   "dec",
   "radius_deg"
  ],
  "type": "object"
 },
 "name": "cone_search",
 "outputSchema": {
  "oneOf": [
   {
    "description": "Parsed JSON response from TAP cone search query",
    "type": "object"
   },
   {
    "properties": {
     "body": {
      "description": "Raw text response body when JSON parsing fails",
      "type": "string"
     },
     "format": {
      "description": "Response format (json)",
      "type": "string"
     }
    },
    "required": [
     "format",
     "body"
    ],
    "type": "object"
   }
  ]
 },
 "title": "Cone Search",
 "version": "v1.03ca4b09"
}
