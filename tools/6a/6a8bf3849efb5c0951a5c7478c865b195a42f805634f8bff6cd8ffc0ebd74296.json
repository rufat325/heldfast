{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get historical events, births, deaths, holidays, or editor-selected highlights that occurred on a given month/day across history, as curated by Wikipedia. Select category with type (births, deaths, events, holidays, selected) and the language edition with lang (default en). Data: Wikipedia REST API (wikipedia.org/api/rest_v1/feed/onthisday), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "day": {
    "description": "2-digit day of month (e.g. 30)",
    "pattern": "^\\d{2}$",
    "type": "string"
   },
   "lang": {
    "description": "Wikipedia language edition subdomain code (default en, e.g. de, fr, ja, zh-yue)",
    "pattern": "^[a-z]{2,3}(-[a-z0-9]{1,8})?$",
    "type": "string"
   },
   "month": {
    "description": "2-digit month (e.g. 08 for August)",
    "pattern": "^\\d{2}$",
    "type": "string"
   },
   "type": {
    "description": "Category of historical events to return",
    "enum": [
     "births",
     "deaths",
     "events",
     "holidays",
     "selected"
    ],
    "type": "string"
   }
  },
  "required": [
   "type",
   "month",
   "day"
  ],
  "type": "object"
 },
 "name": "wikimedia-rest.feed.on_this_day",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Wikipedia On This Day"
}
