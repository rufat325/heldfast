{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "NOT AVAILABLE: the dependency graph came from apiland.deno.dev, which was shut down on 2026-07-20; the registry CDN that replaced it publishes no dependency data. Returns `empty_reason: \"upstream_removed\"` with the module's source repository, so you can read its deps at source. Use version for the file listing and repo pointer.",
 "inputSchema": {
  "examples": [
   {
    "name": "fresh",
    "version": "1.4.0"
   }
  ],
  "properties": {
   "name": {
    "type": "string"
   },
   "version": {
    "type": "string"
   }
  },
  "required": [
   "name",
   "version"
  ],
  "type": "object"
 },
 "name": "dependencies",
 "outputSchema": {
  "properties": {
   "dependencies": {
    "oneOf": [
     {
      "description": "Dependency map from version metadata",
      "type": "object"
     },
     {
      "description": "No dependencies found",
      "type": "null"
     }
    ]
   }
  },
  "required": [
   "dependencies"
  ],
  "type": "object"
 },
 "title": "Dependencies",
 "version": "v1.c0469879"
}
