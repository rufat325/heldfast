{
 "description": "Create a new resource in a workspace. Set `type` to what you're creating: \"file\" (needs libraryId + title + complete content), \"library\" (a context library \u2014 needs name), or \"agent\" / \"skill\" / \"workflow\" (need name; a workflow can bundle projectIds, agentIds, skillIds, and tagIds). Always provide full, real content for files \u2014 never empty or placeholder.\n\nFile linking: to link to another file in the SAME workspace, put //<fileId> inline in the content \u2014 use the target file's UUID (from search/list/get results), not its title. The link renders as the target's title and is indexed automatically on save; the target gains a backlink with no extra step. Add a link only where the text genuinely references an existing file; never invent //<id> for a file that does not exist (it becomes a dangling link). Use get({ type: \"file_links\", id }) to inspect a file's links and backlinks.",
 "inputSchema": {
  "properties": {
   "agentIds": {
    "description": "Agent IDs (type=workflow)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "content": {
    "description": "Full file content \u2014 must be complete (type=file)",
    "type": "string"
   },
   "description": {
    "description": "Description (type=library, agent, skill, workflow)",
    "type": "string"
   },
   "folderId": {
    "description": "Folder ID (type=file)",
    "type": "string"
   },
   "isPrivate": {
    "description": "Workflow visibility (type=workflow). Defaults to true \u2014 visible only to you until shared. Set false to share with the whole team.",
    "type": "boolean"
   },
   "libraryId": {
    "description": "Library ID (type=file)",
    "type": "string"
   },
   "name": {
    "description": "Name (type=library, agent, skill, workflow)",
    "type": "string"
   },
   "path": {
    "description": "File path (type=file)",
    "type": "string"
   },
   "projectIds": {
    "description": "Workflow library IDs (type=workflow)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "skillIds": {
    "description": "Skill IDs (type=workflow)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "tagIds": {
    "description": "Workflow tag IDs (type=workflow)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "title": {
    "description": "Title (type=file)",
    "type": "string"
   },
   "type": {
    "description": "Resource type to create. (Tags are not created here \u2014 a tag exists by adding a #<value> token to a file's content; use manage_relationship type=tag to tag a file.)",
    "enum": [
     "file",
     "library",
     "agent",
     "skill",
     "workflow"
    ],
    "type": "string"
   },
   "workspaceId": {
    "type": "string"
   }
  },
  "required": [
   "type"
  ],
  "type": "object"
 },
 "name": "create"
}
