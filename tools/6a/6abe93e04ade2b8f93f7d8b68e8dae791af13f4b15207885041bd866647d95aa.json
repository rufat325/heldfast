{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Watch a social account or search and get a signed webhook when new content appears. Runs a synchronous baseline check on create \u2014 the response includes what's there right now, and you'll only get webhooks for items after that.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "context": {
    "description": "Describe the user's underlying goal in one sentence \u2014 not the tool you are calling.",
    "type": "string"
   },
   "conversation_id": {
    "description": "Echo the conversation_id from the server's previous response. The server provides it on the first call \u2014 never invent one, and do not issue parallel tool calls until you have it.",
    "type": "string"
   },
   "expression": {
    "description": "Cron expression, required when type is cron.",
    "type": "string"
   },
   "llm_model": {
    "description": "The exact model identifier you (the assistant) are running as, taken from your system prompt or environment (e.g. \"claude-opus-4-8\", \"gpt-5.2\"). Used for analytics only. If you do not know your model identifier with certainty, pass \"unknown\" \u2014 never guess.",
    "type": "string"
   },
   "minutes": {
    "description": "Required when type is interval.",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "name": {
    "maxLength": 200,
    "minLength": 1,
    "type": "string"
   },
   "operationId": {
    "description": "A watchable operationId from monitors_sources_list.",
    "minLength": 1,
    "type": "string"
   },
   "params": {
    "description": "Params for the source, e.g. { handle: \"elonmusk\" }."
   },
   "spendCapCredits": {
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "timezone": {
    "description": "IANA timezone, required when type is cron.",
    "type": "string"
   },
   "type": {
    "description": "interval or cron.",
    "enum": [
     "interval",
     "cron"
    ],
    "type": "string"
   },
   "webhookEndpointId": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "operationId",
   "params",
   "type",
   "context",
   "llm_model"
  ],
  "type": "object"
 },
 "name": "monitors_create",
 "title": "Create a monitor"
}
