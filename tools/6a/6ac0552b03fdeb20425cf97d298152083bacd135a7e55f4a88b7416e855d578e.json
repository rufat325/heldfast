{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Free. Start here: every tool with its price in USD, required arguments, what it returns, the x402 payment terms (USDC on Solana mainnet or Base, payTo per network, facilitator) and the label semantics URL. Returns JSON. Use it first to choose a tool and budget; it carries no market data.",
 "inputSchema": {
  "properties": {},
  "title": "catalogArguments",
  "type": "object"
 },
 "name": "catalog",
 "title": "Catalog (free)"
}
