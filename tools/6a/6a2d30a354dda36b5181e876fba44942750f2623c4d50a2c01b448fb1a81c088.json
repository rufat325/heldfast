{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Extract text from a PDF by URL. Handles compressed streams, PDF 1.5+ object streams and ToUnicode CMaps, and reports encrypted or image-only documents honestly instead of returning garbage. Costs $0.020 in USDC on Base, paid via the x402 protocol, or from a credit token \u2014 call credits_trial for free credit if you have neither.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "credit_token": {
    "description": "Optional. A credit token from credits_trial or /credits/buy. Supplying it pays for this call from that balance, so no x402 payment or wallet is needed.",
    "type": "string"
   },
   "max_pages": {
    "description": "Optional page cap",
    "type": "integer"
   },
   "url": {
    "description": "HTTPS URL of the PDF",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "pdf_parse",
 "outputSchema": {
  "properties": {
   "extracted_at": {
    "description": "ISO-8601 extraction time",
    "type": "string"
   },
   "page_count": {
    "description": "Pages extracted",
    "type": "number"
   },
   "pages": {
    "items": {
     "properties": {
      "page": {
       "description": "1-based page number",
       "type": "number"
      },
      "text": {
       "description": "Text on that page",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "url": {
    "description": "The PDF that was fetched",
    "type": "string"
   }
  },
  "required": [
   "url",
   "page_count",
   "pages"
  ],
  "type": "object"
 },
 "title": "Extract text from a PDF"
}
