{
 "annotations": {
  "readOnlyHint": true,
  "title": "Find connection path"
 },
 "description": "How two objects in the user's graph are connected: the shortest chain of real relationships between them, up to 3 hops, e.g. person -[worked_with]- person -[works_at]- company. Only deliberate relationships count \u2014 bulk-import and system-inferred edges are excluded, so a path is evidence. Returns the path as a list of hops, and an EMPTY list when there is no connection: report that honestly, never invent a path.",
 "inputSchema": {
  "properties": {
   "from_object_id": {
    "description": "Where the path starts (exact object_id).",
    "type": "string"
   },
   "to_object_id": {
    "description": "Where the path ends (exact object_id).",
    "type": "string"
   }
  },
  "required": [
   "from_object_id",
   "to_object_id"
  ],
  "type": "object"
 },
 "name": "find_paths",
 "title": "Find connection path"
}
