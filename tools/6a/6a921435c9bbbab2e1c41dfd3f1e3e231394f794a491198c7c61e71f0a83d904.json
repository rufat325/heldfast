{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Step 2: carry out exactly the mapping you send; returns rows as JSON and CSV, errors per row and hashes. Nothing is stored and nothing is silently corrected. Free, no token.",
 "inputSchema": {
  "properties": {
   "content": {
    "description": "The file's TEXT, for CSV / delimited text / JSON / NDJSON. Give this OR contentBase64.",
    "type": "string"
   },
   "contentBase64": {
    "description": "The file's BYTES, base64-encoded \u2014 required for .xlsx, fine for any format. Give this OR content.",
    "type": "string"
   },
   "fileName": {
    "description": "The file's name, e.g. \"prices.csv\".",
    "type": "string"
   },
   "headerRow": {
    "description": "The row number of the header row, counted from 1.",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "limit": {
    "description": "Rows in this page.",
    "maximum": 50000,
    "minimum": 1,
    "type": "integer"
   },
   "mapping": {
    "description": "THE EXPLICIT MAPPING to carry out \u2014 normally `proposedMapping` from file_to_data_inspect, corrected where needed: { \"targetSchemaId\" | \"targetSchema\", \"fields\": [{ \"target\", \"source\", \"sourceIndex\"?, \"decimalMark\"?: \".\" | \",\", \"dateFormat\"?: \"YMD\" | \"DMY\" | \"MDY\" | \"excel_serial\" }] }. A number is read ONLY under decimalMark, a date ONLY under dateFormat.",
    "type": "object"
   },
   "offset": {
    "description": "First row of the page, starting at 0.",
    "minimum": 0,
    "type": "integer"
   },
   "sheet": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "maximum": 999,
      "minimum": 0,
      "type": "integer"
     }
    ],
    "description": "XLSX only: the sheet's name exactly as written, or its position starting at 0."
   },
   "targetSchema": {
    "description": "Your own target schema: { \"id\"?, \"title\"?, \"fields\": [{ \"name\", \"type\", \"required\"?, \"description\"?, \"synonyms\"? }] } with type one of string, identifier, integer, decimal, date, boolean, email.",
    "type": "object"
   },
   "targetSchemaId": {
    "description": "A built-in target schema: supplier_price_list, customer_import or order_lines. Give this OR targetSchema.",
    "type": "string"
   }
  },
  "required": [
   "mapping"
  ],
  "type": "object"
 },
 "name": "file_to_data_transform",
 "outputSchema": {
  "properties": {
   "data": {
    "type": "object"
   },
   "kind": {
    "type": "string"
   }
  },
  "required": [
   "kind",
   "data"
  ],
  "type": "object"
 }
}
