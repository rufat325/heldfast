{
 "description": "Query the register of signed contracts (read-only): list | find | get | show | stats | history.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "Read-only action.",
    "enum": [
     "list",
     "find",
     "get",
     "show",
     "stats",
     "history"
    ],
    "type": "string"
   },
   "arg": {
    "description": "Argument for find/get/show/history (query or deal id).",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "contract_vault_query"
}
