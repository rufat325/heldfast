{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Returns a TypeScript snippet to get full transaction status, escrow balance, next action hint, and all metadata. Use to check what state a transaction is in.",
 "inputSchema": {
  "properties": {
   "network": {
    "default": "testnet",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   },
   "txId": {
    "description": "Transaction ID returned by agirails_request_service or agirails_list_transactions",
    "type": "string"
   }
  },
  "required": [
   "txId"
  ],
  "type": "object"
 },
 "name": "agirails_get_transaction"
}
