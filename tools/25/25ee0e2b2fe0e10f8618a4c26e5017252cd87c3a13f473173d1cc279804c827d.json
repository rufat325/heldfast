{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get analytics filter values"
 },
 "description": "Get distinct values available for a field in an analytics table. Useful for building valid filter values before querying.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "collection": {
    "description": "Collection name to get filter values for",
    "type": "string"
   },
   "filters": {
    "description": "Fields to get distinct values for",
    "items": {
     "additionalProperties": false,
     "properties": {
      "field": {
       "description": "Field name to get distinct values for",
       "type": "string"
      },
      "operator": {
       "description": "Optional operator to apply when retrieving values",
       "type": "string"
      },
      "value": {
       "description": "Optional seed value"
      }
     },
     "required": [
      "field"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "collection",
   "filters"
  ],
  "type": "object"
 },
 "name": "nevent_analytics_filter_values"
}
