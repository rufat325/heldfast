{
 "description": "Directly execute HumanMirror Safe Preflight over MCP. Unpaid calls return the standard x402 V2 HTTP 402 challenge for 0.010 USDC on Base; retry the same tools/call with PAYMENT-SIGNATURE.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "criteria": {
    "description": "Risk policy, for example max_risk and allow_confirmation.",
    "type": "object"
   },
   "input": {
    "description": "Proposed command or agent tool action to inspect before execution.",
    "type": "object"
   },
   "options": {
    "description": "Optional Safe Preflight execution options.",
    "type": "object"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "humanmirror_safe_preflight"
}
