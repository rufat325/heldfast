{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get a policy overview for a country \u2014 recent and notable policies grouped by type",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "country_code": {
    "description": "ISO alpha-2 code (e.g. \"NG\", \"KE\", \"ZA\")",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   },
   "top_limit": {
    "default": 5,
    "description": "Top items per category",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "country_code"
  ],
  "type": "object"
 },
 "name": "get_country_policies"
}
