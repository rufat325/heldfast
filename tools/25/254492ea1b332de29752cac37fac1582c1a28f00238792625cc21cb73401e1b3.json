{
 "description": "Follow the development of a news story over time. Returns articles in chronological order showing how a story evolved. Use since to control the lookback window.",
 "inputSchema": {
  "properties": {
   "country": {
    "description": "Limit to a country code (optional)",
    "type": "string"
   },
   "limit": {
    "description": "Max articles, 1\u201320 (default 10)",
    "type": "number"
   },
   "since": {
    "description": "Lookback window (default: week)",
    "enum": [
     "today",
     "week",
     "month"
    ],
    "type": "string"
   },
   "topic": {
    "description": "Topic, keyword, event, or person name to track",
    "type": "string"
   }
  },
  "required": [
   "topic"
  ],
  "type": "object"
 },
 "name": "track_story"
}
