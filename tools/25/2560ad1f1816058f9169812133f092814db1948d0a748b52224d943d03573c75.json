{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Workspace"
 },
 "description": "Search workspace and get workspace info.\n\nActions (required params -> optional):\n- info: workspace name, plan, and bot user\n- search (-> query, filter.object=\"page\"|\"data_source\", sort, limit): find pages/databases shared with integration",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Action to perform",
    "enum": [
     "info",
     "search"
    ],
    "type": "string"
   },
   "filter": {
    "properties": {
     "object": {
      "description": "Filter by type: page or data_source (database)",
      "enum": [
       "page",
       "data_source"
      ],
      "type": "string"
     }
    },
    "type": "object"
   },
   "limit": {
    "description": "Max results",
    "type": "number"
   },
   "query": {
    "description": "Search query",
    "type": "string"
   },
   "sort": {
    "properties": {
     "direction": {
      "enum": [
       "ascending",
       "descending"
      ],
      "type": "string"
     },
     "timestamp": {
      "enum": [
       "last_edited_time",
       "created_time"
      ],
      "type": "string"
     }
    },
    "type": "object"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "workspace",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 }
}
