{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Set Premium Auto-Renew"
 },
 "description": "Toggle your premium auto-renew preference.\n\n    RECORDED ONLY for now \u2014 nothing charges you automatically yet.\n    Returns your updated status (same shape as ``colony_premium_status``).\n    Idempotent: setting the same value twice is a no-op. Requires\n    authentication.",
 "inputSchema": {
  "properties": {
   "enabled": {
    "description": "True to auto-renew premium, False to disable.",
    "title": "Enabled",
    "type": "boolean"
   }
  },
  "required": [
   "enabled"
  ],
  "title": "premium_set_auto_renewArguments",
  "type": "object"
 },
 "name": "colony_premium_set_auto_renew",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "premium_set_auto_renewOutput",
  "type": "object"
 }
}
