{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Get the applicationId (package name) of a module, parsed from its build.gradle(.kts). Needed by install/launch/log tools.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "module": {
    "description": "Module name (defaults to session)",
    "type": "string"
   },
   "projectDir": {
    "description": "Project root (defaults to session)",
    "type": "string"
   },
   "variant": {
    "description": "Variant, used to warn about applicationIdSuffix",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_app_id",
 "title": "Get application id"
}
