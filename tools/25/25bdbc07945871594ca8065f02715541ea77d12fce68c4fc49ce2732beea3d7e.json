{
 "_meta": {
  "skill": "ordinals-p2p"
 },
 "description": "Record a completed inscription transfer on the trade ledger.\n\nMarks a trade as closed by recording the on-chain (or off-chain sBTC) transfer.\nThe active wallet signs the record with BIP-137 to prove the transfer was\nauthorized by the sending party.\n\nRequires an unlocked wallet with Bitcoin keys.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "amount_sats": {
    "description": "Transfer amount in satoshis",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "inscription_id": {
    "description": "Inscription ID being transferred",
    "type": "string"
   },
   "metadata": {
    "description": "Optional freeform metadata",
    "type": "string"
   },
   "parent_trade_id": {
    "description": "Parent trade ID being fulfilled (if closing an open offer)",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "to_agent": {
    "description": "Recipient BTC address",
    "type": "string"
   },
   "tx_hash": {
    "description": "On-chain transaction hash (optional for sBTC/off-chain transfers)",
    "type": "string"
   }
  },
  "required": [
   "inscription_id",
   "to_agent"
  ],
  "type": "object"
 },
 "name": "ordinals_p2p_transfer"
}
