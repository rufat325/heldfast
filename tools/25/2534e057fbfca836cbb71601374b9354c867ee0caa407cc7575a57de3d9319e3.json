{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Update article"
 },
 "description": "Update article text content from markdown. Use <SCREENSHOT {id}> placeholders to position screenshots.\nScreenshots in the markdown are kept. Screenshots removed from markdown are deleted. Order in markdown defines final order.\nGet the current article first with get_article, modify the markdown, then call this.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "markdown": {
    "description": "Updated article content as markdown with <SCREENSHOT {id}> placeholders",
    "type": "string"
   },
   "project_id": {
    "description": "Project ID",
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "markdown"
  ],
  "type": "object"
 },
 "name": "update_article",
 "title": "Update article"
}
