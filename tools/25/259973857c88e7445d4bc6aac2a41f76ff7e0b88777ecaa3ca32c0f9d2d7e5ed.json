{
 "description": "Unlock PDF. Remove the password from a document you can already open. You type the password \u2014 we never guess it. Accepts application/pdf, .pdf; up to 100 MB per file.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "outputDir": {
    "description": "Where to put the results. Defaults to the folder each source file came from. Existing files are never overwritten.",
    "type": "string"
   },
   "password": {
    "description": "Document password",
    "minLength": 1,
    "secret": true,
    "type": "string"
   },
   "paths": {
    "description": "Paths to the files to process, absolute or relative to the working directory.",
    "items": {
     "type": "string"
    },
    "maxItems": 25,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "paths",
   "password"
  ],
  "type": "object"
 },
 "name": "pdf_unlock"
}
