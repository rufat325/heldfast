{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "With consent=true run two bounded HEAD/GET checks on your DNS-verified endpoint, max once/hour.\n    With consent=false read your last private result. Not an audit or LLM test; no score effect.\n    ",
 "inputSchema": {
  "properties": {
   "agent_id": {
    "title": "Agent Id",
    "type": "string"
   },
   "api_key": {
    "title": "Api Key",
    "type": "string"
   },
   "consent": {
    "default": false,
    "title": "Consent",
    "type": "boolean"
   }
  },
  "required": [
   "agent_id",
   "api_key"
  ],
  "title": "agent_endpoint_assessmentArguments",
  "type": "object"
 },
 "name": "agent_endpoint_assessment",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "agent_endpoint_assessmentOutput",
  "type": "object"
 }
}
