{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Append an entry to a stream (XADD). Returns the entry ID.",
 "inputSchema": {
  "properties": {
   "data": {
    "description": "Entry data (flat object)",
    "type": "object"
   },
   "maxlen": {
    "description": "Approximate max stream length (trim)",
    "type": "number"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   },
   "stream": {
    "description": "Stream name",
    "type": "string"
   }
  },
  "required": [
   "spaceId",
   "stream",
   "data"
  ],
  "type": "object"
 },
 "name": "stream_append",
 "outputSchema": {
  "properties": {},
  "type": "object"
 }
}
