{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find marine biodiversity datasets in the OBIS network by species name or geographic area. Returns dataset metadata: title, abstract, record count, citation, and source URL. Useful for discovering primary data sources, citing specific surveys, and understanding data provenance for a species or region. The OBIS network aggregates 4,200+ datasets from 900+ institutions worldwide. Source: OBIS \u2014 free, CC BY 4.0, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "areaid": {
    "description": "OBIS area ID to find datasets covering a specific geographic region.",
    "type": "integer"
   },
   "limit": {
    "description": "Number of datasets to return (1-50, default 10).",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "scientificname": {
    "description": "Scientific name to find datasets containing records for that species (e.g. \"Balaenoptera musculus\").",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "obis.marine.dataset_search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "OBIS \u2014 Marine Dataset Search"
}
