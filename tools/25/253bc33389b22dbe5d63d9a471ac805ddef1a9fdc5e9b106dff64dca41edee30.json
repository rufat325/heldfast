{
 "description": "Add a NIP to monitoring. You get notified (via changes_since or email) when its VAT status, bank account, or registry data shown on the Ministry of Finance White List (name, address, KRS, REGON) changes. Requires a skanfirmy API key (Authorization: Bearer, or the api_key argument). Free plan: up to 10 NIPs.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "skanfirmy API key (if not provided in the Authorization header).",
    "type": "string"
   },
   "nip": {
    "description": "10-digit NIP to monitor.",
    "type": "string"
   }
  },
  "required": [
   "nip"
  ],
  "type": "object"
 },
 "name": "observe_nip"
}
