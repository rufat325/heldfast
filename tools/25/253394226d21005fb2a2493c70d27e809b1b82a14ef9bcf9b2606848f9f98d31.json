{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Call once per pick after onchain_agent_recommend_stack. USE WHEN a component has been picked (from onchain_agent_recommend_stack, onchain_agent_build_plan or a search) and the next step is installing it, or you are about to write setup instructions and want steps checked against a clean install rather than copied from a README. Returns the structured deploy manifest for one resource by slug \u2014 runtime, install command(s), entry snippet, required keys/env/wallet/RPC, chains, license, whether it is itself an MCP server, and a deploy_status.\n\nPREFLIGHT COMES WITH IT: every answer carries `preflight_summary` \u2014 the Preflight verdict for this listing from Sato Hub's records (no live handshake), the rule that decided it and the two or three readings it turned on. `full_check` names the onchain_agent_preflight call that returns every evidence line. A verdict is not a security review, and `unknown` means no record, not a problem.\n\ndeploy_status is a trust signal, NOT a safety guarantee: \"verified\" = the install path was reproduced/confirmed from the project's docs; \"self_reported\" = parsed from the project's README and not independently reproduced. Always verify keys, permissions, and funds before running anything.\n\n`save_stack_url` is this listing's page on satohub.ai with the same install steps \u2014 a link to hand to the person you are building for.\n\nReturns (json): { slug, name, github_url, docs_url, sato_url, deploy_spec: { runtime, install[], entry, requires[], chains[], license, mcp_native, mcp_invocation?, deploy_clarity, deploy_status, source_url, as_of }, preflight_summary: { verdict, rule, rule_means, evidence[], checked_at, basis, full_check }, preflight_note, save_stack_url, next_step, note }. `next_step` is one sentence: the Preflight call to run before installing, and the save_stack_url to give the person you are building for \u2014 surface both. Returns an error if the resource has no manifest yet (they are being rolled out resource by resource), with installable alternatives. Read-only.\n\nExample: { slug: \"solana-agent-kit\" }",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "response_format": {
    "default": "markdown",
    "description": "Output format: 'markdown' (human-readable, default) or 'json' (machine-readable).",
    "enum": [
     "markdown",
     "json"
    ],
    "type": "string"
   },
   "slug": {
    "description": "The resource slug to get a deploy manifest for, e.g. 'solana-agent-kit'.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "onchain_agent_get_deploy_spec",
 "title": "Get install steps for a stack pick, with its Preflight attached"
}
