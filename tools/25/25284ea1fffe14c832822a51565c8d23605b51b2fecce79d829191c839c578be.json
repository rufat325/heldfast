{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List the CVEs most likely to be exploited, ranked by EPSS score descending. Answers \"which vulnerabilities are most likely to be exploited right now\" so you can prioritize patching. EPSS is the probability (0-1) a CVE will be exploited in the wild in the next 30 days; this complements CVSS severity rather than replacing it.",
 "inputSchema": {
  "examples": [
   {
    "limit": 30,
    "min_score": 0.9
   },
   {
    "limit": 50,
    "min_score": 0.75
   }
  ],
  "properties": {
   "limit": {
    "description": "Max CVEs to return (default 30, max 100).",
    "type": "number"
   },
   "min_score": {
    "description": "EPSS threshold 0-1; only CVEs with a score greater than this are returned. Default 0.9.",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "top_exploited",
 "title": "Top Exploited",
 "version": "v1.89810b36"
}
