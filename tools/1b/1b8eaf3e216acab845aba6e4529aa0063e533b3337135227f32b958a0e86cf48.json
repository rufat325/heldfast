{
 "description": "Restart a Codex CLI MCP server process. Works reliably only for wrapped MCPs (via wrap_codex_mcp_stdio) by killing the wrapped child PID from its pidfile; otherwise you typically need to restart Codex CLI.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "mcp_name": {
    "description": "The mcp_servers.<name> entry in ~/.codex/config.toml to restart",
    "type": "string"
   }
  },
  "required": [
   "mcp_name"
  ],
  "type": "object"
 },
 "name": "restart_codex_mcp_process"
}
