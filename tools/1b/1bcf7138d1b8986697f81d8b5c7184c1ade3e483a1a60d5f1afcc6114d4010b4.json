{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Counts, salary percentiles by currency, remote share, top employers, seniority and employment-type mix, city and source split for any slice of postings. Accepts the same filters as search_jobs. Use this instead of paging through results when the question is about the market rather than about individual roles.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "board_handle": {
    "description": "Restrict to one ATS board by its handle, exactly as the `handle` field of a posting reports it, e.g. \"EndeavorITSolution\" (SmartRecruiters) or \"walmart:wd504:WalmartExternal\" (Workday). The precise way to pull one employer's postings when names are ambiguous.",
    "type": "string"
   },
   "city": {
    "description": "City name as employers write it, e.g. \"Berlin\", \"New York\", \"Bengaluru\".",
    "type": "string"
   },
   "company": {
    "description": "Restrict to one employer by name.",
    "type": "string"
   },
   "country": {
    "description": "Country as ISO 3166-1 alpha-2 code (\"US\", \"DE\") or an English name (\"Germany\").",
    "type": "string"
   },
   "format": {
    "enum": [
     "markdown",
     "json"
    ],
    "type": "string"
   },
   "keywords": {
    "description": "Terms mentioned anywhere in the posting \u2014 title or description \u2014 e.g. [\"Kubernetes\"]. Any keyword matches. Use titles for the role itself; keywords for a tool, skill or technology the posting talks about.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "posted_within_days": {
    "description": "Only postings published in the last N days.",
    "type": "integer"
   },
   "salary_max": {
    "description": "Highest advertised salary to include.",
    "type": "number"
   },
   "salary_min": {
    "description": "Lowest advertised salary to include. Only postings that disclose a salary match.",
    "type": "number"
   },
   "titles": {
    "description": "Job titles to match, e.g. [\"Data Engineer\"]. Terms shorter than 3 characters are rejected by the index.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "work_arrangement": {
    "description": "Filter by how the work is done. Hybrid is not a separate filter upstream.",
    "items": {
     "enum": [
      "Remote",
      "In Person"
     ],
     "type": "string"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "job_market_insights",
 "title": "Aggregate a slice of the job market"
}
