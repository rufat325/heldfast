{
 "description": "Resume one parked execution member only after the user explicitly requests it. This requires a fresh non-mutating access probe and never authenticates, enters private data, or makes the member mutable by itself.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "browserSurface": {
    "enum": [
     "codex_in_app",
     "chrome_extension",
     "claude_in_chrome"
    ],
    "type": "string"
   },
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "executionId": {
    "minimum": 1,
    "type": "integer"
   },
   "expectedRevision": {
    "minimum": 1,
    "type": "integer"
   },
   "explicitUserResume": {
    "const": true,
    "type": "boolean"
   },
   "idempotencyKey": {
    "maxLength": 200,
    "minLength": 16,
    "pattern": "^[\\x20-\\x7e]+$",
    "type": "string"
   },
   "memberId": {
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "executionId",
   "memberId",
   "expectedRevision",
   "browserSurface",
   "explicitUserResume",
   "idempotencyKey"
  ],
  "type": "object"
 },
 "name": "trackly_resume_parked_apply_member"
}
