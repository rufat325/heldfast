{
 "description": "Everything we publish about one product, including its measurements with the machine and date each one happened on. Read-only. An id we do not have returns an `error` field and the list of ids that do exist \u2014 never an empty product.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "The product id.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_product",
 "title": "Look at one product"
}
