{
 "_meta": {
  "skill": "pillar"
 },
 "description": "Create a new Pillar smart wallet for agent direct operations. This is a bundled operation: generates a signing keypair, unlocks it, and deploys a new smart wallet with the pubkey registered. Backend deploys the contract and calls onboard() in background. After ~20-30 seconds the wallet is ready for pillar_direct_* operations.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "referredBy": {
    "default": "SPV9K21TBFAK4KNRJXF5DFP8N7W46G4V9RCJDC22.beta-v2-wallet",
    "description": "Contract address of the referring wallet. Defaults to the Pillar team wallet if not provided.",
    "type": "string"
   },
   "walletName": {
    "description": "Wallet name (3-20 chars, lowercase letters, numbers, hyphens). The contract will be deployed as {walletName}-wallet.",
    "maxLength": 20,
    "minLength": 3,
    "type": "string"
   }
  },
  "required": [
   "walletName"
  ],
  "type": "object"
 },
 "name": "pillar_direct_create_wallet"
}
