{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Combine images into a PDF"
 },
 "description": "Combine images into one multi-page PDF (one image per page).",
 "inputSchema": {
  "properties": {
   "dpi": {
    "description": "Assumed image resolution, which sets the physical page size (default 72)",
    "type": "number"
   },
   "inputs": {
    "description": "Image paths, in the page order you want",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "output": {
    "description": "Destination .pdf path",
    "type": "string"
   }
  },
  "required": [
   "inputs",
   "output"
  ],
  "type": "object"
 },
 "name": "images_to_pdf"
}
