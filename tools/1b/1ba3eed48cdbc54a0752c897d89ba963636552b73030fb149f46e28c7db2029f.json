{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List workflows with optional filters (active, tag names, project, name) and automatic cursor pagination. Returns id, name, active state, tags and timestamps \u2014 never the heavy node data.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "active": {
    "description": "Filter by active state",
    "type": "boolean"
   },
   "maxItems": {
    "default": 200,
    "description": "Max workflows to return across pages",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "name": {
    "description": "Filter by exact workflow name",
    "type": "string"
   },
   "projectId": {
    "description": "Filter by project ID",
    "type": "string"
   },
   "tags": {
    "description": "Comma-separated tag names to filter by",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_workflows"
}
