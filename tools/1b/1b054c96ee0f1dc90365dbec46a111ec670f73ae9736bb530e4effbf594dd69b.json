{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Renders one node and returns the picture itself, so a model that can see gets to look at the design rather than read a description of it. Nothing is stored anywhere.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "nodeId": {
    "description": "The node to render.",
    "type": "string"
   },
   "scale": {
    "description": "Render scale. Default 2.",
    "maximum": 4,
    "minimum": 1,
    "type": "number"
   }
  },
  "required": [
   "nodeId"
  ],
  "type": "object"
 },
 "name": "figma_export_png",
 "title": "See the design"
}
