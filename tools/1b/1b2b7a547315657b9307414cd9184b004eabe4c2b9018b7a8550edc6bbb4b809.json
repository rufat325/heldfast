{
 "annotations": {
  "readOnlyHint": true,
  "title": "Find VCs"
 },
 "description": "UNDERSTANDING \u2014 Find venture-capital firms by name, thesis tags, or category. Each carries a network-matched, rated portfolio graph. Sort by portfolio_on_network (default), portfolio_total, portfolio_rating, or name. Understanding plan. Priced B1 (single-entity synthesis) \u2014 $0.01 per call under pay-as-you-go; included in Understanding and Influence. See apis://prices.",
 "inputSchema": {
  "properties": {
   "category": {
    "description": "Firm category, e.g. \"Venture Capital\".",
    "type": "string"
   },
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "limit": {
    "default": 25,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "page": {
    "default": 1,
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "description": "Free text over name + description.",
    "type": "string"
   },
   "sort": {
    "enum": [
     "portfolio_on_network",
     "portfolio_total",
     "portfolio_rating",
     "name"
    ],
    "type": "string"
   },
   "tags": {
    "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or find_providers?api_tags= to reach companies whose APIs merely touch it.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "find_vcs"
}
