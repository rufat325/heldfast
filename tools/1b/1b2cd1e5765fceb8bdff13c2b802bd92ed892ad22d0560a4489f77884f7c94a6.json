{
 "description": "Place the order. IMPORTANT: Set confirm=true only when you have explicit user confirmation. Without confirm=true, this returns a preview instead of placing the order.",
 "inputSchema": {
  "properties": {
   "confirm": {
    "description": "Set to true to actually place the order. If false or omitted, returns a preview instead. NEVER set to true without explicit user confirmation.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "instacart_place_order"
}
