{
 "description": "Create a new job hunt to start tracking and applying to jobs. A job hunt defines what jobs you want to find based on titles, locations, skills, salary, etc. You need at least one job hunt to use match_jobs or add_job_to_applications.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "autoMode": {
    "description": "Enable full autopilot mode (default: false). When enabled, jobs are automatically matched, scored against your resume using AI, and applied to if they meet your minMatchScore threshold. Resume customization (if enabled) is applied before each application. Each auto-apply consumes a credit.",
    "type": "boolean"
   },
   "config": {
    "additionalProperties": false,
    "description": "Search filters configuration",
    "properties": {
     "baseSalaryMax": {
      "description": "Maximum salary",
      "type": "number"
     },
     "baseSalaryMin": {
      "description": "Minimum salary",
      "type": "number"
     },
     "companies": {
      "description": "Companies to include",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "companySize": {
      "description": "Company sizes (e.g., [\"xs\" for 1-50, \"s\" for 50-200, \"m\" for 200-1K, \"l\" for 1K-5K, \"xl\" for 5K+])",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "countries": {
      "description": "Country codes (e.g., [\"US\", \"CA\"])",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "dateOffset": {
      "anyOf": [
       {
        "enum": [
         "24H",
         "1D",
         "2D",
         "7D",
         "14D",
         "1M",
         "3M",
         "6M",
         "9M",
         "1Y"
        ],
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "description": "Only match jobs posted within this time window (e.g., \"7D\" for last 7 days). Default is \"7D\"."
     },
     "excludedCompanies": {
      "description": "Companies to exclude",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "excludedKeywords": {
      "description": "Keywords to exclude from job results",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "excludedTitles": {
      "description": "Job titles to exclude from results",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "expLevels": {
      "description": "Experience levels (e.g., [\"SE\" for Senior, \"MI\" for Mid-level, \"EN\" for Entry])",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "h1bSponsorship": {
      "description": "H1B sponsorship required",
      "type": "boolean"
     },
     "industries": {
      "description": "Industries (use get_industries to see valid values)",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "locations": {
      "description": "Locations to match. Use plain city names without state abbreviations (e.g., \"San Francisco\" not \"San Francisco, CA\"). For states, use the full state name (e.g., \"Texas\"). Use \"Remote\" to match remote jobs worldwide without country restrictions.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "relevancy": {
      "anyOf": [
       {
        "enum": [
         "HIGH",
         "MEDIUM"
        ],
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "description": "Search relevancy mode - HIGH for strict title/skills matching (recommended), MEDIUM for broader keyword-based results. Default is null (MEDIUM behavior)."
     },
     "remote": {
      "description": "When true, only return remote jobs. When false or omitted, return all jobs (both remote and non-remote).",
      "type": "boolean"
     },
     "skills": {
      "description": "Required skills",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "titles": {
      "description": "Job titles to match (required, max 6)",
      "items": {
       "type": "string"
      },
      "maxItems": 6,
      "type": "array"
     },
     "workArrangement": {
      "description": "Work arrangement filter (e.g., [\"Full Time\", \"Part Time\", \"Contract\", \"Internship\", \"Freelance\", \"Temporary\"]). Defaults to [\"Full Time\"] if not set.",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "titles"
    ],
    "type": "object"
   },
   "customizeResume": {
    "description": "Enable AI resume customization for applications (default: false)",
    "type": "boolean"
   },
   "dailyLimit": {
    "description": "Maximum jobs to auto-apply per day (default: 5, max: 100)",
    "type": "number"
   },
   "minMatchScore": {
    "description": "Minimum match score for auto-apply (0-1). Jobs below this score will not be auto-applied. Default is 0.70 (70%) when not explicitly set.",
    "type": "number"
   },
   "name": {
    "description": "A name for this job hunt (e.g., \"Senior Engineer roles in SF\")",
    "type": "string"
   }
  },
  "required": [
   "name",
   "config"
  ],
  "type": "object"
 },
 "name": "create_job_hunt"
}
