{
 "description": "Load a .NET assembly (DLL) for analysis. Must be called before other Unity tools.",
 "inputSchema": {
  "properties": {
   "assembly_path": {
    "description": "Full path to the .NET DLL file",
    "type": "string"
   },
   "managed_directory": {
    "description": "Optional: path to the Managed directory for dependency resolution",
    "type": "string"
   }
  },
  "required": [
   "assembly_path"
  ],
  "type": "object"
 },
 "name": "load_assembly"
}
