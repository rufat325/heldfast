{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Cancel a post"
 },
 "description": "Stop a post before it goes out. Destinations that have not started are cancelled; any already being published cannot be recalled and are returned in still_going. Safe to call again.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "post_id": {
    "description": "From posts_list.",
    "type": "string"
   }
  },
  "required": [
   "post_id"
  ],
  "type": "object"
 },
 "name": "posts_cancel",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "cancelled": {
    "description": "How many destinations were stopped before they started.",
    "type": "number"
   },
   "post_id": {
    "type": "string"
   },
   "still_going": {
    "description": "Destinations already in flight, which cannot be stopped.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "post_id",
   "cancelled",
   "still_going"
  ],
  "type": "object"
 },
 "title": "Cancel a post"
}
