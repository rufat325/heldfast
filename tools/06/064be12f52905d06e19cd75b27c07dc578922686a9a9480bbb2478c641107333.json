{
 "description": "Look up CPV (Common Procurement Vocabulary \u2014 EU procurement category) codes by Polish keyword. Use this before search_tenders to find the right CPV filter. Returns code, name, division, and historical tender count.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max results (1-30, default 10)",
    "maximum": 30,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Search term for CPV (Common Procurement Vocabulary) codes \u2014 keyword in Polish, e.g. 'komputer', 'budowa drogi', 'us\u0142ugi prawne'",
    "minLength": 2,
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_cpv",
 "title": "Search CPV codes by keyword"
}
