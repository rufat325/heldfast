{
 "description": "Check whether a password appears in the Have I Been Pwned breach corpus. The password never leaves the machine: only the first 5 characters of its SHA-1 are sent (k-anonymity), and the match is done locally. Network: needs api.pwnedpasswords.com.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "password": {
    "description": "The password to check. Only a 5-character hash prefix is transmitted.",
    "type": "string"
   }
  },
  "required": [
   "password"
  ],
  "type": "object"
 },
 "name": "check_password"
}
