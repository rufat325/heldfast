{
 "description": "Headlines for a ticker over the last N days, merged from the three.ws live feed (192 publishers) and its 660k-article historical archive. Each item carries source, timestamp, sentiment, and link. Archive queries have a free per-IP daily quota.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "days": {
    "description": "Lookback window in days (default 7)",
    "maximum": 90,
    "minimum": 1,
    "type": "integer"
   },
   "limit": {
    "description": "Max headlines (default 30)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "ticker": {
    "description": "Asset ticker, e.g. BTC, SOL, THREE",
    "maxLength": 12,
    "minLength": 2,
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "coin_news",
 "title": "Coin headlines (live + archive)"
}
