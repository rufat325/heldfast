{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Open a recorded run"
 },
 "description": "Open one recorded run: planned against actual timing per step, what ended each step, pauses. Shows where a plan drifts. Needs the user's Rhylthyme account.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "run_id": {
    "description": "Run UUID from list_runs.",
    "type": "string"
   },
   "token": {
    "description": "Access token from the login tool. Omit when the account is connected.",
    "type": "string"
   }
  },
  "required": [
   "run_id"
  ],
  "type": "object"
 },
 "name": "load_run",
 "title": "Open a recorded run"
}
