{
 "_meta": {
  "xdata.kr/credential": {
   "providers": [
    "hometax"
   ],
   "register_url": "https://console.xdata.kr/credentials?link=1&priority=hometax",
   "required": true,
   "windows_pc_needed": true
  }
 },
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "REQUIRES prior hometax__session__login call with same account_link_id. Search cash receipt sales history. from_date/to_date are required (YYYYMMDD or YYYY-MM-DD). Counts: totalCount (and the same value in pageInfoVO.totalCount) is the record count for this query and is never fewer than the rows in this response; collectionMeta says whether this response already holds all of them (complete) and how many came back (collectedCount). When complete is false, ask for the next page.",
 "inputSchema": {
  "properties": {
   "account_link_id": {
    "description": "Account link ID (UUID v4). Optional \u2014 the server auto-resolves from your API key when omitted. Provide explicitly only when you have multiple certificates for the same provider. Each institution has a separate ID (Hometax != NHIS != NPS != FourInsure).",
    "type": "string"
   },
   "fetch_all_pages": {
    "description": "true collects every page instead of one (default false). Each page is a separate NTS request, so it is slower.",
    "type": "boolean"
   },
   "from_date": {
    "description": "Start date YYYYMMDD or YYYY-MM-DD (e.g. 20260101). Required.",
    "type": "string"
   },
   "max_pages": {
    "description": "Safety cap when fetch_all_pages is true (default 100)",
    "type": "integer"
   },
   "page_num": {
    "description": "Page number (default 1)",
    "type": "integer"
   },
   "page_size": {
    "description": "Page size (default 10)",
    "type": "integer"
   },
   "to_date": {
    "description": "End date YYYYMMDD or YYYY-MM-DD (e.g. 20260131). Required.",
    "type": "string"
   }
  },
  "required": [
   "from_date",
   "to_date"
  ],
  "type": "object"
 },
 "name": "hometax__cash_receipt__sales__search_history",
 "securitySchemes": [
  {
   "scopes": [
    "mcp:tools:list",
    "mcp:tools:call"
   ],
   "type": "oauth2"
  }
 ],
 "title": "\ud604\uae08\uc601\uc218\uc99d \ub9e4\ucd9c \ub0b4\uc5ed \uc870\ud68c"
}
