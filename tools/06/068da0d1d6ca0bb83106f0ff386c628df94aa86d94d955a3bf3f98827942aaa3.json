{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Browse the topic hierarchy of Poland's official Local Data Bank (Bank Danych Lokalnych, GUS statistics office) \u2014 prices, demography, finance, environment, labour market, and more. Omit parent_id to list the 33 root topics, or pass a parent_id to list its children. Leaf subjects (has_variables=true) can be passed to gus-poland.variables. Data: bdl.stat.gov.pl (GUS BDL API v1), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "page": {
    "description": "Zero-based page number (default 0).",
    "minimum": 0,
    "type": "integer"
   },
   "page_size": {
    "description": "Results per page, 1-100 (default 20).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "parent_id": {
    "description": "BDL subject-tree node id to list children of (e.g. \"K15\" for PRICES, \"G186\" for a sub-topic). Omit to list the 33 root-level topic categories.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "gus-poland.reference.subjects",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Browse GUS Poland BDL Subject Tree"
}
