{
 "description": "Grant, revoke, or reset app permissions (camera, location, photos, contacts, microphone, etc.).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "Permission action",
    "enum": [
     "grant",
     "revoke",
     "reset"
    ],
    "type": "string"
   },
   "bundleId": {
    "description": "App bundle ID",
    "type": "string"
   },
   "deviceId": {
    "description": "Device (default: booted)",
    "type": "string"
   },
   "service": {
    "description": "Permission service: all, calendar, contacts-limited, contacts, location, location-always, photos-add, photos, media-library, microphone, motion, reminders, siri",
    "type": "string"
   }
  },
  "required": [
   "bundleId",
   "service",
   "action"
  ],
  "type": "object"
 },
 "name": "simulator_grant_permission"
}
