{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Update an existing function (code, timeout, memory, handler, trigger).",
 "inputSchema": {
  "properties": {
   "code": {
    "description": "Updated code",
    "type": "string"
   },
   "cronExpression": {
    "description": "For cron/scheduled: 5-field cron, UTC, minute granularity",
    "type": "string"
   },
   "eventSource": {
    "description": "For event/webhook: subscribed event source name",
    "type": "string"
   },
   "functionId": {
    "description": "Function ID",
    "type": "string"
   },
   "handler": {
    "description": "New handler",
    "type": "string"
   },
   "memory": {
    "description": "New memory",
    "type": "number"
   },
   "timeout": {
    "description": "New timeout",
    "type": "number"
   },
   "trigger": {
    "description": "New trigger type: http, manual, cron, scheduled, event, webhook",
    "type": "string"
   }
  },
  "required": [
   "functionId"
  ],
  "type": "object"
 },
 "name": "tau_update_function"
}
