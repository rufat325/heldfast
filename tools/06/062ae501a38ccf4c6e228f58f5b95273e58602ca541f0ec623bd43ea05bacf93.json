{
 "description": "Publish a file from a local path. Non-HTML files get an unguessable URL; pass page=true to wrap any file in a branded download page (optionally password-protected).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "page": {
    "description": "Wrap the file in a branded download page.",
    "type": "boolean"
   },
   "password": {
    "description": "Password-protect the download page (requires page=true).",
    "type": "string"
   },
   "path": {
    "description": "Absolute path to the local file to publish.",
    "type": "string"
   },
   "slug": {
    "type": "string"
   }
  },
  "required": [
   "path"
  ],
  "type": "object"
 },
 "name": "publish_file"
}
