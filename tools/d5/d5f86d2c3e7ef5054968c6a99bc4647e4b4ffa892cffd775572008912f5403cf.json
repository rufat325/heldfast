{
 "annotations": {
  "readOnlyHint": true,
  "title": "list_chute_sessions"
 },
 "description": "List chute-mode work sessions on the farm (chute-side capture runs: weigh days, preg checks, vaccinations). Optionally filter by status: PROPOSED (assistant-built, awaiting in-app confirmation), ACTIVE (resumable draft), or COMPLETED.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "skip": {
    "description": "Records to skip (pagination).",
    "minimum": 0,
    "type": "integer"
   },
   "status": {
    "description": "Filter by session status.",
    "enum": [
     "PROPOSED",
     "ACTIVE",
     "COMPLETED"
    ],
    "type": "string"
   },
   "take": {
    "description": "Max records to return.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_chute_sessions"
}
