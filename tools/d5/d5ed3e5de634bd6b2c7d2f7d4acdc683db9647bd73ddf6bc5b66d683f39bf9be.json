{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Record exactly one visible decision step. Full visible bull_researcher and bear_researcher calls MUST supply round=1, then round=2, then round=3; both sides of the prior round are required before advancing. Round 2 asks exactly three questions; Round 3 preserves its own questions and answers the opponent's questions with exact bindings. Packets are persisted by role+round: an identical replay is idempotent and conflicting content is rejected. portfolio_manager accepts no round and is rejected until all evidence, selected masters, both three-round sides, and the exact Q&A gate are complete. Every reader-facing field must use the run language.",
 "inputSchema": {
  "allOf": [
   {
    "if": {
     "properties": {
      "role": {
       "enum": [
        "bull_researcher",
        "bear_researcher"
       ]
      }
     },
     "required": [
      "role"
     ]
    },
    "then": {
     "required": [
      "round"
     ]
    }
   },
   {
    "if": {
     "properties": {
      "role": {
       "const": "portfolio_manager"
      }
     },
     "required": [
      "role"
     ]
    },
    "then": {
     "not": {
      "required": [
       "round"
      ]
     }
    }
   }
  ],
  "properties": {
   "packet": {
    "type": "object"
   },
   "role": {
    "enum": [
     "bull_researcher",
     "bear_researcher",
     "portfolio_manager"
    ],
    "type": "string"
   },
   "round": {
    "description": "Required for bull_researcher and bear_researcher. Full visible runs require 1, 2, and 3 in order. Omit for portfolio_manager.",
    "maximum": 3,
    "minimum": 1,
    "type": "integer"
   },
   "run_id": {
    "type": "string"
   },
   "thread_id": {
    "type": "string"
   },
   "thread_title": {
    "type": "string"
   }
  },
  "required": [
   "run_id",
   "role",
   "packet"
  ],
  "type": "object"
 },
 "name": "record_visible_decision",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 },
 "title": "Record a Debate or PM Decision"
}
