{
 "description": "Search for conference rooms by name, space, or location (text search)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "description": "Max results (default 10)"
   },
   "query": {
    "description": "Search text (e.g. 'boardroom', 'WeWork', '10 person')"
   },
   "skip": {
    "description": "Results to skip for pagination"
   }
  },
  "type": "object"
 },
 "name": "croissant_find_rooms"
}
