{
 "description": "Mute a show: it leaves the followed list and later fetches do not re-add it. follow_podcast reverses it. Pass a podcast_id from list_following. Does not consume credits.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "podcast_id": {
    "description": "Show id, from list_following or search_podcasts.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "podcast_id"
  ],
  "type": "object"
 },
 "name": "unfollow_podcast",
 "title": "Stop following a show"
}
