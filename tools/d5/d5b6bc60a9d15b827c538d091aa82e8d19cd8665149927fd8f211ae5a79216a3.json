{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Verify a ProofRail proof and optionally its bound x402 receipt and result digest. Signature validity does not prove source truth.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "proof": {
    "maxLength": 16000,
    "type": "string"
   },
   "receipt": {
    "type": "object"
   },
   "result_sha256": {
    "pattern": "^[a-f0-9]{64}$",
    "type": "string"
   }
  },
  "required": [
   "proof"
  ],
  "type": "object"
 },
 "name": "xguard_verify_receipt"
}
