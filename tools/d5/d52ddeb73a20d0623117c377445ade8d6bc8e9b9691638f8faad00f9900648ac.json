{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Most recent released version of a package.",
 "inputSchema": {
  "examples": [
   {
    "id": "Newtonsoft.Json"
   },
   {
    "id": "xunit",
    "prerelease": true
   }
  ],
  "properties": {
   "id": {
    "description": "Package id",
    "type": "string"
   },
   "prerelease": {
    "description": "Allow prerelease (default false)",
    "type": "boolean"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "latest_version",
 "outputSchema": {
  "description": "Latest version information for a package",
  "properties": {
   "id": {
    "description": "Package identifier",
    "type": "string"
   },
   "latest_version": {
    "description": "Latest version string, or null if not found",
    "type": [
     "string",
     "null"
    ]
   },
   "total_versions": {
    "description": "Total number of published versions",
    "type": "integer"
   }
  },
  "required": [
   "id",
   "latest_version",
   "total_versions"
  ],
  "type": "object"
 },
 "title": "Latest Version",
 "version": "v1.472da158"
}
