{
 "_meta": {
  "fastmcp": {
   "tags": []
  }
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Set the workspace logo in Brand DNA. Every render uses it.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "image_base64": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "The logo bytes as base64, if you have no URL."
   },
   "image_url": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Public https URL of the logo (PNG, JPG or WebP)."
   },
   "variant": {
    "default": "light",
    "description": "light = the mark used on light backgrounds (the usual one); dark = the one for dark backgrounds.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "upload_logo",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 }
}
