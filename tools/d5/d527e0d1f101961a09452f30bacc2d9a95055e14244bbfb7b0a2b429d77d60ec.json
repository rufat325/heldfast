{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Export Community Signup Responses CSV"
 },
 "description": "Export a community's signup answers as a CSV \u2014 one row per member, one column per question, plus Submitted At/Name/Email \u2014 and return a download link. Requires community_id; creator or admin. Read-only.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "community_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "community_id"
  ],
  "type": "object"
 },
 "name": "export_community_signup_responses_csv",
 "title": "Export Community Signup Responses CSV"
}
