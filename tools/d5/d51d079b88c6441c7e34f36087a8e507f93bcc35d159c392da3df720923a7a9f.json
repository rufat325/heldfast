{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "USE WHEN you are about to work with, pay or depend on a registered agent and want its published identity before committing. Returns the full Sato Agent Passport manifest (sato.agent.manifest/v1) for one REGISTERED agent by slug: identity (sato_agent_id), agent types, chains, model/framework, the stack of Sato Hub directory resources it runs on, links, payment/x402 endpoint metadata, and verification + liveness status. Use onchain_agent_search_agents to find slugs.\n\nPayment metadata is self-configured by the creator \u2014 published for interoperability, not as an endorsement. Returns an error if the slug is unknown or the agent is not listed. Read-only.\n\nThere is deliberately no example slug: the registry is small and its slugs are not guessable, so call onchain_agent_search_agents first and pass a slug it returned. ('my-trading-agent' was this tool's own placeholder and four separate clients sent it as if it were real.)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "response_format": {
    "default": "markdown",
    "description": "Output format: 'markdown' (human-readable, default) or 'json' (machine-readable).",
    "enum": [
     "markdown",
     "json"
    ],
    "type": "string"
   },
   "slug": {
    "description": "The registered agent's slug \u2014 <slug from onchain_agent_search_agents>. Call onchain_agent_search_agents first; the registry is small and slugs are not guessable.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "onchain_agent_get_agent_passport",
 "title": "Look up one registered agent"
}
