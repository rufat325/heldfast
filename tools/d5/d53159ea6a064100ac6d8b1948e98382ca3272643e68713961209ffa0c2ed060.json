{
 "description": "Refresh the TikTok access token for an account using its stored refresh token. Returns new access and refresh tokens. Requires client_key and refresh_token in the account config.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "account": {
    "description": "Account name to refresh. Use list_accounts to see available options. Defaults to the first configured account.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "refresh_token"
}
