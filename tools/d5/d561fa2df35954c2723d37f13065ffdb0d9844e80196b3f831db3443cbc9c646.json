{
 "description": "Create a Code Engine application from a container image",
 "inputSchema": {
  "properties": {
   "env_vars": {
    "description": "Key/value environment variables",
    "type": "object"
   },
   "image": {
    "description": "Container image reference",
    "type": "string"
   },
   "image_secret": {
    "description": "Registry secret name for pulling the image (e.g. icr-pull-secret)",
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "port": {
    "description": "Container port (default 8080)",
    "type": "number"
   },
   "project_id": {
    "type": "string"
   },
   "run_args": {
    "description": "Arguments passed to the container entrypoint (run_arguments). Required for supergateway: [\"--stdio\", \"npx -y <mcp-server>\", \"--outputTransport\", \"sse\"]",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "run_commands": {
    "description": "Override the container entrypoint (run_commands). Rarely needed \u2014 use run_args for passing flags.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "scale_cpu_limit": {
    "description": "CPU limit (e.g. 1, 0.5)",
    "type": "string"
   },
   "scale_max_instances": {
    "description": "Max instances (default 10)",
    "type": "number"
   },
   "scale_memory_limit": {
    "description": "Memory limit (e.g. 4G, 2G)",
    "type": "string"
   },
   "scale_min_instances": {
    "description": "Min instances (default 0)",
    "type": "number"
   }
  },
  "required": [
   "project_id",
   "name",
   "image"
  ],
  "type": "object"
 },
 "name": "ce_create_application"
}
