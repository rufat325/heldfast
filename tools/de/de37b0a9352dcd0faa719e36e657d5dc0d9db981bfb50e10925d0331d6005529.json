{
 "description": "Breadth-first traverse the code graph starting from one symbol to return its local neighborhood: what it calls, what calls it, what it imports, inheritance / implementation relationships. Answers \"how does this work?\" structurally \u2014 grep finds the symbol; get_symbol_neighbors tells you what it actually depends on.\n\nUnlike find_references (which only returns direct callers = depth-1 inbound), get_symbol_neighbors walks OUTBOUND by default and can go deeper (up to depth=5) and bidirectional (direction=\"both\"). Unlike explain_code_path (which finds the shortest single path between two given symbols), this tool explores the neighborhood around ONE symbol without a target. Filter by edge_types to focus on just imports, just calls, just inheritance, etc.\n\nPrerequisites: Tentra API auth + a symbol_id (from query_symbols) + snapshot_id. Read-only. Response: { symbolId, depth, neighbors: [{ id, kind, name, qualifiedName, filePath, fanIn, fanOut, isGodNode }], edges: [{ from, to, type }] }.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "depth": {
    "default": 2,
    "description": "BFS depth (default 2, max 5)",
    "maximum": 5,
    "minimum": 1,
    "type": "integer"
   },
   "direction": {
    "default": "outgoing",
    "description": "outgoing = who this calls; both = also who calls this",
    "enum": [
     "outgoing",
     "both"
    ],
    "type": "string"
   },
   "edge_types": {
    "description": "Comma-separated edge types to follow: call,import,inherit,implement,reference",
    "type": "string"
   },
   "snapshot_id": {
    "description": "Snapshot to query",
    "minLength": 1,
    "type": "string"
   },
   "symbol_id": {
    "description": "Symbol ID to start BFS from",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "symbol_id",
   "snapshot_id"
  ],
  "type": "object"
 },
 "name": "get_symbol_neighbors"
}
