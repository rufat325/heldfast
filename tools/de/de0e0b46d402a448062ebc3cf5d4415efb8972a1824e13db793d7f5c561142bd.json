{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Monte Carlo Simulation (single factor)"
 },
 "description": "Draw N samples from one parametric distribution (normal, lognormal, uniform, triangular, beta, or exponential) and summarize the resulting spread. Use to quantify uncertainty around a single random factor: an NPV under an uncertain growth rate, a latency tail, or a reserve estimate. Returns the mean, standard deviation, p5/p25/p50/p75/p95 percentiles, a histogram, and the iteration count; each call re-samples (non-deterministic) and is capped at 2000 iterations. For correlated multi-asset risk, use analyze_risk.",
 "inputSchema": {
  "properties": {
   "confidenceLevel": {
    "description": "Two-sided confidence level for the MCSE intervals (default 0.95).",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "distribution": {
    "description": "Distribution family to sample from.",
    "enum": [
     "normal",
     "lognormal",
     "uniform",
     "triangular",
     "beta",
     "exponential"
    ],
    "type": "string"
   },
   "params": {
    "description": "Distribution parameters. Required keys depend on distribution: normal/lognormal={mean,stddev}, uniform={min,max}, triangular={min,mode,max}, beta={alpha,beta}, exponential={lambda}.",
    "properties": {
     "alpha": {
      "type": "number"
     },
     "beta": {
      "type": "number"
     },
     "lambda": {
      "type": "number"
     },
     "max": {
      "type": "number"
     },
     "mean": {
      "type": "number"
     },
     "min": {
      "type": "number"
     },
     "mode": {
      "type": "number"
     },
     "stddev": {
      "type": "number"
     }
    },
    "type": "object"
   },
   "seed": {
    "description": "Optional integer seed for a reproducible run; recorded in the certificate so anyone can re-derive the same draws.",
    "type": "integer"
   },
   "simulations": {
    "description": "Number of samples (default: 1000, max: 2000 free).",
    "maximum": 2000,
    "minimum": 1,
    "type": "integer"
   },
   "targetHalfWidth": {
    "description": "Optional ABSOLUTE precision target for the mean's CI half-width; the certificate reports replicationAdequacy = (meanHalfWidth <= this).",
    "minimum": 0,
    "type": "number"
   }
  },
  "required": [
   "distribution",
   "params"
  ],
  "type": "object"
 },
 "name": "simulate_montecarlo",
 "outputSchema": {
  "properties": {
   "certificate": {
    "description": "Re-checkable precision certificate: MCSE of the mean (analytic + batch-means), bootstrap MCSE per percentile, replicationAdequacy vs targetHalfWidth, the resolved seed, and a sha256 contentHash binding inputs\u2192outputs. Re-run the seed for the certified n to reproduce the statistics.",
    "type": "object"
   },
   "executionTimeMs": {
    "type": "number"
   },
   "histogram": {
    "description": "Bucketed counts.",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "iterations": {
    "type": "integer"
   },
   "mean": {
    "type": "number"
   },
   "percentiles": {
    "properties": {
     "p25": {
      "type": "number"
     },
     "p5": {
      "type": "number"
     },
     "p50": {
      "type": "number"
     },
     "p75": {
      "type": "number"
     },
     "p95": {
      "type": "number"
     }
    },
    "type": "object"
   },
   "stdDev": {
    "type": "number"
   },
   "timedOut": {
    "type": "boolean"
   }
  },
  "required": [
   "mean",
   "stdDev",
   "percentiles",
   "iterations"
  ],
  "type": "object"
 }
}
