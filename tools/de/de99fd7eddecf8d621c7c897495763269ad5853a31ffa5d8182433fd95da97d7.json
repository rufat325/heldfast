{
 "description": "Proceed to checkout with the current cart. Returns order summary for confirmation.",
 "inputSchema": {
  "properties": {
   "confirm": {
    "description": "Must be true to actually perform this action. Omit or false returns a preview and does nothing.",
    "type": "boolean"
   },
   "fulfillmentType": {
    "description": "Delivery or pickup",
    "enum": [
     "delivery",
     "pickup"
    ],
    "type": "string"
   },
   "paymentMethod": {
    "description": "Payment method ID (saved card)",
    "type": "string"
   },
   "slotId": {
    "description": "Selected time slot ID",
    "type": "string"
   }
  },
  "required": [
   "fulfillmentType",
   "slotId"
  ],
  "type": "object"
 },
 "name": "kroger_checkout"
}
