{
 "annotations": {
  "readOnlyHint": true,
  "title": "Search contacts"
 },
 "description": "Search for contacts by name or email. The search text must not be empty. Returns matching contact records with name, email, phone, and domain.",
 "inputSchema": {
  "properties": {
   "query": {
    "description": "Search query \u2014 matches against name and email (must not be empty)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_contacts",
 "title": "Search contacts"
}
