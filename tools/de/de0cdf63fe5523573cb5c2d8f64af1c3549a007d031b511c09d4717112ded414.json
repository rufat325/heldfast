{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Starting eleven and substitutes for one match, per side, with each player's position, squad number and TheSportsDB player id. COVERAGE IS PER-COMPETITION: a match the source has not covered returns found:false with a reason, not an error.",
 "inputSchema": {
  "examples": [
   {
    "event_id": "2594568"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional TheSportsDB key. The free tier serves this tool; a supporter key raises the rate limit.",
    "type": "string"
   },
   "event_id": {
    "description": "TheSportsDB idEvent, e.g. \"2594568\".",
    "type": "string"
   }
  },
  "required": [
   "event_id"
  ],
  "type": "object"
 },
 "name": "event_lineup",
 "title": "Event Lineup",
 "version": "v1.451eabd2"
}
