{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Temporary forwarded phone number to call the courier of an active claim: phone (e.g. \"+79099999998\"), ext (extension, e.g. \"0163\") and ttl_seconds (how long the number stays operational). Errors: 400 invalid_point_phone; 404 \u2014 claim or courier not found; 409 inappropriate_status (the order is already completed).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "claim_id": {
    "description": "Claim id (claim_id, 32\u201364 characters) from the create_claim or search_claims response.",
    "minLength": 1,
    "type": "string"
   },
   "point_id": {
    "description": "Route point the contact person calls from (int64 point id).",
    "type": "integer"
   }
  },
  "required": [
   "claim_id"
  ],
  "type": "object"
 },
 "name": "get_courier_phone",
 "title": "Get courier phone"
}
