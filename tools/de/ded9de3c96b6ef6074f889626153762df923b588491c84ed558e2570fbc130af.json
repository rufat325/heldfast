{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Investor Event Transcript"
 },
 "description": "Get the speaker-labelled transcript of a specific investor event (earnings call, conference, investor day) by its event id \u2014 every speaker turn in order, attributed to the real person (executive or analyst) with their role when the resolution is trusted; unverified voices show as a role label (e.g. Operator) or a neutral speaker number. Get the event id from ListInvestorEvents. Use this for conferences and other non-earnings events, which have no fiscal quarter to key on.",
 "inputSchema": {
  "properties": {
   "eventId": {
    "description": "The investor event id (from ListInvestorEvents)",
    "format": "uuid",
    "type": "string"
   },
   "limit": {
    "default": 50,
    "description": "Maximum number of speaker turns to return (default 50, max 200; values outside 1-200 are clamped)",
    "type": "integer"
   },
   "offset": {
    "default": 0,
    "description": "Number of leading speaker turns to skip, for paging through events longer than the 200-turn cap (default 0)",
    "type": "integer"
   }
  },
  "required": [
   "eventId"
  ],
  "type": "object"
 },
 "name": "GetInvestorEventTranscript",
 "title": "Investor Event Transcript"
}
