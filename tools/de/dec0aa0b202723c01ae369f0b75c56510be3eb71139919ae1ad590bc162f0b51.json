{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "MCP handshake census snapshot"
 },
 "description": "GET the latest MCP handshake trust snapshot (https://councilof.ai/interop/mcp-trust/latest.json): counts of how many internet-facing MCP servers answer a correct initialize handshake, how many respond with an auth challenge, and how many are unreachable. Counts only by doctrine \u2014 host details withheld by design. A partial round or a cap change is disclosed in the snapshot. Measurement, never certification. Three states: VALID (fetched), UNREACHABLE (could not fetch), UNCHECKABLE (body unreadable).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {},
  "type": "object"
 },
 "name": "mcp_trust",
 "outputSchema": {
  "properties": {
   "as_of": {
    "type": [
     "string",
     "null"
    ]
   },
   "counts": {
    "type": [
     "object",
     "null"
    ]
   },
   "diff": {},
   "enumeration": {
    "type": [
     "object",
     "null"
    ]
   },
   "headline": {
    "type": [
     "string",
     "null"
    ]
   },
   "kind": {},
   "partial": {
    "description": "true whenever the enumeration did not complete \u2014 a cap-limited read is partial",
    "type": "boolean"
   },
   "partial_reason": {
    "type": [
     "string",
     "null"
    ]
   },
   "source": {
    "type": "string"
   },
   "state": {
    "description": "VALID or UNREACHABLE",
    "type": "string"
   }
  },
  "required": [
   "state"
  ],
  "type": "object"
 },
 "title": "MCP handshake census snapshot"
}
