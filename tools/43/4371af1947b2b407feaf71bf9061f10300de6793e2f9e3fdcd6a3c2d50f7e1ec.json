{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true
 },
 "description": "Set the value of a <select> (or input) and fire input/change events. For typing free text use browser_type.",
 "inputSchema": {
  "properties": {
   "selector": {
    "description": "CSS selector of the <select> or input.",
    "type": "string"
   },
   "value": {
    "description": "The option's value attribute (not its visible label) to select.",
    "type": "string"
   }
  },
  "required": [
   "selector",
   "value"
  ],
  "type": "object"
 },
 "name": "browser_select"
}
