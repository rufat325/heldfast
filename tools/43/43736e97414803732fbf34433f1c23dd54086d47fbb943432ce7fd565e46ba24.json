{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the euro area M3 monetary aggregate \u2014 outstanding amount at end of period, working-day and seasonally adjusted, in EUR. M3 is the broad money supply measure the ECB monitors as a reference value for monetary policy. Monthly series. Filter by last_n_observations or start_period/end_period. Data: ECB Statistical Data Warehouse (data-api.ecb.europa.eu), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "end_period": {
    "description": "End of date range: YYYY, YYYY-MM, or YYYY-MM-DD depending on series frequency. Ignored if last_n_observations is set.",
    "pattern": "^\\d{4}(-\\d{2}(-\\d{2})?)?$",
    "type": "string"
   },
   "last_n_observations": {
    "description": "Return only the most recent N observations (max 100). Takes precedence over start_period/end_period. Default 10 if no range given.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "start_period": {
    "description": "Start of date range: YYYY, YYYY-MM, or YYYY-MM-DD depending on series frequency. Ignored if last_n_observations is set.",
    "pattern": "^\\d{4}(-\\d{2}(-\\d{2})?)?$",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ecb-rates.macro.money_supply",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Euro Area M3 Money Supply"
}
