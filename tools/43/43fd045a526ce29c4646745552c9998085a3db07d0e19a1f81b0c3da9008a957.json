{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List the grant opportunities administered by a given California state department, agency, commission, or conservancy, matching the agency name as a forgiving case-insensitive substring so partial names like \"Water Resources\" or \"Recycling\" work. Returns the matched official agency names, a count breakdown by status, and the grant records with title, categories, eligible applicants, estimated funding, and deadline. Answers \"what grants does CalRecycle offer\", \"grant programs from the California Energy Commission\", \"Department of Parks and Recreation funding opportunities\".",
 "inputSchema": {
  "examples": [
   {
    "agency": "Fish and Wildlife",
    "limit": 10,
    "status": "active"
   }
  ],
  "properties": {
   "agency": {
    "description": "California state department / agency name or fragment, case-insensitive, e.g. \"Recycling\", \"Energy Commission\", \"Parks and Recreation\", \"Fish and Wildlife\".",
    "type": "string"
   },
   "limit": {
    "description": "Max records to return (default 25, max 100).",
    "type": [
     "number",
     "string"
    ]
   },
   "offset": {
    "description": "Number of matching records to skip, for pagination (default 0).",
    "type": [
     "number",
     "string"
    ]
   },
   "status": {
    "description": "Which opportunities to include (default \"all\"; use \"active\" for only what is open now).",
    "enum": [
     "active",
     "closed",
     "forecasted",
     "all"
    ],
    "type": "string"
   }
  },
  "required": [
   "agency"
  ],
  "type": "object"
 },
 "name": "ca_grants_by_agency",
 "title": "Ca Grants By Agency",
 "version": "v1.af7d88d2"
}
