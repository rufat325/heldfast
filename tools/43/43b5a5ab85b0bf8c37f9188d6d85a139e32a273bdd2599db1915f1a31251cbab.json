{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Parallel bulk fetch: named context keys, entities, and optional semantic results in one call.",
 "inputSchema": {
  "properties": {
   "entities": {
    "description": "Entity names to fetch",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "entriesLimit": {
    "description": "Max entries to include",
    "type": "number"
   },
   "keys": {
    "description": "Context keys to fetch",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "query": {
    "description": "Semantic query (with semantic=true)",
    "type": "string"
   },
   "semantic": {
    "description": "Include semantic search results",
    "type": "boolean"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   }
  },
  "required": [
   "spaceId"
  ],
  "type": "object"
 },
 "name": "memory_bootstrap_batch",
 "outputSchema": {
  "properties": {},
  "type": "object"
 }
}
