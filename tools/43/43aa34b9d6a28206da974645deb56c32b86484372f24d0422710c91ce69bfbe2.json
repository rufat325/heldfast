{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Return ALL events (server + browser) within +/- windowMs of a timestamp, time-ordered \u2014 the correlation tool. E.g. the browser console error and the backend stack trace from the same moment. Timestamps come from the `ts` field on any event.",
 "inputSchema": {
  "properties": {
   "app": {
    "description": "Optional: scope to one app/project (pane label or id; see pane_list).",
    "type": "string"
   },
   "source": {
    "description": "Optional: limit to one side.",
    "enum": [
     "server",
     "browser",
     "native"
    ],
    "type": "string"
   },
   "ts": {
    "description": "Center timestamp (ms since epoch).",
    "type": "number"
   },
   "windowMs": {
    "description": "Half-window in ms (default 500).",
    "type": "number"
   }
  },
  "required": [
   "ts"
  ],
  "type": "object"
 },
 "name": "get_logs_around"
}
