{
 "_meta": {
  "skill": "wallet"
 },
 "description": "Set how long the agent's wallet stays unlocked before automatically locking. Set to 0 to disable auto-lock.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "minutes": {
    "description": "Minutes until auto-lock (0 = never auto-lock)",
    "minimum": 0,
    "type": "number"
   }
  },
  "required": [
   "minutes"
  ],
  "type": "object"
 },
 "name": "wallet_set_timeout"
}
