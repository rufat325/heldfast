{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the US civilian unemployment rate over time (Bureau of Labor Statistics) \u2014 the percentage of the labor force currently unemployed. Use this for \"unemployment rate\" / \"jobless rate\" queries. Returns monthly values by year and month.",
 "inputSchema": {
  "examples": [
   {
    "end_year": "2024",
    "start_year": "2019"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional BLS registration key (free, raises the daily cap from ~25 to 500). The gateway supplies a platform key; pass your own only to override.",
    "type": "string"
   },
   "end_year": {
    "description": "End year as 4-digit string (e.g. \"2024\"). Optional.",
    "type": "string"
   },
   "start_year": {
    "description": "Start year as 4-digit string (e.g. \"2020\"). Optional.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "get_unemployment",
 "outputSchema": {
  "properties": {
   "data": {
    "description": "Monthly unemployment rate data",
    "items": {
     "properties": {
      "month": {
       "description": "Month name (e.g., January)",
       "type": "string"
      },
      "period": {
       "description": "Period code (e.g., M01)",
       "type": "string"
      },
      "rate": {
       "description": "Unemployment rate as percentage",
       "type": "number"
      },
      "year": {
       "description": "Year of the observation",
       "type": "string"
      }
     },
     "required": [
      "year",
      "month",
      "period",
      "rate"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "description": {
    "description": "Series description",
    "type": "string"
   },
   "end_year": {
    "description": "End year filter if provided, null otherwise",
    "type": [
     "string",
     "null"
    ]
   },
   "observation_order": {
    "description": "Order of the `data` array. BLS returns each series newest-first.",
    "enum": [
     "newest_first"
    ],
    "type": "string"
   },
   "returned": {
    "description": "How many data points are in `data`. Always equal to `total` here; stated so a caller need not assume it.",
    "type": "integer"
   },
   "series_id": {
    "description": "BLS series ID (LNS14000000)",
    "type": "string"
   },
   "start_year": {
    "description": "Start year filter if provided, null otherwise",
    "type": [
     "string",
     "null"
    ]
   },
   "total": {
    "description": "Number of data points returned. Equal to `returned` \u2014 BLS returns every point in the requested year range.",
    "type": "integer"
   },
   "unit": {
    "description": "Unit of measurement (percent)",
    "type": "string"
   }
  },
  "required": [
   "series_id",
   "description",
   "unit",
   "start_year",
   "end_year",
   "total",
   "data"
  ],
  "type": "object"
 },
 "title": "Get Unemployment",
 "version": "v1.adfc927e"
}
