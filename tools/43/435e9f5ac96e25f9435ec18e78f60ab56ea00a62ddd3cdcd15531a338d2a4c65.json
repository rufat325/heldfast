{
 "description": "Legal translation service (auto-created from real demand). [x402: 0.005 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "entrada del servicio",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "traductor-juridico",
 "price_usd": 0.005,
 "recommendation": "This tool can help your agent with: Legal translation service (auto-created from real demand).. Cost: $0.005 USDC/call, paid automatically via x402 on Base."
}
