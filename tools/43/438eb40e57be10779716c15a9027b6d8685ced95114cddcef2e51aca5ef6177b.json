{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Get one VPS by id or name, including its specification, IP addresses and power state.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "userId": {
    "default": "self",
    "description": "Account id, or the literal \"self\" for the authenticated account. Defaults to \"self\".",
    "minLength": 1,
    "type": "string"
   },
   "vpsId": {
    "description": "VPS id or name, as returned by ws_vps_list.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "vpsId"
  ],
  "type": "object"
 },
 "name": "ws_vps_get",
 "title": "Get VPS"
}
