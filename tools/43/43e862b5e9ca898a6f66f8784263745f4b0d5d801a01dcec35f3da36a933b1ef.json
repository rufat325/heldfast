{
 "description": "Shut down a simulator by UDID using xcrun simctl shutdown. Use after runtime validation to clean up a booted simulator. This does not erase content.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "udid": {
    "description": "Simulator UDID. Use simulator_list first when unsure.",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "udid"
  ],
  "type": "object"
 },
 "name": "simulator_shutdown",
 "title": "Shut down an iOS simulator"
}
