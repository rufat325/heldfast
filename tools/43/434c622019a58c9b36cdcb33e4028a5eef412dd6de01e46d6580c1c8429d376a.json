{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the bedrock geologic map unit(s) at a specific point location (surface geology): formation/unit name, dominant lithology, description, age range, and recommended map color, sourced from compiled state/national geologic maps. Data: macrostrat.org/api/v2 /geologic_units/map, no auth required, CC BY 4.0.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lat": {
    "description": "Decimal degree latitude (WGS84)",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "lng": {
    "description": "Decimal degree longitude (WGS84)",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "scale": {
    "description": "Geologic map source scale (default returns the best-available match)",
    "enum": [
     "small",
     "medium",
     "large"
    ],
    "type": "string"
   }
  },
  "required": [
   "lat",
   "lng"
  ],
  "type": "object"
 },
 "name": "macrostrat.geology.map_units",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Macrostrat Bedrock Geologic Map Units"
}
