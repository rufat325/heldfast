{
 "description": "Diffs the ARIA tree before and after a specific action. Returns added and removed elements so the agent sees exactly what changed without comparing two full DOM dumps.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "action_index": {
    "description": "Index of the action to diff (0-based, from get_action_timeline)",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "trace_path": {
    "description": "Absolute path to trace.zip, or a URL (https://) to download it from",
    "type": "string"
   }
  },
  "required": [
   "trace_path",
   "action_index"
  ],
  "type": "object"
 },
 "name": "get_dom_mutation_delta"
}
