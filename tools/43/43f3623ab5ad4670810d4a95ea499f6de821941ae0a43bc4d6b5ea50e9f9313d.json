{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Read one private Factory station"
 },
 "description": "Read one canonical station's private status and allowed operations for a Factory lifecycle. Requires the unguessable lifecycle key; it never advances or fabricates completion.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "lifecycle_id": {
    "minLength": 10,
    "type": "string"
   },
   "lifecycle_key": {
    "minLength": 32,
    "type": "string"
   },
   "station_id": {
    "description": "Canonical station_id returned by factory_stations",
    "maxLength": 64,
    "minLength": 2,
    "type": "string"
   }
  },
  "required": [
   "lifecycle_id",
   "lifecycle_key",
   "station_id"
  ],
  "type": "object"
 },
 "name": "factory_station"
}
