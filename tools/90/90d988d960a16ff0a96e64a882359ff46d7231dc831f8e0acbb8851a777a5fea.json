{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Look up a country by name, ISO alpha-2 code (2 letters), or ISO alpha-3 code (3 letters). Returns the full record: capital, region, official languages, currencies, calling codes, TLD, flag emoji, and IANA timezone IDs. Accepts fuzzy name matching \u2014 \"Brasil\" and \"Brazil\" both resolve.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "by": {
    "default": "auto",
    "description": "Lookup strategy: auto tries alpha2, alpha3, then name in order.",
    "enum": [
     "auto",
     "name",
     "alpha2",
     "alpha3"
    ],
    "type": "string"
   },
   "query": {
    "description": "Country name (e.g., \"Germany\"), ISO alpha-2 code (e.g., \"DE\"), or ISO alpha-3 code (e.g., \"DEU\").",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "ref_geo_lookup",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "anyOf": [
   {
    "not": {
     "required": [
      "error"
     ]
    },
    "required": [
     "alpha2",
     "alpha3",
     "name",
     "native_name",
     "capital",
     "region",
     "subregion",
     "languages",
     "currencies",
     "calling_codes",
     "tld",
     "flag",
     "timezones"
    ]
   },
   {
    "required": [
     "error"
    ]
   }
  ],
  "properties": {
   "alpha2": {
    "description": "ISO 3166-1 alpha-2 code (2 letters).",
    "type": "string"
   },
   "alpha3": {
    "description": "ISO 3166-1 alpha-3 code (3 letters).",
    "type": "string"
   },
   "calling_codes": {
    "description": "International dialing codes with leading + (e.g., \"+1\", \"+44\").",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "capital": {
    "description": "Capital city name, or null if not applicable (e.g., Antarctica).",
    "type": [
     "string",
     "null"
    ]
   },
   "currencies": {
    "description": "Currencies used in the country.",
    "items": {
     "additionalProperties": false,
     "description": "Currency used in the country.",
     "properties": {
      "code": {
       "description": "ISO 4217 currency code (e.g., \"USD\").",
       "type": "string"
      },
      "name": {
       "description": "Full currency name.",
       "type": "string"
      },
      "symbol": {
       "description": "Currency symbol (e.g., \"$\"), or null if unknown.",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "required": [
      "code",
      "name",
      "symbol"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "error": {
    "additionalProperties": {},
    "description": "Present when the call failed. Absent on success.",
    "properties": {
     "code": {
      "description": "JSON-RPC error code for this failure.",
      "maximum": 9007199254740991,
      "minimum": -9007199254740991,
      "type": "integer"
     },
     "data": {
      "additionalProperties": {},
      "properties": {
       "reason": {
        "description": "Machine-readable failure mode. Declared by this tool: `no_match`: No country matched the query. Other values are possible when a failure originates below the handler.",
        "examples": [
         "no_match"
        ],
        "type": "string"
       },
       "recovery": {
        "additionalProperties": {},
        "description": "Actionable next step for the caller.",
        "properties": {
         "hint": {
          "type": "string"
         }
        },
        "required": [
         "hint"
        ],
        "type": "object"
       },
       "retryable": {
        "description": "Whether retrying may succeed.",
        "type": "boolean"
       }
      },
      "type": "object"
     },
     "message": {
      "description": "Human-readable description of what went wrong.",
      "type": "string"
     }
    },
    "required": [
     "code",
     "message"
    ],
    "type": "object"
   },
   "flag": {
    "description": "Flag emoji (e.g., \ud83c\udde9\ud83c\uddea).",
    "type": "string"
   },
   "languages": {
    "description": "Official or widely spoken languages in the country.",
    "items": {
     "additionalProperties": false,
     "description": "Language spoken in the country.",
     "properties": {
      "code": {
       "description": "ISO 639-1 language code (e.g., \"en\").",
       "type": "string"
      },
      "name": {
       "description": "English name of the language.",
       "type": "string"
      }
     },
     "required": [
      "code",
      "name"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "name": {
    "description": "Official English country name.",
    "type": "string"
   },
   "native_name": {
    "description": "Country name in the primary native language.",
    "type": "string"
   },
   "notice": {
    "description": "Present only when the country was resolved via fuzzy name matching (starts-with or contains), not an exact code or name hit. Echoes the query and the resolved country so the caller can normalize follow-up lookups.",
    "type": "string"
   },
   "region": {
    "description": "Continent-level region (Africa, Americas, Asia, Europe, Oceania, Antarctic).",
    "type": "string"
   },
   "subregion": {
    "description": "Subregion within the continent (e.g., \"Western Europe\"), or null if unavailable.",
    "type": [
     "string",
     "null"
    ]
   },
   "timezones": {
    "description": "IANA timezone IDs observed in this country (e.g., \"America/New_York\").",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "tld": {
    "description": "Country-code top-level domain (e.g., \".de\"), or null if not applicable.",
    "type": [
     "string",
     "null"
    ]
   }
  },
  "type": "object"
 },
 "title": "Country Lookup"
}
