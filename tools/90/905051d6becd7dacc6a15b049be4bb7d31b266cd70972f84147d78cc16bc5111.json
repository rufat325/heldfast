{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Issuers whose most recent Form AP filing names a DIFFERENT audit firm than their prior filing, where the new filing landed in the last N days \u2014 the \"who switched auditors recently\" aggregation, which no upstream PCAOB page offers directly. Scans issuers with a Form AP filing in the window (capped at 1500 issuers per call) and compares each against its own filing history.",
 "inputSchema": {
  "examples": [
   {
    "days": 90,
    "limit": 25
   }
  ],
  "properties": {
   "days": {
    "description": "Lookback window in days for the most recent filing (default 90).",
    "type": "number"
   },
   "limit": {
    "description": "Max changes to return, 1-200 (default 50).",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "pcaob_auditor_changes",
 "title": "Pcaob Auditor Changes",
 "version": "v1.d20d6322"
}
