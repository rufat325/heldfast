{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List Join Requests"
 },
 "description": "List the pending requests to join a community you run \u2014 who asked and when. Approve/reject with approve_join_request / reject_join_request. Requires community_id; you must be its creator or admin. Read-only.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "community_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "community_id"
  ],
  "type": "object"
 },
 "name": "list_join_requests",
 "title": "List Join Requests"
}
