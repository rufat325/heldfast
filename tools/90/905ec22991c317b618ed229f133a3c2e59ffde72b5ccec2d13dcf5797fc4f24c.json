{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List support tickets filtered by status (e.g., \"open\", \"closed\") and priority (e.g., \"1\" for urgent). Returns ticket ID, subject, status, priority, and requester.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-freshdesk-api-key",
    "_domain": "mycompany",
    "filter": "new_and_my_open"
   },
   {
    "_apiKey": "your-freshdesk-api-key",
    "_domain": "mycompany",
    "order_by": "updated_at",
    "order_type": "desc",
    "page": 2,
    "per_page": 50
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Freshdesk API key",
    "type": "string"
   },
   "_domain": {
    "description": "Freshdesk subdomain (e.g., \"mycompany\" for mycompany.freshdesk.com)",
    "type": "string"
   },
   "filter": {
    "description": "Predefined filter: new_and_my_open, watching, spam, deleted (default: new_and_my_open)",
    "type": "string"
   },
   "order_by": {
    "description": "Sort by: created_at, due_by, updated_at, status (default: created_at)",
    "type": "string"
   },
   "order_type": {
    "description": "Sort order: asc or desc (default: desc)",
    "type": "string"
   },
   "page": {
    "description": "Page number for pagination (default 1)",
    "type": "number"
   },
   "per_page": {
    "description": "Results per page (default 30, max 100)",
    "type": "number"
   }
  },
  "required": [
   "_apiKey",
   "_domain"
  ],
  "type": "object"
 },
 "name": "freshdesk_list_tickets",
 "outputSchema": {
  "properties": {
   "tickets": {
    "description": "Array of ticket objects",
    "items": {
     "properties": {
      "created_at": {
       "description": "ISO timestamp",
       "type": "string"
      },
      "id": {
       "description": "Ticket ID",
       "type": "number"
      },
      "priority": {
       "description": "Ticket priority level",
       "type": "number"
      },
      "requester_id": {
       "description": "Requester contact ID",
       "type": "number"
      },
      "status": {
       "description": "Ticket status code",
       "type": "number"
      },
      "subject": {
       "description": "Ticket subject",
       "type": "string"
      },
      "updated_at": {
       "description": "ISO timestamp",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "title": "Freshdesk List Tickets",
 "version": "v1.82049430"
}
