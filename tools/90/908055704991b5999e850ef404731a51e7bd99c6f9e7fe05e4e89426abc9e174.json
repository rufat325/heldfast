{
 "annotations": {
  "idempotentHint": true,
  "title": "Update a catalog item"
 },
 "description": "Update a catalog entity: supply only the fields to change in `fields` (same field sets as create_catalog_item). Once a release is submitted or distributed, some release and track fields are locked \u2014 changing one returns a 403 with code RELEASE_LOCKED_FIELDS naming exactly which fields cannot change.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "entity": {
    "description": "The catalog entity kind.",
    "enum": [
     "label",
     "artist",
     "writer",
     "publisher",
     "release",
     "track"
    ],
    "type": "string"
   },
   "fields": {
    "additionalProperties": {},
    "description": "The fields to change, forwarded verbatim.",
    "type": "object"
   },
   "id": {
    "description": "The entity id.",
    "exclusiveMinimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "entity",
   "id",
   "fields"
  ],
  "type": "object"
 },
 "name": "update_catalog_item",
 "title": "Update a catalog item"
}
