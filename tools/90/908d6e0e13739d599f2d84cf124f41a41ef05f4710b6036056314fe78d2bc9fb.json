{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Get elements with testID or accessibilityLabel. Check traversal.complete before treating an empty elements array as definitive.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "maxDepth": {
    "default": 200,
    "description": "Maximum fiber depth to traverse (default 200, maximum 600)",
    "maximum": 600,
    "minimum": 0,
    "type": "integer"
   },
   "maxNodes": {
    "default": 1200,
    "description": "Maximum fibers to scan (default 1200)",
    "maximum": 5000,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "get_testable_elements"
}
