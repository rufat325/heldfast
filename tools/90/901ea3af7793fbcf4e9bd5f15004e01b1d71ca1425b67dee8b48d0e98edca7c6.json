{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Returns the balance of an ERC-20 token for an address on Monad. Also reads the token's symbol and decimals so the output is human-readable.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "Holder address or '.nad' name. Defaults to the authenticated user's wallet.",
    "minLength": 1,
    "type": "string"
   },
   "network": {
    "description": "Monad network \u2014 \"mainnet\" or \"testnet\". Defaults to the server's configured network.",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   },
   "token": {
    "description": "ERC-20 contract address.",
    "pattern": "^0x[a-fA-F0-9]{40}$",
    "type": "string"
   }
  },
  "required": [
   "token"
  ],
  "type": "object"
 },
 "name": "get_token_balance",
 "title": "Get ERC-20 token balance"
}
