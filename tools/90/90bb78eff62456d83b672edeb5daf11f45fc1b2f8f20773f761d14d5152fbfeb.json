{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "IEEE-754 representation of a decimal number in 32-bit (single) or 64-bit (double): sign, exponent, mantissa, raw bits, hex, and the value actually stored.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "number": {
    "type": "number"
   },
   "precision": {
    "default": 64,
    "enum": [
     32,
     64
    ],
    "type": "number"
   }
  },
  "required": [
   "number"
  ],
  "type": "object"
 },
 "name": "ieee754_from_decimal"
}
