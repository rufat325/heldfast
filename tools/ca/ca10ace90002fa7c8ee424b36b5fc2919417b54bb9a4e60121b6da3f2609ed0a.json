{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "readOnlyHint": false
 },
 "description": "Compile the draft into the live page, and report which revision went live (id, publishedAt, fromVersionId). PUBLISH CASCADES: a page sharing a global section with others republishes them too, because a header edited once must not go live on one page and stay stale on the rest. verify:true then fetches the live page and says whether the origin is serving that revision yet \u2014 the storefront caches for 60s, so a reload showing the old page is that, not a failed publish.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dry_run": {
    "type": "boolean"
   },
   "page_id": {
    "type": "string"
   },
   "publish_drafts": {
    "description": "Also publish every other page whose draft is ahead of its live copy (listed as unpublished_drafts)",
    "type": "boolean"
   },
   "site_id": {
    "type": "string"
   },
   "verify": {
    "description": "Fetch the live page afterwards and report whether it serves this revision",
    "type": "boolean"
   }
  },
  "required": [
   "page_id"
  ],
  "type": "object"
 },
 "name": "sb_publish"
}
