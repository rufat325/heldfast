{
 "annotations": {
  "readOnlyHint": false,
  "title": "Capture a verified learning to repo memory"
 },
 "description": "Capture a durable, reusable learning from the current session as a verified repo-local memory packet (committed under .agent_memory/, shared with the team via git). Use it the moment you discover something a future session should know: a decision and its rationale, a bug's root cause and fix, a convention, or a setup step. Prefer it over diff-based proposals when you already know what was learned. The write is rejected if every cited path is missing from the repo (set allow_missing_paths for a file you are about to create), and secrets/PII are scanned out before writing. Returns the new packet id plus any contradiction warnings against existing memory.",
 "inputSchema": {
  "properties": {
   "allow_low_quality": {
    "description": "Admit this capture even though its computed quality score is below the admission floor (60). The write is otherwise rejected \u2014 this is the explicit override.",
    "type": "boolean"
   },
   "allow_missing_paths": {
    "description": "Allow the write even if cited paths do not exist yet (e.g. a file you are about to create).",
    "type": "boolean"
   },
   "discovery_tokens": {
    "description": "Approximate token cost of producing this knowledge (exploration + reasoning). Stored on the packet so recall receipts can report replay value; a conservative per-type default is estimated when omitted.",
    "type": "number"
   },
   "evidence": {
    "description": "How the learning was confirmed (e.g. test output, a reproduced behavior).",
    "type": "string"
   },
   "graph_nodes": {
    "description": "Optional code-graph symbol or file ids this memory is grounded to.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "learning": {
    "description": "The insight to store, in full sentences: what was learned and why it matters to a future session.",
    "type": "string"
   },
   "paths": {
    "description": "Repo files this memory is about; used to verify the citation now and to recall the memory when those files are touched later.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "project_dir": {
    "description": "Absolute path to the repository root.",
    "type": "string"
   },
   "stack": {
    "description": "Optional technologies/frameworks the learning relates to.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "tags": {
    "description": "Optional keywords to aid future recall.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "title": {
    "description": "Short headline for the packet. Derived from the learning if omitted.",
    "type": "string"
   },
   "type": {
    "description": "Memory type: decision, bug_fix, runbook, convention, gotcha, workflow, code_explanation. Inferred if omitted.",
    "type": "string"
   },
   "verified_by": {
    "description": "What verified it (e.g. a command run, a passing test, a reviewer).",
    "type": "string"
   }
  },
  "required": [
   "project_dir",
   "learning"
  ],
  "type": "object"
 },
 "name": "kage_learn"
}
