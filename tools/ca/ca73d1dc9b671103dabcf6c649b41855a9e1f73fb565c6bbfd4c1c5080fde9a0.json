{
 "description": "Read the latest cached commit.show audit for a repo without forcing a re-run. Returns the JSON envelope (project + snapshot + scores). Quota state is in `.quota`.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "repo": {
    "description": "GitHub repo (URL, github.com/owner/repo, or owner/repo).",
    "type": "string"
   }
  },
  "required": [
   "repo"
  ],
  "type": "object"
 },
 "name": "project_status"
}
