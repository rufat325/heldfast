{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Set task summary"
 },
 "description": "Write or replace a task's summary: a concise digest of the task's current state and what has been done, shown in the task's Summary tab. Markdown supported. Replaces any previous summary (one per task).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "summary": {
    "description": "Summary text \u2014 a concise digest (markdown supported)",
    "type": "string"
   },
   "task_id": {
    "description": "Task UUID",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "task_id",
   "summary"
  ],
  "type": "object"
 },
 "name": "set_task_summary",
 "title": "Set task summary"
}
