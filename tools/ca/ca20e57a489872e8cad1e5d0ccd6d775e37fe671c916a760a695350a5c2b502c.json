{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Get the moon phase for a date (or each day of a date range if end_date is given): phase name (New Moon, Waxing Crescent, ...), illuminated fraction, age in days within the 29.53-day cycle, distance (km), apparent diameter (degrees), and waxing/waning.\n\nTide context: spring tides (largest range) occur just after new and full moons; neap tides after quarter moons. Computed locally \u2014 no NOAA data involved.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "date": {
    "description": "Date in YYYY-MM-DD format. Defaults to today.",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "end_date": {
    "$ref": "#/properties/date",
    "description": "Optional range end (YYYY-MM-DD): returns one entry per day from date through end_date."
   },
   "latitude": {
    "description": "Optional latitude for distance/diameter precision.",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "longitude": {
    "description": "Longitude in decimal degrees (-180 to 180).",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "response_format": {
    "default": "markdown",
    "description": "Output format: \"markdown\" for a readable summary table, \"json\" for the complete structured payload.",
    "enum": [
     "markdown",
     "json"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "astro_get_moon_phase",
 "title": "Get Moon Phase"
}
