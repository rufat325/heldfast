{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Create a new Cloudflare Queue. Requires confirm:true (human-approval gate); without it returns a preview only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "account_id": {
    "description": "Cloudflare account ID. Falls back to CLOUDFLARE_ACCOUNT_ID env var when omitted.",
    "type": "string"
   },
   "confirm": {
    "description": "Human-approval gate: must be true to actually perform this mutating operation. Omit or set false to get a non-executing preview of what would happen (with secrets redacted). A human should approve before this is set to true.",
    "type": "boolean"
   },
   "queue_name": {
    "description": "Name for the new queue.",
    "type": "string"
   }
  },
  "required": [
   "queue_name"
  ],
  "type": "object"
 },
 "name": "create_queue"
}
