{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-ai-user"
 },
 "description": "Get users from copilot\n\nTIP: Gets a specific AI user (Copilot-licensed user) by ID. Returns id and displayName; use $expand=interactionHistory or onlineMeetings to traverse to dependent resources.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "aiUserId": {
    "description": "Path parameter: aiUserId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "required": [
   "aiUserId"
  ],
  "type": "object"
 },
 "name": "get-ai-user"
}
