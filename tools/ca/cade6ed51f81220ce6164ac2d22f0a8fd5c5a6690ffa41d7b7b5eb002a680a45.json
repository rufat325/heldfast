{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Off-Exchange (Dark Pool) Volume"
 },
 "description": "Get weekly off-exchange (dark pool / OTC) trading volume for an exact stock or ETF listing from the FINRA OTC/ATS Transparency data. Each week shows ATS (alternative trading system / dark pool) volume and trade count, non-ATS OTC volume and trade count, and the total off-exchange volume (ATS + non-ATS OTC). The FINRA file does not include consolidated tape volume, so the off-exchange share of total market volume is not reported here; compute that share elsewhere against a consolidated-volume source. Weeks before 2025-08-11 may include volume from a case-variant sibling security because they predate the ordinal FINRA symbol-map fix and can no longer be re-imported from FINRA's rolling source window. FINRA publishes each week on a delay (2 weeks for Tier 1 NMS stocks, longer for other tiers), so the latest week lags today.",
 "inputSchema": {
  "properties": {
   "endDate": {
    "default": null,
    "description": "End date in YYYY-MM-DD format (defaults to latest available)",
    "type": "string"
   },
   "maxResults": {
    "default": 26,
    "description": "Maximum number of weeks to return \u2014 keeps the most recent N weeks in the range, displayed oldest to newest (default: 26, max: 500)",
    "type": "integer"
   },
   "startDate": {
    "default": null,
    "description": "Start date in YYYY-MM-DD format (defaults to 6 months ago)",
    "type": "string"
   },
   "ticker": {
    "description": "Listed security ticker (e.g., AAPL, VOO, GME)",
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "GetOffExchangeVolume",
 "title": "Off-Exchange (Dark Pool) Volume"
}
