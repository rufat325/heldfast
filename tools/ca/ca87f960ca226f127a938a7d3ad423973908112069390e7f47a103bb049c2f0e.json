{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List the Signal K data paths available on the boat, optionally filtered by prefix.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "cursor": {
    "description": "Opaque token from a previous response, to fetch the next page.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum paths to return. Omit to return all; with it, page using nextCursor.",
    "exclusiveMinimum": 0,
    "maximum": 1000,
    "type": "integer"
   },
   "prefix": {
    "description": "Only list paths starting with this prefix.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_available_paths",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "nextCursor": {
    "type": "string"
   },
   "paths": {
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "paths"
  ],
  "type": "object"
 },
 "title": "List available paths"
}
