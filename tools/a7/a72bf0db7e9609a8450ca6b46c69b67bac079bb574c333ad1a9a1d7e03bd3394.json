{
 "description": "Search secrets by name, type, or provider. Returns matching secrets with ref codes. Use when you need to find a specific ref code from the vault.",
 "inputSchema": {
  "properties": {
   "provider": {
    "description": "Filter by provider: openrouter, github, npm, stripe, etc.",
    "type": "string"
   },
   "query": {
    "description": "Search text \u2014 matches name, description, or ref code",
    "type": "string"
   },
   "type": {
    "description": "Filter by secret_type: api_key, password, token, ssh_key, cert, other",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "demipass_search"
}
