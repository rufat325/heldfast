{
 "description": "List Code Engine build runs (individual executions of a build)",
 "inputSchema": {
  "properties": {
   "project_id": {
    "type": "string"
   }
  },
  "required": [
   "project_id"
  ],
  "type": "object"
 },
 "name": "ce_list_build_runs"
}
