{
 "description": "Fuzzy entity search by name or type. Use to find entities without knowing exact names.",
 "inputSchema": {
  "properties": {
   "entity_type": {
    "description": "Filter by entity type",
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 10)",
    "type": "number"
   },
   "query": {
    "description": "Entity name to search for (fuzzy)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "velixar_graph_search"
}
