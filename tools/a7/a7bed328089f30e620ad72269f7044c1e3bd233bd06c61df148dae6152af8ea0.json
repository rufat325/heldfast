{
 "description": "Save important information to persistent memory. ALWAYS use this when you: make an architectural decision, discover a bug or gotcha, establish a convention, learn a user preference, identify a TODO, or gain important project context. These memories persist across sessions and help future you work faster. MANDATORY \u2014 call this tool immediately when any trigger fires. Do not batch or defer. When in doubt, store it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "category": {
    "description": "Category: decision (why we chose X), convention (always do Y), bug (gotcha/fix), architecture (how it's built), preference (user wants Z), todo (incomplete work), context (project facts)",
    "enum": [
     "decision",
     "convention",
     "bug",
     "architecture",
     "preference",
     "todo",
     "context",
     "session_summary"
    ],
    "type": "string"
   },
   "content": {
    "description": "What to remember \u2014 be specific and concise",
    "minLength": 1,
    "type": "string"
   },
   "importance": {
    "default": "medium",
    "description": "high = critical to remember, medium = useful context, low = nice to know",
    "enum": [
     "low",
     "medium",
     "high"
    ],
    "type": "string"
   },
   "status": {
    "description": "Status for todo items (default: pending). Only used when category is 'todo'.",
    "enum": [
     "pending",
     "in_progress",
     "done"
    ],
    "type": "string"
   },
   "tags": {
    "description": "Tags for organization (e.g., ['auth', 'api', 'performance'])",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "content",
   "category"
  ],
  "type": "object"
 },
 "name": "memory_store"
}
