{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Extract structured data from a web page using ScrapingAnt AI extraction. Describe the fields you want in `extract_properties` (comma-separated) and get back a JSON object with matching camelCase keys. Example: scrapingant_extract({ url: \"https://example.com/product\", extract_properties: \"product title, price(number), full description\", _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-scrapingant-api-key",
    "extract_properties": "product title, price(number), full description",
    "url": "https://example.com/product"
   },
   {
    "_apiKey": "your-scrapingant-api-key",
    "extract_properties": "business name, address, phone number, ratings(number), reviews(list: reviewer name, review text)",
    "proxy_type": "datacenter",
    "url": "https://example.com/listings"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Your ScrapingAnt API key. Sign up free at https://app.scrapingant.com/signup",
    "type": "string"
   },
   "extract_properties": {
    "description": "Comma-separated free-text description of the fields to extract. Optionally add type/list hints, e.g. \"product title, price(number), reviews(list: review title, review content)\".",
    "type": "string"
   },
   "proxy_country": {
    "description": "Two-letter ISO country code for the proxy exit location, e.g. \"US\".",
    "type": "string"
   },
   "proxy_type": {
    "description": "Proxy pool to use: \"datacenter\" (default) or \"residential\".",
    "type": "string"
   },
   "url": {
    "description": "The absolute URL of the page to extract data from.",
    "type": "string"
   }
  },
  "required": [
   "url",
   "extract_properties",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "scrapingant_extract",
 "title": "Scrapingant Extract",
 "version": "v1.34436428"
}
