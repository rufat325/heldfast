{
 "_meta": {
  "openai/outputTemplate": "ui://hovercode/qr-chatgpt.html?v=1",
  "openai/toolInvocation/invoked": "Page updated",
  "openai/toolInvocation/invoking": "Updating page"
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Update landing page"
 },
 "description": "Change an existing landing page without recreating it: edit its blocks, title, description, or colors, and publish/unpublish it. Use for 'fix the typo on my page', 'add a section', or 'take the page down'. If you pass `blocks`, provide the COMPLETE desired list (same {type, content} shape as build_page; include each block's `id` from get_page to keep it, omit to remove) \u2014 leaving `blocks` off keeps the existing blocks untouched. If the page is already published, edits are re-published so they go live; pass publish:false to take it offline.",
 "inputSchema": {
  "properties": {
   "bg_color": {
    "description": "Hex page background color.",
    "type": "string"
   },
   "blocks": {
    "description": "Complete desired block list (see note); each is {type, content} + optional id.",
    "items": {
     "properties": {
      "content": {
       "description": "The block's content for that type.",
       "type": "object"
      },
      "form": {
       "description": "For a 'form' block: the form id to embed.",
       "type": "string"
      },
      "hidden": {
       "description": "Hide this block from the live page.",
       "type": "boolean"
      },
      "id": {
       "description": "Existing block id to keep/update; omit if new.",
       "type": "string"
      },
      "type": {
       "description": "Block type (see list_block_types).",
       "type": "string"
      }
     },
     "required": [
      "type"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "description": {
    "description": "New SEO/description text.",
    "type": "string"
   },
   "id": {
    "description": "Page id.",
    "type": "string"
   },
   "publish": {
    "description": "true = publish/re-publish so edits go live; false = take offline (draft).",
    "type": "boolean"
   },
   "text_color": {
    "description": "Hex text color.",
    "type": "string"
   },
   "title": {
    "description": "New page title.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "update_page",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "blocks": {
    "items": {
     "additionalProperties": true,
     "properties": {
      "content": {
       "type": "object"
      },
      "form": {
       "description": "Linked form id for form/signup blocks.",
       "type": [
        "string",
        "null"
       ]
      },
      "hidden": {
       "type": "boolean"
      },
      "id": {
       "type": "string"
      },
      "type": {
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "design": {
    "type": "object"
   },
   "error": {
    "additionalProperties": true,
    "description": "Present only when the tool failed (the result is also flagged isError).",
    "properties": {
     "cause": {
      "description": "Machine-readable failure category: invalid_argument, not_found, forbidden, plan_limit, rate_limited, or server_error.",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "retryable": {
      "description": "Whether repeating the same call could succeed.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "id": {
    "type": "string"
   },
   "layout_version": {
    "type": "integer"
   },
   "publish": {
    "additionalProperties": true,
    "properties": {
     "has_unpublished_changes": {
      "type": "boolean"
     },
     "is_published": {
      "type": "boolean"
     },
     "published_at": {
      "type": [
       "string",
       "null"
      ]
     },
     "share_url": {
      "type": [
       "string",
       "null"
      ]
     },
     "status": {
      "type": "string"
     },
     "url_locked": {
      "type": "boolean"
     }
    },
    "type": "object"
   }
  },
  "type": "object"
 }
}
