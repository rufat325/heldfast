{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Set the value of a UI field"
 },
 "description": "Set text in an editable field via UI Automation (no key simulation). REQUIRES action tier + allowlist. Check can_set_value in ui_find; modern apps (e.g. Win11 Notepad) may not expose fields. Confirm with the user first.",
 "inputSchema": {
  "properties": {
   "element_index": {
    "description": "Stable index from ui_find",
    "type": "number"
   },
   "hwnd": {
    "description": "Exact window handle",
    "type": "number"
   },
   "name_contains": {
    "description": "Field name",
    "type": "string"
   },
   "title_contains": {
    "description": "Window title fragment",
    "type": "string"
   },
   "value": {
    "description": "Text to set",
    "type": "string"
   }
  },
  "required": [
   "value"
  ],
  "type": "object"
 },
 "name": "ui_set_value"
}
