{
 "description": "Estimate a GitHub Actions monthly bill using the January 2026 runner rates (Linux $0.006/min, Windows $0.010, macOS $0.062), accounting for plan allowances. Also reports what the same workload cost before the price cut.",
 "inputSchema": {
  "anyOf": [
   {
    "required": [
     "linuxMinutes"
    ]
   },
   {
    "required": [
     "windowsMinutes"
    ]
   },
   {
    "required": [
     "macosMinutes"
    ]
   }
  ],
  "properties": {
   "linuxMinutes": {
    "description": "Linux runner minutes per month",
    "type": "number"
   },
   "macosMinutes": {
    "description": "macOS runner minutes per month",
    "type": "number"
   },
   "plan": {
    "description": "GitHub plan: free, pro, team or enterprise (default free)",
    "type": "string"
   },
   "publicRepo": {
    "description": "Are these workflows in public repositories? (unlimited free)",
    "type": "boolean"
   },
   "windowsMinutes": {
    "description": "Windows runner minutes per month",
    "type": "number"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "calculate_github_actions_cost"
}
