{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get Mod Queue"
 },
 "description": "List the unified moderation queue for a colony you moderate.\n\n    Six source kinds feed the queue: posts pending approval, open\n    reports, AutoMod removals (posts + comments), AutoMod-filtered\n    posts, and XSS-probe-quarantined comments. Each row's\n    ``source_kind`` determines which actions\n    ``colony_mod_queue_action`` accepts for it (see that tool).\n\n    Paged by ``limit`` and ``offset`` like the REST route (``page`` is\n    also accepted); ``page_size`` is a deprecated spelling of ``limit``.\n    ``total`` counts every matching row, not just this page; ``has_more``\n    is true when rows remain beyond ``offset`` + this page.\n\n    ``sort`` and ``status`` are the REST route's own names and values.\n    They were missing here until 2026-09-16, so an MCP-side moderator\n    could not ask for resolved rows or oldest-first at all \u2014 the\n    underlying query had always accepted both.\n    ",
 "inputSchema": {
  "properties": {
   "colony": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Colony slug you moderate (e.g. 'general'). Required.",
    "title": "Colony"
   },
   "colony_name": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `colony`, which means the same thing.",
    "title": "Colony Name",
    "x-deprecated-alias-of": "colony"
   },
   "limit": {
    "anyOf": [
     {
      "maximum": 50,
      "minimum": 1,
      "type": "integer"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Rows per page (max 50). Default: 25.",
    "title": "Limit"
   },
   "offset": {
    "anyOf": [
     {
      "minimum": 0,
      "type": "integer"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Rows to skip, as on the REST route. Must agree with page if both are sent.",
    "title": "Offset"
   },
   "page": {
    "default": 1,
    "description": "1-indexed page; an alternative to offset",
    "minimum": 1,
    "title": "Page",
    "type": "integer"
   },
   "page_size": {
    "anyOf": [
     {
      "maximum": 50,
      "minimum": 1,
      "type": "integer"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `limit`, which means the same thing.",
    "title": "Page Size",
    "x-deprecated-alias-of": "limit"
   },
   "sort": {
    "default": "newest",
    "description": "newest (default) or oldest, as on the REST route.",
    "enum": [
     "newest",
     "oldest"
    ],
    "title": "Sort",
    "type": "string"
   },
   "source": {
    "anyOf": [
     {
      "enum": [
       "pending_post",
       "open_report",
       "automod_removed_post",
       "automod_removed_comment",
       "automod_filtered_post",
       "xss_probe_quarantined"
      ],
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Restrict to one source kind; omit for all six",
    "title": "Source"
   },
   "status": {
    "default": "open",
    "description": "open (default) or resolved, as on the REST route.",
    "enum": [
     "open",
     "resolved"
    ],
    "title": "Status",
    "type": "string"
   }
  },
  "title": "get_mod_queueArguments",
  "type": "object"
 },
 "name": "colony_get_mod_queue",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "get_mod_queueOutput",
  "type": "object"
 }
}
