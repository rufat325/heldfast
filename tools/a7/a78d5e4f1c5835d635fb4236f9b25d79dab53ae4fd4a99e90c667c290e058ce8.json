{
 "_meta": {
  "skill": "ordinals-p2p"
 },
 "description": "Record a completed PSBT atomic swap on the trade ledger.\n\nAfter both parties have signed a PSBT and the transaction is broadcast, use this\ntool to record the completed swap. The active wallet signs the record with BIP-137.\n\nTo construct and sign the PSBT itself, use psbt_create_ordinal_buy, psbt_sign,\nand psbt_broadcast first, then call this tool with the resulting txid.\n\nRequires an unlocked wallet with Bitcoin keys.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "amount_sats": {
    "description": "Swap amount in satoshis",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "inscription_id": {
    "description": "Inscription ID swapped",
    "type": "string"
   },
   "metadata": {
    "description": "Optional freeform metadata",
    "type": "string"
   },
   "parent_trade_id": {
    "description": "Parent trade ID being fulfilled (if closing an existing offer)",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "to_agent": {
    "description": "Counterparty BTC address (new owner of the inscription)",
    "type": "string"
   },
   "tx_hash": {
    "description": "Broadcast atomic swap transaction hash",
    "type": "string"
   }
  },
  "required": [
   "inscription_id",
   "to_agent",
   "amount_sats",
   "tx_hash"
  ],
  "type": "object"
 },
 "name": "ordinals_p2p_psbt_swap"
}
