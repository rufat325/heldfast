{
 "description": "Retrieve an API key from Vault via Keymaster. Returns the secret value for the given service and key name.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "key_name": {
    "default": "api_key",
    "description": "Key field name (default: 'api_key')",
    "type": "string"
   },
   "service": {
    "description": "Service name (e.g. 'openai', 'stripe', 'groq')",
    "type": "string"
   }
  },
  "required": [
   "service"
  ],
  "type": "object"
 },
 "name": "get_secret"
}
