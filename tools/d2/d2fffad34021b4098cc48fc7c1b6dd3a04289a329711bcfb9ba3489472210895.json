{
 "description": "Get the current trust score / detail for a previous LLM evaluation created via trustmodel_evaluate. Takes an integer evaluation_id and returns the evaluation detail including scores.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "evaluation_id": {
    "anyOf": [
     {
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     {
      "pattern": "^\\d+$",
      "type": "string"
     }
    ],
    "description": "Integer evaluation ID returned from a previous trustmodel_evaluate call."
   }
  },
  "required": [
   "evaluation_id"
  ],
  "type": "object"
 },
 "name": "trustmodel_score"
}
