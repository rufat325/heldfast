{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Daily interbank liquidity figures: overnight & 1-month HIBOR, aggregate balance (opening/closing), discount window base rate, the convertibility undertaking strong/weak-side rates (7.75/7.85), and the trade-weighted index (TWI). One record per business day, most recent first. Use from/to to window the dates.",
 "inputSchema": {
  "examples": [
   {}
  ],
  "properties": {
   "fields": {
    "description": "Comma-separated subset of columns to return, e.g. \"end_of_date,hibor_overnight\".",
    "type": "string"
   },
   "from": {
    "description": "Start of date window, YYYY-MM-DD (or YYYY-MM for monthly series), e.g. \"2026-01-01\".",
    "type": "string"
   },
   "lang": {
    "description": "Language of text fields: \"en\" (default), \"tc\", or \"sc\". Required by some datasets (e.g. coin-cart-schedule).",
    "type": "string"
   },
   "offset": {
    "description": "Zero-based record offset for pagination.",
    "type": "integer"
   },
   "pagesize": {
    "description": "Records per page (default ~100). Max varies by dataset.",
    "type": "integer"
   },
   "sortby": {
    "description": "Column to sort by, e.g. \"end_of_date\".",
    "type": "string"
   },
   "sortorder": {
    "description": "Sort direction: \"asc\" or \"desc\".",
    "type": "string"
   },
   "to": {
    "description": "End of date window, YYYY-MM-DD (or YYYY-MM for monthly series), e.g. \"2026-01-31\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "hkma_interbank_liquidity",
 "title": "Hkma Interbank Liquidity",
 "version": "v1.6bc4c762"
}
