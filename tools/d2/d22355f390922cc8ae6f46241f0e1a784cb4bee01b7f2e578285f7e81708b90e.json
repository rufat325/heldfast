{
 "_meta": {
  "dfx.example": {
   "arguments": {
    "limit": 10,
    "query": "Pritzker"
   },
   "goal": "find anything DFX knows under a name"
  }
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Search every DFX domain at once"
 },
 "description": "One search across family offices, independent sponsors and their capital providers, private companies, venture firms, private equity firms and funds, and real estate organisations: by name, or by filters (entity_type, domain, state, sector, ...). Compact cards with stable dfx ids and a per-domain coverage note. Breadth across every domain in one query, with fewer per-domain filters than the domain searches.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "active_only": {
    "type": "boolean"
   },
   "asset_class": {
    "type": "string"
   },
   "city": {
    "type": "string"
   },
   "cursor": {
    "type": "string"
   },
   "domain": {
    "description": "Comma-separated: real_estate, family_office, independent_sponsor, venture_capital, private_equity, ria, allocators, private_credit, real_estate_funds. Default: all.",
    "type": "string"
   },
   "entity_type": {
    "description": "family_office, sponsor, capital_provider, company, vc_firm, pe_firm, platform, fund, person, property, organization, ria_firm, advisor, private_fund, allocator, consultant, manager, credit_provider, bdc, borrower, vehicle.",
    "type": "string"
   },
   "has_real_estate": {
    "type": "boolean"
   },
   "has_sponsor_relationships": {
    "type": "boolean"
   },
   "invests_directly": {
    "type": "boolean"
   },
   "limit": {
    "default": 25,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "min_aum_usd": {
    "type": "number"
   },
   "min_opportunity": {
    "type": "integer"
   },
   "query": {
    "description": "A name (contains).",
    "type": "string"
   },
   "recent_activity_days": {
    "type": "integer"
   },
   "sector": {
    "type": "string"
   },
   "sort": {
    "type": "string"
   },
   "stage": {
    "type": "string"
   },
   "state": {
    "description": "Two-letter US state code.",
    "type": "string"
   },
   "vertical": {
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "search_entities",
 "title": "Search every DFX domain at once"
}
