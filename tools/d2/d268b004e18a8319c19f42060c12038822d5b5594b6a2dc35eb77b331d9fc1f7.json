{
 "description": "Get a compact inbox digest with subject, sender, date, flags and text preview for each message. More efficient than listing then reading individually.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "folder": {
    "description": "Folder (default: INBOX)",
    "type": "string"
   },
   "limit": {
    "description": "Max messages (default: 20, max: 50)",
    "type": "number"
   },
   "offset": {
    "description": "Skip messages (default: 0)",
    "type": "number"
   },
   "previewLength": {
    "description": "Preview text length (default: 200, max: 500)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "inbox_digest"
}
