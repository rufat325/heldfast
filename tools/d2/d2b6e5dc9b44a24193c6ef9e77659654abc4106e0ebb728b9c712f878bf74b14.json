{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Remove Affiliate Code"
 },
 "description": "Remove a ticket tier from an affiliate (disables their code if it was the last tier). Requires event_id + affiliate_id + ticket_class_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "affiliate_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "ticket_class_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "affiliate_id",
   "ticket_class_id"
  ],
  "type": "object"
 },
 "name": "remove_affiliate_code",
 "title": "Remove Affiliate Code"
}
