{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Publish a flow draft"
 },
 "description": "Publish a flow draft. Makes the draft the live definition (new version). Runs the full validation and keyword-conflict check (422 invalid_definition). Publishing a flow that was never live also activates it when the plan has a free live-flow slot (403 plan_limit_reached otherwise). Fires flow.published, and flow.activated when it went live. Send {} as the body.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Numeric id.",
    "type": "integer"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "publish_flow"
}
