{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Removes a node and everything inside it. One undo step, so the designer can take it back with a single Cmd-Z \u2014 but say what you are deleting before you do it.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "nodeId": {
    "description": "The node to delete.",
    "type": "string"
   }
  },
  "required": [
   "nodeId"
  ],
  "type": "object"
 },
 "name": "figma_delete_node",
 "title": "Delete a layer"
}
