{
 "description": "[WRITE] Create a new GTM template. Requires GTM_MCP_ENABLE_WRITES=true and confirm=true. Pass the full resource as a JSON string in bodyJson.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "description": "The GTM account ID.",
    "type": "string"
   },
   "bodyJson": {
    "description": "Full templates resource as a JSON string (e.g. {\"name\":\"...\",\"type\":\"...\"}).",
    "type": "string"
   },
   "confirm": {
    "description": "Must be true to confirm this write operation.",
    "type": "boolean"
   },
   "containerId": {
    "description": "The GTM container ID.",
    "type": "string"
   },
   "workspaceId": {
    "description": "The GTM workspace ID.",
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "containerId",
   "workspaceId",
   "bodyJson",
   "confirm"
  ],
  "type": "object"
 },
 "name": "templates_create"
}
