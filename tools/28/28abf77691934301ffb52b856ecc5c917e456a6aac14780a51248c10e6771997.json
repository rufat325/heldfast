{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List the curated software-development feeds (id, title, category, source). Optionally filter by category (dev) or keyword. Pass an id to read_feed.",
 "inputSchema": {
  "examples": [
   {
    "category": "dev"
   },
   {
    "query": "javascript"
   }
  ],
  "properties": {
   "category": {
    "description": "Filter by category: dev.",
    "type": "string"
   },
   "query": {
    "description": "Keyword to match in feed title/source/description.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_feeds",
 "title": "List Feeds",
 "version": "v1.d2edc41d"
}
