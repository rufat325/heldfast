{
 "description": "Compare clauses between two templates (optionally one named clause). Read-only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "a": {
    "description": "First template reference.",
    "type": "string"
   },
   "b": {
    "description": "Second template reference.",
    "type": "string"
   },
   "clause": {
    "description": "Optional clause name to compare.",
    "type": "string"
   }
  },
  "required": [
   "a",
   "b"
  ],
  "type": "object"
 },
 "name": "template_vault_compare_clauses"
}
