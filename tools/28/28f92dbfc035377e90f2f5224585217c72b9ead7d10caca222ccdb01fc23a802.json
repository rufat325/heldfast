{
 "description": "Check if a crypto token is a honeypot or has security risks. Returns honeypot status, risk score, buy/sell tax, contract verification, owner privileges, and trading restrictions. Use this before buying any token.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "chain": {
    "description": "Blockchain ID (ethereum, solana, bsc, base, arbitrum, polygon, optimism, avalanche, zksync, linea, scroll, mantle, etc.)",
    "type": "string"
   },
   "contract": {
    "description": "Token contract address (0x... for EVM chains, or Solana mint address)",
    "type": "string"
   }
  },
  "required": [
   "chain",
   "contract"
  ],
  "type": "object"
 },
 "name": "check_token_security"
}
