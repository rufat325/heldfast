{
 "description": "Extract structured biomarker values from raw lab report text (OCR output or copy-pasted text from a PDF). Returns normalized biomarker array ready for analysis. Costs 10 credits per page equivalent.",
 "inputSchema": {
  "properties": {
   "raw_text": {
    "description": "Raw text content from a lab report PDF. Can be copy-pasted text or OCR output. Include all test names, values, units, and reference ranges as they appear.",
    "maxLength": 50000,
    "type": "string"
   }
  },
  "required": [
   "raw_text"
  ],
  "type": "object"
 },
 "name": "extract_lab_from_pdf"
}
