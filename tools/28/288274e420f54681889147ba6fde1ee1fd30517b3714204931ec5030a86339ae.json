{
 "description": "Sign and submit an EVM transaction payload returned by printr_create_token. If no private_key is provided, the user will be prompted to select or provision a wallet. Returns the transaction hash and receipt once confirmed. After successful confirmation, present the trade page URL to the user: https://app.printr.money/trade/{token_id} using the token_id from the prior printr_create_token call.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "payload": {
    "properties": {
     "calldata": {
      "description": "Hex-encoded calldata",
      "type": "string"
     },
     "gas_limit": {
      "description": "Max gas",
      "type": "number"
     },
     "to": {
      "description": "Target contract (CAIP-10, e.g. 'eip155:8453:0x...')",
      "type": "string"
     },
     "value": {
      "description": "Native token value in wei (atomic units)",
      "type": "string"
     }
    },
    "required": [
     "to",
     "calldata",
     "value",
     "gas_limit"
    ],
    "type": "object"
   },
   "private_key": {
    "description": "Hex private key for the creator wallet (with or without 0x prefix). WARNING: handle with care \u2014 never share or commit this value. Falls back to EVM_WALLET_PRIVATE_KEY env var or interactive wallet selection.",
    "type": "string"
   },
   "rpc_url": {
    "description": "HTTP RPC endpoint for the target chain. Falls back to the chain's default public RPC if omitted.",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "payload"
  ],
  "type": "object"
 },
 "name": "printr_sign_and_submit_evm",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "block_number": {
    "description": "Block number (as string)",
    "type": "string"
   },
   "status": {
    "description": "Transaction status",
    "enum": [
     "success",
     "reverted"
    ],
    "type": "string"
   },
   "tx_hash": {
    "description": "Transaction hash",
    "type": "string"
   }
  },
  "required": [
   "tx_hash",
   "block_number",
   "status"
  ],
  "type": "object"
 }
}
