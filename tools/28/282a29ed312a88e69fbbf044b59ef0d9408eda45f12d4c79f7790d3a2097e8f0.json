{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": true,
  "title": "Check Audio Status"
 },
 "description": "Collect one of your finished audio takes. When generate_audio answers status \"rendering\" with a request_id (a longer line), call this with that request_id every 15 seconds until it returns status \"done\" with a playable audio_url, duration_seconds and generation_id. Also fetches any earlier take by its generation_id. Never re-run the line while it is rendering.",
 "inputSchema": {
  "properties": {
   "generation_id": {
    "description": "Or: the generation_id of a finished take (from generate_audio or list_my_audio).",
    "type": "string"
   },
   "request_id": {
    "description": "The request_id from a \"rendering\" generate_audio answer.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_audio_status",
 "title": "Check Audio Status"
}
