{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get UK electricity carbon intensity for every 30-minute period on a specific date (e.g., \"2024-01-15\"). Returns gCO2/kWh forecast and actual. Use to identify lowest-carbon hours.",
 "inputSchema": {
  "examples": [
   {
    "date": "2024-01-15"
   },
   {
    "date": "2024-03-20"
   }
  ],
  "properties": {
   "date": {
    "description": "Date in YYYY-MM-DD format (e.g., 2024-03-15)",
    "type": "string"
   }
  },
  "required": [
   "date"
  ],
  "type": "object"
 },
 "name": "get_intensity_by_date",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Total number of 30-minute periods returned for the date",
    "type": "number"
   },
   "date": {
    "description": "Date in YYYY-MM-DD format",
    "type": "string"
   },
   "periods": {
    "description": "Array of 30-minute intensity periods for the specified date",
    "items": {
     "properties": {
      "actual_gco2_per_kwh": {
       "description": "Actual carbon intensity in grams CO2 per kilowatt-hour, or null if not available",
       "type": [
        "number",
        "null"
       ]
      },
      "forecast_gco2_per_kwh": {
       "description": "Forecasted carbon intensity in grams CO2 per kilowatt-hour",
       "type": "number"
      },
      "from": {
       "description": "Start time of the 30-minute period (ISO 8601)",
       "type": "string"
      },
      "index": {
       "description": "Intensity index level",
       "type": "string"
      },
      "to": {
       "description": "End time of the 30-minute period (ISO 8601)",
       "type": "string"
      }
     },
     "required": [
      "from",
      "to",
      "forecast_gco2_per_kwh",
      "actual_gco2_per_kwh",
      "index"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "date",
   "periods",
   "count"
  ],
  "type": "object"
 },
 "title": "Get Intensity By Date",
 "version": "v1.e7064be1"
}
