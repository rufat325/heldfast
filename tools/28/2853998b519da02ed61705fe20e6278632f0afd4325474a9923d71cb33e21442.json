{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Delete a service by UUID. By default Coolify also deletes configurations, volumes, and connected networks and runs docker cleanup; pass the flags as false to keep them.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "delete_configurations": {
    "default": true,
    "type": "boolean"
   },
   "delete_connected_networks": {
    "default": true,
    "type": "boolean"
   },
   "delete_volumes": {
    "default": true,
    "type": "boolean"
   },
   "docker_cleanup": {
    "default": true,
    "type": "boolean"
   },
   "uuid": {
    "type": "string"
   }
  },
  "required": [
   "uuid"
  ],
  "type": "object"
 },
 "name": "deleteService",
 "title": "Delete service"
}
