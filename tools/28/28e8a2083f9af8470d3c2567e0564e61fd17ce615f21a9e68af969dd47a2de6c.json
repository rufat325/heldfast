{
 "description": "Check the status of a profiling session and discover which subsystems were recorded. Can be used to stop recording. When the data is ready provides information about the recorded subsystems (e.g. cpu, jdbc, http_client).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "stopRecording": {
    "description": "Optionally request the recording to be stopped now.",
    "type": "boolean"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "check_status"
}
