{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Create an FAQ"
 },
 "description": "Create an FAQ. Counts against the plan cap. The answer is embedded for retrieval right away; embedding_pending is true if indexing is still catching up.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "active": {
    "type": "boolean"
   },
   "answer": {
    "maxLength": 10000,
    "type": "string"
   },
   "category_id": {
    "type": [
     "integer",
     "null"
    ]
   },
   "keywords": {
    "description": "Comma-separated hints for matching.",
    "maxLength": 1000,
    "type": [
     "string",
     "null"
    ]
   },
   "question": {
    "maxLength": 1000,
    "type": "string"
   }
  },
  "required": [
   "question",
   "answer"
  ],
  "type": "object"
 },
 "name": "create_faq"
}
