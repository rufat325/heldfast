{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Fetch AstraNL record"
 },
 "description": "Fetch the full record for a single result id returned by search (robot passport, category detail, or merchant SKU). Read-only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Result id from search (e.g. robot:DJI-Matrice, sku:16759:Green-flor).",
    "maxLength": 120,
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "fetch"
}
