{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Install an app from the Impreza catalog (Vaultwarden, n8n, Nextcloud, etc.) on a target VPS. Use `impreza_list_apps` to discover available names. Pair with `impreza_list_servers` to find the right `agent_id`. Variables specific to the app (e.g. `signups_allowed` for Vaultwarden) go in `vars`. For non-catalog apps the customer built themselves, use `impreza_deploy_custom` instead.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "agent_id": {
    "description": "Target VPS agent_id.",
    "type": "string"
   },
   "app_name": {
    "description": "Catalog app name (e.g. \"vaultwarden\").",
    "type": "string"
   },
   "app_version": {
    "description": "Optional pinned version. Default: latest published.",
    "type": "string"
   },
   "domain": {
    "description": "Public hostname for clearnet TLS. Omit + set onion:true for onion-only.",
    "type": "string"
   },
   "onion": {
    "description": "Also publish a Tor v3 hidden service mirror.",
    "type": "boolean"
   },
   "onion_import": {
    "additionalProperties": false,
    "description": "BYO hidden-service key pair: BOTH C Tor key files from the hidden service's data directory, base64 each. Requires onion: true; keeps your existing .onion address. Deploy-time only.",
    "properties": {
     "public_key_b64": {
      "description": "Base64 of the hs_ed25519_public_key file (64 bytes).",
      "type": "string"
     },
     "secret_key_b64": {
      "description": "Base64 of the hs_ed25519_secret_key file (96 bytes).",
      "type": "string"
     }
    },
    "required": [
     "secret_key_b64",
     "public_key_b64"
    ],
    "type": "object"
   },
   "onion_profile": {
    "description": "Hardening tier of the hidden service (requires onion: true): standard = intro-point rate limiting; hardened = tighter limits + max streams; max = + experimental proof-of-work. Default standard.",
    "enum": [
     "standard",
     "hardened",
     "max"
    ],
    "type": "string"
   },
   "vars": {
    "description": "App-specific manifest variables (KEY \u2192 value).",
    "type": "object"
   }
  },
  "required": [
   "app_name",
   "agent_id"
  ],
  "type": "object"
 },
 "name": "impreza_deploy_catalog_app"
}
