{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find trials with sponsor-entered primary-completion or study-completion dates in a specified window. These registry dates may be estimated or revised and are planning signals\u2014not guaranteed data readouts, conference presentations, or regulatory events.",
 "inputSchema": {
  "examples": [
   {
    "event_type": "primary_completion",
    "from_date": "2026-08-01",
    "limit": 25,
    "phase": "PHASE3",
    "query": "obesity",
    "to_date": "2026-12-31"
   }
  ],
  "properties": {
   "event_type": {
    "enum": [
     "primary_completion",
     "study_completion"
    ],
    "type": "string"
   },
   "from_date": {
    "description": "YYYY-MM-DD, inclusive.",
    "type": "string"
   },
   "limit": {
    "type": "number"
   },
   "phase": {
    "type": "string"
   },
   "query": {
    "description": "Optional condition, intervention, or keyword.",
    "type": "string"
   },
   "sponsor": {
    "description": "Optional company name. Matched against the registered LEAD sponsor by default.",
    "type": "string"
   },
   "sponsor_match": {
    "description": "Which sponsor role the name must fill. \"lead\" (the default) returns only trials whose registered LEAD sponsor is that company. \"lead_or_collaborator\" also returns trials led by someone else that list the company as a collaborator \u2014 typically academic trials of the company's drug. Every returned study carries sponsor_match_field naming which one matched.",
    "enum": [
     "lead",
     "lead_or_collaborator"
    ],
    "type": "string"
   },
   "status": {
    "type": "string"
   },
   "to_date": {
    "description": "YYYY-MM-DD, inclusive.",
    "type": "string"
   }
  },
  "required": [
   "from_date",
   "to_date"
  ],
  "type": "object"
 },
 "name": "ct_catalyst_calendar",
 "title": "Ct Catalyst Calendar",
 "version": "v1.6c12fe41"
}
