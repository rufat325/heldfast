{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get metadata for a specific Latvia statistics table \u2014 title, dimensions (e.g. INDICATOR, TIME, ContentsCode), valid value codes for each dimension, and latest update timestamp. Use this BEFORE latvia-statistics.table_query to discover accepted filter values. Example path: 'POP/IR/IRS/IRS010' (population at the beginning of year, key vital statistics). Data: data.stat.gov.lv, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "table_path": {
    "description": "Full path to a leaf table (type='t' from latvia-statistics.catalog), e.g. 'POP/IR/IRS/IRS010' (population at the beginning of year, key vital statistics). Returns title, dimension codes, and valid values to use in latvia-statistics.table_query.",
    "type": "string"
   }
  },
  "required": [
   "table_path"
  ],
  "type": "object"
 },
 "name": "latvia-statistics.reference.table_metadata",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Latvia Statistics Table Metadata"
}
