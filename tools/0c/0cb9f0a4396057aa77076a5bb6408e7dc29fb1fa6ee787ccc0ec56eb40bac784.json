{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List executions with filters (status, workflow, project) and automatic pagination. Returns lightweight summaries with duration \u2014 use get_execution for full node-level data.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "maxItems": {
    "default": 100,
    "description": "Max executions to return",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "projectId": {
    "description": "Filter by project ID",
    "type": "string"
   },
   "status": {
    "description": "Filter by execution status (older n8n versions only accept error/success/waiting)",
    "enum": [
     "error",
     "success",
     "waiting",
     "canceled",
     "crashed",
     "new",
     "running"
    ],
    "type": "string"
   },
   "workflowId": {
    "description": "Filter by workflow ID",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_executions"
}
