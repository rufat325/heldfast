{
 "description": "Find documents (filter, projection, sort, limit). Read-only.",
 "inputSchema": {
  "properties": {
   "collection": {
    "type": "string"
   },
   "filter": {
    "type": "object"
   },
   "limit": {
    "default": 50,
    "type": "number"
   },
   "projection": {
    "type": "object"
   },
   "sort": {
    "type": "object"
   }
  },
  "required": [
   "collection"
  ],
  "type": "object"
 },
 "name": "mongo_find"
}
