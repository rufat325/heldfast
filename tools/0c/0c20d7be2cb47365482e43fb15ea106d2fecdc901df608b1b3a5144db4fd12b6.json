{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Call this when the question is about the rail rather than the service: which x402 facilitator actually settles money, and how much. Returns on-chain-verified settlement volume and transaction counts per facilitator for today (UTC), 7d, 30d and all-time, plus a verification flag ('on-chain' once volume has been observed on-chain, else 'listed'), across more than 30 facilitators. Optional daily timeseries (up to 90 days) and per-chain breakdown. Volume is decimal USD. Caveats: PER-FACILITATOR, never per-service; the *_24h fields cover today (UTC) so far, not a trailing 24 hours, and reset at 00:00 UTC, so prefer 7d.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "days": {
    "default": 30,
    "description": "Length of the timeseries in days (only used when include_timeseries is true).",
    "maximum": 90,
    "minimum": 1,
    "type": "integer"
   },
   "include_chains": {
    "default": false,
    "description": "Include a per-chain (network/asset) volume breakdown per facilitator.",
    "type": "boolean"
   },
   "include_timeseries": {
    "default": false,
    "description": "Include a daily volume_usd / tx_count series per facilitator.",
    "type": "boolean"
   },
   "page": {
    "default": 1,
    "minimum": 1,
    "type": "integer"
   },
   "per_page": {
    "default": 25,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "timeframe": {
    "default": "7d",
    "description": "Drives the sort order of the returned facilitators. '24h' sorts by today (UTC) so far, not by a trailing 24-hour window.",
    "enum": [
     "24h",
     "7d",
     "30d",
     "all"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "x402_facilitator_volumes",
 "title": "x402 facilitator volumes"
}
