{
 "description": "Analyze a published URL for AI search optimization. Performs comprehensive content quality analysis including AI slop detection, writing quality, E-E-A-T signals, and actionability.",
 "inputSchema": {
  "properties": {
   "output_format": {
    "default": "detailed",
    "description": "Output verbosity: \"detailed\" (default) includes all suggestions and recommendations; \"summary\" provides condensed results",
    "enum": [
     "detailed",
     "summary"
    ],
    "type": "string"
   },
   "query": {
    "description": "Optional context string describing the content topic (e.g., \"sim racing wheels\", \"content optimization\"). Used for relevance scoring only. Defaults to \"general content analysis\".",
    "type": "string"
   },
   "url": {
    "description": "The URL to analyze",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "analyze_url"
}
