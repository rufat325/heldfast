{
 "description": "Fetch Brussels municipality boundaries from the urban.brussels public WFS endpoint.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "count": {
    "default": 10,
    "description": "Maximum number of municipality features to return",
    "maximum": 500,
    "minimum": 1,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "urban_get_municipalities_boundaries"
}
