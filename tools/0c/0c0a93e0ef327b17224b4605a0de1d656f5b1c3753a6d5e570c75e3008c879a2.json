{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Individual violence events from the newest monthly UCDP GED Candidate release \u2014 the preliminary current-year event list, published monthly: date, place with coordinates, warring sides, type (state-based, non-state, one-sided), fatality best/low/high and the source headline. Answers \"what attacks happened in Nigeria last month\", \"recent conflict events in Myanmar with fatalities\". For yearly totals use ucdp_fatalities_summary; for events in earlier years use ucdp_events. Example: ucdp_recent_events({ country: \"Nigeria\", limit: 20 })",
 "inputSchema": {
  "examples": [
   {
    "country": "Nigeria",
    "limit": 3
   }
  ],
  "properties": {
   "country": {
    "description": "Country name or Gleditsch-Ward code; omit for all countries",
    "type": "string"
   },
   "limit": {
    "description": "Max events (default 50, max 500), newest first",
    "type": "integer"
   },
   "min_deaths": {
    "description": "Only events with at least this many deaths (best estimate)",
    "type": "integer"
   },
   "type_of_violence": {
    "description": "state-based | non-state | one-sided (or 1 | 2 | 3)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ucdp_recent_events",
 "title": "Ucdp Recent Events",
 "version": "v1.be913a7f"
}
