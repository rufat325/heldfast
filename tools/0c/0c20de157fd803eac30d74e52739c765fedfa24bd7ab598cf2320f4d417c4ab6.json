{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Batch exact-key lookup (max 50). Missing keys omitted. Respects DELX_MEMORY_NAMESPACE.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "keys": {
    "description": "Exact keys to fetch (max 50). Missing keys omitted.",
    "items": {
     "description": "Stable identifier for the memory entry. Free-form text. Treat as case-sensitive.",
     "maxLength": 512,
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 50,
    "minItems": 1,
    "type": "array"
   },
   "privacy_mode": {
    "default": "structured",
    "description": "summary = keys/meta without full values; structured/raw = full entries (local store parity).",
    "enum": [
     "summary",
     "structured",
     "raw"
    ],
    "type": "string"
   }
  },
  "required": [
   "keys"
  ],
  "type": "object"
 },
 "name": "memory_get_many"
}
