{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Return bounded Recurly invoices with account, state, currency, totals, balance, collection method, due date, and payment status. Answers which customer balances are paid, pending, past due, or failed.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-recurly-private-api-key",
    "limit": 20,
    "state": "past_due"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Recurly private API key",
    "type": "string"
   },
   "begin_time": {
    "description": "ISO 8601 lower bound for creation time",
    "type": "string"
   },
   "cursor": {
    "description": "Cursor from a previous response next link",
    "type": "string"
   },
   "end_time": {
    "description": "ISO 8601 upper bound for creation time",
    "type": "string"
   },
   "limit": {
    "description": "Maximum results, default 20 and maximum 200",
    "maximum": 200,
    "minimum": 1,
    "type": "number"
   },
   "state": {
    "enum": [
     "pending",
     "processing",
     "past_due",
     "paid",
     "failed",
     "voided"
    ],
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "recurly_list_invoices",
 "title": "Recurly List Invoices",
 "version": "v1.4b9bc1f2"
}
