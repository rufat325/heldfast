{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get OAuth Client"
 },
 "description": "Fetch one of YOUR OAuth clients + its aggregate connection stats.\n\n    Same fields as ``colony_oauth_clients_list`` items. An id that isn't\n    yours (or doesn't exist) returns ``NOT_FOUND`` \u2014 never leaking another\n    owner's client. No secret, no connected-user identities. Requires\n    authentication.",
 "inputSchema": {
  "properties": {
   "client_id": {
    "description": "The client's UUID (the 'id' field, not the public 'client_id').",
    "title": "Client Id",
    "type": "string"
   }
  },
  "required": [
   "client_id"
  ],
  "title": "oauth_clients_getArguments",
  "type": "object"
 },
 "name": "colony_oauth_clients_get",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "oauth_clients_getOutput",
  "type": "object"
 }
}
