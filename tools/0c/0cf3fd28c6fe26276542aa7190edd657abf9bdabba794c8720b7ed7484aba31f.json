{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get multi-day flood forecast for a location with current, mean, and peak river discharge projections. Returns discharge statistics and severity assessment. Use to evaluate flood risk and timeline.",
 "inputSchema": {
  "examples": [
   {
    "latitude": 40.7128,
    "longitude": -74.006
   },
   {
    "forecast_days": 30,
    "latitude": 35.6762,
    "longitude": 139.6503
   }
  ],
  "properties": {
   "forecast_days": {
    "description": "Number of forecast days to retrieve (1\u201392). Defaults to 16.",
    "type": "number"
   },
   "latitude": {
    "description": "Latitude of the location in decimal degrees.",
    "type": "number"
   },
   "longitude": {
    "description": "Longitude of the location in decimal degrees.",
    "type": "number"
   }
  },
  "required": [
   "latitude",
   "longitude"
  ],
  "type": "object"
 },
 "name": "get_flood_forecast",
 "outputSchema": {
  "properties": {
   "days": {
    "description": "Array of daily flood forecast data",
    "items": {
     "properties": {
      "date": {
       "description": "ISO date string",
       "type": "string"
      },
      "river_discharge": {
       "description": "Current river discharge in cubic meters per second",
       "type": [
        "number",
        "null"
       ]
      },
      "river_discharge_max": {
       "description": "Maximum river discharge in cubic meters per second",
       "type": [
        "number",
        "null"
       ]
      },
      "river_discharge_mean": {
       "description": "Mean river discharge in cubic meters per second",
       "type": [
        "number",
        "null"
       ]
      }
     },
     "required": [
      "date"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "latitude": {
    "description": "Latitude of the location in decimal degrees",
    "type": "number"
   },
   "longitude": {
    "description": "Longitude of the location in decimal degrees",
    "type": "number"
   },
   "timezone": {
    "description": "Timezone name for the location",
    "type": "string"
   },
   "units": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "Unit labels for each daily variable",
    "type": "object"
   }
  },
  "required": [
   "latitude",
   "longitude",
   "timezone",
   "units",
   "days"
  ],
  "type": "object"
 },
 "title": "Get Flood Forecast",
 "version": "v1.81d4d7ff"
}
