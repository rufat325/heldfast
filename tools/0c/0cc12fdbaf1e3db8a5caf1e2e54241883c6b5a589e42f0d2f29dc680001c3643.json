{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Create an IP/ASN/country access rule (block, challenge, or whitelist). Requires confirm:true (human-approval gate); without it returns a preview only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "confirm": {
    "description": "Human-approval gate: must be true to actually perform this mutating operation. Omit or set false to get a non-executing preview of what would happen (with secrets redacted). A human should approve before this is set to true.",
    "type": "boolean"
   },
   "mode": {
    "description": "Action to take for matching requests.",
    "enum": [
     "block",
     "challenge",
     "whitelist",
     "js_challenge",
     "managed_challenge"
    ],
    "type": "string"
   },
   "notes": {
    "description": "Optional note describing the rule.",
    "type": "string"
   },
   "target": {
    "description": "What the rule matches on.",
    "enum": [
     "ip",
     "ip_range",
     "asn",
     "country"
    ],
    "type": "string"
   },
   "value": {
    "description": "Match value, e.g. '1.2.3.4', '1.2.3.0/24', 'AS13335', or 'US'.",
    "type": "string"
   },
   "zone_id": {
    "description": "The Cloudflare zone ID (32-char hex). Get it from list_zones.",
    "type": "string"
   }
  },
  "required": [
   "zone_id",
   "mode",
   "target",
   "value"
  ],
  "type": "object"
 },
 "name": "create_access_rule"
}
