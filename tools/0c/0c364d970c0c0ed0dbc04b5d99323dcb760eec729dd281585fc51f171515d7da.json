{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Execute a QuickBooks SQL-like query (QBO query language) against any entity type \u2014 Customer, Invoice, Account, etc. Example: \"SELECT * FROM Customer MAXRESULTS 10\". Returns raw QuickBooks API response with matching records.",
 "inputSchema": {
  "examples": [
   {
    "query": "SELECT * FROM Customer MAXRESULTS 10"
   },
   {
    "query": "SELECT * FROM Invoice WHERE DocNumber = '1001' AND TxnDate >= '2024-01-01'"
   }
  ],
  "properties": {
   "query": {
    "description": "QuickBooks SQL-like query string (e.g., \"SELECT * FROM Customer MAXRESULTS 10\")",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "qb_query",
 "outputSchema": {
  "oneOf": [
   {
    "properties": {
     "error": {
      "description": "Error code if connection not configured",
      "type": "string"
     },
     "message": {
      "description": "Error message with setup instructions",
      "type": "string"
     }
    },
    "required": [
     "error",
     "message"
    ],
    "type": "object"
   },
   {
    "properties": {
     "QueryResponse": {
      "description": "Array of query results matching the QuickBooks query",
      "items": {
       "type": "object"
      },
      "type": "array"
     }
    },
    "type": "object"
   }
  ],
  "type": "object"
 },
 "title": "Qb Query",
 "version": "v1.61d1cc1e"
}
