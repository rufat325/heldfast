{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "\"LOINC code for [lab test]\" / \"standard code for blood test [X]\" / \"lab result identifier lookup\" / \"normalize lab name [Y]\" \u2014 search LOINC lab test codes (Logical Observation Identifiers Names and Codes, universal lab test identifiers). Returns codes like \"2093-3 Cholesterol [Mass/Vol] Ser/Plas\". Use when normalizing lab results across institutions.",
 "inputSchema": {
  "examples": [
   {
    "terms": "hemoglobin A1c"
   },
   {
    "count": 10,
    "terms": "cholesterol"
   }
  ],
  "properties": {
   "count": {
    "default": 7,
    "description": "Maximum matches to return. Default 7, max 500. Use 1\u20133 for typeahead UX, 20\u201350 for browsing.",
    "maximum": 500,
    "minimum": 1,
    "type": "number"
   },
   "df": {
    "description": "Comma-separated display fields to use in the `displays` array. Default varies per table; usually the canonical name.",
    "type": "string"
   },
   "ef": {
    "description": "Comma-separated extra fields to include per match. Field names vary per table; check NLM docs at clinicaltables.nlm.nih.gov.",
    "type": "string"
   },
   "terms": {
    "description": "Search query \u2014 prefix/contains match against canonical names. Whitespace-split into AND tokens. Example: \"cholesterol\".",
    "type": "string"
   }
  },
  "required": [
   "terms"
  ],
  "type": "object"
 },
 "name": "loinc",
 "title": "Loinc",
 "version": "v1.acbb5f46"
}
