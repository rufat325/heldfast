{
 "description": "Generate vector embeddings for one or more texts with Forge (Voxell's hosted embedding API). Use it to turn text into vectors for semantic search, RAG, clustering, or similarity. Set input_type='query' for search queries and 'document' for content you index. Choose model by quality/cost: turbo (1024d, fast, default) -> pro (2560d) -> ultra (4096d, #4 on MTEB English, top usable). Optionally set dim to truncate (Matryoshka, re-normalized).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dim": {
    "description": "Truncate to N dimensions (Matryoshka, re-normalized) \u2014 fewer dims = smaller, cheaper vectors. Omit for the model's native size.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "input": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "A text, or array of texts, to embed."
   },
   "input_type": {
    "description": "'query' applies a retrieval prefix; 'document' is raw. Default 'document'.",
    "enum": [
     "query",
     "document"
    ],
    "type": "string"
   },
   "model": {
    "description": "Model by quality/cost: turbo (1024d, fast, default), pro (2560d), ultra (4096d, #4 on MTEB English, top usable).",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "embed",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "count": {
    "type": "number"
   },
   "dim": {
    "type": "number"
   },
   "embeddings": {
    "items": {
     "items": {
      "type": "number"
     },
     "type": "array"
    },
    "type": "array"
   },
   "model": {
    "type": "string"
   },
   "tokens": {
    "type": "number"
   }
  },
  "required": [
   "model",
   "dim",
   "count",
   "tokens",
   "embeddings"
  ],
  "type": "object"
 },
 "title": "Embed text with Forge"
}
