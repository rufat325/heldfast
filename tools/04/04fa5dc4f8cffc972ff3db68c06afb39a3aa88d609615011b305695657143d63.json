{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Reschedule an appointment"
 },
 "description": "Reschedule an appointment. Moves a pending or confirmed appointment to a new date and time after checking the slot is free, updates the connected calendars, fires appointment.rescheduled, and DMs the customer when the booking came from a conversation. 409 slot_unavailable when the slot is taken, 409 not_active when the appointment is not pending or confirmed.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "date": {
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "id": {
    "description": "Numeric id.",
    "type": "integer"
   },
   "time": {
    "description": "HH:MM, 24-hour, in the appointment timezone.",
    "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
    "type": "string"
   }
  },
  "required": [
   "id",
   "date",
   "time"
  ],
  "type": "object"
 },
 "name": "reschedule_appointment"
}
