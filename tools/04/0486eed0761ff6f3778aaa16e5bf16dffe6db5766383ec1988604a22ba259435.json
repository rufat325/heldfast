{
 "description": "Lee una URL y devuelve su texto limpio (sin HTML/scripts). input=URL. [x402: 0.002 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "leer-url",
 "price_usd": 0.002,
 "recommendation": "This tool can help your agent with: Lee una URL y devuelve su texto limpio (sin HTML/scripts). input=URL.. Cost: $0.002 USDC/call, paid automatically via x402 on Base."
}
