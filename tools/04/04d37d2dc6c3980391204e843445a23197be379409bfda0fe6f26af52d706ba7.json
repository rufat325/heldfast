{
 "_meta": {
  "skill": "credentials"
 },
 "description": "Store or update a credential. Encrypts the value with AES-256-GCM and saves to ~/.aibtc/credentials.enc. If the service/key pair already exists, it is updated.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "key": {
    "description": "Key name within the service (e.g. 'api_key', 'token')",
    "minLength": 1,
    "type": "string"
   },
   "service": {
    "description": "Service name (e.g. 'github', 'openrouter')",
    "minLength": 1,
    "type": "string"
   },
   "value": {
    "description": "The credential value to store \u2014 WARNING: sensitive",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "service",
   "key",
   "value"
  ],
  "type": "object"
 },
 "name": "credentials_set"
}
