{
 "description": "Full-text search (BM25) across all Apple HIG reference topics. Ask in natural language \u2014 e.g. 'minimum touch target size', 'when to use a sheet vs a popover' \u2014 and get ranked topics with snippets. Follow up with hig_lookup using the returned skill/topic.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max results (default 8, cap 25).",
    "type": "number"
   },
   "query": {
    "description": "Natural-language or keyword query.",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "hig_search"
}
