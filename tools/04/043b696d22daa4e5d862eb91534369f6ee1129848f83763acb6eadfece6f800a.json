{
 "description": "List the active public share links for a file, with access mode, permission, and view count.",
 "inputSchema": {
  "properties": {
   "fileId": {
    "description": "UUID of the file",
    "type": "string"
   }
  },
  "required": [
   "fileId"
  ],
  "type": "object"
 },
 "name": "share_list"
}
