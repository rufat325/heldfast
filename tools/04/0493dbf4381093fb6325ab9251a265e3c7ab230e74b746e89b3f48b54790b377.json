{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Query any City of Montreal datastore resource (donnees.montreal.ca, CKAN) by its resource id (a UUID). Supports a free-text `q`, exact-match `filters` (field\u2192value), `sort` (\"field desc\"), limit and offset. Use montreal_datasets to find a resource id, or montreal_recent for the common ones.",
 "inputSchema": {
  "examples": [
   {
    "limit": 100,
    "resource_id": "2cfa0e06-9be4-49a6-b7f1-ee9f2363a872"
   },
   {
    "filters": {
     "district": "B2"
    },
    "resource_id": "2cfa0e06-9be4-49a6-b7f1-ee9f2363a872",
    "sort": "DDS_DATE_CREATION desc"
   }
  ],
  "properties": {
   "filters": {
    "description": "Exact-match filters as a JSON object, e.g. {\"district\":\"B2\"}.",
    "type": "object"
   },
   "limit": {
    "description": "Max rows (default 100, max 1000).",
    "type": "number"
   },
   "offset": {
    "description": "Row offset for paging.",
    "type": "number"
   },
   "q": {
    "description": "Free-text search across columns.",
    "type": "string"
   },
   "resource_id": {
    "description": "CKAN datastore resource id (UUID), e.g. \"2cfa0e06-9be4-49a6-b7f1-ee9f2363a872\".",
    "type": "string"
   },
   "sort": {
    "description": "Sort clause, e.g. \"DDS_DATE_CREATION desc\".",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "montreal_query",
 "title": "Montreal Query",
 "version": "v1.14a204cf"
}
