{
 "description": "Recent API requests for your key \u2014 requestId, status, credits, cacheHit (0 credits). Costs 0 credits. Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cursor": {
    "description": "Pagination cursor from nextCursor (createdAt of the last row). Leave empty for the first page.",
    "type": "string"
   },
   "endpoint": {
    "description": "Exact Captapi path filter, e.g. /v1/instagram/basic-profile.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows to return (default 50, max 500). Free \u2014 does not consume credits.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "since": {
    "description": "Inclusive lower bound on createdAt (ISO date or datetime).",
    "type": "string"
   },
   "statusCode": {
    "description": "Filter by HTTP status code (e.g. 500).",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "until": {
    "description": "Exclusive upper bound on createdAt (ISO date or datetime).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "account_request_history",
 "title": "Request History"
}
