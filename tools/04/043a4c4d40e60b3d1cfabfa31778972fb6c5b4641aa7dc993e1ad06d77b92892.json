{
 "description": "Return the installed Gretl GUI version by running gretl.exe --version.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "gretlGuiPath": {
    "description": "Optional explicit path to gretl or gretl.exe.",
    "type": "string"
   },
   "timeoutSeconds": {
    "description": "Maximum time to wait for gretl.exe --version. Defaults to 10 seconds.",
    "exclusiveMinimum": 0,
    "maximum": 60,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "gretl_gui_version"
}
