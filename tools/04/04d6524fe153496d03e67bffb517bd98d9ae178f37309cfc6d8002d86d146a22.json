{
 "description": "Return page count, first-page size, encryption status, and embedded metadata (title, author, dates, producer, etc.) for a local PDF.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "path": {
    "description": "Absolute path to a PDF on the local filesystem.",
    "type": "string"
   }
  },
  "required": [
   "path"
  ],
  "type": "object"
 },
 "name": "get_info",
 "title": "Get PDF info"
}
