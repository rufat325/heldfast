{
 "description": "Check whether HH_ACCESS_TOKEN is valid via /me and report the user role (applicant/employer). Use to diagnose resume-search access.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "raw": {
    "description": "Return the raw /me JSON instead of the summary.",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "validate_token"
}
