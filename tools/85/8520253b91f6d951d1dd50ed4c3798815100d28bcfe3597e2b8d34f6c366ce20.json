{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch and parse an RSS 2.0 or Atom 1.0 feed. Returns feed-level metadata (title, description, link, updated) plus a list of entries (title, link, id, published, updated, author, summary, content, contentType, categories). Auto-detects RSS vs Atom.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "allow_private_hosts": {
    "description": "Allow loopback / private / link-local targets for this call (default false). Refused unless the server operator launched fetch-mcp with FETCH_MCP_ALLOW_PRIVATE_HOSTS=1 -- SSRF protection stays on by default either way.",
    "type": "boolean"
   },
   "limit": {
    "description": "Max entries to return (default 50)",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "max_bytes": {
    "description": "Max bytes to read (default 10MiB)",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "max_redirects": {
    "maximum": 20,
    "minimum": 0,
    "type": "integer"
   },
   "timeout_ms": {
    "exclusiveMinimum": 0,
    "maximum": 60000,
    "type": "integer"
   },
   "url": {
    "format": "uri",
    "type": "string"
   },
   "user_agent": {
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "fetch_feed"
}
