{
 "description": "Reads the current content and version of one permanent Account Artifact. Pass the returned version to vnsh_artifact_update so another Agent cannot be overwritten.",
 "inputSchema": {
  "properties": {
   "artifact_id": {
    "description": "UUID returned by create or list",
    "type": "string"
   },
   "host": {
    "description": "Override the vnsh host URL",
    "type": "string"
   }
  },
  "required": [
   "artifact_id"
  ],
  "type": "object"
 },
 "name": "vnsh_artifact_read"
}
