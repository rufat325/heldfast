{
 "description": "Delete one of the user's saved designs (and the image copies it owns). Irreversible \u2014 confirm with the user first. Needs an API key with design:write.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "designId": {
    "type": "string"
   }
  },
  "required": [
   "designId"
  ],
  "type": "object"
 },
 "name": "delete_design"
}
