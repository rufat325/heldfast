{
 "description": "Get EUR exchange rates from the European Central Bank.\n\nReturns daily, monthly, or annual reference rates for a currency pair against EUR.\nThe ECB publishes reference rates for ~40 currencies at 16:00 CET each business day.\n\nExamples of questions this tool answers:\n- \"What is the current EUR/USD exchange rate?\"\n- \"Show me EUR/GBP rates for the last week\"\n- \"What was the EUR/JPY rate in January 2025?\"",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "endPeriod": {
    "description": "End date (YYYY-MM-DD, YYYY-MM, or YYYY)",
    "type": "string"
   },
   "frequency": {
    "description": "D=daily (default), M=monthly average, A=annual average",
    "enum": [
     "D",
     "M",
     "A"
    ],
    "type": "string"
   },
   "lastNObservations": {
    "description": "Return only the last N data points",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "startPeriod": {
    "description": "Start date (YYYY-MM-DD, YYYY-MM, or YYYY)",
    "type": "string"
   },
   "target": {
    "description": "Target currency code (e.g. \"USD\", \"GBP\", \"JPY\", \"CHF\"). Use \"+\" for multiple: \"USD+GBP\"",
    "type": "string"
   }
  },
  "required": [
   "target"
  ],
  "type": "object"
 },
 "name": "get_exchange_rates"
}
