{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Read Base contract state"
 },
 "description": "Make a read-only smart-contract call on Base with eth_call without sending a transaction. Costs $0.005 USDC via x402.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "block": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "minimum": 0,
      "type": "integer"
     }
    ],
    "description": "Base block selector: block number or tag such as latest, safe, finalized, pending or earliest."
   },
   "data": {
    "description": "0x-prefixed ABI calldata for a read-only eth_call.",
    "maxLength": 32770,
    "pattern": "^0x[0-9a-fA-F]*$",
    "type": "string"
   },
   "to": {
    "description": "Smart-contract address on Base mainnet.",
    "pattern": "^0x[0-9a-fA-F]{40}$",
    "type": "string"
   }
  },
  "required": [
   "to",
   "data"
  ],
  "type": "object"
 },
 "name": "base_eth_call"
}
