{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Analyze fishing effort intensity across a geographic polygon for a specified date range, aggregated by gear type (trawlers, longliners, purse seiners, squid jiggers, etc.). Returns a grid of 0.1-degree cells with fishing hours and vessel counts per month or year, drawn from Global Fishing Watch's public AIS-based fishing effort dataset covering 2012\u2013present. Useful for marine protected area monitoring, fisheries research, and vessel traffic analysis. Define the study area with a closed polygon in [longitude, latitude] coordinates.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "coordinates": {
    "description": "Polygon boundary as an array of [longitude, latitude] pairs defining the analysis area. The polygon must be closed (first and last coordinate identical). Example: [[-10,35],[-5,35],[-5,40],[-10,40],[-10,35]] for a box in the NE Atlantic. Omit to analyze global waters.",
    "items": {
     "items": [
      {
       "type": "number"
      },
      {
       "type": "number"
      }
     ],
     "maxItems": 2,
     "minItems": 2,
     "type": "array"
    },
    "minItems": 4,
    "type": "array"
   },
   "end_date": {
    "description": "End of the analysis period in ISO format YYYY-MM-DD (e.g. \"2023-06-30\"). Must be after start_date; keep ranges under 12 months for manageable response sizes.",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "start_date": {
    "description": "Start of the analysis period in ISO format YYYY-MM-DD (e.g. \"2023-01-01\"). GFW data coverage starts from 2012.",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "temporal_resolution": {
    "description": "Time granularity for effort aggregation. \"MONTHLY\" groups data by calendar month; \"YEARLY\" by year. Default: \"MONTHLY\".",
    "enum": [
     "MONTHLY",
     "YEARLY"
    ],
    "type": "string"
   }
  },
  "required": [
   "start_date",
   "end_date"
  ],
  "type": "object"
 },
 "name": "gfw.ocean.fishing_effort",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Analyze Fishing Effort"
}
