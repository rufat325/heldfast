{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Check whether a website is blocked or censored in a specific country, using OONI network measurements from volunteer probes. Returns counts of OK, anomalous, confirmed-blocked, and failed measurements over the window plus a blocking assessment \u2014 e.g. is bbc.com blocked in China, is twitter.com blocked in Iran. Great-Firewall / national-censorship / internet-freedom research.",
 "inputSchema": {
  "examples": [
   {
    "country": "IR",
    "domain": "twitter.com"
   },
   {
    "country": "CN",
    "days": 90,
    "domain": "www.bbc.com"
   }
  ],
  "properties": {
   "country": {
    "description": "2-letter country code, e.g. \"CN\", \"IR\", \"RU\".",
    "type": "string"
   },
   "days": {
    "description": "Lookback window in days (default 30, max 365).",
    "type": [
     "number",
     "string"
    ]
   },
   "domain": {
    "description": "Website domain, e.g. \"www.bbc.com\" or \"twitter.com\". A full URL is accepted (the host is extracted).",
    "type": "string"
   }
  },
  "required": [
   "domain",
   "country"
  ],
  "type": "object"
 },
 "name": "ooni_site_reachability",
 "title": "Ooni Site Reachability",
 "version": "v1.88970b1e"
}
