{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Attaches an employee to an SMI subject (for COMPANY-type subjects).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "endMonth": {
    "description": "When this employee's job ended, if it has. A month number from 1 (January) to 12 (December).",
    "maximum": 12,
    "minimum": 1,
    "type": "integer"
   },
   "endYear": {
    "description": "When this employee's job ended, if it has. A four-digit year, e.g. 2019.",
    "maximum": 9999,
    "minimum": 1,
    "type": "integer"
   },
   "firstName": {
    "description": "The employee's first name.",
    "minLength": 1,
    "type": "string"
   },
   "isCurrent": {
    "description": "Whether this person currently works for the subject company, as opposed to a former employee. Ask the user directly if it isn't clear from context.",
    "type": "boolean"
   },
   "lastName": {
    "description": "The employee's last name.",
    "minLength": 1,
    "type": "string"
   },
   "middleName": {
    "description": "The employee's middle name, if known.",
    "minLength": 1,
    "type": "string"
   },
   "position": {
    "description": "The employee's job title or role at the subject company.",
    "minLength": 1,
    "type": "string"
   },
   "startMonth": {
    "description": "When this employee's job started. A month number from 1 (January) to 12 (December).",
    "maximum": 12,
    "minimum": 1,
    "type": "integer"
   },
   "startYear": {
    "description": "When this employee's job started. A four-digit year, e.g. 2019.",
    "maximum": 9999,
    "minimum": 1,
    "type": "integer"
   },
   "subjectId": {
    "anyOf": [
     {
      "minLength": 1,
      "type": "string"
     },
     {
      "maximum": 9007199254740991,
      "minimum": -9007199254740991,
      "type": "integer"
     }
    ],
    "description": "The subject's id, from a prior smi_get_subject or subject-enrichment call."
   }
  },
  "required": [
   "subjectId",
   "firstName",
   "lastName",
   "isCurrent"
  ],
  "type": "object"
 },
 "name": "smi_add_subject_employee",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "recordId": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "number"
     }
    ]
   },
   "subjectId": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "number"
     }
    ]
   }
  },
  "required": [
   "subjectId"
  ],
  "type": "object"
 },
 "title": "Add Subject Employee"
}
