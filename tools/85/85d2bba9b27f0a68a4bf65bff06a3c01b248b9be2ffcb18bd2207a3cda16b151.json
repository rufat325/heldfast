{
 "description": "Each U.S. state's best-value four-year colleges at resident pricing, ranked by 30-year NPV (top 15 per state). Pass a state for its full list; omit it for a nationwide summary (every state's top pick).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "state": {
    "description": "Two-letter state code, e.g. 'CA'. Omit for all states' #1.",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "best_value_colleges",
 "title": "Best-value colleges by state"
}
