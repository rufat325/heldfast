{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get metadata for one country's administrative boundary at a given level: official name, admin unit count, area/perimeter stats, source, license, and download URLs for the full GeoJSON, simplified GeoJSON, TopoJSON, and a preview PNG. Levels: ADM0 (country outline), ADM1 (state/province), ADM2 (county/district), ADM3-ADM5 (finer, country-dependent). Geometry is NOT embedded in the response (files can exceed 10MB) \u2014 fetch the returned URL directly for the shapes. Data: geoBoundaries (William & Mary geoLab), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "adm_level": {
    "description": "Administrative boundary level: \"ADM0\" (country outline), \"ADM1\" (state/province/region), \"ADM2\" (county/district), \"ADM3\"-\"ADM5\" (finer subdivisions, only available for some countries). Defaults to \"ADM0\".",
    "enum": [
     "ADM0",
     "ADM1",
     "ADM2",
     "ADM3",
     "ADM4",
     "ADM5"
    ],
    "type": "string"
   },
   "country": {
    "description": "ISO 3166-1 alpha-3 country code, uppercase (e.g. \"USA\", \"KEN\", \"FRA\"). ISO2 codes and country names are not accepted by the upstream API.",
    "maxLength": 3,
    "minLength": 3,
    "type": "string"
   }
  },
  "required": [
   "country"
  ],
  "type": "object"
 },
 "name": "geoboundaries.boundary.detail",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Administrative Boundary Detail"
}
