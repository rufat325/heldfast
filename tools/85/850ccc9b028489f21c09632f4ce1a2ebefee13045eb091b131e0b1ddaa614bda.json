{
 "description": "Request a quote for using a service at a specific call volume. Returns expected monthly cost, throughput, and subscription URL.",
 "inputSchema": {
  "properties": {
   "email": {
    "description": "Optional contact email for follow-up.",
    "type": "string"
   },
   "expected_calls_per_month": {
    "type": "number"
   },
   "service_id": {
    "type": "string"
   }
  },
  "required": [
   "service_id",
   "expected_calls_per_month"
  ],
  "type": "object"
 },
 "name": "request_quote"
}
