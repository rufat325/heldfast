{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Install counts. Pass formula to scope, or omit for top list.",
 "inputSchema": {
  "examples": [
   {
    "formula": "node"
   },
   {
    "days": 90
   }
  ],
  "properties": {
   "days": {
    "description": "30 | 90 | 365 (default 30).",
    "type": "number"
   },
   "formula": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "analytics_install",
 "outputSchema": {
  "oneOf": [
   {
    "properties": {
     "data": {
      "description": "Install count data for the formula or null if not found"
     },
     "formula": {
      "description": "Formula name queried",
      "type": "string"
     }
    },
    "required": [
     "formula",
     "data"
    ],
    "type": "object"
   },
   {
    "description": "Top install counts across all formulae for the period",
    "type": "object"
   }
  ]
 },
 "title": "Analytics Install",
 "version": "v1.3f7db529"
}
