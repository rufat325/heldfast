{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "jsDelivr Data API \u2014 the file tree of one published npm package version (latest by default) as jsDelivr indexes it for CDN delivery. Returns the directory structure with each file's path, byte size, content hash and type, read from jsDelivr's package index. Answers which files, entry points and build outputs an npm package version actually ships.",
 "inputSchema": {
  "examples": [
   {
    "package_name": "lodash"
   },
   {
    "package_name": "next",
    "version": "14.0.0"
   },
   {
    "package_name": "@types/node",
    "version": "22.0.0"
   }
  ],
  "properties": {
   "package_name": {
    "type": "string"
   },
   "version": {
    "description": "Specific published version, e.g. \"4.17.21\". Omit this argument entirely to get the latest published version \u2014 do not pass the literal string \"latest\"; jsDelivr's API rejects that as an unresolvable version and returns 404.",
    "type": "string"
   }
  },
  "required": [
   "package_name"
  ],
  "type": "object"
 },
 "name": "list_npm_files",
 "outputSchema": {
  "description": "List of files in a published npm package version",
  "type": "object"
 },
 "title": "List Npm Files",
 "version": "v1.25511441"
}
