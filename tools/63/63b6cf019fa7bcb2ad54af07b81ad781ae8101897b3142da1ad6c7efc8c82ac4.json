{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "tier": "free",
  "title": "Popular Tags"
 },
 "description": "List the most-used tags on MemeStack by all-time usage count. (Time-period filtering is not yet supported - tag usage is currently counted across the full lifetime of each tag.)",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "How many tags to return (1-100, default 20)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "popular_tags",
 "outputSchema": {
  "additionalProperties": true,
  "description": "The most-used tags by image count.",
  "properties": {
   "tags": {
    "description": "Tags ordered by usage.",
    "items": {
     "additionalProperties": true,
     "description": "A tag with usage stats.",
     "properties": {
      "name": {
       "description": "Display name.",
       "type": "string"
      },
      "tag": {
       "description": "Tag slug.",
       "type": "string"
      },
      "usage_count": {
       "description": "Number of images using this tag.",
       "type": "number"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 }
}
