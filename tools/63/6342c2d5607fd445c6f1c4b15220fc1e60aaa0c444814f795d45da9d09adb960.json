{
 "description": "List recent jobs for the authenticated organisation, newest first. Cursor-paginated via starting_after. Useful for inspecting recent submissions when you don't have the id to hand.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max items per page (1-100, default 20).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "starting_after": {
    "description": "Cursor: pass the last job id from the previous page to fetch the next page.",
    "pattern": "^job_[0-9a-f]{32}$",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_jobs"
}
