{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List events from one OpenAgenda agenda (get its uid from search_agendas). Defaults to upcoming events. Optionally filter by keyword and date window. Returns title, dates, venue, coordinates and keywords (English where available, else French).",
 "inputSchema": {
  "examples": [
   {
    "agenda_uid": "6008621"
   },
   {
    "agenda_uid": "6008621",
    "from": "2024-01-01",
    "query": "concert",
    "size": 30,
    "to": "2024-12-31"
   }
  ],
  "properties": {
   "agenda_uid": {
    "description": "Agenda uid from search_agendas, e.g. \"6008621\".",
    "type": "string"
   },
   "from": {
    "description": "Earliest event date YYYY-MM-DD (default: today, upcoming only).",
    "type": "string"
   },
   "query": {
    "description": "Keyword filter over the agenda\u2019s events.",
    "type": "string"
   },
   "size": {
    "description": "Max events to return (1-50, default 20).",
    "type": "number"
   },
   "to": {
    "description": "Latest event date YYYY-MM-DD.",
    "type": "string"
   }
  },
  "required": [
   "agenda_uid"
  ],
  "type": "object"
 },
 "name": "events",
 "title": "Events",
 "version": "v1.d93db43b"
}
