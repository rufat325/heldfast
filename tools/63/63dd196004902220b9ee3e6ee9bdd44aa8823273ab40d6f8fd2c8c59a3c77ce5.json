{
 "annotations": {
  "readOnlyHint": true,
  "title": "get_record"
 },
 "description": "Get a single record by its UUID, including affected animals/groups.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "record_id": {
    "description": "The record UUID.",
    "type": "string"
   }
  },
  "required": [
   "record_id"
  ],
  "type": "object"
 },
 "name": "get_record"
}
