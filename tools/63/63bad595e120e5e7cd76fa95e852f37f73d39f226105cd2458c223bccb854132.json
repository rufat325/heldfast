{
 "description": "Generate a complete pricing table HTML component with multiple plan columns. Supports highlighting a recommended plan. Uses dark theme with inline CSS.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "plans": {
    "description": "Array of pricing plans to display",
    "items": {
     "properties": {
      "features": {
       "description": "List of features included in this plan",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "highlighted": {
       "description": "Whether this plan should be visually highlighted as recommended",
       "type": "boolean"
      },
      "name": {
       "description": "Plan name (e.g. Starter, Pro, Enterprise)",
       "type": "string"
      },
      "price": {
       "description": "Price string (e.g. $9/mo, $49/mo, Custom)",
       "type": "string"
      }
     },
     "required": [
      "name",
      "price",
      "features",
      "highlighted"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "plans"
  ],
  "type": "object"
 },
 "name": "generate_pricing_table"
}
