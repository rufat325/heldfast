{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "One Belgian annual-account deposit as filed (NBB CBSO Authentic Data) \u2014 Belgian company financial statements: structured JSON when the NBB publishes one, otherwise the official PDF returned base64-encoded. The filing date does not predict which of the two you get. Reference comes from list_belgian_company_filings. Deposits are immutable. Paid via x402 ($0.15 in USDC or EURC).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "api_key": {
    "description": "Optional Sirenic API key (srn_live_\u2026) to pay with prepaid credits instead of x402 \u2014 no wallet needed. Get one at https://api.sirenic.eu/compte. Ignored when x_payment is provided (the signed payment wins). On insufficient balance the tool returns a credits error, not an x402 quote.",
    "type": "string"
   },
   "id": {
    "description": "10-digit Belgian enterprise number (KBO/BCE)",
    "pattern": "^\\d{10}$",
    "type": "string"
   },
   "reference": {
    "description": "Deposit reference taken from the filings list, e.g. 2026-00123205",
    "pattern": "^\\d{4}-\\d{6,10}$",
    "type": "string"
   },
   "x_payment": {
    "description": "Optional signed x402 PAYMENT-SIGNATURE header value. Omit to receive the payment quote.",
    "type": "string"
   }
  },
  "required": [
   "id",
   "reference"
  ],
  "type": "object"
 },
 "name": "get_belgian_company_filing",
 "title": "Belgian annual accounts (NBB)"
}
