{
 "description": "Update metadata / descriptor / receipt details on an existing payment. Does not mutate amount or method. Use metadata to attach your system's order ids after the fact.",
 "inputSchema": {
  "properties": {
   "description": {
    "description": "Replace the payment description",
    "type": "string"
   },
   "id": {
    "description": "Rapyd payment id (payment_xxx)",
    "type": "string"
   },
   "metadata": {
    "description": "Replace the payment metadata (key-value)",
    "type": "object"
   },
   "receipt_email": {
    "description": "Email used for a refund/receipt notification",
    "type": "string"
   },
   "statement_descriptor": {
    "description": "Descriptor shown on the payer's statement where supported",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "update_payment"
}
