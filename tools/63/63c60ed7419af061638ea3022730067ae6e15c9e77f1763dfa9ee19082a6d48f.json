{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Uninstall a deployment (catalog or custom) by `deployment_id`. Set `purge_data: true` to also wipe the deployment's data volume. Idempotent \u2014 calling on an already-uninstalled deployment is a no-op success.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "deployment_id": {
    "description": "The dpl_... id to remove.",
    "type": "string"
   },
   "purge_data": {
    "description": "Wipe the data volume too. Default false.",
    "type": "boolean"
   }
  },
  "required": [
   "deployment_id"
  ],
  "type": "object"
 },
 "name": "impreza_uninstall_deployment"
}
