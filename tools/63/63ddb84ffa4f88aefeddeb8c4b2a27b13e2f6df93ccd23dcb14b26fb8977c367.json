{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "streamingHint": true,
  "title": "Perplexity Quick Ask"
 },
 "description": "Web search. Put your QUERY in the 'query' parameter.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "files": {
    "description": "File paths to read as code context. Supports line ranges: 'src/foo.ts:100-200'. Model sees ACTUAL CODE.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "query": {
    "description": "The search query or question (REQUIRED - put your question here)",
    "type": "string"
   },
   "searchDomain": {
    "description": "Search domain - must be one of: general, academic, news, social",
    "enum": [
     "general",
     "academic",
     "news",
     "social"
    ],
    "type": "string"
   },
   "searchRecency": {
    "description": "How recent the results should be - must be one of: hour, day, week, month, year",
    "enum": [
     "hour",
     "day",
     "week",
     "month",
     "year"
    ],
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "perplexity_ask"
}
