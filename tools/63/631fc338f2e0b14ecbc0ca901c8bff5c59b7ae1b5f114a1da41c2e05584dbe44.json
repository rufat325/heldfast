{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "End Deal"
 },
 "description": "End an ACTIVE deal prospectively \u2014 sold tickets keep their stamped terms, attached events are untouched, only NEW attachments stop. Terminal for the thread. Requires deal_id.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "deal_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "reason": {
    "maxLength": 1000,
    "type": "string"
   }
  },
  "required": [
   "deal_id"
  ],
  "type": "object"
 },
 "name": "end_deal",
 "title": "End Deal"
}
