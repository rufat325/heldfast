{
 "description": "Send raw bytes over a TLS socket to a target host. Bypasses HTTP library normalization \u2014 useful for smuggling, CRLF injection, malformed requests. Returns the first 4096 bytes of the raw response.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "raw_request": {
    "description": "The literal HTTP request bytes to send (use \\r\\n for CRLF)",
    "type": "string"
   },
   "target_host": {
    "description": "Target hostname, e.g. example.com",
    "type": "string"
   },
   "target_port": {
    "default": 443,
    "description": "Target port (default 443)",
    "type": "number"
   }
  },
  "required": [
   "target_host",
   "raw_request"
  ],
  "type": "object"
 },
 "name": "raw_http_send"
}
