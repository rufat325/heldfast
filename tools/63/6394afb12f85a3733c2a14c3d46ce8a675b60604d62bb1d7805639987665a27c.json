{
 "description": "Update the title or description of an existing phase without changing its status or position. Use this to reword or clarify a phase.",
 "inputSchema": {
  "properties": {
   "description": {
    "description": "New description for the phase (optional, omit to keep current).",
    "type": "string"
   },
   "phaseId": {
    "description": "Phase ID (from list_phases)",
    "type": "string"
   },
   "title": {
    "description": "New title for the phase (optional, omit to keep current).",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "phaseId"
  ],
  "type": "object"
 },
 "name": "edit_phase"
}
