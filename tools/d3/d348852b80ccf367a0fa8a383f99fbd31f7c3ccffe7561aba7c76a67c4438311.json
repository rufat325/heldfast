{
 "description": "Set the global Ableton Live tempo in BPM (20-999). Use for song-wide tempo changes before creating clips or scenes. Idempotent within 0.001 BPM and verified via read-after-write; returns before/after tempo and diff if verification disagrees.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bpm": {
    "description": "Tempo in BPM. Live's valid range: 20\u2013999.",
    "maximum": 999,
    "minimum": 20,
    "type": "number"
   }
  },
  "required": [
   "bpm"
  ],
  "type": "object"
 },
 "name": "set_tempo"
}
