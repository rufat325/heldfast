{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Finds layers by type, by name and by the words in them. Every filter is optional and they narrow together.\n\nThis is the cheap way to locate something. Walking with figma_get_tree and figma_get_children costs a round trip per level and returns every sibling; asking here for {types:[\"INSTANCE\"], name:\"button\"} costs one call. Searches the current page unless you pass nodeId to search inside one branch, or allPages to search the whole file \u2014 which is the only way to find something that is not on the page the designer happens to have open.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "allPages": {
    "description": "Search every page in the file. Slower, and the answer names the page each row is on.",
    "type": "boolean"
   },
   "limit": {
    "description": "Most rows to return. Default 50.",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "name": {
    "description": "Substring of the layer name, case-insensitive.",
    "type": "string"
   },
   "nodeId": {
    "description": "Search inside this branch instead of the whole page.",
    "type": "string"
   },
   "text": {
    "description": "Substring of a text layer\u2019s words, case-insensitive. Implies TEXT.",
    "type": "string"
   },
   "types": {
    "description": "Layer types to keep. Omit for any type.",
    "items": {
     "enum": [
      "FRAME",
      "GROUP",
      "SECTION",
      "COMPONENT",
      "COMPONENT_SET",
      "INSTANCE",
      "TEXT",
      "RECTANGLE",
      "ELLIPSE",
      "POLYGON",
      "STAR",
      "LINE",
      "VECTOR",
      "BOOLEAN_OPERATION"
     ],
     "type": "string"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "figma_find_nodes",
 "title": "Find layers"
}
