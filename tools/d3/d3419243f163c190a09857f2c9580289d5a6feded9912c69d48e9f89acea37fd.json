{
 "description": "Apply an optimization suggestion. Starts a background job and returns immediately; poll get_optimization until the suggestion shows as Applied.",
 "inputSchema": {
  "properties": {
   "id": {
    "type": "string"
   },
   "suggestion_id": {
    "description": "Suggestion ID copied verbatim from get_optimization (opaque string, not always a UUID \u2014 \"suggestion-1\" is valid).",
    "type": "string"
   }
  },
  "required": [
   "id",
   "suggestion_id"
  ],
  "type": "object"
 },
 "name": "apply_optimization"
}
