{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "OSF: search public preprints by title (Open Science Framework, by the Center for Open Science). Returns id, title, description, publication date, DOI, and tags. Keyless public data; the API can be slow.",
 "inputSchema": {
  "examples": [
   {
    "query": "machine learning"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max results (default 15, max 50).",
    "type": "number"
   },
   "query": {
    "description": "Title search text, e.g. \"machine learning\".",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "osf_search_preprints",
 "title": "Osf Search Preprints",
 "version": "v1.26ab25e9"
}
