{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Assign Role"
 },
 "description": "Assign a role (by role_id) to a production item \u2014 entity tasks/receipts/vendors/inventory/schedule_items (single) or projects (multiple). Stored as a name snapshot. op set/add/remove. Requires event_id + entity + item_id + role_id; host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "entity": {
    "enum": [
     "tasks",
     "projects",
     "receipts",
     "vendors",
     "inventory",
     "schedule_items"
    ],
    "type": "string"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "item_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "op": {
    "default": "set",
    "enum": [
     "set",
     "add",
     "remove"
    ],
    "type": "string"
   },
   "role_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "entity",
   "item_id",
   "role_id",
   "op"
  ],
  "type": "object"
 },
 "name": "assign_role",
 "title": "Assign Role"
}
