{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get recent attack/probe activity for a TCP/UDP port from SANS ISC \u2014 daily counts of report records, distinct target IPs, and distinct source IPs hitting the port. Useful for spotting scanning surges against services like SSH (22), RDP (3389), SMB (445), or Telnet (23). Keyless.",
 "inputSchema": {
  "examples": [
   {
    "port": 22
   },
   {
    "port": 3389
   }
  ],
  "properties": {
   "port": {
    "description": "A port number, e.g. 22 (SSH), 3389 (RDP), 445 (SMB), 23 (Telnet).",
    "type": "number"
   }
  },
  "required": [
   "port"
  ],
  "type": "object"
 },
 "name": "port_activity",
 "title": "Port Activity",
 "version": "v1.3ed66657"
}
