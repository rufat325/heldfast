{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Get one mailbox by id, including its address, quota and forwarding configuration.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "hostingId": {
    "description": "Hosting id or uuid the mailbox belongs to.",
    "minLength": 1,
    "type": "string"
   },
   "mailboxId": {
    "description": "Mailbox id, as returned by ws_mailbox_list.",
    "minLength": 1,
    "type": "string"
   },
   "userId": {
    "default": "self",
    "description": "Account id, or the literal \"self\" for the authenticated account. Defaults to \"self\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "hostingId",
   "mailboxId"
  ],
  "type": "object"
 },
 "name": "ws_mailbox_get",
 "title": "Get mailbox"
}
