{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "List the DNS zones on the account. For the records inside a zone use the v2 tool ws_dns_record_list \u2014 v1 record CRUD is deprecated and deliberately not exposed.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "page": {
    "description": "1-based page number.",
    "maximum": 9007199254740991,
    "minimum": 1,
    "type": "integer"
   },
   "pagesize": {
    "description": "Records per page.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "userId": {
    "default": "self",
    "description": "Account id, or the literal \"self\" for the authenticated account. Defaults to \"self\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ws_zone_list",
 "title": "List DNS zones"
}
