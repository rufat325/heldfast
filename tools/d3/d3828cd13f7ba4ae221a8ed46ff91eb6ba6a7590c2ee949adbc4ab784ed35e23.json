{
 "description": "Generate a concise natural language summary of token usage \u2014 like a daily/weekly briefing. Highlights key stats, top spenders, notable trends, and actionable insights in prose form. Use this when the user wants a quick narrative overview rather than tables.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "project": {
    "description": "Filter by project name/path substring",
    "type": "string"
   },
   "providers": {
    "description": "Filter to specific providers (e.g. ['claude-code','cursor'])",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "scope": {
    "description": "Time scope: today, week (last 7d), month (calendar month), or all (default)",
    "enum": [
     "today",
     "week",
     "month",
     "all"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "drishti_digest"
}
