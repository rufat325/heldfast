{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Create task"
 },
 "description": "Create a new task on a board. Requires the board slug and a title. Returns the created task with its generated id and number.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "assignee_ids": {
    "description": "Array of user UUIDs to assign",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "board_slug": {
    "description": "Board slug where the task will be created",
    "type": "string"
   },
   "column_id": {
    "description": "Kanban column ID (use list_board_columns to get IDs)",
    "type": "string"
   },
   "description": {
    "description": "Task description (markdown supported)",
    "type": "string"
   },
   "due_date": {
    "description": "Due date as ISO string (e.g. 2025-12-31)",
    "type": "string"
   },
   "parent_id": {
    "description": "Parent task UUID (for subtasks)",
    "type": "string"
   },
   "priority": {
    "description": "Priority, low to high: MINOR, ORDINARY (\"Low\"), MEDIUM, IMPORTANT (\"High\"), BLOCKER. A board may define custom priorities \u2014 read board.config.priorities from get_board.",
    "type": "string"
   },
   "stage_id": {
    "description": "Stage ID (use get_board to see stages)",
    "type": "string"
   },
   "status": {
    "description": "Task status. Default values: OPEN, IN_PROGRESS, IN_REVIEW, NEEDS_INFO, DONE (= \"Closed - done\"), CLOSED (= \"Closed - won't do\"). Use NEEDS_INFO when the task is blocked waiting for clarification. A board may define custom statuses \u2014 read board.config.statuses from get_board.",
    "type": "string"
   },
   "tag_ids": {
    "description": "Tag IDs from list_board_tags (must belong to the same board)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "tag_names": {
    "description": "Tag names for this task. Tags live on the board: a name that isn't there yet is created on it, an existing one is reused (case-insensitive). Max 10 per task.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "title": {
    "description": "Task title (required)",
    "type": "string"
   },
   "watcher_ids": {
    "description": "Array of user UUIDs to add as watchers",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "board_slug",
   "title"
  ],
  "type": "object"
 },
 "name": "create_task",
 "title": "Create task"
}
