{
 "description": "Flip a feature status. Updates feature.json and regenerates ROADMAP.md. Enforces a transition policy (use force: true to bypass). Appends an audit event (best-effort).",
 "inputSchema": {
  "properties": {
   "code": {
    "type": "string"
   },
   "commit_sha": {
    "description": "Optional commit binding",
    "type": "string"
   },
   "force": {
    "description": "Bypass the transition policy AND the roundtrip fixed-point guard (commits even if ROADMAP.md would not be a generation fixed point). Recorded in audit.",
    "type": "boolean"
   },
   "idempotency_key": {
    "type": "string"
   },
   "reason": {
    "description": "Free-form reason persisted in the audit event",
    "type": "string"
   },
   "status": {
    "enum": [
     "PLANNED",
     "IN_PROGRESS",
     "PARTIAL",
     "COMPLETE",
     "BLOCKED",
     "KILLED",
     "PARKED",
     "SUPERSEDED"
    ],
    "type": "string"
   }
  },
  "required": [
   "code",
   "status"
  ],
  "type": "object"
 },
 "name": "set_feature_status"
}
