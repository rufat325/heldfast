{
 "description": "Compress a prompt into dense language and return the version with fewer tokens (typical 20-40% saving). Use it BEFORE sending prompts to any LLM to cut token cost without losing meaning. input=text to compress. [x402: 0.001 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "Texto/prompt a comprimir",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "compress",
 "price_usd": 0.001,
 "recommendation": "This tool can help your agent with: Compress a prompt into dense language and return the version with fewer tokens (typical 20-40% saving). Use it BEFORE sending prompts to any LLM to cut token cost without losing meaning. input=text to compress.. Cost: $0.001 USDC/call, paid automatically via x402 on Base."
}
