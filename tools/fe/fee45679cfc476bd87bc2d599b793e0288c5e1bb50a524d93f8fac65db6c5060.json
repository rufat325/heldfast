{
 "description": "Stop an active video recording and save the file.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "deviceId": {
    "description": "Device (default: booted)",
    "type": "string"
   },
   "maxFrames": {
    "description": "Max number of key frames to extract (default: 3, max: 6)",
    "type": "number"
   },
   "savePath": {
    "description": "Optional: save the video file to this path instead of discarding it",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "simulator_stop_recording"
}
