{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "configure_project"
 },
 "description": "Link a directory to an org/app by writing seekrit.json (like `seekrit init`). The environment is chosen by the service token at runtime.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "app": {
    "type": "string"
   },
   "dir": {
    "type": "string"
   },
   "org": {
    "type": "string"
   }
  },
  "required": [
   "org",
   "app"
  ],
  "type": "object"
 },
 "name": "configure_project"
}
