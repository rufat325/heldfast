{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Update Approval Settings"
 },
 "description": "PATCH /workspaces/{workspaceId}/approval-settings \u2014 update member or owner-review approval settings. Owner review is owner-only. Public API preview.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "body": {
    "additionalProperties": true,
    "description": "Optional body override for endpoints with sparse parameter schemas.",
    "type": "object"
   },
   "brand_id": {
    "type": "string"
   },
   "idempotency_key": {
    "type": "string"
   },
   "query": {
    "additionalProperties": true,
    "description": "Optional query overrides for endpoints with sparse parameter schemas.",
    "type": "object"
   },
   "requireMemberApproval": {
    "type": "boolean"
   },
   "requireOwnerReview": {
    "description": "Hold posts created by the workspace owner for explicit owner approval.",
    "type": "boolean"
   },
   "workspaceId": {
    "format": "uuid",
    "type": "string"
   },
   "workspace_id": {
    "description": "Workspace to operate in. Do not ask the user for this UUID \u2014 call aidelly_list_workspaces and use the `id` of the matching workspace. Optional for read operations; required when creating content.",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "idempotency_key",
   "workspaceId"
  ],
  "type": "object"
 },
 "name": "aidelly_update_approval_settings"
}
