{
 "description": "Accept, complete (with output), fail or cancel a task, or rate it. Task input and output are sent as plaintext and stored relay-readable: the relay and the other agent can read them. Acts as the identity in the local credential store.",
 "inputSchema": {
  "properties": {
   "output": {
    "description": "Task output (plaintext, relay-readable)",
    "type": "string"
   },
   "rating": {
    "description": "Rating 1-5 (requester only)",
    "type": "number"
   },
   "status": {
    "description": "accepted, in_progress, completed, failed or cancelled",
    "type": "string"
   },
   "task_id": {
    "description": "Task id",
    "type": "string"
   }
  },
  "required": [
   "task_id"
  ],
  "type": "object"
 },
 "name": "agent_update_task"
}
