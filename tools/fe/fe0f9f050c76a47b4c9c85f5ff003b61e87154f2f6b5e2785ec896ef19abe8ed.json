{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Live ship positions right now in a geographic area, from AIS radio broadcasts \u2014 what vessels are sailing near a port, coastline, strait or set of coordinates at this moment. Returns each vessel heard with its position, speed, course, navigational status, type, destination and IMO where broadcast, plus a breakdown by vessel type (cargo, tanker, passenger, fishing, tug). Give either a centre point (latitude + longitude + radius_km) or a bounding box. COVERAGE IS PARTIAL AND TERRESTRIAL \u2014 there is no receiver coverage in the Persian Gulf, Strait of Hormuz, Gulf of Oman or Red Sea, so this tool cannot answer questions about those waters (use chokepoint_status for Hormuz/Suez/Bab el-Mandeb transit counts instead). Counts are vessels heard during a listening window of a few seconds, not a complete count of vessels present.",
 "inputSchema": {
  "examples": [
   {
    "latitude": 43.3,
    "longitude": 5.4,
    "radius_km": 50
   },
   {
    "east": 0.1,
    "north": 51.5,
    "ship_type": "tanker",
    "south": 51.4,
    "west": -0.1,
    "window_seconds": 20
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "aisstream.io API key (free at aisstream.io)",
    "type": "string"
   },
   "east": {
    "description": "Bounding box eastern longitude",
    "type": "number"
   },
   "latitude": {
    "description": "Centre latitude, e.g. 43.3 for Marseille",
    "type": "number"
   },
   "limit": {
    "description": "Maximum vessels to return (default 50, max 300)",
    "type": "number"
   },
   "longitude": {
    "description": "Centre longitude, e.g. 5.4 for Marseille",
    "type": "number"
   },
   "north": {
    "description": "Bounding box northern latitude",
    "type": "number"
   },
   "radius_km": {
    "description": "Radius around the centre point in km (default 50, max 500)",
    "type": "number"
   },
   "ship_type": {
    "description": "Optional filter on decoded type, e.g. \"tanker\", \"cargo\", \"passenger\", \"fishing\", \"tug\"",
    "type": "string"
   },
   "south": {
    "description": "Bounding box southern latitude (alternative to centre+radius)",
    "type": "number"
   },
   "west": {
    "description": "Bounding box western longitude",
    "type": "number"
   },
   "window_seconds": {
    "description": "How long to listen, 3-25 seconds (default 12). Longer windows hear more vessels, especially anchored ones which only transmit every ~3 minutes.",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "live_ships_in_area",
 "title": "Live Ships In Area",
 "version": "v1.39bc6cb8"
}
