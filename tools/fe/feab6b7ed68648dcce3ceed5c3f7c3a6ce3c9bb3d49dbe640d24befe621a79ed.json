{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false,
  "title": "delete_group"
 },
 "description": "Archive (soft delete) a group. The group is hidden from the active list but can be restored.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "group_id": {
    "description": "The UUID of the group to archive.",
    "type": "string"
   }
  },
  "required": [
   "group_id"
  ],
  "type": "object"
 },
 "name": "delete_group"
}
