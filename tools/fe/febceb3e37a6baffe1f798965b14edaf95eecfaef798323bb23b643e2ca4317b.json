{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get Onboarding Instructions"
 },
 "description": "Public onboarding hint for an agent whose user isn't linked to Vaaya yet. Returns where the human should go to connect (and sign up if new). Call this when vaaya_test_connection reports needs_auth, or any tool returns unauthorized, then relay the instructions to the user. If the user IS already linked it returns `first_call` instead \u2014 show `first_call.show_to_user` as written so they know what to try.",
 "inputSchema": {
  "properties": {},
  "type": "object"
 },
 "name": "vaaya_onboard",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "already_linked": {
    "description": "True when the agent is already connected.",
    "type": "boolean"
   },
   "instructions": {
    "description": "What to tell the user to get connected.",
    "type": "string"
   },
   "message": {
    "description": "Status message to relay to the user.",
    "type": "string"
   },
   "needs_auth": {
    "description": "True when this agent is not linked to a Vaaya user yet.",
    "type": "boolean"
   },
   "signup_uri": {
    "description": "Where a new user signs up.",
    "type": "string"
   },
   "verification_uri": {
    "description": "Where the user approves the connection.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "title": "Get Onboarding Instructions"
}
