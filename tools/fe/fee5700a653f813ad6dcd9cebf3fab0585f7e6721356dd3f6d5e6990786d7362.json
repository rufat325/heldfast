{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List/search US grid datasets (LMP prices, load, fuel mix across the 7 ISOs: CAISO, ERCOT, PJM, MISO, SPP, NYISO, ISONE). Use this to discover the exact dataset_id to pass to gridstatus_query. Example: gridstatus_datasets({ filter: \"caiso lmp\", _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-gridstatus-api-key",
    "filter": "caiso lmp"
   },
   {
    "_apiKey": "your-gridstatus-api-key"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "GridStatus API key (free tier at gridstatus.io)",
    "type": "string"
   },
   "filter": {
    "description": "Optional keyword(s) to narrow the list, matched against dataset id/name/description/source (all words must match). E.g. \"caiso lmp\", \"ercot load\", \"pjm fuel mix\". Omit to list all.",
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "gridstatus_datasets",
 "title": "Gridstatus Datasets",
 "version": "v1.39a4365f"
}
