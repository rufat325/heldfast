{
 "description": "Set the treasury routing percentage for future settlement events. Controls how much stays liquid vs routes to yield.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "pct": {
    "description": "Yield percentage (5-50, integer). Default is 20.",
    "maximum": 50,
    "minimum": 5,
    "type": "integer"
   }
  },
  "required": [
   "pct"
  ],
  "type": "object"
 },
 "name": "clicks_set_yield_pct"
}
