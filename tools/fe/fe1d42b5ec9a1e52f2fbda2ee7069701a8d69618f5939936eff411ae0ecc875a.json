{
 "description": "Update timezone or pause state. Pausing is always allowed. Resuming is not: if the pause came from reputation or SES tenant risk, a healthy observation has to exist first.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "marketing_state": {
    "description": "active or paused",
    "enum": [
     "active",
     "paused"
    ],
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "timezone": {
    "description": "IANA timezone",
    "type": "string"
   }
  },
  "required": [
   "organization_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_messaging_settings_update"
}
