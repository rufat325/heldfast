{
 "description": "Keyword search across Reddit (or inside one subreddit) and return matching posts as structured JSON: title, body, postUrl, communityName, authorName, score, commentsCount, createdAt. Use for brand monitoring, lead generation, market research, 'what is Reddit saying about X'. Search is literal keyword matching, so pass short terms (several are fine) rather than sentences.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "includeCommentThreads": {
    "default": true,
    "description": "Fetch comments under each matching post (multiplies result count; false for posts only)",
    "type": "boolean"
   },
   "includeNsfw": {
    "default": false,
    "type": "boolean"
   },
   "maxCommentsPerPost": {
    "default": 20,
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "maxPosts": {
    "default": 25,
    "description": "Total posts across all queries",
    "maximum": 5000,
    "minimum": 1,
    "type": "integer"
   },
   "postedAfter": {
    "description": "Only posts on/after this date (UTC)",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "postedBefore": {
    "$ref": "#/properties/postedAfter",
    "description": "Only posts on/before this date (UTC)"
   },
   "queries": {
    "description": "Search terms; each runs as its own search",
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 40,
    "minItems": 1,
    "type": "array"
   },
   "searchComments": {
    "default": true,
    "description": "Also return comments that mention the terms",
    "type": "boolean"
   },
   "sort": {
    "default": "new",
    "enum": [
     "new",
     "relevance",
     "top",
     "hot",
     "comments"
    ],
    "type": "string"
   },
   "subreddit": {
    "description": "Restrict to one subreddit, e.g. 'SaaS' or 'r/SaaS'",
    "type": "string"
   },
   "time": {
    "default": "all",
    "enum": [
     "all",
     "hour",
     "day",
     "week",
     "month",
     "year"
    ],
    "type": "string"
   }
  },
  "required": [
   "queries"
  ],
  "type": "object"
 },
 "name": "search_reddit",
 "title": "Search Reddit"
}
