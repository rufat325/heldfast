{
 "_meta": {},
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List app store version localizations"
 },
 "description": "List localizations for a specific App Store version (metadata per locale)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "appStoreVersionId": {
    "description": "App Store Connect app store version ID (required)",
    "type": "string"
   },
   "filter.locale": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by locale(s) (e.g. en-US)"
   },
   "limit": {
    "default": 100,
    "description": "Maximum number of localizations to return (default 100, max 200)",
    "type": "number"
   }
  },
  "required": [
   "appStoreVersionId"
  ],
  "type": "object"
 },
 "name": "list-app-store-version-localizations",
 "title": "List app store version localizations"
}
