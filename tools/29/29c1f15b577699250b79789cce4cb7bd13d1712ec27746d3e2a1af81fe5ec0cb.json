{
 "description": "Update your marketplace listing in place \u2014 name, models, payout address, endpoint, or description \u2014 no delete/re-add. Signs an update message with the unlocked payout wallet and PATCHes it. Only fields you pass change. Changing the endpoint re-verifies reachability before taking effect. Requires an unlocked wallet.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "allowUnsafeGateway": {
    "description": "Allow a gateway host outside localhost / *.aibtc.com. Required to send wallet-signed headers (and any apiKey) to an arbitrary host \u2014 leave unset unless you deliberately self-host the gateway.",
    "type": "boolean"
   },
   "apiKey": {
    "description": "Set/replace the key your endpoint requires (locks the endpoint).",
    "type": "string"
   },
   "description": {
    "description": "New description.",
    "type": "string"
   },
   "endpoint": {
    "description": "New OpenAI-compatible base URL (re-verified before it takes effect).",
    "format": "uri",
    "type": "string"
   },
   "gateway": {
    "description": "Marketplace gateway base URL. Defaults to https://inference.aibtc.com. Use http://localhost:8787 for local dev.",
    "format": "uri",
    "type": "string"
   },
   "models": {
    "description": "New Hugging Face repo id list.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "name": {
    "description": "New display name.",
    "type": "string"
   },
   "payoutAddress": {
    "description": "New payout address (must match the gateway network).",
    "type": "string"
   },
   "providerId": {
    "description": "The provider id returned at registration (from inference_list_providers).",
    "type": "string"
   }
  },
  "required": [
   "providerId"
  ],
  "type": "object"
 },
 "name": "inference_update_provider"
}
