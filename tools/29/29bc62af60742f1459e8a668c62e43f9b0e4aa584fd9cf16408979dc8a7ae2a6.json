{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Assets with the lowest or most negative 3-year correlation to the given US stock or ETF, each with its name and kind so you can screen them. Raw lowest-correlation list: it can contain leveraged/inverse ETNs, volatility products and illiquid micro-caps. Check name and kind before presenting anything as a portfolio diversifier. Covers ~4,700 US-listed stocks and ETFs only (no crypto, no foreign listings, no inverse/leveraged short funds, no intraday data); figures use weekly closes and refresh once per trading day. Unknown tickers return an error: resolve them with resolve_symbol first.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "symbol": {
    "description": "Ticker to diversify away from, e.g. QQQ",
    "type": "string"
   }
  },
  "required": [
   "symbol"
  ],
  "type": "object"
 },
 "name": "find_diversifiers",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "as_of": {
    "type": "string"
   },
   "caution": {
    "type": "string"
   },
   "diversifiers_3y": {
    "items": {
     "additionalProperties": true,
     "properties": {
      "correlation": {
       "type": "number"
      },
      "kind": {
       "type": "string"
      },
      "name": {
       "type": "string"
      },
      "symbol": {
       "type": "string"
      }
     },
     "required": [
      "symbol",
      "correlation"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "docs": {
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "page": {
    "type": "string"
   },
   "symbol": {
    "type": "string"
   },
   "terms": {
    "type": "string"
   }
  },
  "required": [
   "symbol",
   "as_of"
  ],
  "type": "object"
 },
 "title": "Find diversifiers"
}
