{
 "description": "Read-only Max for Live patcher discovery for a device. Use when deciding whether a device exposes inspectable M4L patcher metadata. Returns available=false with a reason when the target is not inspectable or the runtime lacks patcher access.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "device_index": {
    "minimum": 0,
    "type": "integer"
   },
   "track_index": {
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "track_index",
   "device_index"
  ],
  "type": "object"
 },
 "name": "device_inspect_patcher"
}
