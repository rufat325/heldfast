{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Retrieve the full context of a VibeMap project including features, user stories, personas, pages, and database schema. Use this before building a feature to understand all the specs.",
 "inputSchema": {
  "properties": {
   "includeFeatures": {
    "default": true,
    "type": "boolean"
   },
   "includePages": {
    "default": true,
    "type": "boolean"
   },
   "includePersonas": {
    "default": true,
    "type": "boolean"
   },
   "includeSchema": {
    "default": true,
    "type": "boolean"
   },
   "includeStories": {
    "default": true,
    "type": "boolean"
   },
   "projectId": {
    "description": "The VibeMap project ID",
    "type": "string"
   }
  },
  "required": [
   "projectId"
  ],
  "type": "object"
 },
 "name": "vibemap_get_project_context"
}
