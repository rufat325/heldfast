{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Return a structured spatial layout, topological graph, 3D asset export (gltf/obj), or high-level environment summary of the known world.",
 "inputSchema": {
  "properties": {
   "format": {
    "description": "Export or view format (default: json, use \"summary\" for high-level environment overview)",
    "enum": [
     "json",
     "geojson",
     "topological_graph",
     "gltf",
     "obj",
     "joint",
     "spatial_vlm",
     "summary"
    ],
    "type": "string"
   },
   "min_confidence": {
    "description": "Filter out entities below confidence threshold",
    "type": "number"
   },
   "project": {
    "description": "Optional project identifier",
    "type": "string"
   },
   "region_id": {
    "description": "Optional region ID to filter",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_spatial_map"
}
