{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Add rows to an existing block. Appends at the end by default; pass after_key or before_key to insert at a position instead. Each row needs a key; values is an object keyed by field name. Fields with a value_formula are auto-materialized by the engine \u2014 don't pass them in values. Validation/editability shadows for the new rows are auto-installed by the engine at InsertRowsInBlock time, so no follow-up is needed. Also inserts the matching sheet rows (one block per sheet-row assumption \u2014 extends the sheet so downstream rows shift down).",
 "inputSchema": {
  "properties": {
   "after_key": {
    "description": "Insert directly after the row with this key, instead of appending at the end. Mutually exclusive with before_key.",
    "type": "string"
   },
   "before_key": {
    "description": "Insert directly before the row with this key. Use the first key to insert at the top. Mutually exclusive with after_key.",
    "type": "string"
   },
   "block": {
    "description": "Block ref name.",
    "type": "string"
   },
   "rows": {
    "items": {
     "properties": {
      "key": {
       "description": "Row key (first column). Used as the row selector elsewhere. Must be unique within the block.",
       "type": "string"
      },
      "values": {
       "description": "Optional initial values keyed by field name. Fields with a value_formula should be omitted. For date/datetime fields, supply ISO strings (\"YYYY-MM-DD\" or \"YYYY-MM-DDTHH:MM\") \u2014 Watson converts them to the numeric form cells store.",
       "type": "object"
      }
     },
     "required": [
      "key"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "block",
   "rows"
  ],
  "type": "object"
 },
 "name": "add_block_rows"
}
