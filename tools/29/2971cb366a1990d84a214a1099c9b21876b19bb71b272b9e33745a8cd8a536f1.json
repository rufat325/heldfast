{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Callers of a symbol: call sites the parser recorded, plus path, import and re-export references, each with the function that holds it. Excludes the defining body and names spelled in strings or comments. Matches by name, so a different definition with the same name is included.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max results (default 100)",
    "type": "integer"
   },
   "name": {
    "description": "Symbol name",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "find_callers"
}
