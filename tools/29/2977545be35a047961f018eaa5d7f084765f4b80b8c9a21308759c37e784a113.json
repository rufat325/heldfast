{
 "description": "List AgentStorefront services that an agent can subscribe to and call. Returns names, prices, and short descriptions. Optionally filter by category.",
 "inputSchema": {
  "properties": {
   "category": {
    "description": "Optional category filter (e.g., 'data', 'communication', 'finance').",
    "type": "string"
   },
   "limit": {
    "default": 20,
    "description": "Max results (default 20, max 100).",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list_services"
}
