{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Per-TLD registration, renewal and transfer pricing in the account currency. Use it to answer \"how much is a .com?\" and to pick a TLD before checking availability with `impreza_domain_check`. Read-only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "tld": {
    "description": "Optional. Narrow to one TLD, e.g. \"com\" or \".com\". Omit for the whole price list.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "impreza_domain_pricing"
}
