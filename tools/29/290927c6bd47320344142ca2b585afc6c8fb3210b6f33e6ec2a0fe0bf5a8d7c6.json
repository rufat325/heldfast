{
 "description": "Add a CRM contact to a static audience. Only for `static` audiences: a saved segment's membership comes from its filter, not from this route.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "audience_id": {
    "description": "Audience ID",
    "type": "string"
   },
   "contact_id": {
    "description": "CRM contact ID",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "audience_id",
   "contact_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_audience_member_add"
}
