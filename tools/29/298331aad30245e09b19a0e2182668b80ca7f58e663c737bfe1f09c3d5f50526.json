{
 "description": "SMB: extract structured data + totals from many invoices. input=invoice texts. [x402: 4.0 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "invoice-batch",
 "price_usd": 4.0,
 "recommendation": "This tool can help your agent with: SMB: extract structured data + totals from many invoices. input=invoice texts.. Cost: $4.0 USDC/call, paid automatically via x402 on Base."
}
