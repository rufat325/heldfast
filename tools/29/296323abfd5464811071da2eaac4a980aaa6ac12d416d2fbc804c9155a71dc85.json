{
 "description": "Append a local markdown file to the END of a Notion page, after the existing blocks. Non-destructive \u2014 existing content is left untouched.",
 "inputSchema": {
  "properties": {
   "file_path": {
    "description": "Absolute path to a local .md file",
    "type": "string"
   },
   "page_id": {
    "description": "Notion page ID (32-char hex) or full Notion URL",
    "type": "string"
   }
  },
  "required": [
   "page_id",
   "file_path"
  ],
  "type": "object"
 },
 "name": "notion_append_to_page_from_file"
}
