{
 "description": "Compare two caller-named teams using caller-supplied strength ratings (1-100). Returns hypothetical strength-share percentages and one seeded simulated winner. No live sports data, injuries, bookmaker odds, draws or validated forecasts. Entertainment and what-if math only. GET accepts scalar query parameters. For object or array inputs, pass the complete input object as JSON in the payload query parameter. Price: 0.005 USDC. Mode: computed_service. Source: User-supplied ratings and a hypothetical strength-share model. Set auto_pay=true for a one-call purchase when the local buyer wallet is configured.",
 "inputSchema": {
  "properties": {
   "agent_token": {
    "description": "Optional pre-funded marketplace bearer token.",
    "type": "string"
   },
   "auto_pay": {
    "description": "Automatically authorize and pay this request with the locally configured buyer wallet. Requires X402_AUTO_PAY=true and X402_EVM_PRIVATE_KEY.",
    "type": "boolean"
   },
   "payload": {
    "type": "string"
   },
   "payment_id": {
    "description": "payment_id returned by the x402 invoice.",
    "type": "string"
   },
   "payment_signature": {
    "description": "Base64 x402 v2 PAYMENT-SIGNATURE produced by a compatible buyer client.",
    "type": "string"
   },
   "preview": {
    "description": "Inspect a rate-limited live contract proof showing source, freshness, response shape, and required inputs. Decision-ready values remain in the paid result.",
    "type": "boolean"
   },
   "rating_a": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "rating_b": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "seed": {
    "maxLength": 100,
    "type": "string"
   },
   "team_a": {
    "maxLength": 60,
    "type": "string"
   },
   "team_b": {
    "maxLength": 60,
    "type": "string"
   },
   "tx_hash": {
    "description": "Base transaction hash after paying an x402 invoice.",
    "type": "string"
   }
  },
  "required": [
   "team_a",
   "team_b",
   "rating_a",
   "rating_b"
  ],
  "type": "object"
 },
 "name": "get_sports_matchup_simulator"
}
