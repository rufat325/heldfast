{
 "description": "Analyze audio from a URL (any public or signed https link, up to 60 MB) with TrackTag AI. Same output and credit cost as analyze_track.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "audio_url": {
    "description": "Direct link to the audio file",
    "format": "uri",
    "type": "string"
   },
   "model": {
    "default": "core",
    "enum": [
     "core",
     "ultra"
    ],
    "type": "string"
   },
   "track_name": {
    "type": "string"
   }
  },
  "required": [
   "audio_url"
  ],
  "type": "object"
 },
 "name": "analyze_url"
}
