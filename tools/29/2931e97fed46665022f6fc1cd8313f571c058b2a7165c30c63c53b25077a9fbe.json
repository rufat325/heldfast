{
 "annotations": {
  "cost": {
   "all": 180
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Recent NEWS coverage with FULL ARTICLE TEXT \u2014 \"latest news about <company/person/topic>\", \"news coverage of X this week\", \"what are people reporting about X\", \"recent headlines on X\". Returns a synthesized answer plus ranked articles each carrying title, url, publication content, and a relevance score \u2014 the full text, not just a headline, so the calling agent can quote and cite. Freshness-windowed via `days` (default 7). This is the premium news path (Tavily, funded key, no per-IP throttle, no shared free-tier quota); prefer it when the question asks what is being REPORTED about a specific entity or topic.",
 "inputSchema": {
  "examples": [
   {
    "query": "Anthropic"
   },
   {
    "days": 14,
    "max_results": 10,
    "query": "GLP-1 drug pricing"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Tavily API key for higher limits; omit to use the shared Pipeworx key.",
    "type": "string"
   },
   "days": {
    "description": "How many days back to search (default 7, max 30).",
    "type": "number"
   },
   "max_results": {
    "description": "Maximum number of articles (default 5, max 20).",
    "type": "number"
   },
   "query": {
    "description": "The entity or topic to get news about, e.g. \"Anthropic\", \"GLP-1 drug pricing\".",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "tavily_news",
 "title": "Tavily News",
 "version": "v1.960006d0"
}
