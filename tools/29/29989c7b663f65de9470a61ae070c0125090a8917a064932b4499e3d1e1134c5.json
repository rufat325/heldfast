{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Form 144 notice-of-proposed-sale filings \u2014 insiders disclose intent to sell roughly 2 days before the matching Form 4 sale lands, so this is an early-warning signal, especially discretionary (non-10b5-1) notices. Filter by ticker, insider name (partial match), date range, or exclude_10b5. Cross-reference with get_transactions/get_insider_transactions to see whether the intent was actually executed. Requires Business plan. Paginated, max 100/page.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "exclude_10b5": {
    "description": "If true, exclude pre-scheduled 10b5-1 plan filings. Recommended for signal analysis \u2014 discretionary sale-intent notices only.",
    "type": "boolean"
   },
   "from": {
    "description": "Start filing date, inclusive, format YYYY-MM-DD (e.g. 2026-01-01).",
    "type": "string"
   },
   "insider_name": {
    "description": "Filter by insider name, case-insensitive partial/substring match, e.g. \"cook\".",
    "type": "string"
   },
   "page": {
    "default": 1,
    "description": "1-based page number. Defaults to 1.",
    "minimum": 1,
    "type": "integer"
   },
   "per_page": {
    "default": 20,
    "description": "Results per page. Defaults to 20, maximum 100.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "ticker": {
    "description": "Stock ticker symbol, case-insensitive, e.g. AAPL.",
    "type": "string"
   },
   "to": {
    "description": "End filing date, inclusive, format YYYY-MM-DD (e.g. 2026-12-31).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_form144"
}
