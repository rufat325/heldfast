{
 "description": "Update a task title, description, or status. Works for all task types (DB tasks, phase tasks, file-backed tasks). Omit workflowId to update a task by id regardless of its workflow (the server resolves it). Use this instead of update_task_status when you also need to set a title or description.",
 "inputSchema": {
  "properties": {
   "description": {
    "description": "New description for the task. Pass empty string to clear.",
    "type": "string"
   },
   "status": {
    "description": "New status for the task.",
    "enum": [
     "not_started",
     "in_progress",
     "done",
     "dismissed"
    ],
    "type": "string"
   },
   "taskId": {
    "description": "Task ID to update.",
    "type": "string"
   },
   "title": {
    "description": "New title for the task.",
    "type": "string"
   },
   "workflowId": {
    "description": "Workflow ID that owns the task. Omit to resolve the task by id regardless of its workflow.",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "taskId"
  ],
  "type": "object"
 },
 "name": "update_task"
}
