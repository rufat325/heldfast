{
 "description": "Check if a pane is running a command or idle at a prompt. Returns the current foreground process \u2014 if it's a shell (bash, zsh, etc.) the pane is idle. Anything else means a command is still running.",
 "inputSchema": {
  "properties": {
   "pane": {
    "description": "Target pane as 'window.pane' (e.g. '0.0'). Defaults to '0.0'.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "pane_status"
}
