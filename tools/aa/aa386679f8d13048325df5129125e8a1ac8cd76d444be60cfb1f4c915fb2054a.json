{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Remove a competitor relationship by its internal relation ID (the `id` field returned by list_competitors or add_competitor \u2014 note that this is the relation row id, not the competitor app id).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "relation_id": {
    "description": "Competitor relation internal ID (the `id` field at the top level of list_competitors / add_competitor responses)",
    "type": "string"
   }
  },
  "required": [
   "relation_id"
  ],
  "type": "object"
 },
 "name": "remove_competitor"
}
