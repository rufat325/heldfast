{
 "description": "Get a post-quantum ML-DSA-65 / Dilithium-3 (NIST FIPS 204) signature over any message or hash \u2014 signed locally, verifiable OFFLINE, with a verifiable on-chain-anchored receipt. Call when your agent needs a durable, quantum-safe signature or proof of authenticity. Price $0.02 USDC on Base.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "hash": {
    "description": "Hex digest already computed; signed directly. Provide message OR hash.",
    "type": "string"
   },
   "message": {
    "description": "UTF-8 message; we sign sha256(message).",
    "type": "string"
   },
   "payment": {
    "description": "Optional base64 x402 X-PAYMENT (EIP-3009 exact-scheme payload, or {txHash}). x402-aware wallet clients pass this automatically via _meta[\"x402/payment\"] \u2014 set it explicitly only if your client does not plumb _meta. Omit on the first call to receive the payment challenge.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "seal_ml_dsa",
 "title": "Post-quantum signature (ML-DSA-65 / Dilithium-3)"
}
