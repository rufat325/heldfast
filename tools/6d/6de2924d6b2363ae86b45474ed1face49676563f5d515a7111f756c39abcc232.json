{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Paste the `tools/list` response of an MCP server \u2014 yours, or a third party's you are evaluating whether to install \u2014 and check its catalogue quality: missing descriptions or annotations, a tool name that suggests it writes or deletes while its annotations say otherwise, context cost per tool (real BPE tokenization), and whether the top-level `instructions` field is set and fits under the ~2 KB some Tool Search clients trim. Add `serverJson`/`packageJson` to also catch the official MCP registry's own field-length and namespace-pattern limits, and a version mismatch between the two \u2014 the exact 422/403/400 errors this project hit publishing its own packages. This is NOT an access-security review: it does not evaluate what the tools are permitted to do, only how the catalogue is described.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "encoding": {
    "default": "o200k_base",
    "description": "BPE encoding for the token-cost measurement",
    "enum": [
     "o200k_base",
     "cl100k_base",
     "p50k_base",
     "r50k_base"
    ],
    "type": "string"
   },
   "instructions": {
    "description": "The server-level `instructions` string, if you have it",
    "type": "string"
   },
   "packageJson": {
    "description": "package.json, to cross-check its version against server.json",
    "type": "string"
   },
   "serverJson": {
    "description": "The registry manifest (server.json), to check its own limits",
    "type": "string"
   },
   "toolsListJson": {
    "description": "The `tools/list` response, either `{\"tools\": [...]}` or the bare array",
    "type": "string"
   }
  },
  "required": [
   "toolsListJson"
  ],
  "type": "object"
 },
 "name": "mcp_catalog_audit",
 "title": "Audit an MCP server catalogue for design quality"
}
