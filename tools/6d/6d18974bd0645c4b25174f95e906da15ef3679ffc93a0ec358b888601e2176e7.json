{
 "description": "Resolve or reopen up to 100 comments in a single call. Comments that are missing or hidden by the visibility gate are returned in `skipped`.",
 "inputSchema": {
  "properties": {
   "comment_ids": {
    "items": {
     "type": "string"
    },
    "maxItems": 100,
    "minItems": 1,
    "type": "array"
   },
   "status": {
    "enum": [
     "open",
     "resolved"
    ],
    "type": "string"
   }
  },
  "required": [
   "comment_ids",
   "status"
  ],
  "type": "object"
 },
 "name": "bulk_set_comment_status"
}
