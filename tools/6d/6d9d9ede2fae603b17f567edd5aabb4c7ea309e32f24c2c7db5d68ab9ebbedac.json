{
 "description": "Start a Code Engine build run from an existing build configuration (created with ce_create_build). Returns the build run whose status can be polled with ce_get_build_run.",
 "inputSchema": {
  "properties": {
   "build_name": {
    "description": "Name of the build configuration to run",
    "type": "string"
   },
   "name": {
    "description": "Optional name for this build run",
    "type": "string"
   },
   "project_id": {
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "build_name"
  ],
  "type": "object"
 },
 "name": "ce_create_build_run"
}
