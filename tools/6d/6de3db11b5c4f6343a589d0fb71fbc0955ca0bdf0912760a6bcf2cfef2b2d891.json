{
 "description": "Get whether a space uses the instance default color scheme (\"global\") or its own (\"custom\").",
 "inputSchema": {
  "properties": {
   "spaceKey": {
    "type": "string"
   }
  },
  "required": [
   "spaceKey"
  ],
  "type": "object"
 },
 "name": "get_space_color_scheme_type"
}
