{
 "description": "Get the bucket distribution for a (event_type, vertical, region) segment over a time window. Returns the share of each bucketed value within the payload (e.g. for booking_created, the share of bookings in each lead_time_bucket). All values are categorical buckets \u2014 no raw prices, durations, or names are ever returned. If the segment has fewer than 5 distinct contributing organizations, returns { ok: false, reason: \"insufficient_data\", contributors }. Use market.list_segments first to know which segments will return data. Example: market.get_benchmark({ event_type: \"payment_settled\", vertical: \"health\", region: \"CL\" }) \u2192 distribution of price bands, payment methods, time-to-collect buckets.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "event_type": {
    "description": "Which event type to query.",
    "enum": [
     "booking_created",
     "service_completed",
     "dispute_opened",
     "payment_settled"
    ],
    "type": "string"
   },
   "from": {
    "description": "Lower bound of the time window (ISO date or datetime). Default: 90 days ago.",
    "type": "string"
   },
   "region": {
    "description": "ISO 3166-1 alpha-2 country code (e.g. CL, AR, MX).",
    "type": "string"
   },
   "to": {
    "description": "Upper bound of the time window (ISO date or datetime). Default: now.",
    "type": "string"
   },
   "vertical": {
    "description": "Industry vertical (e.g. health, legal, home, education).",
    "type": "string"
   }
  },
  "required": [
   "event_type",
   "vertical",
   "region"
  ],
  "type": "object"
 },
 "name": "market_get_benchmark"
}
