{
 "annotations": {
  "readOnlyHint": true,
  "title": "get_feeding"
 },
 "description": "Get a single feeding by its UUID, including its ingredient snapshot (targets and actuals) and per-group deliveries.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "feeding_id": {
    "description": "The feeding UUID.",
    "type": "string"
   }
  },
  "required": [
   "feeding_id"
  ],
  "type": "object"
 },
 "name": "get_feeding"
}
