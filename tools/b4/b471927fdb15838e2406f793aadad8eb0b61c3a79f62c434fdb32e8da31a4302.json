{
 "description": "OCR receipts to structured JSON [x402: 0.05 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "entrada",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "ocr-receipts-biz",
 "price_usd": 0.05,
 "recommendation": "This tool can help your agent with: OCR receipts to structured JSON. Cost: $0.05 USDC/call, paid automatically via x402 on Base."
}
