{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get comment trigger stats"
 },
 "description": "Get comment trigger stats. Lifetime counters for one trigger: fires, public replies, DMs (and how many were AI-written), link clicks, unique people, split by channel.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Numeric id.",
    "type": "integer"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_comment_trigger_stats"
}
