{
 "description": "Update cvrt-seo-manager settings. Provide only keys to change; a blank/omitted secret preserves the stored value.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "settings": {
    "additionalProperties": {},
    "description": "Partial settings object to merge",
    "type": "object"
   },
   "site": {
    "description": "Site id (see list_sites)",
    "type": "string"
   }
  },
  "required": [
   "site",
   "settings"
  ],
  "type": "object"
 },
 "name": "seo_update_settings"
}
