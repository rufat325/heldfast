{
 "description": "Directory brute-force using parallel curl requests. Returns results (path/status/length), found_count, and paths_tested. Read-only GET requests, sends one request per wordlist entry per extension.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "extensions": {
    "description": "Comma-separated extensions to append, e.g. 'php,html,txt'",
    "type": "string"
   },
   "target": {
    "description": "Base URL, e.g. https://example.com",
    "type": "string"
   },
   "threads": {
    "default": 10,
    "description": "Concurrent request count",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   },
   "wordlist": {
    "description": "Path to wordlist file. Uses built-in common paths if not provided.",
    "type": "string"
   }
  },
  "required": [
   "target"
  ],
  "type": "object"
 },
 "name": "recon_directory_bruteforce"
}
