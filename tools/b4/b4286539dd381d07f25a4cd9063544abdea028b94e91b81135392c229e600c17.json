{
 "description": "List recent memories with pagination. Returns full metadata including IDs, tags, and timestamps. Supports filtering by tags, date range, tier, and memory type.",
 "inputSchema": {
  "properties": {
   "after": {
    "description": "ISO timestamp \u2014 only return memories created after this time",
    "type": "string"
   },
   "before": {
    "description": "ISO timestamp \u2014 only return memories created before this time",
    "type": "string"
   },
   "count_only": {
    "description": "Return only the total count, no memory content",
    "type": "boolean"
   },
   "cursor": {
    "description": "Pagination cursor from previous response",
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 10)",
    "type": "number"
   },
   "memory_type": {
    "description": "Filter by memory type",
    "enum": [
     "episodic",
     "semantic"
    ],
    "type": "string"
   },
   "sort": {
    "description": "Sort order (default: recent)",
    "enum": [
     "recent",
     "oldest",
     "tier"
    ],
    "type": "string"
   },
   "tags": {
    "description": "Filter by tags (AND logic)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "tier": {
    "description": "Filter by memory tier (0=pinned, 1=session, 2=semantic, 3=org)",
    "type": "number"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "velixar_list"
}
