{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Answer a question from the live web with citations in one call: web search + grounded LLM synthesis. Returns answer, cited sources, confidence, follow-ups. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "max_sources": {
    "description": "How many web sources to retrieve (1-8, default 5).",
    "type": "integer"
   },
   "question": {
    "description": "The question to answer (required).",
    "type": "string"
   }
  },
  "required": [
   "question"
  ],
  "type": "object"
 },
 "name": "agentbit.research",
 "title": "Research (web answer with citations)"
}
