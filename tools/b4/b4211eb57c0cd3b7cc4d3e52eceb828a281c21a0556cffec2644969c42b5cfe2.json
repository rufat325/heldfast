{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true,
  "title": "Read an artifact's body"
 },
 "description": "Read the actual text of an artifact another teammate attached to a task. get_task lists artifacts and inlines small text bodies; use this when a body was truncated or omitted, or to fetch one artifact by id or by task_id + name. Binary artifacts come back with a short-lived download_url instead of text. A synthesizer must read its inputs with this tool before reconciling them.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "artifact_id": {
    "description": "Artifact id (from get_task's artifacts list).",
    "format": "uuid",
    "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
    "type": "string"
   },
   "max_bytes": {
    "description": "Inline body cap in bytes. Default 200000.",
    "maximum": 1000000,
    "minimum": 1000,
    "type": "integer"
   },
   "name": {
    "description": "Artifact name/filename within that task.",
    "type": "string"
   },
   "task_id": {
    "description": "Task id \u2014 use with `name` when you don't have the artifact id.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_artifact",
 "title": "Read an artifact's body"
}
