{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Create a Worker route mapping a URL pattern to a Worker script. Requires confirm:true (human-approval gate); without it returns a preview only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "confirm": {
    "description": "Human-approval gate: must be true to actually perform this mutating operation. Omit or set false to get a non-executing preview of what would happen (with secrets redacted). A human should approve before this is set to true.",
    "type": "boolean"
   },
   "pattern": {
    "description": "Route pattern, e.g. example.com/api/*.",
    "type": "string"
   },
   "script": {
    "description": "Worker script name to run on this route. Omit to disable on the pattern.",
    "type": "string"
   },
   "zone_id": {
    "description": "The Cloudflare zone ID (32-char hex). Get it from list_zones.",
    "type": "string"
   }
  },
  "required": [
   "zone_id",
   "pattern"
  ],
  "type": "object"
 },
 "name": "create_worker_route"
}
