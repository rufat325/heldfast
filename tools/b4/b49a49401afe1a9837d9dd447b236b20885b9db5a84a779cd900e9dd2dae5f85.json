{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Set Event Split Exception"
 },
 "description": "Adjust the split for ONE event only (setup 80/20, this event 90/10). Re-stamps the attached event immediately; sold tickets keep their terms; the standing setup is untouched; expires with the event. Community-set setups only. Requires community_id + member_uid + event_id + terms.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "community_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "event_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "member_uid": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "terms": {
    "additionalProperties": false,
    "properties": {
     "notes": {
      "maxLength": 2000,
      "type": "string"
     },
     "splits": {
      "items": {
       "additionalProperties": false,
       "properties": {
        "recipient_id": {
         "maxLength": 128,
         "minLength": 1,
         "type": "string"
        },
        "recipient_type": {
         "enum": [
          "producer",
          "community"
         ],
         "type": "string"
        },
        "share_value": {
         "maximum": 100,
         "minimum": 0,
         "type": "number"
        }
       },
       "required": [
        "recipient_id",
        "recipient_type",
        "share_value"
       ],
       "type": "object"
      },
      "maxItems": 20,
      "minItems": 1,
      "type": "array"
     }
    },
    "required": [
     "splits"
    ],
    "type": "object"
   }
  },
  "required": [
   "community_id",
   "member_uid",
   "event_id",
   "terms"
  ],
  "type": "object"
 },
 "name": "set_event_split_exception",
 "title": "Set Event Split Exception"
}
