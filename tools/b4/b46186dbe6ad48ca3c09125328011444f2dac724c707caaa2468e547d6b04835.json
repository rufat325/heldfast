{
 "description": "Test SSRF access to cloud metadata endpoints (AWS/GCP/Azure). Attempts to reach instance metadata services through the SSRF vector. Returns results array with provider, endpoint, status, length, response_snippet. Side effects: May cause target to request cloud metadata. Could expose IAM credentials if successful.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cloud_provider": {
    "description": "Cloud provider to test metadata endpoints for",
    "enum": [
     "aws",
     "gcp",
     "azure",
     "all"
    ],
    "type": "string"
   },
   "method": {
    "description": "HTTP method",
    "enum": [
     "GET",
     "POST"
    ],
    "type": "string"
   },
   "parameter": {
    "description": "Parameter that accepts URLs",
    "type": "string"
   },
   "url": {
    "description": "Target URL with SSRF-vulnerable parameter",
    "type": "string"
   }
  },
  "required": [
   "url",
   "parameter"
  ],
  "type": "object"
 },
 "name": "ssrf_cloud_metadata"
}
