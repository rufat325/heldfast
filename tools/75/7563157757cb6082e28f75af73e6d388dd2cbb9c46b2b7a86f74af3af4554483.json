{
 "_meta": {
  "apiVersion": "1.7.0",
  "examples": [
   {
    "input": {
     "author": "Ada Lovelace",
     "keywords": "invoice, 2026",
     "modDate": "2026-08-22T10:00:00Z",
     "pdfBase64": "<any-pdf-base64>"
    },
    "title": "Set author and keywords"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Rewrite /Info (title, author, subject, keywords) of an EXISTING PDF as an incremental update; XMP stays in sync on PDF/A documents; /ModDate is refreshed (pin `modDate` for reproducible bytes). Earlier revisions and signatures stay byte-identical \u2014 the new revision is unsigned (sign_pdf / timestamp_pdf again if needed). Encrypted sources \u2192 ENCRYPTED_SOURCE. For metadata at generation time use the `metadata` option of the document tools.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "author": {
    "description": "New /Info /Author (XMP dc:creator).",
    "maxLength": 1000,
    "type": "string"
   },
   "keywords": {
    "description": "New /Info /Keywords (XMP pdf:Keywords).",
    "maxLength": 1000,
    "type": "string"
   },
   "modDate": {
    "description": "ISO-8601 modification instant for /ModDate and xmp:ModifyDate. Defaults to now; pin it for reproducible output.",
    "format": "date-time",
    "type": "string"
   },
   "outputMode": {
    "default": "base64",
    "description": "'base64' (default) returns the PDF inline; 'file' writes it inside the PDFNATIVE_MCP_OUTPUT_DIR sandbox (SECURITY_VIOLATION when the sandbox is not configured).",
    "enum": [
     "base64",
     "file"
    ],
    "type": "string"
   },
   "outputPath": {
    "description": "Relative path inside PDFNATIVE_MCP_OUTPUT_DIR (required when outputMode='file').",
    "type": "string"
   },
   "pdfBase64": {
    "description": "Base64-encoded PDF to update (unencrypted).",
    "minLength": 1,
    "type": "string"
   },
   "subject": {
    "description": "New /Info /Subject (XMP dc:description).",
    "maxLength": 1000,
    "type": "string"
   },
   "title": {
    "description": "New /Info /Title (mirrored to XMP dc:title when the document carries XMP).",
    "maxLength": 1000,
    "type": "string"
   }
  },
  "required": [
   "pdfBase64"
  ],
  "type": "object"
 },
 "name": "update_metadata",
 "outputSchema": {
  "additionalProperties": false,
  "description": "Result of a PDF-producing tool. base64 mode: the PDF arrives as an embedded `resource` content block (not duplicated here); file mode: `filePath`.",
  "properties": {
   "diagnostics": {
    "description": "Engine diagnostics (when includeDiagnostics=true).",
    "items": {
     "additionalProperties": false,
     "properties": {
      "code": {
       "type": "string"
      },
      "message": {
       "type": "string"
      },
      "severity": {
       "enum": [
        "warning"
       ],
       "type": "string"
      }
     },
     "required": [
      "code",
      "message",
      "severity"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "filePath": {
    "description": "Sandboxed absolute path (file mode).",
    "type": "string"
   },
   "mode": {
    "enum": [
     "base64",
     "file"
    ],
    "type": "string"
   },
   "sizeBytes": {
    "minimum": 0,
    "type": "integer"
   },
   "summary": {
    "additionalProperties": true,
    "description": "Tool-specific summary, when produced.",
    "type": "object"
   }
  },
  "required": [
   "mode",
   "sizeBytes"
  ],
  "type": "object"
 },
 "title": "Update document metadata"
}
