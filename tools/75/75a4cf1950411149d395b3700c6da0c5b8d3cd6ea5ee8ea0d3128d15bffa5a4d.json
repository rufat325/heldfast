{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Debug a failed execution without flooding the context: fetches the full run data and returns only the failing node, error message/description, a short stack and per-node errors. Use instead of get_execution includeData=true when you only need to know what broke.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Execution ID (see list_executions with status=error)",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "summarize_execution_error"
}
