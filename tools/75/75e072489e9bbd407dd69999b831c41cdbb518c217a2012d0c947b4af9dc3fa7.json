{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "First buyers of a Robinhood Chain token, ranked, with still-holding status. Previously only readable as a count inside a wallet profile ('this wallet was an early buyer of N tokens') \u2014 this is the per-token ranked list. Added 2026-09-10. Tier: PRO+.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "address": {
    "description": "Token address (0x, 40 hex)",
    "type": "string"
   },
   "limit": {
    "description": "Rows, capped at 20 (default 20)",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "address"
  ],
  "type": "object"
 },
 "name": "rhc_token_early_buyers"
}
