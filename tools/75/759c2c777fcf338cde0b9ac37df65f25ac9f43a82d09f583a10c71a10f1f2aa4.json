{
 "description": "[engram-compat] Fetch full untruncated content of an observation by ID (episode|memory|trait).",
 "inputSchema": {
  "properties": {
   "id": {
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "mem_get_observation"
}
