{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "POST a PxWeb query to a Hagstova F\u00f8roya table and return observations as json-stat2. body must be {query:[{code, selection:{filter,values}}], response:{format:'json-stat2'}}. PxWeb rejects requests exceeding its cell limit \u2014 narrow each dimension's selection.values using codes from table_meta.",
 "inputSchema": {
  "examples": [
   {
    "body": {
     "query": [
      {
       "code": "island/highest mountain",
       "selection": {
        "filter": "item",
        "values": [
         "999"
        ]
       }
      },
      {
       "code": "area in km\u00b2, no. of mountains and population",
       "selection": {
        "filter": "item",
        "values": [
         "001"
        ]
       }
      }
     ],
     "response": {
      "format": "json-stat2"
     }
    },
    "path": "H2/UO/UO01/land_oyfj.px"
   }
  ],
  "properties": {
   "body": {
    "description": "{query: [{code, selection: {filter, values}}], response: {format: \"json-stat2\"}}",
    "type": "object"
   },
   "path": {
    "description": "e.g. \"H2/UO/UO01/land_oyfj.px\"",
    "type": "string"
   }
  },
  "required": [
   "path",
   "body"
  ],
  "type": "object"
 },
 "name": "query_table",
 "title": "Query Table",
 "version": "v1.916af3d2"
}
