{
 "description": "Rehost an external https image onto Frase-hosted storage for FraseCMS content (SSRF-guarded), returning a stable hosted URL so published content never hotlinks a third-party image that may move or rate-limit.",
 "inputSchema": {
  "properties": {
   "site_id": {
    "description": "Site to rehost the media for.",
    "type": "string"
   },
   "url": {
    "description": "Public https URL of the image.",
    "type": "string"
   }
  },
  "required": [
   "site_id",
   "url"
  ],
  "type": "object"
 },
 "name": "rehost_cms_media"
}
