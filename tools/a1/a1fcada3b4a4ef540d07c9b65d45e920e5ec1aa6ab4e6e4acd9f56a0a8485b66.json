{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get hourly weather observations (temperature, humidity, precipitation, wind, pressure, cloud cover, weather condition code) for a Meteostat weather station over a date range of up to 7 days within a single calendar year. Station ID comes from meteostat.stations_nearby. Use meteostat.daily_data for longer time spans. Data: Meteostat (CC BY 4.0), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "end_date": {
    "description": "End of the date range in YYYY-MM-DD format (inclusive). Must be within 7 days of start_date and in the same calendar year \u2014 use meteostat.daily_data for longer spans.",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "start_date": {
    "description": "Start of the date range in YYYY-MM-DD format (inclusive).",
    "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
    "type": "string"
   },
   "station_id": {
    "description": "Meteostat weather station ID, obtained from meteostat.stations_nearby.",
    "maxLength": 20,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "station_id",
   "start_date",
   "end_date"
  ],
  "type": "object"
 },
 "name": "meteostat.observations.hourly",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Get Hourly Weather Observations"
}
