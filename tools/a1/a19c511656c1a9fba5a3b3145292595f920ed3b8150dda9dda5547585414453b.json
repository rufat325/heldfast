{
 "description": "Search FCC ID certifications for electronic devices. Look up by FCC ID, grantee code, or applicant name. Returns certification details including equipment class, grant dates, and test reports.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "applicantName": {
    "description": "Company name (e.g. \"Apple\")",
    "type": "string"
   },
   "fccId": {
    "description": "Full FCC ID (e.g. \"BCG-E3994A\")",
    "type": "string"
   },
   "granteeCode": {
    "description": "Grantee code prefix (e.g. \"BCG\" for Apple)",
    "type": "string"
   },
   "limit": {
    "default": 20,
    "description": "Max results (max 100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "search_fcc"
}
