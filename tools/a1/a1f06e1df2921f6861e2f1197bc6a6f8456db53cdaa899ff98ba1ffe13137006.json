{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "On-Balance Volume (OBV)"
 },
 "description": "On-Balance Volume (OBV) for a stock. Running cumulative volume that adds the bar's volume on up-closes, subtracts on down-closes, and stays flat on equal closes. Useful for confirming or diverging from price trends with volume flow. OBV is anchored at 0 on the first bar of the requested range, so absolute values shift with startDate and are not comparable across calls - read the slope and divergences, not the level.",
 "inputSchema": {
  "properties": {
   "endDate": {
    "default": null,
    "description": "End date in YYYY-MM-DD format (defaults to latest available)",
    "type": "string"
   },
   "maxResults": {
    "default": 60,
    "description": "Maximum number of records to return (default: 60, max: 500); the newest rows are kept and listed newest first.",
    "type": "integer"
   },
   "startDate": {
    "default": null,
    "description": "Start date in YYYY-MM-DD format (defaults to 6 months ago)",
    "type": "string"
   },
   "ticker": {
    "description": "Stock ticker symbol (e.g., AAPL, MSFT). Class shares use a dash (BRK-B); the dot form (BRK.B) is also accepted.",
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "GetOnBalanceVolume",
 "title": "On-Balance Volume (OBV)"
}
