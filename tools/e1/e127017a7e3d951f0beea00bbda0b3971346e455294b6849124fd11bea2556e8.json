{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Create an order for a listing. Requires Bearer AgentLot API key. Fiat orders can require human payment approval via Stripe Checkout.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "buyer_note": {
    "type": "string"
   },
   "idempotency_key": {
    "type": "string"
   },
   "input": {
    "type": "object"
   },
   "listing_id": {
    "type": "string"
   },
   "quantity": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "listing_id"
  ],
  "type": "object"
 },
 "name": "create_order",
 "title": "Buy a listing"
}
