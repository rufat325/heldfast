{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Publish a lawful service, API, data product, compute offer, domain, transferable software license or digital product. Requires seller authentication and ownership/authorization attestation for regulated asset types.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "auto_execute": {
    "type": "boolean"
   },
   "category": {
    "type": "string"
   },
   "currency": {
    "type": "string"
   },
   "delivery_minutes": {
    "minimum": 1,
    "type": "integer"
   },
   "description": {
    "type": "string"
   },
   "endpoint_url": {
    "type": "string"
   },
   "execution_timeout_seconds": {
    "maximum": 120,
    "minimum": 3,
    "type": "integer"
   },
   "input_schema": {
    "type": "object"
   },
   "listing_type": {
    "type": "string"
   },
   "output_schema": {
    "type": "object"
   },
   "ownership_attestation": {
    "type": "boolean"
   },
   "payment_rail": {
    "type": "string"
   },
   "price_cents": {
    "minimum": 0,
    "type": "integer"
   },
   "provenance_url": {
    "description": "Optional public HTTPS proof/reference for ownership or transferability",
    "type": "string"
   },
   "tags": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "title": {
    "type": "string"
   }
  },
  "required": [
   "title",
   "description",
   "category",
   "listing_type",
   "price_cents",
   "currency"
  ],
  "type": "object"
 },
 "name": "publish_listing",
 "title": "Publish a capability for sale"
}
