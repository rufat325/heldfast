{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Create creator"
 },
 "description": "Create a new creator (a sub-account used to classify collections and tracks and track per-creator usage). organization_index is your own external identifier for the creator. Provisions a dedicated storage folder linked via folder_id.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "metadata": {
    "additionalProperties": true,
    "type": "object"
   },
   "organization_index": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "organization_index"
  ],
  "type": "object"
 },
 "name": "adn_create_creator"
}
