{
 "description": "List all inbound (collect) payment methods Rapyd supports for a given country + currency. Use to dynamically discover local methods (OXXO in MX, Pix in BR, UPI in IN, MoMo in GH) rather than hard-coding.",
 "inputSchema": {
  "properties": {
   "country": {
    "description": "ISO-3166 alpha-2 country code (MX, BR, IN, GH, PH, etc)",
    "type": "string"
   },
   "currency": {
    "description": "ISO-4217 currency code (optional). Narrows the list to methods that settle in this currency.",
    "type": "string"
   }
  },
  "required": [
   "country"
  ],
  "type": "object"
 },
 "name": "list_payment_methods_by_country"
}
