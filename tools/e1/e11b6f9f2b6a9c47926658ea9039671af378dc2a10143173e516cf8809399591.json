{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Power-control a dedicated server: boot it, shut it down, or reboot it. A shutdown or reboot interrupts everything running on the machine, so confirm with the customer before calling it, and check `impreza_dedicated_status` first.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "One of: on (boot), off (shut down), restart (reboot).",
    "type": "string"
   },
   "service_id": {
    "description": "Service id from impreza_list_dedicated.",
    "type": "number"
   }
  },
  "required": [
   "service_id",
   "action"
  ],
  "type": "object"
 },
 "name": "impreza_dedicated_power"
}
