{
 "description": "Get a paginated list of the user's public TikTok videos with engagement metrics (views, likes, comments, shares). Returns up to 20 videos per page.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "account": {
    "description": "Account name to query. Use list_accounts to see available options. Defaults to the first configured account.",
    "type": "string"
   },
   "cursor": {
    "description": "Pagination cursor (UTC Unix timestamp in ms). Pass the cursor from the previous response to get the next page.",
    "type": "number"
   },
   "max_count": {
    "default": 10,
    "description": "Number of videos to return (1-20, default 10)",
    "maximum": 20,
    "minimum": 1,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "get_videos"
}
