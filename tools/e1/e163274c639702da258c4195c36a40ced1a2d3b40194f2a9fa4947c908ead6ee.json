{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true,
  "title": "Get the daily standup"
 },
 "description": "Daily standup: done since last standup (24h, Monday looks back to Friday), today's plan, blockers & open questions, overdue & stalled. Parked and cancelled work is left out. Run it at the start of your day. scope 'me' = work assigned to or owned by you; 'client' = the whole client team.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "client": {
    "description": "Client name, @handle, or UUID (required for scope 'client', optional filter for 'me').",
    "type": "string"
   },
   "format": {
    "description": "Default 'text' (short summary).",
    "enum": [
     "text",
     "json"
    ],
    "type": "string"
   },
   "scope": {
    "description": "Default 'me'.",
    "enum": [
     "me",
     "client"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_standup",
 "title": "Get the daily standup"
}
