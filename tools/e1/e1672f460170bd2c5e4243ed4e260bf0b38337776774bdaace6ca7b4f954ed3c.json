{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the meetings in a namespace newest first, with titles, dates, participants and whether each one is searchable. Never returns transcripts. Useful for orienting before a search, and for finding meetings that are private and therefore invisible to meetings_search. Costs $0.005 in USDC on Base, paid via the x402 protocol, or from a credit token \u2014 call credits_trial for free credit if you have neither.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "credit_token": {
    "description": "Optional. A credit token from credits_trial or /credits/buy. Supplying it pays for this call from that balance, so no x402 payment or wallet is needed.",
    "type": "string"
   },
   "limit": {
    "description": "Max results, 1-500 (default 100)",
    "type": "integer"
   },
   "namespace": {
    "description": "Isolation scope holding your meetings",
    "type": "string"
   },
   "namespace_token": {
    "description": "One-time token issued by the first call that claimed this namespace. Required for every later call.",
    "type": "string"
   }
  },
  "required": [
   "namespace",
   "namespace_token"
  ],
  "type": "object"
 },
 "name": "meetings_list",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of meetings returned",
    "type": "number"
   },
   "meetings": {
    "items": {
     "properties": {
      "meeting_id": {
       "description": "Identifier",
       "type": "string"
      },
      "searchable": {
       "description": "Whether this meeting can be searched",
       "type": "boolean"
      },
      "title": {
       "description": "Meeting title",
       "type": "string"
      },
      "visibility": {
       "description": "'queryable' or 'private'",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "status": {
    "description": "'ok' on success",
    "type": "string"
   }
  },
  "required": [
   "status",
   "count",
   "meetings"
  ],
  "type": "object"
 },
 "title": "List your meetings"
}
