{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Estimate the monthly bill for a named stack at a given scale"
 },
 "description": "Price a whole stack at a chosen user count using StackTab's verified plan data. Returns the plan each service lands on and an itemised bill \u2014 which lines are included in the base fee and which are metered overages. Use when someone asks \"what will this cost at N users\" instead of estimating from memory.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "stack": {
    "description": "Comma-separated service slugs, e.g. \"supabase,clerk,polar,vercel\". Look them up with lookup_service_pricing.",
    "type": "string"
   },
   "users": {
    "description": "Monthly active users to price at. Default 10,000.",
    "type": "number"
   }
  },
  "required": [
   "stack"
  ],
  "type": "object"
 },
 "name": "estimate_stack_cost",
 "title": "Estimate the monthly bill for a named stack at a given scale"
}
