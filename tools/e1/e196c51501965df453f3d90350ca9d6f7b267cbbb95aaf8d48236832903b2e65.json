{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Delete a context key.",
 "inputSchema": {
  "properties": {
   "key": {
    "description": "Context key to delete",
    "type": "string"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   }
  },
  "required": [
   "spaceId",
   "key"
  ],
  "type": "object"
 },
 "name": "context_delete",
 "outputSchema": {
  "properties": {
   "deleted": {
    "description": "True when the key was deleted",
    "type": "boolean"
   }
  },
  "required": [
   "deleted"
  ],
  "type": "object"
 }
}
