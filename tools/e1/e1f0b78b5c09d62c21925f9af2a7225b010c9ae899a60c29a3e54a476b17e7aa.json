{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Delete one achievement from the bank. There is no undo, so confirm with the candidate first \u2014 read it back to them and remove it only on a clear yes. It takes nothing else with it. This is how a duplicate goes: keep the richer entry, remove the other.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "The achievement id, from experience_list. This is also cv_edit's evidence_id.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "experience_remove_achievement"
}
