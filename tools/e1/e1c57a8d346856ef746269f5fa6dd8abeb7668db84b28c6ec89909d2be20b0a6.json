{
 "description": "Add a source: an adapter with a config, fetched on a schedule (needs a key; admins and Pro). Call list_adapters first for the fields.",
 "inputSchema": {
  "properties": {
   "adapter": {
    "description": "Adapter name, e.g. github-releases",
    "type": "string"
   },
   "cadence_minutes": {
    "description": "5 to 1440",
    "type": "integer"
   },
   "collection": {
    "description": "Collection slug; defaults to the adapter's",
    "type": "string"
   },
   "config": {
    "description": "Adapter config fields",
    "type": "object"
   },
   "name": {
    "description": "Display name",
    "type": "string"
   }
  },
  "required": [
   "adapter"
  ],
  "type": "object"
 },
 "name": "add_source"
}
