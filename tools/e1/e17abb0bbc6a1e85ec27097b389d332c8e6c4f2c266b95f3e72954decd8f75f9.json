{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "The account's base CV (the saved profile, untailored) rendered as a PDF in the chosen design, returned as a file. No model call.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "design": {
    "description": "Optional; defaults to the saved design.",
    "maxLength": 40,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_my_cv_pdf",
 "title": "Render my CV as a PDF"
}
