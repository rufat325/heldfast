{
 "description": "Detect pages competing for the same keywords. Shows keyword groups with conflicting pages and their positions. Requires an audit_id \u2014 get it from `list_audits` or `get_audit`.",
 "inputSchema": {
  "properties": {
   "audit_id": {
    "description": "From list_audits or get_audit",
    "type": "string"
   }
  },
  "required": [
   "audit_id"
  ],
  "type": "object"
 },
 "name": "get_cannibalization"
}
