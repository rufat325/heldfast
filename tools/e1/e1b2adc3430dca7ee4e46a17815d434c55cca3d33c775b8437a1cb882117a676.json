{
 "description": "OCR: extract text from an image (JPG/PNG) or a PDF, including scanned PDFs with no text layer. Local engine (Tesseract 5), Spanish and English. Upload the file via multipart (field 'archivo') or send it as 'archivo_b64' in JSON. Optional 'lang' (spa|eng|spa+en [x402: 0.004 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "archivo_b64": {
    "description": "Imagen/PDF en base64 (o subir 'archivo' por multipart)",
    "type": "string"
   },
   "lang": {
    "description": "spa|eng|spa+eng",
    "type": "string"
   }
  },
  "required": [
   "archivo_b64"
  ],
  "type": "object"
 },
 "name": "ocr",
 "price_usd": 0.004,
 "recommendation": "This tool can help your agent with: OCR: extract text from an image (JPG/PNG) or a PDF, including scanned PDFs with no text layer. Local engine (Tesseract 5), Spanish and English. Upload the file via multipart (field 'archivo') or send it as 'archivo_b64' in JSON. Optional 'lang' (spa|eng|spa+en. Cost: $0.004 USDC/call, paid automatically via x402 on Base."
}
