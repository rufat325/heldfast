{
 "description": "Email an AUTHORISED invoice to the contact's email address on file. Xero uses the default invoice email template. No body required.",
 "inputSchema": {
  "properties": {
   "InvoiceID": {
    "description": "Xero Invoice UUID. Invoice must be AUTHORISED and contact must have an EmailAddress.",
    "type": "string"
   }
  },
  "required": [
   "InvoiceID"
  ],
  "type": "object"
 },
 "name": "email_invoice"
}
