{
 "description": "Clean up stale memories. Archives completed TODOs and optionally removes old low-importance memories that haven't been accessed. Keeps the memory store lean and relevant.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "archive_done_todos": {
    "default": true,
    "description": "Archive todos with status 'done' (default: true)",
    "type": "boolean"
   },
   "delete_stale_days": {
    "description": "Delete low-importance memories older than N days that have been accessed 0-1 times",
    "type": "number"
   },
   "max_importance": {
    "description": "Maximum importance level to delete (default: low)",
    "enum": [
     "low",
     "medium"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "memory_cleanup"
}
