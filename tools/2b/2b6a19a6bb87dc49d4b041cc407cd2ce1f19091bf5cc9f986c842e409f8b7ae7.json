{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Get the highest-priority acceptance criterion in `ready` status for the given project. Returns the criterion to work on next, or null if nothing is ready.",
 "inputSchema": {
  "properties": {
   "projectId": {
    "description": "Project UUID",
    "type": "string"
   }
  },
  "required": [
   "projectId"
  ],
  "type": "object"
 },
 "name": "vibemap_get_next_ready_criterion"
}
