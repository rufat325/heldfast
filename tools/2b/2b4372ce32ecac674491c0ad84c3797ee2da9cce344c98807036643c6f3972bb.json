{
 "description": "Get story clusters gaining momentum right now, ranked by velocity: how quickly coverage and discussion grew within a recent window. Each story includes its velocity score plus commentary and coverage deltas. Use get_story for full detail.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "hours": {
    "default": 24,
    "description": "Look-back window in hours (default 24, max 48)",
    "maximum": 48,
    "minimum": 1,
    "type": "number"
   },
   "limit": {
    "default": 20,
    "description": "Number of stories to return (default 20, max 50)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "get_trending_stories",
 "title": "Trending stories"
}
