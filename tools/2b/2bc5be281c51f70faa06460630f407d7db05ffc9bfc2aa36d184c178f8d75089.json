{
 "description": "Search Danish statutes and regulations by keyword.\n\nSearches provision text using FTS5 with BM25 ranking. Supports boolean operators (AND, OR, NOT), phrase search (\"exact phrase\"), and prefix matching (term*).\n\nReturns matched provisions with snippets, relevance scores, and document metadata.",
 "inputSchema": {
  "properties": {
   "as_of_date": {
    "description": "Optional historical date filter (YYYY-MM-DD). Returns versions valid on that date.",
    "type": "string"
   },
   "document_id": {
    "description": "Filter to a specific statute by document ID (e.g., \"2018:218\")",
    "type": "string"
   },
   "limit": {
    "description": "Maximum results (default: 10, max: 50)",
    "type": "number"
   },
   "query": {
    "description": "Search query in Danish or English. Supports FTS5 syntax.",
    "type": "string"
   },
   "status": {
    "description": "Filter by document status",
    "enum": [
     "in_force",
     "amended",
     "repealed"
    ],
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_legislation"
}
