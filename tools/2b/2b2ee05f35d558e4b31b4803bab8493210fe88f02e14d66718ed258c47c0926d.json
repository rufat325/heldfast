{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Guest view link"
 },
 "description": "Issue a signed, expiring guest view URL for any tier (`?vt=\u2026`). Use for private run reports without the shared site password. Returns `{ url, viewToken, expiresAt }`.",
 "inputSchema": {
  "properties": {
   "expiresSeconds": {
    "maximum": 604800,
    "minimum": 300,
    "type": "integer"
   },
   "hatchId": {
    "description": "Hatch id from the original hatch response.",
    "type": "string"
   },
   "sessionToken": {
    "type": "string"
   },
   "tenantId": {
    "description": "Legacy alias for `hatchId`.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "share",
 "title": "Guest view link"
}
