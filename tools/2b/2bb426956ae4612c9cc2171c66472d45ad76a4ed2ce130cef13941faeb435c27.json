{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Fetch App Reviews"
 },
 "description": "Fetch user reviews for an app. Supports filtering by star rating range and sorting by recent or helpful. Useful for sentiment analysis, feature-request mining, and competitive research.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "country": {
    "default": "us",
    "description": "ISO 3166-1 alpha-2 country code (e.g. \"us\", \"gb\", \"de\"). Default \"us\".",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   },
   "lang": {
    "description": "Android only: fetch one language feed (e.g. ar, fr). Omit to merge the market language plus en, es, fr, ar; coverage is not exhaustive.",
    "pattern": "^[a-z]{2}$",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of reviews to return (1-200).",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "max_rating": {
    "description": "Filter to reviews with a star rating <= this value (1-5).",
    "maximum": 5,
    "minimum": 1,
    "type": "integer"
   },
   "min_rating": {
    "description": "Filter to reviews with a star rating >= this value (1-5).",
    "maximum": 5,
    "minimum": 1,
    "type": "integer"
   },
   "sort": {
    "default": "recent",
    "description": "Sort order. \"recent\" returns newest first, \"helpful\" returns most-voted first.",
    "enum": [
     "recent",
     "helpful"
    ],
    "type": "string"
   },
   "store": {
    "description": "App store. \"ios\" for Apple App Store, \"android\" for Google Play.",
    "enum": [
     "ios",
     "android"
    ],
    "type": "string"
   },
   "store_id": {
    "description": "Store-specific app identifier. iOS: numeric track ID. Android: package name.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "store",
   "store_id"
  ],
  "type": "object"
 },
 "name": "sonar_app_reviews",
 "title": "Fetch App Reviews"
}
