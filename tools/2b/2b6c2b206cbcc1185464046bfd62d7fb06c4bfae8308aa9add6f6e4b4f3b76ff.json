{
 "description": "Check a submitted plan: overall status (pending | approved | partially_approved | denied | expired | revoked) and per-step grant status (approved / denied / consumed). Poll this after dashclaw_plan_submit to learn the operator verdict before executing.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "plan_id": {
    "description": "Plan id from dashclaw_plan_submit, e.g. pa_1234... (required)",
    "type": "string"
   }
  },
  "required": [
   "plan_id"
  ],
  "type": "object"
 },
 "name": "dashclaw_plan_status"
}
