{
 "description": "Paid ($0.02 USDC): current market regime (RISK_ON/RISK_OFF) from a provable chain + whether it changed since the regime you last saw. Cheap poll endpoint to react to regime flips.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "since": {
    "description": "Regime you last saw (e.g. RISK_ON). If omitted, response reports the current regime only.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_regime_changed"
}
