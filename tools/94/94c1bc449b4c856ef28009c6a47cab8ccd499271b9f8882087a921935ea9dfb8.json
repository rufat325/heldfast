{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the IP addresses assigned to a dedicated server together with their current reverse DNS (PTR). Feed one of these ip values into `impreza_dedicated_set_rdns`. Read-only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "service_id": {
    "description": "Service id from impreza_list_dedicated.",
    "type": "number"
   }
  },
  "required": [
   "service_id"
  ],
  "type": "object"
 },
 "name": "impreza_dedicated_ips"
}
