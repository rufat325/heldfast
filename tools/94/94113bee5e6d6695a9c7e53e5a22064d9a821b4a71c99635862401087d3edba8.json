{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get per-embedder agent manifest"
 },
 "description": "Wrap the public ``/embed/<slug>/manifest.json`` \u2014 an agent-discoverable JSON bundle of creative formats, voice options, payout config, and beacon URLs for a registered embedder. Use after `register_embedder` or when an agent is reading another partner's manifest to plan an ad buy. Public \u2014 no auth required.",
 "inputSchema": {
  "properties": {
   "slug": {
    "description": "Embedder slug (e.g. 'theflatlay', 'highsnobiety').",
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "get_embedder_manifest",
 "title": "Get per-embedder agent manifest"
}
