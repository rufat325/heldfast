{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search African heads of state and government \u2014 current and historical",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "country_code": {
    "description": "Filter by country (ISO alpha-2)",
    "type": "string"
   },
   "current_only": {
    "description": "Only current leaders",
    "type": "boolean"
   },
   "limit": {
    "default": 100,
    "description": "Max results",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "description": "Search by name",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "search_leaders"
}
