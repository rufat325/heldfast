{
 "description": "Get or set the security scanning mode for memory content. Modes: \"permissive\" (default), \"strict\" (PII redaction), \"off\" (no scanning).",
 "inputSchema": {
  "properties": {
   "mode": {
    "description": "Security mode to set (omit to get current)",
    "enum": [
     "permissive",
     "strict",
     "off"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "velixar_security"
}
