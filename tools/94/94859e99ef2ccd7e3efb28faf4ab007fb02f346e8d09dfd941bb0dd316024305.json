{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "New supplement product labels entered into NIH's Dietary Supplement Label Database (DSLD) since a given date \u2014 the one NIH supplement upstream that publishes a queryable change signal. Returns each new label's id, brand, product name, and entry date, newest first; deterministic on replay. Scope is stated, not guessed: DSLD does not publish off-market (withdrawal) transitions as dated events, and neither ODS fact sheets nor NCCIH herb pages publish a queryable list of content-update dates \u2014 the response says so explicitly instead of pretending to cover them. Each supplement_factsheet response carries its own 'reviewed' date for direct sweeping.",
 "inputSchema": {
  "examples": [
   {
    "limit": 20,
    "since": "2025-09-01"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max new labels to return (default 50, max 200). If more entered since the cutoff, the response sets truncated: true.",
    "type": "number"
   },
   "since": {
    "description": "Cutoff date, YYYY-MM-DD. Labels entered into DSLD on or after this date are returned.",
    "type": "string"
   }
  },
  "required": [
   "since"
  ],
  "type": "object"
 },
 "name": "supplement_recent_changes",
 "title": "Supplement Recent Changes",
 "version": "v1.b163b159"
}
