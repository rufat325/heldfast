{
 "_meta": {},
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List customer reviews"
 },
 "description": "List App Store customer reviews for an app store version (rating, title, body, response)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "appStoreVersionId": {
    "description": "App Store version ID (from list-app-store-versions or get-app with include appStoreVersions)",
    "type": "string"
   },
   "exists.publishedResponse": {
    "description": "Filter by whether a developer response exists",
    "type": "boolean"
   },
   "filter.rating": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by rating(s), e.g. 1, 2, 3, 4, 5"
   },
   "limit": {
    "default": 50,
    "description": "Maximum number of reviews to return (default 50, max 200)",
    "type": "number"
   },
   "sort": {
    "anyOf": [
     {
      "enum": [
       "rating",
       "-rating",
       "createdDate",
       "-createdDate"
      ],
      "type": "string"
     },
     {
      "items": {
       "enum": [
        "rating",
        "-rating",
        "createdDate",
        "-createdDate"
       ],
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Sort by rating or createdDate (prefix - for descending)"
   }
  },
  "required": [
   "appStoreVersionId"
  ],
  "type": "object"
 },
 "name": "list-customer-reviews",
 "title": "List customer reviews"
}
