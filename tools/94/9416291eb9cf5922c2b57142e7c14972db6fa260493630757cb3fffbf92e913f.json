{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Latest (or last N) Norges Bank exchange rate for a currency against the Norwegian krone (NOK). The value is how many NOK one unit of the base currency buys (e.g. USD \u2192 ~9.25 NOK). Builds the EXR series key B.{CURRENCY}.NOK.SP (business-day spot). currency is the ISO 4217 code, e.g. \"USD\", \"EUR\", \"GBP\", \"SEK\", \"JPY\".",
 "inputSchema": {
  "examples": [
   {
    "currency": "USD"
   },
   {
    "currency": "EUR",
    "last_n": 5
   },
   {
    "currency": "GBP",
    "end_period": "2024-12-31",
    "start_period": "2024-01-01"
   }
  ],
  "properties": {
   "currency": {
    "description": "ISO 4217 base currency code (USD, EUR, GBP, SEK, ...).",
    "type": "string"
   },
   "end_period": {
    "description": "End date (YYYY-MM-DD).",
    "type": "string"
   },
   "last_n": {
    "description": "Return only the last N observations (default 1).",
    "type": "number"
   },
   "start_period": {
    "description": "Start date (YYYY-MM-DD). Overrides last_n if set.",
    "type": "string"
   }
  },
  "required": [
   "currency"
  ],
  "type": "object"
 },
 "name": "get_exchange_rate",
 "title": "Get Exchange Rate",
 "version": "v1.29234c97"
}
