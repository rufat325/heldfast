{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Attach a tag"
 },
 "description": "Attach a tag. Attach by name. The tag is created if it does not exist. Re-attaching is a no-op.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Numeric id.",
    "type": "integer"
   },
   "name": {
    "maxLength": 50,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "id",
   "name"
  ],
  "type": "object"
 },
 "name": "tag_contact"
}
