{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Check what sourced data Streaming Lens holds on a company, market, country or topic \u2014 WITHOUT returning the values. Returns how many datapoints match, which reports they sit in, which countries and years they cover, the confidence mix, when the most recent one was updated, and one sample datapoint with its metric name, unit, country, source and confidence but its value withheld. Use this to tell a user whether the answer to their question exists in the database and what it would take to read it. Matching is word-bounded, so short acronyms do not match inside longer words. Free access \u2014 no API key required. The values themselves are behind the paid MCP server; the response says where.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "country": {
    "description": "Restrict to one country as spelled in the data (e.g. \"Nigeria\").",
    "type": "string"
   },
   "query": {
    "description": "Company, market, country, platform or topic (e.g. \"ReelShort\", \"mobile money\", \"Nigeria\", \"micro-drama\").",
    "maxLength": 60,
    "minLength": 3,
    "type": "string"
   },
   "report_slug": {
    "description": "Restrict to one report silo (e.g. \"vertical-invasion-2026\", \"africa-streaming-2026\").",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "check_data_coverage",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Tool-specific result object. Inspect the `content` array for human-readable text and structured data.",
  "type": "object"
 }
}
