{
 "description": "Creates a pipeline in the tenant CRM. Required: name. Needs crm:write on the organization. Returns the created resource with its absolute url.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "name": {
    "description": "name",
    "type": "string"
   },
   "organization_id": {
    "description": "organization_id",
    "type": "string"
   },
   "stages": {
    "description": "Stage names to create with the pipeline, in order.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "organization_id",
   "name"
  ],
  "type": "object"
 },
 "name": "commsharbor_crm_pipelines_create"
}
