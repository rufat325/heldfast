{
 "description": "Approve a pending proposal. Synchronously applies the change to Valkey and returns the terminal status (applied|failed). Idempotent: a second call on an already-applied proposal returns the cached result.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "actor": {
    "description": "Optional actor identity stamped into the audit trail",
    "minLength": 1,
    "type": "string"
   },
   "proposal_id": {
    "description": "Proposal id",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "proposal_id"
  ],
  "type": "object"
 },
 "name": "cache_approve_proposal"
}
