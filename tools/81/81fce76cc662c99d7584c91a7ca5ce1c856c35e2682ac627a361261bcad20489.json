{
 "description": "List products from Omie ERP",
 "inputSchema": {
  "properties": {
   "apenas_importado_api": {
    "description": "Only API-imported products",
    "enum": [
     "S",
     "N"
    ],
    "type": "string"
   },
   "pagina": {
    "description": "Page number (default 1)",
    "type": "number"
   },
   "registros_por_pagina": {
    "description": "Records per page (default 50)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list_products"
}
