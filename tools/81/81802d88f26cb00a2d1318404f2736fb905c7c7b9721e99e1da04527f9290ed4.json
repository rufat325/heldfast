{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-copilot-agent-registration"
 },
 "description": "Navigation property to access agent registrations at /copilot/agentRegistrations.\n\nTIP: Gets a specific Copilot agent registration by ID. Returns full agent card manifest (endpoint URL, capabilities, scopes), createdBy, dates. Use to audit a specific agent's configuration when investigating data access patterns or compliance with internal AI usage policies. Beta-only endpoint.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "agentRegistrationId": {
    "description": "Path parameter: agentRegistrationId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "required": [
   "agentRegistrationId"
  ],
  "type": "object"
 },
 "name": "get-copilot-agent-registration"
}
