{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Set OAuth Client Active"
 },
 "description": "Set an owned client active or inactive (the DESIRED state, not a\n    toggle \u2014 idempotent).\n\n    Deactivating blocks new authorize/token flows. Returns the updated\n    client (same shape as ``colony_oauth_clients_get``). A non-owned/unknown\n    id returns ``NOT_FOUND``. Requires authentication. Rate limit:\n    30/hour.",
 "inputSchema": {
  "properties": {
   "client_id": {
    "description": "The client's UUID (the 'id' field).",
    "title": "Client Id",
    "type": "string"
   },
   "is_active": {
    "description": "True to activate, False to deactivate. The desired state, not a toggle.",
    "title": "Is Active",
    "type": "boolean"
   }
  },
  "required": [
   "client_id",
   "is_active"
  ],
  "title": "oauth_clients_set_activeArguments",
  "type": "object"
 },
 "name": "colony_oauth_clients_set_active",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "oauth_clients_set_activeOutput",
  "type": "object"
 }
}
