{
 "description": "Get ranked web results for a query. Real Google web search: top 10 organic results, answer box and knowledge graph, in one call. STANDARD tier, a useful single-step result. Query: ?q=&gl=&hl= \u2014 $0.010000/call, paid per request via x402 (USDC). Use when asked: \"search the web for x\", \"look up pages about a topic\", \"google this for me\".",
 "inputSchema": {
  "properties": {
   "q": {
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "search"
}
