{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Given a list of versions, which ones satisfy a SemVer range (^, ~, >=, <, x, \u2026).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "range": {
    "description": "e.g. \"^1.2.0\"",
    "type": "string"
   },
   "versions": {
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "range",
   "versions"
  ],
  "type": "object"
 },
 "name": "semver_satisfies"
}
