{
 "description": "Generate the individual tax micro-account (for PIT/CIT/VAT payments) from a NIP. The server does not accept PESEL (personal ID) \u2014 for a PESEL compute it locally: BBAN = \"101000712221\" + PESEL + \"0\", IBAN = \"PL\" + ISO 7064 mod-97 check digits + BBAN (or use skanfirmy.pl/mikrorachunek, which computes it in the browser).",
 "inputSchema": {
  "properties": {
   "identyfikator": {
    "description": "NIP (10 digits). PESEL is not accepted by the server.",
    "type": "string"
   }
  },
  "required": [
   "identyfikator"
  ],
  "type": "object"
 },
 "name": "generuj_mikrorachunek"
}
