{
 "description": "Live market state for a token: price, market cap, liquidity, volume, buys-vs-sells flow, and momentum read (surging / steady / bleeding out). Works for bonding-curve and graduated tokens. Read-only, not financial advice.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "tokenAddress": {
    "description": "Base58 mint address of the token",
    "type": "string"
   }
  },
  "required": [
   "tokenAddress"
  ],
  "type": "object"
 },
 "name": "rigel_market_snapshot",
 "title": "Market snapshot"
}
