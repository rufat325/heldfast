{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Pull data from a table. id is the numeric SSB table id; body is a PxWeb query object. Omitted dimensions with elimination collapse; otherwise select values per dimension.",
 "inputSchema": {
  "examples": [
   {
    "body": {
     "query": [
      {
       "code": "Region",
       "selection": {
        "filter": "item",
        "values": [
         "0301"
        ]
       }
      }
     ],
     "response": {
      "format": "json-stat2"
     }
    },
    "id": "07459"
   }
  ],
  "properties": {
   "body": {
    "description": "{query: [{code, selection: {filter: \"item\", values: [...]}}], response: {format: \"json-stat2\"}}",
    "type": "object"
   },
   "id": {
    "description": "Numeric table id, e.g. \"07459\".",
    "type": "string"
   }
  },
  "required": [
   "id",
   "body"
  ],
  "type": "object"
 },
 "name": "query_table",
 "title": "Query Table",
 "version": "v1.de40dca3"
}
