{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Detailed model info \u2014 config, tags, downloads, files at root.",
 "inputSchema": {
  "examples": [
   {
    "repo_id": "bert-base-uncased"
   },
   {
    "repo_id": "meta-llama/Llama-2-7b",
    "revision": "main"
   }
  ],
  "properties": {
   "repo_id": {
    "description": "<author>/<repo> or <repo> for HF-owned",
    "type": "string"
   },
   "revision": {
    "description": "Branch/commit/tag (default main)",
    "type": "string"
   }
  },
  "required": [
   "repo_id"
  ],
  "type": "object"
 },
 "name": "get_model",
 "outputSchema": {
  "description": "Detailed model repository information including config, tags, downloads",
  "type": "object"
 },
 "title": "Get Model",
 "version": "v1.e92d5bad"
}
