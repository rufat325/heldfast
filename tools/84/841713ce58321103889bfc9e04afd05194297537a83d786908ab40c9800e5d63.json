{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Compare current readings across multiple location IDs (default: AirGradient public sensors).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "locationIds": {
    "description": "Two to ten provider-specific location identifiers.",
    "items": {
     "type": "string"
    },
    "maxItems": 10,
    "minItems": 2,
    "type": "array"
   },
   "privacy_mode": {
    "description": "Optional privacy mode: summary | structured | raw. summary omits location identifiers and device serials when present; structured/raw return full payload.",
    "enum": [
     "summary",
     "structured",
     "raw"
    ],
    "type": "string"
   }
  },
  "required": [
   "locationIds"
  ],
  "type": "object"
 },
 "name": "air_compare_locations",
 "title": "Air compare locations"
}
