{
 "description": "Manage relationships: add/remove skills on agents (one at a time, or many in one call with apply_bulk/remove_bulk + skillIds), add/remove inline tags on files, and share/unshare a private workflow with an individual workspace member. For type=tag this EDITS THE FILE CONTENT \u2014 it writes (or deletes) a #<value> token in the file body, which is the source of truth for a file's tags; the tag is auto-created if new. (Equivalent to updating the file content yourself.) For type=workflow_member, add is an upsert (re-adding an existing member updates their role).",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Action. apply_bulk/remove_bulk: tag multiple files at once (type=tag, max 100), or add/remove multiple skills on one agent (type=agent_skill, max 100).",
    "enum": [
     "add",
     "remove",
     "apply_bulk",
     "remove_bulk"
    ],
    "type": "string"
   },
   "agentId": {
    "description": "Agent ID (type=agent_skill)",
    "type": "string"
   },
   "fileId": {
    "description": "Single file ID (type=tag, action=add or remove)",
    "type": "string"
   },
   "fileIds": {
    "description": "Multiple file IDs (type=tag, action=apply_bulk or remove_bulk, max 100)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "role": {
    "description": "Access level for the shared member (type=workflow_member, action=add). Defaults to viewer.",
    "enum": [
     "viewer",
     "editor"
    ],
    "type": "string"
   },
   "skillId": {
    "description": "Single skill ID (type=agent_skill, action=add or remove)",
    "type": "string"
   },
   "skillIds": {
    "description": "Multiple skill IDs to add/remove on one agent (type=agent_skill, action=apply_bulk or remove_bulk, max 100)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "type": {
    "description": "Relationship type.",
    "enum": [
     "tag",
     "agent_skill",
     "workflow_member"
    ],
    "type": "string"
   },
   "userId": {
    "description": "Target workspace member's user ID to share the workflow with (type=workflow_member)",
    "type": "string"
   },
   "value": {
    "description": "Tag value to add/remove (type=tag), e.g. \"urgent\". Lowercase letters, digits and hyphens; written into the file content as #<value>.",
    "type": "string"
   },
   "workflowId": {
    "description": "Workflow ID (type=workflow_member)",
    "type": "string"
   },
   "workspaceId": {
    "type": "string"
   }
  },
  "required": [
   "type",
   "action"
  ],
  "type": "object"
 },
 "name": "manage_relationship"
}
