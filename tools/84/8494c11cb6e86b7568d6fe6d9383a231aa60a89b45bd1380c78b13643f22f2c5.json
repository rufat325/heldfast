{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "readOnlyHint": false
 },
 "description": "Remove a Bear note from the context library by removing its #context tag. Triggers a sync to delete the local file.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Note ID (uniqueIdentifier)",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "bear_context_remove"
}
