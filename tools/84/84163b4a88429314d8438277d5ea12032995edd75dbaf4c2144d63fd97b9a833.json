{
 "description": "Suggested reply grounded in THAT listing's fields, house rules, amenityId values, and calendar prices. Never invent rates (use calendar or say unknown). Never include wifi or door codes. Never POSTs a message \u2014 send is always false and there is no send endpoint.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "conversationId": {
    "type": "integer"
   },
   "question": {
    "description": "Optional focus question; defaults to the latest incoming message",
    "type": "string"
   }
  },
  "required": [
   "conversationId"
  ],
  "type": "object"
 },
 "name": "draft_reply"
}
