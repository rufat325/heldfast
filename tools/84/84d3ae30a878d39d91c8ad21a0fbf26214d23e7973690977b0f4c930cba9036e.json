{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get a product's full release/support timeline \u2014 every cycle with release date, EOL date, active-support end, latest patch, and LTS status. Use for \"is Node 18 still supported?\" / \"what's the latest Ubuntu LTS?\". `product` is a slug from list_products, e.g. \"python\", \"nodejs\", \"ubuntu\", \"postgresql\", \"kubernetes\". Keyless.",
 "inputSchema": {
  "examples": [
   {
    "product": "python"
   },
   {
    "product": "ubuntu"
   }
  ],
  "properties": {
   "product": {
    "description": "Product slug from list_products, e.g. \"python\", \"nodejs\", \"ubuntu\", \"postgresql\", \"kubernetes\".",
    "type": "string"
   }
  },
  "required": [
   "product"
  ],
  "type": "object"
 },
 "name": "get_product",
 "title": "Get Product",
 "version": "v1.83feb44b"
}
