{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Watch a web page or endpoint over HTTP/HTTPS and treat an unexpected status code, a timeout or a connection failure as downtime. This is the right type for anything a browser would open. Choose create_api_monitor instead when the response body matters as well as the status code. Checks run from probes in several countries. Call list_monitors first so you do not create a duplicate of an existing url.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "frequency": {
    "description": "Check frequency in minutes (1-1440, default 5)",
    "maximum": 1440,
    "minimum": 1,
    "type": "integer"
   },
   "isActive": {
    "description": "Start monitoring immediately (default true)",
    "type": "boolean"
   },
   "name": {
    "description": "Label shown in the dashboard and in alerts, up to 50 characters. Something recognisable months later beats the bare hostname.",
    "maxLength": 50,
    "type": "string"
   },
   "requestTimeout": {
    "description": "Request timeout in seconds (1-60, default 30)",
    "maximum": 60,
    "minimum": 1,
    "type": "integer"
   },
   "url": {
    "description": "Full URL including scheme, for example https://example.com/health. A bare hostname is not accepted here - use create_ping_monitor for that.",
    "type": "string"
   }
  },
  "required": [
   "name",
   "url"
  ],
  "type": "object"
 },
 "name": "create_http_monitor",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "frequency": {
    "description": "Check interval in minutes.",
    "type": "integer"
   },
   "id": {
    "description": "Monitor id, used by every other tool.",
    "type": "integer"
   },
   "isActive": {
    "description": "False while the monitor is paused.",
    "type": "boolean"
   },
   "name": {
    "type": "string"
   },
   "statusSummary": {
    "description": "Current state, for example GOOD or ERROR.",
    "type": "string"
   },
   "type": {
    "enum": [
     "PING",
     "HTTP",
     "API",
     "SSL",
     "DOMAIN",
     "PORT"
    ],
    "type": "string"
   },
   "url": {
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "title": "Create HTTP monitor"
}
