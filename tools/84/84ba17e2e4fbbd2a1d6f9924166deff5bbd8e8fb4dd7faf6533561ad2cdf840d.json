{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "AI mention sentiment breakdown"
 },
 "description": "Positive/neutral/negative sentiment split of the brand's AI mentions over a date window, per AI engine and per competitor. Use for tone/sentiment questions. For the weighted mention-type counts behind share-of-voice use get_mention_mix; to read the actual mention texts use get_mention_samples. Dates must fall within the data retention window. Answers questions like \"is anything negative being said about my brand\" or \"how positive is my coverage\".",
 "inputSchema": {
  "properties": {
   "countries": {
    "default": [],
    "description": "ISO-3166 alpha-2 country codes (e.g. \"US\", \"GB\", \"DE\"); a project's configured codes are listed by get_available_filters",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "dateFrom": {
    "type": "string"
   },
   "dateTo": {
    "type": "string"
   },
   "engines": {
    "default": [],
    "description": "allowed values: chatgpt, perplexity, google_ai_overview, google_ai_mode, google_serp, google_shopping",
    "items": {
     "enum": [
      "chatgpt",
      "perplexity",
      "google_ai_overview",
      "google_ai_mode",
      "google_serp",
      "google_shopping"
     ],
     "type": "string"
    },
    "type": "array"
   },
   "includeUngroupedQueries": {
    "default": false,
    "type": "boolean"
   },
   "organizationId": {
    "type": "string"
   },
   "projectId": {
    "type": "string"
   },
   "queryClusterIds": {
    "default": [],
    "description": "restrict to these keyword clusters",
    "items": {
     "format": "uuid",
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "organizationId",
   "projectId",
   "dateFrom",
   "dateTo"
  ],
  "type": "object"
 },
 "name": "get_sentiment_breakdown",
 "title": "AI mention sentiment breakdown"
}
