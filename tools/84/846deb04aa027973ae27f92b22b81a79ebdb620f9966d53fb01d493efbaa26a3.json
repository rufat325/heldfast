{
 "description": "Set a spend cap on your wallet. Once the daily or monthly USDC limit is reached, further paid API calls return 429 before payment is attempted. Set limits to null to remove a cap.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "Your 0x wallet address",
    "type": "string"
   },
   "daily_limit_usd": {
    "description": "Max daily spend in USD (null = unlimited)",
    "type": [
     "number",
     "null"
    ]
   },
   "label": {
    "description": "Friendly label (e.g. 'Claude Desktop')",
    "type": [
     "string",
     "null"
    ]
   },
   "monthly_limit_usd": {
    "description": "Max monthly spend in USD (null = unlimited)",
    "type": [
     "number",
     "null"
    ]
   }
  },
  "required": [
   "address",
   "daily_limit_usd",
   "monthly_limit_usd"
  ],
  "type": "object"
 },
 "name": "wallet_set_cap"
}
