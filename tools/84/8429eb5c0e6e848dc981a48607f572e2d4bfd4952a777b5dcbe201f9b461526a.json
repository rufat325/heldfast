{
 "description": "Get a user's current positions from the Slimmed P&L subgraph. Falls back gracefully if indexers are behind.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "account": {
    "description": "Ethereum address of the user (lowercase)",
    "type": "string"
   }
  },
  "required": [
   "account"
  ],
  "type": "object"
 },
 "name": "get_user_positions"
}
