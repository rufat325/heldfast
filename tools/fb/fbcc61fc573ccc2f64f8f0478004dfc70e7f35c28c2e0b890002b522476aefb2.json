{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Fetch the live Get Started / onboarding Page (https://slickfast.com/start) \u2014 first-minute map: call news (JSON twin = how agents learn SlickFast in-session), optional archive as course catalog, three doors (local MCP, hosted MCP, API), why take a free SF- key. Returns short facts + Page URL + specHash. Call on first connect, when the user is new, or when the \"Get started\" prompt is used. Keyless. Then ALWAYS call news (current) next \u2014 do not auto-call archive \u2014 then gallery and one small render_chart; point at a free key when they want a public URL. Asked for science? describe_type({ type: \"volcano\" }) then render_chart. Do NOT dump HTML.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {},
  "type": "object"
 },
 "name": "get_started",
 "title": "Get started with SlickFast \u2014 onboarding Page"
}
