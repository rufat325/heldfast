{
 "description": "Manage site-wide tasks \u2014 assignable, completable work items that are lighter-weight than a Life project/outcome and simpler than a Kanon checklist item. Reach for mind_tasks for a single actionable to-do (optionally attached to a Life project via parent_type=life_item, a CRM contact via parent_type=contact, or an agent) that needs an owner and a done/not-done state; reach for mind_life when the work is itself a deliverable inside the Focus\u2192Project\u2192Outcome hierarchy, and mind_checklists when you need a multi-item checklist with a progress rollup. Assign tasks to a MIND member, an agent, or an external email. Actions: list/create/get/update/complete/reopen/assign/delete, reports (completion analytics). An empty list means no tasks exist under that filter, not that no work is happening \u2014 check mind_life and mind_checklists for the same window before concluding nothing is tracked.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "action": {
    "description": "What to do: list/create/get/update/complete/reopen/assign/delete tasks, or reports (completion analytics)",
    "enum": [
     "list",
     "create",
     "get",
     "update",
     "complete",
     "reopen",
     "assign",
     "delete",
     "reports"
    ],
    "type": "string"
   },
   "assignee_id": {
    "description": "Username, agent slug, or email of the assignee",
    "type": "string"
   },
   "assignee_type": {
    "description": "Assignee: user, agent, external, or unassigned",
    "type": "string"
   },
   "description": {
    "description": "Task description",
    "type": "string"
   },
   "dispatch_agent": {
    "description": "When assigning to an agent, also fire it now",
    "type": "boolean"
   },
   "due_date": {
    "description": "Due date YYYY-MM-DD",
    "type": "string"
   },
   "note": {
    "description": "Completion note (for complete)",
    "type": "string"
   },
   "overdue": {
    "description": "List only overdue tasks",
    "type": "boolean"
   },
   "parent_id": {
    "description": "ID of the parent project / contact / agent",
    "type": "string"
   },
   "parent_type": {
    "description": "Attach to: life_item, contact, agent, or none",
    "type": "string"
   },
   "priority": {
    "description": "Priority: none, low, medium, high, urgent",
    "type": "string"
   },
   "status": {
    "description": "Filter (list) or new status: open, in_progress, blocked, done",
    "type": "string"
   },
   "task_id": {
    "description": "Task ID \u2014 required for get/update/complete/reopen/assign/delete",
    "type": "string"
   },
   "title": {
    "description": "Task title (required for create)",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "mind_tasks"
}
