{
 "description": "Get the autocomplete terms TikTok suggests in its search bar for a keyword \u2014 the real phrases people search, ranked, so you can find trending queries and long-tail keyword ideas. Costs 2 credits. Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cache": {
    "description": "Set true to serve from the 24h response cache (0 credits on hit). Default false \u2014 always fetch fresh. Envelope includes cached + cachedAt on hits.",
    "type": "boolean"
   },
   "country": {
    "description": "Two-letter ISO country code that localizes the suggestions to a market, e.g. US, GB, DE. Default US.",
    "type": "string"
   },
   "language": {
    "description": "Interface language for the suggestions, e.g. en-US or de-DE. Default en-US.",
    "type": "string"
   },
   "limit": {
    "description": "Max items to return (default 20, max 100). Flat 2 credits per call.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "q": {
    "description": "Seed keyword to expand into autocomplete suggestions, e.g. skincare.",
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "tiktok_search_suggestions",
 "title": "TikTok Search Suggestions"
}
