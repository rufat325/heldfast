{
 "description": "Apply only user-approved Experience operations from a current preview.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "approvedOperationIds": {
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "idempotencyKey": {
    "minLength": 1,
    "type": "string"
   },
   "previewHash": {
    "minLength": 1,
    "type": "string"
   },
   "previewToken": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "previewToken",
   "previewHash",
   "approvedOperationIds",
   "idempotencyKey"
  ],
  "type": "object"
 },
 "name": "granoflow_experience_authoring_apply"
}
