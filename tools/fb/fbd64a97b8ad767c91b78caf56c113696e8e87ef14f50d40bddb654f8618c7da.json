{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get ground surface elevation for a single latitude/longitude coordinate from the USGS 3D Elevation Program (3DEP) \u2014 the authoritative US Government digital elevation model, with resolution as fine as 1 meter in many areas. Returns elevation in meters or feet, the source raster resolution, and optionally the DEM acquisition date. Continental US + territories only (no ocean or international coverage). Data: epqs.nationalmap.gov (USGS 3D Elevation Program), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "include_date": {
    "description": "Include the source DEM raster acquisition date in the response (default false).",
    "type": "boolean"
   },
   "latitude": {
    "description": "Latitude in decimal degrees, WGS84 (e.g. 39.7147). Continental US + territories only.",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "longitude": {
    "description": "Longitude in decimal degrees, WGS84 (e.g. -105.0178).",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "units": {
    "description": "Elevation output units, \"Meters\" or \"Feet\" (default \"Meters\").",
    "enum": [
     "Meters",
     "Feet"
    ],
    "type": "string"
   }
  },
  "required": [
   "latitude",
   "longitude"
  ],
  "type": "object"
 },
 "name": "usgs-epqs.elevation.point",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "USGS Elevation Point Query"
}
