{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Estimate App Revenue"
 },
 "description": "Estimate monthly revenue for an app, based on install counts, ratings, and category benchmarks. Returns the dollar estimate, a confidence grade (high/medium/low) with the factors behind it, and the methodology used \u2014 always communicate the confidence alongside the number.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "country": {
    "default": "us",
    "description": "ISO 3166-1 alpha-2 country code (e.g. \"us\", \"gb\", \"de\"). Default \"us\".",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   },
   "store": {
    "description": "App store. \"ios\" for Apple App Store, \"android\" for Google Play.",
    "enum": [
     "ios",
     "android"
    ],
    "type": "string"
   },
   "store_id": {
    "description": "Store-specific app identifier. iOS: numeric track ID. Android: package name.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "store",
   "store_id"
  ],
  "type": "object"
 },
 "name": "sonar_app_revenue",
 "title": "Estimate App Revenue"
}
