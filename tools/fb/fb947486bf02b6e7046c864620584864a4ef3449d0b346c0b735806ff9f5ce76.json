{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search for albums and releases by title or artist name. Returns release IDs, titles, artists, release dates, and formats.",
 "inputSchema": {
  "examples": [
   {
    "query": "Abbey Road"
   },
   {
    "limit": 15,
    "query": "Pink Floyd The Wall"
   }
  ],
  "properties": {
   "limit": {
    "description": "Maximum number of results to return. Defaults to 10.",
    "type": "number"
   },
   "query": {
    "description": "Release title or search query.",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_releases",
 "outputSchema": {
  "properties": {
   "releases": {
    "description": "List of matching releases",
    "items": {
     "properties": {
      "artist_credit": {
       "description": "Artist credits for release",
       "items": {
        "properties": {
         "artist_id": {
          "description": "MusicBrainz artist ID",
          "type": [
           "string",
           "null"
          ]
         },
         "name": {
          "description": "Artist name as credited",
          "type": [
           "string",
           "null"
          ]
         }
        },
        "type": "object"
       },
       "type": "array"
      },
      "date": {
       "description": "Release date (YYYY-MM-DD)",
       "type": [
        "string",
        "null"
       ]
      },
      "id": {
       "description": "MusicBrainz release ID",
       "type": "string"
      },
      "score": {
       "description": "Search result relevance score",
       "type": [
        "number",
        "null"
       ]
      },
      "status": {
       "description": "Release status",
       "type": [
        "string",
        "null"
       ]
      },
      "title": {
       "description": "Release title",
       "type": "string"
      }
     },
     "required": [
      "id",
      "title"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "total": {
    "description": "Total number of matching releases",
    "type": "number"
   }
  },
  "required": [
   "total",
   "releases"
  ],
  "type": "object"
 },
 "title": "Search Releases",
 "version": "v1.6167106a"
}
