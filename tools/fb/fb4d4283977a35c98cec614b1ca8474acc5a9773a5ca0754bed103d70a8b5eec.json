{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Check Space Availability"
 },
 "description": "Check whether a venue Space is free for a specific slot: available (no confirmed overlap), every overlapping booking labeled hard (taken) or soft (pending request \u2014 first approval wins), and a soft listed-hours verdict, with local-time ranges. Requires community_id + space_id + ISO start_time/end_time. Read-only.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "community_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "end_time": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "space_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "start_time": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "community_id",
   "space_id",
   "start_time",
   "end_time"
  ],
  "type": "object"
 },
 "name": "check_space_availability",
 "title": "Check Space Availability"
}
