{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Update which access protocols (SSH, Samba, WebDAV, ZFS, external reachability) are enabled on a Storage Box.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "id": {
    "description": "Storage Box ID",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "reachable_externally": {
    "description": "Whether the Storage Box is reachable from outside the Hetzner network",
    "type": "boolean"
   },
   "samba_enabled": {
    "description": "Whether Samba/CIFS access is enabled",
    "type": "boolean"
   },
   "ssh_enabled": {
    "description": "Whether SSH/SFTP/SCP access is enabled",
    "type": "boolean"
   },
   "webdav_enabled": {
    "description": "Whether WebDAV access is enabled",
    "type": "boolean"
   },
   "zfs_enabled": {
    "description": "Whether the ZFS snapshot directory is exposed",
    "type": "boolean"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "hetzner_update_storage_box_access_settings",
 "title": "Update Storage Box Access Settings"
}
