{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Get one virtual host by id, including its document root and domain configuration.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "hostingId": {
    "description": "Hosting id or uuid, as returned by ws_hosting_list.",
    "minLength": 1,
    "type": "string"
   },
   "userId": {
    "default": "self",
    "description": "Account id, or the literal \"self\" for the authenticated account. Defaults to \"self\".",
    "minLength": 1,
    "type": "string"
   },
   "vhostId": {
    "description": "Virtual host id.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "hostingId",
   "vhostId"
  ],
  "type": "object"
 },
 "name": "ws_hosting_vhost_get",
 "title": "Get virtual host"
}
