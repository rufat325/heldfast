{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Bayesian Belief Update (Beta posterior)"
 },
 "description": "Update a prior probability with weighted evidence signals using a Beta posterior (the prior seeds Beta(prior*10, (1-prior)*10)). Use for incremental belief revision: start from a baseline probability and fold in signals, each a value in [0,1] with a weight, to get a revised posterior. Returns the updated posterior, the prior, per-factor contributions, posterior mean and variance, and a sharpness/calibration score. To combine N independent point predictions use predict_ensemble; to sample a full distribution use simulate_montecarlo.",
 "inputSchema": {
  "properties": {
   "evidence": {
    "description": "Pieces of evidence to fold in.",
    "items": {
     "properties": {
      "factor": {
       "description": "Identifier / label for this signal.",
       "type": "string"
      },
      "value": {
       "description": "Signal value in [0,1].",
       "maximum": 1,
       "minimum": 0,
       "type": "number"
      },
      "weight": {
       "description": "How heavily this signal counts.",
       "type": "number"
      }
     },
     "required": [
      "factor",
      "weight",
      "value"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "prior": {
    "description": "Prior probability of the event (0..1). Used to seed Beta(prior*10, (1-prior)*10).",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   }
  },
  "required": [
   "prior",
   "evidence"
  ],
  "type": "object"
 },
 "name": "predict_bayesian",
 "outputSchema": {
  "properties": {
   "calibrationScore": {
    "description": "1 - sqrt(variance); higher = sharper posterior.",
    "type": "number"
   },
   "factors": {
    "items": {
     "properties": {
      "direction": {
       "enum": [
        "positive",
        "negative"
       ],
       "type": "string"
      },
      "name": {
       "type": "string"
      },
      "value": {
       "type": "number"
      },
      "weight": {
       "type": "number"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "posterior": {
    "description": "Updated probability after folding in evidence.",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "posteriorMean": {
    "type": "number"
   },
   "posteriorVariance": {
    "type": "number"
   },
   "priorProbability": {
    "type": "number"
   }
  },
  "required": [
   "posterior",
   "posteriorMean",
   "posteriorVariance"
  ],
  "type": "object"
 }
}
