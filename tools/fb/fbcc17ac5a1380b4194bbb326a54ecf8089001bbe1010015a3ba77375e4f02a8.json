{
 "description": "Get full details of a specific event by its ID, including exception values, stack trace, breadcrumbs, tags, and request context. Use this to debug a specific occurrence.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "description": "Event ID (hex string). Get these from list_issue_events.",
    "type": "string"
   },
   "org": {
    "description": "Organization slug (e.g. 'acme')",
    "type": "string"
   },
   "project": {
    "description": "Project slug (e.g. 'web-frontend'). Get these from list_projects.",
    "type": "string"
   }
  },
  "required": [
   "org",
   "project",
   "event_id"
  ],
  "type": "object"
 },
 "name": "get_event"
}
