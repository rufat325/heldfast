{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find animal rescues and shelters with RescueGroups.org accounts: name, type (Rescue or Shelter), address, phone, email, website, adoption process, areas served and services. Filter by postal-code radius, state or name. Requires your own RescueGroups API key. Example: rescuegroups_organizations({ postalcode: \"15206\", miles: 25, limit: 5, _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-rescuegroups-api-key",
    "limit": 5,
    "miles": 25,
    "postalcode": "15206"
   },
   {
    "_apiKey": "your-rescuegroups-api-key",
    "limit": 10,
    "state": "PA",
    "type": "shelter"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Your own RescueGroups API key. Request a free public API key at https://rescuegroups.org/services/request-an-api-key/ \u2014 a short form (name, email, organisation, site URL). It is open to individual developers: \"not incorporated\" is an accepted answer and you do not need to be a rescue or shelter. RescueGroups staff review it and email the key back, so it is not instant. Then pass the key as the `_apiKey` argument.",
    "type": "string"
   },
   "city": {
    "description": "City name.",
    "type": "string"
   },
   "kilometers": {
    "description": "Radius in kilometres. Provide exactly one of `miles` or `kilometers`.",
    "type": "number"
   },
   "lat": {
    "description": "Latitude to search around. Use with `lon`.",
    "type": "number"
   },
   "limit": {
    "description": "Rows per page, 1-250. Default 25.",
    "type": "number"
   },
   "lon": {
    "description": "Longitude to search around. Use with `lat`.",
    "type": "number"
   },
   "miles": {
    "description": "Radius in miles. Provide exactly one of `miles` or `kilometers`.",
    "type": "number"
   },
   "name": {
    "description": "Match the organization name (substring).",
    "type": "string"
   },
   "org_id": {
    "description": "Fetch one organization by id instead of searching.",
    "type": "string"
   },
   "page": {
    "description": "Page of results, starting at 1.",
    "type": "number"
   },
   "postalcode": {
    "description": "US or Canadian postal/zip code to search around. Requires `miles` or `kilometers`.",
    "type": "string"
   },
   "sort": {
    "description": "Field to sort by, \"-\" prefixed for descending, e.g. \"orgs.name\". On a distance search \"distance\" sorts nearest-first.",
    "type": "string"
   },
   "state": {
    "description": "Two-letter state or province code, e.g. \"PA\".",
    "type": "string"
   },
   "type": {
    "description": "Restrict to rescues or to shelters. Omit for both.",
    "enum": [
     "rescue",
     "shelter"
    ],
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "rescuegroups_organizations",
 "title": "Rescuegroups Organizations",
 "version": "v1.e9cb3c19"
}
