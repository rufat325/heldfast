{
 "description": "Check the status of an async job (audit, research, content generation, brief outline, optimization). Specify the type for faster lookup, or omit to search all types.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "The job/resource ID to check",
    "type": "string"
   },
   "type": {
    "description": "The type of job (optional, for faster lookup)",
    "enum": [
     "audit",
     "research",
     "content",
     "brief",
     "optimization",
     "atomization"
    ],
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_job_status"
}
