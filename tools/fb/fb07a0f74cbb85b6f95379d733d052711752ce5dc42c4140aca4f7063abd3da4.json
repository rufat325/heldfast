{
 "description": "List the row-numbered errors of one import, so the source file can be fixed.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "cursor": {
    "description": "Opaque pagination cursor",
    "type": "string"
   },
   "import_id": {
    "description": "Contact import ID",
    "type": "string"
   },
   "limit": {
    "default": 50,
    "description": "Page size, maximum 100",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "import_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_contact_import_errors"
}
