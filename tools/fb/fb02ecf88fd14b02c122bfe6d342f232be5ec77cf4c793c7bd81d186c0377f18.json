{
 "description": "Get the top positions for a specific market token from the Beefy P&L subgraph. Shows who holds the largest positions and their P&L.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "first": {
    "default": 20,
    "description": "Number of positions to return",
    "maximum": 100,
    "minimum": 1,
    "type": "number"
   },
   "orderBy": {
    "default": "valueBought",
    "description": "Field to sort by",
    "enum": [
     "realizedPnl",
     "unrealizedPnl",
     "valueBought"
    ],
    "type": "string"
   },
   "tokenId": {
    "description": "The outcome token ID \u2014 same value as clobTokenIds from search_markets/get_market_info, token_id from get_clob_market, or makerAssetId/takerAssetId from get_orderbook_trades",
    "type": "string"
   }
  },
  "required": [
   "tokenId"
  ],
  "type": "object"
 },
 "name": "get_market_positions"
}
