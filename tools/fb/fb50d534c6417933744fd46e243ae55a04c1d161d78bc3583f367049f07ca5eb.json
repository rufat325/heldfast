{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List App Store versions (releases) for an app, including state and platform.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "appId": {
    "description": "App Store Connect app ID",
    "pattern": "^[A-Za-z0-9-]+$",
    "type": "string"
   },
   "appStoreState": {
    "description": "Filter by state, e.g. READY_FOR_SALE, IN_REVIEW, REJECTED",
    "type": "string"
   },
   "auto_paginate": {
    "description": "Follow links.next across pages until the limit is reached (default false).",
    "type": "boolean"
   },
   "limit": {
    "description": "Max versions to return (default 25). With auto_paginate this is the total across pages.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "platform": {
    "description": "Filter by platform",
    "enum": [
     "IOS",
     "MAC_OS",
     "TV_OS",
     "VISION_OS"
    ],
    "type": "string"
   }
  },
  "required": [
   "appId"
  ],
  "type": "object"
 },
 "name": "list_app_store_versions"
}
