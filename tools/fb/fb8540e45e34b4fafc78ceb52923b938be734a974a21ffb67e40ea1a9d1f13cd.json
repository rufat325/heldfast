{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Shared world-memory (one-write, all-see) operations: set, get, list, delete.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Operation",
    "enum": [
     "set",
     "get",
     "list",
     "delete"
    ],
    "type": "string"
   },
   "key": {
    "description": "Shared key (required for set/get/delete)",
    "type": "string"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   },
   "ttl": {
    "description": "TTL in seconds (set)",
    "type": "number"
   },
   "value": {
    "description": "Value to set (set)"
   }
  },
  "required": [
   "spaceId",
   "action"
  ],
  "type": "object"
 },
 "name": "shared_memory",
 "outputSchema": {
  "properties": {},
  "type": "object"
 }
}
