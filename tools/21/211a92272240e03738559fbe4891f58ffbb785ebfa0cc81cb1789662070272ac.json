{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Reject a reply"
 },
 "description": "Reject a reply. Drops the pending draft. Nothing is posted. Only a pending comment can be rejected (409 not_pending). Send an empty JSON object as the body.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Numeric id.",
    "type": "integer"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "reject_comment_reply"
}
