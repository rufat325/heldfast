{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List recorded runs"
 },
 "description": "List recorded runs of a saved program: when, how it ended, actual against planned length. For 'how long did this take last time?'. Needs the user's Rhylthyme account.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "program_id": {
    "description": "Program UUID from list_my_programs.",
    "type": "string"
   },
   "token": {
    "description": "Access token from the login tool. Omit when the account is connected.",
    "type": "string"
   }
  },
  "required": [
   "program_id"
  ],
  "type": "object"
 },
 "name": "list_runs",
 "title": "List recorded runs"
}
