{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch rows from a CZSO dataset's CSV data file (dataset_id from czso.dataset_list). Column names vary per dataset (e.g. \"vuzemi_txt\" for territory, \"rok\" for year) \u2014 call once without filter_column to see the header, then narrow with filter_column + filter_value (substring match). Rows capped 1-500 (default 50). Datasets whose only resource is not CSV (e.g. ZIP) are rejected with a pointer to the raw download URL. Data: vdb.czso.cz (Czech Statistical Office open-data catalog), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataset_id": {
    "description": "CZSO dataset id, from czso.dataset_list (e.g. \"130141r25\" for 2024 population movement).",
    "minLength": 1,
    "type": "string"
   },
   "filter_column": {
    "description": "Column name to filter on, must match a column in the CSV header returned by this same tool (column names vary per dataset, e.g. \"vuzemi_txt\" for territory name, \"rok\" for year, \"vuk\" for indicator code) \u2014 call once without a filter to see the header first. Requires filter_value.",
    "type": "string"
   },
   "filter_value": {
    "description": "Substring to match (case-insensitive) against filter_column's value, e.g. \"Praha\" when filter_column is \"vuzemi_txt\". Required when filter_column is set.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows to return after filtering (1-500, default 50).",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Number of matching rows to skip, for paging through a large filtered result.",
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "dataset_id"
  ],
  "type": "object"
 },
 "name": "czso.series.dataset_data",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "CZSO Dataset Data"
}
