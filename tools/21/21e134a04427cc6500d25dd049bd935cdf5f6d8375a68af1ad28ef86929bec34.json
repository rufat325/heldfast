{
 "description": "Order the catalogue by one measured axis \u2014 rhythm of change, luminance, peak brightness, colour travel, empty space, and the rest \u2014 from lowest to highest or the reverse, and read where each work sits on the catalogue's own scale. Use it for questions like \"the stillest work you have\" or \"the brightest, in this collection\". Every number comes from the master, frame by frame; nothing here is described.",
 "inputSchema": {
  "properties": {
   "availability": {
    "enum": [
     "available",
     "any"
    ],
    "type": "string"
   },
   "collection": {
    "description": "Restrict to one collection",
    "type": "string"
   },
   "count": {
    "type": "integer"
   },
   "metric": {
    "description": "The measured axis to order by",
    "enum": [
     "rhythm",
     "rhythmContrast",
     "compositionMovement",
     "luminance",
     "peakBrightness",
     "dynamicRange",
     "trueBlack",
     "colourTravel",
     "flatGround",
     "emptySpace",
     "elementScale",
     "edgeHardness",
     "radialSymmetry"
    ],
    "type": "string"
   },
   "order": {
    "description": "asc = lowest first (the stillest, the darkest); desc = highest first",
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   }
  },
  "required": [
   "metric"
  ],
  "type": "object"
 },
 "name": "rank_artworks"
}
