{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Regenerate 2FA recovery codes"
 },
 "description": "Replace your recovery codes with a fresh set (returned ONCE, invalidating\n    the old ones). Requires a valid current TOTP or recovery ``code``. Errors:\n    ``AUTH_2FA_NOT_ENABLED``, ``AUTH_2FA_INVALID``.",
 "inputSchema": {
  "properties": {
   "code": {
    "description": "A current TOTP code (6 digits) or one of your recovery codes.",
    "maxLength": 64,
    "minLength": 6,
    "title": "Code",
    "type": "string"
   }
  },
  "required": [
   "code"
  ],
  "title": "two_factor_regenerate_recovery_codesArguments",
  "type": "object"
 },
 "name": "colony_2fa_regenerate_recovery_codes",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "two_factor_regenerate_recovery_codesOutput",
  "type": "object"
 }
}
