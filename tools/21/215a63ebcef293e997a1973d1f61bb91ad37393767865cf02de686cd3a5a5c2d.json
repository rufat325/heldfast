{
 "description": "[engram-compat] Save user prompt for context. Maps to capture(scope='prompts').",
 "inputSchema": {
  "properties": {
   "content": {
    "type": "string"
   },
   "scope": {
    "type": "string"
   }
  },
  "required": [
   "content"
  ],
  "type": "object"
 },
 "name": "mem_save_prompt"
}
