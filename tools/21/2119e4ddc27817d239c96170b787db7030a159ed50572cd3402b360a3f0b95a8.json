{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get a Reddit post and its comments by post ID. Returns the post (title, author, body, permalink) and the comment threads (author, body, date). Vote scores aren't available via Reddit RSS.",
 "inputSchema": {
  "examples": [
   {
    "post_id": "abc123def456"
   }
  ],
  "properties": {
   "post_id": {
    "description": "Reddit post ID (alphanumeric, e.g. \"abc123\").",
    "type": "string"
   }
  },
  "required": [
   "post_id"
  ],
  "type": "object"
 },
 "name": "get_post",
 "outputSchema": {
  "description": "A single post with its comment thread, served from Reddit RSS.",
  "properties": {
   "comment_count": {
    "type": "number"
   },
   "comments": {
    "items": {
     "properties": {
      "author": {
       "type": "string"
      },
      "body": {
       "type": "string"
      },
      "id": {
       "type": "string"
      },
      "permalink": {
       "type": "string"
      },
      "published": {
       "description": "ISO-8601 timestamp",
       "type": "string"
      }
     },
     "required": [
      "id",
      "author",
      "body"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "ok": {
    "type": "boolean"
   },
   "post": {
    "properties": {
     "author": {
      "type": "string"
     },
     "body": {
      "description": "Post self-text, separated from the comments",
      "type": "string"
     },
     "id": {
      "type": "string"
     },
     "permalink": {
      "type": "string"
     },
     "subreddit": {
      "type": [
       "string",
       "null"
      ]
     },
     "title": {
      "type": "string"
     }
    },
    "required": [
     "id",
     "title",
     "author",
     "permalink"
    ],
    "type": "object"
   }
  },
  "required": [
   "ok",
   "post",
   "comments"
  ],
  "type": "object"
 },
 "title": "Get Post",
 "version": "v1.ad7be488"
}
