{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Show covered/uncovered line numbers of one source file from the latest coverage report (run get_coverage_report first).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "file": {
    "description": "Source file name, e.g. 'BookRepository.kt'",
    "type": "string"
   },
   "module": {
    "description": "Module (defaults to session)",
    "type": "string"
   },
   "projectDir": {
    "description": "Project root (defaults to session)",
    "type": "string"
   }
  },
  "required": [
   "file"
  ],
  "type": "object"
 },
 "name": "get_file_coverage",
 "title": "Get file coverage"
}
