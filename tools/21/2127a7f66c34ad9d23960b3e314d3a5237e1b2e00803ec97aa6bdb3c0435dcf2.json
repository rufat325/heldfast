{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch a sitemap.xml (or sitemap-index) and return the contained URLs with their lastmod / changefreq / priority. Follows sitemap-index chaining up to max_depth levels. Gzipped .xml.gz payloads are auto-decompressed. Partial failures (one child sitemap 500s while others work) are returned under 'warnings' without aborting the whole request. SSRF-protected by default.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "allow_private_hosts": {
    "description": "Allow loopback / private / link-local targets for this call (default false). Refused unless the server operator launched fetch-mcp with FETCH_MCP_ALLOW_PRIVATE_HOSTS=1 -- SSRF protection stays on by default either way.",
    "type": "boolean"
   },
   "max_bytes": {
    "description": "Max bytes to read per sitemap response (default 20MiB)",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "max_depth": {
    "description": "How many sitemap-index levels to follow (default 1). 0 keeps the top-level index flat and only returns its childSitemaps list.",
    "maximum": 3,
    "minimum": 0,
    "type": "integer"
   },
   "max_redirects": {
    "maximum": 20,
    "minimum": 0,
    "type": "integer"
   },
   "max_sitemaps": {
    "description": "Cap on sitemap documents fetched -- the index plus every child (default 50, max 1000). Children past the cap are listed under childSitemaps, unfetched.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "max_urls": {
    "description": "Cap on total URLs returned (default 5000)",
    "maximum": 50000,
    "minimum": 1,
    "type": "integer"
   },
   "timeout_ms": {
    "exclusiveMinimum": 0,
    "maximum": 60000,
    "type": "integer"
   },
   "url": {
    "description": "Sitemap URL (sitemap.xml, sitemap.xml.gz, or a sitemap index)",
    "format": "uri",
    "type": "string"
   },
   "user_agent": {
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "fetch_sitemap"
}
