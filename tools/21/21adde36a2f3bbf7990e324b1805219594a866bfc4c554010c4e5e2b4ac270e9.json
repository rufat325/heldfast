{
 "description": "Continue a previous codex conversation by thread ID. Use this for follow-up questions, iterative refinement, or multi-step workflows that build on prior context.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "cwd": {
    "description": "Working directory override",
    "type": "string"
   },
   "model": {
    "description": "Model override",
    "type": "string"
   },
   "prompt": {
    "description": "Follow-up instruction or question",
    "type": "string"
   },
   "thread_id": {
    "description": "Thread ID from a prior codex response",
    "type": "string"
   }
  },
  "required": [
   "thread_id",
   "prompt"
  ],
  "type": "object"
 },
 "name": "codex_reply"
}
