{
 "description": "Read the organization's tracking domain, the CNAME it needs and whether the platform actually answers on it. Null when there is none: links then use the shared platform origin. `null` when the organization has none \u2014 then every link falls back to the platform origin.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_tracking_domain"
}
