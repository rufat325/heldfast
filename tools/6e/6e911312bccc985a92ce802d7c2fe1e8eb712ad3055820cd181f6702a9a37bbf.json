{
 "description": "Read server files via UNION SELECT LOAD_FILE(). Requires MySQL FILE privilege. Uses LOAD_FILE() in a UNION SELECT. Returns file_content, success, target_file. Errors: FILE privilege required. Returns empty if privilege denied.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "column_count": {
    "description": "Number of columns (from previous UNION discovery)",
    "maximum": 20,
    "minimum": 1,
    "type": "number"
   },
   "parameter": {
    "description": "Vulnerable parameter name",
    "type": "string"
   },
   "string_column": {
    "description": "1-indexed column that displays strings",
    "maximum": 20,
    "minimum": 1,
    "type": "number"
   },
   "target_file": {
    "description": "Server-side file to read, e.g. /etc/passwd",
    "type": "string"
   },
   "url": {
    "description": "Full URL with injectable parameter",
    "type": "string"
   }
  },
  "required": [
   "url",
   "parameter"
  ],
  "type": "object"
 },
 "name": "sqli_file_read"
}
