{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List contributed runs"
 },
 "description": "List anonymous runs other people contributed for one exact program version: actual against planned length, and their conditions. For 'how long does this really take?'. No login needed. Pass `program` or `program_hash`.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "limit": {
    "description": "Runs to return, newest first (default 50).",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "program": {
    "additionalProperties": {},
    "description": "Program JSON to look up instead; hashed here.",
    "properties": {},
    "type": "object"
   },
   "program_hash": {
    "description": "Program hash, 'sha256:' + 64 hex (a run record's programVersion).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_public_runs",
 "title": "List contributed runs"
}
