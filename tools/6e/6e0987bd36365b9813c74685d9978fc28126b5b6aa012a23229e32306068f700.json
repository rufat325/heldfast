{
 "description": "Set read/flagged status on a message.\n[governed: scope=Mail mode=write-gated undo=native; runs as dry-run preview unless live mode is on AND a human approves out-of-band]",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "account": {
    "description": "Account name to narrow the lookup",
    "type": "string"
   },
   "flagged": {
    "description": "Set flagged status",
    "type": "boolean"
   },
   "mailbox": {
    "description": "Mailbox name to narrow the lookup",
    "type": "string"
   },
   "messageId": {
    "description": "RFC Message-ID (from mail_search results)",
    "minLength": 1,
    "type": "string"
   },
   "read": {
    "description": "Set read status",
    "type": "boolean"
   }
  },
  "required": [
   "messageId"
  ],
  "type": "object"
 },
 "name": "mail_mark"
}
