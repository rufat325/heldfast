{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Search by Rim"
 },
 "description": "Find all vehicles compatible with a given rim specification. Reverse lookup: \"which cars fit these rims?\" Required: bolt_pattern (e.g. \"5x112\"). Optional: rim_diameter, rim_width, rim_offset (or range), centre bore (cb), fastener diameter (fd), region. Supports pagination with limit and offset. Returns vehicle make/model/year/trim combinations that accept this rim fitment.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bolt_pattern": {
    "description": "Bolt pattern (PCD) expressed as stud-count \u00d7 circle-diameter in mm (e.g. \"5x112\", \"4x100\"). Required. Use list-rim-bolt-patterns to enumerate valid values.",
    "type": "string"
   },
   "cb": {
    "description": "Centre bore diameter in mm (e.g. 66.5). Must be positive. Do not combine with cb_min/cb_max.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "cb_max": {
    "description": "Maximum centre bore in mm. Must be positive. Use instead of cb for a range query.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "cb_min": {
    "description": "Minimum centre bore in mm. Must be positive. Use instead of cb for a range query.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "fd": {
    "description": "Fastener thread diameter in mm. Must be positive. Narrows results to wheels with matching lug-bolt thread size.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "limit": {
    "description": "Maximum results to return (1-100). Defaults to API default when omitted.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Number of results to skip for pagination.",
    "minimum": 0,
    "type": "integer"
   },
   "region": {
    "description": "Market region code (e.g. \"eudm\", \"usdm\"). Get codes from list-regions.",
    "type": "string"
   },
   "rim_diameter": {
    "description": "Rim diameter in inches (e.g. 18). Must be positive. Filters results to this diameter only.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "rim_diameter_max": {
    "description": "Maximum rim diameter in inches. Must be positive. Use instead of rim_diameter for a range query.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "rim_diameter_min": {
    "description": "Minimum rim diameter in inches. Must be positive. Use instead of rim_diameter for a range query.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "rim_offset": {
    "description": "Exact rim offset (ET) in mm, range -100 to 100 (e.g. 35). The distance between the wheel mounting surface and the centre-line of the rim. Do not combine with rim_offset_min/rim_offset_max.",
    "maximum": 100,
    "minimum": -100,
    "type": "number"
   },
   "rim_offset_max": {
    "description": "Maximum rim offset (ET) in mm, range -100 to 100. Use instead of rim_offset for a range query.",
    "maximum": 100,
    "minimum": -100,
    "type": "number"
   },
   "rim_offset_min": {
    "description": "Minimum rim offset (ET) in mm, range -100 to 100. Use instead of rim_offset for a range query.",
    "maximum": 100,
    "minimum": -100,
    "type": "number"
   },
   "rim_width": {
    "description": "Rim width in inches from bead seat to bead seat (e.g. 8.0). Must be positive.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "rim_width_max": {
    "description": "Maximum rim width in inches. Must be positive. Use instead of rim_width for a range query.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "rim_width_min": {
    "description": "Minimum rim width in inches. Must be positive. Use instead of rim_width for a range query.",
    "exclusiveMinimum": 0,
    "type": "number"
   }
  },
  "required": [
   "bolt_pattern"
  ],
  "type": "object"
 },
 "name": "search-by-rim"
}
