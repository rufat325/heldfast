{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Estimate weekly and monthly earnings for a Tewdy provider in a given category. Uses listed hourly rates from existing providers as the baseline and projects earnings under three utilisation scenarios (40%/60%/80%). Returns ranges, not guarantees.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "category": {
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   },
   "currency": {
    "maxLength": 3,
    "minLength": 3,
    "type": "string"
   },
   "hours_per_week": {
    "maximum": 80,
    "minimum": 1,
    "type": "number"
   },
   "location": {
    "maxLength": 200,
    "type": "string"
   }
  },
  "required": [
   "category",
   "hours_per_week"
  ],
  "type": "object"
 },
 "name": "get_earnings_estimate",
 "title": "Estimate earnings"
}
