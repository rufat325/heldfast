{
 "description": "Query a live node in the running scene tree. REQUIRES the game to be running with the MCPRuntime autoload connected. Returns class, path, valid, groups, and a map of property values. By default returns position, global_position, rotation, scale, visible, modulate \u2014 pass `properties:[\"...\"]` to override. Set include_children=true to also list direct child nodes.",
 "inputSchema": {
  "properties": {
   "include_children": {
    "description": "List direct children {name, class}. Default: false.",
    "type": "boolean"
   },
   "include_groups": {
    "description": "Include the node's group memberships. Default: true.",
    "type": "boolean"
   },
   "node_path": {
    "description": "Absolute path (e.g. /root/Main/Player) or relative to current_scene.",
    "type": "string"
   },
   "properties": {
    "description": "Property names to read. Default: position, global_position, rotation, scale, visible, modulate.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "node_path"
  ],
  "type": "object"
 },
 "name": "query_runtime_node"
}
