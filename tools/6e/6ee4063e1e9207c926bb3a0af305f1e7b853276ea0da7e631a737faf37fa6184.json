{
 "description": "Flag reserved names, invalid characters, trailing dots, case collisions, Unicode collisions, and long paths. Costs $0.05 USDC on Base mainnet.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "max_path_length": {
    "maximum": 32767,
    "minimum": 1,
    "type": "integer"
   },
   "paths": {
    "items": {
     "maxLength": 4096,
     "type": "string"
    },
    "maxItems": 5000,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "paths"
  ],
  "type": "object"
 },
 "name": "justhandled_filename_portability_preflight"
}
