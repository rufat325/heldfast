{
 "annotations": {
  "cost": {
   "all": 180
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Extract clean article text from one or more URLs via Tavily: strips boilerplate/navigation and returns up to 20,000 chars of readable content per page. Accepts a single URL string or an array. Ideal for feeding source pages into an LLM.",
 "inputSchema": {
  "examples": [
   {
    "urls": "https://www.example.com/article"
   },
   {
    "urls": [
     "https://www.example.com/article1",
     "https://www.example.com/article2"
    ]
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Tavily API key for higher limits; omit to use the shared Pipeworx key.",
    "type": "string"
   },
   "urls": {
    "description": "A single URL string, or an array of URL strings, to extract clean text from.",
    "items": {
     "type": "string"
    },
    "type": [
     "string",
     "array"
    ]
   }
  },
  "required": [
   "urls"
  ],
  "type": "object"
 },
 "name": "extract",
 "title": "Extract",
 "version": "v1.53d74f90"
}
