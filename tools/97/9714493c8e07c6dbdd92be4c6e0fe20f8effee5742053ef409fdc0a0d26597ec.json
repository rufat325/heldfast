{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Report the size and date range of the FTC Hart-Scott-Rodino (HSR) early-termination notice dataset \u2014 total notice count, earliest and latest dates on record, and the 2021 suspension gap when the FTC paused granting early terminations for about five weeks. Call this before treating an empty hsr_recent, hsr_search, or hsr_party_history result as \"no data\" \u2014 it confirms whether the requested window actually falls inside FTC coverage. Example: hsr_coverage({})",
 "inputSchema": {
  "examples": [
   {}
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own api.data.gov key for higher limits; omit to use the shared Pipeworx key.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "hsr_coverage",
 "title": "Hsr Coverage",
 "version": "v1.dee73332"
}
