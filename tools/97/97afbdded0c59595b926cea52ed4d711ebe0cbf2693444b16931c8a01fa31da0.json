{
 "description": "Load the README + context + index.yaml for a project (defaults to currentProjectId in config.json). Returns the three file bodies for context injection. Read-only by default; pass persist=true to also update config.json currentProjectId (i.e. switch the active project).",
 "inputSchema": {
  "properties": {
   "persist": {
    "description": "If true, write projectId to config.json as currentProjectId. Default false (read-only). Use true only when the caller intends to switch the active project.",
    "type": "boolean"
   },
   "projectId": {
    "description": "Project id; if omitted, resolves to config.json currentProjectId",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "mindbase_load_project"
}
