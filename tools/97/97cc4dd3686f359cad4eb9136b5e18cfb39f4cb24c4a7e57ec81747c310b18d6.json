{
 "_meta": {
  "securitySchemes": [
   {
    "scopes": [
     "email"
    ],
    "type": "oauth2"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Did this employer's board serve more roles than it did 7 days earlier? One row per companyToken (up to 20 per call), judged by the board itself from our own daily observation and passed through untouched: grew, no-growth, or unknown \u2014 and unknown ALWAYS carries unknown_reason (a feed bigger than one visit can read, a board too new or too small for a rate, a gap in our own series, a pool that was replaced rather than grown\u2026): an unknown is a reading we could not take, never a no. The bars the verdict uses: at least 10 roles served at the window's start; then BOTH at least 4 more roles AND at least 25% more, on a board tracked for at least 21 days, with every read in the window whole. Per BOARD (a vendor tenant), never summed across an employer's boards; more roles served is roles opened net of roles that came down \u2014 not a headcount and not a hire. This tool never ranks employers, and no list of growing employers exists here or anywhere on the board. Needs a key or a sign-in.",
 "inputSchema": {
  "properties": {
   "companyTokens": {
    "description": "companyToken values from job cards or search_jobs. Up to 20; more is refused with the count named.",
    "items": {
     "type": "string"
    },
    "maxItems": 20,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "companyTokens"
  ],
  "type": "object"
 },
 "name": "employer_growth",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "asked": {
    "type": "integer"
   },
   "bars": {
    "description": "What the verdict measured against \u2014 for reading a row, never for re-judging one.",
    "properties": {
     "min_baseline_served": {
      "type": "integer"
     },
     "min_net_add": {
      "type": "integer"
     },
     "min_rate": {
      "type": "number"
     },
     "min_tenure_days": {
      "type": "integer"
     },
     "window_days": {
      "type": "integer"
     }
    },
    "required": [
     "window_days",
     "min_baseline_served",
     "min_net_add",
     "min_rate",
     "min_tenure_days"
    ],
    "type": "object"
   },
   "basis": {
    "type": "string"
   },
   "employers": {
    "description": "One row per token asked, in the order asked. A token with no daily series answers unknown with its reason.",
    "items": {
     "additionalProperties": true,
     "properties": {
      "baseline_day": {
       "description": "Our observation date at the window's start.",
       "type": [
        "string",
        "null"
       ]
      },
      "baseline_served": {
       "description": "Roles served on this board on baseline_day.",
       "type": [
        "integer",
        "null"
       ]
      },
      "board_days_bad": {
       "type": "integer"
      },
      "board_days_ok": {
       "description": "Days in the read-quality ledger whose read of this board was whole.",
       "type": "integer"
      },
      "company_token": {
       "type": "string"
      },
      "days_expected": {
       "type": "integer"
      },
      "days_observed": {
       "type": "integer"
      },
      "first_snapshot_day": {
       "type": [
        "string",
        "null"
       ]
      },
      "latest_day": {
       "description": "Our latest observation date.",
       "type": [
        "string",
        "null"
       ]
      },
      "latest_served": {
       "type": [
        "integer",
        "null"
       ]
      },
      "ledger_days_expected": {
       "type": "integer"
      },
      "net": {
       "description": "latest_served minus baseline_served: roles opened net of roles that came down. Not a headcount.",
       "type": [
        "integer",
        "null"
       ]
      },
      "observed_arrivals": {
       "type": [
        "integer",
        "null"
       ]
      },
      "rate": {
       "description": "net over baseline_served. Null when the baseline is zero or unread.",
       "type": [
        "number",
        "null"
       ]
      },
      "removed_departures": {
       "type": [
        "integer",
        "null"
       ]
      },
      "tenure_censored": {
       "description": "True when the board is as old as the series itself, so its real tenure is longer than we can say.",
       "type": [
        "boolean",
        "null"
       ]
      },
      "tenure_days": {
       "description": "Days between the board's first daily observation and the window's start.",
       "type": [
        "integer",
        "null"
       ]
      },
      "unknown_reason": {
       "description": "Null for grew and no-growth. Otherwise the gate that refused, in the migration's own words.",
       "enum": [
        "excluded",
        "series_stale",
        "no_series",
        "too_new",
        "series_gap",
        "too_small",
        "pool_replaced",
        "not_in_ledger",
        "windowed_read",
        "failed_read",
        "ledger_gap",
        null
       ],
       "type": [
        "string",
        "null"
       ]
      },
      "untracked_departures": {
       "description": "OUR removals over the window \u2014 never counted as the employer shrinking.",
       "type": [
        "integer",
        "null"
       ]
      },
      "verdict": {
       "description": "The board's own judgement, passed through untouched. unknown is NOT no-growth: it is a reading we could not take, and unknown_reason says why.",
       "enum": [
        "grew",
        "no-growth",
        "unknown"
       ],
       "type": "string"
      },
      "window_days": {
       "type": "integer"
      }
     },
     "required": [
      "company_token",
      "verdict",
      "unknown_reason"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "employers",
   "bars",
   "basis"
  ],
  "type": "object"
 },
 "securitySchemes": [
  {
   "scopes": [
    "email"
   ],
   "type": "oauth2"
  }
 ],
 "title": "Did this employer's board grow?"
}
