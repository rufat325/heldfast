{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "create_branch"
 },
 "description": "Fork an environment into an ephemeral branch (a per-PR / preview config). The branch inherits the parent's secrets by layering at read time \u2014 nothing is copied or re-encrypted \u2014 and holds only the values you override on it. Generates the branch's data key locally, grants it to the caller, and shares it with the parent's other readers.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "app": {
    "type": "string"
   },
   "from": {
    "description": "the environment to branch",
    "type": "string"
   },
   "org": {
    "type": "string"
   },
   "slug": {
    "description": "the branch name, e.g. pr-142",
    "type": "string"
   },
   "ttl": {
    "description": "lifetime: 12h, 7d, 2w, \u2026 or `never` (default 7d)",
    "type": "string"
   }
  },
  "required": [
   "app",
   "from",
   "slug"
  ],
  "type": "object"
 },
 "name": "create_branch"
}
