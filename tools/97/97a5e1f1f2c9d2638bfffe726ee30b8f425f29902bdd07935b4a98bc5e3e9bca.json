{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true
 },
 "description": "Evaluate a JavaScript expression in the active page (via CDP) and return its value. Runs in the page's main world and can mutate page state. Prefer browser_snapshot for reading structure and browser_click/type for interactions.",
 "inputSchema": {
  "properties": {
   "expression": {
    "description": "A JavaScript expression evaluated in the page; its result is returned (JSON-serializable values).",
    "type": "string"
   }
  },
  "required": [
   "expression"
  ],
  "type": "object"
 },
 "name": "browser_eval"
}
