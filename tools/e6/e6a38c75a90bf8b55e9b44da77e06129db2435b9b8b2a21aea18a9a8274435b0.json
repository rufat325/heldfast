{
 "description": "Report that you did NOT present your PayClaw Badge at a merchant.\n\nCall this when you have a badge but chose not to present it (e.g., abandoned cart, merchant didn't ask).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "merchant": {
    "description": "The merchant where you did not present (e.g., 'starbucks.com')",
    "maxLength": 200,
    "type": "string"
   },
   "reason": {
    "description": "Why you did not present: abandoned, merchant_didnt_ask, other",
    "enum": [
     "abandoned",
     "merchant_didnt_ask",
     "other"
    ],
    "type": "string"
   },
   "verification_token": {
    "description": "The verification_token from payclaw_getAgentIdentity",
    "type": "string"
   }
  },
  "required": [
   "verification_token",
   "merchant",
   "reason"
  ],
  "type": "object"
 },
 "name": "payclaw_reportBadgeNotPresented"
}
