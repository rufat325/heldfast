{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true
 },
 "description": "Manually retry a failed webhook delivery. Re-sends the original payload to the endpoint URL with a fresh attempt count. Use this after fixing an endpoint that was returning errors. Use social_api_list_webhook_deliveries (filtered by status=failed) to find the delivery_id.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "delivery_id": {
    "description": "Delivery attempt ID to retry",
    "type": "string"
   },
   "webhook_id": {
    "description": "Webhook endpoint ID",
    "type": "string"
   }
  },
  "required": [
   "webhook_id",
   "delivery_id"
  ],
  "type": "object"
 },
 "name": "social_api_retry_webhook_delivery",
 "title": "Retry Webhook Delivery"
}
