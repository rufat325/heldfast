{
 "description": "Export audit data. Returns detailed page-level data for analysis. This export is for a single site \u2014 do not compare with exports from other domains.",
 "inputSchema": {
  "properties": {
   "format": {
    "description": "default json",
    "enum": [
     "json",
     "csv"
    ],
    "type": "string"
   },
   "id": {
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "export_audit"
}
