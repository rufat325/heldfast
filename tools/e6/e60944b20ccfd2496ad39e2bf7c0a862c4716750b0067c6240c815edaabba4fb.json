{
 "description": "Detect infrastructure drift \u2014 resources that were changed outside of Terraform (e.g. manually in the cloud console). Runs a refresh-only plan and reports differences between state and reality. Read-only, makes no changes.",
 "inputSchema": {
  "properties": {
   "workdir": {
    "description": "Absolute or relative path to the directory containing .tf files",
    "type": "string"
   },
   "workspace": {
    "description": "Terraform workspace to use (default: current workspace)",
    "type": "string"
   }
  },
  "required": [
   "workdir"
  ],
  "type": "object"
 },
 "name": "tf_drift"
}
