{
 "description": "Scan entire file for a pattern (not limited to a section).",
 "inputSchema": {
  "properties": {
   "file_path": {
    "description": "Path to file",
    "type": "string"
   },
   "max_results": {
    "description": "Max results (default 50)",
    "type": "number"
   },
   "pattern": {
    "description": "Pattern with wildcards",
    "type": "string"
   }
  },
  "required": [
   "file_path",
   "pattern"
  ],
  "type": "object"
 },
 "name": "pattern_scan_all"
}
