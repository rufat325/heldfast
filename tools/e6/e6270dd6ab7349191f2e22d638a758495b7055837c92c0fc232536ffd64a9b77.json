{
 "description": "Register an agent identity on Base mainnet via AgentIdentityRoot. Returns on-chain identity hash and registration transaction. Cost: $0.01 USDC per registration.",
 "inputSchema": {
  "properties": {
   "agent_address": {
    "description": "Agent wallet address",
    "type": "string"
   },
   "capabilities": {
    "description": "List of agent capability strings",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "metadata_uri": {
    "description": "IPFS or HTTPS URI for agent metadata",
    "type": "string"
   }
  },
  "required": [
   "agent_address",
   "capabilities"
  ],
  "type": "object"
 },
 "name": "logicnodes_identity_register"
}
