{
 "description": "Compares a passing and a failing trace of the same test. Aligns actions by sequence, finds the first timing or structural divergence, and summarises network differences. Use to diagnose flakiness \u2014 what was different in the run that failed.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "failing_trace_path": {
    "description": "Absolute path to the failing trace.zip",
    "type": "string"
   },
   "passing_trace_path": {
    "description": "Absolute path to the passing trace.zip",
    "type": "string"
   }
  },
  "required": [
   "passing_trace_path",
   "failing_trace_path"
  ],
  "type": "object"
 },
 "name": "compare_traces"
}
