{
 "description": "Make an API request through the ProphetKey proxy. The proxy automatically injects the real API key. The AI agent never sees the real key.",
 "inputSchema": {
  "properties": {
   "body": {
    "description": "Request body (for POST/PUT)",
    "type": "object"
   },
   "endpoint": {
    "description": "The API endpoint path (e.g., /v1/chat/completions)",
    "type": "string"
   },
   "method": {
    "description": "HTTP method",
    "enum": [
     "GET",
     "POST",
     "PUT",
     "DELETE"
    ],
    "type": "string"
   },
   "safe_key": {
    "description": "The safe_key identifier for the API key to use",
    "type": "string"
   }
  },
  "required": [
   "safe_key",
   "endpoint",
   "method"
  ],
  "type": "object"
 },
 "name": "prophetkey_make_request"
}
