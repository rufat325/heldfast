{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "List every configured mailbox: its logical `name` (pass this as the `mailbox` argument to other tools), its `emailAddress`, provider, connection status, and which one is the default. Reports mailboxes that failed to authenticate too, so it stays useful for diagnosing a broken setup.",
 "inputSchema": {
  "properties": {},
  "type": "object"
 },
 "name": "list_mailboxes",
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "mailboxes": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "emailAddress": {
       "type": [
        "string",
        "null"
       ]
      },
      "error": {
       "type": "string"
      },
      "isDefault": {
       "type": "boolean"
      },
      "name": {
       "type": "string"
      },
      "provider": {
       "type": "string"
      },
      "status": {
       "enum": [
        "connected",
        "error"
       ],
       "type": "string"
      }
     },
     "required": [
      "name",
      "emailAddress",
      "provider",
      "isDefault",
      "status"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "mailboxes"
  ],
  "type": "object"
 }
}
