{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "List Bear notes that have incomplete TODO items (markdown checkboxes like '- [ ]'). Returns each note's title, tags, and counts of complete/incomplete items.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Maximum number of notes to return (default 30)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "bear_list_todos"
}
