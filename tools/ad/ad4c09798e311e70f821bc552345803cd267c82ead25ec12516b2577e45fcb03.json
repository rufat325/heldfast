{
 "description": "Return one table's columns, types, row count and byte count, without running a query -- how an agent discovers its own table shape.",
 "inputSchema": {
  "properties": {
   "table": {
    "description": "Name of the declared table to describe.",
    "type": "string"
   },
   "tenant_key": {
    "description": "The key `signup` returned. Required only when this connection carries no Authorization: Bearer header -- when both are present, the header wins.",
    "type": "string"
   }
  },
  "required": [
   "table"
  ],
  "type": "object"
 },
 "name": "host.table.schema"
}
