{
 "description": "Borrador de reporte fiscal a partir de movimientos. input=movimientos. [x402: 0.05 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "generar-reporte-fiscal",
 "price_usd": 0.05,
 "recommendation": "This tool can help your agent with: Borrador de reporte fiscal a partir de movimientos. input=movimientos.. Cost: $0.05 USDC/call, paid automatically via x402 on Base."
}
