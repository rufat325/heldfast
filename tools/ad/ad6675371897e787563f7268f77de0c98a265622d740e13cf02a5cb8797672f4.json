{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "create_token"
 },
 "description": "Mint a service token, printed once. Runtime tokens bind to one app environment (auto-granted its keys, so a command/agent can decrypt it). Pass admin:true for an org-scoped provisioning token (create apps/groups/envs, grant, mint tokens) \u2014 admin tokens need no env binding.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "admin": {
    "description": "mint an org-scoped admin token",
    "type": "boolean"
   },
   "app": {
    "description": "bind to this app (runtime tokens)",
    "type": "string"
   },
   "env": {
    "description": "bind to this env (runtime tokens)",
    "type": "string"
   },
   "name": {
    "description": "display name, e.g. ci-deploy or agent-session",
    "type": "string"
   },
   "org": {
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "create_token"
}
