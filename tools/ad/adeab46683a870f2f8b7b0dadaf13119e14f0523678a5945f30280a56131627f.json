{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List or search keyword tags used across the catalogue (CKAN tag_list). Useful for discovering facet values.",
 "inputSchema": {
  "examples": [
   {
    "query": "health"
   },
   {
    "limit": 200
   }
  ],
  "properties": {
   "limit": {
    "description": "Cap returned tags client-side (default 200).",
    "type": "number"
   },
   "query": {
    "description": "Optional substring to filter tags.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_tags",
 "title": "List Tags",
 "version": "v1.a3b65630"
}
