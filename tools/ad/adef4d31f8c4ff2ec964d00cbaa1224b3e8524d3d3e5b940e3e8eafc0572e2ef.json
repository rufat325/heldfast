{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Most-boosted tokens, optionally filtered to a chain / token.",
 "inputSchema": {
  "examples": [
   {},
   {
    "chain": "solana"
   }
  ],
  "properties": {
   "chain": {
    "type": "string"
   },
   "token": {
    "description": "Token address (chain required if passed)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "token_boosts_top",
 "outputSchema": {
  "description": "Most-boosted tokens, optionally filtered to a chain/token",
  "properties": {
   "boost_count": {
    "description": "Number of boost entries returned",
    "type": "number"
   },
   "boosts": {
    "description": "Boost entries",
    "items": {
     "properties": {
      "amount": {
       "description": "Boost amount",
       "type": [
        "number",
        "null"
       ]
      },
      "chainId": {
       "description": "Chain identifier",
       "type": "string"
      },
      "description": {
       "description": "Token description",
       "type": [
        "string",
        "null"
       ]
      },
      "icon": {
       "description": "Token icon URL",
       "type": [
        "string",
        "null"
       ]
      },
      "tokenAddress": {
       "description": "Token contract address",
       "type": "string"
      },
      "totalAmount": {
       "description": "Cumulative boost amount",
       "type": [
        "number",
        "null"
       ]
      },
      "url": {
       "description": "Boost profile URL",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "chain": {
    "description": "Chain filter that was applied, when one was passed",
    "type": "string"
   },
   "filtered": {
    "description": "Whether a chain/token filter was applied",
    "type": "boolean"
   },
   "token": {
    "description": "Token filter that was applied, when one was passed",
    "type": "string"
   }
  },
  "required": [
   "boost_count",
   "filtered",
   "boosts"
  ],
  "type": "object"
 },
 "title": "Token Boosts Top",
 "version": "v1.d4bc2d1b"
}
