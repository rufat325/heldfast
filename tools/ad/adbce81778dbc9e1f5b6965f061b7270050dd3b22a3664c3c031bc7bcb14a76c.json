{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Create a new email identity (mailbox address) on this openagent.email server. Admin keys create top-level identities (omit scopes for legacy full permissions). Non-admin tokens need identities:create and mint a child identity owned by the caller (parentIdentity set server-side; default child scopes = [read:messages] when the parent holds that scope, else []; child scopes must be \u2286 parent and cannot include identities:create). Pass 'localpart' for a custom address (e.g. 'qa-bot' gives qa-bot@domain), or omit it for a random one. Returns the full address and a one-time API token.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "canNotifyUser": {
    "description": "Admin only: allow this identity to send human-alert notifications",
    "type": "boolean"
   },
   "domain": {
    "description": "Optional domain for the address. Must be one of the server's configured domains. Defaults to primary DOMAIN.",
    "maxLength": 253,
    "minLength": 1,
    "type": "string"
   },
   "localpart": {
    "description": "Custom email localpart (e.g. 'my-bot' for my-bot@domain). If omitted, a random one is generated.",
    "pattern": "^[a-z0-9][a-z0-9._-]{0,62}$",
    "type": "string"
   },
   "name": {
    "description": "Optional display name for the identity",
    "maxLength": 100,
    "minLength": 1,
    "type": "string"
   },
   "scopes": {
    "description": "Optional token scopes. Supported: read:messages, identities:create, messages:send. Use [] for no API operation permissions; omit for admin = legacy full identity permissions, for scoped parent = default child scopes ([read:messages] if parent has it, else []). Child creates cannot grant identities:create.",
    "items": {
     "enum": [
      "read:messages",
      "identities:create",
      "messages:send"
     ],
     "type": "string"
    },
    "maxItems": 10,
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "mail_new_identity",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "address": {
    "maxLength": 320,
    "pattern": "^[a-z0-9][a-z0-9._-]{0,62}@[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?(?:\\.[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?)*\\.?$",
    "type": "string"
   },
   "canNotifyUser": {
    "type": "boolean"
   },
   "createdAt": {
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "parentIdentity": {
    "type": "string"
   },
   "pushContentTier": {
    "anyOf": [
     {
      "const": 1,
      "type": "number"
     },
     {
      "const": 2,
      "type": "number"
     },
     {
      "const": 3,
      "type": "number"
     }
    ]
   },
   "pushContentTierWarning": {
    "type": "string"
   },
   "scopes": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "token": {
    "type": "string"
   }
  },
  "required": [
   "address"
  ],
  "type": "object"
 },
 "title": "Create Email Identity"
}
