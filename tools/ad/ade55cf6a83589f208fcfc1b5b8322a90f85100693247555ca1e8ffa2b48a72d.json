{
 "description": "Name which known failure categories a broken model reply matches (fenced, prose_wrapped, trailing_comma, unquoted_keys, single_quotes, comments, py_literals, raw_control, brackets).",
 "inputSchema": {
  "properties": {
   "text": {
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "classify_json_failure"
}
