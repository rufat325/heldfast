{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Get definitions for multiple symbol positions in a single call. Returns: list of definition locations for each position. IMPORTANT: Uses ZERO-BASED coordinates. More efficient than calling gdscript_definition repeatedly.",
 "inputSchema": {
  "properties": {
   "positions": {
    "description": "List of positions to look up definitions for",
    "items": {
     "properties": {
      "character": {
       "description": "Zero-based character position",
       "type": "integer"
      },
      "file": {
       "description": "Path to .gd file",
       "type": "string"
      },
      "line": {
       "description": "Zero-based line number",
       "type": "integer"
      }
     },
     "required": [
      "file",
      "line",
      "character"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "positions"
  ],
  "type": "object"
 },
 "name": "gdscript_definitions_batch"
}
