{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch a domain's sitemap(s) and report their STRUCTURE \u2014 URL counts by path prefix \u2014 not their content. This is the fingerprint that separates a normal site from a programmatic content farm: a farm concentrates a huge majority of its URLs under one or two path prefixes (e.g. /best/<x>-software/), while a normal site spreads them across many. Reads robots.txt Sitemap: directives first, falls back to /sitemap.xml then /sitemap_index.xml, follows one level of sitemap-index nesting, and handles gzipped .xml.gz files. Caps total URLs read (default 150,000) and always says when it capped, so a truncated read never looks like a small site. Returns found:false (not an error) when the domain has no sitemap. Example: sitemap_stats({ domain: \"gitnux.org\" })",
 "inputSchema": {
  "examples": [
   {
    "domain": "gitnux.org"
   },
   {
    "domain": "pipeworx.io"
   }
  ],
  "properties": {
   "domain": {
    "description": "Domain or URL to inspect, e.g. \"example.com\" or \"https://example.com\"",
    "type": "string"
   },
   "max_urls": {
    "description": "Cap on total URLs read across all sitemap files (default 150000, hard ceiling 500000)",
    "type": "number"
   }
  },
  "required": [
   "domain"
  ],
  "type": "object"
 },
 "name": "sitemap_stats",
 "title": "Sitemap Stats",
 "version": "v1.4c0cb217"
}
