{
 "description": "Run an arbitrary ADB shell command",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "command": {
    "description": "Shell command to execute on the device",
    "type": "string"
   },
   "device_id": {
    "description": "Device ID (optional if only one device)",
    "type": "string"
   }
  },
  "required": [
   "command"
  ],
  "type": "object"
 },
 "name": "adb_shell"
}
