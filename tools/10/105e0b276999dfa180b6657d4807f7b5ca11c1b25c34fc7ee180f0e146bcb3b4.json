{
 "description": "Analyze a crypto wallet's risk profile, transaction history, token holdings, and risk flags. Detect scam wallets, sybil clusters, and coordinated activity.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "Wallet address (0x... for EVM chains, or Solana base58 address)",
    "type": "string"
   },
   "chain": {
    "description": "Blockchain ID (ethereum, solana, bsc, base, etc.)",
    "type": "string"
   }
  },
  "required": [
   "chain",
   "address"
  ],
  "type": "object"
 },
 "name": "analyze_wallet"
}
