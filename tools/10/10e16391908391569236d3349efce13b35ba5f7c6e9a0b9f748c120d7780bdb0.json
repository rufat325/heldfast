{
 "annotations": {
  "readOnlyHint": true,
  "title": "Get average response time"
 },
 "description": "List individual reply events on a board over a date range, covering follow-up replies as well as first responses. Each event gives intervalTime in milliseconds, the responder, and the thread; firstResponse is true on first responses and absent on follow-ups. Returns raw events, not a computed average.",
 "inputSchema": {
  "properties": {
   "boardId": {
    "description": "The board ID",
    "type": "number"
   },
   "dateFrom": {
    "description": "Start date for the period (ISO-8601)",
    "type": "string"
   },
   "dateTo": {
    "description": "End date for the period (ISO-8601)",
    "type": "string"
   }
  },
  "required": [
   "boardId",
   "dateFrom",
   "dateTo"
  ],
  "type": "object"
 },
 "name": "get_avg_response_time",
 "title": "Get average response time"
}
