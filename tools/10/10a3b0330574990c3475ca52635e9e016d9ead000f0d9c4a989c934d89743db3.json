{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find US banks and savings institutions that file the FFIEC Call Report, by name, city or state. Returns the bank's Federal Reserve IDRSSD, its FDIC certificate number, location, which call report form it files (031/041/051), and total assets. Answers \"banks in Austin Texas\", \"is there a bank called Zions\", \"list Ohio banks\". Name matching is fuzzy, so partial names work. Example: ffiec_search_banks({ name: \"jpmorgan\" }); ffiec_search_banks({ state: \"OH\", limit: 25 }). Keyless.",
 "inputSchema": {
  "examples": [
   {
    "name": "jpmorgan"
   },
   {
    "limit": 25,
    "state": "OH"
   }
  ],
  "properties": {
   "city": {
    "description": "City name",
    "type": "string"
   },
   "limit": {
    "description": "Max results (default 20, max 100)",
    "type": "number"
   },
   "name": {
    "description": "Full or partial bank name, e.g. \"jpmorgan\"",
    "type": "string"
   },
   "state": {
    "description": "Two-letter state code, e.g. \"TX\"",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ffiec_search_banks",
 "title": "Ffiec Search Banks",
 "version": "v1.85adb710"
}
