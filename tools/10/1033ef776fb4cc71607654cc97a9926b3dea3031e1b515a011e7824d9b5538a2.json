{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Set plan block status"
 },
 "description": "Mark a day-planner block done or not-done for one specific local date (recurring blocks track completion per-day, not globally).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Day-planner block UUID",
    "format": "uuid",
    "type": "string"
   },
   "local_date": {
    "description": "Local date YYYY-MM-DD",
    "type": "string"
   },
   "status": {
    "description": "'completed' = done, 'none' = not done",
    "enum": [
     "none",
     "completed"
    ],
    "type": "string"
   }
  },
  "required": [
   "id",
   "local_date",
   "status"
  ],
  "type": "object"
 },
 "name": "set_daily_plan_block_status",
 "title": "Set plan block status"
}
