{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List messages in your inbox with optional filtering by label or read status. Returns message IDs, thread IDs, and preview text. Use gmail_search for complex queries like date ranges or attachments.",
 "inputSchema": {
  "examples": [
   {
    "max_results": 10
   },
   {
    "max_results": 20,
    "query": "from:alice subject:meeting"
   }
  ],
  "properties": {
   "max_results": {
    "description": "Maximum number of messages to return (default 10, max 100)",
    "type": "number"
   },
   "page_token": {
    "description": "Token for fetching the next page of results",
    "type": "string"
   },
   "query": {
    "description": "Gmail search query to filter messages (e.g., \"from:alice subject:meeting\")",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "gmail_list_messages",
 "outputSchema": {
  "properties": {
   "error": {
    "description": "Error code if connection not authenticated",
    "type": "string"
   },
   "message": {
    "description": "Error message if connection not authenticated",
    "type": "string"
   },
   "messages": {
    "description": "Array of message summaries",
    "items": {
     "properties": {
      "id": {
       "description": "Message ID",
       "type": "string"
      },
      "labelIds": {
       "description": "Applied label IDs",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "threadId": {
       "description": "Thread ID",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "nextPageToken": {
    "description": "Token for fetching next page of results",
    "type": "string"
   },
   "resultSizeEstimate": {
    "description": "Estimated total number of matching messages",
    "type": "number"
   }
  },
  "type": "object"
 },
 "title": "Gmail List Messages",
 "version": "v1.8534db21"
}
