{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "The full extraction of one node: HTML measured against what Figma draws, byte-exact figmaCss, a React component, plain CSS and CSS modules. Images are inlined and icons come out as real SVG, so the HTML stands on its own. This is the tool that answers \"what does this design actually say\".\n\nSeveral nodes at once, instead of one call each: nodeIds, urls, selection: true, or saved: true with an optional folder. A batch answers {results:[\u2026]}, one entry per input, each {ok:true,extraction} or {ok:false,error}, so one bad id never sinks the rest. It is capped at 20 entries, and no batch returns an image \u2014 ask figma_export_png for a picture, a node at a time.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "folder": {
    "description": "One folder of the saved set, with saved: true. \"\" is the root. This is a folder in the panel, not a directory on disk \u2014 nothing here writes files; ask for pngData and write the bytes yourself.",
    "type": "string"
   },
   "formats": {
    "description": "Which representations to return. Default html and figmaCss. \"png\" comes back as a real image you can look at, alongside the text \u2014 there is no base64 in the answer, so ask for it freely on a single node. figma_export_png is the same picture with nothing else attached.",
    "items": {
     "enum": [
      "png",
      "html",
      "tsx",
      "moduleCss",
      "css",
      "figmaCss"
     ],
     "type": "string"
    },
    "type": "array"
   },
   "inlineInstances": {
    "description": "Expand component instances instead of referencing them.",
    "type": "boolean"
   },
   "nodeId": {
    "description": "Figma node id, like \"21:10314\". Omit to use whatever is selected on the canvas.",
    "type": "string"
   },
   "nodeIds": {
    "description": "Several node ids, extracted in one call. Up to 20.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "saved": {
    "description": "Extract the designer\u2019s saved set. Narrow it with folder.",
    "type": "boolean"
   },
   "scale": {
    "description": "Render scale for any image output. Default 2.",
    "maximum": 4,
    "minimum": 1,
    "type": "number"
   },
   "selection": {
    "description": "Extract every layer the designer has selected, not just the first.",
    "type": "boolean"
   },
   "topLayerOnly": {
    "description": "Stop at the selected layer rather than walking into it.",
    "type": "boolean"
   },
   "url": {
    "description": "A Figma link, as an alternative to nodeId.",
    "type": "string"
   },
   "urls": {
    "description": "Several Figma links, extracted in one call. Up to 20.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "figma_extract",
 "title": "Read a design as code"
}
