{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false
 },
 "description": "Update a conversation's status. Set status to 'archived' to move it out of the active inbox, or 'active' to restore it. Idempotent -- setting the current status has no effect. Use social_api_list_conversations to find the conversation_id.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "conversation_id": {
    "description": "Conversation ID (from social_api_list_conversations)",
    "type": "string"
   },
   "status": {
    "description": "New status: active or archived",
    "enum": [
     "active",
     "archived"
    ],
    "type": "string"
   }
  },
  "required": [
   "conversation_id",
   "status"
  ],
  "type": "object"
 },
 "name": "social_api_update_conversation",
 "title": "Update Conversation"
}
