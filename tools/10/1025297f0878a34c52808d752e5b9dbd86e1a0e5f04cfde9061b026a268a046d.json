{
 "description": "Submit an agent card URL, MCP endpoint or domain for the registry. The hub verifies it by crawling; nothing in the submission is trusted.",
 "inputSchema": {
  "properties": {
   "kind": {
    "enum": [
     "a2a",
     "mcp"
    ],
    "type": "string"
   },
   "url": {
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "register_agent"
}
