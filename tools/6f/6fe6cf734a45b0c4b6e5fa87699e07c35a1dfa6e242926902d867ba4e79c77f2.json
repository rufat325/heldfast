{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Build Freeze KLV Transaction"
 },
 "description": "Build an unsigned Freeze KLV transaction on the Klever blockchain. Freezing KLV provides energy/bandwidth for network operations and enables staking rewards. Returns the unsigned transaction for client-side signing.",
 "inputSchema": {
  "properties": {
   "amount": {
    "description": "Amount of KLV to freeze in the smallest unit (1 KLV = 1,000,000 units).",
    "minimum": 1,
    "type": "integer"
   },
   "network": {
    "description": "Network to use. Options: \"mainnet\", \"testnet\", \"devnet\", \"local\". Defaults to server default (mainnet).",
    "enum": [
     "mainnet",
     "testnet",
     "devnet",
     "local"
    ],
    "type": "string"
   },
   "sender": {
    "description": "Address to freeze from (klv1... bech32 format).",
    "type": "string"
   }
  },
  "required": [
   "sender",
   "amount"
  ],
  "type": "object"
 },
 "name": "freeze_klv"
}
