{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "L\u00ea o log append-only de decis\u00f5es do board (mais recentes primeiro). Filtros: source, q, cardId, epic, limit. Cada entrada traz id, summary, detail, source, cardId, epic, at.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cardId": {
    "type": "string"
   },
   "epic": {
    "type": "string"
   },
   "limit": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "type": "string"
   },
   "source": {
    "enum": [
     "agent",
     "human"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_decisions",
 "title": "Ler o log de decis\u00f5es do board"
}
