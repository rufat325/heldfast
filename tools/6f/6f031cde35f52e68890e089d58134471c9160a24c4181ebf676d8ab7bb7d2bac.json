{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run a raw SoQL query against any Austin open-data resource (data.austintexas.gov) by its Socrata id (8-char like \"fdj4-gpfu\"). Full SoQL: where/select/group/order/limit/offset. Use austin_datasets to find a resource id, or austin_recent for the common ones.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-data-austin-api-key",
    "group": "crime_type",
    "resource_id": "fdj4-gpfu",
    "select": "crime_type, count(*)",
    "where": "occ_date_time > '2024-01-01'"
   },
   {
    "_apiKey": "your-data-austin-api-key",
    "limit": 100,
    "order": "inspection_date DESC",
    "resource_id": "wvfd-dwi5"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Socrata app token for higher rate limits. Omit to use the keyless endpoint.",
    "type": "string"
   },
   "group": {
    "description": "SoQL $group.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (default 100, max 5000).",
    "type": "number"
   },
   "offset": {
    "description": "Row offset for paging.",
    "type": "number"
   },
   "order": {
    "description": "SoQL $order (e.g. \"occ_date_time DESC\").",
    "type": "string"
   },
   "resource_id": {
    "description": "Socrata resource id, e.g. \"fdj4-gpfu\" (crime reports).",
    "type": "string"
   },
   "select": {
    "description": "SoQL $select (e.g. \"crime_type, count(*)\").",
    "type": "string"
   },
   "where": {
    "description": "SoQL $where filter.",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "austin_query",
 "title": "Austin Query",
 "version": "v1.857e28ca"
}
