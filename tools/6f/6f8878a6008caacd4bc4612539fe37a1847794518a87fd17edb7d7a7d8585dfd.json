{
 "description": "Export atomization results in JSON, CSV, or Markdown format.",
 "inputSchema": {
  "properties": {
   "format": {
    "description": "Export format (default: json)",
    "enum": [
     "json",
     "csv",
     "markdown"
    ],
    "type": "string"
   },
   "id": {
    "description": "Atomization ID (required)",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "export_atomization"
}
