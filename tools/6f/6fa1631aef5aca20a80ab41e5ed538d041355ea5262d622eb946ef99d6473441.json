{
 "description": "Get a site's current FraseCMS headless delivery token (same one the dashboard shows). revoked=true means use rotate_delivery_token to issue a new one.",
 "inputSchema": {
  "properties": {
   "site_id": {
    "description": "The site whose delivery token to fetch",
    "type": "string"
   }
  },
  "required": [
   "site_id"
  ],
  "type": "object"
 },
 "name": "get_delivery_token"
}
