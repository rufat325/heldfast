{
 "description": "Generate a clause-ready redline draft from a review_nda JSON report. Writes redline markdown to `out`. Deterministic; no org policy needed.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "mode": {
    "description": "Redline format (default classic).",
    "enum": [
     "classic",
     "v2"
    ],
    "type": "string"
   },
   "out": {
    "description": "Output path for the redline markdown.",
    "type": "string"
   },
   "review_json": {
    "description": "Path to a saved review_nda JSON report.",
    "type": "string"
   }
  },
  "required": [
   "review_json",
   "out"
  ],
  "type": "object"
 },
 "name": "generate_redlines"
}
