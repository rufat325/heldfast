{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "MUTATES one existing Food Diary row the authenticated user owns. Required: food_id (UUID). Optional patch (same fields as log_food): what, kind, place, merchant, notes, append_notes (concat), consumed_at (ISO instant) / meal_period (profile TZ), amount, currency, paid_by / payment_method, with / people_names / person_ids. Example: add hot tea onto an existing Salmon Bento \u2192 update_food({ food_id, what: \"Salmon Bento + hot tea\" }). Unknown or other-user food_id returns an error and does not create a row. Syncs the mirrored calendar event and rebuilds life_state. Requires API key or OAuth with scope context. Share tokens cannot write. ($0.10; API key required)",
 "destructiveHint": false,
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "amount": {
    "description": "Price if mentioned",
    "type": "number"
   },
   "append_notes": {
    "description": "Append text to existing notes (newline-separated)",
    "type": "string"
   },
   "consumed_at": {
    "description": "ISO instant; wins over meal_period",
    "type": "string"
   },
   "currency": {
    "description": "ISO currency, e.g. SGD",
    "type": "string"
   },
   "food_id": {
    "description": "UUID of a food diary row the authenticated user owns",
    "type": "string"
   },
   "idempotency_key": {
    "description": "Alias for request_id.",
    "type": "string"
   },
   "kind": {
    "enum": [
     "meal",
     "snack",
     "drink"
    ],
    "type": "string"
   },
   "meal_period": {
    "description": "Spoken period when consumed_at is omitted, e.g. \"late lunch\"",
    "type": "string"
   },
   "merchant": {
    "description": "Restaurant, stall, or shop. Empty or null clears",
    "type": "string"
   },
   "notes": {
    "description": "Replaces notes. Empty or null clears",
    "type": "string"
   },
   "paid_by": {
    "description": "Who paid (name or alias)",
    "type": "string"
   },
   "payment_method": {
    "description": "Alias of paid_by for expense-style agents",
    "type": "string"
   },
   "people_names": {
    "description": "Alias for with",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "person_ids": {
    "description": "Owned person UUIDs to tag (user-scoped)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "place": {
    "description": "Venue or location. Empty or null clears",
    "type": "string"
   },
   "request_id": {
    "description": "Client idempotency key (retries return original result).",
    "type": "string"
   },
   "what": {
    "description": "Full updated dish/drink text (replaces, does not append). Example: Salmon Bento + hot tea",
    "type": "string"
   },
   "with": {
    "description": "Companion names, aliases, or ids to add (does not remove existing tags)",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "food_id"
  ],
  "type": "object"
 },
 "name": "update_food",
 "openWorldHint": false,
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "changed": {
    "additionalProperties": true,
    "description": "Changed fields with before and after values.",
    "type": "object"
   },
   "event_id": {
    "type": [
     "string",
     "null"
    ]
   },
   "food_id": {
    "type": "string"
   },
   "food_log": {
    "additionalProperties": true,
    "description": "Updated Food Diary row.",
    "type": "object"
   },
   "life_state_rebuilt": {
    "type": "boolean"
   },
   "people_tagged": {
    "items": {
     "additionalProperties": true,
     "description": "A Dayze person or contact record.",
     "type": "object"
    },
    "type": "array"
   },
   "unresolved_people": {
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "food_id",
   "event_id",
   "food_log",
   "changed",
   "people_tagged",
   "unresolved_people",
   "life_state_rebuilt"
  ],
  "type": "object"
 },
 "readOnlyHint": false,
 "securitySchemes": [
  {
   "scopes": [
    "context.write"
   ],
   "type": "oauth2"
  }
 ],
 "title": "Update Food (diary patch)"
}
