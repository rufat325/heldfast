{
 "description": "Launch an app by bundle ID. Optionally pass launch arguments and environment variables.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "args": {
    "description": "Launch arguments to pass to the app",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "bundleId": {
    "description": "App bundle identifier to launch",
    "type": "string"
   },
   "deviceId": {
    "description": "Device UDID, name, or \"booted\" (default: booted)",
    "type": "string"
   },
   "env": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "Environment variables to set (will be prefixed with SIMCTL_CHILD_)",
    "type": "object"
   },
   "terminateRunning": {
    "description": "Terminate the app first if already running (default: false)",
    "type": "boolean"
   }
  },
  "required": [
   "bundleId"
  ],
  "type": "object"
 },
 "name": "simulator_launch_app"
}
