{
 "description": "Continuous monitoring of public web pages with change detection. Each URL is baselined on subscription and re-checked hourly, daily or weekly. A change is measured over the page's visible text, so routine churn in tokens, timestamps and ad slots does not read as a change. Every check is recorded, and a webhook fires on each detected change if you give one. Priced per URL per month.",
 "inputSchema": {
  "properties": {
   "alert_threshold": {
    "description": "Currently 'any_change': every change to the page's visible text is reported.",
    "type": "string"
   },
   "duration_months": {
    "description": "Whole months, 1-12. The price is $10 per URL per month; more than 12 months is refused before payment, not cut down.",
    "type": "number"
   },
   "frequency": {
    "description": "",
    "enum": [
     "hourly",
     "daily",
     "weekly"
    ],
    "type": "string"
   },
   "urls": {
    "description": "List of URLs to monitor.",
    "type": "string"
   },
   "webhook_url": {
    "description": "",
    "type": "string"
   }
  },
  "required": [
   "urls"
  ],
  "type": "object"
 },
 "name": "hsh-monitoring"
}
