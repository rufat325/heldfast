{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Delete a profile row from ~/.toggl/focus-tools.json. If it was the MCP active profile, active.mcp moves to another remaining profile when possible. This affects both MCP and shared storage; removing a profile used by the CLI removes it for the CLI too. Destructive: call without confirm_token first to receive confirm_required + confirm_token, then repeat with the same profile_name and confirm_token (same pattern as entity mutations).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "confirm_token": {
    "description": "Mutation confirmation token from previous call",
    "type": "string"
   },
   "profile_name": {
    "description": "Profile key as shown by profile-list",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "profile_name"
  ],
  "type": "object"
 },
 "name": "profile-remove",
 "title": "Remove saved profile"
}
