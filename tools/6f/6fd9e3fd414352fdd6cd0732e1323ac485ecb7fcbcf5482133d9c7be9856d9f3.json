{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Start SAP MCP Agent Mode"
 },
 "description": "Return the concise startup playbook for agents using SAP MCP. Call this when the user says \"Start SAP MCP\", \"Initialize SAP MCP\", \"Load SAP\", or asks what SAP MCP can do.\n\nSAP MCP execution guidance: Intent: agent bootstrap, routing, skills, or repair guidance. Pricing: free; call directly without x402. Routing: free hosted call; call directly and keep it small/exact when possible. Signer boundary: hosted reads/builders never receive keypair bytes; value-moving results must be finalized locally when signing is required.",
 "inputSchema": {
  "description": "Start SAP MCP Agent Mode input schema. Intent: agent bootstrap, routing, skills, or repair guidance. Pricing: free; call directly without x402. Routing: free hosted call; call directly and keep it small/exact when possible. Signer boundary: hosted reads/builders never receive keypair bytes; value-moving results must be finalized locally when signing is required. Use exact field names from this schema; do not guess aliases or include private key material.",
  "properties": {
   "goal": {
    "description": "Optional user goal to tailor the startup plan, such as \"register an agent\", \"check wallet balance\", or \"swap tokens\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "sap_agent_start",
 "outputSchema": {
  "properties": {
   "activationPhrases": {
    "description": "Short user phrases that should trigger this bootstrap behavior.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "connectionCheck": {
    "description": "How agents should answer simple connection/status questions.",
    "type": "object"
   },
   "hostedEndpoint": {
    "description": "Canonical hosted SAP MCP endpoint.",
    "type": "string"
   },
   "immediateToolCalls": {
    "description": "Tool calls the agent should run first when available.",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "paymentFlow": {
    "description": "How to handle hosted x402/pay.sh paid calls safely.",
    "type": "object"
   },
   "repairCommand": {
    "description": "Wizard command to repair local hosted/profile/payment bridge setup.",
    "type": "string"
   },
   "serverMode": {
    "description": "Current SAP MCP server mode.",
    "type": "string"
   },
   "sessionContextPacket": {
    "description": "Machine-readable SAP MCP routing, freshness, memory, proof-tape, and forbidden-action rules for this session.",
    "type": "object"
   },
   "success": {
    "description": "Whether the bootstrap instructions were generated.",
    "type": "boolean"
   },
   "userFacingSummary": {
    "description": "Short text the agent can show the user after startup.",
    "type": "string"
   }
  },
  "required": [
   "success",
   "activationPhrases",
   "hostedEndpoint",
   "serverMode",
   "immediateToolCalls",
   "paymentFlow",
   "sessionContextPacket",
   "connectionCheck",
   "userFacingSummary",
   "repairCommand"
  ],
  "type": "object"
 },
 "title": "Start SAP MCP Agent Mode"
}
