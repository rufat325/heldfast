{
 "description": "Capture a previously authorized Webpay Plus transaction (partial-capture / deferred-capture flow).",
 "inputSchema": {
  "properties": {
   "authorization_code": {
    "description": "Authorization code returned at authorization time",
    "type": "string"
   },
   "buy_order": {
    "description": "Original buy_order",
    "type": "string"
   },
   "capture_amount": {
    "description": "Amount to capture in CLP (integer)",
    "type": "number"
   },
   "token": {
    "description": "Webpay token of the authorized transaction",
    "type": "string"
   }
  },
  "required": [
   "token",
   "buy_order",
   "authorization_code",
   "capture_amount"
  ],
  "type": "object"
 },
 "name": "webpay_increase_amount"
}
