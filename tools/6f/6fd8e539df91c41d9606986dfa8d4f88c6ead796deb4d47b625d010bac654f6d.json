{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Fetch a published Figma library catalog through REST. Requires FIGMA_TOKEN; file need not be open.",
 "inputSchema": {
  "properties": {
   "fileKey": {
    "description": "Figma file key \u2014 the path segment after /design/ in the file...",
    "type": "string"
   },
   "outPath": {
    "description": "File path to write the full catalog JSON to (relative to...",
    "type": "string"
   },
   "scope": {
    "description": "Which endpoints to fetch: all (default), components,...",
    "type": "string"
   }
  },
  "required": [
   "fileKey",
   "outPath"
  ],
  "type": "object"
 },
 "name": "fetch_library_catalog"
}
