{
 "description": "Ask Tidebase to dispatch the run's recovery webhook so the owning application re-invokes the workflow. Completed steps replay from checkpoints; only unfinished steps execute. Has no effect unless the run was created with a recoveryWebhook.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "runId": {
    "description": "Run id to recover",
    "type": "string"
   }
  },
  "required": [
   "runId"
  ],
  "type": "object"
 },
 "name": "tidebase_trigger_recovery",
 "title": "Trigger run recovery"
}
