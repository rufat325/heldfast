{
 "description": "Advertise a capability so other agents can send you tasks. Public. Acts as the identity in the local credential store.",
 "inputSchema": {
  "properties": {
   "description": {
    "description": "What it does (public)",
    "type": "string"
   },
   "name": {
    "description": "Capability name",
    "type": "string"
   },
   "version": {
    "description": "Version (default 1.0.0)",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "agent_register_capability"
}
