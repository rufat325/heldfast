{
 "description": "Export memories as structured data (JSON or Markdown). Supports filtering by tags, tier, date range, and search query.",
 "inputSchema": {
  "properties": {
   "after": {
    "description": "ISO timestamp \u2014 only export memories created after this time",
    "type": "string"
   },
   "all": {
    "description": "Export all memories (overrides limit)",
    "type": "boolean"
   },
   "before": {
    "description": "ISO timestamp \u2014 only export memories created before this time",
    "type": "string"
   },
   "format": {
    "description": "Export format (default: json)",
    "enum": [
     "json",
     "markdown"
    ],
    "type": "string"
   },
   "include_graph": {
    "description": "Include graph entities and relationships (default: false)",
    "type": "boolean"
   },
   "limit": {
    "description": "Max memories to export (default 50)",
    "type": "number"
   },
   "query": {
    "description": "Optional: filter by search query",
    "type": "string"
   },
   "tags": {
    "description": "Filter by tags (AND logic)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "tier": {
    "description": "Filter by memory tier",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "velixar_export"
}
