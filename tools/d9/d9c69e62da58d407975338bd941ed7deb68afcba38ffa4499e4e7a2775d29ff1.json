{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List the model's database tables in `tables`, or pass a `tableName` to get that table's `columns` and `indexes` instead. That form echoes the `table` name and adds `rowidAlias` \u2014 the INTEGER PRIMARY KEY aliasing SQLite's rowid, so the fastest lookup path, or null when the table has none \u2014 with `rowidNote` saying which case applies. Use this to discover what data the model holds beyond what the typed ea_* tools return. See ea_get_model_info for the export's identity.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "tableName": {
    "description": "Table name to inspect. Omit to list all tables with row counts.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ea_get_schema"
}
