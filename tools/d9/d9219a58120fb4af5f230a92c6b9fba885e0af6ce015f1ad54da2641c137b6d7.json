{
 "description": "Execute a command on a remote server via SSH and return the output. Uses the system SSH binary so ssh-agent, ProxyCommand, and ~/.ssh/config are honoured automatically.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "agentForward": {
    "default": false,
    "description": "Enable SSH agent forwarding (default: false)",
    "type": "boolean"
   },
   "command": {
    "description": "Command to execute on the remote server",
    "type": "string"
   },
   "extraArgs": {
    "description": "Extra SSH arguments (e.g., ['-o', 'StrictHostKeyChecking=no'])",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "host": {
    "description": "SSH server hostname or IP address (e.g., 'dev234' or '192.168.1.100')",
    "type": "string"
   },
   "port": {
    "default": 22,
    "description": "SSH server port (default: 22)",
    "type": "number"
   },
   "timeout": {
    "default": 30000,
    "description": "Command timeout in milliseconds (default: 30000)",
    "type": "number"
   },
   "username": {
    "description": "SSH username (e.g., 'username'). Defaults to the current OS user.",
    "type": "string"
   }
  },
  "required": [
   "host",
   "command"
  ],
  "type": "object"
 },
 "name": "ssh_execute"
}
