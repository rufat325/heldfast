{
 "description": "Query custom events (purchases, form submits, button clicks, etc.). Filter by category/action/label or UTM fields, group by any combination of those, paginate. Use for 'how many Purchase events from utm_source=newsletter last week' or 'top event categories this month'.",
 "inputSchema": {
  "properties": {
   "filter": {
    "description": "Filters as {field: value}. Allowed top-level fields: category, action, label, utm_*. Event props go under {'props': {'key': 'value'}} which translates to filter[props.key]=value on the wire.",
    "type": "object"
   },
   "group_by": {
    "description": "Fields to group by. Allowed: category, action, label, utm_campaign, utm_source, utm_medium, utm_content, utm_term. Default: ['category', 'action', 'label'].",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "limit": {
    "description": "Max rows. Default 50, max 1000.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Pagination offset. Default 0.",
    "maximum": 10000,
    "minimum": 0,
    "type": "integer"
   },
   "range": {
    "description": "Date range. Accepts natural strings ('today', 'yesterday', 'last 7 days', 'last 30 days', 'this week', 'last week', 'this month', 'last month') or an explicit 'YYYY-MM-DD..YYYY-MM-DD' range. UTC. If you have separate from/to dates, use the explicit form.",
    "type": "string"
   },
   "site_id": {
    "description": "Site ID. Get it from list_sites if you don't know it.",
    "type": "string"
   }
  },
  "required": [
   "site_id",
   "range"
  ],
  "type": "object"
 },
 "name": "query_events"
}
