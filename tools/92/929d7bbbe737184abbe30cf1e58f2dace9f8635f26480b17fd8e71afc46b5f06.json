{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Exact key lookup. Returns the stored value plus timestamps, ttl, tags, metadata. Returns null if missing or expired. Optional privacy_mode=summary omits full values.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "key": {
    "description": "Stable identifier for the memory entry. Free-form text. Treat as case-sensitive.",
    "maxLength": 512,
    "minLength": 1,
    "type": "string"
   },
   "privacy_mode": {
    "default": "structured",
    "description": "summary = keys/meta without full values; structured/raw = full entries (local store parity).",
    "enum": [
     "summary",
     "structured",
     "raw"
    ],
    "type": "string"
   }
  },
  "required": [
   "key"
  ],
  "type": "object"
 },
 "name": "memory_get"
}
