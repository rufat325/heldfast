{
 "description": "Everything the registry knows about one tool before you pay to call it: verdict and its date, price, argument schema, card signals, schema drift, how the provider behaved on calls this hub made, and what comparable priced tools charge. Charged per call.",
 "inputSchema": {
  "properties": {
   "agentId": {
    "type": "string"
   },
   "caller": {
    "type": "string"
   },
   "idempotencyKey": {
    "description": "your key for this request; send it again on a retry and the charge is made once",
    "type": "string"
   },
   "tool": {
    "type": "string"
   }
  },
  "required": [
   "caller",
   "agentId",
   "tool"
  ],
  "type": "object"
 },
 "name": "sapphire_tool_brief"
}
