{
 "description": "Sleep server-side. Useful between input events to let the game process them. Capped at 30000ms / 30s. Pass either ms or seconds (ms wins if both given).",
 "inputSchema": {
  "properties": {
   "ms": {
    "description": "Milliseconds to wait (1..30000).",
    "type": "number"
   },
   "seconds": {
    "description": "Seconds to wait (0.001..30). Convenient when the agent is thinking in seconds.",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "wait"
}
