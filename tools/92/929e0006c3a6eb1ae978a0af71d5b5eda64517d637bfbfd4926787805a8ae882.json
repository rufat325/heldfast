{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Replace complete test suite"
 },
 "description": "Replace the complete reviewed test suite for an existing project after field discovery. Requires 1 to 100 cases and replaces prior tests without creating a project or changing its sources, fields, or guidance. This is destructive. The target API must advertise replacement support before any write. If the response is interrupted, inspect the project before approving another replacement.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "project_id": {
    "description": "Existing project ID whose complete test suite will be replaced",
    "type": "string"
   },
   "test_cases": {
    "description": "The complete authoritative reviewed suite (1-100 cases); this replaces existing tests",
    "items": {
     "additionalProperties": true,
     "properties": {
      "expected_outcome": {
       "description": "Reviewed expected eligibility outcome",
       "enum": [
        "eligible",
        "not_eligible",
        "undetermined"
       ],
       "type": "string"
      },
      "field_values": {
       "additionalProperties": {},
       "description": "Input values using discovered field names",
       "type": "object"
      },
      "name": {
       "description": "Stable reviewed test-case name",
       "minLength": 1,
       "type": "string"
      }
     },
     "required": [
      "name",
      "field_values",
      "expected_outcome"
     ],
     "type": "object"
    },
    "maxItems": 100,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "project_id",
   "test_cases"
  ],
  "type": "object"
 },
 "name": "aethis_set_tests"
}
