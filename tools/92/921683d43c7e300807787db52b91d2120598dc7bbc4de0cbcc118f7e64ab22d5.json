{
 "annotations": {
  "cost": {
   "all": 360
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "What keywords does `<domain>` rank for \u2014 returns the keywords a domain ranks for in Google organic, with search volume, current rank, and ranking URL. Competitor SEO intelligence. Example: seo_domain_ranked_keywords({ target: \"nike.com\", location_code: 2840, limit: 50, _apiKey: \"your-base64-key\" })",
 "inputSchema": {
  "examples": [
   {
    "language_code": "en",
    "limit": 50,
    "location_code": 2840,
    "target": "nike.com"
   },
   {
    "limit": 100,
    "target": "adidas.com"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "DataForSEO API key = base64(\"login:password\") from your dataforseo.com account",
    "type": "string"
   },
   "language_code": {
    "description": "Two-letter language code (default \"en\")",
    "type": "string"
   },
   "limit": {
    "description": "Max keywords to return (default 20, max 100)",
    "type": "integer"
   },
   "location_code": {
    "description": "DataForSEO location code (default 2840 = United States)",
    "type": "integer"
   },
   "order_by": {
    "description": "Optional sort, e.g. \"keyword_data.keyword_info.search_volume,desc\"",
    "type": "string"
   },
   "target": {
    "description": "Domain to analyze (no protocol), e.g. \"nike.com\"",
    "type": "string"
   }
  },
  "required": [
   "target",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "seo_domain_ranked_keywords",
 "title": "Seo Domain Ranked Keywords",
 "version": "v1.4ac1227b"
}
