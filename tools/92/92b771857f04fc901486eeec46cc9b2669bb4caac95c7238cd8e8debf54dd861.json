{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch time-series values for 1-2 UNESCO Institute for Statistics indicator codes, optionally filtered by geo unit and year range \u2014 official education, science, culture, and demographic statistics reported by UN member states. Always call unesco-data.indicator_search first to confirm a valid indicator code, and unesco-data.geounit_list to confirm geo_unit codes. Example: youth literacy rate (indicator='LR.AG15T24'), India (geo_unit='IND'), 2015-2022 (start=2015, end=2022). Data: api.uis.unesco.org, no auth required, CC BY-SA 4.0 (attribution + share-alike required).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "end": {
    "description": "End year (inclusive), e.g. 2022. Omit for the full available history.",
    "type": "integer"
   },
   "footnotes": {
    "description": "Include per-record footnotes (data source caveats) in the response.",
    "type": "boolean"
   },
   "geo_unit": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "One or more 3-letter geo unit codes (comma-separated string or array), e.g. 'IND' or 'KEN,NGA'. Omit to return data for every country/region the indicator covers (can be large \u2014 prefer narrowing with start/end too). Discover codes via unesco-data.geounit_list."
   },
   "geo_unit_type": {
    "description": "Filter to one geo unit level: NATIONAL (countries) or REGIONAL (world regions).",
    "enum": [
     "NATIONAL",
     "REGIONAL"
    ],
    "type": "string"
   },
   "indicator": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "One or two UIS indicator codes (comma-separated string or array), e.g. 'LR.AG15T24' (youth literacy rate). Discover codes via unesco-data.indicator_search."
   },
   "indicator_metadata": {
    "description": "Include full indicator metadata (definition, calculation method, data source) alongside the records.",
    "type": "boolean"
   },
   "start": {
    "description": "Start year (inclusive), e.g. 2015. Omit for the full available history.",
    "type": "integer"
   }
  },
  "required": [
   "indicator"
  ],
  "type": "object"
 },
 "name": "unesco-data.series.get_data",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "UNESCO UIS Get Data"
}
