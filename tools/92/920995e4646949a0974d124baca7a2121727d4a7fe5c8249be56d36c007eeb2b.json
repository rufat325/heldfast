{
 "description": "Create a draft. Markdown is the source of truth \u2014 HTML cannot be submitted, it is rendered server-side. The slug is derived from the title. (POST /api/v1/content/posts; needs the write:content scope)",
 "inputSchema": {
  "properties": {
   "bodyMarkdown": {
    "description": "Markdown body.",
    "type": "string"
   },
   "excerpt": {
    "description": "Short summary.",
    "type": "string"
   },
   "metaDescription": {
    "description": "SEO description.",
    "type": "string"
   },
   "metaTitle": {
    "description": "SEO title.",
    "type": "string"
   },
   "project": {
    "description": "Project id or slug, inside the key owner's portfolio.",
    "type": "string"
   },
   "title": {
    "description": "Post title.",
    "type": "string"
   }
  },
  "required": [
   "project",
   "title"
  ],
  "type": "object"
 },
 "name": "create_post"
}
