{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Robinhood Chain token discovery \u2014 live-priced tokens with market cap, liquidity, peak MC + drawdown, launchpad, and deployer reputation tier. Sortable and filterable. Tier: PRO+.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "launchpad": {
    "description": "Filter by launchpad: pons, flap, clanker, hood.fun, noxa, virtuals",
    "type": "string"
   },
   "limit": {
    "default": 50,
    "description": "Page size (1-100)",
    "maximum": 100,
    "minimum": 1,
    "type": "number"
   },
   "min_liquidity_usd": {
    "description": "Minimum current liquidity (USD)",
    "minimum": 0,
    "type": "number"
   },
   "min_mc_usd": {
    "description": "Minimum current market cap (USD)",
    "minimum": 0,
    "type": "number"
   },
   "sort": {
    "description": "Ordering (all descending, default last_trade)",
    "enum": [
     "last_trade",
     "market_cap",
     "liquidity",
     "peak_mc"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "rhc_tokens"
}
