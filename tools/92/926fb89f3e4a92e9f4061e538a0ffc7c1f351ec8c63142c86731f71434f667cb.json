{
 "description": "Creative Center popular/surging sounds \u2014 rankDiff, trend[], commercialMusic. 110s deadline, 0 credits on 502. Costs 2 credits. Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cache": {
    "description": "Set true to serve from the 24h response cache (0 credits on hit). Default false \u2014 always fetch fresh. Envelope includes cached + cachedAt on hits.",
    "type": "boolean"
   },
   "commercialMusic": {
    "description": "Only Commercial Music Library\u2013cleared sounds.",
    "type": "boolean"
   },
   "country": {
    "description": "Two-letter ISO country. Default US.",
    "type": "string"
   },
   "limit": {
    "description": "Max items to return (default 20, max 20). Flat 2 credits per call.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "newOnBoard": {
    "description": "Only sounds newly on the Top 100.",
    "type": "boolean"
   },
   "page": {
    "description": "Page number (default 1).",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "period": {
    "description": "7, 30, or 120 days (180\u2192120). Default 7.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "rankType": {
    "description": "popular | surging. Default popular.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "tiktok_popular_songs",
 "title": "TikTok Popular Songs"
}
