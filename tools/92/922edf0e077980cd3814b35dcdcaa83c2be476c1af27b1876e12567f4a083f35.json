{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Block or allow specific delivery channels for ONE object, applied across ALL categories.\n\nis_restricted semantics: true blocks delivery on that channel; false re-enables it. Each entry in channel_preferences is a {channel, is_restricted} pair.\n\nWhen NOT to use:\n- For per-category control \u2014 use update_suprsend_category_preference_object.\n- For users \u2014 use update_suprsend_user_channel_preference.\n\nSide effects: takes effect on the next workflow run; in-flight notifications may still send.\n\nReturns: updated channel-preference state on success.",
 "inputSchema": {
  "properties": {
   "channel_preferences": {
    "description": "The channel preferences to update for the users.",
    "items": {
     "properties": {
      "channel": {
       "description": "The channel identifier",
       "type": "string"
      },
      "is_restricted": {
       "description": "Whether the channel is restricted",
       "type": "boolean"
      }
     },
     "required": [
      "channel",
      "is_restricted"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "object_id": {
    "description": "The object_id of the object to update the channel preference for.",
    "type": "string"
   },
   "object_type": {
    "description": "The object_type of the object to update the channel preference for.",
    "type": "string"
   },
   "workspace": {
    "description": "SuprSend workspace to update the channel preference for.",
    "type": "string"
   }
  },
  "required": [
   "object_id",
   "object_type",
   "channel_preferences"
  ],
  "type": "object"
 },
 "name": "update_suprsend_object_channel_preference"
}
