{
 "annotations": {
  "cost": {
   "all": 2
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "The warm-outreach play: gap_analysis filtered to ONLY the linking domains that link to EVERY competitor you listed but not to you. These are the highest-conviction outreach candidates \u2014 every competitor in the set has the link, so the topic/audience is verified. Same async pattern as crawlgraph_gap_analysis (polls up to ~10s; returns job_id if still running). Counts as one gap-analysis quota call.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-crawlgraph-api-key",
    "competitor_domains": [
     "competitor1.com",
     "competitor2.com",
     "competitor3.com"
    ],
    "my_domain": "mysite.com"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "CrawlGraph API key.",
    "type": "string"
   },
   "competitor_domains": {
    "description": "2\u20135 competitor domains. The filter is strongest with more competitors; 2 is the minimum for \"warm outreach\" to be meaningful.",
    "items": {
     "type": "string"
    },
    "maxItems": 5,
    "minItems": 2,
    "type": "array"
   },
   "min_authority": {
    "description": "Optional minimum CrawlGraph authority threshold to filter results client-side.",
    "type": "number"
   },
   "my_domain": {
    "description": "Your domain.",
    "type": "string"
   }
  },
  "required": [
   "my_domain",
   "competitor_domains",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "crawlgraph_gap_outreach_targets",
 "title": "Crawlgraph Gap Outreach Targets",
 "version": "v1.7113001c"
}
