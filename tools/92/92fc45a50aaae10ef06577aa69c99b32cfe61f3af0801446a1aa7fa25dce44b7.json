{
 "description": "Fetch logs from a local container by ID or name \u2014 useful to debug a container started with test_container_locally",
 "inputSchema": {
  "properties": {
   "container_id": {
    "description": "Container ID or name",
    "type": "string"
   },
   "runtime": {
    "enum": [
     "docker",
     "podman"
    ],
    "type": "string"
   },
   "tail": {
    "description": "Number of trailing log lines to return (default: 200)",
    "type": "number"
   }
  },
  "required": [
   "container_id"
  ],
  "type": "object"
 },
 "name": "get_container_logs"
}
