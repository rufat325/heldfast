{
 "description": "Crop image. Cut a photo to a ratio \u2014 square for an avatar, 16:9 for a cover. Nothing gets scaled down. Accepts image/*, .heic, .heif, .avif; up to 50 MB per file.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "aspect": {
    "default": "1:1",
    "description": "Aspect ratio",
    "enum": [
     "1:1",
     "4:3",
     "3:4",
     "16:9",
     "9:16",
     "3:2",
     "2:3"
    ],
    "type": "string"
   },
   "format": {
    "default": "same",
    "description": "Output format. \u00abAs is\u00bb keeps the original",
    "enum": [
     "same",
     "jpeg",
     "png",
     "webp"
    ],
    "type": "string"
   },
   "outputDir": {
    "description": "Where to put the results. Defaults to the folder each source file came from. Existing files are never overwritten.",
    "type": "string"
   },
   "paths": {
    "description": "Paths to the files to process, absolute or relative to the working directory.",
    "items": {
     "type": "string"
    },
    "maxItems": 25,
    "minItems": 1,
    "type": "array"
   },
   "position": {
    "default": "center",
    "description": "Which part to keep",
    "enum": [
     "center",
     "top",
     "bottom",
     "left",
     "right"
    ],
    "type": "string"
   },
   "quality": {
    "default": 88,
    "description": "Quality, 1\u2013100",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "paths"
  ],
  "type": "object"
 },
 "name": "image_crop"
}
