{
 "description": "Preview what session data would be deleted for the given filters. Shows affected files, directories, database rows, total bytes, and per-project/provider breakdown. ALWAYS call this before drishti_cleanup_execute to understand the impact.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "project": {
    "description": "Filter by project name/path substring",
    "type": "string"
   },
   "providers": {
    "description": "Filter to specific providers (e.g. ['claude-code','cursor'])",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "scope": {
    "description": "Time scope: today, week (last 7d), month (calendar month), or all (default)",
    "enum": [
     "today",
     "week",
     "month",
     "all"
    ],
    "type": "string"
   },
   "since": {
    "description": "Start date (YYYY-MM-DD) for custom date range",
    "type": "string"
   },
   "until": {
    "description": "End date (YYYY-MM-DD) for custom date range",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "drishti_cleanup_preview"
}
