{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "\u26a1 ACTION: Delete a DNS record from a Cloudflare zone by record ID. Removes the record immediately (Cloudflare)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "record_id": {
    "description": "DNS record ID to delete (from dns_list results)",
    "minLength": 1,
    "type": "string"
   },
   "zone_id": {
    "description": "Cloudflare Zone ID",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "zone_id",
   "record_id"
  ],
  "type": "object"
 },
 "name": "infra.cloudflare.dns_delete",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Delete DNS Record"
}
