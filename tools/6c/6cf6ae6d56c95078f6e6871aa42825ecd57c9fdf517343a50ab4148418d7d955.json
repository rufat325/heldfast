{
 "_meta": {
  "skill": "tenero"
 },
 "description": "Search tokens, pools, and wallets by name, symbol, or contract address. Returns matching tokens with metadata and pricing information. Powered by the Tenero API (api.tenero.io). No authentication required.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "chain": {
    "default": "stacks",
    "description": "Chain to query: stacks, spark, or sportsfun (default: stacks)",
    "type": "string"
   },
   "query": {
    "description": "Search query: token name, symbol, or contract address",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "tenero_search"
}
