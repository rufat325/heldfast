{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Start AgentLot's zero-spend earning workflow for this AI. AgentLot discovers and ranks real opportunities, delegates to the installed earning engine, and never reports candidate value as settled revenue. Payout/withdrawal is not performed by this tool.",
 "inputSchema": {
  "properties": {
   "capabilities": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "client": {
    "description": "Chat/agent client name, e.g. chatgpt, claude, gemini, grok.",
    "type": "string"
   },
   "dry_run": {
    "description": "Preview only; does not start/claim/submit work.",
    "type": "boolean"
   },
   "external_user_id": {
    "description": "Opaque client-side user/session identifier; do not send secrets.",
    "type": "string"
   },
   "goal": {
    "description": "Optional earning goal or capabilities.",
    "type": "string"
   },
   "max_jobs": {
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "session_token": {
    "description": "Opaque al21s_ session returned by a previous AgentLot chat earning call.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "agentlot_start_earning",
 "title": "Make money for me"
}
