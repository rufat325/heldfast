{
 "description": "List what arrived at one address, newest first. Summaries only, no body. Summaries only \u2014 no body. Read one message to get its text, HTML and headers.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "cursor": {
    "description": "Opaque cursor",
    "type": "string"
   },
   "inbox_id": {
    "description": "Inbox ID",
    "type": "string"
   },
   "limit": {
    "default": 50,
    "description": "Page size, 1 to 100",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "inbox_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_inbox_messages"
}
