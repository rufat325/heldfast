{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Add a prospect to a sequence. Example: { \"prospect_id\": \"123\", \"sequence_id\": \"456\" }\n\nWORKFLOW: Find the prospect (outreach_search_prospects) and sequence (outreach_list_sequences) first.\nCOMMON MISTAKES: Prospect must not already be active in the same sequence.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "mailbox_id": {
    "description": "Mailbox ID to send from (optional, uses default)",
    "pattern": "^\\d+$",
    "type": "string"
   },
   "prospect_id": {
    "description": "Prospect ID to enroll",
    "pattern": "^\\d+$",
    "type": "string"
   },
   "sequence_id": {
    "description": "Sequence ID to enroll into",
    "pattern": "^\\d+$",
    "type": "string"
   }
  },
  "required": [
   "prospect_id",
   "sequence_id"
  ],
  "type": "object"
 },
 "name": "outreach_add_prospect_to_sequence"
}
