{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fast single-value lookup of a mutual-fund scheme's latest NAV (net asset value) from MFAPI. Returns the most recent NAV, its date, and basic scheme metadata. Use search_schemes to find a scheme_code first (e.g. 118550). Keyless.",
 "inputSchema": {
  "examples": [
   {
    "scheme_code": 118550
   }
  ],
  "properties": {
   "scheme_code": {
    "description": "AMFI scheme code, e.g. 118550. Get one from search_schemes.",
    "type": "number"
   }
  },
  "required": [
   "scheme_code"
  ],
  "type": "object"
 },
 "name": "latest_nav",
 "title": "Latest Nav",
 "version": "v1.be7af121"
}
