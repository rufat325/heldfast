{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run a web search via Firecrawl and return ranked results with URL, title, and description snippet. Takes a query string and optional limit (default 5, max 20). Returns {query, results[{url, title, description}]}.",
 "inputSchema": {
  "examples": [
   {
    "query": "best open-source vector databases"
   },
   {
    "limit": 10,
    "query": "machine learning frameworks"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Firecrawl API key for higher limits; omit to use the shared Pipeworx key.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of results to return (default 5, max 20).",
    "type": "number"
   },
   "query": {
    "description": "The search query, e.g. \"best open-source vector databases\".",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search",
 "title": "Search",
 "version": "v1.b20a8e70"
}
