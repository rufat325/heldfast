{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Edit or transform existing image(s) with a text instruction \u2014 restyle, add or remove elements, change background, or combine several images into one scene. The result is saved to disk and its absolute path is returned, along with a small inline preview.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "filename_prefix": {
    "description": "Short label used in the saved filename, e.g. 'hero-banner'. Sanitized to letters, digits, dashes.",
    "maxLength": 40,
    "type": "string"
   },
   "images": {
    "description": "Source image(s): absolute file paths, file:// URLs, https:// URLs, or data: URLs. Pass several images for composition or style-transfer edits.",
    "items": {
     "type": "string"
    },
    "maxItems": 4,
    "minItems": 1,
    "type": "array"
   },
   "model": {
    "description": "Model slug, e.g. 'google/gemini-2.5-flash-image' or 'openai/gpt-5-image'. Omit to use the configured default. Call list_models to see options.",
    "type": "string"
   },
   "output_dir": {
    "description": "Directory to save into (absolute, or ~ for home). Defaults to the configured output directory.",
    "type": "string"
   },
   "prompt": {
    "description": "Instruction describing the edit, e.g. 'remove the background', 'make it night time', 'combine these two photos into one scene'.",
    "maxLength": 4000,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "prompt",
   "images"
  ],
  "type": "object"
 },
 "name": "edit_image",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "failed": {
    "description": "Error messages for variations that failed",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "images": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "bytes": {
       "type": "number"
      },
      "mime": {
       "type": "string"
      },
      "model": {
       "type": "string"
      },
      "path": {
       "description": "Absolute path of the saved file",
       "type": "string"
      }
     },
     "required": [
      "path",
      "mime",
      "bytes",
      "model"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "images",
   "failed"
  ],
  "type": "object"
 },
 "title": "Edit Image"
}
