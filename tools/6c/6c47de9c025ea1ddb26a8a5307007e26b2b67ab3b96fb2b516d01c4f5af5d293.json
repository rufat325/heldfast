{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Returns native MON + canonical-token balances for an address, priced in USD via DexScreener. Skips tokens with zero balance unless `include_zero` is set. USD totals are best-effort \u2014 tokens without a Monad pair on DexScreener are reported with null prices.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "address": {
    "description": "Holder address or '.nad' name. Defaults to the authenticated user's wallet.",
    "minLength": 1,
    "type": "string"
   },
   "include_zero": {
    "default": false,
    "description": "Include canonical tokens with zero balance in the response.",
    "type": "boolean"
   },
   "network": {
    "description": "Monad network \u2014 \"mainnet\" or \"testnet\". Defaults to the server's configured network.",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_portfolio",
 "title": "Get portfolio (balances + USD values)"
}
