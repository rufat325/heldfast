{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Return bounded Recurly customer accounts with codes, contact details, state, tax status, balances, and timestamps. Answers which billing accounts are active, inactive, or recently created.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-recurly-private-api-key",
    "limit": 20,
    "state": "active"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Recurly private API key",
    "type": "string"
   },
   "begin_time": {
    "description": "ISO 8601 lower bound for creation time",
    "type": "string"
   },
   "cursor": {
    "description": "Cursor from a previous response next link",
    "type": "string"
   },
   "end_time": {
    "description": "ISO 8601 upper bound for creation time",
    "type": "string"
   },
   "limit": {
    "description": "Maximum results, default 20 and maximum 200",
    "maximum": 200,
    "minimum": 1,
    "type": "number"
   },
   "state": {
    "enum": [
     "active",
     "inactive"
    ],
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "recurly_list_accounts",
 "title": "Recurly List Accounts",
 "version": "v1.7f2ba1d0"
}
