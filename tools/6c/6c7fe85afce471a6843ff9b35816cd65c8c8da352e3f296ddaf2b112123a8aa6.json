{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "One AML/KYC-style verdict for a counterparty from a single call: wallet sanctions + on-chain risk, name sanctions screening, GLEIF company identity and technical counterparty risk, merged into clear | review | blocked with an itemized findings list. $0.02. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "address": {
    "description": "EVM wallet address to screen (0x + 40 hex)",
    "type": "string"
   },
   "company_name": {
    "description": "Legal company name to verify via GLEIF",
    "type": "string"
   },
   "domain": {
    "description": "Domain to run technical counterparty risk on",
    "type": "string"
   },
   "name": {
    "description": "Person or entity name to screen against sanctions lists",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "agentbit.counterparty_verify",
 "title": "Counterparty Verify (AML)"
}
