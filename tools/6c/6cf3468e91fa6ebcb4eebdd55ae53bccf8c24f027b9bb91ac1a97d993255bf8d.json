{
 "_meta": {
  "skill": "nostr"
 },
 "description": "Get a Nostr profile (kind:0 metadata) for any public key. No wallet required.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "pubkey": {
    "description": "Public key to look up (hex or npub bech32 format).",
    "type": "string"
   },
   "relay": {
    "description": "Single relay URL to query. Defaults to all DEFAULT_RELAYS.",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "pubkey"
  ],
  "type": "object"
 },
 "name": "nostr_get_profile"
}
