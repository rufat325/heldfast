{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Licensed real-estate brokers on the Dubai Land Department register \u2014 broker number and name, licence start/end dates, the brokerage office they sit under, and the office phone published on the register.",
 "inputSchema": {
  "examples": [
   {
    "gender": "1",
    "limit": 3
   }
  ],
  "properties": {
   "gender": {
    "description": "Registered gender: \"0\" male, \"1\" female. Omit for all.",
    "enum": [
     "0",
     "1"
    ],
    "type": "string"
   },
   "include_arabic": {
    "description": "Keep the Arabic (`*_AR`) twin of each bilingual column. Default false.",
    "type": "boolean"
   },
   "limit": {
    "description": "Rows to return, 1-200 (default 25)",
    "type": "number"
   },
   "offset": {
    "description": "Rows to skip, for paging through `total` (default 0)",
    "type": "number"
   },
   "sort": {
    "description": "Sort as \"<COLUMN>_ASC\" or \"<COLUMN>_DESC\" using a column name from the rows, e.g. \"TRANS_VALUE_DESC\". Omit for the upstream default order.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "dld_list_brokers",
 "title": "Dld List Brokers",
 "version": "v1.e8cfb21f"
}
