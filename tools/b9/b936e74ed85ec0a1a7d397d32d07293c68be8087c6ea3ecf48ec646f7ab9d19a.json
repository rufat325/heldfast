{
 "description": "Read the feature-management audit log for a window. Returns events plus derived added[] and status_changed[] arrays.",
 "inputSchema": {
  "properties": {
   "feature_code": {
    "type": "string"
   },
   "since": {
    "description": "Window: shorthand like \"24h\"/\"7d\"/\"30m\", or an ISO date. Default 24h.",
    "type": "string"
   },
   "tool": {
    "description": "Filter to one tool name, e.g. \"set_feature_status\"",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "roadmap_diff"
}
