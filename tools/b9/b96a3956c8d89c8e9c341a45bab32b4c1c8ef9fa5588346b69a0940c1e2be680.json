{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true,
  "title": "List skilled micro-workers you can delegate to"
 },
 "description": "Skilled micro-worker roles (designer, coder, QA, marketer, ...) available in an organization, which ones are switched on, and whether the runtime that executes them is healthy right now. Call this before delegating specialist work so you set `role` on create_task to a slug that will actually be picked up.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "client_id": {
    "description": "Resolve the organization from this client instead of your active organization.",
    "format": "uuid",
    "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_micro_workers",
 "title": "List skilled micro-workers you can delegate to"
}
