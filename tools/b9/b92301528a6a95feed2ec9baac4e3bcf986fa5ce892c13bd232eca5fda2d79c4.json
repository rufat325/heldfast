{
 "description": "Sources with status, last run and item counts. Optionally one collection.",
 "inputSchema": {
  "properties": {
   "collection": {
    "description": "Collection slug, e.g. games",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_sources"
}
