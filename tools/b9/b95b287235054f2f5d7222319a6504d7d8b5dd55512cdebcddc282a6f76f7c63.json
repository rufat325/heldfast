{
 "description": "Get the Git Leaderboard \u2014 global rankings of GitHub developers by contributions, commits, issues, PRs, reviews, or impact score. Filter by year and month.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "description": "Number of results to return (default: 10, max: 25)",
    "maximum": 25,
    "minimum": 1,
    "type": "integer"
   },
   "metric": {
    "description": "Metric to rank by: contributions (default), commits, issues, prs, reviews, or impact",
    "enum": [
     "contributions",
     "commits",
     "issues",
     "prs",
     "reviews",
     "impact"
    ],
    "type": "string"
   },
   "month": {
    "description": "Filter to a specific month (requires year)",
    "maximum": 12,
    "minimum": 1,
    "type": "integer"
   },
   "year": {
    "description": "Filter to a specific year",
    "maximum": 2030,
    "minimum": 2008,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "get_leaderboard"
}
