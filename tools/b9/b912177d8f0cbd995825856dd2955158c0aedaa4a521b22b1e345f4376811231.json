{
 "annotations": {
  "readOnlyHint": true,
  "title": "Find tag groups"
 },
 "description": "START HERE for capability-map questions. The 1,500+ tag groups the catalog computes \u2014 sets of tags that demonstrably travel together across providers (Cards, Logistics, Wealth Management, Observability), the layer between a single tag and a coarse facet. Every response carries a `coverage` block: about a quarter of the vocabulary is in NO group, and any count taken from here should be read against `ungrouped_share`. Pass exclude=cross_cutting,sparse to drop the groups that are plumbing rather than markets.",
 "inputSchema": {
  "properties": {
   "basis": {
    "description": "cooccurrence groups are evidence-based; facet groups are a weaker fallback bucket for tags with no co-occurrence edge.",
    "enum": [
     "cooccurrence",
     "facet"
    ],
    "type": "string"
   },
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "exclude": {
    "description": "Comma-separated: cross_cutting (plumbing every provider has), sparse (a chain label propagation strung together, not a cluster). Use both when drawing a capabilities map.",
    "type": "string"
   },
   "limit": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "min_tags": {
    "description": "Only groups holding at least this many tags.",
    "type": "integer"
   },
   "page": {
    "type": "integer"
   },
   "q": {
    "description": "Substring match on group name, slug or member tag.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "find_tag_groups"
}
