{
 "description": "Extract the JSON value from a language model's raw reply, or refuse. Returns recovered:false rather than inventing a value.",
 "inputSchema": {
  "properties": {
   "text": {
    "description": "the model's raw reply",
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "recover_json"
}
