{
 "description": "Parse any RSS/Atom feed to clean JSON: items with title, link, date, author, summary, categories. Paid: $0.005 via x402.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max items (default 20, max 100)",
    "type": "number"
   },
   "url": {
    "description": "Feed URL",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "parse_feed"
}
