{
 "description": "Dump MCP tool priority enforcement rules into the conversation. Call this when the AI agent is using grep/cat/Glob instead of MCP tools. Returns compact rules the AI must follow.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "project_name": {
    "description": "Project name",
    "type": "string"
   }
  },
  "required": [
   "project_name"
  ],
  "type": "object"
 },
 "name": "priority_prompt"
}
