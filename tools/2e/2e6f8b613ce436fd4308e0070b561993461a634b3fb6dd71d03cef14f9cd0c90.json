{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Stop tracking a keyword for the specified app (soft delete). The historical ranking data is preserved; re-adding the same keyword reactivates the row. Idempotent: calling on an already-removed row returns success.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "app_id": {
    "description": "The application internal ID (numeric, from list_keywords results)",
    "type": "string"
   },
   "keyword_id": {
    "description": "The keyword internal ID (numeric, from list_keywords results)",
    "type": "string"
   }
  },
  "required": [
   "keyword_id",
   "app_id"
  ],
  "type": "object"
 },
 "name": "untrack_keyword"
}
