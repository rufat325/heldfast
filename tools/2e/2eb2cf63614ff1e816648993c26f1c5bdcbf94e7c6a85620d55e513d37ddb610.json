{
 "description": "Render an agent coding session as a recap \"movie\". Default engine is hyperframes \u2014 a self-contained animated HTML page (offline, zero keys, no data out). Set engine to \"cinematic\" for a real gen-video mp4 via wavespeed.ai (BYO key: needs WAVESPEED_API_KEY in the server env and ffmpeg on PATH; falls back to hyperframes when unavailable). Returns the HTML, or the output path when `out` is set.",
 "inputSchema": {
  "properties": {
   "engine": {
    "description": "Render engine (default hyperframes).",
    "enum": [
     "hyperframes",
     "cinematic"
    ],
    "type": "string"
   },
   "events": {
    "description": "Session events: [{ \"kind\": \"task-done\", \"ts\": 1720000000000, \"payload\": { \"label\": \"...\" } }, ...]. Known kinds: task-done, tests-pass, tests-fail, error, pr-opened, pr-merged, session-end. Payload fields used: label, durationMin, files, additions, deletions, filesChanged, pr, branch, reviewers, passed.",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "out": {
    "description": "Optional path to write the HTML (or mp4 for cinematic) to.",
    "type": "string"
   },
   "ratio": {
    "description": "Player aspect ratio (default 16:9).",
    "enum": [
     "16:9",
     "9:16",
     "1:1"
    ],
    "type": "string"
   },
   "template": {
    "description": "Caption tone + pacing (default documentary).",
    "enum": [
     "documentary",
     "speedrun",
     "meme"
    ],
    "type": "string"
   },
   "title": {
    "description": "Session name on the title card.",
    "type": "string"
   }
  },
  "required": [
   "events"
  ],
  "type": "object"
 },
 "name": "render"
}
