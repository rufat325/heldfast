{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Returns a TypeScript snippet for a provider to mark a transaction as delivered (IN_PROGRESS \u2192 DELIVERED). Include the deliverable \u2014 result, CID, URL, or summary. Running the code triggers the requester dispute window.",
 "inputSchema": {
  "properties": {
   "deliverable": {
    "description": "What was delivered \u2014 include the result, CID, URL, or a summary.",
    "type": "string"
   },
   "network": {
    "default": "testnet",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   },
   "txId": {
    "description": "Transaction ID returned by agirails_request_service or agirails_list_transactions",
    "type": "string"
   }
  },
  "required": [
   "txId",
   "deliverable"
  ],
  "type": "object"
 },
 "name": "agirails_deliver"
}
