{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Explicitly add a fact to persistent memory. Use when the user says something the engine should remember across sessions (preferences, conventions, project facts). Complements `save_outcome` reflection, which extracts facts implicitly \u2014 this is the explicit, user-driven path. Returns the new fact id, which can be passed to `memory_forget` later.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "confidence": {
    "default": 1,
    "description": "0-1 confidence. Default 1.0 for explicit user remember. Reflection-extracted facts use 0.6-0.8.",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "object": {
    "description": "The concrete value. Example: 'TypeScript with strict mode'.",
    "type": "string"
   },
   "predicate": {
    "description": "Short verb phrase. Examples: 'prefers', 'uses', 'avoids', 'requires', 'is'.",
    "type": "string"
   },
   "scope": {
    "default": "user",
    "description": "Memory scope. Default 'user' (cross-session, cross-project). Use 'project:<name>' for project-local memory, 'session:<id>' for ephemeral session-only memory.",
    "type": "string"
   },
   "subject": {
    "description": "Who/what the fact is about. Examples: 'user', 'project', 'this codebase', a person's name.",
    "type": "string"
   }
  },
  "required": [
   "subject",
   "predicate",
   "object"
  ],
  "type": "object"
 },
 "name": "memory_remember",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true,
  "properties": {
   "confidence": {
    "type": "number"
   },
   "id": {
    "type": "number"
   },
   "message": {
    "type": "string"
   },
   "object": {
    "type": "string"
   },
   "predicate": {
    "type": "string"
   },
   "scope": {
    "type": "string"
   },
   "subject": {
    "type": "string"
   },
   "success": {
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "title": "Remember a fact"
}
