{
 "description": "Manipulate the device keychain: add root certificates, add certificates, or reset the entire keychain.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "\"add-root-cert\" to add trusted root CA, \"add-cert\" to add certificate, \"reset\" to clear keychain",
    "enum": [
     "add-root-cert",
     "add-cert",
     "reset"
    ],
    "type": "string"
   },
   "deviceId": {
    "description": "Device (default: booted)",
    "type": "string"
   },
   "path": {
    "description": "Path to certificate file (required for add-root-cert and add-cert)",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "simulator_keychain"
}
