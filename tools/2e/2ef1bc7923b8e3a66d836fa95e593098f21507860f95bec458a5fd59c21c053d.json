{
 "description": "Starts an ephemeral local signing session and returns a deep link to the Printr web app where the user can sign the transaction using their browser wallet (MetaMask / Phantom). Call this after printr_create_token when the user wants to sign via browser rather than providing a raw private key. Present the returned URL to the user and ask them to open it. After the user confirms they have signed, proceed to poll printr_get_deployments.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "chain_type": {
    "description": "Chain type of the unsigned transaction",
    "enum": [
     "evm",
     "svm"
    ],
    "type": "string"
   },
   "payload": {
    "description": "Full unsigned tx payload returned by printr_create_token"
   },
   "rpc_url": {
    "description": "Optional RPC endpoint override for signing",
    "format": "uri",
    "type": "string"
   },
   "token_id": {
    "description": "Telecoin ID (hex) returned by printr_create_token",
    "type": "string"
   },
   "token_meta": {
    "description": "Token metadata to display in the signing UI. Include name, symbol, description, and current image URL so the user can preview the token and optionally replace its image before signing.",
    "properties": {
     "description": {
      "description": "Token description",
      "type": "string"
     },
     "image_url": {
      "description": "URL of the current token image",
      "format": "uri",
      "type": "string"
     },
     "name": {
      "description": "Token name",
      "type": "string"
     },
     "symbol": {
      "description": "Token ticker symbol",
      "type": "string"
     }
    },
    "required": [
     "name",
     "symbol"
    ],
    "type": "object"
   }
  },
  "required": [
   "chain_type",
   "payload",
   "token_id"
  ],
  "type": "object"
 },
 "name": "printr_open_web_signer",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "api_port": {
    "description": "Port of the local session API",
    "type": "number"
   },
   "expires_at": {
    "description": "Session expiry timestamp (epoch ms)",
    "type": "number"
   },
   "session_token": {
    "description": "Ephemeral session token",
    "type": "string"
   },
   "url": {
    "description": "Deep link to the Printr web app signing page",
    "type": "string"
   }
  },
  "required": [
   "url",
   "session_token",
   "api_port",
   "expires_at"
  ],
  "type": "object"
 }
}
