{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the dimensions and valid codes for an ISTAT dataflow (dataflow_id from istat.dataflows, e.g. \"101_1015_DF_DCSP_COLTIVAZIONI_1\" for crop areas and production) \u2014 frequency, territory, and measure dimensions each with their code list (capped to 200 codes per dimension, with a total_codes count). Use this to interpret the dimension labels returned by istat.data, or to build a narrower key filter. Data: esploradati.istat.it (ISTAT SDMX public REST API), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataflow_id": {
    "description": "ISTAT dataflow id, from istat.dataflows (e.g. \"101_1015_DF_DCSP_COLTIVAZIONI_1\" for crop areas and production). Returns the dimension list (e.g. FREQ, REF_AREA, DATA_TYPE) with each dimension's valid codes (capped to 200 per dimension, with a total_codes count) \u2014 needed to interpret istat.data results and build a key filter.",
    "minLength": 1,
    "type": "string"
   },
   "version": {
    "description": "Dataflow version, from istat.dataflows (e.g. \"1.0\"). Omit to use the latest version.",
    "type": "string"
   }
  },
  "required": [
   "dataflow_id"
  ],
  "type": "object"
 },
 "name": "istat.reference.structure",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "ISTAT Dataflow Structure"
}
