{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Trace centrelines / G-code"
 },
 "description": "Trace line art to single-stroke centreline SVG (for plotters, lasers, CNC). Optionally emit G-code with tool=laser|pen.",
 "inputSchema": {
  "properties": {
   "gcode": {
    "description": "Emit G-code toolpaths instead of an SVG. Changes the output format.",
    "type": "boolean"
   },
   "input": {
    "description": "Path to the line-art image",
    "type": "string"
   },
   "output": {
    "description": "Optional output path; extension should match the format produced",
    "type": "string"
   },
   "tool": {
    "description": "G-code dialect; only used when gcode is true",
    "enum": [
     "laser",
     "pen"
    ],
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "centerline"
}
