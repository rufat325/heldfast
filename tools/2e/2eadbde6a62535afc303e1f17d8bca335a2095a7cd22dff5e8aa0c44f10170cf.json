{
 "description": "Drain remaining funds from a deployment wallet back to the treasury. NOTE: drain runs automatically inside printr_launch_token \u2014 only call this tool manually to recover a stuck wallet (e.g. after a crash or if printr_launch_token was not called). Automatically calculates gas fees and drains the maximum possible amount. Can recover wallets after MCP restart using persisted state and PRINTR_DEPLOYMENT_PASSWORD.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "chain": {
    "description": "CAIP-2 chain ID (e.g. 'solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp' for Solana mainnet)",
    "type": "string"
   },
   "keep_minimum": {
    "default": "0",
    "description": "Minimum amount to keep in the wallet (default: 0, drain everything possible)",
    "type": "string"
   },
   "wallet_id": {
    "description": "Keystore wallet ID to drain. If not provided, uses the active deployment wallet (from memory or recovered from persisted state after restart).",
    "type": "string"
   }
  },
  "required": [
   "chain"
  ],
  "type": "object"
 },
 "name": "printr_drain_deployment_wallet",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "drained_amount": {
    "description": "Amount drained in human-readable units",
    "type": "string"
   },
   "drained_atomic": {
    "description": "Amount drained in atomic units (lamports/wei)",
    "type": "string"
   },
   "from_address": {
    "description": "Deployment wallet address that was drained",
    "type": "string"
   },
   "remaining_balance": {
    "description": "Remaining balance in the deployment wallet",
    "type": "string"
   },
   "symbol": {
    "description": "Native token symbol",
    "type": "string"
   },
   "to_address": {
    "description": "Treasury wallet address that received funds",
    "type": "string"
   },
   "tx_hash": {
    "description": "EVM transaction hash",
    "type": "string"
   },
   "tx_signature": {
    "description": "Solana transaction signature",
    "type": "string"
   },
   "wallet_id": {
    "description": "Keystore wallet ID that was drained",
    "type": "string"
   }
  },
  "required": [
   "drained_amount",
   "drained_atomic",
   "symbol",
   "from_address",
   "to_address",
   "remaining_balance",
   "wallet_id"
  ],
  "type": "object"
 }
}
