{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the UK Environment Agency hydrology monitoring network for river, groundwater, and rainfall stations, filterable by river name, freetext label search, or what the station measures (waterFlow, waterLevel, rainfall, temperature, groundwaterLevel). Returns station_id, name, river/catchment, coordinates, and measure count \u2014 use the station_id with ea-hydrology.station_measures to see its available data series. Data: environment.data.gov.uk/hydrology, UK Open Government Licence v3.0, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max stations to return (default 20, max 200).",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "observed_property": {
    "description": "Filter stations by what they measure: waterFlow (river flow m3/s), waterLevel (river level), rainfall, temperature, or groundwaterLevel (borehole).",
    "enum": [
     "waterFlow",
     "waterLevel",
     "rainfall",
     "temperature",
     "groundwaterLevel"
    ],
    "type": "string"
   },
   "river_name": {
    "description": "Filter stations by river name, partial match (e.g. \"River Thames\").",
    "maxLength": 200,
    "type": "string"
   },
   "search": {
    "description": "Freetext search matching the station label/name (e.g. \"Farmoor\").",
    "maxLength": 200,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ea-hydrology.stations.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "EA Hydrology Station Search"
}
