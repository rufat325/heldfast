{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "list-user-meeting-attendance-reports"
 },
 "description": "The attendance reports of an online meeting. Read-only.\n\nTIP: Lists attendance reports for a Teams online meeting on behalf of a user. Each report covers ONE session of the meeting \u2014 recurring meetings have multiple reports (one per occurrence), and a meeting that drops and resumes generates separate reports. Each has id, totalParticipantCount, meetingStartDateTime, meetingEndDateTime. Reports are only available AFTER the meeting ends (typically within 1-2 hours). REQUIRES Application Access Policy on the target user. The attendanceRecords array is empty in list responses \u2014 use get-user-meeting-attendance-report or pass $expand=attendanceRecords to retrieve participant-level details. Supports $filter, $top, $skip, $select, $orderby.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "count": {
    "description": "count",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "filter": {
    "description": "OData $filter expression, e.g. status eq 'inProgress'",
    "type": "string"
   },
   "onlineMeetingId": {
    "description": "Path parameter: onlineMeetingId",
    "type": "string"
   },
   "orderby": {
    "description": "orderby",
    "type": "string"
   },
   "search": {
    "description": "search",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "skip": {
    "description": "skip",
    "type": "string"
   },
   "top": {
    "description": "Page size (Graph $top). Start small (5-15) so responses fit the model context. Use @odata.nextLink from the response for more rows.",
    "type": "number"
   },
   "userId": {
    "description": "Path parameter: userId",
    "type": "string"
   }
  },
  "required": [
   "userId",
   "onlineMeetingId"
  ],
  "type": "object"
 },
 "name": "list-user-meeting-attendance-reports"
}
