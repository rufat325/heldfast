{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Run ordered BatchOpCatalog ops in one plugin round trip. For unfamiliar mutations, inspect get_batch_op_spec then use validateOnly.",
 "inputSchema": {
  "properties": {
   "channel": {
    "description": "Optional. Target a specific connected file by its channel id...",
    "type": "string"
   },
   "continueOnError": {
    "description": "Override the default stop policy: true = run all ops and report...",
    "type": "boolean"
   },
   "ops": {
    "description": "Ordered ops. Each: {type: BatchOpCatalog op name, nodeIds?:...",
    "items": {
     "properties": {
      "nodeIds": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "params": {
       "type": "object"
      },
      "type": {
       "description": "BatchOpCatalog op name, e.g. create_frame, set_fills, get_node",
       "type": "string"
      }
     },
     "required": [
      "type"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "origin": {
    "description": "Origin: orchestrator/self=wolfgang; workers use assigned name....",
    "enum": [
     "wolfgang",
     "grace",
     "theo",
     "sunho",
     "zoe",
     "taewon",
     "emma",
     "alex",
     "rick"
    ],
    "type": "string"
   },
   "validateOnly": {
    "description": "Validate the declarative batch/FigmaPlan payload and return a...",
    "type": "boolean"
   }
  },
  "required": [
   "ops",
   "origin"
  ],
  "type": "object"
 },
 "name": "batch"
}
