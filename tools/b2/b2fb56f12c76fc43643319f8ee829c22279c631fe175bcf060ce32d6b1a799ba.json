{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Initialize project.faf"
 },
 "description": "Create a project.faf \u2014 the IANA-registered context file (application/vnd.faf+yaml) that gives Grok persistent project DNA (stack, structure, intent) in one portable file, so an agent reads the whole project cold each session instead of re-discovering it. Writes project.faf to the target directory (created if missing) and refuses to overwrite an existing context file unless `force` is set. Run once at project start, or with `force` to regenerate, then call faf_score to check readiness. Returns a text confirmation with the written file path and the detected project type.",
 "inputSchema": {
  "properties": {
   "directory": {
    "description": "Absolute or ~-relative project directory to initialize; created if it does not exist. Defaults to the current working directory.",
    "type": "string"
   },
   "force": {
    "description": "Overwrite an existing project.faf. Default false: if a context file already exists, faf_init refuses and leaves it untouched.",
    "type": "boolean"
   },
   "path": {
    "description": "Alias for `directory`; takes precedence if both are supplied.",
    "type": "string"
   },
   "projectName": {
    "description": "Project name used to resolve the directory via the Projects convention when no directory/path is given.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "faf_init"
}
