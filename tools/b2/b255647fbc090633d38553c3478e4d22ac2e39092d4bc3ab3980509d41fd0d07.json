{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Deshred pre-confirm deploys filtered to a single deployer wallet \u2014 audit a deployer's recent launches before tracking them. ULTRA only.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "default": 50,
    "description": "Max results",
    "maximum": 200,
    "minimum": 1,
    "type": "number"
   },
   "wallet": {
    "description": "Deployer wallet address (base58)",
    "type": "string"
   }
  },
  "required": [
   "wallet"
  ],
  "type": "object"
 },
 "name": "madeonsol_sniper_by_deployer"
}
