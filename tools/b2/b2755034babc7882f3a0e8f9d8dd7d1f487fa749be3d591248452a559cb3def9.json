{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Create or update a key in memory. Rejects credential-shaped keys or values. Requires explicit_user_intent: true. Returns whether the row was created or updated.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "explicit_user_intent": {
    "const": true,
    "description": "Must be true. Set ONLY when the current user message explicitly asks the agent to modify memory (set/forget/export). Do not infer intent.",
    "type": "boolean"
   },
   "key": {
    "description": "Stable identifier for the memory entry. Free-form text. Treat as case-sensitive.",
    "maxLength": 512,
    "minLength": 1,
    "type": "string"
   },
   "metadata": {
    "additionalProperties": {},
    "description": "Optional small JSON object with provenance/notes. Subject to same secret-detection rules.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "tags": {
    "description": "Optional list of tags for grouping/filter/bulk-delete.",
    "items": {
     "maxLength": 64,
     "minLength": 1,
     "pattern": "^[^,]+$",
     "type": "string"
    },
    "maxItems": 32,
    "type": "array"
   },
   "ttl_seconds": {
    "description": "Optional TTL in seconds. After this, the entry is lazy-deleted on next read.",
    "exclusiveMinimum": 0,
    "maximum": 315360000,
    "type": "integer"
   },
   "value": {
    "description": "The value to store. Will be JSON-serialized. Rejected if it looks like a credential."
   }
  },
  "required": [
   "key",
   "value",
   "explicit_user_intent"
  ],
  "type": "object"
 },
 "name": "memory_set"
}
