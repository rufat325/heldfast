{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List Workers KV namespaces in an account.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "account_id": {
    "description": "Cloudflare account ID. Falls back to CLOUDFLARE_ACCOUNT_ID env var when omitted.",
    "type": "string"
   },
   "fetch_all": {
    "description": "When true, follow pagination and return all pages combined (ignores page/per_page).",
    "type": "boolean"
   },
   "page": {
    "type": "number"
   },
   "per_page": {
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list_kv_namespaces"
}
