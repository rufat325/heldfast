{
 "description": "Create a new matter (lead). By default this also creates a contact from first/last name. Pass contact_id to attach an existing contact, or match_contact_by to reuse a contact matched on email/name/phone (falls back to creating one). Pass company_id or company_name for a company matter. Tags that don't exist yet are created automatically.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "case_blurb": {
    "description": "Short case description.",
    "type": "string"
   },
   "case_title": {
    "type": "string"
   },
   "company_id": {
    "description": "Create as a company matter for this company.",
    "type": "integer"
   },
   "company_name": {
    "description": "Create as a company matter, matching the company by name.",
    "type": "string"
   },
   "contact_id": {
    "description": "Attach an existing contact. Overrides match_contact_by.",
    "type": "integer"
   },
   "email": {
    "type": "string"
   },
   "first_name": {
    "description": "Client first name.",
    "type": "string"
   },
   "last_name": {
    "description": "Client last name.",
    "type": "string"
   },
   "match_contact_by": {
    "description": "Reuse an existing contact matched on this attribute instead of creating a duplicate.",
    "enum": [
     "email",
     "name",
     "phone"
    ],
    "type": "string"
   },
   "notes": {
    "description": "Notes to create with the matter.",
    "items": {
     "additionalProperties": false,
     "properties": {
      "body": {
       "description": "Note body text.",
       "type": "string"
      },
      "name": {
       "description": "Note title.",
       "type": "string"
      }
     },
     "required": [
      "name",
      "body"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "phone": {
    "type": "string"
   },
   "practice_area_id": {
    "description": "From list-practice-areas.",
    "type": "integer"
   },
   "sub_status_id": {
    "description": "From list-sub-statuses.",
    "type": "integer"
   },
   "tags": {
    "description": "Tag names to attach (auto-created if missing).",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "first_name",
   "last_name"
  ],
  "type": "object"
 },
 "name": "create-matter"
}
