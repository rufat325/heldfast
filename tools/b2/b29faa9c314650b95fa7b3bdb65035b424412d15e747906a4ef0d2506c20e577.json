{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Market Profile breakout acceptance: did price accept (hold) beyond the value area / range, or reject back inside (fakeout)? Optional buy/sell delta. Use for \"did the break above VAH get accepted?\". Returns: state, accepted (boolean), direction, confidence, key_levels (VAH/VAL/VPOC), scenario_framing, invalidation level.",
 "inputSchema": {
  "properties": {
   "candles": {
    "description": "Optional OHLCV for the session; omit to fetch from venue (reproducible + 0 COGS when supplied)",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "include_delta": {
    "description": "Include buy/sell delta analysis (default true)",
    "type": "boolean"
   },
   "instrument": {
    "description": "Symbol, e.g. BTCUSDT",
    "type": "string"
   },
   "prev_candles": {
    "description": "Optional OHLCV for the previous session",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "session_date": {
    "description": "Session date YYYY-MM-DD (UTC)",
    "type": "string"
   },
   "timeframe": {
    "description": "Candle timeframe (default 15m)",
    "enum": [
     "1m",
     "5m",
     "15m",
     "30m"
    ],
    "type": "string"
   },
   "value_area_rule": {
    "description": "Value-area fraction 0.5\u20130.9 (default 0.70)",
    "type": "number"
   },
   "venue": {
    "description": "Exchange to fetch candles from when candles[] not supplied",
    "enum": [
     "binance",
     "bybit"
    ],
    "type": "string"
   }
  },
  "required": [
   "instrument",
   "venue",
   "session_date"
  ],
  "type": "object"
 },
 "name": "workflow.run_breakout_acceptance"
}
