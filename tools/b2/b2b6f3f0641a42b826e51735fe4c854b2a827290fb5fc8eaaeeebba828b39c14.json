{
 "description": "Start a RaportAgent market/compliance research report. Generation takes ~7\u201315 min, so this returns a report_id immediately (status: queued) unless you set wait_seconds. Use get_report_status to poll, then get_report to fetch the finished markdown. Each report costs 1 credit.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "depth": {
    "default": "deep",
    "description": "deep = ~15 min, thorough; fast = ~7 min.",
    "enum": [
     "deep",
     "fast"
    ],
    "type": "string"
   },
   "query": {
    "description": "The market/topic to research, e.g. 'EV charging infrastructure Europe 2026'.",
    "minLength": 3,
    "type": "string"
   },
   "template": {
    "description": "Report shape. Use 'compliance' for a legal/regulatory brief.",
    "enum": [
     "compliance",
     "pitch",
     "saas",
     "ecommerce",
     "realestate",
     "local"
    ],
    "type": "string"
   },
   "wait_seconds": {
    "default": 0,
    "description": "If >0, poll until the report completes or this many seconds elapse (max 1200).",
    "maximum": 1200,
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "generate_report"
}
