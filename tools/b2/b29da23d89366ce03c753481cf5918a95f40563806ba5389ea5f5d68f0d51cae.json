{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List Lighter markets"
 },
 "description": "List or search the perpetual markets on a Lighter deployment. Mainnet lists id 182, H100, a perpetual on the rental cost of an NVIDIA H100, which the response always includes. Results are capped and the response reports the full count, so pass a query to narrow the list.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 25,
    "description": "Maximum markets to return, 1 to 100.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Case-insensitive substring match on the ticker, for example \"H100\" or \"SOL\".",
    "maxLength": 32,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "lighter_markets",
 "title": "List Lighter markets"
}
