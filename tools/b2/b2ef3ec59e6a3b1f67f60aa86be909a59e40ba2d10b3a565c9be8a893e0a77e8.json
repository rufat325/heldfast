{
 "description": "7-day censorship-event forecast for one country from Voidly Sentinel: probability, 90% interval, risk band, largest feature contributions, the most similar past incident and recent evidence links. Read-only public GET.",
 "inputSchema": {
  "properties": {
   "country_code": {
    "description": "ISO 3166-1 alpha-2 code (e.g. IR, CN, RU)",
    "type": "string"
   }
  },
  "required": [
   "country_code"
  ],
  "type": "object"
 },
 "name": "sentinel_current_risk"
}
