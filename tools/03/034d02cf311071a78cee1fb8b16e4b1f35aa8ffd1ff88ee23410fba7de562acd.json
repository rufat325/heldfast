{
 "description": "Cancel a scheduled tenant erasure. That is what the seven days are for: after they pass, there is nothing left to cancel.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "request_id": {
    "description": "Deletion request ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "request_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_deletion_cancel"
}
