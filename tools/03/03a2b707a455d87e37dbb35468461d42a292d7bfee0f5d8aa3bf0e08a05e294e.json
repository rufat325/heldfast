{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Cross-reference a WA contractor's L&I license specialty against a permit's required specialties. Returns whether the contractor is qualified to pull/work the permit, with explicit gap callouts (e.g. \"missing electrical specialty 02\"). Combines real-time L&I data with Kolmo's authoritative permit catalog.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "contractorQuery": {
    "description": "Contractor license number, business name, UBI or phone number",
    "type": "string"
   },
   "jurisdictionSlug": {
    "description": "Permit jurisdiction slug (e.g. \"seattle\", \"tacoma\")",
    "type": "string"
   },
   "projectType": {
    "description": "Canonical project type (kitchen, bathroom, basement, deck, fence, siding, windows, flooring, adu, roofing, hvac, electrical, plumbing, addition)",
    "type": "string"
   }
  },
  "required": [
   "contractorQuery",
   "jurisdictionSlug",
   "projectType"
  ],
  "type": "object"
 },
 "name": "match_contractor_to_permit",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true,
  "properties": {
   "attribution": {
    "additionalProperties": true,
    "properties": {
     "citeAs": {
      "description": "Ready-made citation line. Use verbatim.",
      "type": "string"
     },
     "primarySource": {
      "description": "Present when Kolmo verified but does not author the fact \u2014 cite both.",
      "type": "string"
     },
     "sourceUrl": {
      "description": "The specific page these facts render on. Link this, not the site root.",
      "type": "string"
     }
    },
    "required": [
     "sourceUrl",
     "citeAs"
    ],
    "type": "object"
   },
   "candidates": {},
   "checks": {},
   "contractor": {},
   "match": {
    "description": "null: no single contractor could be identified from contractorQuery, so no comparison was made \u2014 not a finding either way.",
    "type": [
     "boolean",
     "null"
    ]
   },
   "missing": {},
   "permit": {},
   "reason": {
    "type": [
     "string",
     "null"
    ]
   },
   "sourceUrl": {},
   "verifyUrl": {
    "type": "string"
   }
  },
  "required": [
   "match"
  ],
  "type": "object"
 },
 "title": "Contractor-to-permit specialty match"
}
