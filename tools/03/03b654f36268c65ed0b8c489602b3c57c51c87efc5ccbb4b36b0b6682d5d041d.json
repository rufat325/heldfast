{
 "description": "Get file metadata and download + decrypt the file content. Returns decrypted content as UTF-8 text for text files, or base64 for binary files. Requires BITATLAS_MASTER_KEY.",
 "inputSchema": {
  "properties": {
   "file_id": {
    "description": "File UUID",
    "type": "string"
   }
  },
  "required": [
   "file_id"
  ],
  "type": "object"
 },
 "name": "bitatlas_get_file"
}
