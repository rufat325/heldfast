{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "readOnlyHint": false
 },
 "description": "Remove a file from the external/ directory in the context library. Deletes the file and regenerates the index. Use when external content is no longer needed.",
 "inputSchema": {
  "properties": {
   "filename": {
    "description": "Filename in external/ to remove",
    "type": "string"
   }
  },
  "required": [
   "filename"
  ],
  "type": "object"
 },
 "name": "bear_context_remove_external"
}
