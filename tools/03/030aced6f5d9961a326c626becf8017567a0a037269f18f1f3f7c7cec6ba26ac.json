{
 "annotations": {
  "openWorldHint": true
 },
 "description": "Escape hatch: invoke any Gatekeeper tool by name (e.g. memory.query) with an args object. The call is policy-gated exactly like the typed tools. The tool name is validated; identity/role/origin are fixed by the server and cannot be set here.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "args": {
    "additionalProperties": {},
    "default": {},
    "description": "Arguments object for the tool.",
    "type": "object"
   },
   "tool": {
    "description": "Gatekeeper tool name, e.g. \"memory.query\" (dotted lowercase; no slashes).",
    "maxLength": 64,
    "pattern": "^[a-z][a-z0-9]*(\\.[a-z][a-z0-9]*)*$",
    "type": "string"
   }
  },
  "required": [
   "tool"
  ],
  "type": "object"
 },
 "name": "gatekeeper_call",
 "title": "Call any Gatekeeper tool (generic, policy-gated)"
}
