{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get historical Net Asset Value (NAV) records for an India mutual fund scheme, most recent first. Returns daily NAV values in Indian Rupees (INR) with dates. Some schemes have 20+ years of history. Useful for performance analysis, CAGR calculations, SIP return modelling, and trend visualization. Limit parameter controls how many records are returned (default 100, max 1000). Use mfapi.schemes.search to find the scheme_code for a fund by name. Data source: MFAPI (api.mfapi.in), MIT license, no authentication required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Maximum number of historical NAV records to return, most recent first (1\u20131000, default 100)",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "scheme_code": {
    "description": "AMFI scheme code (integer). Use mfapi.scheme_search to find the code for a fund.",
    "exclusiveMinimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "scheme_code"
  ],
  "type": "object"
 },
 "name": "mfapi.nav.history",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "India Mutual Fund NAV History"
}
