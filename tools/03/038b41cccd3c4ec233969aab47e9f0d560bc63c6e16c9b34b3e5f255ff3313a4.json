{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search Edamam's food database for foods matching a query and return per-100g macros (calories, protein, fat, carbs). Example: search_food({ query: \"cheddar cheese\", limit: 10 })",
 "inputSchema": {
  "examples": [
   {
    "limit": 10,
    "query": "cheddar cheese"
   },
   {
    "query": "almond milk"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional Edamam credentials (comma-separated nutrition,recipe,food pairs of app_id:app_key). Omit to use the platform key.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum foods to return (default 15)",
    "type": "number"
   },
   "query": {
    "description": "Food name to search for, e.g. \"cheddar cheese\", \"banana\", \"almond milk\"",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_food",
 "title": "Search Food",
 "version": "v1.51c13451"
}
