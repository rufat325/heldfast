{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Coins trending on CoinGecko in the last 24 hours, by search volume \u2014 a heartbeat for \"what's hot in crypto right now\". No parameters. Returns trending coins with rank, USD price, and 24h change. (Upstream reports market cap and volume as pre-formatted display strings, not numbers, so those are omitted rather than presented as numeric data.)",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {},
  "type": "object"
 },
 "name": "coingecko_get_trending",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "anyOf": [
   {
    "not": {
     "required": [
      "error"
     ]
    },
    "required": [
     "coins",
     "nftCount",
     "attribution"
    ]
   },
   {
    "required": [
     "error"
    ]
   }
  ],
  "properties": {
   "attribution": {
    "description": "Required data-source attribution.",
    "type": "string"
   },
   "coins": {
    "description": "Trending coins ranked by CoinGecko search volume. Data provided by CoinGecko.",
    "items": {
     "additionalProperties": false,
     "description": "A trending coin.",
     "properties": {
      "id": {
       "description": "CoinGecko slug \u2014 chain into other tools.",
       "type": "string"
      },
      "marketCapRank": {
       "description": "Global market-cap rank, when available.",
       "type": "number"
      },
      "name": {
       "description": "Display name.",
       "type": "string"
      },
      "priceBtc": {
       "description": "Current price in BTC, when available.",
       "type": "number"
      },
      "priceChangePercentage24hUsd": {
       "description": "24-hour USD price change percentage, when available.",
       "type": "number"
      },
      "priceUsd": {
       "description": "Current price in USD, when available.",
       "type": "number"
      },
      "symbol": {
       "description": "Ticker symbol.",
       "type": "string"
      }
     },
     "required": [
      "id",
      "name",
      "symbol"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "error": {
    "additionalProperties": {},
    "description": "Present when the call failed. Absent on success.",
    "properties": {
     "code": {
      "description": "JSON-RPC error code for this failure.",
      "maximum": 9007199254740991,
      "minimum": -9007199254740991,
      "type": "integer"
     },
     "data": {
      "additionalProperties": {},
      "properties": {
       "reason": {
        "description": "Machine-readable failure mode. Declared by this tool: `rate_limited`: CoinGecko returned HTTP 429 \u2014 the shared keyless pool is throttled, or the Demo-tier quota was hit. `upstream_unreachable`: CoinGecko was unreachable or returned a 5xx/HTML error after all retry attempts. `invalid_response`: CoinGecko returned a body that could not be parsed as JSON. Other values are possible when a failure originates below the handler.",
        "examples": [
         "rate_limited",
         "upstream_unreachable",
         "invalid_response"
        ],
        "type": "string"
       },
       "recovery": {
        "additionalProperties": {},
        "description": "Actionable next step for the caller.",
        "properties": {
         "hint": {
          "type": "string"
         }
        },
        "required": [
         "hint"
        ],
        "type": "object"
       },
       "retryable": {
        "description": "Whether retrying may succeed.",
        "type": "boolean"
       }
      },
      "type": "object"
     },
     "message": {
      "description": "Human-readable description of what went wrong.",
      "type": "string"
     }
    },
    "required": [
     "code",
     "message"
    ],
    "type": "object"
   },
   "nftCount": {
    "description": "Number of trending NFTs upstream also reported (detail not surfaced by this tool).",
    "type": "number"
   }
  },
  "type": "object"
 },
 "title": "coingecko-mcp-server: get trending"
}
