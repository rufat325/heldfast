{
 "annotations": {
  "readOnlyHint": true,
  "title": "Get response times"
 },
 "description": "List individual first-response events for a board over a date range. Every event is a first response to a new email; use get_avg_response_time if you also need follow-up replies. Each event gives intervalTime in milliseconds, the responder, and the thread. Returns raw events, not computed metrics.",
 "inputSchema": {
  "properties": {
   "boardId": {
    "description": "The board ID",
    "type": "number"
   },
   "dateFrom": {
    "description": "Start date for the period (ISO-8601)",
    "type": "string"
   },
   "dateTo": {
    "description": "End date for the period (ISO-8601)",
    "type": "string"
   }
  },
  "required": [
   "boardId",
   "dateFrom",
   "dateTo"
  ],
  "type": "object"
 },
 "name": "get_response_times",
 "title": "Get response times"
}
