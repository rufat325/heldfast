{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Convert between formats"
 },
 "description": "Convert between formats by extension. Supports raster\u2194SVG plus vector export to .dxf/.eps/.pdf (CAD/CNC/print).",
 "inputSchema": {
  "properties": {
   "cmyk": {
    "description": "Emit CMYK colour in EPS/PDF output, for print workflows",
    "type": "boolean"
   },
   "input": {
    "description": "Path to the source file (raster or SVG)",
    "type": "string"
   },
   "output": {
    "description": "Destination path; its extension chooses the target format",
    "type": "string"
   }
  },
  "required": [
   "input",
   "output"
  ],
  "type": "object"
 },
 "name": "convert"
}
