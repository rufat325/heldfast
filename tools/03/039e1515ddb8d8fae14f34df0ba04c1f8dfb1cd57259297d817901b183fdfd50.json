{
 "description": "Convert video. Turn a video into MP4, WebM or MKV. MP4 plays everywhere, WebM is for the web, MKV is what you usually need to open somewhere else. Accepts video/*, .mp4, .mov, .mkv, .webm, .avi, .m4v; up to 500 MB per file.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "format": {
    "default": "mp4",
    "description": "Output format",
    "enum": [
     "mp4",
     "webm",
     "mkv"
    ],
    "type": "string"
   },
   "outputDir": {
    "description": "Where to put the results. Defaults to the folder each source file came from. Existing files are never overwritten.",
    "type": "string"
   },
   "paths": {
    "description": "Paths to the files to process, absolute or relative to the working directory.",
    "items": {
     "type": "string"
    },
    "maxItems": 25,
    "minItems": 1,
    "type": "array"
   },
   "quality": {
    "default": 24,
    "description": "Quality: lower number means better picture and a bigger file",
    "maximum": 35,
    "minimum": 18,
    "type": "integer"
   },
   "speed": {
    "default": "fast",
    "description": "Speed: slower means a smaller file at the same quality",
    "enum": [
     "fast",
     "balanced",
     "small"
    ],
    "type": "string"
   }
  },
  "required": [
   "paths"
  ],
  "type": "object"
 },
 "name": "video_convert"
}
