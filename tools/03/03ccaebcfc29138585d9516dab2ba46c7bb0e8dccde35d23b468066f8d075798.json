{
 "description": "Capture a simulator screenshot to a PNG path using xcrun simctl io screenshot. Use as evidence for visual review and before/after comparison.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "includeImage": {
    "default": false,
    "type": "boolean"
   },
   "outputPath": {
    "description": "Output PNG path.",
    "type": "string"
   },
   "udid": {
    "description": "Simulator UDID. Use simulator_list first when unsure.",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "udid",
   "outputPath"
  ],
  "type": "object"
 },
 "name": "screenshot",
 "title": "Capture a simulator screenshot"
}
