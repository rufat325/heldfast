{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Chart a block by naming its fields, instead of working out which cells they occupy.\nThe chart stays bound to the block: it plots whatever the named fields hold, so records added to the block later appear on their own and inserting rows or columns cannot leave it pointing at the wrong cells. A col-schema block works the same way as a row-schema one.\nUse `edit__describe_block` or `inspect` first if you do not know the field names. Blocks with a \"random\" schema have no field axis and cannot be charted this way \u2014 chart their cell range with chart__insert instead.\nFields are held by name. Renaming one breaks the link (the series goes empty); moving its column does not.",
 "inputSchema": {
  "properties": {
   "anchor": {
    "description": "Top-left cell of the chart. Defaults to two rows below the block.",
    "type": "string"
   },
   "blockId": {
    "type": "integer"
   },
   "categoryField": {
    "description": "Field whose values label the categories (the X axis). The block's key column is not addressable as a range, so name the field that reads as a label. Omitted, the categories are just 1..n.",
    "type": "string"
   },
   "chartType": {
    "description": "Defaults to col.",
    "enum": [
     "col",
     "bar",
     "line",
     "area",
     "pie",
     "doughnut",
     "scatter",
     "radar",
     "bubble",
     "stock",
     "ofPie",
     "barOfPie",
     "surface",
     "surface3d",
     "col3d",
     "bar3d",
     "line3d",
     "area3d",
     "pie3d"
    ],
    "type": "string"
   },
   "sheetIdx": {
    "description": "Zero-based sheet index.",
    "type": "integer"
   },
   "sizeInCells": {
    "properties": {
     "cols": {
      "minimum": 1,
      "type": "integer"
     },
     "rows": {
      "minimum": 1,
      "type": "integer"
     }
    },
    "type": "object"
   },
   "title": {
    "description": "Defaults to the block's schema name.",
    "type": "string"
   },
   "valueFields": {
    "description": "Field names to plot, one series each, in order.",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "sheetIdx",
   "blockId",
   "valueFields"
  ],
  "type": "object"
 },
 "name": "chart_from_block"
}
