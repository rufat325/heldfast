{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Stop one of the signed-in user's in-progress experiments (sets it to 'abandoned' \u2014 no verdict). A completed or already-abandoned experiment is rejected. Requires the user to be signed in (`supstack login`).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "id": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "supstack_experiment_abandon"
}
