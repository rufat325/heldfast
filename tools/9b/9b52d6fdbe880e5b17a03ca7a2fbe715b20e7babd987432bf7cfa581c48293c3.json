{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search for AVAILABLE domain names to register from a keyword \u2014 the domain name search / brainstorming tool. Pass a keyword (\"acme\") and get back which domains are actually free to register: the exact name across .com/.io/.ai/.co/.app/.dev, plus creative variations (getacme.com, acmehq.com, tryacme.io, acmeapp.com, \u2026). Use for \"find me an available domain for X\", \"domain name ideas for my startup\", \"is there an open domain for X\", \"suggest domain names\". Returns available domains ranked (exact match + .com first). Availability is a live registry (RDAP) signal, keyless. For a single specific domain use domain_status; to check one name across TLDs without variations use check_availability.",
 "inputSchema": {
  "examples": [
   {
    "keyword": "acme"
   },
   {
    "include_variations": true,
    "keyword": "nexus",
    "tlds": [
     "com",
     "io",
     "app"
    ]
   }
  ],
  "properties": {
   "include_variations": {
    "description": "Also try prefix/suffix variations (get-, try-, -app, -hq, \u2026). Default true. Set false for exact-keyword-only across TLDs.",
    "type": "boolean"
   },
   "keyword": {
    "description": "Base keyword or brand name to build domain ideas from, e.g. \"acme\".",
    "type": "string"
   },
   "tlds": {
    "description": "TLDs to consider (without the dot), e.g. [\"com\",\"io\",\"ai\"]. Default: com, io, ai, co, app, dev. Max 10.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "keyword"
  ],
  "type": "object"
 },
 "name": "find_available_domains",
 "title": "Find Available Domains",
 "version": "v1.215b84d6"
}
