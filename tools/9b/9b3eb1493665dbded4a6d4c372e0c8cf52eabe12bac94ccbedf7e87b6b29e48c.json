{
 "description": "Acquire or renew the optimistic lease required before mutating browser-bound batch members.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "batchId": {
    "minimum": 1,
    "type": "integer"
   },
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "expectedRevision": {
    "minimum": 1,
    "type": "integer"
   },
   "leaseDurationMs": {
    "maximum": 300000,
    "minimum": 15000,
    "type": "integer"
   },
   "leaseOwner": {
    "maxLength": 1024,
    "minLength": 1,
    "type": "string"
   },
   "leaseToken": {
    "maxLength": 1024,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "batchId",
   "expectedRevision",
   "leaseOwner",
   "leaseToken",
   "leaseDurationMs"
  ],
  "type": "object"
 },
 "name": "trackly_claim_apply_batch"
}
