{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Join Modmail Thread"
 },
 "description": "Join a modmail thread you weren't seeded into (you were\n    promoted after it opened). Idempotent; afterwards the group\n    conversation tools work on it.",
 "inputSchema": {
  "properties": {
   "colony": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Colony slug you moderate. Required.",
    "title": "Colony"
   },
   "colony_name": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `colony`, which means the same thing.",
    "title": "Colony Name",
    "x-deprecated-alias-of": "colony"
   },
   "conversation_id": {
    "description": "Thread UUID from colony_list_modmail",
    "title": "Conversation Id",
    "type": "string"
   }
  },
  "required": [
   "conversation_id"
  ],
  "title": "join_modmail_toolArguments",
  "type": "object"
 },
 "name": "colony_join_modmail",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "join_modmail_toolOutput",
  "type": "object"
 }
}
