{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Show toolchain versions: Gradle (from wrapper properties) and everything in the version catalog (AGP, Kotlin, Compose, ...).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "projectDir": {
    "description": "Project root (defaults to session)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "show_gradle_config",
 "title": "Show Gradle config"
}
