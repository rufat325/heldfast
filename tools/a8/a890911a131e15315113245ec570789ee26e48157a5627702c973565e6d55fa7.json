{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Generate one or more images from a text prompt using a cloud AI model. Every image is saved to disk and its absolute path is returned, along with a small inline preview. Use edit_image instead when starting from an existing image.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "aspect_ratio": {
    "description": "Desired aspect ratio. Support varies by model; treated as a strong hint.",
    "enum": [
     "1:1",
     "16:9",
     "9:16",
     "4:3",
     "3:4",
     "3:2",
     "2:3",
     "21:9"
    ],
    "type": "string"
   },
   "count": {
    "default": 1,
    "description": "Number of variations to generate (1-4, parallel requests, billed per image).",
    "maximum": 4,
    "minimum": 1,
    "type": "integer"
   },
   "filename_prefix": {
    "description": "Short label used in the saved filename, e.g. 'hero-banner'. Sanitized to letters, digits, dashes.",
    "maxLength": 40,
    "type": "string"
   },
   "model": {
    "description": "Model slug, e.g. 'google/gemini-2.5-flash-image' or 'openai/gpt-5-image'. Omit to use the configured default. Call list_models to see options.",
    "type": "string"
   },
   "output_dir": {
    "description": "Directory to save into (absolute, or ~ for home). Defaults to the configured output directory.",
    "type": "string"
   },
   "prompt": {
    "description": "Detailed description of the desired result. Be specific about subject, style, lighting, composition.",
    "maxLength": 4000,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "prompt"
  ],
  "type": "object"
 },
 "name": "generate_image",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "failed": {
    "description": "Error messages for variations that failed",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "images": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "bytes": {
       "type": "number"
      },
      "mime": {
       "type": "string"
      },
      "model": {
       "type": "string"
      },
      "path": {
       "description": "Absolute path of the saved file",
       "type": "string"
      }
     },
     "required": [
      "path",
      "mime",
      "bytes",
      "model"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "images",
   "failed"
  ],
  "type": "object"
 },
 "title": "Generate Image"
}
