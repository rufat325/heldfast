{
 "description": "Create a secret in a Code Engine project (generic, registry, ssh_auth, etc.)",
 "inputSchema": {
  "properties": {
   "data": {
    "description": "Key/value pairs (values must be base64 encoded for binary formats)",
    "type": "object"
   },
   "format": {
    "description": "Secret type",
    "enum": [
     "generic",
     "ssh_auth",
     "basic_auth",
     "tls",
     "registry"
    ],
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "project_id": {
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "name",
   "format",
   "data"
  ],
  "type": "object"
 },
 "name": "ce_create_secret"
}
