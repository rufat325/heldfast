{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Demote Staff To Guest"
 },
 "description": "Remove a staff member's role on a live event and demote them back to a guest (they keep their spot). Requires event_id + user_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "user_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "user_id"
  ],
  "type": "object"
 },
 "name": "demote_staff_to_guest",
 "title": "Demote Staff To Guest"
}
