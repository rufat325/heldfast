{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Read console output the running game produced - print() calls, stdout, stderr, and runtime script errors with their source location. Returns: captured lines with category and originating file/line. THIS IS THE ONLY WAY to see what the game actually did; the language server reports whether code compiles, not what it printed. Output is drained on each call, so successive calls return only what is new.",
 "inputSchema": {
  "properties": {
   "clear": {
    "description": "Drain the buffer (default true)",
    "type": "boolean"
   },
   "wait": {
    "description": "Seconds to wait for output (default 1)",
    "type": "number"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "debug_output"
}
