{
 "description": "Inspect explicit referral attribution for an agent. Returns direct referrals, claimable rewards, referral chain, and team bonus.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "agent_address": {
    "description": "Ethereum address of the AI agent",
    "pattern": "^0x[0-9a-fA-F]{40}$",
    "type": "string"
   }
  },
  "required": [
   "agent_address"
  ],
  "type": "object"
 },
 "name": "clicks_get_referral_stats"
}
