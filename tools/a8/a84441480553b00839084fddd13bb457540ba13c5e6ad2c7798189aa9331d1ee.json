{
 "description": "Animations in an AnimationPlayer and states in an AnimationTree state machine. Needs the editor connected. Operations: create (needs playerNodePath, animationName): a new animation in an AnimationPlayer; add_track (needs playerNodePath, animationName, track): a property or method track with keyframes; add_state (needs animTreePath, stateName, animationName): a state playing an animation, in an AnimationTree; connect_states (needs animTreePath, fromState, toState): a transition between two states.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "advanceCondition": {
    "description": "connect_states: the condition parameter that advances.",
    "type": "string"
   },
   "animTreePath": {
    "description": "The AnimationTree node.",
    "type": "string"
   },
   "animationName": {
    "description": "The animation in the player. add_state: the one that state plays.",
    "type": "string"
   },
   "fromState": {
    "description": "connect_states: the state the transition leaves.",
    "type": "string"
   },
   "length": {
    "description": "create: seconds. Default 1.",
    "type": "number"
   },
   "loopMode": {
    "description": "create: default none.",
    "enum": [
     "none",
     "linear",
     "pingpong"
    ],
    "type": "string"
   },
   "op": {
    "description": "What to do.",
    "enum": [
     "create",
     "add_track",
     "add_state",
     "connect_states"
    ],
    "type": "string"
   },
   "playerNodePath": {
    "description": "The AnimationPlayer node.",
    "type": "string"
   },
   "projectPath": {
    "description": "Absolute path to the project directory, the one holding project.godot.",
    "type": "string"
   },
   "scenePath": {
    "description": "Scene file inside the project, such as \"scenes/main.tscn\" or \"res://scenes/main.tscn\".",
    "type": "string"
   },
   "stateMachinePath": {
    "description": "add_state: a nested state machine. Default the root.",
    "type": "string"
   },
   "stateName": {
    "description": "add_state: the state to add.",
    "type": "string"
   },
   "step": {
    "description": "create: keyframe snap in seconds. Default 0.1.",
    "type": "number"
   },
   "toState": {
    "description": "connect_states: the state it arrives at.",
    "type": "string"
   },
   "track": {
    "additionalProperties": false,
    "description": "The track to add.",
    "properties": {
     "keyframes": {
      "items": {
       "additionalProperties": false,
       "properties": {
        "args": {
         "description": "For method tracks: the arguments to call with.",
         "type": "array"
        },
        "time": {
         "description": "Seconds from the start.",
         "type": "number"
        },
        "value": {
         "description": "For property tracks: the value at this time."
        }
       },
       "required": [
        "time"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "method": {
      "description": "For method tracks: the method to call.",
      "type": "string"
     },
     "nodePath": {
      "description": "Target node, relative to the AnimationPlayer root.",
      "type": "string"
     },
     "property": {
      "description": "For property tracks: the property to animate.",
      "type": "string"
     },
     "type": {
      "enum": [
       "property",
       "method"
      ],
      "type": "string"
     }
    },
    "required": [
     "type",
     "nodePath",
     "keyframes"
    ],
    "type": "object"
   },
   "transitionType": {
    "description": "connect_states: default immediate.",
    "enum": [
     "immediate",
     "sync",
     "at_end"
    ],
    "type": "string"
   }
  },
  "required": [
   "op",
   "projectPath",
   "scenePath"
  ],
  "type": "object"
 },
 "name": "scene_animation"
}
