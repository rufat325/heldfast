{
 "annotations": {
  "readOnlyHint": true,
  "title": "Get daily activity"
 },
 "description": "List individual emails received on a board over a date range, each with its creation timestamp. Returns raw records, not per-day counts. Narrow the date range on busy boards.",
 "inputSchema": {
  "properties": {
   "boardId": {
    "description": "The board ID",
    "type": "number"
   },
   "dateFrom": {
    "description": "Start date for the period (ISO-8601)",
    "type": "string"
   },
   "dateTo": {
    "description": "End date for the period (ISO-8601)",
    "type": "string"
   }
  },
  "required": [
   "boardId",
   "dateFrom",
   "dateTo"
  ],
  "type": "object"
 },
 "name": "get_daily_activity",
 "title": "Get daily activity"
}
