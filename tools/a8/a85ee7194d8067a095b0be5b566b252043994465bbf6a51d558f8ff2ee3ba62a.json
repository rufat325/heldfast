{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Create a tag"
 },
 "description": "Create a tag. Idempotent on name: 201 when created, 200 when it already existed (colour unchanged).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "color": {
    "default": "#5046E5",
    "pattern": "^#[0-9A-Fa-f]{6}$",
    "type": "string"
   },
   "name": {
    "maxLength": 50,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "create_tag"
}
