{
 "description": "Resize a worker pool",
 "inputSchema": {
  "properties": {
   "cluster": {
    "description": "Cluster name or ID",
    "type": "string"
   },
   "pool": {
    "description": "Worker pool name",
    "type": "string"
   },
   "size_per_zone": {
    "description": "New workers per zone",
    "type": "number"
   }
  },
  "required": [
   "cluster",
   "pool",
   "size_per_zone"
  ],
  "type": "object"
 },
 "name": "ibmcloud_ks_worker_pool_resize"
}
