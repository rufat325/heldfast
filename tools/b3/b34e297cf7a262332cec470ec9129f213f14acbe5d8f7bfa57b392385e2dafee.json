{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Find Products by Brand"
 },
 "description": "List products under a specific Amazon brand. Pre-validates the brand name via cached AI check, then filters keyword-search results to rows whose `brand` field actually matches. On no-match, returns the brands that did appear in the keyword pool so callers can suggest alternatives. How to use: assess the brand's Amazon footprint \u2014 lineup breadth, price range, which products carry the revenue, and how strong its review moat is.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "brand": {
    "description": "Amazon brand name to search by",
    "maxLength": 80,
    "minLength": 2,
    "type": "string"
   },
   "count": {
    "default": 100,
    "description": "How many products to return (1\u2013100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "filters": {
    "additionalProperties": false,
    "description": "Filter products by price / sales / revenue / reviews / rating",
    "properties": {
     "maxEstRev": {
      "description": "Maximum estimated monthly revenue, in the marketplace's local currency",
      "minimum": 0,
      "type": "number"
     },
     "maxEstSales": {
      "description": "Maximum estimated monthly sales (units)",
      "minimum": 0,
      "type": "number"
     },
     "maxPrice": {
      "description": "Maximum unit price, in the marketplace's local currency",
      "minimum": 0,
      "type": "number"
     },
     "maxRating": {
      "description": "Maximum average rating (0-5)",
      "maximum": 5,
      "minimum": 0,
      "type": "number"
     },
     "maxReviews": {
      "description": "Maximum review count",
      "minimum": 0,
      "type": "number"
     },
     "minEstRev": {
      "description": "Minimum estimated monthly revenue, in the marketplace's local currency",
      "minimum": 0,
      "type": "number"
     },
     "minEstSales": {
      "description": "Minimum estimated monthly sales (units)",
      "minimum": 0,
      "type": "number"
     },
     "minPrice": {
      "description": "Minimum unit price, in the marketplace's local currency",
      "minimum": 0,
      "type": "number"
     },
     "minRating": {
      "description": "Minimum average rating (0-5)",
      "maximum": 5,
      "minimum": 0,
      "type": "number"
     },
     "minReviews": {
      "description": "Minimum review count",
      "minimum": 0,
      "type": "number"
     }
    },
    "type": "object"
   },
   "marketplace": {
    "description": "Amazon marketplace code \u2014 the one the user named, or the one they chose earlier in this conversation (ask once, then reuse it for every call). Never assume the US for an ASIN: without it, ASIN tools fetch nothing and answer \"MARKETPLACE NEEDED\". Money in results is in this marketplace's currency.",
    "enum": [
     "COM",
     "CO_UK",
     "DE",
     "FR",
     "IT",
     "ES",
     "CA",
     "COM_MX",
     "COM_BR",
     "IN",
     "CO_JP",
     "COM_AU",
     "AE",
     "SA"
    ],
    "type": "string"
   },
   "sort": {
    "default": "revenue",
    "enum": [
     "revenue",
     "sales",
     "newest",
     "rating",
     "reviews"
    ],
    "type": "string"
   }
  },
  "required": [
   "brand"
  ],
  "type": "object"
 },
 "name": "amzscout_find_by_brand"
}
