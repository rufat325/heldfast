{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Get use case scenario flows for an element. `scenarios` holds the parsed flows; each has `name`, `type`, `notes`, and `steps`, and each step carries `stepNumber` plus its attributes (`trigger`, `uses`, `result`, `state`, `link`). Steps are numbered within each scenario. Scenarios ordered by type: Basic Path first, then Alternate, then Exception. A step's `uses` may name a business rule or constraint by code; that code is not independently searchable or resolvable \u2014 look it up among this same elementId's own constraints via `ea_get_element`, not by a separate lookup.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "elementId": {
    "description": "The Object_ID of the element (typically a UseCase) to get scenarios for",
    "type": "number"
   }
  },
  "required": [
   "elementId"
  ],
  "type": "object"
 },
 "name": "ea_get_scenarios"
}
