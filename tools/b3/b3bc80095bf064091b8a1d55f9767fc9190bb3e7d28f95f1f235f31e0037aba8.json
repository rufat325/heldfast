{
 "description": "Sealed bonds from the Affine.Earth corpus (GAIA_SPATIAL_BOND_WIRES). mined_from is LLM_WEIGHTS or LLVM_IR. Stateless: pin at_seq to make the read reproducible across cells, or omit it to read the moving head.",
 "inputSchema": {
  "properties": {
   "at_seq": {
    "description": "decimal string, caller-pinned stream position; same value = same bytes",
    "type": "string"
   },
   "limit": {
    "description": "decimal string, rows up to 8192; JSON numbers still admitted",
    "type": "string"
   },
   "sector": {
    "description": "hex sector prefix",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "corpus_bonds"
}
