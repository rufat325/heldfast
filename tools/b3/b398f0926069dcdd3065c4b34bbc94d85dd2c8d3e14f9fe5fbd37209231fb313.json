{
 "description": "Preview how an incoming USDC revenue event will be split between liquid operations and routed treasury capital.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "agent_address": {
    "description": "Ethereum address of the AI agent",
    "pattern": "^0x[0-9a-fA-F]{40}$",
    "type": "string"
   },
   "amount": {
    "description": "Payment amount in USDC (e.g. \"100\" for 100 USDC)",
    "type": "string"
   }
  },
  "required": [
   "amount",
   "agent_address"
  ],
  "type": "object"
 },
 "name": "clicks_simulate_split"
}
