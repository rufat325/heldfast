{
 "description": "DERIVATION TRUTH: raw upstream payloads, SHA-256-pinned, with the exact collector code hash for each fetch. Replay any published value: fetch the verify_url yourself, run the pinned code, compare. Answers 'a hash proves a footprint, not the derivation' \u2014 divergence from origin is mechanically detectable. Free.",
 "inputSchema": {
  "properties": {
   "symbol": {
    "description": "Optional: filter to one symbol (e.g. AKT). Omit for the coverage list.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_derivation_ledger"
}
