{
 "annotations": {
  "cost": {
   "all": 3
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Aggregate MAUDE reports for a device query by event type, manufacturer, product code, or receive date. Counts reflect reporting and database artifacts\u2014not event rates or causal risk\u2014and must not be compared without exposure denominators.",
 "inputSchema": {
  "examples": [
   {
    "count_field": "device.generic_name.exact",
    "limit": 10
   },
   {
    "count_field": "device.brand_name.exact",
    "limit": 10
   },
   {
    "count_field": "event_type.exact",
    "limit": 10,
    "query": "device.device_report_product_code:MDS"
   }
  ],
  "properties": {
   "count_field": {
    "description": "What to rank by. For \"which DEVICES had the most reports\" use device.generic_name.exact (device type) or device.brand_name.exact (specific product).",
    "enum": [
     "device.generic_name.exact",
     "device.brand_name.exact",
     "device.openfda.device_name.exact",
     "device.manufacturer_d_name.exact",
     "event_type.exact",
     "device.device_report_product_code.exact",
     "date_received"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Buckets (1-100, default 20).",
    "type": "number"
   },
   "query": {
    "description": "Optional raw openFDA MAUDE filter, e.g. 'date_received:[20250101+TO+20261231]'. Omit to rank across all reports.",
    "type": "string"
   }
  },
  "required": [
   "count_field"
  ],
  "type": "object"
 },
 "name": "fda_device_event_counts",
 "outputSchema": {
  "properties": {
   "buckets": {
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "returned": {
    "type": "number"
   },
   "source": {
    "type": "string"
   },
   "total": {
    "type": "number"
   }
  },
  "required": [
   "total",
   "returned",
   "buckets",
   "source"
  ],
  "type": "object"
 },
 "title": "Fda Device Event Counts",
 "version": "v1.8974705e"
}
