{
 "description": "Get a space's top-level (root) pages via the bulk/cursor-paginated endpoint.",
 "inputSchema": {
  "properties": {
   "cursor": {
    "type": "string"
   },
   "limit": {
    "type": "number"
   },
   "spaceKey": {
    "type": "string"
   }
  },
  "required": [
   "spaceKey"
  ],
  "type": "object"
 },
 "name": "get_space_root_pages"
}
