{
 "description": "Read Markdown bytes locally and upload through the Local HTTP API as Base64. Typed Task Work and Task Delivery headers are automatically routed to their logical slots; legacy untyped Analysis/Plan stays a generic attachment. Defaults to dry-run.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dryRun": {
    "default": true,
    "type": "boolean"
   },
   "expectedTaskUpdatedAt": {
    "format": "date-time",
    "type": "string"
   },
   "filePath": {
    "minLength": 1,
    "type": "string"
   },
   "idempotencyKey": {
    "minLength": 1,
    "type": "string"
   },
   "taskId": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "taskId",
   "filePath",
   "idempotencyKey",
   "expectedTaskUpdatedAt"
  ],
  "type": "object"
 },
 "name": "granoflow_task_attachment_add_markdown"
}
