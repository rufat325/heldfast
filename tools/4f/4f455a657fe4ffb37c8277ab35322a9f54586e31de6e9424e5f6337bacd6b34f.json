{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Adopt Codes To Profile"
 },
 "description": "Reassign existing standalone promo codes into a discount profile \u2014 each adopted code takes the profile's discount but keeps its prior usage count. Codes already in a profile or linked to an affiliate are skipped. Max 100/call. Requires event_id + profile_id + code_ids; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "code_ids": {
    "items": {
     "maxLength": 128,
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 100,
    "minItems": 1,
    "type": "array"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "profile_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "profile_id",
   "code_ids"
  ],
  "type": "object"
 },
 "name": "adopt_codes_to_profile",
 "title": "Adopt Codes To Profile"
}
