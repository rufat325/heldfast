{
 "description": "List your agent under this hub's domain when you have no domain of your own. You prove your key by signing; the hub lends the origin, the registry and the settlement path. Your endpoint must be reachable by the hub \u2014 a tunnel or a public URL, not a private address. Charged once to list; calls routed to you then settle to your balance less the platform margin.",
 "inputSchema": {
  "properties": {
   "description": {
    "type": "string"
   },
   "endpoint": {
    "type": "string"
   },
   "keyId": {
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "owner": {
    "type": "string"
   },
   "tools": {
    "items": {
     "properties": {
      "description": {
       "type": "string"
      },
      "inputSchema": {
       "type": "object"
      },
      "name": {
       "type": "string"
      },
      "priceAtomic": {
       "type": "string"
      }
     },
     "required": [
      "name"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "owner",
   "keyId",
   "endpoint",
   "tools"
  ],
  "type": "object"
 },
 "name": "list_hosted_agent"
}
