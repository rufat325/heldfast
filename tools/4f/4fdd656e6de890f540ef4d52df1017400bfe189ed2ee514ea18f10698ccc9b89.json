{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List AniList characters or staff members whose birthday (month/day) is today, up to 50 results (a fixed cap, not paginated \u2014 entries beyond 50 are silently omitted, not an error). Returns character-shaped or staff-shaped objects for kind: CHARACTER/STAFF respectively, but a lighter fetch than get_character/get_staff: it does NOT include those tools' `media`/`staffMedia` filmography, nor each entry's bio (`description`) unless you set `includeDescription` \u2014 call get_character/get_staff by ID for an entry's full roles/works and bio, don't read a missing filmography here as 'none'.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "includeDescription": {
    "default": false,
    "description": "Also fetch each result's full bio (`description`). Kept off by default \u2014 a bio can run to several thousand characters, and always including it would burn tokens on text you may not need; use get_character/get_staff for a single entry's full bio instead.",
    "type": "boolean"
   },
   "kind": {
    "description": "Whether to list characters or staff members.",
    "enum": [
     "CHARACTER",
     "STAFF"
    ],
    "type": "string"
   }
  },
  "required": [
   "kind"
  ],
  "type": "object"
 },
 "name": "get_todays_birthdays",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "results": {
    "items": {
     "additionalProperties": {},
     "properties": {
      "description": {
       "type": [
        "string",
        "null"
       ]
      },
      "favourites": {
       "anyOf": [
        {
         "maximum": 9007199254740991,
         "minimum": 0,
         "type": "integer"
        },
        {
         "type": "null"
        }
       ]
      },
      "id": {
       "exclusiveMinimum": 0,
       "maximum": 2147483647,
       "type": "integer"
      },
      "image": {
       "anyOf": [
        {
         "additionalProperties": false,
         "properties": {
          "large": {
           "anyOf": [
            {
             "format": "uri",
             "type": "string"
            },
            {
             "type": "null"
            }
           ]
          }
         },
         "type": "object"
        },
        {
         "type": "null"
        }
       ]
      },
      "isFavourite": {
       "description": "[Requires login] Whether this character or staff member is in the caller's favourites. Has a brief read-after-write lag on AniList's own backend \u2014 confirmed live, a re-check right after toggle_favourite can briefly still show the pre-toggle value.",
       "type": [
        "boolean",
        "null"
       ]
      },
      "name": {
       "anyOf": [
        {
         "additionalProperties": false,
         "properties": {
          "full": {
           "type": [
            "string",
            "null"
           ]
          },
          "native": {
           "type": [
            "string",
            "null"
           ]
          }
         },
         "type": "object"
        },
        {
         "type": "null"
        }
       ]
      },
      "primaryOccupations": {
       "anyOf": [
        {
         "items": {
          "type": "string"
         },
         "type": "array"
        },
        {
         "type": "null"
        }
       ]
      },
      "siteUrl": {
       "anyOf": [
        {
         "format": "uri",
         "type": "string"
        },
        {
         "type": "null"
        }
       ]
      }
     },
     "required": [
      "id"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "results"
  ],
  "type": "object"
 },
 "title": "Get today's birthday characters or staff"
}
