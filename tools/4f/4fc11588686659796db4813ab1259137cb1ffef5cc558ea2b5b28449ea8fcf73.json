{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Full-text search across the entire context library (Bear notes + external files + inbox). Returns matching snippets with filenames and origin labels. Use when the index alone isn't enough to find the right file.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Maximum results (default: 5)",
    "type": "number"
   },
   "query": {
    "description": "Search query (case-insensitive substring match)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "bear_context_search"
}
