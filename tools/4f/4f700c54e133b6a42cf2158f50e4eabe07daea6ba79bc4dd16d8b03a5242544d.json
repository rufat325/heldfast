{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List Streaming Radar newsletter articles with optional filters. Returns title, subtitle, status, type, views, open_rate, editorial_angle, and clickable links. Free access \u2014 no API key required.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max results (default 20, max 50)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset (default 0)",
    "minimum": 0,
    "type": "number"
   },
   "status": {
    "description": "Filter by status: published, draft, archived",
    "enum": [
     "published",
     "draft",
     "archived"
    ],
    "type": "string"
   },
   "type": {
    "description": "Filter by content type: edition, analysis, special",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "sr_get_articles",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Tool-specific result object. Inspect the `content` array for human-readable text and structured data.",
  "type": "object"
 }
}
