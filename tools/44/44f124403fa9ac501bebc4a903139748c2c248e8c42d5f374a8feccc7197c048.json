{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Generate a tracked affiliate link for a referral-link, query-param-link, or Awin-wrapped vendor. No payment required.",
 "inputSchema": {
  "properties": {
   "affiliate_id": {
    "description": "Override affiliate ID (env var used if omitted)",
    "type": "string"
   },
   "extra_params": {
    "description": "Optional per-link template values, e.g. {vin: '1HGCM82633A004352'} to prefill a VIN on vehicle-history report links. Vendor/product-specific \u2014 see get_opportunity_details for supported keys.",
    "type": "object"
   },
   "product_id": {
    "description": "Product ID within the vendor",
    "type": "string"
   },
   "vendor_id": {
    "description": "Vendor ID (must be a link-based affiliate, not x402)",
    "type": "string"
   }
  },
  "required": [
   "vendor_id",
   "product_id"
  ],
  "type": "object"
 },
 "name": "generate_affiliate_link"
}
