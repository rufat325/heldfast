{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Quick preview of a design token system without full CSS output. Returns colors for light and/or dark mode, font pairing, radius, shadow style, WCAG contrast summary with near-failing pairs, design intent brief, and a visual preview URL on tokven.dev. Recommended first step: call this before generate_tokens so the user can approve the palette visually.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "body_font": {
    "description": "Override body font family name",
    "maxLength": 100,
    "type": "string"
   },
   "colors": {
    "default": "all",
    "description": "Color scope. Defaults to 'all'.",
    "enum": [
     "primary",
     "primary-secondary",
     "all"
    ],
    "type": "string"
   },
   "heading_font": {
    "description": "Override heading font family name",
    "maxLength": 100,
    "type": "string"
   },
   "hex": {
    "description": "Brand color hex",
    "type": "string"
   },
   "mode": {
    "default": "both",
    "description": "Which palette to preview. Defaults to 'both'.",
    "enum": [
     "light",
     "dark",
     "both"
    ],
    "type": "string"
   },
   "mood": {
    "description": "Mood preset",
    "type": "string"
   },
   "secondary_hex": {
    "description": "Custom hex for secondary color. Auto-derived if omitted.",
    "type": "string"
   },
   "tertiary_hex": {
    "description": "Custom hex for tertiary color. Auto-derived if omitted.",
    "type": "string"
   }
  },
  "required": [
   "hex"
  ],
  "type": "object"
 },
 "name": "get_token_preview",
 "title": "Preview Design Tokens"
}
