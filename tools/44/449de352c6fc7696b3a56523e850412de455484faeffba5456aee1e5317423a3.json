{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Search the public LynxPrompt blueprint marketplace",
 "inputSchema": {
  "properties": {
   "category": {
    "description": "Filter by category (optional)",
    "type": "string"
   },
   "query": {
    "description": "Search query string",
    "type": "string"
   },
   "tags": {
    "description": "Comma-separated tags to filter by (optional)",
    "type": "string"
   },
   "type": {
    "description": "Filter by type, e.g. AGENTS_MD, CLAUDE_MD (optional)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_blueprints"
}
