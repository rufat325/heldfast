{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List the files and subfolders inside a Box (enterprise cloud storage) folder. Pass a folder id, or omit it to list the root folder (\"0\"). Returns each item's id, type (file or folder), name, size in bytes, and last-modified time. Use to browse a user's Box documents and files.",
 "inputSchema": {
  "examples": [
   {
    "folder_id": "0"
   },
   {
    "folder_id": "123456789",
    "limit": 50
   }
  ],
  "properties": {
   "folder_id": {
    "description": "The Box folder id to list. Defaults to \"0\", the root folder.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of items to return (default 100, max 1000).",
    "type": "number"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "box_list_folder",
 "title": "Box List Folder",
 "version": "v1.a30d6668"
}
