{
 "description": "Share of Voice report \u2014 your brand keywords vs competitor keywords: per-keyword mention/sentiment totals, your share of relevant conversation (current + previous window), daily series. Requires the Enterprise plan (403 otherwise \u2014 do not retry).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "days": {
    "description": "1-90, default 30.",
    "type": "number"
   },
   "platform": {
    "description": "Restrict to one source, e.g. \"reddit\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_share_of_voice"
}
