{
 "description": "Open a game directory for Unreal asset reading. Scans for .pak/.ucas/.utoc files.",
 "inputSchema": {
  "properties": {
   "aes_key": {
    "description": "AES decryption key if assets are encrypted",
    "type": "string"
   },
   "game_path": {
    "description": "Path to game directory",
    "type": "string"
   },
   "ue_version": {
    "description": "Unreal Engine version (auto-detected if omitted)",
    "type": "string"
   }
  },
  "required": [
   "game_path"
  ],
  "type": "object"
 },
 "name": "open_game"
}
