{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Discover what a dataset accepts: its dimension names in key order, and the codes available for each, filtered by `query`. Use this before get_data \u2014 e.g. search_indicators({dataset:\"CPI\", query:\"united states\"}) finds COUNTRY=USA. Without a query it returns the dimension list and a sample of codes.",
 "inputSchema": {
  "examples": [
   {
    "dataset": "CPI",
    "query": "united states"
   },
   {
    "dataset": "CPI"
   }
  ],
  "properties": {
   "dataset": {
    "description": "Dataset id from get_datasets, e.g. \"CPI\".",
    "type": "string"
   },
   "query": {
    "description": "Optional term matched against code ids and names, e.g. \"united states\", \"GDP\", \"monthly\".",
    "type": "string"
   }
  },
  "required": [
   "dataset"
  ],
  "type": "object"
 },
 "name": "search_indicators",
 "outputSchema": {
  "properties": {
   "query": {
    "description": "Lowercase search query term used for filtering",
    "type": "string"
   },
   "structure": {
    "description": "IMF DataStructure containing indicator code dimensions and metadata",
    "type": "object"
   }
  },
  "required": [
   "structure"
  ],
  "type": "object"
 },
 "title": "Search Indicators",
 "version": "v1.d7c7afd0"
}
