{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "String operations with optional TTL (survives rebuild): set, get (includes remaining ttlMs), delete.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Operation",
    "enum": [
     "set",
     "get",
     "delete"
    ],
    "type": "string"
   },
   "name": {
    "description": "String key name",
    "type": "string"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   },
   "ttlSeconds": {
    "description": "TTL in seconds (set)",
    "type": "number"
   },
   "value": {
    "description": "Value to set (set)",
    "type": "string"
   }
  },
  "required": [
   "spaceId",
   "name",
   "action"
  ],
  "type": "object"
 },
 "name": "kv_string",
 "outputSchema": {
  "properties": {},
  "type": "object"
 }
}
