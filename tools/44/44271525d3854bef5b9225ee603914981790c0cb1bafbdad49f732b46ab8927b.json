{
 "description": "Run a suite of search queries and compare streaming vs non-streaming performance. Reports timing (avg/min/max), result counts, and speedup ratios. Useful for measuring vibe-hnindex performance on your project.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "project_name": {
    "description": "Project to benchmark",
    "type": "string"
   },
   "runs": {
    "default": 2,
    "description": "Number of runs per query (default 2, higher = more accurate)",
    "maximum": 5,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "project_name"
  ],
  "type": "object"
 },
 "name": "benchmark_search"
}
