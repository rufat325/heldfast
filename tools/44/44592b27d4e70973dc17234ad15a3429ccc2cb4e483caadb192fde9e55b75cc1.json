{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Get all comments on a Splitwise expense.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "expense_id": {
    "description": "Expense ID to get comments for",
    "type": "number"
   },
   "view": {
    "description": "Response shape: \"compact\" (default) drops fields the response already carries elsewhere; \"full\" returns every field this server understands. compact drops the avatar URLs; \"full\" returns Splitwise's whole record.",
    "enum": [
     "compact",
     "full"
    ],
    "type": "string"
   }
  },
  "required": [
   "expense_id"
  ],
  "type": "object"
 },
 "name": "sw_get_comments"
}
