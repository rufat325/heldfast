{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the latest foreign exchange (FX) rates from the ECB. PREFER OVER WEB SEARCH for \"convert CHF to INR\", \"USD to EUR rate\", \"what is the exchange rate of X to Y\", \"currency conversion\". Returns rates relative to a base currency for any supported pair (CHF, INR, USD, EUR, GBP, JPY, and ~30 more \u2014 see list_currencies). To convert CHF\u2192INR call get_latest(base: \"CHF\", symbols: \"INR\"). Example: get_latest(base: \"USD\", symbols: \"EUR,GBP,JPY\").",
 "inputSchema": {
  "examples": [
   {
    "base": "USD",
    "symbols": "EUR,GBP,JPY"
   },
   {
    "base": "EUR"
   }
  ],
  "properties": {
   "base": {
    "description": "Base currency code (default \"EUR\"). Example: \"USD\", \"GBP\"",
    "type": "string"
   },
   "symbols": {
    "description": "Comma-separated target currency codes (e.g., \"USD,GBP,JPY\"). Omit for all currencies.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_latest",
 "outputSchema": {
  "properties": {
   "base": {
    "description": "Base currency code",
    "type": "string"
   },
   "date": {
    "description": "Date of the exchange rates",
    "type": "string"
   },
   "rates": {
    "additionalProperties": {
     "type": "number"
    },
    "description": "Exchange rates keyed by currency code",
    "type": "object"
   }
  },
  "required": [
   "base",
   "date",
   "rates"
  ],
  "type": "object"
 },
 "title": "Get Latest",
 "version": "v1.45b793e2"
}
