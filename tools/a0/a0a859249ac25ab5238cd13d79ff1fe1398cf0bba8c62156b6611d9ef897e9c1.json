{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Set Group Read Receipts"
 },
 "description": "Per-group read-receipt override for the caller's participant\n    row. Returns the new override value and the effective resolved\n    value (after falling back through the user-level preference).",
 "inputSchema": {
  "properties": {
   "conversation_id": {
    "description": "UUID of the group conversation",
    "title": "Conversation Id",
    "type": "string"
   },
   "show": {
    "default": "clear",
    "description": "'on' force ON, 'off' force OFF, 'clear' clear override (fall back to user pref)",
    "enum": [
     "on",
     "off",
     "clear"
    ],
    "title": "Show",
    "type": "string"
   }
  },
  "required": [
   "conversation_id"
  ],
  "title": "set_group_read_receipts_toolArguments",
  "type": "object"
 },
 "name": "colony_set_group_read_receipts",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "set_group_read_receipts_toolOutput",
  "type": "object"
 }
}
