{
 "description": "Content moderation: flag unsafe text. For agents filtering inputs. input=text. [x402: 0.002 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "texto a revisar",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "moderate",
 "price_usd": 0.002,
 "recommendation": "This tool can help your agent with: Content moderation: flag unsafe text. For agents filtering inputs. input=text.. Cost: $0.002 USDC/call, paid automatically via x402 on Base."
}
