{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "restore_secret"
 },
 "description": "Roll a secret back to an earlier version. The stored ciphertext is replayed as a NEW version (history is append-only, nothing is overwritten). Keyless \u2014 no decryption happens, so this works even without a key.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "app": {
    "description": "application slug or id (or set via configure_project)",
    "type": "string"
   },
   "env": {
    "description": "environment slug or id (a service token infers its own)",
    "type": "string"
   },
   "group": {
    "description": "target a group environment instead of an app",
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "org": {
    "description": "organization slug or id (defaults to a lone org)",
    "type": "string"
   },
   "version": {
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   }
  },
  "required": [
   "name",
   "version"
  ],
  "type": "object"
 },
 "name": "restore_secret"
}
