{
 "description": "Likes the listing; DELETE undoes it. Calling again does not add up: the counter counts people.",
 "inputSchema": {
  "properties": {
   "guest_token": {
    "type": "string"
   },
   "id": {
    "description": "ID of the listing to like.",
    "type": "string"
   }
  },
  "required": [
   "id",
   "guest_token"
  ],
  "type": "object"
 },
 "name": "like_listing"
}
