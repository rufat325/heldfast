{
 "description": "Create a new webhook to receive notifications. Returns the webhook with its signing secret (shown only once - save it!).",
 "inputSchema": {
  "properties": {
   "events": {
    "description": "Events to subscribe to. Options: order.completed, order.failed, order.refunded",
    "items": {
     "enum": [
      "order.completed",
      "order.failed",
      "order.refunded"
     ],
     "type": "string"
    },
    "type": "array"
   },
   "url": {
    "description": "HTTPS URL to receive webhook payloads",
    "type": "string"
   }
  },
  "required": [
   "url",
   "events"
  ],
  "type": "object"
 },
 "name": "barevalue_webhook_create"
}
