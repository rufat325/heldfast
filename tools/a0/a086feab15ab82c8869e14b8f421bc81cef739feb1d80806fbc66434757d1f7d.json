{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search Censys hosts for <query> \u2014 finds internet-facing hosts matching a Censys query (CenQL), e.g. software, port, product, or ASN. Returns ip, matched services (port/service/transport), location, and autonomous system per host. Example: censys_host_search({ q: \"services.service_name: HTTP and location.country: Germany\", per_page: 25, _apiKey: \"your-censys-token\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-censys-token",
    "per_page": 25,
    "q": "services.port: 443 and location.country: US"
   },
   {
    "_apiKey": "your-censys-token",
    "q": "services.software.product: nginx and autonomous_system.asn: 15169"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Censys Platform Personal Access Token. Optionally \"<token>:<organizationId>\".",
    "type": "string"
   },
   "per_page": {
    "description": "Max hosts to return (default 25, max 50)",
    "type": "integer"
   },
   "q": {
    "description": "Censys query (CenQL), e.g. \"services.port: 22\", \"services.software.product: nginx\", \"autonomous_system.asn: 15169\"",
    "type": "string"
   }
  },
  "required": [
   "q",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "censys_host_search",
 "title": "Censys Host Search",
 "version": "v1.f20583f5"
}
