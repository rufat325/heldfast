{
 "annotations": {
  "readOnlyHint": true,
  "title": "List Vault Entries"
 },
 "description": "List vault entries (metadata only - no plaintext). Use vault_unseal to retrieve the actual credential.",
 "inputSchema": {
  "properties": {
   "include_inactive": {
    "description": "Include soft-deleted entries (default: false)",
    "type": "boolean"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "vault_list"
}
