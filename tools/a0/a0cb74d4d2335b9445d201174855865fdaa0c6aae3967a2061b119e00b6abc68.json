{
 "description": "Look up a GTM container by its linked destination ID (e.g. a GA4 measurement ID or tag ID).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "destinationId": {
    "description": "The destination/tag ID to look up (e.g. \"G-XXXXXXX\").",
    "type": "string"
   }
  },
  "required": [
   "destinationId"
  ],
  "type": "object"
 },
 "name": "containers_lookup"
}
