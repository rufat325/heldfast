{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the latest precious and base metal spot prices (gold, silver, platinum, palladium, copper, etc.). Prices are per troy ounce. Optionally specify base currency and metal symbols.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-metals-api-key"
   },
   {
    "_apiKey": "your-metals-api-key",
    "base": "EUR",
    "symbols": "XAU,XAG,XPT"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Metals-API key",
    "type": "string"
   },
   "base": {
    "description": "Base currency (default \"USD\"). E.g., \"EUR\", \"GBP\"",
    "type": "string"
   },
   "symbols": {
    "description": "Comma-separated metal symbols to filter (e.g., \"XAU,XAG,XPT\"). XAU=gold, XAG=silver, XPT=platinum, XPD=palladium, XCU=copper",
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "get_latest",
 "outputSchema": {
  "properties": {
   "base": {
    "description": "Base currency code (e.g., USD, EUR)",
    "type": "string"
   },
   "date": {
    "description": "Date of the latest prices in YYYY-MM-DD format",
    "type": "string"
   },
   "rates": {
    "additionalProperties": {
     "type": "number"
    },
    "description": "Metal spot prices per troy ounce, keyed by symbol (e.g., XAU, XAG)",
    "type": "object"
   },
   "timestamp": {
    "description": "Unix timestamp of when prices were captured",
    "type": "number"
   }
  },
  "required": [
   "base",
   "date",
   "timestamp",
   "rates"
  ],
  "type": "object"
 },
 "title": "Get Latest",
 "version": "v1.e102edfb"
}
