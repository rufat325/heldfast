{
 "description": "Endpoints that charge via x402, with network, asset and price.",
 "inputSchema": {
  "properties": {
   "limit": {
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list_paid_endpoints"
}
