{
 "description": "Resolve (acknowledge) a comment thread once it has been addressed \u2014 it disappears from default views everywhere (web, desktop, public share page, list_comments). Pass the ROOT comment id; the whole thread resolves. Set resolved=false to reopen.",
 "inputSchema": {
  "properties": {
   "commentId": {
    "description": "Root comment id (from list_comments)",
    "type": "string"
   },
   "resolved": {
    "description": "true = resolve (default), false = reopen",
    "type": "boolean"
   }
  },
  "required": [
   "commentId"
  ],
  "type": "object"
 },
 "name": "resolve_comment"
}
