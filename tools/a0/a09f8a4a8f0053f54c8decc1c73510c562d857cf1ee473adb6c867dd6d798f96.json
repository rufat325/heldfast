{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Start monitoring a URL for changes. Returns the UUID of the new watch.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "check_hours": {
    "description": "Hours between checks (default: uses account setting)",
    "type": "number"
   },
   "check_minutes": {
    "description": "Minutes between checks",
    "type": "number"
   },
   "fetch_backend": {
    "description": "How to fetch the page: html_requests (fast, no JS), html_webdriver (browser with JS, for SPAs), or html_hybrid (plain HTTP first, then a browser and the anti-bot solvers when the page needs them). Protected pages escalate automatically; there is no separate anti-bot option for a monitor.",
    "enum": [
     "html_requests",
     "html_webdriver",
     "html_hybrid"
    ],
    "type": "string"
   },
   "title": {
    "description": "Human-readable name for this monitor",
    "type": "string"
   },
   "url": {
    "description": "URL to monitor for changes",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "create_watch",
 "title": "Create watch"
}
