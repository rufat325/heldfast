{
 "description": "Advance an execution transactionally for the current browser surface. Returns the immutable proposedWave with frozen accessKnowledge receipts and never opens a browser. Same-key replay returns the same members, order, and rationale. Optional hash-bound accessReviewApproval probes the exact proposed job IDs only after personal deferments are cleared.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accessReviewApproval": {
    "additionalProperties": false,
    "properties": {
     "approvalHash": {
      "pattern": "^[a-f0-9]{64}$",
      "type": "string"
     },
     "jobIds": {
      "items": {
       "minimum": 1,
       "type": "integer"
      },
      "maxItems": 20,
      "minItems": 1,
      "type": "array"
     }
    },
    "required": [
     "jobIds",
     "approvalHash"
    ],
    "type": "object"
   },
   "browserSurface": {
    "enum": [
     "codex_in_app",
     "chrome_extension",
     "claude_in_chrome"
    ],
    "type": "string"
   },
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "executionId": {
    "minimum": 1,
    "type": "integer"
   },
   "expectedRevision": {
    "minimum": 1,
    "type": "integer"
   },
   "idempotencyKey": {
    "maxLength": 200,
    "minLength": 16,
    "pattern": "^[\\x20-\\x7e]+$",
    "type": "string"
   }
  },
  "required": [
   "executionId",
   "expectedRevision",
   "browserSurface",
   "idempotencyKey"
  ],
  "type": "object"
 },
 "name": "trackly_advance_apply_execution"
}
