{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the current sun position (altitude and azimuth), sunrise/sunset compass bearings, golden hour window, and solar noon time for any location and date. Sun altitude is the elevation angle above the horizon (negative = below). Sun azimuth is the compass direction of the sun (0=North, 90=East, 180=South, 270=West). Golden hour is the last hour before sunset \u2014 ideal for photography. Includes dawn, dusk, and day length. Useful for solar panel orientation, photography planning, outdoor event scheduling, and navigation. No auth required; free commercial use. Source: SunriseSunset.io.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "date": {
    "description": "Date to query: \"today\", \"tomorrow\", or ISO date YYYY-MM-DD (e.g. 2026-01-15). Defaults to today.",
    "type": "string"
   },
   "lat": {
    "description": "Latitude of the target location (-90 to 90). Positive = North, Negative = South.",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "lng": {
    "description": "Longitude of the target location (-180 to 180). Positive = East, Negative = West.",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "time_format": {
    "description": "Time output format: \"0\"=12-hour AM/PM (e.g. 5:26:02 AM), \"1\"=24-hour (e.g. 05:26:02), \"2\"=Unix timestamp seconds. Defaults to \"0\".",
    "enum": [
     "0",
     "1",
     "2"
    ],
    "type": "string"
   },
   "timezone": {
    "description": "IANA timezone name for output times (e.g. America/New_York, Europe/London, Asia/Tokyo). Auto-detected from lat/lng if omitted.",
    "type": "string"
   }
  },
  "required": [
   "lat",
   "lng"
  ],
  "type": "object"
 },
 "name": "sunrisesunset.sun.position",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "SunriseSunset \u2014 Sun Position & Golden Hour"
}
