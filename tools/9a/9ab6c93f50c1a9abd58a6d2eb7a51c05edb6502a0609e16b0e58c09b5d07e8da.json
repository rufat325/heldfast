{
 "description": "Tap at a point on the simulator screen. Coordinates are in simulator screen points (e.g., 0-393 for iPhone width). Take a screenshot first to identify coordinates.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "deviceId": {
    "description": "Device UDID, name, or \"booted\" (default: booted)",
    "type": "string"
   },
   "duration": {
    "description": "Press duration in seconds (decimal allowed, e.g. 0.5). Default: normal tap",
    "type": "number"
   },
   "x": {
    "description": "X coordinate in simulator screen points",
    "type": "number"
   },
   "y": {
    "description": "Y coordinate in simulator screen points",
    "type": "number"
   }
  },
  "required": [
   "x",
   "y"
  ],
  "type": "object"
 },
 "name": "simulator_tap"
}
