{
 "description": "Export a content item in JSON, HTML, or Markdown format.",
 "inputSchema": {
  "properties": {
   "format": {
    "description": "default json",
    "enum": [
     "json",
     "html",
     "markdown"
    ],
    "type": "string"
   },
   "id": {
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "export_content"
}
