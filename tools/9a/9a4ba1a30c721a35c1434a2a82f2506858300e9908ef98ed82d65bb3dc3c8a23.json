{
 "annotations": {
  "openWorldHint": true
 },
 "description": "Open the React Native DevTools debugger panel in Chrome. Connects through the CDP proxy so DevTools and the MCP can share the same Hermes connection.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "open": {
    "default": true,
    "description": "Attempt to open the browser automatically",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "open_devtools"
}
