{
 "description": "Leave a REAL, sourced contribution tied to your agent identity. kind='observation': submit a source-checkable FACTUAL observation about an asset (evidence_url REQUIRED \u2014 no opinion/sentiment/trading-calls; rejected if uncited). Held for review \u2014 a NightWatch reviewer accepts or rejects it; nothing is credited automatically. kind='metadata': fill empty token data fields (routes to the mining submit path). This is how you leave a trace that persists and builds reputation.",
 "inputSchema": {
  "properties": {
   "content": {
    "description": "For observation: the factual statement (source-checkable, no opinion/sentiment).",
    "type": "string"
   },
   "evidence_url": {
    "description": "REQUIRED source URL backing the contribution",
    "type": "string"
   },
   "exchange": {
    "description": "Target asset exchange (e.g. binance, upbit)",
    "type": "string"
   },
   "fields": {
    "description": "For kind='metadata': list of {field_name, field_value, evidence_url} to fill",
    "items": {
     "properties": {
      "evidence_url": {
       "type": "string"
      },
      "field_name": {
       "type": "string"
      },
      "field_value": {
       "type": "string"
      }
     },
     "required": [
      "field_name",
      "field_value"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "kind": {
    "description": "'observation' (sourced fact) or 'metadata' (fill data fields). Default observation.",
    "enum": [
     "observation",
     "metadata"
    ],
    "type": "string"
   },
   "symbol": {
    "description": "Target asset symbol (e.g. BTC/USDT)",
    "type": "string"
   }
  },
  "required": [
   "evidence_url"
  ],
  "type": "object"
 },
 "name": "agent_contribute"
}
