{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find used cars for sale matching criteria (make/model/price/location) \u2014 active listings from US physical & online dealers. Example: autodev_listings({ make: \"Toyota\", model: \"Camry\", year_min: 2020, price_max: 30000, zip: \"94103\", limit: 20, _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-auto-dev-api-key",
    "limit": 20,
    "make": "Toyota",
    "model": "Camry",
    "price_max": 30000,
    "year_min": 2020,
    "zip": "94103"
   },
   {
    "_apiKey": "your-auto-dev-api-key",
    "make": "Honda",
    "price_max": 25000,
    "radius": 50,
    "year_min": 2018
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Auto.dev API key (1,000 free calls/mo at auto.dev)",
    "type": "string"
   },
   "limit": {
    "description": "Max listings to return (default 20, max 50)",
    "type": "number"
   },
   "make": {
    "description": "Vehicle make, e.g. \"Toyota\" (optional)",
    "type": "string"
   },
   "model": {
    "description": "Vehicle model, e.g. \"Camry\" (optional)",
    "type": "string"
   },
   "price_max": {
    "description": "Maximum listing price in USD (optional)",
    "type": "number"
   },
   "radius": {
    "description": "Search radius in miles from zip (optional)",
    "type": "number"
   },
   "year_max": {
    "description": "Newest model year to include (optional)",
    "type": "number"
   },
   "year_min": {
    "description": "Oldest model year to include (optional)",
    "type": "number"
   },
   "zip": {
    "description": "ZIP code to center the search on (optional)",
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "autodev_listings",
 "title": "Autodev Listings",
 "version": "v1.a49a38b9"
}
