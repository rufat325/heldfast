{
 "description": "Creates a guest mr_\u2026 Asks for no e-mail. Publishing a listing is what requires an account (or a payment). At most 20 new guests per network per hour; a browser that already has one gets it back from the `mr_guest` cookie.",
 "inputSchema": {
  "properties": {},
  "type": "object"
 },
 "name": "create_guest"
}
