{
 "description": "Remove a NIP from monitoring. Requires a skanfirmy API key.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "skanfirmy API key (optional, if not in the header).",
    "type": "string"
   },
   "nip": {
    "description": "10-digit NIP to remove from monitoring.",
    "type": "string"
   }
  },
  "required": [
   "nip"
  ],
  "type": "object"
 },
 "name": "unobserve_nip"
}
