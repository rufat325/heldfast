{
 "description": "Compare real monthly cost on Zapier (per task) versus Make (per operation) for the same workflow volume, including the counting-model difference that makes the two quotes non-obvious.",
 "inputSchema": {
  "properties": {
   "runsPerMonth": {
    "description": "Workflow executions per month",
    "type": "number"
   },
   "stepsPerRun": {
    "description": "Total steps (trigger + actions) per run",
    "type": "number"
   },
   "workflowCount": {
    "description": "Number of distinct workflows (default 1)",
    "type": "number"
   }
  },
  "required": [
   "runsPerMonth",
   "stepsPerRun"
  ],
  "type": "object"
 },
 "name": "compare_automation_platform_costs"
}
