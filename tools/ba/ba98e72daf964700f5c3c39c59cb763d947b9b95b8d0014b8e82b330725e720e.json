{
 "description": "List a volume's background async jobs via its \"jobList\" action. Longhorn does not publish a dedicated input or output schema for this action in /v1/schemas, and it has no corresponding docs page \u2014 use longhorn_describe_resource_type('volume') to check the deployed version's exact shape, or pass fields via extra.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "extra": {
    "additionalProperties": {},
    "description": "Additional Longhorn fields not covered by the named parameters above, merged verbatim into the request body.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "name": {
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "volume_job_list",
 "title": "List volume jobs"
}
