{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Synchronous path analysis: setup and hold slack, and maximum frequency. The clock period appears ONLY in the setup check, so a setup failure is fixed by lowering the frequency and a hold failure is not \u2014 no speed makes it work. Skew enters both with opposite signs. With irDropV+vdd+vth it also derates the logic by voltage (alpha-power law), connecting power delivery to the clock. First-order: ONE path, delays as numbers, no process variation, no jitter, no temperature derating.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "irDropV": {
    "description": "With vdd and vth: slows the logic down by that voltage drop",
    "maximum": 5,
    "minimum": 0,
    "type": "number"
   },
   "tclkNs": {
    "default": 2,
    "description": "Clock period",
    "maximum": 1000,
    "minimum": 0.01,
    "type": "number"
   },
   "tcqNs": {
    "default": 0.1,
    "description": "Launch flop delay, edge to output",
    "maximum": 100,
    "minimum": 0,
    "type": "number"
   },
   "tholdNs": {
    "default": 0.05,
    "maximum": 100,
    "minimum": 0,
    "type": "number"
   },
   "tlogicMinNs": {
    "default": 0.2,
    "description": "Logic, SHORT path. Rules hold",
    "maximum": 500,
    "minimum": 0,
    "type": "number"
   },
   "tlogicNs": {
    "default": 1.2,
    "description": "Logic, LONG path. Rules setup",
    "maximum": 500,
    "minimum": 0,
    "type": "number"
   },
   "tsetupNs": {
    "default": 0.08,
    "maximum": 100,
    "minimum": 0,
    "type": "number"
   },
   "tskewNs": {
    "default": 0,
    "description": "Positive = the clock reaches the capturing flop LATER: gives setup, takes hold",
    "maximum": 100,
    "minimum": -100,
    "type": "number"
   },
   "vdd": {
    "maximum": 5,
    "minimum": 0.05,
    "type": "number"
   },
   "vth": {
    "maximum": 3,
    "minimum": 0.01,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "timing_path"
}
