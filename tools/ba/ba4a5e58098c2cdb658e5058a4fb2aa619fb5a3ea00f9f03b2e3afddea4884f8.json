{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Send test email"
 },
 "description": "Send a test email of the template to one or more email addresses via SES. Use after nevent_preview_template validates rendering. Test emails carry test indicators in headers and are sent via AWS SES. This call may take up to 60 seconds due to SES delivery. Returns success status, list of recipients the email was sent to, and any error details.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "emails": {
    "description": "List of email addresses to send the test email to. Each entry must be a valid email address. Minimum 1, maximum 10 recipients. Example: [\"qa@example.com\", \"dev@company.com\"].",
    "items": {
     "format": "email",
     "type": "string"
    },
    "maxItems": 10,
    "minItems": 1,
    "type": "array"
   },
   "parameters": {
    "additionalProperties": {},
    "description": "Optional custom parameters for merge-tag substitution in the test email. Key-value pairs where keys match merge tag names. Example: {name: \"Juan\", city: \"Madrid\"}. When provided, overrides sample user profile data for the corresponding tags.",
    "type": "object"
   },
   "sample_user_email": {
    "description": "Optional user email within the active tenant for merge-tag personalization. Ignored when sample_user_id is also provided.",
    "format": "email",
    "type": "string"
   },
   "sample_user_id": {
    "description": "Optional user ID for merge-tag personalization in the test email. When provided, {{name}}, {{email}} and custom fields are resolved from this user's profile. Takes priority over sample_user_email.",
    "minLength": 1,
    "type": "string"
   },
   "subject": {
    "description": "Optional subject line for the test email. Omit to use the default subject from the template.",
    "type": "string"
   },
   "template_id": {
    "description": "The ID of the email template to send as a test. Use nevent_list_templates to discover valid template IDs.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "template_id",
   "emails"
  ],
  "type": "object"
 },
 "name": "nevent_send_test_template"
}
