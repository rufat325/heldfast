{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Search Modifications by Tire"
 },
 "description": "Find specific trim/modification variants of a known make+model compatible with a given tyre size. Required: make, model, section_width (mm), aspect_ratio (%), rim_diameter (inches). Optional: region, limit, offset. More granular than search-by-tire. Use when you already know the vehicle.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "aspect_ratio": {
    "description": "Aspect ratio as a percentage integer 1-100 (e.g. 45 for a 45-series tyre). Use list-tire-aspect-ratios to enumerate valid values.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "limit": {
    "description": "Maximum results to return (1-100).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "make": {
    "description": "Manufacturer slug (e.g. \"audi\"). Get slugs from list-makes.",
    "type": "string"
   },
   "model": {
    "description": "Model slug (e.g. \"a4\"). Get slugs from list-models.",
    "type": "string"
   },
   "offset": {
    "description": "Number of results to skip for pagination.",
    "minimum": 0,
    "type": "integer"
   },
   "region": {
    "description": "Market region code (e.g. \"eudm\", \"usdm\"). Get codes from list-regions.",
    "type": "string"
   },
   "rim_diameter": {
    "description": "Rim diameter in inches (e.g. 18). Must be positive. Use list-tire-rim-diameters to enumerate valid values.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "section_width": {
    "description": "Tyre section width in mm (e.g. 245). Must be positive. Use list-tire-section-widths to enumerate valid values.",
    "exclusiveMinimum": 0,
    "type": "number"
   }
  },
  "required": [
   "make",
   "model",
   "section_width",
   "aspect_ratio",
   "rim_diameter"
  ],
  "type": "object"
 },
 "name": "search-modifications-by-tire"
}
