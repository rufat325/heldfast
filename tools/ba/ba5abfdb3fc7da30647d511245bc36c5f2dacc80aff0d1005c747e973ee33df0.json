{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Start an N-of-1 experiment for the signed-in user. `answers` maps baseline question ids (from supstack_experiment_protocol) to the user's answers. If required baseline answers are missing, returns a 400 listing them \u2014 gather those and retry. Returns the created experiment with its first check-in date. Requires the user to be signed in (`supstack login`).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "answers": {
    "additionalProperties": {},
    "default": {},
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "goal": {
    "minLength": 1,
    "type": "string"
   },
   "supplement": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "supplement",
   "goal"
  ],
  "type": "object"
 },
 "name": "supstack_experiment_start"
}
