{
 "description": "START HERE. Returns the AppCraft model JSON Schema, the capability card (what the compiler CAN and CANNOT build \u2014 never promise unsupported features to users), and the recommended tool workflow.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {},
  "type": "object"
 },
 "name": "get_schema"
}
