{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false
 },
 "description": "Enable or disable a Chrome extension by id without uninstalling. Returns the updated list. (Cockpit only.) To uninstall entirely, use ext_remove.",
 "inputSchema": {
  "properties": {
   "enabled": {
    "description": "true to enable, false to disable.",
    "type": "boolean"
   },
   "id": {
    "description": "Extension id to toggle (see ext_list).",
    "type": "string"
   }
  },
  "required": [
   "id",
   "enabled"
  ],
  "type": "object"
 },
 "name": "ext_set_enabled"
}
