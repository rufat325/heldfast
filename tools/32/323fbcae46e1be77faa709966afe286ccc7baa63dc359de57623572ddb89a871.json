{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List email messages from an Outlook / Microsoft 365 mailbox folder (default: inbox). Returns compact message summaries (subject, sender, received time, preview, read status, attachment flag). Supports keyword search and unread-only filtering. Use to browse a user's Outlook email.",
 "inputSchema": {
  "examples": [
   {
    "folder": "inbox",
    "top": 25
   },
   {
    "folder": "inbox",
    "search": "budget proposal",
    "unread_only": true
   }
  ],
  "properties": {
   "folder": {
    "description": "Mail folder to read from (default: \"inbox\"). Use a well-known name like \"inbox\", \"sentitems\", \"drafts\", \"deleteditems\", or a folder ID from outlook_list_folders.",
    "type": "string"
   },
   "search": {
    "description": "Optional free-text keyword search across the messages (subject, body, sender, etc.).",
    "type": "string"
   },
   "top": {
    "description": "Maximum number of messages to return (default 25, max 100).",
    "type": "number"
   },
   "unread_only": {
    "description": "If true, return only unread messages.",
    "type": "boolean"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "outlook_list_messages",
 "title": "Outlook List Messages",
 "version": "v1.d5d928dc"
}
