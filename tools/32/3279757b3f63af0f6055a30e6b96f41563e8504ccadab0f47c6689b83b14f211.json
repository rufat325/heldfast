{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Cancel a rental car booking"
 },
 "description": "Cancel an existing vehicle booking by its Gondola booking ID. Use this when a user wants to cancel a rental they previously booked.",
 "inputSchema": {
  "properties": {
   "booking_id": {
    "description": "The Gondola booking ID (confirmation number) of the vehicle booking.",
    "type": "string"
   },
   "confirm": {
    "default": false,
    "description": "Set true only after the user has seen the cancellation terms from the preview call and agreed.",
    "type": "boolean"
   }
  },
  "required": [
   "booking_id"
  ],
  "type": "object"
 },
 "name": "cancel_vehicle_booking",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "cancel_vehicle_bookingOutput",
  "type": "object"
 }
}
