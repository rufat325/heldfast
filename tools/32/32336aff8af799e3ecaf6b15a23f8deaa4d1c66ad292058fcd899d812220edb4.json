{
 "description": "Decode a JWT without verifying it. POST {\"token\": \"eyJ...\"} -> header, payload, expiry status. Signature is NOT verified. Billed $0.01 in USDC on Base.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "token": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "jwt_decode",
 "title": "jwt_decode ($0.01)"
}
