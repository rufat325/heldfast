{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List Rim Centre Bores"
 },
 "description": "List all centre bore (CB) values available, optionally filtered by rim dimensions, offset range, and bolt pattern. Centre bore is the diameter of the hole in the wheel centre in mm (e.g. 66.5). Use this to discover valid cb values for search-by-rim.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "bolt_pattern": {
    "description": "Bolt pattern (PCD) filter, expressed as stud-count \u00d7 circle-diameter in mm (e.g. \"5x112\", \"4x100\"). Use list-rim-bolt-patterns to enumerate valid values.",
    "type": "string"
   },
   "region": {
    "description": "Market region code (e.g. \"eudm\", \"usdm\"). Get codes from list-regions.",
    "type": "string"
   },
   "rim_diameter": {
    "description": "Rim diameter in inches (e.g. 18). Must be positive. Filters results to this diameter only.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "rim_offset": {
    "description": "Exact rim offset (ET) in mm, range -100 to 100 (e.g. 35). The distance between the wheel mounting surface and the centre-line of the rim. Do not combine with rim_offset_min/rim_offset_max.",
    "maximum": 100,
    "minimum": -100,
    "type": "number"
   },
   "rim_offset_max": {
    "description": "Maximum rim offset (ET) in mm, range -100 to 100. Use instead of rim_offset for a range query.",
    "maximum": 100,
    "minimum": -100,
    "type": "number"
   },
   "rim_offset_min": {
    "description": "Minimum rim offset (ET) in mm, range -100 to 100. Use instead of rim_offset for a range query.",
    "maximum": 100,
    "minimum": -100,
    "type": "number"
   },
   "rim_width": {
    "description": "Rim width in inches from bead seat to bead seat (e.g. 8.0). Must be positive.",
    "exclusiveMinimum": 0,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list-rim-centre-bores"
}
