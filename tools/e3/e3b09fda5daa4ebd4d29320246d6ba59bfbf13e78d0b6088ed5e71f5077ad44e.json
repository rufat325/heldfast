{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "group \u2014 puts nodeIds in a new group, inside the first one\u2019s own parent. ungroup \u2014 dissolves a group or frame and returns the children it let go.\n\nA group is the honest answer when several layers are one thing but need no layout of their own; reach for figma_create_frame with auto layout when they do.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "Default group.",
    "enum": [
     "group",
     "ungroup"
    ],
    "type": "string"
   },
   "name": {
    "description": "For group: what to call it.",
    "type": "string"
   },
   "nodeId": {
    "description": "For ungroup: the group or frame to dissolve.",
    "type": "string"
   },
   "nodeIds": {
    "description": "For group: the layers to gather, up to 20.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "figma_group",
 "title": "Group or ungroup"
}
