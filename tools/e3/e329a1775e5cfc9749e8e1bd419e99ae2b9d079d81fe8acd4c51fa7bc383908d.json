{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Update Form"
 },
 "description": "Edit a form on a live event \u2014 rename and/or replace its questions. Pass questions with their ids (from list_forms) to keep responses mapped. Requires event_id + form_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "form_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "name": {
    "maxLength": 200,
    "minLength": 1,
    "type": "string"
   },
   "questions": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "canonical_key": {
       "anyOf": [
        {
         "maxLength": 120,
         "type": "string"
        },
        {
         "type": "null"
        }
       ]
      },
      "id": {
       "maxLength": 128,
       "type": "string"
      },
      "label": {
       "maxLength": 500,
       "minLength": 1,
       "type": "string"
      },
      "options": {
       "items": {
        "maxLength": 300,
        "type": "string"
       },
       "maxItems": 100,
       "type": "array"
      },
      "placeholder": {
       "maxLength": 300,
       "type": "string"
      },
      "required": {
       "type": "boolean"
      },
      "type": {
       "enum": [
        "text",
        "textarea",
        "select",
        "multiSelect",
        "email",
        "phone",
        "url",
        "number"
       ],
       "type": "string"
      }
     },
     "required": [
      "label",
      "type"
     ],
     "type": "object"
    },
    "maxItems": 100,
    "type": "array"
   }
  },
  "required": [
   "event_id",
   "form_id"
  ],
  "type": "object"
 },
 "name": "update_form",
 "title": "Update Form"
}
