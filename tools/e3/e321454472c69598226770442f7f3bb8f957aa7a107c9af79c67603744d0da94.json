{
 "description": "Start a new comment discussion on a block. If `anchor_text` is provided, the comment is anchored inline to that exact substring of the block's text (the Notion UI highlights it yellow). Without `anchor_text`, the discussion attaches at the block level. Supports markdown **bold**, *italic*, `code`, and [links](url) in the comment body.",
 "inputSchema": {
  "properties": {
   "anchor_text": {
    "description": "Optional: substring of the block's text to anchor the comment to (inline comment). Must match exactly.",
    "type": "string"
   },
   "block_id": {
    "description": "Notion block ID (32-char hex) \u2014 the block containing the text to comment on. A page ID also works (block-level only).",
    "type": "string"
   },
   "text": {
    "description": "Comment body (supports markdown inline formatting)",
    "type": "string"
   }
  },
  "required": [
   "block_id",
   "text"
  ],
  "type": "object"
 },
 "name": "notion_add_comment"
}
