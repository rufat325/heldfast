{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Replace the alias IPs that a server has on a private network. The list overrides any existing alias IPs for that network.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "alias_ips": {
    "description": "Full list of alias IPs to set on the network (replaces existing). Pass [] to clear.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "id": {
    "description": "Server ID",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "network": {
    "description": "ID of the network the server is attached to",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   }
  },
  "required": [
   "id",
   "network",
   "alias_ips"
  ],
  "type": "object"
 },
 "name": "hetzner_change_alias_ips",
 "title": "Change Server Alias IPs"
}
