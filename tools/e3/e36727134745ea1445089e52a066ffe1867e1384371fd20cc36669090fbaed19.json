{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Record one Stage 2b verifier outcome against the seat that cited the claim. Non-clean findings must be explicitly corrected in the portfolio-manager synthesis and may support a sourced one-notch downside adjustment under pm_rating_rubric_v2. They do not create automatic negative votes or mechanically change the final rating; legacy diagnostic weights remain audit metadata only.",
 "inputSchema": {
  "properties": {
   "claim": {
    "type": "string"
   },
   "note": {
    "type": "string"
   },
   "run_id": {
    "type": "string"
   },
   "seat": {
    "description": "The seat whose claim was checked, e.g. bull_researcher or master_buffett.",
    "type": "string"
   },
   "verdict": {
    "description": "Must be one of the verifier's declared verdict_values.",
    "type": "string"
   },
   "verifier": {
    "enum": [
     "source_fidelity",
     "rederivation",
     "refuter"
    ],
    "type": "string"
   }
  },
  "required": [
   "run_id",
   "verifier",
   "seat",
   "verdict"
  ],
  "type": "object"
 },
 "name": "record_verifier_verdict",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 },
 "title": "Record a Claim Verification"
}
