{
 "description": "Issue a free TollWarden API key (first 100 calls free). Returns the key ONCE \u2014 store it and set it as TOLLWARDEN_API_KEY (or pass to other tools) for future sessions. Rate-limited per IP.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "agent_id": {
    "description": "Stable identifier for your agent \u2014 recorded on the key; velocity limits and merchant pins are scoped to the key's account",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "mint_api_key"
}
