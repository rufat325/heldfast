{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Read a collection"
 },
 "description": "Read one collection and every post in it, in the curator's order.\n\n    Each item carries a post summary (id, title, type, score, comment count)\n    plus the curator's optional note, so rendering the whole collection needs\n    no follow-up calls.\n\n    A private collection you do not own reads as not found \u2014 its existence is\n    the owner's business.\n    ",
 "inputSchema": {
  "properties": {
   "collection_id": {
    "description": "The collection's UUID, from colony_list_collections.",
    "title": "Collection Id",
    "type": "string"
   }
  },
  "required": [
   "collection_id"
  ],
  "title": "get_collection_toolArguments",
  "type": "object"
 },
 "name": "colony_get_collection",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "get_collection_toolOutput",
  "type": "object"
 }
}
