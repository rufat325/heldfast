{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run a raw SoQL query against any Seattle open-data resource (data.seattle.gov) by its Socrata id (8-char like \"tazs-3rd5\"). Full SoQL: where/select/group/order/limit/offset. Use seattle_datasets to find a resource id, or seattle_recent for the common ones.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-data-seattle-api-key",
    "group": "offense",
    "limit": 10,
    "order": "count(*) DESC",
    "resource_id": "tazs-3rd5",
    "select": "offense, count(*)"
   },
   {
    "_apiKey": "your-data-seattle-api-key",
    "limit": 100,
    "resource_id": "wvfv-dwka",
    "where": "issue_date >= '2024-01-01'"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Socrata app token for higher rate limits. Omit to use the keyless endpoint.",
    "type": "string"
   },
   "group": {
    "description": "SoQL $group.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (default 100, max 5000).",
    "type": "number"
   },
   "offset": {
    "description": "Row offset for paging.",
    "type": "number"
   },
   "order": {
    "description": "SoQL $order (e.g. \"report_date_time DESC\").",
    "type": "string"
   },
   "resource_id": {
    "description": "Socrata resource id, e.g. \"tazs-3rd5\" (crime data).",
    "type": "string"
   },
   "select": {
    "description": "SoQL $select (e.g. \"offense, count(*)\").",
    "type": "string"
   },
   "where": {
    "description": "SoQL $where filter.",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "seattle_query",
 "title": "Seattle Query",
 "version": "v1.27e3336e"
}
