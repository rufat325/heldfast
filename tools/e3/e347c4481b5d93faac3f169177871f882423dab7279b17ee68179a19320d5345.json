{
 "description": "Immediately before attachment, recompute the prepared resume fingerprint, validate its run and expiration, and lock the confirmed file read-only.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "confirmationId": {
    "maxLength": 200,
    "minLength": 1,
    "type": "string"
   },
   "context": {
    "description": "Briefly explain why this tool helps the current job-search goal. Never include resume text, profile answers, demographic or work-authorization answers, application notes, credentials, or secrets.",
    "type": "string"
   },
   "exactLocalPath": {
    "maxLength": 4096,
    "minLength": 1,
    "type": "string"
   },
   "expiresAt": {
    "format": "date-time",
    "type": "string"
   },
   "resumeId": {
    "minimum": 1,
    "type": "integer"
   },
   "runId": {
    "minimum": 1,
    "type": "integer"
   },
   "sha256": {
    "pattern": "^[a-f0-9]{64}$",
    "type": "string"
   },
   "sizeBytes": {
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "runId",
   "resumeId",
   "confirmationId",
   "exactLocalPath",
   "sha256",
   "sizeBytes",
   "expiresAt"
  ],
  "type": "object"
 },
 "name": "trackly_verify_prepared_resume"
}
