{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Match a config filename against the SchemaStore catalog (schemastore.org) to find which JSON Schemas declare a fileMatch glob covering it, for example package.json, tsconfig.json, or .github/workflows/ci.yml. Returns the matching catalog entries with schema name, description, fileMatch globs, and the json.schemastore.org schema URL. Answers which JSON Schema validates a given config file so an editor or linter can be pointed at it.",
 "inputSchema": {
  "examples": [
   {
    "filename": "package.json"
   },
   {
    "filename": ".github/workflows/ci.yml"
   }
  ],
  "properties": {
   "filename": {
    "description": "e.g. package.json, .github/workflows/ci.yml",
    "type": "string"
   }
  },
  "required": [
   "filename"
  ],
  "type": "object"
 },
 "name": "find_schema_for",
 "outputSchema": {
  "properties": {
   "filename": {
    "description": "Input filename searched",
    "type": "string"
   },
   "matches": {
    "description": "Catalog entries with matching fileMatch globs",
    "items": {
     "properties": {
      "description": {
       "description": "Schema description",
       "type": "string"
      },
      "fileMatch": {
       "description": "Glob patterns matching filenames",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "name": {
       "description": "Schema name",
       "type": "string"
      },
      "url": {
       "description": "Schema URL",
       "type": "string"
      },
      "versions": {
       "additionalProperties": {
        "type": "string"
       },
       "description": "Schema versions mapping",
       "type": "object"
      }
     },
     "required": [
      "name",
      "url"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "filename",
   "matches"
  ],
  "type": "object"
 },
 "title": "Find Schema For",
 "version": "v1.1c248641"
}
