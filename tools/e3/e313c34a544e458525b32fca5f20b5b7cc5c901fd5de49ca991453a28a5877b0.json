{
 "description": "Your weight, share, weight lock, sBTC balance, and every propose precondition folded from the contract's `propose-status` \u2014 so a blocked propose names the gate to wait on.\n\nRequires an unlocked wallet. Signs nothing.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "era": {
    "description": "Which legion deployment to read: 1 (live, default). Proposal ids restart at 1 each era, so an id alone is ambiguous.",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "legion_my_position"
}
