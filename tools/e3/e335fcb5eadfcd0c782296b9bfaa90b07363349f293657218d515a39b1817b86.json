{
 "description": "Generate per-scene TTS narration (skips unchanged scenes). Returns a jobId immediately \u2014 poll job_status. Rejects if another job is running.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dir": {
    "description": "demo project directory \u2014 pass an absolute path",
    "type": "string"
   },
   "force": {
    "type": "boolean"
   },
   "lang": {
    "description": "render a language variant from scene.narrations[<code>] over the shared take (artifacts namespaced: audio/<code>/, captions.<code>.*, output/final-demo.<code>.mp4). Omit for the default single-language render.",
    "type": "string"
   },
   "params": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "storyboard template params (name \u2192 value); each must be declared in the storyboard's params block. Substitutes {{name}} across all stages.",
    "type": "object"
   },
   "scene": {
    "description": "regenerate only this scene id",
    "type": "string"
   }
  },
  "required": [
   "dir"
  ],
  "type": "object"
 },
 "name": "voice",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "demoDir": {
    "description": "resolved absolute demo directory",
    "type": "string"
   },
   "jobId": {
    "type": "string"
   },
   "kind": {
    "type": "string"
   },
   "logFile": {
    "type": "string"
   },
   "status": {
    "type": "string"
   }
  },
  "required": [
   "jobId",
   "kind",
   "demoDir",
   "status",
   "logFile"
  ],
  "type": "object"
 }
}
