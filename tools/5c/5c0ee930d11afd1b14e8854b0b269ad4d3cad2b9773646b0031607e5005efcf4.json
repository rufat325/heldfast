{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Fingerprint fleet schemas"
 },
 "description": "Cluster a fleet of SQLite databases by schema and report how many distinct schemas are running, with each cluster's drift from the canonical (largest) one. Reads a fleet config file (litescope.fleet.yaml). Read-only.",
 "inputSchema": {
  "properties": {
   "config": {
    "description": "Path to the fleet config (default: litescope.fleet.yaml)",
    "type": "string"
   },
   "tag": {
    "description": "Only include databases with this tag",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "litescope_fingerprint",
 "outputSchema": {
  "properties": {
   "clusters": {
    "description": "Schema clusters, canonical first, with drift from canonical.",
    "type": "array"
   },
   "total": {
    "description": "Databases successfully fingerprinted.",
    "type": "integer"
   },
   "unreachable": {
    "description": "Databases that could not be read.",
    "type": "array"
   }
  },
  "required": [
   "clusters"
  ],
  "type": "object"
 },
 "title": "Fingerprint fleet schemas"
}
