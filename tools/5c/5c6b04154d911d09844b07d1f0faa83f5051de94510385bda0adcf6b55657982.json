{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Generate Brand Interview Questions"
 },
 "description": "POST /ideas/interview/questions \u2014 generate a brand-aware track of interview questions. Default count 8. Public API preview.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "body": {
    "additionalProperties": true,
    "description": "Optional body override for endpoints with sparse parameter schemas.",
    "type": "object"
   },
   "brand_id": {
    "type": "string"
   },
   "count": {
    "default": 8,
    "maximum": 20,
    "minimum": 4,
    "type": "integer"
   },
   "idempotency_key": {
    "type": "string"
   },
   "query": {
    "additionalProperties": true,
    "description": "Optional query overrides for endpoints with sparse parameter schemas.",
    "type": "object"
   },
   "steer": {
    "description": "Optional steering hint to bias the question set.",
    "type": "string"
   },
   "workspace_id": {
    "description": "Workspace to operate in. Do not ask the user for this UUID \u2014 call aidelly_list_workspaces and use the `id` of the matching workspace. Optional for read operations; required when creating content.",
    "format": "uuid",
    "type": "string"
   }
  },
  "required": [
   "idempotency_key"
  ],
  "type": "object"
 },
 "name": "aidelly_generate_brand_interview_questions"
}
