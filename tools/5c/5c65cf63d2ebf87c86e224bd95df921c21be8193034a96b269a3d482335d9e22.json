{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Run the dynamic targeted-proof oracle for a CodeSymbol and append a metadata-only ledger certificate. Public Proven closes only when baseline green plus a valid sentinel mutation of the credited symbol fails the same test at an assertion. Static record_run is diagnostics only.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "jest_config": {
    "description": "Repo-relative Jest config path.",
    "type": "string"
   },
   "link_node_modules": {
    "description": "Trusted-repo speed mode: symlink node_modules into temp copies.",
    "type": "boolean"
   },
   "method": {
    "description": "Optional consistency check. If provided, must match the member derived from target_symbol.",
    "type": "string"
   },
   "replacement": {
    "description": "TS/JS: inert sentinel body, e.g. 'return null;' or 'return {\"ok\":false};'. Must be JSON-only per the oracle (required for TS/JS targets). Go derives its own sentinel.",
    "type": "string"
   },
   "replacement_mode": {
    "description": "Sentinel wrapping mode. Default return-json.",
    "enum": [
     "return-json",
     "promise-json"
    ],
    "type": "string"
   },
   "run_id": {
    "description": "Optional caller-supplied run id. Defaults to a local monotonic id.",
    "type": "string"
   },
   "runner": {
    "description": "Test runner override. Default auto.",
    "enum": [
     "auto",
     "vitest",
     "jest",
     "mocha",
     "pytest"
    ],
    "type": "string"
   },
   "source": {
    "description": "Source checkout containing the test and target files. Defaults to workspace.",
    "type": "string"
   },
   "target_file": {
    "description": "Optional consistency check. If provided, must match the file derived from target_symbol.",
    "type": "string"
   },
   "target_id": {
    "description": "Optional graph target id that must resolve to exactly one hard-linked CodeSymbol.",
    "type": "string"
   },
   "target_symbol": {
    "description": "Exact CodeSymbol external id, e.g. sym:src/service.ts#OrderService.createOrder. Preferred.",
    "type": "string"
   },
   "test_env": {
    "description": "Explicit non-secret KEY=value env entries for the test run.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "test_path": {
    "description": "TS/JS: repo-relative test file to run (required for TS/JS targets).",
    "type": "string"
   },
   "test_run": {
    "description": "Go: fully-anchored test name for `go test -run`, e.g. '^TestCompute$' (required for Go targets). Java: 'TestClass#testMethod', e.g. 'CalculatorTest#addsTwoNumbers' (required for Java targets).",
    "type": "string"
   },
   "timeout_ms": {
    "description": "Per baseline/mutant test timeout.",
    "maximum": 600000,
    "minimum": 1000,
    "type": "integer"
   },
   "vitest_config": {
    "description": "Repo-relative Vitest config path.",
    "type": "string"
   },
   "workspace": {
    "description": "Local workspace root. Defaults to the current directory.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "orangepro_prove",
 "title": "Run dynamic targeted proof"
}
