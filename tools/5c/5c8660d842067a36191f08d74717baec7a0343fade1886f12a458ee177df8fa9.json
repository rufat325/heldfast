{
 "_meta": {
  "fastmcp": {
   "tags": []
  }
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "The still style library, one ranked page at a time. Every painted\nstill (an ad creative, poster, launch card, App Store frame,\nthumbnail) is one style; Siren picks one when you don't. Styles sit\non section shelves; pass section and the human's goal and take one\nfrom the top. made_recently = this brand already got that look,\nprefer another. To paint in a style, put its id in the\ncreate_campaign brief (asset_type card). The whole library as one\nfile: https://api.mysiren.ai/api/v1/styles.md (also .txt/.json).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "all": {
    "default": false,
    "description": "true = every style that passes the filters on one page. Prefer section + page, or the styles.md file.",
    "type": "boolean"
   },
   "aspect": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Only styles that paint on this shape: square-ig, vertical-ig-feed, story-reel, wide-desktop."
   },
   "goal": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "The human's own words for the still, e.g. \"a hot take about launches\" or \"our pricing is simple\". Ranks the styles by fit."
   },
   "page": {
    "default": 1,
    "description": "Page number, from 1. next_page in the answer says what to call next.",
    "type": "integer"
   },
   "page_size": {
    "default": 8,
    "description": "Styles per page, 1 to 25.",
    "type": "integer"
   },
   "search": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Find styles by word in their id or when-to-pick line, e.g. \"comic\", \"receipt\", \"billboard\"."
   },
   "section": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "The shelf: social-ad (feed ads), poster, launch, brand, og (link previews), youtube (thumbnails), app-store, event, wallpaper, arts-creative. Omit to rank the whole library."
   }
  },
  "type": "object"
 },
 "name": "list_styles",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 }
}
