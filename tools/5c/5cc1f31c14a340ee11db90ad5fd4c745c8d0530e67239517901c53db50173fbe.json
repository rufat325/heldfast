{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Moves a node into another parent, or to a different position among its siblings. Index 0 is the back of the canvas and the top of a layer list; leaving it out puts the node last.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "index": {
    "description": "Position among siblings.",
    "minimum": 0,
    "type": "integer"
   },
   "nodeId": {
    "description": "The node to move.",
    "type": "string"
   },
   "parentId": {
    "description": "New parent. Default: keep the one it has.",
    "type": "string"
   }
  },
  "required": [
   "nodeId"
  ],
  "type": "object"
 },
 "name": "figma_move_node",
 "title": "Reparent or reorder a layer"
}
