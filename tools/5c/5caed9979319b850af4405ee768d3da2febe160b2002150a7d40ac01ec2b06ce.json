{
 "description": "Does this MCP server still answer, with which access, and is its repository alive? What independent MCP indexes measured about one listing of this catalog, each credited with its license. Take the id from list_listings. A signal belongs to a listing only when it is the SAME object: the name in the official MCP registry, the exact remote endpoint URL or the GitHub `owner/repo`, in that order. A shared domain never matches \u2014 two servers on one host are different products. Up to 20 signals, one per index and record.",
 "inputSchema": {
  "properties": {
   "api_pass": {
    "description": "MAT-only private pass: mat_<32 random hex>_<64 random hex>. Generate and save before buying.",
    "type": "string"
   },
   "listing": {
    "description": "Listing id from list_listings.",
    "type": "string"
   },
   "retry_key": {
    "description": "Optional retry key, up to 80 ASCII characters; same read for five minutes.",
    "type": "string"
   }
  },
  "required": [
   "listing"
  ],
  "type": "object"
 },
 "name": "mcp_index_signals"
}
