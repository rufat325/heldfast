{
 "description": "Shrink MCP tool descriptions and JSON schemas to cut context cost without changing how agents call them. Returns the compressed definitions plus exact token savings.",
 "inputSchema": {
  "properties": {
   "definitions": {
    "description": "JSON array of tool definitions to compress. Each item: {name, description, inputSchema}. Accepts a JSON array, or a JSON string containing one.",
    "type": [
     "string",
     "array",
     "object"
    ]
   }
  },
  "required": [
   "definitions"
  ],
  "type": "object"
 },
 "name": "compress_tool_definitions"
}
