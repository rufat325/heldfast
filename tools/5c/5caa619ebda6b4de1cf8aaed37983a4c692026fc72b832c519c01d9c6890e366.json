{
 "description": "Remove a local container image to free disk space",
 "inputSchema": {
  "properties": {
   "force": {
    "description": "Force removal even if the image is used by a stopped container (default: false)",
    "type": "boolean"
   },
   "image_name": {
    "description": "Image name/tag to remove (e.g. us.icr.io/mynamespace/myapp:v1.0.0)",
    "type": "string"
   },
   "runtime": {
    "enum": [
     "docker",
     "podman"
    ],
    "type": "string"
   }
  },
  "required": [
   "image_name"
  ],
  "type": "object"
 },
 "name": "remove_local_image"
}
