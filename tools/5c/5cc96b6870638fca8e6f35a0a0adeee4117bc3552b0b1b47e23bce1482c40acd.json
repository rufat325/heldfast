{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Validate whether an input string is well-formed JSON. Returns a valid flag and a parser error message when invalid. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "input": {
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "agentbit.json_validate",
 "title": "JSON Validate"
}
