{
 "_meta": {
  "openai/toolInvocation/invoked": "Order status",
  "openai/toolInvocation/invoking": "Checking order\u2026"
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Given a Stripe Checkout session id (cs_\u2026), reports whether it is paid and, if so, returns the permanent download link for the CSV. Works for whole files, filtered rows and combined (several-dataset) orders.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "sessionId": {
    "description": "Checkout session id from create_checkout",
    "pattern": "^cs_",
    "type": "string"
   }
  },
  "required": [
   "sessionId"
  ],
  "type": "object"
 },
 "name": "check_order",
 "title": "Check order status"
}
