{
 "description": "Hybrid search: FTS5 across all layers plus semantic ranking for memories. Reinforces returned memories (Hebbian).",
 "inputSchema": {
  "properties": {
   "limit": {
    "default": 10,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "type": "string"
   },
   "scope": {
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "recall"
}
