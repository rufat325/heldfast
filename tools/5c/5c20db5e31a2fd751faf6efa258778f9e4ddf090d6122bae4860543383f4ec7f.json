{
 "description": "Stop (and optionally remove) a local container by ID or name \u2014 cleans up after test_container_locally",
 "inputSchema": {
  "properties": {
   "container_id": {
    "description": "Container ID or name",
    "type": "string"
   },
   "remove": {
    "description": "Remove the container after stopping (default: false)",
    "type": "boolean"
   },
   "runtime": {
    "enum": [
     "docker",
     "podman"
    ],
    "type": "string"
   }
  },
  "required": [
   "container_id"
  ],
  "type": "object"
 },
 "name": "stop_local_container"
}
