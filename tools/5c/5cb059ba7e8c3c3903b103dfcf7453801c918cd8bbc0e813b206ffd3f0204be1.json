{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Real-estate development projects registered with the Dubai Land Department \u2014 developer, project status, percent complete, start/completion dates and location. A date range AND a date_type are both required; without date_type the upstream returns nothing.",
 "inputSchema": {
  "examples": [
   {
    "date_type": "1",
    "from_date": "2026-08-01",
    "limit": 3,
    "to_date": "2026-09-06"
   }
  ],
  "properties": {
   "date_type": {
    "description": "Which project date the range applies to: \"1\" start, \"2\" end, \"3\" adoption, \"4\" completion",
    "enum": [
     "1",
     "2",
     "3",
     "4"
    ],
    "type": "string"
   },
   "from_date": {
    "description": "Start of the range, ISO date e.g. \"2026-09-01\"",
    "type": "string"
   },
   "include_arabic": {
    "description": "Keep the Arabic (`*_AR`) twin of each bilingual column. Default false.",
    "type": "boolean"
   },
   "limit": {
    "description": "Rows to return, 1-200 (default 25)",
    "type": "number"
   },
   "offset": {
    "description": "Rows to skip, for paging through `total` (default 0)",
    "type": "number"
   },
   "project_type_id": {
    "description": "Upstream project-type id",
    "type": "string"
   },
   "sort": {
    "description": "Sort as \"<COLUMN>_ASC\" or \"<COLUMN>_DESC\" using a column name from the rows, e.g. \"TRANS_VALUE_DESC\". Omit for the upstream default order.",
    "type": "string"
   },
   "status": {
    "description": "Project status",
    "enum": [
     "ACTIVE",
     "FINISHED",
     "CANCELLED",
     "FRIEZED",
     "UNDER_REVIEWING",
     "UNDER_CANCELATION_DECISION",
     "UNDER_CANCELATION_NOTIFICATION",
     "CONDITIONAL_ACTIVATING"
    ],
    "type": "string"
   },
   "to_date": {
    "description": "End of the range, ISO date e.g. \"2026-09-06\"",
    "type": "string"
   },
   "zone_id": {
    "description": "Zone: \"1\" Deira, \"2\" Dubai",
    "enum": [
     "1",
     "2"
    ],
    "type": "string"
   }
  },
  "required": [
   "from_date",
   "to_date",
   "date_type"
  ],
  "type": "object"
 },
 "name": "dld_search_projects",
 "title": "Dld Search Projects",
 "version": "v1.ac085351"
}
