{
 "_meta": {
  "skill": "ordinals"
 },
 "description": "Get inscription content from a Bitcoin reveal transaction. Fetches the transaction from mempool.space and parses inscription data from the witness. Returns content type, body (as base64 and text if applicable), and metadata tags.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "txid": {
    "description": "Transaction ID of the reveal transaction containing the inscription",
    "maxLength": 64,
    "minLength": 64,
    "type": "string"
   }
  },
  "required": [
   "txid"
  ],
  "type": "object"
 },
 "name": "get_inscription"
}
