{
 "description": "Markdown summary of a map for chat context: key figures, layers, tilesets, properties, objects and the warnings from loading.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "path": {
    "description": "Path to ONE Tiled map file (.tmx or .tmj) \u2014 no directory, no wildcards. Please pass it absolute: a relative path is resolved against the working directory of the server process, and that is chosen by the MCP host, not by the project.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "path"
  ],
  "type": "object"
 },
 "name": "map_summary",
 "title": "Map summary"
}
