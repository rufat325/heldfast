{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Stop checking a monitor without deleting it. History and configuration survive, and resume_monitor puts it back to work. Use this around planned maintenance so the downtime does not land in the uptime figures or fire alerts. A paused monitor reports neither up nor down, so it is easy to forget one is off.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Monitor id to pause, as returned by list_monitors.",
    "exclusiveMinimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "pause_monitor",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "frequency": {
    "description": "Check interval in minutes.",
    "type": "integer"
   },
   "id": {
    "description": "Monitor id, used by every other tool.",
    "type": "integer"
   },
   "isActive": {
    "description": "False while the monitor is paused.",
    "type": "boolean"
   },
   "name": {
    "type": "string"
   },
   "statusSummary": {
    "description": "Current state, for example GOOD or ERROR.",
    "type": "string"
   },
   "type": {
    "enum": [
     "PING",
     "HTTP",
     "API",
     "SSL",
     "DOMAIN",
     "PORT"
    ],
    "type": "string"
   },
   "url": {
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "title": "Pause monitor"
}
