{
 "description": "Fork a template into a new derived template in the vault. Writes a new versioned template.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "as_ref": {
    "description": "New template reference to create.",
    "type": "string"
   },
   "base": {
    "description": "Base template reference to fork from.",
    "type": "string"
   }
  },
  "required": [
   "base",
   "as_ref"
  ],
  "type": "object"
 },
 "name": "template_vault_compose"
}
