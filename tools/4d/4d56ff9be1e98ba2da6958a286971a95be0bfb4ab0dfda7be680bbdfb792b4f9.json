{
 "description": "Get one email message by ID. Returns the parsed message: extracted code, action_url, every link with its anchor text, and the body as plain text. Set format='text' for a flat rendering to read directly, or include_html=true only if the raw HTML is genuinely needed.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "format": {
    "default": "json",
    "description": "json (structured) or text (flat, prompt-ready)",
    "enum": [
     "json",
     "text"
    ],
    "type": "string"
   },
   "include_html": {
    "default": false,
    "description": "Include the raw html_body in json output (default: false \u2014 it is large and rarely needed)",
    "type": "boolean"
   },
   "message_id": {
    "description": "Message ID from list_messages",
    "type": "string"
   }
  },
  "required": [
   "message_id"
  ],
  "type": "object"
 },
 "name": "get_message"
}
