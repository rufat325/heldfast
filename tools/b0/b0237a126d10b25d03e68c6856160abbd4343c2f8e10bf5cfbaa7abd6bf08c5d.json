{
 "description": "Update a Granoflow milestone with common structured fields. Defaults to dry-run. Use granoflow_milestone_context_update for description upkeep; description updates fail closed for archived milestones.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "description": {
    "type": "string"
   },
   "dryRun": {
    "default": true,
    "description": "When true, previews the request without writing.",
    "type": "boolean"
   },
   "dueAt": {
    "type": "string"
   },
   "milestoneId": {
    "description": "Granoflow milestone id.",
    "minLength": 1,
    "type": "string"
   },
   "projectId": {
    "minLength": 1,
    "type": "string"
   },
   "status": {
    "minLength": 1,
    "type": "string"
   },
   "title": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "milestoneId"
  ],
  "type": "object"
 },
 "name": "granoflow_milestone_update"
}
