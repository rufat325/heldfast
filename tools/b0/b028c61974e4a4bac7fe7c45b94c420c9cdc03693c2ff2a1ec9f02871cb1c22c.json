{
 "description": "Search for text patterns in a binary file using stream-based approach.",
 "inputSchema": {
  "properties": {
   "file_path": {
    "description": "Path to binary file",
    "type": "string"
   },
   "max_results": {
    "description": "Max results per pattern (default 50)",
    "type": "number"
   },
   "patterns": {
    "description": "Text patterns to search for",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "file_path",
   "patterns"
  ],
  "type": "object"
 },
 "name": "search_binary_pattern"
}
