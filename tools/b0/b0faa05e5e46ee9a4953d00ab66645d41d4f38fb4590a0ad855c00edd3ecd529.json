{
 "description": "Search for valid location names for use with start_competitive_analysis. Type a city or region name to find the exact location_name value. Tip: append a 2-letter country code after a comma to narrow results (e.g., 'Birm, US').",
 "inputSchema": {
  "properties": {
   "q": {
    "description": "Search query, at least 2 characters (e.g., 'Birmingham', 'London, GB', 'Abuja, NG')",
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "search_locations"
}
