{
 "description": "Fetch the current state of a single transcribe job by ID, including status (pending/processing/completed/failed/canceled) and `output_url` when completed. Mirrors `get_transcode` but for SRT generation jobs created via `transcribe_audio`.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Transcribe job UUID",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_transcribe",
 "title": "Get Transcribe"
}
