{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Expand the IP range of an existing network. The new CIDR range must contain the current range; shrinking is not supported.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "id": {
    "description": "Network ID",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "ip_range": {
    "description": "New IP range in CIDR notation (e.g. \"10.0.0.0/8\"). Must be a superset of the current range.",
    "type": "string"
   }
  },
  "required": [
   "id",
   "ip_range"
  ],
  "type": "object"
 },
 "name": "hetzner_change_ip_range",
 "title": "Change Network IP Range"
}
