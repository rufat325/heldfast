{
 "description": "Retrieve listing calendar via GET /v1/listings/{id}/calendar?includeResources=1. Each day has status (available/blocked/reserved/pending), isAvailable, and price (null means unknown \u2014 do not invent a rate). Nested reservations are denylist-stripped.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "endDate": {
    "description": "Y-m-d",
    "type": "string"
   },
   "listingId": {
    "type": "integer"
   },
   "startDate": {
    "description": "Y-m-d",
    "type": "string"
   }
  },
  "required": [
   "listingId",
   "startDate",
   "endDate"
  ],
  "type": "object"
 },
 "name": "get_calendar"
}
