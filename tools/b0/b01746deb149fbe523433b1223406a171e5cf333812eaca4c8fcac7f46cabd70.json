{
 "description": "List the comments on a file \u2014 open (unresolved) threads by default. Comments are feedback to act on: each shows the author (members and external share-link guests), the anchored line number if any, and the comment id needed by resolve_comment / add_comment replies.",
 "inputSchema": {
  "properties": {
   "fileId": {
    "description": "UUID of the file",
    "type": "string"
   },
   "includeResolved": {
    "description": "Also include resolved threads (default false)",
    "type": "boolean"
   }
  },
  "required": [
   "fileId"
  ],
  "type": "object"
 },
 "name": "list_comments"
}
