{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List scheduled meetings (events). Requires a user URI OR an organization URI (get them from current_user). Filter by status, time window, and paginate. Returns event URIs/UUIDs, names, start/end times, status, and locations.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-calendly-api-key",
    "user": "https://api.calendly.com/users/XXXXXXXXXXXXXXXX"
   },
   {
    "_apiKey": "your-calendly-api-key",
    "count": 50,
    "max_start_time": "2024-12-31T23:59:59Z",
    "min_start_time": "2024-01-01T00:00:00Z",
    "organization": "https://api.calendly.com/organizations/XXXXXXXXXXXXXXXX",
    "status": "active"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Calendly personal access token (sent as Bearer token)",
    "type": "string"
   },
   "count": {
    "description": "Number of results per page (default 20, max 100).",
    "type": "number"
   },
   "max_start_time": {
    "description": "ISO 8601 timestamp; only events starting at/before this time.",
    "type": "string"
   },
   "min_start_time": {
    "description": "ISO 8601 timestamp; only events starting at/after this time.",
    "type": "string"
   },
   "organization": {
    "description": "Organization URI (from current_user). Provide user OR organization.",
    "type": "string"
   },
   "page_token": {
    "description": "Token from a previous response for the next page.",
    "type": "string"
   },
   "status": {
    "description": "Filter by event status.",
    "enum": [
     "active",
     "canceled"
    ],
    "type": "string"
   },
   "user": {
    "description": "User URI (from current_user). Provide user OR organization.",
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "list_scheduled_events",
 "title": "List Scheduled Events",
 "version": "v1.5e6a335e"
}
