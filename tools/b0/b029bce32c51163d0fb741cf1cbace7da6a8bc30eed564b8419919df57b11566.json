{
 "description": "Check or reconcile project context YAML freshness. Low-risk factual snapshot deltas can reconcile; rules and wording conflicts return a proposal.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "attachment": {
    "default": "snapshot",
    "enum": [
     "snapshot",
     "rules",
     "project_snapshot.yaml",
     "project_rules.yaml"
    ],
    "type": "string"
   },
   "dryRun": {
    "default": true,
    "description": "When true, previews reconcile without writing.",
    "type": "boolean"
   },
   "projectId": {
    "description": "Granoflow project id.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "projectId"
  ],
  "type": "object"
 },
 "name": "granoflow_project_context_attachment_reconcile"
}
