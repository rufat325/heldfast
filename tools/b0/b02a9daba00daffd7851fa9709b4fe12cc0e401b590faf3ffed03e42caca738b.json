{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Verify Base transaction context"
 },
 "description": "Use when an agent needs one-call Base transaction context to verify a payment, debug a failed transaction, or confirm settlement. Costs $0.008 USDC via x402.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "tx_hash": {
    "description": "32-byte 0x-prefixed Base transaction hash.",
    "pattern": "^0x[0-9a-fA-F]{64}$",
    "type": "string"
   }
  },
  "required": [
   "tx_hash"
  ],
  "type": "object"
 },
 "name": "base_transaction_context"
}
