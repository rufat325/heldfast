{
 "description": "Exact integer inverse kinematics over a declared piece chain: compose local frames from the sovereign anchor and report each piece's pose in whole milli-units. Zero floats, one Q16 normalization point, byte-identical on arm64 and wasm32.",
 "inputSchema": {
  "properties": {
   "piece_id": {
    "description": "decimal string, the piece to solve for",
    "type": "string"
   },
   "pieces": {
    "description": "declared PieceFrame rows as decimal strings",
    "items": {
     "properties": {
      "extent_milli": {
       "description": "decimal string",
       "type": "string"
      },
      "origin_x_milli": {
       "description": "decimal string",
       "type": "string"
      },
      "origin_y_milli": {
       "description": "decimal string",
       "type": "string"
      },
      "origin_z_milli": {
       "description": "decimal string",
       "type": "string"
      },
      "parent_id": {
       "description": "decimal string",
       "type": "string"
      },
      "piece_id": {
       "description": "decimal string",
       "type": "string"
      },
      "rotation_milli_deg": {
       "description": "decimal string",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "relative_to_head": {
    "description": "state the pose relative to the head (camera-free) rather than the anchor",
    "type": "boolean"
   }
  },
  "required": [
   "pieces",
   "piece_id"
  ],
  "type": "object"
 },
 "name": "twin.robotics.evaluate_exact_ik"
}
