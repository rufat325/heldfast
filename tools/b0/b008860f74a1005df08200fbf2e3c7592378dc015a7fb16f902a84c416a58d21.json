{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the OpenTopography catalog for raster Digital Elevation Model (DEM) datasets covering a specified bounding box. Returns community-contributed and curated DEM datasets beyond the standard global datasets \u2014 including specialised regional surveys, UAV-derived models, and research DEMs. Returns dataset name, ID, DOI, format, creation date, and coverage bounds. Complements the standard elevation query tools by exposing niche high-resolution datasets. Source: OpenTopography catalog, CC BY 4.0, free registration (5K req/day).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "max_lat": {
    "description": "Northern boundary of search area in decimal degrees (-90 to 90)",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "max_lon": {
    "description": "Eastern boundary of search area in decimal degrees (-180 to 180)",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "min_lat": {
    "description": "Southern boundary of search area in decimal degrees (-90 to 90)",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "min_lon": {
    "description": "Western boundary of search area in decimal degrees (-180 to 180)",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   }
  },
  "required": [
   "min_lon",
   "min_lat",
   "max_lon",
   "max_lat"
  ],
  "type": "object"
 },
 "name": "opentopo.catalog.dem",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "OpenTopography DEM Raster Catalog"
}
