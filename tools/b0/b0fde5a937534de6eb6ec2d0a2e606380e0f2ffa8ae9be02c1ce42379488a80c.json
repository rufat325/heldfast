{
 "description": "Read the most recent assistant message from a Claude Code session log. If session_id is omitted, the most recently updated session is used.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "session_id": {
    "description": "Optional session id. Defaults to the most recently modified session.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_latest_output"
}
