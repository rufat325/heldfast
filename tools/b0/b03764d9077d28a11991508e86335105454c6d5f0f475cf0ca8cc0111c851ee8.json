{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Dump the current UI as a compact list of actionable elements (text, id, content-desc, tap coordinates). Use the @(x,y) centers with tap/long_press. Text-heavy screens are capped; pass 'filter' to search within them.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "filter": {
    "description": "Only include elements whose text/desc/id contains this (case-insensitive)",
    "type": "string"
   },
   "serial": {
    "description": "Device serial (defaults to session or the only device)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "snapshot_ui",
 "title": "Snapshot UI hierarchy"
}
