{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get availability calendar"
 },
 "description": "Open and blocked nights for one property over a date range of up to 93 days, with its minimum stay. Use this to suggest alternative dates; use get_live_quote to confirm and price a specific stay.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "end_date": {
    "description": "Last date to check, YYYY-MM-DD. At most 93 days after start_date.",
    "type": "string"
   },
   "property_id": {
    "description": "Property id or slug.",
    "type": "string"
   },
   "start_date": {
    "description": "First date to check, YYYY-MM-DD.",
    "type": "string"
   }
  },
  "required": [
   "property_id",
   "start_date",
   "end_date"
  ],
  "type": "object"
 },
 "name": "get_availability",
 "outputSchema": {
  "properties": {
   "blocked_dates": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "end_date": {
    "type": "string"
   },
   "minimum_nights": {
    "type": [
     "number",
     "null"
    ]
   },
   "nights_blocked": {
    "type": "integer"
   },
   "nights_open": {
    "type": "integer"
   },
   "note": {
    "type": "string"
   },
   "open_dates": {
    "description": "YYYY-MM-DD nights that are open. They still have to satisfy minimum stay and arrival rules.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "property_id": {
    "type": "integer"
   },
   "start_date": {
    "type": "string"
   }
  },
  "required": [
   "property_id",
   "start_date",
   "end_date",
   "nights_open",
   "nights_blocked",
   "open_dates",
   "blocked_dates",
   "note"
  ],
  "type": "object"
 },
 "title": "Get availability calendar"
}
