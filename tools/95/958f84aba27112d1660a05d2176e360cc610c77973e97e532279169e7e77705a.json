{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "The components, styles and variables this file has, with the ids the other tools need. Read this before \"make it match our button\" or \"use our brand colour\" \u2014 instantiating the real component or binding the real variable is what makes a change survive contact with the design system, rather than a hex code that looks right today.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "only": {
    "description": "Narrow the answer. Default all three.",
    "enum": [
     "components",
     "styles",
     "variables",
     "all"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "figma_list_library",
 "title": "The design system in this file"
}
