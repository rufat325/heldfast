{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get Product Detail"
 },
 "description": "Get the full detail of one or more tours or activities on GuruWalk in a single call. Pass an `items` array \u2014 each entry has its own type, product_id and language, and is processed independently. Returns a `results` array where every entry echoes its `product_id` and `type` so you can match each response to its request. Always batch when you need details for several tours (e.g. before recommending or comparing them): send them all in one call instead of invoking this tool several times. Each successful entry returns description, images, reviews, duration, available languages, cancellation policies, and meeting point info. Paid `product` entries also return `highlights`, `included`/`excluded`, `pricing_from`, and `where` (address + coordinates). `free_tour` entries return `itinerary` as a flat array of point-title strings (no descriptions), plus `guide.name`, `meeting_point_url`, and `how_to_find_me`. Both types return `meeting_point_url` (Google Maps link) whenever the meeting point has coordinates. For a `free_tour` that is the meeting point. For a paid `product` it is the reference point published on the listing: useful to orient a traveler who has not booked yet, but the operative meeting point of a purchased activity \u2014 with the supplier's own instructions \u2014 is the one in its voucher. Offer it as a reference, never as the confirmed meeting point of a booking. Paid `product` also returns the address text and coordinates in `where`; `free_tour` also returns `meeting_point_latitude` and `meeting_point_longitude` (use these coords as destination for routing), plus `how_to_find_me`: a free-text note written by the guide describing how the traveler can recognize them at the meeting point. Per-item errors (product not found) are reported inside that item's result without failing the rest of the batch. Use this tool whenever the traveler asks what a tour covers, which places it visits, its itinerary, route, description, meeting point, duration, or any content-related question. For the meeting point of a paid activity the traveler has already booked, the booking's voucher comes first; fall back to this tool only when that voucher has no meeting point yet, and say then that this is the point published on the listing and that the definitive one arrives with the voucher. Always call this tool BEFORE answering questions about a specific tour \u2014 never give generic opinions or advice without consulting the real data first. Supports en, es, de, it; any other value falls back to English. Pass the traveler's language code anyway and translate the answer into their language. Each result includes the tour's url \u2014 when recommending or confirming a tour, share it so the traveler can open it to book.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "items": {
    "description": "One or more tours/products to fetch details for (max 10 per call). Use a single entry for one tour, or many entries to batch multiple tours in the same call.",
    "items": {
     "properties": {
      "language": {
       "description": "Language code. Supported: en, es, de, it; any other value falls back to English. Defaults to en.",
       "type": "string"
      },
      "product_id": {
       "description": "The numeric ID of the product or free tour.",
       "type": "integer"
      },
      "type": {
       "description": "Type of listing: 'product' for paid activities, 'free_tour' for free walking tours.",
       "enum": [
        "product",
        "free_tour"
       ],
       "type": "string"
      }
     },
     "required": [
      "product_id",
      "type"
     ],
     "type": "object"
    },
    "maxItems": 10,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "items"
  ],
  "type": "object"
 },
 "name": "get_product_detail",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "results": {
    "description": "One entry per requested item, in request order, echoing its product_id and type.",
    "items": {
     "properties": {
      "attribution": {
       "type": "string"
      },
      "audio_guide_languages": {
       "description": "Paid activities only.",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "available_languages": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "cancellation_policies": {
       "description": "Paid activities only.",
       "type": "array"
      },
      "description_html": {
       "type": "string"
      },
      "duration": {
       "description": "Free tours only.",
       "type": [
        "string",
        "integer"
       ]
      },
      "duration_minutes": {
       "description": "Paid activities only.",
       "properties": {
        "from": {
         "type": "integer"
        },
        "to": {
         "type": "integer"
        }
       },
       "type": "object"
      },
      "error": {
       "description": "Per-item failure; other items in the batch are unaffected.",
       "type": "string"
      },
      "excluded": {
       "description": "Paid activities only.",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "features": {
       "description": "Free tours only.",
       "properties": {
        "accessible": {
         "type": [
          "boolean",
          "null"
         ]
        },
        "large_groups_allowed": {
         "type": [
          "boolean",
          "null"
         ]
        },
        "large_groups_limit": {
         "type": [
          "integer",
          "null"
         ]
        },
        "pets_allowed": {
         "type": [
          "boolean",
          "null"
         ]
        }
       },
       "type": "object"
      },
      "guide": {
       "description": "Free tours only.",
       "properties": {
        "name": {
         "type": "string"
        }
       },
       "type": "object"
      },
      "highlights": {
       "description": "Paid activities only.",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "how_to_find_me": {
       "description": "Free tours only: guide's note on how to recognize them at the meeting point.",
       "type": "string"
      },
      "id": {
       "type": "integer"
      },
      "images": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "included": {
       "description": "Paid activities only.",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "instructions_html": {
       "description": "Paid activities only.",
       "type": "string"
      },
      "itinerary": {
       "description": "Free tours only: point titles.",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "live_guide_languages": {
       "description": "Paid activities only.",
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "meeting_point_html": {
       "description": "Paid activities only.",
       "type": "string"
      },
      "meeting_point_latitude": {
       "description": "Free tours only.",
       "type": "number"
      },
      "meeting_point_longitude": {
       "description": "Free tours only.",
       "type": "number"
      },
      "meeting_point_url": {
       "description": "Google Maps link to the meeting point, returned for both paid activities and free tours. Built from the meeting point coordinates, so it is absent when they are unknown.",
       "type": "string"
      },
      "name": {
       "type": "string"
      },
      "other_info": {
       "description": "Paid activities only.",
       "type": "array"
      },
      "place": {
       "properties": {
        "country": {
         "description": "Free tours only.",
         "type": [
          "string",
          "null"
         ]
        },
        "id": {
         "type": [
          "integer",
          "null"
         ]
        },
        "name": {
         "type": [
          "string",
          "null"
         ]
        },
        "slug": {
         "type": [
          "string",
          "null"
         ]
        },
        "timezone": {
         "description": "Paid activities only.",
         "type": [
          "string",
          "null"
         ]
        }
       },
       "type": "object"
      },
      "pricing_from": {
       "description": "Paid activities only.",
       "properties": {
        "currency": {
         "type": [
          "string",
          "null"
         ]
        },
        "retail": {
         "type": "number"
        }
       },
       "type": "object"
      },
      "product_id": {
       "type": "integer"
      },
      "reviews": {
       "properties": {
        "count": {
         "type": [
          "integer",
          "null"
         ]
        },
        "rating": {
         "type": [
          "number",
          "string",
          "null"
         ]
        }
       },
       "type": "object"
      },
      "slug": {
       "type": "string"
      },
      "source": {
       "type": "string"
      },
      "special_notes_html": {
       "description": "Paid activities only.",
       "type": "string"
      },
      "subtitle_html": {
       "description": "Paid activities only.",
       "type": "string"
      },
      "type": {
       "enum": [
        "product",
        "free_tour"
       ],
       "type": "string"
      },
      "url": {
       "description": "Tour page URL. Share it with the traveler exactly as returned \u2014 never rewrite the domain or drop query parameters, they carry required attribution.",
       "type": "string"
      },
      "when_starting_time": {
       "description": "Paid activities only.",
       "type": "string"
      },
      "where": {
       "description": "Paid activities only: meeting point address and coordinates.",
       "properties": {
        "address_html": {
         "type": [
          "string",
          "null"
         ]
        },
        "latitude": {
         "type": [
          "number",
          "null"
         ]
        },
        "longitude": {
         "type": [
          "number",
          "null"
         ]
        },
        "name": {
         "type": [
          "string",
          "null"
         ]
        }
       },
       "type": "object"
      }
     },
     "required": [
      "product_id",
      "type"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "results"
  ],
  "type": "object"
 }
}
