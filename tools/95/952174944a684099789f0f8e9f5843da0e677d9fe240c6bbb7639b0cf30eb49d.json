{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Find tools or hosted agents for a task. Filter by type, category or connection protocol; returns documentation, access requirements and limitations. Unfiltered topic queries can also return public research in findings, separately from provider results; inspect tested revisions and limits. No external tool execution.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "category": {
    "enum": [
     "web-search",
     "data-extraction",
     "browser-automation",
     "code-execution",
     "developer-tools",
     "coding-agent",
     "agent-orchestration"
    ],
    "type": "string"
   },
   "kind": {
    "enum": [
     "tool",
     "agent",
     "framework"
    ],
    "type": "string"
   },
   "limit": {
    "default": 5,
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "protocol": {
    "maxLength": 40,
    "type": "string"
   },
   "query": {
    "maxLength": 300,
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_capabilities"
}
