{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Remove Codes From Profile"
 },
 "description": "Remove promo codes from a discount profile \u2014 'detach' (default) keeps each code standalone; 'delete' removes unused codes but disables any with redemptions. Requires event_id + profile_id + code_ids; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "action": {
    "enum": [
     "detach",
     "delete"
    ],
    "type": "string"
   },
   "code_ids": {
    "items": {
     "maxLength": 128,
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 500,
    "minItems": 1,
    "type": "array"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "profile_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "profile_id",
   "code_ids"
  ],
  "type": "object"
 },
 "name": "remove_codes_from_profile",
 "title": "Remove Codes From Profile"
}
