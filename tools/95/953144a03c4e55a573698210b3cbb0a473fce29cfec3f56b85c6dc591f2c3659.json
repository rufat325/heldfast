{
 "description": "Create a payment in Asaas (Pix, boleto, or credit card). Pass `installments` (>=2) with `billingType: CREDIT_CARD` to split the value into equal monthly installments.",
 "inputSchema": {
  "properties": {
   "billingType": {
    "description": "Payment method",
    "enum": [
     "BOLETO",
     "CREDIT_CARD",
     "PIX"
    ],
    "type": "string"
   },
   "customer": {
    "description": "Customer ID (cus_xxx)",
    "type": "string"
   },
   "description": {
    "description": "Payment description",
    "type": "string"
   },
   "dueDate": {
    "description": "Due date of the first installment (YYYY-MM-DD)",
    "type": "string"
   },
   "installments": {
    "description": "Number of equal installments (>=2). Only valid with billingType: CREDIT_CARD.",
    "minimum": 2,
    "type": "number"
   },
   "value": {
    "description": "Total amount in BRL",
    "type": "number"
   }
  },
  "required": [
   "customer",
   "billingType",
   "value",
   "dueDate"
  ],
  "type": "object"
 },
 "name": "create_payment"
}
