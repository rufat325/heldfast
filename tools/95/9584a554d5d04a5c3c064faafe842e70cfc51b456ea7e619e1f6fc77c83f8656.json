{
 "annotations": {
  "openWorldHint": true,
  "title": "Create project"
 },
 "description": "Create a project for an expired domain. Starts an async URL-fetch job from the Wayback Machine and returns a job_id.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "domain": {
    "description": "Expired domain to analyze (e.g. example.fr)",
    "type": "string"
   },
   "name": {
    "description": "Project name (defaults to domain)",
    "type": "string"
   }
  },
  "required": [
   "domain"
  ],
  "type": "object"
 },
 "name": "create_project"
}
