{
 "description": "Create a new service request. Needs serviceId (or slug) and answers matching the service formConfig.",
 "inputSchema": {
  "properties": {
   "answers": {
    "description": "Form answers keyed by field name",
    "type": "object"
   },
   "customTitle": {
    "description": "Optional custom title for ad-hoc requests",
    "type": "string"
   },
   "serviceId": {
    "description": "Service id or slug",
    "type": "string"
   }
  },
  "required": [
   "serviceId"
  ],
  "type": "object"
 },
 "name": "create_service_request"
}
