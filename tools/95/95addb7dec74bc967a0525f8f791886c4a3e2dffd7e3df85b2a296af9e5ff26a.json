{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Rank routable global supply by expected outcome using trust, success rate, latency and total buyer price.",
 "inputSchema": {
  "properties": {
   "budget_cents": {
    "minimum": 0,
    "type": "integer"
   },
   "currency": {
    "type": "string"
   },
   "goal": {
    "type": "string"
   },
   "limit": {
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "goal"
  ],
  "type": "object"
 },
 "name": "route_global_market",
 "title": "Route across global supply"
}
