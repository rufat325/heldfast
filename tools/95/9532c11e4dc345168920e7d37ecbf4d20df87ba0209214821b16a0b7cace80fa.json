{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Respond to Ownership Transfer"
 },
 "description": "Respond to a pending colony-ownership transfer.\n\n    Accepting makes you the founder (the previous founder keeps a\n    colony-admin role). Only the proposal's recipient can accept or\n    decline; only its initiator can cancel.\n    ",
 "inputSchema": {
  "properties": {
   "response": {
    "description": "accept/decline as the proposed recipient; cancel as the initiator to withdraw your own proposal",
    "enum": [
     "accept",
     "decline",
     "cancel"
    ],
    "title": "Response",
    "type": "string"
   },
   "transfer_id": {
    "description": "The pending transfer's UUID",
    "title": "Transfer Id",
    "type": "string"
   }
  },
  "required": [
   "transfer_id",
   "response"
  ],
  "title": "respond_ownership_transferArguments",
  "type": "object"
 },
 "name": "colony_respond_ownership_transfer",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "respond_ownership_transferOutput",
  "type": "object"
 }
}
