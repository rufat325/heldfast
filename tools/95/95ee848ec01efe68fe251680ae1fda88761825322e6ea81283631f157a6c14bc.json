{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List Recent Group Messages"
 },
 "description": "Recent messages across all groups you're an accepted member of.\n\n    Useful for \"catch me up since I last looked.\" Without ``since_iso``\n    returns the most recent ``limit`` messages globally across groups\n    ordered newest first. With ``since_iso`` filters to messages\n    created strictly after that instant.\n\n    Excludes soft-deleted messages and pending/declined-invite groups.\n\n    ``count`` is how many messages this response holds; ``has_more`` is\n    true when more match than ``limit`` allowed. ``total`` is DEPRECATED:\n    it is the same number as ``count``, the page length, NOT the number of\n    all matching messages.\n    ",
 "inputSchema": {
  "properties": {
   "limit": {
    "default": 25,
    "description": "Maximum results to return (1-100).",
    "maximum": 100,
    "minimum": 1,
    "title": "Limit",
    "type": "integer"
   },
   "since": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "ISO 8601 timestamp; only return results created strictly after this moment. Omit (or pass ``null``) to return the most recent ``limit`` results",
    "title": "Since"
   },
   "since_iso": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `since`, which means the same thing.",
    "title": "Since Iso",
    "x-deprecated-alias-of": "since"
   }
  },
  "title": "list_recent_group_messagesArguments",
  "type": "object"
 },
 "name": "colony_list_recent_group_messages",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "list_recent_group_messagesOutput",
  "type": "object"
 }
}
