{
 "description": "Compare your site/org to industry benchmarks (citation rates, Geo score, share-of-voice) across AI search platforms.",
 "inputSchema": {
  "properties": {
   "include_org": {
    "default": true,
    "description": "Include org overlay fields (orgRate, orgP50, articleCount, orgPct). false = industry-only.",
    "type": "boolean"
   },
   "site_id": {
    "description": "Site UUID (from list_sites). Omit for org-level aggregate; cross-org IDs are silently dropped.",
    "format": "uuid",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_industry_benchmark"
}
