{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List contacts (people) in Close. Returns a `data` array of contacts with id, lead_id, name, title, emails, and phones, plus `has_more` / `total_results`. Optionally filter to one lead with `lead_id`. Page with `_limit` / `_skip`.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-close-crm-api-key"
   },
   {
    "_apiKey": "your-close-crm-api-key",
    "lead_id": "lead_abc123def456"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Close API key",
    "type": "string"
   },
   "_limit": {
    "description": "Results per page (default 100, max 200)",
    "type": "number"
   },
   "_skip": {
    "description": "Number of results to skip for pagination (default 0)",
    "type": "number"
   },
   "lead_id": {
    "description": "Optional lead ID to filter contacts to a single lead",
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "close_list_contacts",
 "title": "Close List Contacts",
 "version": "v1.77751ff6"
}
