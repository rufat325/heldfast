{
 "description": "Extend an image beyond its original boundaries with AI-generated content (canvas expansion). Different from recraft_generate_background, which only fills masked areas within the existing canvas. Specify a target size, per-side expand amounts, and/or a zoom-out percentage. V3/V3 Vector only.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "controls": {
    "description": "Generation controls",
    "properties": {
     "artistic_level": {
      "description": "Artistic level 0-5 (V3 only)",
      "maximum": 5,
      "minimum": 0,
      "type": "integer"
     },
     "background_color": {
      "description": "Background color RGB (0-255)",
      "properties": {
       "b": {
        "maximum": 255,
        "minimum": 0,
        "type": "integer"
       },
       "g": {
        "maximum": 255,
        "minimum": 0,
        "type": "integer"
       },
       "r": {
        "maximum": 255,
        "minimum": 0,
        "type": "integer"
       }
      },
      "required": [
       "r",
       "g",
       "b"
      ],
      "type": "object"
     },
     "colors": {
      "description": "Preferred color palette, e.g. [[255,0,0],[0,255,0]]",
      "items": {
       "items": {
        "maximum": 255,
        "minimum": 0,
        "type": "integer"
       },
       "type": "array"
      },
      "type": "array"
     },
     "no_text": {
      "description": "Exclude text layouts (V3 only)",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "expand_bottom": {
    "description": "Pixels to add to the bottom. Cannot combine with size.",
    "maximum": 4096,
    "minimum": 0,
    "type": "integer"
   },
   "expand_left": {
    "description": "Pixels to add to the left. Cannot combine with size.",
    "maximum": 4096,
    "minimum": 0,
    "type": "integer"
   },
   "expand_right": {
    "description": "Pixels to add to the right. Cannot combine with size.",
    "maximum": 4096,
    "minimum": 0,
    "type": "integer"
   },
   "expand_top": {
    "description": "Pixels to add to the top. Cannot combine with size.",
    "maximum": 4096,
    "minimum": 0,
    "type": "integer"
   },
   "file_path": {
    "description": "Local path to source image",
    "type": "string"
   },
   "model": {
    "default": "recraftv3",
    "description": "Model (V3 only): recraftv3, recraftv3_vector",
    "type": "string"
   },
   "n": {
    "default": 1,
    "description": "Number of images to generate (1-6)",
    "maximum": 6,
    "minimum": 1,
    "type": "integer"
   },
   "negative_prompt": {
    "description": "What to exclude",
    "type": "string"
   },
   "prompt": {
    "description": "Description of the content to generate in the extended area",
    "type": "string"
   },
   "response_format": {
    "default": "url",
    "description": "Response format",
    "enum": [
     "url",
     "b64_json"
    ],
    "type": "string"
   },
   "size": {
    "description": "Target size for the resulting image \u2014 the source image is placed inside it. Cannot combine with expand_*.",
    "type": "string"
   },
   "style": {
    "description": "Broad style family. One of: any, digital_illustration, icon, realistic_image, vector_illustration, logo_raster. A specific look (e.g. pixel_art, kawaii, b_and_w) belongs in `substyle`, but passing one here is accepted and routed to the right field.",
    "type": "string"
   },
   "style_id": {
    "description": "Custom style UUID",
    "type": "string"
   },
   "substyle": {
    "description": "Specific look within the family \u2014 104 values, validated strictly. Common ones: 2d_art_poster, 3d, 80s, glow, grain, hand_drawn, infantile_sketch, kawaii, pixel_art, psychedelic, seamless, voxel. An invalid value returns the full list.",
    "type": "string"
   },
   "text_layout": {
    "description": "Text placement on image",
    "items": {
     "properties": {
      "bbox": {
       "description": "4-point polygon with relative coordinates [0-1]",
       "items": {
        "items": {
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "type": "array"
       },
       "maxItems": 4,
       "minItems": 4,
       "type": "array"
      },
      "text": {
       "description": "Text to render (single word)",
       "type": "string"
      }
     },
     "required": [
      "text",
      "bbox"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "zoom_out_percentage": {
    "description": "Scales the source image down by this percentage before outpainting, revealing more surrounding context [0-100). Can combine with size or expand_*.",
    "maximum": 100,
    "minimum": 0,
    "type": "number"
   }
  },
  "required": [
   "file_path",
   "prompt"
  ],
  "type": "object"
 },
 "name": "recraft_outpaint"
}
