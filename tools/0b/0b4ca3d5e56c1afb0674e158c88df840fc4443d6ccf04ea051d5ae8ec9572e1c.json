{
 "description": "Build and test regex patterns. POST /build creates a regex from a pattern string or components. POST /test validates a pattern against test strings. Useful for generating and debugging regular expressions.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "flags": {
    "description": "Regex flags (g, i, m, s, u, y)",
    "type": "string"
   },
   "mode": {
    "description": "'build' to create a regex, 'test' to validate against strings",
    "enum": [
     "build",
     "test"
    ],
    "type": "string"
   },
   "pattern": {
    "description": "Regex pattern string",
    "maxLength": 500,
    "type": "string"
   },
   "testStrings": {
    "description": "Test strings (required for 'test' mode)",
    "items": {
     "maxLength": 1000,
     "type": "string"
    },
    "maxItems": 20,
    "type": "array"
   }
  },
  "required": [
   "mode",
   "pattern"
  ],
  "type": "object"
 },
 "name": "regex_builder"
}
