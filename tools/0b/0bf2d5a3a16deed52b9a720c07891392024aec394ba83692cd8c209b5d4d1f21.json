{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "readOnlyHint": false
 },
 "description": "Resolve a criterion in review. Transitions in_review \u2192 passed | failed. NOTE: agents (env_token:agent) cannot self-resolve their own work \u2014 this tool requires a CI-scoped token (env_token:ci) or a session user.",
 "inputSchema": {
  "properties": {
   "criterionId": {
    "type": "string"
   },
   "notes": {
    "type": "string"
   },
   "outcome": {
    "enum": [
     "passed",
     "failed"
    ],
    "type": "string"
   },
   "testRunUrl": {
    "type": "string"
   }
  },
  "required": [
   "criterionId",
   "outcome"
  ],
  "type": "object"
 },
 "name": "vibemap_resolve_review"
}
