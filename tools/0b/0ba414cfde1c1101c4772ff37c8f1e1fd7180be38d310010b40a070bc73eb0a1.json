{
 "description": "Connect to a messaging platform. Idempotent \u2014 safe to call if already connected. For WhatsApp: if QR scan is needed, returns the raw pairing URI string.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "platform": {
    "description": "Platform to connect",
    "enum": [
     "telegram",
     "discord",
     "slack",
     "whatsapp"
    ],
    "type": "string"
   }
  },
  "required": [
   "platform"
  ],
  "type": "object"
 },
 "name": "channel_connect"
}
