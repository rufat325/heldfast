{
 "description": "List all API keys stored in ProphetKey. Returns safe_keys (not real keys) for use with the local proxy.",
 "inputSchema": {
  "properties": {
   "provider": {
    "description": "Filter by provider name (optional)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "prophetkey_list_keys"
}
