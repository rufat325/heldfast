{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Valida publicamente una constancia por su folio: confirma que la verificacion existio, cuando y con que resultado. No requiere llave, no tiene costo.",
 "inputSchema": {
  "properties": {
   "folio": {
    "title": "Folio",
    "type": "string"
   }
  },
  "required": [
   "folio"
  ],
  "title": "validar_constanciaArguments",
  "type": "object"
 },
 "name": "validar_constancia",
 "title": "Validar una constancia por folio"
}
