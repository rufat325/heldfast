{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "title": "Feedback"
 },
 "description": "Rate an engram's usefulness \u2014 trains injection relevance over time. Supports single or batch mode.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Engram ID (single mode)",
    "type": "string"
   },
   "scope": {
    "description": "Store scope to target directly, e.g. \"primary\" for the local store or a remote scope like \"group:plur/plur-ai/engineering\". Required when the same engram ID exists in multiple stores (#850).",
    "type": "string"
   },
   "signal": {
    "description": "Feedback signal (single mode)",
    "enum": [
     "positive",
     "negative",
     "neutral"
    ],
    "type": "string"
   },
   "signals": {
    "description": "Batch feedback signals",
    "items": {
     "properties": {
      "id": {
       "description": "Engram ID",
       "type": "string"
      },
      "scope": {
       "description": "Store scope to target directly (optional, same semantics as top-level scope).",
       "type": "string"
      },
      "signal": {
       "enum": [
        "positive",
        "negative",
        "neutral"
       ],
       "type": "string"
      }
     },
     "required": [
      "id",
      "signal"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "name": "plur_feedback"
}
