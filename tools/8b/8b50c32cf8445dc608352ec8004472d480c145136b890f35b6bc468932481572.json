{
 "description": "Upgrade the engine image used by a volume. `image` is not validated against a known-good list \u2014 a bad image can make the volume inoperable. Destructive. Do not call this unless the user has explicitly authorized it in this conversation \u2014 never proactively, speculatively, or as a side effect of another task. Longhorn docs: https://longhorn.io/docs/1.12.1/deploy/upgrade/",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "image": {
    "type": "string"
   },
   "name": {
    "type": "string"
   }
  },
  "required": [
   "name",
   "image"
  ],
  "type": "object"
 },
 "name": "volume_engine_upgrade",
 "title": "Upgrade volume engine image"
}
