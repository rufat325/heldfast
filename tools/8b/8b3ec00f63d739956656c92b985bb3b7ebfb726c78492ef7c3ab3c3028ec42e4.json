{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "USE WHEN someone wants a ready-made onchain agent package to download or fork rather than assemble one. Searches Sato Hub's published agent packages \u2014 each a public view of one sealed, immutable version: its task, inputs/outputs, required permissions, model support (\"configured, not evaluated\"), license, and evidence freshness for that exact digest.\n\nReturns (json): { listings: [{ slug, title, task, version: { digest }, recipe, license, fork_permitted, evidence_freshness, integrity, acquisition_modes, sato_url, ... }], next_cursor }.\n\nRULE ENFORCED: evidence counts only rows recorded against the package's exact digest \u2014 \"not_checked\" means none exist. A result is scoped to its test; it is not a safety, security or returns claim. No signed download URL is ever returned; cite sato_url. Read-only.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "cursor": {
    "description": "The next_cursor from a previous page.",
    "maxLength": 200,
    "type": "string"
   },
   "license": {
    "description": "Only packages under this SPDX license id, e.g. 'MIT'.",
    "maxLength": 64,
    "type": "string"
   },
   "limit": {
    "default": 20,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Free text over a published package's title, task and description.",
    "maxLength": 100,
    "type": "string"
   },
   "recipe": {
    "description": "Only packages built from this recipe id, e.g. 'treasury-balance-monitor'.",
    "maxLength": 64,
    "pattern": "^[a-z0-9-]+$",
    "type": "string"
   },
   "response_format": {
    "default": "markdown",
    "description": "Output format: 'markdown' (human-readable, default) or 'json' (machine-readable).",
    "enum": [
     "markdown",
     "json"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "onchain_agent_search_listings",
 "title": "Find a published agent package"
}
