{
 "description": "Advance the current step for a request to the next step (completes current step and moves currentStepId). Validates phase transitions.",
 "inputSchema": {
  "properties": {
   "requestId": {
    "type": "string"
   },
   "stepId": {
    "description": "Optional: specific step id to complete (defaults to current step)",
    "type": "string"
   }
  },
  "required": [
   "requestId"
  ],
  "type": "object"
 },
 "name": "advance_step"
}
