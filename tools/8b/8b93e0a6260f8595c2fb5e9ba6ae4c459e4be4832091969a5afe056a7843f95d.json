{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "List Atisbo documentation pages with their URL, title and description. Omit limit for the whole corpus; pass limit to cap the response.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Maximum number of pages to return (1-50). Omitted, non-integer, or out-of-range values return the WHOLE corpus \u2014 this tool never pages, it only trims.",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_docs"
}
