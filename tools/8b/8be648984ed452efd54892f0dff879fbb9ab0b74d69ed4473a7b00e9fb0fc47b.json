{
 "annotations": {
  "readOnlyHint": true,
  "title": "Get provider schema"
 },
 "description": "Every JSON Schema a provider publishes \u2014 the data shapes their API works in, in one list, without walking their artifacts. Free.",
 "inputSchema": {
  "properties": {
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "limit": {
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "page": {
    "type": "integer"
   },
   "q": {
    "description": "Substring match on schema name or slug.",
    "type": "string"
   },
   "slug": {
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "get_provider_schema"
}
