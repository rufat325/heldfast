{
 "description": "Find pages connected to a given page via wikilinks, shared tags, or shared sources. Returns a ranked list with the connection reason.",
 "inputSchema": {
  "properties": {
   "depth": {
    "description": "Hop depth (1-3; default 2)",
    "type": "number"
   },
   "slug": {
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "find_related"
}
