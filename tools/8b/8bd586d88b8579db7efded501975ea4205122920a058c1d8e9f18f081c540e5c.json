{
 "description": "Middleware hook to run before any agent calls an LLM. Returns enhanced_prompt, sanitized_message, and C3/security status. Use this when GSEP-MCP protects an existing external agent rather than owning the LLM call.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "batch_size": {
    "description": "Optional batch size hint for batch workflows",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "genome_id": {
    "description": "Unique identifier for this agent genome",
    "type": "string"
   },
   "message": {
    "description": "User message or external content before it reaches the LLM",
    "type": "string"
   },
   "task_type": {
    "description": "Task type hint, e.g. support, coding, research",
    "type": "string"
   },
   "user_id": {
    "description": "User identifier for personalization and audit context",
    "type": "string"
   }
  },
  "required": [
   "genome_id",
   "message"
  ],
  "type": "object"
 },
 "name": "gsep_before_llm"
}
