{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": true
 },
 "description": "Get one service by id, including its type, status and expiry. Use ws_service_list first to find the id.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "serviceId": {
    "description": "Service id, as returned by ws_service_list.",
    "minLength": 1,
    "type": "string"
   },
   "userId": {
    "default": "self",
    "description": "Account id, or the literal \"self\" for the authenticated account. Defaults to \"self\".",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "serviceId"
  ],
  "type": "object"
 },
 "name": "ws_service_get",
 "title": "Get service"
}
