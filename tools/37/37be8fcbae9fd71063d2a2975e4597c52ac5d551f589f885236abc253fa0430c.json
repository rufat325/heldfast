{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false,
  "title": "delete_record"
 },
 "description": "Delete (soft delete) a record.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "record_id": {
    "description": "The UUID of the record to delete.",
    "type": "string"
   }
  },
  "required": [
   "record_id"
  ],
  "type": "object"
 },
 "name": "delete_record"
}
