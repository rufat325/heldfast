{
 "description": "List invoices, newest first. Cursor pagination via starting_after / ending_before.",
 "inputSchema": {
  "properties": {
   "ending_before": {
    "description": "Cursor: return results before this invoice id",
    "type": "string"
   },
   "limit": {
    "description": "Max results per page (default 25, max 100)",
    "type": "number"
   },
   "order": {
    "description": "Sort order by created_at. Defaults to desc.",
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   },
   "starting_after": {
    "description": "Cursor: return results after this invoice id",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_invoices"
}
