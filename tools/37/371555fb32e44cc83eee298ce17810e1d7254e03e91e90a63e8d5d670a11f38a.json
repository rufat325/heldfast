{
 "annotations": {
  "readOnlyHint": true,
  "title": "List Audit Logs"
 },
 "description": "Query the immutable audit trail. Logs are protected by a SHA3-256 hash chain + ML-DSA-65 signatures - tamper-evident and exportable for compliance evidence. Filter by action, date range, or actor.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Filter by action (e.g. 'vault.seal', 'encryption.encrypt', 'proxy.outbound')",
    "type": "string"
   },
   "end_date": {
    "description": "ISO 8601 end date (e.g. '2026-12-31')",
    "type": "string"
   },
   "limit": {
    "description": "Number of entries (default: 50, max: 500)",
    "maximum": 500,
    "minimum": 1,
    "type": "number"
   },
   "start_date": {
    "description": "ISO 8601 start date (e.g. '2026-01-01')",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "list_audit_logs"
}
