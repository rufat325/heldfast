{
 "description": "Rotate (replace) a secret in Vault. For security, this operation is not available through the read-only Keymaster proxy.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "key_name": {
    "default": "api_key",
    "description": "Key field name (default: 'api_key')",
    "type": "string"
   },
   "service": {
    "description": "Service name (e.g. 'openai', 'stripe')",
    "type": "string"
   }
  },
  "required": [
   "service"
  ],
  "type": "object"
 },
 "name": "rotate_secret"
}
