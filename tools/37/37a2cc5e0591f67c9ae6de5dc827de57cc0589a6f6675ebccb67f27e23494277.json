{
 "description": "Surface conflicting beliefs, preferences, or facts. Supports resolve action to mark contradictions as resolved. Returns pairs of contradicting statements with severity and linked memory IDs.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Action (default: list)",
    "enum": [
     "list",
     "resolve"
    ],
    "type": "string"
   },
   "contradiction_id": {
    "description": "Contradiction ID to resolve (required for resolve action)",
    "type": "string"
   },
   "resolution": {
    "description": "Resolution note (required for resolve action)",
    "type": "string"
   },
   "severity_min": {
    "description": "Minimum severity threshold (0-1)",
    "type": "number"
   },
   "status": {
    "description": "Filter by status (default: open)",
    "enum": [
     "open",
     "resolved",
     "all"
    ],
    "type": "string"
   },
   "topic": {
    "description": "Filter contradictions by topic (semantic relevance)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "velixar_contradictions"
}
