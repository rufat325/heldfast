{
 "description": "US EIA retail electricity series for one state and sector: price, revenue and sales by period. $0.005 per call via x402 (USDC on Base); response includes a provenance chain and an Ed25519 attestation.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "end": {
    "maxLength": 10,
    "type": "string"
   },
   "entity": {
    "maxLength": 4,
    "type": "string"
   },
   "payment": {
    "description": "Encoded x402 X-PAYMENT header value. Omit to receive the payment requirements (free quote).",
    "type": "string"
   },
   "start": {
    "maxLength": 10,
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "electricity_retail_series"
}
