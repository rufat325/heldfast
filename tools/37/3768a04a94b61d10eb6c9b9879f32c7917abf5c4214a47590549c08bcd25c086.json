{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Extract every string of copy in the file (or in one subtree via `scope`), in document order: guid, layer name, the characters, the page, and the base typography (font, size, line-height, colour). With includeRuns:true each node also carries its styled runs \u2014 the mixed-format spans Figma stores per UTF-16 code unit \u2014 with only the fields each run overrides. Use this for copy audits and translation passes rather than walking the tree.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "cursor": {
    "description": "Opaque nextCursor from a previous truncated response; resumes where it stopped.",
    "type": "string"
   },
   "file": {
    "description": "Path to the .fig / .figma file (absolute, or relative to the server CWD).",
    "type": "string"
   },
   "includePath": {
    "description": "Include the ancestor breadcrumb per node (default false; costs tokens).",
    "type": "boolean"
   },
   "includeRuns": {
    "description": "Include per-run style overrides (default false; costs many more tokens).",
    "type": "boolean"
   },
   "includeStyle": {
    "description": "Include font / size / line-height / colour per node (default true).",
    "type": "boolean"
   },
   "scope": {
    "description": "Only collect text inside this guid subtree. A Figma node-id or link works too.",
    "type": "string"
   }
  },
  "required": [
   "file"
  ],
  "type": "object"
 },
 "name": "fig_text",
 "title": "Text inventory"
}
