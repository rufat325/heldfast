{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Diagnose unexpected outcome"
 },
 "description": "Diagnose why a ruleset produced an unexpected outcome for specific test inputs. Use during rule authoring when a test fails \u2014 returns the diagnosis, criteria with DSL metadata (waivable, review_required), and a targeted hint for fixing the rule.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "expected_outcome": {
    "description": "The outcome you expected from this input",
    "enum": [
     "eligible",
     "not_eligible",
     "undetermined"
    ],
    "type": "string"
   },
   "field_values": {
    "additionalProperties": {},
    "description": "The test input values that produced the unexpected outcome",
    "type": "object"
   },
   "ruleset_id": {
    "description": "The ID of the rule ruleset to diagnose",
    "type": "string"
   },
   "test_name": {
    "description": "Name of the failing test case (included in the diagnosis for context)",
    "type": "string"
   }
  },
  "required": [
   "ruleset_id",
   "field_values",
   "expected_outcome"
  ],
  "type": "object"
 },
 "name": "aethis_explain_failure"
}
