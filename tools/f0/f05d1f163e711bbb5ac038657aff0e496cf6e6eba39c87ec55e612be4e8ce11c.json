{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Generate realistic Japanese test data for prototypes and development. Returns names (kanji + katakana furigana + romaji), addresses (real postal codes and prefectures), phone numbers (correct 3-field format), company names, and dates (both Gregorian and Japanese era format like \u5e73\u62104\u5e743\u670815\u65e5).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "age_range": {
    "description": "Age range like \"20-40\" for date_of_birth generation",
    "title": "Age Range",
    "type": "string"
   },
   "count": {
    "default": 5,
    "description": "Number of records to generate",
    "maximum": 50,
    "minimum": 1,
    "title": "Count",
    "type": "number"
   },
   "fields": {
    "description": "Data fields to include",
    "items": {
     "enum": [
      "name",
      "email",
      "phone",
      "address",
      "company",
      "date_of_birth"
     ],
     "type": "string"
    },
    "title": "Fields",
    "type": "array"
   },
   "gender": {
    "default": "mixed",
    "description": "Gender for name generation",
    "enum": [
     "mixed",
     "male",
     "female"
    ],
    "title": "Gender",
    "type": "string"
   }
  },
  "required": [
   "fields"
  ],
  "type": "object"
 },
 "name": "generate_jp_placeholder",
 "title": "Generate JP Placeholder Data"
}
