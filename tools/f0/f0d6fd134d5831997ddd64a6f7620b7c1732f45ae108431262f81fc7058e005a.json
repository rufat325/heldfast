{
 "description": "Read the HTML source of an old version WITHOUT publishing it. Use before qhs_restore, and whenever the user asks 'what was in version N', 'show me the old one'. Restoring is reversible, but the exposure is not \u2014 an old version may contain something the user deliberately removed, and restoring it makes it visible to everyone holding the share link immediately.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "editToken": {
    "type": "string"
   },
   "slug": {
    "pattern": "^[a-z0-9]{8,16}$",
    "type": "string"
   },
   "version": {
    "exclusiveMinimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "slug",
   "version"
  ],
  "type": "object"
 },
 "name": "qhs_preview_version"
}
