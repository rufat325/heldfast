{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Build and start a test run from a suite, a test plan, or an explicit list of case IDs, in one step. Use it to launch a regression cycle without first querying for cases and then creating a run around them \u2014 give it the source and it resolves the cases itself. For a run you assemble by hand, use qase_run_upsert and pass the case IDs. For a pipeline that has already finished and just needs its results filed, use qase_ci_report instead: this tool opens a run, it does not close one. Cost: two API calls behind one tool call \u2014 resolving the source, then creating the run \u2014 roughly 1s, growing with the number of cases the source resolves to.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "code": {
    "description": "Project code (2-10 uppercase letters, numbers, or underscores)",
    "maxLength": 10,
    "minLength": 2,
    "pattern": "^[A-Z0-9_]+$",
    "type": "string"
   },
   "description": {
    "description": "What this regression run covers",
    "type": "string"
   },
   "environment_id": {
    "description": "ID of the environment the run executes against",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "include_cases": {
    "description": "Explicit case IDs to include",
    "items": {
     "exclusiveMinimum": 0,
     "type": "integer"
    },
    "type": "array"
   },
   "milestone_id": {
    "description": "ID of the milestone the run belongs to",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "plan_id": {
    "description": "Create run from an existing test plan",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "suite_ids": {
    "description": "Include cases from these suites",
    "items": {
     "exclusiveMinimum": 0,
     "type": "integer"
    },
    "type": "array"
   },
   "title": {
    "description": "Run title",
    "maxLength": 255,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "code",
   "title"
  ],
  "type": "object"
 },
 "name": "qase_regression_run",
 "outputSchema": {
  "properties": {
   "cases_added": {
    "description": "Number of cases added to the run",
    "type": "integer"
   },
   "run": {
    "description": "Full run entity",
    "type": "object"
   },
   "run_id": {
    "description": "Created run ID",
    "type": "integer"
   }
  },
  "required": [
   "run_id",
   "cases_added"
  ],
  "type": "object"
 },
 "title": "Build regression run"
}
