{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Rank the products two countries actually trade, largest value first \u2014 \"what does the US import most from China\". Returns HS categories with trade value and net weight. Defaults to HS 2-digit chapters (~97 categories, e.g. 85 Electrical machinery); pass hs_level 4 or 6 for finer product detail.",
 "inputSchema": {
  "examples": [
   {
    "flow": "M",
    "limit": 15,
    "partner_code": "156",
    "reporter_code": "842",
    "year": "2024"
   },
   {
    "flow": "X",
    "partner_code": "842",
    "reporter_code": "392",
    "year": "2023"
   }
  ],
  "properties": {
   "flow": {
    "description": "Trade flow, REQUIRED because it decides what the answer MEANS: \"X\" for exports (what a country SELLS / ships abroad) or \"M\" for imports (what it BUYS / receives). Deliberately not defaulted \u2014 guessing returns imports for an export question, which reads as a confident wrong answer instead of an error.",
    "type": "string"
   },
   "frequency": {
    "description": "Data frequency: \"annual\" (default) or \"monthly\". Monthly coverage is thinner \u2014 not every reporter files monthly, and recent months lag. When \"monthly\", pass `year` as YYYYMM.",
    "type": "string"
   },
   "hs_level": {
    "description": "HS aggregation level: 2 for chapters (default, ~97 broad categories), 4 for headings, 6 for subheadings. Higher numbers give finer product detail and many more rows.",
    "type": "number"
   },
   "limit": {
    "description": "Number of top commodities to return (default 20)",
    "type": "number"
   },
   "partner_code": {
    "description": "Partner country name or UN code (0=World). Optional \u2014 defaults to World. Names resolved automatically.",
    "type": "string"
   },
   "reporter_code": {
    "description": "Country name (e.g. \"USA\", \"China\") or UN numeric code (842=US). Names resolved automatically.",
    "type": "string"
   },
   "year": {
    "description": "Trade period. For frequency=\"annual\" (default): a year, e.g. \"2024\". For frequency=\"monthly\": year+month as YYYYMM, e.g. \"202401\".",
    "type": "string"
   }
  },
  "required": [
   "reporter_code",
   "year",
   "flow"
  ],
  "type": "object"
 },
 "name": "comtrade_top_commodities",
 "outputSchema": {
  "properties": {
   "flow": {
    "description": "Trade flow type (Imports or Exports)",
    "type": "string"
   },
   "partner": {
    "description": "Partner country name",
    "type": "string"
   },
   "reporter": {
    "description": "Reporting country name",
    "type": "string"
   },
   "top_commodities": {
    "items": {
     "properties": {
      "commodity": {
       "description": "Commodity description or code",
       "type": "string"
      },
      "hs_code": {
       "description": "HS commodity code",
       "type": "string"
      },
      "net_weight_kg": {
       "description": "Net weight in kilograms",
       "type": "number"
      },
      "rank": {
       "description": "Commodity rank by trade value",
       "type": "number"
      },
      "trade_value_usd": {
       "description": "Trade value in USD",
       "type": "number"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "total_commodities": {
    "description": "Total number of top commodities returned",
    "type": "number"
   },
   "year": {
    "description": "Trade year queried",
    "type": "string"
   }
  },
  "required": [
   "reporter",
   "partner",
   "year",
   "flow",
   "total_commodities",
   "top_commodities"
  ],
  "type": "object"
 },
 "title": "Comtrade Top Commodities",
 "version": "v1.a0593cac"
}
