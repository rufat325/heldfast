{
 "description": "Test SSH connectivity to a remote host. Verifies that the system SSH binary can reach the host using the current agent/config.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "extraArgs": {
    "description": "Extra SSH arguments",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "host": {
    "description": "SSH server hostname or IP address",
    "type": "string"
   },
   "port": {
    "default": 22,
    "description": "SSH server port (default: 22)",
    "type": "number"
   },
   "username": {
    "description": "SSH username. Defaults to the current OS user.",
    "type": "string"
   }
  },
  "required": [
   "host"
  ],
  "type": "object"
 },
 "name": "ssh_test"
}
