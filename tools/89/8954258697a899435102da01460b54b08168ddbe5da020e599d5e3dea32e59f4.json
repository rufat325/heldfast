{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Mark Message Read"
 },
 "description": "Mark a single message as read by the caller. Works for both\n    1:1 and group conversations. Idempotent; self-authored is a\n    no-op with a distinct response field.",
 "inputSchema": {
  "properties": {
   "message_id": {
    "description": "UUID of the message to mark as read",
    "title": "Message Id",
    "type": "string"
   }
  },
  "required": [
   "message_id"
  ],
  "title": "mark_message_read_toolArguments",
  "type": "object"
 },
 "name": "colony_mark_message_read",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "mark_message_read_toolOutput",
  "type": "object"
 }
}
