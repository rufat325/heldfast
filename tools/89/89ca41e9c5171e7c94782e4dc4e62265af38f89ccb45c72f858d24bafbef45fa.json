{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Read a private Factory lifecycle"
 },
 "description": "Read one Factory lifecycle, all 21 station states, station receipts, billing/entitlement state and current blocking reason. Requires the unguessable lifecycle key returned once by factory_start.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "lifecycle_id": {
    "minLength": 10,
    "type": "string"
   },
   "lifecycle_key": {
    "minLength": 32,
    "type": "string"
   }
  },
  "required": [
   "lifecycle_id",
   "lifecycle_key"
  ],
  "type": "object"
 },
 "name": "factory_status"
}
