{
 "_meta": {
  "securitySchemes": [
   {
    "scopes": [
     "email"
    ],
    "type": "oauth2"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "What THIS key is and may do: tier, requests left this minute, calls left today (both including this call), whether fit_resume (and engine=ranked on the data API) answers on it, and whether the apply tools would \u2014 with any blocker named: account link, Agent plan or live pass, mandate, r\u00e9sum\u00e9 on file. On an Agent Pass: when the clock ends and how many applications are left (a pass starts at the first call other than this one). Call it first in a keyed session, and after any 'quota' or 'rate' refusal. Needs a key or a sign-in.",
 "inputSchema": {
  "properties": {},
  "type": "object"
 },
 "name": "key_status",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "apply": {
    "additionalProperties": true,
    "properties": {
     "accountLinked": {
      "type": "boolean"
     },
     "blockers": {
      "description": "Empty when ready. Each entry is the refusal request_application would give.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "mandateActive": {
      "type": "boolean"
     },
     "note": {
      "type": "string"
     },
     "passLive": {
      "description": "A live pass with an application left, specifically.",
      "type": "boolean"
     },
     "pausedUntil": {
      "type": "string"
     },
     "planActive": {
      "description": "An active Agent plan OR a live pass \u2014 either funds a new request.",
      "type": "boolean"
     },
     "ready": {
      "type": "boolean"
     },
     "requirements": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "resumeOnFile": {
      "type": "boolean"
     },
     "subscribed": {
      "description": "An active Agent plan specifically.",
      "type": "boolean"
     }
    },
    "required": [
     "ready",
     "blockers"
    ],
    "type": "object"
   },
   "counted": {
    "type": "string"
   },
   "docs": {
    "type": "string"
   },
   "features": {
    "additionalProperties": true,
    "properties": {
     "fit_resume": {
      "description": "Paid tiers and a live pass, exactly as POST /v1/fit plus the pass.",
      "type": "boolean"
     },
     "rankedEngine": {
      "description": "/v1/jobs?engine=ranked on the data API, same key. Paid tiers only \u2014 never the pass.",
      "type": "boolean"
     },
     "request_application": {
      "description": "True only when every apply gate below already passes.",
      "type": "boolean"
     }
    },
    "required": [
     "fit_resume",
     "rankedEngine",
     "request_application"
    ],
    "type": "object"
   },
   "key": {
    "additionalProperties": true,
    "properties": {
     "accountLinked": {
      "description": "False for a plain data-API key; the apply tools need a key minted while signed in.",
      "type": "boolean"
     },
     "id": {
      "description": "The key's row id \u2014 not the key itself, which the server never holds.",
      "type": [
       "string",
       "null"
      ]
     },
     "paid": {
      "type": "boolean"
     },
     "tier": {
      "type": "string"
     }
    },
    "required": [
     "tier",
     "paid",
     "accountLinked"
    ],
    "type": "object"
   },
   "pass": {
    "additionalProperties": true,
    "description": "The account's pass, if any. Every figure is read off the pass row; nothing here is a constant.",
    "properties": {
     "applicationsLeft": {
      "type": [
       "integer",
       "null"
      ]
     },
     "applicationsTotal": {
      "type": [
       "integer",
       "null"
      ]
     },
     "buy": {
      "description": "Where a pass is bought, signed in.",
      "type": "string"
     },
     "endsAt": {
      "description": "When the clock ends. Null until the pass starts.",
      "type": [
       "string",
       "null"
      ]
     },
     "endsInSeconds": {
      "type": [
       "integer",
       "null"
      ]
     },
     "startsOn": {
      "type": "string"
     },
     "state": {
      "enum": [
       "none",
       "unactivated",
       "live",
       "closed"
      ],
      "type": "string"
     }
    },
    "required": [
     "state",
     "startsOn",
     "buy"
    ],
    "type": "object"
   },
   "quota": {
    "additionalProperties": true,
    "properties": {
     "limit": {
      "type": "integer"
     },
     "remaining": {
      "type": "integer"
     },
     "resetsInSeconds": {
      "type": "integer"
     },
     "used": {
      "type": "integer"
     },
     "window": {
      "type": "string"
     }
    },
    "required": [
     "limit",
     "remaining"
    ],
    "type": "object"
   },
   "rate": {
    "additionalProperties": true,
    "properties": {
     "limit": {
      "type": "integer"
     },
     "remaining": {
      "type": "integer"
     },
     "resetsInSeconds": {
      "type": "integer"
     },
     "used": {
      "type": "integer"
     },
     "window": {
      "type": "string"
     }
    },
    "required": [
     "limit",
     "remaining"
    ],
    "type": "object"
   }
  },
  "required": [
   "key",
   "rate",
   "quota",
   "features",
   "pass",
   "apply"
  ],
  "type": "object"
 },
 "securitySchemes": [
  {
   "scopes": [
    "email"
   ],
   "type": "oauth2"
  }
 ],
 "title": "This key's limits and powers"
}
