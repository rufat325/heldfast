{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Returns the well-known tokens on Monad (MON, WMON, USDC, USDT0, AUSD, WETH, sMON, shMON). For dynamic discovery (new tokens, memes), use `resolve_token` with a name/symbol query.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "network": {
    "description": "Monad network \u2014 \"mainnet\" or \"testnet\". Defaults to the server's configured network.",
    "enum": [
     "mainnet",
     "testnet"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_canonical_tokens",
 "title": "List canonical Monad tokens"
}
