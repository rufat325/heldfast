{
 "description": "Paid ($0.25 USDC): walk-forward replay of the live multi-timeframe strategy over recent 1m history. Returns win-rate + average forward return per BUY/SELL signal so an agent can measure recent edge before subscribing.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "forward": {
    "default": 30,
    "description": "Forward window in 1m bars to score each signal against (5-120)",
    "maximum": 120,
    "minimum": 5,
    "type": "integer"
   },
   "pair": {
    "default": "BTC/USD",
    "description": "Kraken */USD pair, e.g. BTC/USD, ETH/USD",
    "type": "string"
   },
   "step": {
    "default": 5,
    "description": "Walk-forward step in 1m bars (1-15). Smaller = denser sample but slower.",
    "maximum": 15,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "get_backtest"
}
