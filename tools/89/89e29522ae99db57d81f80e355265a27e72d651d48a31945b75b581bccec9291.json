{
 "_meta": {
  "xdata.kr/credential": {
   "providers": [
    "nhis_edi"
   ],
   "register_url": "https://console.xdata.kr/credentials?link=1&priority=nhis_edi",
   "required": true,
   "windows_pc_needed": true
  }
 },
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Call this FIRST before any other tool for this provider. Login to NHIS EDI via NPKI certificate. Returns API_KEY for chaining.",
 "inputSchema": {
  "properties": {
   "account_link_id": {
    "description": "Account link ID (UUID v4). Optional \u2014 the server auto-resolves from your API key when omitted. Provide explicitly only when you have multiple certificates for the same provider. Each institution has a separate ID (Hometax != NHIS != NPS != FourInsure).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "nhis_edi__session__login",
 "securitySchemes": [
  {
   "scopes": [
    "mcp:tools:list",
    "mcp:tools:call"
   ],
   "type": "oauth2"
  }
 ],
 "title": "\uac74\uac15\ubcf4\ud5d8 EDI \ub85c\uadf8\uc778"
}
