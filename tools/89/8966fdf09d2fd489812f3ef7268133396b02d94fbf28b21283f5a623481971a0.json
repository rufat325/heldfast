{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Look up the open-access status of a scholarly work by DOI and get a direct link to a free, legal full-text copy if one exists \u2014 aggregated from 50K+ publisher and repository sources (institutional repositories, PubMed Central, arXiv, publisher OA pages). Returns oa_status (gold/green/hybrid/bronze/closed), the best OA location (PDF URL, license, host type, version), and all known OA locations. Complements education.doi_lookup and crossref.works_search (which return bibliographic metadata, not full-text availability). Data: api.unpaywall.org, no auth required (an internal contact email is sent per API terms, not a user credential).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "doi": {
    "description": "DOI of the scholarly work to look up, with or without the \"https://doi.org/\" prefix (e.g. 10.1038/nature12373)",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "doi"
  ],
  "type": "object"
 },
 "name": "unpaywall.oa.lookup",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Unpaywall Open-Access Lookup"
}
