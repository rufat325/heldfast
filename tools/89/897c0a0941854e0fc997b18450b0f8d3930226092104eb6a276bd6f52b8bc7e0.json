{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Look up one exact 7-character ICD-10-PCS code, plus the sibling codes for the same procedure on the same body part (keyless, offline: CMS FY2026). E.g. \"0SR9019\" -> its title plus the other 0SR9* right-hip-replacement codes, which is how you find the uncemented or ceramic alternative to a code you already have.",
 "inputSchema": {
  "examples": [
   {
    "code": "0SR9019"
   },
   {
    "code": "0DTJ4ZZ",
    "siblings": 5
   }
  ],
  "properties": {
   "code": {
    "description": "A 7-character ICD-10-PCS code, e.g. \"0SR9019\".",
    "type": "string"
   },
   "siblings": {
    "description": "Max sibling codes sharing the first 4 characters (same procedure on the same body part) to include (0-100, default 10).",
    "type": "number"
   }
  },
  "required": [
   "code"
  ],
  "type": "object"
 },
 "name": "get_icd10pcs",
 "title": "Get Icd10pcs",
 "version": "v1.a8d9996c"
}
