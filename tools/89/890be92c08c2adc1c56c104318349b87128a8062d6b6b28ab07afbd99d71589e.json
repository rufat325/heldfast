{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search 10 million+ open access articles indexed by the Directory of Open Access Journals (DOAJ). All articles are from peer-reviewed journals with immediate free access (no embargo, no paywall). Query by topic, author, title, DOI, or DOAJ query syntax. Returns title, authors, journal, year, subjects, keywords, abstract (up to 500 chars), DOI, and full-text URL for direct access to the paper. Ideal for finding freely readable academic literature across any discipline.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "page": {
    "description": "Result page number (1-based). Default: 1.",
    "minimum": 1,
    "type": "integer"
   },
   "page_size": {
    "description": "Number of articles per page (1\u201350). Default: 10.",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Full-text search query for articles (e.g. \"climate change mitigation\", \"title:CRISPR\", \"doi:10.1002/xyz\"). Supports DOAJ query syntax.",
    "type": "string"
   },
   "sort": {
    "description": "Sort order. Examples: \"title:asc\", \"created_date:desc\". Default: relevance.",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "doaj.articles.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "DOAJ \u2014 Search Open Access Articles"
}
