{
 "_meta": {
  "skill": "signing"
 },
 "description": "Compute the SIP-018 message hash without signing. Returns the full encoded hash, domain hash, and message hash. Useful for preparing data for on-chain verification or multi-sig coordination. Does not require an unlocked wallet.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "domain": {
    "description": "Domain binding for the hash",
    "properties": {
     "chainId": {
      "description": "Optional chain ID. Defaults to current network (1 for mainnet, 2147483648 for testnet)",
      "type": "number"
     },
     "name": {
      "description": "Application name",
      "type": "string"
     },
     "version": {
      "description": "Application version",
      "type": "string"
     }
    },
    "required": [
     "name",
     "version"
    ],
    "type": "object"
   },
   "message": {
    "additionalProperties": {},
    "description": "The structured data as a JSON object. Use type hints for explicit types: {type: 'uint', value: 100}, {type: 'principal', value: 'SP...'}, etc.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   }
  },
  "required": [
   "message",
   "domain"
  ],
  "type": "object"
 },
 "name": "sip018_hash"
}
