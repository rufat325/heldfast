{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true
 },
 "description": "One-shot reproduce-and-correlate: clear the buffer (unless clear=false), perform one action OR a sequence in order, wait for async console/network/server events to land, then return EVERYTHING that happened on both sides plus per-step results and an errors summary. Use a sequence for flows like navigate \u2192 click \u2192 type \u2192 click submit.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "A single action (convenience for a one-step sequence). Ignored if `actions` is given.",
    "properties": {
     "expression": {
      "description": "for kind=eval",
      "type": "string"
     },
     "key": {
      "description": "for kind=press (e.g. Enter, Escape, Tab)",
      "type": "string"
     },
     "kind": {
      "description": "The action to perform.",
      "enum": [
       "navigate",
       "click",
       "type",
       "hover",
       "scroll",
       "select",
       "press",
       "eval",
       "wait",
       "none"
      ],
      "type": "string"
     },
     "selector": {
      "description": "for kind=click|type|hover|select|wait (and optional for scroll/press)",
      "type": "string"
     },
     "text": {
      "description": "for kind=type, or kind=wait (text to wait for)",
      "type": "string"
     },
     "timeoutMs": {
      "description": "for kind=wait",
      "type": "number"
     },
     "url": {
      "description": "for kind=navigate",
      "type": "string"
     },
     "value": {
      "description": "for kind=select",
      "type": "string"
     },
     "x": {
      "description": "for kind=scroll",
      "type": "number"
     },
     "y": {
      "description": "for kind=scroll",
      "type": "number"
     }
    },
    "type": "object"
   },
   "actions": {
    "description": "Sequence of actions performed in order. Use this OR `action`.",
    "items": {
     "properties": {
      "expression": {
       "description": "for kind=eval",
       "type": "string"
      },
      "key": {
       "description": "for kind=press (e.g. Enter, Escape, Tab)",
       "type": "string"
      },
      "kind": {
       "description": "The action to perform.",
       "enum": [
        "navigate",
        "click",
        "type",
        "hover",
        "scroll",
        "select",
        "press",
        "eval",
        "wait",
        "none"
       ],
       "type": "string"
      },
      "selector": {
       "description": "for kind=click|type|hover|select|wait (and optional for scroll/press)",
       "type": "string"
      },
      "text": {
       "description": "for kind=type, or kind=wait (text to wait for)",
       "type": "string"
      },
      "timeoutMs": {
       "description": "for kind=wait",
       "type": "number"
      },
      "url": {
       "description": "for kind=navigate",
       "type": "string"
      },
      "value": {
       "description": "for kind=select",
       "type": "string"
      },
      "x": {
       "description": "for kind=scroll",
       "type": "number"
      },
      "y": {
       "description": "for kind=scroll",
       "type": "number"
      }
     },
     "required": [
      "kind"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "clear": {
    "description": "Clear the buffer first (default true).",
    "type": "boolean"
   },
   "continueOnError": {
    "description": "Keep going if a step fails (default false: stop after the failing step).",
    "type": "boolean"
   },
   "idleMs": {
    "description": "Quiet period that counts as idle for waitFor=networkidle (default 500).",
    "type": "number"
   },
   "settleMs": {
    "description": "Fixed wait after the FINAL action for waitFor=settle (default 1000).",
    "type": "number"
   },
   "stepSettleMs": {
    "description": "Fixed wait BETWEEN steps for waitFor=settle (default 300).",
    "type": "number"
   },
   "timeoutMs": {
    "description": "Max wait for waitFor=networkidle before giving up (default 10000).",
    "type": "number"
   },
   "waitFor": {
    "description": "How to wait after each action: 'settle' = fixed sleep; 'networkidle' = wait until no network activity (more reliable for slow/streaming). Default 'settle'.",
    "enum": [
     "settle",
     "networkidle"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "repro"
}
