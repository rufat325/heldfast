{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Get the list of timestamps when changes were detected for a watch. Use these timestamps with get_snapshot or get_diff.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "limit": {
    "description": "Return at most this many entries. Default: all of them.",
    "maximum": 9007199254740991,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Skip this many entries before returning. Use with limit.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "uuid": {
    "description": "Watch UUID",
    "type": "string"
   }
  },
  "required": [
   "uuid"
  ],
  "type": "object"
 },
 "name": "get_change_history",
 "title": "List change timestamps"
}
