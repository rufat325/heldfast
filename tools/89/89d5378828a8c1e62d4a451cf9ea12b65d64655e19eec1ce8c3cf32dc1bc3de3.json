{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Delete a chart. Get the chart_id from chart__list. Undoable, but it removes the chart and its formatting.",
 "inputSchema": {
  "properties": {
   "chartId": {
    "description": "From chart__list.",
    "type": "string"
   },
   "sheetIdx": {
    "type": "integer"
   }
  },
  "required": [
   "sheetIdx",
   "chartId"
  ],
  "type": "object"
 },
 "name": "chart_delete"
}
