{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Query a Tweede Kamer (Dutch Parliament) OData v4 entity set with full OData passthrough. Entity names and fields are Dutch. Returns matching records under `value`. Use $filter to subset (e.g. \"Achternaam eq 'Rutte'\" or \"contains(NaamNL,'Groen')\"), $select to pick fields, $orderby to sort, $expand for related records. Always set $top to bound responses.",
 "inputSchema": {
  "examples": [
   {
    "entity": "Persoon",
    "filter": "Achternaam eq 'Rutte'",
    "select": "Id,Achternaam,Roepnaam,Functie",
    "top": 10
   },
   {
    "entity": "Fractie",
    "filter": "AantalZetels gt 10",
    "orderby": "AantalZetels desc",
    "select": "Id,Naam,Afkorting,AantalZetels",
    "top": 25
   }
  ],
  "properties": {
   "count": {
    "description": "If true, include total match count as @odata.count.",
    "type": "boolean"
   },
   "entity": {
    "description": "Dutch entity name. Common: Persoon (people/MPs), Fractie (parties), Zaak (cases/motions/bills), Activiteit (activities/debates), Vergadering (meetings), Stemming (votes), Besluit (decisions), Document (documents).",
    "type": "string"
   },
   "expand": {
    "description": "OData $expand: navigation properties to inline, e.g. \"FractieZetel\".",
    "type": "string"
   },
   "filter": {
    "description": "OData $filter. eq/ne/gt/lt + contains()/startswith()/endswith(). String literals in single quotes (double an embedded quote). E.g. \"Afkorting eq 'VVD'\", \"contains(Achternaam,'Rut')\", \"AantalZetels gt 10\".",
    "type": "string"
   },
   "orderby": {
    "description": "OData $orderby, e.g. \"Achternaam asc\" or \"GewijzigdOp desc\".",
    "type": "string"
   },
   "select": {
    "description": "OData $select: comma-separated Dutch field names, e.g. \"Id,Achternaam,Roepnaam,Functie\".",
    "type": "string"
   },
   "skip": {
    "description": "Records to skip for paging (OData $skip).",
    "type": "number"
   },
   "top": {
    "description": "Max records to return (default 25, OData $top).",
    "type": "number"
   }
  },
  "required": [
   "entity"
  ],
  "type": "object"
 },
 "name": "query_entity",
 "title": "Query Entity",
 "version": "v1.7d2bc0aa"
}
