{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List the subject-area themes used across open.canada.ca (e.g. Nature And Environment, Science And Technology, Health And Safety), ranked by dataset count. Each theme includes an fq filter string to pass to the search tool to list that theme's datasets. Use this to browse what Canada publishes by topic before searching.",
 "inputSchema": {
  "examples": [
   {
    "limit": 15
   }
  ],
  "properties": {
   "limit": {
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "groups",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of themes returned",
    "type": "integer"
   },
   "groups": {
    "description": "Themes from the Solr `subject` facet.",
    "items": {
     "properties": {
      "datasets": {
       "description": "Number of datasets carrying this theme",
       "type": "integer"
      },
      "fq": {
       "description": "Solr filter query that reproduces this theme, e.g. subject:\"transport\"",
       "type": "string"
      },
      "name": {
       "description": "Theme name",
       "type": "string"
      },
      "title": {
       "description": "Human-readable theme title",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "note": {
    "description": "Explanatory note about the theme source",
    "type": "string"
   },
   "total_datasets": {
    "description": "Total datasets across the portal",
    "type": "integer"
   }
  },
  "required": [
   "groups",
   "count"
  ],
  "type": "object"
 },
 "title": "Groups",
 "version": "v1.eb249f00"
}
