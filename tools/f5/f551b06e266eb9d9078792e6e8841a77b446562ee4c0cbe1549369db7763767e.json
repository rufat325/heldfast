{
 "_meta": {
  "skill": "bns"
 },
 "description": "Register a BNS domain name after preorder is confirmed. This is step 2 of a 2-step process. You MUST use the same salt from the preorder step. Only call this after the preorder transaction has been confirmed on-chain (~10 minutes). Auto-detects contract version: V2 for .btc names, V1 for other namespaces.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "name": {
    "description": "BNS name to register (must match the preordered name)",
    "type": "string"
   },
   "salt": {
    "description": "The hex salt used in the preorder step (REQUIRED - must match exactly)",
    "type": "string"
   }
  },
  "required": [
   "name",
   "salt"
  ],
  "type": "object"
 },
 "name": "register_bns_name"
}
