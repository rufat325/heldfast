{
 "description": "Portfolio overlap between two funds: the percentage invested in the same positions, plus the largest shared holdings. A fund against itself is 100.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "as_of": {
    "description": "Point-in-time date (YYYY-MM-DD). Returns the latest snapshot on or before this date. Omit for the most recent.",
    "type": "string"
   },
   "available_as_of": {
    "description": "Lookahead guard (YYYY-MM-DD): only consider filings actually published by this date. Use for backtests.",
    "type": "string"
   },
   "other": {
    "type": "string"
   },
   "ticker": {
    "type": "string"
   }
  },
  "required": [
   "ticker",
   "other"
  ],
  "type": "object"
 },
 "name": "fund_overlap"
}
