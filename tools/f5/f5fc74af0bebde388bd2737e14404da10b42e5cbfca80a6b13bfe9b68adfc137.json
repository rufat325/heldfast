{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List an organizer's events \u2014 the public way to see everything one organizer is running.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "continuation": {
    "description": "Pagination continuation token from a previous response",
    "type": "string"
   },
   "order_by": {
    "enum": [
     "start_asc",
     "start_desc",
     "created_asc",
     "created_desc"
    ],
    "type": "string"
   },
   "organizer_id": {
    "description": "Numeric organizer id",
    "type": "string"
   },
   "status": {
    "description": "Event status filter (default all)",
    "enum": [
     "all",
     "live",
     "draft",
     "started",
     "ended",
     "completed",
     "canceled"
    ],
    "type": "string"
   },
   "view": {
    "description": "Response shape: \"compact\" (default) drops fields the response already carries elsewhere; \"full\" returns every field this server understands. compact strips image/avatar URLs from the response; \"full\" returns Eventbrite's payload untouched. No field projection on this tool: this server has no verified record of which Eventbrite fields matter here, and inventing one would risk dropping a field a caller needs.",
    "enum": [
     "compact",
     "full"
    ],
    "type": "string"
   }
  },
  "required": [
   "organizer_id"
  ],
  "type": "object"
 },
 "name": "eb_organizer_events"
}
