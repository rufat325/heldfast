{
 "description": "Update only a Granoflow project description as living context. Defaults to dry-run and requires an evidence summary.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "description": {
    "minLength": 1,
    "type": "string"
   },
   "dryRun": {
    "default": true,
    "description": "When true, previews the request without writing.",
    "type": "boolean"
   },
   "evidenceSummary": {
    "description": "Short evidence summary for why this context description changed.",
    "minLength": 1,
    "type": "string"
   },
   "projectId": {
    "description": "Granoflow project id.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "projectId",
   "description",
   "evidenceSummary"
  ],
  "type": "object"
 },
 "name": "granoflow_project_context_update"
}
