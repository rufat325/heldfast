{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search real-world wildlife sightings logged by the iNaturalist community, filterable by species (taxon_name or taxon_id), place (place_id from inaturalist.places_search), a lat/lng radius, date range, and data quality grade (research/needs_id/casual). Returns each observation's species guess, taxon info, observed date, location, observer, and photo. Data: iNaturalist.org global citizen-science community, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lat": {
    "description": "Latitude for a radius search. Must be paired with lng.",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "lng": {
    "description": "Longitude for a radius search. Must be paired with lat.",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "observed_after": {
    "description": "Only include observations on/after this date, ISO 8601 (YYYY-MM-DD).",
    "type": "string"
   },
   "observed_before": {
    "description": "Only include observations on/before this date, ISO 8601 (YYYY-MM-DD).",
    "type": "string"
   },
   "per_page": {
    "description": "Number of results to return, 1-20 (default 10). At least one of taxon_name, taxon_id, place_id, or lat/lng is required.",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "place_id": {
    "description": "Numeric place id from inaturalist.places_search to filter observations by.",
    "type": "integer"
   },
   "quality_grade": {
    "description": "Filter by data quality: \"research\" (community-verified ID), \"needs_id\" (unconfirmed), or \"casual\" (no photo/no location/captive).",
    "enum": [
     "casual",
     "needs_id",
     "research"
    ],
    "type": "string"
   },
   "radius_km": {
    "description": "Search radius in kilometers around lat/lng (default 10).",
    "maximum": 500,
    "minimum": 0.1,
    "type": "number"
   },
   "taxon_id": {
    "description": "Numeric taxon id from inaturalist.taxa_search to filter observations by.",
    "type": "integer"
   },
   "taxon_name": {
    "description": "Scientific or common name to filter observations by, e.g. \"Danaus plexippus\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "inaturalist.observations.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Search iNaturalist Wildlife Observations"
}
