{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Authenticated user's calendar events. Prefer explicit from/to (YYYY-MM-DD). Shortcuts: today|week|month|year (calendar year)|decade. Browsing noise excluded by default. ($0.10; API key required)",
 "destructiveHint": false,
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "cursor": {
    "type": "string"
   },
   "from": {
    "description": "Inclusive YYYY-MM-DD (preferred over range)",
    "type": "string"
   },
   "include_archived": {
    "type": "boolean"
   },
   "include_browsing": {
    "description": "Include extension \"Browsed domain \u00b7 Xm\" auto-events (default false)",
    "type": "boolean"
   },
   "include_calendar_blocks": {
    "description": "Include Focus Time / calendar_block rows (default false)",
    "type": "boolean"
   },
   "kinds": {
    "description": "Filter by event_kind: life_event, calendar_block, todo, goal, travel_segment, food_log, health_record, transaction, media_log, work, family, personal. Alias \"food\" (also meal/drink/snack) matches the displayed Food category: food_log rows plus Meal/Drink/Snack life_events. total/has_more count only matching rows.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "limit": {
    "maximum": 500,
    "minimum": 1,
    "type": "number"
   },
   "range": {
    "enum": [
     "today",
     "week",
     "month",
     "year",
     "decade",
     "all"
    ],
    "type": "string"
   },
   "to": {
    "description": "Inclusive YYYY-MM-DD",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_events",
 "openWorldHint": false,
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "cap": {
    "type": "number"
   },
   "events": {
    "items": {
     "additionalProperties": true,
     "description": "A Dayze calendar event record.",
     "type": "object"
    },
    "type": "array"
   },
   "expires_at": {
    "type": "string"
   },
   "from": {
    "description": "Inclusive YYYY-MM-DD lower bound; null for all records.",
    "type": [
     "string",
     "null"
    ]
   },
   "has_more": {
    "type": "boolean"
   },
   "next_cursor": {
    "type": [
     "string",
     "null"
    ]
   },
   "ordering": {
    "type": "string"
   },
   "range": {
    "type": "string"
   },
   "server_time": {
    "type": "string"
   },
   "snapshot_at": {
    "type": "string"
   },
   "snapshot_id": {
    "type": "string"
   },
   "timezone": {
    "description": "Resolved timezone used for this event window and as fallback for rows without one.",
    "type": "string"
   },
   "to": {
    "description": "Inclusive YYYY-MM-DD upper bound; null for all records.",
    "type": [
     "string",
     "null"
    ]
   },
   "total": {
    "type": "number"
   },
   "truncated": {
    "type": "boolean"
   }
  },
  "required": [
   "events",
   "timezone",
   "range",
   "from",
   "to",
   "cap",
   "truncated"
  ],
  "type": "object"
 },
 "readOnlyHint": true,
 "securitySchemes": [
  {
   "scopes": [
    "context.read"
   ],
   "type": "oauth2"
  }
 ],
 "title": "Calendar Events"
}
