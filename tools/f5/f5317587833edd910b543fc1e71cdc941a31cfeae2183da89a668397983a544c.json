{
 "description": "Creative Center creators + createTime / bioLinkRisk / ttSeller hydrate for partnership vetting. Costs 2 credits. Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cache": {
    "description": "Set true to serve from the response cache (default TTL). Default false \u2014 always fetch fresh. Prefer cacheMaxAge when you need 1d\u201330d freshness control. Envelope includes cached + cachedAt on hits.",
    "type": "boolean"
   },
   "cacheMaxAge": {
    "description": "Max age of a cached response: 1d, 3d, 7d, 14d, or 30d. When set, enables caching with that TTL. Envelope: cached + cachedAt.",
    "type": "string"
   },
   "country": {
    "description": "Two-letter ISO country code. Default US.",
    "type": "string"
   },
   "follower_count": {
    "description": "Optional range on FYP/Apify fallthrough: 10k-100k, 100k-1m, 1m-10m, >10m.",
    "type": "string"
   },
   "limit": {
    "description": "Max items to return (default 20, max 100). Flat 2 credits per call.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "page": {
    "description": "Creative Center page (default 1).",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "sort": {
    "description": "follower, engagement, or popularity. Default follower.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "tiktok_popular_creators",
 "title": "TikTok Popular Creators"
}
