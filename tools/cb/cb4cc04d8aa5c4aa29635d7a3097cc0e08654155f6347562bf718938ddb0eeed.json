{
 "description": "Create an invoice in Xero. Type ACCREC = accounts-receivable (sales invoice to a customer), ACCPAY = accounts-payable (bill from a supplier). Default Status=DRAFT; set AUTHORISED to issue immediately.",
 "inputSchema": {
  "properties": {
   "Contact": {
    "description": "Contact reference \u2014 provide ContactID of an existing contact.",
    "properties": {
     "ContactID": {
      "description": "Xero Contact UUID.",
      "type": "string"
     }
    },
    "required": [
     "ContactID"
    ],
    "type": "object"
   },
   "CurrencyCode": {
    "description": "ISO-4217 currency code. Defaults to organization base currency.",
    "type": "string"
   },
   "Date": {
    "description": "Invoice date, YYYY-MM-DD.",
    "type": "string"
   },
   "DueDate": {
    "description": "Payment due date, YYYY-MM-DD.",
    "type": "string"
   },
   "InvoiceNumber": {
    "description": "Optional merchant invoice number. If omitted, Xero auto-numbers (ACCREC only).",
    "type": "string"
   },
   "LineAmountTypes": {
    "description": "Whether LineItem UnitAmount is tax-exclusive, tax-inclusive, or tax-free.",
    "enum": [
     "Exclusive",
     "Inclusive",
     "NoTax"
    ],
    "type": "string"
   },
   "LineItems": {
    "description": "Array of line items. Each: Description, Quantity, UnitAmount, AccountCode (or ItemCode), TaxType, DiscountRate.",
    "type": "array"
   },
   "Reference": {
    "description": "ACCREC only \u2014 free-text reference visible on invoice.",
    "type": "string"
   },
   "Status": {
    "description": "DRAFT (default), SUBMITTED (awaiting approval), or AUTHORISED (live/issued).",
    "enum": [
     "DRAFT",
     "SUBMITTED",
     "AUTHORISED"
    ],
    "type": "string"
   },
   "Type": {
    "description": "ACCREC (sales/AR) or ACCPAY (bill/AP).",
    "enum": [
     "ACCREC",
     "ACCPAY"
    ],
    "type": "string"
   }
  },
  "required": [
   "Type",
   "Contact",
   "LineItems"
  ],
  "type": "object"
 },
 "name": "create_invoice"
}
