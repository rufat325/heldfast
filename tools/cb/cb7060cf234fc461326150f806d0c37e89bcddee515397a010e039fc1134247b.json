{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Restore a space from an export payload. Mode: empty-only (default), merge, or replace.",
 "inputSchema": {
  "properties": {
   "mode": {
    "description": "Import mode (default empty-only)",
    "enum": [
     "empty-only",
     "merge",
     "replace"
    ],
    "type": "string"
   },
   "payload": {
    "description": "Export payload (from space_export)",
    "type": "object"
   },
   "spaceId": {
    "description": "Target memory space ID",
    "type": "string"
   }
  },
  "required": [
   "spaceId",
   "payload"
  ],
  "type": "object"
 },
 "name": "space_import",
 "outputSchema": {
  "properties": {},
  "type": "object"
 }
}
