{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Renames a layer. Layer names are what the generated code turns into class names, so tidying them is a real change and not cosmetic.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "name": {
    "type": "string"
   },
   "nodeId": {
    "description": "Figma node id, like \"21:10314\". Omit to use whatever is selected on the canvas.",
    "type": "string"
   }
  },
  "required": [
   "nodeId",
   "name"
  ],
  "type": "object"
 },
 "name": "figma_set_node_name",
 "title": "Rename a layer"
}
