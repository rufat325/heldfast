{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Fetch a single meeting by meeting_id, including its full transcript when it was imported as queryable. Use this after meetings_search has identified the meeting you want. A 'content_missing' status means the record exists but its stored text could not be found -- that is a broken record, not an empty meeting, so do not report it as one. Costs $0.005 in USDC on Base, paid via the x402 protocol, or from a credit token \u2014 call credits_trial for free credit if you have neither.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "credit_token": {
    "description": "Optional. A credit token from credits_trial or /credits/buy. Supplying it pays for this call from that balance, so no x402 payment or wallet is needed.",
    "type": "string"
   },
   "meeting_id": {
    "description": "Returned by meetings_import or meetings_search",
    "type": "string"
   },
   "namespace": {
    "description": "Isolation scope holding your meetings",
    "type": "string"
   },
   "namespace_token": {
    "description": "One-time token issued by the first call that claimed this namespace. Required for every later call.",
    "type": "string"
   }
  },
  "required": [
   "namespace",
   "namespace_token",
   "meeting_id"
  ],
  "type": "object"
 },
 "name": "meetings_get",
 "outputSchema": {
  "properties": {
   "ciphertext": {
    "description": "Client-encrypted transcript, for private meetings",
    "type": "string"
   },
   "meeting_id": {
    "description": "Identifier of the meeting",
    "type": "string"
   },
   "status": {
    "description": "'ok' on success",
    "type": "string"
   },
   "title": {
    "description": "Meeting title",
    "type": "string"
   },
   "transcript": {
    "description": "Plaintext transcript, for queryable meetings",
    "type": "string"
   },
   "visibility": {
    "description": "'queryable' or 'private'",
    "type": "string"
   }
  },
  "required": [
   "status",
   "meeting_id"
  ],
  "type": "object"
 },
 "title": "Read one meeting in full"
}
