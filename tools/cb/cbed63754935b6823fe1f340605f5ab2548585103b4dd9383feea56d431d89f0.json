{
 "description": "List a company's reports \u2014 metadata only (id, title, period), no body text. Each item carries a `source` field naming the official disclosure platform; keep that attribution when you cite it.",
 "inputSchema": {
  "properties": {
   "doc_type": {
    "default": "annual",
    "description": "Report type to filter on. Defaults to annual.",
    "enum": [
     "annual",
     "semiannual",
     "q1",
     "q3"
    ],
    "type": "string"
   },
   "size": {
    "default": 10,
    "description": "Number of reports to return (default 10).",
    "type": "integer"
   },
   "symbol": {
    "description": "FMP-style symbol, e.g. 600519.SS / 005930.KS / 7203.T / 2330.TW",
    "type": "string"
   }
  },
  "required": [
   "symbol"
  ],
  "type": "object"
 },
 "name": "list_reports"
}
