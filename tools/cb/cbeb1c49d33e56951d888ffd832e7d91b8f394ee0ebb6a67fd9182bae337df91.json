{
 "description": "Set a game as the active modding target. Auto-detects engine, sets up session state, and returns what tools are available for this game type.",
 "inputSchema": {
  "properties": {
   "game_name": {
    "description": "Optional human-readable game name (auto-detected from path if omitted)",
    "type": "string"
   },
   "game_path": {
    "description": "Full path to the game directory",
    "type": "string"
   }
  },
  "required": [
   "game_path"
  ],
  "type": "object"
 },
 "name": "load_game"
}
