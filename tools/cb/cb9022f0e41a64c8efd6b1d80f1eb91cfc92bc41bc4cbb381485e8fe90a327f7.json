{
 "annotations": {
  "destructiveHint": false
 },
 "description": "Scroll through React, then use installed SimView or IDB on iOS and the selected ADB serial on Android. Native results identify the backend and dispatch state.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "direction": {
    "description": "Swipe direction",
    "enum": [
     "up",
     "down",
     "left",
     "right"
    ],
    "type": "string"
   },
   "platform": {
    "default": "auto",
    "enum": [
     "ios",
     "android",
     "auto"
    ],
    "type": "string"
   }
  },
  "required": [
   "direction"
  ],
  "type": "object"
 },
 "name": "swipe"
}
