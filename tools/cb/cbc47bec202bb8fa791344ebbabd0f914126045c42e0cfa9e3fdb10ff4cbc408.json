{
 "description": "Save a session summary for later recall. Auto-generates session_id if not provided. Returns the session_id for later recall.",
 "inputSchema": {
  "properties": {
   "session_id": {
    "description": "Session/conversation ID (auto-generated if omitted)",
    "type": "string"
   },
   "summary": {
    "description": "Session summary to save",
    "type": "string"
   },
   "tags": {
    "description": "Additional tags",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "summary"
  ],
  "type": "object"
 },
 "name": "velixar_session_save"
}
