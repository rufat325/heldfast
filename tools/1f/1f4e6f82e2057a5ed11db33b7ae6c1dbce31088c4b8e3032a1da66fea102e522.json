{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List OECD dataflow refs we have pre-vetted, grouped by topic (gdp, labour, prices, finance, households, health, demographics, projections, tax, education, environment, technology). Pass the flow_ref to fetch_dataset. For everything else use search_dataflows or browse https://data-explorer.oecd.org.",
 "inputSchema": {
  "examples": [
   {
    "topic": "gdp"
   },
   {}
  ],
  "properties": {
   "topic": {
    "description": "Optional topic filter",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "list_curated_flows",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of flows in result",
    "type": "number"
   },
   "flows": {
    "description": "Curated dataflow definitions",
    "items": {
     "properties": {
      "flow_ref": {
       "description": "SDMX dataflow reference identifier",
       "type": "string"
      },
      "notes": {
       "description": "Optional additional notes",
       "type": "string"
      },
      "title": {
       "description": "Human-readable dataflow title",
       "type": "string"
      },
      "topic": {
       "description": "Topic category (gdp, labour, prices, etc.)",
       "type": "string"
      }
     },
     "required": [
      "flow_ref",
      "topic",
      "title"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "note": {
    "description": "Reference to additional data sources",
    "type": "string"
   },
   "topics": {
    "description": "Available topics across all curated flows",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "count",
   "topics",
   "flows",
   "note"
  ],
  "type": "object"
 },
 "title": "List Curated Flows",
 "version": "v1.08ac1ef5"
}
