{
 "description": "Fetch one report's full text (metadata + Markdown body). The `source` field names the official disclosure platform; keep that attribution when you cite it. Expensive in tokens \u2014 prefer get_section when you only need one chapter.",
 "inputSchema": {
  "properties": {
   "document_id": {
    "description": "Report id, from list_reports items[].id",
    "type": "integer"
   }
  },
  "required": [
   "document_id"
  ],
  "type": "object"
 },
 "name": "get_report"
}
