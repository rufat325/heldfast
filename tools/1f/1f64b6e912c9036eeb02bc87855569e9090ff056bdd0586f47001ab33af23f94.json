{
 "description": "Middleware hook to run after a tool returns external content and before that content is fed back into an agent/LLM. Scans for prompt injection and dangerous action instructions.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "genome_id": {
    "description": "Unique identifier for this agent genome",
    "type": "string"
   },
   "task_type": {
    "description": "Task type hint, e.g. support, coding, research",
    "type": "string"
   },
   "tool_name": {
    "description": "Name of the tool that produced this result",
    "type": "string"
   },
   "tool_result": {
    "description": "Raw tool output before it is sent back into the agent/LLM context",
    "type": "string"
   },
   "user_id": {
    "description": "User identifier for personalization and audit context",
    "type": "string"
   },
   "user_message": {
    "description": "Original user request, if available",
    "type": "string"
   }
  },
  "required": [
   "genome_id",
   "tool_name",
   "tool_result"
  ],
  "type": "object"
 },
 "name": "gsep_after_tool"
}
