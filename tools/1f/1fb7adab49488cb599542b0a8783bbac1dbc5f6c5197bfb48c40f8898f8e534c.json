{
 "description": "Fire a command and return immediately without waiting for output. Use get_pane_output later to read results. Ideal for long-running commands (builds, servers, tests).",
 "inputSchema": {
  "properties": {
   "command": {
    "description": "The shell command to execute.",
    "type": "string"
   },
   "pane": {
    "description": "Target pane as 'window.pane' (e.g. '0.0'). Defaults to '0.0'.",
    "type": "string"
   }
  },
  "required": [
   "command"
  ],
  "type": "object"
 },
 "name": "execute_async"
}
