{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Portfolio VaR / CVaR (Premium)"
 },
 "description": "[Premium] Compute portfolio Value-at-Risk and Conditional VaR (Expected Shortfall) from a historical [asset][time] return matrix and portfolio weights, accounting for cross-asset correlation. Use to size downside risk on a weighted multi-asset book, attribute risk, or run drawdown scenarios with auditable inputs. Returns VaR and CVaR (loss as a positive number) at the requested confidence, plus expected return, volatility, and the horizon used. To sample outcomes from a parametric distribution instead, use simulate_montecarlo. Premium: needs an ORACLAW_API_KEY OR a per-call x402 payment (no signup).",
 "inputSchema": {
  "properties": {
   "ciLevel": {
    "description": "Two-sided confidence level for the estimation-error CIs in the certificate (default: 0.95).",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "confidence": {
    "description": "VaR confidence level (default: 0.95).",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "horizonDays": {
    "description": "Horizon in days, scales VaR by sqrt(horizon) (default: 1).",
    "minimum": 1,
    "type": "integer"
   },
   "realizedExceedances": {
    "description": "Optional backtest hit sequence (1 = VaR breach); enables a Kupiec unconditional-coverage test in the certificate.",
    "items": {
     "enum": [
      0,
      1
     ],
     "type": "number"
    },
    "type": "array"
   },
   "returns": {
    "description": "[asset][time] matrix of period returns (e.g. daily). Each row same length.",
    "items": {
     "items": {
      "type": "number"
     },
     "type": "array"
    },
    "minItems": 1,
    "type": "array"
   },
   "weights": {
    "description": "Portfolio weights per asset. Length must equal returns.length. Should sum to 1.",
    "items": {
     "type": "number"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "returns",
   "weights"
  ],
  "type": "object"
 },
 "name": "analyze_risk",
 "outputSchema": {
  "properties": {
   "assets": {
    "type": "integer"
   },
   "certificate": {
    "description": "Re-checkable estimation-error certificate: delta-method SE/CI for VaR and ES under the iid-normal assumption, esStatisticallyDistinctFromVaR, effectiveSampleSupport (the estimation window T), an optional Kupiec backtest, and a sha256 contentHash binding inputs\u2192outputs. SE is invalid under heavy tails / autocorrelation (stated in the certificate).",
    "type": "object"
   },
   "confidence": {
    "type": "number"
   },
   "cvar": {
    "description": "Conditional VaR (mean loss beyond VaR threshold).",
    "type": "number"
   },
   "expectedReturn": {
    "type": "number"
   },
   "horizonDays": {
    "type": "integer"
   },
   "var": {
    "description": "Value-at-Risk at the requested confidence (loss expressed as positive number).",
    "type": "number"
   },
   "volatility": {
    "type": "number"
   }
  },
  "required": [
   "var",
   "cvar",
   "expectedReturn",
   "volatility"
  ],
  "type": "object"
 }
}
