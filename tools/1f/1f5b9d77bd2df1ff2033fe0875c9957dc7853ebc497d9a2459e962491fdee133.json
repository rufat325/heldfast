{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": false
 },
 "description": "Set a job's application stage. The server validates the value; valid stages are applied/screening/responded/interview/offer/accepted/rejected/withdrawn.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "slug": {
    "description": "The job's public slug (from search or facets results).",
    "minLength": 1,
    "type": "string"
   },
   "stage": {
    "description": "Application stage, e.g. interview, offer, rejected.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "slug",
   "stage"
  ],
  "type": "object"
 },
 "name": "stage"
}
