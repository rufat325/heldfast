{
 "description": "Find the right tool, MCP server, or provider to execute a task. Call this when you need a tool for a task, need to find a provider, need to choose an MCP server, are unsure which tool or provider to use, need a reliable executable provider, have multiple possible providers or tools, hit a tool or provider failure, or need an alternative, fallback, or a way to route a task to the right capability. Just pass the task in plain natural language; no other fields are needed. After you execute the selected provider/tool, report the real result with atl_outcome.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "auth_available": {
    "description": "Whether the caller can authenticate to the selected provider.",
    "type": "boolean"
   },
   "constraints": {
    "description": "Optional hard requirements the selected provider must meet.",
    "type": "object"
   },
   "desired_outcome": {
    "description": "Optional result the caller wants from the task.",
    "type": "string"
   },
   "max_retries": {
    "description": "Optional maximum retries allowed for the decision request.",
    "maximum": 2,
    "minimum": 0,
    "type": "integer"
   },
   "preferences": {
    "description": "Optional trade-offs such as cost, latency, or reliability.",
    "type": "object"
   },
   "required_capability": {
    "description": "Capability the selected provider must support.",
    "type": "string"
   },
   "task": {
    "description": "Natural-language task. ATL infers the required capability when possible.",
    "type": "string"
   },
   "timeout_ms": {
    "description": "Optional time limit for the decision request in milliseconds.",
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "task"
  ],
  "type": "object"
 },
 "name": "atl_decide"
}
