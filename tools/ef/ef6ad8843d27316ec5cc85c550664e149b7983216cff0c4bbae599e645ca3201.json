{
 "description": "Build an APK with Gradle (assemble<Variant>). On failure returns compact compiler errors with file:line. On success returns the APK path.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "module": {
    "description": "Gradle module, e.g. 'app' (defaults to session)",
    "type": "string"
   },
   "projectDir": {
    "description": "Project root (defaults to session)",
    "type": "string"
   },
   "variant": {
    "description": "Build variant, e.g. 'debug' (defaults to session)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "build_app",
 "title": "Build app"
}
