{
 "description": "Reorder the phases in a workflow's project plan. Provide the full list of phase IDs in the desired order; the plan's sort order is updated to match.",
 "inputSchema": {
  "properties": {
   "orderedIds": {
    "description": "Phase IDs in the desired order (typically the full set of phases for the plan).",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "workflowId": {
    "description": "Workflow ID",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "workflowId",
   "orderedIds"
  ],
  "type": "object"
 },
 "name": "reorder_phases"
}
