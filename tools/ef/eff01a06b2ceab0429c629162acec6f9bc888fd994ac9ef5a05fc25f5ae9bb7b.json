{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Query Salesforce records using SOQL. Returns matching records with all requested fields. Use sf_describe first to learn available fields for your object.",
 "inputSchema": {
  "examples": [
   {
    "query": "SELECT Id, Name, Industry FROM Account LIMIT 10"
   },
   {
    "query": "SELECT Id, FirstName, LastName, Email FROM Contact WHERE Email LIKE '%@acme.com%'"
   }
  ],
  "properties": {
   "query": {
    "description": "SOQL query (e.g., \"SELECT Id, Name FROM Account LIMIT 10\")",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "sf_query",
 "outputSchema": {
  "properties": {
   "done": {
    "description": "Whether all records have been returned",
    "type": "boolean"
   },
   "error": {
    "description": "Error code if connection failed",
    "type": "string"
   },
   "message": {
    "description": "Error message if connection failed",
    "type": "string"
   },
   "records": {
    "description": "Array of matching records",
    "items": {
     "type": "object"
    },
    "type": "array"
   },
   "totalSize": {
    "description": "Total number of records matching query",
    "type": "number"
   }
  },
  "type": "object"
 },
 "title": "Sf Query",
 "version": "v1.8a04b8ee"
}
