{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Delete folder"
 },
 "description": "Delete a folder. The codes in it (and in its subfolders) are NOT deleted \u2014 they become unfiled \u2014 and its subfolders move up to its parent.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Folder id (from list_folders).",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "delete_folder",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "deleted": {
    "type": "boolean"
   },
   "error": {
    "additionalProperties": true,
    "description": "Present only when the tool failed (the result is also flagged isError).",
    "properties": {
     "cause": {
      "description": "Machine-readable failure category: invalid_argument, not_found, forbidden, plan_limit, rate_limited, or server_error.",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "retryable": {
      "description": "Whether repeating the same call could succeed.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "id": {
    "type": "string"
   }
  },
  "type": "object"
 }
}
