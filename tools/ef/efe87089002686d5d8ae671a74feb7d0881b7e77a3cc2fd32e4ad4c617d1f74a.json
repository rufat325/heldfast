{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "PolyDoc: HTML/URL to PDF"
 },
 "description": "Convert a web page, inline HTML, or a saved PolyDoc template to a PDF. Provide exactly one source: url, html, or templateId (with optional templateData). By default the PDF is written to the configured output directory and the file path is returned. Note: HTML that loads scripts from a CDN can hang the renderer; prefer self-contained HTML.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "advanced": {
    "additionalProperties": {},
    "description": "Raw fields deep-merged into the request body for any API option not exposed here (e.g. pdf.watermark, render, request).",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "delivery": {
    "default": "download",
    "description": "download writes the file locally; cloudStorage uploads to a presigned URL; webhook posts the file to a URL.",
    "enum": [
     "download",
     "cloudStorage",
     "webhook"
    ],
    "type": "string"
   },
   "filename": {
    "description": "Output filename. Written under the configured output directory (basename only).",
    "type": "string"
   },
   "html": {
    "description": "Inline HTML to render. Provide exactly one of url, html, or templateId.",
    "type": "string"
   },
   "pdfOptions": {
    "description": "Page layout and PDF conformance options.",
    "properties": {
     "format": {
      "enum": [
       "A3",
       "A4",
       "A5",
       "Ledger",
       "Legal",
       "Letter",
       "Tabloid"
      ],
      "type": "string"
     },
     "landscape": {
      "type": "boolean"
     },
     "marginBottom": {
      "type": "string"
     },
     "marginLeft": {
      "type": "string"
     },
     "marginRight": {
      "type": "string"
     },
     "marginTop": {
      "description": "e.g. \"10mm\".",
      "type": "string"
     },
     "outline": {
      "description": "Generate PDF bookmarks from HTML headings.",
      "type": "boolean"
     },
     "pageRanges": {
      "description": "e.g. \"1-5, 8, 11-13\".",
      "type": "string"
     },
     "pdfa": {
      "description": "Produce archivable PDF/A output. Best-effort unless verify is set.",
      "properties": {
       "level": {
        "description": "Archival target. Use \"3b\" unless a downstream system requires \"2b\" or \"1b\".",
        "enum": [
         "1b",
         "2b",
         "3b"
        ],
        "type": "string"
       },
       "verify": {
        "description": "Validate the output with veraPDF and fail the request (422) if it does not conform.",
        "type": "boolean"
       }
      },
      "required": [
       "level"
      ],
      "type": "object"
     },
     "pdfua": {
      "description": "Produce accessible PDF/UA-1 (ISO 14289-1) output; forces a tagged render. Best-effort unless verify is set.",
      "properties": {
       "verify": {
        "description": "Validate the output with veraPDF and fail the request (422) if it does not conform.",
        "type": "boolean"
       }
      },
      "type": "object"
     },
     "printBackground": {
      "type": "boolean"
     },
     "scale": {
      "maximum": 2,
      "minimum": 0.1,
      "type": "number"
     },
     "tagged": {
      "description": "Produce a tagged (accessible) PDF.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "presignedUrl": {
    "description": "Presigned PUT URL, required when delivery is cloudStorage.",
    "type": "string"
   },
   "returnBase64": {
    "description": "Also return the file as base64 in structured output (heavy; for clients without a filesystem).",
    "type": "boolean"
   },
   "sandbox": {
    "description": "Override the server default for this call. Sandbox output is watermarked and rate-limited (about 5 requests/sec).",
    "type": "boolean"
   },
   "tag": {
    "description": "Label for logging and analytics (max 30 chars).",
    "type": "string"
   },
   "templateData": {
    "additionalProperties": {},
    "description": "Variables for the Liquid template (used with templateId).",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "templateId": {
    "description": "ID of a saved PolyDoc template. Provide exactly one of url, html, or templateId.",
    "type": "string"
   },
   "timeout": {
    "description": "Conversion timeout in milliseconds.",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "url": {
    "description": "Public URL to render. Provide exactly one of url, html, or templateId.",
    "type": "string"
   },
   "webhook": {
    "additionalProperties": {},
    "description": "Webhook config, required when delivery is webhook. Shape: { url, async?, method?, headers?, retries? }.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   }
  },
  "type": "object"
 },
 "name": "polydoc_html_to_pdf",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "base64": {
    "type": "string"
   },
   "contentType": {
    "type": "string"
   },
   "conversionId": {
    "type": "string"
   },
   "creditUsed": {
    "type": "string"
   },
   "delivery": {
    "enum": [
     "download",
     "cloudStorage",
     "webhook"
    ],
    "type": "string"
   },
   "filePath": {
    "description": "Absolute path of the written file (download).",
    "type": "string"
   },
   "sandbox": {
    "type": "boolean"
   },
   "sizeBytes": {
    "type": "number"
   },
   "url": {
    "description": "Delivery URL (cloudStorage / webhook).",
    "type": "string"
   }
  },
  "required": [
   "delivery",
   "sandbox"
  ],
  "type": "object"
 },
 "title": "PolyDoc: HTML/URL to PDF"
}
