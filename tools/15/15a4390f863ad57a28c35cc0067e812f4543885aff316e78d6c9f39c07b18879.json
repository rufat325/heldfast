{
 "description": "Confirm an import with a mandatory idempotency key. The idempotency key is mandatory here. Reusing it returns the same import and never creates a second Queue message \u2014 which is what keeps a retry from importing everyone twice.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "idempotency_key": {
    "description": "Unique confirmation key",
    "type": "string"
   },
   "import_id": {
    "description": "Contact import ID",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "import_id",
   "idempotency_key"
  ],
  "type": "object"
 },
 "name": "commsharbor_contact_import_confirm"
}
