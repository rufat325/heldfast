{
 "description": "Trigger a session clip slot. Use when the user wants to launch a specific clip in Session view. Idempotent for already-playing clips, returning changed=false; playback state is async, so verification is marked unverified.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "clip_slot_index": {
    "minimum": 0,
    "type": "integer"
   },
   "track_index": {
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "track_index",
   "clip_slot_index"
  ],
  "type": "object"
 },
 "name": "clip_fire"
}
