{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List paid ads"
 },
 "description": "List individual ads for a paid ads provider, optionally filtered by campaign and/or ad group. Use after nevent_list_paid_campaigns or nevent_list_paid_ad_groups to drill down to ad level. Returns ad IDs, names, statuses, UTM fields (utmSource, utmMedium, utmCampaign, utmContainsMacros), and lastSyncedAt. Use the returned adId values with nevent_get_paid_ad_creative.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "adGroupId": {
    "description": "Optional: filter ads by parent ad group ID. Use nevent_list_paid_ad_groups to get valid adGroupId values.",
    "minLength": 1,
    "type": "string"
   },
   "campaignId": {
    "description": "Optional: filter ads by parent campaign ID. Use nevent_list_paid_campaigns to get valid campaignId values.",
    "minLength": 1,
    "type": "string"
   },
   "provider": {
    "description": "Ad provider: meta | google | tiktok",
    "enum": [
     "meta",
     "google",
     "tiktok"
    ],
    "type": "string"
   }
  },
  "required": [
   "provider"
  ],
  "type": "object"
 },
 "name": "nevent_list_paid_ads"
}
