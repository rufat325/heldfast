{
 "description": "Recent on-chain trades for a token mint.",
 "inputSchema": {
  "properties": {
   "limit": {
    "default": 20,
    "maximum": 100,
    "type": "integer"
   },
   "mint": {
    "type": "string"
   }
  },
  "required": [
   "mint"
  ],
  "type": "object"
 },
 "name": "recent_trades"
}
