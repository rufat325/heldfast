{
 "description": "Updates one contact in the tenant CRM; only the fields you send change. Needs crm:write on the organization. A foreign or missing id answers 404. Being in the CRM is not permission to email: consent lives in the `marketing` sub-resource.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "company_id": {
    "description": "company id",
    "type": "string"
   },
   "contact_id": {
    "description": "contact_id",
    "type": "string"
   },
   "email": {
    "description": "email",
    "type": "string"
   },
   "first_name": {
    "description": "first name",
    "type": "string"
   },
   "organization_id": {
    "description": "organization_id",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "contact_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_crm_contacts_update"
}
