{
 "description": "Confirm the key is live and list the scopes it holds. Returns no portfolio data, so it is safe to call first. (GET /api/v1; needs the read:projects scope)",
 "inputSchema": {
  "properties": {},
  "type": "object"
 },
 "name": "whoami"
}
