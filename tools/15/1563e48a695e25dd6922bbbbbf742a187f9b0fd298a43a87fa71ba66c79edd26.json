{
 "description": "The user's Qumge balance, plus what each app has spent this month against its monthly cap. Call this before a paid call when the user asks whether they can afford it, and after a 402 that says the balance or a cap stopped the call \u2014 it returns the top-up link to hand to the user. Needs the user's qumge key.",
 "inputSchema": {
  "properties": {
   "qumge_key": {
    "description": "Optional: your Qumge API key, for clients that cannot set an Authorization header. Takes precedence over the header. Never echoed back.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "get_balance"
}
