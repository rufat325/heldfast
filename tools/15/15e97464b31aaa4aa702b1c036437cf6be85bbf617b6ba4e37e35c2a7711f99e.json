{
 "description": "One signed minute by its number, or `head` for the newest. Each one names the digest of the one before it, which is what makes rewriting the past visible.",
 "inputSchema": {
  "properties": {
   "seq": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "time_pulse"
}
