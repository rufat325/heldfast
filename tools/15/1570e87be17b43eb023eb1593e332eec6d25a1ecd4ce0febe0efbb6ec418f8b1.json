{
 "description": "Get full details on a single AgentStorefront service: schema, pricing, rate limits, SLA, sample inputs/outputs.",
 "inputSchema": {
  "properties": {
   "service_id": {
    "description": "The service ID (UUID or slug).",
    "type": "string"
   }
  },
  "required": [
   "service_id"
  ],
  "type": "object"
 },
 "name": "get_service"
}
