{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": true,
  "title": "Remind list classes & chats"
 },
 "description": "List everything in the Remind sidebar \u2014 classes and chats \u2014 as the app itself renders it, with unread counts. This is the entry point: it yields the uuids the other tools take. Optionally filter with a search query, and page with the returned cursor.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "cursor": {
    "description": "`cursor` from a previous call, to page.",
    "type": "string"
   },
   "query": {
    "description": "Filter by name.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "remind_list_entities"
}
