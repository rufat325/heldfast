{
 "description": "Fetch the current state of a single transcode job by ID, including status (pending/processing/completed/failed/canceled) and `output_url` when completed.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Transcode job UUID",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_transcode",
 "title": "Get Transcode"
}
