{
 "description": "Reorder blocks in a document by setting new sort orders.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "documentId": {
    "description": "The document ID",
    "type": "string"
   },
   "order": {
    "description": "Array of {blockId, sortOrder} pairs",
    "items": {
     "additionalProperties": false,
     "properties": {
      "blockId": {
       "type": "string"
      },
      "sortOrder": {
       "type": "number"
      }
     },
     "required": [
      "blockId",
      "sortOrder"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "documentId",
   "order"
  ],
  "type": "object"
 },
 "name": "reorder_blocks"
}
