{
 "description": "Read the full content (markdown body + frontmatter + incoming/outgoing wikilinks) of a wiki page by its slug.",
 "inputSchema": {
  "properties": {
   "slug": {
    "description": "Page slug (e.g. \"rag-architecture\")",
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "read_wiki_page"
}
