{
 "description": "Manage scheduled emails: create a new scheduled email, list pending ones, or cancel one. Accepts flexible time formats for create: ISO 8601, relative (\"in 30 minutes\"), named (\"tomorrow 8am\"), day-based (\"next monday 9am\"), or human-friendly (\"02-14-2026 3:30 PM EST\").",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "action": {
    "description": "Action to perform (default: create)",
    "enum": [
     "create",
     "list",
     "cancel"
    ],
    "type": "string"
   },
   "id": {
    "description": "Scheduled email ID (for cancel)",
    "type": "string"
   },
   "sendAt": {
    "description": "When to send (for create)",
    "type": "string"
   },
   "subject": {
    "description": "Email subject (for create)",
    "type": "string"
   },
   "text": {
    "description": "Body text (for create)",
    "type": "string"
   },
   "to": {
    "description": "Recipient email (for create)",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "manage_scheduled"
}
