{
 "description": "Brand reads. action=context: brand voice, terminology, reference + approved-brand sections (author on-brand before writing). action=terminology: check raw text against brand terms.",
 "inputSchema": {
  "properties": {
   "action": {
    "enum": [
     "context",
     "terminology"
    ],
    "type": "string"
   },
   "site_id": {
    "format": "uuid",
    "type": "string"
   },
   "text": {
    "description": "terminology only: the text to check",
    "type": "string"
   },
   "topic": {
    "description": "context only",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "brand"
}
