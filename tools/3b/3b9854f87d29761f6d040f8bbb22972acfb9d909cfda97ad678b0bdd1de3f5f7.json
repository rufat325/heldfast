{
 "description": "Append a contributor entry to the current project. Writes to sources/contributors/<user>/<YYYY-MM-DD>.md (append-only) plus log entry. Route mode forces routing: auto (LLM decides), daily (only contributor file), concept (also flag for /mb:build to extract concept), daily+concept (both).",
 "inputSchema": {
  "properties": {
   "mode": {
    "description": "auto | daily | concept | daily+concept",
    "type": "string"
   },
   "projectId": {
    "description": "Project id; if omitted, resolves via config.json",
    "type": "string"
   },
   "text": {
    "description": "Body text to contribute",
    "type": "string"
   },
   "user": {
    "description": "Contributor username; if omitted, resolves to os.userInfo().username",
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "mindbase_contribute"
}
