{
 "description": "Read and parse an API discovery dump file generated by gorebox_generate_discovery_mod. Returns structured information about all discovered globals, functions, tables, and callbacks.",
 "inputSchema": {
  "properties": {
   "dump_path": {
    "description": "Path to the dump.txt file",
    "type": "string"
   },
   "filter": {
    "description": "Optional filter string to match entries (case-insensitive)",
    "type": "string"
   }
  },
  "required": [
   "dump_path"
  ],
  "type": "object"
 },
 "name": "gorebox_read_api_dump"
}
