{
 "description": "Append a structured entry to logs/<today>.md. Format: ## [YYYY-MM-DD HH:MM] {operation} | {topic} | {details}",
 "inputSchema": {
  "properties": {
   "details": {
    "description": "Optional details (e.g., \"created 4, updated 3\")",
    "type": "string"
   },
   "operation": {
    "description": "ingest | build | lint | research | migrate | export | ...",
    "type": "string"
   },
   "projectId": {
    "description": "Project id; defaults to the current project (config.json)",
    "type": "string"
   },
   "topic": {
    "type": "string"
   }
  },
  "required": [
   "operation",
   "topic"
  ],
  "type": "object"
 },
 "name": "mindbase_append_log"
}
