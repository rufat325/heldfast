{
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "claim": {
    "description": "The claim or AI-generated reasoning to verify. Can be a statement, decision, analysis, or any text that needs adversarial verification.",
    "minLength": 1,
    "type": "string"
   },
   "domain": {
    "description": "Domain context for specialized verification. Default: general",
    "enum": [
     "financial",
     "medical",
     "legal",
     "code",
     "general"
    ],
    "type": "string"
   },
   "speed": {
    "description": "Verification depth. fast=$0.008 (2 models), standard=$0.02 (4 models), deep=$0.08 (5+ models). Default: standard",
    "enum": [
     "fast",
     "standard",
     "deep"
    ],
    "type": "string"
   },
   "stakeLevel": {
    "description": "Risk level \u2014 higher stakes trigger more thorough verification. Default: medium",
    "enum": [
     "low",
     "medium",
     "high",
     "critical"
    ],
    "type": "string"
   }
  },
  "required": [
   "claim"
  ],
  "type": "object"
 },
 "name": "verify_claim"
}
