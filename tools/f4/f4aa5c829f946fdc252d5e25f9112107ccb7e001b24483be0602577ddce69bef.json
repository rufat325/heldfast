{
 "annotations": {
  "cost": {
   "all": 2
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Poll a previously-submitted gap analysis job by ID. Use this when crawlgraph_gap_analysis or crawlgraph_gap_outreach_targets returned status=pending with a job_id \u2014 call this with the same job_id to retrieve the result once status=completed. Read-only; no quota cost.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-crawlgraph-api-key",
    "job_id": "gap_a1b2c3d4e5f6"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "CrawlGraph API key.",
    "type": "string"
   },
   "job_id": {
    "description": "The gap job ID (looks like \"gap_a1b2c3\").",
    "type": "string"
   }
  },
  "required": [
   "job_id",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "crawlgraph_gap_status",
 "title": "Crawlgraph Gap Status",
 "version": "v1.f6a8e849"
}
