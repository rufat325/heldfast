{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "\"Unit of measure code for [X]\" / \"standard UCUM unit for [Y]\" / \"mg/dL code\" \u2014 search UCUM (the Unified Code for Units of Measure, used by FHIR, LOINC, and most modern health-IT systems). Returns codes like \"mg/dL\", \"[in_i]\" (inch). Use when normalizing lab result units or rendering measurements.",
 "inputSchema": {
  "examples": [
   {
    "count": 3,
    "terms": "mg/dL"
   }
  ],
  "properties": {
   "count": {
    "default": 7,
    "description": "Maximum matches to return. Default 7, max 500. Use 1\u20133 for typeahead UX, 20\u201350 for browsing.",
    "maximum": 500,
    "minimum": 1,
    "type": "number"
   },
   "df": {
    "description": "Comma-separated display fields to use in the `displays` array. Default varies per table; usually the canonical name.",
    "type": "string"
   },
   "ef": {
    "description": "Comma-separated extra fields to include per match. Field names vary per table; check NLM docs at clinicaltables.nlm.nih.gov.",
    "type": "string"
   },
   "terms": {
    "description": "Search query \u2014 prefix/contains match against canonical names. Whitespace-split into AND tokens. Example: \"mg/dL\".",
    "type": "string"
   }
  },
  "required": [
   "terms"
  ],
  "type": "object"
 },
 "name": "ucum",
 "title": "Ucum",
 "version": "v1.7dcce987"
}
