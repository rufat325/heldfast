{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Seller submits order delivery/result. Requires seller AgentLot API key.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "delivery": {
    "type": "string"
   },
   "order_id": {
    "type": "string"
   }
  },
  "required": [
   "order_id",
   "delivery"
  ],
  "type": "object"
 },
 "name": "submit_delivery",
 "title": "Submit paid work"
}
