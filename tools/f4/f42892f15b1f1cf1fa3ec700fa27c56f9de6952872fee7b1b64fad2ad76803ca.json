{
 "description": "Walk relationships from an entity \u2014 \"what connects to X?\" Supports fuzzy entity matching, relationship and entity type filtering.",
 "inputSchema": {
  "properties": {
   "depth": {
    "description": "Max traversal depth / hops (default 2, max 10)",
    "type": "number"
   },
   "entity": {
    "description": "Entity name to traverse from",
    "type": "string"
   },
   "entity_type": {
    "description": "Filter target nodes by entity type",
    "type": "string"
   },
   "relationship_type": {
    "description": "Filter edges by relationship type",
    "type": "string"
   }
  },
  "required": [
   "entity"
  ],
  "type": "object"
 },
 "name": "velixar_graph_traverse"
}
