{
 "description": "Get debug information about the current Velixar MCP server state. Returns workspace config, cache state, API timings, retry counts, circuit breaker state.",
 "inputSchema": {
  "properties": {
   "verbose": {
    "description": "Set verbose logging on/off (omit to just get debug info)",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "velixar_debug"
}
