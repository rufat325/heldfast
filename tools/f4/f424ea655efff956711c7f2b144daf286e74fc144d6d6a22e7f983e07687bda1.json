{
 "description": "Return paste-ready SVG markup. Pass one name, or an array of names to get several in a single call \u2014 building a toolbar needs one request, not eight. The path uses fill=\"currentColor\", so it inherits the surrounding text colour. Licensed MIT \u2014 free to use, no attribution required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "name": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "maxItems": 24,
      "minItems": 1,
      "type": "array"
     }
    ],
    "description": "One icon name from search_icons, e.g. \"bell\", or several: [\"bell\", \"trash\"]"
   },
   "style": {
    "description": "Default stroke",
    "enum": [
     "stroke",
     "fill"
    ],
    "type": "string"
   },
   "weight": {
    "description": "Default regular",
    "enum": [
     "thin",
     "ultralight",
     "light",
     "regular",
     "medium",
     "semibold",
     "bold"
    ],
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "get_icon",
 "title": "Get an Iconoteka icon as SVG"
}
