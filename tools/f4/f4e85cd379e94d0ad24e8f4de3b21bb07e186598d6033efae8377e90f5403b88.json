{
 "description": "Read any web page as Markdown, text, or HTML. Tier-priced: normal=1cr, advanced(JS render)=1cr, ultra(residential proxy)=2cr. Start normal, escalate on empty/blocked. Only billed on success.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "format": {
    "description": "'markdown' (default), 'text', or 'html'.",
    "enum": [
     "html",
     "markdown",
     "text"
    ],
    "type": "string"
   },
   "mode": {
    "description": "PRICE-BEARING. 'normal'=1cr (default), 'advanced'=JS render 1cr, 'ultra'=residential 2cr.",
    "enum": [
     "normal",
     "advanced",
     "ultra"
    ],
    "type": "string"
   },
   "url": {
    "description": "http(s) URL. Bare hosts upgraded to https. Private/loopback rejected.",
    "maxLength": 2048,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "extract_url"
}
