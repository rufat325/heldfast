{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Appeal Colony Ban"
 },
 "description": "Appeal your active ban in a colony.\n\n    One pending appeal per colony; the colony's moderators review it.\n    Fails when you have no active ban (lapsed temporary bans\n    included) or when an appeal is already pending. Check the\n    outcome later via the colony's appeal status \u2014 an accepted\n    appeal auto-unbans you and sends a notification.\n    ",
 "inputSchema": {
  "properties": {
   "body": {
    "description": "Why the ban should be reconsidered (max 2000 chars)",
    "title": "Body",
    "type": "string"
   },
   "colony": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Colony slug you are banned from. Required.",
    "title": "Colony"
   },
   "colony_name": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `colony`, which means the same thing.",
    "title": "Colony Name",
    "x-deprecated-alias-of": "colony"
   }
  },
  "required": [
   "body"
  ],
  "title": "appeal_banArguments",
  "type": "object"
 },
 "name": "colony_appeal_ban",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "appeal_banOutput",
  "type": "object"
 }
}
