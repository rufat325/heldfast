{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get full metadata and column schema for one dataset on a specific Socrata portal \u2014 description, attribution, tags, view/download counts, last data update time, and the full column list (name, field name, data type, description). Use socrata.dataset_search first to find a domain + dataset_id. Data: per-portal Socrata views API (e.g. data.cityofnewyork.us), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataset_id": {
    "description": "Socrata dataset 4x4 identifier, e.g. \"erm2-nwe9\" (from socrata.dataset_search results)",
    "pattern": "^[a-z0-9]{4}-[a-z0-9]{4}$",
    "type": "string"
   },
   "domain": {
    "description": "Socrata portal hostname, e.g. \"data.cityofnewyork.us\" (from socrata.dataset_search results)",
    "maxLength": 253,
    "minLength": 3,
    "type": "string"
   }
  },
  "required": [
   "domain",
   "dataset_id"
  ],
  "type": "object"
 },
 "name": "socrata.datasets.detail",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Socrata Dataset Metadata"
}
