{
 "description": "Raise an operational ticket. WORLD-AFFECTING: this is gated on a human approval and will return APPROVAL_REQUIRED rather than executing.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "approval_id": {
    "description": "after a person approved this exact call, the id of that approval; the call then runs once",
    "type": "string"
   },
   "description": {
    "type": "string"
   },
   "tenant_slug": {
    "type": "string"
   },
   "title": {
    "type": "string"
   }
  },
  "required": [
   "tenant_slug",
   "title"
  ],
  "type": "object"
 },
 "name": "flocore_create_ticket"
}
