{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Run sandboxed code"
 },
 "best_for": [
  "coding agents (Claude / Cursor / Cline)",
  "trading bots needing backtests",
  "research agents",
  "any agent that occasionally needs more power than local"
 ],
 "description": "Paid secure Python execution that serious agents actually use. Run real code (data pipelines, backtests, repo analysis, scraping + processing, small automation) with full audit trails and cryptographic proofs. Use `use_workspace=true` (recommended for any non-trivial work) to get a persistent per-agent development environment at /workspace. Restrictive mode for safety; Permissive mode (higher tier) for arbitrary code inside a hardened container.",
 "execution_layer": true,
 "inputSchema": {
  "examples": [
   {
    "code": "print(sum(range(100)))",
    "language": "python",
    "tier": 1,
    "timeout_seconds": 2
   }
  ],
  "properties": {
   "agent_id": {
    "description": "Optional caller agent ID",
    "type": "string"
   },
   "code": {
    "maxLength": 6000,
    "type": "string"
   },
   "language": {
    "default": "python",
    "enum": [
     "python"
    ],
    "type": "string"
   },
   "permissive": {
    "default": false,
    "description": "Allow arbitrary imports and code (container is the security boundary). Priced higher.",
    "type": "boolean"
   },
   "stdin": {
    "default": "",
    "type": "string"
   },
   "tier": {
    "default": 1,
    "maximum": 3,
    "minimum": 0,
    "type": "integer"
   },
   "timeout_seconds": {
    "default": 60,
    "maximum": 600,
    "minimum": 0.5,
    "type": "number"
   },
   "use_workspace": {
    "default": false,
    "description": "Mount a persistent per-agent workspace at /workspace (rw). Files, git clones, and installed packages survive across calls. Strongly recommended for any non-trivial multi-step work.",
    "type": "boolean"
   }
  },
  "required": [
   "code"
  ],
  "type": "object"
 },
 "name": "execute",
 "pricing": "Tiered from 700 sats per job (Permissive mode \u00d73)",
 "supported_payments": [
  "Bearer Token (credits)"
 ]
}
