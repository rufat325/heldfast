{
 "description": "Preview or write one user-confirmed MCP-owned non-secret Granoflow API URL or local port. Defaults to dry-run; writes are reread and verified immediately.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "apiBaseUrl": {
    "format": "uri",
    "type": "string"
   },
   "apiPort": {
    "maximum": 65535,
    "minimum": 1,
    "type": "integer"
   },
   "dryRun": {
    "default": true,
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "granoflow_setup_write_config"
}
