{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search OpenStreetMap (OSM) for amenities of a specific type within a geographic bounding box. Returns name, coordinates, address, phone, website, and opening hours for each result. Covers restaurants, cafes, bars, banks, ATMs, pharmacies, hospitals, schools, universities, fuel stations, parking lots, hotels, supermarkets, post offices, police stations, cinemas, museums, and more. Data sourced from the global OpenStreetMap database via Overpass API \u2014 no authentication required. Ideal for finding local services near a point of interest.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "amenity_type": {
    "description": "OSM amenity type to search for (e.g. \"restaurant\", \"cafe\", \"atm\", \"hospital\", \"pharmacy\", \"school\").",
    "enum": [
     "restaurant",
     "cafe",
     "bar",
     "pub",
     "fast_food",
     "bank",
     "atm",
     "pharmacy",
     "hospital",
     "clinic",
     "doctors",
     "dentist",
     "school",
     "university",
     "library",
     "fuel",
     "parking",
     "hotel",
     "supermarket",
     "marketplace",
     "post_office",
     "police",
     "fire_station",
     "cinema",
     "theatre",
     "museum",
     "place_of_worship",
     "toilets",
     "charging_station"
    ],
    "type": "string"
   },
   "lat_max": {
    "description": "Northern boundary latitude of the search bounding box (e.g. 51.52).",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "lat_min": {
    "description": "Southern boundary latitude of the search bounding box (e.g. 51.50).",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "limit": {
    "description": "Maximum number of results to return (1\u201350, default 20).",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "lon_max": {
    "description": "Eastern boundary longitude of the search bounding box (e.g. -0.10).",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "lon_min": {
    "description": "Western boundary longitude of the search bounding box (e.g. -0.13).",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   }
  },
  "required": [
   "amenity_type",
   "lat_min",
   "lon_min",
   "lat_max",
   "lon_max"
  ],
  "type": "object"
 },
 "name": "overpass.search.amenities",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Find OSM Amenities in Bounding Box"
}
