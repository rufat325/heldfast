{
 "description": "Rule-2 insider-buy signals filed within an inclusive ISO date range. Returns the newest 25 by default (set `limit`; page with `offset`; optional `days`/`ticker`); `total_matched` reports the full count. Each record carries its source filing_url.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "days": {
    "description": "Only rows filed within the last N days (relative to today).",
    "type": "integer"
   },
   "end": {
    "description": "Inclusive end date, ISO YYYY-MM-DD",
    "format": "date",
    "type": "string"
   },
   "limit": {
    "description": "Max rows to return (default 25, newest first). Page with offset; the response's total_matched shows the full count. limit<=0 = no cap.",
    "type": "integer"
   },
   "offset": {
    "description": "Rows to skip before this page (pagination). Default 0.",
    "type": "integer"
   },
   "start": {
    "description": "Inclusive start date, ISO YYYY-MM-DD",
    "format": "date",
    "type": "string"
   },
   "ticker": {
    "description": "Case-insensitive exact ticker filter, e.g. DKS.",
    "type": "string"
   }
  },
  "required": [
   "start",
   "end"
  ],
  "type": "object"
 },
 "name": "insider_signals_by_date_range",
 "title": "Insider-buy signals in a date range"
}
