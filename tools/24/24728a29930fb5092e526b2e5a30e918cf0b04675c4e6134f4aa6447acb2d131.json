{
 "description": "Reload the connected app and verify a fresh runtime. Uses Page.reload, with a directed Metro message fallback only for a verified app/device peer.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "timeout": {
    "default": 15000,
    "description": "Maximum time in milliseconds to submit and verify the reload",
    "maximum": 60000,
    "minimum": 100,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "reload_app"
}
