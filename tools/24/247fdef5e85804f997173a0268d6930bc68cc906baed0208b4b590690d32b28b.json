{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "List all platform invites for a brand, ordered by creation date (newest first). Returns both active and expired/used invites. Use is_active to filter in the response.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "brand_id": {
    "description": "Brand ID to list invites for",
    "type": "string"
   }
  },
  "required": [
   "brand_id"
  ],
  "type": "object"
 },
 "name": "social_api_list_invites",
 "title": "List Invites"
}
