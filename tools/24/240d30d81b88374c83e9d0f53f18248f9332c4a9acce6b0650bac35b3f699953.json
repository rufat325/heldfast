{
 "description": "Propose a cache invalidation for review. Filter shape depends on cache type: semantic_cache requires filter_kind=valkey_search + filter_expression; agent_cache requires filter_kind in (tool|key_prefix|session) + filter_value. Warns when estimated_affected exceeds 10000.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "cache_name": {
    "description": "Name of the cache to invalidate",
    "minLength": 1,
    "type": "string"
   },
   "estimated_affected": {
    "description": "Caller-estimated number of affected entries",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "filter_expression": {
    "description": "Required when filter_kind=valkey_search; FT.SEARCH filter",
    "minLength": 1,
    "type": "string"
   },
   "filter_kind": {
    "description": "Discriminator: valkey_search for semantic_cache; tool|key_prefix|session for agent_cache",
    "enum": [
     "valkey_search",
     "tool",
     "key_prefix",
     "session"
    ],
    "type": "string"
   },
   "filter_value": {
    "description": "Required when filter_kind in (tool|key_prefix|session); the matching value",
    "minLength": 1,
    "type": "string"
   },
   "instanceId": {
    "description": "Connection ID; defaults to the active instance",
    "pattern": "^[a-zA-Z0-9_-]+$",
    "type": "string"
   },
   "reasoning": {
    "description": "Why the invalidation is being proposed (\u226520 chars)",
    "minLength": 20,
    "type": "string"
   }
  },
  "required": [
   "cache_name",
   "filter_kind",
   "estimated_affected",
   "reasoning"
  ],
  "type": "object"
 },
 "name": "cache_propose_invalidate"
}
