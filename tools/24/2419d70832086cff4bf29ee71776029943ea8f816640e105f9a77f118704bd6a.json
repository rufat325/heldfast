{
 "annotations": {
  "readOnlyHint": true,
  "title": "Find event channels"
 },
 "description": "AsyncAPI event channels across the catalog. Individual event channels (topics/streams). Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
 "inputSchema": {
  "properties": {
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "include": {
    "items": {
     "enum": [
      "content"
     ]
    },
    "type": "array"
   },
   "limit": {
    "default": 25,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "match": {
    "default": "any",
    "enum": [
     "any",
     "all"
    ],
    "type": "string"
   },
   "page": {
    "default": 1,
    "minimum": 1,
    "type": "integer"
   },
   "protocol": {
    "description": "Transport the channel runs over: ws, http, sse, kafka, mqtt, amqp, nats, grpc\u2026 Empty where the document never says, which is most of them.",
    "type": "string"
   },
   "providers": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "q": {
    "description": "Free text over name + description.",
    "type": "string"
   },
   "sort": {
    "description": "Order: relevance (catalog order), provider, or name.",
    "enum": [
     "relevance",
     "provider",
     "name"
    ],
    "type": "string"
   },
   "tags": {
    "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or find_providers?api_tags= to reach companies whose APIs merely touch it.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "version": {
    "description": "AsyncAPI spec version the channel is defined in, e.g. 2.6.0 or 3.0.0.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "find_channels"
}
