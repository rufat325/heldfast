{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Write one or more arbitrary cells on a sheet in a single atomic transaction (one undo step). Each cell is addressed by zero-based row/col.\nContent is a literal (string / number / boolean) or a formula prefixed with '='. null or '' clears the cell.\nFor cells that belong to a block, prefer edit set_block_cells (it respects the schema); use this for plain, non-block cells.\nAt most 200 cells per call.",
 "inputSchema": {
  "properties": {
   "cells": {
    "items": {
     "properties": {
      "col": {
       "description": "Zero-based column.",
       "type": "integer"
      },
      "content": {
       "description": "Literal value, or a formula starting with '='. null clears.",
       "type": [
        "string",
        "number",
        "boolean",
        "null"
       ]
      },
      "row": {
       "description": "Zero-based row.",
       "type": "integer"
      }
     },
     "required": [
      "row",
      "col",
      "content"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "sheetIdx": {
    "description": "Zero-based sheet index.",
    "type": "integer"
   }
  },
  "required": [
   "sheetIdx",
   "cells"
  ],
  "type": "object"
 },
 "name": "set_cells"
}
