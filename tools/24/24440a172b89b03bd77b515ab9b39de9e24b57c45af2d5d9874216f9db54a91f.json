{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Research an operating company, ETF, mutual fund or market index and write a manager-style decision summary. The instrument is classified before evidence routing: funds use holdings look-through, indices use aggregate methodology, and Company Facts screens apply only to operating companies. Set council_mode=quick for the bounded news-inclusive quick_v1 path; the default remains the full council.",
 "inputSchema": {
  "properties": {
   "as_of": {
    "description": "Analysis date YYYY-MM-DD. Defaults to today.",
    "type": "string"
   },
   "council_mode": {
    "default": "full",
    "description": "full runs the analyst scope frozen in the receipt (core=8, all=11) and a three-round cross-exam. quick runs the fixed news-inclusive four-role preset, up to four methods, one parallel bull/bear round, a short PM, and a hard global budget.",
    "enum": [
     "full",
     "quick"
    ],
    "type": "string"
   },
   "council_pace": {
    "description": "Full-council depth/time tier. fast = 15 minutes, normal = 30, slow = 60. The tier sets the total budget AND every per-stage cap together: fast gives each evidence seat 4 minutes and each debate side 85 seconds; normal gives 6 and 3 minutes; slow gives 12 and 6 minutes. These are configured ceilings, not measured completion times. Raising total_timeout_ms alone buys idle time, not depth. Quick rejects this field.",
    "enum": [
     "fast",
     "normal",
     "slow"
    ],
    "type": "string"
   },
   "dry_run": {
    "default": false,
    "description": "Default false. Set true only for planning/self-tests without launching Codex subagents.",
    "type": "boolean"
   },
   "language": {
    "default": "auto",
    "description": "Reader-facing language for subagents and final report, e.g. auto, zh-CN, en-US, ja-JP. Auto infers from prompt.",
    "type": "string"
   },
   "max_concurrency": {
    "default": 8,
    "type": "number"
   },
   "output_mode": {
    "default": "public_equity",
    "description": "Final synthesis target shape.",
    "enum": [
     "chat",
     "documents",
     "pdf",
     "presentations",
     "data_analytics",
     "public_equity",
     "investment_banking"
    ],
    "type": "string"
   },
   "prompt": {
    "description": "User objective or extra instructions.",
    "type": "string"
   },
   "seat_weights": {
    "description": "Deprecated compatibility metadata. It may be persisted for audit but does not alter the PM rating rubric, count method votes, or override objective/horizon contribution classes.",
    "type": "object"
   },
   "selection_receipt": {
    "description": "One-run receipt returned by confirm_master_selection. Required for every council run and consumed exactly once.",
    "type": "string"
   },
   "symbol": {
    "description": "Exchange ticker. US, HK, JP, KR, CN and TW symbols all work, e.g. AAPL, 0700.HK, 7203.T, 005930.KS, 600519.SS.",
    "type": "string"
   },
   "synthesis": {
    "default": true,
    "description": "Run bull, bear, and portfolio-manager synthesis after evidence collection.",
    "type": "boolean"
   },
   "synthesis_timeout_ms": {
    "description": "Optional legacy synthesis ceiling. Omit it to use the selected council pace's debate and portfolio-manager caps.",
    "type": "number"
   },
   "tasks": {
    "items": {
     "enum": [
      "macro_regime",
      "market_data",
      "earnings_deep_dive",
      "forward_expectations",
      "quant_factor",
      "valuation_long_short",
      "news_industry_management",
      "market_narrative",
      "social_pulse",
      "insider_sec",
      "ib_event_analysis"
     ],
     "type": "string"
    },
    "type": "array"
   },
   "timeout_ms": {
    "description": "Optional legacy per-worker ceiling. Omit it to use the selected council pace's stage caps; callers may only use it to lower those caps.",
    "type": "number"
   },
   "total_timeout_ms": {
    "description": "Hard queue-to-persistence wall-clock budget. Cannot exceed the selected council_pace's total (fast 900000, normal 1800000, slow 3600000) or ten minutes for quick. Callers may lower the applicable ceiling, never raise it.",
    "maximum": 3600000,
    "type": "number"
   },
   "visibility_required": {
    "default": false,
    "description": "When true, headless MCP execution is rejected; use host-visible agents/threads and record their outputs.",
    "type": "boolean"
   },
   "wait_for_completion": {
    "default": false,
    "description": "Default false for real runs: return a run_id immediately and poll read_run. Set true only when the MCP client can remain connected for the entire council. Dry runs remain synchronous when this field is omitted.",
    "type": "boolean"
   }
  },
  "required": [
   "symbol",
   "selection_receipt"
  ],
  "type": "object"
 },
 "name": "analyze_symbol",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 },
 "title": "Start a Stock Research Council"
}
