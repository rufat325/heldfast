{
 "description": "What other wallets have said about an Arc agent on chain: every ERC-8004 reputation entry for it (who gave it, the value, the tags, whether it was revoked) and the registry's own summary. Includes the weekly uptime records the APEX Watchtower writes. Free, 200 calls a day.",
 "inputSchema": {
  "properties": {
   "agentId": {
    "description": "the agent id in Arc's ERC-8004 registry",
    "type": "integer"
   }
  },
  "required": [
   "agentId"
  ],
  "type": "object"
 },
 "name": "arc_agent_reputation"
}
