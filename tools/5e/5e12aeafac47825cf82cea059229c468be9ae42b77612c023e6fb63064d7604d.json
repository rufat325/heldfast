{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Convert kanji-mixed Japanese text to hiragana, katakana, or romaji, with a per-token reading breakdown.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "text": {
    "description": "Japanese text to read.",
    "type": "string"
   },
   "to": {
    "description": "Target script. Default hiragana.",
    "enum": [
     "hiragana",
     "katakana",
     "romaji"
    ],
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "furigana_convert"
}
