{
 "description": "User profile \u2014 preferences, expertise, communication style, recurring goals, stable constraints. Supports get (default), store, update, delete, list, and history actions. Workspace-scoped.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Action (default: get)",
    "enum": [
     "get",
     "store",
     "update",
     "delete",
     "list",
     "history"
    ],
    "type": "string"
   },
   "field": {
    "description": "Identity field to store/update/delete/history (e.g. \"expertise\", \"communication_style\")",
    "type": "string"
   },
   "value": {
    "description": "Value to store/update (string or array of strings)"
   }
  },
  "type": "object"
 },
 "name": "velixar_identity"
}
