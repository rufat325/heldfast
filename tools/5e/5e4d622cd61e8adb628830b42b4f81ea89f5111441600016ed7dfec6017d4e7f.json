{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch up to 1000 rows from a CDC Socrata dataset by its four-by-four dataset ID (e.g., \"9mfq-cb36\"). Returns row array with all columns plus column names extracted from the first row. Use search_datasets first to find the dataset ID.",
 "inputSchema": {
  "examples": [
   {
    "id": "9mfq-cb36"
   },
   {
    "id": "r8kz-9hhg",
    "limit": 500
   }
  ],
  "properties": {
   "id": {
    "description": "Socrata dataset ID in four-by-four format (e.g., \"9mfq-cb36\")",
    "type": "string"
   },
   "limit": {
    "description": "Number of rows to return (default 50, max 1000)",
    "type": "number"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_dataset",
 "outputSchema": {
  "properties": {
   "columns": {
    "description": "Column names from the dataset",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "dataset_id": {
    "description": "Socrata dataset ID",
    "type": "string"
   },
   "row_count": {
    "description": "Number of rows returned",
    "type": "number"
   },
   "rows": {
    "description": "Data rows with all columns as key-value pairs",
    "items": {
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "dataset_id",
   "row_count",
   "columns",
   "rows"
  ],
  "type": "object"
 },
 "title": "Get Dataset",
 "version": "v1.51b4db87"
}
