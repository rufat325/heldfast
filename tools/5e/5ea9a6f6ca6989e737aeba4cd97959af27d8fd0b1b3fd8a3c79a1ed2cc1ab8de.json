{
 "description": "[WRITE] Create a GTM variable by KIND rather than by GTM's internal type code, so the right shape is guaranteed. kind: \"constant\" (a fixed reused value, e.g. a Measurement ID); \"data_layer\" (read a key the SITE pushes to the dataLayer, dot-notation for nested, e.g. ecommerce.value) - only correct when the site actually pushes that key; \"javascript\" (Custom JavaScript: a function(){...} that COMPUTES a value, and the right choice for deriving something from the page or the click, e.g. extracting the address out of a mailto: {{Click URL}} - a data_layer variable CANNOT do this and would report blank); \"event_data\" and \"request_header\" (SERVER containers only). Requires GTM_MCP_ENABLE_WRITES=true and confirm=true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "description": "The GTM account ID.",
    "type": "string"
   },
   "confirm": {
    "description": "Must be true to confirm this write operation.",
    "type": "boolean"
   },
   "containerId": {
    "description": "The GTM container ID.",
    "type": "string"
   },
   "dataLayerName": {
    "description": "For kind \"data_layer\": the dataLayer key, e.g. \"ecommerce.value\".",
    "type": "string"
   },
   "defaultValue": {
    "description": "For kind \"event_data\": value when the key is absent.",
    "type": "string"
   },
   "headerName": {
    "description": "For kind \"request_header\" (server): the HTTP header to read.",
    "type": "string"
   },
   "javascript": {
    "description": "For kind \"javascript\": the full function, e.g. \"function() { var u = {{Click URL}} || ''; return u.indexOf('mailto:') === 0 ? u.replace('mailto:', '').split('?')[0] : undefined; }\". Any {{variable}} it reads must be enabled.",
    "type": "string"
   },
   "keyPath": {
    "description": "For kind \"event_data\" (server): the event-data key to read.",
    "type": "string"
   },
   "kind": {
    "description": "constant | data_layer | javascript | event_data | request_header",
    "type": "string"
   },
   "name": {
    "description": "Variable name as it will be referenced, e.g. \"JS - Email Address\".",
    "type": "string"
   },
   "value": {
    "description": "For kind \"constant\": the fixed value.",
    "type": "string"
   },
   "workspaceId": {
    "description": "The GTM workspace ID.",
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "containerId",
   "workspaceId",
   "name",
   "kind",
   "confirm"
  ],
  "type": "object"
 },
 "name": "create_gtm_variable_typed"
}
