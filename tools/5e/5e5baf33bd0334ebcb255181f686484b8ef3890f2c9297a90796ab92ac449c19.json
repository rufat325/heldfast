{
 "description": "Add a text block (Tiptap JSON, type 'doc'). sortOrder defaults to end-of-document. New blocks appear live for any open editor \u2014 the Yjs propagation issue only affects update_block on existing blocks.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "content": {
    "description": "Tiptap JSON content object with type 'doc'"
   },
   "documentId": {
    "description": "The document ID",
    "type": "string"
   },
   "sortOrder": {
    "description": "Position (0-based). Omit to append at the end.",
    "type": "number"
   }
  },
  "required": [
   "documentId"
  ],
  "type": "object"
 },
 "name": "add_text_block"
}
