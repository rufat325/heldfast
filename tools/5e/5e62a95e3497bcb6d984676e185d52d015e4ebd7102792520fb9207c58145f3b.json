{
 "description": "Systematic bug triage (Grok 4.3): RANKED root-cause hypotheses with likelihoods, the cheapest discriminating check for each, and the minimal fix for the top candidate. Provide the error/stack trace in 'error'.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "code": {
    "description": "Relevant code (or use 'files')",
    "type": "string"
   },
   "context": {
    "description": "Repro steps, recent changes, frequency (always/intermittent)",
    "type": "string"
   },
   "error": {
    "description": "The error message or stack trace \u2014 REQUIRED",
    "type": "string"
   },
   "files": {
    "description": "File paths to read server-side. Supports line ranges: 'src/foo.ts:100-200'.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "runtime": {
    "description": "Runtime/environment (e.g. 'node 22', 'python 3.12/django', browser)",
    "type": "string"
   }
  },
  "required": [
   "error"
  ],
  "type": "object"
 },
 "name": "debug_triage"
}
