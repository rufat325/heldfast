{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Lists actual places \u2014 cafes, shops, museums, pharmacies, parks \u2014 that can be reached from a point within a time budget, each with how long it takes to get there. Filter by group (food, shops, culture, health, learning, outdoors, money) or by specific kinds such as 'cafe' or 'pharmacy'. Called without group or kinds it returns a breakdown by group rather than an arbitrary mixture, so ask the user which they want. Use this for 'what can I get to', and reachable_area for 'how far can I get'. Currently covers Berlin, Germany.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "group": {
    "description": "Restrict to one group: food, shops, culture, health, learning, outdoors, money",
    "type": "string"
   },
   "kinds": {
    "description": "Restrict to specific kinds, e.g. ['cafe','bakery']. Takes precedence over group.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "lat": {
    "description": "Origin latitude (WGS84)",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "limit": {
    "default": 15,
    "description": "How many to list, nearest first",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "lon": {
    "description": "Origin longitude (WGS84)",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "minutes": {
    "description": "Time budget in minutes. Omit for the profile's default.",
    "maximum": 25,
    "minimum": 1,
    "type": "integer"
   },
   "profile": {
    "default": "walk",
    "description": "Mobility profile",
    "enum": [
     "walk",
     "stroller",
     "wheelchair",
     "bike"
    ],
    "type": "string"
   }
  },
  "required": [
   "lat",
   "lon"
  ],
  "type": "object"
 },
 "name": "places_nearby",
 "title": "Places reachable from a point"
}
