{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Retrieve the bibliography (reference list) of a biomedical article from Europe PMC. Provide the article PMID (for PubMed/MEDLINE) or other source-specific ID. Returns the total reference count and bibliographic details for each reference (title, authors, journal abbreviation, year, PMID, DOI where available). Paginate through all references with page_size and page parameters. Useful for backward citation tracking, understanding research foundations, identifying cited authors, and building citation networks.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Numeric PMID (for MED/PubMed) or other source-specific ID of the article whose bibliography you want to retrieve (e.g. \"30449648\").",
    "minLength": 1,
    "type": "string"
   },
   "page": {
    "description": "Page number for pagination when a paper has many references (default 1).",
    "minimum": 1,
    "type": "integer"
   },
   "page_size": {
    "description": "Number of references to return per page (1\u20131000, default 10).",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "src": {
    "description": "Source database of the target article. \"MED\" = PubMed (default). \"PMC\" = PubMed Central full-text. Use the same src you used to find the article.",
    "enum": [
     "MED",
     "PMC",
     "PPR",
     "PAT",
     "NBK",
     "ETH",
     "HIR",
     "CTX"
    ],
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "europepmc.literature.references",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Get Article References"
}
