{
 "description": "Create a new note in the vault with a title, body, and optional YAML frontmatter. The new note is indexed immediately so semantic search and graph tools can find it. Auto-injects a `title:` field into frontmatter matching the note title unless frontmatter already has one.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "content": {
    "description": "Markdown body (do not include frontmatter here).",
    "type": "string"
   },
   "directory": {
    "description": "Vault-relative subdirectory to create the note in.",
    "type": "string"
   },
   "frontmatter": {
    "additionalProperties": {},
    "description": "YAML frontmatter key/value map. `title` is auto-injected unless explicitly set.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "title": {
    "description": "Note title. Used as the filename base and auto-injected into frontmatter.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "title",
   "content"
  ],
  "type": "object"
 },
 "name": "create_note"
}
