{
 "description": "Timeline of the listing's repository: last events (abandono = no push for 12 months, sumiu, renomeado, readme_mudou, manifesto_mudou), newest first; empty when nothing changed since the series began. The listing page carries the same list as `historico`; updates may be cached for up to 15 minutes. An empty list means no change since the series began \u2014 the listing still exists. Stale means no push for 12 months. Reading this endpoint does not refresh the record.",
 "inputSchema": {
  "properties": {
   "api_pass": {
    "description": "MAT-only private pass: mat_<32 random hex>_<64 random hex>. Generate and save before buying.",
    "type": "string"
   },
   "id": {
    "description": "Listing ID, from `Anuncio.id`.",
    "type": "string"
   },
   "retry_key": {
    "description": "Optional retry key, up to 80 ASCII characters; same read for five minutes.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "listing_history"
}
