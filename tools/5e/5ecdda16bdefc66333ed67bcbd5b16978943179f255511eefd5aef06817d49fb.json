{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Compare or rank US banks on a call report figure \u2014 largest by assets, most deposits, highest net income \u2014 optionally within one state. Answers \"largest banks in Ohio\", \"compare JPMorgan and Bank of America\", \"which banks have the most deposits\". Pass `names`/`certs` to compare a specific set, or omit them to rank the whole industry. Amounts are in whole US dollars, normalised from the thousands banks file in, so figures line up with the ncua pack for credit unions. Example: ffiec_compare_banks({ metric: \"total_assets\", state: \"OH\", limit: 10 }); ffiec_compare_banks({ names: [\"jpmorgan chase bank\", \"bank of america\"] }). Keyless.",
 "inputSchema": {
  "examples": [
   {
    "limit": 10,
    "metric": "total_assets",
    "state": "OH"
   },
   {
    "names": [
     "jpmorgan chase bank",
     "bank of america"
    ]
   }
  ],
  "properties": {
   "certs": {
    "description": "Specific banks to compare, by FDIC certificate number",
    "items": {
     "type": "number"
    },
    "type": "array"
   },
   "limit": {
    "description": "Max results when ranking (default 20, max 100)",
    "type": "number"
   },
   "metric": {
    "description": "total_assets (default), total_loans, total_deposits, total_equity, net_income, past_due_90, or a raw MDRM code",
    "type": "string"
   },
   "names": {
    "description": "Specific banks to compare, by name",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "quarter": {
    "description": "Quarter end as YYYY-MM-DD; defaults to latest",
    "type": "string"
   },
   "state": {
    "description": "Restrict a ranking to one state",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ffiec_compare_banks",
 "title": "Ffiec Compare Banks",
 "version": "v1.ab21de34"
}
