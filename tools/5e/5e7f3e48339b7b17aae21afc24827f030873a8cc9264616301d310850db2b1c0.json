{
 "description": "List recent builds with processing state, TestFlight internal/external status. Shows which builds are ready for release.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "appId": {
    "description": "App ID",
    "type": "string"
   },
   "limit": {
    "default": 10,
    "description": "Number of builds to fetch",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   }
  },
  "required": [
   "appId"
  ],
  "type": "object"
 },
 "name": "asc_list_builds"
}
