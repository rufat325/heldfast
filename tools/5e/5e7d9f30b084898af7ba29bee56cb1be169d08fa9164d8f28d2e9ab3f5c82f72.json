{
 "description": "Search for tokens by symbol or name across all monitored exchanges. Requires: query.",
 "inputSchema": {
  "properties": {
   "exchange": {
    "description": "Filter by exchange (optional)",
    "type": "string"
   },
   "query": {
    "description": "Search query (e.g. BTC, ETH, DOGE)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_tokens"
}
