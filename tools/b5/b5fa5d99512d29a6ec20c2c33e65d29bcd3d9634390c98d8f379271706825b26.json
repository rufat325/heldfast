{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Restart every application in a project (optionally a single environment). Asks for confirmation on clients that support elicitation.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "environment": {
    "description": "Environment name or UUID to restrict to",
    "type": "string"
   },
   "project_uuid": {
    "type": "string"
   }
  },
  "required": [
   "project_uuid"
  ],
  "type": "object"
 },
 "name": "restartProjectApps",
 "title": "Restart all project applications"
}
