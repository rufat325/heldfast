{
 "_meta": {
  "securitySchemes": [
   {
    "scopes": [
     "watch:manage"
    ],
    "type": "oauth2"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Track a fashion product in Remode"
 },
 "description": "Persistently add a specific fashion product URL to the connected user's Remode watch list for lower-price or second-hand monitoring. Exact item only: the watch stays on this product and never widens to similar items. Optional say-when terms: a price finish line (max_price), a condition, and a size that Remode applies only when the store's own per-size stock record can be read. When the supplied product is explicitly marked in_stock=false, the same watch also detects a restock; never infer stock state. Use when the user explicitly asks to watch or track a fashion item or product link. This stores product-watch state inside Remode; it is not a ChatGPT Scheduled task, calendar reminder, or one-time notification, so do not schedule it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "color": {
    "description": "The exact item's colour as the store names it. When the store sells the item in several colours, a size is applied only together with one of the store's own colour labels; on a single-colour product page the size is applied without a colour.",
    "maxLength": 40,
    "minLength": 1,
    "type": "string"
   },
   "condition": {
    "description": "Say-when condition for this exact item: new (new listings only; second-hand listings never count), pre_owned (second hand), or any (both). Use instead of preferences.",
    "enum": [
     "new",
     "pre_owned",
     "any"
    ],
    "type": "string"
   },
   "image_url": {
    "format": "uri",
    "maxLength": 4096,
    "type": "string"
   },
   "in_stock": {
    "type": "boolean"
   },
   "max_price": {
    "description": "Price finish line for this exact item: alert only at or below this price, in this currency. Must be the item's own currency; Remode never converts currencies. Requires condition new or any.",
    "properties": {
     "amount": {
      "exclusiveMinimum": 0,
      "maximum": 10000000,
      "type": "number"
     },
     "currency": {
      "description": "ISO currency code, such as CHF, EUR, USD.",
      "pattern": "^[A-Za-z]{3}$",
      "type": "string"
     }
    },
    "required": [
     "amount",
     "currency"
    ],
    "type": "object"
   },
   "preferences": {
    "description": "What Remode should look for. Defaults to lower price and second hand.",
    "items": {
     "enum": [
      "lower_price",
      "second_hand"
     ],
     "type": "string"
    },
    "maxItems": 2,
    "minItems": 1,
    "type": "array"
   },
   "price": {
    "description": "Visible current price including currency when known.",
    "maxLength": 40,
    "type": "string"
   },
   "region": {
    "description": "Shopping country code, such as US, CH, DE, or GB.",
    "maxLength": 8,
    "minLength": 2,
    "type": "string"
   },
   "size": {
    "description": "The shopper's size as the store labels it. Remode applies it only when it can read the store's own per-size stock; otherwise the watch continues for the exact item without a size and the result says so.",
    "maxLength": 24,
    "minLength": 1,
    "type": "string"
   },
   "title": {
    "maxLength": 200,
    "minLength": 1,
    "type": "string"
   },
   "url": {
    "description": "Retailer product URL from a Remode result.",
    "format": "uri",
    "maxLength": 4096,
    "type": "string"
   },
   "use_saved_size": {
    "description": "Use the shopper's saved usual size for this category as the size, when size is not given. Read-only; never changes the saved sizes.",
    "enum": [
     "tops",
     "bottoms",
     "shoes"
    ],
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "watch_item",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "ok": {
    "type": "boolean"
   },
   "preferences": {
    "items": {
     "enum": [
      "lower_price",
      "second_hand"
     ],
     "type": "string"
    },
    "type": "array"
   },
   "price_target": {
    "additionalProperties": false,
    "properties": {
     "amount": {
      "type": "number"
     },
     "currency": {
      "type": "string"
     }
    },
    "required": [
     "amount",
     "currency"
    ],
    "type": "object"
   },
   "region": {
    "type": "string"
   },
   "size": {
    "additionalProperties": false,
    "properties": {
     "applied": {
      "type": "boolean"
     },
     "color_options": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "evidence": {
      "type": "string"
     },
     "origin": {
      "enum": [
       "request",
       "profile"
      ],
      "type": "string"
     },
     "reason": {
      "type": "string"
     },
     "requested": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ]
     },
     "store_label": {
      "type": "string"
     }
    },
    "required": [
     "requested",
     "origin",
     "applied"
    ],
    "type": "object"
   },
   "watch_id": {
    "type": "string"
   },
   "watched": {
    "type": "boolean"
   }
  },
  "required": [
   "ok",
   "watched",
   "watch_id",
   "region",
   "preferences"
  ],
  "type": "object"
 },
 "title": "Track a fashion product in Remode"
}
