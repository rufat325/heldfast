{
 "description": "Latest FX reference rates (ECB) for ~30 currencies with optional amount conversion. Paid: $0.005 via x402.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "amount": {
    "description": "Optional amount to convert",
    "type": "number"
   },
   "base": {
    "description": "Base currency code (default USD)",
    "type": "string"
   },
   "symbols": {
    "description": "Comma-separated targets, e.g. ZAR,EUR",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "fx_rates"
}
