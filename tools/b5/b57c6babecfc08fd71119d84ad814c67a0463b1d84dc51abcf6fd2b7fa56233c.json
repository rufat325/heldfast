{
 "description": "Autonomous regulatory attestation for agent transactions. Checks action against MiCA, EU AI Act, and US EO compliance rules. Returns attestation hash and compliance score. Cost: $0.01 USDC per check.",
 "inputSchema": {
  "properties": {
   "action_type": {
    "description": "Type of agent action (transfer, inference, governance, trade)",
    "type": "string"
   },
   "amount_usd": {
    "description": "Transaction value in USD if applicable",
    "type": "number"
   },
   "jurisdiction": {
    "default": "GLOBAL",
    "description": "Regulatory jurisdiction (EU, US, GLOBAL)",
    "type": "string"
   }
  },
  "required": [
   "action_type"
  ],
  "type": "object"
 },
 "name": "logicnodes_compliance_sentry"
}
