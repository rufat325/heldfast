{
 "annotations": {
  "cost": {
   "all": 15
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List ad groups within a campaign by campaign ID. Returns ad group names, IDs, statuses, and CPC bids. Use to explore campaign structure or select an ad group for analysis.",
 "inputSchema": {
  "examples": [
   {
    "campaign_id": "9876543210",
    "customer_id": "1234567890"
   },
   {
    "campaign_id": "9876543210",
    "customer_id": "1234567890",
    "limit": 30,
    "status": "ENABLED"
   }
  ],
  "properties": {
   "campaign_id": {
    "description": "Campaign ID to list ad groups for",
    "type": "string"
   },
   "customer_id": {
    "description": "Google Ads customer ID",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of ad groups to return (default 50)",
    "type": "number"
   },
   "status": {
    "description": "Filter by ad group status (optional)",
    "enum": [
     "ENABLED",
     "PAUSED",
     "REMOVED"
    ],
    "type": "string"
   }
  },
  "required": [
   "customer_id",
   "campaign_id"
  ],
  "type": "object"
 },
 "name": "gads_list_ad_groups",
 "outputSchema": {
  "properties": {
   "results": {
    "description": "List of ad groups in campaign",
    "items": {
     "properties": {
      "ad_group": {
       "description": "Ad group resource data",
       "properties": {
        "cpc_bid_micros": {
         "description": "CPC bid in micros",
         "type": "number"
        },
        "id": {
         "description": "Ad group ID",
         "type": "string"
        },
        "name": {
         "description": "Ad group name",
         "type": "string"
        },
        "status": {
         "description": "Ad group status (ENABLED, PAUSED, REMOVED)",
         "type": "string"
        },
        "type": {
         "description": "Ad group type",
         "type": "string"
        }
       },
       "type": "object"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "title": "Gads List Ad Groups",
 "version": "v1.142fa854"
}
