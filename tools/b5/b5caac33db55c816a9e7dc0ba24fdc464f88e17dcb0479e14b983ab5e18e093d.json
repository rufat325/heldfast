{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-call-record-participant"
 },
 "description": "List of distinct participants in the call.\n\nTIP: Gets a specific participant in a call record (v2 API). Returns participant identity and details.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "callRecordId": {
    "description": "Path parameter: callRecordId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "participantId": {
    "description": "Path parameter: participantId",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "required": [
   "callRecordId",
   "participantId"
  ],
  "type": "object"
 },
 "name": "get-call-record-participant"
}
