{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search for Polish official statistical variables by keyword (e.g. \"population\", \"unemployment\", \"average salary\"), or list all variables under a subject_id from gus-poland.subjects. Returns each variable's numeric variable_id and measurement unit, for use with gus-poland.data. Data: bdl.stat.gov.pl (GUS BDL API v1), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "page": {
    "description": "Zero-based page number (default 0).",
    "minimum": 0,
    "type": "integer"
   },
   "page_size": {
    "description": "Results per page, 1-100 (default 20).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Search variables by keyword in their name, e.g. \"population\", \"unemployment\", \"average salary\". Either query or subject_id is required.",
    "type": "string"
   },
   "subject_id": {
    "description": "List all variables under a leaf subject id (hasVariables=true) from gus-poland.subjects, e.g. \"P1458\". Either query or subject_id is required.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "gus-poland.reference.variables",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Search GUS Poland BDL Variables"
}
