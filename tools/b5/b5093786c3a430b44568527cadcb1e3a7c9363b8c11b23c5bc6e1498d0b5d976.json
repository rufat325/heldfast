{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "API Ninjas cars: vehicle specifications by make, model, year, or fuel type. Returns a list of { make, model, year, fuel_type, cylinders, transmission, drive, city_mpg, highway_mpg, class }. Example: cars({ make: \"toyota\", model: \"camry\", year: 2020 }).",
 "inputSchema": {
  "examples": [
   {
    "make": "toyota",
    "model": "camry",
    "year": 2020
   },
   {
    "fuel_type": "gas",
    "limit": 10,
    "make": "ford"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own API Ninjas key for higher limits; omit to use the shared Pipeworx key.",
    "type": "string"
   },
   "fuel_type": {
    "description": "Fuel type, e.g. 'gas', 'diesel', 'electricity'",
    "type": "string"
   },
   "limit": {
    "description": "Max results to return (default 5, max 50)",
    "type": "number"
   },
   "make": {
    "description": "Vehicle manufacturer, e.g. 'toyota', 'ford'",
    "type": "string"
   },
   "model": {
    "description": "Vehicle model, e.g. 'camry', 'mustang'",
    "type": "string"
   },
   "year": {
    "description": "Model year, e.g. 2020",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "cars",
 "title": "Cars",
 "version": "v1.c10fdfab"
}
