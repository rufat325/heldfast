{
 "description": "List TechSnif editorial articles (original coverage written by TechSnif). Filter by category (AI, Startups, Venture, Robotics) or tag, and paginate with offset. Use get_article for the full body text.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "category": {
    "description": "Filter by category (e.g. \"AI\", \"Startups\", \"Venture\", \"Robotics\")",
    "type": "string"
   },
   "limit": {
    "default": 10,
    "description": "Number of articles to return (default 10, max 50)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   },
   "offset": {
    "description": "Number of articles to skip (pagination)",
    "minimum": 0,
    "type": "number"
   },
   "tag": {
    "description": "Filter by tag (e.g. \"Security\", \"Fintech\", \"Open Source\")",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_articles",
 "title": "TechSnif articles"
}
