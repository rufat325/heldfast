{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Change the title, body, or tags of an article or draft that already exists.\n\nOnly the fields you pass are touched; omitted fields keep their current values. Passing `tags` REPLACES the whole tag list rather than adding to it, so send the full set you want. Editing a published article changes what readers see immediately; it does not unpublish or re-date it.\n\nSafe to repeat: the same call twice leaves the same result. Requires an API key, and the account must own the article. Errors if no updatable field is supplied. Returns the updated article.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "body_markdown": {
    "description": "Replacement body as Markdown. Replaces the whole body, not a patch.",
    "minLength": 1,
    "type": "string"
   },
   "id": {
    "description": "Article or draft id from a listing tool. This is the id, not the slug.",
    "minLength": 1,
    "type": "string"
   },
   "tags": {
    "description": "Replacement tag list, up to 10. Overwrites the existing tags entirely.",
    "items": {
     "type": "string"
    },
    "maxItems": 10,
    "type": "array"
   },
   "title": {
    "description": "Replacement headline.",
    "maxLength": 500,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "update_article",
 "title": "Update an article"
}
