{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Peek at a queue without consuming: returns length and the head envelope.",
 "inputSchema": {
  "properties": {
   "queue": {
    "description": "Queue name",
    "type": "string"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   }
  },
  "required": [
   "spaceId",
   "queue"
  ],
  "type": "object"
 },
 "name": "queue_peek",
 "outputSchema": {
  "properties": {
   "head": {
    "description": "The head envelope without consuming it, or null if empty",
    "type": [
     "object",
     "null"
    ]
   },
   "length": {
    "description": "Current queue length",
    "type": "number"
   },
   "queue": {
    "description": "Queue name",
    "type": "string"
   }
  },
  "required": [
   "queue"
  ],
  "type": "object"
 }
}
