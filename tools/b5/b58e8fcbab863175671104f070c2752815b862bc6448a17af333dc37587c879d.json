{
 "annotations": {
  "destructiveHint": true,
  "title": "Manage a track license"
 },
 "description": "Manage license documents on a track (cover or cleared sample). `upload` attaches a new license \u2014 `file_path` required, `type` selects cover/sample; optional metadata fields. `update` replaces the file and/or metadata \u2014 `track_license_id` (from list_track_licenses) and `file_path` required. `delete` permanently removes a license and its file \u2014 `track_license_id` required; cannot be undone. Immutability-governed once the release is live.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "Which license action.",
    "enum": [
     "upload",
     "update",
     "delete"
    ],
    "type": "string"
   },
   "file_path": {
    "description": "Local path to the license file. Required for upload/update.",
    "type": "string"
   },
   "license_id": {
    "description": "The license/clearance reference number, if any.",
    "type": "string"
   },
   "license_provider": {
    "description": "Where the license came from.",
    "enum": [
     "licensing_agency",
     "direct_from_publisher"
    ],
    "type": "string"
   },
   "license_provider_name": {
    "type": "string"
   },
   "original_track_link": {
    "description": "URL to the source track.",
    "type": "string"
   },
   "track_id": {
    "description": "The track id.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "track_license_id": {
    "description": "Required for update/delete.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "type": {
    "description": "Kind of license (upload).",
    "enum": [
     "cover",
     "sample"
    ],
    "type": "string"
   }
  },
  "required": [
   "action",
   "track_id"
  ],
  "type": "object"
 },
 "name": "manage_track_license",
 "title": "Manage a track license"
}
