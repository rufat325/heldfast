{
 "description": "Check completion, partial, and blocked outcome records for the evidence and remaining-work details their status requires. Costs $0.05 USDC on Base mainnet.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "outcomes": {
    "items": {
     "additionalProperties": true,
     "required": [
      "id",
      "status",
      "evidence"
     ],
     "type": "object"
    },
    "maxItems": 1000,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "outcomes"
  ],
  "type": "object"
 },
 "name": "justhandled_completion_evidence_receipt_auditor"
}
