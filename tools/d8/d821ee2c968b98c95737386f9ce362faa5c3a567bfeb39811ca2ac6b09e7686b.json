{
 "description": "List recent AI application traces ingested via OpenTelemetry (LLM calls, cache lookups, memory recalls, retrieval spans). Not tied to a Valkey instance \u2014 traces come from instrumented AI apps. Use get_ai_trace for a full span waterfall and correlate_ai_trace to join a trace with live Valkey state.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "hours": {
    "description": "Look-back window in whole hours, 1-168 (default 1)",
    "maximum": 168,
    "minimum": 1,
    "type": "integer"
   },
   "limit": {
    "description": "Max traces to return, 1-1000 (default 100)",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "service": {
    "description": "Filter by emitting service name",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_ai_traces"
}
