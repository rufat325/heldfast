{
 "description": "Is this Instagram sound trending? Reels that use it + isTrendingInClips / trendRank / rich music{}. Costs ~28 credits (1.4/result). Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "audioId": {
    "description": "Instagram audio/music ID or full audio URL, e.g. 27919946310946207.",
    "type": "string"
   },
   "cache": {
    "description": "Set true to serve from the 24h response cache (0 credits on hit). Default false \u2014 always fetch fresh. Envelope includes cached + cachedAt on hits.",
    "type": "boolean"
   },
   "limit": {
    "description": "Max items to return (default 20, max 200). Billed per result.",
    "exclusiveMinimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "audioId"
  ],
  "type": "object"
 },
 "name": "instagram_reels_by_audio_id",
 "title": "Instagram Reels By Audio ID"
}
