{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search The Met's open-access collection by keyword. Optionally filter to objects with images, currently on view, by medium, or by a year range. Returns compact records (title, artist, date, medium, image) hydrated from object detail. Keyless.",
 "inputSchema": {
  "examples": [
   {
    "q": "sunflowers"
   },
   {
    "date_begin": 1600,
    "date_end": 1700,
    "has_images": true,
    "limit": 10,
    "q": "Vermeer"
   }
  ],
  "properties": {
   "date_begin": {
    "description": "Start year of the date range, e.g. 1700. Must be paired with date_end.",
    "type": "number"
   },
   "date_end": {
    "description": "End year of the date range, e.g. 1800. Must be paired with date_begin.",
    "type": "number"
   },
   "has_images": {
    "description": "Only return objects that have images (default true).",
    "type": "boolean"
   },
   "limit": {
    "description": "How many matching objects to hydrate with full detail (default 8, max 15).",
    "type": "number"
   },
   "on_view": {
    "description": "Only return objects currently on view in a gallery.",
    "type": "boolean"
   },
   "q": {
    "description": "Search query, e.g. \"sunflowers\", \"Vermeer\", \"samurai armor\".",
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "search_artworks",
 "title": "Search Artworks",
 "version": "v1.bbd5068f"
}
