{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-app-federated-credential"
 },
 "description": "Read the properties and relationships of a federatedIdentityCredential object assigned to an application.\n\nTIP: Gets a specific federated identity credential. Returns name, issuer (e.g. https://token.actions.githubusercontent.com), subject (e.g. repo:org/repo:ref:refs/heads/main), audiences, and description.\n\nRISK LEVEL: MEDIUM. This response contains PII or authentication metadata (sign-ins, MFA factors, mail metadata, risk detections). Handle with care and avoid casual disclosure.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "applicationId": {
    "description": "Path parameter: applicationId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "federatedIdentityCredentialId": {
    "description": "Path parameter: federatedIdentityCredentialId",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "required": [
   "applicationId",
   "federatedIdentityCredentialId"
  ],
  "type": "object"
 },
 "name": "get-app-federated-credential"
}
