{
 "description": "Generate a complete themed asset set for game design. Creates a hero asset, builds a custom style from it, then generates all remaining symbols with visual consistency. Optionally generates background and outputs a Pixi.js-compatible asset manifest.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "bg_prompt": {
    "description": "Background image prompt (if omitted, no background is generated)",
    "type": "string"
   },
   "bg_size": {
    "default": "1344x768",
    "description": "Background image size (default: 1344x768 / 16:9)",
    "type": "string"
   },
   "generate_manifest": {
    "default": true,
    "description": "Generate asset-manifest.json for Pixi.js integration",
    "type": "boolean"
   },
   "output_dir": {
    "description": "Base output directory (symbols/ and ui/ subdirs will be created)",
    "type": "string"
   },
   "prompt_suffix": {
    "description": "Suffix appended to every symbol prompt for consistent style (e.g., \", ultra detailed 3D render, polished metallic surface, dramatic volumetric lighting from above, centered composition on pure black background, no text, no letters, sharp focus, cinematic lighting\")",
    "type": "string"
   },
   "remove_bg": {
    "default": true,
    "description": "Remove background from symbols (default: true)",
    "type": "boolean"
   },
   "size": {
    "default": "1024x1024",
    "description": "Image size (e.g., 1024x1024, 1344x768) or ratio (e.g., 16:9, 1:1 for vector models)",
    "type": "string"
   },
   "symbols": {
    "description": "Array of symbols to generate",
    "items": {
     "properties": {
      "name": {
       "description": "Symbol filename without extension (e.g., \"scarab\", \"ankh\")",
       "type": "string"
      },
      "prompt_detail": {
       "description": "Symbol-specific prompt prefix (e.g., \"A golden scarab beetle amulet\")",
       "type": "string"
      }
     },
     "required": [
      "name",
      "prompt_detail"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   },
   "theme": {
    "description": "Theme name (e.g., \"Egyptian\", \"Viking\", \"Space\")",
    "type": "string"
   }
  },
  "required": [
   "theme",
   "prompt_suffix",
   "symbols",
   "output_dir"
  ],
  "type": "object"
 },
 "name": "recraft_generate_themed_set"
}
