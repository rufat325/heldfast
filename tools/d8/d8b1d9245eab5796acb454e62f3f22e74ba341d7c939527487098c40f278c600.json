{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Dota 2 hero meta: per-hero pick/win/ban counts in pro play, primary attribute, attack type, and roles. Optionally filter by role (e.g. \"Carry\", \"Support\"). OpenDota keyless data.",
 "inputSchema": {
  "examples": [
   {
    "limit": 10,
    "role": "Carry"
   },
   {
    "limit": 30
   }
  ],
  "properties": {
   "limit": {
    "description": "Max heroes to return (default 30).",
    "type": "number"
   },
   "role": {
    "description": "Optional role filter, e.g. \"Carry\", \"Support\", \"Initiator\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "hero_stats",
 "title": "Hero Stats",
 "version": "v1.e6bad1f5"
}
