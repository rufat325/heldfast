{
 "description": "Search OTTASIA's catalog for movies or TV shows by name. Returns multiple candidates with year, type, brief overview, and country-aware OTTASIA link.\n\nUse this when a user asks 'find me X' or when a query is ambiguous (e.g. 'joker', 'the office') and you need to clarify which title they meant before checking availability.\nUse where_to_watch instead when the user has already named a specific title and country and just wants the streaming providers.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "country": {
    "description": "2-letter ISO country code for the resulting OTTASIA links (so clicking through opens that country's streaming context). One of: IN, PK, BD, ID, MY, PH, SG, TH, MM, KH, LA, BN, CN, HK, JP, KR, TW, KZ, UZ, TR, IL, LB, JO, SA, AE, KW, QA, BH, OM, EG. Defaults to IN.",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   },
   "limit": {
    "description": "Max number of results (1-20, default 8).",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "description": "Search query (movie or TV show name). English, romanized, or original-language all work.",
    "minLength": 2,
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "search_titles",
 "title": "Search titles"
}
