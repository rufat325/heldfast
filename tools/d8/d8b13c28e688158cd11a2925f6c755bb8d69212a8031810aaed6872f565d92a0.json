{
 "description": "List primary models the user can navigate to via menus (get_models()), or list relational models reachable from a base model via its form-view fields (get_models({ base: \"sale.order\" })).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "base": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_models"
}
