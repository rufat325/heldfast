{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Evaluate caller-provided candidate context and return decision-ready output. This is the primary FreshContext judgment path: it does not fetch, crawl, scrape, browse, read folders, or call adapters.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "intent": {
    "description": "Intent Profile id, e.g. citation_check, student_research, developer_adoption, job_search, market_watch, business_due_diligence, medical_literature_triage.",
    "minLength": 1,
    "type": "string"
   },
   "now": {
    "description": "Optional ISO timestamp for deterministic evaluation.",
    "type": "string"
   },
   "profile": {
    "description": "Source Profile id, e.g. academic_research, jobs_opportunities, market_finance, official_docs, product_research, multi_agent_handoff, local_custom.",
    "minLength": 1,
    "type": "string"
   },
   "signals": {
    "description": "Candidate context items provided by the caller. FreshContext evaluates these; it does not retrieve them.",
    "items": {
     "additionalProperties": true,
     "properties": {
      "content": {
       "type": "string"
      },
      "content_date": {
       "type": [
        "string",
        "null"
       ]
      },
      "date_confidence": {
       "enum": [
        "high",
        "medium",
        "low",
        "unknown"
       ],
       "type": "string"
      },
      "freshness_confidence": {
       "enum": [
        "high",
        "medium",
        "low"
       ],
       "type": "string"
      },
      "id": {
       "type": "string"
      },
      "metadata": {
       "additionalProperties": {},
       "type": "object"
      },
      "published_at": {
       "type": [
        "string",
        "null"
       ]
      },
      "retrieved_at": {
       "type": [
        "string",
        "null"
       ]
      },
      "semantic_score": {
       "description": "Optional relevance score from 0..1. Core clamps out-of-range values.",
       "type": "number"
      },
      "source": {
       "description": "Source URL, URI, document id, or stable source label.",
       "minLength": 1,
       "type": "string"
      },
      "source_type": {
       "description": "Source type such as arxiv, jobs, official_docs, custom, or user_provided.",
       "type": "string"
      },
      "status": {
       "enum": [
        "success",
        "partial",
        "stale",
        "failed",
        "unknown"
       ],
       "type": "string"
      },
      "title": {
       "type": "string"
      }
     },
     "required": [
      "source"
     ],
     "type": "object"
    },
    "maxItems": 100,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "profile",
   "intent",
   "signals"
  ],
  "type": "object"
 },
 "name": "evaluate_context"
}
