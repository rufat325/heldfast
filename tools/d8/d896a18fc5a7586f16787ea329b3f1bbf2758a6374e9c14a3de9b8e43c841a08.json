{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get taxonomic/geographic specimen counts grouped by a field (top values + counts) across iDigBio. Optionally scope the counts with taxonomy/locality filters. Keyless.",
 "inputSchema": {
  "examples": [
   {
    "field": "country"
   },
   {
    "field": "family",
    "genus": "acer",
    "top_count": 20
   }
  ],
  "properties": {
   "country": {
    "description": "Optional filter to scope the counts.",
    "type": "string"
   },
   "family": {
    "description": "Optional filter to scope the counts.",
    "type": "string"
   },
   "field": {
    "description": "Field to group by: one of country, stateprovince, family, genus, institutioncode, basisofrecord.",
    "type": "string"
   },
   "genus": {
    "description": "Optional filter to scope the counts.",
    "type": "string"
   },
   "scientific_name": {
    "description": "Optional filter to scope the counts.",
    "type": "string"
   },
   "top_count": {
    "description": "Number of top buckets to return (default 10, max 25).",
    "type": "number"
   }
  },
  "required": [
   "field"
  ],
  "type": "object"
 },
 "name": "count_by_field",
 "title": "Count By Field",
 "version": "v1.762483a3"
}
