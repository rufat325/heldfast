{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Export a completed transcript as text: txt (paragraphs), srt/vtt (subtitles), md (Markdown), csv (one row per segment). PDF/DOCX are binary \u2014 use the REST API or the web app for those.",
 "inputSchema": {
  "properties": {
   "format": {
    "enum": [
     "txt",
     "srt",
     "vtt",
     "md",
     "csv"
    ],
    "type": "string"
   },
   "id": {
    "type": "string"
   },
   "speakers": {
    "description": "Include cached speaker labels (default false).",
    "type": "boolean"
   },
   "timestamps": {
    "description": "Include timestamps in txt/md (default true).",
    "type": "boolean"
   }
  },
  "required": [
   "id",
   "format"
  ],
  "type": "object"
 },
 "name": "export_transcript",
 "title": "Export a transcript"
}
