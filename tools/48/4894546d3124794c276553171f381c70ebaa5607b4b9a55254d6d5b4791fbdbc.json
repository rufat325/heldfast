{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Discover available series. Optionally filter by a case-insensitive substring matched against series label/description, e.g. \"exchange rate\" or \"CPI\". Omit query to list all (large).",
 "inputSchema": {
  "examples": [
   {
    "limit": 20,
    "query": "exchange rate"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max results to return (default 50).",
    "type": "integer"
   },
   "query": {
    "description": "Substring to match against label/description (case-insensitive).",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_series",
 "title": "List Series",
 "version": "v1.b6796ca2"
}
