{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Update a contact"
 },
 "description": "Update a contact. Partial update. Only the fields you send change. An empty string clears name, email, phone or notes. custom_fields merges per key; null clears one value.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "custom_fields": {
    "additionalProperties": true,
    "description": "Merged per key. Null clears one value.",
    "type": "object"
   },
   "email": {
    "description": "Valid email, or empty string to clear.",
    "type": "string"
   },
   "id": {
    "description": "Numeric id.",
    "type": "integer"
   },
   "name": {
    "description": "Empty string clears.",
    "maxLength": 255,
    "type": "string"
   },
   "notes": {
    "maxLength": 5000,
    "type": "string"
   },
   "phone": {
    "description": "Digits, +, spaces, dashes, brackets. Empty string clears.",
    "type": "string"
   },
   "stage": {
    "enum": [
     "new",
     "contacted",
     "qualified",
     "customer",
     "lost"
    ],
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "update_contact"
}
