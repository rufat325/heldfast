{
 "description": "Describe any OpenShift resource and share the output in various formats including human-readable summary",
 "inputSchema": {
  "properties": {
   "context": {
    "default": "",
    "description": "OpenShift context to use (optional)",
    "type": "string"
   },
   "name": {
    "description": "Name of the resource to describe",
    "type": "string"
   },
   "namespace": {
    "default": "default",
    "description": "OpenShift namespace/project",
    "type": "string"
   },
   "output": {
    "default": "human-readable",
    "description": "Output format: text (default oc describe), yaml, json, or human-readable (concise summary)",
    "enum": [
     "text",
     "yaml",
     "json",
     "human-readable"
    ],
    "type": "string"
   },
   "resourceType": {
    "description": "Type of resource to describe (pod, deployment, service, route, etc.)",
    "type": "string"
   }
  },
  "required": [
   "resourceType",
   "name"
  ],
  "type": "object"
 },
 "name": "oc_describe"
}
