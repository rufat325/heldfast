{
 "description": "Push JettyScript automation rules to a device. Rules run on-device and can trigger alerts when sensor thresholds are crossed.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "device_id": {
    "description": "Device ID",
    "type": "string"
   },
   "rules": {
    "description": "JettyScript rules array",
    "items": {
     "properties": {
      "id": {
       "type": "string"
      },
      "then": {
       "items": {
        "properties": {
         "action": {
          "type": "string"
         },
         "params": {
          "additionalProperties": {},
          "propertyNames": {
           "type": "string"
          },
          "type": "object"
         }
        },
        "required": [
         "action"
        ],
        "type": "object"
       },
       "type": "array"
      },
      "when": {
       "properties": {
        "op": {
         "type": "string"
        },
        "sensor": {
         "type": "string"
        },
        "type": {
         "type": "string"
        },
        "value": {
         "type": "number"
        }
       },
       "required": [
        "type"
       ],
       "type": "object"
      }
     },
     "required": [
      "id",
      "when",
      "then"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "device_id",
   "rules"
  ],
  "type": "object"
 },
 "name": "push_rules"
}
