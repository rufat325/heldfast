{
 "_meta": {},
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List nominations"
 },
 "description": "List App Store nominations (e.g. custom product pages, in-app events)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "filter.relatedApps": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by related app ID(s)"
   },
   "filter.state": {
    "anyOf": [
     {
      "enum": [
       "DRAFT",
       "SUBMITTED",
       "ARCHIVED"
      ],
      "type": "string"
     },
     {
      "items": {
       "enum": [
        "DRAFT",
        "SUBMITTED",
        "ARCHIVED"
       ],
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by nomination state(s) (required)"
   },
   "filter.type": {
    "anyOf": [
     {
      "enum": [
       "APP_LAUNCH",
       "APP_ENHANCEMENTS",
       "NEW_CONTENT"
      ],
      "type": "string"
     },
     {
      "items": {
       "enum": [
        "APP_LAUNCH",
        "APP_ENHANCEMENTS",
        "NEW_CONTENT"
       ],
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by nomination type(s)"
   },
   "limit": {
    "default": 100,
    "description": "Maximum number of nominations to return (default 100, max 200)",
    "type": "number"
   }
  },
  "required": [
   "filter.state"
  ],
  "type": "object"
 },
 "name": "list-nominations",
 "title": "List nominations"
}
