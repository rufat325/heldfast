{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Replaces the text of one TEXT node. The font is loaded first; a layer whose font is missing from this machine is refused rather than silently retyped in a substitute.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "nodeId": {
    "description": "The TEXT node to retype.",
    "type": "string"
   },
   "text": {
    "description": "The new characters.",
    "type": "string"
   }
  },
  "required": [
   "nodeId",
   "text"
  ],
  "type": "object"
 },
 "name": "figma_set_text",
 "title": "Set the characters of a text layer"
}
