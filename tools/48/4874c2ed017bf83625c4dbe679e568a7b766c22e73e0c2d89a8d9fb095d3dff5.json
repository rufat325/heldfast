{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Google News search via Serper: returns recent news articles (title, link, snippet, source, date) matching the query. Accepts query, result count (max 20), and optional country code (gl).",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-serper-api-key",
    "query": "federal reserve rate decision"
   },
   {
    "_apiKey": "your-serper-api-key",
    "gl": "gb",
    "num": 20,
    "query": "artificial intelligence regulation"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Serper API key for higher limits; omit to use the shared Pipeworx key.",
    "type": "string"
   },
   "gl": {
    "description": "Country code for geolocation, e.g. \"us\", \"gb\", \"de\"",
    "type": "string"
   },
   "num": {
    "description": "Number of results (default 10, max 20)",
    "type": "number"
   },
   "query": {
    "description": "Search query, e.g. \"federal reserve rate decision\"",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "news",
 "title": "News",
 "version": "v1.643d7032"
}
