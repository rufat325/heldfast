{
 "description": "Ask clink's shop to BUILD a tool or data source that nobody sells yet. Use this right after find_agent_tools comes back unsatisfied. clink reads every request the same day; buildable asks ship as pay-per-call x402 endpoints, usually within a week. Returns a request id and a status URL; pass `notify` (email or https webhook) to be told the moment it ships.",
 "inputSchema": {
  "properties": {
   "budget_usd": {
    "description": "Optional. What one call is worth to you, in USD \u2014 helps clink price it.",
    "type": "number"
   },
   "notify": {
    "description": "Optional. An email address or https webhook URL to ping when it ships (or is declined).",
    "type": "string"
   },
   "want": {
    "description": "What you need, plain language, 3-500 chars. Describe the data or capability and how you'd call it.",
    "type": "string"
   }
  },
  "required": [
   "want"
  ],
  "type": "object"
 },
 "name": "request_agent_tool"
}
