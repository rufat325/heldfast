{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Expand Percy Visual Testing"
 },
 "description": "Set up or expand Percy visual testing configuration with comprehensive coverage for existing projects that might have Percy integrated. This supports both Percy Web Standalone and Percy Automate. Example prompts: Expand percy coverage for this project {project_name}",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "detectedBrowserAutomationFramework": {
    "enum": [
     "playwright",
     "selenium",
     "cypress",
     "webdriverio"
    ],
    "type": "string"
   },
   "detectedLanguage": {
    "enum": [
     "nodejs",
     "python",
     "java",
     "csharp",
     "ruby"
    ],
    "type": "string"
   },
   "detectedTestingFramework": {
    "enum": [
     "jest",
     "codeceptjs",
     "playwright",
     "pytest",
     "robot",
     "behave",
     "cucumber",
     "nightwatch",
     "mocha",
     "junit4",
     "junit5",
     "testng",
     "cypress",
     "nunit",
     "mstest",
     "xunit",
     "specflow",
     "reqnroll",
     "rspec",
     "serenity"
    ],
    "type": "string"
   },
   "filePaths": {
    "description": "An array of absolute file paths to specific UI test files. Use this when you want to target specific test files rather than entire folders. If not provided, will use folderPaths instead.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "folderPaths": {
    "description": "An array of absolute folder paths containing UI test files. If not provided, analyze codebase for UI test folders by scanning for test patterns which contain UI test cases as per framework. Return empty array if none found.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "integrationType": {
    "description": "Specify the Percy integration type: web (Percy Web) or automate (Percy Automate). If not provided, always prompt the user with: 'Please specify the Percy integration type.' Do not proceed without an explicit selection. Never use a default.",
    "enum": [
     "web",
     "automate"
    ],
    "type": "string"
   },
   "projectName": {
    "description": "A unique name for your Percy project.",
    "type": "string"
   }
  },
  "required": [
   "projectName",
   "detectedLanguage",
   "detectedBrowserAutomationFramework",
   "detectedTestingFramework",
   "integrationType"
  ],
  "type": "object"
 },
 "name": "expandPercyVisualTesting"
}
