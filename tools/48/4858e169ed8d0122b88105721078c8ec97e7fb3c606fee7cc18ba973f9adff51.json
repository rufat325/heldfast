{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get cart filters"
 },
 "description": "Get the valid filter values for mhlb_get_cart \u2014 the ordering periods (semesters) and status tabs, with which one is selected by default. Call this before filtering the cart.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "period": {
    "description": "Ordering period to scope the tabs to.",
    "type": "string"
   },
   "studentId": {
    "description": "Limit to one student.",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "tabName": {
    "description": "Status tab whose counts to compute.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "mhlb_get_cart_tabs"
}
