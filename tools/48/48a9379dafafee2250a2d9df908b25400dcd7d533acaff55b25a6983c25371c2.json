{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "No-key stock quote data via Stooq \u2014 close, open, high, low, volume, quote timestamp, and source. Accepts up to 5 comma-separated tickers. Returns timestamped freshcontext only for successful observations.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "max_length": {
    "default": 5000,
    "type": "number"
   },
   "url": {
    "description": "Ticker symbol(s) e.g. 'AAPL' or 'MSFT,GOOG,PLTR'",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "extract_finance"
}
