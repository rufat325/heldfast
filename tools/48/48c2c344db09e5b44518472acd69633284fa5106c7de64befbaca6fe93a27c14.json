{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Search by HF Tire"
 },
 "description": "Find all vehicles compatible with a given high-flotation (HF) tyre size. For pickup trucks and off-road vehicles. HF size format: OD x SW R RD (e.g. 33x12.5R15). Required: overall_diameter (inches), section_width (INCHES, not mm!), rim_diameter (inches). Optional: region, limit, offset.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Maximum results to return (1-100).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Number of results to skip for pagination.",
    "minimum": 0,
    "type": "integer"
   },
   "overall_diameter": {
    "description": "Overall outside diameter of the inflated tyre in inches (e.g. 33.0). Required. Must be positive. Part of the HF size notation: OD x SW R RD (e.g. 33x12.5R15).",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "region": {
    "description": "Market region code (e.g. \"usdm\", \"eudm\"). Get codes from list-regions.",
    "type": "string"
   },
   "rim_diameter": {
    "description": "Rim diameter in inches (e.g. 15). Required. Must be positive. Use list-hf-tire-rim-diameters to enumerate valid values.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "section_width": {
    "description": "Tyre section width in INCHES (e.g. 12.5). Required. Must be positive. Note: this differs from standard by_tire tools where section_width is in mm.",
    "exclusiveMinimum": 0,
    "type": "number"
   }
  },
  "required": [
   "overall_diameter",
   "section_width",
   "rim_diameter"
  ],
  "type": "object"
 },
 "name": "search-by-hf-tire"
}
