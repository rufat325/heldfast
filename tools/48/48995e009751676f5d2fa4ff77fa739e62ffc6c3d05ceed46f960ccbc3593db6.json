{
 "description": "Create an editable Swift app starter, implementation brief and optional XcodeGen specification/icon layers. Writes a NEW project; does not implement the full app idea. Then use source retrieval, review and simulator tools to implement and verify it.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "brief": {
    "type": "string"
   },
   "directory": {
    "description": "Parent directory for the new project",
    "type": "string"
   },
   "name": {
    "type": "string"
   },
   "xcodegen": {
    "default": true,
    "type": "boolean"
   }
  },
  "required": [
   "name",
   "directory",
   "brief"
  ],
  "type": "object"
 },
 "name": "create_app"
}
