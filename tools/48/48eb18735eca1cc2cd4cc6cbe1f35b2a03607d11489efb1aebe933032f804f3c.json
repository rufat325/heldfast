{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the Maryland Open Data catalog of open datasets by keyword. Returns each dataset's resource_id, name, description, category and update date \u2014 pass the resource_id to query/metadata.",
 "inputSchema": {
  "examples": [
   {
    "query": "crime"
   },
   {
    "limit": 50,
    "offset": 0,
    "query": "budget"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max datasets (1-100, default 20).",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset.",
    "type": "number"
   },
   "query": {
    "description": "Keyword to search dataset titles/descriptions (e.g. \"budget\", \"crime\", \"health\").",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "datasets",
 "title": "Datasets",
 "version": "v1.b82e2d3d"
}
