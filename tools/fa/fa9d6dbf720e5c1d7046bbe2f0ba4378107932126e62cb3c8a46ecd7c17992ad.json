{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get a single memory space (config, usage counters).",
 "inputSchema": {
  "properties": {
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   }
  },
  "required": [
   "spaceId"
  ],
  "type": "object"
 },
 "name": "spaces_get",
 "outputSchema": {
  "properties": {
   "space": {
    "properties": {
     "_id": {
      "description": "Space ID (use as spaceId in other tools)",
      "type": "string"
     },
     "config": {
      "description": "Space configuration (TTLs, limits, encoding)",
      "type": "object"
     },
     "createdAt": {
      "description": "ISO 8601 creation timestamp",
      "type": "string"
     },
     "name": {
      "description": "Human-readable space name",
      "type": "string"
     },
     "slug": {
      "description": "URL-safe slug",
      "type": "string"
     },
     "usage": {
      "description": "Usage counters (message count, etc.)",
      "type": "object"
     }
    },
    "type": "object"
   }
  },
  "type": "object"
 }
}
