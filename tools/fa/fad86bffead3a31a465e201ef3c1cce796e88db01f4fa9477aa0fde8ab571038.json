{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the EU Open Data Portal catalog (1M+ datasets aggregated from national and regional EU/EEA open-data portals plus EU institutions) by free text, country, and/or DCAT-AP theme, returning dataset id, localized title/description, source catalogue, country, categories, and available distribution formats. Use data-europa.theme_list for valid theme codes. Data: data.europa.eu Hub-Search API, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "country": {
    "description": "2-letter lowercase ISO country code to filter by (e.g. \"de\", \"fr\", \"it\", \"es\")",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   },
   "limit": {
    "description": "Number of datasets to return (1-20, default 10)",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "locale": {
    "description": "2-letter language code for localized fields (default \"en\")",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   },
   "query": {
    "description": "Free-text search across dataset title/description (e.g. \"air quality\")",
    "type": "string"
   },
   "theme": {
    "description": "DCAT-AP data theme code to filter by (e.g. \"ENVI\" for Environment, \"HEAL\" for Health) \u2014 see data-europa.theme_list for the full list",
    "enum": [
     "AGRI",
     "ECON",
     "EDUC",
     "ENER",
     "ENVI",
     "GOVE",
     "HEAL",
     "INTR",
     "JUST",
     "OP_DATPRO",
     "REGI",
     "SOCI",
     "TECH",
     "TRAN"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "data-europa.datasets.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "EU Open Data Search"
}
