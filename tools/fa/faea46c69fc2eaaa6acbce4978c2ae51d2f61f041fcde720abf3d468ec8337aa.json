{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch a country's selected residential property price index (nominal or real) from the BIS quarterly property price series (WS_SPP) \u2014 a long-run, cross-country comparable house price index used to track housing market cycles and financial stability risk. Real values are deflated by the consumer price index. Returns one or more indexed time series with period/value pairs.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "country": {
    "description": "ISO 2-letter BIS reference-area code (e.g. US, XM for euro area, JP, GB, CN, DE, FR, BR, IN, CA, AU, CH). See the BIS reference area codelist for the full list of covered economies.",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   },
   "end_period": {
    "description": "End of the requested time range, e.g. \"2024-06\" for monthly data or \"2024-Q2\" for quarterly data. Defaults to the latest available observation if omitted.",
    "type": "string"
   },
   "max_series": {
    "description": "Maximum number of time series to return (1\u2013100, default 20). Each series is a unique combination of dimensions such as rate type or unit of measure.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "start_period": {
    "description": "Start of the requested time range, e.g. \"2020-01\" for monthly data or \"2020-Q1\" for quarterly data. Defaults to the last 24 observations if both start_period and end_period are omitted.",
    "type": "string"
   },
   "value_type": {
    "description": "Price value type: \"N\" = nominal (default), \"R\" = real (inflation-adjusted).",
    "enum": [
     "N",
     "R"
    ],
    "type": "string"
   }
  },
  "required": [
   "country"
  ],
  "type": "object"
 },
 "name": "bis-stats.finance.property_prices",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "BIS Residential Property Prices"
}
