{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Ghost Audit Site"
 },
 "contract": {
  "auth_scope": "public",
  "idempotent": false,
  "output_schema": {
   "audit_id": "string",
   "download_url": "string",
   "stages": "string[]",
   "status": "string",
   "stream_url": "string"
  },
  "price": {
   "free_tier": "one completed audit/day",
   "paid": "unlimited (Zambo Pass or Day Pass)",
   "per_call": "free"
  },
  "side_effects": true
 },
 "description": "Achilles 10-stage audit for a website. Returns SEO gaps, AI discoverability issues, conversion leaks, brand-presence gaps, competitor intelligence, a score from 0 to 100, stage findings, an audit ID, a live stream URL, and a report URL.",
 "destructiveHint": true,
 "inputSchema": {
  "examples": [
   {
    "url": "https://example.com"
   }
  ],
  "properties": {
   "email": {
    "description": "Optional email tied to an active Zambo Pass or Day Pass for unlimited audits.",
    "type": "string"
   },
   "url": {
    "description": "Full website URL to audit (e.g., https://yoursite.com). Include https://.",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "ghost_audit_site",
 "openWorldHint": true,
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "audit_id": {
    "type": "string"
   },
   "download_url": {
    "type": "string"
   },
   "stages": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "status": {
    "type": "string"
   },
   "stream_url": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "readOnlyHint": false,
 "title": "Ghost Audit Site"
}
