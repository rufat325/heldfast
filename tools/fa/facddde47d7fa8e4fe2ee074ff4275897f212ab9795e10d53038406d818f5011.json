{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Update Affiliate Commission"
 },
 "description": "Change an affiliate's commission rates on a live event (map of promo_code_id \u2192 commission). Affects future sales only. Requires event_id + affiliate_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "affiliate_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "commission_updates": {
    "additionalProperties": {
     "additionalProperties": false,
     "properties": {
      "commission_type": {
       "enum": [
        "percent",
        "fixed"
       ],
       "type": "string"
      },
      "commission_value": {
       "maximum": 1000000,
       "minimum": 0,
       "type": "number"
      }
     },
     "required": [
      "commission_type",
      "commission_value"
     ],
     "type": "object"
    },
    "propertyNames": {
     "maxLength": 128,
     "type": "string"
    },
    "type": "object"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "affiliate_id",
   "commission_updates"
  ],
  "type": "object"
 },
 "name": "update_affiliate_commission",
 "title": "Update Affiliate Commission"
}
