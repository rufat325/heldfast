{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Track a new mobile application by its store bundle ID. The app metadata is fetched from the store, an initial visibility score is computed, and the app is linked to the user workspace. Counts against the plan app cap.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "app_id": {
    "description": "Store bundle ID (e.g. \"com.spotify.music\" for GPLAY, \"284882215\" or a bundle ID like \"com.facebook.Facebook\" for ITUNES)",
    "type": "string"
   },
   "country": {
    "description": "ISO country code (e.g., US, FR, DE)",
    "type": "string"
   },
   "lang": {
    "description": "BCP-47 language-region code (e.g., en-US, fr-FR, de-DE)",
    "type": "string"
   },
   "store": {
    "description": "App store: GPLAY (Google Play) or ITUNES (App Store)",
    "enum": [
     "GPLAY",
     "ITUNES"
    ],
    "type": "string"
   }
  },
  "required": [
   "app_id",
   "store",
   "country",
   "lang"
  ],
  "type": "object"
 },
 "name": "add_application"
}
