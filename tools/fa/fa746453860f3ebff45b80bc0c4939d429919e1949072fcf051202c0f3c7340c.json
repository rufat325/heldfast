{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Return a bounded page of Customer.io message deliveries with recipients, channel, campaign or newsletter parent, and delivery engagement timestamps. Answers which messages reached, bounced, opened, clicked, converted, or unsubscribed recipients.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-customer-io-app-api-key",
    "_region": "us",
    "limit": 50,
    "metric": "converted"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Customer.io App API key",
    "type": "string"
   },
   "_region": {
    "description": "Workspace region; defaults to us",
    "enum": [
     "us",
     "eu"
    ],
    "type": "string"
   },
   "campaign_id": {
    "type": "number"
   },
   "end_ts": {
    "description": "Delivery creation range end as Unix seconds",
    "type": "number"
   },
   "limit": {
    "description": "Maximum deliveries, default 50 and maximum 100",
    "maximum": 100,
    "minimum": 1,
    "type": "number"
   },
   "message_type": {
    "enum": [
     "email",
     "webhook",
     "twilio",
     "whatsapp",
     "slack",
     "push",
     "in_app",
     "live_notification"
    ],
    "type": "string"
   },
   "metric": {
    "enum": [
     "attempted",
     "sent",
     "delivered",
     "opened",
     "clicked",
     "converted",
     "bounced",
     "spammed",
     "unsubscribed",
     "dropped",
     "failed",
     "undeliverable"
    ],
    "type": "string"
   },
   "start": {
    "description": "Cursor from the previous response next field",
    "type": "string"
   },
   "start_ts": {
    "description": "Delivery creation range start as Unix seconds",
    "type": "number"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "cio_list_messages",
 "title": "Cio List Messages",
 "version": "v1.40ccfcad"
}
