{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "readOnlyHint": false
 },
 "description": "Persist a VibeMap project's database schema \u2014 tables, columns, and relationships \u2014 in one call. Author this LAST, after the rest of the spec exists, grounded on the spec (idea-first) or the codebase's models/migrations (code-first). One table per domain entity; give every table an `id` primary key and created_at/updated_at; set real Postgres column types + constraints; express foreign keys via each column's `foreignKey` (relationships auto-derive from these, so `relationships` is optional); add junction tables for many-to-many. All params are camelCase (this IS VibeMap's SchemaJSON). Existing schema is visible via vibemap_get_project_context (dbSchema) \u2014 the server keyed-reconciles, so re-running is safe. Table/page access rules are handled by the separate access-rules flow, not here.",
 "inputSchema": {
  "properties": {
   "projectId": {
    "type": "string"
   },
   "relationships": {
    "description": "Optional \u2014 foreign keys on columns auto-derive relationships. Provide only for relationships not expressed by a column FK.",
    "items": {
     "properties": {
      "onDelete": {
       "description": "e.g. CASCADE, SET NULL",
       "type": "string"
      },
      "sourceCardinality": {
       "enum": [
        "1",
        "N"
       ],
       "type": "string"
      },
      "sourceColumn": {
       "type": "string"
      },
      "sourceTable": {
       "type": "string"
      },
      "targetCardinality": {
       "enum": [
        "1",
        "N"
       ],
       "type": "string"
      },
      "targetColumn": {
       "type": "string"
      },
      "targetTable": {
       "type": "string"
      }
     },
     "required": [
      "sourceTable",
      "sourceColumn",
      "targetTable",
      "targetColumn",
      "sourceCardinality",
      "targetCardinality"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "tables": {
    "description": "One entry per table (snake_case, plural names).",
    "items": {
     "properties": {
      "columns": {
       "items": {
        "properties": {
         "check": {
          "description": "CHECK constraint expression",
          "type": "string"
         },
         "default": {
          "description": "SQL default, e.g. gen_random_uuid(), now()",
          "type": "string"
         },
         "description": {
          "type": "string"
         },
         "foreignKey": {
          "description": "FK reference \u2014 relationship auto-derives from this",
          "properties": {
           "column": {
            "type": "string"
           },
           "table": {
            "type": "string"
           }
          },
          "required": [
           "table",
           "column"
          ],
          "type": "object"
         },
         "maxLength": {
          "description": "VARCHAR/CHAR length",
          "type": "string"
         },
         "name": {
          "description": "Column name (snake_case)",
          "type": "string"
         },
         "nullable": {
          "type": "boolean"
         },
         "primaryKey": {
          "type": "boolean"
         },
         "type": {
          "description": "Postgres type, e.g. UUID, TEXT, INTEGER, TIMESTAMPTZ, BOOLEAN",
          "type": "string"
         },
         "unique": {
          "type": "boolean"
         }
        },
        "required": [
         "name",
         "type"
        ],
        "type": "object"
       },
       "type": "array"
      },
      "description": {
       "description": "What this table stores",
       "type": "string"
      },
      "name": {
       "description": "Table name (snake_case, plural)",
       "type": "string"
      }
     },
     "required": [
      "name",
      "columns"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "projectId",
   "tables"
  ],
  "type": "object"
 },
 "name": "vibemap_create_schema"
}
