{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Retrieve the history (audit trail) for a specific quote in Xero. Returns history records including dates, users, details, and changes.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "quoteID": {
    "description": "The Xero quote ID.",
    "type": "string"
   }
  },
  "required": [
   "quoteID"
  ],
  "type": "object"
 },
 "name": "get-quote-history"
}
