{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Get labelled or interactive elements from the focused React screen. Check traversal.complete before treating an empty elements array as definitive.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "interactiveOnly": {
    "default": false,
    "description": "Return only elements with onPress handlers",
    "type": "boolean"
   },
   "maxDepth": {
    "default": 200,
    "maximum": 600,
    "minimum": 0,
    "type": "integer"
   },
   "maxNodes": {
    "default": 1200,
    "maximum": 5000,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_elements"
}
