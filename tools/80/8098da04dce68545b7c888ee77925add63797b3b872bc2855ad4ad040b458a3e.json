{
 "description": "Get shipping labels (PDF or ZPL) for one or more shipments. Returns the raw response URL; caller is expected to download binary.",
 "inputSchema": {
  "properties": {
   "format": {
    "description": "Label format (default: pdf)",
    "enum": [
     "pdf",
     "zpl2"
    ],
    "type": "string"
   },
   "shipmentIds": {
    "description": "Shipment IDs (max 50)",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "shipmentIds"
  ],
  "type": "object"
 },
 "name": "get_shipping_label"
}
