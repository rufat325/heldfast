{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Batch-screen up to 10,000 records against sanctions/PEP/watchlists in one request. Pass a `records` array of objects (each with a name, plus optional entity_type/external_identifier/date_of_birth/identifier/country). Top-level min_score/data_source/entity_type act as defaults applied to any record that omits them. Example: sanctions_bulk({ records: [{ name: \"Vladimir Putin\", external_identifier: \"cust-1\" }, { name: \"Acme Corp\", external_identifier: \"cust-2\" }], min_score: 0.9, _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-sanctions-io-api-key",
    "min_score": 0.9,
    "records": [
     {
      "entity_type": "individual",
      "external_identifier": "cust-001",
      "name": "Vladimir Putin"
     },
     {
      "entity_type": "entity",
      "external_identifier": "cust-002",
      "name": "Acme Corp"
     }
    ]
   },
   {
    "_apiKey": "your-sanctions-io-api-key",
    "data_source": "FATF,PEP",
    "records": [
     {
      "country": "GB",
      "date_of_birth": "1975-03-15",
      "external_identifier": "emp-042",
      "name": "Jane Smith"
     },
     {
      "external_identifier": "vendor-108",
      "identifier": "GB123456789",
      "name": "XYZ Trading Ltd"
     }
    ]
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "sanctions.io API token",
    "type": "string"
   },
   "data_source": {
    "description": "Default comma-separated watchlist source(s) applied to records that omit their own (optional)",
    "type": "string"
   },
   "entity_type": {
    "description": "Default entity_type applied to records that omit their own (optional)",
    "enum": [
     "individual",
     "entity"
    ],
    "type": "string"
   },
   "min_score": {
    "description": "Default minimum match score applied to records that omit their own. Default 0.88.",
    "type": "number"
   },
   "records": {
    "description": "Records to screen (up to 10,000). Each item: { name (required), entity_type?, external_identifier?, data_source?, min_score?, country?, date_of_birth?, identifier? }",
    "items": {
     "properties": {
      "country": {
       "description": "Country filter (optional)",
       "type": "string"
      },
      "data_source": {
       "description": "Watchlist source(s) for this record (optional)",
       "type": "string"
      },
      "date_of_birth": {
       "description": "YYYY-MM-DD (optional)",
       "type": "string"
      },
      "entity_type": {
       "description": "\"individual\" or \"entity\" (optional)",
       "type": "string"
      },
      "external_identifier": {
       "description": "Your own internal ID for correlating this record to its result (optional)",
       "type": "string"
      },
      "identifier": {
       "description": "Known identifier (optional)",
       "type": "string"
      },
      "min_score": {
       "description": "Per-record minimum score (optional)",
       "type": "number"
      },
      "name": {
       "description": "Name to screen",
       "type": "string"
      }
     },
     "required": [
      "name"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "records",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "sanctions_bulk",
 "title": "Sanctions Bulk",
 "version": "v1.28e8bd2f"
}
