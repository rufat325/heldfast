{
 "description": "Search FixGraph for engineering fixes. Returns matching issues with trust scores and fix counts.",
 "inputSchema": {
  "properties": {
   "category": {
    "description": "Optional category filter (e.g. \"databases\", \"networking\")",
    "type": "string"
   },
   "limit": {
    "description": "Number of results (1\u201320, default 5)",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Error message, technology, or symptom to search for",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "fixgraph_search"
}
