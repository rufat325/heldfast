{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-user-meeting-attendance-report"
 },
 "description": "The attendance reports of an online meeting. Read-only.\n\nTIP: Gets a specific attendance report by ID for a Teams online meeting (app-only). Pass $expand=attendanceRecords to get participant-level details inline (recommended \u2014 avoids a second call to list-user-meeting-attendance-records). REQUIRES Application Access Policy on the target user. Useful for: HR audits (who attended this mandatory training?), engagement reporting (avg participant duration), GDPR / Loi 25 subject-rights requests, forensic investigations (who joined this meeting at what time?).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "meetingAttendanceReportId": {
    "description": "Path parameter: meetingAttendanceReportId",
    "type": "string"
   },
   "onlineMeetingId": {
    "description": "Path parameter: onlineMeetingId",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   },
   "userId": {
    "description": "Path parameter: userId",
    "type": "string"
   }
  },
  "required": [
   "userId",
   "onlineMeetingId",
   "meetingAttendanceReportId"
  ],
  "type": "object"
 },
 "name": "get-user-meeting-attendance-report"
}
