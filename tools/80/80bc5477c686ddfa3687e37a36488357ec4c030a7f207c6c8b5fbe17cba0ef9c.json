{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Insert a chart that reads its values live from the given ranges \u2014 editing those cells later updates the chart.\nLook at the data first so the series are right: a column of labels belongs in categoriesRef, each column of numbers is one series, and its header is that series' name.\nReferences are A1 (B2:B10, or Sheet2!B2:B10 to read another sheet). The chart is placed below the data unless `anchor` says otherwise.",
 "inputSchema": {
  "properties": {
   "anchor": {
    "description": "Top-left cell of the chart, e.g. 'H2'. Defaults to two rows below the data.",
    "type": "string"
   },
   "categoriesRef": {
    "description": "A1 range of the category labels (the X axis), e.g. A2:A10. For scatter/bubble these are the numeric X values.",
    "type": "string"
   },
   "chartType": {
    "description": "col = vertical bars, bar = horizontal. pie/doughnut plot one series. scatter/bubble take numeric X from categoriesRef. stock needs 4 series (open/high/low/close) or 3 (high/low/close). surface plots a grid, one series per row.",
    "enum": [
     "col",
     "bar",
     "line",
     "area",
     "pie",
     "doughnut",
     "scatter",
     "radar",
     "bubble",
     "stock",
     "ofPie",
     "barOfPie",
     "surface",
     "surface3d",
     "col3d",
     "bar3d",
     "line3d",
     "area3d",
     "pie3d"
    ],
    "type": "string"
   },
   "series": {
    "description": "One entry per data series. Each valueRef is an A1 range of the values to plot.",
    "items": {
     "properties": {
      "color": {
       "description": "RGB hex without \"#\", e.g. 4472C4.",
       "type": "string"
      },
      "name": {
       "description": "Legend name, e.g. the column's header text.",
       "type": "string"
      },
      "seriesType": {
       "description": "Draw this one series as a different kind, making a combo chart. Only valid when the chart itself is col/bar/line/area.",
       "enum": [
        "col",
        "bar",
        "line",
        "area"
       ],
       "type": "string"
      },
      "sizeRef": {
       "description": "Bubble sizes (bubble charts only): a third A1 range.",
       "type": "string"
      },
      "valueRef": {
       "description": "A1 range of this series' values, e.g. B2:B10.",
       "type": "string"
      }
     },
     "required": [
      "valueRef"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "sheetIdx": {
    "description": "Sheet the chart is placed on (zero-based).",
    "type": "integer"
   },
   "sizeInCells": {
    "description": "Chart size in cells. Defaults to 15 \u00d7 8.",
    "properties": {
     "cols": {
      "minimum": 1,
      "type": "integer"
     },
     "rows": {
      "minimum": 1,
      "type": "integer"
     }
    },
    "type": "object"
   },
   "title": {
    "description": "Chart title.",
    "type": "string"
   }
  },
  "required": [
   "sheetIdx",
   "chartType",
   "series"
  ],
  "type": "object"
 },
 "name": "chart_insert"
}
