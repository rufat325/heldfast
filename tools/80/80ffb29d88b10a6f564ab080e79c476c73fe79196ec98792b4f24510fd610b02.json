{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the eBird taxonomy by common or scientific name. Returns the eBird species code needed by the observation tools.",
 "inputSchema": {
  "examples": [
   {
    "query": "Great Blue Heron"
   },
   {
    "query": "Ardea herodias"
   }
  ],
  "properties": {
   "query": {
    "description": "Common or scientific name",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "find_species",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of species results",
    "type": "number"
   },
   "query": {
    "description": "Search query string",
    "type": "string"
   },
   "species": {
    "description": "List of matching species",
    "items": {
     "properties": {
      "name": {
       "description": "Species common or scientific name",
       "type": [
        "string",
        "null"
       ]
      },
      "species_code": {
       "description": "eBird species code",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "required": [],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "query",
   "count",
   "species"
  ],
  "type": "object"
 },
 "title": "Find Species",
 "version": "v1.c26721b6"
}
