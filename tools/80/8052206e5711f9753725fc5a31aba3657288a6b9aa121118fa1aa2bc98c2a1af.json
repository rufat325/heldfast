{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Access weekly and monthly supply chain performance indicators from BTS, including containerized import/export volumes at US ports, railroad terminal dwell times, truck speeds at bottleneck locations, freight rates (Shanghai\u2013LA), vessel waiting times, carrier employment, and the Freight Transportation Services Index. Filter by indicator name (partial match), year, or date range. Source: BTS Supply Chain and Freight Indicators (Socrata y5ut-ibwt). No auth \u2014 US Gov public domain.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "end_date": {
    "description": "End of date range in YYYY-MM-DD format (e.g. \"2024-12-31\").",
    "type": "string"
   },
   "indicator": {
    "description": "Partial name of the indicator to filter by (e.g. \"Freight Transportation Services Index\", \"Containerized Imports\", \"Railroad\", \"Truck Speed\"). Case-insensitive substring match.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of indicator records to return (1\u2013500, default 20).",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "start_date": {
    "description": "Start of date range in YYYY-MM-DD format (e.g. \"2024-01-01\").",
    "type": "string"
   },
   "year": {
    "description": "Filter by year (e.g. 2024). Data available from 2019.",
    "maximum": 2030,
    "minimum": 2019,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "bts.freight.indicators",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "BTS \u2014 Supply Chain & Freight Indicators"
}
