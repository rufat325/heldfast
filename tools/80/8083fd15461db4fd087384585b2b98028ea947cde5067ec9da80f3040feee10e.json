{
 "annotations": {
  "readOnlyHint": false,
  "title": "Save the layers as one picture"
 },
 "description": "Save the current arrangement of your own layers project as a finished picture in your library, composited at full quality from the original layer files. The project stays editable and you can flatten again.",
 "inputSchema": {
  "properties": {
   "layer_set_id": {
    "description": "The layers project id.",
    "type": "string"
   }
  },
  "required": [
   "layer_set_id"
  ],
  "type": "object"
 },
 "name": "flatten_layer_set",
 "title": "Save the layers as one picture"
}
