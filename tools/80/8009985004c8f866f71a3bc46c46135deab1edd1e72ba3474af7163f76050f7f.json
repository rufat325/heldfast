{
 "description": "List events \u2014 the lifecycle signals (charge:created, charge:confirmed, charge:failed, charge:delayed, charge:pending, charge:resolved) that Coinbase Commerce also delivers via webhook. Useful for reconciliation and agent polling.",
 "inputSchema": {
  "properties": {
   "ending_before": {
    "description": "Cursor: return results before this event id",
    "type": "string"
   },
   "limit": {
    "description": "Max results per page (default 25, max 100)",
    "type": "number"
   },
   "order": {
    "description": "Sort order by created_at. Defaults to desc.",
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   },
   "starting_after": {
    "description": "Cursor: return results after this event id",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_events"
}
