{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Multilingual metadata for one Statbel dataset by its id (a UUID, e.g. the dataSourceId from a view). Returns the dataset code `name`, the `descriptions` map (nl/fr/de/en text), supportedLocales, defaultLocale, category id and update dates.",
 "inputSchema": {
  "examples": [
   {
    "id": "89cf1b74-dc88-4a76-a215-a25dffed7f37"
   }
  ],
  "properties": {
   "id": {
    "description": "Dataset (datasource) UUID, e.g. \"89cf1b74-dc88-4a76-a215-a25dffed7f37\".",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_dataset",
 "title": "Get Dataset",
 "version": "v1.aaee6576"
}
