{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List or keyword-search tags in the data.gov.au CKAN catalogue; returns tag names useful as filters in `search`.",
 "inputSchema": {
  "examples": [
   {
    "query": "environment"
   },
   {
    "limit": 20,
    "query": "health"
   }
  ],
  "properties": {
   "limit": {
    "type": "number"
   },
   "query": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "tags",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of items returned.",
    "type": "integer"
   },
   "items": {
    "description": "List of tags, optionally filtered by query",
    "items": {
     "properties": {
      "id": {
       "description": "Tag UUID",
       "type": "string"
      },
      "name": {
       "description": "Tag name",
       "type": "string"
      },
      "vocabulary_id": {
       "description": "Vocabulary UUID if applicable",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "items",
   "count"
  ],
  "type": "object"
 },
 "title": "Tags",
 "version": "v1.0a610692"
}
