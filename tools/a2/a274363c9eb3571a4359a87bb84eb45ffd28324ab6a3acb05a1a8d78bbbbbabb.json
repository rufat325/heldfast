{
 "description": "Set a cookie in the browser, e.g. a session or feature-flag cookie to skip a login screen or switch on a test mode. Browsers only accept cookies for the site that is currently open, so navigate to a page on that domain first. The page does not see the cookie until its next request, so refresh (history) or navigate afterwards. Setting an existing name replaces that cookie.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "domain": {
    "description": "Domain the cookie applies to, e.g. '.example.com' to include subdomains. Defaults to the current page's host.",
    "type": "string"
   },
   "expiry": {
    "description": "Expiry as a Unix timestamp in seconds. Omit for a session cookie that ends when the browser closes.",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "httpOnly": {
    "description": "Hide the cookie from page JavaScript (document.cookie).",
    "type": "boolean"
   },
   "name": {
    "description": "Cookie name, e.g. 'session_id'.",
    "minLength": 1,
    "type": "string"
   },
   "path": {
    "description": "URL path the cookie applies to (default '/').",
    "type": "string"
   },
   "sameSite": {
    "description": "SameSite policy: Strict, Lax, or None (None also requires secure: true). Omit for the browser default.",
    "enum": [
     "Strict",
     "Lax",
     "None"
    ],
    "type": "string"
   },
   "secure": {
    "description": "Only send the cookie over HTTPS.",
    "type": "boolean"
   },
   "value": {
    "description": "Cookie value.",
    "type": "string"
   }
  },
  "required": [
   "name",
   "value"
  ],
  "type": "object"
 },
 "name": "add_cookie"
}
