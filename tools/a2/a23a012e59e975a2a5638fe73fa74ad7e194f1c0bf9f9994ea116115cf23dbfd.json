{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Rename a VPS \u2014 sets the hostname the machine reports and, on Proxmox, the label shown in the panel. Available for Proxmox VPS only; pass the service_id from `impreza_list_services` and the right one is used. Cosmetic on its own: it does not move DNS, so if the customer wants a name that resolves, add a DNS record too. Some guests only pick the new name up after a reboot.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "hostname": {
    "description": "New hostname, e.g. web-01.example.com.",
    "type": "string"
   },
   "service_id": {
    "description": "VPS service id. Proxmox VPS only; basic Cloud VPS does not support this operation.",
    "type": "number"
   }
  },
  "required": [
   "service_id",
   "hostname"
  ],
  "type": "object"
 },
 "name": "impreza_vps_set_hostname"
}
