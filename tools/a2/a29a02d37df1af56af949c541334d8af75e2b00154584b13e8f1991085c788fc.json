{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Retrieve prevalence of undernourishment (% of population) and count of undernourished people (millions) for any country over time. Data from FAO FAOSTAT \u2014 UN SDG Indicator 2.1.1. Covers 190+ countries, annual data since 2001. Accepts ISO alpha-2 (e.g. \"IN\"), alpha-3 (e.g. \"IND\"), or ISO numeric (e.g. \"356\") country codes.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "country_code": {
    "description": "Country code: ISO 3166-1 alpha-2 (e.g. \"IN\", \"US\", \"ET\"), alpha-3 (e.g. \"IND\"), or ISO numeric (e.g. \"356\")",
    "maxLength": 3,
    "minLength": 2,
    "type": "string"
   },
   "end_year": {
    "description": "End year for the time series (default: latest available)",
    "maximum": 2030,
    "minimum": 1961,
    "type": "integer"
   },
   "start_year": {
    "description": "Start year for the time series (default: 2015, earliest available: 1961)",
    "maximum": 2030,
    "minimum": 1961,
    "type": "integer"
   }
  },
  "required": [
   "country_code"
  ],
  "type": "object"
 },
 "name": "faostat.agriculture.food_security",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "FAO Undernourishment (SDG 2.1.1)"
}
