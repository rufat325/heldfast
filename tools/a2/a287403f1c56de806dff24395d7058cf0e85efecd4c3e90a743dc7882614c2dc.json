{
 "description": "Free. Read an agent's PUBLIC state from the FRC-55 registry on the live FractalAI L1 node (fractal_getAgent) \u2014 name, owner, status, model, price_per_call, total_calls, rating, total_ratings, created_at \u2014 for reputation/status checks. Omit agent_id to list all registered agents (fractal_listAgents). Always returns registry-wide stats (fractal_getAgentStats). This is on-chain registry data as reported by the node; ratings are the registry's own counters, not an independent audit.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "agent_id": {
    "description": "A 0x FRC-55 on-chain agent id to fetch. Omit to list all agents.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "lookup_agent",
 "title": "Look up an agent's public on-chain state"
}
