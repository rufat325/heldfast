{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get daily river discharge forecast in cubic meters per second for a location. Returns discharge values with timestamps. Use to monitor water flow rates for flood risk assessment.",
 "inputSchema": {
  "examples": [
   {
    "latitude": 40.7128,
    "longitude": -74.006
   },
   {
    "forecast_days": 14,
    "latitude": 48.8566,
    "longitude": 2.3522
   }
  ],
  "properties": {
   "forecast_days": {
    "description": "Number of forecast days to retrieve (1\u201392). Defaults to 7.",
    "type": "number"
   },
   "latitude": {
    "description": "Latitude of the location in decimal degrees.",
    "type": "number"
   },
   "longitude": {
    "description": "Longitude of the location in decimal degrees.",
    "type": "number"
   }
  },
  "required": [
   "latitude",
   "longitude"
  ],
  "type": "object"
 },
 "name": "get_river_discharge",
 "outputSchema": {
  "properties": {
   "days": {
    "description": "Array of daily forecast data",
    "items": {
     "properties": {
      "date": {
       "description": "ISO date string",
       "type": "string"
      },
      "river_discharge": {
       "description": "River discharge in cubic meters per second",
       "type": [
        "number",
        "null"
       ]
      }
     },
     "required": [
      "date"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "latitude": {
    "description": "Latitude of the location in decimal degrees",
    "type": "number"
   },
   "longitude": {
    "description": "Longitude of the location in decimal degrees",
    "type": "number"
   },
   "timezone": {
    "description": "Timezone name for the location",
    "type": "string"
   },
   "units": {
    "additionalProperties": {
     "type": "string"
    },
    "description": "Unit labels for each daily variable",
    "type": "object"
   }
  },
  "required": [
   "latitude",
   "longitude",
   "timezone",
   "units",
   "days"
  ],
  "type": "object"
 },
 "title": "Get River Discharge",
 "version": "v1.51424c8c"
}
