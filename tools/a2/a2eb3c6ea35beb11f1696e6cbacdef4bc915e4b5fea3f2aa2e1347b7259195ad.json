{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "The biggest greenhouse-gas-emitting facilities in a US state (EPA GHGRP), ranked by total CO2-equivalent emissions. Returns each facility's name, location, industry sector(s), and total metric tons CO2e. State-scoped (pass a state). Data lags ~1.5 years \u2014 latest full year is auto-selected (currently 2023) unless a `year` is given.",
 "inputSchema": {
  "examples": [
   {
    "state": "Texas"
   },
   {
    "facility_name": "Power Plant",
    "limit": 50,
    "state": "California"
   }
  ],
  "properties": {
   "facility_name": {
    "description": "Optional \u2014 filter to facilities whose name contains this text.",
    "type": "string"
   },
   "limit": {
    "description": "Max facilities to return, ranked by emissions (default 20, max 200).",
    "type": "number"
   },
   "state": {
    "description": "US state \u2014 2-letter code (\"TX\", \"CA\") or full name (\"Texas\"). Required (this is state-scoped, not national).",
    "type": "string"
   },
   "year": {
    "description": "Reporting year (default: latest available, ~2023).",
    "type": "number"
   }
  },
  "required": [
   "state"
  ],
  "type": "object"
 },
 "name": "ghg_facility_emissions",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of facilities returned",
    "type": "number"
   },
   "facilities": {
    "description": "Array of facility records with emissions data",
    "items": {
     "properties": {
      "city": {
       "description": "City location",
       "type": [
        "string",
        "null"
       ]
      },
      "facility_id": {
       "description": "Facility identifier",
       "type": [
        "number",
        "null"
       ]
      },
      "industry_type": {
       "description": "Industry classification",
       "type": [
        "string",
        "null"
       ]
      },
      "latitude": {
       "description": "Geographic latitude",
       "type": [
        "number",
        "null"
       ]
      },
      "longitude": {
       "description": "Geographic longitude",
       "type": [
        "number",
        "null"
       ]
      },
      "name": {
       "description": "Facility name",
       "type": [
        "string",
        "null"
       ]
      },
      "state": {
       "description": "State name",
       "type": [
        "string",
        "null"
       ]
      },
      "total_ghg_emissions_mt_co2e": {
       "description": "Total GHG emissions in metric tons CO2 equivalent",
       "type": [
        "number",
        "null"
       ]
      },
      "year": {
       "description": "Reporting year",
       "type": [
        "number",
        "null"
       ]
      },
      "zip": {
       "description": "ZIP code",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "state": {
    "description": "State name searched",
    "type": "string"
   }
  },
  "required": [
   "count",
   "state",
   "facilities"
  ],
  "type": "object"
 },
 "title": "Ghg Facility Emissions",
 "version": "v1.40534239"
}
