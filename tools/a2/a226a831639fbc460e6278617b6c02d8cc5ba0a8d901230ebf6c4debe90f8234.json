{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the most recently published FBI wanted posters, newest first, optionally restricted to one FBI field office. Useful for monitoring newly added subjects (api.fbi.gov, public US Government data)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "field_office": {
    "description": "Restrict to a specific FBI field office slug (e.g. \"seattle\", \"miami\", \"losangeles\").",
    "type": "string"
   },
   "page_size": {
    "description": "Number of most recently published entries to return (default 20, max 50).",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "fbiwanted.persons.recent",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Recently Published FBI Wanted Posters"
}
