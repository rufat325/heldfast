{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List folders"
 },
 "description": "List the workspace's folders with their full path, code count, id and slug. Use a folder's id or slug with create_qr_code / update_qr_code (`folder`) to file codes, or with list_qr_codes to filter.",
 "inputSchema": {
  "properties": {
   "workspace": {
    "description": "Workspace id. Optional when the account has one workspace; use list_workspaces to resolve otherwise.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_folders",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "count": {
    "description": "Total matching rows across all pages.",
    "type": "integer"
   },
   "error": {
    "additionalProperties": true,
    "description": "Present only when the tool failed (the result is also flagged isError).",
    "properties": {
     "cause": {
      "description": "Machine-readable failure category: invalid_argument, not_found, forbidden, plan_limit, rate_limited, or server_error.",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "retryable": {
      "description": "Whether repeating the same call could succeed.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "next": {
    "description": "URL of the next page, or null on the last page.",
    "type": [
     "string",
     "null"
    ]
   },
   "previous": {
    "type": [
     "string",
     "null"
    ]
   },
   "results": {
    "items": {
     "additionalProperties": true,
     "properties": {
      "code_count": {
       "type": "integer"
      },
      "created": {
       "type": "string"
      },
      "id": {
       "type": "string"
      },
      "name": {
       "type": "string"
      },
      "parent": {
       "description": "Parent folder id, null at the top level.",
       "type": [
        "string",
        "null"
       ]
      },
      "path": {
       "description": "Full path, e.g. Campaigns / Spring 2026.",
       "type": "string"
      },
      "slug": {
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 }
}
