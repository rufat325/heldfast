{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "LLM-as-judge for a prompt. Scores it 0\u201310 across 6 default dimensions (clarity, specificity, intent_alignment, format_fitness, length_appropriateness, plain_language) \u2014 or your own custom criteria \u2014 and returns per-dimension rationale + concrete suggestions, an overall score, and a verdict (`accept` / `revise` / `reject`). When the score is below `revise_threshold` (default 7.0), the tool also returns an `improvedPrompt` you can use as a drop-in replacement. Use it pre-flight (is this prompt good enough for the expensive model?), postmortem (was the prompt the cause of a bad output?), or to A/B-pick the best of N optimization variants. Pass `original_prompt` when critiquing an optimized version so the judge can verify intent was preserved.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "category": {
    "enum": [
     "chat",
     "image",
     "voice",
     "video",
     "music",
     "code",
     "document"
    ],
    "type": "string"
   },
   "criteria": {
    "description": "Override the default 6 criteria. Up to ~8 dimensions; more bloats the judge call.",
    "items": {
     "additionalProperties": false,
     "properties": {
      "description": {
       "description": "One sentence explaining what this dimension measures.",
       "type": "string"
      },
      "name": {
       "description": "Short snake_case identifier.",
       "type": "string"
      }
     },
     "required": [
      "name",
      "description"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "cwd": {
    "type": "string"
   },
   "file_excerpt": {
    "type": "string"
   },
   "file_language": {
    "type": "string"
   },
   "file_path": {
    "type": "string"
   },
   "original_prompt": {
    "description": "If `prompt` is an optimized version, the user's original ask. Used for the intent_alignment dimension.",
    "type": "string"
   },
   "prompt": {
    "description": "The candidate prompt to critique.",
    "type": "string"
   },
   "revise_threshold": {
    "default": 7,
    "description": "Overall score below this triggers the rewrite pass. Default 7.0.",
    "maximum": 10,
    "minimum": 0,
    "type": "number"
   },
   "skip_rewrite": {
    "default": false,
    "description": "Skip the rewrite pass even when below threshold (faster; just returns scores).",
    "type": "boolean"
   },
   "user_locale": {
    "type": "string"
   }
  },
  "required": [
   "prompt"
  ],
  "type": "object"
 },
 "name": "critique_prompt",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true,
  "properties": {
   "analysis": {
    "additionalProperties": true,
    "properties": {
     "category": {
      "type": "string"
     },
     "confidence": {
      "type": "string"
     },
     "intent": {
      "type": "string"
     },
     "recommendedMode": {
      "type": "string"
     },
     "source": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "dimensions": {
    "items": {
     "additionalProperties": true,
     "properties": {},
     "type": "object"
    },
    "type": "array"
   },
   "improvedPrompt": {
    "type": "string"
   },
   "improvements": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "judgeModel": {
    "type": "string"
   },
   "latencyMs": {
    "type": "number"
   },
   "overallScore": {
    "type": "number"
   },
   "summary": {
    "type": "string"
   },
   "verdict": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "title": "Critique a prompt (LLM-as-judge)"
}
