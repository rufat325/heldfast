{
 "description": "List providers registered on the marketplace with their models, health/status, and flagged state. No signature required. Use it to find your provider id (match on payoutAddress) after registering.\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "allowUnsafeGateway": {
    "description": "Allow a gateway host outside localhost / *.aibtc.com. Required to send wallet-signed headers (and any apiKey) to an arbitrary host \u2014 leave unset unless you deliberately self-host the gateway.",
    "type": "boolean"
   },
   "gateway": {
    "description": "Marketplace gateway base URL. Defaults to https://inference.aibtc.com. Use http://localhost:8787 for local dev.",
    "format": "uri",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "inference_list_providers"
}
