{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List Sessions"
 },
 "description": "List sessions for a hashed Automate/App Automate build: session IDs, status, OS, browser/device, dashboard URL.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "buildId": {
    "description": "Hashed build id from the dashboard, or the observability build id from getBuildId.",
    "type": "string"
   },
   "limit": {
    "description": "Max sessions to return. Defaults to 10.",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "offset": {
    "description": "Pagination offset for the REST session list.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "sessionType": {
    "description": "Type of BrowserStack session",
    "enum": [
     "automate",
     "app-automate"
    ],
    "type": "string"
   },
   "status": {
    "description": "Optional session status filter (e.g. done, running, error). Applied client-side.",
    "type": "string"
   }
  },
  "required": [
   "sessionType",
   "buildId"
  ],
  "type": "object"
 },
 "name": "listSessions"
}
