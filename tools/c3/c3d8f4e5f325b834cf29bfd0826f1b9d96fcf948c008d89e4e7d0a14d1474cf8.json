{
 "annotations": {
  "readOnlyHint": true,
  "title": "Filter threads"
 },
 "description": "Filter email threads by criteria such as assignee, tags, or column. More targeted than search \u2014 use this when you know specific filter values.",
 "inputSchema": {
  "properties": {
   "assignees": {
    "description": "Filter by assignee user IDs (as strings). Use \"-1\" for unassigned threads. Get user IDs from list_teams or list_board_members.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "boardId": {
    "description": "The board ID to filter within",
    "type": "number"
   },
   "columnId": {
    "description": "The column ID to filter within (required)",
    "type": "string"
   },
   "tagList": {
    "description": "Filter by shared label IDs (as strings). Use list_labels to get available label IDs.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "boardId",
   "columnId"
  ],
  "type": "object"
 },
 "name": "filter_threads",
 "title": "Filter threads"
}
