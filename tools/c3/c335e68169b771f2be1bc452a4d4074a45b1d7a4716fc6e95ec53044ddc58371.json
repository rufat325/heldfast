{
 "description": "Create a credential on the instance. Use get_credential_schema first to see which fields the credential type requires. The n8n public API never returns credential secrets \u2014 only metadata.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "data": {
    "additionalProperties": {},
    "description": "Credential fields as required by the type's schema",
    "type": "object"
   },
   "name": {
    "description": "Display name for the credential",
    "minLength": 1,
    "type": "string"
   },
   "type": {
    "description": "Credential type name, e.g. 'slackApi', 'httpHeaderAuth'",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "name",
   "type",
   "data"
  ],
  "type": "object"
 },
 "name": "create_credential"
}
