{
 "description": "Change the password gate on a hosted drop. Zero-knowledge means re-keying re-encrypts, so re-supply the HTML; returns the new URL + password.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "html": {
    "description": "The drop's HTML (required \u2014 content is re-encrypted under the new password).",
    "type": "string"
   },
   "password": {
    "description": "New password (auto-generated if omitted).",
    "type": "string"
   },
   "slug": {
    "description": "The slug to re-key.",
    "type": "string"
   }
  },
  "required": [
   "slug",
   "html"
  ],
  "type": "object"
 },
 "name": "set_password"
}
