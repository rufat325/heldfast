{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Check the tenant's approved sources for real changes and store signal records in its own Memory Vault. It never posts. 2 credits (EUR 0.02) per successful call; needs a bearer token.",
 "inputSchema": {
  "properties": {
   "maxSources": {
    "description": "How many due sources to check this run (default 5, max 5).",
    "maximum": 5,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "monitor_sources",
 "outputSchema": {
  "properties": {
   "billing": {
    "type": "object"
   },
   "data": {
    "type": "object"
   },
   "kind": {
    "type": "string"
   }
  },
  "required": [
   "kind",
   "data",
   "billing"
  ],
  "type": "object"
 }
}
