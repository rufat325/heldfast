{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Weather forecast for any coordinates worldwide: current conditions plus a condensed hourly forecast (temperature, wind, humidity, precipitation, condition codes) and a will-it-rain summary. Official MET Norway model data (the institute behind yr.no) \u2014 commercial use permitted, attribution included. $0.002 per call. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "hours": {
    "description": "Hourly entries to return (1-48, default 24)",
    "type": "integer"
   },
   "lat": {
    "description": "Latitude -90..90",
    "type": "number"
   },
   "lon": {
    "description": "Longitude -180..180",
    "type": "number"
   }
  },
  "required": [
   "lat",
   "lon"
  ],
  "type": "object"
 },
 "name": "agentbit.weather",
 "title": "Weather Forecast"
}
