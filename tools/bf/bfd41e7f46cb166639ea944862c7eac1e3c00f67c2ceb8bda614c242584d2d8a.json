{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List Funcheap event facet tags (slug, name, count). These are the filterable facets like \"free\", \"live-music\", \"comedy\", neighborhoods. Sorted by usage.",
 "inputSchema": {
  "examples": [
   {
    "query": "music"
   },
   {
    "per_page": 25
   }
  ],
  "properties": {
   "per_page": {
    "description": "How many to return (1-50, default 40).",
    "type": "number"
   },
   "query": {
    "description": "Optional search to find matching tags, e.g. \"music\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "tags",
 "title": "Tags",
 "version": "v1.d2910983"
}
