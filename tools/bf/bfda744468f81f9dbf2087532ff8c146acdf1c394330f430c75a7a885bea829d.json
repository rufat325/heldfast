{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Deny Guest Request"
 },
 "description": "Deny someone on the event WAITLIST / approval queue \u2014 releases their held slot back to the tier, frees them to re-register, and emails them. Get request_id from list_guest_requests. Requires event_id + request_id; you must be a host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "reason": {
    "maxLength": 1000,
    "type": "string"
   },
   "request_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "request_id"
  ],
  "type": "object"
 },
 "name": "deny_guest_request",
 "title": "Deny Guest Request"
}
