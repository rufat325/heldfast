{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Delete product"
 },
 "description": "[MyStoq] Delete a product from the authenticated store (reversible cleanup \u2014 used by the agent's ephemeral live-demo). Requires Bearer token; only the owner's own product. WRITE/DESTRUCTIVE.",
 "inputSchema": {
  "properties": {
   "product_id": {
    "description": "Product id to delete.",
    "type": "integer"
   }
  },
  "required": [
   "product_id"
  ],
  "type": "object"
 },
 "name": "mystoq__delete_product"
}
