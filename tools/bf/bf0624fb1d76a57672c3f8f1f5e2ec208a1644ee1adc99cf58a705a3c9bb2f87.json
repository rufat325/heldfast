{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "POST a PxWeb query to a Statistics Greenland table and return observations as json-stat2. body must be {query:[{code, selection:{filter,values}}], response:{format:'json-stat2'}}. An empty query ({query:[]}) requests the full table but may be rejected for large tables \u2014 narrow selections using codes from table_meta.",
 "inputSchema": {
  "examples": [
   {
    "body": {
     "query": [
      {
       "code": "time",
       "selection": {
        "filter": "item",
        "values": [
         "2024",
         "2025"
        ]
       }
      },
      {
       "code": "type",
       "selection": {
        "filter": "item",
        "values": [
         "Number"
        ]
       }
      },
      {
       "code": "place of birth",
       "selection": {
        "filter": "item",
        "values": [
         "Total"
        ]
       }
      }
     ],
     "response": {
      "format": "json-stat2"
     }
    },
    "path": "BE/BE01/BEXSAT1.PX"
   }
  ],
  "properties": {
   "body": {
    "description": "{query: [{code, selection: {filter, values}}], response: {format: \"json-stat2\"}}",
    "type": "object"
   },
   "path": {
    "description": "e.g. \"BE/BE01/BEXSAT1.PX\"",
    "type": "string"
   }
  },
  "required": [
   "path",
   "body"
  ],
  "type": "object"
 },
 "name": "query_table",
 "title": "Query Table",
 "version": "v1.754ff18c"
}
