{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search Bioregistry for biological database prefixes by substring match against prefix name, title, or description (e.g. \"gene ontology\"). Returns up to `limit` matching prefix records.",
 "inputSchema": {
  "examples": [
   {
    "query": "protein"
   },
   {
    "limit": 10,
    "query": "gene"
   }
  ],
  "properties": {
   "limit": {
    "type": "number"
   },
   "query": {
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search",
 "outputSchema": {
  "description": "Search results matching query",
  "properties": {
   "count": {
    "description": "Total number of matches",
    "type": "number"
   },
   "query": {
    "description": "The search query",
    "type": "string"
   },
   "results": {
    "description": "Matching prefix entries",
    "items": {
     "properties": {
      "description": {
       "description": "Prefix description",
       "type": [
        "string",
        "null"
       ]
      },
      "name": {
       "description": "Prefix name",
       "type": [
        "string",
        "null"
       ]
      },
      "prefix": {
       "description": "Prefix identifier",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "query",
   "count",
   "results"
  ],
  "type": "object"
 },
 "title": "Search",
 "version": "v1.d3182749"
}
