{
 "description": "Get the top content moves linked to higher AI search citation rates in your industry. Returns a plain-language recommendation (or use flavor=full for the ranked list of all moves). Some industries may have no recommendation yet if the data is still maturing.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "flavor": {
    "default": "active",
    "description": "'active' (default) returns a single top recommendation. 'full' returns the ranked list of every positive move for your industry.",
    "enum": [
     "active",
     "full"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_citation_formula"
}
