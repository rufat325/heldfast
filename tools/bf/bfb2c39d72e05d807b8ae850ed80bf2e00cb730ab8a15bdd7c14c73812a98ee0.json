{
 "annotations": {
  "destructiveHint": true,
  "title": "Delete a release/track asset"
 },
 "description": "Delete a track asset (track_*) or an animated cover / motion artwork video (release_motion_*). Allowed only while the parent release is an editable draft; refused once locked or distributed. Cover art cannot be deleted.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "The track id (track_*) or release id (release_motion_*).",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "target": {
    "description": "Which asset to delete.",
    "enum": [
     "track_stereo",
     "track_dolby",
     "track_lyrics",
     "release_motion_square",
     "release_motion_tall"
    ],
    "type": "string"
   }
  },
  "required": [
   "target",
   "id"
  ],
  "type": "object"
 },
 "name": "delete_asset",
 "title": "Delete a release/track asset"
}
