{
 "_meta": {
  "openai/widgetAccessible": true
 },
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Download QR code files"
 },
 "description": "Get downloadable, print-ready image links for a QR code \u2014 a high-resolution PNG and a vector SVG (best for print/large sizes). Returns URLs you can share or save.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "QR code id.",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "download_qr",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "error": {
    "additionalProperties": true,
    "description": "Present only when the tool failed (the result is also flagged isError).",
    "properties": {
     "cause": {
      "description": "Machine-readable failure category: invalid_argument, not_found, forbidden, plan_limit, rate_limited, or server_error.",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "retryable": {
      "description": "Whether repeating the same call could succeed.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "id": {
    "type": "string"
   },
   "png_url": {
    "description": "Print-ready PNG; null if rendering failed.",
    "type": [
     "string",
     "null"
    ]
   },
   "svg_url": {
    "type": [
     "string",
     "null"
    ]
   }
  },
  "type": "object"
 }
}
