{
 "description": "Inspect a local container image \u2014 shows architecture, labels, environment variables, entrypoint, exposed ports, and layer count",
 "inputSchema": {
  "properties": {
   "image_name": {
    "description": "Image name/tag to inspect",
    "type": "string"
   },
   "runtime": {
    "enum": [
     "docker",
     "podman"
    ],
    "type": "string"
   }
  },
  "required": [
   "image_name"
  ],
  "type": "object"
 },
 "name": "inspect_container_image"
}
