{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Add a DNS record to an Impreza-managed domain. `host` is the record name (\"@\" for the apex, \"www\", \"mail\", \u2026); `value` is the target (IP, hostname, or text).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "domain": {
    "description": "The domain name, e.g. example.com.",
    "type": "string"
   },
   "host": {
    "description": "Record name/host, e.g. \"@\" for the apex, \"www\", \"mail\".",
    "type": "string"
   },
   "priority": {
    "description": "Priority (MX / SRV only).",
    "type": "number"
   },
   "ttl": {
    "description": "Time-to-live in seconds. Default 14400.",
    "type": "number"
   },
   "type": {
    "description": "Record type: A, AAAA, CNAME, MX, TXT, NS, SRV, \u2026",
    "type": "string"
   },
   "value": {
    "description": "Record value/target (IP, hostname, or text).",
    "type": "string"
   }
  },
  "required": [
   "domain",
   "type",
   "host",
   "value"
  ],
  "type": "object"
 },
 "name": "impreza_add_dns_record"
}
