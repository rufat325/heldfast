{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get SAP MCP Pricing Catalog"
 },
 "description": "Free machine-readable x402/pay.sh pricing catalog generated from the hosted SAP MCP pricing registry. Use before paid calls to understand free, micro-read, read-premium, builder, value-action, and batch tiers.\n\nSAP MCP execution guidance: Intent: SAP MCP tool workflow. Pricing: free; call directly without x402. Routing: free hosted call; call directly and keep it small/exact when possible. Signer boundary: hosted reads/builders never receive keypair bytes; value-moving results must be finalized locally when signing is required.",
 "inputSchema": {
  "description": "Get SAP MCP Pricing Catalog input schema. Intent: SAP MCP tool workflow. Pricing: free; call directly without x402. Routing: free hosted call; call directly and keep it small/exact when possible. Signer boundary: hosted reads/builders never receive keypair bytes; value-moving results must be finalized locally when signing is required. Use exact field names from this schema; do not guess aliases or include private key material.",
  "properties": {},
  "type": "object"
 },
 "name": "sap_pricing_catalog",
 "outputSchema": {
  "properties": {
   "currency": {
    "description": "Display currency for USD-denominated prices.",
    "type": "string"
   },
   "runtimeRules": {
    "description": "Agent routing rules for paid and write flows.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "source": {
    "description": "Pricing catalog source identifier.",
    "type": "string"
   },
   "strictTools": {
    "description": "Whether strict hosted tool pricing is enabled.",
    "type": "boolean"
   },
   "tiers": {
    "description": "Tier pricing rules and examples.",
    "type": "object"
   },
   "toolSets": {
    "description": "Current built-in tool sets for each tier.",
    "type": "object"
   },
   "version": {
    "description": "Pricing catalog schema version.",
    "type": "number"
   }
  },
  "required": [
   "source",
   "version",
   "strictTools",
   "currency",
   "tiers",
   "toolSets",
   "runtimeRules"
  ],
  "type": "object"
 },
 "title": "Get SAP MCP Pricing Catalog"
}
