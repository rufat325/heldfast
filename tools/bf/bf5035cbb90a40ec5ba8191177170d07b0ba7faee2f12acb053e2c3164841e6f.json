{
 "description": "Take a screenshot of the iOS Simulator screen. Returns the image directly for viewing.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "deviceId": {
    "description": "Device UDID, name, or \"booted\" (default: booted)",
    "type": "string"
   },
   "display": {
    "description": "Display to capture (default: internal)",
    "enum": [
     "internal",
     "external"
    ],
    "type": "string"
   },
   "format": {
    "description": "Image format (default: jpeg). JPEG recommended for AI \u2014 smaller, faster.",
    "enum": [
     "png",
     "jpeg"
    ],
    "type": "string"
   },
   "mask": {
    "description": "For non-rectangular displays, handle mask by policy",
    "enum": [
     "ignored",
     "alpha",
     "black"
    ],
    "type": "string"
   },
   "savePath": {
    "description": "Optional: save a copy to this path on disk",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "simulator_screenshot"
}
