{
 "description": "Create a new X-Matrix. Use this to start a strategy from scratch: create the matrix, then add elements with upsert_element and wire them with set_correlation. The first matrix of an organization becomes the default one.",
 "inputSchema": {
  "properties": {
   "description": {
    "description": "What this deployment covers",
    "type": "string"
   },
   "is_default": {
    "description": "Make this the default matrix of the organization",
    "type": "boolean"
   },
   "name": {
    "description": "Name of the matrix, e.g. \"Strategy 2027\" (required)",
    "type": "string"
   },
   "period_end": {
    "description": "End of the period, ISO date (YYYY-MM-DD)",
    "type": "string"
   },
   "period_start": {
    "description": "Start of the period, ISO date (YYYY-MM-DD)",
    "type": "string"
   },
   "period_type": {
    "description": "Planning horizon (default: annual)",
    "enum": [
     "annual",
     "multi_year",
     "quarterly"
    ],
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "create_matrix"
}
