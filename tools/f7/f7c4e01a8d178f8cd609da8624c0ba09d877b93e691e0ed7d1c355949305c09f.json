{
 "description": "Columns (type/nullable/default), primary key, foreign keys, and indexes for a table.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "connection": {
    "description": "Configured connection name (see list_connections)",
    "type": "string"
   },
   "schema": {
    "type": "string"
   },
   "table": {
    "type": "string"
   }
  },
  "required": [
   "connection",
   "table"
  ],
  "type": "object"
 },
 "name": "describe_table",
 "title": "Describe table"
}
