{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "General LLM completion: send a prompt (optional system role), get an answer. Reason, rewrite, classify, draft or answer. Flat price, input/output capped. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "max_tokens": {
    "description": "Max output tokens (default 512, hard cap 1024).",
    "type": "integer"
   },
   "prompt": {
    "description": "The user prompt (required, up to ~16 KB).",
    "type": "string"
   },
   "system": {
    "description": "Optional system instruction to steer style/role.",
    "type": "string"
   },
   "temperature": {
    "description": "Sampling temperature 0-2 (default 0.3).",
    "type": "number"
   }
  },
  "required": [
   "prompt"
  ],
  "type": "object"
 },
 "name": "agentbit.ai_ask",
 "title": "AI Ask (LLM completion)"
}
