{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Create the visible-host-thread AlphaCouncil run after a confirmed master selection. Requires a one-time selection_receipt. Does NOT execute. Run every returned evidence_agent, then every returned master_agent. Physical v3 master agents explain a frozen deterministic stance and may not change it. Record all required bull/bear rounds before the portfolio_manager decision; PM rejects any returned master_agent that was not recorded.",
 "inputSchema": {
  "properties": {
   "as_of": {
    "description": "Analysis date YYYY-MM-DD. Defaults to today.",
    "type": "string"
   },
   "council_mode": {
    "default": "full",
    "description": "full runs the analyst scope frozen in the receipt (core=8, all=11) and a three-round cross-exam. quick runs the fixed news-inclusive four-role preset, up to four methods, one parallel bull/bear round, a short PM, and a hard global budget.",
    "enum": [
     "full",
     "quick"
    ],
    "type": "string"
   },
   "grounding": {
    "description": "The `grounding` object from compose_research_brief. Injected into every analyst prompt.",
    "type": "object"
   },
   "language": {
    "default": "auto",
    "description": "Reader-facing language for subagents and final report, e.g. auto, zh-CN, en-US, ja-JP. Auto infers from prompt.",
    "type": "string"
   },
   "prompt": {
    "description": "User objective or extra instructions.",
    "type": "string"
   },
   "run_id": {
    "type": "string"
   },
   "seat_weights": {
    "description": "Deprecated compatibility metadata. It may be persisted for audit but does not alter the PM rating rubric, count method votes, or override objective/horizon contribution classes.",
    "type": "object"
   },
   "selection_receipt": {
    "description": "One-run receipt returned by confirm_master_selection. Required for every council run and consumed exactly once.",
    "type": "string"
   },
   "symbol": {
    "description": "Exchange ticker. US, HK, JP, KR, CN and TW symbols all work, e.g. AAPL, 0700.HK, 7203.T, 005930.KS, 600519.SS.",
    "type": "string"
   },
   "tasks": {
    "items": {
     "enum": [
      "macro_regime",
      "market_data",
      "earnings_deep_dive",
      "forward_expectations",
      "quant_factor",
      "valuation_long_short",
      "news_industry_management",
      "market_narrative",
      "social_pulse",
      "insider_sec",
      "ib_event_analysis"
     ],
     "type": "string"
    },
    "type": "array"
   },
   "total_timeout_ms": {
    "description": "Hard queue-to-persistence wall-clock budget. Cannot exceed the selected council_pace's total (fast 900000, normal 1800000, slow 3600000) or ten minutes for quick. Callers may lower the applicable ceiling, never raise it.",
    "maximum": 3600000,
    "type": "number"
   }
  },
  "required": [
   "symbol",
   "selection_receipt"
  ],
  "type": "object"
 },
 "name": "plan_visible_run",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 },
 "title": "Plan a Visible Research Run"
}
