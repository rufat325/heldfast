{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Check income eligibility thresholds (extremely low, very low, low-income) for HUD programs by area and family size. Returns income limits by category. Use to determine program qualification.",
 "inputSchema": {
  "examples": [
   {
    "state_code": "TX"
   },
   {
    "entity_id": "06037",
    "state_code": "CA",
    "year": 2024
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "HUD API token",
    "type": "string"
   },
   "entity_id": {
    "description": "FIPS code or metro area code for a specific area. Omit to get all areas in the state.",
    "type": "string"
   },
   "state_code": {
    "description": "Two-letter state code (e.g., \"CA\", \"NY\").",
    "type": "string"
   },
   "year": {
    "description": "Fiscal year (e.g., 2024). Omit for the most recent year.",
    "type": "number"
   }
  },
  "required": [
   "state_code",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "hud_income_limits",
 "outputSchema": {
  "properties": {
   "data": {
    "description": "Income limits data from HUD API",
    "type": "object"
   },
   "entity_id": {
    "description": "FIPS code or metro area code if provided, null otherwise",
    "type": [
     "string",
     "null"
    ]
   },
   "state": {
    "description": "Two-letter state code provided in request",
    "type": "string"
   },
   "year": {
    "description": "Fiscal year requested or 'latest'",
    "type": [
     "number",
     "string"
    ]
   }
  },
  "required": [
   "state",
   "entity_id",
   "year",
   "data"
  ],
  "type": "object"
 },
 "title": "Hud Income Limits",
 "version": "v1.0450d822"
}
