{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Delete an account group"
 },
 "description": "Remove a group. The accounts in it are not touched and stay connected \u2014 only the name and its membership go. Posts already scheduled through it are unaffected, because a post records the accounts rather than the group.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "name": {
    "description": "The group to remove, from groups_list.",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "groups_delete",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "deleted": {
    "type": "string"
   }
  },
  "required": [
   "deleted"
  ],
  "type": "object"
 },
 "title": "Delete an account group"
}
