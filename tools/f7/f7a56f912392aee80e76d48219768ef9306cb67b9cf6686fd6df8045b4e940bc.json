{
 "annotations": {
  "readOnlyHint": true,
  "title": "Find operations"
 },
 "description": "UNDERSTANDING \u2014 CATALOG-WIDE operation search: which providers expose a path matching your terms. \"Who has a POST /refunds\" had no answer before this \u2014 tags are what a company says about itself, a path is what its API accepts. Terms match path SEGMENTS (all must appear), so q=\"payment intents\" finds /payments/v1/intents. Add method to narrow by verb, deprecated=true for the retiring ones. One row per real operation: a provider's spec is split per tag, so the same call can live in several of its documents, and `apis` lists them all. Reads matching providers' surfaces up to max_providers (cap 100) and tells you in `meta.query.truncated` when more matched than were read. Priced per result \u2014 $0.005 per request plus $0.0002 per record returned (less past 100), capped at $0.50 a request, under pay-as-you-go; included in Understanding and Influence. Estimate first at /api/v1/prices/estimate. See apis://prices.",
 "inputSchema": {
  "properties": {
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "deprecated": {
    "description": "Only operations the provider marked deprecated.",
    "type": "boolean"
   },
   "limit": {
    "default": 25,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "max_providers": {
    "description": "How many matching providers to read (default 40, cap 100).",
    "type": "integer"
   },
   "method": {
    "enum": [
     "GET",
     "POST",
     "PUT",
     "PATCH",
     "DELETE",
     "HEAD",
     "OPTIONS"
    ],
    "type": "string"
   },
   "page": {
    "default": 1,
    "minimum": 1,
    "type": "integer"
   },
   "q": {
    "description": "Path words, all of which must appear, e.g. \"refunds\" or \"payment intents\".",
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "find_operations"
}
