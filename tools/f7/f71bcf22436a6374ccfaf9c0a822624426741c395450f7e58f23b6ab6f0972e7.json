{
 "description": "Detach a recurring job (or job group) from a volume \u2014 this can remove the volume's only backup/snapshot schedule. Destructive. Do not call this unless the user has explicitly authorized it in this conversation \u2014 never proactively, speculatively, or as a side effect of another task. Longhorn docs: https://longhorn.io/docs/1.12.1/snapshots-and-backups/scheduling-backups-and-snapshots/",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "isGroup": {
    "type": "boolean"
   },
   "jobName": {
    "type": "string"
   },
   "name": {
    "type": "string"
   }
  },
  "required": [
   "name",
   "jobName"
  ],
  "type": "object"
 },
 "name": "volume_recurring_job_delete",
 "title": "Remove recurring job from volume"
}
