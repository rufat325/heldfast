{
 "description": "Fetch the last N workflow runs and compute job-level timing statistics (avg, min, max, p95) across those runs. Useful for identifying slow jobs and trends.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "count": {
    "description": "Number of recent runs to analyse (default: 10, max: 100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "owner": {
    "description": "GitHub repository owner (user or org)",
    "pattern": "^[a-zA-Z0-9._-]+$",
    "type": "string"
   },
   "repo": {
    "description": "GitHub repository name",
    "pattern": "^[a-zA-Z0-9._-]+$",
    "type": "string"
   },
   "workflow_id": {
    "description": "Workflow file name (e.g. ci.yml) or numeric workflow ID",
    "type": "string"
   }
  },
  "required": [
   "owner",
   "repo",
   "workflow_id"
  ],
  "type": "object"
 },
 "name": "list_workflow_performance",
 "title": "List Workflow Performance"
}
