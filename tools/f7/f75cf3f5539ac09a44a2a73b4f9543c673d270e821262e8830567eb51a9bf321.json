{
 "description": "Salvage a volume from a list of (potentially stale) replica names. Destructive. Do not call this unless the user has explicitly authorized it in this conversation \u2014 never proactively, speculatively, or as a side effect of another task. Longhorn docs: https://longhorn.io/docs/1.12.1/nodes-and-volumes/volumes/create-volumes/",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "name": {
    "type": "string"
   },
   "replicaNames": {
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "name",
   "replicaNames"
  ],
  "type": "object"
 },
 "name": "volume_salvage",
 "title": "Salvage volume"
}
