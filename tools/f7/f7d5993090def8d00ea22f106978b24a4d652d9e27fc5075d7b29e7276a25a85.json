{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the City of Toronto open-data catalogue (open.toronto.ca, CKAN) by keyword. Returns each matching dataset's title and its queryable datastore resource ids (use with toronto_query).",
 "inputSchema": {
  "examples": [
   {
    "query": "building permits"
   },
   {
    "limit": 10,
    "query": "parking"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max datasets (1-50, default 15).",
    "type": "number"
   },
   "query": {
    "description": "Keyword(s), e.g. \"parking\", \"trees\", \"budget\".",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "toronto_datasets",
 "title": "Toronto Datasets",
 "version": "v1.58becb1b"
}
