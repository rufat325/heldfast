{
 "description": "Retrieve active stickies relevant to the current session, ordered by importance.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "include_global": {
    "default": true,
    "description": "Also include global (no project) stickies.",
    "type": "boolean"
   },
   "limit": {
    "default": 50,
    "description": "Max stickies to return.",
    "exclusiveMinimum": 0,
    "maximum": 500,
    "type": "integer"
   },
   "min_importance": {
    "default": "P3",
    "description": "Lowest importance to include (P1 = only critical).",
    "enum": [
     "P1",
     "P2",
     "P3"
    ],
    "type": "string"
   },
   "project_path": {
    "description": "Absolute project root to scope to. Omit to read across all projects.",
    "type": [
     "string",
     "null"
    ]
   }
  },
  "type": "object"
 },
 "name": "stickies_read",
 "title": "Read stickies"
}
