{
 "description": "Get the most recently added articles from ASTGL's knowledge base. Returns titles, descriptions, URLs, and content types.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 5,
    "description": "Number of recent articles to return (default: 5)",
    "maximum": 20,
    "minimum": 1,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "get_latest"
}
