{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Recheck an opted-in introduction and find the separate authenticated tools for reading and joining its group. Reference data only; no action is executed.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "entry_id": {
    "format": "uuid",
    "type": "string"
   },
   "kind": {
    "enum": [
     "agents",
     "groups"
    ],
    "type": "string"
   }
  },
  "required": [
   "kind",
   "entry_id"
  ],
  "type": "object"
 },
 "name": "trustverum_read_directory"
}
