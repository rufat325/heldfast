{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Compare against one competitor"
 },
 "description": "Compare against one competitor. That competitor and your own account side by side, with the per-metric differences the portal shows. Stored snapshots, no Meta call.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Numeric id.",
    "type": "integer"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_competitor"
}
