{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Nameserver (glue) record from RDAP \u2014 the registry's record for a host like \"ns1.example.com\", with its IP addresses and status. Answers \"what are the IPs behind this nameserver\" and \"is this nameserver registered\".",
 "inputSchema": {
  "examples": [
   {
    "host": "ns1.google.com"
   },
   {
    "host": "a.ns.apple.com"
   }
  ],
  "properties": {
   "base": {
    "description": "Optional registry RDAP base URL. Skips IANA bootstrap lookup \u2014 use it when a TLD comes back as tld_not_in_iana_bootstrap.",
    "type": "string"
   },
   "host": {
    "description": "Nameserver hostname, e.g. \"ns1.google.com\".",
    "type": "string"
   }
  },
  "required": [
   "host"
  ],
  "type": "object"
 },
 "name": "nameserver",
 "outputSchema": {
  "description": "RDAP nameserver record",
  "type": "object"
 },
 "title": "Nameserver",
 "version": "v1.841e28cd"
}
