{
 "description": "CONSUMER: exact token count (tiktoken) for a model. input=text, modelo=optional. B2B: budget LLM calls. [x402: 0.001 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "service input",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "contar-tokens",
 "price_usd": 0.001,
 "recommendation": "This tool can help your agent with: CONSUMER: exact token count (tiktoken) for a model. input=text, modelo=optional. B2B: budget LLM calls.. Cost: $0.001 USDC/call, paid automatically via x402 on Base."
}
