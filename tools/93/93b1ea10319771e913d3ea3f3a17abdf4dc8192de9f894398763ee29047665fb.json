{
 "annotations": {
  "cost": {
   "all": 25
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find properties under contract in a region (e.g., \"Miami, FL\"). Returns address, price, beds, baths, and days pending.",
 "inputSchema": {
  "examples": [
   {
    "_altosKey": "your-altos-api-key",
    "region": "ca_los-angeles"
   },
   {
    "_altosKey": "your-altos-api-key",
    "limit": 25,
    "region": "ca_94105"
   }
  ],
  "properties": {
   "_altosKey": {
    "description": "Altos Research API key",
    "type": "string"
   },
   "date": {
    "description": "Date (must be a Friday, YYYY-MM-DD). Defaults to most recent Friday.",
    "type": "string"
   },
   "limit": {
    "description": "Max rows to return (default 100)",
    "type": "number"
   },
   "region": {
    "description": "Region code (e.g., \"ca_los-angeles\", \"ca_94105\")",
    "type": "string"
   }
  },
  "required": [
   "_altosKey",
   "region"
  ],
  "type": "object"
 },
 "name": "altos_pending_sales",
 "outputSchema": {
  "properties": {
   "date": {
    "description": "Friday date (YYYY-MM-DD) for snapshot",
    "type": "string"
   },
   "pendings": {
    "description": "Properties under contract",
    "items": {
     "description": "Raw CSV row data as key-value pairs",
     "type": "object"
    },
    "type": "array"
   },
   "region": {
    "description": "Region code queried",
    "type": "string"
   },
   "returned": {
    "description": "Number of pending sales returned",
    "type": "number"
   },
   "total_available": {
    "description": "Total pending sales in region",
    "type": "number"
   }
  },
  "required": [
   "region",
   "date",
   "total_available",
   "returned",
   "pendings"
  ],
  "type": "object"
 },
 "title": "Altos Pending Sales",
 "version": "v1.67f9f25a"
}
