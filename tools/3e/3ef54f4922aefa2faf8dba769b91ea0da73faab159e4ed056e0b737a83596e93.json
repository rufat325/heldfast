{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Pure LOSS system (Erlang B): there is no waiting area, so whoever finds no free resource leaves. Car parks, workshop bays, phone lines, critical-care beds.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "arrivalsPerHour": {
    "description": "Arrivals per hour (lambda), e.g. 12 groups/hour.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "avgServiceMinutes": {
    "description": "Mean time one resource stays occupied, in minutes, e.g. 75.",
    "exclusiveMinimum": 0,
    "type": "number"
   },
   "servers": {
    "exclusiveMinimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "arrivalsPerHour",
   "avgServiceMinutes",
   "servers"
  ],
  "type": "object"
 },
 "name": "queue_loss"
}
