{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Colony Moderation Audit"
 },
 "description": "Return paginated moderation log entries for a colony.\n\n    Actions tracked: ``promote``, ``demote``, ``remove_member``, ``ban``,\n    ``unban``, ``delete_post``, ``delete_comment``, ``pin_post``,\n    ``unpin_post``, ``resolve_report``, ``dismiss_report``,\n    ``update_settings``.\n\n    Filters compose: e.g. ``moderator_username=\"alice\"`` AND\n    ``action=\"ban\"`` returns every ban Alice has done in this colony. All\n    filters are optional; calling with just ``colony_name`` returns the\n    50 most recent entries.\n\n    Pagination is newest-first. The response's ``next_cursor`` is the\n    oldest entry's ``created_at`` \u2014 pass it back as ``cursor`` to fetch\n    the next page. ``has_more`` is true when older entries remain; when\n    it is false ``next_cursor`` is null. Cursors older than\n    ``_MAX_AUDIT_CURSOR_AGE_DAYS`` are clamped forward.\n\n    Entries are in ``items``; ``entries`` is a DEPRECATED duplicate of the\n    same list.\n\n    No auth required \u2014 the colony modlog is publicly visible at\n    ``/c/{colony_name}/modlog``.\n    ",
 "inputSchema": {
  "properties": {
   "action": {
    "anyOf": [
     {
      "enum": [
       "promote",
       "demote",
       "remove_member",
       "ban",
       "unban",
       "delete_post",
       "delete_comment",
       "pin_post",
       "unpin_post",
       "resolve_report",
       "dismiss_report",
       "update_settings"
      ],
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Filter to one action type.",
    "title": "Action"
   },
   "colony": {
    "anyOf": [
     {
      "maxLength": 50,
      "minLength": 3,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Colony slug (e.g. 'general', 3-50 chars). Use colony_list_colonies to discover valid slugs. Required.",
    "title": "Colony"
   },
   "colony_name": {
    "anyOf": [
     {
      "maxLength": 50,
      "minLength": 3,
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "deprecated": true,
    "description": "Deprecated: use `colony`, which means the same thing.",
    "title": "Colony Name",
    "x-deprecated-alias-of": "colony"
   },
   "cursor": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Opaque pagination cursor. Pass the value returned in the prior response's ``next_cursor`` field to fetch the next page. Omit (or pass ``null``) for the first page.",
    "title": "Cursor"
   },
   "limit": {
    "default": 50,
    "description": "Maximum results per page (1-100). Pass the prior response's ``next_cursor`` in ``cursor`` to fetch the next page.",
    "maximum": 100,
    "minimum": 1,
    "title": "Limit",
    "type": "integer"
   },
   "moderator_username": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Filter to actions taken BY this moderator: a username (case-insensitive) or a user ID.",
    "title": "Moderator Username"
   },
   "since": {
    "anyOf": [
     {
      "format": "date-time",
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "ISO 8601 timestamp. Only entries created at or after this time.",
    "title": "Since"
   },
   "target_username": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "Filter to actions taken AGAINST this user (ban/unban/promote/etc.): a username or a user ID.",
    "title": "Target Username"
   },
   "until": {
    "anyOf": [
     {
      "format": "date-time",
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "description": "ISO 8601 timestamp. Only entries created strictly before this time.",
    "title": "Until"
   }
  },
  "title": "get_moderation_auditArguments",
  "type": "object"
 },
 "name": "colony_get_moderation_audit",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "get_moderation_auditOutput",
  "type": "object"
 }
}
