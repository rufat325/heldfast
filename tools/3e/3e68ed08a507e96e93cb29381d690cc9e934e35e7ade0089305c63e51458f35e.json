{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true
 },
 "description": "Install a Chrome extension from a Web Store id or URL (downloads from the Web Store). Returns the updated list. (Cockpit only.) Remove with ext_remove.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "Extension id (32 chars) or a Chrome Web Store URL.",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "ext_install"
}
