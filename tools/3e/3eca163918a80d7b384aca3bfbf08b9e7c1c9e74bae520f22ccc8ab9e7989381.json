{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search Figshare's open research repository for datasets, figures, papers, preprints, media, and other research outputs, e.g. \"soil moisture\" or \"gene expression\". Filter by free-text query, exact DOI, or research output type (dataset, figure, journal_contribution, preprint, thesis, software, etc.), sorted by publication date. Returns each article's id (for use with figshare.article_details), title, DOI, item type, publish date, and URL. Data: Figshare.com, CC-licensed research outputs, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "doi": {
    "description": "Filter by exact DOI (e.g. \"10.6084/m9.figshare.33350571.v1\").",
    "type": "string"
   },
   "item_type": {
    "description": "Filter by research output type: figure, media, dataset, poster, journal_contribution, presentation, thesis, software, online_resource, preprint, book, or conference_contribution.",
    "enum": [
     "figure",
     "media",
     "dataset",
     "poster",
     "journal_contribution",
     "presentation",
     "thesis",
     "software",
     "online_resource",
     "preprint",
     "book",
     "conference_contribution"
    ],
    "type": "string"
   },
   "order_direction": {
    "description": "Sort direction by published date (default \"desc\" \u2014 newest first).",
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   },
   "page": {
    "description": "Page number for pagination (default 1).",
    "minimum": 1,
    "type": "integer"
   },
   "page_size": {
    "description": "Results per page (default 10, max 50).",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Free-text search across title, description, and author (e.g. \"soil moisture\", \"gene expression\").",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "figshare.articles.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Search Figshare Research Articles"
}
