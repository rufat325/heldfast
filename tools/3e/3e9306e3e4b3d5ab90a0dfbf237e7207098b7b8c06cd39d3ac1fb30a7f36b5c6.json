{
 "description": "Sentinel's published accuracy: live precision, recall, Brier score and calibration over a rolling window, whether the model is marked degraded, and the training holdout labelled as such. Read this before acting on a forecast. Read-only public GET.",
 "inputSchema": {
  "properties": {
   "window_days": {
    "description": "Rolling window in days, 1-365 (default 30)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "sentinel_accuracy"
}
