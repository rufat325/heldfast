{
 "description": "Renew an existing TLS secret in Code Engine by reading updated PEM files from disk. Use this when a Let's Encrypt cert has been renewed (every 90 days). Updates the secret in-place \u2014 no need to delete and recreate or update domain mappings.",
 "inputSchema": {
  "properties": {
   "cert_pem_path": {
    "description": "Path to the renewed fullchain.pem (~ supported)",
    "type": "string"
   },
   "key_pem_path": {
    "description": "Path to the renewed privkey.pem (~ supported)",
    "type": "string"
   },
   "project_id": {
    "type": "string"
   },
   "secret_name": {
    "description": "Name of the existing TLS secret to renew",
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "secret_name",
   "cert_pem_path",
   "key_pem_path"
  ],
  "type": "object"
 },
 "name": "ce_renew_tls_secret_from_pem"
}
