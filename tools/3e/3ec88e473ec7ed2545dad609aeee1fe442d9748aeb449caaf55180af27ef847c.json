{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get SBIR/STTR award counts by agency. Specify agency (e.g., \"DOD\", \"NASA\", \"NSF\") or omit to see all major agencies.",
 "inputSchema": {
  "examples": [
   {
    "agency": "DOW"
   },
   {}
  ],
  "properties": {
   "agency": {
    "description": "Specific agency to count, e.g. \"DOW\" (formerly DOD), \"NASA\", \"NSF\". Omit to cover all major agencies. Note SBIR publishes no total-count field, so a result at the page cap reports `award_count_at_least` rather than a total.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "sbir_agency_stats",
 "outputSchema": {
  "oneOf": [
   {
    "properties": {
     "agency": {
      "description": "Agency code",
      "type": "string"
     },
     "award_count": {
      "description": "Total award count for agency",
      "type": "number"
     }
    },
    "required": [
     "agency",
     "award_count"
    ],
    "type": "object"
   },
   {
    "properties": {
     "agencies": {
      "items": {
       "properties": {
        "agency": {
         "description": "Agency code",
         "type": "string"
        },
        "award_count": {
         "description": "Total award count for agency",
         "type": [
          "number",
          "null"
         ]
        },
        "error": {
         "description": "Error message if retrieval failed",
         "type": "string"
        }
       },
       "required": [
        "agency"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "agencies"
    ],
    "type": "object"
   }
  ],
  "type": "object"
 },
 "title": "Sbir Agency Stats",
 "version": "v1.fe9b27de"
}
