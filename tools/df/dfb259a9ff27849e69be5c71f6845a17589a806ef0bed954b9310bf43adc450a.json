{
 "description": "Register the organization's exact sending domain and queue SES provisioning. Asking twice does not provision twice. Publish the returned DKIM records, then call `verify` to have the state observed.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "domain": {
    "description": "Sending domain",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "domain"
  ],
  "type": "object"
 },
 "name": "commsharbor_domain_create"
}
