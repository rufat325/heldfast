{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List keys in a KV namespace, optionally filtered by prefix.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "account_id": {
    "description": "Cloudflare account ID. Falls back to CLOUDFLARE_ACCOUNT_ID env var when omitted.",
    "type": "string"
   },
   "limit": {
    "description": "Max keys (default 1000).",
    "type": "number"
   },
   "namespace_id": {
    "description": "KV namespace ID.",
    "type": "string"
   },
   "prefix": {
    "description": "Filter keys by prefix.",
    "type": "string"
   }
  },
  "required": [
   "namespace_id"
  ],
  "type": "object"
 },
 "name": "kv_list_keys"
}
