{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Turn a chart into a PUBLIC, embeddable URL \u2014 for a README, email, Slack, or a live auto-updating dashboard. Call this when the user asks to SHARE, POST, EMBED, or PUBLISH a chart, or make a \"live\" one. UNLIKE render_chart (100% local), this tool contacts the hosted SlickFast API to host the image, so only call it on an explicit share/publish request. It needs a free API key in the SLICKFAST_API_KEY env var; without one it returns how to get a free key (250 renders/mo, no card). The returned URL contains NO key \u2014 safe to paste anywhere.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "name": {
    "description": "a handle for the chart (a-z 0-9 - _); reuse the same name to update the same URL. Default \"chart\".",
    "type": "string"
   },
   "spec": {
    "description": "the chart spec to publish \u2014 same shape as render_chart (any type, including dashboard)"
   }
  },
  "type": "object"
 },
 "name": "share_chart",
 "title": "Share / publish a chart \u2014 get a public URL"
}
