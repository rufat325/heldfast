{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "\"Find jobs / openings / roles / positions for [title] in [city]\" / \"remote [role] jobs\" / \"[language / framework] developer jobs\" / \"salary range for [role] in [country]\" \u2014 search live job postings via Adzuna (global job-board aggregator). Filters: country (gb/us/ca/de/fr/...), title query, location, distance, salary band, full-time/permanent, posted recency. Returns ranked listings with title, company, location, salary, posted date.",
 "inputSchema": {
  "examples": [
   {
    "country": "gb",
    "results_per_page": 25,
    "what": "python developer",
    "where": "London"
   },
   {
    "country": "us",
    "full_time": true,
    "salary_min": 120000,
    "sort": "salary",
    "what_phrase": "senior software engineer",
    "where": "San Francisco"
   }
  ],
  "properties": {
   "country": {
    "description": "gb, us, ca, de, fr, ...",
    "type": "string"
   },
   "distance": {
    "description": "Search radius from `where`, in km",
    "type": "number"
   },
   "full_time": {
    "type": "boolean"
   },
   "max_days_old": {
    "description": "Restrict to jobs posted in the last N days",
    "type": "number"
   },
   "page": {
    "description": "1-based page (default 1)",
    "type": "number"
   },
   "permanent": {
    "type": "boolean"
   },
   "results_per_page": {
    "description": "1-50 (default 20)",
    "type": "number"
   },
   "salary_max": {
    "description": "Upper bound",
    "type": "number"
   },
   "salary_min": {
    "description": "Lower bound, in local currency",
    "type": "number"
   },
   "sort": {
    "description": "default | hybrid | date | salary | relevance",
    "type": "string"
   },
   "what": {
    "description": "Free-text query (title + description)",
    "type": "string"
   },
   "what_phrase": {
    "description": "Exact-phrase variant of `what`",
    "type": "string"
   },
   "where": {
    "description": "Location (city, region)",
    "type": "string"
   }
  },
  "required": [
   "country"
  ],
  "type": "object"
 },
 "name": "search",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Total matching jobs",
    "type": "number"
   },
   "mean_salary": {
    "description": "Mean salary for results",
    "type": "number"
   },
   "results": {
    "description": "List of job postings",
    "items": {
     "properties": {
      "company": {
       "properties": {
        "display_name": {
         "description": "Company name",
         "type": "string"
        }
       },
       "type": "object"
      },
      "created": {
       "description": "Creation timestamp",
       "type": "string"
      },
      "description": {
       "description": "Job description",
       "type": "string"
      },
      "id": {
       "description": "Job ID",
       "type": "string"
      },
      "location": {
       "properties": {
        "display_name": {
         "description": "Location",
         "type": "string"
        }
       },
       "type": "object"
      },
      "redirect_url": {
       "description": "Job posting URL",
       "type": "string"
      },
      "salary_currency": {
       "description": "Currency code",
       "type": "string"
      },
      "salary_max": {
       "description": "Maximum salary",
       "type": "number"
      },
      "salary_min": {
       "description": "Minimum salary",
       "type": "number"
      },
      "title": {
       "description": "Job title",
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "title": "Search",
 "version": "v1.dffe8553"
}
