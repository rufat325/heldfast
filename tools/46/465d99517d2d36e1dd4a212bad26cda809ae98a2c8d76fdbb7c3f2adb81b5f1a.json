{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get observed meteorological data from a NOAA CO-OPS station. Supports five products: air_temperature (\u00b0F or \u00b0C), water_temperature (sea surface \u00b0F or \u00b0C), air_pressure (millibars), humidity (relative humidity %), and wind (speed and direction \u2014 available at select stations). Returns 6-minute interval readings for the specified date range (default: last 24 hours) plus the most recent reading for quick lookups. Note: not all stations have all sensors \u2014 if a product is unavailable at the requested station, the API returns an error. Try station 8443970 (Boston) for air_temperature, 9414290 (San Francisco) for water_temperature. Source: NOAA CO-OPS \u2014 tidesandcurrents.noaa.gov. No auth, US Gov public domain.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "begin_date": {
    "description": "Start date for observations in YYYY-MM-DD or YYYYMMDD format (e.g. 2025-07-01). Defaults to yesterday. Maximum range: 31 days.",
    "type": "string"
   },
   "end_date": {
    "description": "End date for observations in YYYY-MM-DD or YYYYMMDD format (e.g. 2025-07-02). Defaults to today. Maximum range: 31 days.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of recent readings to return (1\u2013500). Readings are at 6-minute intervals. Defaults to 60 (last 6 hours).",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "product": {
    "description": "Meteorological product to retrieve. air_temperature = air temp in \u00b0F (or \u00b0C), water_temperature = sea surface temp, air_pressure = barometric pressure in mb, humidity = relative humidity %, wind = wind speed/direction. Not all stations have all sensors. Defaults to air_temperature.",
    "enum": [
     "air_temperature",
     "water_temperature",
     "air_pressure",
     "humidity",
     "wind"
    ],
    "type": "string"
   },
   "station_id": {
    "description": "NOAA CO-OPS station ID (4\u20137 digits, e.g. 8443970 for Boston MA). Find station IDs with the coops.tides.stations tool.",
    "type": "string"
   },
   "units": {
    "description": "Unit system: english = \u00b0F / knots / feet, metric = \u00b0C / m/s / meters. Defaults to english.",
    "enum": [
     "english",
     "metric"
    ],
    "type": "string"
   }
  },
  "required": [
   "station_id"
  ],
  "type": "object"
 },
 "name": "coops.tides.conditions",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "NOAA CO-OPS \u2014 Marine Meteorological Conditions"
}
