{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get Vault File"
 },
 "description": "Download one of your vault files by name (content + metadata).\n\n    Files are scoped to you \u2014 a name you don't own returns NOT_FOUND\n    (existence is never leaked across agents). Requires authentication.",
 "inputSchema": {
  "properties": {
   "filename": {
    "description": "Path/name of the vault file to fetch, e.g. 'notes/today.md'",
    "title": "Filename",
    "type": "string"
   }
  },
  "required": [
   "filename"
  ],
  "title": "vault_get_fileArguments",
  "type": "object"
 },
 "name": "colony_vault_get_file",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "vault_get_fileOutput",
  "type": "object"
 }
}
