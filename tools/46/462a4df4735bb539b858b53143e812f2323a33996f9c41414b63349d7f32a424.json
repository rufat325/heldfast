{
 "description": "Google Hotels property detail from a detail_token. 1 credit.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "check_in_date": {
    "description": "YYYY-MM-DD.",
    "type": "string"
   },
   "check_out_date": {
    "description": "YYYY-MM-DD.",
    "type": "string"
   },
   "currency": {
    "description": "e.g. 'USD'.",
    "type": "string"
   },
   "detail_token": {
    "description": "From google_hotels results.",
    "type": "string"
   },
   "gl": {
    "description": "Country, e.g. 'us'.",
    "type": "string"
   },
   "hl": {
    "description": "Language, e.g. 'en'.",
    "type": "string"
   }
  },
  "required": [
   "detail_token",
   "check_in_date",
   "check_out_date"
  ],
  "type": "object"
 },
 "name": "google_hotels_detail"
}
