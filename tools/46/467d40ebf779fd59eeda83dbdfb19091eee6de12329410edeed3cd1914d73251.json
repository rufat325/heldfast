{
 "description": "Create a new rule set. Rule sets group related rules together for batch evaluation. Optionally include existing rule IDs to populate the set.",
 "inputSchema": {
  "properties": {
   "description": {
    "type": "string"
   },
   "name": {
    "description": "e.g. 'Blog Post Quality Standards'",
    "type": "string"
   },
   "rule_ids": {
    "description": "Existing rule IDs to add",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "create_rule_set"
}
