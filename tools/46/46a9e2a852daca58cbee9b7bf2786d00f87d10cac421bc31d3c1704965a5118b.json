{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search US export data by HS commodity code and/or country. Returns export values, quantities, and commodity details. Pass `country` as a plain English name (e.g. \"Mexico\"). Omit hs_code for total exports to a country; omit country for all countries combined.",
 "inputSchema": {
  "examples": [
   {
    "country": "Mexico",
    "hs_code": "87",
    "year": "2025"
   },
   {
    "hs_code": "8471",
    "year": "2024"
   },
   {
    "country_code": "2010",
    "hs_code": "2709",
    "limit": 30,
    "year": "2023"
   },
   {
    "country_code": "2010",
    "year": "2024"
   }
  ],
  "properties": {
   "country": {
    "description": "Country name in plain English (e.g., \"Mexico\", \"South Korea\", \"European Union\"). Preferred over country_code \u2014 this is resolved for you. Optional \u2014 omit for all countries combined.",
    "type": "string"
   },
   "country_code": {
    "description": "Census Schedule C country code if you already have one (e.g., \"5700\" China, \"2010\" Mexico). NOT an ISO code \u2014 \"MX\" and \"CN\" will not work. Prefer `country` and pass a name instead. Optional.",
    "type": "string"
   },
   "hs_code": {
    "description": "HS commodity code at 2, 4, or 6 digit level (e.g., \"8471\" for computers). Optional \u2014 omit for total exports across all commodities.",
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of records to return (default 20)",
    "type": "number"
   },
   "month": {
    "description": "Trade month 01-12. Optional \u2014 omit for annual data.",
    "type": "string"
   },
   "year": {
    "description": "Trade year (e.g., \"2024\")",
    "type": "string"
   }
  },
  "required": [
   "year"
  ],
  "type": "object"
 },
 "name": "census_exports",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of records returned",
    "type": "number"
   },
   "hs_code": {
    "description": "HS commodity code queried",
    "type": "string"
   },
   "period": {
    "description": "Trade period (year or year-month)",
    "type": "string"
   },
   "records": {
    "items": {
     "properties": {
      "commodity_code": {
       "description": "HS commodity code",
       "type": "string"
      },
      "country_code": {
       "description": "Census country code",
       "type": "string"
      },
      "country_name": {
       "description": "Country name",
       "type": "string"
      },
      "export_value_usd": {
       "description": "Export value in USD",
       "type": "number"
      },
      "period": {
       "description": "Trade period",
       "type": "string"
      },
      "quantity": {
       "description": "Export quantity",
       "type": "number"
      }
     },
     "required": [
      "commodity_code",
      "country_code",
      "country_name",
      "export_value_usd",
      "quantity",
      "period"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "type": {
    "description": "Trade direction indicator",
    "type": "string"
   }
  },
  "required": [
   "type",
   "hs_code",
   "period",
   "count",
   "records"
  ],
  "type": "object"
 },
 "title": "Census Exports",
 "version": "v1.bca8d20b"
}
