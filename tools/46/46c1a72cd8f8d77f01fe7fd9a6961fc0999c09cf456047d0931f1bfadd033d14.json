{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List bookable event types (meeting templates). Requires a user URI OR an organization URI (get them from current_user). Returns event type URIs, names, durations, scheduling URLs, and active state.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-calendly-api-key",
    "user": "https://api.calendly.com/users/XXXXXXXXXXXXXXXX"
   },
   {
    "_apiKey": "your-calendly-api-key",
    "active": true,
    "organization": "https://api.calendly.com/organizations/XXXXXXXXXXXXXXXX"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Calendly personal access token (sent as Bearer token)",
    "type": "string"
   },
   "active": {
    "description": "If true, only return active event types.",
    "type": "boolean"
   },
   "count": {
    "description": "Number of results per page (default 20, max 100).",
    "type": "number"
   },
   "organization": {
    "description": "Organization URI (from current_user). Provide user OR organization.",
    "type": "string"
   },
   "page_token": {
    "description": "Token from a previous response for the next page.",
    "type": "string"
   },
   "user": {
    "description": "User URI (from current_user). Provide user OR organization.",
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "list_event_types",
 "title": "List Event Types",
 "version": "v1.b4293339"
}
