{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Reconfigure an existing chart in place, keeping its position. Anything left out keeps its current value; an empty string clears a text field (title, axis title, number format).\nGet the chart_id from chart__list first.",
 "inputSchema": {
  "properties": {
   "catAxisTitle": {
    "type": "string"
   },
   "categoriesRef": {
    "description": "Re-point the category labels (A1 range).",
    "type": "string"
   },
   "chartId": {
    "description": "From chart__list.",
    "type": "string"
   },
   "chartType": {
    "enum": [
     "col",
     "bar",
     "line",
     "area",
     "pie",
     "doughnut",
     "scatter",
     "radar",
     "bubble",
     "stock",
     "ofPie",
     "barOfPie",
     "surface",
     "surface3d",
     "col3d",
     "bar3d",
     "line3d",
     "area3d",
     "pie3d"
    ],
    "type": "string"
   },
   "legendPos": {
    "enum": [
     "top",
     "bottom",
     "left",
     "right",
     "none"
    ],
    "type": "string"
   },
   "numFmt": {
    "description": "Excel number-format code for the value axis and labels, e.g. \"#,##0.00\" or \"0%\". Empty falls back to the source cells' own format.",
    "type": "string"
   },
   "series": {
    "description": "One entry per data series. Each valueRef is an A1 range of the values to plot.",
    "items": {
     "properties": {
      "color": {
       "description": "RGB hex without \"#\", e.g. 4472C4.",
       "type": "string"
      },
      "name": {
       "description": "Legend name, e.g. the column's header text.",
       "type": "string"
      },
      "seriesType": {
       "description": "Draw this one series as a different kind, making a combo chart. Only valid when the chart itself is col/bar/line/area.",
       "enum": [
        "col",
        "bar",
        "line",
        "area"
       ],
       "type": "string"
      },
      "sizeRef": {
       "description": "Bubble sizes (bubble charts only): a third A1 range.",
       "type": "string"
      },
      "valueRef": {
       "description": "A1 range of this series' values, e.g. B2:B10.",
       "type": "string"
      }
     },
     "required": [
      "valueRef"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "sheetIdx": {
    "type": "integer"
   },
   "showDataLabels": {
    "description": "Print each point's value next to it.",
    "type": "boolean"
   },
   "showPercentLabels": {
    "description": "Add the percentage to labels (pie-like charts).",
    "type": "boolean"
   },
   "stacked": {
    "description": "Stack the series. Ignored by pie and scatter.",
    "type": "boolean"
   },
   "title": {
    "description": "Empty string clears it.",
    "type": "string"
   },
   "valAxisScale": {
    "description": "Replaces the whole value-axis scale \u2014 fields left out become automatic, which is how a fixed bound is cleared.",
    "properties": {
     "logBase": {
      "description": "Log scale base (2\u20131000).",
      "type": "number"
     },
     "majorUnit": {
      "description": "Spacing between gridlines.",
      "type": "number"
     },
     "max": {
      "type": "number"
     },
     "min": {
      "type": "number"
     },
     "reversed": {
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "valAxisTitle": {
    "type": "string"
   }
  },
  "required": [
   "sheetIdx",
   "chartId"
  ],
  "type": "object"
 },
 "name": "chart_update"
}
