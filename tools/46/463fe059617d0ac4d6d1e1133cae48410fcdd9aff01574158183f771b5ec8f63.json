{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find cultural events & activities in Madrid, Spain (next ~100 days). Filter by keyword, free admission, and date window. Returns events with venue, address, geo, audience and price. Content is in Spanish.",
 "inputSchema": {
  "examples": [
   {
    "free_only": false,
    "from": "2025-01-15",
    "limit": 20,
    "query": "m\u00fasica",
    "to": "2025-04-15"
   },
   {
    "free_only": true,
    "limit": 10,
    "query": "exposici\u00f3n"
   }
  ],
  "properties": {
   "free_only": {
    "description": "If true, only free events.",
    "type": "boolean"
   },
   "from": {
    "description": "Include events on/after this date YYYY-MM-DD (default: today).",
    "type": "string"
   },
   "limit": {
    "description": "Max events (1-50, default 20).",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset (default 0).",
    "type": "number"
   },
   "query": {
    "description": "Keyword over title, description and venue, e.g. \"m\u00fasica\", \"exposici\u00f3n\", \"teatro\".",
    "type": "string"
   },
   "to": {
    "description": "Include events starting on/before this date YYYY-MM-DD.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "events",
 "title": "Events",
 "version": "v1.ed263f9e"
}
