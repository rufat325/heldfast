{
 "description": "List all resources of a given type in a workspace \u2014 the fast way to see what already exists before you act. Returns lightweight summaries (IDs, names, key fields), not full content: use `get` for one resource's full detail, or `search` to find resources by text or tag. Set `type` to choose what to list \u2014 workspaces, libraries (context libraries), agents, skills, files (pass libraryId), workflows, tags, file_tags (pass fileId), workspace_status, or members (with roles + activity). Uses your active workspace when workspaceId is omitted.",
 "inputSchema": {
  "properties": {
   "fileId": {
    "description": "File ID to list tags for (type=file_tags)",
    "type": "string"
   },
   "folderId": {
    "description": "Filter by folder (type=files)",
    "type": "string"
   },
   "libraryId": {
    "description": "Required when type=files",
    "type": "string"
   },
   "status": {
    "description": "Filter by status (type=files)",
    "enum": [
     "active",
     "archived"
    ],
    "type": "string"
   },
   "type": {
    "description": "Resource type to list. members: list all workspace members with their roles and activity status.",
    "enum": [
     "workspaces",
     "libraries",
     "agents",
     "skills",
     "files",
     "workflows",
     "tags",
     "file_tags",
     "workspace_status",
     "members"
    ],
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID (uses default if omitted)",
    "type": "string"
   }
  },
  "required": [
   "type"
  ],
  "type": "object"
 },
 "name": "list"
}
