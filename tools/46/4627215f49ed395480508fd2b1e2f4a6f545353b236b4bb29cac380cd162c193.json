{
 "description": "Delete a memory key. Acts as the identity in the local credential store.",
 "inputSchema": {
  "properties": {
   "key": {
    "description": "Key name",
    "type": "string"
   },
   "namespace": {
    "description": "Namespace",
    "type": "string"
   }
  },
  "required": [
   "namespace",
   "key"
  ],
  "type": "object"
 },
 "name": "agent_memory_delete"
}
