{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Read the sealed export blob of a completed impreza_export_onion_key command \u2014 EXACTLY ONCE: this read removes the ciphertext from the platform and a second read is a 404 (re-export if you lose it). Returns the .onion address and the sealed blob; unseal it locally with the recipient PRIVATE key (NaCl/libsodium sealed box) to recover the versioned JSON key-pair bundle. 409 while the agent has not finished yet \u2014 poll the deployment and retry.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "command_id": {
    "description": "The cmd_... id returned by impreza_export_onion_key.",
    "type": "string"
   },
   "deployment_id": {
    "description": "The dpl_... id the export belongs to.",
    "type": "string"
   }
  },
  "required": [
   "deployment_id",
   "command_id"
  ],
  "type": "object"
 },
 "name": "impreza_fetch_onion_key_export"
}
