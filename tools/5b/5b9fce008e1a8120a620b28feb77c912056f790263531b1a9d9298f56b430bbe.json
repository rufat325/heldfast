{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Build or reuse a persisted disclosure history for one issuer and topic, with deterministic per-filing fingerprints and same-form transition edges backed by exact SEC evidence. Use this when historical evolution or repeat reuse matters. Use filedproof.compare_filings for a one-off exact filing pair, filedproof.build_timeline for simple chronology without reusable transition state, or filedproof.build_disclosure_matrix when the objective is comparison across multiple companies. Structural change is not a materiality judgment.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "comparisonLimit": {
    "default": 250,
    "description": "Maximum changed blocks inspected in each same-form structural comparison.",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   },
   "filings": {
    "default": 6,
    "description": "Maximum filings to inspect in this invocation. Previously persisted lineage can contain more accumulated history.",
    "maximum": 6,
    "minimum": 2,
    "type": "integer"
   },
   "fingerprintLimit": {
    "default": 50,
    "description": "Maximum relevant evidence records used to fingerprint one filing.",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "forms": {
    "description": "SEC forms to include. Defaults to 10-K and 10-Q. Same-form transitions are linked independently.",
    "items": {
     "type": "string"
    },
    "maxItems": 8,
    "type": "array"
   },
   "history": {
    "default": true,
    "description": "Use bounded historical SEC submission archives when the durable catalog does not already provide enough history.",
    "type": "boolean"
   },
   "identifier": {
    "description": "Ticker, CIK, or exact SEC company name.",
    "minLength": 1,
    "type": "string"
   },
   "item": {
    "description": "Optional SEC filing item such as 1A or 7.",
    "type": "string"
   },
   "limit": {
    "default": 30,
    "description": "Maximum topic-relevant change evidence records retained per transition.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "section": {
    "description": "Optional exact normalized section heading.",
    "type": "string"
   },
   "topic": {
    "description": "Public-company disclosure topic whose filing-to-filing history should be accumulated, such as AI regulation, cybersecurity, or supply-chain concentration.",
    "maxLength": 256,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "identifier",
   "topic"
  ],
  "type": "object"
 },
 "name": "filedproof.build_disclosure_lineage",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Structured FiledProof result with operation-specific evidence, provenance, coverage, and uncertainty fields.",
  "type": "object"
 },
 "title": "Build historical disclosure lineage"
}
