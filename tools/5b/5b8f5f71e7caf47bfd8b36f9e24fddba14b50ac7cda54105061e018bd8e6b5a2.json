{
 "description": "Top DeFi yield pools (min $1M TVL) with base vs reward APY, filterable by chain and stablecoin-only. Paid: $0.01 via x402.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "chain": {
    "description": "Filter by chain, e.g. Base",
    "type": "string"
   },
   "limit": {
    "description": "Max pools (default 15, max 50)",
    "type": "number"
   },
   "stablecoin": {
    "description": "true for stablecoin pools only",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "defi_yields"
}
