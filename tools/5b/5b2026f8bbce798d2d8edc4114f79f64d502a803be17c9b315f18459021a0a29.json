{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get observation values for a Chilean SIMEL dataflow_id (e.g. \"DF_NOCU_SEXO\" for employed persons by sex). Filter by last_n_observations (most recent N per series, default 10, max 100) or a start_period/end_period range (e.g. \"2020\", \"2020-01\"). Each series is labeled with its dimension values (sex, region, etc.) and a list of period/value pairs. Data: sdmx.ine.gob.cl (INE Chile SDMX API), no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataflow_id": {
    "description": "SIMEL dataflow id, from ine-chile.dataflows (e.g. \"DF_NOCU_SEXO\").",
    "minLength": 1,
    "type": "string"
   },
   "end_period": {
    "description": "End of period range, e.g. \"2024\" (annual) or \"2024-12\" (monthly). Takes precedence over last_n_observations when set.",
    "type": "string"
   },
   "last_n_observations": {
    "description": "Return only the most recent N observations per series (max 100, default 10). Ignored if start_period or end_period is set.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "start_period": {
    "description": "Start of period range, e.g. \"2020\" (annual) or \"2020-01\" (monthly). Takes precedence over last_n_observations when set.",
    "type": "string"
   }
  },
  "required": [
   "dataflow_id"
  ],
  "type": "object"
 },
 "name": "ine-chile.series.data",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "INE Chile SIMEL Dataflow Data"
}
