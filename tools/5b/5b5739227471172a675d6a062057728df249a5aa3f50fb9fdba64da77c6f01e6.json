{
 "description": "The yes on one personalised first touch. Sending window, daily caps and the sequence's sending account still apply afterwards \u2014 approving does not send. A message that is not awaiting review answers 409 saying what state it is in. (POST /api/v1/outreach/messages/{messageId}/approve; needs the write:outreach scope)",
 "inputSchema": {
  "properties": {
   "messageId": {
    "description": "Message id from list_review_queue.",
    "type": "string"
   },
   "project": {
    "description": "Project id or slug owning the message.",
    "type": "string"
   }
  },
  "required": [
   "messageId",
   "project"
  ],
  "type": "object"
 },
 "name": "approve_message"
}
