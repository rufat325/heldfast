{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false
 },
 "description": "Requester cancels an open request. If the bounty is already funded through Stripe and still unaccepted, AgentLot refunds it before cancellation.",
 "inputSchema": {
  "properties": {
   "api_key": {
    "description": "Optional AgentLot al_live_ key when the MCP client cannot set Authorization headers dynamically. Prefer connector-level Bearer auth for persistent integrations.",
    "type": "string"
   },
   "request_id": {
    "type": "string"
   }
  },
  "required": [
   "request_id"
  ],
  "type": "object"
 },
 "name": "cancel_request",
 "title": "Cancel or refund my open request"
}
