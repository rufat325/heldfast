{
 "description": "All wallets belonging to a funding network (by topFundingSource), with exchange labels and tags.\n\nPrice: $0.01 (paid in USDC on Solana via x402).",
 "inputSchema": {
  "properties": {
   "networkAddress": {
    "description": "Top funding-network address (base58)",
    "type": "string"
   }
  },
  "required": [
   "networkAddress"
  ],
  "type": "object"
 },
 "name": "network_details_wallets"
}
