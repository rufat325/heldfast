{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "List Events"
 },
 "description": "List the events you host \u2014 name, date, status, visibility, location, capacity, and headcount, plus the total count. See how many events you have, or find an event's id before acting on it. Optional filters: status, upcoming_only, limit. Requires a connected account. Read-only.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "exclusiveMinimum": 0,
    "maximum": 200,
    "type": "integer"
   },
   "status": {
    "maxLength": 40,
    "type": "string"
   },
   "upcoming_only": {
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "list_events",
 "title": "List Events"
}
