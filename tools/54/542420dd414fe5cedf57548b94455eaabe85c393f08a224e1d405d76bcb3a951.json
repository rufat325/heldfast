{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "create_chute_session"
 },
 "description": "Propose a chute-mode work session from a description of the day's work. Creates a PROPOSED session only \u2014 the user reviews, edits, and starts it at the chute (the in-app confirmation); proposing never records any data by itself. Build the widget grid from these types: weight (scale readout; options.unit), score (tap row; options.min/max, BCS defaults 1\u20135), select (tap chips; options.choices), boolean (yes/no tap, e.g. \"Feet trimmed\" for work done only if needed), number (options.unit), treatment (product checklist; options.presets), text (notes/dictation), photo. Arrangement rules (hipster/design chute_widget_grid): weight first with size \"full\"; tap-only widgets (score, select, boolean) size \"half\", grouped after weight; number/treatment next; text and photo last with size \"full\"; keep it to ~6 widgets or fewer. EID scanning is the identify step of every session \u2014 never add a widget for it.",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "group_name": {
    "description": "Group the processed animals join; created if it does not exist.",
    "type": "string"
   },
   "name": {
    "description": "Session name, e.g. \"Weigh Day\". Defaults to \"Chute \u2014 <today>\".",
    "type": "string"
   },
   "new_animal_fields": {
    "description": "Identity fields on the new-animal form (tag, name, sex, color, dob).",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "record_type": {
    "description": "RecordType for the per-animal pass records (e.g. HEALTH for vaccination).",
    "enum": [
     "FEED",
     "GENETIC",
     "HEALTH",
     "MOVEMENT",
     "OTHER"
    ],
    "type": "string"
   },
   "widgets": {
    "description": "The widget grid, in display order.",
    "items": {
     "additionalProperties": false,
     "properties": {
      "id": {
       "minLength": 1,
       "type": "string"
      },
      "label": {
       "minLength": 1,
       "type": "string"
      },
      "options": {
       "additionalProperties": {},
       "type": "object"
      },
      "size": {
       "enum": [
        "full",
        "half"
       ],
       "type": "string"
      },
      "type": {
       "enum": [
        "boolean",
        "number",
        "photo",
        "score",
        "select",
        "text",
        "treatment",
        "weight"
       ],
       "type": "string"
      }
     },
     "required": [
      "id",
      "type",
      "label"
     ],
     "type": "object"
    },
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "widgets"
  ],
  "type": "object"
 },
 "name": "create_chute_session"
}
