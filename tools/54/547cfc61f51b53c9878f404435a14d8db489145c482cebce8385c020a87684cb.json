{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search across an entire Outlook / Microsoft 365 mailbox by keyword. Returns matching email summaries (subject, sender, received time, preview). Use to find emails by topic, sender, or content regardless of folder.",
 "inputSchema": {
  "examples": [
   {
    "query": "project deadline next week"
   },
   {
    "query": "from:john@example.com",
    "top": 50
   }
  ],
  "properties": {
   "query": {
    "description": "Free-text search query to match against email messages.",
    "type": "string"
   },
   "top": {
    "description": "Maximum number of messages to return (default 25).",
    "type": "number"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "outlook_search_messages",
 "title": "Outlook Search Messages",
 "version": "v1.1667703f"
}
