{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Most recent REAL reading for (station, sensor), looking back three days. CDEC writes -9999 into slots it has no reading for and the current day's trailing hours are routinely unwritten, so this skips those and reports how many missing slots it skipped rather than returning the marker as a number.",
 "inputSchema": {
  "examples": [
   {
    "sensor": 1,
    "station_id": "SHA"
   },
   {
    "sensor": 2,
    "station_id": "ORO"
   }
  ],
  "properties": {
   "sensor": {
    "type": "number"
   },
   "station_id": {
    "type": "string"
   }
  },
  "required": [
   "station_id",
   "sensor"
  ],
  "type": "object"
 },
 "name": "latest",
 "outputSchema": {
  "additionalProperties": true,
  "description": "Response from CDEC API containing latest value or raw text",
  "properties": {
   "body": {
    "description": "Raw text response from API",
    "type": "string"
   },
   "format": {
    "description": "Indicates response is plain text (non-JSON)",
    "enum": [
     "text"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "title": "Latest",
 "version": "v1.74a64ef8"
}
