{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-retention-event-type"
 },
 "description": "Read the properties and relationships of a retentionEventType object.\n\nTIP: Gets a specific retention event type definition by ID. Returns displayName, description, createdBy, createdDateTime, lastModifiedDateTime. Event types are the categories of events that can trigger event-based retention (e.g. 'project_closed', 'contract_terminated', 'employee_left'). Use get-retention-event-types to list available types before creating an event.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "retentionEventTypeId": {
    "description": "Path parameter: retentionEventTypeId",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "required": [
   "retentionEventTypeId"
  ],
  "type": "object"
 },
 "name": "get-retention-event-type"
}
