{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search historical JMA earthquake records via the P2PQuake history endpoint. Supports pagination, intensity and magnitude filters, and prefecture filtering for deeper analysis. Returns epicenter name, coordinates, depth, Richter magnitude, max JMA intensity label, domestic tsunami advisory, report type, and observation point count. Use this for trend analysis, seismic risk assessment, or retrieving older events not in the recent feed. Filter examples: min_scale=60 for Shindo 6+ events, min_magnitude=7.0 for major quakes, prefecture=13 for Tokyo area events. Data source: P2PQuake \u2014 community-operated JMA relay, open data, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Number of historical earthquake records to return (default 20, max 100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "max_magnitude": {
    "description": "Maximum Richter magnitude",
    "type": "number"
   },
   "max_scale": {
    "description": "Maximum JMA seismic intensity raw scale value",
    "type": "integer"
   },
   "min_magnitude": {
    "description": "Minimum Richter magnitude (e.g. 6.0 for strong earthquakes)",
    "type": "number"
   },
   "min_scale": {
    "description": "Minimum JMA intensity (10=Shindo1, 20=Shindo2, 30=Shindo3, 40=Shindo4, 45=Shindo4+, 50=Shindo5w, 55=Shindo5s, 60=Shindo6w, 65=Shindo6s, 70=Shindo7)",
    "type": "integer"
   },
   "offset": {
    "description": "Pagination offset for browsing older events (0-based, default 0)",
    "minimum": 0,
    "type": "integer"
   },
   "order": {
    "description": "Sort direction: -1 for newest-first (default), 1 for oldest-first",
    "type": "integer"
   },
   "prefecture": {
    "description": "Filter by prefecture JIS code (1=Hokkaido, 13=Tokyo, 27=Osaka, 40=Fukuoka, 47=Okinawa)",
    "maximum": 47,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "p2pquake.seismic.quake_history",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Japan Earthquake History"
}
