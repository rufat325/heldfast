{
 "description": "Build the APK, install it on a device/emulator (adb install -r), and launch the app. Needs appId (session default or argument) to launch.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "appId": {
    "description": "Application id to launch (defaults to session)",
    "type": "string"
   },
   "module": {
    "description": "Gradle module, e.g. 'app' (defaults to session)",
    "type": "string"
   },
   "projectDir": {
    "description": "Project root (defaults to session)",
    "type": "string"
   },
   "serial": {
    "description": "Device serial (defaults to session or the only device)",
    "type": "string"
   },
   "variant": {
    "description": "Build variant, e.g. 'debug' (defaults to session)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "build_run",
 "title": "Build, install and run"
}
