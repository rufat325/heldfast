{
 "description": "Enable or disable auto-optimization for a site. When enabled, the watchdog monitors content for decay (age, traffic trend, ranking trend, competitor activity) and auto-applies safe optimization suggestions.",
 "inputSchema": {
  "properties": {
   "enabled": {
    "description": "true = enable, false = disable",
    "type": "boolean"
   },
   "site_id": {
    "type": "string"
   }
  },
  "required": [
   "site_id",
   "enabled"
  ],
  "type": "object"
 },
 "name": "toggle_auto_optimization"
}
