{
 "description": "EGRESS (one-step): Request a use-token AND redeem it in a single call. Self-healing: if the context is missing, it auto-creates one and retries. You should never see \"context not found\" \u2014 the tool handles it.",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Action type: http_header, ssh_exec, http_body, document",
    "type": "string"
   },
   "command": {
    "description": "Command to execute (for SSH)",
    "type": "string"
   },
   "name": {
    "description": "Secret name (if not using ref)",
    "type": "string"
   },
   "owner_did": {
    "description": "Owner DID (only for delegated access without ref)",
    "type": "string"
   },
   "params": {
    "description": "Additional action-specific parameters",
    "type": "object"
   },
   "ref": {
    "description": "Routed reference code (e.g. DP-API-openrout-7f3a9c1e). Preferred.",
    "type": "string"
   },
   "target_host": {
    "description": "Target host (required for SSH)",
    "type": "string"
   },
   "target_url": {
    "description": "Target URL for HTTP header/body and database actions",
    "type": "string"
   },
   "target_user": {
    "description": "SSH user (default: root)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "demipass_use"
}
