{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Read text from a window"
 },
 "description": "Read TEXT from inside a window via UI Automation \u2014 no screenshot. Works with consoles (cmd, PowerShell, Windows Terminal), editors, and most native apps. Typical use: read render progress from a console (\"frame 4812/5000\"). Identify the window by title_contains or hwnd from list_windows.",
 "inputSchema": {
  "properties": {
   "hwnd": {
    "description": "Exact window handle from list_windows",
    "type": "number"
   },
   "max_chars": {
    "description": "Max characters (default 20000)",
    "type": "number"
   },
   "title_contains": {
    "description": "Window title fragment (case-insensitive)",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "read_window"
}
