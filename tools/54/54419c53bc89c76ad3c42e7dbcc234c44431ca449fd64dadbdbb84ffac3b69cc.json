{
 "description": "Create a service binding to connect an IBM Cloud service instance to a Code Engine component",
 "inputSchema": {
  "properties": {
   "component_name": {
    "description": "Name of the CE app or job to bind the service to",
    "type": "string"
   },
   "component_resource_type": {
    "description": "Resource type of the component",
    "enum": [
     "app_v2",
     "job_v2",
     "function_v2"
    ],
    "type": "string"
   },
   "prefix": {
    "description": "Prefix for environment variable names injected into the component (optional)",
    "type": "string"
   },
   "project_id": {
    "type": "string"
   },
   "secret_name": {
    "description": "Name of the operator secret referencing the IBM Cloud service instance",
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "component_name",
   "component_resource_type",
   "secret_name"
  ],
  "type": "object"
 },
 "name": "ce_create_binding"
}
