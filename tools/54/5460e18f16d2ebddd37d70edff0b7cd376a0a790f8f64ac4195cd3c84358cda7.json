{
 "description": "Refund a committed Webpay Plus transaction. Full refund if amount equals original; partial otherwise.",
 "inputSchema": {
  "properties": {
   "amount": {
    "description": "Refund amount in CLP (integer)",
    "type": "number"
   },
   "token": {
    "description": "Webpay token of the original transaction",
    "type": "string"
   }
  },
  "required": [
   "token",
   "amount"
  ],
  "type": "object"
 },
 "name": "webpay_refund_transaction"
}
