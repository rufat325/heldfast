{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Get a curated provider\n\nA curated provider navigable to its seats and each seat's observed hosts.",
 "inputSchema": {
  "properties": {
   "slug": {
    "description": "The curated provider slug.",
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "get_provider"
}
