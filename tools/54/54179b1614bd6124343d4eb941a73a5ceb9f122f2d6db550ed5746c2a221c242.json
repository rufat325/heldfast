{
 "description": "Check how well retrieved memories cover a topic. Returns coverage ratio, gaps, and suggested follow-up queries. Anti-hallucination guardrail \u2014 use before synthesizing an answer to verify completeness.",
 "inputSchema": {
  "properties": {
   "auto_retrieve": {
    "description": "Run internal search to assess coverage (makes memory_ids optional)",
    "type": "boolean"
   },
   "memory_ids": {
    "description": "Memory IDs already retrieved",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "topic": {
    "description": "The topic being investigated",
    "type": "string"
   }
  },
  "required": [
   "topic"
  ],
  "type": "object"
 },
 "name": "velixar_coverage_check"
}
