{
 "description": "Return prioritized, rewrite-level suggestions for a resume, optionally aimed at a specific role or job description. Each suggestion has a type, priority, tip, and an example rewrite. Pass missingMustHave / missingNiceToHave from analyze_job_description to close keyword gaps.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "jdText": {
    "description": "Target job description, for role-specific suggestions.",
    "type": "string"
   },
   "jobTitle": {
    "description": "Target job title, e.g. \"Senior Backend Engineer\".",
    "type": "string"
   },
   "missingMustHave": {
    "description": "Required keywords the resume is missing.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "missingNiceToHave": {
    "description": "Preferred keywords the resume is missing.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "resumeText": {
    "description": "Full plain-text resume.",
    "type": "string"
   }
  },
  "required": [
   "resumeText"
  ],
  "type": "object"
 },
 "name": "optimize_resume",
 "title": "Get targeted resume improvement suggestions"
}
