{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Convert several documents to Markdown"
 },
 "description": "Convert several documents at once and return a summary of what succeeded. Use when the user points at a set of files rather than one.",
 "inputSchema": {
  "properties": {
   "output_directory": {
    "description": "Absolute directory to write each .md file into.",
    "type": "string"
   },
   "paths": {
    "description": "Absolute paths of the files to convert.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "paths",
   "output_directory"
  ],
  "type": "object"
 },
 "name": "convert_folder_to_markdown"
}
