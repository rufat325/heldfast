{
 "description": "Compile a mod project and deploy the output to the game mods directory.",
 "inputSchema": {
  "properties": {
   "build_command": {
    "description": "Custom build command (auto-detected if omitted)",
    "type": "string"
   },
   "deploy_path": {
    "description": "Path to deploy the built mod (auto-detected if omitted)",
    "type": "string"
   },
   "project_path": {
    "description": "Path to the mod project directory",
    "type": "string"
   }
  },
  "required": [
   "project_path"
  ],
  "type": "object"
 },
 "name": "build_and_deploy"
}
