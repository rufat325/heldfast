{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "List TestFlight beta groups (internal and external). Filter by app or group type.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "appId": {
    "description": "Filter to beta groups for a single app ID",
    "type": "string"
   },
   "auto_paginate": {
    "description": "Follow links.next across pages until the limit is reached (default false).",
    "type": "boolean"
   },
   "isInternalGroup": {
    "description": "true = internal-only, false = external",
    "type": "boolean"
   },
   "limit": {
    "description": "Max groups (default 50). With auto_paginate this is the total across pages.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_beta_groups"
}
