{
 "description": "Check current Optibot authentication status. Shows whether credentials are configured via environment variable or config file, and the active organization (if any).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "properties": {},
  "type": "object"
 },
 "name": "check_auth"
}
