{
 "description": "Given a package + version, return breaking changes since. Saves agents from inspecting changelogs.",
 "inputSchema": {
  "properties": {
   "current_version": {
    "type": "string"
   },
   "package_id": {
    "type": "string"
   }
  },
  "required": [
   "package_id",
   "current_version"
  ],
  "type": "object"
 },
 "name": "check_version"
}
