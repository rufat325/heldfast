{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List flights at an airport \u2014 arrivals and/or departures with scheduled & actual times. The `id` is an airport code (ICAO like \"KSFO\" or IATA like \"SFO\"). Use `type` to pick \"arrivals\", \"departures\", or \"all\" (default \"all\", which also includes scheduled flights). Example: flightaware_airport_flights({ id: \"KSFO\", type: \"departures\", _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-flightaware-api-key",
    "id": "KSFO"
   },
   {
    "_apiKey": "your-flightaware-api-key",
    "id": "EGLL",
    "start": "2026-07-01T00:00:00Z",
    "type": "departures"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Your FlightAware AeroAPI key",
    "type": "string"
   },
   "end": {
    "description": "Optional ISO8601 date/time upper bound.",
    "type": "string"
   },
   "id": {
    "description": "Airport code \u2014 ICAO (e.g. \"KSFO\", \"EGLL\") or IATA (e.g. \"SFO\", \"LHR\").",
    "type": "string"
   },
   "start": {
    "description": "Optional ISO8601 date/time lower bound.",
    "type": "string"
   },
   "type": {
    "description": "Which flights to return: \"arrivals\", \"departures\", or \"all\" (default). \"all\" hits the combined endpoint and returns arrivals, departures, scheduled_arrivals and scheduled_departures.",
    "type": "string"
   }
  },
  "required": [
   "id",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "flightaware_airport_flights",
 "title": "Flightaware Airport Flights",
 "version": "v1.fedbeea2"
}
