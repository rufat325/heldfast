{
 "description": "Permanently deletes an identity by ID. Use dryRun=true first to preview which workflows will have their identity_id set to null.",
 "inputSchema": {
  "properties": {
   "dryRun": {
    "description": "If true, returns affected workflows without deleting. Default false.",
    "type": "boolean"
   },
   "identityId": {
    "description": "The ID of the identity to delete.",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. If not provided, uses your default workspace.",
    "type": "string"
   }
  },
  "required": [
   "identityId"
  ],
  "type": "object"
 },
 "name": "delete_identity"
}
