{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Show what breaks if a file changes. Follows the full reverse dependency chain transitively.",
 "inputSchema": {
  "properties": {
   "max_depth": {
    "description": "Max traversal depth (default 10)",
    "type": "integer"
   },
   "path": {
    "description": "File path to analyze impact for",
    "type": "string"
   }
  },
  "required": [
   "path"
  ],
  "type": "object"
 },
 "name": "get_change_impact"
}
