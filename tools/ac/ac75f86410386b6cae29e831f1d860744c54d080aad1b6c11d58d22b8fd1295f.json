{
 "description": "Create a campaign draft. All three must already exist and be usable: a draft cannot be created against an unverified domain or an unpublished template.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "audience_id": {
    "description": "Audience ID",
    "type": "string"
   },
   "domain_id": {
    "description": "Active sending domain ID",
    "type": "string"
   },
   "name": {
    "description": "Campaign name",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "template_id": {
    "description": "Published marketing template ID",
    "type": "string"
   },
   "template_version": {
    "description": "Published version to freeze into the campaign.",
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "organization_id",
   "name",
   "domain_id",
   "audience_id",
   "template_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_campaign_create"
}
