{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Manage sprints. Methods: list, get_current, create, update, assign (features to sprint)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "end_date": {
    "description": "End date (for: create, update)",
    "type": "string"
   },
   "feature_ids": {
    "description": "Feature IDs to assign (required for: assign)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "goal": {
    "description": "Sprint goal (for: create, update)",
    "type": "string"
   },
   "inferred_fields": {
    "description": "Inferred fields (for: create)",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "method": {
    "description": "Operation to perform",
    "enum": [
     "list",
     "get_current",
     "create",
     "update",
     "assign"
    ],
    "type": "string"
   },
   "name": {
    "description": "Sprint name (required for: create)",
    "type": "string"
   },
   "project_id": {
    "description": "Project ID (for: list, get_current, create \u2014 auto-detected if omitted)",
    "type": "string"
   },
   "retrospective": {
    "additionalProperties": false,
    "description": "Retrospective data (for: update)",
    "properties": {
     "actionItems": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "toImprove": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "wentWell": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "wentWell",
     "toImprove",
     "actionItems"
    ],
    "type": "object"
   },
   "sprint_id": {
    "description": "Sprint ID (required for: update, assign)",
    "type": "string"
   },
   "start_date": {
    "description": "Start date (for: create, update)",
    "type": "string"
   },
   "status": {
    "description": "Status (for: update)",
    "enum": [
     "planning",
     "active",
     "completed"
    ],
    "type": "string"
   }
  },
  "required": [
   "method"
  ],
  "type": "object"
 },
 "name": "sprints"
}
