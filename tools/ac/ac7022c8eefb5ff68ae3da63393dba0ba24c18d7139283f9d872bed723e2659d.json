{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List The Guardian's geographic editions (UK, US, Australia, International). Returns edition IDs and hostnames. Use to discover edition-specific content streams before filtering search() by edition.",
 "inputSchema": {
  "examples": [
   {
    "q": "uk"
   }
  ],
  "properties": {
   "q": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "editions",
 "outputSchema": {
  "description": "Guardian editions",
  "properties": {
   "response": {
    "properties": {
     "results": {
      "description": "Array of edition results",
      "items": {
       "properties": {
        "apiUrl": {
         "description": "Edition API endpoint",
         "type": "string"
        },
        "id": {
         "description": "Edition ID",
         "type": "string"
        },
        "webTitle": {
         "description": "Edition web title",
         "type": "string"
        },
        "webUrl": {
         "description": "Edition web URL",
         "type": "string"
        }
       },
       "type": "object"
      },
      "type": "array"
     },
     "status": {
      "description": "Response status",
      "type": "string"
     },
     "userTier": {
      "description": "User tier level",
      "type": "string"
     }
    },
    "type": "object"
   }
  },
  "type": "object"
 },
 "title": "Editions",
 "version": "v1.9579d366"
}
