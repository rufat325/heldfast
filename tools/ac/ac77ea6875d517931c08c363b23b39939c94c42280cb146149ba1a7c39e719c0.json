{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Manage Focus custom-fields with the following actions:\n\n- **create**: Create Custom Fields. Requires workspace admin (403 otherwise). field_type is one of select, multi_select, text, number, date, checkbox, users; select/multi_select fields can be seeded with options inline. (data: payload*{entity_type*, field_type*, name*, description, options})\n- **delete**: Delete Custom Fields. Requires workspace admin. Soft-deletes the field and removes it from entities; this also discards stored values. (data: custom_field_id*)\n- **get**: Get Custom Fields. (data: custom_field_id*)\n- **list**: List Custom Fields. Custom fields are workspace-level definitions attached to project or task entities. Reads work for all members; create/update/delete return 403 for non-admins. Reordering fields is not available through this tool yet. (data: entity_type, order_by, page, per_page)\n- **options-create**: Options Create Custom Fields. Requires workspace admin. Only valid for select and multi_select fields. (data: custom_field_id*, payload*{name*, color, description})\n- **options-delete**: Options Delete Custom Fields. Requires workspace admin. Deleting an option clears it from entities that have it selected. (data: custom_field_id*, option_id*)\n- **options-list**: Options List Custom Fields. (data: custom_field_id*, name, order_by, page, per_page)\n- **options-update**: Options Update Custom Fields. Requires workspace admin. Full update (PUT) of the option's name/color/description. (data: custom_field_id*, option_id*, payload*{name*, color, description})\n- **update**: Update Custom Fields. Requires workspace admin. Full update (PUT) of name/description only; field_type and entity_type are immutable. Manage options via the options-* actions. (data: custom_field_id*, payload*{name*, description})\n- **update-options-order**: Update Options Order Custom Fields. (data: custom_field_id*, payload*{ordered_ids*})\n- **update-order**: Update Order Custom Fields. (data: payload*{entity_type*, ordered_ids*})\n- **update-user-custom-field-values**: Update User Custom Field Values Custom Fields. (data: payload*, user_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "action": {
    "description": "One of: create, delete, get, list, options-create, options-delete, options-list, options-update, update, update-options-order, update-order, update-user-custom-field-values",
    "enum": [
     "create",
     "delete",
     "get",
     "list",
     "options-create",
     "options-delete",
     "options-list",
     "options-update",
     "update",
     "update-options-order",
     "update-order",
     "update-user-custom-field-values"
    ],
    "type": "string"
   },
   "confirm_token": {
    "description": "Mutation confirmation token from previous call",
    "type": "string"
   },
   "data": {
    "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
   },
   "dry_run": {
    "type": "boolean"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "custom-fields",
 "title": "Custom-fields"
}
