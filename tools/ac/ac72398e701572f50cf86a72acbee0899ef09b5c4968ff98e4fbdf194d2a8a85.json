{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search French legal units (companies, associations, sole traders) in the INSEE Sirene national registry using a Lucene-style filter query. Supports filtering by company name (denominationUniteLegale), NAF activity code (activitePrincipaleUniteLegale, e.g. \"62.01Z\" for software publishing), administrative status (etatAdministratifUniteLegale: A=active, C=ceased), and legal category code (categorieJuridiqueUniteLegale, e.g. 5710 for SA). Combine clauses with AND / OR operators. Returns SIREN, name, acronym, status, NAF code, legal category, and creation date for each matching company. Use insee.company.by_siren to retrieve full details for a specific SIREN number.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "max": {
    "description": "Maximum number of results to return (1\u201320, default 10).",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Zero-based starting offset for pagination (default 0).",
    "minimum": 0,
    "type": "integer"
   },
   "q": {
    "description": "Lucene-style filter expression on legal-unit fields. Examples: \"denominationUniteLegale:AIRBUS\" searches by company name; \"activitePrincipaleUniteLegale:62.01Z\" searches by NAF activity code; \"etatAdministratifUniteLegale:A\" returns only active companies (A=active, C=ceased); \"categorieJuridiqueUniteLegale:5710\" searches by legal category code. Combine with AND / OR operators.",
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "insee.company.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Search French Companies in Sirene"
}
