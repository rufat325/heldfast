{
 "description": "Create a system-neutral adventure starter for 1 to 8 players: player handout, private game-master twist, three timed scenes, clues, alternate approaches and a reward. Choose fantasy, space or cozy. Original template combinations, not an LLM or a game-system rules engine; seeds can repeat combinations. GET accepts scalar query parameters. For object or array inputs, pass the complete input object as JSON in the payload query parameter. Price: 0.01 USDC. Mode: computed_service. Source: DopamineDesk original system-neutral story templates v1. Set auto_pay=true for a one-call purchase when the local buyer wallet is configured.",
 "inputSchema": {
  "properties": {
   "agent_token": {
    "description": "Optional pre-funded marketplace bearer token.",
    "type": "string"
   },
   "auto_pay": {
    "description": "Automatically authorize and pay this request with the locally configured buyer wallet. Requires X402_AUTO_PAY=true and X402_EVM_PRIVATE_KEY.",
    "type": "boolean"
   },
   "minutes": {
    "default": 60,
    "maximum": 180,
    "minimum": 20,
    "type": "integer"
   },
   "payload": {
    "type": "string"
   },
   "payment_id": {
    "description": "payment_id returned by the x402 invoice.",
    "type": "string"
   },
   "payment_signature": {
    "description": "Base64 x402 v2 PAYMENT-SIGNATURE produced by a compatible buyer client.",
    "type": "string"
   },
   "players": {
    "default": 4,
    "maximum": 8,
    "minimum": 1,
    "type": "integer"
   },
   "preview": {
    "description": "Inspect a rate-limited live contract proof showing source, freshness, response shape, and required inputs. Decision-ready values remain in the paid result.",
    "type": "boolean"
   },
   "seed": {
    "maxLength": 100,
    "type": "string"
   },
   "theme": {
    "default": "fantasy",
    "enum": [
     "fantasy",
     "space",
     "cozy"
    ],
    "type": "string"
   },
   "tx_hash": {
    "description": "Base transaction hash after paying an x402 invoice.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "get_tabletop_quest_seed"
}
