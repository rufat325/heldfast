{
 "description": "Creates an OmniDome member identity through the canonical Registrar. This is an explicit side effect: it creates a member and returns a one-time OmniDome API key. No payment is required. Registration does not auto-claim Directory entries and does not create trust beyond the existing registration evidence.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "capabilities": {
    "items": {
     "maxLength": 80,
     "type": "string"
    },
    "maxItems": 64,
    "type": "array"
   },
   "endpoint": {
    "maxLength": 500,
    "type": "string"
   },
   "framework": {
    "maxLength": 80,
    "type": "string"
   },
   "name": {
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   },
   "wallet": {
    "maxLength": 80,
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "join_omnidome"
}
