{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search Poshmark SOLD listings and return realized/ask-at-sale prices \u2014 the best open sold-price comp data on Poshmark (no login, no API key). Sourced live from poshmark.com/search?availability=sold_out. Returns up to ~48 results per call (one page, no pagination cursor). `price` is the listing price shown on the sold post, as Poshmark reports it \u2014 not an independently verified receipt.",
 "inputSchema": {
  "examples": [
   {
    "limit": 10,
    "query": "lululemon align",
    "sort_by": "relevance"
   },
   {
    "limit": 5,
    "query": "coach tabby bag",
    "sort_by": "price_desc"
   }
  ],
  "properties": {
   "department": {
    "description": "Optional department filter as Poshmark spells it, e.g. \"Women\", \"Men\", \"Kids\", \"Home\"",
    "type": "string"
   },
   "limit": {
    "description": "Max results to return, 1-48 (default 24)",
    "type": "number"
   },
   "query": {
    "description": "Search text, e.g. \"lululemon align leggings\" or \"coach tabby bag\"",
    "type": "string"
   },
   "sort_by": {
    "description": "relevance (default) | price_desc | price_asc",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "poshmark_sold_search",
 "title": "Poshmark Sold Search",
 "version": "v1.bc9966ab"
}
