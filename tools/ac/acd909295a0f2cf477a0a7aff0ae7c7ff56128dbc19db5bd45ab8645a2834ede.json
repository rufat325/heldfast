{
 "description": "List an Instagram user's posts. Pass pagination_token or next_max_id as cursor. 2 credits.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "count": {
    "default": 12,
    "description": "Posts per page (best effort).",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "cursor": {
    "description": "pagination_token or next_max_id from previous response.",
    "type": "string"
   },
   "user_id": {
    "description": "Numeric user ID.",
    "type": "string"
   },
   "username": {
    "description": "Handle without @.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_instagram_user_posts"
}
