{
 "description": "Inspect a GTM workspace for common analytics implementation issues. Returns structured findings with severity levels (error/warning/info). This is a read-only operation \u2014 it never modifies anything.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "accountId": {
    "type": "string"
   },
   "containerId": {
    "type": "string"
   },
   "includeInfo": {
    "default": false,
    "description": "Include informational findings (verbose). Default: false.",
    "type": "boolean"
   },
   "workspaceId": {
    "type": "string"
   }
  },
  "required": [
   "accountId",
   "containerId",
   "workspaceId"
  ],
  "type": "object"
 },
 "name": "audit_container"
}
