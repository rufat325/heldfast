{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Activate a flow"
 },
 "description": "Activate a flow. Sets the flow live. Publishes the draft first when there is one, exactly like the builder's toggle. Plans cap live flows (403 plan_limit_reached). Send {} as the body.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Numeric id.",
    "type": "integer"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "activate_flow"
}
