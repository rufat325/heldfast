{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the Human Protein Atlas for human genes/proteins by gene symbol or keyword. Returns each gene with its Ensembl gene id (needed by get_protein and top_tissues), synonyms, and description. Keyless.",
 "inputSchema": {
  "examples": [
   {
    "query": "EGFR"
   },
   {
    "limit": 10,
    "query": "insulin receptor"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max genes to return (default 15).",
    "type": "number"
   },
   "query": {
    "description": "Gene symbol (e.g. \"EGFR\") or keyword (e.g. \"insulin receptor\").",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_genes",
 "title": "Search Genes",
 "version": "v1.a5ba95a3"
}
