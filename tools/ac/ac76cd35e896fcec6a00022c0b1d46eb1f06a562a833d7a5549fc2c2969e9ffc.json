{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "create_record"
 },
 "description": "Create a record (an event such as a feeding, health treatment, or movement) and attach it to animals and/or groups. Always pass at least one resolved animal or group UUID \u2014 a record attached to nothing will not appear in any animal's history.",
 "inputSchema": {
  "properties": {
   "animal_ids": {
    "default": [],
    "description": "Animal UUIDs this record applies to.",
    "items": {
     "format": "uuid",
     "type": "string"
    },
    "type": "array"
   },
   "applied_at": {
    "description": "When the event occurred (ISO date/time).",
    "type": "string"
   },
   "description": {
    "description": "Optional description of the record.",
    "type": "string"
   },
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "group_ids": {
    "default": [],
    "description": "Group UUIDs this record applies to.",
    "items": {
     "format": "uuid",
     "type": "string"
    },
    "type": "array"
   },
   "name": {
    "description": "Short name for the event.",
    "type": "string"
   },
   "type": {
    "description": "The kind of event.",
    "enum": [
     "FEED",
     "GENETIC",
     "HEALTH",
     "MOVEMENT",
     "OTHER"
    ],
    "type": "string"
   }
  },
  "required": [
   "name",
   "type",
   "applied_at"
  ],
  "type": "object"
 },
 "name": "create_record"
}
