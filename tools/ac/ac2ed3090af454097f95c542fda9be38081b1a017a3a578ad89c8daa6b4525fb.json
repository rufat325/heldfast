{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search 150M+ scholarly works (journal articles, books, conference papers, datasets) by title, author, or full text. Returns DOI, title, authors, publisher, container (journal/book) title, published date, citation count, and abstract. Complements education.doi_lookup (which resolves a single known DOI) by letting agents discover DOIs from a text query. Data: api.crossref.org (CrossRef REST API), no auth required, CC0 metadata.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "query": {
    "description": "Search query across title, author, and full text (e.g. \"quantum computing\")",
    "minLength": 1,
    "type": "string"
   },
   "rows": {
    "description": "Number of results to return (1-20, default 10)",
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "crossref.works.search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "CrossRef Works Search"
}
