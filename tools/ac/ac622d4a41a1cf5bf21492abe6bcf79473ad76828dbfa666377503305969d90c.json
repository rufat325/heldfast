{
 "description": "List comments on a content article (human + AI suggestions). Default returns only open comments; pass status=all to include resolved. Non-admin users only see AI comments they requested or were @-mentioned in.",
 "inputSchema": {
  "properties": {
   "content_id": {
    "description": "UUID of the content article.",
    "type": "string"
   },
   "status": {
    "description": "Filter by comment status. Defaults to 'open'.",
    "enum": [
     "open",
     "resolved",
     "all"
    ],
    "type": "string"
   }
  },
  "required": [
   "content_id"
  ],
  "type": "object"
 },
 "name": "list_content_comments"
}
