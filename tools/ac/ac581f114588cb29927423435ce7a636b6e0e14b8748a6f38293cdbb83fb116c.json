{
 "annotations": {
  "destructiveHint": true,
  "readOnlyHint": false,
  "title": "Reply to a thread"
 },
 "description": "Reply to an existing email thread. Sent from the current user's connected Gmail address. For single-message threads, threadId works as messageId. For multi-message threads, pass the specific messageId you're replying to \u2014 get_thread returns it. Confirm with the user before sending when intent is unclear. Reference: https://www.dragapp.com/docs/mcp/",
 "inputSchema": {
  "properties": {
   "bcc": {
    "description": "BCC recipient email address",
    "type": "string"
   },
   "boardId": {
    "description": "The board ID the thread belongs to",
    "type": "number"
   },
   "body": {
    "description": "HTML body of the reply",
    "type": "string"
   },
   "cc": {
    "description": "CC recipient email address",
    "type": "string"
   },
   "messageId": {
    "description": "Specific message ID to reply to. Defaults to threadId, which is correct for threads with a single message.",
    "type": "string"
   },
   "threadId": {
    "description": "The thread ID to reply to",
    "type": "string"
   },
   "to": {
    "description": "Recipient email address (use get_thread's replyTo.to or sender field)",
    "type": "string"
   }
  },
  "required": [
   "threadId",
   "boardId",
   "to",
   "body"
  ],
  "type": "object"
 },
 "name": "reply_to_thread",
 "title": "Reply to a thread"
}
