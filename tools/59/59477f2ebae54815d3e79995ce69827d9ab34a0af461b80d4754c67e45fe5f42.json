{
 "description": "List concierge import requests across all farms (admin only). Defaults to PENDING status.",
 "inputSchema": {
  "properties": {
   "skip": {
    "description": "Pagination offset",
    "type": "number"
   },
   "status": {
    "description": "Filter by status (default PENDING)",
    "enum": [
     "PENDING",
     "PROCESSING",
     "COMPLETED",
     "FAILED"
    ],
    "type": "string"
   },
   "take": {
    "description": "Page size",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list_pending_imports"
}
