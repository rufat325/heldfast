{
 "description": "[engram-compat] Update by id: tries memories.gist, then traits.name, else appends a new episode referencing the original.",
 "inputSchema": {
  "properties": {
   "content": {
    "type": "string"
   },
   "id": {
    "type": "string"
   }
  },
  "required": [
   "id",
   "content"
  ],
  "type": "object"
 },
 "name": "mem_update"
}
