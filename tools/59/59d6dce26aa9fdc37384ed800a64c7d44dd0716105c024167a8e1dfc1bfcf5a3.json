{
 "description": "Retract a fact by id, or retract EVERYTHING from a given source_ref (provenance-based deletion: 'forget everything from that session'). Tombstoned, not erased: as_of queries before the retraction still show history; current recall never returns it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "fact_id": {
    "description": "Retract this fact",
    "type": "integer"
   },
   "source_ref": {
    "description": "Retract every fact recorded from this source",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "forget"
}
