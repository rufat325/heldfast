{
 "description": "Register a webhook subscription for a Shopify event topic (orders/create, orders/paid, products/update, app/uninstalled, etc).",
 "inputSchema": {
  "properties": {
   "address": {
    "description": "HTTPS URL that will receive webhook POSTs",
    "type": "string"
   },
   "fields": {
    "description": "Optional list of fields to include in the payload",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "format": {
    "description": "Payload format. Defaults to 'json'.",
    "enum": [
     "json",
     "xml"
    ],
    "type": "string"
   },
   "topic": {
    "description": "Webhook topic (e.g. 'orders/create', 'orders/paid', 'products/update', 'customers/create', 'app/uninstalled')",
    "type": "string"
   }
  },
  "required": [
   "topic",
   "address"
  ],
  "type": "object"
 },
 "name": "register_webhook"
}
