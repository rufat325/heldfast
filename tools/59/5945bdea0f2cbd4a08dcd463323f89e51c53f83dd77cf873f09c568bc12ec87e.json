{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List pages"
 },
 "description": "List pages of a project with filters, search, sort, and pagination. Each page carries a `source` field: 'wayback' (scrapeable from the archive) or 'haloscan' (no Wayback snapshot, Wisewand-rewrite only). Does not include the rewritten body \u2014 use get_page_content for that.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "exclude_system": {
    "description": "Exclude legal/info pages (contact, mentions-legales, CGV, CGU, privacy, a-propos, about, sitemap). These are kept in the DB with excluded_from_redirects=true so they remain reachable on the target site.",
    "type": "boolean"
   },
   "has_data": {
    "description": "Keep only pages with SEO data: haloscan (traffic>0 OR keywords), majestic (backlinks>0), any (either).",
    "enum": [
     "haloscan",
     "majestic",
     "any"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Items per page (default 50, max 100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "order": {
    "description": "Sort order (default desc)",
    "enum": [
     "asc",
     "desc"
    ],
    "type": "string"
   },
   "page": {
    "description": "Page number (default 1)",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "project_id": {
    "description": "Project UUID",
    "format": "uuid",
    "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
    "type": "string"
   },
   "search": {
    "description": "Search by URL or title",
    "type": "string"
   },
   "sort": {
    "description": "Sort field",
    "enum": [
     "created_at",
     "total_traffic",
     "backlinks_count",
     "url",
     "title"
    ],
    "type": "string"
   },
   "source": {
    "description": "Filter by page origin. 'wayback' = discovered via the Wayback Machine CDX API. 'haloscan' = discovered via Haloscan with no Wayback snapshot.",
    "enum": [
     "wayback",
     "haloscan",
     "semrush"
    ],
    "type": "string"
   },
   "status": {
    "description": "Filter by page status. 'empty' = scraped but no content on Wayback. 'failed' = scrape error. 'haloscan' = Haloscan-origin pages with no Wayback snapshot. 'rewritable_wisewand' = every page eligible for Wisewand rewrite (scraped + Haloscan-origin + scrape-failed + empty).",
    "enum": [
     "pending",
     "scraped",
     "empty",
     "failed",
     "rewritten",
     "published",
     "haloscan",
     "rewritable_wisewand"
    ],
    "type": "string"
   }
  },
  "required": [
   "project_id"
  ],
  "type": "object"
 },
 "name": "list_pages"
}
