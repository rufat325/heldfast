{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Read entries from the MindForge audit log (.planning/AUDIT.jsonl), optionally filtered by event type or phase. Use to review what the framework has recorded for this project (task completions, security findings, decisions).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "event": {
    "description": "Filter by event type (e.g. task_completed, security_finding)",
    "type": "string"
   },
   "limit": {
    "description": "Max entries to return (default 50, newest last)",
    "exclusiveMinimum": 0,
    "maximum": 500,
    "type": "integer"
   },
   "phase": {
    "description": "Filter by phase number",
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "mindforge_audit_log",
 "title": "Read MindForge audit log"
}
