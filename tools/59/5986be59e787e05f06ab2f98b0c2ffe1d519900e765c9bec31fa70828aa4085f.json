{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find scholarly works (journal articles, preprints) that cite a given dataset DOI. Returns each citing work's DOI, work type, publisher member ID, and the citation timestamp. Useful for measuring dataset reuse/impact or tracing which papers relied on a specific dataset. Data: api.crossref.org/beta/datacitations (CrossRef Data Citations API), no auth required, CC0 metadata, ~5-day delay between deposit and API appearance.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataset_doi": {
    "description": "DOI of the dataset to find citing works for (e.g. \"10.1037/t00742-000\")",
    "minLength": 1,
    "type": "string"
   },
   "rows": {
    "description": "Number of citation events to return (1-500, default 20)",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "dataset_doi"
  ],
  "type": "object"
 },
 "name": "crossref-datacitations.citations.to_dataset",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "CrossRef Papers Citing a Dataset"
}
