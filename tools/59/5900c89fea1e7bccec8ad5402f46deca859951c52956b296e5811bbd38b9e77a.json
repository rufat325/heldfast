{
 "annotations": {
  "readOnlyHint": true,
  "title": "Find similar providers"
 },
 "description": "Providers similar to a given one (alternatives) by shared tags, areas, industry, region, and artifact coverage. Products under the same owner (a sister, not a rival) are left out of the ranking and listed in meta.query.family; include_family=true ranks them in, flagged same_estate.",
 "inputSchema": {
  "properties": {
   "context": {
    "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable.",
    "type": "string"
   },
   "include_family": {
    "description": "Rank same-owner products in with the rivals (flagged same_estate) instead of listing them in meta.query.family.",
    "type": "boolean"
   },
   "limit": {
    "default": 25,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "slug": {
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "find_similar_providers"
}
