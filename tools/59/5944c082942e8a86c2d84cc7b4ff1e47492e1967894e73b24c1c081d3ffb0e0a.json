{
 "description": "List the cheapest indexed peers in the same category, ranked by FMV ascending.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "limit": {
    "maximum": 20,
    "minimum": 1,
    "type": "integer"
   },
   "slug": {
    "minLength": 1,
    "type": "string"
   },
   "unit": {
    "type": "string"
   }
  },
  "required": [
   "slug"
  ],
  "type": "object"
 },
 "name": "find_substitutes",
 "title": "Find cheaper substitutes for a service"
}
