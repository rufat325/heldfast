{
 "description": "Extract text from a local PDF as plain text, Markdown, or JSON. The file is read from disk and never uploaded.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "format": {
    "description": "Output format (default: text)",
    "enum": [
     "text",
     "markdown",
     "json"
    ],
    "type": "string"
   },
   "input": {
    "description": "Path to the input PDF",
    "type": "string"
   },
   "output": {
    "description": "Optional path to write the extracted text to",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "pdf_extract_text",
 "title": "Extract text from a PDF (local, private)"
}
