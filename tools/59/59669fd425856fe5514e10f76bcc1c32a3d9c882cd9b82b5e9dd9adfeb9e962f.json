{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List StatCan series that changed (new release) on a given date (default today). Useful to detect updated cubes for scheduled refreshes.",
 "inputSchema": {
  "examples": [
   {},
   {
    "date": "2024-01-15"
   }
  ],
  "properties": {
   "date": {
    "description": "YYYY-MM-DD (default today).",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "statcan_changed_series",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of changed series",
    "type": "number"
   },
   "date": {
    "description": "Query date (YYYY-MM-DD or 'today')",
    "type": "string"
   },
   "series": {
    "description": "List of changed series",
    "items": {
     "properties": {
      "coordinate": {
       "description": "Series coordinate",
       "type": [
        "string",
        "null"
       ]
      },
      "product_id": {
       "description": "Cube product ID",
       "type": [
        "number",
        "null"
       ]
      },
      "released_at": {
       "description": "Release timestamp",
       "type": [
        "string",
        "null"
       ]
      },
      "vector_id": {
       "description": "Series vector ID",
       "type": [
        "number",
        "null"
       ]
      }
     },
     "required": [
      "product_id",
      "coordinate",
      "vector_id",
      "released_at"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "date",
   "count",
   "series"
  ],
  "type": "object"
 },
 "title": "Statcan Changed Series",
 "version": "v1.6630b98f"
}
