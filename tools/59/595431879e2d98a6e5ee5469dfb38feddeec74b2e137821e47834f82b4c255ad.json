{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "Explain why a generated test exists: which graph evidence anchors support it, source refs, and whether weak/candidate evidence was used. Exposes trust artifacts only, not internal generation logic.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "generated_test_id": {
    "description": "The generated test id (or title) from orangepro_generate_tests.",
    "minLength": 1,
    "type": "string"
   },
   "workspace": {
    "description": "Local workspace root. Defaults to the current directory.",
    "type": "string"
   }
  },
  "required": [
   "generated_test_id"
  ],
  "type": "object"
 },
 "name": "orangepro_explain_test",
 "title": "Explain a generated test"
}
