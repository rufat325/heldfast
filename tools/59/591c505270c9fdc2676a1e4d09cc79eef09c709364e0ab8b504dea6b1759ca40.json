{
 "annotations": {
  "idempotentHint": true,
  "readOnlyHint": true,
  "title": "Get today's premium projection slate (MCP Connect or Pro)"
 },
 "description": "Return today's protected premium slate for an entitled agent.\n\n    Requires ``Authorization: Bearer obmcp_...``. MCP Connect and MCP Pro are\n    both accepted. The response includes premium selections, price, calibrated\n    probability/edge, units, and customer-facing analysis, but excludes raw\n    generator scores, zone rules, audit fields, and other internal features.\n    Also includes today's Premium Leans (``leans``) -- the model's ranked\n    disagreements with the price, published every slate day and graded flat at\n    0.5u in their own track-record column; they are not Kelly-sized picks.\n\n    ``week=True`` (Sep 5 2026, U5) returns the NFL week board instead of\n    today's slate -- the publish-and-hold union of ACTIVE Plays + Leans\n    across every date the current published NFL week spans, so a caller\n    sees the whole week even when called on a date with no NFL kickoff.\n    NFL-only: ``league`` is forced to NFL when omitted and must be NFL\n    (case-insensitive) when given alongside ``week=True``.\n    ",
 "inputSchema": {
  "properties": {
   "league": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "default": null,
    "title": "League"
   },
   "limit": {
    "default": 100,
    "title": "Limit",
    "type": "integer"
   },
   "week": {
    "default": false,
    "title": "Week",
    "type": "boolean"
   }
  },
  "title": "get_premium_slateArguments",
  "type": "object"
 },
 "name": "get_premium_slate",
 "outputSchema": {
  "additionalProperties": true,
  "title": "get_premium_slateDictOutput",
  "type": "object"
 }
}
