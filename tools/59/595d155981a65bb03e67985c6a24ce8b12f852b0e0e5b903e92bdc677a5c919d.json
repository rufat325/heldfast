{
 "description": "Search TikTok Commercial Content Library \u2014 relevance-filtered, uniform null schema (2 credits). Costs 2 credits. Empty results and failures are never charged. Pass cache=true for a free 24h cache hit (default always fresh).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "cache": {
    "description": "Set true to serve from the 24h response cache (0 credits on hit). Default false \u2014 always fetch fresh. Envelope includes cached + cachedAt on hits.",
    "type": "boolean"
   },
   "country": {
    "description": "Two-letter ISO country code (e.g. GB, DE, FR). Default GB (EU DSA library; US often empty).",
    "type": "string"
   },
   "limit": {
    "description": "Max items to return (default 20, max 200). Flat 2 credits per call.",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "match": {
    "description": "Keyword token mode: \"any\" (default, OR whole-word) or \"all\" (AND). hair \u2260 wheelchair. Empty results are free.",
    "type": "string"
   },
   "q": {
    "description": "Keyword or advertiser to search TikTok Commercial Content Library (min 2 characters).",
    "type": "string"
   }
  },
  "required": [
   "q"
  ],
  "type": "object"
 },
 "name": "tiktok_ad_library_search",
 "title": "TikTok Ad Library Search"
}
