{
 "description": "Create a new MIDI or audio track at an optional index. Use when the user explicitly wants another track. NOT idempotent: every call creates a track; returns the created track and verifies that its type matches the request.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "index": {
    "minimum": 0,
    "type": "integer"
   },
   "name": {
    "minLength": 1,
    "type": "string"
   },
   "type": {
    "enum": [
     "midi",
     "audio"
    ],
    "type": "string"
   }
  },
  "required": [
   "type"
  ],
  "type": "object"
 },
 "name": "track_create"
}
