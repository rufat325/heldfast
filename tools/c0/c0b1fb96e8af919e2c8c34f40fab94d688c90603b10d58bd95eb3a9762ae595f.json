{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Find Emerging Skills"
 },
 "description": "Skills with low-but-consistently-growing market adoption \u2014 early-stage signals. Analyst tier. Tracks companyCount across the last 3 monthly snapshots and surfaces skills that climbed consistently (non-decreasing) from a low base, with a meaningful absolute company-count gain \u2014 not just a big percentage on a tiny base. Use for 'what skills are quietly trending?' or 'what should I learn before everyone else?'. Defaults: adoptionMin 5, adoptionMax 25, growthMin 30%, minDelta 4, baseMax 8, limit 20. Returns a deliberately tight list; loosen minDelta / baseMax / growthMin to widen it. Not for trend lines on an established, named skill (`get_skill_history`) or role-level emergence (`find_emerging_roles`).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "adoptionMax": {
    "description": "Maximum companies \u2014 excludes already-mainstream skills (default 25). The point is to find what's *emerging*, not what's already everywhere.",
    "minimum": 1,
    "type": "integer"
   },
   "adoptionMin": {
    "description": "Minimum companies that must currently mention the skill (default 5). Lower = catches earlier-stage skills.",
    "minimum": 1,
    "type": "integer"
   },
   "baseMax": {
    "description": "Maximum companies at the START of the window (default 8). Enforces the 'started obscure' criterion so already-established skills don't qualify.",
    "minimum": 1,
    "type": "integer"
   },
   "growthMin": {
    "description": "Minimum growth percentage across the snapshot window (default 30)",
    "minimum": 0,
    "type": "integer"
   },
   "limit": {
    "description": "Max skills to return (1-50, default 20)",
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   },
   "minDelta": {
    "description": "Minimum ABSOLUTE company-count gain over the window (default 4). Filters small-base noise \u2014 a skill going 2\u21924 is +100% but only +2 companies.",
    "minimum": 0,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "find_emerging_skills",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "citation_url": {
    "description": "hirejack.com URL to cite when surfacing this result",
    "type": "string"
   },
   "data": {
    "additionalProperties": true,
    "description": "Skills climbing consistently from a low adoption base",
    "properties": {
     "filters": {
      "additionalProperties": true,
      "description": "Echo of the applied thresholds",
      "properties": {},
      "type": "object"
     },
     "matched": {
      "description": "Skills that cleared the thresholds (before the limit)",
      "type": "number"
     },
     "note": {
      "description": "Present when there isn't enough snapshot history",
      "type": "string"
     },
     "skills": {
      "description": "Emerging skills, largest absolute company gain first",
      "items": {
       "additionalProperties": true,
       "properties": {
        "baseCompanyCount": {
         "description": "Companies at the start of the window",
         "type": "number"
        },
        "category": {
         "description": "Skill category, e.g. 'language', 'framework', 'ml'",
         "type": "string"
        },
        "companyCount": {
         "description": "Companies mentioning it in the latest month",
         "type": "number"
        },
        "companyCountDelta": {
         "description": "Absolute company gain over the window",
         "type": "number"
        },
        "growthPct": {
         "description": "Growth over the window, %",
         "type": "number"
        },
        "id": {
         "description": "Skill id \u2014 pass to get_skill_history",
         "type": "string"
        },
        "jobMentions": {
         "description": "Job postings mentioning it in the latest month",
         "type": "number"
        },
        "name": {
         "type": "string"
        },
        "prevCompanyCount": {
         "description": "Back-compat alias for baseCompanyCount",
         "type": "number"
        },
        "trajectory": {
         "description": "Per-month climb so the non-decreasing-growth claim is auditable",
         "items": {
          "additionalProperties": true,
          "properties": {
           "companyCount": {
            "type": "number"
           },
           "jobMentions": {
            "type": "number"
           },
           "month": {
            "description": "YYYY-MM",
            "type": "string"
           }
          },
          "type": "object"
         },
         "type": "array"
        },
        "window": {
         "additionalProperties": true,
         "properties": {
          "from": {
           "description": "YYYY-MM",
           "type": "string"
          },
          "to": {
           "description": "YYYY-MM",
           "type": "string"
          }
         },
         "type": "object"
        }
       },
       "type": "object"
      },
      "type": "array"
     },
     "window": {
      "additionalProperties": true,
      "properties": {
       "from": {
        "description": "YYYY-MM",
        "type": "string"
       },
       "months": {
        "description": "Monthly snapshots in the window (up to 3)",
        "type": "number"
       },
       "to": {
        "description": "YYYY-MM",
        "type": "string"
       }
      },
      "type": "object"
     }
    },
    "type": "object"
   },
   "meta": {
    "additionalProperties": true,
    "description": "Request/response metadata (pagination cursors, snapshot timestamps, applied filters)",
    "properties": {},
    "type": "object"
   }
  },
  "required": [
   "data"
  ],
  "type": "object"
 },
 "title": "Find Emerging Skills"
}
