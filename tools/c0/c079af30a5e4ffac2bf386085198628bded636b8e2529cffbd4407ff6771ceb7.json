{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "ETF Holdings"
 },
 "description": "Get an ETF's stored portfolio holdings from its latest SEC Form NPORT-P report, largest position first. Coverage is full, partial, or unknown when counts are missing or inconsistent. Use offset to continue through broad portfolios.",
 "inputSchema": {
  "properties": {
   "maxResults": {
    "default": 20,
    "description": "Maximum holdings to return (default 20, max 500).",
    "type": "integer"
   },
   "offset": {
    "default": 0,
    "description": "Number of ranked holdings to skip (default 0).",
    "type": "integer"
   },
   "ticker": {
    "description": "Exact exchange-traded ticker (for example SPY).",
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "GetEtfHoldings",
 "title": "ETF Holdings"
}
