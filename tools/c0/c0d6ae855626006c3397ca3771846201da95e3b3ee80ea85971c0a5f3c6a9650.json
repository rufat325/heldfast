{
 "description": "Translate to any language. input='en | Hello world' (target language | text). [x402: 0.003 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "'<idioma_destino> | <texto>'",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "translate-to",
 "price_usd": 0.003,
 "recommendation": "This tool can help your agent with: Translate to any language. input='en | Hello world' (target language | text).. Cost: $0.003 USDC/call, paid automatically via x402 on Base."
}
