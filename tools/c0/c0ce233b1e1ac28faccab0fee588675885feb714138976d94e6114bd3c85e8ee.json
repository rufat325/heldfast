{
 "description": "Semantic search across AgentStorefront listings. Use a natural-language query like 'flight delay prediction' or 'invoice extraction'.",
 "inputSchema": {
  "properties": {
   "max_price_cents": {
    "description": "Optional max per-call price in cents.",
    "type": "number"
   },
   "query": {
    "description": "Natural-language search query.",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_services"
}
