{
 "description": "List or remove inactive non-persistent agent accounts (requires master API key)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "_account": {
    "description": "Optional. Override identity for THIS call: pass the AgenticMail agent name (e.g. \"Fola\") to authenticate as that agent. Requires AGENTICMAIL_ACCOUNT_KEYS_JSON to contain a matching key. Omit to use the default identity (AGENTICMAIL_API_KEY).",
    "type": "string"
   },
   "action": {
    "description": "Action to perform",
    "enum": [
     "list_inactive",
     "cleanup",
     "set_persistent"
    ],
    "type": "string"
   },
   "agentId": {
    "description": "Agent ID (for set_persistent)",
    "type": "string"
   },
   "dryRun": {
    "description": "Preview without deleting (for cleanup)",
    "type": "boolean"
   },
   "hours": {
    "description": "Inactivity threshold in hours (default: 24)",
    "type": "number"
   },
   "persistent": {
    "description": "Set persistent flag (for set_persistent)",
    "type": "boolean"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "cleanup_agents"
}
