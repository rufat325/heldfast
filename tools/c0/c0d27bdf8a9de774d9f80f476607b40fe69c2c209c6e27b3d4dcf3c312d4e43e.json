{
 "description": "Retrieves complete details for one or more Moves, based on OrderID(s). Returns the raw order details.",
 "execution": {
  "taskSupport": "optional"
 },
 "inputSchema": {
  "properties": {
   "body": {
    "description": "The order-details request body (the OrderID(s) to look up)",
    "properties": {
     "orderIds": {
      "items": {
       "type": "integer"
      },
      "minItems": 1,
      "type": "array"
     }
    },
    "required": [
     "orderIds"
    ],
    "type": "object"
   }
  },
  "required": [
   "body"
  ],
  "type": "object"
 },
 "name": "get_order_details"
}
