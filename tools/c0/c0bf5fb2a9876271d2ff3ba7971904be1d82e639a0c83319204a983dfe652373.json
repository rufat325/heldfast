{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "[Requires login] Apply ONE set of values to many entries on the authenticated user's own AniList list in a single request \u2014 e.g. mark thirty titles COMPLETED, or set the same score on a batch. Use this instead of calling update_list_entry in a loop whenever the entries share the values you're writing; use update_list_entry when each entry needs DIFFERENT values (this tool cannot vary them per entry), or when you need to set `customLists`/`advancedScores`, which this tool deliberately doesn't accept. Takes list-ENTRY ids (not media ids) \u2014 get them from get_user_list. The whole call is all-or-nothing: if any id is unknown or isn't yours, AniList rejects the request and applies NOTHING, so there is never a half-applied batch to clean up \u2014 but the error does not say which id was at fault, so re-check the ids against get_user_list. Returns a summary (how many were updated, and their ids), not the full entries.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "completedAt": {
    "description": "New completion date for every listed entry.",
    "properties": {
     "day": {
      "maximum": 31,
      "minimum": 1,
      "type": "integer"
     },
     "month": {
      "maximum": 12,
      "minimum": 1,
      "type": "integer"
     },
     "year": {
      "exclusiveMinimum": 0,
      "maximum": 9007199254740991,
      "type": "integer"
     }
    },
    "type": "object"
   },
   "hiddenFromStatusLists": {
    "description": "Hide/unhide every listed entry from the public status-grouped list views.",
    "type": "boolean"
   },
   "listEntryIds": {
    "description": "The list ENTRY ids to update (not media ids), from get_user_list. Every id must exist and belong to you \u2014 one bad id fails the entire call without changing anything.",
    "items": {
     "exclusiveMinimum": 0,
     "maximum": 2147483647,
     "type": "integer"
    },
    "minItems": 1,
    "type": "array"
   },
   "notes": {
    "description": "New free-text notes, written to every listed entry (per AniList's own schema, capped at 6000 characters).",
    "maxLength": 6000,
    "type": "string"
   },
   "priority": {
    "description": "New list priority (higher = more important; per AniList's own schema, capped at 255).",
    "maximum": 255,
    "minimum": 0,
    "type": "integer"
   },
   "private": {
    "description": "Hide/unhide every listed entry.",
    "type": "boolean"
   },
   "progress": {
    "description": "New episodes watched / chapters read, applied to every listed entry \u2014 only meaningful when they genuinely share a progress value.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "progressVolumes": {
    "description": "New volumes read (manga only).",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "repeat": {
    "description": "New rewatch/reread count (per AniList's own schema, capped at 1000).",
    "maximum": 1000,
    "minimum": 0,
    "type": "integer"
   },
   "score": {
    "description": "New score out of 10 (decimals allowed), applied to every listed entry. Always on this scale regardless of the account's configured scoreFormat.",
    "maximum": 10,
    "minimum": 0,
    "type": "number"
   },
   "startedAt": {
    "description": "New start date for every listed entry.",
    "properties": {
     "day": {
      "maximum": 31,
      "minimum": 1,
      "type": "integer"
     },
     "month": {
      "maximum": 12,
      "minimum": 1,
      "type": "integer"
     },
     "year": {
      "exclusiveMinimum": 0,
      "maximum": 9007199254740991,
      "type": "integer"
     }
    },
    "type": "object"
   },
   "status": {
    "description": "New list status, applied to every listed entry.",
    "enum": [
     "CURRENT",
     "PLANNING",
     "COMPLETED",
     "DROPPED",
     "PAUSED",
     "REPEATING"
    ],
    "type": "string"
   }
  },
  "required": [
   "listEntryIds"
  ],
  "type": "object"
 },
 "name": "update_list_entries",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "listEntryIds": {
    "description": "The list-entry ids AniList reported as updated.",
    "items": {
     "exclusiveMinimum": 0,
     "maximum": 2147483647,
     "type": "integer"
    },
    "type": "array"
   },
   "updated": {
    "description": "How many entries AniList reported as updated.",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "updated",
   "listEntryIds"
  ],
  "type": "object"
 },
 "title": "Update many entries on your AniList list at once"
}
