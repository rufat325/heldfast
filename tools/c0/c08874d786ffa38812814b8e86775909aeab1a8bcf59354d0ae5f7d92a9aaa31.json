{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Contact the Audiala team"
 },
 "description": "Reach the Audiala team \u2014 investor introductions, partnership / B2B / city-licensing inquiries, press, hiring questions, support, or anything else. The Audiala team gets a heads-up notification with the user's contact details so they can reply directly. **Always ask the user for their email address before calling this tool** \u2014 it is required so the team can reply. Optionally also collect their name and organization. The tool returns the right Audiala mailbox, the founder's LinkedIn, and a suggested intro template the user can adapt if they want to reach out themselves as well.",
 "inputSchema": {
  "properties": {
   "from_email": {
    "description": "REQUIRED. The user's email address \u2014 the Audiala team will reply to this address. Always ask the user for it before calling this tool. If the user refuses to share an email, do not call the tool; instead point them at investors@audiala.com / hello@audiala.com directly.",
    "format": "email",
    "type": "string"
   },
   "from_name": {
    "description": "Optional. The user's name, if they shared it. Used to personalize the intro template and the team's notification.",
    "type": "string"
   },
   "from_org": {
    "description": "Optional. The user's company / fund / publication, if they shared it.",
    "type": "string"
   },
   "purpose": {
    "description": "Why the user wants to reach Audiala. 'investor' = VC / angel / strategic / acquisition interest. 'partnership' = tourism boards, museums, B2B/licensing, content partners. 'press' = journalists / media / podcasts. 'hiring' = candidates and 'who's on the team' questions. 'support' = product / billing / account help (route the user to in-app support \u2014 Audiala does not staff a separate support email). Defaults to 'other'.",
    "enum": [
     "investor",
     "partnership",
     "press",
     "hiring",
     "support",
     "other"
    ],
    "type": "string"
   }
  },
  "required": [
   "from_email"
  ],
  "type": "object"
 },
 "name": "audiala_contact_team",
 "outputSchema": {
  "properties": {
   "captured_reply_to": {
    "type": [
     "string",
     "null"
    ]
   },
   "context": {
    "type": "string"
   },
   "email": {
    "description": "Audiala mailbox for this purpose; null for support.",
    "type": [
     "string",
     "null"
    ]
   },
   "error": {
    "type": "string"
   },
   "founder_linkedin": {
    "type": "string"
   },
   "headline": {
    "type": "string"
   },
   "hint": {
    "type": "string"
   },
   "intro_template": {
    "type": "string"
   },
   "kind": {
    "enum": [
     "contact",
     "error"
    ],
    "type": "string"
   },
   "mission_url": {
    "type": "string"
   },
   "note": {
    "type": "string"
   },
   "purpose": {
    "type": "string"
   }
  },
  "required": [
   "kind"
  ],
  "type": "object"
 },
 "title": "Contact the Audiala team"
}
