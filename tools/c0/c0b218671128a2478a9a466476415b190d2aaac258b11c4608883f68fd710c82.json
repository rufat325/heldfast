{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Set Screenshot Translations"
 },
 "description": "Write a locale's translation overrides for screens in a screenshot set (text copy, localized captures/images). Geometry and styling always come from the source layout; anything not overridden falls back to it. The locale is auto-enabled on the set. Requires an authorized Sonar account or a write-scope API key.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "entries": {
    "description": "One entry per screen to localize.",
    "items": {
     "properties": {
      "overrides": {
       "additionalProperties": {},
       "description": "Sparse overrides keyed by layer id, e.g. {\"headline\": {\"text\": \"\u2026\"}, \"device\": {\"screenshotDataUrl\": \"https://\u2026\"}}. Replaces the screen's stored overrides for this locale wholesale.",
       "propertyNames": {
        "type": "string"
       },
       "type": "object"
      },
      "screenshot_id": {
       "description": "Screen this entry localizes.",
       "minLength": 1,
       "type": "string"
      }
     },
     "required": [
      "screenshot_id",
      "overrides"
     ],
     "type": "object"
    },
    "maxItems": 50,
    "minItems": 1,
    "type": "array"
   },
   "locale": {
    "description": "Target locale code, e.g. \"de-DE\", \"pt-BR\", \"zh-Hans\".",
    "maxLength": 8,
    "minLength": 2,
    "type": "string"
   },
   "set_id": {
    "description": "Screenshot set id.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "set_id",
   "locale",
   "entries"
  ],
  "type": "object"
 },
 "name": "sonar_set_screenshot_translations",
 "title": "Set Screenshot Translations"
}
