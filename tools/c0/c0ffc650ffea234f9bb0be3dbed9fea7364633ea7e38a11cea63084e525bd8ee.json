{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Browse or search Figshare's subject taxonomy (2000+ categories, e.g. \"Ecohydrology\", \"Agricultural biotechnology\") used to classify research articles. Optionally filter by a free-text title query. Returns category id, title, parent_id, and taxonomy path \u2014 useful for understanding how Figshare organizes research by subject area. Data: Figshare.com, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max categories to return (default 50, max 200).",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Optional free-text filter on category/subject title (e.g. \"ecology\", \"biotechnology\"). Omit to browse the full taxonomy.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "figshare.taxonomy.categories",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Browse Figshare Subject Categories"
}
