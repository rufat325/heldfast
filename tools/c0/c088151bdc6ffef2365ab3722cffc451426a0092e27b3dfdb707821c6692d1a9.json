{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List Members of the European Parliament (MEPs) currently serving in the active parliamentary term. Filter by country (ISO 3166-1 alpha-2 code), political group (PPE, S-D, RENEW, VERTS-ALE, ECR, THE-LEFT, ID, NI), or gender. Returns each MEP's numeric CODICT identifier, full name, family name, given name, country of representation, and political group abbreviation. Use the returned identifier with the meps.details tool to fetch the full membership history. Supports pagination via limit (max 100) and offset. Data source: European Parliament Open Data API v2 \u2014 CC BY 4.0, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "country": {
    "description": "Filter by EU member state using ISO 3166-1 alpha-2 code. Example: DE (Germany), FR (France), PL (Poland).",
    "maxLength": 2,
    "minLength": 2,
    "type": "string"
   },
   "gender": {
    "description": "Filter MEPs by gender (MALE or FEMALE).",
    "enum": [
     "MALE",
     "FEMALE"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Number of MEPs to return (1\u2013100, default 50).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Number of results to skip for pagination (default 0).",
    "minimum": 0,
    "type": "integer"
   },
   "parliamentary_term": {
    "description": "Parliamentary term number (1\u201310). Omit for current term (10). Example: 10 for the 2024\u20132029 term.",
    "maximum": 10,
    "minimum": 1,
    "type": "integer"
   },
   "political_group": {
    "description": "Filter by EP political group abbreviation. PPE=centre-right, S-D=social-democrat, RENEW=liberal, VERTS-ALE=green, ECR=conservative, THE-LEFT=far-left, ID=far-right, NI=non-attached.",
    "enum": [
     "PPE",
     "NI",
     "S-D",
     "VERTS-ALE",
     "ECR",
     "RENEW",
     "THE-LEFT",
     "ID"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "eu_parliament.meps.list",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "List EU Parliament MEPs"
}
