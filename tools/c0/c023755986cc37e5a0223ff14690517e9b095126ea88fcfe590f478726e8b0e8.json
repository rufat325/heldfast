{
 "description": "Write raw bytes to a file at a specific offset.",
 "inputSchema": {
  "properties": {
   "file_path": {
    "description": "Path to the file",
    "type": "string"
   },
   "hex_data": {
    "description": "Hex string to write (e.g. \"90 90 90\" or \"909090\")",
    "type": "string"
   },
   "offset": {
    "description": "Byte offset to write at",
    "type": "number"
   }
  },
  "required": [
   "file_path",
   "offset",
   "hex_data"
  ],
  "type": "object"
 },
 "name": "hex_write"
}
