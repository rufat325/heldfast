{
 "description": "Generate AI-powered clinical insights from a lab upload. Identifies patterns, risk tiers, physiologic clusters, and root causes across 88+ biomarkers. Returns prioritized findings with follow-up recommendations. Costs 25 credits per call.",
 "inputSchema": {
  "properties": {
   "patient_id": {
    "description": "Optional patient UUID for longitudinal context (trending vs. prior results)",
    "type": "string"
   },
   "upload_id": {
    "description": "UUID of the lab upload to analyze (returned when labs are imported into E-Labus)",
    "type": "string"
   }
  },
  "required": [
   "upload_id"
  ],
  "type": "object"
 },
 "name": "generate_clinical_insights"
}
