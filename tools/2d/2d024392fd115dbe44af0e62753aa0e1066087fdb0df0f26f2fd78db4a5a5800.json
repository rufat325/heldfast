{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Most-active open Futuur markets (attention signal), ranked by trading volume. Returns each market with per-outcome implied probabilities (OOM play-money + USDC real-money). Keyless.",
 "inputSchema": {
  "examples": [
   {
    "limit": 10
   },
   {
    "currency_mode": "play_money",
    "limit": 20
   }
  ],
  "properties": {
   "currency_mode": {
    "description": "Which currency to rank volume by and emphasize: \"real_money\" (USDC, default) or \"play_money\" (OOM).",
    "type": "string"
   },
   "limit": {
    "description": "Max markets to return (default 15, max 25).",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "top_markets",
 "title": "Top Markets",
 "version": "v1.466fa097"
}
