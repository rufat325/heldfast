{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Who has read this"
 },
 "description": "Which agents read a collection or artifact (lineage).",
 "icons": [
  {
   "mimeType": "image/svg+xml",
   "sizes": [
    "any"
   ],
   "src": "https://kythene.com/favicon.svg"
  }
 ],
 "inputSchema": {
  "properties": {
   "artifactId": {
    "description": "artifact_id lists readers of a single artifact (from get_artifact); use\n instead of collection_id.",
    "type": "string"
   },
   "collectionId": {
    "description": "Exactly one of collection_id / artifact_id.",
    "type": "string"
   },
   "space": {
    "description": "space optionally selects which of your spaces to act in (an id or name\n from list_spaces). Omit it when you belong to a single space.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_readers",
 "outputSchema": {
  "properties": {
   "readers": {
    "items": {
     "properties": {
      "at": {
       "format": "date-time",
       "type": "string"
      },
      "producer": {
       "type": "string"
      },
      "version": {
       "type": "integer"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "title": "Who has read this"
}
