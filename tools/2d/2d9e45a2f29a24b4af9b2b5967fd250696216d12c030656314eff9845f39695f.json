{
 "annotations": {
  "readOnlyHint": true,
  "title": "Decrypt Data"
 },
 "description": "Decrypt a HybridCryptoEnvelope from the encrypt tool. Verifies both ML-DSA-65 PQC signature and HMAC-SHA-512 before decrypting.",
 "inputSchema": {
  "properties": {
   "envelope": {
    "additionalProperties": true,
    "description": "The complete HybridCryptoEnvelope from a previous encrypt call",
    "type": "object"
   }
  },
  "required": [
   "envelope"
  ],
  "type": "object"
 },
 "name": "decrypt"
}
