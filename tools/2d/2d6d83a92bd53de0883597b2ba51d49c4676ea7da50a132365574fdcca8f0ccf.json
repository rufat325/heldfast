{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run a Socrata SoQL query against a New Jersey Open Data dataset by resource_id (e.g. \"44xg-bswk\"). Filter with where/select/group/order (SoQL clauses, without the leading $) plus limit/offset. Returns matching rows as JSON.",
 "inputSchema": {
  "examples": [
   {
    "limit": 5,
    "resource_id": "44xg-bswk"
   },
   {
    "limit": 5,
    "resource_id": "44xg-bswk",
    "select": "as_of_date"
   }
  ],
  "properties": {
   "group": {
    "description": "SoQL $group column(s).",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (default Socrata 1000).",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset.",
    "type": "number"
   },
   "order": {
    "description": "SoQL $order, e.g. \"date DESC\".",
    "type": "string"
   },
   "resource_id": {
    "description": "Dataset id, e.g. \"44xg-bswk\" (from datasets).",
    "type": "string"
   },
   "select": {
    "description": "SoQL $select, e.g. \"name, count(*) AS n\".",
    "type": "string"
   },
   "where": {
    "description": "SoQL $where filter, e.g. \"year >= 2020 AND status = 'Active'\".",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "query",
 "title": "Query",
 "version": "v1.80946160"
}
