{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Say which LAYERS of a Dockerfile would be rebuilt when given files change, and which come from cache. Dockerfile linters spot suspicious ordering patterns; this SIMULATES invalidation, which is a different thing: it needs to know which files exist and which changed. In Docker, when one layer is invalidated so is EVERY layer after it, so a `COPY . .` before the `RUN` that installs dependencies condemns you to reinstalling all of them every time you touch a line of code \u2014 and that shows up here as numbers. Respects the `.dockerignore`: what stays out of the context cannot invalidate anything.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "changed": {
    "description": "Files changed since the last build. Empty = none",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "dockerignore": {
    "description": ".dockerignore rules pasted (otherwise looked up in the folder)",
    "type": "string"
   },
   "file": {
    "description": "Path to a specific Dockerfile",
    "type": "string"
   },
   "files": {
    "description": "Project paths, if no folder is given",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "project": {
    "description": "Project folder: its Dockerfile, .dockerignore and files are read",
    "type": "string"
   },
   "text": {
    "description": "The Dockerfile pasted",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "dockerfile_cache"
}
