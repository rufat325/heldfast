{
 "_meta": {
  "skill": "stx"
 },
 "description": "Call a function on a Stacks smart contract. Signs and broadcasts the transaction.\n\nFor typed arguments, use objects like {type: 'uint', value: 100} or {type: 'principal', value: 'SP...'}\n\nPost conditions constrain what assets the transaction can move. Each condition is an object:\n- STX: {type: 'stx', principal: 'SP...', conditionCode: 'eq'|'gt'|'gte'|'lt'|'lte', amount: '1000000'}\n- FT: {type: 'ft', principal: 'SP...', asset: 'SP...contract', assetName: 'token-name', conditionCode: 'eq', amount: '1000'}\n- NFT: {type: 'nft', principal: 'SP...', asset: 'SP...contract', assetName: 'nft-name', tokenId: '1', notSend?: boolean}\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "contractAddress": {
    "description": "The contract deployer's address (e.g., SP2...)",
    "type": "string"
   },
   "contractName": {
    "description": "The contract name (e.g., 'my-token')",
    "type": "string"
   },
   "fee": {
    "description": "Optional fee: 'low' | 'medium' | 'high' preset or micro-STX amount. Clamped to 50,000 uSTX max for contract calls. If omitted, medium-priority fee is auto-resolved. Ignored when sponsored=true.",
    "type": "string"
   },
   "functionArgs": {
    "default": [],
    "description": "Function arguments. For explicit types: {type: 'uint'|'int'|'principal'|..., value: ...}",
    "items": {},
    "type": "array"
   },
   "functionName": {
    "description": "The function to call (e.g., 'transfer')",
    "type": "string"
   },
   "postConditionMode": {
    "default": "deny",
    "description": "'deny' (default): Blocks unexpected transfers. 'allow': Permits any transfers.",
    "enum": [
     "allow",
     "deny"
    ],
    "type": "string"
   },
   "postConditions": {
    "description": "Optional post conditions to constrain asset movements. See description for format.",
    "items": {},
    "type": "array"
   },
   "sponsored": {
    "default": false,
    "description": "Use sponsored transaction relay so a relay service pays fees instead of your wallet. Requires SPONSOR_API_KEY env var or wallet-level sponsorApiKey.",
    "type": "boolean"
   }
  },
  "required": [
   "contractAddress",
   "contractName",
   "functionName"
  ],
  "type": "object"
 },
 "name": "call_contract"
}
