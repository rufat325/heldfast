{
 "description": "Switch a device/emulator between dark and light mode (cmd uimode night).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "mode": {
    "description": "Appearance to set",
    "enum": [
     "dark",
     "light"
    ],
    "type": "string"
   },
   "serial": {
    "description": "Emulator serial (defaults to session or the only device)",
    "type": "string"
   }
  },
  "required": [
   "mode"
  ],
  "type": "object"
 },
 "name": "set_appearance",
 "title": "Set dark/light mode"
}
