{
 "description": "Neural/semantic search via Exa \u2014 finds pages by concept similarity, not keywords. Best when you know WHAT you want but not exact keywords. Requires user to configure exaApiKey via suite_setup.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "category": {
    "description": "Restrict to a content category",
    "enum": [
     "research paper",
     "news",
     "github",
     "tweet",
     "company",
     "personal site",
     "linkedin profile",
     "financial report"
    ],
    "type": "string"
   },
   "excludeDomains": {
    "description": "Exclude these domains",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "includeDomains": {
    "description": "Only include these domains",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "includeText": {
    "description": "Include 1000-char text excerpts (default: false, uses highlights only)",
    "type": "boolean"
   },
   "maxResults": {
    "description": "Max results (default: 10)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   },
   "query": {
    "description": "Conceptual search query (e.g. \"approaches to reducing API costs in agent loops\")",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_semantic"
}
