{
 "description": "Cancels (deletes) a pending reschedule request for a job. Returns the raw result.",
 "execution": {
  "taskSupport": "optional"
 },
 "inputSchema": {
  "properties": {
   "jobId": {
    "description": "The Job ID to delete the reschedule request for",
    "type": "integer"
   }
  },
  "required": [
   "jobId"
  ],
  "type": "object"
 },
 "name": "delete_job_reschedule_request"
}
