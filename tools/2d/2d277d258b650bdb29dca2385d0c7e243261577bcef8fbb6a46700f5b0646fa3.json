{
 "description": "Extract class names from a .NET DLL using stream-based analysis. Works with very large files.",
 "inputSchema": {
  "properties": {
   "file_path": {
    "description": "Path to DLL file",
    "type": "string"
   },
   "max_classes": {
    "description": "Max classes to return (default 200)",
    "type": "number"
   },
   "search_terms": {
    "description": "Filter by these terms (case-insensitive)",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "file_path"
  ],
  "type": "object"
 },
 "name": "extract_dll_classes"
}
