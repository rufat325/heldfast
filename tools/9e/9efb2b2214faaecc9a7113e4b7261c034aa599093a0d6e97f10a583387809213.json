{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Return bounded LaunchDarkly projects with keys, names, tags, environments, and creation metadata. Answers which applications and deployment environments are managed in LaunchDarkly.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-launchdarkly-reader-token",
    "limit": 20
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "LaunchDarkly API access token",
    "type": "string"
   },
   "limit": {
    "description": "Maximum records, default 20 and maximum 100",
    "maximum": 100,
    "minimum": 1,
    "type": "number"
   },
   "offset": {
    "description": "Records to skip for pagination",
    "minimum": 0,
    "type": "number"
   },
   "query": {
    "type": "string"
   }
  },
  "required": [
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "ld_list_projects",
 "title": "Ld List Projects",
 "version": "v1.8749bd9a"
}
