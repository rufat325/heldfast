{
 "description": "Update the status of a project phase. Valid statuses: pending, in_progress, done, blocked. Use this to progress phases as work is completed. Setting a phase to \"done\" automatically records the completion time.",
 "inputSchema": {
  "properties": {
   "phaseId": {
    "description": "Phase ID (from list_phases or get_project)",
    "type": "string"
   },
   "status": {
    "description": "New status for the phase.",
    "enum": [
     "pending",
     "in_progress",
     "done",
     "blocked"
    ],
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID. Defaults to your configured workspace.",
    "type": "string"
   }
  },
  "required": [
   "phaseId",
   "status"
  ],
  "type": "object"
 },
 "name": "update_phase_status"
}
