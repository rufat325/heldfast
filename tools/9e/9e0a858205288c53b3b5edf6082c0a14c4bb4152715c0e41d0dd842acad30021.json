{
 "description": "Fetch a URL and extract readable content.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "extract": {
    "default": "markdown",
    "enum": [
     "text",
     "markdown"
    ],
    "type": "string"
   },
   "url": {
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "fetch_url"
}
