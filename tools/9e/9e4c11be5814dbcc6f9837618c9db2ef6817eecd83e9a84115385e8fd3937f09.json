{
 "_meta": {
  "apiVersion": "1.0.0",
  "examples": [
   {
    "input": {
     "fields": [
      "defaultLimits",
      "ceilings",
      "sandbox"
     ]
    },
    "title": "Which limits apply here?"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Offline preflight (no archive needed): server / engine / tool-API versions, the deflate tier in use and the pinned deterministic tier, runtime codecs (node:zlib, CompressionStream, DecompressionStream) and worker threads, the codec registry, the engine's default `limits`, the operator ceilings (ZIPNATIVE_MCP_MAX_UNCOMPRESSED_BYTES, ZIPNATIVE_MCP_MAX_ENTRIES, ZIPNATIVE_MCP_WORKERS), every server cap, whether the sandbox and the cache are enabled, the 39 frozen ZIP_* error codes and 11 diagnostic codes, and the engine exports this server deliberately does not expose (with why). `network` is always 'none'. Call it first in a session, or whenever a cap / ceiling refusal needs interpreting.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "fields": {
    "description": "Dot-path projection applied after verbosity (e.g. ['entryCount', 'entries.name']); array segments map over elements; unmatched paths are reported in _meta.unmatchedFields.",
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "maxItems": 16,
    "type": "array"
   },
   "verbosity": {
    "default": "full",
    "description": "'summary' returns only the scalar verdict fields (drops arrays and payloads) \u2014 the token-frugal form.",
    "enum": [
     "full",
     "summary"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "describe_engine",
 "outputSchema": {
  "additionalProperties": false,
  "description": "Capability manifest. Every property is optional because the result can be projected: verbosity:'summary' returns only the scalar summary fields, fields:[...] keeps a dot-path subset. Defaults return the full shape.",
  "properties": {
   "capabilities": {
    "additionalProperties": false,
    "properties": {
     "compressionStream": {
      "type": "boolean"
     },
     "cpuCount": {
      "type": "integer"
     },
     "decompressionStream": {
      "type": "boolean"
     },
     "defaultWorkers": {
      "type": "integer"
     },
     "nodeZlib": {
      "type": "boolean"
     },
     "workerThreads": {
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "caps": {
    "additionalProperties": {
     "type": "integer"
    },
    "type": "object"
   },
   "ceilings": {
    "additionalProperties": false,
    "properties": {
     "env": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "maxEntries": {
      "type": "integer"
     },
     "maxUncompressedBytes": {
      "type": "integer"
     },
     "maxWorkers": {
      "type": "integer"
     }
    },
    "type": "object"
   },
   "codecs": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "id": {
       "type": "integer"
      },
      "name": {
       "type": "string"
      },
      "stream": {
       "type": "boolean"
      },
      "sync": {
       "type": "boolean"
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "defaultLimits": {
    "additionalProperties": {
     "type": "number"
    },
    "type": "object"
   },
   "diagnosticCodes": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "engine": {
    "additionalProperties": false,
    "properties": {
     "deflateTier": {
      "additionalProperties": false,
      "properties": {
       "default": {
        "type": "string"
       },
       "deterministic": {
        "type": "string"
       }
      },
      "type": "object"
     },
     "name": {
      "type": "string"
     },
     "version": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "errorCodes": {
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "network": {
    "enum": [
     "none"
    ],
    "type": "string"
   },
   "sandbox": {
    "additionalProperties": false,
    "properties": {
     "cacheEnabled": {
      "type": "boolean"
     },
     "enabled": {
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "server": {
    "additionalProperties": false,
    "properties": {
     "name": {
      "type": "string"
     },
     "protocol": {
      "type": "string"
     },
     "toolApiVersion": {
      "type": "string"
     },
     "toolCount": {
      "type": "integer"
     },
     "version": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "unexposed": {
    "items": {
     "additionalProperties": false,
     "properties": {
      "export": {
       "type": "string"
      },
      "rationale": {
       "type": "string"
      }
     },
     "type": "object"
    },
    "type": "array"
   }
  },
  "type": "object"
 },
 "title": "Describe engine & server capabilities"
}
