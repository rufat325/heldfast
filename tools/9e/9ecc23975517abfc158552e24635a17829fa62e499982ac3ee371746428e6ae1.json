{
 "description": "Get the status of a Code Engine build run (pending, running, succeeded, failed)",
 "inputSchema": {
  "properties": {
   "build_run_name": {
    "type": "string"
   },
   "project_id": {
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "build_run_name"
  ],
  "type": "object"
 },
 "name": "ce_get_build_run"
}
