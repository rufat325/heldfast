{
 "description": "List contents of an opened JAR file.",
 "inputSchema": {
  "properties": {
   "filter_pattern": {
    "description": "Optional filename filter",
    "type": "string"
   },
   "max_results": {
    "description": "Max results (default 100)",
    "type": "number"
   },
   "session_id": {
    "description": "Session ID",
    "type": "string"
   }
  },
  "required": [
   "session_id"
  ],
  "type": "object"
 },
 "name": "jar_list"
}
