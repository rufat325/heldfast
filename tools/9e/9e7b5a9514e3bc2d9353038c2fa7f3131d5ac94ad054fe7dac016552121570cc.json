{
 "description": "Create or reactivate a link between an AnchorID and a source record. Idempotent \u2014 calling twice returns the existing link.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "confidence": {
    "description": "Confidence score for this link (0-1)",
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "entity_id": {
    "description": "UUID of the AnchorID to link to",
    "type": "string"
   },
   "linked_by": {
    "description": "Who/what created this link (default: \"api\")",
    "type": "string"
   },
   "source_record_id": {
    "description": "UUID of the source record to link",
    "type": "string"
   }
  },
  "required": [
   "entity_id",
   "source_record_id"
  ],
  "type": "object"
 },
 "name": "link_source_record"
}
