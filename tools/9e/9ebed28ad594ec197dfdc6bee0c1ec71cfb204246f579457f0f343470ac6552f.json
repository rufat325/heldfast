{
 "description": "Get structured company-level insight data from Outscraper.\n\nBest for:\n- enriching known companies or domains\n- pulling structured company metadata instead of raw search results\n- larger async enrichment jobs that can be tracked later with requests_get\n\nPrefer this tool when:\n- the user already has domains or company URLs\n- the goal is firmographics, company profile data, size, revenue, industry, or founding details\n- you want structured company enrichment rather than contact extraction\n\nUse async mode when:\n- you send many companies at once\n- the endpoint is expected to do expensive enrichment work\n\nUse emails_and_contacts instead when:\n- the user mainly wants emails, phones, socials, or people/contact data\n- company profile enrichment is secondary to lead/contact discovery\n\nReturns:\n- direct response payload in sync mode\n- async request metadata in async mode",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "async": {
    "description": "Deprecated compatibility flag. Prefer execution_mode.",
    "type": "boolean"
   },
   "enrichments": {
    "description": "Optional enrichments supported by Outscraper for company insights.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "execution_mode": {
    "description": "Execution strategy. Use auto to let the MCP server choose between sync and async.",
    "enum": [
     "auto",
     "sync",
     "async"
    ],
    "type": "string"
   },
   "fields": {
    "description": "Specific company insight fields to return. Leave empty to receive the default payload.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "query": {
    "description": "One or more company domains, company names, or URLs supported by Outscraper.",
    "items": {
     "minLength": 1,
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "webhook": {
    "description": "Optional webhook URL for async completion.",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "company_insights",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "async": {
    "additionalProperties": false,
    "properties": {
     "id": {
      "type": "string"
     },
     "is_async_submission": {
      "type": "boolean"
     },
     "next_step": {
      "type": "string"
     },
     "results_location": {
      "type": "string"
     },
     "status": {
      "type": "string"
     }
    },
    "required": [
     "is_async_submission"
    ],
    "type": "object"
   },
   "data": {},
   "meta": {
    "additionalProperties": false,
    "properties": {
     "operation": {
      "type": "string"
     },
     "service": {
      "type": "string"
     }
    },
    "required": [
     "service",
     "operation"
    ],
    "type": "object"
   }
  },
  "required": [
   "data",
   "meta"
  ],
  "type": "object"
 },
 "title": "Company Insights"
}
