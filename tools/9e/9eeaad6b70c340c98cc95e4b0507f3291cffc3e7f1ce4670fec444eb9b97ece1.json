{
 "description": "Get details of a specific audit, including summary statistics and optionally page-level data. IMPORTANT: Each audit is scoped to a single site. Never compare metrics between audits from different domains.",
 "inputSchema": {
  "properties": {
   "id": {
    "type": "string"
   },
   "include_pages": {
    "description": "Include page-level data with issues; default true",
    "type": "boolean"
   },
   "page_limit": {
    "description": "Max pages when include_pages, 1-100, default 50",
    "type": "number"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_audit"
}
