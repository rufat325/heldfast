{
 "annotations": {
  "readOnlyHint": true,
  "title": "List Vibo event users"
 },
 "description": "List the hosts and guests on an event. With no usersType, returns both groups merged ({hosts, guests, hostsCount, guestsCount}) and `limit`/`skip` apply per group; with usersType, returns that one group's page. Default (compact) view returns each member's id, name and role only; pass view:'full' for email addresses and avatars.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "eventId": {
    "description": "Event id.",
    "type": "string"
   },
   "limit": {
    "description": "Max items to return (default 20). Applies per group when usersType is omitted.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "skip": {
    "description": "Number of items to skip, for paging (default 0).",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "usersType": {
    "description": "Filter to only hosts or only guests.",
    "enum": [
     "host",
     "guest"
    ],
    "type": "string"
   },
   "view": {
    "description": "Response shape: \"compact\" (default) drops fields the response already carries elsewhere; \"full\" returns every field this server understands. compact (default) returns each member as {_id, firstName, lastName, role} only \u2014 no email addresses or avatar URLs; \"full\" returns Vibo's payload untouched, emails included. This document's field list is fixed by its query, so the projection cannot drop an unknown field.",
    "enum": [
     "compact",
     "full"
    ],
    "type": "string"
   }
  },
  "required": [
   "eventId"
  ],
  "type": "object"
 },
 "name": "vibo_list_event_users"
}
