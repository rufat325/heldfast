{
 "description": "Admin-only. Resolve an open dispute. Outcome \u2208 {reporter, respondent, invalid}.",
 "inputSchema": {
  "properties": {
   "dispute_id": {
    "type": "string"
   },
   "notes": {
    "type": "string"
   },
   "outcome": {
    "enum": [
     "reporter",
     "respondent",
     "invalid"
    ],
    "type": "string"
   }
  },
  "required": [
   "dispute_id",
   "outcome"
  ],
  "type": "object"
 },
 "name": "resolve_dispute"
}
