{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Estimate CO2 emissions from a flight. Provide number of passengers and flight legs (departure/arrival airport IATA codes). Returns per-passenger and total carbon emissions. Example: estimate_flight(2, [{\"departure_airport\": \"SFO\", \"destination_airport\": \"JFK\"}]).",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-carbon-interface-api-key",
    "legs": [
     {
      "departure_airport": "SFO",
      "destination_airport": "JFK"
     }
    ],
    "passengers": 2
   },
   {
    "_apiKey": "your-carbon-interface-api-key",
    "legs": [
     {
      "cabin_class": "business",
      "departure_airport": "LHR",
      "destination_airport": "CDG"
     }
    ],
    "passengers": 1
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Carbon Interface API key",
    "type": "string"
   },
   "legs": {
    "description": "Array of flight legs with IATA airport codes",
    "items": {
     "properties": {
      "cabin_class": {
       "description": "Cabin class: \"economy\", \"premium\", or \"business\" (optional)",
       "type": "string"
      },
      "departure_airport": {
       "description": "Departure airport IATA code (e.g., \"SFO\")",
       "type": "string"
      },
      "destination_airport": {
       "description": "Arrival airport IATA code (e.g., \"JFK\")",
       "type": "string"
      }
     },
     "required": [
      "departure_airport",
      "destination_airport"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "passengers": {
    "description": "Number of passengers (e.g., 2)",
    "type": "number"
   }
  },
  "required": [
   "_apiKey",
   "passengers",
   "legs"
  ],
  "type": "object"
 },
 "name": "estimate_flight",
 "outputSchema": {
  "properties": {
   "carbon_g": {
    "description": "Carbon emissions in grams",
    "type": [
     "number",
     "null"
    ]
   },
   "carbon_kg": {
    "description": "Carbon emissions in kilograms",
    "type": [
     "number",
     "null"
    ]
   },
   "carbon_lb": {
    "description": "Carbon emissions in pounds",
    "type": [
     "number",
     "null"
    ]
   },
   "carbon_mt": {
    "description": "Carbon emissions in metric tons",
    "type": [
     "number",
     "null"
    ]
   },
   "estimated_at": {
    "description": "ISO timestamp of estimate",
    "type": [
     "string",
     "null"
    ]
   },
   "legs": {
    "description": "Flight legs with airport codes",
    "items": {
     "properties": {
      "cabin_class": {
       "description": "Cabin class of the flight",
       "type": [
        "string",
        "null"
       ]
      },
      "departure_airport": {
       "description": "Departure airport IATA code",
       "type": [
        "string",
        "null"
       ]
      },
      "destination_airport": {
       "description": "Arrival airport IATA code",
       "type": [
        "string",
        "null"
       ]
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "passengers": {
    "description": "Number of passengers",
    "type": "number"
   }
  },
  "required": [
   "passengers",
   "legs",
   "estimated_at",
   "carbon_g",
   "carbon_kg",
   "carbon_mt",
   "carbon_lb"
  ],
  "type": "object"
 },
 "title": "Estimate Flight",
 "version": "v1.43741bbd"
}
