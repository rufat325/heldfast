{
 "description": "List every open browser session (id, browser, headless, window size, start time) and which one is active. Use it to find session ids for session_select or session_destroy, or to check what is still running.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "properties": {},
  "type": "object"
 },
 "name": "session_list"
}
