{
 "description": "Get a swap quote by simulating a Move call. Returns estimated output amount and gas cost without executing.",
 "inputSchema": {
  "properties": {
   "amount": {
    "description": "Amount to swap (in token units, not MIST)",
    "type": "number"
   },
   "fromCoin": {
    "description": "Source coin type or shorthand (SUI, USDC, etc.)",
    "type": "string"
   },
   "poolId": {
    "description": "Pool object ID to use for the swap",
    "type": "string"
   },
   "toCoin": {
    "description": "Destination coin type or shorthand",
    "type": "string"
   }
  },
  "required": [
   "fromCoin",
   "toCoin",
   "amount",
   "poolId"
  ],
  "type": "object"
 },
 "name": "swap_quote"
}
