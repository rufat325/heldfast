{
 "description": "Resolve multiple companies to AnchorIDs in a single call (max 200). Each item needs a client_ref for correlation and at least one identifying field. Ambiguous matches return status needs_review with candidate AnchorIDs.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "items": {
    "description": "Array of company resolution requests",
    "items": {
     "additionalProperties": false,
     "properties": {
      "city": {
       "type": "string"
      },
      "client_ref": {
       "description": "Your reference ID for correlation",
       "type": "string"
      },
      "domain": {
       "type": "string"
      },
      "identifiers": {
       "additionalProperties": false,
       "properties": {
        "hubspot_company_id": {
         "type": "string"
        },
        "phone": {
         "type": "string"
        },
        "salesforce_account_id": {
         "type": "string"
        },
        "stripe_customer_id": {
         "type": "string"
        }
       },
       "type": "object"
      },
      "min_confidence": {
       "maximum": 1,
       "minimum": 0,
       "type": "number"
      },
      "name": {
       "type": "string"
      },
      "state": {
       "type": "string"
      }
     },
     "required": [
      "client_ref"
     ],
     "type": "object"
    },
    "maxItems": 200,
    "type": "array"
   }
  },
  "required": [
   "items"
  ],
  "type": "object"
 },
 "name": "resolve_company_batch"
}
