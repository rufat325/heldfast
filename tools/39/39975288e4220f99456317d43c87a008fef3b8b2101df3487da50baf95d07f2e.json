{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Full-text search of PANGAEA earth/environmental science datasets (oceanography, climate, geology, biology, paleo). Returns dataset title, DOI, authors, year, and topics. Use for questions like \"ocean temperature datasets\", \"Arctic sea ice cores\", \"CO2 flux measurements\".",
 "inputSchema": {
  "examples": [
   {},
   {
    "from": 0,
    "size": 20
   }
  ],
  "properties": {
   "from": {
    "description": "Offset for pagination (default 0).",
    "type": "number"
   },
   "query": {
    "description": "Free-text search, e.g. \"ocean temperature\", \"Arctic sea ice\". Omit to browse everything.",
    "type": "string"
   },
   "size": {
    "description": "Results to return (default 20, max 50).",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "search",
 "outputSchema": {
  "description": "Full-text dataset search results from PANGAEA Elasticsearch",
  "properties": {
   "count": {
    "type": "number"
   },
   "from": {
    "type": "number"
   },
   "results": {
    "items": {
     "properties": {
      "authors": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "doi": {
       "type": "string"
      },
      "id": {
       "type": "number"
      },
      "registered": {
       "type": [
        "string",
        "null"
       ]
      },
      "title": {
       "type": "string"
      },
      "topics": {
       "items": {
        "type": [
         "string",
         null
        ]
       },
       "type": "array"
      },
      "url": {
       "type": "string"
      },
      "year": {
       "type": [
        "number",
        "null"
       ]
      }
     },
     "type": "object"
    },
    "type": "array"
   },
   "total": {
    "type": "number"
   }
  },
  "type": "object"
 },
 "title": "Search",
 "version": "v1.b9bc0639"
}
