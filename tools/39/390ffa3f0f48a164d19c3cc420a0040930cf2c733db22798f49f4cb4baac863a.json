{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get exchange rates over a date range for trend analysis. Returns daily rates between start and end dates. Example: get_timeseries(start_date: \"2024-01-01\", end_date: \"2024-03-31\", base: \"USD\", symbols: \"EUR\").",
 "inputSchema": {
  "examples": [
   {
    "base": "USD",
    "end_date": "2024-03-31",
    "start_date": "2024-01-01",
    "symbols": "EUR"
   },
   {
    "base": "EUR",
    "end_date": "2023-12-31",
    "start_date": "2023-06-01",
    "symbols": "GBP,JPY,CHF"
   }
  ],
  "properties": {
   "base": {
    "description": "Base currency code (default \"EUR\")",
    "type": "string"
   },
   "end_date": {
    "description": "End date in YYYY-MM-DD format",
    "type": "string"
   },
   "start_date": {
    "description": "Start date in YYYY-MM-DD format",
    "type": "string"
   },
   "symbols": {
    "description": "Comma-separated target currency codes (e.g., \"USD,GBP\")",
    "type": "string"
   }
  },
  "required": [
   "start_date",
   "end_date"
  ],
  "type": "object"
 },
 "name": "get_timeseries",
 "outputSchema": {
  "properties": {
   "base": {
    "description": "Base currency code",
    "type": "string"
   },
   "end_date": {
    "description": "End date of the time series",
    "type": "string"
   },
   "rates": {
    "additionalProperties": {
     "additionalProperties": {
      "type": "number"
     },
     "type": "object"
    },
    "description": "Daily exchange rates keyed by date then currency",
    "type": "object"
   },
   "start_date": {
    "description": "Start date of the time series",
    "type": "string"
   }
  },
  "required": [
   "base",
   "start_date",
   "end_date",
   "rates"
  ],
  "type": "object"
 },
 "title": "Get Timeseries",
 "version": "v1.3a01b6d2"
}
