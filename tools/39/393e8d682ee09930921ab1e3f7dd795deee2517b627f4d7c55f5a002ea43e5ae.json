{
 "description": "Rank Polymarket CLOB V2 builders by routed volume, order count or fees. A 'builder' is the frontend/bot/integrator whose code is attached to a fill \u2014 this answers 'who is actually routing the flow', which no V1 subgraph can, because the V1 contracts never emitted it. V2-era only (from block 84902353, migration 2026-04-28).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "How many builders to return (default 10)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "orderBy": {
    "description": "Ranking metric (default scaledVolume)",
    "enum": [
     "scaledVolume",
     "orderCount",
     "scaledFees"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_builder_leaderboard"
}
