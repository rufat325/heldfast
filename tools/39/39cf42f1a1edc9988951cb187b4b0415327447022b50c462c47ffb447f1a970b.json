{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search the National Landslide Database for recorded UK landslide events near a point. Returns landslide name, locality details, and known-date range where recorded. Data: British Geological Survey (ogcapi.bgs.ac.uk), Open Government Licence, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lat": {
    "description": "Decimal degree latitude (WGS84) \u2014 must be supplied together with lng. Omit both for full Great Britain coverage.",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "limit": {
    "description": "Maximum landslide records to return (default 20, max 100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "lng": {
    "description": "Decimal degree longitude (WGS84) \u2014 must be supplied together with lat.",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "radius_km": {
    "description": "Search radius in kilometres around lat/lng (default 20, max 200)",
    "maximum": 200,
    "minimum": 0.5,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "bgs-ogcapi.hazards.landslide_search",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "BGS National Landslide Database Search"
}
