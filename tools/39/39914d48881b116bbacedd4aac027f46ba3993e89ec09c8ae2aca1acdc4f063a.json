{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Delete every entry carrying the given tag. Returns deleted_count. Requires explicit_user_intent: true.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "explicit_user_intent": {
    "const": true,
    "description": "Must be true. Set ONLY when the current user message explicitly asks the agent to modify memory (set/forget/export). Do not infer intent.",
    "type": "boolean"
   },
   "tag": {
    "description": "Tag to bulk-delete.",
    "maxLength": 64,
    "minLength": 1,
    "pattern": "^[^,]+$",
    "type": "string"
   }
  },
  "required": [
   "tag",
   "explicit_user_intent"
  ],
  "type": "object"
 },
 "name": "memory_forget_by_tag"
}
