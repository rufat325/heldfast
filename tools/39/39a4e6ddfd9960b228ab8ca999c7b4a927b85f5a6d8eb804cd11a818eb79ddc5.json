{
 "description": "Set the color of one Ableton Push 2/3 pad on the 0..63 grid. Use for visual feedback or performance layouts after hardware detection. Requires Push 2/3; returns sent=true with the pad and color accepted by the bridge.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "color": {
    "maximum": 127,
    "minimum": 0,
    "type": "integer"
   },
   "pad": {
    "description": "Push pad grid 0..63 (8x8).",
    "maximum": 63,
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "pad",
   "color"
  ],
  "type": "object"
 },
 "name": "push_set_pad_color"
}
