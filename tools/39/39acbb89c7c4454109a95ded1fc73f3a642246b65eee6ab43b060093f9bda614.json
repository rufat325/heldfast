{
 "description": "Read a local file, encrypt it client-side with AES-256-GCM, and upload it to the vault. Requires BITATLAS_MASTER_KEY. Files over 100 MB are not supported via MCP.",
 "inputSchema": {
  "properties": {
   "category": {
    "description": "File category",
    "enum": [
     "identity",
     "financial",
     "legal",
     "medical",
     "digital"
    ],
    "type": "string"
   },
   "file_path": {
    "description": "Absolute or relative path to the local file",
    "type": "string"
   },
   "folder_id": {
    "description": "Destination folder ID (omit for root)",
    "type": "string"
   },
   "name": {
    "description": "Override display name (defaults to the filename)",
    "type": "string"
   }
  },
  "required": [
   "file_path"
  ],
  "type": "object"
 },
 "name": "bitatlas_upload_file"
}
