{
 "description": "Edit existing Evidence with optimistic revision checking.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "evidenceId": {
    "minLength": 1,
    "type": "string"
   },
   "expectedRevision": {
    "minimum": 1,
    "type": "integer"
   },
   "statement": {
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "evidenceId",
   "statement",
   "expectedRevision"
  ],
  "type": "object"
 },
 "name": "granoflow_evidence_update"
}
