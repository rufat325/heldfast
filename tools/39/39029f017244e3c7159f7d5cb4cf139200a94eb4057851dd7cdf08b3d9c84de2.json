{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Change the reverse DNS entry for a floating IP. Set dns_ptr to null to reset.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "dns_ptr": {
    "description": "Reverse DNS PTR record value, or null to reset",
    "type": [
     "string",
     "null"
    ]
   },
   "id": {
    "description": "Floating IP ID",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "ip": {
    "description": "IP address to set the reverse DNS entry for",
    "type": "string"
   }
  },
  "required": [
   "id",
   "ip",
   "dns_ptr"
  ],
  "type": "object"
 },
 "name": "hetzner_change_floating_ip_rdns",
 "title": "Change Floating IP Reverse DNS"
}
