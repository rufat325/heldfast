{
 "description": "Sell bonding curve tokens for any token (auto-swaps via Uniswap)",
 "inputSchema": {
  "properties": {
   "amount": {
    "description": "Amount of tokens to sell",
    "type": "string"
   },
   "outputToken": {
    "description": "Token to receive (ETH, USDC, etc.)",
    "type": "string"
   },
   "token": {
    "description": "Token to sell",
    "type": "string"
   }
  },
  "required": [
   "token",
   "outputToken",
   "amount"
  ],
  "type": "object"
 },
 "name": "zap_sell"
}
