{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search an Entrez database (e.g. pubmed, gene, nucleotide) by query term and return a list of matching UIDs (up to 10,000) with hit count. Supports pagination via retstart and sort (e.g. pub_date).",
 "inputSchema": {
  "examples": [
   {
    "db": "pubmed",
    "term": "BRCA1 mutations"
   },
   {
    "db": "nucleotide",
    "retmax": 100,
    "sort": "pub_date",
    "term": "human insulin"
   }
  ],
  "properties": {
   "db": {
    "type": "string"
   },
   "retmax": {
    "description": "1-10000 (default 20)",
    "type": "number"
   },
   "retstart": {
    "type": "number"
   },
   "sort": {
    "description": "e.g. \"pub_date\" (PubMed), \"relevance\"",
    "type": "string"
   },
   "term": {
    "type": "string"
   }
  },
  "required": [
   "db",
   "term"
  ],
  "type": "object"
 },
 "name": "esearch",
 "outputSchema": {
  "description": "NCBI esearch response - search results with UIDs",
  "type": "object"
 },
 "title": "Esearch",
 "version": "v1.1859d5ac"
}
