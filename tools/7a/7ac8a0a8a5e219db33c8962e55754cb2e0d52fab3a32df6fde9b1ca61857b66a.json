{
 "description": "List transcriptions from Memo AI with titles, summaries, speakers, and topics. Use this to find relevant meetings before fetching full text.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "date_from": {
    "description": "Filter from date (YYYY-MM-DD)",
    "type": "string"
   },
   "date_to": {
    "description": "Filter to date (YYYY-MM-DD)",
    "type": "string"
   },
   "language": {
    "description": "Filter by language code (ru, en, etc.)",
    "type": "string"
   },
   "limit": {
    "description": "Max results per page (default 20, max 100)",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset",
    "type": "number"
   },
   "project_id": {
    "description": "Filter by project UUID",
    "type": "string"
   },
   "sort": {
    "description": "Sort: created_at, -created_at, duration, -duration",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "memo_list_transcriptions"
}
