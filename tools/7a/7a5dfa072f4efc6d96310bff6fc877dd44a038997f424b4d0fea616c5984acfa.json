{
 "description": "Generate test JWTs with custom claims and expiry for local development. Returns a signed HS256 token. Useful for testing auth flows without a real identity provider.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "expiresInSeconds": {
    "description": "Token expiry in seconds (default: 3600, max: 7 days)",
    "maximum": 604800,
    "minimum": 10,
    "type": "number"
   },
   "payload": {
    "additionalProperties": {},
    "description": "JWT payload claims (e.g. { sub: '123', role: 'admin' })",
    "type": "object"
   },
   "secret": {
    "description": "HMAC secret for HS256 signing",
    "minLength": 3,
    "type": "string"
   }
  },
  "required": [
   "payload",
   "secret"
  ],
  "type": "object"
 },
 "name": "mock_jwt_generator"
}
