{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Remove Member Setup"
 },
 "description": "End a member's producer setup in one tap (prospective \u2014 sold tickets and attached events keep their terms; its event exceptions end with it). Requires deal_id.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "deal_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "reason": {
    "maxLength": 1000,
    "type": "string"
   }
  },
  "required": [
   "deal_id"
  ],
  "type": "object"
 },
 "name": "remove_member_setup",
 "title": "Remove Member Setup"
}
