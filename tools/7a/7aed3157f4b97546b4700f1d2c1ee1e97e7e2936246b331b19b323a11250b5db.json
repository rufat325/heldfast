{
 "description": "Ask FLOCORE's MEMORY: 'what do I know about X?'. Hybrid-lite retrieval over the semantic world-model; every result is PROVENANCE-STAMPED (who / source / trust). Read-only, silo-scoped.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "limit": {
    "type": "integer"
   },
   "query": {
    "type": "string"
   },
   "scope": {
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "flocore_recall"
}
