{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Retrieve a historical time series of Norges Bank key policy interest rates. Choose rate type (SD = key policy rate, OL = overnight lending rate, RR = reserve rate), frequency (B = business daily, M = monthly, A = annual), and date range. Returns {date, rate} pairs sorted ascending. Useful for monetary-policy research, macro models, or comparing Norway's rate cycle against other central banks. Data: Norges Bank SDMX IR dataflow, no auth.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "end_date": {
    "description": "End date of the historical range in ISO 8601 format (YYYY-MM-DD or YYYY-MM). Defaults to today if omitted.",
    "type": "string"
   },
   "frequency": {
    "description": "Observation frequency: \"B\" = business daily (default), \"M\" = monthly average, \"A\" = annual average.",
    "enum": [
     "B",
     "M",
     "A"
    ],
    "type": "string"
   },
   "rate_type": {
    "description": "Type of key policy rate to retrieve: \"SD\" = key policy rate (deposit rate), \"OL\" = overnight lending rate, \"RR\" = reserve rate. Omit to get all three types.",
    "enum": [
     "SD",
     "OL",
     "RR"
    ],
    "type": "string"
   },
   "start_date": {
    "description": "Start date of the historical range in ISO 8601 format (YYYY-MM-DD or YYYY-MM). Defaults to 1 year ago if omitted.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "norgesbank.rates.history",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Norges Bank Interest Rate History"
}
