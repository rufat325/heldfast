{
 "description": "List content items, filtered by status or site. For only Content Guard guarded content, use list_guarded_content. status 'archived' lists trashed items.",
 "inputSchema": {
  "properties": {
   "is_monitored": {
    "description": "true = only Content Guard monitored articles, false = exclude, omit = all.",
    "type": "boolean"
   },
   "page": {
    "description": "default: 1",
    "type": "number"
   },
   "page_size": {
    "description": "max 100 (default: 20)",
    "type": "number"
   },
   "site_id": {
    "description": "Filter by site ID (from list_sites).",
    "type": "string"
   },
   "status": {
    "description": "Filter by status. archived = trashed/deleted items.",
    "enum": [
     "drafting",
     "review",
     "published",
     "archived"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "list_content"
}
