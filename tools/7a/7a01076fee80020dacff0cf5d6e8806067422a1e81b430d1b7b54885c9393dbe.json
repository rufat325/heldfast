{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false,
  "title": "Add a label to a thread"
 },
 "description": "Add a shared label to an email thread (or task). Use list_labels first to find available label IDs for the board.",
 "inputSchema": {
  "properties": {
   "boardId": {
    "description": "The board ID the thread belongs to",
    "type": "number"
   },
   "labelId": {
    "description": "The label ID to assign",
    "type": "number"
   },
   "threadId": {
    "description": "The thread or task ID to label",
    "type": "string"
   }
  },
  "required": [
   "threadId",
   "labelId",
   "boardId"
  ],
  "type": "object"
 },
 "name": "add_label_to_thread",
 "title": "Add a label to a thread"
}
