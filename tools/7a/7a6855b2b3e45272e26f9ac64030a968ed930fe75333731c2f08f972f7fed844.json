{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Federal Contracts by Company"
 },
 "description": "Get federal government contract awards (from USAspending.gov) won by a specific public company. Shows the award (action) date, recipient named by the government, awarding agency, total value (obligated dollars plus unexercised ceiling \u2014 not revenue received), outlays when reported, period-of-performance end date, and description. Coverage: only prime contract awards of $1M or more that resolve to a listed company are included, so sums understate total federal revenue. Useful for gauging a company's reliance on federal spending; use GetTopGovernmentContractors to rank companies market-wide.",
 "inputSchema": {
  "properties": {
   "agency": {
    "default": null,
    "description": "Optional case-insensitive substring filter on the awarding agency (e.g., 'Defense')",
    "type": "string"
   },
   "endDate": {
    "default": null,
    "description": "End date in YYYY-MM-DD format (defaults to today)",
    "type": "string"
   },
   "maxResults": {
    "default": 50,
    "description": "Maximum number of awards to return (default: 50)",
    "type": "integer"
   },
   "offset": {
    "default": 0,
    "description": "Number of matching awards to skip before returning rows (default: 0)",
    "type": "integer"
   },
   "sortBy": {
    "default": "amount",
    "description": "Sort order: 'amount' (largest total value first, default) or 'date' (most recent award first)",
    "type": "string"
   },
   "startDate": {
    "default": null,
    "description": "Start date in YYYY-MM-DD format, filtering on the award action date (defaults to 1 year ago)",
    "type": "string"
   },
   "ticker": {
    "description": "Stock ticker symbol (e.g., LMT, RTX, BA)",
    "type": "string"
   }
  },
  "required": [
   "ticker"
  ],
  "type": "object"
 },
 "name": "GetGovernmentContracts",
 "title": "Federal Contracts by Company"
}
