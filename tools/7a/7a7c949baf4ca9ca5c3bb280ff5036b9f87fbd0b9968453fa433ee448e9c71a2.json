{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get details of a Cloudflare Tunnel by ID.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "account_id": {
    "description": "Cloudflare account ID. Falls back to CLOUDFLARE_ACCOUNT_ID env var when omitted.",
    "type": "string"
   },
   "tunnel_id": {
    "description": "Tunnel ID (from list_tunnels).",
    "type": "string"
   }
  },
  "required": [
   "tunnel_id"
  ],
  "type": "object"
 },
 "name": "get_tunnel"
}
