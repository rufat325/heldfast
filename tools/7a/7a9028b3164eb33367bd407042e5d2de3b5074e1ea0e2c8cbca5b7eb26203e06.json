{
 "description": "Convert a public PDF to plain text \u2014 full extracted text in one call, up to 25MB / 500k chars, with page count and a likely_scanned flag (text-based PDFs only, no OCR). Costs $0.01 per call, paid from clink's shop credits (get a key with buy_credits or at /buy/credits). A failed or empty call is free.",
 "inputSchema": {
  "properties": {
   "url": {
    "description": "public http(s) URL of a PDF file",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "pdf_text"
}
