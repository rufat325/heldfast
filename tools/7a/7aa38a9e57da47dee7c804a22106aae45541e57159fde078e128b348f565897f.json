{
 "_meta": {},
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "List beta groups"
 },
 "description": "Get a list of all beta groups (internal and external)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "filter.app": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by app ID(s)"
   },
   "filter.id": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by beta group ID(s)"
   },
   "filter.name": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    ],
    "description": "Filter by group name(s)"
   },
   "limit": {
    "default": 100,
    "description": "Maximum number of groups to return (default 100, max 200)",
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "list-beta-groups",
 "title": "List beta groups"
}
