{
 "description": "Generate images visually similar to a source image previously created with recraft_explore. similarity controls how closely results resemble the source (1=slightly, 5=extremely similar).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "response_format": {
    "default": "url",
    "description": "Response format",
    "enum": [
     "url",
     "b64_json"
    ],
    "type": "string"
   },
   "similarity": {
    "description": "How closely to match the source image (1-5)",
    "maximum": 5,
    "minimum": 1,
    "type": "integer"
   },
   "source_image_id": {
    "description": "Image ID from a previous recraft_explore result",
    "type": "string"
   }
  },
  "required": [
   "source_image_id",
   "similarity"
  ],
  "type": "object"
 },
 "name": "recraft_explore_similar"
}
