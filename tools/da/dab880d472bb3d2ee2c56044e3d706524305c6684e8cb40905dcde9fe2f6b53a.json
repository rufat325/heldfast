{
 "description": "Apply an RFC-6902 JSON patch to the model (paths address the parsed YAML, e.g. /data/entities/0/attributes/-). Returns the updated YAML plus its validation result. Note: YAML comments are not preserved; for comment-heavy models, rewrite the YAML directly instead.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "modelYaml": {
    "description": "Current app.acm.yaml content",
    "type": "string"
   },
   "patch": {
    "description": "RFC-6902 operations: [{op, path, value?, from?}, ...]",
    "items": {
     "additionalProperties": {},
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "modelYaml",
   "patch"
  ],
  "type": "object"
 },
 "name": "edit_model"
}
