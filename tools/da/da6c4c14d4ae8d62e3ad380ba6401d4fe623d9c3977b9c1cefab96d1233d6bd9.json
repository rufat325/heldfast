{
 "description": "Type text into the focused field as Unicode, so it works on any keyboard layout. The text is never written to the audit log in clear text.",
 "inputSchema": {
  "properties": {
   "cps": {
    "description": "Characters per second, default 240.",
    "type": "number"
   },
   "text": {
    "type": "string"
   }
  },
  "required": [
   "text"
  ],
  "type": "object"
 },
 "name": "computer_type"
}
