{
 "description": "Crawl a page and check all links for broken URLs (404s, timeouts, redirects)",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "max_links": {
    "default": 50,
    "description": "Maximum number of links to check (default 50)",
    "type": "number"
   },
   "url": {
    "description": "The page URL to check links on",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "find_broken_links"
}
