{
 "description": "Ship Confidence Gate (Rule 42). Verifies dual-tier authorization for publish-class actions: HMAC-signed Verdict (paid) OR approval token (free). Returns allow/block with self-stamped evidence.",
 "inputSchema": {
  "properties": {
   "artifact_id": {
    "description": "The artifact being shipped (e.g. '@sunaiva/gate@1.1.0')",
    "type": "string"
   },
   "command_preview": {
    "description": "First 300 chars of the command that triggered the check (optional)",
    "type": "string"
   },
   "label": {
    "description": "Human-readable label of the publish action (optional)",
    "type": "string"
   }
  },
  "required": [
   "artifact_id"
  ],
  "type": "object"
 },
 "name": "ship_confidence_check"
}
