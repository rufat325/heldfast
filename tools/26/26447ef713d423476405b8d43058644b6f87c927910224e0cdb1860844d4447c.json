{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Historical DNS records for <hostname>. Returns past record values with first/last-seen dates for a given record type (a, aaaa, mx, ns, soa, txt). Example: securitytrails_dns_history({ hostname: \"github.com\", type: \"a\", _apiKey: \"your-key\" })",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-securitytrails-api-key",
    "hostname": "github.com",
    "type": "a"
   },
   {
    "_apiKey": "your-securitytrails-api-key",
    "hostname": "example.org",
    "type": "mx"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "SecurityTrails API key",
    "type": "string"
   },
   "hostname": {
    "description": "Apex domain / hostname, e.g. \"github.com\"",
    "type": "string"
   },
   "type": {
    "description": "DNS record type: a, aaaa, mx, ns, soa, or txt (default \"a\")",
    "enum": [
     "a",
     "aaaa",
     "mx",
     "ns",
     "soa",
     "txt"
    ],
    "type": "string"
   }
  },
  "required": [
   "hostname",
   "_apiKey"
  ],
  "type": "object"
 },
 "name": "securitytrails_dns_history",
 "title": "Securitytrails Dns History",
 "version": "v1.0e82abdd"
}
