{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search FDA 510(k) premarket clearance submissions \u2014 175,000+ cleared medical devices. Returns k_number, device name, applicant, decision (SESE=cleared), decision date, product code, advisory committee specialty, and clearance type (Traditional/Special/Abbreviated). Filter by device name, k-number, applicant, or product code. A 510(k) clearance means FDA determined the device is substantially equivalent to a legally marketed predicate device. Useful for market research, competitive analysis, and regulatory pathway planning. Source: FDA CDRH 510(k) database, US public domain.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Number of records to return (1\u201399, default 10).",
    "maximum": 99,
    "minimum": 1,
    "type": "integer"
   },
   "search": {
    "description": "OpenFDA search expression using Lucene syntax. Single field: device_name:\"pacemaker\" or recall_status:\"Ongoing\". Combined: device_name:\"insulin pump\"+AND+recall_status:\"Ongoing\". Omit to return recent records sorted by date.",
    "type": "string"
   },
   "skip": {
    "description": "Number of records to skip for pagination (default 0).",
    "minimum": 0,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "openfda_devices.clearance.premarket_510k",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "FDA 510(k) Premarket Clearances"
}
