{
 "description": "Get upcoming tech industry events (conferences, summits, expos) with dates, locations, and URLs.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 10,
    "description": "Number of events to return (default 10, max 50)",
    "maximum": 50,
    "minimum": 1,
    "type": "number"
   }
  },
  "type": "object"
 },
 "name": "get_tech_events",
 "title": "Tech events"
}
