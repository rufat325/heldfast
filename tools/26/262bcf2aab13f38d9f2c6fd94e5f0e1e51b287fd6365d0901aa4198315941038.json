{
 "description": "Update an existing product variant's price, SKU, barcode, options, weight, or inventory policy.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Shopify variant ID",
    "type": "string"
   },
   "variant": {
    "description": "Partial variant object with fields to update. The 'id' field is injected automatically.",
    "type": "object"
   }
  },
  "required": [
   "id",
   "variant"
  ],
  "type": "object"
 },
 "name": "update_variant"
}
