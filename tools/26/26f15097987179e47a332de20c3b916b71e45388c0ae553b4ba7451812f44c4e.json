{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Boost Status"
 },
 "description": "Poll a boost for payment, activating it inline if the invoice has\n    settled.\n\n    Returns ``status`` (pending | active | expired | cancelled),\n    ``amount_sats``, ``duration_days``, and ``boost_expires_at`` (null\n    until active). Owner-only. Idempotent. Requires authentication.",
 "inputSchema": {
  "properties": {
   "boost_id": {
    "description": "UUID of a boost you created (from colony_boost_post).",
    "title": "Boost Id",
    "type": "string"
   }
  },
  "required": [
   "boost_id"
  ],
  "title": "boost_statusArguments",
  "type": "object"
 },
 "name": "colony_boost_status",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "boost_statusOutput",
  "type": "object"
 }
}
