{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Given an ambiguous draft prompt, return 1\u20133 targeted clarifying questions instead of guessing. Each question carries a `suggested_answer` you can accept verbatim to keep moving, an optional 2\u20134 quick-pick `options` list, and a `dimension` tag (audience/scope/format/length/tone/constraints/goal/platform). When the analyzer is highly confident AND the prompt is non-trivially long, the tool short-circuits with `clarificationNeeded: false` so callers can pipeline this in front of optimize_prompt without paying a latency tax on every call. Pass `force: true` to always generate questions. Pass `elicit: true` to collect answers interactively through the host's native form UI (MCP elicitation) instead of returning the raw questions \u2014 when the client supports it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "category": {
    "description": "Category hint. Will skip questions about category/platform if you pass it.",
    "enum": [
     "chat",
     "image",
     "voice",
     "video",
     "music",
     "code",
     "document"
    ],
    "type": "string"
   },
   "cwd": {
    "description": "Working directory to pull workspace rules (CLAUDE.md / AGENTS.md / .cursorrules) from. Defaults to server cwd.",
    "type": "string"
   },
   "elicit": {
    "default": false,
    "description": "When true AND the connected client supports MCP elicitation, render the questions as a native form, collect the user's answers inline, and return them as `answers` (each question's suggested answer is the field default). Falls back to returning the raw questions when the client can't elicit. Default false (back-compat).",
    "type": "boolean"
   },
   "file_excerpt": {
    "description": "Short excerpt of the active file to ground the questions.",
    "type": "string"
   },
   "file_language": {
    "description": "Explicit language override for the active file.",
    "type": "string"
   },
   "file_path": {
    "description": "Active file path \u2014 informs the clarifier's defaults.",
    "type": "string"
   },
   "force": {
    "default": false,
    "description": "Always generate questions even when the analyzer is highly confident. Useful for UIs that want to surface clarification on every call.",
    "type": "boolean"
   },
   "max_questions": {
    "default": 3,
    "description": "Cap on returned questions. Default 3, hard max 5.",
    "exclusiveMinimum": 0,
    "maximum": 5,
    "type": "integer"
   },
   "prompt": {
    "description": "The draft prompt the user is unsure about.",
    "type": "string"
   },
   "user_locale": {
    "type": "string"
   }
  },
  "required": [
   "prompt"
  ],
  "type": "object"
 },
 "name": "clarify_with_user",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true,
  "properties": {
   "analysis": {
    "additionalProperties": true,
    "properties": {
     "category": {
      "type": "string"
     },
     "confidence": {
      "type": "string"
     },
     "intent": {
      "type": "string"
     },
     "recommendedMode": {
      "type": "string"
     },
     "source": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "clarificationNeeded": {
    "type": "boolean"
   },
   "latencyMs": {
    "type": "number"
   },
   "questions": {
    "items": {
     "additionalProperties": true,
     "properties": {},
     "type": "object"
    },
    "type": "array"
   },
   "reason": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "title": "Ask clarifying questions"
}
