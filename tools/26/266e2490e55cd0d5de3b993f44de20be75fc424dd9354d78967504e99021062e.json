{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "FINRA Reg SHO daily short-sale volume for one symbol on one trading day, summed across every consolidated-NMS reporting facility (Nasdaq TRF Carteret, NYSE TRF, Nasdaq TRF Chicago) so the total matches FINRA's consolidated CNMS figure. Returns short_volume, short_exempt_volume, total_volume, short_ratio (short / total), and the per-facility breakdown. Publishing lag is T+1 or better; omit `date` for the most recent published day. Zero-rated: FINRA's terms forbid charging for this data, so the call costs 0 credits.",
 "inputSchema": {
  "examples": [
   {
    "symbol": "AAPL"
   },
   {
    "date": "2026-08-21",
    "symbol": "GME"
   },
   {
    "date": "2026-08-21",
    "symbol": "BCpC"
   }
  ],
  "properties": {
   "date": {
    "description": "YYYY-MM-DD or YYYYMMDD. Defaults to the most recent published trading day.",
    "type": "string"
   },
   "symbol": {
    "description": "Ticker (case-insensitive), e.g. \"AAPL\"",
    "type": "string"
   }
  },
  "required": [
   "symbol"
  ],
  "type": "object"
 },
 "name": "short_volume_daily",
 "outputSchema": {
  "oneOf": [
   {
    "properties": {
     "date": {
      "description": "Requested date in YYYY-MM-DD format",
      "type": "string"
     },
     "found": {
      "description": "Symbol not found in file",
      "enum": [
       false
      ],
      "type": "boolean"
     },
     "hint": {
      "description": "Guidance for the user",
      "type": "string"
     },
     "reason": {
      "description": "Reason symbol was not found",
      "enum": [
       "symbol_not_in_file"
      ],
      "type": "string"
     },
     "symbol": {
      "description": "Requested ticker symbol",
      "type": "string"
     }
    },
    "required": [
     "found",
     "symbol",
     "date",
     "reason",
     "hint"
    ],
    "type": "object"
   },
   {
    "properties": {
     "by_trf": {
      "description": "Breakdown by Trade Reporting Facility",
      "items": {
       "properties": {
        "short_exempt_volume": {
         "description": "Short exempt volume for this TRF",
         "type": "number"
        },
        "short_volume": {
         "description": "Short volume for this TRF",
         "type": "number"
        },
        "total_volume": {
         "description": "Total volume for this TRF",
         "type": "number"
        },
        "trf_code": {
         "description": "TRF code (FNSQ or FNYX)",
         "type": "string"
        },
        "trf_label": {
         "description": "TRF label",
         "type": "string"
        }
       },
       "required": [
        "trf_code",
        "trf_label",
        "short_volume",
        "short_exempt_volume",
        "total_volume"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "combined": {
      "properties": {
       "short_exempt_volume": {
        "description": "Total short exempt volume",
        "type": "number"
       },
       "short_ratio": {
        "description": "Short volume / total volume ratio",
        "type": "number"
       },
       "short_volume": {
        "description": "Total short volume across both TRFs",
        "type": "number"
       },
       "total_volume": {
        "description": "Total volume across both TRFs",
        "type": "number"
       }
      },
      "required": [
       "short_volume",
       "short_exempt_volume",
       "total_volume",
       "short_ratio"
      ],
      "type": "object"
     },
     "date": {
      "description": "Date in YYYY-MM-DD format",
      "type": "string"
     },
     "symbol": {
      "description": "Ticker symbol",
      "type": "string"
     }
    },
    "required": [
     "symbol",
     "date",
     "combined",
     "by_trf"
    ],
    "type": "object"
   }
  ]
 },
 "title": "Short Volume Daily",
 "version": "v1.14345570"
}
