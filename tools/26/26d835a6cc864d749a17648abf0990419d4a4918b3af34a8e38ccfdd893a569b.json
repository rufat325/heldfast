{
 "description": "Start a review or coverage iteration loop on a feature. Returns loop state.",
 "inputSchema": {
  "properties": {
   "id": {
    "description": "Item ID or semanticId",
    "type": "string"
   },
   "loopType": {
    "description": "Type of iteration loop",
    "enum": [
     "review",
     "coverage"
    ],
    "type": "string"
   },
   "maxIterations": {
    "description": "Override max iterations (optional, defaults from settings)",
    "type": "number"
   }
  },
  "required": [
   "id",
   "loopType"
  ],
  "type": "object"
 },
 "name": "start_iteration_loop"
}
