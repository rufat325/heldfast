{
 "description": "Get status summary for one upload. Returns overall status plus per-test name, status, durationSeconds, failReason. Provide either uploadId or name (most recent matching name wins).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "name": {
    "type": "string"
   },
   "uploadId": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_upload_status"
}
