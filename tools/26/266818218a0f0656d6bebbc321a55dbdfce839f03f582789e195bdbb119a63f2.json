{
 "description": "Remove one of this tenant's registered OAuth issuers; bearer JWTs from it stop authenticating immediately.",
 "inputSchema": {
  "properties": {
   "issuer": {
    "description": "The issuer to remove.",
    "type": "string"
   },
   "tenant_key": {
    "description": "The key `signup` returned. Required only when this connection carries no Authorization: Bearer header -- when both are present, the header wins.",
    "type": "string"
   }
  },
  "required": [
   "issuer"
  ],
  "type": "object"
 },
 "name": "host.oauth.issuer_remove"
}
