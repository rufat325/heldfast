{
 "description": "Read Blueprint class definition, components, and default properties.",
 "inputSchema": {
  "properties": {
   "asset_path": {
    "description": "Blueprint asset path",
    "type": "string"
   },
   "max_depth": {
    "description": "Max depth (default 3)",
    "type": "number"
   }
  },
  "required": [
   "asset_path"
  ],
  "type": "object"
 },
 "name": "read_blueprint"
}
