{
 "annotations": {
  "openWorldHint": true,
  "title": "Rewrite pages (bulk)"
 },
 "description": "Rewrite multiple pages in one async job. Basic mode (1 credit/page) covers SCRAPED pages only. Wisewand mode (wisewand=true, 5 credits/page or 1 with own key, 2-4 hours) covers scraped pages plus Haloscan-origin and scrape-failed/empty pages via synthetic-content fallback (slug + keywords). Max 50 pages per batch. Returns a job_id and a breakdown {wayback_count, haloscan_count, scrape_failed_count}.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "article_params": {
    "additionalProperties": {},
    "description": "Default Wisewand parameters applied to every page (type, lang, country, etc.). For Haloscan-origin pages, per-page AI prep fills in missing fields.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "page_ids": {
    "description": "Array of page UUIDs to rewrite. Basic mode requires scraped pages; Wisewand mode also accepts Haloscan-origin pages.",
    "items": {
     "format": "uuid",
     "pattern": "^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$",
     "type": "string"
    },
    "maxItems": 50,
    "minItems": 1,
    "type": "array"
   },
   "wisewand": {
    "description": "Use Wisewand for premium SEO-optimized rewrite (5 credits/page, 1 with own key). Required to include Haloscan-origin pages.",
    "type": "boolean"
   },
   "wisewand_api_key": {
    "description": "Caller's own Wisewand API key (implies wisewand=true, reduces cost to 1 credit/page)",
    "type": "string"
   }
  },
  "required": [
   "page_ids"
  ],
  "type": "object"
 },
 "name": "rewrite_bulk"
}
