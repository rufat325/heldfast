{
 "annotations": {
  "readOnlyHint": true,
  "title": "List Vibo song-idea songs"
 },
 "description": "List the suggested songs inside a song-idea collection (returns songUrl/viboSongId/title/artist to pass to vibo_add_song_to_section).",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "eventId": {
    "description": "Event id.",
    "type": "string"
   },
   "limit": {
    "description": "Max items to return (default 20).",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "sectionId": {
    "description": "Section id (from vibo_list_sections).",
    "type": "string"
   },
   "skip": {
    "description": "Number of items to skip, for paging (default 0).",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "songIdeasId": {
    "description": "The _id from vibo_list_section_song_ideas.",
    "type": "string"
   },
   "view": {
    "description": "Response shape: \"compact\" (default) drops fields the response already carries elsewhere; \"full\" returns every field this server understands. compact strips image/avatar URLs from the response; \"full\" returns Vibo's payload untouched. No field projection: this server has no verified record of which Vibo fields matter, and inventing one would risk dropping a field a caller needs.",
    "enum": [
     "compact",
     "full"
    ],
    "type": "string"
   }
  },
  "required": [
   "eventId",
   "sectionId",
   "songIdeasId"
  ],
  "type": "object"
 },
 "name": "vibo_list_song_ideas_songs"
}
