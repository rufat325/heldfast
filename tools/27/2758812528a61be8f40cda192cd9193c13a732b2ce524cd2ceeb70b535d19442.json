{
 "_meta": {
  "ui": {
   "resourceUri": "ui://impreza/server-card"
  }
 },
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Get a Proxmox VPS's live power state + resource usage (CPU, memory, disk, network, uptime). Find the `service_id` via `impreza_list_services`. Read-only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "service_id": {
    "description": "VPS service id (numeric; from impreza_list_services).",
    "type": "string"
   }
  },
  "required": [
   "service_id"
  ],
  "type": "object"
 },
 "name": "impreza_vps_status"
}
