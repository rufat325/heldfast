{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get results for a specific election \u2014 candidates, parties, and vote counts",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "election_wikidata_id": {
    "description": "Wikidata ID of the election",
    "type": "string"
   },
   "limit": {
    "default": 100,
    "description": "Max results",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "election_wikidata_id"
  ],
  "type": "object"
 },
 "name": "get_election_results"
}
