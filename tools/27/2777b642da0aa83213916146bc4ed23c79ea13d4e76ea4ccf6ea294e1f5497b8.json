{
 "description": "Gets the latest reschedule status for a job (e.g. waiting for provider, successfully changed).",
 "execution": {
  "taskSupport": "optional"
 },
 "inputSchema": {
  "properties": {
   "jobId": {
    "description": "The Job ID to get reschedule details for",
    "type": "integer"
   }
  },
  "required": [
   "jobId"
  ],
  "type": "object"
 },
 "name": "get_job_reschedule_request"
}
