{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-device-configuration"
 },
 "description": "The device configurations.\n\nTIP: Gets a specific device configuration profile. Returns full details including platform-specific settings (Wi-Fi SSID, VPN config, device restrictions, etc.).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "deviceConfigurationId": {
    "description": "Path parameter: deviceConfigurationId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "required": [
   "deviceConfigurationId"
  ],
  "type": "object"
 },
 "name": "get-device-configuration"
}
