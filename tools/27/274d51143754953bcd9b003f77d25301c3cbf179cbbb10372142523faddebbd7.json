{
 "description": "Ask a plain-English question about a tenant's FLOCORE operations (tickets, queue, state). Answered by FLOCORE's Smart Switch Sentinel (routes across our hosted-LLM suite, cost-capped, on-box backup; every answer distilled into our own models), grounded on the tenant's real data and validated: an answer not supported by the data is refused, never invented. Read-only, tenant-scoped, AEGIS-screened.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "question": {
    "description": "your plain-English question",
    "type": "string"
   },
   "tenant_slug": {
    "description": "the tenant to ask about",
    "type": "string"
   }
  },
  "required": [
   "tenant_slug",
   "question"
  ],
  "type": "object"
 },
 "name": "flocore_ask"
}
