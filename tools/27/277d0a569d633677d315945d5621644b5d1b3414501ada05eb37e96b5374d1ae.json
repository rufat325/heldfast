{
 "annotations": {
  "cost": {
   "all": 2
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get SVG code and dimensions for specific icons. Input icon names in prefix:name format (e.g., \"mdi:home\", \"fa:star\"). Returns SVG markup, width, and height.",
 "inputSchema": {
  "examples": [
   {
    "icons": "home,arrow-left,user",
    "prefix": "mdi"
   },
   {
    "icons": "star",
    "prefix": "fa"
   }
  ],
  "properties": {
   "icons": {
    "description": "Comma-separated icon names within the collection (e.g., \"home,arrow-left,user\")",
    "type": "string"
   },
   "prefix": {
    "description": "Collection prefix (e.g., \"mdi\", \"fa\", \"heroicons\", \"lucide\")",
    "type": "string"
   }
  },
  "required": [
   "prefix",
   "icons"
  ],
  "type": "object"
 },
 "name": "get_icons",
 "outputSchema": {
  "properties": {
   "icons": {
    "description": "Array of icon objects with SVG data and dimensions",
    "items": {
     "properties": {
      "full_name": {
       "description": "Full icon identifier in prefix:name format",
       "type": "string"
      },
      "height": {
       "description": "Icon height in pixels",
       "type": "number"
      },
      "name": {
       "description": "Icon name within the collection",
       "type": "string"
      },
      "svg_body": {
       "description": "SVG markup content (inner body)",
       "type": "string"
      },
      "width": {
       "description": "Icon width in pixels",
       "type": "number"
      }
     },
     "required": [
      "name",
      "full_name",
      "svg_body",
      "width",
      "height"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "prefix": {
    "description": "Collection prefix (e.g., mdi, fa, heroicons)",
    "type": "string"
   }
  },
  "required": [
   "prefix",
   "icons"
  ],
  "type": "object"
 },
 "title": "Get Icons",
 "version": "v1.90a21026"
}
