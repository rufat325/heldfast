{
 "description": "Apply a manifest string with server-side dry-run",
 "inputSchema": {
  "properties": {
   "dry_run": {
    "description": "Server-side dry-run (default: true)",
    "type": "boolean"
   },
   "manifest": {
    "description": "YAML or JSON manifest string",
    "type": "string"
   },
   "namespace": {
    "description": "Namespace to apply in (default: from manifest or \"default\")",
    "type": "string"
   }
  },
  "required": [
   "manifest"
  ],
  "type": "object"
 },
 "name": "k8s__apply_manifest"
}
