{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Run a raw SoQL query against any Calgary open-data resource (data.calgary.ca) by its Socrata id (8-char like \"iahh-g8bj\"). Full SoQL: where/select/group/order/limit/offset. Use calgary_datasets to find a resource id, or calgary_recent for the common ones.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-data-calgary-api-key",
    "limit": 100,
    "resource_id": "iahh-g8bj",
    "where": "incident_year=2025"
   },
   {
    "_apiKey": "your-data-calgary-api-key",
    "group": "incident_category",
    "order": "count(*) DESC",
    "resource_id": "iahh-g8bj",
    "select": "incident_category, count(*)"
   }
  ],
  "properties": {
   "_apiKey": {
    "description": "Optional \u2014 your own Socrata app token for higher rate limits. Omit to use the keyless endpoint.",
    "type": "string"
   },
   "group": {
    "description": "SoQL $group (e.g. \"incident_category\").",
    "type": "string"
   },
   "limit": {
    "description": "Max rows (default 100, max 5000).",
    "type": "number"
   },
   "offset": {
    "description": "Row offset for paging.",
    "type": "number"
   },
   "order": {
    "description": "SoQL $order (e.g. \"incident_datetime DESC\").",
    "type": "string"
   },
   "resource_id": {
    "description": "Socrata resource id, e.g. \"iahh-g8bj\" (police incidents).",
    "type": "string"
   },
   "select": {
    "description": "SoQL $select (e.g. \"incident_category, count(*)\").",
    "type": "string"
   },
   "where": {
    "description": "SoQL $where filter (e.g. \"incident_year=2025\").",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "calgary_query",
 "title": "Calgary Query",
 "version": "v1.102678e9"
}
