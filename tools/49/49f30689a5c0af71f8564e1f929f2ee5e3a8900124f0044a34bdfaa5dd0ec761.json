{
 "description": "Analyze the top-ranking pages for a search keyword. Pass your own page as the first `urls` entry to also get its ranking vs. those competitors.",
 "inputSchema": {
  "properties": {
   "country": {
    "description": "Two-letter country code (default: us)",
    "type": "string"
   },
   "keyword": {
    "description": "The search keyword (required)",
    "type": "string"
   },
   "language": {
    "description": "Two-letter language code (default: en)",
    "type": "string"
   },
   "urls": {
    "description": "Optional. The FIRST entry is your own page, scored against the SERP; omit to just list competitors.",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "keyword"
  ],
  "type": "object"
 },
 "name": "analyze_competitors"
}
