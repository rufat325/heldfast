{
 "description": "Kanon checklists at kanon.theastraway.com, attached to Life items or tasks. Actions: templates_list, template_get, template_create, template_update, template_delete, list, get, create, update, delete, toggle_item, complete, progress (rollup for a parent_type+parent_id).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "action": {
    "description": "Which checklist operation to perform.",
    "enum": [
     "templates_list",
     "template_get",
     "template_create",
     "template_update",
     "template_delete",
     "list",
     "get",
     "create",
     "update",
     "delete",
     "toggle_item",
     "complete",
     "progress"
    ],
    "type": "string"
   },
   "assigned_to": {
    "description": "Assignee (e.g. 'Dae') \u2014 create/update.",
    "type": "string"
   },
   "checklist_id": {
    "description": "Checklist id \u2014 get/update/delete/toggle_item/complete (required).",
    "type": "string"
   },
   "description": {
    "description": "Long description \u2014 template_create/template_update.",
    "type": "string"
   },
   "item_id": {
    "description": "Checklist item id \u2014 required for toggle_item.",
    "type": "string"
   },
   "origin": {
    "description": "Template origin \u2014 template_create (default 'user').",
    "enum": [
     "user",
     "flagship"
    ],
    "type": "string"
   },
   "parent_id": {
    "description": "Parent id (a Life item_id or task id) \u2014 create (required); list/progress filter (progress requires it).",
    "type": "string"
   },
   "parent_label": {
    "description": "Human-readable parent label \u2014 create.",
    "type": "string"
   },
   "parent_type": {
    "description": "Parent type \u2014 create (required); list/progress filter (progress requires it).",
    "enum": [
     "life_item",
     "task"
    ],
    "type": "string"
   },
   "schedule": {
    "additionalProperties": {},
    "description": "Recurrence config \u2014 template_create/template_update; create/update.",
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "sections": {
    "description": "[{section_id?, title, phase: 'pre'|'run'|'post', items: [{item_id?, label, note?, checked?, assignee?}]}] \u2014 template_create/template_update; create/update (inline sections, alternative to template_id).",
    "items": {
     "additionalProperties": {},
     "propertyNames": {
      "type": "string"
     },
     "type": "object"
    },
    "type": "array"
   },
   "template_id": {
    "description": "Checklist template id \u2014 template_get/template_update/template_delete (required); create (instantiate from a template).",
    "type": "string"
   },
   "title": {
    "description": "Title \u2014 template_create (required)/template_update; create/update (checklist title).",
    "type": "string"
   }
  },
  "required": [
   "action"
  ],
  "type": "object"
 },
 "name": "mind_checklists"
}
