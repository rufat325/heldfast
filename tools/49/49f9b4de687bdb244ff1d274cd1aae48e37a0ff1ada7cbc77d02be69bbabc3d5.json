{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Search Dryad, a curated repository of open research datasets (mostly the data underlying published, peer-reviewed studies in any field of science). Full-text query over titles, abstracts, authors and keywords. Returns matching datasets with DOI, title, authors, truncated abstract, keywords, publication date, field of science and license. Keyless.",
 "inputSchema": {
  "examples": [
   {
    "query": "coral bleaching"
   },
   {
    "limit": 50,
    "page": 1,
    "query": "maize genome"
   }
  ],
  "properties": {
   "limit": {
    "description": "Max results per page (default 20, max 100).",
    "type": "number"
   },
   "page": {
    "description": "Page number, 1-based (default 1).",
    "type": "number"
   },
   "query": {
    "description": "Search terms (e.g. \"coral bleaching\", \"maize genome\").",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_datasets",
 "title": "Search Datasets",
 "version": "v1.f19359d9"
}
