{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "tier": "free",
  "title": "Get Tag Profile"
 },
 "description": "Get the profile for a tag (description, image count, total zaps, related tags) without fetching any images. Use browse_by_tag to also retrieve images.",
 "inputSchema": {
  "properties": {
   "tag": {
    "description": "Tag slug (e.g. \"bitcoin\", \"meme\")",
    "type": "string"
   }
  },
  "required": [
   "tag"
  ],
  "type": "object"
 },
 "name": "get_tag_profile",
 "outputSchema": {
  "additionalProperties": true,
  "description": "A tag profile (description, related tags, stats) without images.",
  "properties": {
   "description": {
    "description": "Tag description.",
    "type": [
     "string",
     "null"
    ]
   },
   "image_count": {
    "description": "Number of images with this tag.",
    "type": "number"
   },
   "name": {
    "description": "Tag display name.",
    "type": "string"
   },
   "related_tags": {
    "description": "Related tag slugs.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "slug": {
    "description": "Tag slug.",
    "type": "string"
   }
  },
  "type": "object"
 }
}
