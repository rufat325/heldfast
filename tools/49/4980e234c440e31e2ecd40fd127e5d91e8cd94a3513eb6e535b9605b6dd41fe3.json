{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Update Inventory Details"
 },
 "description": "Set an inventory item's typed details \u2014 kind vehicle/housing/bike/structure/power + that variant's fields (vehicle: plate/makeModel/vin/fuelType; housing: sleepCapacity/powerNeeds; power: rating/phase/voltage). Requires event_id + item_id + kind + details; host.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "details": {
    "additionalProperties": {},
    "propertyNames": {
     "type": "string"
    },
    "type": "object"
   },
   "event_id": {
    "maxLength": 64,
    "minLength": 1,
    "type": "string"
   },
   "item_id": {
    "maxLength": 128,
    "minLength": 1,
    "type": "string"
   },
   "kind": {
    "enum": [
     "vehicle",
     "housing",
     "bike",
     "structure",
     "power"
    ],
    "type": "string"
   }
  },
  "required": [
   "event_id",
   "item_id",
   "kind",
   "details"
  ],
  "type": "object"
 },
 "name": "update_inventory_details",
 "title": "Update Inventory Details"
}
