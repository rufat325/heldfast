{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Find products by words in their name, slug, tagline or description. Returns the matches with their one-time price. Empty query returns the catalogue.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "1-50, default 8.",
    "type": "integer"
   },
   "q": {
    "description": "Words to match. Optional.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "search_catalogue",
 "title": "Search the catalogue"
}
