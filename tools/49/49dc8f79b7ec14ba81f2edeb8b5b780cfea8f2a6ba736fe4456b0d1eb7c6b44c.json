{
 "description": "Analyze a page's performance: load time, page size, render-blocking resources, DOM complexity, compression, caching, and image optimization.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "url": {
    "description": "The URL to audit for performance",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "audit_performance"
}
