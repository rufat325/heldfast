{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true,
  "streamingHint": false,
  "title": "Preview Prompt Technique"
 },
 "description": "Preview how a technique enhances your prompt WITHOUT executing (returns an execution_token). Or pass technique=\"auto\" with just a query to get the top techniques recommended for that task.",
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "query": {
    "description": "Your query, problem, or task",
    "type": "string"
   },
   "technique": {
    "description": "Technique name (e.g. scot, pre_mortem), or \"auto\" to recommend techniques for the query",
    "type": "string"
   },
   "tool": {
    "description": "Target tool (e.g. grok_reason). Not needed when technique=\"auto\"",
    "type": "string"
   }
  },
  "required": [
   "technique",
   "query"
  ],
  "type": "object"
 },
 "name": "preview_prompt_technique"
}
