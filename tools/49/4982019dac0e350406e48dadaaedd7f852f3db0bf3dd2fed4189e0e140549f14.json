{
 "description": "Get the top podcast apps and players downloading a show, from OP3, over the last three calendar months. Returns each app with its download count and percent share. Answers 'what apps do my listeners use'. Needs a show UUID.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 10,
    "description": "Max apps to return, ranked by downloads (default 10).",
    "type": "number"
   },
   "show_uuid": {
    "description": "OP3 show UUID (32 hex chars).",
    "type": "string"
   }
  },
  "required": [
   "show_uuid"
  ],
  "type": "object"
 },
 "name": "op3_top_apps"
}
