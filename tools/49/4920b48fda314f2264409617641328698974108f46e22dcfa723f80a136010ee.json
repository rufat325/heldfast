{
 "description": "Produce clause-level evidence for policy additions, removals, text, applicability, and requirement changes, with version and effective-date guardrails. Costs $0.05 USDC on Base mainnet.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "policies": {
    "items": {
     "additionalProperties": true,
     "required": [
      "id",
      "baseline",
      "current"
     ],
     "type": "object"
    },
    "maxItems": 1000,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "policies"
  ],
  "type": "object"
 },
 "name": "justhandled_policy_change_evidence_builder"
}
