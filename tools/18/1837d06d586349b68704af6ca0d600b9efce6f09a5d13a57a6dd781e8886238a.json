{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get the seafloor depth or land elevation at a single latitude/longitude point from the GEBCO global bathymetric grid (General Bathymetric Chart of the Oceans, IHO/IOC Seabed 2030 project). Returns elevation in meters relative to sea level \u2014 negative for ocean depth, positive for land height, e.g. -5943 near the Mariana Trench or 2947 in the Himalayan foothills. Optionally query \"sub_ice_topo\" to get bedrock topography beneath Antarctic/Greenland ice sheets instead of the ice surface. Data: GEBCO WMS, public domain, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "lat": {
    "description": "Latitude in decimal degrees (e.g. 51.4769 for Greenwich).",
    "maximum": 90,
    "minimum": -90,
    "type": "number"
   },
   "lon": {
    "description": "Longitude in decimal degrees (e.g. -0.0005 for Greenwich).",
    "maximum": 180,
    "minimum": -180,
    "type": "number"
   },
   "surface": {
    "description": "Which GEBCO grid to query: \"standard\" (default) returns surface elevation, including the top of ice sheets where present; \"sub_ice_topo\" returns bedrock topography beneath Antarctic/Greenland ice sheets instead.",
    "enum": [
     "standard",
     "sub_ice_topo"
    ],
    "type": "string"
   }
  },
  "required": [
   "lat",
   "lon"
  ],
  "type": "object"
 },
 "name": "gebco.elevation.point",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Get GEBCO Point Elevation"
}
