{
 "description": "Check whether a URL is allowed under a network policy. Returns { allowed, reason } without making any actual request. Use this to gate tool calls before they execute.",
 "inputSchema": {
  "properties": {
   "method": {
    "description": "HTTP method (default GET).",
    "type": "string"
   },
   "policy": {
    "description": "A simplified network policy. Use { allow: [\"api.openai.com\", \"*.example.com\"] }. Optional: deny: string[], methods: string[].",
    "properties": {
     "allow": {
      "description": "Host patterns to allow. Supports exact and *.suffix wildcards.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "deny": {
      "description": "Host patterns to deny. Wins over allow.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "methods": {
      "description": "HTTP methods to permit (e.g. [\"GET\", \"POST\"]). Default any.",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "type": "object"
   },
   "url": {
    "description": "URL to check.",
    "type": "string"
   }
  },
  "required": [
   "url",
   "policy"
  ],
  "type": "object"
 },
 "name": "check_url"
}
