{
 "description": "Convert a Word document to PDF (needs a PDF backend such as LibreOffice on the host).",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "input": {
    "description": "Path to the .docx.",
    "type": "string"
   },
   "output": {
    "description": "Output .pdf path (optional).",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "convert_to_pdf"
}
