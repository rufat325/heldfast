{
 "description": "Attach tags to a matter, contact, company, or task. Tags that don't exist yet are created.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "tags": {
    "description": "Tag names. Nonexistent tags are created automatically on attach.",
    "items": {
     "type": "string"
    },
    "minItems": 1,
    "type": "array"
   },
   "target_id": {
    "description": "The record's ID.",
    "type": "integer"
   },
   "target_type": {
    "description": "What kind of record to tag.",
    "enum": [
     "matter",
     "contact",
     "company",
     "task"
    ],
    "type": "string"
   }
  },
  "required": [
   "target_type",
   "target_id",
   "tags"
  ],
  "type": "object"
 },
 "name": "attach-tags"
}
