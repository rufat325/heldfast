{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Read an object's category-level notification preferences and (optionally) per-channel overrides.\n\nWhen to use:\n- Before update_suprsend_category_preference_object, to read current state.\n- Before sending to an object, to check delivery permission.\n\nWhen NOT to use:\n- For the object's identity or channels \u2014 use get_suprsend_object.\n- For users \u2014 use get_suprsend_user_preferences.\n\nReturns: the object's preference tree. Pass category to scope to one preference; omit for the full tree. Set channel_preferences=true to include per-channel overrides.",
 "inputSchema": {
  "properties": {
   "category": {
    "description": "The category_slug of the object to get preferences from, if not provided, it will get all the preferences for the object.",
    "type": "string"
   },
   "channel_preferences": {
    "description": "set this to true to get all the channel preferences for the object.",
    "type": "boolean"
   },
   "object_id": {
    "description": "The object_id of the object to get preferences from.",
    "type": "string"
   },
   "object_type": {
    "description": "The object_type of the object to get preferences from.",
    "type": "string"
   },
   "workspace": {
    "description": "SuprSend workspace to get the user from.",
    "type": "string"
   }
  },
  "required": [
   "object_id",
   "object_type"
  ],
  "type": "object"
 },
 "name": "get_suprsend_object_preferences"
}
