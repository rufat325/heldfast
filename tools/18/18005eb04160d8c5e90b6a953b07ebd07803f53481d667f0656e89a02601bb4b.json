{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Ranks symbols on a given trading day by short_ratio (short_volume / total_volume) using FINRA Reg SHO daily data summed across every consolidated-NMS reporting facility. Scans the whole published day (~12,000 symbols) and returns the highest-ratio names. Useful for spotting heavily-shorted names; raise min_total_volume to exclude thinly-traded micro-caps. Zero-rated: costs 0 credits.",
 "inputSchema": {
  "examples": [
   {
    "limit": 25,
    "min_total_volume": 1000000
   },
   {
    "date": "2026-08-21",
    "limit": 5,
    "min_total_volume": 1000000
   }
  ],
  "properties": {
   "date": {
    "description": "YYYY-MM-DD or YYYYMMDD; defaults to most recent published day",
    "type": "string"
   },
   "limit": {
    "description": "1\u2013100 (default 25)",
    "type": "number"
   },
   "min_total_volume": {
    "description": "Skip symbols below this consolidated total volume (default 100000)",
    "type": "number"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "short_volume_top",
 "outputSchema": {
  "oneOf": [
   {
    "properties": {
     "found": {
      "description": "No data available",
      "enum": [
       false
      ],
      "type": "boolean"
     },
     "hint": {
      "description": "Guidance for the user",
      "type": "string"
     },
     "reason": {
      "description": "Reason for failure",
      "enum": [
       "invalid_date",
       "no_recent_data"
      ],
      "type": "string"
     }
    },
    "required": [
     "found",
     "reason",
     "hint"
    ],
    "type": "object"
   },
   {
    "properties": {
     "date": {
      "description": "Date in YYYY-MM-DD format",
      "type": "string"
     },
     "min_total_volume": {
      "description": "Minimum total volume filter applied",
      "type": "number"
     },
     "symbols_considered": {
      "description": "Total symbols in the file",
      "type": "number"
     },
     "symbols_returned": {
      "description": "Number of symbols returned",
      "type": "number"
     },
     "top": {
      "description": "Ranked symbols by short_ratio, descending",
      "items": {
       "properties": {
        "short_exempt_volume": {
         "description": "Combined short exempt volume",
         "type": "number"
        },
        "short_ratio": {
         "description": "Short volume / total volume ratio",
         "type": "number"
        },
        "short_volume": {
         "description": "Combined short volume",
         "type": "number"
        },
        "symbol": {
         "description": "Ticker symbol",
         "type": "string"
        },
        "total_volume": {
         "description": "Combined total volume",
         "type": "number"
        }
       },
       "required": [
        "symbol",
        "short_volume",
        "short_exempt_volume",
        "total_volume",
        "short_ratio"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "date",
     "min_total_volume",
     "symbols_considered",
     "symbols_returned",
     "top"
    ],
    "type": "object"
   }
  ]
 },
 "title": "Short Volume Top",
 "version": "v1.343f7479"
}
