{
 "description": "Look up the canonical signed body for a previously-issued receipt id, so an agent can show the exact bytes it relied on when making a payment decision. Canonical input: `receipt_id`. The legacy alias `for_request_id` is still accepted for one release.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "for_request_id": {
    "minLength": 1,
    "type": "string"
   },
   "receipt_id": {
    "minLength": 1,
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_signed_receipt",
 "title": "Re-fetch a previous signed receipt body"
}
