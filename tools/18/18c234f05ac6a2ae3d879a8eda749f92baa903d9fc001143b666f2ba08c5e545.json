{
 "annotations": {
  "readOnlyHint": false
 },
 "description": "Runs a Go program and returns structured output (stdout, stderr, exit code).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "args": {
    "default": [],
    "description": "Arguments to pass to the program",
    "items": {
     "maxLength": 65536,
     "type": "string"
    },
    "maxItems": 1000,
    "type": "array"
   },
   "buildArgs": {
    "default": [],
    "description": "Build flags to pass to go run (e.g., '-tags=integration'). Note: flags like -race and -tags have dedicated params for discoverability, but can also be passed here. Flag injection checks are not applied to buildArgs since they are intentionally build flags.",
    "items": {
     "maxLength": 255,
     "type": "string"
    },
    "maxItems": 1000,
    "type": "array"
   },
   "compact": {
    "default": true,
    "description": "Prefer compact output",
    "type": "boolean"
   },
   "exec": {
    "description": "Custom execution wrapper (-exec). The built binary is passed to this command instead of being run directly.",
    "maxLength": 4096,
    "type": "string"
   },
   "file": {
    "default": ".",
    "description": "Go file or package to run (default: .)",
    "maxLength": 4096,
    "type": "string"
   },
   "maxOutput": {
    "description": "Maximum length in characters for stdout/stderr output. Output exceeding this limit will be truncated. Default: no limit. Max: 1048576 (1MB).",
    "maximum": 1048576,
    "minimum": 1024,
    "type": "integer"
   },
   "path": {
    "description": "Project root path",
    "maxLength": 4096,
    "type": "string"
   },
   "race": {
    "default": false,
    "description": "Enable data race detection (-race)",
    "type": "boolean"
   },
   "stream": {
    "default": false,
    "description": "Return tail-focused output for long-running programs. This is a streaming-like mode that keeps recent lines.",
    "type": "boolean"
   },
   "tags": {
    "description": "Build tags for conditional compilation (-tags)",
    "items": {
     "maxLength": 255,
     "type": "string"
    },
    "maxItems": 1000,
    "type": "array"
   },
   "tailLines": {
    "default": 200,
    "description": "When stream is true, keep this many trailing stdout/stderr lines",
    "maximum": 5000,
    "minimum": 10,
    "type": "integer"
   },
   "timeout": {
    "description": "Program execution timeout in milliseconds. Overrides the default 300s build timeout. Min: 1000 (1s), Max: 600000 (10m).",
    "maximum": 600000,
    "minimum": 1000,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "run",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "exitCode": {
    "type": "number"
   },
   "stderr": {
    "type": "string"
   },
   "stderrTotalLines": {
    "type": "number"
   },
   "stderrTruncated": {
    "type": "boolean"
   },
   "stdout": {
    "type": "string"
   },
   "stdoutTotalLines": {
    "type": "number"
   },
   "stdoutTruncated": {
    "type": "boolean"
   },
   "success": {
    "type": "boolean"
   },
   "timedOut": {
    "type": "boolean"
   }
  },
  "required": [
   "exitCode",
   "success"
  ],
  "type": "object"
 },
 "title": "Go Run"
}
