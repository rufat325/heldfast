{
 "annotations": {
  "cost": {
   "all": 10
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Download a file from Dropbox and return its content as text plus metadata (size, type, modified date). Use to retrieve file contents for processing or inspection.",
 "inputSchema": {
  "examples": [
   {
    "path": "/Documents/contract.pdf"
   }
  ],
  "properties": {
   "path": {
    "description": "File path to download",
    "type": "string"
   }
  },
  "required": [
   "path"
  ],
  "type": "object"
 },
 "name": "dropbox_download",
 "outputSchema": {
  "properties": {
   "content": {
    "description": "Downloaded file content as text",
    "type": "string"
   },
   "error": {
    "description": "Error code if connection failed",
    "type": "string"
   },
   "message": {
    "description": "Error message if connection failed",
    "type": "string"
   },
   "metadata": {
    "description": "File metadata from download response",
    "properties": {
     "id": {
      "description": "Unique file ID",
      "type": "string"
     },
     "modified": {
      "description": "Last modified timestamp",
      "type": "string"
     },
     "name": {
      "description": "File name",
      "type": "string"
     },
     "path_display": {
      "description": "Display path",
      "type": "string"
     },
     "size": {
      "description": "File size in bytes",
      "type": "number"
     }
    },
    "type": "object"
   }
  },
  "type": "object"
 },
 "title": "Dropbox Download",
 "version": "v1.1304e872"
}
