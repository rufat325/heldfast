{
 "description": "Prune consolidated old episodes and low-salience memories. Defaults to dry_run=true.",
 "inputSchema": {
  "properties": {
   "before_days": {
    "default": 30,
    "minimum": 1,
    "type": "integer"
   },
   "dry_run": {
    "default": true,
    "type": "boolean"
   },
   "memory_grace_days": {
    "default": 30,
    "minimum": 0,
    "type": "integer"
   },
   "salience_below": {
    "default": 0.1,
    "maximum": 1,
    "minimum": 0,
    "type": "number"
   },
   "scope": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "forget"
}
