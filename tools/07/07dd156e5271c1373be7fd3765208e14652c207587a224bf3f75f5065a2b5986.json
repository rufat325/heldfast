{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Use this tool for ANY 'what is going on for me now / today / this week / this month', daily-weekly-monthly-yearly horoscope, forecast or transit question \u2014 never estimate transiting positions yourself and never web-search them. Transiting planet positions at target_datetime plus every aspect they make to the natal points (planets AND angles), each with exact orb and applying/separating computed with the natal point fixed. Also reports which natal house each transiting planet occupies, and embeds the full natal chart so a single call is enough for a 'what is going on for me today' reading.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "include_minor_aspects": {
    "default": false,
    "description": "Also report minor aspects (semi-sextile, semi-square, quintile, sesquiquadrate, biquintile, quincunx) with tight orbs. Default false = the five major aspects only.",
    "type": "boolean"
   },
   "natal": {
    "additionalProperties": false,
    "description": "The person's birth data",
    "properties": {
     "birth_datetime": {
      "description": "ISO-8601 date-time WITH timezone, e.g. '1990-05-17T14:30:00Z' or '1990-05-17T17:30:00+03:00'. Convert the local birth time to an offset yourself; naive values are rejected.",
      "format": "date-time",
      "type": "string"
     },
     "house_system": {
      "default": "placidus",
      "description": "House system; default 'placidus'. One of: placidus, koch, porphyry, regiomontanus, campanus, equal, whole_sign, topocentric, morinus, alcabitius",
      "enum": [
       "placidus",
       "koch",
       "porphyry",
       "regiomontanus",
       "campanus",
       "equal",
       "whole_sign",
       "topocentric",
       "morinus",
       "alcabitius"
      ],
      "type": "string"
     },
     "latitude": {
      "description": "Birth latitude in decimal degrees (north positive)",
      "maximum": 90,
      "minimum": -90,
      "type": "number"
     },
     "longitude": {
      "description": "Birth longitude in decimal degrees (east positive)",
      "maximum": 180,
      "minimum": -180,
      "type": "number"
     }
    },
    "required": [
     "birth_datetime",
     "latitude",
     "longitude"
    ],
    "type": "object"
   },
   "target_datetime": {
    "description": "Moment to compute transits for, ISO-8601 WITH timezone (e.g. now, or a date the user asks about)",
    "format": "date-time",
    "type": "string"
   }
  },
  "required": [
   "natal",
   "target_datetime"
  ],
  "type": "object"
 },
 "name": "get_transits_and_aspects",
 "title": "Transits to a natal chart"
}
