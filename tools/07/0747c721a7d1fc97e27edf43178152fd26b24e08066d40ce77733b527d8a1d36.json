{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Access simulation data: agent profiles, configuration, action logs, social media posts, round-by-round timeline, per-agent activity stats, and interview history. Paginated \u2014 use offset to get more results when has_more is true.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action_type": {
    "description": "Filter actions by type (CREATE_POST, LIKE_POST, etc.)",
    "type": "string"
   },
   "agent_name": {
    "description": "Filter actions by agent name",
    "type": "string"
   },
   "data_type": {
    "description": "What data to retrieve: overview (condensed summary: entities, agents, graph, config, action stats \u2014 start here), profiles (full agent personas), config (simulation parameters), actions (agent action log), posts (social media posts from SQLite), timeline (per-round summaries), agent_stats (per-agent activity breakdown), interview_history (past interview transcripts)",
    "enum": [
     "overview",
     "profiles",
     "config",
     "actions",
     "posts",
     "timeline",
     "agent_stats",
     "interview_history"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Max results per page (default 50)",
    "maximum": 200,
    "minimum": 1,
    "type": "integer"
   },
   "offset": {
    "description": "Offset for pagination (default 0)",
    "minimum": 0,
    "type": "integer"
   },
   "platform": {
    "description": "Filter by platform (for actions and posts)",
    "enum": [
     "twitter",
     "reddit"
    ],
    "type": "string"
   },
   "simulation_id": {
    "description": "The simulation ID",
    "type": "string"
   }
  },
  "required": [
   "simulation_id",
   "data_type"
  ],
  "type": "object"
 },
 "name": "simulation_data",
 "title": "Simulation Data"
}
