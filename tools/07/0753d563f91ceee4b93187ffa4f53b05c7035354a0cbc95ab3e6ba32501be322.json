{
 "annotations": {
  "destructiveHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Negative-acknowledge a leased envelope (requeues it to the tail).",
 "inputSchema": {
  "properties": {
   "leaseToken": {
    "description": "Lease token from queue_lease",
    "type": "string"
   },
   "queue": {
    "description": "Queue name",
    "type": "string"
   },
   "spaceId": {
    "description": "Memory space ID",
    "type": "string"
   }
  },
  "required": [
   "spaceId",
   "queue",
   "leaseToken"
  ],
  "type": "object"
 },
 "name": "queue_nack",
 "outputSchema": {
  "properties": {},
  "type": "object"
 }
}
