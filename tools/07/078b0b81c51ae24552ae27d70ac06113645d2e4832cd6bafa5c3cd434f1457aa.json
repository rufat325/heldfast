{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Dubai property transactions (sales, mortgages and gifts) registered with the Dubai Land Department, with transaction value, procedure, area/district, project, property type, room count and nearest metro/mall/landmark. Coverage is the current calendar year \u2014 earlier years come back empty. A date range is required.",
 "inputSchema": {
  "examples": [
   {
    "from_date": "2026-09-01",
    "limit": 3,
    "sort": "TRANS_VALUE_DESC",
    "to_date": "2026-09-06",
    "transaction_type": "1"
   }
  ],
  "properties": {
   "free_hold": {
    "description": "\"1\" freehold only, \"0\" non-freehold only",
    "enum": [
     "0",
     "1"
    ],
    "type": "string"
   },
   "from_date": {
    "description": "Start of the range, ISO date e.g. \"2026-09-01\"",
    "type": "string"
   },
   "include_arabic": {
    "description": "Keep the Arabic (`*_AR`) twin of each bilingual column. Default false.",
    "type": "boolean"
   },
   "limit": {
    "description": "Rows to return, 1-200 (default 25)",
    "type": "number"
   },
   "off_plan": {
    "description": "\"1\" off-plan only, \"0\" ready only",
    "enum": [
     "0",
     "1"
    ],
    "type": "string"
   },
   "offset": {
    "description": "Rows to skip, for paging through `total` (default 0)",
    "type": "number"
   },
   "property_type_id": {
    "description": "Upstream property-type id, as seen in PROPERTY_TYPE_ID",
    "type": "string"
   },
   "sort": {
    "description": "Sort as \"<COLUMN>_ASC\" or \"<COLUMN>_DESC\" using a column name from the rows, e.g. \"TRANS_VALUE_DESC\". Omit for the upstream default order.",
    "type": "string"
   },
   "to_date": {
    "description": "End of the range, ISO date e.g. \"2026-09-06\"",
    "type": "string"
   },
   "transaction_type": {
    "description": "Group: \"1\" Sales, \"2\" Mortgages, \"3\" Gifts. Omit for all.",
    "enum": [
     "1",
     "2",
     "3"
    ],
    "type": "string"
   },
   "usage": {
    "description": "Property usage: \"1\" Residential, \"2\" Commercial, \"3\" Other. Omit for all.",
    "enum": [
     "1",
     "2",
     "3"
    ],
    "type": "string"
   }
  },
  "required": [
   "from_date",
   "to_date"
  ],
  "type": "object"
 },
 "name": "dld_search_transactions",
 "title": "Dld Search Transactions",
 "version": "v1.70f122e7"
}
