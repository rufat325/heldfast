{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": false,
  "title": "Publish product"
 },
 "description": "[MyStoq] Publish a product (set status=active) so it shows on the storefront. Requires Bearer token. WRITE.",
 "inputSchema": {
  "properties": {
   "product_id": {
    "description": "Product id to publish.",
    "type": "integer"
   }
  },
  "required": [
   "product_id"
  ],
  "type": "object"
 },
 "name": "mystoq__publish"
}
