{
 "description": "Publish an immutable template version. The version's identity is the hash of its compiled content \u2014 publishing identical content does not create a second version.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "template_id": {
    "description": "Template ID",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "template_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_template_publish"
}
