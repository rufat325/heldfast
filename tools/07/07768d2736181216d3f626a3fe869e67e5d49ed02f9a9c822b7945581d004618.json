{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Calls the StupidAPIs always-seven endpoint, which claims to return a random number between 1 and 10 but always returns 7. Optional `question` and `force` params are accepted but have no effect on the result.",
 "inputSchema": {
  "examples": [
   {
    "question": "What is my lucky number?"
   },
   {
    "force": 5,
    "question": "Give me a random number"
   }
  ],
  "properties": {
   "force": {
    "description": "Attempt to force a different number (1-10). It will not work.",
    "type": "number"
   },
   "question": {
    "description": "Your question. It will not affect the number.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "always_seven_generate",
 "outputSchema": {
  "properties": {
   "number": {
    "description": "Random number between 1 and 10 (always 7)",
    "type": "number"
   }
  },
  "required": [
   "number"
  ],
  "type": "object"
 },
 "title": "Always Seven Generate",
 "version": "v1.e15af73a"
}
