{
 "description": "Generate an image using AI. BYOK: configure your API keys via suite_setup(module: \"image\"). Providers: OpenAI DALL-E 3 (best for text/logos), Flux Pro (best for photorealistic), Gemini (versatile fallback). \"auto\" mode selects the best provider based on prompt content.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "prompt": {
    "description": "Image generation prompt. Be descriptive for best results.",
    "maxLength": 4000,
    "minLength": 1,
    "type": "string"
   },
   "provider": {
    "description": "Provider to use. \"auto\" (default) picks based on prompt: OpenAI for text-heavy, Flux for photorealistic, Gemini as fallback.",
    "enum": [
     "auto",
     "openai",
     "flux",
     "gemini"
    ],
    "type": "string"
   },
   "quality": {
    "description": "Image quality (OpenAI only). hd=finer detail but slower. Default: standard.",
    "enum": [
     "standard",
     "hd"
    ],
    "type": "string"
   },
   "size": {
    "description": "Image dimensions. square=1024x1024, landscape=1792x1024, portrait=1024x1792. Default: square.",
    "enum": [
     "square",
     "landscape",
     "portrait"
    ],
    "type": "string"
   },
   "style": {
    "description": "Image style (OpenAI only). vivid=hyper-real/dramatic, natural=more subtle. Default: vivid.",
    "enum": [
     "vivid",
     "natural"
    ],
    "type": "string"
   }
  },
  "required": [
   "prompt"
  ],
  "type": "object"
 },
 "name": "image_generate"
}
