{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Screen wallet"
 },
 "description": "Screen a wallet: OFAC SDN address match, on-chain signals (contract check, native and USDC balance, transaction count, first seen on Base) and named risk rules with up to 3 advice items. On Base, also reports any ERC-8004 agent the wallet owns and its open on-chain reputation as descriptive signals, never scored. Free ($0).\n\n        Returns risk_score, risk_level, sanctions (hit, lists, as_of), signals\n        with their source, the names of signals that could not be read\n        (unavailable), rules_fired, advice and linked_entities (entities you\n        checked that were linked to this wallet). On Base, also\n        erc8004_agent_ids and erc8004_reputation (open ERC-8004 feedback,\n        descriptive only -- never folded into risk_score or advice).\n        Accepts EVM (0x...) and Solana (base58) addresses.\n\n        Args:\n            wallet_address: Blockchain wallet address\n            chain_id: CAIP-2 chain ID (default: eip155:8453 for Base); base, eth, solana, btc also accepted\n        ",
 "inputSchema": {
  "properties": {
   "chain_id": {
    "default": "eip155:8453",
    "title": "Chain Id",
    "type": "string"
   },
   "wallet_address": {
    "title": "Wallet Address",
    "type": "string"
   }
  },
  "required": [
   "wallet_address"
  ],
  "title": "verify_walletArguments",
  "type": "object"
 },
 "name": "verify_wallet",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "verify_walletOutput",
  "type": "object"
 }
}
