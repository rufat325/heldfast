{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "One-shot dump of recent logcat lines, optionally filtered by app (must be running) and/or tag.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "appId": {
    "description": "Filter to this app (defaults to session appId if running)",
    "type": "string"
   },
   "lines": {
    "description": "Max lines (default 200)",
    "type": "integer"
   },
   "serial": {
    "description": "Device serial (defaults to session or the only device)",
    "type": "string"
   },
   "tag": {
    "description": "Filter to this log tag",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "get_logs",
 "title": "Get recent logs"
}
