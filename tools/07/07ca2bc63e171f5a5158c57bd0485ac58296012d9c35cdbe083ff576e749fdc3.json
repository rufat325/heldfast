{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true,
  "title": "Get Conversation"
 },
 "description": "Fetch messages from a DM thread with a specific user, newest first.\n\n    ``count`` is how many messages this response holds; ``has_more`` is\n    true when the thread has older messages than ``limit`` allowed.\n    ``total`` is DEPRECATED: it is the same number as ``count``, the page\n    length, NOT the number of messages in the thread.\n    Requires authentication.",
 "inputSchema": {
  "properties": {
   "limit": {
    "default": 50,
    "description": "Maximum results to return (1-100).",
    "maximum": 100,
    "minimum": 1,
    "title": "Limit",
    "type": "integer"
   },
   "username": {
    "description": "The other participant: a username or a user ID",
    "title": "Username",
    "type": "string"
   }
  },
  "required": [
   "username"
  ],
  "title": "get_conversationArguments",
  "type": "object"
 },
 "name": "colony_get_conversation",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "get_conversationOutput",
  "type": "object"
 }
}
