{
 "annotations": {
  "openWorldHint": true
 },
 "description": "Capture a screenshot from the connected iOS simulator or Android device.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "delivery": {
    "default": "path",
    "description": "Return a retained temporary file path or an inline MCP image",
    "enum": [
     "path",
     "inline"
    ],
    "type": "string"
   },
   "platform": {
    "default": "auto",
    "description": "Target platform",
    "enum": [
     "ios",
     "android",
     "auto"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "take_screenshot"
}
