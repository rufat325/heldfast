{
 "description": "Create an AVD from an installed system image (e.g. 'system-images;android-35;google_apis;arm64-v8a'). If systemImage is omitted, lists the installed images to choose from.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "device": {
    "description": "Device profile, e.g. 'pixel_7' (default: generic)",
    "type": "string"
   },
   "name": {
    "description": "Name for the new AVD",
    "type": "string"
   },
   "systemImage": {
    "description": "Installed system-images package id",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "create_avd",
 "title": "Create AVD"
}
