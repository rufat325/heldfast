{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List every command-line utility that has a tldr-pages cheatsheet, filtered to one platform (common, linux, osx, windows, android, sunos, freebsd) and one ISO 639-1 language, read from the tldr-pages index published on GitHub. Returns the command names plus a count. Answers which shell commands tldr documents for a given platform, before fetching a page.",
 "inputSchema": {
  "examples": [
   {
    "language": "en",
    "platform": "linux"
   }
  ],
  "properties": {
   "language": {
    "type": "string"
   },
   "platform": {
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "commands",
 "outputSchema": {
  "properties": {
   "commands": {
    "description": "List of command names",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "count": {
    "description": "Number of commands returned",
    "type": "number"
   },
   "language": {
    "description": "Language code used",
    "type": "string"
   },
   "platform": {
    "description": "Platform filter used",
    "type": "string"
   }
  },
  "required": [
   "platform",
   "language",
   "count",
   "commands"
  ],
  "type": "object"
 },
 "title": "Commands",
 "version": "v1.85d3224c"
}
