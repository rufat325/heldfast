{
 "description": "Edit, pause, resume or cancel a campaign. Content can only change while the campaign is a draft. After launch this route moves state \u2014 the frozen recipient set and the frozen template version do not change.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "audience_id": {
    "description": "Audience ID",
    "type": "string"
   },
   "campaign_id": {
    "description": "Campaign ID",
    "type": "string"
   },
   "domain_id": {
    "description": "Active sending domain ID",
    "type": "string"
   },
   "name": {
    "description": "Campaign name",
    "type": "string"
   },
   "organization_id": {
    "description": "Active organization ID",
    "type": "string"
   },
   "status": {
    "description": "paused, running or canceled",
    "enum": [
     "paused",
     "sending",
     "cancelled"
    ],
    "type": "string"
   },
   "template_id": {
    "description": "Published marketing template ID",
    "type": "string"
   },
   "template_version": {
    "description": "Published version to freeze into the campaign.",
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "organization_id",
   "campaign_id"
  ],
  "type": "object"
 },
 "name": "commsharbor_campaign_update"
}
