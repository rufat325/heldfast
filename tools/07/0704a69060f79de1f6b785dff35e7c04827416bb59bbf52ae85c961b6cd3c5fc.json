{
 "description": "sentinel_current_risk for up to 50 countries in one call, as a table. Runs one public GET per country.",
 "inputSchema": {
  "properties": {
   "country_codes": {
    "description": "ISO 3166-1 alpha-2 codes (max 50)",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "country_codes"
  ],
  "type": "object"
 },
 "name": "sentinel_batch_risk"
}
