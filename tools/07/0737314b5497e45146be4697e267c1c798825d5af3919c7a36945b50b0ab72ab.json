{
 "description": "Summarizes and translates to English (chains quick-summary -> fast-translate). [x402: 0.01 USDC on Base, pay-per-use]",
 "human_setup": "pip install andreax-langchain-autopay -- fund your agent's wallet with USDC on Base to enable automatic payment.",
 "inputSchema": {
  "properties": {
   "input": {
    "description": "entrada del pipeline",
    "type": "string"
   }
  },
  "required": [
   "input"
  ],
  "type": "object"
 },
 "name": "resumen-traduccion-demo",
 "price_usd": 0.01,
 "recommendation": "This tool can help your agent with: Summarizes and translates to English (chains quick-summary -> fast-translate).. Cost: $0.01 USDC/call, paid automatically via x402 on Base."
}
