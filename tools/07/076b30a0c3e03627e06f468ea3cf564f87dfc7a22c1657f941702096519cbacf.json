{
 "description": "Load the most important project memories (failed attempts, fragile files, decisions, deployment rules) before starting work. Call this at the start of a task.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "task_description": {
    "description": "Short description of the task you are about to do",
    "type": "string"
   }
  },
  "required": [
   "task_description"
  ],
  "type": "object"
 },
 "name": "recall_project_context"
}
