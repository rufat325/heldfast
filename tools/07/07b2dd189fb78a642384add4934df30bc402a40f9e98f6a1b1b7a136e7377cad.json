{
 "description": "Add an image to the END of a Notion page. `image_path` is either an absolute path to a local image file (uploaded to Notion) or an http(s) URL (referenced as an external image). Non-destructive \u2014 appended after existing blocks.",
 "inputSchema": {
  "properties": {
   "caption": {
    "description": "Optional caption (also used as markdown alt text on export)",
    "type": "string"
   },
   "image_path": {
    "description": "Absolute path to a local image file, or an http(s) image URL",
    "type": "string"
   },
   "page_id": {
    "description": "Notion page ID (32-char hex) or full Notion URL",
    "type": "string"
   }
  },
  "required": [
   "page_id",
   "image_path"
  ],
  "type": "object"
 },
 "name": "notion_add_image"
}
