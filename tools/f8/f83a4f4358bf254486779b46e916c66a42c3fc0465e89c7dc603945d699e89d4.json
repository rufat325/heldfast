{
 "description": "Flexible search across all token usage records with filtering by model, provider, project, date range, and cost thresholds. Returns individual records sorted by timestamp. Use this to find specific usage events or investigate high-cost records.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max records to return (default: 25)",
    "type": "number"
   },
   "max_cost": {
    "description": "Maximum cost per record (USD)",
    "type": "number"
   },
   "min_cost": {
    "description": "Minimum cost per record (USD)",
    "type": "number"
   },
   "model": {
    "description": "Filter by model name substring",
    "type": "string"
   },
   "project": {
    "description": "Filter by project name/path substring",
    "type": "string"
   },
   "providers": {
    "description": "Filter to specific providers (e.g. ['claude-code','cursor'])",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "scope": {
    "description": "Time scope: today, week (last 7d), month (calendar month), or all (default)",
    "enum": [
     "today",
     "week",
     "month",
     "all"
    ],
    "type": "string"
   },
   "since": {
    "description": "Start date (YYYY-MM-DD) for custom date range",
    "type": "string"
   },
   "sort_by": {
    "description": "Sort by cost (desc), tokens (desc), or time (desc). Default: time",
    "enum": [
     "cost",
     "tokens",
     "time"
    ],
    "type": "string"
   },
   "until": {
    "description": "End date (YYYY-MM-DD) for custom date range",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "drishti_search"
}
