{
 "description": "Mark a file as fragile/risky so future sessions get a guardrail warning (or block) before touching it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "file_path": {
    "type": "string"
   },
   "reason": {
    "description": "Why this file is fragile \u2014 what happened when it was mishandled",
    "type": "string"
   },
   "severity": {
    "default": "warn",
    "enum": [
     "warn",
     "block"
    ],
    "type": "string"
   }
  },
  "required": [
   "file_path",
   "reason"
  ],
  "type": "object"
 },
 "name": "remember_fragile_file"
}
