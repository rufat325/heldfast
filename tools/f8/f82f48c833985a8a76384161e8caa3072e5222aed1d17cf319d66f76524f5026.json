{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "High-quality LLM translation with control: target language, optional formality and tone, auto source detection. Preserves nuance and idiom. Pay-per-call via x402 (USDC, Base).",
 "inputSchema": {
  "properties": {
   "_payment": {
    "description": "Optional base64 x402 PAYMENT-SIGNATURE payload. Omit to receive payment requirements.",
    "type": "string"
   },
   "formality": {
    "description": "Optional register.",
    "enum": [
     "formal",
     "informal"
    ],
    "type": "string"
   },
   "source_language": {
    "description": "Optional source language; auto-detected if omitted.",
    "type": "string"
   },
   "target_language": {
    "description": "Target language, e.g. \"Romanian\" or \"es\" (required).",
    "type": "string"
   },
   "text": {
    "description": "Text to translate (required).",
    "type": "string"
   },
   "tone": {
    "description": "Optional tone, e.g. \"friendly\", \"professional\".",
    "type": "string"
   }
  },
  "required": [
   "text",
   "target_language"
  ],
  "type": "object"
 },
 "name": "agentbit.ai_translate",
 "title": "AI Translate (LLM)"
}
