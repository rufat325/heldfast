{
 "description": "DEPRECATED \u2014 phases are now DB-backed. Use list_phases (replaces get_next_phase), update_phase_status(phaseId, \"done\") (replaces mark_phase_complete), and load_workflow (replaces load_session/close_session).",
 "inputSchema": {
  "properties": {
   "action": {
    "description": "Legacy file-based harness. get_next_phase: oldest pending phase FILE; mark_phase_complete: re-tag the phase FILE status:pending \u2192 status:passing and archive it; close_session: tag session log as closed; load_session: full project state",
    "enum": [
     "get_next_phase",
     "mark_phase_complete",
     "close_session",
     "load_session"
    ],
    "type": "string"
   },
   "phaseFileId": {
    "description": "Phase file ID (action=mark_phase_complete)",
    "type": "string"
   },
   "sessionLogFileId": {
    "description": "Session log file ID (action=close_session)",
    "type": "string"
   },
   "workflowId": {
    "description": "Workflow ID",
    "type": "string"
   },
   "workspaceId": {
    "description": "Workspace ID (uses default if omitted)",
    "type": "string"
   }
  },
  "required": [
   "action",
   "workflowId"
  ],
  "type": "object"
 },
 "name": "project"
}
