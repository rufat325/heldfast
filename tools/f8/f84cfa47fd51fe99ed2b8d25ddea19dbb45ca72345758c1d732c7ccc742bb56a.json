{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "OSM Taginfo: list the most common values for an OpenStreetMap key, by frequency (e.g. all amenity= values from \"parking\" down). Useful for understanding how features are tagged in OSM. Keyless.",
 "inputSchema": {
  "examples": [
   {
    "key": "amenity"
   },
   {
    "key": "cuisine",
    "limit": 50
   }
  ],
  "properties": {
   "key": {
    "description": "An OSM key, e.g. \"amenity\", \"highway\", \"cuisine\".",
    "type": "string"
   },
   "limit": {
    "description": "Max values to return (default 20, max 100).",
    "type": "number"
   }
  },
  "required": [
   "key"
  ],
  "type": "object"
 },
 "name": "key_values",
 "title": "Key Values",
 "version": "v1.f60d408e"
}
