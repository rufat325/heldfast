{
 "description": "Expand a short prompt into a richer, more detailed description with added visual context, style cues, and composition details. Use the result with any generation tool for more vivid output.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "prompt": {
    "description": "The original prompt to enhance (max 2000 chars)",
    "type": "string"
   }
  },
  "required": [
   "prompt"
  ],
  "type": "object"
 },
 "name": "recraft_enhance_prompt"
}
