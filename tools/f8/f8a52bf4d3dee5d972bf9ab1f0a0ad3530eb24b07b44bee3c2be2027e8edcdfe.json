{
 "description": "Check ALL DeFi positions for a wallet in one call. Returns lending positions (Aave: supplied, borrowed, health factor, liquidation risk), token balances, and an overall portfolio summary with risk assessment. Tells the agent exactly what actions to consider.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "chain": {
    "description": "Chain: 'base' (default), 'ethereum', 'arbitrum'",
    "type": "string"
   },
   "protocols": {
    "description": "Filter to specific protocols: ['aave', 'uniswap', 'aerodrome']. Default: all.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "wallet": {
    "description": "Wallet address to check (0x...)",
    "type": "string"
   }
  },
  "required": [
   "wallet"
  ],
  "type": "object"
 },
 "name": "defi_check_positions"
}
