{
 "description": "Enable or disable verbose device logging for deep debugging. Requires device reboot to take effect.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "deviceId": {
    "description": "Device (default: booted)",
    "type": "string"
   },
   "enabled": {
    "description": "Enable or disable verbose logging (device reboot may be required)",
    "type": "boolean"
   }
  },
  "required": [
   "enabled"
  ],
  "type": "object"
 },
 "name": "simulator_verbose_logging"
}
