{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Generate a binding, deterministic quote with agent discount pricing and wholesale volume tiers. Quote is valid for 7 days. AUTH: requires an API key - get one instantly with POST https://growaify.com/api/v1/agents/register using name and purpose fields. No email needed. Send it as the X-API-Key header.",
 "inputSchema": {
  "properties": {
   "partner_id": {
    "description": "Optional partner ID \u2014 earns 20% recurring commission on the resulting order",
    "type": "string"
   },
   "quantity": {
    "default": 1,
    "description": "Number of units to quote (volume discounts apply)",
    "minimum": 1,
    "type": "integer"
   },
   "service_id": {
    "description": "Service identifier: task-worker, website-booking, automation-n8n, ai-receptionist, or smart-consultation",
    "type": "string"
   }
  },
  "required": [
   "service_id"
  ],
  "type": "object"
 },
 "name": "create_binding_quote",
 "outputSchema": {
  "properties": {
   "expires_at": {
    "description": "Quote expiry (7 days)",
    "type": "string"
   },
   "fulfillment_sla": {
    "type": "string"
   },
   "quote_id": {
    "description": "Binding quote ID (Q-MCP-...) used for purchase_service",
    "type": "string"
   },
   "service_id": {
    "type": "string"
   },
   "total_usd": {
    "type": "number"
   },
   "unit_price": {
    "description": "Agent price per unit in USD",
    "type": "number"
   }
  },
  "required": [
   "quote_id",
   "total_usd"
  ],
  "type": "object"
 },
 "title": "Create Binding Quote"
}
