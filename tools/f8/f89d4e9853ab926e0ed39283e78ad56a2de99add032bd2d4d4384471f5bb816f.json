{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Real-time airport weather station observations (METAR report): current temperature, dew point, wind, visibility, cloud ceiling, altimeter, straight from that airport's own sensor. For airport-station temperature/conditions questions, e.g. current temperature at SFO/KSFO, ORD/KORD, LHR/EGLL. ids = comma-sep ICAO codes (KSFO,EGLL,LFPG).",
 "inputSchema": {
  "examples": [
   {
    "ids": "KSFO,EGLL,LFPG"
   },
   {
    "format": "raw",
    "hours_before": 6,
    "ids": "KJFK"
   }
  ],
  "properties": {
   "format": {
    "description": "json (default) | raw | xml | html",
    "type": "string"
   },
   "hours_before": {
    "description": "How far back to look, 1-24 (default 1)",
    "type": "number"
   },
   "ids": {
    "description": "Comma-separated ICAO airport codes (4 letters, e.g. KSFO,EGLL,LFPG) \u2014 not IATA codes.",
    "type": "string"
   }
  },
  "required": [
   "ids"
  ],
  "type": "object"
 },
 "name": "metar",
 "outputSchema": {
  "oneOf": [
   {
    "description": "METAR observations in JSON format",
    "type": "object"
   },
   {
    "properties": {
     "body": {
      "description": "Raw response body",
      "type": "string"
     },
     "format": {
      "description": "Response format (raw, xml, or html)",
      "type": "string"
     }
    },
    "required": [
     "format",
     "body"
    ],
    "type": "object"
   }
  ]
 },
 "title": "Metar",
 "version": "v1.d1dc8164"
}
