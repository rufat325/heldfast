{
 "_meta": {
  "securitySchemes": [
   {
    "scopes": [
     "openid",
     "email",
     "profile"
    ],
    "type": "oauth2"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Validate a self-authored generation plan against the schema and the target model's real capabilities before execution. Returns directive errors to fix and revalidate; only a valid plan is executable.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "plan": {
    "additionalProperties": true,
    "description": "The generation plan object authored from generation_plan_brief's planJsonSchema.",
    "type": "object"
   }
  },
  "required": [
   "plan"
  ],
  "type": "object"
 },
 "name": "generation_plan_validate",
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "errors": {
    "description": "Empty when valid. Each entry states exactly what to change.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "valid": {
    "type": "boolean"
   }
  },
  "required": [
   "valid",
   "errors"
  ],
  "type": "object"
 },
 "title": "Validate Generation Plan"
}
