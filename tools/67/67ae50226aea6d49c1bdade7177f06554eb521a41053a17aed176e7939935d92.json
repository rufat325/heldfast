{
 "description": "Report why this Robinhood server has no working credentials and what to set. Every other tool is unavailable until credentials are configured, so call this first if no trading or market-data tools are listed.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {},
  "type": "object"
 },
 "name": "get_setup_status",
 "title": "Get setup status"
}
