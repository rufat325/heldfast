{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Add a custom hostname (SSL for SaaS) to a zone. Requires confirm:true (human-approval gate); without it returns a preview only.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "confirm": {
    "description": "Human-approval gate: must be true to actually perform this mutating operation. Omit or set false to get a non-executing preview of what would happen (with secrets redacted). A human should approve before this is set to true.",
    "type": "boolean"
   },
   "hostname": {
    "description": "The custom hostname to add, e.g. app.customer.com.",
    "type": "string"
   },
   "ssl_method": {
    "description": "SSL validation method. Default 'http'.",
    "enum": [
     "http",
     "txt",
     "email"
    ],
    "type": "string"
   },
   "ssl_type": {
    "description": "Certificate type. Default 'dv'.",
    "enum": [
     "dv"
    ],
    "type": "string"
   },
   "zone_id": {
    "description": "The Cloudflare zone ID (32-char hex). Get it from list_zones.",
    "type": "string"
   }
  },
  "required": [
   "zone_id",
   "hostname"
  ],
  "type": "object"
 },
 "name": "create_custom_hostname"
}
