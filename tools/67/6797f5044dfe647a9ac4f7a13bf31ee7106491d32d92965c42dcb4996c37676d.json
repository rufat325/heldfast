{
 "description": "Get detailed information about a specific API key (name, provider, safe_key, health status). Does NOT return the real key value.",
 "inputSchema": {
  "properties": {
   "name": {
    "description": "The key name",
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "prophetkey_get_key_info"
}
