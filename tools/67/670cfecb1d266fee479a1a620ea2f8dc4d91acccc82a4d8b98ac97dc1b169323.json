{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Look up the archived/historical version of a web page in the Internet Archive Wayback Machine. Returns the closest available snapshot (its Wayback URL, timestamp and HTTP status). Pass a timestamp to find the snapshot nearest a specific date; omit it to get the latest archived version.",
 "inputSchema": {
  "examples": [
   {
    "url": "https://example.com"
   },
   {
    "timestamp": "20200101",
    "url": "https://example.com/page"
   }
  ],
  "properties": {
   "timestamp": {
    "description": "Optional target date as YYYYMMDD or YYYYMMDDhhmmss. The closest snapshot to this time is returned. Defaults to the latest snapshot.",
    "type": "string"
   },
   "url": {
    "description": "The page URL to look up, e.g. \"example.com\" or \"https://example.com/page\".",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "get_snapshot",
 "title": "Get Snapshot",
 "version": "v1.a1f0da88"
}
