{
 "description": "Get metadata for a specific secret (keys only, no values)",
 "inputSchema": {
  "properties": {
   "project_id": {
    "type": "string"
   },
   "secret_name": {
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "secret_name"
  ],
  "type": "object"
 },
 "name": "ce_get_secret"
}
