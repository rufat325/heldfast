{
 "description": "Full-text search across all analysis files. Returns matching analyses with file:line context snippets.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "Max results (default: 10)",
    "type": "number"
   },
   "query": {
    "description": "Search query (case-insensitive)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "alive_search"
}
