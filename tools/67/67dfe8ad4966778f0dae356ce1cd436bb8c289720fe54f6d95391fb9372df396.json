{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-call-record-organizer"
 },
 "description": "Identity of the organizer of the call. This relationship is expanded by default in callRecord methods.\n\nTIP: Gets the organizer of a call record (v2 API). Returns the organizer's identity (user or application).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "callRecordId": {
    "description": "Path parameter: callRecordId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "required": [
   "callRecordId"
  ],
  "type": "object"
 },
 "name": "get-call-record-organizer"
}
