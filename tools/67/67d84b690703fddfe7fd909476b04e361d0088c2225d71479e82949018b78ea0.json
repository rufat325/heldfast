{
 "description": "Detects potential race conditions by finding network requests that were still in-flight when a user interaction action fired. Returns flagged actions with pending requests.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "trace_path": {
    "description": "Absolute path to trace.zip, or a URL (https://) to download it from",
    "type": "string"
   }
  },
  "required": [
   "trace_path"
  ],
  "type": "object"
 },
 "name": "analyze_race_conditions"
}
