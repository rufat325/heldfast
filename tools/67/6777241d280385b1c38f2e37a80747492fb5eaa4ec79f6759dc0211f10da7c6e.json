{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "get-custom-auth-extension"
 },
 "description": "Read the properties and relationships of a customAuthenticationExtension object. The following derived types are currently supported.\n\nTIP: Gets a specific custom authentication extension. Returns displayName, authenticationConfiguration, endpointConfiguration, and claimsForTokenConfiguration.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "customAuthenticationExtensionId": {
    "description": "Path parameter: customAuthenticationExtensionId",
    "type": "string"
   },
   "excludeResponse": {
    "description": "Exclude the full response body and only return success or failure",
    "type": "boolean"
   },
   "expand": {
    "description": "expand",
    "type": "string"
   },
   "select": {
    "description": "Comma-separated fields to return",
    "type": "string"
   }
  },
  "required": [
   "customAuthenticationExtensionId"
  ],
  "type": "object"
 },
 "name": "get-custom-auth-extension"
}
