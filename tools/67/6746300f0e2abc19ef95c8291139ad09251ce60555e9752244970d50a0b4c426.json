{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "\"What is [condition]\" / \"look up medical condition by name\" / \"find a disease called [X]\" / \"patient-friendly medical term for [Y]\" \u2014 search the NLM patient-friendly medical conditions vocabulary (~700 common conditions written for lay readers). Returns canonical names like \"Migraine\", \"Type 2 diabetes\". Use for symptom-to-condition lookup, intake forms, or simplifying clinical text for patients.",
 "inputSchema": {
  "examples": [
   {
    "count": 3,
    "terms": "migraine"
   }
  ],
  "properties": {
   "count": {
    "default": 7,
    "description": "Maximum matches to return. Default 7, max 500. Use 1\u20133 for typeahead UX, 20\u201350 for browsing.",
    "maximum": 500,
    "minimum": 1,
    "type": "number"
   },
   "df": {
    "description": "Comma-separated display fields to use in the `displays` array. Default varies per table; usually the canonical name.",
    "type": "string"
   },
   "ef": {
    "description": "Comma-separated extra fields to include per match. Field names vary per table; check NLM docs at clinicaltables.nlm.nih.gov.",
    "type": "string"
   },
   "terms": {
    "description": "Search query \u2014 prefix/contains match against canonical names. Whitespace-split into AND tokens. Example: \"migraine\".",
    "type": "string"
   }
  },
  "required": [
   "terms"
  ],
  "type": "object"
 },
 "name": "conditions",
 "title": "Conditions",
 "version": "v1.e1334568"
}
