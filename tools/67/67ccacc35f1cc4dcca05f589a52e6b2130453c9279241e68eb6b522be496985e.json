{
 "description": "Read one post whole: the body in Markdown (`conteudo`) and the author's formatting as safe HTML (`conteudo_html`), plus the link to the original. `ord` comes from feed_posts; 0 is the newest. Two bodies for the same post, both from the copy we stored when we READ the feed \u2014 a request never fetches the site: `conteudo_html` keeps the author's formatting through an allowlist (only known tags and attributes survive; script, style, iframe and event handlers are dropped with their content), and `conteudo` is the same text as Markdown \u2014 the shape an agent usually wants. Neither can carry script. 404 when the feed or the position is unknown.",
 "inputSchema": {
  "properties": {
   "api_pass": {
    "description": "MAT-only private pass: mat_<32 random hex>_<64 random hex>. Generate and save before buying.",
    "type": "string"
   },
   "id": {
    "description": "Feed id, prefix fd_.",
    "type": "string"
   },
   "ord": {
    "description": "Position of the post in the feed; 0 is the newest.",
    "type": "integer"
   },
   "retry_key": {
    "description": "Optional retry key, up to 80 ASCII characters; same read for five minutes.",
    "type": "string"
   }
  },
  "required": [
   "id",
   "ord"
  ],
  "type": "object"
 },
 "name": "feed_post"
}
