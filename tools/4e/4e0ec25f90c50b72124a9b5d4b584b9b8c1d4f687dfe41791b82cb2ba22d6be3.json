{
 "description": "File checksum. Work out the MD5, SHA-1, SHA-256 or SHA-512 of a file to check that what you downloaded is what was published. The file never leaves your device. Accepts */*; up to 500 MB per file.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "algorithm": {
    "default": "sha256",
    "description": "Which checksum to compute",
    "enum": [
     "md5",
     "sha1",
     "sha256",
     "sha512"
    ],
    "type": "string"
   },
   "outputDir": {
    "description": "Where to put the results. Defaults to the folder each source file came from. Existing files are never overwritten.",
    "type": "string"
   },
   "paths": {
    "description": "Paths to the files to process, absolute or relative to the working directory.",
    "items": {
     "type": "string"
    },
    "maxItems": 25,
    "minItems": 1,
    "type": "array"
   }
  },
  "required": [
   "paths"
  ],
  "type": "object"
 },
 "name": "utility_hash"
}
