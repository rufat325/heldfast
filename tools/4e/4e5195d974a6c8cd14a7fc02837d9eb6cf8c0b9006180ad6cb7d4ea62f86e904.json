{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "title": "Configure WordPress"
 },
 "description": "Configure a WordPress connection. Two modes: plugin mode (mode='plugin') uses the Web Resurrect Connector plugin; Basic Auth mode requires username + app_password.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "app_password": {
    "description": "WordPress application password (Basic Auth only)",
    "type": "string"
   },
   "mode": {
    "description": "Set to 'plugin' for plugin mode. Omit for Basic Auth.",
    "enum": [
     "plugin"
    ],
    "type": "string"
   },
   "post_as_draft": {
    "description": "Publish as draft by default (default: true)",
    "type": "boolean"
   },
   "site_url": {
    "description": "WordPress site URL (e.g. https://example.com)",
    "type": "string"
   },
   "username": {
    "description": "WordPress username (Basic Auth only)",
    "type": "string"
   }
  },
  "required": [
   "site_url"
  ],
  "type": "object"
 },
 "name": "wordpress_configure"
}
