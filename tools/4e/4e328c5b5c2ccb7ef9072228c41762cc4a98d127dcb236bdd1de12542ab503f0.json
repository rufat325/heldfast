{
 "description": "Fetch a source now instead of waiting for its schedule (needs a key; owner or admin).",
 "inputSchema": {
  "properties": {
   "source": {
    "description": "Source slug",
    "type": "string"
   }
  },
  "required": [
   "source"
  ],
  "type": "object"
 },
 "name": "run_source"
}
