{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Look up a quote and return a deep link to the Tewdy app where the user can confirm acceptance and proceed to payment. This tool deliberately does not accept or pay \u2014 quote acceptance and payment confirmation always happen on tewdy.com via the deep link, not in chat. Requires authentication.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "quote_id": {
    "maxLength": 120,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "quote_id"
  ],
  "type": "object"
 },
 "name": "accept_quote",
 "title": "Get accept-quote deep link"
}
