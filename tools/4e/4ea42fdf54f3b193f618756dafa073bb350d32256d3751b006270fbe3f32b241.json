{
 "description": "Generate a short-lived (10 minute) signed HTTPS URL for a completed transcode's output file. The job must be in `completed` status. Use this instead of the `output_url` field on the job object, which is a `gs://` URL that HTTP clients cannot fetch directly.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Completed transcode job UUID",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "get_download_url",
 "title": "Get Download URL"
}
