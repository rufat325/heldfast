{
 "description": "List backups belonging to a backup volume via the volume-scoped action (requires write access \u2014 use backup_list for read-only introspection). Longhorn docs: https://longhorn.io/docs/1.12.1/snapshots-and-backups/backup-and-restore/",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "name": {
    "type": "string"
   }
  },
  "required": [
   "name"
  ],
  "type": "object"
 },
 "name": "backupvolume_backup_list",
 "title": "List backups (via backup volume)"
}
