{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch the full time-series history for a specific series from DBnomics. Returns an array of {period, value} observations covering up to decades of data. Periods are formatted as YYYY for annual, YYYY-QN for quarterly, YYYY-MM for monthly. Example: provider=WB, dataset=WDI, series=A-US.NY.GDP.MKTP.CD \u2192 US nominal GDP in current USD annually. Example: provider=OECD, dataset=QNA, series=USA.B1GQ.LNBARSA.Q \u2192 US real GDP quarterly. Use last_n_periods to limit output to the most recent N observations. Covers data from IMF, World Bank, OECD, Eurostat, ECB, BIS, ILO, and 85+ other agencies. No API key required \u2014 open data under CC-BY 4.0.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "dataset_code": {
    "description": "Dataset code within the provider (e.g. WDI, QNA). Use dbnomics.datasets to list codes.",
    "minLength": 1,
    "type": "string"
   },
   "last_n_periods": {
    "description": "Return only the last N observations (most recent). Omit to return the full history.",
    "maximum": 1000,
    "minimum": 1,
    "type": "integer"
   },
   "provider_code": {
    "description": "DBnomics provider code (e.g. WB, OECD, IMF). Use dbnomics.providers to list codes.",
    "minLength": 1,
    "type": "string"
   },
   "series_code": {
    "description": "Series code within the dataset (e.g. A-US.NY.GDP.MKTP.CD for US annual nominal GDP in WB/WDI). Use dbnomics.series to browse available codes.",
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "provider_code",
   "dataset_code",
   "series_code"
  ],
  "type": "object"
 },
 "name": "dbnomics.data.fetch_series",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "DBnomics \u2014 Fetch Time-Series Data"
}
