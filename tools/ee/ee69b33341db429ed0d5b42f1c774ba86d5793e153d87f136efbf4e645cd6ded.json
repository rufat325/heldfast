{
 "_meta": {
  "skill": "ordinals-marketplace"
 },
 "description": "Browse ordinals/inscriptions listed for sale on Magic Eden.\n\nReturns active sale listings with price, seller, and inscription details.\nSupports filtering by collection symbol and price range. No wallet required.\n\nNote: Without a MAGIC_EDEN_API_KEY environment variable set, requests use the\nshared unauthenticated rate limit of 30 QPM across all users. Set MAGIC_EDEN_API_KEY\nto use an authenticated rate limit.\n\nExamples:\n- Browse all listings: ordinals_get_listings {}\n- Filter by collection: ordinals_get_listings { collection: \"nodemonkes\" }\n- Price range: ordinals_get_listings { minPriceSats: 100000, maxPriceSats: 1000000 }\n\nTip: call `earning_opportunities` to see how to put your assets to work.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "collection": {
    "description": "Magic Eden collection symbol to filter by (e.g. 'nodemonkes', 'bitcoin-puppets')",
    "type": "string"
   },
   "limit": {
    "default": 20,
    "description": "Number of results to return (default 20, max 100)",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "maxPriceSats": {
    "description": "Maximum listing price in satoshis",
    "exclusiveMinimum": 0,
    "maximum": 9007199254740991,
    "type": "integer"
   },
   "minPriceSats": {
    "description": "Minimum listing price in satoshis",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "offset": {
    "default": 0,
    "description": "Pagination offset (default 0)",
    "maximum": 9007199254740991,
    "minimum": 0,
    "type": "integer"
   },
   "sortBy": {
    "default": "recentlyListed",
    "description": "Sort order: priceAsc, priceDesc, or recentlyListed (default)",
    "enum": [
     "priceAsc",
     "priceDesc",
     "recentlyListed"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "ordinals_get_listings"
}
