{
 "description": "Cancel a pending or processing transcode job. Jobs that are already completed, failed, or canceled cannot be canceled and return an error.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "id": {
    "description": "Transcode job UUID to cancel",
    "type": "string"
   }
  },
  "required": [
   "id"
  ],
  "type": "object"
 },
 "name": "cancel_transcode",
 "title": "Cancel Transcode"
}
