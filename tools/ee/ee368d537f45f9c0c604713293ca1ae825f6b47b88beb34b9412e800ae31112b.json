{
 "description": "Track which tools are actually called vs loaded in a session. Register tools, log calls, and generate waste reports showing unused tools ranked by token cost.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "Action to perform",
    "enum": [
     "register_tool",
     "log_call",
     "report"
    ],
    "type": "string"
   },
   "session_id": {
    "description": "Unique session identifier",
    "type": "string"
   },
   "tool_name": {
    "description": "Tool name (required for register_tool and log_call)",
    "type": "string"
   },
   "tool_tokens": {
    "description": "Token cost of the tool (for register_tool)",
    "minimum": 0,
    "type": "integer"
   }
  },
  "required": [
   "session_id",
   "action"
  ],
  "type": "object"
 },
 "name": "analyze_session_usage"
}
