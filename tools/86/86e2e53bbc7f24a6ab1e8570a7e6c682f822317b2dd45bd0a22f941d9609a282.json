{
 "_meta": {
  "securitySchemes": [
   {
    "scopes": [
     "openid",
     "email",
     "profile"
    ],
    "type": "oauth2"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Scan a user-controlled public page and return an importable video URL found on it without creating media.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "confirmAuthorizedToScan": {
    "default": false,
    "description": "Set true only after the user confirms they control the page or are authorized to scan it.",
    "type": "boolean"
   },
   "url": {
    "description": "Video page URL or direct media URL to inspect for an importable video.",
    "format": "uri",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "media_import_scan_page",
 "outputSchema": {
  "additionalProperties": false,
  "properties": {
   "resolved": {
    "type": "object"
   }
  },
  "required": [
   "resolved"
  ],
  "type": "object"
 },
 "title": "Scan Page For Video"
}
