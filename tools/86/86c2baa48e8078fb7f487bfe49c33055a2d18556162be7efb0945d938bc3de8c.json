{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Search news"
 },
 "description": "Search financial news across FinTech Studios' curated archive and the open web. Returns articles with source, date, summary, and a citation. Costs ~3 credits (call estimate_endpoint_costs for exact pricing).",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "from_date": {
    "description": "ISO start date (YYYY-MM-DD)",
    "type": "string"
   },
   "per_page": {
    "default": 10,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "query": {
    "description": "Search query",
    "maxLength": 500,
    "minLength": 1,
    "type": "string"
   },
   "sources": {
    "description": "'curated' = licensed news archive, 'web' = open web search. Defaults to both.",
    "items": {
     "enum": [
      "curated",
      "web"
     ],
     "type": "string"
    },
    "type": "array"
   },
   "to_date": {
    "description": "ISO end date (YYYY-MM-DD)",
    "type": "string"
   }
  },
  "required": [
   "query"
  ],
  "type": "object"
 },
 "name": "search_news",
 "title": "Search news"
}
