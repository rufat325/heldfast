{
 "annotations": {
  "cost": {
   "all": 3
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Build a bounded FDA regulatory snapshot for one device company across 510(k), PMA, recalls, and MAUDE. Dataset name matching is imperfect and MAUDE counts are signals, not safety rates.",
 "inputSchema": {
  "examples": [
   {
    "company": "Dexcom",
    "limit_per_dataset": 5
   }
  ],
  "properties": {
   "company": {
    "description": "Company/manufacturer name.",
    "type": "string"
   },
   "limit_per_dataset": {
    "description": "Rows per dataset (1-20, default 5).",
    "type": "number"
   }
  },
  "required": [
   "company"
  ],
  "type": "object"
 },
 "name": "fda_device_company_profile",
 "outputSchema": {
  "properties": {
   "clearances_510k": {
    "type": "object"
   },
   "company": {
    "type": "string"
   },
   "interpretation": {
    "type": "string"
   },
   "maude_reports": {
    "type": "object"
   },
   "pma_decisions": {
    "type": "object"
   },
   "recalls": {
    "type": "object"
   }
  },
  "required": [
   "company",
   "clearances_510k",
   "pma_decisions",
   "recalls",
   "maude_reports",
   "interpretation"
  ],
  "type": "object"
 },
 "title": "Fda Device Company Profile",
 "version": "v1.93d118f3"
}
