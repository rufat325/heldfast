{
 "description": "Download one output of a finished LayerBack conversion to a local file. Formats: vsdx (Visio), pptx (PowerPoint), drawio, svg, ir (structured JSON), preview (PNG). Downloads are free and repeatable.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "format": {
    "default": "vsdx",
    "description": "Which output to download",
    "enum": [
     "vsdx",
     "pptx",
     "drawio",
     "svg",
     "ir",
     "preview"
    ],
    "type": "string"
   },
   "job_id": {
    "description": "The job id of a succeeded conversion",
    "type": "string"
   },
   "output_path": {
    "description": "Absolute path to write the file, e.g. /tmp/diagram.vsdx",
    "type": "string"
   }
  },
  "required": [
   "job_id",
   "output_path"
  ],
  "type": "object"
 },
 "name": "download_converted_diagram"
}
