{
 "annotations": {
  "cost": {
   "all": 1
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List the Chinese, Hong Kong, Taiwanese and overseas-Chinese news publishers Pipeworx has audited \u2014 106 of them \u2014 with each one's jurisdiction, publishing language, how its content is reachable, whether china_news_search can actually search it, and when its most recent indexed article was published. Use this before searching to see what coverage genuinely exists, instead of inferring it from an empty search result.",
 "inputSchema": {
  "examples": [
   {
    "searchable_only": true
   },
   {
    "region": "tw"
   }
  ],
  "properties": {
   "method": {
    "description": "Filter by how the publisher's content is reachable: \"gdelt-only\", \"native-rss\" or \"scrape\" (substring match).",
    "type": "string"
   },
   "region": {
    "description": "One of: cn | hk | tw | overseas \u2014 the publisher's own jurisdiction (cn = mainland China, hk = Hong Kong, tw = Taiwan, overseas = diaspora/foreign Chinese-language outlets). This is NOT the language it publishes in; use lang for that.",
    "type": "string"
   },
   "searchable_only": {
    "description": "true to list only the publishers china_news_search can actually search.",
    "type": [
     "boolean",
     "string"
    ]
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "china_news_outlets",
 "title": "China News Outlets",
 "version": "v1.923dc81b"
}
