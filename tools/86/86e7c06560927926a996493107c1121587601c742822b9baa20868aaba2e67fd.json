{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get historical daily hospital/ICU occupancy and weekly new admission rates per 100k for COVID-19 by country from ECDC surveillance data. Filter by country name, optionally by one of four indicators (daily hospital occupancy, daily ICU occupancy, weekly hospital admissions per 100k, weekly ICU admissions per 100k). Covers 2020-W01 through 2023-W47 only \u2014 ECDC discontinued routine COVID-19 reporting in December 2023, so this reflects the final frozen historical dataset, not live surveillance. Data: ECDC opendata, CC BY 4.0, no auth required.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "country": {
    "description": "Country name to filter by (e.g. \"Germany\"). Required \u2014 the upstream dataset has no server-side query, so this narrows the full historical dump.",
    "maxLength": 60,
    "minLength": 2,
    "type": "string"
   },
   "indicator": {
    "description": "Filter to a single indicator series. Omit to return all four indicators.",
    "enum": [
     "Daily hospital occupancy",
     "Daily ICU occupancy",
     "Weekly new hospital admissions per 100k",
     "Weekly new ICU admissions per 100k"
    ],
    "type": "string"
   },
   "limit": {
    "description": "Maximum number of records to return (default 100, max 500).",
    "maximum": 500,
    "minimum": 1,
    "type": "integer"
   }
  },
  "required": [
   "country"
  ],
  "type": "object"
 },
 "name": "ecdc-surveillance.covid.hospital_icu",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "Get ECDC COVID-19 Hospital & ICU Rates"
}
