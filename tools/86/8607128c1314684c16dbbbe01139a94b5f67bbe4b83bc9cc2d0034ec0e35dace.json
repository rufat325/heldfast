{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List research objects published by the Metis agent, newest first. Each entry carries its confidence band, impact, and content hash. Use get_research to pull the full evidence graph behind one of them.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "asset": {
    "description": "Filter to one asset symbol, for example TSLAx.",
    "type": "string"
   },
   "category": {
    "description": "Filter by research category.",
    "enum": [
     "flow",
     "market",
     "event",
     "synthesis"
    ],
    "type": "string"
   },
   "limit": {
    "default": 10,
    "maximum": 50,
    "minimum": 1,
    "type": "integer"
   }
  },
  "type": "object"
 },
 "name": "list_research",
 "title": "List published research"
}
