{
 "description": "Ask the graph a question, e.g. 'what depends on postgres?' or 'what broke?'.",
 "inputSchema": {
  "properties": {
   "question": {
    "title": "Question",
    "type": "string"
   }
  },
  "required": [
   "question"
  ],
  "title": "query_infra_graphArguments",
  "type": "object"
 },
 "name": "query_infra_graph",
 "outputSchema": {
  "properties": {
   "result": {
    "title": "Result",
    "type": "string"
   }
  },
  "required": [
   "result"
  ],
  "title": "query_infra_graphOutput",
  "type": "object"
 }
}
