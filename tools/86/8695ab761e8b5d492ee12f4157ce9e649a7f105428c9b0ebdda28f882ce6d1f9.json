{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Get aggregated statistics across DataCite research outputs. Returns total DOI count, breakdown by resource type (datasets 70M+, software, preprints, journal articles, dissertations), registration year distribution, top contributing organizations (arXiv, CERN/Zenodo, Figshare, Mendeley), and top repositories. Optionally scope to a keyword query or specific resource type/year to get focused analytics. Useful for understanding the research landscape in a topic area \u2014 e.g. how many climate datasets exist, which repositories host the most neuroscience data.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "publication_year": {
    "description": "Scope statistics to a specific publication year.",
    "maximum": 2030,
    "minimum": 1900,
    "type": "integer"
   },
   "query": {
    "description": "Optional keyword query to scope the statistics (e.g. \"machine learning\" returns stats only for ML-related DOIs). If omitted, stats cover all 70M+ DataCite works.",
    "type": "string"
   },
   "resource_type": {
    "description": "Scope statistics to a single resource type (e.g. \"dataset\" to see dataset-specific provider breakdowns).",
    "enum": [
     "dataset",
     "software",
     "text",
     "image",
     "audiovisual",
     "collection",
     "preprint",
     "journal-article",
     "conference-paper",
     "dissertation",
     "report",
     "model",
     "workflow",
     "computational-notebook",
     "other"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "datacite.works.stats",
 "outputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "error": {
    "description": "Present only when the call failed. Includes error code, message, request_id, and any provider-specific extras."
   },
   "result": {
    "description": "Tool response payload. Shape varies per tool \u2014 consult the tool description and inputSchema. May be an object, array, string, or number depending on the upstream provider response."
   }
  },
  "type": "object"
 },
 "title": "DataCite Works Statistics"
}
