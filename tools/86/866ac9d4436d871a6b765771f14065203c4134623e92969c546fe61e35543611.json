{
 "description": "Sales analytics via ShopifyQL. Default columns: total_sales, gross_sales, net_sales, total_orders, average_order_value over the date range. Pass `query` to override with raw ShopifyQL.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "endDate": {
    "description": "End date (YYYY-MM-DD)",
    "type": "string"
   },
   "granularity": {
    "default": "DAY",
    "enum": [
     "HOUR",
     "DAY",
     "WEEK",
     "MONTH",
     "QUARTER",
     "YEAR"
    ],
    "type": "string"
   },
   "query": {
    "description": "Optional raw ShopifyQL query to override the default",
    "type": "string"
   },
   "startDate": {
    "description": "Start date (YYYY-MM-DD)",
    "type": "string"
   }
  },
  "required": [
   "startDate",
   "endDate"
  ],
  "type": "object"
 },
 "name": "getSalesReport"
}
