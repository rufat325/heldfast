{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Aggregate matching datasets by a facet \u2014 top values of a category field (e.g. \"agg-mainTopic\", \"agg-author\", \"agg-method\", \"agg-location\", \"agg-campaign\") with counts. Optionally scoped to a full-text query.",
 "inputSchema": {
  "examples": [
   {
    "field": "source"
   },
   {
    "field": "topic",
    "size": 15
   }
  ],
  "properties": {
   "field": {
    "description": "A keyword facet field, e.g. \"agg-mainTopic\", \"agg-author\".",
    "type": "string"
   },
   "query": {
    "description": "Optional full-text scope for the aggregation.",
    "type": "string"
   },
   "size": {
    "description": "Top-N buckets (default 25).",
    "type": "number"
   }
  },
  "required": [
   "field"
  ],
  "type": "object"
 },
 "name": "facets",
 "outputSchema": {
  "description": "Facet aggregation results on specified field",
  "properties": {
   "_shards": {
    "type": "object"
   },
   "aggregations": {
    "description": "Aggregation results",
    "properties": {
     "facet": {
      "properties": {
       "buckets": {
        "description": "Facet buckets",
        "items": {
         "properties": {
          "doc_count": {
           "description": "Document count",
           "type": "number"
          },
          "key": {
           "description": "Facet value",
           "type": "string"
          }
         },
         "type": "object"
        },
        "type": "array"
       }
      },
      "type": "object"
     }
    },
    "type": "object"
   },
   "hits": {
    "properties": {
     "hits": {
      "type": "array"
     },
     "max_score": {
      "type": [
       "number",
       "null"
      ]
     },
     "total": {
      "type": "object"
     }
    },
    "type": "object"
   },
   "timed_out": {
    "type": "boolean"
   },
   "took": {
    "type": "number"
   }
  },
  "type": "object"
 },
 "title": "Facets",
 "version": "v1.9409fe0f"
}
