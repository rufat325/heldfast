{
 "_meta": {
  "securitySchemes": [
   {
    "scopes": [
     "email"
    ],
    "type": "oauth2"
   }
  ]
 },
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Full detail for up to 10 job ids in ONE call \u2014 the shortlist form of get_job. Each id answers with a card plus its description; ids that closed, aged out or were never on this board come back in `unavailable` with the reason named, so one dead id never costs you the other nine. Set includeDescription=false for cards and freshness only (much smaller, and no vendor fetch). Needs a key or a sign-in.",
 "inputSchema": {
  "properties": {
   "ids": {
    "description": "Job ids from search_jobs. Up to 10 per call \u2014 each one is a separate detail read that may fetch the employer's page.",
    "items": {
     "type": "string"
    },
    "maxItems": 10,
    "type": "array"
   },
   "includeDescription": {
    "description": "Default true. Descriptions are capped at 8,000 characters here; call get_job for the whole text of one.",
    "type": "boolean"
   }
  },
  "required": [
   "ids"
  ],
  "type": "object"
 },
 "name": "get_jobs",
 "outputSchema": {
  "additionalProperties": true,
  "properties": {
   "jobs": {
    "items": {
     "additionalProperties": true,
     "properties": {
      "agency": {
       "description": "Present and true when the posting comes from a staffing/recruiting agency.",
       "type": "boolean"
      },
      "agentReady": {
       "description": "True when request_application can submit to this hiring system.",
       "type": "boolean"
      },
      "applyUrl": {
       "type": [
        "string",
        "null"
       ]
      },
      "category": {
       "type": [
        "string",
        "null"
       ]
      },
      "company": {
       "type": [
        "string",
        "null"
       ]
      },
      "companyToken": {
       "description": "The employer handle; pass it back in search_jobs `companies`.",
       "type": [
        "string",
        "null"
       ]
      },
      "country": {
       "description": "ISO-2.",
       "type": [
        "string",
        "null"
       ]
      },
      "department": {
       "description": "The employer's own team name. ABSENT when the posting carries none.",
       "type": "string"
      },
      "employmentType": {
       "type": [
        "string",
        "null"
       ]
      },
      "experienceBand": {
       "description": "ABSENT when the posting's seniority could not be read.",
       "enum": [
        "entry",
        "mid",
        "senior",
        "expert"
       ],
       "type": "string"
      },
      "id": {
       "description": "vendor:employer:externalId \u2014 the id every other tool takes.",
       "type": "string"
      },
      "location": {
       "type": [
        "string",
        "null"
       ]
      },
      "minYears": {
       "description": "Years of experience the posting asks for. ABSENT when it names none (~71%).",
       "type": "integer"
      },
      "postedAt": {
       "description": "The employer's own date, ISO-8601. Null when the feed carries none \u2014 never the date we first saw it.",
       "type": [
        "string",
        "null"
       ]
      },
      "recheckedAt": {
       "description": "When the employer's feed was last fetched and still carried this employer's board.",
       "type": "string"
      },
      "salary": {
       "description": "The employer's own pay text, verbatim and unparsed.",
       "type": [
        "string",
        "null"
       ]
      },
      "salaryCurrency": {
       "description": "ISO-4217, as stated. ABSENT when unstated.",
       "type": "string"
      },
      "salaryMaxAnnual": {
       "description": "Annual USD-equivalent ceiling. ABSENT when unstated.",
       "type": "number"
      },
      "salaryMinAnnual": {
       "description": "Annual USD-equivalent floor, parsed by the board. ABSENT when the posting states no pay \u2014 absence is not zero.",
       "type": "number"
      },
      "salaryPeriod": {
       "description": "The period the employer stated: hour, month, year. ABSENT when unstated (~89% of the board).",
       "type": "string"
      },
      "title": {
       "type": [
        "string",
        "null"
       ]
      },
      "workMode": {
       "description": "Stated or inferred from title/location; null when neither says.",
       "enum": [
        "remote",
        "hybrid",
        "onsite",
        null
       ],
       "type": [
        "string",
        "null"
       ]
      }
     },
     "required": [
      "id",
      "agentReady"
     ],
     "type": "object"
    },
    "type": "array"
   },
   "notFetched": {
    "description": "Ids past the per-call cap \u2014 sent, not read. Call again with these.",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "requested": {
    "type": "integer"
   },
   "returned": {
    "type": "integer"
   },
   "unavailable": {
    "items": {
     "additionalProperties": true,
     "properties": {
      "agedOut": {
       "additionalProperties": true,
       "type": "object"
      },
      "closed": {
       "additionalProperties": true,
       "type": "object"
      },
      "id": {
       "type": "string"
      },
      "reason": {
       "enum": [
        "closed",
        "agedOut",
        "notFound",
        "error"
       ],
       "type": "string"
      }
     },
     "required": [
      "id",
      "reason"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "jobs",
   "unavailable"
  ],
  "type": "object"
 },
 "securitySchemes": [
  {
   "scopes": [
    "email"
   ],
   "type": "oauth2"
  }
 ],
 "title": "Get several jobs"
}
