{
 "annotations": {
  "destructiveHint": true,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Change a Proxmox VPS power state: start, shutdown (graceful ACPI), reboot, or stop (hard power-off). shutdown/stop take the server offline \u2014 confirm with the customer first.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "action": {
    "description": "Power action to perform.",
    "enum": [
     "start",
     "shutdown",
     "reboot",
     "stop"
    ],
    "type": "string"
   },
   "service_id": {
    "description": "VPS service id (numeric; from impreza_list_services).",
    "type": "string"
   }
  },
  "required": [
   "service_id",
   "action"
  ],
  "type": "object"
 },
 "name": "impreza_vps_power"
}
