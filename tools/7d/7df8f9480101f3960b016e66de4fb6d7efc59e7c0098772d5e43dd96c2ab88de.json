{
 "description": "Append a page, or insert one before another. TWO WAYS, and the CONTENT decides: pass a templateId from list_templates for a page that arrives COMPOSED \u2014 heading, blocks, shape and design already chosen, its text placeholder until set_page fills every slot in one call; or pass no templateId for a BLANK page you compose yourself with add_block, picking each block, its shape and its style for what the content is. A template is a STARTING POINT, never a restriction: add_block adds to it, set_block swaps or restyles any block it holds, remove takes one away. Reach for a template when one fits the content; build the page when the content wants what the shelf does not have \u2014 a deck mixes both, and opens on a COVER. Either way this INSERTS the page: never add one and then a template on top of it.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": {},
  "properties": {
   "align": {
    "anyOf": [
     {
      "enum": [
       "top",
       "middle",
       "bottom"
      ],
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "Where THIS page's content sits in the frame's free space \u2014 `top`, `middle` or `bottom` \u2014 overriding the document's. A cover or a divider reads best `bottom`; leave every other page to inherit. null goes back to inheriting."
   },
   "before": {
    "description": "Page id to insert before. Omit to append at the end. Never an index.",
    "type": "string"
   },
   "eyebrow": {
    "description": "Small label above the title \u2014 section or number. Pass '' to remove it.",
    "type": "string"
   },
   "mode": {
    "anyOf": [
     {
      "enum": [
       "light",
       "dark"
      ],
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "Override the document's light/dark for THIS page only. null to go back to inheriting."
   },
   "notes": {
    "anyOf": [
     {
      "type": "string"
     },
     {
      "type": "null"
     }
    ],
    "description": "The presenter's own text for this page, as markdown \u2014 never shown on the slide itself, only in the presenter view. Plain **bold** and lists work; pictures do not. null removes it."
   },
   "subtitle": {
    "description": "A line under the title. Pass '' to remove it.",
    "type": "string"
   },
   "templateId": {
    "description": "A template id from list_templates. Omit for a blank page of your own composition.",
    "type": "string"
   },
   "title": {
    "description": "The page's heading. Pass '' to remove it.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "add_page",
 "title": "Add a page"
}
