{
 "description": "Batch-fetch multiple objects by their IDs in one call.",
 "inputSchema": {
  "properties": {
   "objectIds": {
    "description": "Array of object IDs to fetch",
    "items": {
     "type": "string"
    },
    "type": "array"
   },
   "showContent": {
    "description": "Include content (default: true)",
    "type": "boolean"
   },
   "showType": {
    "description": "Include type (default: true)",
    "type": "boolean"
   }
  },
  "required": [
   "objectIds"
  ],
  "type": "object"
 },
 "name": "multi_get_objects"
}
