{
 "annotations": {
  "readOnlyHint": true
 },
 "description": "Return the on-demand contribution strategy snapshot via the typed `computeStrategy` function. Always runs against local state regardless of the auto-display cadence gate. Returns `strategy: null` with a human-readable `message` when the merged-PR floor is not met. Read-only; no API calls, no state mutation.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {},
  "type": "object"
 },
 "name": "strategy"
}
