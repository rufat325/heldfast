{
 "description": "List matters in flight for the calling buyer organization. Useful for status sweeps and pipeline reporting from inside an AI workflow.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "limit": {
    "default": 20,
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "status": {
    "default": "all",
    "enum": [
     "open",
     "awarded",
     "in_progress",
     "completed",
     "all"
    ],
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "scope_list_matters"
}
