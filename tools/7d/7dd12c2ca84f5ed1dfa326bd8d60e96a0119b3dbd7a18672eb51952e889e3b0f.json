{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Read row-level data from a tabular resource (one with a tabular_data relationship). Returns JSON:API \"row\" objects whose attributes map column names (col1, col2, ...) to {repr, val} pairs. Supports paging and full-text row filtering via q. Use list_resources first to find a tabular resource id.",
 "inputSchema": {
  "examples": [
   {
    "resource_id": "22509"
   },
   {
    "page": 1,
    "per_page": 50,
    "q": "AUSTRIA",
    "resource_id": "22509"
   }
  ],
  "properties": {
   "page": {
    "description": "Page number (default 1).",
    "type": "integer"
   },
   "per_page": {
    "description": "Rows per page, 1-100 (default 20).",
    "type": "integer"
   },
   "q": {
    "description": "Full-text filter across row cells (Polish or English), e.g. \"AUSTRIA\".",
    "type": "string"
   },
   "resource_id": {
    "description": "Resource numeric id, e.g. \"22509\".",
    "type": "string"
   }
  },
  "required": [
   "resource_id"
  ],
  "type": "object"
 },
 "name": "resource_data",
 "title": "Resource Data",
 "version": "v1.568877cc"
}
