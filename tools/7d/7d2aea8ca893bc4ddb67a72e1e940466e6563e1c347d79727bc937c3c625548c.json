{
 "description": "Get creators with rising momentum \u2014 those with consistent breakout frequency + intensity over the window (steady surgers, not one-offs), ranked. Each returns { rank, name, slug, handle, niche, vertical, subscribers, crossPlatformReach, audienceRatePerHour (followers/hour), breakoutEpisodes (count of breakout content in window), peakRelativeScore }. The \"who is heating up\" list. Supports `limit`, `niche`, `minSubs`, `maxSubs`.",
 "inputSchema": {
  "properties": {
   "limit": {
    "description": "How many creators to return.",
    "type": "integer"
   },
   "maxSubs": {
    "description": "Optional \u2014 maximum subscriber count (find smaller creators about to pop).",
    "type": "integer"
   },
   "minSubs": {
    "description": "Optional \u2014 minimum subscriber count.",
    "type": "integer"
   },
   "niche": {
    "description": "Optional niche filter.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "rising_creators"
}
