{
 "description": "Create, list, inspect, cancel or download a complete farm archive of data you can access. No subscription needed. Downloads write a new private ZIP at output_path on this MCP host. Other members\u2019 private conversations and account credentials are excluded.",
 "inputSchema": {
  "properties": {
   "export_id": {
    "type": "string"
   },
   "farm_id": {
    "type": "string"
   },
   "operation": {
    "enum": [
     "create",
     "list",
     "status",
     "cancel",
     "download"
    ],
    "type": "string"
   },
   "output_path": {
    "type": "string"
   }
  },
  "required": [
   "operation"
  ],
  "type": "object"
 },
 "name": "farm_archive"
}
