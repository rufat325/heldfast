{
 "annotations": {
  "readOnlyHint": true,
  "title": "get_chute_session"
 },
 "description": "Get a single chute session by its UUID, including its widget config and the animals processed (entries).",
 "inputSchema": {
  "properties": {
   "farm_id": {
    "description": "The ID of the farm (optional if a default is set).",
    "type": "string"
   },
   "session_id": {
    "description": "The chute session UUID.",
    "type": "string"
   }
  },
  "required": [
   "session_id"
  ],
  "type": "object"
 },
 "name": "get_chute_session"
}
