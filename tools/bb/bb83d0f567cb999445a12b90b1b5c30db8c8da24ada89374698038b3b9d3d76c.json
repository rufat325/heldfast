{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Find upcoming events across the \u00cele-de-France / Greater Paris region (Paris + suburbs). Filter by keyword, city/commune, free admission, and date window. Returns events sorted by start date. Titles/descriptions are in French.",
 "inputSchema": {
  "examples": [
   {
    "_apiKey": "your-idf-events-api-key",
    "query": "concert"
   },
   {
    "_apiKey": "your-idf-events-api-key",
    "city": "Paris",
    "free_only": true,
    "from": "2025-01-15",
    "limit": 10,
    "query": "exposition",
    "to": "2025-02-28"
   }
  ],
  "properties": {
   "city": {
    "description": "Filter to one commune, e.g. \"Paris\", \"Versailles\", \"Saint-Denis\".",
    "type": "string"
   },
   "free_only": {
    "description": "If true, best-effort free events (conditions mention \"gratuit\").",
    "type": "boolean"
   },
   "from": {
    "description": "Include events on/after this date YYYY-MM-DD (default: today).",
    "type": "string"
   },
   "limit": {
    "description": "Max events (1-50, default 20).",
    "type": "number"
   },
   "offset": {
    "description": "Pagination offset (default 0).",
    "type": "number"
   },
   "query": {
    "description": "Keyword (full-text), e.g. \"concert\", \"exposition\", \"th\u00e9\u00e2tre\".",
    "type": "string"
   },
   "to": {
    "description": "Include events starting on/before this date YYYY-MM-DD.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "events",
 "title": "Events",
 "version": "v1.7f40ae5e"
}
