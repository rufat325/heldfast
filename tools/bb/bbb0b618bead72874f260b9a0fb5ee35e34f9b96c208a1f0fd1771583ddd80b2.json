{
 "description": "Get the total number of unread notifications (activity + broadcasts) for the current user.",
 "inputSchema": {
  "properties": {
   "workspaceId": {
    "description": "Workspace ID. Defaults to configured workspace.",
    "type": "string"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "get_unread_count"
}
