{
 "annotations": {
  "cost": {
   "all": 5
  },
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Check if a URL was archived and retrieve the closest snapshot. Returns capture dates and direct link to the archived version.",
 "inputSchema": {
  "examples": [
   {
    "url": "https://example.com/some-page"
   },
   {
    "url": "https://www.cnn.com/2001/09/11/"
   }
  ],
  "properties": {
   "url": {
    "description": "The URL to look up (e.g., \"https://example.com/some-page\")",
    "type": "string"
   }
  },
  "required": [
   "url"
  ],
  "type": "object"
 },
 "name": "wayback_check",
 "outputSchema": {
  "properties": {
   "archived": {
    "description": "Whether the URL has been archived",
    "type": "boolean"
   },
   "snapshot_url": {
    "description": "Direct URL to the archived snapshot",
    "type": [
     "string",
     "null"
    ]
   },
   "status": {
    "description": "Status code of the snapshot",
    "type": [
     "string",
     "null"
    ]
   },
   "timestamp": {
    "description": "Timestamp of the closest snapshot",
    "type": [
     "string",
     "null"
    ]
   },
   "url": {
    "description": "The queried URL",
    "type": "string"
   }
  },
  "required": [
   "url",
   "archived",
   "snapshot_url",
   "timestamp",
   "status"
  ],
  "type": "object"
 },
 "title": "Wayback Check",
 "version": "v1.3d3f6691"
}
