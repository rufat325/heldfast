{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": false,
  "openWorldHint": false,
  "readOnlyHint": false
 },
 "description": "Confirm both independent one-run choices after the catalog was displayed: (1) method seats via IDs/select_all/text and (2) analyst_scope=core|all. Full runs reject an omitted analyst scope. Returns a one-time receipt binding both choices and the pace.",
 "inputSchema": {
  "oneOf": [
   {
    "not": {
     "anyOf": [
      {
       "required": [
        "select_all"
       ]
      },
      {
       "required": [
        "selection"
       ]
      }
     ]
    },
    "required": [
     "selected_master_ids"
    ]
   },
   {
    "not": {
     "anyOf": [
      {
       "required": [
        "selected_master_ids"
       ]
      },
      {
       "required": [
        "selection"
       ]
      }
     ]
    },
    "required": [
     "select_all"
    ]
   },
   {
    "not": {
     "anyOf": [
      {
       "required": [
        "selected_master_ids"
       ]
      },
      {
       "required": [
        "select_all"
       ]
      }
     ]
    },
    "required": [
     "selection"
    ]
   }
  ],
  "properties": {
   "analyst_scope": {
    "description": "Required for full: core runs 8 analyst seats; all runs all 11. Separate from select_all, which selects method seats only. Omit for quick.",
    "enum": [
     "core",
     "all"
    ],
    "type": "string"
   },
   "catalog_hash": {
    "description": "The catalog_hash returned by begin_council_selection.",
    "type": "string"
   },
   "council_pace": {
    "description": "The depth tier the user picked from the pace_options this selection returned. Full only; omit to accept the default (normal, 30-minute persistence ceiling; observed completion remains unvalidated until live evidence exists). Binds into the receipt, so an execution call may repeat it but never change it.",
    "enum": [
     "fast",
     "normal",
     "slow"
    ],
    "type": "string"
   },
   "decision_context_hash": {
    "description": "Required when begin_council_selection returned a calibrated decision_context_hash. It acknowledges the exact objective, horizon, PM rubric requirement, and method contribution classes displayed, including fail-closed panels with no recommendation_hash.",
    "pattern": "^sha256:[a-f0-9]{64}$",
    "type": "string"
   },
   "display_ack": {
    "description": "Must be true after the host displayed the catalog to the user.",
    "type": "boolean"
   },
   "recommendation_hash": {
    "description": "Required only when begin_council_selection returned a non-null advisory recommendation_hash. It acknowledges the exact 26-decision recommendation displayed; it does not select seats.",
    "pattern": "^sha256:[a-f0-9]{64}$",
    "type": "string"
   },
   "select_all": {
    "const": true,
    "type": "boolean"
   },
   "selected_master_ids": {
    "items": {
     "enum": [
      "master_buffett",
      "master_graham",
      "master_simons",
      "master_soros",
      "master_cathie_wood",
      "master_druckenmiller",
      "master_fisher",
      "master_munger",
      "master_thorp",
      "master_asness",
      "master_dalio",
      "master_duan_yongping",
      "master_jhunjhunwala",
      "master_lynch",
      "master_forensic_short",
      "master_li_lu",
      "master_marks",
      "master_burry",
      "master_klarman",
      "master_pabrai",
      "master_ackman",
      "master_damodaran",
      "master_taleb",
      "master_natenberg",
      "master_sinclair",
      "master_bogle"
     ],
     "type": "string"
    },
    "minItems": 1,
    "type": "array",
    "uniqueItems": true
   },
   "selection": {
    "description": "Text fallback: stable numbers/ranges/IDs, or all.",
    "minLength": 1,
    "type": "string"
   },
   "selection_id": {
    "type": "string"
   }
  },
  "required": [
   "selection_id",
   "catalog_hash",
   "display_ack"
  ],
  "type": "object"
 },
 "name": "confirm_master_selection",
 "outputSchema": {
  "additionalProperties": true,
  "type": "object"
 },
 "title": "Confirm Council Seats"
}
