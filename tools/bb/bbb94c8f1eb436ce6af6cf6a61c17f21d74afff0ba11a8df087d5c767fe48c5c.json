{
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": true
 },
 "description": "List requirements/flows with weak or missing test evidence, plus top_risk_gaps for unproven code symbols ranked by the OrangePro Risk Score (Probability \u00d7 Impact \u00d7 DetectionDifficulty). Risk is prioritization only; it never changes coverage/proof status.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "limit": {
    "description": "Max gaps to return. Default 10.",
    "maximum": 100,
    "minimum": 1,
    "type": "integer"
   },
   "min_priority": {
    "description": "Minimum priority to include (low|medium|high|critical).",
    "type": "string"
   },
   "workspace": {
    "description": "Local workspace root. Defaults to the current directory.",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "orangepro_find_test_gaps",
 "title": "Find test gaps"
}
