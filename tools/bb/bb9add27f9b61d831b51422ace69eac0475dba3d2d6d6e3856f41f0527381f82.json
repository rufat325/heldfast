{
 "_meta": {
  "xdata.kr/credential": {
   "providers": [
    "kb_biz_card"
   ],
   "register_url": "https://console.xdata.kr/credentials?link=1&priority=kb_biz_card",
   "required": true,
   "windows_pc_needed": true
  }
 },
 "annotations": {
  "destructiveHint": false,
  "readOnlyHint": false
 },
 "description": "Call this FIRST before any other tool for this provider. Create a KB Biz Card certificate/NPKI session from a registered account link.",
 "inputSchema": {
  "properties": {
   "account_link_id": {
    "description": "Account link ID (UUID v4). Optional \u2014 the server auto-resolves from your API key when omitted. Provide explicitly only when you have multiple certificates for the same provider. Each institution has a separate ID (Hometax != NHIS != NPS != FourInsure).",
    "type": "string"
   },
   "auth_provider": {
    "description": "Authentication provider; default npki_delfino",
    "type": "string"
   }
  },
  "type": "object"
 },
 "name": "kb_biz_card__session__login",
 "securitySchemes": [
  {
   "scopes": [
    "mcp:tools:list",
    "mcp:tools:call"
   ],
   "type": "oauth2"
  }
 ],
 "title": "KB\uad6d\ubbfc\uce74\ub4dc \uae30\uc5c5 \ub85c\uadf8\uc778"
}
