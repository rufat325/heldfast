{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "fyyd, the open podcast directory \u2014 search podcast shows by title term, optionally restricted to a language code, up to 30 per page. Returns each show's fyyd podcast id, title, author, description, RSS feed URL, artwork and episode count. Answers which podcast shows exist under a name and where the RSS feed is.",
 "inputSchema": {
  "examples": [
   {
    "term": "technology"
   },
   {
    "count": 20,
    "lang": "en",
    "page": 1,
    "term": "true crime"
   }
  ],
  "properties": {
   "count": {
    "description": "1-30 (default 10)",
    "type": "number"
   },
   "lang": {
    "description": "ISO 639-1 language filter (e.g. en, de)",
    "type": "string"
   },
   "page": {
    "type": "number"
   },
   "term": {
    "type": "string"
   }
  },
  "required": [
   "term"
  ],
  "type": "object"
 },
 "name": "search_podcasts",
 "outputSchema": {
  "description": "Search results for podcasts matching the term",
  "type": "object"
 },
 "title": "Search Podcasts",
 "version": "v1.fe99eee9"
}
