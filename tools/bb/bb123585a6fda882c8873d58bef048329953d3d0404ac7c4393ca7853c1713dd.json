{
 "annotations": {
  "destructiveHint": false,
  "openWorldHint": true,
  "readOnlyHint": false,
  "title": "Create play session"
 },
 "description": "Mint a play session that returns signed, time-gated playback URLs. Scope is \"collection\" or \"track\", and exactly one of collection_id / track_id must be provided. (Playlist scope is reserved for a future release and is not supported yet.)",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "collection_id": {
    "format": "uuid",
    "type": "string"
   },
   "scope": {
    "enum": [
     "collection",
     "track"
    ],
    "type": "string"
   },
   "track_id": {
    "format": "uuid",
    "type": "string"
   },
   "variants": {
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "scope"
  ],
  "type": "object"
 },
 "name": "adn_create_play_session"
}
