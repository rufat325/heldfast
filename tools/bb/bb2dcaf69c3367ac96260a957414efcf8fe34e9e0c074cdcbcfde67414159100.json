{
 "annotations": {
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "List Cloudflare Tunnels (cloudflared) in an account.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "account_id": {
    "description": "Cloudflare account ID. Falls back to CLOUDFLARE_ACCOUNT_ID env var when omitted.",
    "type": "string"
   },
   "is_deleted": {
    "description": "Include deleted tunnels. Default false (active only).",
    "type": "boolean"
   }
  },
  "type": "object"
 },
 "name": "list_tunnels"
}
