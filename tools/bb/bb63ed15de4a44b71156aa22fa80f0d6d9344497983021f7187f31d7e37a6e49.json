{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true
 },
 "description": "Fetch all fake blog posts for testing and prototyping. Returns post ID, user ID, title, and body text.",
 "inputSchema": {
  "examples": [
   {
    "limit": 10
   },
   {
    "limit": 25
   }
  ],
  "properties": {
   "limit": {
    "description": "Maximum number of posts to return (default 10, max 100).",
    "type": "number"
   }
  },
  "required": [],
  "type": "object"
 },
 "name": "get_posts",
 "outputSchema": {
  "properties": {
   "count": {
    "description": "Number of posts returned",
    "type": "number"
   },
   "posts": {
    "description": "Array of blog posts",
    "items": {
     "properties": {
      "body": {
       "description": "Post body text",
       "type": "string"
      },
      "id": {
       "description": "Post ID",
       "type": "number"
      },
      "title": {
       "description": "Post title",
       "type": "string"
      },
      "user_id": {
       "description": "ID of the user who created the post",
       "type": "number"
      }
     },
     "required": [
      "id",
      "user_id",
      "title",
      "body"
     ],
     "type": "object"
    },
    "type": "array"
   }
  },
  "required": [
   "count",
   "posts"
  ],
  "type": "object"
 },
 "title": "Get Posts",
 "version": "v1.861b2f28"
}
