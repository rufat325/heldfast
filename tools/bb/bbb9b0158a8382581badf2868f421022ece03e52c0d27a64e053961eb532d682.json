{
 "description": "Importa un Postman Environment (JSON) como entorno local. Soporta variables con valores initial/current.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "activate": {
    "description": "Activar el entorno importado como entorno activo (default: false)",
    "type": "boolean"
   },
   "file": {
    "description": "Ruta al archivo .postman_environment.json exportado de Postman",
    "type": "string"
   },
   "name": {
    "description": "Nombre para el entorno (default: usa el nombre del archivo Postman)",
    "type": "string"
   },
   "overwrite": {
    "description": "Sobreescribir si ya existe un entorno con el mismo nombre (default: false)",
    "type": "boolean"
   }
  },
  "required": [
   "file"
  ],
  "type": "object"
 },
 "name": "import_postman_environment"
}
