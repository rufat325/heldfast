{
 "annotations": {
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": true,
  "title": "Get category-author mapping"
 },
 "description": "Get the current category-to-author mapping for a WordPress domain. The mapping determines which author is assigned when publishing a page in a given category.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
   "domain": {
    "description": "WordPress domain (e.g. example.com)",
    "type": "string"
   }
  },
  "required": [
   "domain"
  ],
  "type": "object"
 },
 "name": "wordpress_get_mapping"
}
