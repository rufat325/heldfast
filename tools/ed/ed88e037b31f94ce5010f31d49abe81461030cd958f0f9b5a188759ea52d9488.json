{
 "annotations": {
  "openWorldHint": false,
  "readOnlyHint": true
 },
 "description": "Get symbols from multiple files in a single call. Returns: map of file path to symbol tree for each file. More efficient than calling gdscript_symbols repeatedly. Use to understand the structure of multiple files at once.",
 "inputSchema": {
  "properties": {
   "files": {
    "description": "List of absolute or relative paths to .gd files",
    "items": {
     "type": "string"
    },
    "type": "array"
   }
  },
  "required": [
   "files"
  ],
  "type": "object"
 },
 "name": "gdscript_symbols_batch"
}
