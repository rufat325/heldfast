{
 "description": "Smart swap \u2014 auto-routes via buy/sell/zap/Uniswap V3/V4",
 "inputSchema": {
  "properties": {
   "amount": {
    "description": "Amount of input token",
    "type": "string"
   },
   "inputToken": {
    "description": "Input token (ETH, HUNT, USDC, or address)",
    "type": "string"
   },
   "outputToken": {
    "description": "Output token",
    "type": "string"
   },
   "slippage": {
    "description": "Slippage tolerance % (default: 1)",
    "type": "string"
   }
  },
  "required": [
   "inputToken",
   "outputToken",
   "amount"
  ],
  "type": "object"
 },
 "name": "swap"
}
