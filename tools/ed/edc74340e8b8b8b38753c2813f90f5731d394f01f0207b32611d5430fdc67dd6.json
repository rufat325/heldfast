{
 "description": "Creates a contact in the tenant CRM. Required: email, first_name. Needs crm:write on the organization. Returns the created resource with its absolute url. Being in the CRM is not permission to email: consent lives in the `marketing` sub-resource.",
 "inputSchema": {
  "additionalProperties": false,
  "properties": {
   "company_id": {
    "description": "company id",
    "type": "string"
   },
   "email": {
    "description": "email",
    "type": "string"
   },
   "first_name": {
    "description": "first name",
    "type": "string"
   },
   "organization_id": {
    "description": "organization_id",
    "type": "string"
   }
  },
  "required": [
   "organization_id",
   "email",
   "first_name"
  ],
  "type": "object"
 },
 "name": "commsharbor_crm_contacts_create"
}
