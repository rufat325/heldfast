{
 "description": "Importa un spec OpenAPI/Swagger desde una URL o archivo local (JSON o YAML). Guarda los endpoints y schemas para consulta.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "name": {
    "description": "Nombre para identificar este API (ej: \"mi-backend\", \"cocaxcode-api\")",
    "type": "string"
   },
   "source": {
    "description": "URL o ruta a archivo local del spec OpenAPI (JSON o YAML, ej: http://localhost:3001/api-docs-json, ./openapi.yaml)",
    "type": "string"
   }
  },
  "required": [
   "name",
   "source"
  ],
  "type": "object"
 },
 "name": "api_import"
}
