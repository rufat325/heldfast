{
 "annotations": {
  "destructiveHint": false,
  "idempotentHint": true,
  "openWorldHint": true,
  "readOnlyHint": false
 },
 "description": "Mark a message as read (seen=true) or unread (seen=false). This flag is shared across all consumers of the mailbox \u2014 agents that only need new-mail detection should prefer GET /v1/messages?since= or mail_wait_for. Reading a message never changes this flag by itself.",
 "inputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
   "address": {
    "description": "Full email address of the identity that received it",
    "maxLength": 320,
    "pattern": "^[a-z0-9][a-z0-9._-]{0,62}@[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?(?:\\.[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?)*\\.?$",
    "type": "string"
   },
   "id": {
    "description": "Message id from mail_list_messages / mail_wait_for",
    "pattern": "^[1-9]\\d*$",
    "type": "string"
   },
   "seen": {
    "description": "true = mark as read (default), false = mark as unread",
    "type": "boolean"
   }
  },
  "required": [
   "address",
   "id"
  ],
  "type": "object"
 },
 "name": "mail_mark_seen",
 "outputSchema": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
   "id": {
    "type": "string"
   },
   "seen": {
    "type": "boolean"
   }
  },
  "required": [
   "id",
   "seen"
  ],
  "type": "object"
 },
 "title": "Mark Email Seen"
}
