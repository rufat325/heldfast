{
 "description": "Get a full thread with all messages in chronological order.",
 "inputSchema": {
  "properties": {
   "thread_id": {
    "type": "string"
   }
  },
  "required": [
   "thread_id"
  ],
  "type": "object"
 },
 "name": "mails.get_thread"
}
