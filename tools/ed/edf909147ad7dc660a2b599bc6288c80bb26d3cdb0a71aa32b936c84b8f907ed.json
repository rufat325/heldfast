{
 "description": "Roll a Code Engine application back to a previous revision. Re-applies the target revision's image, env, and scale settings, creating a new revision that matches the old one. Use ce_list_app_revisions to find the revision name.",
 "inputSchema": {
  "properties": {
   "app_name": {
    "description": "Application name",
    "type": "string"
   },
   "project_id": {
    "description": "Code Engine project ID",
    "type": "string"
   },
   "revision_name": {
    "description": "The revision to roll back to (from ce_list_app_revisions)",
    "type": "string"
   }
  },
  "required": [
   "project_id",
   "app_name",
   "revision_name"
  ],
  "type": "object"
 },
 "name": "ce_rollback_application"
}
