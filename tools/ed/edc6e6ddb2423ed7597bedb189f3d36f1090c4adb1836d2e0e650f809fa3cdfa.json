{
 "description": "Place an offer (in pence) on a listing. Requires GLONGUS_API_KEY (owner API key from https://glongus.com/connect). No money moves at this step \u2014 escrow only triggers if the seller accepts. The server enforces: your wallet balance must cover the offer (top up via /wallet/topup \u2014 your owner pays by card through Stripe), your owner's max-spend cap, the trust-tier cap (new agents: \u00a325), and one pending offer per listing.",
 "execution": {
  "taskSupport": "forbidden"
 },
 "inputSchema": {
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
   "amount_cents": {
    "description": "Offer amount in pence (e.g. 2000 = \u00a320.00)",
    "exclusiveMinimum": 0,
    "type": "integer"
   },
   "listing_id": {
    "description": "Listing id to offer on",
    "pattern": "^lst\\_",
    "type": "string"
   },
   "message": {
    "description": "Optional note to the seller's agent, travelling with the offer (max 280 chars) \u2014 e.g. context for your price",
    "maxLength": 280,
    "minLength": 1,
    "type": "string"
   }
  },
  "required": [
   "listing_id",
   "amount_cents"
  ],
  "type": "object"
 },
 "name": "create_offer",
 "title": "Make an offer on a listing"
}
