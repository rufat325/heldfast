{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "screenpipe-mcp",
 "protocol": "2025-06-18",
 "published": "2026-08-03T21:46:10.578Z",
 "tools": [
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Activity Summary"
   },
   "description": "Rich activity overview: app usage, window/tab titles with URLs and time spent, key text per context, audio transcriptions. USE WHEN: any broad question about what the user did \u2014 'what was I doing?', 'how long on X?', 'which apps?', 'recap my morning'. This is almost always the right first call for time-range questions \u2014 usually sufficient without follow-up searches. DO NOT USE for: finding a specific keyword (use keyword-search) or a specific UI control (use search-elements).",
   "inputSchema": {
    "properties": {
     "app_name": {
      "description": "Optional app name filter to focus on one app",
      "type": "string"
     },
     "end_time": {
      "description": "ISO 8601 UTC or relative (e.g. 'now')",
      "type": "string"
     },
     "start_time": {
      "description": "ISO 8601 UTC or relative (e.g. '3h ago')",
      "type": "string"
     }
    },
    "required": [
     "start_time",
     "end_time"
    ],
    "type": "object"
   },
   "name": "activity-summary"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Add Tags"
   },
   "description": "Tag a screen frame (vision) or audio chunk (audio) so it can be retrieved later. Tags are a shared linking layer: use namespaced tags (person:ada, project:atlas, topic:pricing) to connect a capture to a person, project, or topic. The SAME tag string also works on memories (via update-memory), so tagging a frame and a memory with person:ada links them. Retrieve later with search-content tags='person:ada' (add content_type+start_time/end_time to scope to a timeframe). Note: frames are pruned by retention, so for durable links prefer tagging a memory; tag frames/audio for shorter-term recall.",
   "inputSchema": {
    "properties": {
     "content_type": {
      "description": "vision = screen frame, audio = audio chunk. Get the id from search-content results (frame_id / chunk_id).",
      "enum": [
       "vision",
       "audio"
      ],
      "type": "string"
     },
     "id": {
      "description": "Content item ID (OCR result frame_id, or audio result chunk_id)",
      "type": "integer"
     },
     "tags": {
      "description": "Tags to add. Prefer namespaced: person:<name>, project:<name>, topic:<name>.",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "content_type",
     "id",
     "tags"
    ],
    "type": "object"
   },
   "name": "add-tags"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Control Recording"
   },
   "description": "Start or stop audio recording. This does not pause or resume screen capture.",
   "inputSchema": {
    "properties": {
     "action": {
      "description": "Audio recording action",
      "enum": [
       "start-audio",
       "stop-audio"
      ],
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "control-recording"
  },
  {
   "annotations": {
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Create Pipe"
   },
   "description": "Create a pipe \u2014 a scheduled AI automation that runs a markdown prompt on a schedule (e.g. 'every day at 9am'). Writes ~/.screenpipe/pipes/<name>/pipe.md, installs it, enables it, and (by default) runs it once to test. USE WHEN: the user wants to automate a recurring task (daily summary, reminder, report, monitor, sync). IMPORTANT: read the screenpipe://guide/pipes resource FIRST \u2014 it documents the prompt format, schedule syntax, presets, and how the pipe prompt should query screenpipe. After creating, check pipe-logs to confirm the test run worked.",
   "inputSchema": {
    "properties": {
     "enabled": {
      "default": true,
      "description": "Enable on creation (default true).",
      "type": "boolean"
     },
     "history": {
      "default": false,
      "description": "Feed the previous run's output back in as context on the next run (default false).",
      "type": "boolean"
     },
     "name": {
      "description": "kebab-case id, e.g. 'daily-time-report'. Becomes the folder name + pipe id.",
      "type": "string"
     },
     "preset": {
      "description": "Optional AI model preset name(s), e.g. ['Primary','Fallback']. Omit to use the default preset.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "prompt": {
      "description": "The markdown instructions the AI agent runs every scheduled execution. Be specific: what to query (which screenpipe endpoints + time range), how to process it, and what to output (write a note, send a notification, push to a connection). screenpipe prepends a context header (time range, timezone, OS, API base + key) before each run, so no template variables are needed. See screenpipe://guide/pipes.",
      "type": "string"
     },
     "run_now": {
      "default": true,
      "description": "Run once immediately after creating, to test it (default true).",
      "type": "boolean"
     },
     "schedule": {
      "description": "When to run: 'every 30m', 'every 1h', 'every day at 9am', 'every monday at 9am', or a cron expression like '0 9 * * *'.",
      "type": "string"
     }
    },
    "required": [
     "name",
     "prompt",
     "schedule"
    ],
    "type": "object"
   },
   "name": "create-pipe"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Export Video"
   },
   "description": "Export an MP4 of screen recordings for a time range, with synced microphone audio. Frames are placed at their real timestamps, so the clip's duration matches the wall-clock span you requested (not a sped-up timelapse). Returns the file path. Can take a few minutes for long ranges.",
   "inputSchema": {
    "properties": {
     "end_time": {
      "description": "ISO 8601 UTC or relative (e.g. \"5m ago\", \"now\")",
      "type": "string"
     },
     "output_path": {
      "description": "Optional absolute path for the MP4 (e.g. ~/Downloads/clip.mp4). Defaults to the screenpipe data dir's exports/ folder.",
      "type": "string"
     },
     "start_time": {
      "description": "ISO 8601 UTC or relative (e.g. \"5m ago\", \"now\")",
      "type": "string"
     }
    },
    "required": [
     "start_time",
     "end_time"
    ],
    "type": "object"
   },
   "name": "export-video"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Frame Context"
   },
   "description": "Get full accessibility text, parsed tree nodes, and URLs for a specific frame ID. Use after search-content to get detailed context for a specific moment.",
   "inputSchema": {
    "properties": {
     "frame_id": {
      "description": "Frame ID from search-content results (content.frame_id field)",
      "type": "integer"
     }
    },
    "required": [
     "frame_id"
    ],
    "type": "object"
   },
   "name": "frame-context"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Get AI Feedback"
   },
   "description": "Search local user ratings and written comments attached to AI-produced notifications, chats, memories, blocks, artifacts, and other targets. Use before generating related work so you preserve what earned up ratings and correct what earned down ratings.",
   "inputSchema": {
    "properties": {
     "kind": {
      "description": "Optional target kind, such as notification, chat, memory, block, artifact, or structured_output.",
      "type": "string"
     },
     "limit": {
      "default": 50,
      "description": "Maximum records (default 50, max 500).",
      "type": "integer"
     },
     "producer": {
      "description": "Optional producer reference, for example pipe:daily-recap. Pipe tokens are always restricted to themselves.",
      "type": "string"
     },
     "q": {
      "description": "Optional text search across comments, snapshots, target ids, producers, and context.",
      "type": "string"
     },
     "rating": {
      "description": "Optional rating filter.",
      "enum": [
       "up",
       "down"
      ],
      "type": "string"
     },
     "since": {
      "description": "Optional RFC3339 lower bound on updated_at.",
      "type": "string"
     },
     "target_id": {
      "description": "Optional exact target id.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "get-feedback"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Get Frame Elements"
   },
   "description": "Get all UI elements for a specific frame. More targeted than search-elements when you already have a frame_id.",
   "inputSchema": {
    "properties": {
     "frame_id": {
      "description": "Frame ID",
      "type": "integer"
     },
     "purpose": {
      "description": "read returns the memory outline; automation returns targeting context for a downstream automation tool. Omit to follow the desktop capture profile. Refresh before each action.",
      "enum": [
       "read",
       "automation"
      ],
      "type": "string"
     }
    },
    "required": [
     "frame_id"
    ],
    "type": "object"
   },
   "name": "get-frame-elements"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Get Meeting"
   },
   "description": "Get a meeting by ID: title, attendees, times, and the full note. Pass include_transcript=true to also get the speaker-attributed transcript segments \u2014 do this when the note is empty and you need to reconstruct what was said (much better than searching raw audio by time range).",
   "inputSchema": {
    "properties": {
     "id": {
      "description": "Meeting ID (from list-meetings results)",
      "type": "integer"
     },
     "include_transcript": {
      "default": false,
      "description": "Also return the meeting's transcript segments with speaker names and timestamps.",
      "type": "boolean"
     },
     "transcript_offset": {
      "default": 0,
      "description": "Skip this many transcript segments (pagination for long meetings).",
      "type": "integer"
     }
    },
    "required": [
     "id"
    ],
    "type": "object"
   },
   "name": "get-meeting"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Health Check"
   },
   "description": "Check if screenpipe is running and healthy. Returns recording status, frame/audio stats, timestamps.",
   "inputSchema": {
    "properties": {},
    "type": "object"
   },
   "name": "health-check"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Keyword Search"
   },
   "description": "Fast FTS5 keyword search across OCR + audio combined. Returns matches with frame_id, app, timestamp, and text positions. USE WHEN: you have a specific keyword/phrase and want the fastest hit-list (e.g. 'find every screen where I typed \"stripe\"'). DO NOT USE for: structured filters by content_type / speaker / window \u2014 this endpoint ignores those (use search-content instead). DO NOT USE for: broad questions like 'what was I doing' (use activity-summary).",
   "inputSchema": {
    "properties": {
     "app_name": {
      "description": "Filter by exact app name (case-sensitive, e.g. 'Google Chrome')",
      "type": "string"
     },
     "end_time": {
      "description": "Same formats as start_time",
      "type": "string"
     },
     "fuzzy_match": {
      "default": false,
      "description": "Enable typo-tolerant matching",
      "type": "boolean"
     },
     "limit": {
      "default": 20,
      "description": "Max results (default 20)",
      "type": "integer"
     },
     "offset": {
      "default": 0,
      "description": "Pagination offset",
      "type": "integer"
     },
     "q": {
      "description": "Keyword query (FTS5 syntax: quoted phrases, AND/OR, prefix*)",
      "type": "string"
     },
     "start_time": {
      "description": "ISO 8601 UTC, 'Nh ago' / 'Nd ago' / 'Nw ago', 'now', 'yesterday', 'today', or 'YYYY-MM-DD'",
      "type": "string"
     }
    },
    "required": [
     "q"
    ],
    "type": "object"
   },
   "name": "keyword-search"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Audio Devices"
   },
   "description": "List available audio input/output devices for recording.",
   "inputSchema": {
    "properties": {},
    "type": "object"
   },
   "name": "list-audio-devices"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Meetings"
   },
   "description": "List detected meetings (Zoom, Teams, Meet, etc.) with id, duration, app, attendees, and note status. Pass `q` to substring-match title, attendee names/emails, and notes \u2014 `q` searches ALL meeting history, so when looking for a meeting with a person or on a topic ('when did I last talk to Noah?'), pass `q` and OMIT start_time. Only constrain the time range when the question itself is time-bound. Results are newest-first; without `q`, old meetings only surface via time range or offset pagination. Follow up with get-meeting (id from results) for the full note and transcript.",
   "inputSchema": {
    "properties": {
     "end_time": {
      "description": "ISO 8601 UTC or relative",
      "type": "string"
     },
     "limit": {
      "default": 20,
      "description": "Max results (default 20)",
      "type": "integer"
     },
     "offset": {
      "default": 0,
      "description": "Pagination offset",
      "type": "integer"
     },
     "q": {
      "description": "Case-insensitive substring filter on title, attendees (names/emails), and note. Searches all history.",
      "type": "string"
     },
     "start_time": {
      "description": "ISO 8601 UTC or relative (e.g. '1d ago'). Omit when searching by q \u2014 it filters all history.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "list-meetings"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Monitors"
   },
   "description": "List available monitors/screens for capture.",
   "inputSchema": {
    "properties": {},
    "type": "object"
   },
   "name": "list-monitors"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Pipes"
   },
   "description": "List the user's pipes (scheduled AI automations) with their enabled state + schedule. USE WHEN: the user asks what automations/pipes exist, or before you create or edit one.",
   "inputSchema": {
    "properties": {},
    "type": "object"
   },
   "name": "list-pipes"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Unnamed Speakers"
   },
   "description": "List speakers that haven't been named yet. Useful for speaker identification workflow.",
   "inputSchema": {
    "properties": {
     "limit": {
      "default": 10,
      "description": "Max results (default 10)",
      "type": "integer"
     },
     "offset": {
      "default": 0,
      "description": "Pagination offset",
      "type": "integer"
     }
    },
    "type": "object"
   },
   "name": "list-unnamed-speakers"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Merge Speakers"
   },
   "description": "Merge two speakers into one (e.g. when the same person was detected as different speakers).",
   "inputSchema": {
    "properties": {
     "speaker_to_keep_id": {
      "description": "Speaker ID to keep",
      "type": "integer"
     },
     "speaker_to_merge_id": {
      "description": "Speaker ID to merge into the kept one",
      "type": "integer"
     }
    },
    "required": [
     "speaker_to_keep_id",
     "speaker_to_merge_id"
    ],
    "type": "object"
   },
   "name": "merge-speakers"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Pipe Logs"
   },
   "description": "Get a pipe's recent execution logs / output. USE WHEN: debugging why a pipe misbehaved, or reading the result of a test run.",
   "inputSchema": {
    "properties": {
     "name": {
      "description": "The pipe id/name.",
      "type": "string"
     }
    },
    "required": [
     "name"
    ],
    "type": "object"
   },
   "name": "pipe-logs"
  },
  {
   "annotations": {
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Run Pipe"
   },
   "description": "Run a pipe once immediately (a test run), independent of its schedule. USE WHEN: you just created/edited a pipe and want to verify it, or the user says 'run X now'. Then read pipe-logs to see what it did.",
   "inputSchema": {
    "properties": {
     "name": {
      "description": "The pipe id/name.",
      "type": "string"
     }
    },
    "required": [
     "name"
    ],
    "type": "object"
   },
   "name": "run-pipe"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Search Content"
   },
   "description": "Search screen text, audio transcriptions, input events, memories, and parsed app data. Returns timestamped results with app context. USE WHEN: you need the actual text/content of a moment \u2014 quotes, screen text, transcript lines, or compact parsed messages, emails, tasks, documents, and code review \u2014 or want to filter by speaker/window. DO NOT USE for: broad questions like 'what was I doing?' (use activity-summary, it pre-summarizes apps + windows + transcripts). Also DO NOT USE for: targeted UI controls (use search-elements). Start with limit=5, increase only if needed. Per-result text is auto-truncated to 1000 chars; pass max_content_length=0 to opt out, or a custom integer to override.",
   "inputSchema": {
    "properties": {
     "actor_id": {
      "description": "With content_type='parsed', filter items by a resolved actor identity.",
      "type": "integer"
     },
     "app_name": {
      "description": "Filter by app name (e.g. 'Google Chrome', 'Slack', 'zoom.us'). Case-sensitive.",
      "type": "string"
     },
     "content_type": {
      "default": "all",
      "description": "Filter by content type. Use 'parsed' for compact app-specific records such as messages, emails, tasks, documents, and code review; it is experimental and may be empty when parsing is disabled or unsupported. NOTE on screen text: 'ocr' is a legacy label \u2014 it returns ALL screen-text rows, which are accessibility-derived for most apps (the result tag [Screen\u00b7a11y] vs [Screen\u00b7ocr] tells you which). Use 'ocr' for screen text (covers both paths), 'audio' for transcriptions, 'input' for keyboard/mouse events, 'memory' for stored facts. Default: 'all'.",
      "enum": [
       "all",
       "ocr",
       "audio",
       "input",
       "accessibility",
       "memory",
       "parsed"
      ],
      "type": "string"
     },
     "end_time": {
      "description": "ISO 8601 UTC or relative (e.g. 'now'). Defaults to now.",
      "type": "string"
     },
     "frame_id": {
      "description": "With content_type='parsed', return parsed data attached to one frame.",
      "type": "integer"
     },
     "include_frames": {
      "default": false,
      "description": "Include base64 screenshots (OCR only). Warning: large response.",
      "type": "boolean"
     },
     "include_related": {
      "default": false,
      "description": "With tags set, also return the co-occurring tags (the people/projects/topics seen alongside yours, ranked by frequency) as a 'Related:' line. One call for the surrounding context instead of several follow-ups. Ignored without tags.",
      "type": "boolean"
     },
     "limit": {
      "default": 10,
      "description": "Max results (default 10, max 20). Start with 5 for exploration.",
      "type": "integer"
     },
     "max_content_length": {
      "description": "Truncate each result's text via middle-truncation. Use 200-500 to keep responses compact.",
      "type": "integer"
     },
     "max_length": {
      "description": "Max content length in characters",
      "type": "integer"
     },
     "min_length": {
      "description": "Min content length in characters",
      "type": "integer"
     },
     "offset": {
      "default": 0,
      "description": "Pagination offset. Use when results say 'use offset=N for more'.",
      "type": "integer"
     },
     "q": {
      "description": "Full-text search query. Omit to return all content in time range. Avoid for audio \u2014 transcriptions are noisy, q filters too aggressively.",
      "type": "string"
     },
     "speaker_ids": {
      "description": "Comma-separated speaker IDs to filter audio",
      "type": "string"
     },
     "speaker_name": {
      "description": "Filter audio by speaker name (case-insensitive partial match)",
      "type": "string"
     },
     "start_time": {
      "description": "Accepted: ISO 8601 ('2024-01-15T10:00:00Z'), 'Nh ago' / 'Nd ago' / 'Nw ago', 'now', 'yesterday', 'today', or bare 'YYYY-MM-DD'. Always provide to avoid scanning entire history.",
      "type": "string"
     },
     "tags": {
      "description": "Comma-separated tags; returns only items carrying ALL of them (e.g. 'person:ada,project:atlas'). Works for screen + audio (content_type 'ocr'/'audio'/'all', tags written by add-tags) AND memories (content_type 'memory', tags written by update-memory). Same tag string links across all three, so two items sharing a tag are connected. Use namespaced tags (person:, project:, topic:) to link people/projects/topics. content_type 'input' and 'accessibility' have no tags and return nothing when this is set; 'parsed' does not support tags.",
      "type": "string"
     },
     "window_name": {
      "description": "Filter by window title substring",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "search-content"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Search Elements"
   },
   "description": "Search UI elements (buttons, links, text fields) from the accessibility tree, filterable by role. USE WHEN: you want a specific UI control or page-structure question \u2014 'find every Submit button I saw', 'list the links in that page'. DO NOT USE for: general text/content (use search-content) or fast keyword lookup (use keyword-search).",
   "inputSchema": {
    "properties": {
     "app_name": {
      "description": "Filter by app name",
      "type": "string"
     },
     "end_time": {
      "description": "ISO 8601 UTC or relative",
      "type": "string"
     },
     "frame_id": {
      "description": "Filter to specific frame ID from search results",
      "type": "integer"
     },
     "limit": {
      "default": 50,
      "description": "Max results (default 50). Start with 10-20.",
      "type": "integer"
     },
     "offset": {
      "default": 0,
      "description": "Pagination offset",
      "type": "integer"
     },
     "purpose": {
      "description": "read returns the compact memory outline; automation returns fresh refs, best-effort keys, state, bounds, and allowed actions. Omit to follow the desktop capture profile.",
      "enum": [
       "read",
       "automation"
      ],
      "type": "string"
     },
     "q": {
      "description": "Full-text search on element text",
      "type": "string"
     },
     "role": {
      "description": "Element role filter (e.g. 'AXButton', 'AXLink', 'AXTextField')",
      "type": "string"
     },
     "source": {
      "description": "Element source. 'accessibility' is preferred (OS-native tree). 'ocr' for apps without a11y.",
      "enum": [
       "accessibility",
       "ocr"
      ],
      "type": "string"
     },
     "start_time": {
      "description": "ISO 8601 UTC or relative",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "search-elements"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Search Speakers"
   },
   "description": "Search for speakers by name prefix. Returns speaker ID, name, and metadata.",
   "inputSchema": {
    "properties": {
     "name": {
      "description": "Speaker name prefix to search for (case-insensitive)",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "search-speakers"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Send Notification"
   },
   "description": "Send a notification to the screenpipe desktop UI. Use high priority only for time-sensitive failures or decisions needing human attention; routine findings and completed tasks should be normal or low.",
   "inputSchema": {
    "properties": {
     "actions": {
      "description": "Up to 5 action buttons. Each needs id, label, type ('pipe'|'chat'|'api'|'deeplink'|'dismiss').",
      "items": {
       "properties": {
        "auto_send": {
         "description": "type=chat: auto-send the prompt (default true). Set false to pre-fill chat for the user to review before sending.",
         "type": "boolean"
        },
        "context": {
         "description": "Data passed to the action (type=pipe \u2192 injected into the pipe prompt; type=chat \u2192 included as background context)",
         "type": "object"
        },
        "id": {
         "description": "Unique action ID",
         "type": "string"
        },
        "label": {
         "description": "Button label",
         "type": "string"
        },
        "open_in_chat": {
         "description": "Open pipe run in chat UI instead of background (type=pipe)",
         "type": "boolean"
        },
        "pipe": {
         "description": "Target pipe to run when clicked (type=pipe). Set this explicitly \u2014 e.g. a 'share-data' pipe. If omitted it falls back to the sending pipe, which usually does nothing useful.",
         "type": "string"
        },
        "prompt": {
         "description": "Instruction to run in a fresh chat session when clicked (type=chat). Write the whole task inline \u2014 no pre-installed pipe required.",
         "type": "string"
        },
        "type": {
         "description": "Action type. 'pipe' runs an installed pipe; 'chat' runs an inline prompt in a fresh chat session (no installed pipe needed).",
         "enum": [
          "pipe",
          "chat",
          "api",
          "deeplink",
          "dismiss"
         ],
         "type": "string"
        },
        "url": {
         "description": "URL for api/deeplink actions",
         "type": "string"
        }
       },
       "required": [
        "id",
        "label",
        "type"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "body": {
      "description": "Notification body (markdown supported)",
      "type": "string"
     },
     "pipe_name": {
      "description": "Name of the pipe/tool sending this notification",
      "type": "string"
     },
     "priority": {
      "default": "normal",
      "description": "High interrupts and appears in the focused Priority view. Normal (default) and low remain available in All without interrupting.",
      "enum": [
       "high",
       "normal",
       "low"
      ],
      "type": "string"
     },
     "timeout_secs": {
      "default": 20,
      "description": "Auto-dismiss after N seconds (default 20). Use 0 for persistent.",
      "type": "integer"
     },
     "title": {
      "description": "Notification title (short, descriptive)",
      "type": "string"
     }
    },
    "required": [
     "title",
     "pipe_name"
    ],
    "type": "object"
   },
   "name": "send-notification"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Start Meeting"
   },
   "description": "Manually start a meeting recording session.",
   "inputSchema": {
    "properties": {
     "app": {
      "default": "manual",
      "description": "App name (default 'manual')",
      "type": "string"
     },
     "attendees": {
      "description": "Comma-separated attendee names",
      "type": "string"
     },
     "title": {
      "description": "Meeting title",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "start-meeting"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Stop Meeting"
   },
   "description": "Stop the current manual meeting recording session.",
   "inputSchema": {
    "properties": {},
    "type": "object"
   },
   "name": "stop-meeting"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Update Meeting"
   },
   "description": "Update a meeting's mutable fields (title, attendees, note, app, start/end). Partial: only the fields you pass are written, others stay as-is. Use this to save an AI-generated summary into the meeting note \u2014 read the current note first via get-meeting and pass the existing notes plus your additions so you don't overwrite the user's writing. Convention: append AI-generated summary text under a `## Summary` heading at the bottom of the existing note.",
   "inputSchema": {
    "properties": {
     "attendees": {
      "description": "Comma-separated attendee names",
      "type": "string"
     },
     "id": {
      "description": "Meeting ID",
      "type": "integer"
     },
     "meeting_app": {
      "description": "App / source name (e.g. 'meet.google.com', 'manual')",
      "type": "string"
     },
     "meeting_end": {
      "description": "ISO 8601 end time (rarely needed)",
      "type": "string"
     },
     "meeting_start": {
      "description": "ISO 8601 start time (rarely needed)",
      "type": "string"
     },
     "note": {
      "description": "Full new note body. To preserve existing notes, fetch them first via get-meeting and concatenate before passing.",
      "type": "string"
     },
     "title": {
      "description": "Meeting title",
      "type": "string"
     }
    },
    "required": [
     "id"
    ],
    "type": "object"
   },
   "name": "update-meeting"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Update Memory"
   },
   "description": "Create, update, or delete a persistent memory (facts, preferences, decisions the user wants to remember). To retrieve memories, use search-content with content_type='memory'. To create: provide content + tags. To update: provide id + fields to change. To delete: provide id + delete=true.",
   "inputSchema": {
    "properties": {
     "content": {
      "description": "Memory text (required for creation)",
      "type": "string"
     },
     "delete": {
      "description": "Set true to delete the memory identified by id",
      "type": "boolean"
     },
     "id": {
      "description": "Memory ID \u2014 omit to create new, provide to update/delete",
      "type": "integer"
     },
     "importance": {
      "description": "0.0 (trivial) to 1.0 (critical). Default 0.5.",
      "type": "number"
     },
     "source_context": {
      "description": "Optional metadata linking to source (app, timestamp, etc.)",
      "type": "object"
     },
     "tags": {
      "description": "Tags. Prefer namespaced (person:ada, project:atlas, topic:pricing) so this memory links to the same people/projects you tag on frames/audio. Retrieve with search-content content_type='memory' tags='person:ada'.",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "type": "object"
   },
   "name": "update-memory"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Update Speaker"
   },
   "description": "Rename a speaker or update their metadata.",
   "inputSchema": {
    "properties": {
     "id": {
      "description": "Speaker ID",
      "type": "integer"
     },
     "metadata": {
      "description": "JSON metadata string",
      "type": "string"
     },
     "name": {
      "description": "New speaker name",
      "type": "string"
     }
    },
    "required": [
     "id"
    ],
    "type": "object"
   },
   "name": "update-speaker"
  }
 ],
 "version": "0.19.1"
}