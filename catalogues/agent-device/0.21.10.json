{
 "args": [
  "mcp"
 ],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "agent-device",
 "protocol": "2025-06-18",
 "published": "2026-09-22T13:02:09.700Z",
 "tools": [
  {
   "description": "Press the iPhone or iPad Action Button once. The press is dispatched without activating the session app and nothing is re-observed afterwards, so the app keeps the state the press found. What the system does with the press is not observed by this command: Simulators run no Shortcuts or App Intents, so delivery to an assigned Shortcut is verifiable only on a physical iPhone. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "action-button",
   "outputSchema": {
    "properties": {
     "action": {
      "const": "action-button",
      "type": "string"
     },
     "message": {
      "type": "string"
     }
    },
    "required": [
     "action",
     "message"
    ],
    "type": "object"
   }
  },
  {
   "description": "Inspect, wait for, accept, or dismiss a platform alert. Use get before acting when the alert content matters; accept and dismiss change the active alert state. Each iOS XCTest execution activates once, then only observes. Inspect again after an unconfirmed action; never assume a timeout means nothing happened. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "get",
       "accept",
       "dismiss",
       "wait"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "alert"
  },
  {
   "description": "Open the device app switcher to inspect or change foreground apps. This changes the visible system UI and may move focus away from the current app. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "app-switcher",
   "outputSchema": {
    "properties": {
     "action": {
      "const": "app-switcher",
      "type": "string"
     },
     "message": {
      "type": "string"
     }
    },
    "required": [
     "action",
     "message"
    ],
    "type": "object"
   }
  },
  {
   "description": "List the apps installed on the selected device. Include system or OEM apps only when they are needed as automation targets. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "appsFilter": {
      "description": "Restrict the listing to user-installed apps, or include system and OEM apps.",
      "enum": [
       "user-installed",
       "all"
      ],
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "apps",
   "outputSchema": {
    "properties": {
     "apps": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "apps"
    ],
    "type": "object"
   }
  },
  {
   "description": "Show foreground app/activity (Android; iOS answers per command) Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "appstate",
   "outputSchema": {
    "oneOf": [
     {
      "properties": {
       "appBundleId": {
        "type": "string"
       },
       "appName": {
        "type": "string"
       },
       "device_udid": {
        "description": "iOS only \u2014 the session device UDID.",
        "type": "string"
       },
       "ios_simulator_device_set": {
        "description": "iOS only \u2014 the simulator set path, or null when unknown.",
        "type": [
         "string",
         "null"
        ]
       },
       "platform": {
        "enum": [
         "ios",
         "macos"
        ],
        "type": "string"
       },
       "source": {
        "const": "session",
        "type": "string"
       },
       "surface": {
        "enum": [
         "app",
         "frontmost-app",
         "desktop",
         "menubar"
        ],
        "type": "string"
       }
      },
      "required": [
       "platform",
       "appName",
       "source",
       "surface"
      ],
      "type": "object"
     },
     {
      "properties": {
       "activity": {
        "type": "string"
       },
       "package": {
        "type": "string"
       },
       "platform": {
        "const": "android",
        "type": "string"
       }
      },
      "required": [
       "platform",
       "package",
       "activity"
      ],
      "type": "object"
     }
    ],
    "type": "object"
   }
  },
  {
   "description": "List daemon or cloud provider artifacts for an active or completed session. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "provider": {
      "description": "Cloud provider name, for example browserstack or aws-device-farm.",
      "type": "string"
     },
     "providerSessionId": {
      "description": "Cloud provider session id or ARN.",
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "artifacts"
  },
  {
   "description": "Measure browser or host-rendered simulator/emulator audio as compact dBFS buckets. Start a probe before requesting its status or stopping it. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "probe"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "bucketMs": {
      "description": "Audio level bucket size in milliseconds.",
      "type": "integer"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "durationMs": {
      "description": "Probe duration in milliseconds.",
      "type": "integer"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "probeAction": {
      "enum": [
       "start",
       "status",
       "stop"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "audio"
  },
  {
   "description": "Navigate back in the app or through system navigation. Use in-app for the app navigation stack and system when the platform back behavior is required. Times out after 90s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "mode": {
      "enum": [
       "in-app",
       "system"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "settle": {
      "description": "After the action, wait for the UI to go quiet and return the settled diff vs the pre-action tree in the same response. Best-effort; never fails the action.",
      "type": "boolean"
     },
     "settleQuietMs": {
      "description": "Settle: quiet window in milliseconds (default 500).",
      "minimum": 0,
      "type": "integer"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "description": "Settle: wait deadline in milliseconds (default 10000).",
      "minimum": 1,
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "back",
   "outputSchema": {
    "properties": {
     "action": {
      "const": "back",
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "mode": {
      "enum": [
       "in-app",
       "system"
      ],
      "type": "string"
     },
     "settle": {
      "properties": {
       "captures": {
        "type": "number"
       },
       "diff": {
        "description": "Settled diff vs the pre-action tree (changed lines only).",
        "properties": {
         "lines": {
          "items": {
           "properties": {
            "kind": {
             "enum": [
              "added",
              "removed"
             ],
             "type": "string"
            },
            "ref": {
             "description": "Plain ref body (e12) for added lines.",
             "type": "string"
            },
            "text": {
             "type": "string"
            }
           },
           "required": [
            "kind",
            "text"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "summary": {
          "properties": {
           "additions": {
            "type": "number"
           },
           "removals": {
            "type": "number"
           },
           "unchanged": {
            "type": "number"
           }
          },
          "required": [
           "additions",
           "removals",
           "unchanged"
          ],
          "type": "object"
         },
         "truncated": {
          "description": "Lines were capped to the response bound.",
          "type": "boolean"
         }
        },
        "required": [
         "summary",
         "lines"
        ],
        "type": "object"
       },
       "hint": {
        "type": "string"
       },
       "quietMs": {
        "type": "number"
       },
       "refs": {
        "items": {
         "properties": {
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          }
         },
         "required": [
          "ref"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "refsGeneration": {
        "description": "Snapshot generation of the stored settled tree; refs on added diff lines were minted from it.",
        "type": "number"
       },
       "settled": {
        "description": "Whether the UI held the quiet window before the deadline. false is advisory, not failure.",
        "type": "boolean"
       },
       "surfaceChange": {
        "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
        "properties": {
         "disclosure": {
          "description": "Agent-facing sentence explaining the surface transition.",
          "type": "string"
         },
         "from": {
          "description": "Surface the pre-action baseline described: a host bundle id, or app.",
          "type": "string"
         },
         "to": {
          "description": "Surface the post-action capture describes: a host bundle id, or app.",
          "type": "string"
         }
        },
        "required": [
         "from",
         "to",
         "disclosure"
        ],
        "type": "object"
       },
       "tail": {
        "description": "Unchanged interactive refs tail: still-present, actionable elements from the settled tree, attached only when diff carries zero added-line refs (a modal-dismiss/toast-only diff).",
        "items": {
         "properties": {
          "label": {
           "type": "string"
          },
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          },
          "role": {
           "type": "string"
          }
         },
         "required": [
          "ref",
          "role"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "tailTruncated": {
        "description": "Present (true) when tail candidates exceeded the response cap.",
        "type": "boolean"
       },
       "timeoutMs": {
        "type": "number"
       },
       "waitedMs": {
        "type": "number"
       }
      },
      "required": [
       "settled",
       "waitedMs",
       "captures",
       "quietMs",
       "timeoutMs"
      ],
      "type": "object"
     }
    },
    "required": [
     "action",
     "mode",
     "message"
    ],
    "type": "object"
   }
  },
  {
   "description": "Execute multiple commands in one daemon request. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "maxSteps": {
      "description": "Maximum number of steps accepted for this batch.",
      "maximum": 1000,
      "minimum": 1,
      "type": "integer"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "onError": {
      "description": "Batch failure policy.",
      "enum": [
       "stop"
      ],
      "type": "string"
     },
     "out": {
      "description": "Optional output path for command artifacts.",
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "steps": {
      "description": "Structured batch steps. Each step uses a command name and the same input object as that command tool.",
      "items": {
       "additionalProperties": false,
       "properties": {
        "command": {
         "description": "Command name to run with structured input.",
         "enum": [
          "devices",
          "capabilities",
          "doctor",
          "apps",
          "boot",
          "shutdown",
          "appstate",
          "perf",
          "logs",
          "network",
          "audio",
          "test",
          "clipboard",
          "keyboard",
          "install",
          "reinstall",
          "push",
          "trigger-app-event",
          "open",
          "close",
          "snapshot",
          "diff",
          "wait",
          "alert",
          "settings",
          "react-native",
          "record",
          "trace",
          "find",
          "click",
          "fill",
          "longpress",
          "hover",
          "press",
          "type",
          "get",
          "is",
          "back",
          "gesture",
          "home",
          "tv-remote",
          "orientation",
          "fold",
          "scroll",
          "swipe",
          "focus",
          "screenshot",
          "app-switcher",
          "action-button",
          "install-from-source"
         ],
         "type": "string"
        },
        "input": {
         "additionalProperties": true,
         "commandInputFor": "command",
         "description": "Structured command input for the nested command. Use the matching MCP tool schema for this object.",
         "type": "object"
        },
        "runtime": {
         "additionalProperties": true,
         "description": "Optional per-step runtime payload.",
         "type": "object"
        }
       },
       "required": [
        "command",
        "input"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "steps"
    ],
    "type": "object"
   },
   "name": "batch"
  },
  {
   "description": "Boot or prepare the selected device or simulator so later commands can target it. The device is chosen through the device-selection inputs, not by naming it here. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "headless": {
      "description": "Boot without showing simulator UI when supported.",
      "type": "boolean"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "boot",
   "outputSchema": {
    "properties": {
     "booted": {
      "const": true,
      "type": "boolean"
     },
     "device": {
      "description": "Human-readable device name.",
      "type": "string"
     },
     "id": {
      "description": "Stable device id.",
      "type": "string"
     },
     "kind": {
      "enum": [
       "simulator",
       "emulator",
       "device"
      ],
      "type": "string"
     },
     "platform": {
      "enum": [
       "ios",
       "macos",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web"
      ],
      "type": "string"
     },
     "target": {
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     }
    },
    "required": [
     "platform",
     "target",
     "device",
     "id",
     "kind",
     "booted"
    ],
    "type": "object"
   }
  },
  {
   "description": "List the commands supported by the selected device or active session. Use device-selection inputs when checking support before a session is open. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "capabilities"
  },
  {
   "description": "Activate a UI target by snapshot ref, selector, or coordinates. Prefer a ref or selector after a snapshot; use coordinates only when semantic targeting is unavailable. This can change app state; use settle or verify to confirm the result without a follow-up snapshot. Times out after 90s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "button": {
      "description": "Pointer button for platforms that support mouse buttons.",
      "enum": [
       "primary",
       "secondary",
       "middle"
      ],
      "type": "string"
     },
     "count": {
      "description": "Number of press/click repetitions.",
      "minimum": 1,
      "type": "integer"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "depth": {
      "description": "Snapshot traversal depth.",
      "minimum": 0,
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "doubleTap": {
      "description": "Request a double-tap action.",
      "type": "boolean"
     },
     "holdMs": {
      "description": "Hold duration for each action.",
      "minimum": 0,
      "type": "integer"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "intervalMs": {
      "description": "Delay between repeated press/click actions.",
      "minimum": 0,
      "type": "integer"
     },
     "jitterPx": {
      "description": "Randomization radius in pixels.",
      "minimum": 0,
      "type": "integer"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "raw": {
      "description": "Use raw snapshot data during selector resolution.",
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scope": {
      "description": "Snapshot scope selector used before resolution.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "settle": {
      "description": "After the action, wait for the UI to go quiet and return the settled diff vs the pre-action tree in the same response. Best-effort; never fails the action.",
      "type": "boolean"
     },
     "settleQuietMs": {
      "description": "Settle: quiet window in milliseconds (default 500).",
      "minimum": 0,
      "type": "integer"
     },
     "target": {
      "description": "UI target. This is separate from deviceTarget, which selects the device form.",
      "oneOf": [
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "ref",
          "type": "string"
         },
         "label": {
          "description": "Optional human label for the ref.",
          "type": "string"
         },
         "ref": {
          "description": "Snapshot element ref such as @e12.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "ref"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "selector",
          "type": "string"
         },
         "selector": {
          "description": "Agent-device selector expression.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "selector"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "point",
          "type": "string"
         },
         "x": {
          "type": "number"
         },
         "y": {
          "type": "number"
         }
        },
        "required": [
         "kind",
         "x",
         "y"
        ],
        "type": "object"
       }
      ]
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "description": "Settle: wait deadline in milliseconds (default 10000).",
      "minimum": 1,
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "verify": {
      "description": "Capture cheap post-action evidence (AX digest, node counts, changedFromBefore) instead of a follow-up snapshot.",
      "type": "boolean"
     }
    },
    "required": [
     "target"
    ],
    "type": "object"
   },
   "name": "click",
   "outputSchema": {
    "properties": {
     "button": {
      "enum": [
       "secondary",
       "middle"
      ],
      "type": "string"
     },
     "cost": {
      "properties": {
       "nodeCount": {
        "description": "Number of nodes in the original node tree when the response carries one.",
        "type": "number"
       },
       "runnerRoundTrips": {
        "description": "Number of real runner round-trips made while serving the request.",
        "type": "number"
       },
       "wallClockMs": {
        "description": "Total wall-clock time for the request in milliseconds.",
        "type": "number"
       }
      },
      "required": [
       "wallClockMs",
       "runnerRoundTrips"
      ],
      "type": "object"
     },
     "count": {
      "description": "Number of press/click repetitions.",
      "type": "number"
     },
     "doubleTap": {
      "description": "Whether the command requested a double-tap action.",
      "type": "boolean"
     },
     "evidence": {
      "properties": {
       "changedFromBefore": {
        "description": "Whether the post-action digest differs from the pre-action capture digest. false is evidence, not failure. With surfaceChange present, no digest comparison is made: it reports that surface transition.",
        "type": "boolean"
       },
       "digest": {
        "description": "Order-independent digest of the post-action node multiset.",
        "type": "string"
       },
       "foregroundApp": {
        "description": "Foreground app bundle id or name, when the capture carries it.",
        "type": "string"
       },
       "interactiveNodeCount": {
        "description": "Subset of nodeCount the platform reports as hittable.",
        "type": "number"
       },
       "nodeCount": {
        "description": "Node count in the post-action interactive-only capture.",
        "type": "number"
       },
       "surfaceChange": {
        "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
        "properties": {
         "disclosure": {
          "description": "Agent-facing sentence explaining the surface transition.",
          "type": "string"
         },
         "from": {
          "description": "Surface the pre-action baseline described: a host bundle id, or app.",
          "type": "string"
         },
         "to": {
          "description": "Surface the post-action capture describes: a host bundle id, or app.",
          "type": "string"
         }
        },
        "required": [
         "from",
         "to",
         "disclosure"
        ],
        "type": "object"
       }
      },
      "required": [
       "nodeCount",
       "interactiveNodeCount",
       "digest",
       "changedFromBefore"
      ],
      "type": "object"
     },
     "hint": {
      "type": "string"
     },
     "holdMs": {
      "description": "Hold duration for each action.",
      "type": "number"
     },
     "intervalMs": {
      "description": "Delay between repeated press/click actions.",
      "type": "number"
     },
     "jitterPx": {
      "description": "Randomization radius in pixels.",
      "type": "number"
     },
     "maestroFallbackReason": {
      "const": "non-hittable-coordinate",
      "type": "string"
     },
     "maestroNonHittableCoordinateFallbackAllowed": {
      "description": "Whether the direct iOS Maestro coordinate fallback was allowed for this selector.",
      "type": "boolean"
     },
     "maestroNonHittableCoordinateFallbackUsed": {
      "description": "Whether the direct iOS Maestro coordinate fallback was actually used.",
      "type": "boolean"
     },
     "message": {
      "type": "string"
     },
     "ref": {
      "description": "Snapshot ref without the @ prefix when the target was an @ref.",
      "type": "string"
     },
     "refLabel": {
      "type": "string"
     },
     "referenceHeight": {
      "description": "Reference frame height for visualizing the interaction point.",
      "type": "number"
     },
     "referenceWidth": {
      "description": "Reference frame width for visualizing the interaction point.",
      "type": "number"
     },
     "resolution": {
      "description": "Pre-action disclosure of how the acting path resolved its target. Absent when resolutionDisclosure is inapplicable for the path.",
      "oneOf": [
       {
        "properties": {
         "kind": {
          "const": "unique",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "runtime",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "alternatives": {
          "description": "At most 5 losing candidates, document order. Present at default/full response levels and omitted in digest. The winner is never included.",
          "items": {
           "properties": {
            "diagnosticRef": {
             "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
             "type": "string"
            },
            "label": {
             "description": "UTF-8 truncated to 256 bytes.",
             "type": "string"
            },
            "role": {
             "description": "UTF-8 truncated to 256 bytes.",
             "type": "string"
            }
           },
           "required": [
            "diagnosticRef"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "kind": {
          "const": "disambiguated",
          "type": "string"
         },
         "matchCount": {
          "description": "Total matches resolveSelectorChain found before disambiguation.",
          "type": "number"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "runtime",
          "type": "string"
         },
         "tiebreak": {
          "description": "The comparison that decided the winner.",
          "enum": [
           "visible",
           "deepest",
           "smallest-area",
           "structural-equivalence"
          ],
          "type": "string"
         },
         "winnerDiagnostic": {
          "properties": {
           "diagnosticRef": {
            "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
            "type": "string"
           },
           "label": {
            "description": "UTF-8 truncated to 256 bytes.",
            "type": "string"
           },
           "role": {
            "description": "UTF-8 truncated to 256 bytes.",
            "type": "string"
           }
          },
          "required": [
           "diagnosticRef"
          ],
          "type": "object"
         }
        },
        "required": [
         "source",
         "phase",
         "kind",
         "matchCount",
         "winnerDiagnostic",
         "tiebreak"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "exact",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "ref",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "label-fallback",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "ref",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "not-observed",
          "type": "string"
         },
         "source": {
          "const": "direct-ios",
          "type": "string"
         }
        },
        "required": [
         "source",
         "kind"
        ],
        "type": "object"
       }
      ],
      "type": "object"
     },
     "selector": {
      "description": "Selector expression when the target was a selector.",
      "type": "string"
     },
     "selectorChain": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "settle": {
      "properties": {
       "captures": {
        "type": "number"
       },
       "diff": {
        "description": "Settled diff vs the pre-action tree (changed lines only).",
        "properties": {
         "lines": {
          "items": {
           "properties": {
            "kind": {
             "enum": [
              "added",
              "removed"
             ],
             "type": "string"
            },
            "ref": {
             "description": "Plain ref body (e12) for added lines.",
             "type": "string"
            },
            "text": {
             "type": "string"
            }
           },
           "required": [
            "kind",
            "text"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "summary": {
          "properties": {
           "additions": {
            "type": "number"
           },
           "removals": {
            "type": "number"
           },
           "unchanged": {
            "type": "number"
           }
          },
          "required": [
           "additions",
           "removals",
           "unchanged"
          ],
          "type": "object"
         },
         "truncated": {
          "description": "Lines were capped to the response bound.",
          "type": "boolean"
         }
        },
        "required": [
         "summary",
         "lines"
        ],
        "type": "object"
       },
       "hint": {
        "type": "string"
       },
       "quietMs": {
        "type": "number"
       },
       "refs": {
        "items": {
         "properties": {
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          }
         },
         "required": [
          "ref"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "refsGeneration": {
        "description": "Snapshot generation of the stored settled tree; refs on added diff lines were minted from it.",
        "type": "number"
       },
       "settled": {
        "description": "Whether the UI held the quiet window before the deadline. false is advisory, not failure.",
        "type": "boolean"
       },
       "surfaceChange": {
        "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
        "properties": {
         "disclosure": {
          "description": "Agent-facing sentence explaining the surface transition.",
          "type": "string"
         },
         "from": {
          "description": "Surface the pre-action baseline described: a host bundle id, or app.",
          "type": "string"
         },
         "to": {
          "description": "Surface the post-action capture describes: a host bundle id, or app.",
          "type": "string"
         }
        },
        "required": [
         "from",
         "to",
         "disclosure"
        ],
        "type": "object"
       },
       "tail": {
        "description": "Unchanged interactive refs tail: still-present, actionable elements from the settled tree, attached only when diff carries zero added-line refs (a modal-dismiss/toast-only diff).",
        "items": {
         "properties": {
          "label": {
           "type": "string"
          },
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          },
          "role": {
           "type": "string"
          }
         },
         "required": [
          "ref",
          "role"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "tailTruncated": {
        "description": "Present (true) when tail candidates exceeded the response cap.",
        "type": "boolean"
       },
       "timeoutMs": {
        "type": "number"
       },
       "waitedMs": {
        "type": "number"
       }
      },
      "required": [
       "settled",
       "waitedMs",
       "captures",
       "quietMs",
       "timeoutMs"
      ],
      "type": "object"
     },
     "targetHittable": {
      "type": "boolean"
     },
     "targetKind": {
      "description": "Resolved interaction target kind.",
      "enum": [
       "point",
       "ref",
       "selector"
      ],
      "type": "string"
     },
     "warning": {
      "type": "string"
     },
     "x": {
      "description": "Resolved interaction x coordinate when available.",
      "type": "number"
     },
     "y": {
      "description": "Resolved interaction y coordinate when available.",
      "type": "number"
     }
    },
    "required": [
     "targetKind"
    ],
    "type": "object"
   }
  },
  {
   "description": "Read the current device clipboard text, or replace its contents with the given text. Android runs both through the clipboard service shell command, and a build that implements none (Android 16 does not) refuses with UNSUPPORTED_OPERATION rather than reporting an empty clipboard. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "read",
       "write"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "text": {
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "clipboard",
   "outputSchema": {
    "oneOf": [
     {
      "properties": {
       "action": {
        "const": "read",
        "type": "string"
       },
       "text": {
        "type": "string"
       }
      },
      "required": [
       "action",
       "text"
      ],
      "type": "object"
     },
     {
      "properties": {
       "action": {
        "const": "write",
        "type": "string"
       },
       "message": {
        "type": "string"
       },
       "textLength": {
        "type": "number"
       }
      },
      "required": [
       "action",
       "textLength",
       "message"
      ],
      "type": "object"
     }
    ],
    "type": "object"
   }
  },
  {
   "description": "Close the named app, or close the active session app when app is omitted. Use shutdown only when the selected simulator or emulator should also stop. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "app": {
      "description": "Optional app to close.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "force": {
      "description": "Overwrite an existing --save-script target instead of refusing (alias: --overwrite).",
      "type": "boolean"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "saveScript": {
      "oneOf": [
       {
        "type": "boolean"
       },
       {
        "type": "string"
       }
      ]
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "shutdown": {
      "description": "Shutdown the session/device where supported.",
      "type": "boolean"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "close"
  },
  {
   "description": "Symbolicate Apple crash artifacts with matching dSYM UUIDs. This debug namespace is intentionally narrow: use logs for app logs, network for HTTP evidence, perf for performance samples, record/trace for media and traces, and react-devtools for React Native profiles. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "symbols"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "artifact": {
      "description": "Apple crash artifact path (.ips, .crash, or .log).",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "dsym": {
      "description": "Path to a matching .dSYM bundle.",
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "out": {
      "description": "Output path for the symbolicated artifact.",
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "searchPath": {
      "description": "Directory to scan for matching .dSYM bundles.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "action",
     "artifact"
    ],
    "type": "object"
   },
   "name": "debug"
  },
  {
   "description": "List available devices and simulators that can be selected for automation. Use platform, device, udid, or serial inputs on later commands to target one result. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "devices",
   "outputSchema": {
    "properties": {
     "devices": {
      "items": {
       "properties": {
        "id": {
         "type": "string"
        },
        "kind": {
         "enum": [
          "simulator",
          "emulator",
          "device"
         ],
         "type": "string"
        },
        "name": {
         "type": "string"
        },
        "platform": {
         "enum": [
          "ios",
          "macos",
          "android",
          "harmonyos",
          "vega",
          "linux",
          "web"
         ],
         "type": "string"
        },
        "target": {
         "enum": [
          "mobile",
          "tv",
          "desktop"
         ],
         "type": "string"
        }
       },
       "required": [
        "platform",
        "target",
        "kind",
        "id",
        "name"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "devices"
    ],
    "type": "object"
   }
  },
  {
   "description": "Compare accessibility snapshots or screenshots to identify UI changes. Use snapshot comparisons for semantic tree changes and screenshot comparisons for pixel differences. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "depth": {
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "interactiveOnly": {
      "type": "boolean"
     },
     "kind": {
      "const": "snapshot",
      "type": "string"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "out": {
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "raw": {
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scope": {
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "kind"
    ],
    "type": "object"
   },
   "name": "diff",
   "outputSchema": {
    "properties": {
     "baselineInitialized": {
      "type": "boolean"
     },
     "lines": {
      "items": {
       "properties": {
        "kind": {
         "enum": [
          "added",
          "removed"
         ],
         "type": "string"
        },
        "ref": {
         "type": "string"
        },
        "text": {
         "type": "string"
        }
       },
       "required": [
        "kind",
        "text"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "mode": {
      "const": "snapshot",
      "type": "string"
     },
     "summary": {
      "properties": {
       "additions": {
        "type": "number"
       },
       "removals": {
        "type": "number"
       },
       "unchanged": {
        "type": "number"
       }
      },
      "required": [
       "additions",
       "removals",
       "unchanged"
      ],
      "type": "object"
     },
     "warnings": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "mode",
     "baselineInitialized",
     "summary",
     "lines"
    ],
    "type": "object"
   }
  },
  {
   "description": "Diagnose device, app, development-server, and React Native or Expo readiness issues. Returns compact evidence for local inventory, sessions, optional app discovery, toolchains, and server reachability. On iOS simulators it also warms the XCTest runner build cache in the background when missing, so run it before the first Apple snapshot or interaction of a session. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "remote": {
      "description": "Check remote connection setup instead of local device inventory.",
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "targetApp": {
      "description": "Installed app package/bundle id or app name to verify without opening a session.",
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "doctor",
   "outputSchema": {
    "properties": {
     "checks": {
      "items": {
       "properties": {
        "command": {
         "type": "string"
        },
        "evidence": {
         "additionalProperties": true,
         "type": "object"
        },
        "hint": {
         "type": "string"
        },
        "id": {
         "type": "string"
        },
        "status": {
         "enum": [
          "pass",
          "warn",
          "fail",
          "info"
         ],
         "type": "string"
        },
        "summary": {
         "type": "string"
        }
       },
       "required": [
        "id",
        "status",
        "summary"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "kind": {
      "enum": [
       "auto",
       "react-native",
       "expo",
       "repack"
      ],
      "type": "string"
     },
     "metro": {
      "properties": {
       "host": {
        "type": "string"
       },
       "port": {
        "type": "number"
       }
      },
      "required": [
       "host",
       "port"
      ],
      "type": "object"
     },
     "platform": {
      "type": "string"
     },
     "status": {
      "enum": [
       "pass",
       "warn",
       "fail",
       "info"
      ],
      "type": "string"
     },
     "summary": {
      "type": "string"
     },
     "target": {
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "targetApp": {
      "type": "string"
     }
    },
    "required": [
     "status",
     "summary",
     "kind",
     "checks"
    ],
    "type": "object"
   }
  },
  {
   "description": "Read the daemon-owned session event timeline as paged JSON-friendly entries Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "cursor": {
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "limit": {
      "type": "integer"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "events"
  },
  {
   "description": "Replace text in a UI input selected by snapshot ref, selector, or coordinates. Pass an empty text to clear the field. Prefer refs or selectors after snapshot; use recordAs to keep sensitive text out of a recorded replay while sending it to the live app. Times out after 90s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "delayMs": {
      "description": "Delay between typed characters.",
      "minimum": 0,
      "type": "integer"
     },
     "depth": {
      "description": "Snapshot traversal depth.",
      "minimum": 0,
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "raw": {
      "description": "Use raw snapshot data during selector resolution.",
      "type": "boolean"
     },
     "recordAs": {
      "description": "When script recording is armed, send text to the live app but publish it as ${VAR}. Use an uppercase replay variable name such as PASSWORD.",
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scope": {
      "description": "Snapshot scope selector used before resolution.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "settle": {
      "description": "After the action, wait for the UI to go quiet and return the settled diff vs the pre-action tree in the same response. Best-effort; never fails the action.",
      "type": "boolean"
     },
     "settleQuietMs": {
      "description": "Settle: quiet window in milliseconds (default 500).",
      "minimum": 0,
      "type": "integer"
     },
     "target": {
      "description": "UI target. This is separate from deviceTarget, which selects the device form.",
      "oneOf": [
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "ref",
          "type": "string"
         },
         "label": {
          "description": "Optional human label for the ref.",
          "type": "string"
         },
         "ref": {
          "description": "Snapshot element ref such as @e12.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "ref"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "selector",
          "type": "string"
         },
         "selector": {
          "description": "Agent-device selector expression.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "selector"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "point",
          "type": "string"
         },
         "x": {
          "type": "number"
         },
         "y": {
          "type": "number"
         }
        },
        "required": [
         "kind",
         "x",
         "y"
        ],
        "type": "object"
       }
      ]
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "text": {
      "description": "Text to enter into the target. Pass \"\" to clear it.",
      "type": "string"
     },
     "timeoutMs": {
      "description": "Settle: wait deadline in milliseconds (default 10000).",
      "minimum": 1,
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "verify": {
      "description": "Capture cheap post-action evidence (AX digest, node counts, changedFromBefore) instead of a follow-up snapshot.",
      "type": "boolean"
     }
    },
    "required": [
     "target",
     "text"
    ],
    "type": "object"
   },
   "name": "fill",
   "outputSchema": {
    "description": "Fill response. Android may return target-bound unconfirmed evidence when the exact app-owned field changed but formatting prevented raw equality.",
    "oneOf": [
     {
      "not": {
       "properties": {},
       "required": [
        "verification"
       ],
       "type": "object"
      },
      "properties": {
       "cost": {
        "properties": {
         "nodeCount": {
          "description": "Number of nodes in the original node tree when the response carries one.",
          "type": "number"
         },
         "runnerRoundTrips": {
          "description": "Number of real runner round-trips made while serving the request.",
          "type": "number"
         },
         "wallClockMs": {
          "description": "Total wall-clock time for the request in milliseconds.",
          "type": "number"
         }
        },
        "required": [
         "wallClockMs",
         "runnerRoundTrips"
        ],
        "type": "object"
       },
       "delayMs": {
        "description": "Delay between typed characters in milliseconds.",
        "type": "number"
       },
       "evidence": {
        "properties": {
         "changedFromBefore": {
          "description": "Whether the post-action digest differs from the pre-action capture digest. false is evidence, not failure. With surfaceChange present, no digest comparison is made: it reports that surface transition.",
          "type": "boolean"
         },
         "digest": {
          "description": "Order-independent digest of the post-action node multiset.",
          "type": "string"
         },
         "foregroundApp": {
          "description": "Foreground app bundle id or name, when the capture carries it.",
          "type": "string"
         },
         "interactiveNodeCount": {
          "description": "Subset of nodeCount the platform reports as hittable.",
          "type": "number"
         },
         "nodeCount": {
          "description": "Node count in the post-action interactive-only capture.",
          "type": "number"
         },
         "surfaceChange": {
          "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
          "properties": {
           "disclosure": {
            "description": "Agent-facing sentence explaining the surface transition.",
            "type": "string"
           },
           "from": {
            "description": "Surface the pre-action baseline described: a host bundle id, or app.",
            "type": "string"
           },
           "to": {
            "description": "Surface the post-action capture describes: a host bundle id, or app.",
            "type": "string"
           }
          },
          "required": [
           "from",
           "to",
           "disclosure"
          ],
          "type": "object"
         }
        },
        "required": [
         "nodeCount",
         "interactiveNodeCount",
         "digest",
         "changedFromBefore"
        ],
        "type": "object"
       },
       "hint": {
        "type": "string"
       },
       "maestroFallbackReason": {
        "const": "non-hittable-coordinate",
        "type": "string"
       },
       "maestroNonHittableCoordinateFallbackAllowed": {
        "description": "Whether the direct iOS Maestro coordinate fallback was allowed for this selector.",
        "type": "boolean"
       },
       "maestroNonHittableCoordinateFallbackUsed": {
        "description": "Whether the direct iOS Maestro coordinate fallback was actually used.",
        "type": "boolean"
       },
       "message": {
        "type": "string"
       },
       "ref": {
        "description": "Snapshot ref without the @ prefix when the target was an @ref.",
        "type": "string"
       },
       "refLabel": {
        "type": "string"
       },
       "referenceHeight": {
        "description": "Reference frame height for visualizing the interaction point.",
        "type": "number"
       },
       "referenceWidth": {
        "description": "Reference frame width for visualizing the interaction point.",
        "type": "number"
       },
       "resolution": {
        "description": "Pre-action disclosure of how the acting path resolved its target. Absent when resolutionDisclosure is inapplicable for the path.",
        "oneOf": [
         {
          "properties": {
           "kind": {
            "const": "unique",
            "type": "string"
           },
           "phase": {
            "const": "pre-action",
            "type": "string"
           },
           "source": {
            "const": "runtime",
            "type": "string"
           }
          },
          "required": [
           "source",
           "phase",
           "kind"
          ],
          "type": "object"
         },
         {
          "properties": {
           "alternatives": {
            "description": "At most 5 losing candidates, document order. Present at default/full response levels and omitted in digest. The winner is never included.",
            "items": {
             "properties": {
              "diagnosticRef": {
               "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
               "type": "string"
              },
              "label": {
               "description": "UTF-8 truncated to 256 bytes.",
               "type": "string"
              },
              "role": {
               "description": "UTF-8 truncated to 256 bytes.",
               "type": "string"
              }
             },
             "required": [
              "diagnosticRef"
             ],
             "type": "object"
            },
            "type": "array"
           },
           "kind": {
            "const": "disambiguated",
            "type": "string"
           },
           "matchCount": {
            "description": "Total matches resolveSelectorChain found before disambiguation.",
            "type": "number"
           },
           "phase": {
            "const": "pre-action",
            "type": "string"
           },
           "source": {
            "const": "runtime",
            "type": "string"
           },
           "tiebreak": {
            "description": "The comparison that decided the winner.",
            "enum": [
             "visible",
             "deepest",
             "smallest-area",
             "structural-equivalence"
            ],
            "type": "string"
           },
           "winnerDiagnostic": {
            "properties": {
             "diagnosticRef": {
              "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
              "type": "string"
             },
             "label": {
              "description": "UTF-8 truncated to 256 bytes.",
              "type": "string"
             },
             "role": {
              "description": "UTF-8 truncated to 256 bytes.",
              "type": "string"
             }
            },
            "required": [
             "diagnosticRef"
            ],
            "type": "object"
           }
          },
          "required": [
           "source",
           "phase",
           "kind",
           "matchCount",
           "winnerDiagnostic",
           "tiebreak"
          ],
          "type": "object"
         },
         {
          "properties": {
           "kind": {
            "const": "exact",
            "type": "string"
           },
           "phase": {
            "const": "pre-action",
            "type": "string"
           },
           "source": {
            "const": "ref",
            "type": "string"
           }
          },
          "required": [
           "source",
           "phase",
           "kind"
          ],
          "type": "object"
         },
         {
          "properties": {
           "kind": {
            "const": "label-fallback",
            "type": "string"
           },
           "phase": {
            "const": "pre-action",
            "type": "string"
           },
           "source": {
            "const": "ref",
            "type": "string"
           }
          },
          "required": [
           "source",
           "phase",
           "kind"
          ],
          "type": "object"
         },
         {
          "properties": {
           "kind": {
            "const": "not-observed",
            "type": "string"
           },
           "source": {
            "const": "direct-ios",
            "type": "string"
           }
          },
          "required": [
           "source",
           "kind"
          ],
          "type": "object"
         }
        ],
        "type": "object"
       },
       "selector": {
        "description": "Selector expression when the target was a selector.",
        "type": "string"
       },
       "selectorChain": {
        "items": {
         "type": "string"
        },
        "type": "array"
       },
       "settle": {
        "properties": {
         "captures": {
          "type": "number"
         },
         "diff": {
          "description": "Settled diff vs the pre-action tree (changed lines only).",
          "properties": {
           "lines": {
            "items": {
             "properties": {
              "kind": {
               "enum": [
                "added",
                "removed"
               ],
               "type": "string"
              },
              "ref": {
               "description": "Plain ref body (e12) for added lines.",
               "type": "string"
              },
              "text": {
               "type": "string"
              }
             },
             "required": [
              "kind",
              "text"
             ],
             "type": "object"
            },
            "type": "array"
           },
           "summary": {
            "properties": {
             "additions": {
              "type": "number"
             },
             "removals": {
              "type": "number"
             },
             "unchanged": {
              "type": "number"
             }
            },
            "required": [
             "additions",
             "removals",
             "unchanged"
            ],
            "type": "object"
           },
           "truncated": {
            "description": "Lines were capped to the response bound.",
            "type": "boolean"
           }
          },
          "required": [
           "summary",
           "lines"
          ],
          "type": "object"
         },
         "hint": {
          "type": "string"
         },
         "quietMs": {
          "type": "number"
         },
         "refs": {
          "items": {
           "properties": {
            "ref": {
             "description": "Plain ref body (e12) minted from the stored settled tree.",
             "type": "string"
            }
           },
           "required": [
            "ref"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "refsGeneration": {
          "description": "Snapshot generation of the stored settled tree; refs on added diff lines were minted from it.",
          "type": "number"
         },
         "settled": {
          "description": "Whether the UI held the quiet window before the deadline. false is advisory, not failure.",
          "type": "boolean"
         },
         "surfaceChange": {
          "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
          "properties": {
           "disclosure": {
            "description": "Agent-facing sentence explaining the surface transition.",
            "type": "string"
           },
           "from": {
            "description": "Surface the pre-action baseline described: a host bundle id, or app.",
            "type": "string"
           },
           "to": {
            "description": "Surface the post-action capture describes: a host bundle id, or app.",
            "type": "string"
           }
          },
          "required": [
           "from",
           "to",
           "disclosure"
          ],
          "type": "object"
         },
         "tail": {
          "description": "Unchanged interactive refs tail: still-present, actionable elements from the settled tree, attached only when diff carries zero added-line refs (a modal-dismiss/toast-only diff).",
          "items": {
           "properties": {
            "label": {
             "type": "string"
            },
            "ref": {
             "description": "Plain ref body (e12) minted from the stored settled tree.",
             "type": "string"
            },
            "role": {
             "type": "string"
            }
           },
           "required": [
            "ref",
            "role"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "tailTruncated": {
          "description": "Present (true) when tail candidates exceeded the response cap.",
          "type": "boolean"
         },
         "timeoutMs": {
          "type": "number"
         },
         "waitedMs": {
          "type": "number"
         }
        },
        "required": [
         "settled",
         "waitedMs",
         "captures",
         "quietMs",
         "timeoutMs"
        ],
        "type": "object"
       },
       "targetHittable": {
        "type": "boolean"
       },
       "targetKind": {
        "description": "Resolved interaction target kind.",
        "enum": [
         "point",
         "ref",
         "selector"
        ],
        "type": "string"
       },
       "text": {
        "description": "Text submitted to the field.",
        "type": "string"
       },
       "warning": {
        "type": "string"
       },
       "x": {
        "description": "Resolved interaction x coordinate when available.",
        "type": "number"
       },
       "y": {
        "description": "Resolved interaction y coordinate when available.",
        "type": "number"
       }
      },
      "required": [
       "targetKind",
       "text"
      ],
      "type": "object"
     },
     {
      "properties": {
       "after": {
        "description": "Raw target text captured after the fill.",
        "type": [
         "string",
         "null"
        ]
       },
       "before": {
        "description": "Raw target text captured before the fill.",
        "type": [
         "string",
         "null"
        ]
       },
       "cost": {
        "properties": {
         "nodeCount": {
          "description": "Number of nodes in the original node tree when the response carries one.",
          "type": "number"
         },
         "runnerRoundTrips": {
          "description": "Number of real runner round-trips made while serving the request.",
          "type": "number"
         },
         "wallClockMs": {
          "description": "Total wall-clock time for the request in milliseconds.",
          "type": "number"
         }
        },
        "required": [
         "wallClockMs",
         "runnerRoundTrips"
        ],
        "type": "object"
       },
       "delayMs": {
        "description": "Delay between typed characters in milliseconds.",
        "type": "number"
       },
       "evidence": {
        "properties": {
         "changedFromBefore": {
          "description": "Whether the post-action digest differs from the pre-action capture digest. false is evidence, not failure. With surfaceChange present, no digest comparison is made: it reports that surface transition.",
          "type": "boolean"
         },
         "digest": {
          "description": "Order-independent digest of the post-action node multiset.",
          "type": "string"
         },
         "foregroundApp": {
          "description": "Foreground app bundle id or name, when the capture carries it.",
          "type": "string"
         },
         "interactiveNodeCount": {
          "description": "Subset of nodeCount the platform reports as hittable.",
          "type": "number"
         },
         "nodeCount": {
          "description": "Node count in the post-action interactive-only capture.",
          "type": "number"
         },
         "surfaceChange": {
          "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
          "properties": {
           "disclosure": {
            "description": "Agent-facing sentence explaining the surface transition.",
            "type": "string"
           },
           "from": {
            "description": "Surface the pre-action baseline described: a host bundle id, or app.",
            "type": "string"
           },
           "to": {
            "description": "Surface the post-action capture describes: a host bundle id, or app.",
            "type": "string"
           }
          },
          "required": [
           "from",
           "to",
           "disclosure"
          ],
          "type": "object"
         }
        },
        "required": [
         "nodeCount",
         "interactiveNodeCount",
         "digest",
         "changedFromBefore"
        ],
        "type": "object"
       },
       "hint": {
        "type": "string"
       },
       "maestroFallbackReason": {
        "const": "non-hittable-coordinate",
        "type": "string"
       },
       "maestroNonHittableCoordinateFallbackAllowed": {
        "description": "Whether the direct iOS Maestro coordinate fallback was allowed for this selector.",
        "type": "boolean"
       },
       "maestroNonHittableCoordinateFallbackUsed": {
        "description": "Whether the direct iOS Maestro coordinate fallback was actually used.",
        "type": "boolean"
       },
       "message": {
        "type": "string"
       },
       "ref": {
        "description": "Snapshot ref without the @ prefix when the target was an @ref.",
        "type": "string"
       },
       "refLabel": {
        "type": "string"
       },
       "referenceHeight": {
        "description": "Reference frame height for visualizing the interaction point.",
        "type": "number"
       },
       "referenceWidth": {
        "description": "Reference frame width for visualizing the interaction point.",
        "type": "number"
       },
       "requested": {
        "description": "Literal text requested by the fill command.",
        "type": "string"
       },
       "resolution": {
        "description": "Pre-action disclosure of how the acting path resolved its target. Absent when resolutionDisclosure is inapplicable for the path.",
        "oneOf": [
         {
          "properties": {
           "kind": {
            "const": "unique",
            "type": "string"
           },
           "phase": {
            "const": "pre-action",
            "type": "string"
           },
           "source": {
            "const": "runtime",
            "type": "string"
           }
          },
          "required": [
           "source",
           "phase",
           "kind"
          ],
          "type": "object"
         },
         {
          "properties": {
           "alternatives": {
            "description": "At most 5 losing candidates, document order. Present at default/full response levels and omitted in digest. The winner is never included.",
            "items": {
             "properties": {
              "diagnosticRef": {
               "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
               "type": "string"
              },
              "label": {
               "description": "UTF-8 truncated to 256 bytes.",
               "type": "string"
              },
              "role": {
               "description": "UTF-8 truncated to 256 bytes.",
               "type": "string"
              }
             },
             "required": [
              "diagnosticRef"
             ],
             "type": "object"
            },
            "type": "array"
           },
           "kind": {
            "const": "disambiguated",
            "type": "string"
           },
           "matchCount": {
            "description": "Total matches resolveSelectorChain found before disambiguation.",
            "type": "number"
           },
           "phase": {
            "const": "pre-action",
            "type": "string"
           },
           "source": {
            "const": "runtime",
            "type": "string"
           },
           "tiebreak": {
            "description": "The comparison that decided the winner.",
            "enum": [
             "visible",
             "deepest",
             "smallest-area",
             "structural-equivalence"
            ],
            "type": "string"
           },
           "winnerDiagnostic": {
            "properties": {
             "diagnosticRef": {
              "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
              "type": "string"
             },
             "label": {
              "description": "UTF-8 truncated to 256 bytes.",
              "type": "string"
             },
             "role": {
              "description": "UTF-8 truncated to 256 bytes.",
              "type": "string"
             }
            },
            "required": [
             "diagnosticRef"
            ],
            "type": "object"
           }
          },
          "required": [
           "source",
           "phase",
           "kind",
           "matchCount",
           "winnerDiagnostic",
           "tiebreak"
          ],
          "type": "object"
         },
         {
          "properties": {
           "kind": {
            "const": "exact",
            "type": "string"
           },
           "phase": {
            "const": "pre-action",
            "type": "string"
           },
           "source": {
            "const": "ref",
            "type": "string"
           }
          },
          "required": [
           "source",
           "phase",
           "kind"
          ],
          "type": "object"
         },
         {
          "properties": {
           "kind": {
            "const": "label-fallback",
            "type": "string"
           },
           "phase": {
            "const": "pre-action",
            "type": "string"
           },
           "source": {
            "const": "ref",
            "type": "string"
           }
          },
          "required": [
           "source",
           "phase",
           "kind"
          ],
          "type": "object"
         },
         {
          "properties": {
           "kind": {
            "const": "not-observed",
            "type": "string"
           },
           "source": {
            "const": "direct-ios",
            "type": "string"
           }
          },
          "required": [
           "source",
           "kind"
          ],
          "type": "object"
         }
        ],
        "type": "object"
       },
       "selector": {
        "description": "Selector expression when the target was a selector.",
        "type": "string"
       },
       "selectorChain": {
        "items": {
         "type": "string"
        },
        "type": "array"
       },
       "settle": {
        "properties": {
         "captures": {
          "type": "number"
         },
         "diff": {
          "description": "Settled diff vs the pre-action tree (changed lines only).",
          "properties": {
           "lines": {
            "items": {
             "properties": {
              "kind": {
               "enum": [
                "added",
                "removed"
               ],
               "type": "string"
              },
              "ref": {
               "description": "Plain ref body (e12) for added lines.",
               "type": "string"
              },
              "text": {
               "type": "string"
              }
             },
             "required": [
              "kind",
              "text"
             ],
             "type": "object"
            },
            "type": "array"
           },
           "summary": {
            "properties": {
             "additions": {
              "type": "number"
             },
             "removals": {
              "type": "number"
             },
             "unchanged": {
              "type": "number"
             }
            },
            "required": [
             "additions",
             "removals",
             "unchanged"
            ],
            "type": "object"
           },
           "truncated": {
            "description": "Lines were capped to the response bound.",
            "type": "boolean"
           }
          },
          "required": [
           "summary",
           "lines"
          ],
          "type": "object"
         },
         "hint": {
          "type": "string"
         },
         "quietMs": {
          "type": "number"
         },
         "refs": {
          "items": {
           "properties": {
            "ref": {
             "description": "Plain ref body (e12) minted from the stored settled tree.",
             "type": "string"
            }
           },
           "required": [
            "ref"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "refsGeneration": {
          "description": "Snapshot generation of the stored settled tree; refs on added diff lines were minted from it.",
          "type": "number"
         },
         "settled": {
          "description": "Whether the UI held the quiet window before the deadline. false is advisory, not failure.",
          "type": "boolean"
         },
         "surfaceChange": {
          "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
          "properties": {
           "disclosure": {
            "description": "Agent-facing sentence explaining the surface transition.",
            "type": "string"
           },
           "from": {
            "description": "Surface the pre-action baseline described: a host bundle id, or app.",
            "type": "string"
           },
           "to": {
            "description": "Surface the post-action capture describes: a host bundle id, or app.",
            "type": "string"
           }
          },
          "required": [
           "from",
           "to",
           "disclosure"
          ],
          "type": "object"
         },
         "tail": {
          "description": "Unchanged interactive refs tail: still-present, actionable elements from the settled tree, attached only when diff carries zero added-line refs (a modal-dismiss/toast-only diff).",
          "items": {
           "properties": {
            "label": {
             "type": "string"
            },
            "ref": {
             "description": "Plain ref body (e12) minted from the stored settled tree.",
             "type": "string"
            },
            "role": {
             "type": "string"
            }
           },
           "required": [
            "ref",
            "role"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "tailTruncated": {
          "description": "Present (true) when tail candidates exceeded the response cap.",
          "type": "boolean"
         },
         "timeoutMs": {
          "type": "number"
         },
         "waitedMs": {
          "type": "number"
         }
        },
        "required": [
         "settled",
         "waitedMs",
         "captures",
         "quietMs",
         "timeoutMs"
        ],
        "type": "object"
       },
       "target": {
        "description": "Target identity captured before fill and matched after fill.",
        "properties": {
         "className": {
          "description": "Android class name of the exact field that changed.",
          "type": [
           "string",
           "null"
          ]
         },
         "packageName": {
          "description": "Android package name that owns the exact field.",
          "type": [
           "string",
           "null"
          ]
         },
         "rect": {
          "description": "Screen-space rectangle of the exact field that changed.",
          "properties": {
           "height": {
            "type": "number"
           },
           "width": {
            "type": "number"
           },
           "x": {
            "type": "number"
           },
           "y": {
            "type": "number"
           }
          },
          "required": [
           "x",
           "y",
           "width",
           "height"
          ],
          "type": "object"
         },
         "resourceId": {
          "description": "Android resource id of the exact field that changed.",
          "type": [
           "string",
           "null"
          ]
         }
        },
        "required": [
         "resourceId",
         "className",
         "packageName",
         "rect"
        ],
        "type": "object"
       },
       "targetHittable": {
        "type": "boolean"
       },
       "targetKind": {
        "description": "Resolved interaction target kind.",
        "enum": [
         "point",
         "ref",
         "selector"
        ],
        "type": "string"
       },
       "text": {
        "description": "Text submitted to the field.",
        "type": "string"
       },
       "verification": {
        "const": "unconfirmed",
        "type": "string"
       },
       "warning": {
        "type": "string"
       },
       "x": {
        "description": "Resolved interaction x coordinate when available.",
        "type": "number"
       },
       "y": {
        "description": "Resolved interaction y coordinate when available.",
        "type": "number"
       }
      },
      "required": [
       "targetKind",
       "text",
       "verification",
       "requested",
       "before",
       "after",
       "target"
      ],
      "type": "object"
     }
    ],
    "type": "object"
   }
  },
  {
   "description": "Find by text/label/value/role/id and run action Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "click",
       "focus",
       "exists",
       "list",
       "getText",
       "getAttrs",
       "wait",
       "fill",
       "type"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "depth": {
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "first": {
      "type": "boolean"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "last": {
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "locator": {
      "enum": [
       "any",
       "text",
       "label",
       "value",
       "role",
       "id"
      ],
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "query": {
      "type": "string"
     },
     "raw": {
      "type": "boolean"
     },
     "record": {
      "description": "Force-record this out-of-band observation into a repair-armed heal (mutually exclusive with noRecord). Authored replay steps are recorded automatically and never need this. On find, valid only for a read-only action.",
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "value": {
      "type": "string"
     }
    },
    "required": [
     "query"
    ],
    "type": "object"
   },
   "name": "find",
   "outputSchema": {
    "description": "Daemon response data for the find command.",
    "properties": {
     "cost": {
      "properties": {
       "nodeCount": {
        "description": "Number of nodes in the original node tree when the response carries one.",
        "type": "number"
       },
       "runnerRoundTrips": {
        "description": "Number of real runner round-trips made while serving the request.",
        "type": "number"
       },
       "wallClockMs": {
        "description": "Total wall-clock time for the request in milliseconds.",
        "type": "number"
       }
      },
      "required": [
       "wallClockMs",
       "runnerRoundTrips"
      ],
      "type": "object"
     },
     "found": {
      "description": "Whether a wait/exists/read-only find satisfied its condition.",
      "type": "boolean"
     },
     "locator": {
      "description": "Locator kind used for the find action.",
      "type": "string"
     },
     "matches": {
      "description": "Every match for the read-only find list action (#1625): { ref, node } each.",
      "items": {
       "additionalProperties": true,
       "description": "One listed match with its snapshot ref and node.",
       "type": "object"
      },
      "type": "array"
     },
     "message": {
      "description": "Diagnostic message for mutating find actions.",
      "type": "string"
     },
     "node": {
      "additionalProperties": true,
      "description": "Snapshot node for find get_attrs/get_text.",
      "type": "object"
     },
     "query": {
      "description": "Query argument used for the find action.",
      "type": "string"
     },
     "ref": {
      "description": "Snapshot ref without the @ prefix when the find action returns one.",
      "type": "string"
     },
     "refsGeneration": {
      "description": "ADR 0014 ref frame epoch for read-only find actions.",
      "type": "number"
     },
     "text": {
      "description": "Text value returned by find get_text.",
      "type": "string"
     },
     "waitedMs": {
      "description": "Milliseconds waited for a read-only find condition.",
      "type": "number"
     },
     "x": {
      "description": "Resolved x coordinate for mutating find actions.",
      "type": "number"
     },
     "y": {
      "description": "Resolved y coordinate for mutating find actions.",
      "type": "number"
     }
    },
    "type": "object"
   }
  },
  {
   "description": "Move input focus to explicit screen coordinates without entering text. Prefer semantic interactions when a snapshot ref or selector is available; use type or fill after focus. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "x": {
      "description": "X coordinate.",
      "type": "number"
     },
     "y": {
      "description": "Y coordinate.",
      "type": "number"
     }
    },
    "required": [
     "x",
     "y"
    ],
    "type": "object"
   },
   "name": "focus"
  },
  {
   "description": "Fold or unfold a foldable iPhone simulator (iPhone Duo) into the closed, half-open, or open pose by pressing the pose control in Xcode Device Hub, then read the hinge angle back from CoreDevice to confirm it. A pose change moves the app to a different panel with a different point size, so every ref and coordinate from before it is stale: re-snapshot after this command. Taps, long presses, and scrolling target the app window on its current panel in closed, half-open, and open poses. Simulator-only; the device window must be open in Device Hub and the host needs Accessibility permission. Times out after 150s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "pose": {
      "description": "The hinge pose to reach: closed lights the outer panel; half-open (Device Hub Book) and open light the inner panel.",
      "enum": [
       "closed",
       "half-open",
       "open"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "pose"
    ],
    "type": "object"
   },
   "name": "fold",
   "outputSchema": {
    "properties": {
     "action": {
      "const": "fold",
      "type": "string"
     },
     "hingeAngleDegrees": {
      "description": "Hinge angle CoreDevice read back after the pose settled.",
      "type": "number"
     },
     "message": {
      "type": "string"
     },
     "pose": {
      "enum": [
       "closed",
       "half-open",
       "open"
      ],
      "type": "string"
     },
     "screen": {
      "properties": {
       "coordinateSpace": {
        "const": "native-panel",
        "type": "string"
       },
       "display": {
        "description": "CoreDevice name of the panel the device now lights.",
        "type": "string"
       },
       "heightPt": {
        "description": "Panel height in native panel points (pixels divided by point scale), NOT snapshot coordinates; take a fresh snapshot to place a tap.",
        "type": "number"
       },
       "widthPt": {
        "description": "Panel width in native panel points (pixels divided by point scale), NOT snapshot coordinates; take a fresh snapshot to place a tap.",
        "type": "number"
       }
      },
      "required": [
       "display",
       "coordinateSpace",
       "widthPt",
       "heightPt"
      ],
      "type": "object"
     }
    },
    "required": [
     "action",
     "pose",
     "hingeAngleDegrees",
     "message"
    ],
    "type": "object"
   }
  },
  {
   "description": "Perform a structured pan, fling, swipe, pinch, rotate, transform, or drag gesture. Select the gesture kind, then provide only the inputs that apply to that kind. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "degrees": {
      "description": "Rotation in degrees.",
      "type": "number"
     },
     "delta": {
      "additionalProperties": false,
      "description": "Movement delta for pan or transform gestures.",
      "properties": {
       "x": {
        "type": "number"
       },
       "y": {
        "type": "number"
       }
      },
      "required": [
       "x",
       "y"
      ],
      "type": "object"
     },
     "destination": {
      "description": "Drag destination @ref or selector.",
      "type": "string"
     },
     "destinationHoldMs": {
      "description": "Hold before releasing at the destination.",
      "maximum": 10000,
      "minimum": 0,
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "direction": {
      "description": "Fling direction.",
      "enum": [
       "up",
       "down",
       "left",
       "right"
      ],
      "type": "string"
     },
     "distance": {
      "description": "Fling distance.",
      "minimum": 0,
      "type": "integer"
     },
     "durationMs": {
      "description": "Pan/transform duration.",
      "maximum": 10000,
      "minimum": 16,
      "type": "integer"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "kind": {
      "description": "Gesture variant.",
      "enum": [
       "pan",
       "fling",
       "swipe",
       "pinch",
       "rotate",
       "transform",
       "drag"
      ],
      "type": "string"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "moveMs": {
      "description": "Drag movement duration.",
      "maximum": 10000,
      "minimum": 16,
      "type": "integer"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "origin": {
      "additionalProperties": false,
      "description": "Gesture origin point.",
      "properties": {
       "x": {
        "type": "number"
       },
       "y": {
        "type": "number"
       }
      },
      "required": [
       "x",
       "y"
      ],
      "type": "object"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "pointerCount": {
      "description": "Pan touch pointer count (1 or 2).",
      "maximum": 2,
      "minimum": 1,
      "type": "integer"
     },
     "preset": {
      "description": "Swipe preset.",
      "enum": [
       "left",
       "right",
       "left-edge",
       "right-edge"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scale": {
      "description": "Pinch or transform scale.",
      "type": "number"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "source": {
      "description": "Drag source @ref or selector.",
      "type": "string"
     },
     "sourceHoldMs": {
      "description": "Drag activation hold duration.",
      "maximum": 10000,
      "minimum": 1,
      "type": "integer"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "kind"
    ],
    "type": "object"
   },
   "name": "gesture"
  },
  {
   "description": "Read text or accessibility attributes from a snapshot ref or selector without changing the app. Use format text for visible content or attrs for the element attribute map. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "depth": {
      "description": "Snapshot traversal depth.",
      "minimum": 0,
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "format": {
      "enum": [
       "text",
       "attrs"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "raw": {
      "description": "Use raw snapshot data during selector resolution.",
      "type": "boolean"
     },
     "record": {
      "description": "Force-record this out-of-band observation into a repair-armed heal (mutually exclusive with noRecord). Authored replay steps are recorded automatically and never need this. On find, valid only for a read-only action.",
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scope": {
      "description": "Snapshot scope selector used before resolution.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "UI element target by snapshot ref or selector expression.",
      "oneOf": [
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "ref",
          "type": "string"
         },
         "label": {
          "description": "Optional human label for the ref.",
          "type": "string"
         },
         "ref": {
          "description": "Snapshot element ref such as @e12.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "ref"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "selector",
          "type": "string"
         },
         "selector": {
          "description": "Agent-device selector expression.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "selector"
        ],
        "type": "object"
       }
      ]
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "format",
     "target"
    ],
    "type": "object"
   },
   "name": "get"
  },
  {
   "description": "Usage guides. No topic: the full workflow card. topic = one of commands, manual-qa, maestro, workflow, scripting, gestures, tv, debugging, react-devtools, cdp, react-native, physical-device, ios-system-ui, foldable, remote, macos, web, dogfood, validate for that guide, or a tool name for its complete flag reference. For specialized work or an unclear command shape only; not a startup step, not needed after an error that carries a hint.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "topic": {
      "description": "Guide topic or tool name; omit for the card.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "help"
  },
  {
   "description": "Send the selected device to its home screen. This leaves the app session open but moves the foreground away from the app. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "home",
   "outputSchema": {
    "properties": {
     "action": {
      "const": "home",
      "type": "string"
     },
     "message": {
      "type": "string"
     }
    },
    "required": [
     "action",
     "message"
    ],
    "type": "object"
   }
  },
  {
   "description": "Move the pointer over a UI target by snapshot ref, selector, or coordinates without pressing, to reveal hover-gated UI such as row toolbars or menus. Web only; touch platforms have no hover state. Use settle to observe what the hover revealed without a follow-up snapshot. Times out after 90s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "depth": {
      "description": "Snapshot traversal depth.",
      "minimum": 0,
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "raw": {
      "description": "Use raw snapshot data during selector resolution.",
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scope": {
      "description": "Snapshot scope selector used before resolution.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "settle": {
      "description": "After the action, wait for the UI to go quiet and return the settled diff vs the pre-action tree in the same response. Best-effort; never fails the action.",
      "type": "boolean"
     },
     "settleQuietMs": {
      "description": "Settle: quiet window in milliseconds (default 500).",
      "minimum": 0,
      "type": "integer"
     },
     "target": {
      "description": "UI target. This is separate from deviceTarget, which selects the device form.",
      "oneOf": [
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "ref",
          "type": "string"
         },
         "label": {
          "description": "Optional human label for the ref.",
          "type": "string"
         },
         "ref": {
          "description": "Snapshot element ref such as @e12.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "ref"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "selector",
          "type": "string"
         },
         "selector": {
          "description": "Agent-device selector expression.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "selector"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "point",
          "type": "string"
         },
         "x": {
          "type": "number"
         },
         "y": {
          "type": "number"
         }
        },
        "required": [
         "kind",
         "x",
         "y"
        ],
        "type": "object"
       }
      ]
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "description": "Settle: wait deadline in milliseconds (default 10000).",
      "minimum": 1,
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "target"
    ],
    "type": "object"
   },
   "name": "hover",
   "outputSchema": {
    "properties": {
     "cost": {
      "properties": {
       "nodeCount": {
        "description": "Number of nodes in the original node tree when the response carries one.",
        "type": "number"
       },
       "runnerRoundTrips": {
        "description": "Number of real runner round-trips made while serving the request.",
        "type": "number"
       },
       "wallClockMs": {
        "description": "Total wall-clock time for the request in milliseconds.",
        "type": "number"
       }
      },
      "required": [
       "wallClockMs",
       "runnerRoundTrips"
      ],
      "type": "object"
     },
     "evidence": {
      "properties": {
       "changedFromBefore": {
        "description": "Whether the post-action digest differs from the pre-action capture digest. false is evidence, not failure. With surfaceChange present, no digest comparison is made: it reports that surface transition.",
        "type": "boolean"
       },
       "digest": {
        "description": "Order-independent digest of the post-action node multiset.",
        "type": "string"
       },
       "foregroundApp": {
        "description": "Foreground app bundle id or name, when the capture carries it.",
        "type": "string"
       },
       "interactiveNodeCount": {
        "description": "Subset of nodeCount the platform reports as hittable.",
        "type": "number"
       },
       "nodeCount": {
        "description": "Node count in the post-action interactive-only capture.",
        "type": "number"
       },
       "surfaceChange": {
        "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
        "properties": {
         "disclosure": {
          "description": "Agent-facing sentence explaining the surface transition.",
          "type": "string"
         },
         "from": {
          "description": "Surface the pre-action baseline described: a host bundle id, or app.",
          "type": "string"
         },
         "to": {
          "description": "Surface the post-action capture describes: a host bundle id, or app.",
          "type": "string"
         }
        },
        "required": [
         "from",
         "to",
         "disclosure"
        ],
        "type": "object"
       }
      },
      "required": [
       "nodeCount",
       "interactiveNodeCount",
       "digest",
       "changedFromBefore"
      ],
      "type": "object"
     },
     "gesture": {
      "const": "hover",
      "type": "string"
     },
     "hint": {
      "type": "string"
     },
     "maestroFallbackReason": {
      "const": "non-hittable-coordinate",
      "type": "string"
     },
     "maestroNonHittableCoordinateFallbackAllowed": {
      "description": "Whether the direct iOS Maestro coordinate fallback was allowed for this selector.",
      "type": "boolean"
     },
     "maestroNonHittableCoordinateFallbackUsed": {
      "description": "Whether the direct iOS Maestro coordinate fallback was actually used.",
      "type": "boolean"
     },
     "message": {
      "type": "string"
     },
     "ref": {
      "description": "Snapshot ref without the @ prefix when the target was an @ref.",
      "type": "string"
     },
     "refLabel": {
      "type": "string"
     },
     "referenceHeight": {
      "description": "Reference frame height for visualizing the interaction point.",
      "type": "number"
     },
     "referenceWidth": {
      "description": "Reference frame width for visualizing the interaction point.",
      "type": "number"
     },
     "resolution": {
      "description": "Pre-action disclosure of how the acting path resolved its target. Absent when resolutionDisclosure is inapplicable for the path.",
      "oneOf": [
       {
        "properties": {
         "kind": {
          "const": "unique",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "runtime",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "alternatives": {
          "description": "At most 5 losing candidates, document order. Present at default/full response levels and omitted in digest. The winner is never included.",
          "items": {
           "properties": {
            "diagnosticRef": {
             "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
             "type": "string"
            },
            "label": {
             "description": "UTF-8 truncated to 256 bytes.",
             "type": "string"
            },
            "role": {
             "description": "UTF-8 truncated to 256 bytes.",
             "type": "string"
            }
           },
           "required": [
            "diagnosticRef"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "kind": {
          "const": "disambiguated",
          "type": "string"
         },
         "matchCount": {
          "description": "Total matches resolveSelectorChain found before disambiguation.",
          "type": "number"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "runtime",
          "type": "string"
         },
         "tiebreak": {
          "description": "The comparison that decided the winner.",
          "enum": [
           "visible",
           "deepest",
           "smallest-area",
           "structural-equivalence"
          ],
          "type": "string"
         },
         "winnerDiagnostic": {
          "properties": {
           "diagnosticRef": {
            "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
            "type": "string"
           },
           "label": {
            "description": "UTF-8 truncated to 256 bytes.",
            "type": "string"
           },
           "role": {
            "description": "UTF-8 truncated to 256 bytes.",
            "type": "string"
           }
          },
          "required": [
           "diagnosticRef"
          ],
          "type": "object"
         }
        },
        "required": [
         "source",
         "phase",
         "kind",
         "matchCount",
         "winnerDiagnostic",
         "tiebreak"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "exact",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "ref",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "label-fallback",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "ref",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "not-observed",
          "type": "string"
         },
         "source": {
          "const": "direct-ios",
          "type": "string"
         }
        },
        "required": [
         "source",
         "kind"
        ],
        "type": "object"
       }
      ],
      "type": "object"
     },
     "selector": {
      "description": "Selector expression when the target was a selector.",
      "type": "string"
     },
     "selectorChain": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "settle": {
      "properties": {
       "captures": {
        "type": "number"
       },
       "diff": {
        "description": "Settled diff vs the pre-action tree (changed lines only).",
        "properties": {
         "lines": {
          "items": {
           "properties": {
            "kind": {
             "enum": [
              "added",
              "removed"
             ],
             "type": "string"
            },
            "ref": {
             "description": "Plain ref body (e12) for added lines.",
             "type": "string"
            },
            "text": {
             "type": "string"
            }
           },
           "required": [
            "kind",
            "text"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "summary": {
          "properties": {
           "additions": {
            "type": "number"
           },
           "removals": {
            "type": "number"
           },
           "unchanged": {
            "type": "number"
           }
          },
          "required": [
           "additions",
           "removals",
           "unchanged"
          ],
          "type": "object"
         },
         "truncated": {
          "description": "Lines were capped to the response bound.",
          "type": "boolean"
         }
        },
        "required": [
         "summary",
         "lines"
        ],
        "type": "object"
       },
       "hint": {
        "type": "string"
       },
       "quietMs": {
        "type": "number"
       },
       "refs": {
        "items": {
         "properties": {
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          }
         },
         "required": [
          "ref"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "refsGeneration": {
        "description": "Snapshot generation of the stored settled tree; refs on added diff lines were minted from it.",
        "type": "number"
       },
       "settled": {
        "description": "Whether the UI held the quiet window before the deadline. false is advisory, not failure.",
        "type": "boolean"
       },
       "surfaceChange": {
        "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
        "properties": {
         "disclosure": {
          "description": "Agent-facing sentence explaining the surface transition.",
          "type": "string"
         },
         "from": {
          "description": "Surface the pre-action baseline described: a host bundle id, or app.",
          "type": "string"
         },
         "to": {
          "description": "Surface the post-action capture describes: a host bundle id, or app.",
          "type": "string"
         }
        },
        "required": [
         "from",
         "to",
         "disclosure"
        ],
        "type": "object"
       },
       "tail": {
        "description": "Unchanged interactive refs tail: still-present, actionable elements from the settled tree, attached only when diff carries zero added-line refs (a modal-dismiss/toast-only diff).",
        "items": {
         "properties": {
          "label": {
           "type": "string"
          },
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          },
          "role": {
           "type": "string"
          }
         },
         "required": [
          "ref",
          "role"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "tailTruncated": {
        "description": "Present (true) when tail candidates exceeded the response cap.",
        "type": "boolean"
       },
       "timeoutMs": {
        "type": "number"
       },
       "waitedMs": {
        "type": "number"
       }
      },
      "required": [
       "settled",
       "waitedMs",
       "captures",
       "quietMs",
       "timeoutMs"
      ],
      "type": "object"
     },
     "targetHittable": {
      "type": "boolean"
     },
     "targetKind": {
      "description": "Resolved interaction target kind.",
      "enum": [
       "point",
       "ref",
       "selector"
      ],
      "type": "string"
     },
     "warning": {
      "type": "string"
     },
     "x": {
      "description": "Resolved interaction x coordinate when available.",
      "type": "number"
     },
     "y": {
      "description": "Resolved interaction y coordinate when available.",
      "type": "number"
     }
    },
    "required": [
     "targetKind"
    ],
    "type": "object"
   }
  },
  {
   "description": "Install an app binary from a local path. Provide an app identifier with the path when the target needs explicit app selection; use reinstall to replace an already installed app. Times out after 180s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "app": {
      "description": "Optional app identifier hint.",
      "type": "string"
     },
     "appPath": {
      "description": "Path to app binary.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "appPath"
    ],
    "type": "object"
   },
   "name": "install"
  },
  {
   "description": "Install app builds from URLs, remote source specs, or CI artifacts resolved by a remote daemon. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "retainPaths": {
      "type": "boolean"
     },
     "retentionMs": {
      "type": "integer"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "source": {
      "additionalProperties": true,
      "description": "Install source object.",
      "type": "object"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "source"
    ],
    "type": "object"
   },
   "name": "install-from-source"
  },
  {
   "description": "Check whether a selector satisfies a UI predicate such as visible, hidden, exists, absent, editable, selected, focused, or text. `absent` passes only when one readable, complete, unscoped, full-depth accessibility capture has zero matches. Use wait when the condition may appear asynchronously. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "depth": {
      "description": "Snapshot traversal depth.",
      "minimum": 0,
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "predicate": {
      "enum": [
       "visible",
       "hidden",
       "exists",
       "absent",
       "editable",
       "selected",
       "focused",
       "text"
      ],
      "type": "string"
     },
     "raw": {
      "description": "Use raw snapshot data during selector resolution.",
      "type": "boolean"
     },
     "record": {
      "description": "Force-record this out-of-band observation into a repair-armed heal (mutually exclusive with noRecord). Authored replay steps are recorded automatically and never need this. On find, valid only for a read-only action.",
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scope": {
      "description": "Snapshot scope selector used before resolution.",
      "type": "string"
     },
     "selector": {
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "value": {
      "type": "string"
     }
    },
    "required": [
     "predicate",
     "selector"
    ],
    "type": "object"
   },
   "name": "is"
  },
  {
   "description": "Inspect Android keyboard visibility/type or press/dismiss the device keyboard. To hide the keyboard, use keyboard dismiss. It taps the keyboard dismiss/hide key when one is exposed, verifies the keyboard closed, and reports UNSUPPORTED_OPERATION when no dismiss key exists \u2014 background taps are never attempted. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "status",
       "dismiss",
       "enter",
       "return"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "keyboard",
   "outputSchema": {
    "properties": {
     "action": {
      "enum": [
       "status",
       "dismiss",
       "enter"
      ],
      "type": "string"
     },
     "attempts": {
      "type": "number"
     },
     "dismissed": {
      "type": "boolean"
     },
     "focusedPackage": {
      "type": "string"
     },
     "focusedResourceId": {
      "type": "string"
     },
     "inputMethodPackage": {
      "type": "string"
     },
     "inputOwner": {
      "enum": [
       "app",
       "ime",
       "unknown"
      ],
      "type": "string"
     },
     "inputType": {
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "platform": {
      "enum": [
       "android",
       "ios"
      ],
      "type": "string"
     },
     "type": {
      "enum": [
       "text",
       "number",
       "email",
       "phone",
       "password",
       "datetime",
       "unknown"
      ],
      "type": "string"
     },
     "visible": {
      "type": "boolean"
     },
     "wasVisible": {
      "type": "boolean"
     }
    },
    "required": [
     "platform",
     "action"
    ],
    "type": "object"
   }
  },
  {
   "description": "Session app log info, start/stop streaming, diagnostics, and markers Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "path",
       "start",
       "stop",
       "doctor",
       "mark",
       "clear"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "restart": {
      "type": "boolean"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "logs"
  },
  {
   "description": "Hold a UI target by snapshot ref, selector, or coordinates to open a context menu or perform another hold gesture. Set durationMs when the default hold duration is unsuitable. Times out after 210s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "depth": {
      "description": "Snapshot traversal depth.",
      "minimum": 0,
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "durationMs": {
      "description": "Long press duration in milliseconds.",
      "minimum": 0,
      "type": "integer"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "raw": {
      "description": "Use raw snapshot data during selector resolution.",
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scope": {
      "description": "Snapshot scope selector used before resolution.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "settle": {
      "description": "After the action, wait for the UI to go quiet and return the settled diff vs the pre-action tree in the same response. Best-effort; never fails the action.",
      "type": "boolean"
     },
     "settleQuietMs": {
      "description": "Settle: quiet window in milliseconds (default 500).",
      "minimum": 0,
      "type": "integer"
     },
     "target": {
      "description": "UI target. This is separate from deviceTarget, which selects the device form.",
      "oneOf": [
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "ref",
          "type": "string"
         },
         "label": {
          "description": "Optional human label for the ref.",
          "type": "string"
         },
         "ref": {
          "description": "Snapshot element ref such as @e12.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "ref"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "selector",
          "type": "string"
         },
         "selector": {
          "description": "Agent-device selector expression.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "selector"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "point",
          "type": "string"
         },
         "x": {
          "type": "number"
         },
         "y": {
          "type": "number"
         }
        },
        "required": [
         "kind",
         "x",
         "y"
        ],
        "type": "object"
       }
      ]
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "description": "Settle: wait deadline in milliseconds (default 10000).",
      "minimum": 1,
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "target"
    ],
    "type": "object"
   },
   "name": "longpress",
   "outputSchema": {
    "properties": {
     "cost": {
      "properties": {
       "nodeCount": {
        "description": "Number of nodes in the original node tree when the response carries one.",
        "type": "number"
       },
       "runnerRoundTrips": {
        "description": "Number of real runner round-trips made while serving the request.",
        "type": "number"
       },
       "wallClockMs": {
        "description": "Total wall-clock time for the request in milliseconds.",
        "type": "number"
       }
      },
      "required": [
       "wallClockMs",
       "runnerRoundTrips"
      ],
      "type": "object"
     },
     "durationMs": {
      "type": "number"
     },
     "evidence": {
      "properties": {
       "changedFromBefore": {
        "description": "Whether the post-action digest differs from the pre-action capture digest. false is evidence, not failure. With surfaceChange present, no digest comparison is made: it reports that surface transition.",
        "type": "boolean"
       },
       "digest": {
        "description": "Order-independent digest of the post-action node multiset.",
        "type": "string"
       },
       "foregroundApp": {
        "description": "Foreground app bundle id or name, when the capture carries it.",
        "type": "string"
       },
       "interactiveNodeCount": {
        "description": "Subset of nodeCount the platform reports as hittable.",
        "type": "number"
       },
       "nodeCount": {
        "description": "Node count in the post-action interactive-only capture.",
        "type": "number"
       },
       "surfaceChange": {
        "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
        "properties": {
         "disclosure": {
          "description": "Agent-facing sentence explaining the surface transition.",
          "type": "string"
         },
         "from": {
          "description": "Surface the pre-action baseline described: a host bundle id, or app.",
          "type": "string"
         },
         "to": {
          "description": "Surface the post-action capture describes: a host bundle id, or app.",
          "type": "string"
         }
        },
        "required": [
         "from",
         "to",
         "disclosure"
        ],
        "type": "object"
       }
      },
      "required": [
       "nodeCount",
       "interactiveNodeCount",
       "digest",
       "changedFromBefore"
      ],
      "type": "object"
     },
     "gesture": {
      "const": "longpress",
      "type": "string"
     },
     "hint": {
      "type": "string"
     },
     "maestroFallbackReason": {
      "const": "non-hittable-coordinate",
      "type": "string"
     },
     "maestroNonHittableCoordinateFallbackAllowed": {
      "description": "Whether the direct iOS Maestro coordinate fallback was allowed for this selector.",
      "type": "boolean"
     },
     "maestroNonHittableCoordinateFallbackUsed": {
      "description": "Whether the direct iOS Maestro coordinate fallback was actually used.",
      "type": "boolean"
     },
     "message": {
      "type": "string"
     },
     "ref": {
      "description": "Snapshot ref without the @ prefix when the target was an @ref.",
      "type": "string"
     },
     "refLabel": {
      "type": "string"
     },
     "referenceHeight": {
      "description": "Reference frame height for visualizing the interaction point.",
      "type": "number"
     },
     "referenceWidth": {
      "description": "Reference frame width for visualizing the interaction point.",
      "type": "number"
     },
     "resolution": {
      "description": "Pre-action disclosure of how the acting path resolved its target. Absent when resolutionDisclosure is inapplicable for the path.",
      "oneOf": [
       {
        "properties": {
         "kind": {
          "const": "unique",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "runtime",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "alternatives": {
          "description": "At most 5 losing candidates, document order. Present at default/full response levels and omitted in digest. The winner is never included.",
          "items": {
           "properties": {
            "diagnosticRef": {
             "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
             "type": "string"
            },
            "label": {
             "description": "UTF-8 truncated to 256 bytes.",
             "type": "string"
            },
            "role": {
             "description": "UTF-8 truncated to 256 bytes.",
             "type": "string"
            }
           },
           "required": [
            "diagnosticRef"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "kind": {
          "const": "disambiguated",
          "type": "string"
         },
         "matchCount": {
          "description": "Total matches resolveSelectorChain found before disambiguation.",
          "type": "number"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "runtime",
          "type": "string"
         },
         "tiebreak": {
          "description": "The comparison that decided the winner.",
          "enum": [
           "visible",
           "deepest",
           "smallest-area",
           "structural-equivalence"
          ],
          "type": "string"
         },
         "winnerDiagnostic": {
          "properties": {
           "diagnosticRef": {
            "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
            "type": "string"
           },
           "label": {
            "description": "UTF-8 truncated to 256 bytes.",
            "type": "string"
           },
           "role": {
            "description": "UTF-8 truncated to 256 bytes.",
            "type": "string"
           }
          },
          "required": [
           "diagnosticRef"
          ],
          "type": "object"
         }
        },
        "required": [
         "source",
         "phase",
         "kind",
         "matchCount",
         "winnerDiagnostic",
         "tiebreak"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "exact",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "ref",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "label-fallback",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "ref",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "not-observed",
          "type": "string"
         },
         "source": {
          "const": "direct-ios",
          "type": "string"
         }
        },
        "required": [
         "source",
         "kind"
        ],
        "type": "object"
       }
      ],
      "type": "object"
     },
     "selector": {
      "description": "Selector expression when the target was a selector.",
      "type": "string"
     },
     "selectorChain": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "settle": {
      "properties": {
       "captures": {
        "type": "number"
       },
       "diff": {
        "description": "Settled diff vs the pre-action tree (changed lines only).",
        "properties": {
         "lines": {
          "items": {
           "properties": {
            "kind": {
             "enum": [
              "added",
              "removed"
             ],
             "type": "string"
            },
            "ref": {
             "description": "Plain ref body (e12) for added lines.",
             "type": "string"
            },
            "text": {
             "type": "string"
            }
           },
           "required": [
            "kind",
            "text"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "summary": {
          "properties": {
           "additions": {
            "type": "number"
           },
           "removals": {
            "type": "number"
           },
           "unchanged": {
            "type": "number"
           }
          },
          "required": [
           "additions",
           "removals",
           "unchanged"
          ],
          "type": "object"
         },
         "truncated": {
          "description": "Lines were capped to the response bound.",
          "type": "boolean"
         }
        },
        "required": [
         "summary",
         "lines"
        ],
        "type": "object"
       },
       "hint": {
        "type": "string"
       },
       "quietMs": {
        "type": "number"
       },
       "refs": {
        "items": {
         "properties": {
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          }
         },
         "required": [
          "ref"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "refsGeneration": {
        "description": "Snapshot generation of the stored settled tree; refs on added diff lines were minted from it.",
        "type": "number"
       },
       "settled": {
        "description": "Whether the UI held the quiet window before the deadline. false is advisory, not failure.",
        "type": "boolean"
       },
       "surfaceChange": {
        "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
        "properties": {
         "disclosure": {
          "description": "Agent-facing sentence explaining the surface transition.",
          "type": "string"
         },
         "from": {
          "description": "Surface the pre-action baseline described: a host bundle id, or app.",
          "type": "string"
         },
         "to": {
          "description": "Surface the post-action capture describes: a host bundle id, or app.",
          "type": "string"
         }
        },
        "required": [
         "from",
         "to",
         "disclosure"
        ],
        "type": "object"
       },
       "tail": {
        "description": "Unchanged interactive refs tail: still-present, actionable elements from the settled tree, attached only when diff carries zero added-line refs (a modal-dismiss/toast-only diff).",
        "items": {
         "properties": {
          "label": {
           "type": "string"
          },
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          },
          "role": {
           "type": "string"
          }
         },
         "required": [
          "ref",
          "role"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "tailTruncated": {
        "description": "Present (true) when tail candidates exceeded the response cap.",
        "type": "boolean"
       },
       "timeoutMs": {
        "type": "number"
       },
       "waitedMs": {
        "type": "number"
       }
      },
      "required": [
       "settled",
       "waitedMs",
       "captures",
       "quietMs",
       "timeoutMs"
      ],
      "type": "object"
     },
     "targetHittable": {
      "type": "boolean"
     },
     "targetKind": {
      "description": "Resolved interaction target kind.",
      "enum": [
       "point",
       "ref",
       "selector"
      ],
      "type": "string"
     },
     "warning": {
      "type": "string"
     },
     "x": {
      "description": "Resolved interaction x coordinate when available.",
      "type": "number"
     },
     "y": {
      "description": "Resolved interaction y coordinate when available.",
      "type": "number"
     }
    },
    "required": [
     "targetKind"
    ],
    "type": "object"
   }
  },
  {
   "description": "Prepare a React Native development server or ask connected apps to reload, using the development server this session is bound to. Provide explicit runtime inputs only to override that binding for one call. The binding is cleared when the session closes, and a fresh open without runtime hints also clears any leftover binding from a previous same-name session. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "prepare",
       "reload"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "bridgeScope": {
      "additionalProperties": true,
      "type": "object"
     },
     "bundleUrl": {
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "installDependenciesIfNeeded": {
      "type": "boolean"
     },
     "kind": {
      "type": "string"
     },
     "launchUrl": {
      "type": "string"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "listenHost": {
      "type": "string"
     },
     "logPath": {
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "metroHost": {
      "type": "string"
     },
     "metroPort": {
      "type": "integer"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "port": {
      "type": "integer"
     },
     "probeTimeoutMs": {
      "type": "integer"
     },
     "projectRoot": {
      "type": "string"
     },
     "publicBaseUrl": {
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "reuseExisting": {
      "type": "boolean"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "runtimeFilePath": {
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "startupTimeoutMs": {
      "type": "integer"
     },
     "statusHost": {
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "metro"
  },
  {
   "description": "Dump recent HTTP(s) traffic parsed from the session app log Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "dump",
       "log"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "include": {
      "enum": [
       "summary",
       "headers",
       "body",
       "all"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "limit": {
      "type": "integer"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "network"
  },
  {
   "description": "Boot the selected device when needed, then open an app, deep link, or URL in a session. Use the app or URL inputs to choose what becomes the foreground automation target. Metro and debug runtime hints given here are recorded as the session's dev-server binding, so a later reload reuses them; a fresh open without them clears any binding left by a previous same-name session. Times out after 90s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "activity": {
      "description": "Android activity name.",
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "app": {
      "description": "App name, bundle id, package, or URL.",
      "type": "string"
     },
     "bundleUrl": {
      "description": "Session-scoped bundle URL hint applied to the opened app.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceHub": {
      "description": "Use Xcode Device Hub when surfacing Apple simulators.",
      "type": "boolean"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "force": {
      "description": "Overwrite an existing --save-script target instead of refusing (alias: --overwrite).",
      "type": "boolean"
     },
     "foreground": {
      "description": "Include an initial interactive snapshot in a fresh open response. With no app argument, discover the sole running app on the sole booted iOS simulator; ambiguous environments fail closed.",
      "type": "boolean"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "launchArgs": {
      "description": "Launch arguments forwarded verbatim to the platform launch command.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "launchConsole": {
      "description": "Launch console mode.",
      "type": "string"
     },
     "launchUrl": {
      "description": "Session-scoped launch URL hint applied to the opened app.",
      "type": "string"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "metroHost": {
      "description": "Session-scoped Metro/debug host hint applied to the opened app.",
      "type": "string"
     },
     "metroPort": {
      "description": "Session-scoped Metro/debug port hint applied to the opened app. On an emulator/simulator the host defaults to the loopback alias (Android 10.0.2.2, iOS 127.0.0.1) when --metro-host is omitted; physical devices still require an explicit --metro-host.",
      "maximum": 65535,
      "minimum": 1,
      "type": "integer"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "relaunch": {
      "description": "Force relaunch.",
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "saveScript": {
      "oneOf": [
       {
        "type": "boolean"
       },
       {
        "type": "string"
       }
      ]
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "surface": {
      "description": "macOS presentation surface to open: the app itself, the frontmost app, the desktop, or the menu bar.",
      "enum": [
       "app",
       "frontmost-app",
       "desktop",
       "menubar"
      ],
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "testIme": {
      "description": "Activate the headless Android test IME for deterministic Unicode text entry (default on for emulators; opt-in on real devices).",
      "type": "boolean"
     },
     "timeoutMs": {
      "description": "Startup budget in milliseconds. Bounds the Simulator boot wait, so a never-booted Simulator can finish its first-boot migration; omit for the default startup behavior.",
      "minimum": 1,
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "url": {
      "description": "Optional URL passed with an app shell.",
      "type": "string"
     },
     "waitMs": {
      "description": "Block this open for up to n milliseconds (100-120000) while another session holds the device, then fail with DEVICE_IN_USE naming that session. Only session contention is waited for: a device claim held by another workspace is refused at once with its recovery command. A device that never frees, or is taken again while this open waits, costs the full budget, which extends this command's timeout envelope rather than eating into it.",
      "maximum": 120000,
      "minimum": 100,
      "type": "integer"
     }
    },
    "type": "object"
   },
   "name": "open"
  },
  {
   "description": "Set device orientation on iOS and Android Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "orientation": {
      "enum": [
       "portrait",
       "portrait-upside-down",
       "landscape-left",
       "landscape-right"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "orientation"
    ],
    "type": "object"
   },
   "name": "orientation",
   "outputSchema": {
    "properties": {
     "action": {
      "const": "orientation",
      "type": "string"
     },
     "confirmed": {
      "type": "boolean"
     },
     "message": {
      "type": "string"
     },
     "orientation": {
      "enum": [
       "portrait",
       "portrait-upside-down",
       "landscape-left",
       "landscape-right"
      ],
      "type": "string"
     },
     "warning": {
      "type": "string"
     }
    },
    "required": [
     "action",
     "orientation",
     "message"
    ],
    "type": "object"
   }
  },
  {
   "description": "Collect frame health, memory diagnostics, and platform profiling artifacts with compact agent-readable summaries. For CPU profiles, start and stop write the raw artifact while report writes a compact summary; request the report when the task needs readable native CPU evidence. Profiling output is evidence only: compact state, artifact path, and size. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "sample",
       "snapshot",
       "start",
       "stop",
       "report"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "area": {
      "enum": [
       "frames",
       "memory",
       "cpu",
       "trace"
      ],
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "kind": {
      "enum": [
       "xctrace",
       "simpleperf",
       "perfetto",
       "android-hprof",
       "memgraph"
      ],
      "type": "string"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "out": {
      "description": "Output artifact path.",
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "subject": {
      "enum": [
       "profile"
      ],
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "template": {
      "description": "xctrace template name, for example Time Profiler.",
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "tracePath": {
      "description": "Existing .trace path to report, defaults to the latest session trace.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "area"
    ],
    "type": "object"
   },
   "name": "perf"
  },
  {
   "description": "Short-press a UI target by snapshot ref, selector, or coordinates. Use longpress instead when the target requires a context-menu or hold gesture. Times out after 90s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "count": {
      "description": "Number of press/click repetitions.",
      "minimum": 1,
      "type": "integer"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "depth": {
      "description": "Snapshot traversal depth.",
      "minimum": 0,
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "doubleTap": {
      "description": "Request a double-tap action.",
      "type": "boolean"
     },
     "holdMs": {
      "description": "Hold duration for each action.",
      "minimum": 0,
      "type": "integer"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "intervalMs": {
      "description": "Delay between repeated press/click actions.",
      "minimum": 0,
      "type": "integer"
     },
     "jitterPx": {
      "description": "Randomization radius in pixels.",
      "minimum": 0,
      "type": "integer"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "raw": {
      "description": "Use raw snapshot data during selector resolution.",
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scope": {
      "description": "Snapshot scope selector used before resolution.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "settle": {
      "description": "After the action, wait for the UI to go quiet and return the settled diff vs the pre-action tree in the same response. Best-effort; never fails the action.",
      "type": "boolean"
     },
     "settleQuietMs": {
      "description": "Settle: quiet window in milliseconds (default 500).",
      "minimum": 0,
      "type": "integer"
     },
     "target": {
      "description": "UI target. This is separate from deviceTarget, which selects the device form.",
      "oneOf": [
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "ref",
          "type": "string"
         },
         "label": {
          "description": "Optional human label for the ref.",
          "type": "string"
         },
         "ref": {
          "description": "Snapshot element ref such as @e12.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "ref"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "selector",
          "type": "string"
         },
         "selector": {
          "description": "Agent-device selector expression.",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "selector"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "point",
          "type": "string"
         },
         "x": {
          "type": "number"
         },
         "y": {
          "type": "number"
         }
        },
        "required": [
         "kind",
         "x",
         "y"
        ],
        "type": "object"
       }
      ]
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "description": "Settle: wait deadline in milliseconds (default 10000).",
      "minimum": 1,
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "verify": {
      "description": "Capture cheap post-action evidence (AX digest, node counts, changedFromBefore) instead of a follow-up snapshot.",
      "type": "boolean"
     }
    },
    "required": [
     "target"
    ],
    "type": "object"
   },
   "name": "press",
   "outputSchema": {
    "properties": {
     "button": {
      "enum": [
       "secondary",
       "middle"
      ],
      "type": "string"
     },
     "cost": {
      "properties": {
       "nodeCount": {
        "description": "Number of nodes in the original node tree when the response carries one.",
        "type": "number"
       },
       "runnerRoundTrips": {
        "description": "Number of real runner round-trips made while serving the request.",
        "type": "number"
       },
       "wallClockMs": {
        "description": "Total wall-clock time for the request in milliseconds.",
        "type": "number"
       }
      },
      "required": [
       "wallClockMs",
       "runnerRoundTrips"
      ],
      "type": "object"
     },
     "count": {
      "description": "Number of press/click repetitions.",
      "type": "number"
     },
     "doubleTap": {
      "description": "Whether the command requested a double-tap action.",
      "type": "boolean"
     },
     "evidence": {
      "properties": {
       "changedFromBefore": {
        "description": "Whether the post-action digest differs from the pre-action capture digest. false is evidence, not failure. With surfaceChange present, no digest comparison is made: it reports that surface transition.",
        "type": "boolean"
       },
       "digest": {
        "description": "Order-independent digest of the post-action node multiset.",
        "type": "string"
       },
       "foregroundApp": {
        "description": "Foreground app bundle id or name, when the capture carries it.",
        "type": "string"
       },
       "interactiveNodeCount": {
        "description": "Subset of nodeCount the platform reports as hittable.",
        "type": "number"
       },
       "nodeCount": {
        "description": "Node count in the post-action interactive-only capture.",
        "type": "number"
       },
       "surfaceChange": {
        "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
        "properties": {
         "disclosure": {
          "description": "Agent-facing sentence explaining the surface transition.",
          "type": "string"
         },
         "from": {
          "description": "Surface the pre-action baseline described: a host bundle id, or app.",
          "type": "string"
         },
         "to": {
          "description": "Surface the post-action capture describes: a host bundle id, or app.",
          "type": "string"
         }
        },
        "required": [
         "from",
         "to",
         "disclosure"
        ],
        "type": "object"
       }
      },
      "required": [
       "nodeCount",
       "interactiveNodeCount",
       "digest",
       "changedFromBefore"
      ],
      "type": "object"
     },
     "hint": {
      "type": "string"
     },
     "holdMs": {
      "description": "Hold duration for each action.",
      "type": "number"
     },
     "intervalMs": {
      "description": "Delay between repeated press/click actions.",
      "type": "number"
     },
     "jitterPx": {
      "description": "Randomization radius in pixels.",
      "type": "number"
     },
     "maestroFallbackReason": {
      "const": "non-hittable-coordinate",
      "type": "string"
     },
     "maestroNonHittableCoordinateFallbackAllowed": {
      "description": "Whether the direct iOS Maestro coordinate fallback was allowed for this selector.",
      "type": "boolean"
     },
     "maestroNonHittableCoordinateFallbackUsed": {
      "description": "Whether the direct iOS Maestro coordinate fallback was actually used.",
      "type": "boolean"
     },
     "message": {
      "type": "string"
     },
     "ref": {
      "description": "Snapshot ref without the @ prefix when the target was an @ref.",
      "type": "string"
     },
     "refLabel": {
      "type": "string"
     },
     "referenceHeight": {
      "description": "Reference frame height for visualizing the interaction point.",
      "type": "number"
     },
     "referenceWidth": {
      "description": "Reference frame width for visualizing the interaction point.",
      "type": "number"
     },
     "resolution": {
      "description": "Pre-action disclosure of how the acting path resolved its target. Absent when resolutionDisclosure is inapplicable for the path.",
      "oneOf": [
       {
        "properties": {
         "kind": {
          "const": "unique",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "runtime",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "alternatives": {
          "description": "At most 5 losing candidates, document order. Present at default/full response levels and omitted in digest. The winner is never included.",
          "items": {
           "properties": {
            "diagnosticRef": {
             "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
             "type": "string"
            },
            "label": {
             "description": "UTF-8 truncated to 256 bytes.",
             "type": "string"
            },
            "role": {
             "description": "UTF-8 truncated to 256 bytes.",
             "type": "string"
            }
           },
           "required": [
            "diagnosticRef"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "kind": {
          "const": "disambiguated",
          "type": "string"
         },
         "matchCount": {
          "description": "Total matches resolveSelectorChain found before disambiguation.",
          "type": "number"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "runtime",
          "type": "string"
         },
         "tiebreak": {
          "description": "The comparison that decided the winner.",
          "enum": [
           "visible",
           "deepest",
           "smallest-area",
           "structural-equivalence"
          ],
          "type": "string"
         },
         "winnerDiagnostic": {
          "properties": {
           "diagnosticRef": {
            "description": "Opaque non-@ diagnostic token. Never a snapshot ref: not issued via refsGeneration and cannot be pinned or reused as an @ref target. UTF-8 truncated to 256 bytes.",
            "type": "string"
           },
           "label": {
            "description": "UTF-8 truncated to 256 bytes.",
            "type": "string"
           },
           "role": {
            "description": "UTF-8 truncated to 256 bytes.",
            "type": "string"
           }
          },
          "required": [
           "diagnosticRef"
          ],
          "type": "object"
         }
        },
        "required": [
         "source",
         "phase",
         "kind",
         "matchCount",
         "winnerDiagnostic",
         "tiebreak"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "exact",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "ref",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "label-fallback",
          "type": "string"
         },
         "phase": {
          "const": "pre-action",
          "type": "string"
         },
         "source": {
          "const": "ref",
          "type": "string"
         }
        },
        "required": [
         "source",
         "phase",
         "kind"
        ],
        "type": "object"
       },
       {
        "properties": {
         "kind": {
          "const": "not-observed",
          "type": "string"
         },
         "source": {
          "const": "direct-ios",
          "type": "string"
         }
        },
        "required": [
         "source",
         "kind"
        ],
        "type": "object"
       }
      ],
      "type": "object"
     },
     "selector": {
      "description": "Selector expression when the target was a selector.",
      "type": "string"
     },
     "selectorChain": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "settle": {
      "properties": {
       "captures": {
        "type": "number"
       },
       "diff": {
        "description": "Settled diff vs the pre-action tree (changed lines only).",
        "properties": {
         "lines": {
          "items": {
           "properties": {
            "kind": {
             "enum": [
              "added",
              "removed"
             ],
             "type": "string"
            },
            "ref": {
             "description": "Plain ref body (e12) for added lines.",
             "type": "string"
            },
            "text": {
             "type": "string"
            }
           },
           "required": [
            "kind",
            "text"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "summary": {
          "properties": {
           "additions": {
            "type": "number"
           },
           "removals": {
            "type": "number"
           },
           "unchanged": {
            "type": "number"
           }
          },
          "required": [
           "additions",
           "removals",
           "unchanged"
          ],
          "type": "object"
         },
         "truncated": {
          "description": "Lines were capped to the response bound.",
          "type": "boolean"
         }
        },
        "required": [
         "summary",
         "lines"
        ],
        "type": "object"
       },
       "hint": {
        "type": "string"
       },
       "quietMs": {
        "type": "number"
       },
       "refs": {
        "items": {
         "properties": {
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          }
         },
         "required": [
          "ref"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "refsGeneration": {
        "description": "Snapshot generation of the stored settled tree; refs on added diff lines were minted from it.",
        "type": "number"
       },
       "settled": {
        "description": "Whether the UI held the quiet window before the deadline. false is advisory, not failure.",
        "type": "boolean"
       },
       "surfaceChange": {
        "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
        "properties": {
         "disclosure": {
          "description": "Agent-facing sentence explaining the surface transition.",
          "type": "string"
         },
         "from": {
          "description": "Surface the pre-action baseline described: a host bundle id, or app.",
          "type": "string"
         },
         "to": {
          "description": "Surface the post-action capture describes: a host bundle id, or app.",
          "type": "string"
         }
        },
        "required": [
         "from",
         "to",
         "disclosure"
        ],
        "type": "object"
       },
       "tail": {
        "description": "Unchanged interactive refs tail: still-present, actionable elements from the settled tree, attached only when diff carries zero added-line refs (a modal-dismiss/toast-only diff).",
        "items": {
         "properties": {
          "label": {
           "type": "string"
          },
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          },
          "role": {
           "type": "string"
          }
         },
         "required": [
          "ref",
          "role"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "tailTruncated": {
        "description": "Present (true) when tail candidates exceeded the response cap.",
        "type": "boolean"
       },
       "timeoutMs": {
        "type": "number"
       },
       "waitedMs": {
        "type": "number"
       }
      },
      "required": [
       "settled",
       "waitedMs",
       "captures",
       "quietMs",
       "timeoutMs"
      ],
      "type": "object"
     },
     "targetHittable": {
      "type": "boolean"
     },
     "targetKind": {
      "description": "Resolved interaction target kind.",
      "enum": [
       "point",
       "ref",
       "selector"
      ],
      "type": "string"
     },
     "warning": {
      "type": "string"
     },
     "x": {
      "description": "Resolved interaction x coordinate when available.",
      "type": "number"
     },
     "y": {
      "description": "Resolved interaction y coordinate when available.",
      "type": "number"
     }
    },
    "required": [
     "targetKind"
    ],
    "type": "object"
   }
  },
  {
   "description": "Deliver push notification payloads to an installed app. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "app": {
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "payload": {
      "oneOf": [
       {
        "type": "string"
       },
       {
        "additionalProperties": true,
        "type": "object"
       }
      ]
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "app",
     "payload"
    ],
    "type": "object"
   },
   "name": "push",
   "outputSchema": {
    "oneOf": [
     {
      "properties": {
       "bundleId": {
        "type": "string"
       },
       "message": {
        "type": "string"
       },
       "platform": {
        "const": "ios",
        "type": "string"
       }
      },
      "required": [
       "platform",
       "bundleId",
       "message"
      ],
      "type": "object"
     },
     {
      "properties": {
       "action": {
        "type": "string"
       },
       "extrasCount": {
        "type": "number"
       },
       "message": {
        "type": "string"
       },
       "package": {
        "type": "string"
       },
       "platform": {
        "const": "android",
        "type": "string"
       }
      },
      "required": [
       "platform",
       "package",
       "action",
       "extrasCount",
       "message"
      ],
      "type": "object"
     }
    ],
    "type": "object"
   }
  },
  {
   "description": "Run supported React Native automation helpers. Use dismiss-overlay to close a visible development error overlay before continuing normal UI automation. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "dismiss-overlay"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "react-native"
  },
  {
   "description": "Start or stop a screen recording for the active app session or, where supported, the selected device, or build a contact-sheet PNG from a recording already exported. Long Android recordings can return multiple video artifacts; HarmonyOS supports whole-screen recording on physical devices. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "start",
       "stop"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "fps": {
      "type": "integer"
     },
     "hideTouches": {
      "type": "boolean"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "path": {
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "quality": {
      "enum": [
       "medium",
       "high"
      ],
      "type": "string"
     },
     "recordingScope": {
      "enum": [
       "app",
       "device",
       "system"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "record",
   "outputSchema": {
    "oneOf": [
     {
      "properties": {
       "activeSessionApp": {
        "additionalProperties": true,
        "type": "object"
       },
       "outPath": {
        "type": "string"
       },
       "recordOnlySession": {
        "type": "boolean"
       },
       "recording": {
        "const": "started",
        "type": "string"
       },
       "recordingBackend": {
        "type": "string"
       },
       "recordingScope": {
        "type": "string"
       },
       "sessionStateDir": {
        "type": "string"
       },
       "showTouches": {
        "type": "boolean"
       }
      },
      "required": [
       "recording",
       "outPath",
       "sessionStateDir",
       "showTouches"
      ],
      "type": "object"
     },
     {
      "properties": {
       "activeSessionApp": {
        "additionalProperties": true,
        "type": "object"
       },
       "artifacts": {
        "items": {
         "properties": {
          "artifactType": {
           "type": "string"
          },
          "field": {
           "type": "string"
          },
          "fileName": {
           "type": "string"
          },
          "localPath": {
           "type": "string"
          },
          "path": {
           "type": "string"
          }
         },
         "required": [
          "field"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "capturedDurationMs": {
        "type": "number"
       },
       "chunks": {
        "items": {
         "additionalProperties": true,
         "type": "object"
        },
        "type": "array"
       },
       "durationMs": {
        "type": "number"
       },
       "nativePathDisposition": {
        "description": "What became of the artifact path the recorder writes to: retirable while it still owes a removal, retired once that removal was verified. ADR 0024 reserves pending.",
        "enum": [
         "pending",
         "retirable",
         "retired"
        ],
        "type": "string"
       },
       "outPath": {
        "type": "string"
       },
       "overlayWarning": {
        "type": "string"
       },
       "recordOnlySession": {
        "type": "boolean"
       },
       "recorder": {
        "description": "What the recorder was observed doing when the recording was stopped: confirmed, or lost when the session holding it died. ADR 0024 reserves unconfirmed for the step that gains the probe.",
        "enum": [
         "confirmed",
         "unconfirmed",
         "lost"
        ],
        "type": "string"
       },
       "recording": {
        "const": "stopped",
        "type": "string"
       },
       "recordingBackend": {
        "type": "string"
       },
       "recordingScope": {
        "type": "string"
       },
       "showTouches": {
        "type": "boolean"
       },
       "telemetryPath": {
        "type": "string"
       },
       "warning": {
        "type": "string"
       }
      },
      "required": [
       "recording",
       "outPath",
       "artifacts",
       "durationMs",
       "showTouches"
      ],
      "type": "object"
     }
    ],
    "type": "object"
   }
  },
  {
   "description": "Replace an installed app with a binary from a local path. Use this when preserving the same app identity while installing a new build on the selected device. Times out after 180s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "app": {
      "type": "string"
     },
     "appPath": {
      "description": "Path to app binary.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "app",
     "appPath"
    ],
    "type": "object"
   },
   "name": "reinstall"
  },
  {
   "description": "Run a recorded automation script, including compatible Maestro YAML flows. A script without a terminal close leaves its session active for subsequent automation. Times out after 90s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "backend": {
      "type": "string"
     },
     "bundleUrl": {
      "description": "Bundle URL hint inherited by replay-opened sessions.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "env": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "force": {
      "type": "boolean"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "keepSession": {
      "description": "Leave the session active by suppressing exactly an authored terminal close in native .ad.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "maestro": {
      "type": "boolean"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "metroHost": {
      "description": "Metro/debug host hint inherited by replay-opened sessions.",
      "type": "string"
     },
     "metroPort": {
      "description": "Metro/debug port hint inherited by replay-opened sessions.",
      "type": "integer"
     },
     "path": {
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "resumeFrom": {
      "type": "integer"
     },
     "resumePlanDigest": {
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "saveScript": {
      "oneOf": [
       {
        "type": "boolean"
       },
       {
        "type": "string"
       }
      ]
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "description": "Maximum wall-clock duration for the replay request.",
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "update": {
      "type": "boolean"
     }
    },
    "required": [
     "path"
    ],
    "type": "object"
   },
   "name": "replay",
   "outputSchema": {
    "properties": {
     "artifactPaths": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "healed": {
      "type": "number"
     },
     "message": {
      "type": "string"
     },
     "replayed": {
      "type": "number"
     },
     "session": {
      "type": "string"
     },
     "sessionActive": {
      "description": "True iff the session is still active \u2014 the script had no terminal close.",
      "type": "boolean"
     },
     "snapshotDiagnostics": {
      "additionalProperties": true,
      "type": "object"
     }
    },
    "required": [
     "replayed",
     "healed",
     "session",
     "sessionActive",
     "artifactPaths",
     "message"
    ],
    "type": "object"
   }
  },
  {
   "description": "Capture a screenshot of the active app or web session. Choose the capture scope, density, size, or annotations through the corresponding input fields when needed. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "cropOn": {
      "description": "Selector expression; the capture is cropped to the frame the selector resolves on the same screen.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "fullscreen": {
      "type": "boolean"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "normalizeStatusBar": {
      "type": "boolean"
     },
     "overlayRefs": {
      "type": "boolean"
     },
     "path": {
      "description": "Output path.",
      "type": "string"
     },
     "pixelDensity": {
      "description": "Output screenshot pixel density in pixels per logical point.",
      "minimum": 1,
      "type": "integer"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scale": {
      "description": "Screenshot scale factor.",
      "maximum": 1,
      "minimum": 0.01,
      "type": "number"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "stabilize": {
      "type": "boolean"
     },
     "surface": {
      "enum": [
       "app",
       "frontmost-app",
       "desktop",
       "menubar"
      ],
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "screenshot"
  },
  {
   "description": "Scroll in a direction, or toward the top/bottom edge of scrollable content. Set until to a selector to reach an off-screen target in one command rather than a scroll-and-check loop. The optional amount is the finger-path fraction of the viewport axis, honored up to 0.8 of it; directional scrolls reduce release momentum, while app scroll physics determine the final content offset. A visible keyboard shortens the swiped band instead of being dismissed; when too little is left, the command refuses with scroll_keyboard_occludes_surface. Times out after 90s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "amount": {
      "description": "Platform scroll amount.",
      "type": "number"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "direction": {
      "enum": [
       "up",
       "down",
       "left",
       "right",
       "top",
       "bottom"
      ],
      "type": "string"
     },
     "durationMs": {
      "description": "Scroll duration in milliseconds when the backend supports pacing.",
      "maximum": 10000,
      "minimum": 0,
      "type": "integer"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "pixels": {
      "description": "Pixel scroll amount.",
      "minimum": 0,
      "type": "integer"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "settle": {
      "description": "After the action, wait for the UI to go quiet and return the settled diff vs the pre-action tree in the same response. Best-effort; never fails the action.",
      "type": "boolean"
     },
     "settleQuietMs": {
      "description": "Settle: quiet window in milliseconds (default 500).",
      "minimum": 0,
      "type": "integer"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "description": "Settle: wait deadline in milliseconds (default 10000).",
      "minimum": 1,
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "until": {
      "description": "Repeat scroll passes until this selector is visible on screen, then stop. Not valid with the top/bottom edge directions, which carry their own stop condition.",
      "type": "string"
     }
    },
    "required": [
     "direction"
    ],
    "type": "object"
   },
   "name": "scroll",
   "outputSchema": {
    "properties": {
     "amount": {
      "type": "number"
     },
     "direction": {
      "enum": [
       "up",
       "down",
       "left",
       "right"
      ],
      "type": "string"
     },
     "durationMs": {
      "type": "number"
     },
     "edge": {
      "enum": [
       "top",
       "bottom"
      ],
      "type": "string"
     },
     "keyboardAvoided": {
      "description": "Present only when an on-screen keyboard forced the swipe into the band above it; the reported pixels were planned against the shorter referenceHeight.",
      "type": "boolean"
     },
     "keyboardMinY": {
      "description": "Where the keyboard began, in the same unit as the gesture coordinates. Clipped scrolls only.",
      "type": "number"
     },
     "message": {
      "type": "string"
     },
     "passes": {
      "description": "Edge and until scrolls only: how many scroll-and-check passes ran.",
      "type": "number"
     },
     "pixels": {
      "type": "number"
     },
     "settle": {
      "properties": {
       "captures": {
        "type": "number"
       },
       "diff": {
        "description": "Settled diff vs the pre-action tree (changed lines only).",
        "properties": {
         "lines": {
          "items": {
           "properties": {
            "kind": {
             "enum": [
              "added",
              "removed"
             ],
             "type": "string"
            },
            "ref": {
             "description": "Plain ref body (e12) for added lines.",
             "type": "string"
            },
            "text": {
             "type": "string"
            }
           },
           "required": [
            "kind",
            "text"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "summary": {
          "properties": {
           "additions": {
            "type": "number"
           },
           "removals": {
            "type": "number"
           },
           "unchanged": {
            "type": "number"
           }
          },
          "required": [
           "additions",
           "removals",
           "unchanged"
          ],
          "type": "object"
         },
         "truncated": {
          "description": "Lines were capped to the response bound.",
          "type": "boolean"
         }
        },
        "required": [
         "summary",
         "lines"
        ],
        "type": "object"
       },
       "hint": {
        "type": "string"
       },
       "quietMs": {
        "type": "number"
       },
       "refs": {
        "items": {
         "properties": {
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          }
         },
         "required": [
          "ref"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "refsGeneration": {
        "description": "Snapshot generation of the stored settled tree; refs on added diff lines were minted from it.",
        "type": "number"
       },
       "settled": {
        "description": "Whether the UI held the quiet window before the deadline. false is advisory, not failure.",
        "type": "boolean"
       },
       "surfaceChange": {
        "description": "Present when an in-place system surface (web sign-in or Apple Pay sheet) was presented over the app, or left it.",
        "properties": {
         "disclosure": {
          "description": "Agent-facing sentence explaining the surface transition.",
          "type": "string"
         },
         "from": {
          "description": "Surface the pre-action baseline described: a host bundle id, or app.",
          "type": "string"
         },
         "to": {
          "description": "Surface the post-action capture describes: a host bundle id, or app.",
          "type": "string"
         }
        },
        "required": [
         "from",
         "to",
         "disclosure"
        ],
        "type": "object"
       },
       "tail": {
        "description": "Unchanged interactive refs tail: still-present, actionable elements from the settled tree, attached only when diff carries zero added-line refs (a modal-dismiss/toast-only diff).",
        "items": {
         "properties": {
          "label": {
           "type": "string"
          },
          "ref": {
           "description": "Plain ref body (e12) minted from the stored settled tree.",
           "type": "string"
          },
          "role": {
           "type": "string"
          }
         },
         "required": [
          "ref",
          "role"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "tailTruncated": {
        "description": "Present (true) when tail candidates exceeded the response cap.",
        "type": "boolean"
       },
       "timeoutMs": {
        "type": "number"
       },
       "waitedMs": {
        "type": "number"
       }
      },
      "required": [
       "settled",
       "waitedMs",
       "captures",
       "quietMs",
       "timeoutMs"
      ],
      "type": "object"
     },
     "until": {
      "description": "Until scrolls only: the selector the passes stopped on.",
      "type": "string"
     }
    },
    "required": [
     "direction"
    ],
    "type": "object"
   }
  },
  {
   "description": "List active sessions, print the effective daemon state directory, or publish an armed open-to-destination script without closing its session Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "list shows active sessions; state-dir prints the daemon state directory; save-script publishes an armed recording without teardown.",
      "enum": [
       "list",
       "state-dir",
       "save-script"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "force": {
      "description": "Atomically replace an existing save-script target.",
      "type": "boolean"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "path": {
      "description": "Optional .ad output path for save-script.",
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "session"
  },
  {
   "description": "Read or change supported operating-system settings, animation scales, appearance, or app permissions on the selected target. Platform support varies by setting and action. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "app": {
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "latitude": {
      "type": "number"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "longitude": {
      "type": "number"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "mode": {
      "enum": [
       "full",
       "limited"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "permission": {
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "setting": {
      "type": "string"
     },
     "state": {
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "setting"
    ],
    "type": "object"
   },
   "name": "settings"
  },
  {
   "description": "Shutdown a selected simulator or emulator. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "shutdown",
   "outputSchema": {
    "properties": {
     "device": {
      "description": "Human-readable device name.",
      "type": "string"
     },
     "id": {
      "description": "Stable device id.",
      "type": "string"
     },
     "kind": {
      "enum": [
       "simulator",
       "emulator",
       "device"
      ],
      "type": "string"
     },
     "platform": {
      "enum": [
       "ios",
       "macos",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web"
      ],
      "type": "string"
     },
     "shutdown": {
      "properties": {
       "error": {
        "additionalProperties": true,
        "description": "Normalized error detail when shutdown failed.",
        "type": "object"
       },
       "exitCode": {
        "type": "number"
       },
       "stderr": {
        "type": "string"
       },
       "stdout": {
        "type": "string"
       },
       "success": {
        "type": "boolean"
       }
      },
      "required": [
       "success",
       "exitCode",
       "stdout",
       "stderr"
      ],
      "type": "object"
     },
     "target": {
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     }
    },
    "required": [
     "platform",
     "target",
     "device",
     "id",
     "kind",
     "shutdown"
    ],
    "type": "object"
   }
  },
  {
   "description": "Capture the accessibility tree or compare it with the previous session baseline. Use the returned refs for subsequent semantic interactions and the diff option to verify UI changes. Times out after 90s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "customActions": {
      "description": "Name the affordances an element merged away (iOS UIAccessibilityCustomAction, React Native accessibilityActions) \u2014 a card whose reply/options controls are not separate elements still lists them here. The names are for PLANNING, not invocation: there is no API to trigger them, so reach the affordance through the element detail screen, through the same control exposed as a labeled element elsewhere, or by coordinates from its rect. iOS simulator only; costs one accessibility round trip per merged element.",
      "type": "boolean"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "depth": {
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "forceFull": {
      "type": "boolean"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "interactiveOnly": {
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "raw": {
      "type": "boolean"
     },
     "record": {
      "description": "Force-record this out-of-band observation into a repair-armed heal (mutually exclusive with noRecord). Authored replay steps are recorded automatically and never need this.",
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scope": {
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "description": "Maximum wall-clock time for the snapshot command.",
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "snapshot"
  },
  {
   "description": "Quick coordinate fling with optional repeat pattern. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "count": {
      "description": "Number of swipe repetitions.",
      "maximum": 200,
      "minimum": 1,
      "type": "integer"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "from": {
      "additionalProperties": false,
      "description": "Swipe start point.",
      "properties": {
       "x": {
        "type": "number"
       },
       "y": {
        "type": "number"
       }
      },
      "required": [
       "x",
       "y"
      ],
      "type": "object"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "pattern": {
      "enum": [
       "one-way",
       "ping-pong"
      ],
      "type": "string"
     },
     "pauseMs": {
      "description": "Pause between repeated swipes.",
      "maximum": 10000,
      "minimum": 0,
      "type": "integer"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "to": {
      "additionalProperties": false,
      "description": "Swipe end point.",
      "properties": {
       "x": {
        "type": "number"
       },
       "y": {
        "type": "number"
       }
      },
      "required": [
       "x",
       "y"
      ],
      "type": "object"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "from",
     "to"
    ],
    "type": "object"
   },
   "name": "swipe"
  },
  {
   "description": "Run one or more replay scripts as a serial test suite Streams progress; no fixed client timeout.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "artifactsDir": {
      "type": "string"
     },
     "backend": {
      "type": "string"
     },
     "bundleUrl": {
      "description": "Bundle URL hint inherited by each test session.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "env": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "failFast": {
      "type": "boolean"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "maestro": {
      "type": "boolean"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "metroHost": {
      "description": "Metro/debug host hint inherited by each test session.",
      "type": "string"
     },
     "metroPort": {
      "description": "Metro/debug port hint inherited by each test session.",
      "type": "integer"
     },
     "paths": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "recordVideo": {
      "type": "boolean"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "retries": {
      "type": "integer"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "shardAll": {
      "type": "integer"
     },
     "shardSplit": {
      "type": "integer"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "timeoutMs": {
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "update": {
      "type": "boolean"
     }
    },
    "required": [
     "paths"
    ],
    "type": "object"
   },
   "name": "test",
   "outputSchema": {
    "properties": {
     "durationMs": {
      "type": "number"
     },
     "executed": {
      "type": "number"
     },
     "failed": {
      "type": "number"
     },
     "failures": {
      "items": {
       "additionalProperties": true,
       "type": "object"
      },
      "type": "array"
     },
     "notRun": {
      "type": "number"
     },
     "passed": {
      "type": "number"
     },
     "skipped": {
      "type": "number"
     },
     "snapshotDiagnostics": {
      "additionalProperties": true,
      "type": "object"
     },
     "tests": {
      "items": {
       "additionalProperties": true,
       "type": "object"
      },
      "type": "array"
     },
     "total": {
      "type": "number"
     }
    },
    "required": [
     "total",
     "executed",
     "passed",
     "failed",
     "skipped",
     "notRun",
     "durationMs",
     "failures",
     "tests"
    ],
    "type": "object"
   }
  },
  {
   "description": "Start or stop trace-log capture and return the resulting artifact when capture ends. Use the same artifact path for the matching start and stop requests when an explicit path is required. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "action": {
      "enum": [
       "start",
       "stop"
      ],
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "path": {
      "type": "string"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "trace",
   "outputSchema": {
    "oneOf": [
     {
      "properties": {
       "outPath": {
        "type": "string"
       },
       "trace": {
        "const": "started",
        "type": "string"
       }
      },
      "required": [
       "trace",
       "outPath"
      ],
      "type": "object"
     },
     {
      "properties": {
       "artifacts": {
        "items": {
         "properties": {
          "artifactType": {
           "type": "string"
          },
          "field": {
           "type": "string"
          },
          "fileName": {
           "type": "string"
          },
          "localPath": {
           "type": "string"
          },
          "path": {
           "type": "string"
          }
         },
         "required": [
          "field"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "outPath": {
        "type": "string"
       },
       "trace": {
        "const": "stopped",
        "type": "string"
       }
      },
      "required": [
       "trace",
       "outPath",
       "artifacts"
      ],
      "type": "object"
     }
    ],
    "type": "object"
   }
  },
  {
   "description": "Ask the app to handle an app-defined automation or test event, with an optional structured payload. Call this only for event names and payload shapes the app documents. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "event": {
      "description": "Name of an app-defined automation or test event the app documents.",
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "payload": {
      "additionalProperties": true,
      "description": "Structured payload passed to the event, in the shape the app documents for it.",
      "type": "object"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "event"
    ],
    "type": "object"
   },
   "name": "trigger-app-event",
   "outputSchema": {
    "properties": {
     "event": {
      "type": "string"
     },
     "eventUrl": {
      "type": "string"
     },
     "message": {
      "type": "string"
     },
     "transport": {
      "const": "deep-link",
      "type": "string"
     }
    },
    "required": [
     "event",
     "eventUrl",
     "transport",
     "message"
    ],
    "type": "object"
   }
  },
  {
   "description": "Press or long-press a TV remote or D-pad button on Android TV, tvOS, or Vega OS. Choose the button and optional hold duration through the input fields. The aliases ok, center, and enter all map to select. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "button": {
      "enum": [
       "up",
       "down",
       "left",
       "right",
       "select",
       "menu",
       "home",
       "back"
      ],
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "durationMs": {
      "description": "Press duration in milliseconds. tvOS and Vega OS use the exact hold duration; Android TV maps any positive value to an ADB longpress (longpress).",
      "minimum": 0,
      "type": "integer"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "button"
    ],
    "type": "object"
   },
   "name": "tv-remote",
   "outputSchema": {
    "properties": {
     "action": {
      "const": "tv-remote",
      "type": "string"
     },
     "button": {
      "enum": [
       "up",
       "down",
       "left",
       "right",
       "select",
       "menu",
       "home",
       "back"
      ],
      "type": "string"
     },
     "durationMs": {
      "type": "number"
     },
     "message": {
      "type": "string"
     }
    },
    "required": [
     "action",
     "button",
     "message"
    ],
    "type": "object"
   }
  },
  {
   "description": "Append text to the currently focused input. Use fill when the existing field value should be replaced, and focus first when no input is active. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "delayMs": {
      "description": "Delay between typed characters.",
      "minimum": 0,
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "text": {
      "description": "Text to type.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "required": [
     "text"
    ],
    "type": "object"
   },
   "name": "type"
  },
  {
   "description": "Resize the active web viewport before taking snapshots or screenshots. Useful for fixed-layout or 100vh apps where changing the viewport reveals different content. Times out after 90s.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "height": {
      "description": "Viewport height in CSS pixels.",
      "minimum": 1,
      "type": "integer"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     },
     "width": {
      "description": "Viewport width in CSS pixels.",
      "minimum": 1,
      "type": "integer"
     }
    },
    "required": [
     "width",
     "height"
    ],
    "type": "object"
   },
   "name": "viewport",
   "outputSchema": {
    "properties": {
     "height": {
      "type": "number"
     },
     "message": {
      "type": "string"
     },
     "width": {
      "type": "number"
     }
    },
    "required": [
     "width",
     "height",
     "message"
    ],
    "type": "object"
   }
  },
  {
   "description": "Wait for a duration, text, snapshot ref, selector to appear, selector to be strictly absent, or stable UI. Use the strict absence target when zero selector matches are required; stable waits until the UI stays quiet for the requested window. Times out after 90s; a caller-supplied budget extends it.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "absent": {
      "type": "string"
     },
     "androidDeviceAllowlist": {
      "description": "Android serial allowlist used for device resolution.",
      "type": "string"
     },
     "debug": {
      "description": "Enable debug diagnostics.",
      "type": "boolean"
     },
     "depth": {
      "type": "integer"
     },
     "device": {
      "description": "Device name selector (a UDID belongs in udid, a serial in serial).",
      "type": "string"
     },
     "deviceTarget": {
      "description": "Device target form. Maps to the CLI --target flag.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "durationMs": {
      "type": "integer"
     },
     "includeCost": {
      "description": "Include per-command agent-cost (cost.wallClockMs, \u2026) in structuredContent. Defaults to off; the default response shape is unchanged.",
      "type": "boolean"
     },
     "kind": {
      "enum": [
       "duration",
       "text",
       "ref",
       "selector",
       "absent",
       "stable"
      ],
      "type": "string"
     },
     "leaseId": {
      "description": "Existing lease identifier.",
      "type": "string"
     },
     "mcpOutputFormat": {
      "description": "MCP text content format. Defaults to optimized agent-friendly text; use json for JSON text. Structured content is always returned separately.",
      "enum": [
       "optimized",
       "json"
      ],
      "type": "string"
     },
     "noRecord": {
      "description": "Do not record this action.",
      "type": "boolean"
     },
     "platform": {
      "description": "Platform selector used to resolve a device.",
      "enum": [
       "apple",
       "android",
       "harmonyos",
       "vega",
       "linux",
       "web",
       "ios",
       "macos"
      ],
      "type": "string"
     },
     "quietMs": {
      "type": "integer"
     },
     "raw": {
      "type": "boolean"
     },
     "ref": {
      "type": "string"
     },
     "responseLevel": {
      "description": "Response verbosity: token-cheap digest / default (today) / full. Defaults to default; the default response shape is unchanged.",
      "enum": [
       "digest",
       "default",
       "full"
      ],
      "type": "string"
     },
     "runId": {
      "description": "Lease run identifier.",
      "type": "string"
     },
     "scope": {
      "type": "string"
     },
     "selector": {
      "type": "string"
     },
     "serial": {
      "description": "Android, HarmonyOS, or Vega VVD serial selector.",
      "type": "string"
     },
     "session": {
      "description": "Agent-device session name.",
      "type": "string"
     },
     "stable": {
      "type": "boolean"
     },
     "target": {
      "description": "Alias for deviceTarget on commands without a UI target field. Interaction commands reserve target for the UI element.",
      "enum": [
       "mobile",
       "tv",
       "desktop"
      ],
      "type": "string"
     },
     "tenant": {
      "description": "Remote tenant identifier.",
      "type": "string"
     },
     "text": {
      "type": "string"
     },
     "timeoutMs": {
      "type": "integer"
     },
     "udid": {
      "description": "Apple device or simulator UDID; the selector that pins one device when several share a name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "wait",
   "outputSchema": {
    "properties": {
     "captures": {
      "type": "number"
     },
     "hint": {
      "type": "string"
     },
     "kind": {
      "const": "selector",
      "type": "string"
     },
     "nodeCount": {
      "type": "number"
     },
     "selector": {
      "type": "string"
     },
     "text": {
      "type": "string"
     },
     "waitedMs": {
      "type": "number"
     },
     "warning": {
      "type": "string"
     }
    },
    "required": [
     "waitedMs"
    ],
    "type": "object"
   }
  }
 ],
 "version": "0.21.10"
}