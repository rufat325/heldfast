{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "@browserstack/mcp-server",
 "protocol": "2025-06-18",
 "published": "2026-09-18T14:30:41.824Z",
 "tools": [
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Accessibility Expert"
   },
   "description": "\ud83d\udea8 REQUIRED: Use this tool for any accessibility/a11y/WCAG questions. Do NOT answer accessibility questions directly - always use this tool.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "query": {
      "description": "Any accessibility, a11y, WCAG, or web accessibility question",
      "type": "string"
     }
    },
    "required": [
     "query"
    ],
    "type": "object"
   },
   "name": "accessibilityExpert"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Add Percy Snapshot Commands"
   },
   "description": "Adds Percy snapshot commands to the specified test files.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "index": {
      "description": "Index of the test file to update",
      "type": "number"
     }
    },
    "required": [
     "index"
    ],
    "type": "object"
   },
   "name": "addPercySnapshotCommands"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Add Test Result"
   },
   "description": "Add a test result to a specific test run via BrowserStack Test Management API. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "project_identifier": {
      "description": "Identifier of the project (Starts with 'PR-')",
      "type": "string"
     },
     "test_case_id": {
      "description": "Identifier of the test case, e.g., 'TC-13'.",
      "type": "string"
     },
     "test_result": {
      "properties": {
       "description": {
        "description": "Optional description of the test result.",
        "type": "string"
       },
       "status": {
        "description": "Status of the test result, e.g., 'passed', 'failed'.",
        "type": "string"
       }
      },
      "required": [
       "status"
      ],
      "type": "object"
     },
     "test_run_id": {
      "description": "Identifier of the test run (e.g., TR-678)",
      "type": "string"
     }
    },
    "required": [
     "project_identifier",
     "test_run_id",
     "test_result",
     "test_case_id"
    ],
    "type": "object"
   },
   "name": "addTestResult"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false,
    "title": "Ask BrowserStack AI (Alpha)"
   },
   "description": "(Alpha, limited availability. Enabled per account and per product; if it is not enabled the call returns an entitlement error, nothing runs, and you should complete the task with the individual tools instead. To request access, the user should contact their BrowserStack account owner.) Use this when no other BrowserStack tool here fits the task, or when the ones you tried did not get you there. Prefer a specific tool whenever one fits: it is faster and more predictable than handing the job to an agent. Otherwise, describe what you want in plain language and BrowserStack's agent decides which calls to make, then returns its answer plus the steps it took. Anything that would change data pauses and asks you to confirm it first, in your own client; deletes are refused outright. If your client cannot show you a prompt, the run is read-only and everything it wanted to change comes back in `needs_approval` instead. One task per call.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "product": {
      "description": "Which product to work in: tm (Test Management), tra (Test Reporting & Analytics).",
      "enum": [
       "tm",
       "tra"
      ],
      "type": "string"
     },
     "query": {
      "description": "What you want, in plain language. One thing per call.",
      "type": "string"
     }
    },
    "required": [
     "product",
     "query"
    ],
    "type": "object"
   },
   "name": "askBrowserStackAI"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Create Accessibility Auth Config"
   },
   "description": "Create an authentication configuration for accessibility scans. Supports both form-based and basic authentication.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "name": {
      "description": "Name for the auth configuration",
      "type": "string"
     },
     "password": {
      "description": "Site password; omit to have it requested from the user.",
      "type": "string"
     },
     "passwordSelector": {
      "description": "CSS selector for password field (required for form auth)",
      "type": "string"
     },
     "submitSelector": {
      "description": "CSS selector for submit button (required for form auth)",
      "type": "string"
     },
     "type": {
      "description": "Authentication type: 'form' for form-based auth, 'basic' for HTTP basic auth",
      "enum": [
       "form",
       "basic"
      ],
      "type": "string"
     },
     "url": {
      "description": "URL of the authentication page",
      "type": "string"
     },
     "username": {
      "description": "Site username; omit to have it requested from the user.",
      "type": "string"
     },
     "usernameSelector": {
      "description": "CSS selector for username field (required for form auth)",
      "type": "string"
     }
    },
    "required": [
     "name",
     "type",
     "url"
    ],
    "type": "object"
   },
   "name": "createAccessibilityAuthConfig"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Create LCA Steps"
   },
   "description": "Generate Low Code Automation (LCA) steps for a test case in BrowserStack Test Management using the Low Code Automation Agent. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "base_url": {
      "description": "Base URL for the test (e.g., 'google.com')",
      "type": "string"
     },
     "credentials": {
      "description": "Login credentials; omit to have them requested from the user.",
      "properties": {
       "password": {
        "description": "Password for authentication",
        "type": "string"
       },
       "username": {
        "description": "Username for authentication",
        "type": "string"
       }
      },
      "required": [
       "username",
       "password"
      ],
      "type": "object"
     },
     "local_enabled": {
      "default": false,
      "description": "Whether local testing is enabled",
      "type": "boolean"
     },
     "project_identifier": {
      "description": "ID of the project (Starts with 'PR-')",
      "type": "string"
     },
     "requires_authentication": {
      "default": false,
      "description": "Set true if the test case requires login.",
      "type": "boolean"
     },
     "test_case_details": {
      "description": "Test case details including steps",
      "properties": {
       "description": {
        "description": "Description of the test case",
        "type": "string"
       },
       "name": {
        "description": "Name of the test case",
        "type": "string"
       },
       "preconditions": {
        "description": "Preconditions for the test case",
        "type": "string"
       },
       "test_case_steps": {
        "description": "Array of test case steps with expected results",
        "items": {
         "properties": {
          "result": {
           "description": "Expected result",
           "type": "string"
          },
          "step": {
           "description": "Test step description",
           "type": "string"
          }
         },
         "required": [
          "step",
          "result"
         ],
         "type": "object"
        },
        "type": "array"
       }
      },
      "required": [
       "name",
       "description",
       "preconditions",
       "test_case_steps"
      ],
      "type": "object"
     },
     "test_case_identifier": {
      "description": "Identifier of the test case (e.g., 'TC-12345')",
      "type": "string"
     },
     "test_name": {
      "description": "Name of the test",
      "type": "string"
     },
     "wait_for_completion": {
      "default": true,
      "description": "Whether to wait for LCA build completion (default: true)",
      "type": "boolean"
     }
    },
    "required": [
     "project_identifier",
     "test_case_identifier",
     "base_url",
     "test_name",
     "test_case_details"
    ],
    "type": "object"
   },
   "name": "createLCASteps"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Create Project or Folder"
   },
   "description": "Create a project and/or folder in BrowserStack Test Management. Creating a project needs no identifier. Creating a folder inside an EXISTING project needs that project's identifier (PR-*); no tool here lists projects, so ask askBrowserStackAI with product \"tm\" for it.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "folder_description": {
      "description": "Description for the new folder.",
      "type": "string"
     },
     "folder_name": {
      "description": "Name of the folder to create.",
      "type": "string"
     },
     "parent_id": {
      "description": "Parent folder ID; if omitted, folder is created at root.",
      "type": "number"
     },
     "project_description": {
      "description": "Description for the new project.",
      "type": "string"
     },
     "project_identifier": {
      "description": "Existing project identifier to use for folder creation.",
      "type": "string"
     },
     "project_name": {
      "description": "Name of the project to create.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "createProjectOrFolder"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Create Test Case"
   },
   "description": "Use this tool to create a test case in BrowserStack Test Management. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "automation_status": {
      "description": "Automation status of the test case. Common values include 'not_automated', 'automated', 'automation_not_required'.",
      "type": "string"
     },
     "case_type": {
      "description": "Test case type display or internal name (per-project). Omit for project default.",
      "type": "string"
     },
     "custom_fields": {
      "additionalProperties": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "number"
        },
        {
         "type": "boolean"
        },
        {
         "items": {
          "type": [
           "string",
           "number"
          ]
         },
         "type": "array"
        }
       ]
      },
      "description": "Map of custom field NAME to value; use an array for multi-select fields.",
      "propertyNames": {
       "type": "string"
      },
      "type": "object"
     },
     "description": {
      "description": "Brief description of the test case.",
      "type": "string"
     },
     "folder_id": {
      "description": "The ID of the folder within the project where the test case should be created. If not provided, ask the user if they would like to create a new folder using the createProjectOrFolder tool.",
      "type": "string"
     },
     "issue_tracker": {
      "properties": {
       "host": {
        "description": "Base URL of the issue tracker.",
        "format": "uri",
        "type": "string"
       },
       "name": {
        "description": "Issue tracker name,  For example, use jira for Jira, azure for Azure DevOps, or asana for Asana.",
        "type": "string"
       }
      },
      "required": [
       "name",
       "host"
      ],
      "type": "object"
     },
     "issues": {
      "description": "List of the linked Jira, Asana or Azure issues ID's. This should be strictly in array format not the string of json.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "name": {
      "description": "Name of the test case.",
      "type": "string"
     },
     "owner": {
      "description": "Email of the test case owner.",
      "format": "email",
      "pattern": "^(?:[A-Za-z0-9_'+\\-]+\\.)*[A-Za-z0-9_'+\\-]*[A-Za-z0-9_+-]@(?:[A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
      "type": "string"
     },
     "preconditions": {
      "description": "Any preconditions (HTML allowed).",
      "type": "string"
     },
     "priority": {
      "description": "Priority of the test case. Accepts either display name (e.g. 'Critical', 'High', 'Medium', 'Low') or internal name (e.g. 'medium'). If omitted, the project default (usually 'Medium') is applied. Valid values are per-project and discoverable via the form-fields endpoint.",
      "type": "string"
     },
     "project_identifier": {
      "description": "The ID of the BrowserStack project where the test case should be created. If no project identifier is provided, ask the user if they would like to create a new project using the createProjectOrFolder tool.",
      "type": "string"
     },
     "tags": {
      "description": "Tags to attach to the test case. This should be strictly in array format not the string of json",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "template": {
      "description": "System template slug only: 'test_case_steps' or 'test_case_bdd'. For a custom template, use template_id instead.",
      "type": "string"
     },
     "template_id": {
      "description": "Numeric ID of a custom template (from listTestCaseTemplates); applies that template. Overrides 'template'.",
      "type": "number"
     },
     "test_case_steps": {
      "description": "List of steps and expected results.",
      "items": {
       "properties": {
        "result": {
         "description": "Expected result of this step.",
         "type": "string"
        },
        "step": {
         "description": "Action to perform in this step.",
         "type": "string"
        }
       },
       "required": [
        "step",
        "result"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "project_identifier",
     "folder_id",
     "name",
     "test_case_steps"
    ],
    "type": "object"
   },
   "name": "createTestCase"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Create Test Cases from File"
   },
   "description": "Generate test cases from a file in BrowserStack Test Management using the Test Case Generator AI Agent.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "documentId": {
      "description": "Internal document identifier",
      "type": "string"
     },
     "folderId": {
      "description": "BrowserStack folder ID",
      "type": "string"
     },
     "projectReferenceId": {
      "description": "The BrowserStack project reference ID is a unique identifier found in the project URL within the BrowserStack Test Management Platform. This ID is also returned by the Upload Document tool.",
      "type": "string"
     }
    },
    "required": [
     "documentId",
     "folderId",
     "projectReferenceId"
    ],
    "type": "object"
   },
   "name": "createTestCasesFromFile"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Create Test Run"
   },
   "description": "Create a test run in BrowserStack Test Management. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "project_identifier": {
      "description": "Identifier of the project in which to create the test run.",
      "type": "string"
     },
     "test_run": {
      "properties": {
       "description": {
        "description": "Brief information about the test run",
        "type": "string"
       },
       "folder_ids": {
        "description": "Folder IDs to include",
        "items": {
         "type": "number"
        },
        "type": "array"
       },
       "issue_tracker": {
        "description": "Issue tracker configuration",
        "properties": {
         "host": {
          "format": "uri",
          "type": "string"
         },
         "name": {
          "type": "string"
         }
        },
        "required": [
         "name",
         "host"
        ],
        "type": "object"
       },
       "issues": {
        "description": "Linked issue IDs",
        "items": {
         "type": "string"
        },
        "type": "array"
       },
       "name": {
        "description": "Name of the test run",
        "type": "string"
       },
       "run_state": {
        "description": "State of the test run. One of new_run, in_progress, under_review, rejected, done, closed",
        "enum": [
         "new_run",
         "in_progress",
         "under_review",
         "rejected",
         "done",
         "closed"
        ],
        "type": "string"
       },
       "test_cases": {
        "description": "List of test case IDs",
        "items": {
         "type": "string"
        },
        "type": "array"
       }
      },
      "required": [
       "name"
      ],
      "type": "object"
     }
    },
    "required": [
     "project_identifier",
     "test_run"
    ],
    "type": "object"
   },
   "name": "createTestRun"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Expand Percy Visual Testing"
   },
   "description": "Set up or expand Percy visual testing configuration with comprehensive coverage for existing projects that might have Percy integrated. This supports both Percy Web Standalone and Percy Automate. Example prompts: Expand percy coverage for this project {project_name}",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "detectedBrowserAutomationFramework": {
      "enum": [
       "playwright",
       "selenium",
       "cypress",
       "webdriverio"
      ],
      "type": "string"
     },
     "detectedLanguage": {
      "enum": [
       "nodejs",
       "python",
       "java",
       "csharp",
       "ruby"
      ],
      "type": "string"
     },
     "detectedTestingFramework": {
      "enum": [
       "jest",
       "codeceptjs",
       "playwright",
       "pytest",
       "robot",
       "behave",
       "cucumber",
       "nightwatch",
       "mocha",
       "junit4",
       "junit5",
       "testng",
       "cypress",
       "nunit",
       "mstest",
       "xunit",
       "specflow",
       "reqnroll",
       "rspec",
       "serenity"
      ],
      "type": "string"
     },
     "filePaths": {
      "description": "An array of absolute file paths to specific UI test files. Use this when you want to target specific test files rather than entire folders. If not provided, will use folderPaths instead.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "folderPaths": {
      "description": "An array of absolute folder paths containing UI test files. If not provided, analyze codebase for UI test folders by scanning for test patterns which contain UI test cases as per framework. Return empty array if none found.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "integrationType": {
      "description": "Specify the Percy integration type: web (Percy Web) or automate (Percy Automate). If not provided, always prompt the user with: 'Please specify the Percy integration type.' Do not proceed without an explicit selection. Never use a default.",
      "enum": [
       "web",
       "automate"
      ],
      "type": "string"
     },
     "projectName": {
      "description": "A unique name for your Percy project.",
      "type": "string"
     }
    },
    "required": [
     "projectName",
     "detectedLanguage",
     "detectedBrowserAutomationFramework",
     "detectedTestingFramework",
     "integrationType"
    ],
    "type": "object"
   },
   "name": "expandPercyVisualTesting"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Fetch Accessibility Issues"
   },
   "description": "Fetch accessibility issues from a completed scan with pagination support. Use cursor parameter to get subsequent pages of results. Requires the ids of a completed scan, which are returned by startAccessibilityScan. No tool here lists past scans, so if you do not have the ids, start a new scan rather than guessing.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "cursor": {
      "description": "Character offset for pagination (default: 0)",
      "type": "number"
     },
     "scanId": {
      "description": "The scan ID from a completed accessibility scan",
      "type": "string"
     },
     "scanRunId": {
      "description": "The scan run ID from a completed accessibility scan",
      "type": "string"
     }
    },
    "required": [
     "scanId",
     "scanRunId"
    ],
    "type": "object"
   },
   "name": "fetchAccessibilityIssues"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Fetch Automation Screenshots"
   },
   "description": "Fetch screenshots captured during an Automate/App Automate session.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "sessionId": {
      "description": "The BrowserStack session ID to fetch screenshots from",
      "type": "string"
     },
     "sessionType": {
      "description": "Type of BrowserStack session",
      "enum": [
       "automate",
       "app-automate"
      ],
      "type": "string"
     }
    },
    "required": [
     "sessionId",
     "sessionType"
    ],
    "type": "object"
   },
   "name": "fetchAutomationScreenshots"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Fetch Build Insights"
   },
   "description": "Fetch build details and quality gate results. Includes hashed_id and session_type for listSessions.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "buildId": {
      "description": "The build UUID of the BrowserStack build",
      "type": "string"
     }
    },
    "required": [
     "buildId"
    ],
    "type": "object"
   },
   "name": "fetchBuildInsights"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Fetch Percy Changes"
   },
   "description": "Retrieves and summarizes all visual changes detected by Percy AI between the latest and previous builds, helping quickly review what has changed in your project.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "project_name": {
      "description": "The name of the BrowserStack project. If not found, ask user directly.",
      "type": "string"
     }
    },
    "required": [
     "project_name"
    ],
    "type": "object"
   },
   "name": "fetchPercyChanges"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Fetch Root Cause Analysis"
   },
   "description": "Fetch AI Root Cause Analysis for the current user's failed BrowserStack Automate/App-Automate tests. Suggests fixes only; never auto-apply, require explicit user approval. Requires test ids. Call listTestIds first if you do not have it.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "testId": {
      "description": "Array of integer test IDs to fetch RCA data for (maximum 3 IDs). These must be numeric test IDs, not session IDs or strings. If not provided, use the listTestIds tool to get all failed testcases. If more than 3 IDs are provided, only the first 3 will be processed.",
      "items": {
       "maximum": 9007199254740991,
       "minimum": -9007199254740991,
       "type": "integer"
      },
      "maxItems": 3,
      "type": "array"
     }
    },
    "required": [
     "testId"
    ],
    "type": "object"
   },
   "name": "fetchRCA"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Fetch Self-Healed Selectors"
   },
   "description": "Fetch BrowserStack self-healed selectors plus the test source code for the run. Provide exactly one of `sessionId` (single Automate / App-Automate session) or `buildUuid` (full self-healing report for a build). Pass the returned locator pairs to `prepareSelfHealingPlan` to plan edits. Requires a session id. listSessions lists them for a build; if you do not have the build either, getBuildId or listBuildId resolves one from a project and build name.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "buildUuid": {
      "description": "Build UUID. Fetches the build's self-healing report.",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID. Mutually exclusive with buildUuid.",
      "type": "string"
     },
     "sessionType": {
      "description": "Used with sessionId. Defaults to automate.",
      "enum": [
       "automate",
       "app-automate"
      ],
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "fetchSelfHealedSelectors"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Get Accessibility Auth Config"
   },
   "description": "Retrieve an existing authentication configuration by ID. Requires the numeric id returned by createAccessibilityAuthConfig. No tool here lists existing configurations, so if you do not have the id, create one rather than guessing.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "configId": {
      "description": "ID of the auth configuration to retrieve",
      "type": "number"
     }
    },
    "required": [
     "configId"
    ],
    "type": "object"
   },
   "name": "getAccessibilityAuthConfig"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Get Build ID"
   },
   "description": "Get the BrowserStack build ID for a given project and build name, scoped to the current user's builds.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "browserStackBuildName": {
      "description": "The BrowserStack build name used during test run creation. Action: First, check browserstack.yml or any equivalent project configuration files. If the build name is found, extract and return it. If it is not found or if there is any uncertainty, immediately prompt the user to provide the value. Do not infer, guess, or assume a default.",
      "type": "string"
     },
     "browserStackProjectName": {
      "description": "The BrowserStack project name used during test run creation. Action: First, check browserstack.yml or any equivalent project configuration files. If the project name is found, extract and return it. If it is not found or if there is any uncertainty, immediately prompt the user to provide the value. Do not infer, guess, or assume a default.",
      "type": "string"
     }
    },
    "required": [
     "browserStackProjectName",
     "browserStackBuildName"
    ],
    "type": "object"
   },
   "name": "getBuildId"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Get Failure Logs"
   },
   "description": "Fetch logs, or the session video URL, for an Automate/App Automate session. Requires a session id. listSessions lists them for a build; if you do not have the build either, getBuildId or listBuildId resolves one from a project and build name.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "buildId": {
      "description": "App Automate build ID. Optional \u2014 resolved from the session if omitted.",
      "type": "string"
     },
     "logTypes": {
      "description": "The types of logs to fetch.",
      "items": {
       "enum": [
        "networkLogs",
        "sessionLogs",
        "consoleLogs",
        "deviceLogs",
        "appiumLogs",
        "crashLogs",
        "video"
       ],
       "type": "string"
      },
      "type": "array"
     },
     "sessionId": {
      "description": "The BrowserStack session ID. Must be explicitly provided by the user.",
      "type": "string"
     },
     "sessionType": {
      "description": "Type of BrowserStack session. Must be explicitly provided by the user.",
      "enum": [
       "automate",
       "app-automate"
      ],
      "type": "string"
     }
    },
    "required": [
     "sessionType",
     "sessionId",
     "logTypes"
    ],
    "type": "object"
   },
   "name": "getFailureLogs"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Get Sub Test Plan"
   },
   "description": "Fetch a sub-test-plan (STP-*) under a parent plan (TP-*). Returns metadata and linked test runs. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns. Requires a test plan identifier (TP-*). Call listTestPlans first if you do not have it. Creating a test plan or sub-plan, and linking or unlinking test runs on one, are not available as tools here: call askBrowserStackAI with product \"tm\" and describe what you want. It asks you to confirm before changing anything.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "parent_test_plan_identifier": {
      "description": "Parent test plan identifier (TP-*).",
      "type": "string"
     },
     "project_identifier": {
      "description": "Project identifier (PR-*).",
      "type": "string"
     },
     "sub_test_plan_identifier": {
      "description": "Sub-test-plan identifier (STP-*).",
      "type": "string"
     }
    },
    "required": [
     "project_identifier",
     "parent_test_plan_identifier",
     "sub_test_plan_identifier"
    ],
    "type": "object"
   },
   "name": "getSubTestPlan"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Get Test Plan"
   },
   "description": "Fetch a test plan by identifier (TP-*) from BrowserStack Test Management. Returns plan metadata, the full list of linked test runs, total test-case count across runs, and a status summary \u2014 suitable for generating test documentation or QA status reports. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns. Requires a test plan identifier (TP-*). Call listTestPlans first if you do not have it. Creating a test plan or sub-plan, and linking or unlinking test runs on one, are not available as tools here: call askBrowserStackAI with product \"tm\" and describe what you want. It asks you to confirm before changing anything.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "project_identifier": {
      "description": "Identifier of the project (starts with PR- followed by a number).",
      "type": "string"
     },
     "test_plan_identifier": {
      "description": "Identifier of the test plan (starts with TP- followed by a number).",
      "type": "string"
     }
    },
    "required": [
     "project_identifier",
     "test_plan_identifier"
    ],
    "type": "object"
   },
   "name": "getTestPlan"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Build IDs"
   },
   "description": "Get the latest build ID for a project and build name, across all users (no user filter).",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "browserStackBuildName": {
      "description": "The BrowserStack build name used during test run creation. Action: First, check browserstack.yml or any equivalent project configuration files. If the build name is found, extract and return it. If it is not found or if there is any uncertainty, immediately prompt the user to provide the value. Do not infer, guess, or assume a default.",
      "type": "string"
     },
     "browserStackProjectName": {
      "description": "The BrowserStack project name used during test run creation. Action: First, check browserstack.yml or any equivalent project configuration files. If the project name is found, extract and return it. If it is not found or if there is any uncertainty, immediately prompt the user to provide the value. Do not infer, guess, or assume a default.",
      "type": "string"
     }
    },
    "required": [
     "browserStackProjectName",
     "browserStackBuildName"
    ],
    "type": "object"
   },
   "name": "listBuildId"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Folders"
   },
   "description": "List folders in a BrowserStack Test Management project, returning each folder's id and name (plus case counts and sub-folder counts). Pass parent_id to list sub-folders under a specific folder instead of top-level folders. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "p": {
      "description": "Page number.",
      "type": "number"
     },
     "parent_id": {
      "description": "If provided, list sub-folders under this parent folder id. If omitted, lists top-level folders.",
      "type": "number"
     },
     "project_identifier": {
      "description": "Identifier of the project to fetch folders from (starts with PR- followed by a number).",
      "type": "string"
     }
    },
    "required": [
     "project_identifier"
    ],
    "type": "object"
   },
   "name": "listFolders"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Sessions"
   },
   "description": "List sessions for a hashed Automate/App Automate build: session IDs, status, OS, browser/device, dashboard URL.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "buildId": {
      "description": "Hashed build id from the dashboard, or the observability build id from getBuildId.",
      "type": "string"
     },
     "limit": {
      "description": "Max sessions to return. Defaults to 10.",
      "exclusiveMinimum": 0,
      "maximum": 9007199254740991,
      "type": "integer"
     },
     "offset": {
      "description": "Pagination offset for the REST session list.",
      "maximum": 9007199254740991,
      "minimum": 0,
      "type": "integer"
     },
     "sessionType": {
      "description": "Type of BrowserStack session",
      "enum": [
       "automate",
       "app-automate"
      ],
      "type": "string"
     },
     "status": {
      "description": "Optional session status filter (e.g. done, running, error). Applied client-side.",
      "type": "string"
     }
    },
    "required": [
     "sessionType",
     "buildId"
    ],
    "type": "object"
   },
   "name": "listSessions"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Sub Test Plans"
   },
   "description": "List sub-test-plans under a parent test plan (TP-*) in a Test Management project. Supports pagination. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns. Requires a test plan identifier (TP-*). Call listTestPlans first if you do not have it. Creating a test plan or sub-plan, and linking or unlinking test runs on one, are not available as tools here: call askBrowserStackAI with product \"tm\" and describe what you want. It asks you to confirm before changing anything.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "p": {
      "description": "Page number.",
      "type": "number"
     },
     "parent_test_plan_identifier": {
      "description": "Parent test plan identifier (TP-*).",
      "type": "string"
     },
     "project_identifier": {
      "description": "Project identifier (PR-*).",
      "type": "string"
     }
    },
    "required": [
     "project_identifier",
     "parent_test_plan_identifier"
    ],
    "type": "object"
   },
   "name": "listSubTestPlans"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Test Case Templates"
   },
   "description": "List test-case templates with their numeric template_id. Use the id with createTestCase to apply a custom template (the 'template' slug only selects system templates).",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "name": {
      "description": "Case-insensitive substring filter on template name.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "listTestCaseTemplates"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Test Cases"
   },
   "description": "List test cases in a project, optionally scoped to a specific folder. Omit folder_id to list all test cases in the project; provide folder_id (discoverable via listFolders) to list only that folder's cases. Supports filters: case_type, priority, pagination. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "case_type": {
      "description": "Comma-separated list of case types (e.g. functional,regression).",
      "type": "string"
     },
     "folder_id": {
      "description": "Optional. If provided, only return test cases in this folder. If omitted, returns all test cases in the project. Folder ids can be discovered via listFolders.",
      "type": "string"
     },
     "p": {
      "description": "Page number.",
      "type": "number"
     },
     "priority": {
      "description": "Comma-separated list of priorities (e.g. critical,medium,low).",
      "type": "string"
     },
     "project_identifier": {
      "description": "Identifier of the project to fetch test cases from. This id starts with a PR- and is followed by a number.",
      "type": "string"
     }
    },
    "required": [
     "project_identifier"
    ],
    "type": "object"
   },
   "name": "listTestCases"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Test Files"
   },
   "description": "Lists all test files for a given set of directories.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {},
    "type": "object"
   },
   "name": "listTestFiles"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Test IDs"
   },
   "description": "List all tests of a BrowserStack build (each with its status and session id); optional status filter. Requires a BrowserStack build id. Call getBuildId or listBuildId first if you do not have it.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "buildId": {
      "description": "The Browserstack Build ID of the test run. If not known, use the getBuildId tool to fetch it using project and build name",
      "type": "string"
     },
     "includeFailureDetail": {
      "description": "Add per-test failure signature for clustering. Default false.",
      "type": "boolean"
     },
     "status": {
      "description": "Filter tests by status. If not provided, all tests are returned. Example for RCA usecase always use failed status",
      "enum": [
       "passed",
       "failed",
       "pending",
       "skipped"
      ],
      "type": "string"
     }
    },
    "required": [
     "buildId",
     "status"
    ],
    "type": "object"
   },
   "name": "listTestIds"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Test Plans"
   },
   "description": "List test plans in a BrowserStack Test Management project. Returns each plan's identifier (TP-*), name, status, description, dates, and active/closed test-run counts. Supports pagination. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns. Creating a test plan or sub-plan, and linking or unlinking test runs on one, are not available as tools here: call askBrowserStackAI with product \"tm\" and describe what you want. It asks you to confirm before changing anything.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "p": {
      "description": "Page number.",
      "type": "number"
     },
     "project_identifier": {
      "description": "Identifier of the project to fetch test plans from (starts with PR- followed by a number).",
      "type": "string"
     }
    },
    "required": [
     "project_identifier"
    ],
    "type": "object"
   },
   "name": "listTestPlans"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "List Test Runs"
   },
   "description": "List test runs in a project with optional filters (date ranges, assignee, state, etc.) Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "project_identifier": {
      "description": "Identifier of the project to fetch test runs from (usually starts with PR-).",
      "type": "string"
     },
     "run_state": {
      "description": "Return all test runs with this state (comma-separated)",
      "type": "string"
     }
    },
    "required": [
     "project_identifier"
    ],
    "type": "object"
   },
   "name": "listTestRuns"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Manage Percy Build Approval"
   },
   "description": "Approve or reject a Percy build",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "action": {
      "description": "The action to perform on the Percy build.",
      "enum": [
       "approve",
       "unapprove",
       "reject"
      ],
      "type": "string"
     },
     "buildId": {
      "description": "The ID of the Percy build to approve or reject.",
      "type": "string"
     }
    },
    "required": [
     "buildId",
     "action"
    ],
    "type": "object"
   },
   "name": "managePercyBuildApproval"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Percy Visual Test Integration Agent"
   },
   "description": "Integrate Percy visual testing into new projects and demonstrate visual change detection through a step-by-step simulation. This tool handles initial Percy integration, setup, and creates a working demonstration for new users. Primary tool for prompts like: \"Integrate percy for this project\", \"Integrate percy in this project {project_name}\"",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "detectedBrowserAutomationFramework": {
      "enum": [
       "playwright",
       "selenium",
       "cypress",
       "webdriverio"
      ],
      "type": "string"
     },
     "detectedLanguage": {
      "enum": [
       "nodejs",
       "python",
       "java",
       "csharp",
       "ruby"
      ],
      "type": "string"
     },
     "detectedTestingFramework": {
      "enum": [
       "jest",
       "codeceptjs",
       "playwright",
       "pytest",
       "robot",
       "behave",
       "cucumber",
       "nightwatch",
       "mocha",
       "junit4",
       "junit5",
       "testng",
       "cypress",
       "nunit",
       "mstest",
       "xunit",
       "specflow",
       "reqnroll",
       "rspec",
       "serenity"
      ],
      "type": "string"
     },
     "filePaths": {
      "description": "An array of absolute file paths to specific UI test files. Use this when you want to target specific test files rather than entire folders. If not provided, will use folderPaths instead.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "folderPaths": {
      "description": "An array of absolute folder paths containing UI test files. If not provided, analyze codebase for UI test folders by scanning for test patterns which contain UI test cases as per framework. Return empty array if none found.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "integrationType": {
      "description": "Specify the Percy integration type: web (Percy Web) or automate (Percy Automate). If not provided, always prompt the user with: 'Please specify the Percy integration type.' Do not proceed without an explicit selection. Never use a default.",
      "enum": [
       "web",
       "automate"
      ],
      "type": "string"
     },
     "projectName": {
      "description": "A unique name for your Percy project.",
      "type": "string"
     }
    },
    "required": [
     "projectName",
     "detectedLanguage",
     "detectedBrowserAutomationFramework",
     "detectedTestingFramework",
     "integrationType"
    ],
    "type": "object"
   },
   "name": "percyVisualTestIntegrationAgent"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Prepare Self-Healing Plan"
   },
   "description": "Build a self-healing edit plan that bundles locator pairs with test source code for the calling LLM to apply with its own editing tools. This tool does NOT modify files \u2014 it returns structured context so the LLM can edit only the relevant call sites (avoids blind find/replace across shared ids/classes). `sessions` accepts the canonical shape `[{sessionId, locators: [{original, healed, thought?}]}]`, a single session object, an envelope `{sessions: [...]}`, the raw report `{healing_logs: [...]}` (with `healed_selectors` aliasing `locators`), and snake_case keys (`session_id`, `original_locator`, `healed_locator`, `healing_thought`). Requires a session id. listSessions lists them for a build; if you do not have the build either, getBuildId or listBuildId resolves one from a project and build name.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "sessions": {
      "description": "Sessions to plan edits for. See tool description for accepted shapes.",
      "items": {
       "properties": {
        "locators": {
         "description": "Healed locator pairs for this session.",
         "items": {
          "properties": {
           "healed": {
            "properties": {
             "strategy": {
              "description": "Locator strategy, e.g. 'css selector', 'xpath'.",
              "type": "string"
             },
             "value": {
              "description": "The locator string itself.",
              "type": "string"
             }
            },
            "required": [
             "strategy",
             "value"
            ],
            "type": "object"
           },
           "original": {
            "properties": {
             "strategy": {
              "description": "Locator strategy, e.g. 'css selector', 'xpath'.",
              "type": "string"
             },
             "value": {
              "description": "The locator string itself.",
              "type": "string"
             }
            },
            "required": [
             "strategy",
             "value"
            ],
            "type": "object"
           },
           "thought": {
            "type": "string"
           }
          },
          "required": [
           "original",
           "healed"
          ],
          "type": "object"
         },
         "minItems": 1,
         "type": "array"
        },
        "sessionId": {
         "description": "BrowserStack session ID.",
         "type": "string"
        },
        "sessionName": {
         "type": "string"
        }
       },
       "required": [
        "sessionId",
        "locators"
       ],
       "type": "object"
      },
      "minItems": 1,
      "type": "array"
     }
    },
    "required": [
     "sessions"
    ],
    "type": "object"
   },
   "name": "prepareSelfHealingPlan"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Run App Live Session"
   },
   "description": "Use this tool when user wants to manually check their app on a particular mobile device using BrowserStack's cloud infrastructure. Can be used to debug crashes, slow performance, etc.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "appPath": {
      "description": "The path to the .ipa or .apk file to install on the device. Always ask the user for the app path, do not assume it. Must be located inside the directory set in MCP_UPLOAD_BASE_DIR.",
      "type": "string"
     },
     "desiredPhone": {
      "description": "The full name of the device to run the app on. Example: 'iPhone 12 Pro' or 'Samsung Galaxy S20' or 'Google Pixel 6'. Always ask the user for the device they want to use, do not assume it. ",
      "type": "string"
     },
     "desiredPlatform": {
      "description": "Which platform to run on, examples: 'android', 'ios'. Set this based on the app path provided.",
      "enum": [
       "android",
       "ios"
      ],
      "type": "string"
     },
     "desiredPlatformVersion": {
      "description": "Specifies the platform version to run the app on. For example, use '12.0' for Android or '16.0' for iOS. If the user says 'latest', 'newest', or similar, normalize it to 'latest'. Likewise, convert terms like 'earliest' or 'oldest' to 'oldest'.",
      "type": "string"
     }
    },
    "required": [
     "desiredPhone",
     "desiredPlatformVersion",
     "desiredPlatform",
     "appPath"
    ],
    "type": "object"
   },
   "name": "runAppLiveSession"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Run App Tests on BrowserStack"
   },
   "description": "Execute pre-built native mobile test suites (Espresso for Android, XCUITest for iOS) by direct upload to BrowserStack. ONLY for compiled .apk/.ipa test files. This is NOT for SDK integration or Appium tests. For Appium-based testing with SDK setup, use 'setupBrowserStackAppAutomateTests' instead.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "appPath": {
      "description": "Path to your application file:\nIf in development IDE directory:\n\u2022 For Android: 'gradle assembleDebug'\n\u2022 For iOS:\n  xcodebuild clean -scheme YOUR_SCHEME && \\\n  xcodebuild archive -scheme YOUR_SCHEME -configuration Release -archivePath build/app.xcarchive && \\\n  xcodebuild -exportArchive -archivePath build/app.xcarchive -exportPath build/ipa -exportOptionsPlist exportOptions.plist\n\nIf in other directory, provide existing app path.\nThe resolved file must be located inside the directory set in MCP_UPLOAD_BASE_DIR.",
      "type": "string"
     },
     "detectedAutomationFramework": {
      "description": "The automation framework used in the project, such as 'espresso' (Android) or 'xcuitest' (iOS).",
      "enum": [
       "espresso",
       "xcuitest"
      ],
      "type": "string"
     },
     "devices": {
      "default": [],
      "description": "Tuples describing target mobile devices. Add device only when user asks explicitly for it. Defaults to [] . Example: [['android', 'Samsung Galaxy S24', '14'], ['ios', 'iPhone 15', '17']]",
      "items": {
       "properties": {
        "deviceName": {
         "description": "Device name, e.g. 'Samsung Galaxy S24', 'Google Pixel 8', 'iPhone 15', 'iPhone 14 Pro'",
         "type": "string"
        },
        "osVersion": {
         "description": "OS version, e.g. '14', '16', '17', 'latest'",
         "type": "string"
        },
        "platform": {
         "description": "Platform name: 'android' or 'ios'",
         "enum": [
          "android",
          "ios"
         ],
         "type": "string"
        }
       },
       "required": [
        "platform",
        "deviceName",
        "osVersion"
       ],
       "type": "object"
      },
      "maxItems": 3,
      "type": "array"
     },
     "project": {
      "default": "BStack-AppAutomate-Suite",
      "description": "Project name for organizing test runs on BrowserStack.",
      "type": "string"
     },
     "testSuitePath": {
      "description": "Path to your test suite file:\nIf in development IDE directory:\n\u2022 For Android: 'gradle assembleAndroidTest'\n\u2022 For iOS:\n  xcodebuild test-without-building -scheme YOUR_SCHEME -destination 'generic/platform=iOS' && \\\n  cd ~/Library/Developer/Xcode/DerivedData/*/Build/Products/Debug-iphonesimulator/ && \\\n  zip -r Tests.zip *.xctestrun *-Runner.app\n\nIf in other directory, provide existing test file path.\nThe resolved file must be located inside the directory set in MCP_UPLOAD_BASE_DIR.",
      "type": "string"
     }
    },
    "required": [
     "appPath",
     "testSuitePath",
     "detectedAutomationFramework"
    ],
    "type": "object"
   },
   "name": "runAppTestsOnBrowserStack"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false,
    "title": "Run Browser Live Session"
   },
   "description": "Launch a BrowserStack Live session (desktop or mobile).",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "desiredBrowser": {
      "description": "Browser for desktop (Chrome, IE, Firefox, Safari, Edge)",
      "enum": [
       "chrome",
       "firefox",
       "safari",
       "edge",
       "ie"
      ],
      "type": "string"
     },
     "desiredBrowserVersion": {
      "description": "Browser version for desktop (e.g. '133.2', 'latest'). If the user says 'latest', 'newest', or similar, normalize it to 'latest'. Likewise, convert terms like 'earliest' or 'oldest' to 'oldest'.",
      "type": "string"
     },
     "desiredDevice": {
      "description": "Device name for mobile",
      "type": "string"
     },
     "desiredOS": {
      "description": "Desktop OS ('Windows' or 'OS X') or mobile OS ('android','ios','winphone')",
      "enum": [
       "Windows",
       "OS X",
       "android",
       "ios",
       "winphone"
      ],
      "type": "string"
     },
     "desiredOSVersion": {
      "description": "The OS version must be specified as a version number (e.g., '10', '14.0') or as a keyword such as 'latest' or 'oldest'. Normalize variations like 'newest' or 'most recent' to 'latest', and terms like 'earliest' or 'first' to 'oldest'. For macOS, version names (e.g., 'Sequoia') must be used instead of numeric versions.",
      "type": "string"
     },
     "desiredURL": {
      "description": "The URL to test",
      "format": "uri",
      "type": "string"
     },
     "platformType": {
      "description": "Must be 'desktop' or 'mobile'",
      "enum": [
       "desktop",
       "mobile"
      ],
      "type": "string"
     }
    },
    "required": [
     "platformType",
     "desiredURL",
     "desiredOS",
     "desiredOSVersion",
     "desiredBrowser"
    ],
    "type": "object"
   },
   "name": "runBrowserLiveSession"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Run Percy Scan"
   },
   "description": "Run a Percy visual test scan. Example prompts : Run this Percy build/scan. Never run percy scan/build without this tool",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "integrationType": {
      "description": "Specifies whether to integrate with Percy Web or Percy Automate. If not explicitly provided, prompt the user to select the desired integration type.",
      "enum": [
       "web",
       "automate"
      ],
      "type": "string"
     },
     "percyRunCommand": {
      "description": "The test command to run with Percy. Optional \u2014 the LLM should try to infer it first from project context.",
      "type": "string"
     },
     "projectName": {
      "description": "The name of the project to run Percy on.",
      "type": "string"
     }
    },
    "required": [
     "projectName",
     "integrationType"
    ],
    "type": "object"
   },
   "name": "runPercyScan"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Set Up App Automate Tests"
   },
   "description": "Set up BrowserStack App Automate SDK integration for Appium-based mobile app testing. ONLY for Appium based framework . This tool configures SDK for various languages with appium. For pre-built Espresso or XCUITest test suites, use 'runAppTestsOnBrowserStack' instead.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "appPath": {
      "description": "Path to the mobile app file (.apk for Android, .ipa for iOS). Can be a local file path or a BrowserStack app URL (bs://)",
      "type": "string"
     },
     "detectedFramework": {
      "description": "The mobile automation framework configured in the project. Example: 'appium'",
      "enum": [
       "appium"
      ],
      "type": "string"
     },
     "detectedLanguage": {
      "description": "The programming language used in the project. Supports Java and C#. Example: 'java', 'csharp'",
      "enum": [
       "java",
       "nodejs",
       "python",
       "ruby",
       "csharp"
      ],
      "type": "string"
     },
     "detectedTestingFramework": {
      "description": "The testing framework used in the project. Be precise with framework selection Example: 'testng', 'behave', 'pytest', 'robot'",
      "enum": [
       "testng",
       "junit5",
       "junit4",
       "selenide",
       "jbehave",
       "serenity",
       "cucumberTestng",
       "cucumberJunit4",
       "cucumberJunit5",
       "webdriverio",
       "nightwatch",
       "jest",
       "mocha",
       "cucumberJs",
       "robot",
       "pytest",
       "behave",
       "lettuce",
       "rspec",
       "cucumberRuby",
       "nunit",
       "mstest",
       "xunit",
       "specflow",
       "reqnroll"
      ],
      "type": "string"
     },
     "devices": {
      "default": [],
      "description": "Tuples describing target mobile devices. Add device only when user asks explicitly for it. Defaults to [] . Example: [['android', 'Samsung Galaxy S24', '14'], ['ios', 'iPhone 15', '17']]",
      "items": {
       "properties": {
        "deviceName": {
         "description": "Device name, e.g. 'Samsung Galaxy S24', 'Google Pixel 8', 'iPhone 15', 'iPhone 14 Pro'",
         "type": "string"
        },
        "osVersion": {
         "description": "OS version, e.g. '14', '16', '17', 'latest'",
         "type": "string"
        },
        "platform": {
         "description": "Platform name: 'android' or 'ios'",
         "enum": [
          "android",
          "ios"
         ],
         "type": "string"
        }
       },
       "required": [
        "platform",
        "deviceName",
        "osVersion"
       ],
       "type": "object"
      },
      "maxItems": 3,
      "type": "array"
     },
     "project": {
      "default": "BStack-AppAutomate-Suite",
      "description": "Project name for organizing test runs on BrowserStack.",
      "type": "string"
     }
    },
    "required": [
     "detectedFramework",
     "detectedTestingFramework",
     "detectedLanguage",
     "appPath"
    ],
    "type": "object"
   },
   "name": "setupBrowserStackAppAutomateTests"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Set Up Automate Tests"
   },
   "description": "Set up and run automated web-based tests on BrowserStack using the BrowserStack SDK. Use this tool for functional or integration test setup on BrowserStack only. For any visual testing or Percy integration, use the dedicated Percy setup tool. Example prompts: run this test on browserstack; set up this project for browserstack.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "detectedBrowserAutomationFramework": {
      "enum": [
       "playwright",
       "selenium",
       "cypress",
       "webdriverio"
      ],
      "type": "string"
     },
     "detectedLanguage": {
      "enum": [
       "nodejs",
       "python",
       "java",
       "csharp",
       "ruby"
      ],
      "type": "string"
     },
     "detectedTestingFramework": {
      "enum": [
       "jest",
       "codeceptjs",
       "playwright",
       "pytest",
       "robot",
       "behave",
       "cucumber",
       "nightwatch",
       "mocha",
       "junit4",
       "junit5",
       "testng",
       "cypress",
       "nunit",
       "mstest",
       "xunit",
       "specflow",
       "reqnroll",
       "rspec",
       "serenity"
      ],
      "type": "string"
     },
     "devices": {
      "default": [],
      "description": "Device objects array. Use the object format directly - no transformation needed. Add only when user explicitly requests devices. Examples: [{ platform: 'windows', osVersion: '11', browser: 'chrome', browserVersion: 'latest' }] or [{ platform: 'android', deviceName: 'Samsung Galaxy S24', osVersion: '14', browser: 'chrome' }].",
      "items": {
       "properties": {
        "browser": {
         "description": "Browser name, e.g. 'chrome', 'safari', 'edge', 'firefox'",
         "type": "string"
        },
        "browserVersion": {
         "description": "Browser version for desktop platforms only (windows, macos), e.g. '132', 'latest', 'oldest'. Not used for mobile devices (android, ios).",
         "type": "string"
        },
        "deviceName": {
         "description": "Device name for mobile platforms, e.g. 'iPhone 15', 'Samsung Galaxy S24'",
         "type": "string"
        },
        "osVersion": {
         "description": "OS version, e.g. '11', 'Sequoia', '14', '17', 'latest'",
         "type": "string"
        },
        "platform": {
         "description": "Platform name, e.g. 'windows', 'macos', 'android', 'ios'",
         "enum": [
          "windows",
          "macos",
          "android",
          "ios"
         ],
         "type": "string"
        }
       },
       "required": [
        "platform",
        "osVersion"
       ],
       "type": "object"
      },
      "maxItems": 3,
      "type": "array"
     },
     "projectName": {
      "description": "A single name for your project to organize all your tests.",
      "type": "string"
     }
    },
    "required": [
     "projectName",
     "detectedLanguage",
     "detectedBrowserAutomationFramework",
     "detectedTestingFramework"
    ],
    "type": "object"
   },
   "name": "setupBrowserStackAutomateTests"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false,
    "title": "Start Accessibility Scan"
   },
   "description": "Start an accessibility scan via BrowserStack and retrieve a local CSV report path.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "advancedRules": {
      "description": "Enable advanced accessibility rules in the scan settings",
      "type": "boolean"
     },
     "authConfigId": {
      "description": "Optional auth config ID for authenticated scans",
      "type": "number"
     },
     "name": {
      "description": "Name of the accessibility scan",
      "type": "string"
     },
     "pageURL": {
      "description": "The URL to scan for accessibility issues",
      "type": "string"
     }
    },
    "required": [
     "name",
     "pageURL"
    ],
    "type": "object"
   },
   "name": "startAccessibilityScan"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Take App Screenshot"
   },
   "description": "Use this tool to take a screenshot of an app running on a BrowserStack device. This is useful for visual testing and debugging.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "appPath": {
      "description": "The path to the .apk or .ipa file. Required for app installation. Must be located inside the directory set in MCP_UPLOAD_BASE_DIR.",
      "type": "string"
     },
     "desiredPhone": {
      "description": "The full name of the device to run the app on. Example: 'iPhone 12 Pro' or 'Samsung Galaxy S20'. Always ask the user for the device they want to use.",
      "type": "string"
     },
     "desiredPlatform": {
      "description": "Platform to run the app on. Either 'android' or 'ios'.",
      "enum": [
       "android",
       "ios"
      ],
      "type": "string"
     },
     "desiredPlatformVersion": {
      "description": "The platform version to run the app on. Use 'latest' or 'oldest' for dynamic resolution.",
      "type": "string"
     }
    },
    "required": [
     "desiredPhone",
     "desiredPlatformVersion",
     "desiredPlatform",
     "appPath"
    ],
    "type": "object"
   },
   "name": "takeAppScreenshot"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Update Test Case"
   },
   "description": "Update an existing test case in BrowserStack Test Management. Any subset of the following fields may be changed: name, description, preconditions, test_case_steps, owner, priority, case_type, automation_status, status, tags, issues, custom_fields. Only the supplied fields are modified. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "automation_status": {
      "description": "Updated automation status. Use internal name such as 'not_automated', 'automated', 'automation_not_required', 'cannot_be_automated', or 'obsolete'.",
      "type": "string"
     },
     "case_type": {
      "description": "Updated test case type. Accepts either display name (e.g. 'Functional', 'Regression', 'Smoke & Sanity') or internal name (e.g. 'functional', 'smoke_sanity'). Valid values are per-project.",
      "type": "string"
     },
     "custom_fields": {
      "additionalProperties": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "number"
        },
        {
         "type": "boolean"
        },
        {
         "items": {
          "type": [
           "string",
           "number"
          ]
         },
         "type": "array"
        }
       ]
      },
      "description": "Map of custom field NAME to value; use an array for multi-select fields.",
      "propertyNames": {
       "type": "string"
      },
      "type": "object"
     },
     "description": {
      "description": "Updated brief description of the test case.",
      "type": "string"
     },
     "issues": {
      "description": "Replacement list of linked Jira/Asana/Azure issue IDs for the test case.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "name": {
      "description": "Updated name of the test case.",
      "type": "string"
     },
     "owner": {
      "description": "Email of the test case owner.",
      "format": "email",
      "pattern": "^(?:[A-Za-z0-9_'+\\-]+\\.)*[A-Za-z0-9_'+\\-]*[A-Za-z0-9_+-]@(?:[A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
      "type": "string"
     },
     "preconditions": {
      "description": "Updated preconditions for the test case.",
      "type": "string"
     },
     "priority": {
      "description": "Updated priority. Accepts either display name (e.g. 'Medium', 'Critical', 'High', 'Low') or internal name (e.g. 'medium'). Valid values are per-project and discoverable via the form-fields endpoint.",
      "type": "string"
     },
     "project_identifier": {
      "description": "The ID of the BrowserStack project containing the test case to update.",
      "type": "string"
     },
     "status": {
      "description": "Updated review status of the test case (e.g. 'active', 'draft', 'in_review', 'outdated', 'rejected').",
      "type": "string"
     },
     "tags": {
      "description": "Replacement list of tags for the test case.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "test_case_identifier": {
      "description": "The ID of the test case to update. This can be found using the listTestCases tool.",
      "type": "string"
     },
     "test_case_steps": {
      "description": "Updated list of test case steps with expected results.",
      "items": {
       "properties": {
        "result": {
         "description": "The expected result of this step.",
         "type": "string"
        },
        "step": {
         "description": "The action to perform in this step.",
         "type": "string"
        }
       },
       "required": [
        "step",
        "result"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "project_identifier",
     "test_case_identifier"
    ],
    "type": "object"
   },
   "name": "updateTestCase"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Update Test Run"
   },
   "description": "Update a test run's metadata and/or add test cases to it. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "project_identifier": {
      "description": "Identifier of the project (Starts with 'PR-')",
      "type": "string"
     },
     "test_run": {
      "properties": {
       "add_test_cases": {
        "description": "Test cases to add to the run",
        "items": {
         "properties": {
          "configuration_ids": {
           "description": "Configuration IDs to apply",
           "items": {
            "type": "number"
           },
           "type": "array"
          },
          "test_case_ids": {
           "description": "Test case IDs, e.g. TC-123",
           "items": {
            "type": "string"
           },
           "minItems": 1,
           "type": "array"
          }
         },
         "required": [
          "test_case_ids"
         ],
         "type": "object"
        },
        "type": "array"
       },
       "name": {
        "description": "New name of the test run",
        "type": "string"
       },
       "preserve_existing_results": {
        "description": "Keep existing results when adding cases (default true)",
        "type": "boolean"
       },
       "run_state": {
        "description": "Updated state of the test run",
        "enum": [
         "new_run",
         "in_progress",
         "under_review",
         "rejected",
         "done",
         "closed"
        ],
        "type": "string"
       }
      },
      "type": "object"
     },
     "test_run_id": {
      "description": "Test run identifier (e.g., TR-678)",
      "type": "string"
     }
    },
    "required": [
     "project_identifier",
     "test_run_id",
     "test_run"
    ],
    "type": "object"
   },
   "name": "updateTestRun"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
    "title": "Upload Product Requirement File"
   },
   "description": "Upload files (e.g., PDRs, PDFs) to BrowserStack Test Management and retrieve a file mapping ID. This is utilized for generating test cases from files and is part of the Test Case Generator AI Agent in BrowserStack. Requires a project identifier (PR-*). No tool here lists projects, so if you do not have one, call askBrowserStackAI with product \"tm\" and ask which projects exist, then retry this tool with the identifier it returns.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "file_path": {
      "description": "Full path to the file that should be uploaded. Must be inside the directory configured via the MCP_UPLOAD_BASE_DIR environment variable.",
      "type": "string"
     },
     "project_identifier": {
      "description": "ID of the project where the file should be uploaded. Do not assume it, always ask user for it.",
      "type": "string"
     }
    },
    "required": [
     "project_identifier",
     "file_path"
    ],
    "type": "object"
   },
   "name": "uploadProductRequirementFile"
  }
 ],
 "version": "1.4.2"
}