{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "mulmocast-vision",
 "protocol": "2025-06-18",
 "published": "2025-12-05T23:28:26.898Z",
 "tools": [
  {
   "description": "Create or update one slide in the presentation that shows the agenda. This slide lists the main topics as bullet points under a clear title, giving the audience an overview of what will be covered.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "items": {
      "description": "Agenda bullet points",
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     },
     "title": {
      "description": "Heading shown at the top of the agenda slide",
      "type": "string"
     }
    },
    "required": [
     "title",
     "items"
    ],
    "type": "object"
   },
   "name": "createAgendaSlide"
  },
  {
   "description": "Create or update a balance sheet summary placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "assets": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "equity": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "liabilities": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "assets",
     "liabilities",
     "equity"
    ],
    "type": "object"
   },
   "name": "createBalanceSheetSlide"
  },
  {
   "description": "Create or update a balanced scorecard slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "perspectives": {
      "items": {
       "properties": {
        "items": {
         "items": {
          "type": "string"
         },
         "type": "array"
        },
        "name": {
         "type": "string"
        }
       },
       "required": [
        "name",
        "items"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "perspectives"
    ],
    "type": "object"
   },
   "name": "createBalancedScorecardSlide"
  },
  {
   "description": "Create or update a BCG matrix placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "cashCows": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "dogs": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "questionMarks": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "stars": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "stars",
     "cashCows",
     "questionMarks",
     "dogs"
    ],
    "type": "object"
   },
   "name": "createBcgMatrixSlide"
  },
  {
   "description": "Create or update a before vs after comparison slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "after": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     },
     "before": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     },
     "title": {
      "type": "string"
     }
    },
    "required": [
     "before",
     "after"
    ],
    "type": "object"
   },
   "name": "createBeforeAfterSlide"
  },
  {
   "description": "Create or update a benchmarking comparison table.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "competitors": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     },
     "metrics": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     }
    },
    "required": [
     "metrics",
     "competitors"
    ],
    "type": "object"
   },
   "name": "createBenchmarkingTableSlide"
  },
  {
   "description": "Create or update a bubble chart placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "points": {
      "items": {
       "properties": {
        "label": {
         "type": "string"
        },
        "r": {
         "type": "number"
        },
        "x": {
         "type": "number"
        },
        "y": {
         "type": "number"
        }
       },
       "required": [
        "label",
        "x",
        "y",
        "r"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "title": {
      "type": "string"
     }
    },
    "required": [
     "points"
    ],
    "type": "object"
   },
   "name": "createBubbleChartPlaceholderSlide"
  },
  {
   "description": "Create or update a Business Model Canvas (9 blocks).",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "blocks": {
      "additionalProperties": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "type": "object"
     }
    },
    "required": [
     "blocks"
    ],
    "type": "object"
   },
   "name": "createBusinessModelCanvasSlide"
  },
  {
   "description": "Create or update a capability maturity model (1\u20135).",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "capabilities": {
      "items": {
       "properties": {
        "level": {
         "type": "number"
        },
        "name": {
         "type": "string"
        }
       },
       "required": [
        "name",
        "level"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "capabilities"
    ],
    "type": "object"
   },
   "name": "createCapabilityMaturitySlide"
  },
  {
   "description": "Create or update a cash flow summary placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "inflows": {
      "items": {
       "type": "number"
      },
      "type": "array"
     },
     "outflows": {
      "items": {
       "type": "number"
      },
      "type": "array"
     }
    },
    "required": [
     "inflows",
     "outflows"
    ],
    "type": "object"
   },
   "name": "createCashFlowSlide"
  },
  {
   "description": "Create or update a change curve with stages.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "notes": {
      "type": "string"
     },
     "stages": {
      "items": {
       "type": "string"
      },
      "minItems": 4,
      "type": "array"
     }
    },
    "required": [
     "stages"
    ],
    "type": "object"
   },
   "name": "createChangeCurveSlide"
  },
  {
   "description": "Create or update a circular economy loop map placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "loops": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     }
    },
    "required": [
     "loops"
    ],
    "type": "object"
   },
   "name": "createCircularEconomyMapSlide"
  },
  {
   "description": "Create or update a communication plan matrix.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "audiences": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "cadence": {
      "type": "string"
     },
     "channels": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "audiences",
     "channels"
    ],
    "type": "object"
   },
   "name": "createCommunicationPlanSlide"
  },
  {
   "description": "Create or update a compliance heatmap placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "areas": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "levels": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "areas"
    ],
    "type": "object"
   },
   "name": "createComplianceHeatmapSlide"
  },
  {
   "description": "Create or update a cost structure with buckets and notes.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "buckets": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     },
     "fixedVsVariable": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "buckets"
    ],
    "type": "object"
   },
   "name": "createCostStructureSlide"
  },
  {
   "description": "Create or update a CSR initiatives slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "initiatives": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     }
    },
    "required": [
     "initiatives"
    ],
    "type": "object"
   },
   "name": "createCsrInitiativesSlide"
  },
  {
   "description": "Create or update a culture and values slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "behaviors": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "values": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     }
    },
    "required": [
     "values"
    ],
    "type": "object"
   },
   "name": "createCultureValuesSlide"
  },
  {
   "description": "Create or update a customer journey with stages and touchpoints.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "stages": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     },
     "touchpoints": {
      "items": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "type": "array"
     }
    },
    "required": [
     "stages"
    ],
    "type": "object"
   },
   "name": "createCustomerJourneySlide"
  },
  {
   "description": "Create or update a customer success journey map.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "metrics": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "stages": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     }
    },
    "required": [
     "stages"
    ],
    "type": "object"
   },
   "name": "createCustomerSuccessJourneySlide"
  },
  {
   "description": "Create or update a digital maturity assessment.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "dimensions": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "levels": {
      "items": {
       "type": "number"
      },
      "type": "array"
     },
     "notes": {
      "type": "string"
     }
    },
    "required": [
     "dimensions"
    ],
    "type": "object"
   },
   "name": "createDigitalMaturitySlide"
  },
  {
   "description": "Create or update one slide in the presentation that visualizes a driver tree. This slide shows a key business metric at the top and the drivers that influence it branching below.",
   "inputSchema": {
    "properties": {
     "drivers": {
      "_index": {
       "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
       "type": "integer"
      },
      "description": "Driver branches that affect the main metric",
      "items": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "minItems": 1,
      "type": "array"
     },
     "metric": {
      "description": "The main business metric being analyzed",
      "type": "string"
     }
    },
    "required": [
     "metric",
     "drivers"
    ],
    "type": "object"
   },
   "name": "createDriverTreeSlide"
  },
  {
   "description": "Create or update an ecosystem or partner map.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "categories": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "entities": {
      "items": {
       "properties": {
        "category": {
         "type": "string"
        },
        "name": {
         "type": "string"
        }
       },
       "required": [
        "category",
        "name"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "entities"
    ],
    "type": "object"
   },
   "name": "createEcosystemMapSlide"
  },
  {
   "description": "Create or update an ESG framework summary.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "environmental": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "governance": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "social": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "environmental",
     "social",
     "governance"
    ],
    "type": "object"
   },
   "name": "createEsgFrameworkSlide"
  },
  {
   "description": "Create or update one slide in the presentation that provides an executive summary. This slide should include a clear title and 3\u20137 concise bullet points highlighting the key takeaways.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "bullets": {
      "description": "Key takeaway points to summarize",
      "items": {
       "type": "string"
      },
      "maxItems": 7,
      "minItems": 3,
      "type": "array"
     },
     "title": {
      "description": "Slide title for the executive summary",
      "type": "string"
     }
    },
    "required": [
     "title",
     "bullets"
    ],
    "type": "object"
   },
   "name": "createExecutiveSummarySlide"
  },
  {
   "description": "Create or update a marketing 4P slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "place": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "price": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "product": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "promotion": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "product",
     "price",
     "place",
     "promotion"
    ],
    "type": "object"
   },
   "name": "createFourPSlide"
  },
  {
   "description": "Create or update a funnel stages placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "stages": {
      "items": {
       "properties": {
        "label": {
         "type": "string"
        },
        "value": {
         "type": "number"
        }
       },
       "required": [
        "label",
        "value"
       ],
       "type": "object"
      },
      "minItems": 3,
      "type": "array"
     }
    },
    "required": [
     "stages"
    ],
    "type": "object"
   },
   "name": "createFunnelSlide"
  },
  {
   "description": "Create or update a simple Gantt placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "tasks": {
      "items": {
       "properties": {
        "end": {
         "type": "string"
        },
        "name": {
         "type": "string"
        },
        "start": {
         "type": "string"
        }
       },
       "required": [
        "name",
        "start",
        "end"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "tasks"
    ],
    "type": "object"
   },
   "name": "createGanttSimpleSlide"
  },
  {
   "description": "Create or update a GE-McKinsey 3x3 matrix placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "competitiveStrength": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "industryAttractiveness": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "placements": {
      "items": {
       "properties": {
        "col": {
         "type": "number"
        },
        "name": {
         "type": "string"
        },
        "row": {
         "type": "number"
        }
       },
       "required": [
        "name",
        "row",
        "col"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "placements"
    ],
    "type": "object"
   },
   "name": "createGeMcKinseyMatrixSlide"
  },
  {
   "description": "Create or update a heatmap placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "cols": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     },
     "rows": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     },
     "values": {
      "items": {
       "items": {
        "type": "number"
       },
       "type": "array"
      },
      "type": "array"
     }
    },
    "required": [
     "rows",
     "cols",
     "values"
    ],
    "type": "object"
   },
   "name": "createHeatmapPlaceholderSlide"
  },
  {
   "description": "Create or update one slide in the presentation that presents a hypothesis. This slide states the hypothesis clearly and may also outline implications and suggested next steps.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "hypothesis": {
      "description": "The main hypothesis statement",
      "type": "string"
     },
     "implications": {
      "description": "Possible implications if the hypothesis is true",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "nextSteps": {
      "description": "Recommended next actions",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "hypothesis"
    ],
    "type": "object"
   },
   "name": "createHypothesisSlide"
  },
  {
   "description": "Create or update an innovation funnel placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "counts": {
      "items": {
       "type": "number"
      },
      "type": "array"
     },
     "stages": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     }
    },
    "required": [
     "stages"
    ],
    "type": "object"
   },
   "name": "createInnovationFunnelSlide"
  },
  {
   "description": "Create or update a post-merger integration (PMI) plan placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "milestones": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "workstreams": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "workstreams"
    ],
    "type": "object"
   },
   "name": "createIntegrationPlanSlide"
  },
  {
   "description": "Create or update one slide in the presentation that visualizes an issue tree. This slide shows the root issue at the top and branches into MECE sub-issues to structure the problem.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "branches": {
      "description": "Sub-issues or branches grouped under the root issue",
      "items": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "minItems": 2,
      "type": "array"
     },
     "rootIssue": {
      "description": "Main issue or problem statement",
      "type": "string"
     }
    },
    "required": [
     "rootIssue",
     "branches"
    ],
    "type": "object"
   },
   "name": "createIssueTreeSlide"
  },
  {
   "description": "Create or update one slide in the presentation that emphasizes a single key message. The headline should state the main message, supported by at least two bullet points.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "headline": {
      "description": "Main key message shown as the headline",
      "type": "string"
     },
     "supports": {
      "description": "Supporting points or evidence",
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "headline",
     "supports"
    ],
    "type": "object"
   },
   "name": "createKeyMessageWithSupportsSlide"
  },
  {
   "description": "Create or update a slide highlighting key KPIs.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "kpis": {
      "items": {
       "properties": {
        "delta": {
         "type": "string"
        },
        "label": {
         "type": "string"
        },
        "value": {
         "type": "string"
        }
       },
       "required": [
        "label",
        "value"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "title": {
      "type": "string"
     }
    },
    "required": [
     "kpis"
    ],
    "type": "object"
   },
   "name": "createKpiHighlightSlide"
  },
  {
   "description": "Create or update a go-to-market launch plan.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "milestones": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "risks": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "workstreams": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "workstreams"
    ],
    "type": "object"
   },
   "name": "createLaunchPlanSlide"
  },
  {
   "description": "Create or update an M&A pipeline placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "stages": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     },
     "targets": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "stages"
    ],
    "type": "object"
   },
   "name": "createMAPipelineSlide"
  },
  {
   "description": "Create or update a Marimekko chart placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "categories": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     },
     "title": {
      "type": "string"
     }
    },
    "required": [
     "categories"
    ],
    "type": "object"
   },
   "name": "createMarimekkoPlaceholderSlide"
  },
  {
   "description": "Create or update a list of market growth drivers.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "drivers": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     },
     "title": {
      "type": "string"
     }
    },
    "required": [
     "drivers"
    ],
    "type": "object"
   },
   "name": "createMarketDriversSlide"
  },
  {
   "description": "Create or update a marketing mix summary.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "levers": {
      "items": {
       "type": "string"
      },
      "minItems": 4,
      "type": "array"
     },
     "notes": {
      "type": "string"
     }
    },
    "required": [
     "levers"
    ],
    "type": "object"
   },
   "name": "createMarketingMixSlide"
  },
  {
   "description": "Create or update one slide in the presentation that organizes items into MECE (Mutually Exclusive, Collectively Exhaustive) groups. Each group has a label and its own bullet list.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "groups": {
      "items": {
       "properties": {
        "items": {
         "description": "Bullet points within this group",
         "items": {
          "type": "string"
         },
         "minItems": 1,
         "type": "array"
        },
        "label": {
         "description": "Group label",
         "type": "string"
        }
       },
       "required": [
        "label",
        "items"
       ],
       "type": "object"
      },
      "minItems": 2,
      "type": "array"
     },
     "title": {
      "description": "Title of the MECE slide",
      "type": "string"
     }
    },
    "required": [
     "title",
     "groups"
    ],
    "type": "object"
   },
   "name": "createMeceListSlide"
  },
  {
   "description": "Create or update a milestone timeline.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "milestones": {
      "items": {
       "properties": {
        "date": {
         "type": "string"
        },
        "label": {
         "type": "string"
        },
        "notes": {
         "type": "string"
        }
       },
       "required": [
        "label"
       ],
       "type": "object"
      },
      "minItems": 2,
      "type": "array"
     },
     "title": {
      "type": "string"
     }
    },
    "required": [
     "milestones"
    ],
    "type": "object"
   },
   "name": "createMilestoneTimelineSlide"
  },
  {
   "description": "Create or update an NPV summary placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "assumptions": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "npv": {
      "type": "number"
     }
    },
    "required": [
     "npv"
    ],
    "type": "object"
   },
   "name": "createNpvSummarySlide"
  },
  {
   "description": "Create or update an OKR/KPI dashboard with tiles.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "metrics": {
      "items": {
       "properties": {
        "label": {
         "type": "string"
        },
        "status": {
         "type": "string"
        },
        "target": {
         "type": "string"
        },
        "value": {
         "type": "string"
        }
       },
       "required": [
        "label",
        "value"
       ],
       "type": "object"
      },
      "minItems": 3,
      "type": "array"
     },
     "title": {
      "type": "string"
     }
    },
    "required": [
     "metrics"
    ],
    "type": "object"
   },
   "name": "createOkrKpiDashboardSlide"
  },
  {
   "description": "Create or update an options vs criteria evaluation matrix.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "criteria": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     },
     "options": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     },
     "scores": {
      "items": {
       "items": {
        "type": "number"
       },
       "type": "array"
      },
      "type": "array"
     }
    },
    "required": [
     "criteria",
     "options"
    ],
    "type": "object"
   },
   "name": "createOptionEvaluationSlide"
  },
  {
   "description": "Create or update a simple org chart placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "nodes": {
      "items": {
       "properties": {
        "id": {
         "type": "string"
        },
        "label": {
         "type": "string"
        },
        "parentId": {
         "type": "string"
        }
       },
       "required": [
        "id",
        "label"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "nodes"
    ],
    "type": "object"
   },
   "name": "createOrgChartSlide"
  },
  {
   "description": "combine images to create a PDF.",
   "inputSchema": {
    "properties": {
     "filename": {
      "description": "a file name including the .pdf extension",
      "type": "string"
     },
     "images": {
      "description": "Optional. If provided, combine exactly these image file name in the given order. those image file name should be the basename (including the .pdf extension) without any directory path, corresponding to the slides that were created. If omitted, images will be discovered in dir, sorted by filename ascending, and combined.",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "filename"
    ],
    "type": "object"
   },
   "name": "createPDF"
  },
  {
   "description": "Create or update a P&L breakdown placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "categories": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     },
     "values": {
      "items": {
       "type": "number"
      },
      "minItems": 3,
      "type": "array"
     }
    },
    "required": [
     "categories",
     "values"
    ],
    "type": "object"
   },
   "name": "createPLBreakdownSlide"
  },
  {
   "description": "Create or update a partnership ecosystem map.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "categories": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "partners": {
      "items": {
       "properties": {
        "category": {
         "type": "string"
        },
        "name": {
         "type": "string"
        }
       },
       "required": [
        "name"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "partners"
    ],
    "type": "object"
   },
   "name": "createPartnershipMapSlide"
  },
  {
   "description": "Create or update customer persona cards.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "personas": {
      "items": {
       "properties": {
        "bio": {
         "type": "string"
        },
        "name": {
         "type": "string"
        },
        "needs": {
         "items": {
          "type": "string"
         },
         "type": "array"
        }
       },
       "required": [
        "name"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "personas"
    ],
    "type": "object"
   },
   "name": "createPersonasSlide"
  },
  {
   "description": "Create or update a pipeline funnel placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "stages": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     },
     "values": {
      "items": {
       "type": "number"
      },
      "type": "array"
     }
    },
    "required": [
     "stages"
    ],
    "type": "object"
   },
   "name": "createPipelineFunnelSlide"
  },
  {
   "description": "Create or update a Porter\u2019s Five Forces slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "buyers": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "newEntrants": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "rivalry": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "substitutes": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "suppliers": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "newEntrants",
     "suppliers",
     "buyers",
     "substitutes",
     "rivalry"
    ],
    "type": "object"
   },
   "name": "createPorterFiveForcesSlide"
  },
  {
   "description": "Create or update a competitive positioning map.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "players": {
      "items": {
       "properties": {
        "name": {
         "type": "string"
        },
        "x": {
         "type": "number"
        },
        "y": {
         "type": "number"
        }
       },
       "required": [
        "name",
        "x",
        "y"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "xAxis": {
      "type": "string"
     },
     "yAxis": {
      "type": "string"
     }
    },
    "required": [
     "xAxis",
     "yAxis",
     "players"
    ],
    "type": "object"
   },
   "name": "createPositioningMapSlide"
  },
  {
   "description": "Create or update a pricing waterfall placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "steps": {
      "items": {
       "properties": {
        "label": {
         "type": "string"
        },
        "value": {
         "type": "number"
        }
       },
       "required": [
        "label",
        "value"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "steps"
    ],
    "type": "object"
   },
   "name": "createPricingWaterfallSlide"
  },
  {
   "description": "Create or update a product roadmap.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "items": {
      "items": {
       "properties": {
        "label": {
         "type": "string"
        },
        "release": {
         "type": "string"
        }
       },
       "required": [
        "release",
        "label"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "releases": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "releases"
    ],
    "type": "object"
   },
   "name": "createProductRoadmapSlide"
  },
  {
   "description": "Create or update one slide in the presentation that follows the pyramid principle. The slide starts with the key message, followed by supporting reasons, and optional detailed breakdowns.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "details": {
      "description": "Optional deeper details for each supporting reason",
      "items": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "type": "array"
     },
     "keyMessage": {
      "description": "Top-level conclusion or main message",
      "type": "string"
     },
     "supports": {
      "description": "Supporting reasons for the key message",
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "keyMessage",
     "supports"
    ],
    "type": "object"
   },
   "name": "createPyramidPrincipleSlide"
  },
  {
   "description": "Create or update a quarterly roadmap.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "items": {
      "items": {
       "properties": {
        "label": {
         "type": "string"
        },
        "quarter": {
         "type": "string"
        }
       },
       "required": [
        "quarter",
        "label"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "quarters": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "quarters",
     "items"
    ],
    "type": "object"
   },
   "name": "createQuarterlyRoadmapSlide"
  },
  {
   "description": "Create or update a RACI matrix.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "assignments": {
      "items": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "type": "array"
     },
     "roles": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     },
     "tasks": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     }
    },
    "required": [
     "tasks",
     "roles"
    ],
    "type": "object"
   },
   "name": "createRaciSlide"
  },
  {
   "description": "Create or update revenue streams and pricing notes.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "pricingNotes": {
      "type": "string"
     },
     "streams": {
      "items": {
       "type": "string"
      },
      "minItems": 1,
      "type": "array"
     }
    },
    "required": [
     "streams"
    ],
    "type": "object"
   },
   "name": "createRevenueModelSlide"
  },
  {
   "description": "Create or update a risk register with mitigations.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "risks": {
      "items": {
       "properties": {
        "impact": {
         "type": "string"
        },
        "likelihood": {
         "type": "string"
        },
        "mitigation": {
         "type": "string"
        },
        "risk": {
         "type": "string"
        }
       },
       "required": [
        "risk"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "risks"
    ],
    "type": "object"
   },
   "name": "createRiskMitigationSlide"
  },
  {
   "description": "Create or update a sales dashboard placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "metrics": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     },
     "notes": {
      "type": "string"
     }
    },
    "required": [
     "metrics"
    ],
    "type": "object"
   },
   "name": "createSalesDashboardSlide"
  },
  {
   "description": "Create or update a scenario planning slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "impacts": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "scenarios": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "scenarios"
    ],
    "type": "object"
   },
   "name": "createScenarioPlanningSlide"
  },
  {
   "description": "Create or update one slide in the presentation that explains a storyline using the SCQA framework: Situation, Complication, Question, and Answer.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "answer": {
      "description": "The answer or proposed resolution",
      "type": "string"
     },
     "complication": {
      "description": "The complication or challenge",
      "type": "string"
     },
     "question": {
      "description": "The key question that arises",
      "type": "string"
     },
     "situation": {
      "description": "The initial situation or context",
      "type": "string"
     }
    },
    "required": [
     "situation",
     "complication",
     "question",
     "answer"
    ],
    "type": "object"
   },
   "name": "createScqaSlide"
  },
  {
   "description": "Create or update a new slide in the presentation that serves as a section divider. This slide must include both a large main heading and a subtitle. Use this when the user wants to create a single slide that clearly introduces or separates a new section in their presentation.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "heading": {
      "description": "Main section title, shown prominently on the slide",
      "type": "string"
     },
     "subheading": {
      "description": "Subtitle that provides context or explanation for the section",
      "type": "string"
     }
    },
    "required": [
     "heading",
     "subheading"
    ],
    "type": "object"
   },
   "name": "createSectionDividerSlide"
  },
  {
   "description": "Create or update a customer segmentation slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "descriptors": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "segments": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "segments"
    ],
    "type": "object"
   },
   "name": "createSegmentationSlide"
  },
  {
   "description": "Create or update a sensitivity analysis placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "scenarios": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     },
     "variables": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "variables",
     "scenarios"
    ],
    "type": "object"
   },
   "name": "createSensitivityAnalysisSlide"
  },
  {
   "description": "Create or update a McKinsey 7S slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "sharedValues": {
      "type": "string"
     },
     "skills": {
      "type": "string"
     },
     "staff": {
      "type": "string"
     },
     "strategy": {
      "type": "string"
     },
     "structure": {
      "type": "string"
     },
     "style": {
      "type": "string"
     },
     "systems": {
      "type": "string"
     }
    },
    "required": [
     "strategy",
     "structure",
     "systems",
     "sharedValues",
     "skills",
     "style",
     "staff"
    ],
    "type": "object"
   },
   "name": "createSevenSSlide"
  },
  {
   "description": "Create or update a shareholder value driver tree.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "drivers": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     }
    },
    "required": [
     "drivers"
    ],
    "type": "object"
   },
   "name": "createShareholderValueTreeSlide"
  },
  {
   "description": "Create or update a stakeholder influence-interest map.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "stakeholders": {
      "items": {
       "properties": {
        "influence": {
         "type": "number"
        },
        "interest": {
         "type": "number"
        },
        "name": {
         "type": "string"
        },
        "notes": {
         "type": "string"
        }
       },
       "required": [
        "name",
        "influence",
        "interest"
       ],
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "stakeholders"
    ],
    "type": "object"
   },
   "name": "createStakeholderMapSlide"
  },
  {
   "description": "Create or update a support organization model placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "roles": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "tiers": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "tiers"
    ],
    "type": "object"
   },
   "name": "createSupportOrgModelSlide"
  },
  {
   "description": "Create or update a survey results summary.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "questions": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "summaries": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "questions"
    ],
    "type": "object"
   },
   "name": "createSurveyResultsSlide"
  },
  {
   "description": "Create or update a sustainability roadmap.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "actions": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "phases": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "phases"
    ],
    "type": "object"
   },
   "name": "createSustainabilityRoadmapSlide"
  },
  {
   "description": "Create or update a SWOT matrix.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "opportunities": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "strengths": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "threats": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "weaknesses": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "strengths",
     "weaknesses",
     "opportunities",
     "threats"
    ],
    "type": "object"
   },
   "name": "createSwotSlide"
  },
  {
   "description": "Create or update a synergy capture plan placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "sources": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "values": {
      "items": {
       "type": "number"
      },
      "type": "array"
     }
    },
    "required": [
     "sources"
    ],
    "type": "object"
   },
   "name": "createSynergyCaptureSlide"
  },
  {
   "description": "Create or update a market size (TAM/SAM/SOM) slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "notes": {
      "type": "string"
     },
     "sam": {
      "type": "number"
     },
     "som": {
      "type": "number"
     },
     "tam": {
      "type": "number"
     }
    },
    "required": [
     "tam",
     "sam",
     "som"
    ],
    "type": "object"
   },
   "name": "createTamSamSomSlide"
  },
  {
   "description": "Create or update a technology roadmap with phases.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "items": {
      "items": {
       "properties": {
        "label": {
         "type": "string"
        },
        "phase": {
         "type": "string"
        }
       },
       "required": [
        "phase",
        "label"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "phases": {
      "items": {
       "type": "string"
      },
      "minItems": 2,
      "type": "array"
     }
    },
    "required": [
     "phases"
    ],
    "type": "object"
   },
   "name": "createTechRoadmapSlide"
  },
  {
   "description": "Create or update a closing slide with a thank-you message and contacts.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "email": {
      "type": "string"
     },
     "message": {
      "default": "Thank you!",
      "type": "string"
     },
     "name": {
      "type": "string"
     },
     "qrImageUrl": {
      "type": "string"
     },
     "url": {
      "type": "string"
     }
    },
    "required": [
     "message"
    ],
    "type": "object"
   },
   "name": "createThankYouContactSlide"
  },
  {
   "description": "Create or update a 3C (Company, Customer, Competitor) slide.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "company": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "competitor": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "customer": {
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "company",
     "customer",
     "competitor"
    ],
    "type": "object"
   },
   "name": "createThreeCSlide"
  },
  {
   "description": "Create or update a 2x2 matrix with quadrant items.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "quadrants": {
      "items": {
       "items": {
        "type": "string"
       },
       "type": "array"
      },
      "maxItems": 4,
      "minItems": 4,
      "type": "array"
     },
     "xAxis": {
      "type": "string"
     },
     "yAxis": {
      "type": "string"
     }
    },
    "required": [
     "xAxis",
     "yAxis",
     "quadrants"
    ],
    "type": "object"
   },
   "name": "createTwoByTwoMatrixSlide"
  },
  {
   "description": "Create or update a value chain with primary and support activities.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "primary": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     },
     "support": {
      "items": {
       "type": "string"
      },
      "minItems": 3,
      "type": "array"
     }
    },
    "required": [
     "primary",
     "support"
    ],
    "type": "object"
   },
   "name": "createValueChainSlide"
  },
  {
   "description": "Create or update a numeric waterfall steps placeholder.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "steps": {
      "items": {
       "properties": {
        "label": {
         "type": "string"
        },
        "value": {
         "type": "number"
        }
       },
       "required": [
        "label",
        "value"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "title": {
      "type": "string"
     }
    },
    "required": [
     "steps"
    ],
    "type": "object"
   },
   "name": "createWaterfallSlide"
  },
  {
   "description": "Create or update a closing slide with a thank-you message and contacts.",
   "inputSchema": {
    "properties": {
     "_index": {
      "description": "If 0 <= _index < slides.length, update that position. Omit or -1/out-of-range to append to the end (default -1).",
      "type": "integer"
     },
     "imageUrl": {
      "type": "string"
     },
     "title": {
      "type": "string"
     }
    },
    "required": [
     "imageUrl"
    ],
    "type": "object"
   },
   "name": "createimageSlide"
  },
  {
   "description": "Specify a directory for each session and output the content to that directory. Call it once before creating a slide, and again when creating the next new slide.",
   "inputSchema": {
    "properties": {
     "directoryName": {
      "description": "Directory name",
      "type": "string"
     }
    },
    "required": [
     "directoryName"
    ],
    "type": "object"
   },
   "name": "setDirectory"
  }
 ],
 "version": "1.0.7"
}