{
 "args": [
  "server"
 ],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "@trusty-squire/mcp",
 "protocol": "2025-06-18",
 "published": "2026-08-19T03:16:16.967Z",
 "tools": [
  {
   "_meta": {
    "anthropic/alwaysLoad": true
   },
   "annotations": {
    "readOnlyHint": true
   },
   "description": "Read the vault audit ledger \u2014 \"show me everything that touched my keys.\"\nReturns account-scoped events newest-first: stored / retrieved / rotated /\ndeleted / proxy_executed / proxy_rejected, each with a timestamp, the credential\nreference, requester, and outcome. NO secret values are ever included.\n\nFilter with `type` (an event kind) or `reference` (a single credential), cap with\n`limit` (default 50, max 200), and page older with `before` (pass the prior\nresponse's `next_before` cursor; null means no more pages). Use this to answer\n\"what used my Stripe key in the last N days\" or to investigate a suspected leak.",
   "inputSchema": {
    "properties": {
     "before": {
      "type": "string"
     },
     "limit": {
      "type": "number"
     },
     "reference": {
      "type": "string"
     },
     "type": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "audit_log"
  },
  {
   "_meta": {
    "anthropic/alwaysLoad": true
   },
   "description": "Mint an egress grant: a revocable token + base URL so an app you DEPLOYED can\ncall a provider using a vaulted credential, WITHOUT ever holding the raw key.\nSquire injects the real secret server-side at the boundary and enforces the\ncredential's allowed_hosts. Point the app's SDK base URL at the returned\n`base_url` and authenticate with the returned `token` \u2014 the SDK's requests are\nforwarded to the provider with the real key swapped in.\n\nPass `service` or `reference` to pick which vaulted credential to leash. Limits\nare OPT-IN and UNLIMITED by default: pass `rate_limit_per_hour` and/or\n`spend_cap_usd` ONLY if the caller wants a cap. Omit them for no rate/spend limit\n(the grant is still revocable + host-scoped + audited).\n\nSECURITY: the token is returned once through this MCP result, so it is visible to\nthe host/model even though the raw provider key is not. Move it directly into\nBACKEND-ONLY deployment secret storage; never put it in client/browser code,\nlogs, or source control. It is strictly safer than the raw key (scoped to one\ncredential's hosts, rate-limitable, instantly revocable, audited), but it is\nstill a bearer secret. Use use_credential instead when zero grant-token exposure\nto the model is required. Revoke any time; revocation is instant and needs no key\nrotation.",
   "inputSchema": {
    "properties": {
     "rate_limit_per_hour": {
      "type": "number"
     },
     "reference": {
      "type": "string"
     },
     "service": {
      "type": "string"
     },
     "spend_cap_usd": {
      "type": "number"
     }
    },
    "type": "object"
   },
   "name": "grant_app_access"
  },
  {
   "_meta": {
    "anthropic/alwaysLoad": true
   },
   "annotations": {
    "readOnlyHint": true
   },
   "description": "List the egress grants minted for this account \u2014 grant_id, which vaulted\ncredential each leashes, rate limit, spend cap, created/revoked timestamps. The\nbackend token itself is never returned (it is shown ONCE at mint time). Use this\nto find the grant_id to hand to revoke_app_access, or to audit what standing app\naccess exists.",
   "inputSchema": {
    "properties": {},
    "type": "object"
   },
   "name": "list_app_access"
  },
  {
   "description": "List the credentials already stored in the user's vault for this account.\n\nWHEN TO CALL THIS TOOL:\n- BEFORE provisioning a service \u2014 check whether the account already has\n  a usable key for it, so you reuse the existing key instead of signing\n  up for a duplicate\n- At the start of a task, to see what API keys are already available\n- Whenever you need a service's key and aren't sure it exists yet\n\nBEHAVIOR:\n- Returns metadata only \u2014 service, key name, type, age, and a vault\n  `reference` \u2014 never the secret values themselves\n- The raw secret is never returned to you; to *use* a key, call\n  use_credential with its `reference` (the server injects it)\n- Scoped to the user's account; only this account's credentials list",
   "inputSchema": {
    "properties": {},
    "type": "object"
   },
   "name": "list_credentials"
  },
  {
   "annotations": {
    "readOnlyHint": true
   },
   "description": "List saved payment cards by opaque ID and user-visible label only. Never returns encrypted blobs or card data.",
   "inputSchema": {
    "properties": {},
    "type": "object"
   },
   "name": "list_payment_cards"
  },
  {
   "description": "Take one action in an operate session, then return the resulting observation. kinds: click (target=element ref, preferably an el_table row's ref), type (target + text; model-supplied card-number-shaped text is refused \u2014 card payment must use operate_pay, which fills a vaulted card without exposing it to the model), TARGET FALLBACK (clicking or typing only) \u2014 when a control you can SEE (in the observation text or screenshot) has NO ref in el_table (a bare click-handler <div> with no role/label, e.g. a SPA \"Add To Cart\"), pass target as a locator instead of a ref: `text=\"Add To Cart\"` (a matching clickable or typeable element, case-insensitive, hidden descendants ignored, open shadow roots pierced) or `css=<selector>` (a raw CSS selector). Only for click / js_click / type / type_secret. Resolved against the LIVE main document and ordinary child frames, not el_table, so it reaches controls the inventory never emitted. It refuses an ambiguous match (returns the candidate texts \u2014 narrow with an exact text= or a css= selector). `click` is actionability-checked and throws if an overlay intercepts; dismiss the overlay or deliberately use `js_click`, the explicit direct DOM dispatch that fires through a transparent overlay. Prefer a real ref when one exists; reach for text=/css= only when none does. Observed child-frame refs and locator matches retain their frame origin. Same-registrable-domain frames are reachable; other frame actions must pass the goto/allow_host domain scope, opaque frames are refused, and type_secret never targets a cross-domain frame. Frame-scoped select supports native <select> controls only; drive a custom framed widget with click. upload/oauth_click/oauth_login refuse frame refs. select (target + text \u2014 pick an option in a native <select> or custom listbox by its visible text, e.g. a country/state dropdown that `type` can't drive), set_phone_country (country \u2014 set the dial-code country on a phone field's native <select>, including react-phone-number-input's hidden country select; other phone widget families are not yet supported and throw; no target needed), goto (url \u2014 domain-scoped), press (key, e.g. Enter), oauth_login (target \u2014 the ONLY action for a Login/Continue with provider button: it runs the OAuth popup/redirect atomically, retains the product tab if the provider closes its window, and returns the post-login observation in this call), oauth_click and oauth_settle are legacy replay compatibility actions; do not use them for new operator work, allow_host (host \u2014 cross into another app's domain mid-task, e.g. from the GCP console into Firebase), type_secret (slot + target \u2014 type a secret you captured into a sealed slot via operate_act { kind: \"extract\", into_slot: \"<slot>\" } into a field on the current site; the value never leaves the browser), scroll (direction down/up/bottom/top, default down \u2014 reveal below-the-fold controls on a long form, then operate_observe to pick up the newly-visible elements), upload (target + path \u2014 attach a LOCAL file: target is the visible upload button/menu-item (or the file <input>), path is an absolute local file path. The bot sets the file via the browser's file chooser, so no OS dialog is driven and no API credential is needed \u2014 it uses the session you're already signed into). cart_add (product_identity + options_hash + idempotency_key \u2014 reserve an idempotent cart mutation, exact-line post-verify it, and return its postcondition), select_many (ordered selections map \u2014 select sequentially, re-observe after each success, and retain partial results), extract (into_slot/secret_label/store \u2014 reveal masked keys and extract credentials from the current page, sealed slot or vaulted), solve_captcha (detect and drive the in-session captcha gate; settled=false carries a needs_user{gate,message,remedy} \u2014 FAIL FAST and relay it to the user), await_verification (sender/into_slot/grant_inbox_consent \u2014 read the user's OWN inbox through their signed-in browser session for an email verification code/link, sealed into a slot with into_slot). Sealed username/password login lifecycle, never exposing raw values: login_prepare_signup (login_slot/password_slot/password_length \u2014 seal the user's captured email and a generated strong password into session slots; fill the signup form with type_secret using the returned slots), login_store_signup (service + login_hosts, optional label/signin_url \u2014 after the account is created, vault the prepared signup slots as a username_password credential so the user can sign back in; the signin_url host, if given, is always included in login_hosts), login_load_saved (reference or service, optional fields/slot_prefix \u2014 for a sign-in page, fetch a saved username/password credential only if the current browser host is allowed for login, then seal its fields into session slots; fill with type_secret). For type/select/set_phone_country, pass provenance when the value came from a Squire-known address, contact, product_query, or quantity input; type_secret and operate_pay record credential and card provenance from their sealed sources. When repairing a replay fallback field, pass its replay_step_index and replay_hole. Stable target refs remain reusable while their element exists. If an @e: ref is stale, the response is {status:target_stale, replacement_candidates, retry_policy:do_not_retry_old_ref}; call operate_observe and choose a new ref instead of retrying the old one. detail (default \"compact\") controls the returned payload: \"none\" skips it entirely for chained fills (then operate_observe before the next ref action), \"full\" returns the legacy screen+accessibility payload. For legible product/variant hints on a cart-affecting action, pass product_identity and options_hash together; returned checkout_state is best-effort informational state and never a payment charge input. Compact observations carry their elements in `el_table`: a TAB-delimited table whose FIRST line is the header (tab-joined column names, a subset of ref,label,tag,role,type,value_len,checked,href,testId,topmost,occluded_by, always starting ref,label,tag) and each following line is ONE element (tab-joined cells in header order). An empty cell = that field is absent for that element; value_len is a number, checked/topmost are true/false; a tab, newline, carriage-return or backslash inside a cell is backslash-escaped (\\t \\n \\r \\\\). el_table is absent when the emit has no element rows. stable refs remain reusable across observes while their controls still exist. The first observe, a URL change, or high churn returns delta:false as a full resync: discard the prior element map and rebuild it from this el_table (or from snapshot_file \u2014 the table may omit collapsed chrome links, which the file keeps); a delta:false with NO snapshot_file already has the complete, uncollapsed set in el_table (a persistence-fallback response) \u2014 reset from it directly. Only when delta:true, el_table lists ONLY the changed elements: upsert them by ref, delete refs listed in removed, and retain the remaining elements counted by unchanged. text_unchanged:true means reuse the prior text because text is empty. snapshot_file points to the complete current snapshot (all elements, with path). An empty delta (no el_table) means nothing changed, not an empty page. detail:\"full\" instead returns the legacy `elements` JSON array (every field), never el_table. If a control you can see in `text`/the screenshot has NO row in el_table (a bare unlabeled clickable div \u2014 e.g. some SPA \"Add To Cart\" buttons), it has no ref: click it with operate_act click/js_click target=`text=\"\u2026\"` or `css=\u2026` (see operate_act). `click` respects actionability and throws if an overlay intercepts; dismiss the overlay or deliberately use `js_click`, which directly dispatches through a transparent overlay. ",
   "inputSchema": {
    "properties": {
     "country": {
      "type": "string"
     },
     "detail": {
      "enum": [
       "none",
       "compact",
       "full"
      ],
      "type": "string"
     },
     "direction": {
      "enum": [
       "down",
       "up",
       "bottom",
       "top"
      ],
      "type": "string"
     },
     "fields": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "grant_inbox_consent": {
      "type": "boolean"
     },
     "host": {
      "type": "string"
     },
     "idempotency_key": {
      "minLength": 1,
      "type": "string"
     },
     "into_slot": {
      "type": "string"
     },
     "key": {
      "type": "string"
     },
     "kind": {
      "enum": [
       "click",
       "js_click",
       "type",
       "select",
       "set_phone_country",
       "goto",
       "press",
       "oauth_login",
       "oauth_click",
       "oauth_settle",
       "allow_host",
       "type_secret",
       "scroll",
       "upload",
       "cart_add",
       "select_many",
       "extract",
       "solve_captcha",
       "await_verification",
       "login_prepare_signup",
       "login_store_signup",
       "login_load_saved"
      ],
      "type": "string"
     },
     "label": {
      "type": "string"
     },
     "login_hosts": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "login_slot": {
      "type": "string"
     },
     "options_hash": {
      "minLength": 1,
      "type": "string"
     },
     "password_length": {
      "type": "number"
     },
     "password_slot": {
      "type": "string"
     },
     "path": {
      "type": "string"
     },
     "product_identity": {
      "minLength": 1,
      "type": "string"
     },
     "provenance": {
      "pattern": "^(?:address|contact|credential|card)(?:\\.[a-zA-Z0-9_-]+)?$|^(?:product_query|quantity)$",
      "type": "string"
     },
     "reference": {
      "type": "string"
     },
     "replay_hole": {
      "pattern": "^(?:address|contact|credential|card)(?:\\.[a-zA-Z0-9_-]+)?$|^(?:product_query|quantity)$",
      "type": "string"
     },
     "replay_step_index": {
      "minimum": 0,
      "type": "integer"
     },
     "secret_label": {
      "type": "string"
     },
     "selections": {
      "additionalProperties": {
       "type": "string"
      },
      "description": "Map each observed field label or @e: ref to its visible option text.",
      "maxProperties": 12,
      "minProperties": 1,
      "type": "object"
     },
     "sender": {
      "type": "string"
     },
     "service": {
      "type": "string"
     },
     "session_id": {
      "type": "string"
     },
     "signin_url": {
      "type": "string"
     },
     "slot": {
      "type": "string"
     },
     "slot_prefix": {
      "type": "string"
     },
     "store": {
      "properties": {
       "auth_shape": {
        "type": "string"
       },
       "egress_hosts": {
        "items": {
         "type": "string"
        },
        "type": "array"
       },
       "env_var_suggestion": {
        "type": "string"
       },
       "label": {
        "type": "string"
       },
       "service": {
        "type": "string"
       },
       "type": {
        "type": "string"
       }
      },
      "required": [
       "service"
      ],
      "type": "object"
     },
     "target": {
      "type": "string"
     },
     "text": {
      "type": "string"
     },
     "url": {
      "type": "string"
     }
    },
    "required": [
     "session_id",
     "kind"
    ],
    "type": "object"
   },
   "name": "operate_act"
  },
  {
   "description": "Finish an operate task and close its session. outcome.kind='none' closes without a reported outcome (and remains the default for compatibility); 'credentials' extracts and vault-stores a credential using required `store`; 'result' reports required `summary` or `data` and can verify_recipe before closing. All credential values remain server-side; after Chrome closes, an eligible clean profile may return to the closed warm slot for the next task.",
   "inputSchema": {
    "properties": {
     "outcome": {
      "oneOf": [
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "none"
         }
        },
        "required": [
         "kind"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "kind": {
          "const": "credentials"
         },
         "store": {
          "properties": {
           "auth_shape": {
            "type": "string"
           },
           "egress_hosts": {
            "items": {
             "type": "string"
            },
            "type": "array"
           },
           "env_var_suggestion": {
            "type": "string"
           },
           "label": {
            "type": "string"
           },
           "service": {
            "type": "string"
           },
           "type": {
            "type": "string"
           }
          },
          "required": [
           "service"
          ],
          "type": "object"
         }
        },
        "required": [
         "kind",
         "store"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "anyOf": [
         {
          "required": [
           "summary"
          ]
         },
         {
          "required": [
           "data"
          ]
         }
        ],
        "properties": {
         "data": {
          "type": "object"
         },
         "kind": {
          "const": "result"
         },
         "summary": {
          "type": "string"
         },
         "verify_recipe": {
          "type": "string"
         }
        },
        "required": [
         "kind"
        ],
        "type": "object"
       }
      ]
     },
     "session_id": {
      "type": "string"
     }
    },
    "required": [
     "session_id"
    ],
    "type": "object"
   },
   "name": "operate_finish"
  },
  {
   "description": "Re-read the current page of an operate session. DEFAULT is a COMPACT payload whose elements ride in `el_table` (a tab-delimited table; each row's stable `ref` is the operate_act.target) with compact label/role/href/value_len columns and `frame_origin` on child-frame controls; ordinary same- and cross-origin frames are included, while known captcha challenge frames stay behind the dedicated captcha flow. Path is retained only in snapshot_file, and redundant screen/accessibility trees are omitted. Compact observations carry their elements in `el_table`: a TAB-delimited table whose FIRST line is the header (tab-joined column names, a subset of ref,label,tag,role,type,value_len,checked,href,testId,topmost,occluded_by, always starting ref,label,tag) and each following line is ONE element (tab-joined cells in header order). An empty cell = that field is absent for that element; value_len is a number, checked/topmost are true/false; a tab, newline, carriage-return or backslash inside a cell is backslash-escaped (\\t \\n \\r \\\\). el_table is absent when the emit has no element rows. stable refs remain reusable across observes while their controls still exist. The first observe, a URL change, or high churn returns delta:false as a full resync: discard the prior element map and rebuild it from this el_table (or from snapshot_file \u2014 the table may omit collapsed chrome links, which the file keeps); a delta:false with NO snapshot_file already has the complete, uncollapsed set in el_table (a persistence-fallback response) \u2014 reset from it directly. Only when delta:true, el_table lists ONLY the changed elements: upsert them by ref, delete refs listed in removed, and retain the remaining elements counted by unchanged. text_unchanged:true means reuse the prior text because text is empty. snapshot_file points to the complete current snapshot (all elements, with path). An empty delta (no el_table) means nothing changed, not an empty page. detail:\"full\" instead returns the legacy `elements` JSON array (every field), never el_table. If a control you can see in `text`/the screenshot has NO row in el_table (a bare unlabeled clickable div \u2014 e.g. some SPA \"Add To Cart\" buttons), it has no ref: click it with operate_act click/js_click target=`text=\"\u2026\"` or `css=\u2026` (see operate_act). `click` respects actionability and throws if an overlay intercepts; dismiss the overlay or deliberately use `js_click`, which directly dispatches through a transparent overlay. Pass detail:\"full\" for the legacy screen+accessibility+full-field payload on a genuinely ambiguous step.",
   "inputSchema": {
    "properties": {
     "detail": {
      "enum": [
       "compact",
       "full"
      ],
      "type": "string"
     },
     "session_id": {
      "type": "string"
     }
    },
    "required": [
     "session_id"
    ],
    "type": "object"
   },
   "name": "operate_observe"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "readOnlyHint": false
   },
   "description": "Pay the checkout in the addressed operate_start browser session. session_id is required when multiple operator sessions are active (and optional only for a sole local session). phase=\"single\" (the default, also implied by omitting phase) is an ordinary one-step checkout. Reads the live merchant and checkout total independently of informational checkout_state. When the live total cannot be machine-read, caller-supplied amount_cents and currency become the authoritative approval amount and take precedence over a prior cart observation. creates a phone approval link, waits for approval, verifies the passkey-signed purchase mandate, opens the card only in this process, fills common checkout fields, submits, and audits only the last four digits. Never solves 3-D Secure; waits for user completion, then returns a needs_user handoff if unresolved. With no card_ref/card_label and no card on file, the approval link becomes a first-time add-card ceremony and the card is bound server-side before the mandate is signed. On approval resume, an unreadable live checkout reuses the original mandate-bound checkout; a successfully-read checkout with a different amount, currency, merchant, or origin returns payment_amount_mismatch without filling the card. For a SPLIT checkout (a card-entry step with no visible total, e.g. Rakuten): call with phase=\"fill_card\" on that step \u2014 the approval amount is sourced from the most recent real total this session observed (e.g. the cart page) only when the card-entry page itself shows none and amount_cents plus currency were not supplied, and releases the card into recognized payment-provider fields without charging. Then drive the checkout to the order-confirmation step, VERIFY the live final total there matches the returned approved amount_cents and currency, and place the order yourself via operate_act, handling any 3-D Secure challenge directly \u2014 Trusty Squire never re-reads the total or clicks the pay/place-order control. Call phase=\"confirm\" any time after the fill to close out the approval; it is not a prerequisite to placing the order. Exactly one human approval per purchase. If the payment gets stuck or the card is declined, recover with operate_finish and start a fresh session \u2014 operate_pay does not support refilling a different card mid-session.",
   "inputSchema": {
    "not": {
     "required": [
      "card_ref",
      "card_label"
     ]
    },
    "properties": {
     "amount_cents": {
      "minimum": 0,
      "type": "integer"
     },
     "card_label": {
      "type": "string"
     },
     "card_ref": {
      "type": "string"
     },
     "currency": {
      "pattern": "^[A-Za-z]{3}$",
      "type": "string"
     },
     "item": {
      "minLength": 1,
      "type": "string"
     },
     "merchant": {
      "type": "string"
     },
     "phase": {
      "description": "\"single\" (the default; also implied by omitting phase) is an ordinary one-step checkout. Split checkouts only: \"fill_card\" approves an amount (falling back to the most recent real total this session observed when the card-entry page itself has none) and releases the vaulted card without charging \u2014 the caller then verifies the final total against that amount and places the order itself via operate_act. \"confirm\" just closes out that approval afterward (no total check, no click, no charge).",
      "enum": [
       "single",
       "fill_card",
       "confirm"
      ],
      "type": "string"
     },
     "reason": {
      "minLength": 1,
      "type": "string"
     },
     "session_id": {
      "description": "The operate_start session to charge. Omit only when exactly one local operator session is active.",
      "format": "uuid",
      "type": "string"
     },
     "three_ds_wait_seconds": {
      "description": "How long to wait for the user to complete a 3-D Secure challenge before handing back (default 180s, 0 = don't wait).",
      "maximum": 600,
      "minimum": 0,
      "type": "integer"
     }
    },
    "required": [
     "item",
     "reason"
    ],
    "type": "object"
   },
   "name": "operate_pay"
  },
  {
   "annotations": {
    "readOnlyHint": true
   },
   "description": "Read-only: report the status of the addressed session's payment approval currently awaiting the human's phone tap, if any \u2014 started by an operate_pay call that returned approval_pending. Pass `wait_seconds` (0-15, default 0) to bound-wait for a change instead of an instant peek; never blocks longer than that. Never verifies a mandate or opens a card. Only candidate_kind=approval with ready_to_charge=true is a final authorization; a review candidate still requires final approval.",
   "inputSchema": {
    "properties": {
     "session_id": {
      "description": "The payment session; omit only when exactly one local session is active.",
      "format": "uuid",
      "type": "string"
     },
     "wait_seconds": {
      "description": "Upper bound on this call's wait. 0 (default) is an instant peek; 1-15 bound-waits for a change.",
      "maximum": 15,
      "minimum": 0,
      "type": "integer"
     }
    },
    "type": "object"
   },
   "name": "operate_payment_status"
  },
  {
   "description": "Replay a local prepared-statement recipe selected by the host-classified closed-enum verb plus service_url. Lookup uses eTLD+1 plus an allow-listed action path derived from the URL path, then falls back to the eTLD+1 catch-all; query parameters are ignored. A legacy name opens the saved workflow as a planning hint without deterministic replay. Binds hole values and executes each step through ordered target fallback. A single miss returns replay.status='fallback_required' with that step and next_index; repair only that step, then call operate_recipe_run again with the same params plus session_id + resume_from=next_index. A recipe whose entry or declared hosts would leave its own site (a tampered or malicious shared recipe) is refused outright: replay.status='domain_lock_violation', and driving continues cold. A recorded operate_pay step is never replayed and instead returns fallback_required so the charge runs through a fresh, human-approved operate_pay. Pass verb + session_id + leg:'checkout' (no service_url) to resolve+replay just the CHECKOUT leg against an already-open session's current page \u2014 keyed by the checkout page's own field-name-set signature, so a checkout plan recorded on one store can replay on a different, unrelated store of the same checkout platform (cross-domain reuse). replay.status='cache_miss' means no recipe matches this page's shape; drive the checkout leg cold. A replay field failure on a recipe with a real catalog/storefront prefix returns replay.status='leg_fallback_required' (not human_required): do not resume that recipe; drive the checkout leg cold from from_step_index, and route any charge through a fresh, human-approved operate_pay on the live session.",
   "inputSchema": {
    "properties": {
     "leg": {
      "enum": [
       "checkout"
      ],
      "type": "string"
     },
     "name": {
      "type": "string"
     },
     "params": {
      "type": "object"
     },
     "require_live_identity": {
      "type": "boolean"
     },
     "resume_from": {
      "type": "integer"
     },
     "service_url": {
      "type": "string"
     },
     "session_id": {
      "type": "string"
     },
     "verb": {
      "enum": [
       "purchase",
       "get_api_key",
       "signup",
       "subscribe",
       "cancel",
       "login",
       "book",
       "reserve",
       "renew",
       "upgrade",
       "downgrade",
       "add_to_cart",
       "checkout",
       "download",
       "configure"
      ],
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "operate_recipe_run"
  },
  {
   "description": "Save the CURRENT successful operate session as a replayable local recipe. Pass the host-classified closed-enum `verb` and the complete authoritative `inputs` ledger (address, contact, product_query, credential, card, quantity), plus a name, goal, and `postcondition`. The postcondition is checked BEFORE anything is written \u2014 the machine-checkable success signal: kind 'execute_capability' observes the end-state now; `success_signal` is {field_text,min_value_len} (a field whose value is at least N chars \u2014 checked by LENGTH, never the value), {text_present}, or {url_contains}. The recipe stores the session's TEXT-targeted action trace as a rail; sealed secrets become slot references, NEVER values. Call AFTER the task succeeded; replay later by (verb, service URL), or by legacy name.",
   "inputSchema": {
    "properties": {
     "goal": {
      "type": "string"
     },
     "inputs": {
      "type": "object"
     },
     "name": {
      "type": "string"
     },
     "postcondition": {
      "properties": {
       "describe": {
        "type": "string"
       },
       "kind": {
        "enum": [
         "execute_capability",
         "observe_artifact"
        ],
        "type": "string"
       },
       "probe_url": {
        "type": "string"
       },
       "success_signal": {
        "type": "object"
       }
      },
      "required": [
       "kind",
       "describe",
       "success_signal"
      ],
      "type": "object"
     },
     "session_id": {
      "type": "string"
     },
     "verb": {
      "enum": [
       "purchase",
       "get_api_key",
       "signup",
       "subscribe",
       "cancel",
       "login",
       "book",
       "reserve",
       "renew",
       "upgrade",
       "downgrade",
       "add_to_cart",
       "checkout",
       "download",
       "configure"
      ],
      "type": "string"
     }
    },
    "required": [
     "session_id",
     "name",
     "goal",
     "verb",
     "inputs",
     "postcondition"
    ],
    "type": "object"
   },
   "name": "operate_recipe_save"
  },
  {
   "description": "Begin an interactive website task: opens a scoped browser on the user's machine at service_url and returns the initial compact observation {session_id, url, text, el_table, delta, snapshot_file}. Compact observations carry their elements in `el_table`: a TAB-delimited table whose FIRST line is the header (tab-joined column names, a subset of ref,label,tag,role,type,value_len,checked,href,testId,topmost,occluded_by, always starting ref,label,tag) and each following line is ONE element (tab-joined cells in header order). An empty cell = that field is absent for that element; value_len is a number, checked/topmost are true/false; a tab, newline, carriage-return or backslash inside a cell is backslash-escaped (\\t \\n \\r \\\\). el_table is absent when the emit has no element rows. stable refs remain reusable across observes while their controls still exist. The first observe, a URL change, or high churn returns delta:false as a full resync: discard the prior element map and rebuild it from this el_table (or from snapshot_file \u2014 the table may omit collapsed chrome links, which the file keeps); a delta:false with NO snapshot_file already has the complete, uncollapsed set in el_table (a persistence-fallback response) \u2014 reset from it directly. Only when delta:true, el_table lists ONLY the changed elements: upsert them by ref, delete refs listed in removed, and retain the remaining elements counted by unchanged. text_unchanged:true means reuse the prior text because text is empty. snapshot_file points to the complete current snapshot (all elements, with path). An empty delta (no el_table) means nothing changed, not an empty page. detail:\"full\" instead returns the legacy `elements` JSON array (every field), never el_table. If a control you can see in `text`/the screenshot has NO row in el_table (a bare unlabeled clickable div \u2014 e.g. some SPA \"Add To Cart\" buttons), it has no ref: click it with operate_act click/js_click target=`text=\"\u2026\"` or `css=\u2026` (see operate_act). `click` respects actionability and throws if an overlay intercepts; dismiss the overlay or deliberately use `js_click`, which directly dispatches through a transparent overlay. YOU are the planner \u2014 read the observation, then drive the signup, setup, or checkout with operate_act (and operate_pay for a purchase), re-read with operate_observe, and call operate_act { kind: \"extract\" } when you reach the credentials. Always operate_finish when done. The browser is domain-scoped to the target + its identity providers. If the registry knows this service, the first observation includes a `hint` \u2014 the route (login method, where the key lives, how many credentials). Read it and drive toward it; fall back to your own judgment if the live page diverges.",
   "inputSchema": {
    "properties": {
     "allowed_hosts": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "extra_allowed_hosts": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "require_live_identity": {
      "type": "boolean"
     },
     "service_url": {
      "type": "string"
     }
    },
    "required": [
     "service_url"
    ],
    "type": "object"
   },
   "name": "operate_start"
  },
  {
   "_meta": {
    "anthropic/alwaysLoad": true
   },
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": true
   },
   "description": "Revoke an egress grant on the spot \u2014 \"something leaked, kill that token now.\"\nPass the `grant_id` returned by grant_app_access (or list_app_access). Revocation\nis INSTANT and global: the next request through that grant's proxy is rejected\n(403), no key rotation required. The vaulted credential itself is untouched and\nany other grants on it keep working. Use this the moment a backend egress token\nis suspected leaked.",
   "inputSchema": {
    "properties": {
     "grant_id": {
      "type": "string"
     }
    },
    "required": [
     "grant_id"
    ],
    "type": "object"
   },
   "name": "revoke_app_access"
  },
  {
   "_meta": {
    "anthropic/alwaysLoad": true
   },
   "annotations": {
    "idempotentHint": true
   },
   "description": "Save a secret the user just shared into the encrypted vault. CALL THIS\nAUTOMATICALLY whenever the user pastes a secret-shaped value (sk-, ghp_,\nAKIA, eyJ; password/token/connection-string patterns) \u2014 don't ask first.\nOne entry per (service, label) \u2014 re-storing the same service OVERWRITES\nit (that's how you rotate a key: just store the new value). For\nmulti-part credentials (AWS access key + secret, DB user+password) pass\n`fields` (e.g. {access_key_id, secret_access_key}); for a lone key pass\n`value`. Optional `label` (default \"default\") keeps prod/dev keys for\nthe same service apart. Optional `auth_shape` records how the provider\nexpects the key so an EGRESS GRANT (grant_app_access) auto-injects it\ncorrectly: \"bearer\" (default) | \"header:<name>\" (e.g. \"header:x-api-key\")\n| \"query:<param>\". Set it for non-bearer providers; bearer needs nothing.\nOptional `observed_hosts` carries hosts seen during signup/extraction and is\nunioned into the credential's allowed_hosts with the service defaults.\nFor username/password credentials, pass `auth_strategy: \"username_password\"`\nand explicit `login_hosts`; those credentials cannot be spent through\n`use_credential` and can only be sealed into browser-fill slots on allowed\nsignin hosts. Returns the reference, field names, and allowed_hosts. The value\nis never readable back to you afterwards.",
   "inputSchema": {
    "properties": {
     "auth_shape": {
      "type": "string"
     },
     "auth_strategy": {
      "type": "string"
     },
     "env_var_suggestion": {
      "type": "string"
     },
     "fields": {
      "additionalProperties": {
       "type": "string"
      },
      "type": "object"
     },
     "label": {
      "type": "string"
     },
     "login_hosts": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "observed_hosts": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "service": {
      "type": "string"
     },
     "signin_url": {
      "type": "string"
     },
     "type": {
      "type": "string"
     },
     "value": {
      "type": "string"
     }
    },
    "required": [
     "service"
    ],
    "type": "object"
   },
   "name": "store_credential"
  },
  {
   "_meta": {
    "anthropic/alwaysLoad": true
   },
   "annotations": {
    "destructiveHint": true
   },
   "description": "Execute an authenticated HTTP request against an external API using a\nvaulted credential. The secret value NEVER crosses to this agent \u2014 the\nserver injects it and returns only the upstream response. In your\nheaders/body use ${SECRET} for a single-field credential, or\n${SECRET.<field>} for a multi-field one (e.g. ${SECRET.access_key_id},\n${SECRET.secret_access_key}); ${SECRET_JSON[.field]} JSON-escapes. Call\nlist_credentials to see a credential's field names. Pass `service` or\n`reference` plus the HTTP fields (method, url, headers, body). This is\nthe ONLY way to use a stored secret \u2014 there is no raw-value extraction.\nFor APIs that authenticate via a query-string key (e.g. FRED's\n`api_key`), put the secret in `query` \u2014 `query: { api_key: \"${SECRET}\" }`\n\u2014 NOT in the url (a ${SECRET} in the url is rejected; the server injects\nquery params after the host check so the key never lands in a log).\nThe target host must be on the credential's allowed_hosts (editable in\nthe web vault) or the call is rejected.",
   "inputSchema": {
    "properties": {
     "http": {
      "properties": {
       "body": {
        "type": "string"
       },
       "headers": {
        "additionalProperties": {
         "type": "string"
        },
        "type": "object"
       },
       "method": {
        "type": "string"
       },
       "query": {
        "additionalProperties": {
         "type": "string"
        },
        "type": "object"
       },
       "url": {
        "type": "string"
       }
      },
      "required": [
       "method",
       "url"
      ],
      "type": "object"
     },
     "reference": {
      "type": "string"
     },
     "service": {
      "type": "string"
     }
    },
    "required": [
     "http"
    ],
    "type": "object"
   },
   "name": "use_credential"
  }
 ],
 "version": "1.1.10"
}