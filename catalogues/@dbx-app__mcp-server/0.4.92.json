{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "@dbx-app/mcp-server",
 "protocol": "2025-06-18",
 "published": "2026-09-20T19:27:58.950Z",
 "tools": [
  {
   "description": "Add a new database connection to DBX",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "database": {
      "type": "string"
     },
     "db_type": {
      "type": "string"
     },
     "driver_profile": {
      "type": "string"
     },
     "host": {
      "type": "string"
     },
     "name": {
      "type": "string"
     },
     "password": {
      "default": "",
      "type": "string"
     },
     "port": {
      "format": "uint16",
      "maximum": 65535,
      "minimum": 0,
      "type": "integer"
     },
     "ssl": {
      "default": false,
      "type": "boolean"
     },
     "username": {
      "default": "",
      "type": "string"
     }
    },
    "required": [
     "name",
     "db_type",
     "host"
    ],
    "type": "object"
   },
   "name": "dbx_add_connection"
  },
  {
   "description": "Begin a transaction on a transaction-enabled native MySQL session",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "session_id": {
      "description": "Transaction-enabled session ID returned by dbx_open_session",
      "type": "string"
     }
    },
    "required": [
     "session_id"
    ],
    "type": "object"
   },
   "name": "dbx_begin_transaction"
  },
  {
   "description": "Close a stateful query session and release its pinned backend connection",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "session_id": {
      "description": "Session ID returned by dbx_open_session",
      "type": "string"
     }
    },
    "required": [
     "session_id"
    ],
    "type": "object"
   },
   "name": "dbx_close_session"
  },
  {
   "description": "Commit the active transaction; committed is reported only after MySQL acknowledges COMMIT",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "session_id": {
      "description": "Transaction-enabled session ID returned by dbx_open_session",
      "type": "string"
     }
    },
    "required": [
     "session_id"
    ],
    "type": "object"
   },
   "name": "dbx_commit_transaction"
  },
  {
   "description": "Get column definitions for a table",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "database": {
      "description": "Database name",
      "type": "string"
     },
     "schema": {
      "description": "Schema name",
      "type": "string"
     },
     "table": {
      "description": "Table name",
      "type": "string"
     }
    },
    "required": [
     "table"
    ],
    "type": "object"
   },
   "name": "dbx_describe_table"
  },
  {
   "description": "Duplicate a DBX connection with its complete settings, credentials, tunnels, and sidebar group",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "new_name": {
      "description": "Name for the copied connection",
      "type": "string"
     }
    },
    "required": [
     "new_name"
    ],
    "type": "object"
   },
   "name": "dbx_duplicate_connection"
  },
  {
   "description": "Execute a SQL query in DBX desktop app UI and show results there. Requires DBX to be running.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "database": {
      "type": "string"
     },
     "sql": {
      "type": "string"
     }
    },
    "required": [
     "sql"
    ],
    "type": "object"
   },
   "name": "dbx_execute_and_show"
  },
  {
   "description": "Execute a SQL script containing multiple statements in one call and return a result per statement. Statements are split with a database-dialect-aware parser, so semicolons inside strings, comments and stored procedures are handled. Stops at the first failing statement unless continue_on_error is true. Pass session_id (from dbx_open_session) when statements must share one connection (e.g. temporary tables, USE/SET). When use_transaction is true and the script has multiple statements, the whole script runs in one transaction and the call returns a single merged result instead of one result per statement (single-statement scripts run normally and return that one result); use_transaction cannot be combined with session_id or continue_on_error, and is rejected when the backend cannot provide a rollbackable transaction or for MySQL-family DDL scripts (DDL implicitly commits and cannot be rolled back).",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "continue_on_error": {
      "description": "When true, execution continues after a statement error instead of stopping at the first failure. Connection-level failures always stop the batch. Default false (stop at first failure).",
      "type": "boolean"
     },
     "database": {
      "description": "Database name",
      "type": "string"
     },
     "session_id": {
      "description": "Session ID from dbx_open_session. When set, every statement runs on the session's pinned connection, preserving USE/SET, temporary tables and other session state across statements in the script and across calls.",
      "type": "string"
     },
     "sql": {
      "description": "SQL script containing one or more statements. Statements are split using the database-dialect-aware splitter, so semicolons inside strings, comments, and stored procedures are handled.",
      "type": "string"
     },
     "use_transaction": {
      "description": "When true and the script has more than one statement, all statements run inside one transaction (BEGIN \u2026 COMMIT) so the whole batch succeeds or rolls back, and the call returns a single merged result. For a single-statement script the option is ignored and the statement runs as normal auto-commit. Backends that cannot provide a rollbackable transaction reject this option. Cannot be combined with session_id or continue_on_error. Rejected for MySQL-family DDL scripts because DDL implicitly commits and cannot be rolled back. Default is auto-commit for each statement.",
      "type": "boolean"
     }
    },
    "required": [
     "sql"
    ],
    "type": "object"
   },
   "name": "dbx_execute_batch"
  },
  {
   "description": "Execute a SQL query on a database connection (default 100 rows; pass max_rows to return up to 1000 rows; larger values are clamped; MongoDB shell commands and multi-statement scripts routed through the batch executor stay at 100 rows). For backwards compatibility, multi-statement scripts and stored-procedure scripts are routed through the dialect-aware batch executor.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "cell_char_limit": {
      "description": "Maximum characters returned per string cell (default 200, max 4000). Increase only for an explicit long-value expansion.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
     },
     "cell_char_offset": {
      "description": "Start character offset for every string cell (default 0, max 1000000). Use the next offset reported by a truncated result to slide through a long value; narrow the query to the target row and column first.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
     },
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "database": {
      "description": "Database name",
      "type": "string"
     },
     "max_rows": {
      "description": "Maximum rows returned for this call (default 100, max 1000; larger values are clamped). SQL connections only - MongoDB shell commands always return at most 100 rows, and multi-statement scripts routed to the batch executor return at most 100 rows per statement.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
     },
     "session_id": {
      "description": "Session ID from dbx_open_session. When set, the query runs on the session's pinned connection, preserving USE/SET and other session state across calls.",
      "type": "string"
     },
     "sql": {
      "description": "SQL query to execute",
      "type": "string"
     }
    },
    "required": [
     "sql"
    ],
    "type": "object"
   },
   "name": "dbx_execute_query"
  },
  {
   "description": "Execute a Redis command on a Redis connection. Use the db argument to select a logical database instead of sending SELECT. Use SCAN rather than KEYS when enumerating keys.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "command": {
      "description": "Redis command to execute, for example GET mykey or INFO",
      "type": "string"
     },
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "db": {
      "description": "Redis logical database number. Use this argument instead of the SELECT command.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
     }
    },
    "required": [
     "command"
    ],
    "type": "object"
   },
   "name": "dbx_execute_redis_command"
  },
  {
   "description": "Get the full source text of a stored procedure or function",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "database": {
      "description": "Database name",
      "type": "string"
     },
     "name": {
      "description": "Routine name (as returned by dbx_list_routines)",
      "type": "string"
     },
     "object_type": {
      "description": "Routine type: PROCEDURE or FUNCTION",
      "type": "string"
     },
     "schema": {
      "description": "Schema name",
      "type": "string"
     },
     "signature": {
      "description": "Optional routine signature for databases that overload routine names (e.g. Oracle)",
      "type": "string"
     }
    },
    "required": [
     "name",
     "object_type"
    ],
    "type": "object"
   },
   "name": "dbx_get_routine_source"
  },
  {
   "description": "Get compact table and column context for writing SQL",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "database": {
      "type": "string"
     },
     "max_tables": {
      "description": "Maximum number of tables to include, from 1 to 20",
      "format": "uint",
      "minimum": 0,
      "type": "integer"
     },
     "schema": {
      "type": "string"
     },
     "tables": {
      "description": "Specific table names to include",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "type": "object"
   },
   "name": "dbx_get_schema_context"
  },
  {
   "description": "List database connections configured in DBX. Returns connection IDs, names, group paths, database types, endpoints, and selected databases.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object"
   },
   "name": "dbx_list_connections"
  },
  {
   "description": "List database names available through a DBX connection. When the connection has a database allowlist, returns only the databases allowed by DBX MCP settings; use this before listing tables if the connection has no default database.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "dbx_list_databases"
  },
  {
   "description": "List stored procedures and functions for a database schema",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "database": {
      "description": "Database name",
      "type": "string"
     },
     "routine_type": {
      "description": "Optional routine type filter: PROCEDURE or FUNCTION. Omit to list both. Unsupported databases return an empty list.",
      "type": "string"
     },
     "schema": {
      "description": "Schema name",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "dbx_list_routines"
  },
  {
   "description": "List tables and views for a database connection",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "database": {
      "description": "Database name",
      "type": "string"
     },
     "schema": {
      "description": "Schema name",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "dbx_list_tables"
  },
  {
   "description": "Open a stateful query session pinned to a single backend connection. Returns a session ID for dbx_execute_query: USE, SET CATALOG, session variables and temporary tables persist across calls within the session. Close with dbx_close_session when done; idle sessions expire after 30 minutes.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "database": {
      "description": "Database name",
      "type": "string"
     },
     "enable_transactions": {
      "default": false,
      "description": "Eagerly reserve one native MySQL connection for explicit transaction tools",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "dbx_open_session"
  },
  {
   "description": "Open a table in DBX desktop app. Requires DBX to be running.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "database": {
      "type": "string"
     },
     "schema": {
      "type": "string"
     },
     "table": {
      "type": "string"
     }
    },
    "required": [
     "table"
    ],
    "type": "object"
   },
   "name": "dbx_open_table"
  },
  {
   "description": "Read up to 100 Kafka messages without committing consumer offsets. Defaults to the latest 20 messages. Returns JSON with base64 payloads, UTF-8 previews, metadata, incomplete (partial broker read), and outputTruncated (256 KiB message output budget). Only Kafka is supported.",
   "inputSchema": {
    "$defs": {
     "PeekStartPosition": {
      "enum": [
       "latest",
       "earliest",
       "offset"
      ],
      "type": "string"
     }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "count": {
      "description": "Number of messages, 1 to 100 (default 20)",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
     },
     "offset": {
      "description": "Non-negative offset, required only for start_position=offset",
      "format": "int64",
      "type": "integer"
     },
     "partition": {
      "description": "Non-negative partition; omit to read across partitions",
      "format": "int32",
      "type": "integer"
     },
     "start_position": {
      "$ref": "#/$defs/PeekStartPosition",
      "default": "latest",
      "description": "Read latest messages (default), earliest messages, or from an offset"
     },
     "topic": {
      "description": "Kafka topic name",
      "type": "string"
     }
    },
    "required": [
     "topic"
    ],
    "type": "object"
   },
   "name": "dbx_peek_messages"
  },
  {
   "description": "Remove a database connection from DBX",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection when connection_id is not provided",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "dbx_remove_connection"
  },
  {
   "description": "Roll back the active transaction. Cleanup remains available after write-policy revocation.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "session_id": {
      "description": "Transaction-enabled session ID returned by dbx_open_session",
      "type": "string"
     }
    },
    "required": [
     "session_id"
    ],
    "type": "object"
   },
   "name": "dbx_rollback_transaction"
  },
  {
   "description": "Send a base64-encoded message to a Kafka, RocketMQ, or RabbitMQ topic/queue",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "connection_id": {
      "description": "Unique ID of the DBX connection",
      "type": "string"
     },
     "connection_name": {
      "description": "Name of the DBX connection",
      "type": "string"
     },
     "exchange": {
      "default": null,
      "description": "RabbitMQ exchange name",
      "type": "string"
     },
     "headers": {
      "additionalProperties": {
       "type": "string"
      },
      "default": {},
      "description": "Optional message headers",
      "type": "object"
     },
     "key": {
      "default": null,
      "description": "Optional message key used for partitioning",
      "type": "string"
     },
     "namespace": {
      "default": null,
      "description": "RabbitMQ virtual-host namespace",
      "type": "string"
     },
     "partition": {
      "default": null,
      "description": "Optional target partition",
      "format": "int32",
      "type": "integer"
     },
     "payload_base64": {
      "description": "Message payload encoded as standard base64",
      "type": "string"
     },
     "payload_text": {
      "default": null,
      "description": "Optional UTF-8 text preview of the payload",
      "type": "string"
     },
     "routing_key": {
      "default": null,
      "description": "RabbitMQ routing key",
      "type": "string"
     },
     "topic": {
      "description": "Message queue topic name",
      "type": "string"
     }
    },
    "required": [
     "topic",
     "payload_base64"
    ],
    "type": "object"
   },
   "name": "dbx_send_message"
  }
 ],
 "version": "0.4.92"
}