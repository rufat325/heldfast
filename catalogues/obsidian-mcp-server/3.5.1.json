{
 "args": [
  "run",
  "start:stdio"
 ],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "obsidian-mcp-server",
 "protocol": "2025-06-18",
 "published": "2026-09-04T09:50:27.004Z",
 "tools": [
  {
   "annotations": {
    "destructiveHint": true
   },
   "description": "Append content to a note. **Without `section`: appends to the end of the file, or creates the file if it does not exist (your content becomes the full file).** With `section`: appends to the end of that heading/block/frontmatter \u2014 use `obsidian_get_note` with `format: \"document-map\"` to discover available targets. A nested heading may be named either by its full `Parent::Child` path or by a bare leaf name that matches exactly one heading; a leaf shared by several headings is rejected with `ambiguous_section`. For block-reference targets, content is concatenated adjacent to the block line without inserting a separator \u2014 include a leading newline in `content` if you want one. Set `createTargetIfMissing` to bring the target section into existence rather than failing when it does not exist.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "content": {
      "description": "Body to append. Markdown unless `contentType` is `json`.",
      "type": "string"
     },
     "contentType": {
      "default": "markdown",
      "description": "Content body format. Use \"json\" for typed frontmatter values or block-targeted table rows. JSON values must be valid JSON literals \u2014 strings need quoting (`\"\\\"draft\\\"\"`, not `\"draft\"`), and numbers/booleans/arrays/objects pass through as-is.",
      "enum": [
       "markdown",
       "json"
      ],
      "type": "string"
     },
     "createTargetIfMissing": {
      "default": false,
      "description": "When `section` is provided, create the section if it does not already exist (otherwise the call fails when the section is missing).",
      "type": "boolean"
     },
     "section": {
      "description": "Optional sub-document target. When set, content is appended to that section instead of the file.",
      "properties": {
       "target": {
        "description": "Heading name \u2014 either the full path (`\"Parent::Child\"`) or a bare leaf name that matches exactly one heading \u2014 a block reference without the leading caret (e.g. \"2d9b4a\", not \"^2d9b4a\"), or a frontmatter field name.",
        "minLength": 1,
        "type": "string"
       },
       "type": {
        "description": "Heading by name, block by reference, or frontmatter field by key.",
        "enum": [
         "heading",
         "block",
         "frontmatter"
        ],
        "type": "string"
       }
      },
      "required": [
       "type",
       "target"
      ],
      "type": "object"
     },
     "target": {
      "description": "Where the note lives.",
      "oneOf": [
       {
        "description": "Address a note by its vault-relative path.",
        "properties": {
         "path": {
          "description": "Vault-relative path including extension, e.g. \"Projects/foo.md\".",
          "minLength": 1,
          "type": "string"
         },
         "type": {
          "const": "path",
          "description": "Address by vault-relative path.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "path"
        ],
        "type": "object"
       },
       {
        "description": "Address whichever file is currently active in the Obsidian UI.",
        "properties": {
         "type": {
          "const": "active",
          "description": "Address the file currently open in Obsidian.",
          "type": "string"
         }
        },
        "required": [
         "type"
        ],
        "type": "object"
       },
       {
        "description": "Address a periodic note (current or dated).",
        "properties": {
         "date": {
          "description": "ISO date YYYY-MM-DD. Omit for the current period.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "type": "string"
         },
         "period": {
          "description": "Periodic note granularity.",
          "enum": [
           "daily",
           "weekly",
           "monthly",
           "quarterly",
           "yearly"
          ],
          "type": "string"
         },
         "type": {
          "const": "periodic",
          "description": "Address a daily/weekly/monthly/etc. periodic note.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "period"
        ],
        "type": "object"
       }
      ]
     }
    },
    "required": [
     "target",
     "content"
    ],
    "type": "object"
   },
   "name": "obsidian_append_to_note",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "path",
       "sectionTargeted",
       "created",
       "previousSizeInBytes",
       "currentSizeInBytes"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "created": {
      "description": "True when the whole-file append created a new file. Always false for section appends \u2014 PATCH requires the file to exist. Best-effort under concurrent writers, racy between the existence check and the write.",
      "type": "boolean"
     },
     "currentSizeInBytes": {
      "description": "Byte size of the note after the append, read from the upstream after the operation completed. Compare against `previousSizeInBytes` and your own content length to detect unexpected upstream behavior (e.g. auto-newline injection, concurrent writers).",
      "type": "number"
     },
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `path_forbidden`: The target path is outside OBSIDIAN_WRITE_PATHS, or OBSIDIAN_READ_ONLY=true denies all writes. `note_missing`: Section append targets a path that does not resolve to an existing note (PATCH requires the file to exist). `no_active_file`: Target was `active` but no file is currently open in Obsidian. `periodic_not_found`: Target was `periodic` but no matching periodic note exists. `periodic_disabled`: Target was `periodic` but the requested period is not enabled in Obsidian's Periodic Notes plugin settings. `section_target_missing`: `section` was provided but the named heading/block/frontmatter field does not exist in the note. `ambiguous_section`: A bare heading leaf name matches more than one heading in the note, so the append target is undetermined. `content_preexists`: Section append where the supplied content already appears at the target \u2014 rejected to keep retries idempotent (the default for the section path). `path_is_directory`: The supplied path names a folder rather than a note file. `path_traversal`: The path contains a `.` or `..` segment, which is rejected to prevent vault escape. Other values are possible when a failure originates below the handler.",
          "examples": [
           "path_forbidden",
           "note_missing",
           "no_active_file",
           "periodic_not_found",
           "periodic_disabled",
           "section_target_missing",
           "ambiguous_section",
           "content_preexists",
           "path_is_directory",
           "path_traversal"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "path": {
      "description": "Resolved vault-relative path of the note.",
      "type": "string"
     },
     "previousSizeInBytes": {
      "description": "Byte size of the note before the append. Zero when `created` is true.",
      "type": "number"
     },
     "sectionTarget": {
      "description": "Section locator the append was applied to. Present when `section` was supplied. For headings this is the resolved locator \u2014 a bare leaf name is reported back as its full `Parent::Child` path.",
      "type": "string"
     },
     "sectionTargeted": {
      "description": "True when the append went to a heading/block/frontmatter section (PATCH); false for whole-file appends (POST).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "title": "Obsidian Append To Note"
  },
  {
   "annotations": {
    "destructiveHint": true
   },
   "description": "Permanently delete a note from the vault. Asks the user to confirm before deleting \u2014 the call is answered with a confirmation request and retried with the answer. Recovery requires the local trash in Obsidian \u2014 there is no API-level undo.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "target": {
      "description": "Which note to delete.",
      "oneOf": [
       {
        "description": "Address a note by its vault-relative path.",
        "properties": {
         "path": {
          "description": "Vault-relative path including extension, e.g. \"Projects/foo.md\".",
          "minLength": 1,
          "type": "string"
         },
         "type": {
          "const": "path",
          "description": "Address by vault-relative path.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "path"
        ],
        "type": "object"
       },
       {
        "description": "Address whichever file is currently active in the Obsidian UI.",
        "properties": {
         "type": {
          "const": "active",
          "description": "Address the file currently open in Obsidian.",
          "type": "string"
         }
        },
        "required": [
         "type"
        ],
        "type": "object"
       },
       {
        "description": "Address a periodic note (current or dated).",
        "properties": {
         "date": {
          "description": "ISO date YYYY-MM-DD. Omit for the current period.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "type": "string"
         },
         "period": {
          "description": "Periodic note granularity.",
          "enum": [
           "daily",
           "weekly",
           "monthly",
           "quarterly",
           "yearly"
          ],
          "type": "string"
         },
         "type": {
          "const": "periodic",
          "description": "Address a daily/weekly/monthly/etc. periodic note.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "period"
        ],
        "type": "object"
       }
      ]
     }
    },
    "required": [
     "target"
    ],
    "type": "object"
   },
   "name": "obsidian_delete_note",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "path",
       "deleted",
       "previousSizeInBytes",
       "currentSizeInBytes"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "currentSizeInBytes": {
      "description": "Always 0 after a successful delete \u2014 the file no longer exists.",
      "type": "number"
     },
     "deleted": {
      "description": "True when the file was removed.",
      "type": "boolean"
     },
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `path_forbidden`: The target path is outside OBSIDIAN_WRITE_PATHS, or OBSIDIAN_READ_ONLY=true denies all writes. `cancelled`: User declined, cancelled, or answered false to the confirmation request. `note_missing`: The vault path does not resolve to an existing note. `no_active_file`: Target was `active` but no file is currently open in Obsidian. `periodic_not_found`: Target was `periodic` but no matching periodic note exists. `periodic_disabled`: Target was `periodic` but the requested period is not enabled in Obsidian's Periodic Notes plugin settings. `path_is_directory`: The supplied path names a folder rather than a note file. Deleting a folder is not offered \u2014 the upstream removes it and everything inside it in one unrecoverable step. `path_traversal`: The path contains a `.` or `..` segment, which is rejected to prevent vault escape. Other values are possible when a failure originates below the handler.",
          "examples": [
           "path_forbidden",
           "cancelled",
           "note_missing",
           "no_active_file",
           "periodic_not_found",
           "periodic_disabled",
           "path_is_directory",
           "path_traversal"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "path": {
      "description": "Resolved vault-relative path of the deleted note.",
      "type": "string"
     },
     "previousSizeInBytes": {
      "description": "Byte size of the note immediately before deletion. Confirms the size of what was removed.",
      "type": "number"
     }
    },
    "type": "object"
   },
   "title": "Obsidian Delete Note"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "Read a note from the vault \u2014 by path, the active file, or a periodic note. Choose a `format` projection: raw body, full object, structural document map, or a single section.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "format": {
      "description": "Which projection to return. `content` \u2014 raw markdown body. `full` \u2014 content plus parsed frontmatter, tags, and file metadata. `document-map` \u2014 catalog of headings, block IDs, and frontmatter field names (use to discover patch targets). `section` \u2014 a single heading, block, or frontmatter section (requires `section`); heading sections include the full subtree under that heading and use `Parent::Child` syntax for nesting.",
      "enum": [
       "content",
       "full",
       "document-map",
       "section"
      ],
      "type": "string"
     },
     "includeLinks": {
      "default": false,
      "description": "When true with `format: \"full\"`, parses outgoing wiki and markdown link references from the note body. Skipped for other formats.",
      "type": "boolean"
     },
     "section": {
      "description": "Required when `format` is `\"section\"`. Identifies the heading/block/frontmatter to extract.",
      "properties": {
       "target": {
        "description": "Heading name \u2014 either the full path (`\"Parent::Child\"`) or a bare leaf name that matches exactly one heading \u2014 a block reference without the leading caret (e.g. \"2d9b4a\", not \"^2d9b4a\"), or a frontmatter field name.",
        "minLength": 1,
        "type": "string"
       },
       "type": {
        "description": "Heading by name, block by reference, or frontmatter field by key.",
        "enum": [
         "heading",
         "block",
         "frontmatter"
        ],
        "type": "string"
       }
      },
      "required": [
       "type",
       "target"
      ],
      "type": "object"
     },
     "target": {
      "description": "Where the note lives.",
      "oneOf": [
       {
        "description": "Address a note by its vault-relative path.",
        "properties": {
         "path": {
          "description": "Vault-relative path including extension, e.g. \"Projects/foo.md\".",
          "minLength": 1,
          "type": "string"
         },
         "type": {
          "const": "path",
          "description": "Address by vault-relative path.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "path"
        ],
        "type": "object"
       },
       {
        "description": "Address whichever file is currently active in the Obsidian UI.",
        "properties": {
         "type": {
          "const": "active",
          "description": "Address the file currently open in Obsidian.",
          "type": "string"
         }
        },
        "required": [
         "type"
        ],
        "type": "object"
       },
       {
        "description": "Address a periodic note (current or dated).",
        "properties": {
         "date": {
          "description": "ISO date YYYY-MM-DD. Omit for the current period.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "type": "string"
         },
         "period": {
          "description": "Periodic note granularity.",
          "enum": [
           "daily",
           "weekly",
           "monthly",
           "quarterly",
           "yearly"
          ],
          "type": "string"
         },
         "type": {
          "const": "periodic",
          "description": "Address a daily/weekly/monthly/etc. periodic note.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "period"
        ],
        "type": "object"
       }
      ]
     }
    },
    "required": [
     "format",
     "target"
    ],
    "type": "object"
   },
   "name": "obsidian_get_note",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "result"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `section_required`: `format` is \"section\" but no `section` locator was provided. `path_forbidden`: The target path is outside OBSIDIAN_READ_PATHS (and OBSIDIAN_WRITE_PATHS, since write paths imply read access). `note_missing`: The vault path does not resolve to an existing note. `ambiguous_path`: The parent directory contains multiple files whose names differ only in case (case-sensitive filesystems only). `no_active_file`: Target was `active` but no file is currently open in Obsidian. `periodic_not_found`: Target was `periodic` but no matching periodic note exists. `periodic_disabled`: Target was `periodic` but the requested period is not enabled in Obsidian's Periodic Notes plugin settings. `section_missing`: `format` was `\"section\"` and the named heading, block reference, or frontmatter field does not exist in the resolved note. `path_is_directory`: The supplied path names a folder rather than a note file. `path_traversal`: The path contains a `.` or `..` segment, which is rejected to prevent vault escape. Other values are possible when a failure originates below the handler.",
          "examples": [
           "section_required",
           "path_forbidden",
           "note_missing",
           "ambiguous_path",
           "no_active_file",
           "periodic_not_found",
           "periodic_disabled",
           "section_missing",
           "path_is_directory",
           "path_traversal"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "result": {
      "description": "Mode-discriminated projection of the requested note.",
      "oneOf": [
       {
        "additionalProperties": false,
        "description": "Content-only projection.",
        "properties": {
         "content": {
          "description": "Raw markdown body.",
          "type": "string"
         },
         "format": {
          "const": "content",
          "description": "Echoed format discriminator.",
          "type": "string"
         },
         "path": {
          "description": "Resolved vault-relative path of the note.",
          "type": "string"
         }
        },
        "required": [
         "format",
         "path",
         "content"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "description": "Full projection \u2014 content plus parsed metadata.",
        "properties": {
         "content": {
          "description": "Raw markdown body.",
          "type": "string"
         },
         "format": {
          "const": "full",
          "description": "Echoed format discriminator.",
          "type": "string"
         },
         "frontmatter": {
          "additionalProperties": {},
          "description": "Parsed YAML frontmatter. Values are strings, numbers, booleans, arrays, or nested objects.",
          "propertyNames": {
           "type": "string"
          },
          "type": "object"
         },
         "outgoingLinks": {
          "description": "Outgoing link references parsed from the note body. Present when `includeLinks` is true. Vault-internal references only \u2014 external URLs (http, mailto, etc.) are filtered out.",
          "items": {
           "additionalProperties": false,
           "description": "A single outgoing link reference.",
           "properties": {
            "target": {
             "description": "Link target as written \u2014 vault path, basename, or alias. No existence check.",
             "type": "string"
            },
            "type": {
             "description": "Source syntax.",
             "enum": [
              "wikilink",
              "markdown"
             ],
             "type": "string"
            }
           },
           "required": [
            "target",
            "type"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "path": {
          "description": "Resolved vault-relative path of the note.",
          "type": "string"
         },
         "stat": {
          "additionalProperties": false,
          "description": "File metadata.",
          "properties": {
           "ctime": {
            "description": "Created time, ms since epoch.",
            "type": "number"
           },
           "mtime": {
            "description": "Modified time, ms since epoch.",
            "type": "number"
           },
           "size": {
            "description": "File size in bytes.",
            "type": "number"
           }
          },
          "required": [
           "ctime",
           "mtime",
           "size"
          ],
          "type": "object"
         },
         "tags": {
          "description": "Tags from frontmatter and inline #tag syntax.",
          "items": {
           "type": "string"
          },
          "type": "array"
         }
        },
        "required": [
         "format",
         "path",
         "content",
         "frontmatter",
         "tags",
         "stat"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "description": "Document-map projection \u2014 catalog of patch targets.",
        "properties": {
         "blocks": {
          "description": "All block reference IDs.",
          "items": {
           "type": "string"
          },
          "type": "array"
         },
         "format": {
          "const": "document-map",
          "description": "Echoed format discriminator.",
          "type": "string"
         },
         "frontmatterFields": {
          "description": "All frontmatter field keys.",
          "items": {
           "type": "string"
          },
          "type": "array"
         },
         "headings": {
          "description": "All headings in document order.",
          "items": {
           "type": "string"
          },
          "type": "array"
         },
         "path": {
          "description": "Resolved vault-relative path of the note.",
          "type": "string"
         }
        },
        "required": [
         "format",
         "path",
         "headings",
         "blocks",
         "frontmatterFields"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "description": "Single-section projection.",
        "properties": {
         "format": {
          "const": "section",
          "description": "Echoed format discriminator.",
          "type": "string"
         },
         "path": {
          "description": "Resolved vault-relative path of the note.",
          "type": "string"
         },
         "section": {
          "additionalProperties": false,
          "description": "Echoed section locator.",
          "properties": {
           "target": {
            "description": "Heading name \u2014 either the full path (`\"Parent::Child\"`) or a bare leaf name that matches exactly one heading \u2014 a block reference without the leading caret (e.g. \"2d9b4a\", not \"^2d9b4a\"), or a frontmatter field name.",
            "minLength": 1,
            "type": "string"
           },
           "type": {
            "description": "Heading by name, block by reference, or frontmatter field by key.",
            "enum": [
             "heading",
             "block",
             "frontmatter"
            ],
            "type": "string"
           }
          },
          "required": [
           "type",
           "target"
          ],
          "type": "object"
         },
         "valueJson": {
          "description": "Section value as the JSON-typed frontmatter value (frontmatter sections only)."
         },
         "valueText": {
          "description": "Section value as raw markdown (heading/block sections).",
          "type": "string"
         }
        },
        "required": [
         "format",
         "path",
         "section"
        ],
        "type": "object"
       }
      ]
     }
    },
    "type": "object"
   },
   "title": "Obsidian Get Note"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "List notes and subdirectories at a vault path. Defaults to the vault root when `path` is omitted. Tune recursion with `depth`, or filter the walk with `extension` / `nameRegex`. Capped at 1000 entries per call \u2014 when reached, walking stops and `excluded` is set; narrow `path` or tighten filters to surface the rest.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "depth": {
      "default": 2,
      "description": "How many directory levels to walk. `1` = target directory only (no recursion); `2` = target plus its immediate children \u2014 a structural overview; bump higher to drill in. Prefer narrowing `path` to a subdirectory over a high `depth` on the vault root.",
      "maximum": 20,
      "minimum": 1,
      "type": "integer"
     },
     "extension": {
      "description": "Only include files matching this extension, with or without leading dot. Applies to files only \u2014 directories are returned regardless.",
      "type": "string"
     },
     "nameRegex": {
      "description": "Optional ECMAScript regex (no flags, \u2264256 chars, no nested quantifiers like `(a+)+`) applied to entry names. Matches both files and directories; directories that fail the regex are skipped without recursing into them.",
      "type": "string"
     },
     "path": {
      "description": "Vault-relative directory path. Omit for the vault root.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "obsidian_list_notes",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "path",
       "entries",
       "totals",
       "appliedFilters"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "appliedFilters": {
      "additionalProperties": false,
      "description": "Active filters and the recursion depth that produced this listing.",
      "properties": {
       "depth": {
        "description": "Recursion depth used to produce this listing.",
        "type": "number"
       },
       "extension": {
        "description": "Extension filter applied, normalized with leading dot.",
        "type": "string"
       },
       "nameRegex": {
        "description": "nameRegex filter applied to this listing.",
        "type": "string"
       }
      },
      "required": [
       "depth"
      ],
      "type": "object"
     },
     "entries": {
      "description": "Entries in DFS order \u2014 top-level first, then descendants.",
      "items": {
       "additionalProperties": false,
       "description": "A single entry in the listing.",
       "properties": {
        "path": {
         "description": "Vault-relative path of this entry.",
         "type": "string"
        },
        "truncated": {
         "description": "On directory entries: true when the depth limit prevented walking into this directory. Pass a deeper `depth` to expand.",
         "type": "boolean"
        },
        "type": {
         "description": "Whether this entry is a regular file or a subdirectory.",
         "enum": [
          "file",
          "directory"
         ],
         "type": "string"
        }
       },
       "required": [
        "path",
        "type"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `regex_invalid`: The supplied `nameRegex` is not a valid ECMAScript regex. `regex_unsafe`: The supplied `nameRegex` is well-formed but exceeds the 256-character limit or contains nested quantifiers known to cause catastrophic backtracking. `path_forbidden`: The supplied `path` is outside OBSIDIAN_READ_PATHS (root listings always pass; specific subdirectories must be readable). `note_missing`: The supplied `path` does not exist in the vault. Sub-directories that disappear mid-walk are silently skipped \u2014 only the root path surfaces this error. Other values are possible when a failure originates below the handler.",
          "examples": [
           "regex_invalid",
           "regex_unsafe",
           "path_forbidden",
           "note_missing"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "excluded": {
      "additionalProperties": false,
      "description": "Present when the walk was truncated by the global entry cap.",
      "properties": {
       "cap": {
        "description": "The cap value at which walking stopped.",
        "type": "number"
       },
       "hint": {
        "description": "Suggestion for narrowing the listing.",
        "type": "string"
       },
       "reason": {
        "const": "entry_cap",
        "description": "`entry_cap`: walk stopped because the per-call entry limit was reached.",
        "type": "string"
       }
      },
      "required": [
       "reason",
       "cap",
       "hint"
      ],
      "type": "object"
     },
     "notice": {
      "description": "Guidance when the walk was truncated by the entry cap, or the listed directory is empty.",
      "type": "string"
     },
     "path": {
      "description": "The directory listed (empty string for vault root).",
      "type": "string"
     },
     "totals": {
      "additionalProperties": false,
      "description": "Counts across the returned tree.",
      "properties": {
       "directories": {
        "description": "Number of directory entries.",
        "type": "number"
       },
       "entries": {
        "description": "Total entries returned across all walked depths.",
        "type": "number"
       },
       "files": {
        "description": "Number of file entries.",
        "type": "number"
       }
      },
      "required": [
       "entries",
       "files",
       "directories"
      ],
      "type": "object"
     }
    },
    "type": "object"
   },
   "title": "Obsidian List Notes"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "List the vault's tags with usage counts, ordered by count descending and capped at `limit` (default 200) \u2014 the response says so when it withheld any. Includes hierarchical parents: `work/tasks` contributes to both `work` and `work/tasks`. Narrow with `nameRegex` for a known prefix or `minCount` to drop the single-use tail. To find notes by tag, use `obsidian_search_notes` in jsonlogic mode (e.g. `{\"in\": [\"work\", {\"var\": \"tags\"}]}`).",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "limit": {
      "default": 200,
      "description": "Maximum tags to return, applied after `nameRegex` and `minCount` and after ordering by count descending, so the cap keeps the most-used. Max 10000 \u2014 high enough to return any real vault whole. When the cap bites, the response reports `truncated`, `shown`, and `cap`.",
      "maximum": 10000,
      "minimum": 1,
      "type": "integer"
     },
     "minCount": {
      "description": "Keep only tags used at least this many times. Omit (or pass 0) for no filter. On a vault with a long single-use tail this drops it in one argument.",
      "maximum": 9007199254740991,
      "minimum": 0,
      "type": "integer"
     },
     "nameRegex": {
      "description": "Optional ECMAScript regex (no flags, \u2264256 chars, no nested quantifiers like `(a+)+`) matched against the bare tag name (no leading `#`). Hierarchical tags like `work/tasks` are matched as the full string. Use character classes (`[Mm]cp`) for case-insensitivity.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "obsidian_list_tags",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "tags",
       "appliedFilters"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "appliedFilters": {
      "additionalProperties": false,
      "description": "Filters and cap that produced this listing.",
      "properties": {
       "limit": {
        "description": "Cap applied to this listing.",
        "type": "number"
       },
       "minCount": {
        "description": "minCount filter applied to this listing. Absent when none was supplied.",
        "type": "number"
       },
       "nameRegex": {
        "description": "nameRegex filter applied to this listing.",
        "type": "string"
       }
      },
      "required": [
       "limit"
      ],
      "type": "object"
     },
     "cap": {
      "description": "The `limit` that was applied.",
      "type": "number"
     },
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `regex_invalid`: The supplied `nameRegex` is not a valid ECMAScript regex. `regex_unsafe`: The supplied `nameRegex` is well-formed but exceeds the 256-character limit or contains nested quantifiers known to cause catastrophic backtracking. Other values are possible when a failure originates below the handler.",
          "examples": [
           "regex_invalid",
           "regex_unsafe"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "notice": {
      "description": "Guidance when no tags matched the applied filters, when the vault has no tags, or \u2014 since `ctx.enrich.truncated` routes its guidance here \u2014 when the cap withheld some.",
      "type": "string"
     },
     "shown": {
      "description": "Number of tags returned.",
      "type": "number"
     },
     "tags": {
      "description": "Matching tags ordered by `count` descending, ties broken by name ascending, truncated to `appliedFilters.limit`.",
      "items": {
       "additionalProperties": false,
       "description": "A tag with its usage count.",
       "properties": {
        "count": {
         "description": "Usage count across the vault.",
         "type": "number"
        },
        "name": {
         "description": "Tag name without the leading `#`.",
         "type": "string"
        }
       },
       "required": [
        "name",
        "count"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "truncated": {
      "description": "True when more tags matched the filters than `limit` allowed through.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "title": "Obsidian List Tags"
  },
  {
   "annotations": {
    "destructiveHint": true
   },
   "description": "Get, set, or delete a single frontmatter key on a note, atomically. `set` requires a JSON-typed `value` (string, number, boolean, array, or object).",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "key": {
      "description": "Frontmatter field name.",
      "minLength": 1,
      "type": "string"
     },
     "operation": {
      "description": "Operation to perform on `key`. `get` \u2014 read the current value. `set` \u2014 write `value`, creating the key if absent. `delete` \u2014 remove the key from frontmatter.",
      "enum": [
       "get",
       "set",
       "delete"
      ],
      "type": "string"
     },
     "target": {
      "description": "Where the note lives.",
      "oneOf": [
       {
        "description": "Address a note by its vault-relative path.",
        "properties": {
         "path": {
          "description": "Vault-relative path including extension, e.g. \"Projects/foo.md\".",
          "minLength": 1,
          "type": "string"
         },
         "type": {
          "const": "path",
          "description": "Address by vault-relative path.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "path"
        ],
        "type": "object"
       },
       {
        "description": "Address whichever file is currently active in the Obsidian UI.",
        "properties": {
         "type": {
          "const": "active",
          "description": "Address the file currently open in Obsidian.",
          "type": "string"
         }
        },
        "required": [
         "type"
        ],
        "type": "object"
       },
       {
        "description": "Address a periodic note (current or dated).",
        "properties": {
         "date": {
          "description": "ISO date YYYY-MM-DD. Omit for the current period.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "type": "string"
         },
         "period": {
          "description": "Periodic note granularity.",
          "enum": [
           "daily",
           "weekly",
           "monthly",
           "quarterly",
           "yearly"
          ],
          "type": "string"
         },
         "type": {
          "const": "periodic",
          "description": "Address a daily/weekly/monthly/etc. periodic note.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "period"
        ],
        "type": "object"
       }
      ]
     },
     "value": {
      "description": "Required when `operation` is `\"set\"`. JSON-typed value to write \u2014 strings, numbers, booleans, arrays, and objects all accepted."
     }
    },
    "required": [
     "operation",
     "target",
     "key"
    ],
    "type": "object"
   },
   "name": "obsidian_manage_frontmatter",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "result"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `path_forbidden`: `get` requires the path to be readable; `set`/`delete` require it to be inside OBSIDIAN_WRITE_PATHS, with OBSIDIAN_READ_ONLY=false. `value_required`: `operation` is \"set\" but no `value` was supplied. `note_missing`: The vault path does not resolve to an existing note. `no_active_file`: Target was `active` but no file is currently open in Obsidian. `periodic_not_found`: Target was `periodic` but no matching periodic note exists. `periodic_disabled`: Target was `periodic` but the requested period is not enabled in Obsidian's Periodic Notes plugin settings. `path_is_directory`: The supplied path names a folder rather than a note file. `path_traversal`: The path contains a `.` or `..` segment, which is rejected to prevent vault escape. Other values are possible when a failure originates below the handler.",
          "examples": [
           "path_forbidden",
           "value_required",
           "note_missing",
           "no_active_file",
           "periodic_not_found",
           "periodic_disabled",
           "path_is_directory",
           "path_traversal"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "result": {
      "description": "Operation-discriminated result payload.",
      "oneOf": [
       {
        "additionalProperties": false,
        "description": "Result for `get`.",
        "properties": {
         "exists": {
          "description": "True when the key was present in the frontmatter.",
          "type": "boolean"
         },
         "key": {
          "description": "Echoed frontmatter key.",
          "type": "string"
         },
         "operation": {
          "const": "get",
          "description": "Echoed operation.",
          "type": "string"
         },
         "path": {
          "description": "Resolved vault-relative path.",
          "type": "string"
         },
         "value": {
          "description": "Current value, or `null` when the key is absent."
         }
        },
        "required": [
         "operation",
         "path",
         "key",
         "exists",
         "value"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "description": "Result for `set`.",
        "properties": {
         "currentSizeInBytes": {
          "description": "Byte size of the note after the set.",
          "type": "number"
         },
         "frontmatter": {
          "additionalProperties": {},
          "description": "Full frontmatter after the change.",
          "propertyNames": {
           "type": "string"
          },
          "type": "object"
         },
         "key": {
          "description": "Echoed frontmatter key.",
          "type": "string"
         },
         "operation": {
          "const": "set",
          "description": "Echoed operation.",
          "type": "string"
         },
         "path": {
          "description": "Resolved vault-relative path.",
          "type": "string"
         },
         "previousSizeInBytes": {
          "description": "Byte size of the note before the set.",
          "type": "number"
         }
        },
        "required": [
         "operation",
         "path",
         "key",
         "frontmatter",
         "previousSizeInBytes",
         "currentSizeInBytes"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "description": "Result for `delete`.",
        "properties": {
         "currentSizeInBytes": {
          "description": "Byte size of the note after the delete. Equals `previousSizeInBytes` when the key was already absent and no write was issued.",
          "type": "number"
         },
         "frontmatter": {
          "additionalProperties": {},
          "description": "Full frontmatter after the change.",
          "propertyNames": {
           "type": "string"
          },
          "type": "object"
         },
         "key": {
          "description": "Echoed frontmatter key.",
          "type": "string"
         },
         "operation": {
          "const": "delete",
          "description": "Echoed operation.",
          "type": "string"
         },
         "path": {
          "description": "Resolved vault-relative path.",
          "type": "string"
         },
         "previousSizeInBytes": {
          "description": "Byte size of the note before the delete.",
          "type": "number"
         }
        },
        "required": [
         "operation",
         "path",
         "key",
         "frontmatter",
         "previousSizeInBytes",
         "currentSizeInBytes"
        ],
        "type": "object"
       }
      ]
     }
    },
    "type": "object"
   },
   "title": "Obsidian Manage Frontmatter"
  },
  {
   "annotations": {
    "destructiveHint": true
   },
   "description": "Add, remove, or list a note's tags. Defaults to the frontmatter `tags:` array \u2014 set `location` to `inline` or `both` to mutate the note body. `add` ensures the tag is present in the requested location(s); `remove` strips it; `both` reconciles across both representations. Inline `#tag` occurrences inside fenced code blocks are intentionally left alone, and inline-location additions append the new tag at end-of-file. `list` ignores the input `tags` array.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "location": {
      "default": "frontmatter",
      "description": "Where to apply the change. Defaults to `frontmatter` (the canonical Obsidian tag location, leaves the body untouched). `inline` mutates the note body \u2014 `add` appends `#tag` at end-of-file. `both` is opt-in reconciliation of frontmatter and inline tag locations.",
      "enum": [
       "frontmatter",
       "inline",
       "both"
      ],
      "type": "string"
     },
     "operation": {
      "description": "`add` and `remove` mutate the note; `list` reads the current tag set.",
      "enum": [
       "add",
       "remove",
       "list"
      ],
      "type": "string"
     },
     "tags": {
      "description": "Tags to add or remove. Omit the leading `#`. Required for add/remove.",
      "items": {
       "minLength": 1,
       "type": "string"
      },
      "type": "array"
     },
     "target": {
      "description": "Where the note lives.",
      "oneOf": [
       {
        "description": "Address a note by its vault-relative path.",
        "properties": {
         "path": {
          "description": "Vault-relative path including extension, e.g. \"Projects/foo.md\".",
          "minLength": 1,
          "type": "string"
         },
         "type": {
          "const": "path",
          "description": "Address by vault-relative path.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "path"
        ],
        "type": "object"
       },
       {
        "description": "Address whichever file is currently active in the Obsidian UI.",
        "properties": {
         "type": {
          "const": "active",
          "description": "Address the file currently open in Obsidian.",
          "type": "string"
         }
        },
        "required": [
         "type"
        ],
        "type": "object"
       },
       {
        "description": "Address a periodic note (current or dated).",
        "properties": {
         "date": {
          "description": "ISO date YYYY-MM-DD. Omit for the current period.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "type": "string"
         },
         "period": {
          "description": "Periodic note granularity.",
          "enum": [
           "daily",
           "weekly",
           "monthly",
           "quarterly",
           "yearly"
          ],
          "type": "string"
         },
         "type": {
          "const": "periodic",
          "description": "Address a daily/weekly/monthly/etc. periodic note.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "period"
        ],
        "type": "object"
       }
      ]
     }
    },
    "required": [
     "target",
     "operation"
    ],
    "type": "object"
   },
   "name": "obsidian_manage_tags",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "result"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `path_forbidden`: `list` requires the path to be readable; `add`/`remove` require it to be inside OBSIDIAN_WRITE_PATHS, with OBSIDIAN_READ_ONLY=false. `tags_required`: `operation` is \"add\" or \"remove\" but `tags` was empty or omitted. `note_missing`: The vault path does not resolve to an existing note. `no_active_file`: Target was `active` but no file is currently open in Obsidian. `periodic_not_found`: Target was `periodic` but no matching periodic note exists. `periodic_disabled`: Target was `periodic` but the requested period is not enabled in Obsidian's Periodic Notes plugin settings. `path_is_directory`: The supplied path names a folder rather than a note file. `path_traversal`: The path contains a `.` or `..` segment, which is rejected to prevent vault escape. Other values are possible when a failure originates below the handler.",
          "examples": [
           "path_forbidden",
           "tags_required",
           "note_missing",
           "no_active_file",
           "periodic_not_found",
           "periodic_disabled",
           "path_is_directory",
           "path_traversal"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "result": {
      "description": "Operation-discriminated result payload.",
      "oneOf": [
       {
        "additionalProperties": false,
        "description": "Result for `list`.",
        "properties": {
         "operation": {
          "const": "list",
          "description": "Echoed operation.",
          "type": "string"
         },
         "path": {
          "description": "Resolved vault-relative path.",
          "type": "string"
         },
         "tags": {
          "additionalProperties": false,
          "description": "Tags split by location plus the deduplicated union.",
          "properties": {
           "all": {
            "description": "Deduplicated union of frontmatter and inline tags.",
            "items": {
             "type": "string"
            },
            "type": "array"
           },
           "frontmatter": {
            "description": "Tags from frontmatter `tags:` array.",
            "items": {
             "type": "string"
            },
            "type": "array"
           },
           "inline": {
            "description": "Tags found in inline `#tag` syntax.",
            "items": {
             "type": "string"
            },
            "type": "array"
           }
          },
          "required": [
           "frontmatter",
           "inline",
           "all"
          ],
          "type": "object"
         }
        },
        "required": [
         "operation",
         "path",
         "tags"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "description": "Result for `add`.",
        "properties": {
         "applied": {
          "description": "Tags actually changed by this call.",
          "items": {
           "type": "string"
          },
          "type": "array"
         },
         "currentSizeInBytes": {
          "description": "Byte size of the note after the add. Equals `previousSizeInBytes` when no tags were applied.",
          "type": "number"
         },
         "operation": {
          "const": "add",
          "description": "Echoed operation.",
          "type": "string"
         },
         "path": {
          "description": "Resolved vault-relative path.",
          "type": "string"
         },
         "previousSizeInBytes": {
          "description": "Byte size of the note before the add.",
          "type": "number"
         },
         "skipped": {
          "description": "Tags already present at the targeted location(s).",
          "items": {
           "type": "string"
          },
          "type": "array"
         },
         "tags": {
          "description": "All tags on the note after the change.",
          "items": {
           "type": "string"
          },
          "type": "array"
         }
        },
        "required": [
         "operation",
         "path",
         "applied",
         "skipped",
         "tags",
         "previousSizeInBytes",
         "currentSizeInBytes"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "description": "Result for `remove`.",
        "properties": {
         "applied": {
          "description": "Tags actually changed by this call.",
          "items": {
           "type": "string"
          },
          "type": "array"
         },
         "currentSizeInBytes": {
          "description": "Byte size of the note after the remove. Equals `previousSizeInBytes` when no tags were applied.",
          "type": "number"
         },
         "operation": {
          "const": "remove",
          "description": "Echoed operation.",
          "type": "string"
         },
         "path": {
          "description": "Resolved vault-relative path.",
          "type": "string"
         },
         "previousSizeInBytes": {
          "description": "Byte size of the note before the remove.",
          "type": "number"
         },
         "skipped": {
          "description": "Tags absent from the targeted location(s).",
          "items": {
           "type": "string"
          },
          "type": "array"
         },
         "tags": {
          "description": "All tags on the note after the change.",
          "items": {
           "type": "string"
          },
          "type": "array"
         }
        },
        "required": [
         "operation",
         "path",
         "applied",
         "skipped",
         "tags",
         "previousSizeInBytes",
         "currentSizeInBytes"
        ],
        "type": "object"
       }
      ]
     }
    },
    "type": "object"
   },
   "title": "Obsidian Manage Tags"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true
   },
   "description": "Open a file in the Obsidian app UI. By default fails when the path does not exist; the `failIfMissing` flag controls the open-or-create behavior. Opening an existing file needs read access; opening a missing one creates it, so that case needs write access to the path.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "failIfMissing": {
      "default": true,
      "description": "When true (default), fails if the file does not exist. When false, allows Obsidian to create the file on open \u2014 which requires write access to the path and is rejected in read-only mode.",
      "type": "boolean"
     },
     "newLeaf": {
      "default": false,
      "description": "Open in a new leaf (split pane) instead of the active one.",
      "type": "boolean"
     },
     "path": {
      "description": "Vault-relative path of the file to open.",
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "path"
    ],
    "type": "object"
   },
   "name": "obsidian_open_in_ui",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "path",
       "opened",
       "createdIfMissing"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "createdIfMissing": {
      "description": "True when the file did not exist before the call and was created by Obsidian.",
      "type": "boolean"
     },
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `path_forbidden`: The path is outside OBSIDIAN_READ_PATHS, or \u2014 when the file does not exist and `failIfMissing: false` would have Obsidian create it \u2014 outside OBSIDIAN_WRITE_PATHS or blocked by OBSIDIAN_READ_ONLY=true. `note_missing`: `failIfMissing: true` (default) and the path does not exist in the vault. Pass `failIfMissing: false` to allow Obsidian to create the file on open. `ambiguous_path`: The parent directory contains multiple files whose names differ only in case (case-sensitive filesystems only). `path_is_directory`: The supplied path names a folder rather than a file. `path_traversal`: The path contains a `.` or `..` segment, which is rejected to prevent vault escape. Other values are possible when a failure originates below the handler.",
          "examples": [
           "path_forbidden",
           "note_missing",
           "ambiguous_path",
           "path_is_directory",
           "path_traversal"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "opened": {
      "description": "True when the open call succeeded.",
      "type": "boolean"
     },
     "path": {
      "description": "Resolved vault-relative path that was opened.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "title": "Obsidian Open In Ui"
  },
  {
   "annotations": {
    "destructiveHint": true
   },
   "description": "Edit a heading, block reference, or frontmatter field in place \u2014 append to, prepend to, or replace the target's body. Use `obsidian_get_note` with `format: \"document-map\"` to discover available targets first. A nested heading may be named either by its full `Parent::Child` path or by a bare leaf name that matches exactly one heading; a leaf shared by several headings is rejected with `ambiguous_section`.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "content": {
      "description": "Body to insert/replace. Markdown unless `contentType` is `json`.",
      "type": "string"
     },
     "contentType": {
      "default": "markdown",
      "description": "Content body format. Use \"json\" for typed frontmatter values or block-targeted table rows. JSON values must be valid JSON literals \u2014 strings need quoting (`\"\\\"draft\\\"\"`, not `\"draft\"`), and numbers/booleans/arrays/objects pass through as-is.",
      "enum": [
       "markdown",
       "json"
      ],
      "type": "string"
     },
     "operation": {
      "description": "How to apply `content` relative to the targeted section. `append` \u2014 at the end of the target's body (for headings, before the next sibling/parent heading; for frontmatter array fields, as a new array item). `prepend` \u2014 at the start. `replace` \u2014 swaps the target's body.",
      "enum": [
       "append",
       "prepend",
       "replace"
      ],
      "type": "string"
     },
     "patchOptions": {
      "description": "Optional flags: createTargetIfMissing, applyIfContentPreexists, trimTargetWhitespace.",
      "properties": {
       "applyIfContentPreexists": {
        "default": false,
        "description": "When false (default), the patch is rejected if the supplied content already appears in the target \u2014 idempotent against retries. Set to true to force-apply even when it would duplicate. Replace operations are never rejected.",
        "type": "boolean"
       },
       "createTargetIfMissing": {
        "default": false,
        "description": "Create the target heading/block/frontmatter field if it does not exist.",
        "type": "boolean"
       },
       "trimTargetWhitespace": {
        "default": false,
        "description": "Trim whitespace from the target section before applying the operation.",
        "type": "boolean"
       }
      },
      "type": "object"
     },
     "section": {
      "description": "Which heading/block/frontmatter field to edit.",
      "properties": {
       "target": {
        "description": "Heading name \u2014 either the full path (`\"Parent::Child\"`) or a bare leaf name that matches exactly one heading \u2014 a block reference without the leading caret (e.g. \"2d9b4a\", not \"^2d9b4a\"), or a frontmatter field name.",
        "minLength": 1,
        "type": "string"
       },
       "type": {
        "description": "Heading by name, block by reference, or frontmatter field by key.",
        "enum": [
         "heading",
         "block",
         "frontmatter"
        ],
        "type": "string"
       }
      },
      "required": [
       "type",
       "target"
      ],
      "type": "object"
     },
     "target": {
      "description": "Where the note lives.",
      "oneOf": [
       {
        "description": "Address a note by its vault-relative path.",
        "properties": {
         "path": {
          "description": "Vault-relative path including extension, e.g. \"Projects/foo.md\".",
          "minLength": 1,
          "type": "string"
         },
         "type": {
          "const": "path",
          "description": "Address by vault-relative path.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "path"
        ],
        "type": "object"
       },
       {
        "description": "Address whichever file is currently active in the Obsidian UI.",
        "properties": {
         "type": {
          "const": "active",
          "description": "Address the file currently open in Obsidian.",
          "type": "string"
         }
        },
        "required": [
         "type"
        ],
        "type": "object"
       },
       {
        "description": "Address a periodic note (current or dated).",
        "properties": {
         "date": {
          "description": "ISO date YYYY-MM-DD. Omit for the current period.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "type": "string"
         },
         "period": {
          "description": "Periodic note granularity.",
          "enum": [
           "daily",
           "weekly",
           "monthly",
           "quarterly",
           "yearly"
          ],
          "type": "string"
         },
         "type": {
          "const": "periodic",
          "description": "Address a daily/weekly/monthly/etc. periodic note.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "period"
        ],
        "type": "object"
       }
      ]
     }
    },
    "required": [
     "target",
     "section",
     "operation",
     "content"
    ],
    "type": "object"
   },
   "name": "obsidian_patch_note",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "path",
       "section",
       "operation",
       "previousSizeInBytes",
       "currentSizeInBytes"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "currentSizeInBytes": {
      "description": "Byte size of the note after the patch, read from the upstream after the operation completed.",
      "type": "number"
     },
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `path_forbidden`: The target path is outside OBSIDIAN_WRITE_PATHS, or OBSIDIAN_READ_ONLY=true denies all writes. `note_missing`: The vault path does not resolve to an existing note. `no_active_file`: Target was `active` but no file is currently open in Obsidian. `periodic_not_found`: Target was `periodic` but no matching periodic note exists. `periodic_disabled`: Target was `periodic` but the requested period is not enabled in Obsidian's Periodic Notes plugin settings. `section_target_missing`: The named heading/block/frontmatter field does not exist in the note. Use `obsidian_get_note` with `format: \"document-map\"` to discover available targets. `ambiguous_section`: A bare heading leaf name matches more than one heading in the note, so the write target is undetermined. `content_preexists`: The supplied content already appears at the target \u2014 the patch was rejected to keep retries idempotent (the default). `path_is_directory`: The supplied path names a folder rather than a note file. `path_traversal`: The path contains a `.` or `..` segment, which is rejected to prevent vault escape. Other values are possible when a failure originates below the handler.",
          "examples": [
           "path_forbidden",
           "note_missing",
           "no_active_file",
           "periodic_not_found",
           "periodic_disabled",
           "section_target_missing",
           "ambiguous_section",
           "content_preexists",
           "path_is_directory",
           "path_traversal"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "operation": {
      "description": "Echoed operation that was applied.",
      "enum": [
       "append",
       "prepend",
       "replace"
      ],
      "type": "string"
     },
     "path": {
      "description": "Resolved vault-relative path of the note.",
      "type": "string"
     },
     "previousSizeInBytes": {
      "description": "Byte size of the note before the patch was applied.",
      "type": "number"
     },
     "section": {
      "additionalProperties": false,
      "description": "Section locator the patch was applied to. For headings this is the resolved locator \u2014 a bare leaf name is reported back as its full `Parent::Child` path.",
      "properties": {
       "target": {
        "description": "Heading name \u2014 either the full path (`\"Parent::Child\"`) or a bare leaf name that matches exactly one heading \u2014 a block reference without the leading caret (e.g. \"2d9b4a\", not \"^2d9b4a\"), or a frontmatter field name.",
        "minLength": 1,
        "type": "string"
       },
       "type": {
        "description": "Heading by name, block by reference, or frontmatter field by key.",
        "enum": [
         "heading",
         "block",
         "frontmatter"
        ],
        "type": "string"
       }
      },
      "required": [
       "type",
       "target"
      ],
      "type": "object"
     }
    },
    "type": "object"
   },
   "title": "Obsidian Patch Note"
  },
  {
   "annotations": {
    "destructiveHint": true
   },
   "description": "Search and replace inside a single note, literally or by regex. Replacements run in array order, each over the previous one's output, and cover the note body only unless `scope` says otherwise \u2014 the YAML frontmatter block is left byte-identical by default, because a prose edit that lands in a scalar can silently invalidate the whole block. Use for edits that don't fit `obsidian_patch_note`'s structural targets \u2014 e.g., body-wide find-and-replace. Prefer `obsidian_manage_frontmatter` for typed edits to a single property.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "replacements": {
      "description": "Replacements to apply in array order over the evolving content.",
      "items": {
       "description": "A single search/replace operation.",
       "properties": {
        "caseSensitive": {
         "default": true,
         "description": "When false, match case-insensitively.",
         "type": "boolean"
        },
        "flexibleWhitespace": {
         "default": false,
         "description": "Treat any run of whitespace in `search` as matching any whitespace in the body. Literal mode only \u2014 has no effect when `useRegex: true` (express it directly with `\\s+`).",
         "type": "boolean"
        },
        "replace": {
         "description": "Replacement text. Empty string deletes matches.",
         "type": "string"
        },
        "replaceAll": {
         "default": true,
         "description": "When false, only the first match is replaced.",
         "type": "boolean"
        },
        "search": {
         "description": "Substring or regex pattern to match.",
         "minLength": 1,
         "type": "string"
        },
        "useRegex": {
         "default": false,
         "description": "Treat `search` as an ECMAScript regex pattern (\u22641024 chars, no nested quantifiers like `(a+)+` \u2014 catastrophic-backtracking guard).",
         "type": "boolean"
        },
        "wholeWord": {
         "default": false,
         "description": "Match only at word boundaries. Applies in both literal and regex modes \u2014 the search pattern is wrapped with `\\b\u2026\\b`.",
         "type": "boolean"
        }
       },
       "required": [
        "search",
        "replace"
       ],
       "type": "object"
      },
      "minItems": 1,
      "type": "array"
     },
     "scope": {
      "default": "body",
      "description": "Which part of the note the replacements run over. `body` leaves the YAML frontmatter block byte-identical. `frontmatter` and `both` also rewrite the YAML between the `---` fences \u2014 never the fences themselves \u2014 and re-parse it afterwards, failing the whole call and writing nothing if the result no longer parses. That check catches YAML that breaks, not YAML that still parses while meaning something else: a replacement that renames a key or strips a scalar's quotes passes it.",
      "enum": [
       "body",
       "frontmatter",
       "both"
      ],
      "type": "string"
     },
     "target": {
      "description": "Where the note lives.",
      "oneOf": [
       {
        "description": "Address a note by its vault-relative path.",
        "properties": {
         "path": {
          "description": "Vault-relative path including extension, e.g. \"Projects/foo.md\".",
          "minLength": 1,
          "type": "string"
         },
         "type": {
          "const": "path",
          "description": "Address by vault-relative path.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "path"
        ],
        "type": "object"
       },
       {
        "description": "Address whichever file is currently active in the Obsidian UI.",
        "properties": {
         "type": {
          "const": "active",
          "description": "Address the file currently open in Obsidian.",
          "type": "string"
         }
        },
        "required": [
         "type"
        ],
        "type": "object"
       },
       {
        "description": "Address a periodic note (current or dated).",
        "properties": {
         "date": {
          "description": "ISO date YYYY-MM-DD. Omit for the current period.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "type": "string"
         },
         "period": {
          "description": "Periodic note granularity.",
          "enum": [
           "daily",
           "weekly",
           "monthly",
           "quarterly",
           "yearly"
          ],
          "type": "string"
         },
         "type": {
          "const": "periodic",
          "description": "Address a daily/weekly/monthly/etc. periodic note.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "period"
        ],
        "type": "object"
       }
      ]
     }
    },
    "required": [
     "target",
     "replacements"
    ],
    "type": "object"
   },
   "name": "obsidian_replace_in_note",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "path",
       "totalReplacements",
       "perReplacement",
       "previousSizeInBytes",
       "currentSizeInBytes"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "currentSizeInBytes": {
      "description": "Byte size of the note after replacements were applied. Equals `previousSizeInBytes` when no matches were found and no write was issued.",
      "type": "number"
     },
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `path_forbidden`: The target path is outside OBSIDIAN_WRITE_PATHS, or OBSIDIAN_READ_ONLY=true denies all writes. (The pre-read also requires the path to be readable.) `regex_invalid`: A `useRegex: true` replacement supplied a `search` pattern that is not a valid ECMAScript regex. `regex_unsafe`: A `useRegex: true` replacement supplied a `search` pattern that is well-formed but exceeds the 1024-character limit or contains nested quantifiers known to cause catastrophic backtracking against the note body. `frontmatter_invalid`: A `scope` of \"frontmatter\" or \"both\" produced YAML that no longer parses as a mapping of properties. Nothing is written \u2014 the note keeps its original bytes. The check reads the rewritten YAML only, so a replacement that renames a key or changes a scalar's type while still parsing is not caught by it. `note_missing`: The vault path does not resolve to an existing note. `no_active_file`: Target was `active` but no file is currently open in Obsidian. `periodic_not_found`: Target was `periodic` but no matching periodic note exists. `periodic_disabled`: Target was `periodic` but the requested period is not enabled in Obsidian's Periodic Notes plugin settings. `path_is_directory`: The supplied path names a folder rather than a note file. `path_traversal`: The path contains a `.` or `..` segment, which is rejected to prevent vault escape. Other values are possible when a failure originates below the handler.",
          "examples": [
           "path_forbidden",
           "regex_invalid",
           "regex_unsafe",
           "frontmatter_invalid",
           "note_missing",
           "no_active_file",
           "periodic_not_found",
           "periodic_disabled",
           "path_is_directory",
           "path_traversal"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "path": {
      "description": "Resolved vault-relative path of the note.",
      "type": "string"
     },
     "perReplacement": {
      "description": "Per-entry counts in the order replacements were applied.",
      "items": {
       "additionalProperties": false,
       "description": "Counts for one replacement entry.",
       "properties": {
        "bodyCount": {
         "description": "Matches replaced in the note body.",
         "type": "number"
        },
        "count": {
         "description": "Number of matches replaced for this entry, across every scope it ran in.",
         "type": "number"
        },
        "frontmatterCount": {
         "description": "Matches replaced inside the YAML frontmatter block. Always 0 under the default `body` scope.",
         "type": "number"
        },
        "search": {
         "description": "The search term/pattern that ran.",
         "type": "string"
        }
       },
       "required": [
        "search",
        "count",
        "bodyCount",
        "frontmatterCount"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "previousSizeInBytes": {
      "description": "Byte size of the note before replacements were applied.",
      "type": "number"
     },
     "totalReplacements": {
      "description": "Total number of substitutions applied across all replacement entries.",
      "type": "number"
     }
    },
    "type": "object"
   },
   "title": "Obsidian Replace In Note"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "Search the vault by text substring or JSONLogic predicate. Pick the mode that matches the query shape. Results paginate via opaque cursors: omit `cursor` for the first page, then pass `nextCursor` from the prior response. Text-mode hits additionally clip per file at `maxMatchesPerHit`.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "contextLength": {
      "default": 100,
      "description": "Characters of context on each side of the match (text mode only). Sizes the rendered text as well as the structured payload, so a wide window multiplies the response across every match on the page.",
      "exclusiveMinimum": 0,
      "maximum": 9007199254740991,
      "type": "integer"
     },
     "cursor": {
      "description": "Opaque cursor from a prior response. Omit for the first page. Page size is server-determined; do not assume a fixed value.",
      "type": "string"
     },
     "logic": {
      "additionalProperties": {},
      "description": "JSONLogic tree. Required for `jsonlogic` mode; ignored in `text` and `omnisearch` modes (use `query` instead \u2014 this field must be an object, so passing a string here is rejected). `glob` and `regexp` take `[PATTERN, VALUE]` \u2014 pattern first: `{\"glob\": [\"Projects/*.md\", {\"var\": \"path\"}]}`. Backlinks (\"what links here\") have no dedicated tool or upstream endpoint but are expressible this way: `{\"regexp\": [\"\\\\[\\\\[Target Note(\\\\||#|\\\\]\\\\])\", {\"var\": \"content\"}]}` finds every note whose body wikilinks `Target Note`, in plain, aliased, or section form. `obsidian_get_note` with `includeLinks: true` covers the outgoing direction.",
      "propertyNames": {
       "type": "string"
      },
      "type": "object"
     },
     "maxMatchesPerHit": {
      "default": 10,
      "description": "Cap on match contexts returned per file in text mode. When clipped, the hit carries `truncated: true` and `totalMatches`.",
      "exclusiveMinimum": 0,
      "maximum": 9007199254740991,
      "type": "integer"
     },
     "mode": {
      "description": "Which search algorithm to run. `text` matches a substring case-insensitively across filenames and note bodies, returning surrounding context windows. `jsonlogic` evaluates a JSONLogic tree against each note, with `var` paths into `path`, `content`, `frontmatter.<key>`, `tags`, and `stat.{ctime,mtime,size}`, plus `glob` and `regexp` operators \u2014 both take their arguments as `[PATTERN, VALUE]`, so the pattern comes first and the `{\"var\": ...}` reference second.",
      "enum": [
       "text",
       "jsonlogic"
      ],
      "type": "string"
     },
     "pathPrefix": {
      "description": "Filter returned filenames by prefix (text mode only, applied after matching \u2014 does not narrow the search itself).",
      "type": "string"
     },
     "query": {
      "description": "The query string. Required for `text` and `omnisearch` modes; ignored in `jsonlogic` mode (use `logic` instead \u2014 this field must be a string, so passing a JSONLogic tree here is rejected).",
      "type": "string"
     }
    },
    "required": [
     "mode"
    ],
    "type": "object"
   },
   "name": "obsidian_search_notes",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "result"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "effectiveQuery": {
      "description": "The query string as submitted (text and omnisearch modes only).",
      "type": "string"
     },
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `path_prefix_invalid_mode`: `pathPrefix` was provided in a non-text mode (only `text` supports prefix filtering). `query_required`: `query` is missing for `text` or `omnisearch` mode (required for both). `context_length_too_large`: The Local REST API exhausted its string capacity building one context window per match, so the whole text search failed. `logic_required`: `logic` is missing for `jsonlogic` mode. `logic_invalid`: The Local REST API rejected the JSONLogic tree \u2014 an unrecognized operator, or an operand it could not evaluate (an uncompilable `regexp` pattern). A mis-arity operator is not rejected: it evaluates to false and the search returns zero hits. `omnisearch_unreachable`: Omnisearch was reachable at startup but is now unreachable (Obsidian quit, plugin disabled, or mobile session). Other values are possible when a failure originates below the handler.",
          "examples": [
           "path_prefix_invalid_mode",
           "query_required",
           "context_length_too_large",
           "logic_required",
           "logic_invalid",
           "omnisearch_unreachable"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "notice": {
      "description": "Recovery guidance when the search returned no hits.",
      "type": "string"
     },
     "result": {
      "description": "Mode-discriminated search payload.",
      "oneOf": [
       {
        "additionalProperties": false,
        "description": "Text-search results.",
        "properties": {
         "hits": {
          "description": "Matching files with per-match context.",
          "items": {
           "additionalProperties": false,
           "description": "A file with one or more text-search matches.",
           "properties": {
            "filename": {
             "description": "Vault-relative path of the matching note.",
             "type": "string"
            },
            "matches": {
             "description": "Per-match context windows. Capped per file by `maxMatchesPerHit`.",
             "items": {
              "additionalProperties": false,
              "description": "A single match within a file.",
              "properties": {
               "context": {
                "description": "Surrounding text around the match.",
                "type": "string"
               },
               "match": {
                "additionalProperties": false,
                "description": "Where the match sits, on two origins: `start`/`end` index the matched subject, `contextStart`/`contextEnd` index the accompanying `context` window.",
                "properties": {
                 "contextEnd": {
                  "description": "Match end offset within `context`.",
                  "type": "number"
                 },
                 "contextStart": {
                  "description": "Match start offset within `context`. `context.slice(contextStart, contextEnd)` is the matched text.",
                  "type": "number"
                 },
                 "end": {
                  "description": "Match end offset within the same subject `start` indexes.",
                  "type": "number"
                 },
                 "start": {
                  "description": "Match start offset within the subject upstream matched \u2014 the note body in the usual case, or the note basename (extension stripped) when the filename itself matched, in which case `context` is that basename. This is not an offset into `context`; use `contextStart` for that. Note-body values index the same string `obsidian_get_note` returns.",
                  "type": "number"
                 }
                },
                "required": [
                 "start",
                 "end",
                 "contextStart",
                 "contextEnd"
                ],
                "type": "object"
               }
              },
              "required": [
               "context",
               "match"
              ],
              "type": "object"
             },
             "type": "array"
            },
            "totalMatches": {
             "description": "Total matches in this file. Present only when `matches` was clipped to `maxMatchesPerHit`.",
             "type": "number"
            },
            "truncated": {
             "description": "True when `matches` was clipped to `maxMatchesPerHit`. Use `obsidian_get_note` to read the full file when more context is needed.",
             "type": "boolean"
            }
           },
           "required": [
            "filename",
            "matches"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "mode": {
          "const": "text",
          "description": "Echoed mode.",
          "type": "string"
         },
         "nextCursor": {
          "description": "Opaque cursor for the next page. Omitted on the last page (do not treat absent as null).",
          "type": "string"
         },
         "totalCount": {
          "description": "Total post-path-policy hit count across all pages, before pagination.",
          "type": "number"
         }
        },
        "required": [
         "mode",
         "hits",
         "totalCount"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "description": "JSONLogic results.",
        "properties": {
         "hits": {
          "description": "Matching files with the JSONLogic result per file.",
          "items": {
           "additionalProperties": false,
           "description": "A file with a structured (Dataview/JSONLogic) result value.",
           "properties": {
            "filename": {
             "description": "Vault-relative path of the matching note.",
             "type": "string"
            },
            "result": {
             "description": "The query result for this file \u2014 shape determined by the query."
            }
           },
           "required": [
            "filename",
            "result"
           ],
           "type": "object"
          },
          "type": "array"
         },
         "mode": {
          "const": "jsonlogic",
          "description": "Echoed mode.",
          "type": "string"
         },
         "nextCursor": {
          "description": "Opaque cursor for the next page. Omitted on the last page (do not treat absent as null).",
          "type": "string"
         },
         "totalCount": {
          "description": "Total post-path-policy hit count across all pages, before pagination.",
          "type": "number"
         }
        },
        "required": [
         "mode",
         "hits",
         "totalCount"
        ],
        "type": "object"
       }
      ]
     }
    },
    "type": "object"
   },
   "title": "Obsidian Search Notes"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": true
   },
   "description": "Create or overwrite a note. With `section`, replaces just that heading/block/frontmatter section in place \u2014 use `obsidian_get_note` with `format: \"document-map\"` to discover available targets. A nested heading may be named either by its full `Parent::Child` path or by a bare leaf name that matches exactly one heading; a leaf shared by several headings is rejected with `ambiguous_section`. Whole-file writes fail with `file_exists` against an existing note unless `overwrite: true` \u2014 for in-place edits, prefer `obsidian_patch_note` (sections), `obsidian_append_to_note` (append), or `obsidian_replace_in_note` (find-and-replace). For heading sections, `content` is the new body; the heading line is preserved automatically.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
     "content": {
      "description": "Body to write. For heading sections, the new section body \u2014 do not repeat the heading line (it stays in place). Markdown unless `contentType` is `json`.",
      "type": "string"
     },
     "contentType": {
      "default": "markdown",
      "description": "Content body format. Use \"json\" for typed frontmatter values or block-targeted table rows. JSON values must be valid JSON literals \u2014 strings need quoting (`\"\\\"draft\\\"\"`, not `\"draft\"`), and numbers/booleans/arrays/objects pass through as-is.",
      "enum": [
       "markdown",
       "json"
      ],
      "type": "string"
     },
     "overwrite": {
      "default": false,
      "description": "Whole-file mode only (ignored when `section` is set). When `false` (default), the call fails with `file_exists` if the target note already exists \u2014 read it first and use `obsidian_patch_note` / `obsidian_append_to_note` / `obsidian_replace_in_note` for in-place edits, or retry with `overwrite: true` for a deliberate full replacement.",
      "type": "boolean"
     },
     "section": {
      "description": "Optional sub-document target. When set, only this section is replaced; rest of the note is untouched.",
      "properties": {
       "target": {
        "description": "Heading name \u2014 either the full path (`\"Parent::Child\"`) or a bare leaf name that matches exactly one heading \u2014 a block reference without the leading caret (e.g. \"2d9b4a\", not \"^2d9b4a\"), or a frontmatter field name.",
        "minLength": 1,
        "type": "string"
       },
       "type": {
        "description": "Heading by name, block by reference, or frontmatter field by key.",
        "enum": [
         "heading",
         "block",
         "frontmatter"
        ],
        "type": "string"
       }
      },
      "required": [
       "type",
       "target"
      ],
      "type": "object"
     },
     "target": {
      "description": "Where the note lives.",
      "oneOf": [
       {
        "description": "Address a note by its vault-relative path.",
        "properties": {
         "path": {
          "description": "Vault-relative path including extension, e.g. \"Projects/foo.md\".",
          "minLength": 1,
          "type": "string"
         },
         "type": {
          "const": "path",
          "description": "Address by vault-relative path.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "path"
        ],
        "type": "object"
       },
       {
        "description": "Address whichever file is currently active in the Obsidian UI.",
        "properties": {
         "type": {
          "const": "active",
          "description": "Address the file currently open in Obsidian.",
          "type": "string"
         }
        },
        "required": [
         "type"
        ],
        "type": "object"
       },
       {
        "description": "Address a periodic note (current or dated).",
        "properties": {
         "date": {
          "description": "ISO date YYYY-MM-DD. Omit for the current period.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "type": "string"
         },
         "period": {
          "description": "Periodic note granularity.",
          "enum": [
           "daily",
           "weekly",
           "monthly",
           "quarterly",
           "yearly"
          ],
          "type": "string"
         },
         "type": {
          "const": "periodic",
          "description": "Address a daily/weekly/monthly/etc. periodic note.",
          "type": "string"
         }
        },
        "required": [
         "type",
         "period"
        ],
        "type": "object"
       }
      ]
     }
    },
    "required": [
     "target",
     "content"
    ],
    "type": "object"
   },
   "name": "obsidian_write_note",
   "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "anyOf": [
     {
      "not": {
       "required": [
        "error"
       ]
      },
      "required": [
       "path",
       "sectionTargeted",
       "created",
       "previousSizeInBytes",
       "currentSizeInBytes"
      ]
     },
     {
      "required": [
       "error"
      ]
     }
    ],
    "properties": {
     "created": {
      "description": "True when the write created a new file. False when it replaced an existing one or targeted a section.",
      "type": "boolean"
     },
     "currentSizeInBytes": {
      "description": "Byte size of the note after the write, read from the upstream after the operation completed. Compare against `previousSizeInBytes` and your own content length to detect unexpected upstream behavior.",
      "type": "number"
     },
     "error": {
      "additionalProperties": {},
      "description": "Present when the call failed. Absent on success.",
      "properties": {
       "code": {
        "description": "JSON-RPC error code for this failure.",
        "maximum": 9007199254740991,
        "minimum": -9007199254740991,
        "type": "integer"
       },
       "data": {
        "additionalProperties": {},
        "properties": {
         "reason": {
          "description": "Machine-readable failure mode. Declared by this tool: `file_exists`: Whole-file write was attempted against an existing note and `overwrite` was not set to `true`. `path_forbidden`: The target path is outside OBSIDIAN_WRITE_PATHS, or OBSIDIAN_READ_ONLY=true denies all writes. `note_missing`: Section replace targets a path that does not resolve to an existing note (PATCH requires the file to exist). `no_active_file`: Target was `active` but no file is currently open in Obsidian. `periodic_not_found`: Target was `periodic` but no matching periodic note exists. `periodic_disabled`: Target was `periodic` but the requested period is not enabled in Obsidian's Periodic Notes plugin settings. `section_target_missing`: `section` was provided but the named heading/block/frontmatter field does not exist in the note. `ambiguous_section`: A bare heading leaf name matches more than one heading in the note, so the replacement target is undetermined. `path_is_directory`: The supplied path names a folder rather than a note file. `path_traversal`: The path contains a `.` or `..` segment, which is rejected to prevent vault escape. Other values are possible when a failure originates below the handler.",
          "examples": [
           "file_exists",
           "path_forbidden",
           "note_missing",
           "no_active_file",
           "periodic_not_found",
           "periodic_disabled",
           "section_target_missing",
           "ambiguous_section",
           "path_is_directory",
           "path_traversal"
          ],
          "type": "string"
         },
         "recovery": {
          "additionalProperties": {},
          "description": "Actionable next step for the caller.",
          "properties": {
           "hint": {
            "type": "string"
           }
          },
          "required": [
           "hint"
          ],
          "type": "object"
         },
         "retryable": {
          "description": "Whether retrying may succeed.",
          "type": "boolean"
         }
        },
        "type": "object"
       },
       "message": {
        "description": "Human-readable description of what went wrong.",
        "type": "string"
       }
      },
      "required": [
       "code",
       "message"
      ],
      "type": "object"
     },
     "path": {
      "description": "Resolved vault-relative path of the note that was written.",
      "type": "string"
     },
     "previousSizeInBytes": {
      "description": "Byte size of the note before the write. Zero when `created` is true. On overwrites this is the destructive blast radius.",
      "type": "number"
     },
     "sectionTarget": {
      "description": "Section locator the replacement was applied to. Present when `section` was supplied. For headings this is the resolved locator \u2014 a bare leaf name is reported back as its full `Parent::Child` path.",
      "type": "string"
     },
     "sectionTargeted": {
      "description": "True when only a section was replaced; false for full-file writes.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "title": "Obsidian Write Note"
  }
 ],
 "version": "3.5.1"
}