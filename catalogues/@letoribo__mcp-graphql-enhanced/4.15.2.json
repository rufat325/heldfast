{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "@letoribo/mcp-graphql-enhanced",
 "protocol": "2025-06-18",
 "published": "2026-08-29T01:33:43.110Z",
 "tools": [
  {
   "description": "Retrieve GraphQL schema details or system manifest. READ-ONLY: Non-destructive metadata discovery. Usage: 1. If 'typeNames' is provided: Returns the full SDL (Schema Definition Language) for the requested types, including fields and relations. 2. If 'typeNames' is omitted: Returns a Federated Manifest\u2014a high-level summary of connected nodes, their capabilities, and available domain entities (not the full schema). Use this to navigate the federated graph topology before executing queries.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "endpoint": {
      "description": "Optional target GraphQL HTTP/HTTPS URL to dynamically switch endpoint before execution.",
      "type": "string"
     },
     "headers": {
      "description": "JSON stringified object of extra HTTP headers for the introspection request (e.g. '{\"Authorization\": \"Bearer token\"}').",
      "type": "string"
     },
     "typeDepth": {
      "description": "Depth of nested fields to retrieve (default: 2)",
      "type": "number"
     },
     "typeNames": {
      "description": "List of specific GraphQL type names to introspect. If provided, returns the detailed SDL definitions for these types. If omitted, returns a system-wide Federated Manifest overview.",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "type": "object"
   },
   "name": "introspect-schema"
  },
  {
   "description": "Execute GraphQL operations (queries and mutations) against the federated system. WARNING: This tool performs remote operations. 'Mutation' operations will modify persistent state; execute these only when a state change is intended. Prerequisites: Verify schema structure using 'introspect-schema' before executing complex queries. Security: Inherits environment-based authentication. Returns: A JSON object containing the execution result ('data') or a list of 'errors' in case of failure.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "endpoint": {
      "description": "Optional target GraphQL HTTP/HTTPS URL to dynamically switch endpoint before execution.",
      "type": "string"
     },
     "headers": {
      "description": "JSON stringified object of extra HTTP headers for the request.",
      "type": "string"
     },
     "query": {
      "description": "The GraphQL query or mutation string. Example: 'query { guilds { id name } }'.",
      "type": "string"
     },
     "variables": {
      "description": "JSON stringified object of variables. Example: '{\"id\": \"123\"}'.",
      "type": "string"
     }
    },
    "required": [
     "query"
    ],
    "type": "object"
   },
   "name": "query-graphql"
  }
 ],
 "version": "4.15.2"
}