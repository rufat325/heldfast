{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "mbox-mcp",
 "protocol": "2025-06-18",
 "published": "2026-08-14T23:06:57.298Z",
 "tools": [
  {
   "description": "Fully parse one message by its id (from search_messages): headers, decoded text body (truncated if huge), and attachment names/types/sizes. Attachment contents are never returned.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "id": {
      "description": "Message id from search results",
      "minimum": 0,
      "type": "integer"
     },
     "path": {
      "description": "Path to an .mbox file (e.g. a Google Takeout export) or a directory of .eml files",
      "type": "string"
     }
    },
    "required": [
     "path",
     "id"
    ],
    "type": "object"
   },
   "name": "get_message",
   "title": "Read a message"
  },
  {
   "description": "Index an .mbox file (Google Takeout, Thunderbird) or a directory of .eml files \u2014 entirely locally, nothing leaves the machine. Returns message count, date range, and top senders. Large archives are indexed once and cached.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "path": {
      "description": "Path to an .mbox file (e.g. a Google Takeout export) or a directory of .eml files",
      "type": "string"
     }
    },
    "required": [
     "path"
    ],
    "type": "object"
   },
   "name": "open_archive",
   "title": "Open a local email archive"
  },
  {
   "description": "Search indexed envelopes (from/to/subject/date). `query` matches sender and subject. `body_query` additionally full-parses candidate messages to search body text \u2014 it is capped, so narrow with sender/date filters first; the result reports how many candidates were scanned.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "after": {
      "description": "Only messages on/after this date, YYYY-MM-DD",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "type": "string"
     },
     "before": {
      "description": "Only messages on/before this date, YYYY-MM-DD",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "type": "string"
     },
     "body_query": {
      "description": "Substring searched in message bodies (bounded scan)",
      "type": "string"
     },
     "from": {
      "description": "Substring of the sender",
      "type": "string"
     },
     "limit": {
      "default": 20,
      "description": "Max results",
      "maximum": 100,
      "minimum": 1,
      "type": "integer"
     },
     "path": {
      "description": "Path to an .mbox file (e.g. a Google Takeout export) or a directory of .eml files",
      "type": "string"
     },
     "query": {
      "description": "Keyword matched against sender and subject",
      "type": "string"
     },
     "subject": {
      "description": "Substring of the subject",
      "type": "string"
     }
    },
    "required": [
     "path"
    ],
    "type": "object"
   },
   "name": "search_messages",
   "title": "Search the archive"
  }
 ],
 "version": "0.1.0"
}