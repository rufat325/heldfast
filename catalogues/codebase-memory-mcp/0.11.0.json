{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "codebase-memory-mcp",
 "protocol": "2025-06-18",
 "published": "2026-09-15T23:40:58.264Z",
 "tools": [
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Best-effort exact-path/scope coverage and freshness, paged separately. full diagnostics adds raw detail. Clean is not proof of completeness.",
   "inputSchema": {
    "properties": {
     "diagnostics": {
      "default": "none",
      "enum": [
       "none",
       "full"
      ],
      "type": "string"
     },
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "path_limit": {
      "default": 20,
      "maximum": 128,
      "minimum": 1,
      "type": "integer"
     },
     "path_offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "paths": {
      "items": {
       "type": "string"
      },
      "maxItems": 128,
      "type": "array"
     },
     "project": {
      "type": "string"
     },
     "scope_limit": {
      "default": 20,
      "maximum": 1000,
      "minimum": 1,
      "type": "integer"
     },
     "scope_offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "scopes": {
      "items": {
       "type": "string"
      },
      "maxItems": 32,
      "type": "array"
     }
    },
    "required": [
     "project"
    ],
    "type": "object"
   },
   "name": "check_index_coverage"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Compare two indexed snapshots: deterministic target-only additions and base-only removals of stable node/edge identities; each set capped by limit and a 512 KiB budget with exact totals and truncation reasons.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "base_project": {
      "minLength": 1,
      "type": "string"
     },
     "limit": {
      "default": 200,
      "maximum": 1000,
      "minimum": 1,
      "type": "integer"
     },
     "scan_limit": {
      "default": 2000000,
      "maximum": 10000000,
      "minimum": 1,
      "type": "integer"
     },
     "target_project": {
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "base_project",
     "target_project"
    ],
    "type": "object"
   },
   "name": "compare_graphs"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Delete a project from the index",
   "inputSchema": {
    "properties": {
     "project": {
      "type": "string"
     }
    },
    "required": [
     "project"
    ],
    "type": "object"
   },
   "name": "delete_project"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Map a Git diff to files and impact. Page with snapshot cursors.",
   "inputSchema": {
    "properties": {
     "base_branch": {
      "default": "main",
      "type": "string"
     },
     "changed_cursor": {
      "type": "string"
     },
     "changed_limit": {
      "default": 20,
      "maximum": 5000,
      "minimum": 0,
      "type": "integer"
     },
     "changed_offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "depth": {
      "default": 2,
      "type": "integer"
     },
     "direction": {
      "default": "inbound",
      "description": "inbound=callers; outbound=dependencies; both=union.",
      "enum": [
       "inbound",
       "outbound",
       "both"
      ],
      "type": "string"
     },
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "impact_cursor": {
      "type": "string"
     },
     "impact_offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "limit": {
      "default": 200,
      "maximum": 5000,
      "type": "integer"
     },
     "max_output_tokens": {
      "default": 3200,
      "description": "Sizing hint; hard ceiling is 4 UTF-8 bytes/token.",
      "maximum": 1000000,
      "minimum": 128,
      "type": "integer"
     },
     "module_cursor": {
      "type": "string"
     },
     "module_limit": {
      "default": 20,
      "maximum": 256,
      "minimum": 0,
      "type": "integer"
     },
     "module_offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "project": {
      "type": "string"
     },
     "scope": {
      "default": "impact",
      "enum": [
       "files",
       "impact"
      ],
      "type": "string"
     },
     "since": {
      "type": "string"
     }
    },
    "required": [
     "project"
    ],
    "type": "object"
   },
   "name": "detect_changes"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Compact counts, languages, packages, entry points. Request structure, dependencies, routes, hotspots, boundaries, layers, clusters, cycles, or file_tree; path scopes a directory.",
   "inputSchema": {
    "properties": {
     "aspects": {
      "description": "all=everything; overview=compact except file_tree; omitted=languages/packages/entry_points; cycles is opt-in.",
      "items": {
       "enum": [
        "all",
        "overview",
        "structure",
        "dependencies",
        "routes",
        "languages",
        "packages",
        "entry_points",
        "hotspots",
        "boundaries",
        "layers",
        "file_tree",
        "clusters",
        "cycles"
       ],
       "type": "string"
      },
      "type": "array"
     },
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "path": {
      "description": "Directory prefix (for example apps/hoa).",
      "type": "string"
     },
     "project": {
      "type": "string"
     }
    },
    "required": [
     "project"
    ],
    "type": "object"
   },
   "name": "get_architecture"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Read a search_graph symbol. auto bounds source and outlines large containers; full restores up to 500 lines. Source/outline pages continue; coverage_note marks gaps.",
   "inputSchema": {
    "properties": {
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "include_neighbors": {
      "default": false,
      "type": "boolean"
     },
     "max_lines": {
      "maximum": 500,
      "minimum": 1,
      "type": "integer"
     },
     "max_output_tokens": {
      "description": "auto/outline sizing hint (default 2500), hard ceiling 4 UTF-8 bytes/token. Lines stay whole; explicit full is uncapped unless set.",
      "maximum": 1000000,
      "minimum": 128,
      "type": "integer"
     },
     "member_limit": {
      "default": 50,
      "maximum": 500,
      "minimum": 1,
      "type": "integer"
     },
     "member_offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "project": {
      "type": "string"
     },
     "qualified_name": {
      "description": "search_graph qn, or short name.",
      "type": "string"
     },
     "source_mode": {
      "default": "auto",
      "description": "auto outlines 200+ line containers; full returns source; outline lists members.",
      "enum": [
       "auto",
       "full",
       "outline"
      ],
      "type": "string"
     },
     "start_line": {
      "minimum": 1,
      "type": "integer"
     }
    },
    "required": [
     "qualified_name",
     "project"
    ],
    "type": "object"
   },
   "name": "get_code_snippet"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Declaration outline of one exact repository-relative file: optional exact label filter, source order, exact total/offset/limit paging; file/folder/container nodes excluded.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "file_path": {
      "description": "Exact repository-relative file path",
      "type": "string"
     },
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "labels": {
      "description": "Optional exact node-label filter",
      "items": {
       "type": "string"
      },
      "maxItems": 16,
      "type": "array"
     },
     "limit": {
      "default": 100,
      "maximum": 200,
      "minimum": 1,
      "type": "integer"
     },
     "offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "project": {
      "type": "string"
     }
    },
    "required": [
     "project",
     "file_path"
    ],
    "type": "object"
   },
   "name": "get_file_outline"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Get node-label and edge-type counts. diagnostics=full also lists queryable properties.",
   "inputSchema": {
    "properties": {
     "diagnostics": {
      "default": "none",
      "enum": [
       "none",
       "full"
      ],
      "type": "string"
     },
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "limit": {
      "default": 50,
      "maximum": 500,
      "minimum": 1,
      "type": "integer"
     },
     "offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "project": {
      "type": "string"
     }
    },
    "required": [
     "project"
    ],
    "type": "object"
   },
   "name": "get_graph_schema"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Index a repository. full/moderate add semantics; fast omits them; cross-repo-intelligence links services. Reports coverage gaps.",
   "inputSchema": {
    "properties": {
     "mode": {
      "default": "full",
      "description": "full: all+semantic; moderate: filtered+semantic; fast: filtered only; cross-repo-intelligence: link services.",
      "enum": [
       "full",
       "moderate",
       "fast",
       "cross-repo-intelligence"
      ],
      "type": "string"
     },
     "name": {
      "description": "Name override; Non-ASCII bytes are encoded; unsafe characters normalized.",
      "type": "string"
     },
     "persistence": {
      "default": false,
      "description": "Write .codebase-memory/graph.db.zst.",
      "type": "boolean"
     },
     "repo_path": {
      "description": "Repository path",
      "type": "string"
     },
     "target_projects": {
      "description": "Cross-repo targets; [\"*\"] means all.",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "repo_path"
    ],
    "type": "object"
   },
   "name": "index_repository"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Project readiness, counts, root, and coverage gaps. diagnostics adds coverage rows; verbose adds Git paths. Best-effort only; verify cited paths with check_index_coverage.",
   "inputSchema": {
    "properties": {
     "diagnostics": {
      "default": "none",
      "description": "Coverage rows: counts, five samples, or up to 500.",
      "enum": [
       "none",
       "summary",
       "full"
      ],
      "type": "string"
     },
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "project": {
      "type": "string"
     },
     "verbose": {
      "default": false,
      "description": "Add worktree/shadow Git paths for index-location debugging.",
      "type": "boolean"
     }
    },
    "required": [
     "project"
    ],
    "type": "object"
   },
   "name": "index_status"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Validate and count traces; graph edge creation is not implemented",
   "inputSchema": {
    "properties": {
     "project": {
      "type": "string"
     },
     "traces": {
      "items": {
       "additionalProperties": false,
       "properties": {
        "callee": {
         "type": "string"
        },
        "caller": {
         "type": "string"
        },
        "count": {
         "type": "integer"
        }
       },
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "traces",
     "project"
    ],
    "type": "object"
   },
   "name": "ingest_traces"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "List projects with stable paging. Identity is lean; stats adds graph sizes.",
   "inputSchema": {
    "properties": {
     "detail": {
      "default": "identity",
      "description": "stats adds node/edge/database-size counts.",
      "enum": [
       "identity",
       "stats"
      ],
      "type": "string"
     },
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "include_details": {
      "default": false,
      "description": "Alias for detail=stats: include branch, node/edge counts and database size. Slower.",
      "type": "boolean"
     },
     "limit": {
      "default": 50,
      "maximum": 500,
      "minimum": 1,
      "type": "integer"
     },
     "metadata_only": {
      "default": false,
      "description": "Compatibility: omit counts, size, and branch.",
      "type": "boolean"
     },
     "offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     }
    },
    "type": "object"
   },
   "name": "list_projects"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Outline an ADR by default; get reads it; update replaces it",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "content": {
      "description": "Whole document for update",
      "type": "string"
     },
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "mode": {
      "default": "outline",
      "description": "outline pages headings; get reads; update replaces the whole document; set_sections rewrites only the named sections and leaves every other byte untouched (an identical repeated write is byte-identical, so retrying is safe); sections is legacy.",
      "enum": [
       "outline",
       "get",
       "update",
       "set_sections",
       "sections"
      ],
      "type": "string"
     },
     "project": {
      "type": "string"
     },
     "section_limit": {
      "default": 50,
      "maximum": 500,
      "minimum": 1,
      "type": "integer"
     },
     "section_offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "section_updates": {
      "additionalProperties": {
       "type": "string"
      },
      "description": "set_sections: section name -> new body. Any heading name works; names match exactly, including case.",
      "type": "object"
     }
    },
    "required": [
     "project"
    ],
    "type": "object"
   },
   "name": "manage_adr"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Read-only Cypher for multi-hop, aggregation, complexity, or cross-service analysis. Default: 200 visible rows with exact/lower-bound totals and truncation; continue safely with next_cursor. graph=missed is a file tree of flagged coverage gaps; absence is not proof of completeness. Use get_graph_schema(diagnostics=full) for properties.",
   "inputSchema": {
    "properties": {
     "cursor": {
      "description": "Snapshot continuation; keep query/project/graph. Format, budget, and max_rows may change.",
      "type": "string"
     },
     "format": {
      "default": "tree",
      "description": "tree may use local @N+suffix prefix refs; json uses direct strings.",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "graph": {
      "default": "code",
      "description": "code graph (default) or missed coverage-gap file tree.",
      "enum": [
       "code",
       "missed"
      ],
      "type": "string"
     },
     "max_output_tokens": {
      "description": "Sizing hint; hard ceiling is 4 UTF-8 bytes/token. Rows stay whole.",
      "maximum": 1000000,
      "minimum": 128,
      "type": "integer"
     },
     "max_rows": {
      "default": 200,
      "description": "Visible rows (default 200; max 99998); 0 uses the legacy maximum. Evaluation is unchanged.",
      "minimum": 0,
      "type": "integer"
     },
     "offset": {
      "default": 0,
      "description": "Live compatibility paging; cannot be combined with cursor.",
      "minimum": 0,
      "type": "integer"
     },
     "project": {
      "type": "string"
     },
     "query": {
      "description": "Cypher query",
      "type": "string"
     }
    },
    "required": [
     "query",
     "project"
    ],
    "type": "object"
   },
   "name": "query_graph"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Graph-ranked text search: compact symbols, full bounded source, or file paths.",
   "inputSchema": {
    "properties": {
     "context": {
      "type": "integer"
     },
     "debug": {
      "default": false,
      "description": "Add scope_ms/scan_ms/enrich_ms phase timings.",
      "type": "boolean"
     },
     "directory_limit": {
      "default": 20,
      "maximum": 64,
      "minimum": 0,
      "type": "integer"
     },
     "directory_offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "file_pattern": {
      "type": "string"
     },
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "limit": {
      "default": 10,
      "maximum": 500,
      "minimum": 1,
      "type": "integer"
     },
     "match_limit": {
      "default": 8,
      "maximum": 500,
      "minimum": 1,
      "type": "integer"
     },
     "max_output_tokens": {
      "maximum": 1000000,
      "minimum": 128,
      "type": "integer"
     },
     "mode": {
      "default": "compact",
      "enum": [
       "compact",
       "full",
       "files"
      ],
      "type": "string"
     },
     "path_filter": {
      "type": "string"
     },
     "pattern": {
      "type": "string"
     },
     "project": {
      "type": "string"
     },
     "raw_content_offset": {
      "description": "Raw-line byte offset; omit for match-centered preview.",
      "minimum": 0,
      "type": "integer"
     },
     "raw_limit": {
      "default": 5,
      "maximum": 100,
      "minimum": 0,
      "type": "integer"
     },
     "raw_offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "regex": {
      "default": false,
      "type": "boolean"
     },
     "result_limit": {
      "default": 10,
      "maximum": 500,
      "minimum": 1,
      "type": "integer"
     },
     "result_offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "source_max_lines": {
      "default": 20,
      "maximum": 200,
      "minimum": 1,
      "type": "integer"
     }
    },
    "required": [
     "pattern",
     "project"
    ],
    "type": "object"
   },
   "name": "search_code"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Find symbols via BM25 query, regex name/qn filters, or semantic_query. Rows keep qn/file/lines and in/out over CALLS/USAGE/CALL_REFERENCE/INHERITS/IMPLEMENTS.",
   "inputSchema": {
    "properties": {
     "detail": {
      "default": "default",
      "enum": [
       "ids",
       "default"
      ],
      "type": "string"
     },
     "exclude_entry_points": {
      "type": "boolean"
     },
     "fields": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "file_pattern": {
      "type": "string"
     },
     "format": {
      "default": "tree",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "include_connected": {
      "type": "boolean"
     },
     "label": {
      "type": "string"
     },
     "limit": {
      "default": 50,
      "maximum": 500,
      "minimum": 1,
      "type": "integer"
     },
     "max_degree": {
      "type": "integer"
     },
     "max_output_tokens": {
      "default": 3200,
      "maximum": 1000000,
      "minimum": 128,
      "type": "integer"
     },
     "min_degree": {
      "type": "integer"
     },
     "name_pattern": {
      "type": "string"
     },
     "offset": {
      "default": 0,
      "minimum": 0,
      "type": "integer"
     },
     "project": {
      "type": "string"
     },
     "qn_pattern": {
      "type": "string"
     },
     "query": {
      "type": "string"
     },
     "relationship": {
      "type": "string"
     },
     "semantic_limit": {
      "default": 50,
      "maximum": 500,
      "minimum": 0,
      "type": "integer"
     },
     "semantic_offset": {
      "default": 0,
      "maximum": 99998,
      "minimum": 0,
      "type": "integer"
     },
     "semantic_query": {
      "description": "Not with query.",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "project"
    ],
    "type": "object"
   },
   "name": "search_graph"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Trace callers/callees, data flow, or cross-service paths. Defaults exclude tests and resolver evidence. Rows keep qn/hop with explicit totals, relations, and continuations.",
   "inputSchema": {
    "properties": {
     "cursor": {
      "description": "Pass next/next_cursor with traversal args unchanged; budget may increase.",
      "type": "string"
     },
     "depth": {
      "default": 3,
      "maximum": 15,
      "minimum": 1,
      "type": "integer"
     },
     "direction": {
      "default": "both",
      "enum": [
       "inbound",
       "outbound",
       "both"
      ],
      "type": "string"
     },
     "edge_types": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "format": {
      "default": "tree",
      "description": "tree chooses smaller complete direct/grouped output; json uses stable grouped tables.",
      "enum": [
       "tree",
       "json"
      ],
      "type": "string"
     },
     "function_name": {
      "type": "string"
     },
     "include_evidence": {
      "default": false,
      "description": "Add resolver class and confidence.",
      "type": "boolean"
     },
     "include_tests": {
      "default": false,
      "type": "boolean"
     },
     "limit": {
      "default": 100,
      "description": "Rows/page; gte flags the 5000-node engine ceiling.",
      "maximum": 5000,
      "minimum": 1,
      "type": "integer"
     },
     "max_output_tokens": {
      "default": 3200,
      "description": "Sizing hint; hard ceiling is 4 UTF-8 bytes/token. Evidence/args yield before nearest graph rows.",
      "maximum": 1000000,
      "minimum": 128,
      "type": "integer"
     },
     "mode": {
      "default": "calls",
      "description": "calls, argument-aware data_flow, or service edges.",
      "enum": [
       "calls",
       "data_flow",
       "cross_service"
      ],
      "type": "string"
     },
     "parameter_name": {
      "description": "data_flow parameter filter.",
      "type": "string"
     },
     "project": {
      "type": "string"
     },
     "risk_labels": {
      "default": false,
      "description": "Add hop-risk labels.",
      "type": "boolean"
     }
    },
    "required": [
     "function_name",
     "project"
    ],
    "type": "object"
   },
   "name": "trace_path"
  }
 ],
 "version": "0.11.0"
}