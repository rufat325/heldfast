{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "@togglhq/mcp",
 "protocol": "2025-06-18",
 "published": "2026-09-22T14:58:52.139Z",
 "tools": [
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus alerts with the following actions:\n\n- **create**: Create Alerts. (data: payload*{config*, rule_type*, project_id, recipient_group_ids, recipient_roles, recipient_user_ids})\n- **delete**: Delete Alerts. (data: alert_id*)\n- **get**: Get Alerts. (data: alert_id*)\n- **list**: List Alerts. (data: rule_type, project_id, scope)\n- **partial-update**: Partial Update Alerts. (data: alert_id*, payload*{config, recipient_group_ids, recipient_roles, recipient_user_ids, rule_type})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create, delete, get, list, partial-update",
      "enum": [
       "create",
       "delete",
       "get",
       "list",
       "partial-update"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "alerts",
   "title": "Alerts"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus attachments with the following actions:\n\n- **get-workspace-attachment-storage**: Get Workspace Attachment Storage Attachments. (data: none)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "const": "get-workspace-attachment-storage"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "properties": {},
      "required": [],
      "type": "object"
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "attachments",
   "title": "Attachments"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus audit-log with the following actions:\n\n- **get-organization**: Get Organization Audit Log. (data: action, custom_field_id, entity_id, entity_type, export, export_, from*, page_number, page_size, project_id, to*, user_id, workspace_id)\n- **list**: List Audit Log. (data: action, custom_field_id, entity_id, entity_type, export, export_, from*, page_number, page_size, project_id, to*, user_id)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: get-organization, list",
      "enum": [
       "get-organization",
       "list"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "audit-log",
   "title": "Audit-log"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Authenticate this MCP server with Toggl 2.0 via OAuth. Opens browser login and stores credentials locally. Optionally pass workspace_id to pick a specific workspace when multiple are available.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "accounts_api_url": {
      "description": "Optional accounts API URL override (defaults from ENV or production)",
      "type": "string"
     },
     "focus_api_url": {
      "description": "Optional focus API URL override (defaults from ENV or production)",
      "type": "string"
     },
     "workspace_id": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "number"
       }
      ]
     }
    },
    "required": [],
    "type": "object"
   },
   "name": "auth",
   "title": "Authenticate MCP"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus capacities with the following actions:\n\n- **get-computations**: Compute user or ghost capacity for a date range. (data: end_date*, group*, include_drafts*, include_task_estimates*, private_*, start_date*, task_source*, team_id*, unit*, user_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "const": "get-computations"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "properties": {
       "end_date": {
        "format": "date",
        "type": "string"
       },
       "group": {
        "enum": [
         "users"
        ],
        "type": "string"
       },
       "include_drafts": {},
       "include_task_estimates": {},
       "private_": {},
       "start_date": {
        "format": "date",
        "type": "string"
       },
       "task_source": {},
       "team_id": {
        "type": "number"
       },
       "unit": {},
       "user_id": {
        "type": "number"
       }
      },
      "required": [
       "end_date",
       "group",
       "include_drafts",
       "include_task_estimates",
       "private_",
       "start_date",
       "task_source",
       "team_id",
       "unit",
       "user_id"
      ],
      "type": "object"
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "capacities",
   "title": "Capacities"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus clients with the following actions:\n\n- **create**: Create Clients. (data: name*, currency)\n- **delete**: Delete Clients. (data: client_id*)\n- **get**: Get Clients. (data: client_id*)\n- **list**: List Clients. Clients group billable work; projects can reference client_id. Filter projects with projects.list --client-id. (data: name, page, per_page)\n- **restore**: Restore Clients. (data: client_id*)\n- **update**: Update Clients. (data: client_id*, payload*{name, currency})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create, delete, get, list, restore, update",
      "enum": [
       "create",
       "delete",
       "get",
       "list",
       "restore",
       "update"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "clients",
   "title": "Clients"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus custom-fields with the following actions:\n\n- **create**: Create Custom Fields. Requires workspace admin (403 otherwise). field_type is one of select, multi_select, text, number, date, checkbox, users; select/multi_select fields can be seeded with options inline. (data: payload*{entity_type*, field_type*, name*, description, options})\n- **delete**: Delete Custom Fields. Requires workspace admin. Soft-deletes the field and removes it from entities; this also discards stored values. (data: custom_field_id*)\n- **get**: Get Custom Fields. (data: custom_field_id*)\n- **get-allowed-users**: Get Allowed Users Custom Fields. (data: custom_field_id*)\n- **list**: List Custom Fields. Custom fields are workspace-level definitions attached to project or task entities. Reads work for all members; create/update/delete return 403 for non-admins. Reordering fields is not available through this tool yet. (data: entity_type, order_by, page, per_page)\n- **options-create**: Options Create Custom Fields. Requires workspace admin. Only valid for select and multi_select fields. (data: custom_field_id*, payload*{name*, color, description})\n- **options-delete**: Options Delete Custom Fields. Requires workspace admin. Deleting an option clears it from entities that have it selected. (data: custom_field_id*, option_id*)\n- **options-list**: Options List Custom Fields. (data: custom_field_id*, name, order_by, page, per_page)\n- **options-update**: Options Update Custom Fields. Requires workspace admin. Full update (PUT) of the option's name/color/description. (data: custom_field_id*, option_id*, payload*{name*, color, description})\n- **replace-custom-field-allowed-users**: Replace Custom Field Allowed Users Custom Fields. (data: custom_field_id*, payload*{user_ids*})\n- **update**: Update Custom Fields. Requires workspace admin. Full update (PUT) of name/description only; field_type and entity_type are immutable. Manage options via the options-* actions. (data: custom_field_id*, payload*{name*, description})\n- **update-options-order**: Update Options Order Custom Fields. (data: custom_field_id*, payload*{ordered_ids*})\n- **update-order**: Update Order Custom Fields. (data: payload*{entity_type*, ordered_ids*})\n- **update-user-custom-field-values**: Update User Custom Field Values Custom Fields. (data: payload*, user_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create, delete, get, get-allowed-users, list, options-create, options-delete, options-list, options-update, replace-custom-field-allowed-users, update, update-options-order, update-order, update-user-custom-field-values",
      "enum": [
       "create",
       "delete",
       "get",
       "get-allowed-users",
       "list",
       "options-create",
       "options-delete",
       "options-list",
       "options-update",
       "replace-custom-field-allowed-users",
       "update",
       "update-options-order",
       "update-order",
       "update-user-custom-field-values"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "custom-fields",
   "title": "Custom-fields"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus goals with the following actions:\n\n- **create**: Create Goals. (data: payload*{...})\n- **delete**: Delete Goals. (data: goal_id*)\n- **get**: Get Goals. (data: goal_id*)\n- **list**: List Goals. (data: none)\n- **preview-goal-tracked-time**: Preview Goal Tracked Time Goals. (data: payload*{...})\n- **reorder**: Reorder Goals. (data: payload*{goal_ids*})\n- **update**: Update Goals. (data: goal_id*, payload*{active, client_ids, comparison, icon, name, project_ids, recurrence, start_date, tag_ids, target_seconds, task_ids, weekdays})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create, delete, get, list, preview-goal-tracked-time, reorder, update",
      "enum": [
       "create",
       "delete",
       "get",
       "list",
       "preview-goal-tracked-time",
       "reorder",
       "update"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "goals",
   "title": "Goals"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Clear local MCP authentication state. Removes stored credentials so subsequent tool calls require auth again.",
   "inputSchema": {
    "properties": {},
    "type": "object"
   },
   "name": "logout",
   "title": "Logout MCP"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus milestones with the following actions:\n\n- **create**: Create Milestones. (data: payload*{color*, due_date*, name*, completed_at, project_ids, team_ids})\n- **delete**: Delete Milestones. (data: milestone_id*)\n- **get**: Get Milestones. (data: milestone_id*)\n- **get-classic**: Get Classic Milestones. (data: due_date_from, due_date_to, order_by, page, per_page, project_id, team_id)\n- **get-stream**: Get Stream Milestones. (data: due_date_from, due_date_to, order_by, project_id, team_id)\n- **partial-update**: Partial Update Milestones. (data: milestone_id*, payload*{due_date, name, project_ids, team_ids})\n- **restore**: Restore Milestones. (data: milestone_id*)\n- **update**: Update Milestones. (data: milestone_id*, payload*{color*, due_date*, name*, completed_at, project_ids, team_ids})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create, delete, get, get-classic, get-stream, partial-update, restore, update",
      "enum": [
       "create",
       "delete",
       "get",
       "get-classic",
       "get-stream",
       "partial-update",
       "restore",
       "update"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "milestones",
   "title": "Milestones"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus org-invitations with the following actions:\n\n- **list**: List pending invitations for the current user account. (data: none)\n- **create**: Send organization invitations (uses organization ID from your workspace profile). (data: initiating_product*, invitations*, skip_email*)\n- **get**: Get an invitation by ID. (data: invitation_id*)\n- **resend**: Resend an invitation email for an organization user record. (data: organization_user_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: list, create, get, resend",
      "enum": [
       "list",
       "create",
       "get",
       "resend"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "org-invitations",
   "title": "Org-invitations"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus org-members with the following actions:\n\n- **get**: Get an organization user by organization user ID. (data: organization_user_id*)\n- **update**: Replace organization user settings (roles, groups, tags, etc.). (data: organization_user_id*, payload*{accounts_role_id*, active*, groups*, name*, tags*, workspaces*})\n- **set-active**: Activate or deactivate an organization member. (data: organization_user_id*, status*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: get, update, set-active",
      "enum": [
       "get",
       "update",
       "set-active"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "org-members",
   "title": "Org-members"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus org-time-off-me with the following actions:\n\n- **list**: List your time off entries between two dates (YYYY-MM-DD). (data: from*, to*)\n- **update**: Update your time off entries (JSON array). (data: array payload)\n- **create**: Create your time off entries (JSON array). (data: array payload)\n- **delete**: Delete your time off entries by ID. (data: ids*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: list, update, create, delete",
      "enum": [
       "list",
       "update",
       "create",
       "delete"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "org-time-off-me",
   "title": "Org-time-off-me"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus org-workspaces with the following actions:\n\n- **list**: List workspaces under the active organization. (data: none)\n- **my-roles**: Get your roles for a workspace (defaults to the CLI active workspace when omitted). (data: workspace_id)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: list, my-roles",
      "enum": [
       "list",
       "my-roles"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "org-workspaces",
   "title": "Org-workspaces"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus organization with the following actions:\n\n- **create-group**: Create an organization group. (data: payload*{emoji*, name*, organization_users*, workspaces*})\n- **delete-group**: Delete an organization group. (data: group_id*)\n- **get-member-tags**: Get Member Tags Organization. (data: workspace_id*)\n- **get-my-groups**: List organization groups for the current user. (data: none)\n- **get-roles**: List organization roles. (data: none)\n- **get-teams**: List organization teams. (data: active_members_only*, filter*, group_ids*, organization_user_id*, page*, per_page*, query*, sort_dir*, workspace_id*)\n- **update-group**: Update an organization group. (data: group_id*, payload*{emoji*, name*, organization_users*, workspaces*})\n- **update-group-user-role**: Update Group User Role Organization. (data: group_id*, payload*{organization_user_id*, workspace_id*})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create-group, delete-group, get-member-tags, get-my-groups, get-roles, get-teams, update-group, update-group-user-role",
      "enum": [
       "create-group",
       "delete-group",
       "get-member-tags",
       "get-my-groups",
       "get-roles",
       "get-teams",
       "update-group",
       "update-group-user-role"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "organization",
   "title": "Organization"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "List saved credential profiles from ~/.toggl/focus-tools.json. The row where active is true is the MCP active profile (active.mcp). Profiles may be shared with the Toggl CLI.",
   "inputSchema": {
    "properties": {},
    "type": "object"
   },
   "name": "profile-list",
   "title": "List MCP profiles"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Delete a profile row from ~/.toggl/focus-tools.json. If it was the MCP active profile, active.mcp moves to another remaining profile when possible. This affects both MCP and shared storage; removing a profile used by the CLI removes it for the CLI too. Destructive: call without confirm_token first to receive confirm_required + confirm_token, then repeat with the same profile_name and confirm_token (same pattern as entity mutations).",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "profile_name": {
      "description": "Profile key as shown by profile-list",
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "profile_name"
    ],
    "type": "object"
   },
   "name": "profile-remove",
   "title": "Remove saved profile"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Set active.mcp to an existing profile name so subsequent entity calls use that credential row.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "profile_name": {
      "description": "Profile key as shown by profile-list",
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "profile_name"
    ],
    "type": "object"
   },
   "name": "profile-switch",
   "title": "Switch MCP profile"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus projects with the following actions:\n\n- **create-project**: Create a project with related data such as members, milestones, rates, and billable rates. Follow the create action's privacy and billable guidance. Project fields go under payload.project. Set rates through payload.billable_rates, not the legacy payload.rates (start_at/end_at). For payload.billable_rates: hourly_rate is in the currency's smallest unit, so it depends on the currency: two-decimal currencies like USD/EUR use cents (5000 = 50.00), zero-decimal ones like JPY/KRW use whole units (5000 = 5000), three-decimal ones like KWD/BHD use thousandths. Reads return the same unit. Omit end_date for an open-ended schedule; when set it must be on or after start_date. (data: payload*{project*, billable_rates, members, milestones, rates})\n- **add-project-members**: Add Project Members Projects. (data: payload*{role_id*, group_ids, user_ids}, project_id*)\n- **archive**: Archive Projects. (data: project_id*)\n- **attachments-bulk-create**: Attachments Bulk Create Projects. Uploads several local files in one call. names and display_orders are optional parallel arrays; they default to the file basenames and array positions. (data: project_id*, file_paths*, names, display_orders)\n- **attachments-create**: Attachments Create Projects. Uploads a local file (file_path) as an attachment. The mime type is inferred from the extension and the API rejects disallowed types; uploads are capped at 20 MB. (data: project_id*, file_path*, name, display_order)\n- **attachments-delete**: Attachments Delete Projects. (data: attachment_id*, project_id*)\n- **attachments-list**: Attachments List Projects. Lists attachment metadata. Upload files with attachments-create (file_path), download with attachments-view (save_to). (data: order_by, project_id*)\n- **attachments-update**: Attachments Update Projects. Full update (PUT): both name and display_order are required; the response body is empty, verify via attachments-list. (data: attachment_id*, payload*{display_order*, name*}, project_id*)\n- **attachments-view**: Attachments View Projects. Downloads the attachment's binary content to a local path (save_to). Refuses to replace an existing file unless overwrite is true. (data: project_id*, attachment_id*, save_to*, overwrite)\n- **bulk-archive**: Bulk Archive Projects. (data: project_ids*)\n- **bulk-delete**: Bulk Delete Projects. Deletes all listed project IDs in one HTTP call. (data: ids*)\n- **bulk-patch**: Bulk Patch Projects. Prefer over many project updates: JSON array of { id: projectId, ...partial fields } matching single-project patch fields. (data: array payload)\n- **bulk-restore**: Bulk Restore Projects. (data: project_ids*)\n- **bulk-unarchive**: Bulk Unarchive Projects. (data: project_ids*)\n- **complete**: Complete Projects. (data: project_id*)\n- **create**: Create Projects. Required: name. Recommended: color (hex). The API defaults private and billable to false when omitted. Match the Toggl UI on privacy. If the user explicitly asks for a shared or public project, pass private=false; no permissions check is needed, because only private projects require an entitlement. Otherwise call workspace-settings get-user-permissions and pass private=true when permissions includes access_private_projects_tasks_feature. If that permission is absent, pass private=false and tell the user; if the user explicitly required a private project, do not create a shared one. Leave billable unset unless the user explicitly says the work is billable or provides a rate. Associate with a client via client_id. (data: name*, auto_compute_estimates, billable, client_id, color, completion_mode, custom_fields, description, draft, end_date, estimated_mins, fixed_fee{amount*, currency*}, is_template, parent_project_id, pinned, private, rollover_mode, rrule, start_date, tag_ids)\n- **create-from-template**: Create From Template Projects. (data: payload*{auto_compute_estimates, completion_mode, end_date, name, pinned, rollover_mode, rrule, start_date}, project_id*)\n- **create-recurring-project-instance**: Create Recurring Project Instance Projects. (data: date*, payload*{auto_compute_estimates, billable, private}, project_id*)\n- **create-template-from**: Create Template From Projects. (data: payload*{auto_compute_estimates, estimated_mins, fixed_fee, name, rrule}, project_id*)\n- **delete**: Delete Projects. Permanently deletes the project and disassociates its tasks. (data: project_id*)\n- **duplicate**: Duplicate Projects. (data: payload*{name}, project_id*)\n- **duplicate-template**: Duplicate Template Projects. (data: payload*{name}, project_id*)\n- **get**: Get Projects. Fetch a single project with aggregated task counts and time estimates. (data: project_id*)\n- **get-batch-projects-members**: Get Batch Projects Members Projects. (data: ids*)\n- **get-members**: Get Members Projects. (data: project_id*)\n- **get-recurring-project-instance**: Get Recurring Project Instance Projects. (data: date*, project_id*)\n- **get-recurring-project-instances**: Get Recurring Project Instances Projects. (data: end_date, include_rollover, order_by, page, per_page, project_id*, start_date)\n- **get-templates**: Get Templates Projects. (data: archived, name, order_by, page, per_page)\n- **get-with-children**: Get With Children Projects. (data: order_by, page, per_page, project_id*)\n- **get-with-parents**: Get With Parents Projects. (data: order_by, page, per_page, project_id*)\n- **groups-get**: Groups Get Projects. (data: archived, assignee_user_id, cf_filter, client_id, completed, creator_id, end_date, group*, include_drafts, include_group_totals, include_unassigned, name, only_me, order_by, page, parent_project_id, per_page, private_, project_id, project_order_by, projects_per_group, start_date, tag_id, user_id)\n- **list**: List Projects. Use user_id to filter to projects that include a specific member. Filter by pinned=true for starred projects. (data: name, pinned, user_id, client_id, page, per_page)\n- **make-project-public**: Make Project Public Projects. (data: project_id*)\n- **margin-targets-clear**: Margin Targets Clear Projects. (data: project_id*)\n- **margin-targets-get**: Margin Targets Get Projects. (data: project_id*)\n- **margin-targets-get-workspace**: Margin Targets Get Workspace Projects. (data: none)\n- **margin-targets-list**: Margin Targets List Projects. (data: none)\n- **margin-targets-set**: Margin Targets Set Projects. (data: payload*{target_percent*}, project_id*)\n- **margin-targets-set-workspace**: Margin Targets Set Workspace Projects. (data: payload*{target_percent*})\n- **pin**: Pin Projects. (data: project_id*)\n- **remove-project-member**: Remove Project Member Projects. (data: payload*{group_id, user_id}, project_id*)\n- **restore**: Restore Projects. (data: project_id*)\n- **unarchive**: Unarchive Projects. (data: project_id*)\n- **uncomplete**: Uncomplete Projects. (data: project_id*)\n- **unpin**: Unpin Projects. (data: project_id*)\n- **update**: Update Projects. Partial update \u2014 provide project_id and only the fields to change under payload. custom_fields writes values as a map keyed by custom field ID; reads return the same data as custom_field_values[], so the write shape is not the read shape. (data: project_id*, payload*{...})\n- **full-update**: Full Update Projects. (data: payload*{...}, project_id*)\n- **update-member-role**: Update Member Role Projects. (data: payload*{role_id*, group_id, user_id}, project_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create-project, add-project-members, archive, attachments-bulk-create, attachments-create, attachments-delete, attachments-list, attachments-update, attachments-view, bulk-archive, bulk-delete, bulk-patch, bulk-restore, bulk-unarchive, complete, create, create-from-template, create-recurring-project-instance, create-template-from, delete, duplicate, duplicate-template, get, get-batch-projects-members, get-members, get-recurring-project-instance, get-recurring-project-instances, get-templates, get-with-children, get-with-parents, groups-get, list, make-project-public, margin-targets-clear, margin-targets-get, margin-targets-get-workspace, margin-targets-list, margin-targets-set, margin-targets-set-workspace, pin, remove-project-member, restore, unarchive, uncomplete, unpin, update, full-update, update-member-role",
      "enum": [
       "create-project",
       "add-project-members",
       "archive",
       "attachments-bulk-create",
       "attachments-create",
       "attachments-delete",
       "attachments-list",
       "attachments-update",
       "attachments-view",
       "bulk-archive",
       "bulk-delete",
       "bulk-patch",
       "bulk-restore",
       "bulk-unarchive",
       "complete",
       "create",
       "create-from-template",
       "create-recurring-project-instance",
       "create-template-from",
       "delete",
       "duplicate",
       "duplicate-template",
       "get",
       "get-batch-projects-members",
       "get-members",
       "get-recurring-project-instance",
       "get-recurring-project-instances",
       "get-templates",
       "get-with-children",
       "get-with-parents",
       "groups-get",
       "list",
       "make-project-public",
       "margin-targets-clear",
       "margin-targets-get",
       "margin-targets-get-workspace",
       "margin-targets-list",
       "margin-targets-set",
       "margin-targets-set-workspace",
       "pin",
       "remove-project-member",
       "restore",
       "unarchive",
       "uncomplete",
       "unpin",
       "update",
       "full-update",
       "update-member-role"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "projects",
   "title": "Projects"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus public-holidays with the following actions:\n\n- **get-by-user-ids**: Get By User Ids Public Holidays. (data: end_date*, start_date*, user_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "const": "get-by-user-ids"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "properties": {
       "end_date": {
        "format": "date",
        "type": "string"
       },
       "start_date": {
        "format": "date",
        "type": "string"
       },
       "user_id": {
        "type": "number"
       }
      },
      "required": [
       "end_date",
       "start_date",
       "user_id"
      ],
      "type": "object"
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "public-holidays",
   "title": "Public-holidays"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus rates with the following actions:\n\n- **billable-check-project-billable-rate-conflicts**: Billable Check Project Billable Rate Conflicts Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, project_id*)\n- **billable-check-project-user-billable-rate-conflicts**: Billable Check Project User Billable Rate Conflicts Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, project_id*, user_id*)\n- **billable-check-workspace-billable-rate-conflicts**: Billable Check Workspace Billable Rate Conflicts Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date})\n- **billable-check-workspace-user-billable-rate-conflicts**: Billable Check Workspace User Billable Rate Conflicts Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, user_id*)\n- **billable-delete-by-id**: Billable Delete By Id Rates. (data: rate_id*)\n- **billable-delete-project-billable-rate-range**: Billable Delete Project Billable Rate Range Rates. (data: from*, project_id*, to)\n- **billable-delete-project-user-billable-rate-range**: Billable Delete Project User Billable Rate Range Rates. (data: from*, project_id*, to, user_id*)\n- **billable-delete-workspace-billable-rate-range**: Billable Delete Workspace Billable Rate Range Rates. (data: from*, to)\n- **billable-delete-workspace-user-billable-rate-range**: Billable Delete Workspace User Billable Rate Range Rates. (data: from*, to, user_id*)\n- **billable-get-all-project-user**: Billable Get All Project User Rates. (data: date, order_by, project_id, user_id)\n- **billable-get-all-workspace-user**: Billable Get All Workspace User Rates. (data: order_by, user_id)\n- **billable-get-project**: Billable Get Project Rates. (data: order_by, project_id*)\n- **billable-get-project-user**: Billable Get Project User Rates. (data: order_by, project_id*, user_id*)\n- **billable-get-projects**: Billable Get Projects Rates. (data: date*, project_id*)\n- **billable-get-workspace**: Billable Get Workspace Rates. (data: order_by)\n- **billable-get-workspace-user**: Billable Get Workspace User Rates. (data: order_by, user_id*)\n- **billable-upsert-project**: Billable Upsert Project Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, project_id*)\n- **billable-upsert-project-user**: Billable Upsert Project User Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, project_id*, user_id*)\n- **billable-upsert-workspace**: Billable Upsert Workspace Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date})\n- **billable-upsert-workspace-user**: Billable Upsert Workspace User Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, user_id*)\n- **create-task**: Create Task Rates. (data: payload*{billable*}, task_id*)\n- **delete-project**: Delete Project Rates. (data: from*, project_id*, to*)\n- **get-current-project**: Get Current Project Rates. (data: project_id*)\n- **get-project**: Get Project Rates. (data: order_by, project_id*)\n- **get-project-rates-summary**: Get Project Rates Summary Rates. (data: order_by, page, rows, search)\n- **labor-check-project-labor-cost-conflicts**: Labor Check Project Labor Cost Conflicts Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, project_id*)\n- **labor-check-project-user-labor-cost-conflicts**: Labor Check Project User Labor Cost Conflicts Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, project_id*, user_id*)\n- **labor-check-workspace-labor-cost-conflicts**: Labor Check Workspace Labor Cost Conflicts Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date})\n- **labor-check-workspace-user-labor-cost-conflicts**: Labor Check Workspace User Labor Cost Conflicts Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, user_id*)\n- **labor-delete-project-labor-cost-rate-range**: Labor Delete Project Labor Cost Rate Range Rates. (data: from*, project_id*, to)\n- **labor-delete-project-user-labor-cost-rate-range**: Labor Delete Project User Labor Cost Rate Range Rates. (data: from*, project_id*, to, user_id*)\n- **labor-delete-rate-by-id**: Labor Delete Rate By Id Rates. (data: rate_id*)\n- **labor-delete-workspace-labor-cost-rate-range**: Labor Delete Workspace Labor Cost Rate Range Rates. (data: from*, to)\n- **labor-delete-workspace-user-labor-cost-rate-range**: Labor Delete Workspace User Labor Cost Rate Range Rates. (data: from*, to, user_id*)\n- **labor-get-all-project-user-labor-cost-rates**: Labor Get All Project User Labor Cost Rates Rates. (data: date, order_by, project_id, user_id)\n- **labor-get-all-workspace-user-labor-cost-rates**: Labor Get All Workspace User Labor Cost Rates Rates. (data: order_by, user_id)\n- **labor-get-project-labor-cost-rates**: Labor Get Project Labor Cost Rates Rates. (data: order_by, project_id*)\n- **labor-get-project-user-labor-cost-rates**: Labor Get Project User Labor Cost Rates Rates. (data: order_by, project_id*, user_id*)\n- **labor-get-workspace-labor-cost-rates**: Labor Get Workspace Labor Cost Rates Rates. (data: order_by)\n- **labor-get-workspace-user-labor-cost-rates**: Labor Get Workspace User Labor Cost Rates Rates. (data: order_by, user_id*)\n- **labor-upsert-project-labor-cost-rate**: Labor Upsert Project Labor Cost Rate Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, project_id*)\n- **labor-upsert-project-user-labor-cost-rate**: Labor Upsert Project User Labor Cost Rate Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, project_id*, user_id*)\n- **labor-upsert-workspace-labor-cost-rate**: Labor Upsert Workspace Labor Cost Rate Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date})\n- **labor-upsert-workspace-user-labor-cost-rate**: Labor Upsert Workspace User Labor Cost Rate Rates. (data: payload*{currency*, hourly_rate*, start_date*, end_date}, user_id*)\n\nApplies to billable-check-project-billable-rate-conflicts, billable-check-project-user-billable-rate-conflicts, billable-check-workspace-billable-rate-conflicts, billable-check-workspace-user-billable-rate-conflicts, billable-upsert-project, billable-upsert-project-user, billable-upsert-workspace, billable-upsert-workspace-user, labor-check-project-labor-cost-conflicts, labor-check-project-user-labor-cost-conflicts, labor-check-workspace-labor-cost-conflicts, labor-check-workspace-user-labor-cost-conflicts, labor-upsert-project-labor-cost-rate, labor-upsert-project-user-labor-cost-rate, labor-upsert-workspace-labor-cost-rate, labor-upsert-workspace-user-labor-cost-rate: hourly_rate is in the currency's smallest unit, so it depends on the currency: two-decimal currencies like USD/EUR use cents (5000 = 50.00), zero-decimal ones like JPY/KRW use whole units (5000 = 5000), three-decimal ones like KWD/BHD use thousandths. Reads return the same unit. Omit end_date for an open-ended schedule; when set it must be on or after start_date.\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: billable-check-project-billable-rate-conflicts, billable-check-project-user-billable-rate-conflicts, billable-check-workspace-billable-rate-conflicts, billable-check-workspace-user-billable-rate-conflicts, billable-delete-by-id, billable-delete-project-billable-rate-range, billable-delete-project-user-billable-rate-range, billable-delete-workspace-billable-rate-range, billable-delete-workspace-user-billable-rate-range, billable-get-all-project-user, billable-get-all-workspace-user, billable-get-project, billable-get-project-user, billable-get-projects, billable-get-workspace, billable-get-workspace-user, billable-upsert-project, billable-upsert-project-user, billable-upsert-workspace, billable-upsert-workspace-user, create-task, delete-project, get-current-project, get-project, get-project-rates-summary, labor-check-project-labor-cost-conflicts, labor-check-project-user-labor-cost-conflicts, labor-check-workspace-labor-cost-conflicts, labor-check-workspace-user-labor-cost-conflicts, labor-delete-project-labor-cost-rate-range, labor-delete-project-user-labor-cost-rate-range, labor-delete-rate-by-id, labor-delete-workspace-labor-cost-rate-range, labor-delete-workspace-user-labor-cost-rate-range, labor-get-all-project-user-labor-cost-rates, labor-get-all-workspace-user-labor-cost-rates, labor-get-project-labor-cost-rates, labor-get-project-user-labor-cost-rates, labor-get-workspace-labor-cost-rates, labor-get-workspace-user-labor-cost-rates, labor-upsert-project-labor-cost-rate, labor-upsert-project-user-labor-cost-rate, labor-upsert-workspace-labor-cost-rate, labor-upsert-workspace-user-labor-cost-rate",
      "enum": [
       "billable-check-project-billable-rate-conflicts",
       "billable-check-project-user-billable-rate-conflicts",
       "billable-check-workspace-billable-rate-conflicts",
       "billable-check-workspace-user-billable-rate-conflicts",
       "billable-delete-by-id",
       "billable-delete-project-billable-rate-range",
       "billable-delete-project-user-billable-rate-range",
       "billable-delete-workspace-billable-rate-range",
       "billable-delete-workspace-user-billable-rate-range",
       "billable-get-all-project-user",
       "billable-get-all-workspace-user",
       "billable-get-project",
       "billable-get-project-user",
       "billable-get-projects",
       "billable-get-workspace",
       "billable-get-workspace-user",
       "billable-upsert-project",
       "billable-upsert-project-user",
       "billable-upsert-workspace",
       "billable-upsert-workspace-user",
       "create-task",
       "delete-project",
       "get-current-project",
       "get-project",
       "get-project-rates-summary",
       "labor-check-project-labor-cost-conflicts",
       "labor-check-project-user-labor-cost-conflicts",
       "labor-check-workspace-labor-cost-conflicts",
       "labor-check-workspace-user-labor-cost-conflicts",
       "labor-delete-project-labor-cost-rate-range",
       "labor-delete-project-user-labor-cost-rate-range",
       "labor-delete-rate-by-id",
       "labor-delete-workspace-labor-cost-rate-range",
       "labor-delete-workspace-user-labor-cost-rate-range",
       "labor-get-all-project-user-labor-cost-rates",
       "labor-get-all-workspace-user-labor-cost-rates",
       "labor-get-project-labor-cost-rates",
       "labor-get-project-user-labor-cost-rates",
       "labor-get-workspace-labor-cost-rates",
       "labor-get-workspace-user-labor-cost-rates",
       "labor-upsert-project-labor-cost-rate",
       "labor-upsert-project-user-labor-cost-rate",
       "labor-upsert-workspace-labor-cost-rate",
       "labor-upsert-workspace-user-labor-cost-rate"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "rates",
   "title": "Rates"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus recurring-budget-suggestions with the following actions:\n\n- **accept-arecurring-project-budget-suggestion**: Accept Arecurring Project Budget Suggestion Recurring Budget Suggestions. (data: project_id*)\n- **accept-atemplate-estimate-suggestion**: Accept Atemplate Estimate Suggestion Recurring Budget Suggestions. (data: project_id*)\n- **dismiss-arecurring-project-budget-suggestion**: Dismiss Arecurring Project Budget Suggestion Recurring Budget Suggestions. (data: project_id*)\n- **dismiss-atemplate-estimate-suggestion**: Dismiss Atemplate Estimate Suggestion Recurring Budget Suggestions. (data: project_id*)\n- **get-recurring-project-budget-suggestion**: Get Recurring Project Budget Suggestion Recurring Budget Suggestions. (data: project_id*, target_period_start*)\n- **get-template-estimate-suggestion**: Get Template Estimate Suggestion Recurring Budget Suggestions. (data: project_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: accept-arecurring-project-budget-suggestion, accept-atemplate-estimate-suggestion, dismiss-arecurring-project-budget-suggestion, dismiss-atemplate-estimate-suggestion, get-recurring-project-budget-suggestion, get-template-estimate-suggestion",
      "enum": [
       "accept-arecurring-project-budget-suggestion",
       "accept-atemplate-estimate-suggestion",
       "dismiss-arecurring-project-budget-suggestion",
       "dismiss-atemplate-estimate-suggestion",
       "get-recurring-project-budget-suggestion",
       "get-template-estimate-suggestion"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "recurring-budget-suggestions",
   "title": "Recurring-budget-suggestions"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus reports with the following actions:\n\n- **get-flexq-data**: Get Flexq Data Reports. (data: payload*{...}, response_format)\n- **get-flexq-dictionary**: Get Flexq Dictionary Reports. (data: payload*)\n- **get-forecast-availability**: Get Forecast Availability Reports. (data: none)\n- **get-profitability**: Get Profitability Reports. (data: include_dicts, payload*{...}, response_format, xTogglReportExport)\n- **get-time-accounting**: Get Time Accounting Reports. (data: payload*{...})\n- **get-workload**: Get Workload Reports. (data: payload*{...}, xTogglReportExport)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: get-flexq-data, get-flexq-dictionary, get-forecast-availability, get-profitability, get-time-accounting, get-workload",
      "enum": [
       "get-flexq-data",
       "get-flexq-dictionary",
       "get-forecast-availability",
       "get-profitability",
       "get-time-accounting",
       "get-workload"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "reports",
   "title": "Reports"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus reportschedules with the following actions:\n\n- **delete-shared-report-schedule**: Delete Shared Report Schedule Reportschedules. (data: id*)\n- **upsert-saved-view-schedule**: Upsert Saved View Schedule Reportschedules. (data: id*, payload*{cadence*, charts*, time*, day, period, recipient_team_ids, recipient_user_ids, recipient_workspace_ids})\n- **upsert-shared-report-schedule**: Upsert Shared Report Schedule Reportschedules. (data: id*, payload*{cadence*, time*, day, recipient_team_ids, recipient_user_ids, recipient_workspace_ids})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: delete-shared-report-schedule, upsert-saved-view-schedule, upsert-shared-report-schedule",
      "enum": [
       "delete-shared-report-schedule",
       "upsert-saved-view-schedule",
       "upsert-shared-report-schedule"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "reportschedules",
   "title": "Reportschedules"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus saved-views with the following actions:\n\n- **create**: Create Saved Views. Required: name, type, pinned, and params (the view's filter/layout state as an object, e.g. { project_id, filters }). Project-scoped types (project, project.tasks, project.board, project.timeline) expect the project context inside params. (data: payload*{name*, params*, pinned*, type*, icon_name})\n- **delete**: Delete Saved Views. Deletes the authenticated user's saved view; it cannot be restored. (data: id*)\n- **get**: Get Saved Views. id is the saved view ID (number) from saved-views list. (data: id*)\n- **list**: List Saved Views. Saved views are per-user, so results belong to the authenticated user. Filter by type (inbox, archive, board, timeline, project, project.tasks, project.board, project.timeline, projects) or by partial name match. pinned=true entries are the user's starred views. (data: name, order_by, type)\n- **update**: Update Saved Views. The payload requires the full updatable set (name, type, pinned, params), so fetch the view first and resend it with your changes to avoid clobbering params. Toggling pinned is how views are starred/unstarred. (data: id*, payload*{name*, params*, pinned*, type*, icon_name})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create, delete, get, list, update",
      "enum": [
       "create",
       "delete",
       "get",
       "list",
       "update"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "saved-views",
   "title": "Saved-views"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus search with the following actions:\n\n- **mentions**: Mentions Search. (data: keyword, per_group)\n- **search**: Search across Toggl 2.0 entities. (data: keyword*, per_group, assignee_user_id, project_id, task_id, include_drafts, exclude_calendar_events, assignee_team_id, group_by_tags, only_me)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: mentions, search",
      "enum": [
       "mentions",
       "search"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "search",
   "title": "Search"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus shared-holidays with the following actions:\n\n- **list**: List organization holiday calendar dates for a year (optional ISO country override). (data: year*, country)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "const": "list"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "properties": {
       "country": {
        "description": "ISO 3166-1 alpha-2, lowercase",
        "type": "string"
       },
       "year": {
        "type": "integer"
       }
      },
      "required": [
       "year"
      ],
      "type": "object"
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "shared-holidays",
   "title": "Shared-holidays"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus shared-status with the following actions:\n\n- **get**: Health check for the Shared Data API. (data: none)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "const": "get"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "properties": {},
      "required": [],
      "type": "object"
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "shared-status",
   "title": "Shared-status"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus shared-working-hours with the following actions:\n\n- **list**: Get your working hours entries for the active organization. (data: none)\n- **put**: Replace working hours by ID (JSON array of updates). (data: array payload)\n- **create**: Create working hour rows (JSON array). (data: array payload)\n- **delete**: Delete working hour rows by ID. (data: ids*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: list, put, create, delete",
      "enum": [
       "list",
       "put",
       "create",
       "delete"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "shared-working-hours",
   "title": "Shared-working-hours"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus sharedreports with the following actions:\n\n- **add-shared-report-grants**: Add Shared Report Grants Sharedreports. (data: id*, payload*{grantees*})\n- **get-saved-view-shared-report**: Get Saved View Shared Report Sharedreports. (data: id*)\n- **get-shared-report-for-viewer**: Get Shared Report For Viewer Sharedreports. (data: id*)\n- **get-shared-reports-shared-by-me**: Get Shared Reports Shared By Me Sharedreports. (data: none)\n- **get-shared-reports-shared-with-me**: Get Shared Reports Shared With Me Sharedreports. (data: none)\n- **revoke-shared-report-grant**: Revoke Shared Report Grant Sharedreports. (data: grant_id*, id*)\n- **share-saved-view**: Share Saved View Sharedreports. (data: id*, payload*{charts*, period, team_id, user_id, workspace_id})\n- **share-saved-view-many**: Share Saved View Many Sharedreports. (data: id*, payload*{charts*, grantees*, period})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: add-shared-report-grants, get-saved-view-shared-report, get-shared-report-for-viewer, get-shared-reports-shared-by-me, get-shared-reports-shared-with-me, revoke-shared-report-grant, share-saved-view, share-saved-view-many",
      "enum": [
       "add-shared-report-grants",
       "get-saved-view-shared-report",
       "get-shared-report-for-viewer",
       "get-shared-reports-shared-by-me",
       "get-shared-reports-shared-with-me",
       "revoke-shared-report-grant",
       "share-saved-view",
       "share-saved-view-many"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "sharedreports",
   "title": "Sharedreports"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus statuses with the following actions:\n\n- **create-status**: Create a task status. (data: payload*{emoji*, name*, position*})\n- **delete-status**: Delete a task status. (data: status_id*)\n- **get-status**: List task statuses with generated API filters. (data: status_id*)\n- **list**: List Statuses. Lists all task status options for the workspace. Use returned status IDs when creating or updating tasks via status_id. Statuses have types: todo, in_progress, done, blocked. (data: page, per_page)\n- **restore**: Restore Statuses. (data: status_id*)\n- **update-status**: Update a task status. (data: payload*{emoji*, name*, position*}, status_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create-status, delete-status, get-status, list, restore, update-status",
      "enum": [
       "create-status",
       "delete-status",
       "get-status",
       "list",
       "restore",
       "update-status"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "statuses",
   "title": "Statuses"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus subscriptions with the following actions:\n\n- **get-current**: Get Current Subscriptions. (data: none)\n- **get-current-trial**: Get Current Trial Subscriptions. (data: none)\n- **get-features-upsell**: Get Features Upsell Subscriptions. (data: none)\n- **get-plans**: Get Plans Subscriptions. (data: none)\n- **upsert-forced-trial**: Upsert Forced Trial Subscriptions. (data: payload*{plan_workspace_id})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: get-current, get-current-trial, get-features-upsell, get-plans, upsert-forced-trial",
      "enum": [
       "get-current",
       "get-current-trial",
       "get-features-upsell",
       "get-plans",
       "upsert-forced-trial"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "subscriptions",
   "title": "Subscriptions"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus tags with the following actions:\n\n- **archive**: Archive Tags. (data: tag_id*)\n- **bulk-archive**: Bulk Archive Tags. (data: payload*{tag_ids*})\n- **bulk-create**: Bulk Create Tags. (data: payload*{tags*})\n- **bulk-delete**: Bulk Delete Tags. (data: ids*)\n- **bulk-restore**: Bulk Restore Tags. (data: payload*{tag_ids*})\n- **bulk-unarchive**: Bulk Unarchive Tags. (data: payload*{tag_ids*})\n- **create**: Create Tags. Both name and color are required by the API (hex color, e.g. #FF5733). (data: color*, name*)\n- **delete**: Delete Tags. (data: tag_id*)\n- **get**: Get Tags. (data: tag_id*)\n- **list**: List Tags. Workspace tags label tasks and projects. Use tag IDs with tasks (tag_ids) and organization user filters (tag_ids on users.list). (data: name, page, per_page)\n- **merge**: Merge Tags. (data: payload*{source_ids*, target_id*})\n- **restore**: Restore Tags. (data: tag_id*)\n- **unarchive**: Unarchive Tags. (data: tag_id*)\n- **update**: Update Tags. (data: tag_id*, payload*{color, name})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: archive, bulk-archive, bulk-create, bulk-delete, bulk-restore, bulk-unarchive, create, delete, get, list, merge, restore, unarchive, update",
      "enum": [
       "archive",
       "bulk-archive",
       "bulk-create",
       "bulk-delete",
       "bulk-restore",
       "bulk-unarchive",
       "create",
       "delete",
       "get",
       "list",
       "merge",
       "restore",
       "unarchive",
       "update"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "tags",
   "title": "Tags"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus tasks with the following actions:\n\n- **create-task**: Create a task with related data such as subtasks, time blocks, and an initial time entry. (data: payload*{task*, estimate_allocations, subtasks, time_blocks, time_entry})\n- **archive**: Archive Tasks. (data: from_recurring_task_id, task_id*)\n- **attachments-bulk-create**: Attachments Bulk Create Tasks. Uploads several local files in one call. names and display_orders are optional parallel arrays; they default to the file basenames and array positions. (data: task_id*, file_paths*, names, display_orders)\n- **attachments-create**: Attachments Create Tasks. Uploads a local file (file_path) as an attachment. The mime type is inferred from the extension and the API rejects disallowed types; uploads are capped at 20 MB. (data: task_id*, file_path*, name, display_order)\n- **attachments-delete**: Attachments Delete Tasks. (data: attachment_id*, task_id*)\n- **attachments-list**: Attachments List Tasks. Lists attachment metadata. Upload files with attachments-create (file_path), download with attachments-view (save_to). (data: order_by, task_id*)\n- **attachments-update**: Attachments Update Tasks. Full update (PUT): both name and display_order are required; the response body is empty, verify via attachments-list. (data: attachment_id*, payload*{display_order*, name*}, task_id*)\n- **attachments-view**: Attachments View Tasks. Downloads the attachment's binary content to a local path (save_to). Refuses to replace an existing file unless overwrite is true. (data: task_id*, attachment_id*, save_to*, overwrite)\n- **bulk-archive**: Bulk Archive Tasks. (data: task_ids*)\n- **bulk-create**: Bulk Create Tasks. (data: tasks*)\n- **bulk-delete**: Bulk Delete Tasks. Deletes all listed task IDs in one HTTP call. Requires confirm flow like single delete. (data: ids*, outbound_sync)\n- **bulk-duplicate**: Bulk Duplicate Tasks. (data: cascade_duplicate_children*, task_ids*, private)\n- **bulk-patch**: Bulk Patch Tasks. Prefer over many single updates: pass a JSON array of { id: taskId, ...partial fields }. Same field names as single task update. (data: array payload)\n- **bulk-restore**: Bulk Restore Tasks. (data: task_ids*)\n- **bulk-unarchive**: Bulk Unarchive Tasks. (data: task_ids*)\n- **constraints-list**: Constraints List Tasks. (data: none)\n- **constraints-upsert**: Constraints Upsert Tasks. (data: payload*{enabled*, required_fields*})\n- **create**: Create Tasks. Required: name. Strongly recommended: project_id, status_id, assignee_user_ids. Use start_date/end_date for scheduling as date-only YYYY-MM-DD values; never pass RFC 3339 datetimes. Set estimated_mins for time estimates. (data: name*, allocation_unit, assignee_user_ids, auto_log_time, billable, color, custom_fields, description, end_date, estimate_type, estimated_mins, integration_ext_id, integration_source, notes, parent_task_id, pinned, position, priority, priority_at, private, project_id, rollup_enabled, rrule, start_date, status_id, tag_ids, team_assignee_ids)\n- **create-recurring-occurrence**: Create Recurring Occurrence Tasks. (data: return_occurrence, task_id*)\n- **delete**: Delete Tasks. Permanently deletes the task and all its subtasks. Cannot be undone. (data: task_id*)\n- **estimate-allocations-create**: Estimate Allocations Create Tasks. (data: payload*, task_id*)\n- **estimate-allocations-delete**: Estimate Allocations Delete Tasks. (data: id, task_id*)\n- **estimate-allocations-list**: Estimate Allocations List Tasks. (data: task_id*)\n- **estimate-allocations-update**: Estimate Allocations Update Tasks. (data: payload*, task_id*)\n- **get**: Get Tasks. Fetch a single task with full detail including subtasks and time estimates. (data: task_id*)\n- **get-by-external-id**: Get By External Id Tasks. (data: ext_id*, source*)\n- **get-calendar-events**: Get Calendar Events Tasks. (data: calendar_event_id, date_from, date_to, hidden)\n- **get-recurring-instances**: Get Recurring Instances Tasks. (data: archived, assignee_team_id, assignee_user_id, dates_overlap_end, dates_overlap_start, end_date, include_assignees, include_projected_occurrences, max_recurring_occurrences_after, page, per_page, recurring_occurrences_after, recurring_task_ids, recurring_task_read, start_date, tag_id, team_id)\n- **get-stream**: Get Stream Tasks. (data: active_by_date, archived, assignee_team_id, assignee_user_id, cf_filter, client_id, dates_overlap_end, dates_overlap_start, end_date, end_date_from, end_date_to, estimated_mins_max, estimated_mins_min, has_dates, has_time_blocks, has_time_entries, include_assignees, include_drafts, include_parent_matches, include_project_completed, include_projected_occurrences, is_priority, max_recurring_occurrences_after, name, order_by, parent_task_id, pinned, priority, private_, project_id, recurring, recurring_occurrences_after, recurring_task_ids, recurring_task_read, source, start_date, start_date_from, start_date_to, state, status_id, tag_id, team_id, time_block_completed, time_block_id, toggl_user_id, with_entries)\n- **get-with-children**: Get With Children Tasks. (data: archived, assignee_user_id, include_assignees, order_by, page, per_page, source, status_id, tag_id, task_id*, team_id)\n- **get-with-parents**: Get With Parents Tasks. (data: archived, assignee_user_id, include_assignees, order_by, page, per_page, source, status_id, tag_id, task_id*, team_id)\n- **groups-get**: Groups Get Tasks. (data: assignee_user_id, cf_filter, client_id, collapse_recurring, creator_id, end_date, end_date_from, end_date_to, exclude_empty, group*, group_id, include_assignees, include_drafts, include_projected_occurrences, include_unassigned, include_unmaterialized, max_recurring_occurrences_after, min_days, name, order_by, page, per_page, priority, private_, project_id, recurring_occurrences_after, recurring_task_read, source, start_date, start_date_from, start_date_to, status_id, tag_id, team_id, user_cf_filter, user_tag_id, xTimelineFetchTrigger)\n- **groups-get-user-task-groups-by-project**: Groups Get User Task Groups By Project Tasks. (data: assignee_user_id*, cf_filter*, client_id*, collapse_recurring*, creator_id*, end_date*, end_date_from*, end_date_to*, exclude_empty*, group_id*, include_assignees*, include_drafts*, include_projected_occurrences*, include_unassigned*, include_unmaterialized*, max_recurring_occurrences_after*, min_days*, name*, priority*, private_*, project_id*, recurring_occurrences_after*, recurring_task_read*, source*, start_date*, start_date_from*, start_date_to*, status_id*, tag_id*, team_id*, user_cf_filter*, user_tag_id*, xTimelineFetchTrigger*)\n- **groups-get-user-task-groups-by-team**: Groups Get User Task Groups By Team Tasks. (data: assignee_user_id, cf_filter, client_id, collapse_recurring, creator_id, end_date, end_date_from, end_date_to, exclude_empty, group_id, include_assignees, include_drafts, include_projected_occurrences, include_unassigned, include_unmaterialized, max_recurring_occurrences_after, min_days, name, priority, private_, project_id, recurring_occurrences_after, recurring_task_read, source, start_date, start_date_from, start_date_to, status_id, tag_id, team_id, user_cf_filter, user_tag_id, xTimelineFetchTrigger)\n- **groups-update**: Groups Update Tasks. (data: group*, payload*)\n- **list**: List Tasks. Filter by start_date/end_date (ISO 8601, e.g. '2025-04-27') for date ranges, project_id, status_id, or assignee_user_id. Returns paginated results \u2014 use page/per_page for more. Use parent_task_id to list subtasks. Priority values: none, low, medium, high. (data: name, status_id, project_id, tag_id, assignee_user_id, priority, start_date, end_date, parent_task_id, page, per_page)\n- **restore**: Restore Tasks. (data: task_id*)\n- **suggestions-get-project-suggestion-for-atask**: Suggestions Get Project Suggestion For Atask Tasks. (data: payload*{task_id*})\n- **suggestions-get-project-suggestions-for-multiple-tasks**: Suggestions Get Project Suggestions For Multiple Tasks Tasks. (data: payload*{task_ids*})\n- **unarchive**: Unarchive Tasks. (data: task_id*)\n- **update**: Update Tasks. Partial update \u2014 only include fields to change in payload. Provide task_id at top level and the fields to update under payload. custom_fields writes values as a map keyed by custom field ID; reads return the same data as custom_field_values[], so the write shape is not the read shape. (data: task_id*, payload*{...})\n- **full-update**: Full Update Tasks. (data: payload*{...}, task_id*)\n- **update-calendar-range**: Update Calendar Range Tasks. (data: date_from*, date_to*, skip_provider_refresh)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create-task, archive, attachments-bulk-create, attachments-create, attachments-delete, attachments-list, attachments-update, attachments-view, bulk-archive, bulk-create, bulk-delete, bulk-duplicate, bulk-patch, bulk-restore, bulk-unarchive, constraints-list, constraints-upsert, create, create-recurring-occurrence, delete, estimate-allocations-create, estimate-allocations-delete, estimate-allocations-list, estimate-allocations-update, get, get-by-external-id, get-calendar-events, get-recurring-instances, get-stream, get-with-children, get-with-parents, groups-get, groups-get-user-task-groups-by-project, groups-get-user-task-groups-by-team, groups-update, list, restore, suggestions-get-project-suggestion-for-atask, suggestions-get-project-suggestions-for-multiple-tasks, unarchive, update, full-update, update-calendar-range",
      "enum": [
       "create-task",
       "archive",
       "attachments-bulk-create",
       "attachments-create",
       "attachments-delete",
       "attachments-list",
       "attachments-update",
       "attachments-view",
       "bulk-archive",
       "bulk-create",
       "bulk-delete",
       "bulk-duplicate",
       "bulk-patch",
       "bulk-restore",
       "bulk-unarchive",
       "constraints-list",
       "constraints-upsert",
       "create",
       "create-recurring-occurrence",
       "delete",
       "estimate-allocations-create",
       "estimate-allocations-delete",
       "estimate-allocations-list",
       "estimate-allocations-update",
       "get",
       "get-by-external-id",
       "get-calendar-events",
       "get-recurring-instances",
       "get-stream",
       "get-with-children",
       "get-with-parents",
       "groups-get",
       "groups-get-user-task-groups-by-project",
       "groups-get-user-task-groups-by-team",
       "groups-update",
       "list",
       "restore",
       "suggestions-get-project-suggestion-for-atask",
       "suggestions-get-project-suggestions-for-multiple-tasks",
       "unarchive",
       "update",
       "full-update",
       "update-calendar-range"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "tasks",
   "title": "Tasks"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus time-blocks with the following actions:\n\n- **archive**: Archive Time Blocks. (data: task_id*, time_block_id*)\n- **bulk-create**: Bulk Create Time Blocks. Body shape: { time_blocks: [{ task_id, start, duration?, completed? }, ...] }. Creates many blocks in one request. (data: time_blocks*)\n- **bulk-delete**: Bulk Delete Time Blocks. Deletes time blocks by numeric id list in one HTTP call (not task_id/time_block_id pairs). (data: ids*, outbound_sync)\n- **bulk-patch**: Bulk Patch Time Blocks. JSON array of { id: timeBlockId, ...partial fields }; same fields as single time-block update. (data: array payload)\n- **create**: Create Time Blocks. Required: task_id and payload.start (ISO 8601 datetime). Optionally set payload.duration (minutes) and payload.completed. (data: task_id*, payload*{start*, completed, duration, outbound_sync})\n- **delete**: Delete Time Blocks. Permanently removes the scheduled time block from the task. (data: task_id*, time_block_id*)\n- **get**: Get Time Blocks. Fetch a single time block by task ID and time block ID. (data: task_id*, time_block_id*)\n- **get-range-stream**: Get Range Stream Time Blocks. (data: archived*, date_from*, date_to*, order_by*, status_id*, task_id*)\n- **get-task-time-blocks**: Get Task Time Blocks Time Blocks. (data: archived, order_by, page, per_page, task_id*)\n- **list**: List Time Blocks. Returns scheduled time blocks in a date range. Requires date_from and date_to. Accepts date-only strings (YYYY-MM-DD) or RFC3339 datetimes \u2014 date-only is automatically expanded: date_from to T00:00:00Z, date_to to T23:59:59Z. Results include associated task summaries. (data: date_from*, date_to*, task_id, page, per_page)\n- **restore**: Restore Time Blocks. (data: task_id*, time_block_id*)\n- **unarchive**: Unarchive Time Blocks. (data: task_id*, time_block_id*)\n- **update**: Update Time Blocks. Partial update \u2014 provide task_id, time_block_id, and fields to change in payload. (data: task_id*, time_block_id*, payload*{completed, duration, outbound_sync, start, task_id})\n- **update-with-org**: Update With Org Time Blocks. (data: payload*{start*, completed, duration, outbound_sync}, task_id*, time_block_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: archive, bulk-create, bulk-delete, bulk-patch, create, delete, get, get-range-stream, get-task-time-blocks, list, restore, unarchive, update, update-with-org",
      "enum": [
       "archive",
       "bulk-create",
       "bulk-delete",
       "bulk-patch",
       "create",
       "delete",
       "get",
       "get-range-stream",
       "get-task-time-blocks",
       "list",
       "restore",
       "unarchive",
       "update",
       "update-with-org"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "time-blocks",
   "title": "Time-blocks"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus time-entries with the following actions:\n\n- **archive**: Archive Time Entries. (data: time_entry_id*)\n- **bulk-create**: Bulk Create Time Entries. JSON array of entries with task_id, start, duration, type ('activity'|'break'). One HTTP request for many creates. (data: array payload)\n- **bulk-delete**: Bulk Delete Time Entries. Soft-delete multiple time entries by id in one request. (data: ids*)\n- **bulk-edit**: Bulk Edit Time Entries. (data: payload*{changes*, ids*})\n- **bulk-patch**: Bulk Patch Time Entries. JSON array of { id: timeEntryId, ...partial fields } for bulk partial update. (data: array payload)\n- **bulk-restore**: Bulk Restore Time Entries. Restore deleted time entries by id in one request. (data: ids*)\n- **bulk-set-day-duration**: Bulk Set Day Duration Time Entries. (data: payload*)\n- **constraints-list**: Constraints List Time Entries. (data: none)\n- **constraints-upsert**: Constraints Upsert Time Entries. (data: payload*{enabled*, required_fields*, disallow_billable_override})\n- **create**: Create a completed task time entry. payload.type defaults to 'activity' when omitted. Logs a completed task time entry. payload.type defaults to 'activity' when omitted; pass 'break' explicitly for a break. provide payload.start and payload.duration for completed tracked time. (data: from_recurring_task_id, payload*{...}, return_occurrence, task_id*)\n- **create-taskless**: Create a completed taskless time entry. payload.type defaults to 'activity' when omitted. Logs a completed taskless time entry. payload.type defaults to 'activity' when omitted; pass 'break' explicitly for a break. (data: from_recurring_task_id, payload*{...}, return_occurrence)\n- **delete**: Delete Time Entries. (data: from_recurring_task_id, return_occurrence, time_entry_id*)\n- **get**: Get Time Entries. (data: time_entry_id*)\n- **get-batch**: Get Batch Time Entries. (data: ids*)\n- **get-stream-time-entries-range**: Get Stream Time Entries Range Time Entries. (data: archived*, date_from*, date_to*, expand_calendar_event*, include_taskless*, order_by*, project_id*, status_id*, task_id*, type*)\n- **get-stream-time-entries-time-block-ids**: Get Stream Time Entries Time Block Ids Time Entries. (data: time_block_id*)\n- **get-task-time-entries**: Get Task Time Entries Time Entries. (data: archived, order_by, page, per_page, task_id*, time_block_id, type)\n- **groups-get-users**: Groups Get Users Time Entries. (data: end_date*, order_by*, page*, per_page*, start_date*, user_id*)\n- **list**: List Time Entries. Returns tracked time entries in a date range, including taskless entries (tracked on a project with no task). Requires date_from and date_to. Accepts date-only strings (YYYY-MM-DD) or RFC3339 datetimes \u2014 date-only is automatically expanded: date_from to T00:00:00Z, date_to to T23:59:59Z. Optional project_id keeps only entries on that project (sent to the API; client also scans up to 50 list pages of 100, then slices page/per_page; not a full workspace scan). If truncated is true, more unfiltered rows exist than were scanned \u2014 narrow date_from/date_to. Pass include_taskless:false to hide taskless rows. Includes task and project context. (data: date_from*, date_to*, project_id, task_id, time_block_id, type, include_taskless, page, per_page)\n- **log-planned-time**: Log Planned Time Time Entries. (data: time_entry_id*)\n- **partial-update**: Partial Update Time Entries. (data: from_recurring_task_id, payload*{billable, duration, reset_billable_to_default, type}, return_occurrence, time_entry_id*)\n- **restore**: Restore Time Entries. (data: time_entry_id*)\n- **set-day-duration**: Set Day Duration Time Entries. (data: payload*{date_from*, date_to*, duration*, discriminant, start_time, task_id})\n- **start**: Start Time Entries. type defaults to 'activity' if omitted; pass type:'break' to start a break. Optionally provide task_id, project_id, description, and time_block_id. Only one timer can be active at a time \u2014 stops the current one if running. (data: type, description, project_id, start, task_id, time_block_id)\n- **stop**: Stop Time Entries. Stops the currently running timer. No input required. (data: none)\n- **sync**: Sync Time Entries. (data: client_uuid*, payload*{billable_source*, client_updated_at*, type*, billable})\n- **tracking-get-tracked-time-entry**: Tracking Get Tracked Time Entry Time Entries. (data: none)\n- **tracking-start-tracking-from-description**: Tracking Start Tracking From Description Time Entries. (data: payload*{...})\n- **unarchive**: Unarchive Time Entries. (data: time_entry_id*)\n- **update**: Update Time Entries. (data: from_recurring_task_id, payload*{type*, calendar_event_id, description, duration, planned_at, planned_duration, planned_start, start, time_block_id, tracked_at}, return_occurrence, time_entry_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: archive, bulk-create, bulk-delete, bulk-edit, bulk-patch, bulk-restore, bulk-set-day-duration, constraints-list, constraints-upsert, create, create-taskless, delete, get, get-batch, get-stream-time-entries-range, get-stream-time-entries-time-block-ids, get-task-time-entries, groups-get-users, list, log-planned-time, partial-update, restore, set-day-duration, start, stop, sync, tracking-get-tracked-time-entry, tracking-start-tracking-from-description, unarchive, update",
      "enum": [
       "archive",
       "bulk-create",
       "bulk-delete",
       "bulk-edit",
       "bulk-patch",
       "bulk-restore",
       "bulk-set-day-duration",
       "constraints-list",
       "constraints-upsert",
       "create",
       "create-taskless",
       "delete",
       "get",
       "get-batch",
       "get-stream-time-entries-range",
       "get-stream-time-entries-time-block-ids",
       "get-task-time-entries",
       "groups-get-users",
       "list",
       "log-planned-time",
       "partial-update",
       "restore",
       "set-day-duration",
       "start",
       "stop",
       "sync",
       "tracking-get-tracked-time-entry",
       "tracking-start-tracking-from-description",
       "unarchive",
       "update"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "time-entries",
   "title": "Time-entries"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus time-off with the following actions:\n\n- **create-timeoff-for-user**: Create Timeoff For User Time Off. (data: payload*, user_id*)\n- **delete-timeoff-for-user**: Delete Timeoff For User Time Off. (data: payload*, user_id*)\n- **get-timeoff-for-user**: Get Timeoff For User Time Off. (data: from*, to*, user_id*)\n- **get-timeoffs-by-user-ids**: Get Timeoffs By User Ids Time Off. (data: end_date*, start_date*, user_id*)\n- **update-timeoff-for-user**: Update Timeoff For User Time Off. (data: payload*, user_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create-timeoff-for-user, delete-timeoff-for-user, get-timeoff-for-user, get-timeoffs-by-user-ids, update-timeoff-for-user",
      "enum": [
       "create-timeoff-for-user",
       "delete-timeoff-for-user",
       "get-timeoff-for-user",
       "get-timeoffs-by-user-ids",
       "update-timeoff-for-user"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "time-off",
   "title": "Time-off"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus timesheets with the following actions:\n\n- **add-timesheet-flag**: Add Timesheet Flag Timesheets. (data: payload*{comment*, time_entry_id*}, setup_id*, start_date*, user_account_id*)\n- **approve**: Approve Timesheets. (data: setup_id*, start_date*, user_account_id*)\n- **bulk-approve**: Bulk Approve Timesheets. (data: payload*{keys*})\n- **bulk-request-changes**: Bulk Request Changes Timesheets. (data: payload*{keys*, comment})\n- **bulk-withdraw**: Bulk Withdraw Timesheets. (data: payload*{keys*})\n- **get**: Get Timesheets. (data: approver_user_account_ids*, current_layer_approver_user_account_id*, date_from*, date_to*, page*, per_page*, setup_id*, statuses*, team_ids*, user_account_ids*)\n- **get-approvers**: Get Approvers Timesheets. (data: none)\n- **get-details**: Get Details Timesheets. (data: setup_id*, start_date*, user_account_id*)\n- **get-flags**: Get Flags Timesheets. (data: setup_id*, start_date*, user_account_id*)\n- **get-history**: Get History Timesheets. (data: setup_id*, start_date*, user_account_id*)\n- **get-hours**: Get Hours Timesheets. (data: setup_id*, start_date*, user_account_id*)\n- **remove-timesheet-flag**: Remove Timesheet Flag Timesheets. (data: flag_id*)\n- **request-changes**: Request Changes Timesheets. (data: payload*{comment*}, setup_id*, start_date*, user_account_id*)\n- **resubmit**: Resubmit Timesheets. (data: payload*{comment*}, setup_id*, start_date*, user_account_id*)\n- **setups-add-timesheet-setup-member**: Setups Add Timesheet Setup Member Timesheets. (data: setup_id*, user_account_id*)\n- **setups-create**: Setups Create Timesheets. (data: payload*{approvers*, member_user_account_ids*, periodicity*, start_date*, email_reminder_enabled, period_days, reminder_day, reminder_time})\n- **setups-delete**: Setups Delete Timesheets. (data: setup_id*)\n- **setups-delete-member**: Setups Delete Member Timesheets. (data: setup_id*, user_account_id*)\n- **setups-discontinue**: Setups Discontinue Timesheets. (data: payload*{end_date*}, setup_id*)\n- **setups-discontinue-timesheet-setup-member**: Setups Discontinue Timesheet Setup Member Timesheets. (data: payload*{end_date}, setup_id*, user_account_id*)\n- **setups-get**: Setups Get Timesheets. (data: setup_id*)\n- **setups-get-approvers**: Setups Get Approvers Timesheets. (data: setup_id*)\n- **setups-get-members**: Setups Get Members Timesheets. (data: include_discontinued, setup_id*)\n- **setups-list**: Setups List Timesheets. (data: approver_user_account_id, include_discontinued, user_account_id)\n- **setups-reactivate-timesheet-setup-member**: Setups Reactivate Timesheet Setup Member Timesheets. (data: setup_id*, user_account_id*)\n- **setups-update**: Setups Update Timesheets. (data: payload*{approvers, email_reminder_enabled, member_user_account_ids, reminder, unset_reminder}, setup_id*)\n- **submit**: Submit Timesheets. (data: setup_id*, start_date*, user_account_id*)\n- **withdraw**: Withdraw Timesheets. (data: setup_id*, start_date*, user_account_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: add-timesheet-flag, approve, bulk-approve, bulk-request-changes, bulk-withdraw, get, get-approvers, get-details, get-flags, get-history, get-hours, remove-timesheet-flag, request-changes, resubmit, setups-add-timesheet-setup-member, setups-create, setups-delete, setups-delete-member, setups-discontinue, setups-discontinue-timesheet-setup-member, setups-get, setups-get-approvers, setups-get-members, setups-list, setups-reactivate-timesheet-setup-member, setups-update, submit, withdraw",
      "enum": [
       "add-timesheet-flag",
       "approve",
       "bulk-approve",
       "bulk-request-changes",
       "bulk-withdraw",
       "get",
       "get-approvers",
       "get-details",
       "get-flags",
       "get-history",
       "get-hours",
       "remove-timesheet-flag",
       "request-changes",
       "resubmit",
       "setups-add-timesheet-setup-member",
       "setups-create",
       "setups-delete",
       "setups-delete-member",
       "setups-discontinue",
       "setups-discontinue-timesheet-setup-member",
       "setups-get",
       "setups-get-approvers",
       "setups-get-members",
       "setups-list",
       "setups-reactivate-timesheet-setup-member",
       "setups-update",
       "submit",
       "withdraw"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "timesheets",
   "title": "Timesheets"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus users with the following actions:\n\n- **list**: List Users. Lists organization users (members). Use returned IDs for assignee_user_ids on tasks. Invite or provision new users from the Toggl Track/Focus web UI or billing admin \u2014 this API lists existing users only. (data: page, per_page, filter, sort_dir, only_admins, include_working_hours, include_rates, workspaces, tag_ids, roles, active_status, groups, pinned_ids, user_account_ids, org_user_ids)\n- **settings-email-unsubscribe**: Settings Email Unsubscribe Users. (data: token*)\n- **settings-email-unsubscribe-page**: Settings Email Unsubscribe Page Users. (data: token*)\n- **settings-list**: Settings List Users. (data: none)\n- **settings-partial-update**: Settings Partial Update Users. (data: payload*{...})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: list, settings-email-unsubscribe, settings-email-unsubscribe-page, settings-list, settings-partial-update",
      "enum": [
       "list",
       "settings-email-unsubscribe",
       "settings-email-unsubscribe-page",
       "settings-list",
       "settings-partial-update"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "users",
   "title": "Users"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus version with the following actions:\n\n- **service**: MCP package version plus the Focus API service revision. mcp.package is this published MCP package version (e.g. 1.8.0). focus_api is the Focus API process revision (git SHA / build number) \u2014 it does not change when you update the npm MCP package. (data: none)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "const": "service"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "properties": {},
      "required": [],
      "type": "object"
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "version",
   "title": "Version"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus working-hours with the following actions:\n\n- **create-for-user**: Create For User Working Hours. (data: payload*, user_id*)\n- **delete-for-user**: Delete For User Working Hours. (data: payload*, user_id*)\n- **get-for-user**: Get For User Working Hours. (data: user_id*)\n- **update-for-user**: Update For User Working Hours. (data: payload*, user_id*)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: create-for-user, delete-for-user, get-for-user, update-for-user",
      "enum": [
       "create-for-user",
       "delete-for-user",
       "get-for-user",
       "update-for-user"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "working-hours",
   "title": "Working-hours"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Manage Focus workspace with the following actions:\n\n- **get-context**: Get Context Workspace. (data: none)\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "const": "get-context"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "properties": {},
      "required": [],
      "type": "object"
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "workspace",
   "title": "Workspace"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true,
    "readOnlyHint": true
   },
   "description": "List workspaces for the active MCP profile. Pass refresh: true to refetch from Accounts when a workspace is missing from cache.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "refresh": {
      "type": "boolean"
     }
    },
    "required": [],
    "type": "object"
   },
   "name": "workspace-list",
   "title": "List Workspaces"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Focus workspace-settings with the following actions:\n\n- **workspace-settings-list**: List user workspace settings. (data: none)\n- **workspace-settings-partial-update**: Update user workspace settings. (data: payload*)\n- **get-user-permissions**: Get User Permissions Workspace Settings. (data: none)\n- **get-workspace-currency**: Get Workspace Currency Workspace Settings. (data: none)\n- **get-workspace-export-branding**: Get Workspace Export Branding Workspace Settings. (data: none)\n- **get-workspace-watermark**: Get Workspace Watermark Workspace Settings. (data: none)\n- **list**: List Workspace Settings. (data: none)\n- **update**: Update Workspace Settings. (data: payload*{settings*})\n- **update-workspace-currency**: Update Workspace Currency Workspace Settings. (data: payload*{currency*})\n- **update-workspace-watermark**: Update Workspace Watermark Workspace Settings. (data: payload*{show_watermark*})\n\nPass the action-specific fields under `data` (required fields are marked with *).\nMutations require a confirm_token handshake: call without confirm_token first to receive a token, then call again with the token to confirm.\nOptional dry_run: true validates input and returns { dry_run, operation, input } without calling the API.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "action": {
      "description": "One of: workspace-settings-list, workspace-settings-partial-update, get-user-permissions, get-workspace-currency, get-workspace-export-branding, get-workspace-watermark, list, update, update-workspace-currency, update-workspace-watermark",
      "enum": [
       "workspace-settings-list",
       "workspace-settings-partial-update",
       "get-user-permissions",
       "get-workspace-currency",
       "get-workspace-export-branding",
       "get-workspace-watermark",
       "list",
       "update",
       "update-workspace-currency",
       "update-workspace-watermark"
      ],
      "type": "string"
     },
     "confirm_token": {
      "description": "Mutation confirmation token from previous call",
      "type": "string"
     },
     "data": {
      "description": "Action-specific payload. See each action's parameters in this tool's description; pass dry_run: true to validate it without calling the API."
     },
     "dry_run": {
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "workspace-settings",
   "title": "Workspace-settings"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Switch the active workspace by updating the stored configuration. The new workspace takes effect on the next tool call. Use workspace-list to see available workspace IDs.",
   "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "properties": {
     "workspace_id": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "number"
       }
      ]
     }
    },
    "required": [
     "workspace_id"
    ],
    "type": "object"
   },
   "name": "workspace-switch",
   "title": "Switch Workspace"
  }
 ],
 "version": "1.11.25"
}