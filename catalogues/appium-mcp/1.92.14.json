{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "appium-mcp",
 "protocol": "2025-06-18",
 "published": "2026-09-04T13:50:33.690Z",
 "tools": [
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Handle system alerts with action=accept|dismiss, or read alert text with action=get_text.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "Action to perform on alert: accept, dismiss, or get_text",
      "enum": [
       "accept",
       "dismiss",
       "get_text"
      ],
      "type": "string"
     },
     "buttonLabel": {
      "description": "Optional label of the button to click for accept/dismiss.",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_alert"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage app lifecycle, installation, state, data, and deep links.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "activate: foreground app; terminate: stop app; is_installed: check installation; clear: clear app data without uninstalling (all require id or name). install: requires path. uninstall: requires id/name; Android keepData is optional. list: optional iOS applicationType. query_state: get state 0=not installed,1=not running,2=background suspended,3=background,4=foreground (requires id or name). background: send foreground app to background; optional seconds (default 5). deep_link: requires url; id/name is optional.",
      "enum": [
       "activate",
       "terminate",
       "install",
       "uninstall",
       "list",
       "is_installed",
       "query_state",
       "background",
       "clear",
       "deep_link"
      ],
      "type": "string"
     },
     "applicationType": {
      "description": "iOS list filter: User (default) or System.",
      "enum": [
       "User",
       "System"
      ],
      "type": "string"
     },
     "id": {
      "description": "Android package or iOS bundle ID; takes precedence over name.",
      "type": "string"
     },
     "keepData": {
      "description": "Android uninstall: preserve app data and cache.",
      "type": "boolean"
     },
     "name": {
      "description": "Human-readable app name resolved to an ID; alternative to id.",
      "type": "string"
     },
     "path": {
      "description": "App file path; required for install.",
      "type": "string"
     },
     "seconds": {
      "description": "Background duration; default 5. Use -1 to remain in background.",
      "maximum": 86400,
      "minimum": -1,
      "type": "number"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     },
     "url": {
      "description": "URL for deep_link (e.g. https://example.com or myapp://path).",
      "type": "string"
     },
     "waitForLaunch": {
      "description": "Android deep_link: wait for the activity to return; default true.",
      "type": "boolean"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_app_lifecycle"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Appium contexts with one tool. action=list returns all contexts and current context. action=switch changes to a target context.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "Use list to fetch contexts or switch to change context.",
      "enum": [
       "list",
       "switch"
      ],
      "type": "string"
     },
     "context": {
      "description": "Required when action is switch. Common values: NATIVE_APP or WEBVIEW_<id>/WEBVIEW_<package>.",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_context"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Perform a drag-and-drop gesture from a source location to a target location. The gesture: long press the source (default 600ms), drag to the target (default 1200ms), then release. Source and target can each be specified as either an element UUID or coordinates. Useful for reordering lists, moving items, drag-to-delete.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "duration": {
      "description": "Duration of the drag movement in milliseconds. Default 1200.",
      "maximum": 5000,
      "minimum": 100,
      "type": "integer"
     },
     "longPressDuration": {
      "description": "Duration of the long press before dragging in milliseconds. Default 600.",
      "maximum": 2000,
      "minimum": 400,
      "type": "integer"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     },
     "sourceElementUUID": {
      "description": "UUID of source element to drag from. Either sourceElementUUID or sourceX+sourceY must be provided.",
      "type": "string"
     },
     "sourceX": {
      "description": "Source X coordinate. Required if sourceElementUUID is not provided.",
      "maximum": 9007199254740991,
      "minimum": 0,
      "type": "integer"
     },
     "sourceY": {
      "description": "Source Y coordinate. Required if sourceElementUUID is not provided.",
      "maximum": 9007199254740991,
      "minimum": 0,
      "type": "integer"
     },
     "targetElementUUID": {
      "description": "UUID of target element to drop on. Either targetElementUUID or targetX+targetY must be provided.",
      "type": "string"
     },
     "targetX": {
      "description": "Target X coordinate. Required if targetElementUUID is not provided.",
      "maximum": 9007199254740991,
      "minimum": 0,
      "type": "integer"
     },
     "targetY": {
      "description": "Target Y coordinate. Required if targetElementUUID is not provided.",
      "maximum": 9007199254740991,
      "minimum": 0,
      "type": "integer"
     }
    },
    "type": "object"
   },
   "name": "appium_drag_and_drop"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Read or update Appium driver session settings (e.g. idle timeouts, selector waits). Use action=get to return JSON settings; action=update merges a map into the session. Works for embedded UiAutomator2/XCUITest sessions and remote WebDriver clients that support Appium settings.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "get: read current Appium driver session settings (timeouts, selector waits, flags). update: merge a settings map into the session (requires settings).",
      "enum": [
       "get",
       "update"
      ],
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     },
     "settings": {
      "additionalProperties": false,
      "description": "Required when action is update. Driver-specific keys (e.g. Android UiAutomator2: waitForIdleTimeout, waitForSelectorTimeout, ignoreUnimportantViews; iOS XCUITest has its own set). Use action=get first to inspect current values.",
      "propertyNames": {
       "type": "string"
      },
      "type": "object"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_driver_settings"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Find a specific element by strategy and selector which will return a uuid that can be used for interactions.\n\n[PRIORITY 2: Use this to search for a target element.]\n\n**Strategy priority**: accessibility id > id > platform-native (`-ios predicate string` / `-ios class chain` on iOS, `-android uiautomator` on Android) > xpath (last resort \u2014 slow & brittle). See the `strategy` parameter for the full ranking.\n\n**Scrolling until an element appears**: use `appium_gesture` with `action=scroll_to_element` (same strategy + selector), not this tool.\n\n**Vision / natural-language find**: use `appium_ai` with `action=find_element`, not this tool.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "selector": {
      "description": "Selector string for the chosen strategy. Do not pass natural-language descriptions of the target here; use appium_ai (action=find_element) for that.",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     },
     "strategy": {
      "description": "Locator strategy. Try in priority order: (1) accessibility id [cross-platform, fastest, most stable], (2) id [Android resource-id; iOS aliases accessibility id], (3) -ios predicate string [iOS native, fast], (4) -ios class chain [iOS native, hierarchy queries], (5) -android uiautomator [Android native, expressive UiSelector], (6) xpath [LAST RESORT \u2014 slow on iOS XCUITest, brittle to layout changes], (7) name [legacy; often aliased on iOS], (8) class name [too generic, usually multi-match], (9) css selector [webview/hybrid contexts only]. Platform tips: iOS prefer (1)\u2192(3)\u2192(4); Android prefer (1)\u2192(2)\u2192(5); xpath last on both. For natural-language / vision-based find, use the appium_ai tool (action=find_element), not this one.",
      "enum": [
       "accessibility id",
       "id",
       "-ios predicate string",
       "-ios class chain",
       "-android uiautomator",
       "xpath",
       "name",
       "class name",
       "css selector"
      ],
      "type": "string"
     }
    },
    "required": [
     "strategy",
     "selector"
    ],
    "type": "object"
   },
   "name": "appium_find_element"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Generate tests for a mobile app: follow the returned instructions to drive the real session with MCP tools (select_device, appium_session_management, appium_find_element, appium_gesture, appium_set_value, etc.), then emit code. Use generate_locators only when you need a full locator snapshot; prefer appium_find_element for normal steps.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "steps": {
      "description": "The steps of the test",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "steps"
    ],
    "type": "object"
   },
   "name": "appium_generate_tests"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Get, set, or reset the device geolocation (GPS coordinates). Works on both iOS (simulators and real devices) and Android (emulators and real devices with mock location enabled). Use action=get to read current coordinates, action=set with latitude/longitude (and optional altitude for Android) to simulate a location, or action=reset to restore the system default. Note: On Android emulators, reset is not supported \u2014 use action=set to manually restore coordinates instead. On Android real devices, the mocked location may persist until the GPS cache refreshes.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "Action to perform. get: read the current device geolocation. set: set the device geolocation (requires latitude and longitude; optional altitude for Android). reset: reset the geolocation to the default/system value. Not supported on Android emulators \u2014 use action=set instead.",
      "enum": [
       "get",
       "set",
       "reset"
      ],
      "type": "string"
     },
     "altitude": {
      "description": "Altitude value in meters. Android only, defaults to 0. Ignored on iOS. Used with: set.",
      "type": "number"
     },
     "latitude": {
      "description": "Latitude value (-90 to 90). Measurement of distance north or south of the Equator. Required for: set.",
      "maximum": 90,
      "minimum": -90,
      "type": "number"
     },
     "longitude": {
      "description": "Longitude value (-180 to 180). Measurement of distance east or west of the prime meridian. Required for: set.",
      "maximum": 180,
      "minimum": -180,
      "type": "number"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_geolocation"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Perform a touch gesture. Use 'action' to choose: tap, double_tap, long_press, scroll, swipe, pinch_zoom, scroll_to_element, back. Choose scroll vs swipe by intent: scroll to browse content in a list or feed; swipe to dismiss, switch screens, navigate carousels, or pull-to-refresh (speed=fast). For drag-and-drop use appium_drag_and_drop. For custom multi-touch use appium_perform_actions.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "Gesture to perform. tap: tap an element or a coordinate. double_tap: trigger a double-tap action (e.g. zoom in on an image, favorite a post). long_press: press and hold to open a context menu or initiate drag. scroll: browse a list, feed, or page to reveal content. swipe: dismiss a card, switch screens or tabs, navigate a carousel, or pull-to-refresh (use speed=fast). pinch_zoom: zoom in (scale > 1) or out (scale < 1) on maps, images, or any zoomable view. scroll_to_element: scroll until a specific element is on screen (strategy + selector + direction up|down). Stops when the element is found, page source is unchanged after a scroll (end of scrollable content), or maxScrollAttempts is reached. Optional scrollDistance (0.05\u20131) or scrollDistancePreset (small|medium|large). back: triggers the system back navigation (e.g., Android back button or iOS navigation controller pop).",
      "enum": [
       "tap",
       "double_tap",
       "long_press",
       "scroll",
       "swipe",
       "pinch_zoom",
       "scroll_to_element",
       "back"
      ],
      "type": "string"
     },
     "direction": {
      "description": "Direction for scroll or swipe. Coordinates are auto-calculated from screen or element bounds. Either direction OR custom coordinates (x, y, endX, endY) must be provided for these actions.",
      "enum": [
       "up",
       "down",
       "left",
       "right"
      ],
      "type": "string"
     },
     "duration": {
      "description": "Duration in milliseconds. long_press default 2000 (range 500-10000). scroll default 800. For swipe, prefer the speed parameter; duration overrides it if both are provided.",
      "maximum": 10000,
      "minimum": 0,
      "type": "integer"
     },
     "elementUUID": {
      "description": "UUID of the element to act on. Used by: tap, double_tap, long_press, pinch_zoom. For scroll/swipe, when provided with direction, the gesture is calculated relative to this element instead of the whole screen.",
      "type": "string"
     },
     "endX": {
      "description": "Ending X coordinate. Used by: scroll, swipe (custom-coordinate mode).",
      "maximum": 9007199254740991,
      "minimum": 0,
      "type": "integer"
     },
     "endY": {
      "description": "Ending Y coordinate. Used by: scroll, swipe (custom-coordinate mode).",
      "maximum": 9007199254740991,
      "minimum": 0,
      "type": "integer"
     },
     "maxScrollAttempts": {
      "default": 10,
      "description": "scroll_to_element only: maximum scroll attempts after the element is not yet visible (default 10).",
      "maximum": 80,
      "minimum": 1,
      "type": "integer"
     },
     "scale": {
      "description": "Pinch scale factor. < 1 = zoom out (pinch close), > 1 = zoom in (pinch open). Example: 0.5 = zoom out 50%, 2.0 = zoom in 2x. Required for: pinch_zoom.",
      "maximum": 10,
      "minimum": 0.01,
      "type": "number"
     },
     "scrollDistance": {
      "description": "scroll_to_element only: vertical swipe length as a fraction 0.05\u20131 (same scale as legacy scroll). Ignored when scrollDistancePreset is set. Default 0.45 if neither preset nor scrollDistance is set.",
      "maximum": 1,
      "minimum": 0.05,
      "type": "number"
     },
     "scrollDistancePreset": {
      "description": "scroll_to_element only: convenience preset \u2014 small \u2248 light nudge (0.25), medium \u2248 0.45, large = full default swipe (1). When set, overrides scrollDistance.",
      "enum": [
       "small",
       "medium",
       "large"
      ],
      "type": "string"
     },
     "selector": {
      "description": "Locator selector value. Required for: scroll_to_element.",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     },
     "speed": {
      "description": "Swipe speed. slow = deliberate drag; normal = default navigation speed; fast = flick with no hold, use for pull-to-refresh and other velocity-sensitive UIs. Used by: swipe.",
      "enum": [
       "slow",
       "normal",
       "fast"
      ],
      "type": "string"
     },
     "strategy": {
      "description": "Locator strategy. Required for: scroll_to_element. Priority: accessibility id > id > platform-native (-ios predicate string / -ios class chain on iOS, -android uiautomator on Android) > xpath (LAST RESORT \u2014 slow on iOS XCUITest, brittle) > name > class name > css selector (webview only). Same ranking as appium_find_element.",
      "enum": [
       "accessibility id",
       "id",
       "-ios predicate string",
       "-ios class chain",
       "-android uiautomator",
       "xpath",
       "name",
       "class name",
       "css selector"
      ],
      "type": "string"
     },
     "velocity": {
      "description": "Pinch velocity in scale factor per second. Default 2.2. Used by: pinch_zoom.",
      "maximum": 20,
      "minimum": 0.1,
      "type": "number"
     },
     "x": {
      "description": "X coordinate. For tap/double_tap/long_press: tap location (alternative to elementUUID). For scroll/swipe: starting X for custom-coordinate mode (requires y, endX, endY). For pinch_zoom: center X of the pinch. Requires y. Ignored if elementUUID is set.",
      "maximum": 9007199254740991,
      "minimum": 0,
      "type": "integer"
     },
     "y": {
      "description": "Y coordinate. For tap/double_tap/long_press: tap location. For scroll/swipe: starting Y for custom-coordinate mode. For pinch_zoom: center Y of the pinch. Requires x. Ignored if elementUUID is set.",
      "maximum": 9007199254740991,
      "minimum": 0,
      "type": "integer"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_gesture"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Get the currently active/focused element and return its UUID for follow-up interactions. [PRIORITY 1: Use this first when you need to find what element currently has focus]",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "appium_get_active_element"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Get the value of an element attribute. Use this to check element state (enabled, selected, checked, focused, displayed, clickable) or read properties (name, value, label, content-desc, resource-id, class).",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "attribute": {
      "description": "The attribute name to retrieve. Common attributes: \"enabled\", \"selected\", \"displayed\", \"checked\", \"focused\", \"clickable\", \"scrollable\", \"focusable\", \"name\", \"value\", \"label\", \"text\", \"content-desc\", \"resource-id\", \"class\", \"package\".",
      "type": "string"
     },
     "elementUUID": {
      "description": "Element ID from appium_find_element.",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "elementUUID",
     "attribute"
    ],
    "type": "object"
   },
   "name": "appium_get_element_attribute"
  },
  {
   "_meta": {
    "ui": {
     "resourceUri": "ui://appium-mcp/page-source-inspector"
    }
   },
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Get the page source (XML) from the current screen",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "appium_get_page_source"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Get text from an element",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "elementUUID": {
      "description": "Element ID from appium_find_element.",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "elementUUID"
    ],
    "type": "object"
   },
   "name": "appium_get_text"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Get the width and height of the device screen in pixels. Useful for calculating coordinates for swipes, taps, and scrolls.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "appium_get_window_size"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Read or set the device clipboard as plain text (Android UiAutomator2 / iOS XCUITest). action=get returns current text; action=set requires content.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "get: read device clipboard as plain text. set: write plain text to the clipboard.",
      "enum": [
       "get",
       "set"
      ],
      "type": "string"
     },
     "content": {
      "description": "Required when action is set. Plain text to put on the clipboard.",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_mobile_clipboard"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Control device behavior: lock/unlock the screen, shake the device, or open the notifications panel. Use the action parameter to choose what to do.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "Action to perform. lock: lock the device (optional seconds for timed lock). unlock: unlock the device. shake: perform shake gesture (iOS only). open_notifications: open notifications panel (Android only).",
      "enum": [
       "lock",
       "unlock",
       "shake",
       "open_notifications"
      ],
      "type": "string"
     },
     "seconds": {
      "description": "Only for action=lock: lock duration in seconds before auto-unlock. Omit to remain locked until unlock.",
      "maximum": 9007199254740991,
      "minimum": 1,
      "type": "integer"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_mobile_device_control"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Get device information, battery status, or current device time in a single call. Use the \"action\" parameter to select which data to retrieve. Works on both iOS and Android.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "Action to perform: \"info\" returns device model/OS/locale/etc., \"battery\" returns battery level and charging state, \"time\" returns the current device time.",
      "enum": [
       "info",
       "battery",
       "time"
      ],
      "type": "string"
     },
     "format": {
      "description": "Only used when action is \"time\". moment.js format string for the returned time. Defaults to ISO 8601 (YYYY-MM-DDTHH:mm:ssZ).",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_mobile_device_info"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Push or pull a file using Appium mobile extensions. action=push uses payloadBase64, action=pull returns contentBase64.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "push uploads a file to device; pull downloads from device.",
      "enum": [
       "push",
       "pull"
      ],
      "type": "string"
     },
     "payloadBase64": {
      "description": "Required when action=push. Ignored when action=pull.",
      "type": "string"
     },
     "remotePath": {
      "description": "Path to the file on the device. Android (UiAutomator2): use an absolute path (e.g. /data/local/tmp/foo.txt or /sdcard/Download/foo.txt). iOS (XCUITest): use the formats described in the Appium XCUITest file transfer guide (e.g. @com.example.app:documents/file.txt or simulator-relative paths).",
      "minLength": 1,
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "action",
     "remotePath"
    ],
    "type": "object"
   },
   "name": "appium_mobile_file"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Hide the software keyboard or check if it is visible (Android UiAutomator2 / iOS XCUITest). action=hide uses mobile: hideKeyboard; action=is_shown uses mobile: isKeyboardShown.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "hide: dismiss the software keyboard (mobile: hideKeyboard). is_shown: whether the keyboard is visible (mobile: isKeyboardShown).",
      "enum": [
       "hide",
       "is_shown"
      ],
      "type": "string"
     },
     "keys": {
      "description": "hide only: optional key names to dismiss the keyboard (e.g. \"done\"). Forwarded to mobile: hideKeyboard when non-empty. Ignored for is_shown.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_mobile_keyboard"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Get/update Android app permissions or iOS Simulator privacy services; reset iOS privacy prompts. See action-specific parameters.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "access": {
      "additionalProperties": false,
      "description": "iOS update only: map of access rule \u2192 yes|no|unset|limited (Simulator + AppleSimulatorUtils). Required for iOS update.",
      "propertyNames": {
       "type": "string"
      },
      "type": "object"
     },
     "action": {
      "description": "get: list (Android) or read one privacy state (iOS Simulator). update: grant/revoke (Android) or set privacy map (iOS Simulator). reset: restore a privacy prompt for the app under test (iOS only).",
      "enum": [
       "get",
       "update",
       "reset"
      ],
      "type": "string"
     },
     "id": {
      "description": "App ID; overrides name. Android defaults to the app under test; required for iOS get/update.",
      "type": "string"
     },
     "name": {
      "description": "App name resolved to an ID. Android defaults to the app under test; alternative to id for iOS get/update.",
      "type": "string"
     },
     "permissionChangeAction": {
      "description": "Android update: for pm target grant (default) or revoke; for appops allow, deny, ignore, default.",
      "type": "string"
     },
     "permissionFilter": {
      "description": "Android get only: which bucket to return. Defaults to requested per UiAutomator2.",
      "enum": [
       "denied",
       "granted",
       "requested"
      ],
      "type": "string"
     },
     "permissions": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "items": {
         "type": "string"
        },
        "type": "array"
       }
      ],
      "description": "Android update only: permission name(s), `all` (with pm target), or appops names. Required for Android update."
     },
     "service": {
      "description": "iOS get: privacy service name (e.g. camera, microphone, photos). iOS reset: service name or numeric XCUIProtectedResource id.",
      "type": [
       "string",
       "number"
      ]
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     },
     "target": {
      "description": "Android update: pm (default) or appops.",
      "enum": [
       "pm",
       "appops"
      ],
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_mobile_permissions"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Press navigation keys (BACK, HOME, APP_SWITCH) on Android or physical buttons (HOME, volume, etc.) on iOS/tvOS.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "isLongPress": {
      "description": "Android only. Whether to perform a long press. Defaults to false.",
      "type": "boolean"
     },
     "key": {
      "description": "Logical key/button to press. On Android: BACK, HOME, APP_SWITCH. On iOS/tvOS: HOME, VOLUME_UP, VOLUME_DOWN, UP, DOWN, LEFT, RIGHT, MENU, PLAY_PAUSE, SELECT.",
      "enum": [
       "BACK",
       "HOME",
       "APP_SWITCH",
       "VOLUME_UP",
       "VOLUME_DOWN",
       "UP",
       "DOWN",
       "LEFT",
       "RIGHT",
       "MENU",
       "PLAY_PAUSE",
       "SELECT"
      ],
      "type": "string"
     },
     "keyCode": {
      "description": "Android keycode to press. If provided, takes precedence over key for Android.",
      "maximum": 9007199254740991,
      "minimum": -9007199254740991,
      "type": "integer"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "appium_mobile_press_key"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Get or set the device/screen orientation. Supports action=get and action=set (LANDSCAPE or PORTRAIT).",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "Use get to read current orientation, set to change orientation.",
      "enum": [
       "get",
       "set"
      ],
      "type": "string"
     },
     "orientation": {
      "description": "Required when action is set.",
      "enum": [
       "LANDSCAPE",
       "PORTRAIT"
      ],
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_orientation"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Execute raw W3C Actions API sequences for advanced multi-touch gestures not covered by appium_gesture. Use this for custom multi-finger gestures (rotate, three-finger swipe, edge swipes), complex timing sequences, or any gesture requiring precise control over individual touch points. Prefer appium_gesture for standard gestures (tap, scroll, swipe, pinch) \u2014 it handles platform differences automatically.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "actions": {
      "description": "W3C Actions API input source array. Each entry is one input source (pointer/key/none) with its action sequence. Multiple pointer sources enable multi-touch gestures (e.g. two-finger rotate, three-finger swipe). All sources execute in parallel, synchronized tick-by-tick.",
      "items": {
       "properties": {
        "actions": {
         "description": "Ordered list of action steps for this input source.",
         "items": {
          "properties": {
           "button": {
            "description": "Button index (pointerDown/pointerUp). 0 = primary.",
            "maximum": 9007199254740991,
            "minimum": -9007199254740991,
            "type": "integer"
           },
           "duration": {
            "description": "Duration in milliseconds. Required for pause and pointerMove.",
            "maximum": 9007199254740991,
            "minimum": 0,
            "type": "integer"
           },
           "origin": {
            "description": "Coordinate origin: \"viewport\" (default) or \"pointer\" (relative to current position).",
            "type": "string"
           },
           "type": {
            "description": "Action step type.",
            "enum": [
             "pointerMove",
             "pointerDown",
             "pointerUp",
             "pointerCancel",
             "keyDown",
             "keyUp",
             "pause"
            ],
            "type": "string"
           },
           "value": {
            "description": "Key value (keyDown/keyUp only).",
            "type": "string"
           },
           "x": {
            "description": "X coordinate (pointerMove only).",
            "maximum": 9007199254740991,
            "minimum": -9007199254740991,
            "type": "integer"
           },
           "y": {
            "description": "Y coordinate (pointerMove only).",
            "maximum": 9007199254740991,
            "minimum": -9007199254740991,
            "type": "integer"
           }
          },
          "required": [
           "type"
          ],
          "type": "object"
         },
         "type": "array"
        },
        "id": {
         "description": "Unique identifier for this input source (e.g. \"finger1\", \"finger2\").",
         "type": "string"
        },
        "parameters": {
         "properties": {
          "pointerType": {
           "description": "Pointer type. Use \"touch\" for mobile gestures. Only for type=pointer.",
           "enum": [
            "touch",
            "mouse",
            "pen"
           ],
           "type": "string"
          }
         },
         "type": "object"
        },
        "type": {
         "description": "Input source type. pointer = touch/mouse, key = keyboard, none = pause/timing.",
         "enum": [
          "pointer",
          "key",
          "none"
         ],
         "type": "string"
        }
       },
       "required": [
        "type",
        "id",
        "actions"
       ],
       "type": "object"
      },
      "minItems": 1,
      "type": "array"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "required": [
     "actions"
    ],
    "type": "object"
   },
   "name": "appium_perform_actions"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Prepare an iOS real device for Appium testing in a single call. Two-mode flow: (1) Call without provisioningProfileUuid to receive the list of available .mobileprovision profiles \u2014 present them to the user (highlight any with recommendedForWda=true) and ask them to pick one. (2) Call again with the chosen UUID to download the matching WebDriverAgent release, package it as an IPA, and resign it with the chosen profile (wildcard \"*\" profiles are supported \u2014 a concrete WDA bundle ID is substituted at sign time). WDA download and unsigned IPA are cached per WDA version; the signed IPA is rebuilt every call. Pass the returned capabilitiesHint to appium_session_management (action=create) so Appium installs and launches the signed prebuilt WDA instead of rebuilding. Requires macOS, Xcode 16+, and a paired developer-mode device.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "forceRebuild": {
      "description": "If true, ignore the cached WDA download and unsigned IPA and start clean. The signed IPA is always rebuilt regardless. Default: false.",
      "type": "boolean"
     },
     "provisioningProfileUuid": {
      "description": "UUID of the .mobileprovision profile to sign WDA with. If omitted, the tool returns the list of available profiles so you can ask the user to pick one.",
      "type": "string"
     },
     "udid": {
      "description": "UDID of the connected iOS real device. Use select_device to discover it.",
      "type": "string"
     }
    },
    "required": [
     "udid"
    ],
    "type": "object"
   },
   "name": "appium_prepare_ios_real_device"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Start or stop screen recording. action=start begins recording; action=stop stops and saves to MP4.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "start begins recording; stop ends, retrieves, and saves it.",
      "enum": [
       "start",
       "stop"
      ],
      "type": "string"
     },
     "bitRate": {
      "description": "Android only. Video bit rate in bits per second.",
      "maximum": 9007199254740991,
      "minimum": -9007199254740991,
      "type": "integer"
     },
     "bugReport": {
      "description": "Android only. Display timestamp overlay. Requires API level 27+.",
      "type": "boolean"
     },
     "forceRestart": {
      "description": "Restart and discard any active recording; default false.",
      "type": "boolean"
     },
     "hardwareAcceleration": {
      "description": "iOS only. FFMPEG hardware acceleration backend.",
      "enum": [
       "videoToolbox",
       "cuda",
       "amf_dx11",
       "qsv",
       "vaapi"
      ],
      "type": "string"
     },
     "pixelFormat": {
      "description": "iOS only. Output pixel format (e.g. yuv420p).",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     },
     "timeLimit": {
      "description": "Recorder limit in seconds; it does not retrieve/save automatically, so call stop. iOS default 180/max 4200; Android default 180/max 1800.",
      "maximum": 9007199254740991,
      "minimum": 1,
      "type": "integer"
     },
     "videoFilters": {
      "description": "iOS only. FFMPEG video filters. Takes precedence over videoScale.",
      "type": "string"
     },
     "videoFps": {
      "description": "iOS only. Frames per second. Default: 10.",
      "maximum": 60,
      "minimum": 1,
      "type": "integer"
     },
     "videoQuality": {
      "description": "iOS only. Video quality preset. Default: medium.",
      "enum": [
       "low",
       "medium",
       "high",
       "photo"
      ],
      "type": "string"
     },
     "videoScale": {
      "description": "iOS only. Scaling value (e.g. 1280:720).",
      "type": "string"
     },
     "videoSize": {
      "description": "Android only. Frame size in WIDTHxHEIGHT format (e.g. 1280x720).",
      "type": "string"
     },
     "videoType": {
      "description": "iOS only. Video codec to use (e.g. libx264).",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_screen_recording"
  },
  {
   "_meta": {
    "ui": {
     "resourceUri": "ui://appium-mcp/screenshot-viewer"
    }
   },
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Take a screenshot and save as PNG. Optionally provide elementUUID to capture only that element.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "elementUUID": {
      "description": "Optional element UUID. If provided, captures only this element. If omitted, captures full screen.",
      "type": "string"
     },
     "maxWidth": {
      "description": "Optional maximum width in pixels to resize the screenshot. The aspect ratio is preserved. Useful for reducing token usage when sending screenshots to LLMs.",
      "type": "number"
     },
     "returnRawBase64": {
      "default": false,
      "description": "When true, returns the raw base64-encoded PNG image instead of saving it to disk. This should only be enabled when a human explicitly invokes the tool manually, typically to view the screenshot on a different machine (e.g. when the server runs on a remote machine and the saved file is not accessible). An LLM must always keep this false and rely on the saved file path.",
      "type": "boolean"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "appium_screenshot"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Manage Appium sessions. Use action=create to start a session, attach to connect to an existing one, detach to forget an attached session, delete to stop one, list to see all active sessions, or select to switch the active session.",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "action": {
      "description": "Action to perform. create: Create a new Appium session with Android, iOS or any device/driver Appium supports.\n      DEFAULT MODE (no remoteServerUrl) \u2014 USE THIS UNLESS THE USER EXPLICITLY PROVIDES A SERVER URL:\n      - Drivers run embedded inside this MCP server; no separate Appium process is needed\n      - Use select_device tool FIRST to discover devices and let the user choose platform and device\n      - Then call action=create with the selected platform (do NOT pass remoteServerUrl)\n      - For iOS simulators, call prepare_ios_simulator before action=create\n      - DO NOT assume or default to any platform\n      - NEVER invent a localhost URL (e.g. http://localhost:4723) \u2014 omitting remoteServerUrl IS the local/embedded mode\n      REMOTE SERVER MODE (only when user explicitly provides a URL like http://localhost:4723):\n      - SKIP select_device tool entirely\n      - Infer the platform from the user's request (e.g., 'ios', 'android', or 'general')\n      - If platform is 'general', treat the provided capabilities as a pass-through W3C/Appium capability set (useful for non-Android/iOS drivers like Windows, macOS, or custom drivers)\n      - Infer device type from context when possible (e.g., 'simulator', 'real device')\n      - Call session with action=create directly with platform, remoteServerUrl, and any other capabilities from the user's request\n      - Example: User says 'start session with http://localhost:4723 for ios with iphone 17' \u2192 infer platform='ios' and call session(action=create) with remoteServerUrl and platform parametersattach: Attach MCP Appium to an existing remote Appium session without taking ownership of its lifecycle. Requires remoteServerUrl and sessionId. Always pass capabilities with at least platformName (e.g. '{\"platformName\":\"iOS\"}' or '{\"platformName\":\"Android\"}') so the client is configured with the correct protocol commands.detach: Remove an attached Appium session from MCP Appium without deleting the real remote session. Defaults to the active session.delete: Delete a mobile session and clean up resources. If sessionId is omitted, deletes the active session.list: List all active Appium sessions managed by this MCP server, including active flag, ownership, and current context.select: Set an existing Appium session as the active session for subsequent tool calls (requires sessionId).",
      "enum": [
       "create",
       "attach",
       "detach",
       "delete",
       "list",
       "select"
      ],
      "type": "string"
     },
     "capabilities": {
      "description": "Optional W3C capabilities for create. Provide as a JSON string (e.g. '{\"appium:app\":\"/path/to/app\",\"appium:platformVersion\":\"17.0\"}'). For create: applied on top of defaults for ios/android, or used as-is for general. Common: appium:app, appium:deviceName, appium:platformVersion, appium:bundleId. When passing from a capabilitiesHint result, serialize the full object to JSON \u2014 do NOT drop boolean or numeric values. For attach: always include platformName (\"iOS\" or \"Android\") so the WebDriver client loads the correct Appium protocol commands (e.g. '{\"platformName\":\"iOS\"}').",
      "type": "string"
     },
     "platform": {
      "description": "Required for create. For local servers, must match the platform selected via select_device. Use \"general\" for non-Android/iOS drivers (Windows, macOS, custom). For remote servers, infer from context.",
      "enum": [
       "ios",
       "android",
       "general"
      ],
      "type": "string"
     },
     "remoteServerUrl": {
      "description": "Remote Appium server URL for create or attach (e.g. http://localhost:4723). Omit to use local server for create.",
      "type": "string"
     },
     "sessionId": {
      "description": "For attach: existing session to connect to. For delete: session to remove (defaults to active). For detach: attached session to remove from MCP (defaults to active). For select: session to activate. Required for attach and select.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "appium_session_management"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Enter text into an element",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "elementUUID": {
      "description": "Element ID from appium_find_element.",
      "type": "string"
     },
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     },
     "text": {
      "description": "The text to enter",
      "type": "string"
     },
     "w3cActions": {
      "description": "When true, type text via the W3C Actions API (performActions) instead of the driver-specific setValue. No elementUUID needed \u2014 key events are sent to whatever element currently has focus. Works on both Android and iOS.",
      "type": "boolean"
     }
    },
    "required": [
     "text"
    ],
    "type": "object"
   },
   "name": "appium_set_value"
  },
  {
   "_meta": {
    "ui": {
     "resourceUri": "ui://appium-mcp/locator-generator"
    }
   },
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Generate locators for all interactable elements on the current page. [PRIORITY 3: Use this for debugging/inspection or when you need comprehensive element info with locator suggestions]",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "sessionId": {
      "description": "Session ID to target. If omitted, uses the active session.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "generate_locators"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Prepare an iOS/tvOS simulator for Appium testing in a single call. Automatically boots the simulator, downloads prebuilt WDA (if not cached), and installs/launches WDA on a free per-simulator port (so multiple simulators can run in parallel without colliding on the default 8100). Pass the returned capabilitiesHint (appium:webDriverAgentUrl) to appium_session_management (action=create) so the session reuses this running WDA instead of trying to start its own. Use skipWda=true to only boot without WDA. Set APPIUM_MCP_WDA_APP_PATH to an absolute path to a pre-extracted WebDriverAgentRunner-Runner.app to skip download entirely (useful in environments where external downloads are blocked).",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "forceRefreshWda": {
      "description": "If true, re-download WDA even if already cached. Default: false.",
      "type": "boolean"
     },
     "platform": {
      "default": "ios",
      "description": "The simulator platform to download WDA for. Default is \"ios\". Use \"tvos\" for Apple TV simulators.",
      "enum": [
       "ios",
       "tvos"
      ],
      "type": "string"
     },
     "skipWda": {
      "description": "If true, only boot the simulator without downloading or installing WDA. Default: false.",
      "type": "boolean"
     },
     "udid": {
      "description": "The UDID of the iOS simulator to prepare. Use select_device to get this.",
      "type": "string"
     }
    },
    "required": [
     "udid"
    ],
    "type": "object"
   },
   "name": "prepare_ios_simulator"
  },
  {
   "annotations": {
    "openWorldHint": false,
    "readOnlyHint": false
   },
   "description": "Discover and select a device for LOCAL Appium servers ONLY.\n      DO NOT use this tool for REMOTE Appium servers - remoteServerUrl indicates a remote server.\n      WORKFLOW FOR LOCAL SERVERS:\n      1. ASK THE USER which platform they want (Android or iOS) - do not assume\n      2. Call this tool with the chosen platform (and iosDeviceType for iOS)\n      3. If only one device is found, it is auto-selected - proceed to appium_session_management (action=create) (or prepare_ios_simulator for iOS simulators)\n      4. If multiple devices are found, ask the user which one they want, then call this tool again with deviceUdid\n      5. After selection, proceed to appium_session_management (action=create) (or prepare_ios_simulator for iOS simulators, then appium_session_management with action=create)\n      WORKFLOW FOR REMOTE SERVERS:\n      - SKIP this tool entirely\n      - Device selection should be handled via capabilities on appium_session_management (action=create) (e.g., appium:deviceName, appium:udid)\n      - The remote Appium server is already configured for specific device(s)\n      ",
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "deviceUdid": {
      "description": "The UDID of the device selected by the user. If not provided, this tool will list available devices for the user to choose from.",
      "type": "string"
     },
     "iosDeviceType": {
      "description": "For iOS only: Specify whether to use 'simulator' or 'real' device. REQUIRED when platform is 'ios'.",
      "enum": [
       "simulator",
       "real"
      ],
      "type": "string"
     },
     "platform": {
      "description": "The platform to list devices for (must match previously selected platform)",
      "enum": [
       "ios",
       "android"
      ],
      "type": "string"
     }
    },
    "required": [
     "platform"
    ],
    "type": "object"
   },
   "name": "select_device"
  }
 ],
 "version": "1.92.14"
}