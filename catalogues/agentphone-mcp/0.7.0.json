{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "agentphone-mcp",
 "protocol": "2025-06-18",
 "published": "2026-08-25T08:17:58.578Z",
 "tools": [
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "Get a complete snapshot of your AgentPhone account: agents, phone numbers, webhook status, and usage limits.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {},
    "type": "object"
   },
   "name": "account_overview"
  },
  {
   "annotations": {
    "idempotentHint": true
   },
   "description": "Attach a phone number to an agent so the agent handles calls/SMS on that number. Takes an agent ID and a phone number ID.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "The agent ID",
      "type": "string"
     },
     "number_id": {
      "description": "The phone number ID to attach",
      "type": "string"
     }
    },
    "required": [
     "agent_id",
     "number_id"
    ],
    "type": "object"
   },
   "name": "attach_number"
  },
  {
   "annotations": {
    "openWorldHint": true
   },
   "description": "Purchase a new phone number. This is a paid purchase that charges the account's balance. area_code requests a specific region (e.g. '415' for San Francisco); agent_id attaches it on purchase. Requires confirm=true to proceed.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "Agent ID to attach this number to immediately",
      "type": "string"
     },
     "area_code": {
      "description": "3-digit area code for a specific region (e.g. '415', '212', '310')",
      "maxLength": 3,
      "minLength": 3,
      "type": "string"
     },
     "confirm": {
      "default": false,
      "description": "Must be true to complete the purchase. When false, no number is bought.",
      "type": "boolean"
     },
     "country": {
      "default": "US",
      "description": "2-letter ISO country code (e.g. US, CA, GB)",
      "maxLength": 2,
      "minLength": 2,
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "buy_number"
  },
  {
   "description": "Create a new agent. An agent owns phone numbers and handles calls/SMS.\n\nA newly created agent has no phone number until one is attached.\nSet voice_mode to 'hosted' with a system_prompt for autonomous AI voice calls, or 'webhook' (default) to forward call transcripts to your webhook URL.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "ambient_sound": {
      "description": "Optional background ambience for call audio. Not a substitute for the AI self-identification, which is always disclosed.",
      "enum": [
       "none",
       "office",
       "coffee-shop",
       "outdoor"
      ],
      "type": "string"
     },
     "begin_message": {
      "description": "What the AI says when a call connects. Only used in 'hosted' mode.",
      "type": "string"
     },
     "denoising_mode": {
      "description": "Audio denoising. The aggressive mode helps callers in cars/cafes (small surcharge).",
      "enum": [
       "noise-cancellation",
       "noise-and-background-speech-cancellation"
      ],
      "type": "string"
     },
     "description": {
      "description": "Description of what this agent does",
      "type": "string"
     },
     "enable_backchannel": {
      "description": "Whether the agent interjects 'uh-huh'/'mhmm' while the caller speaks. Default true.",
      "type": "boolean"
     },
     "enable_messaging": {
      "description": "Whether a hosted agent can send and read texts during a call. Default true.",
      "type": "boolean"
     },
     "interruption_sensitivity": {
      "description": "How easily callers can interrupt (barge in). 0 = never, 1 = at first sound. Default 0.8.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "language": {
      "description": "BCP-47 locale for speech recognition and synthesis, e.g. 'en-US', 'es-ES', 'ja-JP'.",
      "type": "string"
     },
     "max_silence_ms": {
      "description": "Hang up after this many ms of caller silence. Default 600000 (10 min).",
      "maximum": 3600000,
      "minimum": 10000,
      "type": "number"
     },
     "model_tier": {
      "description": "Model quality/speed tier for hosted-mode agents. 'turbo' = fastest/cheapest, 'balanced' (default) = general use, 'max' = highest quality.",
      "enum": [
       "turbo",
       "balanced",
       "max"
      ],
      "type": "string"
     },
     "name": {
      "description": "Name for the agent (e.g. 'Customer Support', 'Sales Bot')",
      "type": "string"
     },
     "stt_mode": {
      "description": "Speech-to-text mode: 'fast' (default) lowest latency, 'accurate' for exact names/numbers (~200ms slower).",
      "enum": [
       "fast",
       "accurate"
      ],
      "type": "string"
     },
     "system_prompt": {
      "description": "Required when voice_mode is 'hosted'. The AI's personality and instructions for voice calls.",
      "type": "string"
     },
     "transfer_number": {
      "description": "Phone number to transfer calls to (E.164 format). Enables call transfer during conversations.",
      "type": "string"
     },
     "voice": {
      "description": "Voice ID for the agent. Defaults to 'Skylar - Friendly Guide'.",
      "type": "string"
     },
     "voice_mode": {
      "description": "'webhook' (default) forwards transcripts to your webhook. 'hosted' uses built-in AI with system_prompt.",
      "enum": [
       "webhook",
       "hosted"
      ],
      "type": "string"
     },
     "voice_speed": {
      "description": "Speech speed multiplier. 1.0 = normal, 0.5 = half speed, 2.0 = double.",
      "maximum": 2,
      "minimum": 0.5,
      "type": "number"
     },
     "voicemail_message": {
      "description": "Voicemail greeting text. When set, unanswered calls hear this message and can leave a voicemail.",
      "type": "string"
     }
    },
    "required": [
     "name"
    ],
    "type": "object"
   },
   "name": "create_agent"
  },
  {
   "annotations": {
    "destructiveHint": true
   },
   "description": "Delete an agent permanently. Phone numbers attached to it are kept but unassigned. This cannot be undone.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "The agent ID to delete",
      "type": "string"
     }
    },
    "required": [
     "agent_id"
    ],
    "type": "object"
   },
   "name": "delete_agent"
  },
  {
   "annotations": {
    "destructiveHint": true
   },
   "description": "Remove a webhook. Pass agent_id to remove an agent's webhook (falls back to project default). Omit agent_id to remove the project-level webhook.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "Agent ID to delete that agent's webhook. Omit for project-level.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "delete_webhook"
  },
  {
   "annotations": {
    "idempotentHint": true
   },
   "description": "Detach a phone number from an agent. The number is kept in your account but becomes unassigned.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "The agent ID that currently owns the number",
      "type": "string"
     },
     "number_id": {
      "description": "The phone number ID to detach",
      "type": "string"
     }
    },
    "required": [
     "agent_id",
     "number_id"
    ],
    "type": "object"
   },
   "name": "detach_number"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "Get details for a specific agent including its phone numbers, voice configuration, and system prompt.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "The agent ID",
      "type": "string"
     }
    },
    "required": [
     "agent_id"
    ],
    "type": "object"
   },
   "name": "get_agent"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "Get details and transcript for a specific call, identified by its call ID. Pass wait=true to block until an in-progress call finishes before returning.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "call_id": {
      "description": "The call ID",
      "type": "string"
     },
     "timeout": {
      "default": 120,
      "description": "Max seconds to wait when wait=true. Defaults to 120.",
      "maximum": 300,
      "minimum": 10,
      "type": "number"
     },
     "wait": {
      "default": false,
      "description": "When true, long-polls until the call completes before returning. Useful for checking back on a call you initiated earlier.",
      "type": "boolean"
     }
    },
    "required": [
     "call_id"
    ],
    "type": "object"
   },
   "name": "get_call"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "Get a specific SMS conversation with full message history, identified by its conversation ID.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "conversation_id": {
      "description": "The conversation ID",
      "type": "string"
     },
     "message_limit": {
      "default": 50,
      "description": "Max messages to include",
      "maximum": 100,
      "minimum": 1,
      "type": "number"
     }
    },
    "required": [
     "conversation_id"
    ],
    "type": "object"
   },
   "name": "get_conversation"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "Get SMS messages for a specific phone number, identified by its number ID. Returns individual messages rather than grouped conversation threads.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "limit": {
      "default": 50,
      "description": "Max messages to return",
      "maximum": 200,
      "minimum": 1,
      "type": "number"
     },
     "number_id": {
      "description": "The ID of the phone number",
      "type": "string"
     }
    },
    "required": [
     "number_id"
    ],
    "type": "object"
   },
   "name": "get_messages"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "Get account usage statistics. By default returns a summary with number usage plus message, call, and webhook volume. Use breakdown='daily' or 'monthly' for time-series data.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "breakdown": {
      "default": "summary",
      "description": "'summary' for number usage and totals, 'daily' for per-day breakdown, 'monthly' for per-month breakdown",
      "enum": [
       "summary",
       "daily",
       "monthly"
      ],
      "type": "string"
     },
     "days": {
      "default": 30,
      "description": "Number of days to look back (only used with breakdown='daily')",
      "maximum": 365,
      "minimum": 1,
      "type": "number"
     },
     "months": {
      "default": 12,
      "description": "Number of months to look back (only used with breakdown='monthly')",
      "maximum": 24,
      "minimum": 1,
      "type": "number"
     }
    },
    "type": "object"
   },
   "name": "get_usage"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "Get the webhook configuration. Pass agent_id to get an agent-specific webhook, or omit for the project-level default.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "Agent ID to get that agent's webhook. Omit for project-level webhook.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "get_webhook"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "List all agents with their phone numbers and voice configuration. An agent is required before you can make calls \u2014 it owns phone numbers and handles voice/SMS.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "limit": {
      "default": 20,
      "description": "Max results to return",
      "maximum": 100,
      "minimum": 1,
      "type": "number"
     }
    },
    "type": "object"
   },
   "name": "list_agents"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "List recent calls. Scope by agent_id or number_id, or use status/direction/search to filter globally.\n\nWhen agent_id or number_id is passed, status/direction/search filters are not applied.\nReturns call summaries including each call's ID, direction, participants, and status.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "Filter to calls for a specific agent",
      "type": "string"
     },
     "direction": {
      "description": "Filter by direction: 'inbound' or 'outbound'",
      "enum": [
       "inbound",
       "outbound"
      ],
      "type": "string"
     },
     "limit": {
      "default": 20,
      "description": "Max results to return",
      "maximum": 100,
      "minimum": 1,
      "type": "number"
     },
     "number_id": {
      "description": "Filter to calls for a specific phone number",
      "type": "string"
     },
     "offset": {
      "default": 0,
      "description": "Number of results to skip (for pagination)",
      "minimum": 0,
      "type": "number"
     },
     "search": {
      "description": "Search by phone number or keyword",
      "type": "string"
     },
     "status": {
      "description": "Filter by status: ringing, in-progress, completed, failed, busy, no-answer",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "list_calls"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "List saved contacts (your address book). Optionally filter with a search term that matches name or phone number.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "limit": {
      "default": 50,
      "description": "Max results to return",
      "maximum": 100,
      "minimum": 1,
      "type": "number"
     },
     "offset": {
      "default": 0,
      "description": "Number of results to skip (for pagination)",
      "minimum": 0,
      "type": "number"
     },
     "search": {
      "description": "Filter by name or phone number",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "list_contacts"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "List SMS conversations. Optionally filter by agent_id to see conversations for a specific agent.\n\nEach conversation is a thread between your number and an external contact, with its ID, participant, message count, and last-message preview.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "Filter to conversations for a specific agent",
      "type": "string"
     },
     "limit": {
      "default": 20,
      "description": "Max results to return",
      "maximum": 100,
      "minimum": 1,
      "type": "number"
     },
     "offset": {
      "default": 0,
      "description": "Number of results to skip (for pagination)",
      "minimum": 0,
      "type": "number"
     }
    },
    "type": "object"
   },
   "name": "list_conversations"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "List all phone numbers in your account, each with its ID, status, country, and assigned agent.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "limit": {
      "default": 20,
      "description": "Max results to return",
      "maximum": 100,
      "minimum": 1,
      "type": "number"
     },
     "offset": {
      "default": 0,
      "description": "Number of results to skip (for pagination)",
      "minimum": 0,
      "type": "number"
     }
    },
    "type": "object"
   },
   "name": "list_numbers"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "List available voices for agents. Each voice has a voice_id used in an agent's voice field.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {},
    "type": "object"
   },
   "name": "list_voices"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true
   },
   "description": "View recent webhook delivery history. Shows which events were delivered, HTTP status codes, and timing.\n\nPass agent_id to see deliveries for that agent's webhook. Omit for project-level.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "Agent ID to see deliveries for that agent's webhook. Omit for project-level.",
      "type": "string"
     },
     "hours": {
      "description": "Only show deliveries from the last N hours",
      "minimum": 1,
      "type": "number"
     },
     "limit": {
      "default": 20,
      "description": "Max results to return",
      "maximum": 100,
      "minimum": 1,
      "type": "number"
     }
    },
    "type": "object"
   },
   "name": "list_webhook_deliveries"
  },
  {
   "annotations": {
    "openWorldHint": true
   },
   "description": "Initiate an outbound, webhook-driven phone call where your backend handles the conversation logic.\n\nThe agent must have a phone number attached and a webhook configured. The call always opens by identifying itself as an AI assistant (added automatically).",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "The agent ID (the agent must have a phone number attached)",
      "type": "string"
     },
     "from_number_id": {
      "description": "Specific phone number ID to call from (if agent has multiple numbers)",
      "type": "string"
     },
     "initial_greeting": {
      "description": "What the agent says when the call connects",
      "type": "string"
     },
     "to_number": {
      "description": "Recipient phone number in E.164 format (e.g. +14155551234)",
      "type": "string"
     },
     "voice": {
      "description": "Voice ID override for this call",
      "type": "string"
     }
    },
    "required": [
     "agent_id",
     "to_number"
    ],
    "type": "object"
   },
   "name": "make_call"
  },
  {
   "annotations": {
    "openWorldHint": true
   },
   "description": "Place a phone call where the AI has an autonomous conversation about a given topic \u2014 scheduling, surveys, follow-ups, etc. No webhook setup needed.\n\nThe agent must have a phone number attached.\nEvery call opens by identifying itself as an AI assistant (added automatically and cannot be disabled via topic or initial_greeting).\nBy default this blocks until the call finishes and returns the full transcript. Set wait=false for fire-and-forget.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "The agent ID (must have a phone number attached)",
      "type": "string"
     },
     "from_number_id": {
      "description": "Specific phone number ID to call from (if agent has multiple numbers)",
      "type": "string"
     },
     "initial_greeting": {
      "description": "What the AI says when the call connects. If not set, the AI will generate one from the topic.",
      "type": "string"
     },
     "max_wait_seconds": {
      "default": 300,
      "description": "Maximum seconds to wait for the call to complete. Defaults to 300 (5 minutes).",
      "maximum": 600,
      "minimum": 10,
      "type": "number"
     },
     "to_number": {
      "description": "Recipient phone number in E.164 format (e.g. +14155551234)",
      "type": "string"
     },
     "topic": {
      "description": "The conversation goal for the AI to pursue. Embedded as the call objective inside a locked system prompt that always enforces AI self-identification (identity rules can't be overridden). Be specific about what to discuss and any goals.",
      "type": "string"
     },
     "voice": {
      "description": "Voice ID override for this call",
      "type": "string"
     },
     "wait": {
      "default": true,
      "description": "When true (default), blocks until the call ends and returns the full transcript.",
      "type": "boolean"
     }
    },
    "required": [
     "agent_id",
     "to_number",
     "topic"
    ],
    "type": "object"
   },
   "name": "make_conversation_call"
  },
  {
   "annotations": {
    "destructiveHint": true
   },
   "description": "Create, update, or delete a saved contact. Set `action` to choose the operation.\n\n- create: requires phone_number and name\n- update: requires contact_id; only the fields you pass are changed\n- delete: requires contact_id (permanent)\n\ncontact_id refers to a saved contact.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "action": {
      "description": "The operation to perform",
      "enum": [
       "create",
       "update",
       "delete"
      ],
      "type": "string"
     },
     "contact_id": {
      "description": "Contact ID. Required for 'update' and 'delete'.",
      "type": "string"
     },
     "email": {
      "description": "Contact email address",
      "format": "email",
      "pattern": "^(?:[A-Za-z0-9_'+\\-]+\\.)*[A-Za-z0-9_'+\\-]*[A-Za-z0-9_+-]@(?:[A-Za-z0-9][A-Za-z0-9\\-]*\\.)+[A-Za-z]{2,}$",
      "type": "string"
     },
     "name": {
      "description": "Contact name. Required for 'create'.",
      "type": "string"
     },
     "notes": {
      "description": "Freeform notes about the contact",
      "type": "string"
     },
     "phone_number": {
      "description": "Phone number in E.164 format (e.g. +14155551234). Required for 'create'.",
      "type": "string"
     }
    },
    "required": [
     "action"
    ],
    "type": "object"
   },
   "name": "manage_contact"
  },
  {
   "annotations": {
    "openWorldHint": true
   },
   "description": "Send an SMS or iMessage from one of your agent's phone numbers, or react to a message.\n\nThe agent must have at least one phone number attached. If the agent has multiple numbers, number_id or from_number selects which one to send from.\niMessage extras (silently ignored on SMS): reply_to_message_id threads the reply under an earlier message, and send_style adds an expressive screen/bubble effect.\nTo react to a message instead of sending one, set reaction and react_to_message_id (iMessage only); to_number and body are not needed in that case.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "The agent ID to send from (the agent must have a phone number attached). Optional if you pass from_number or number_id instead.",
      "type": "string"
     },
     "body": {
      "default": "",
      "description": "The message text to send (may be empty when sending media only)",
      "type": "string"
     },
     "from_number": {
      "description": "Exact number to send from in E.164 format (alternative to number_id)",
      "type": "string"
     },
     "media_url": {
      "description": "URL of a single image/media file to attach",
      "format": "uri",
      "type": "string"
     },
     "media_urls": {
      "description": "Multiple media URLs to attach (delivered as an image carousel on iMessage)",
      "items": {
       "format": "uri",
       "type": "string"
      },
      "type": "array"
     },
     "number_id": {
      "description": "Specific phone number ID to send from (if agent has multiple numbers)",
      "type": "string"
     },
     "react_to_message_id": {
      "description": "iMessage only. ID of a message to react to. Set together with `reaction`.",
      "type": "string"
     },
     "reaction": {
      "description": "iMessage only. Tapback or single emoji applied to react_to_message_id: one of love, like, dislike, laugh, emphasize, question, or a single emoji (e.g. '\ud83d\udd25'). When set, this reacts instead of sending a message.",
      "type": "string"
     },
     "reply_to_message_id": {
      "description": "iMessage only. ID of an earlier message to reply to inline (threaded reply)",
      "type": "string"
     },
     "send_style": {
      "description": "iMessage only. Expressive screen/bubble effect to send with the message",
      "enum": [
       "celebration",
       "fireworks",
       "lasers",
       "love",
       "confetti",
       "balloons",
       "spotlight",
       "echo",
       "invisible",
       "gentle",
       "loud",
       "slam"
      ],
      "type": "string"
     },
     "to_number": {
      "description": "Recipient: a phone number in E.164 format (e.g. +14155551234), a US short code, or a group ID (grp_...) to post into an iMessage group chat. Required when sending a message (not needed for a reaction).",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "send_message"
  },
  {
   "annotations": {
    "idempotentHint": true
   },
   "description": "Set a webhook URL to receive inbound messages and call events.\n\nPass agent_id to set a webhook for a specific agent (overrides project default). Omit agent_id to set the project-level webhook for all agents.\nThe webhook secret is returned \u2014 use it to verify signatures.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "Agent ID to set webhook for that agent only. Omit for project-level.",
      "type": "string"
     },
     "context_limit": {
      "description": "Number of recent messages to include as conversation context (0-50)",
      "maximum": 50,
      "minimum": 0,
      "type": "number"
     },
     "timeout": {
      "description": "Webhook response timeout in seconds",
      "type": "number"
     },
     "url": {
      "description": "The publicly accessible webhook URL (must be HTTPS in production)",
      "format": "uri",
      "type": "string"
     }
    },
    "required": [
     "url"
    ],
    "type": "object"
   },
   "name": "set_webhook"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": true
   },
   "description": "Send a test event to verify a webhook is working. Returns the HTTP status code and response time.\n\nPass agent_id to test that agent's webhook. Omit to test the project-level webhook.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "Agent ID to test that agent's webhook. Omit for project-level.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "test_webhook"
  },
  {
   "annotations": {
    "idempotentHint": true
   },
   "description": "Update an agent's configuration \u2014 name, description, voice settings, system prompt, greeting, call transfer, or voicemail. Only provided fields are updated.\nSwitching voice_mode to 'hosted' requires a system_prompt.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "agent_id": {
      "description": "The agent ID to update",
      "type": "string"
     },
     "ambient_sound": {
      "description": "Optional background ambience for call audio. Not a substitute for the AI self-identification, which is always disclosed.",
      "enum": [
       "none",
       "office",
       "coffee-shop",
       "outdoor"
      ],
      "type": "string"
     },
     "begin_message": {
      "description": "What the AI says when a call connects (hosted mode only).",
      "type": "string"
     },
     "denoising_mode": {
      "description": "Audio denoising. The aggressive mode helps callers in cars/cafes (small surcharge).",
      "enum": [
       "noise-cancellation",
       "noise-and-background-speech-cancellation"
      ],
      "type": "string"
     },
     "description": {
      "description": "New description",
      "type": "string"
     },
     "enable_backchannel": {
      "description": "Whether the agent interjects 'uh-huh'/'mhmm' while the caller speaks. Default true.",
      "type": "boolean"
     },
     "enable_messaging": {
      "description": "Whether a hosted agent can send and read texts during a call. Default true.",
      "type": "boolean"
     },
     "interruption_sensitivity": {
      "description": "How easily callers can interrupt (barge in). 0 = never, 1 = at first sound. Default 0.8.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "language": {
      "description": "BCP-47 locale for speech recognition and synthesis, e.g. 'en-US', 'es-ES', 'ja-JP'.",
      "type": "string"
     },
     "max_silence_ms": {
      "description": "Hang up after this many ms of caller silence. Default 600000 (10 min).",
      "maximum": 3600000,
      "minimum": 10000,
      "type": "number"
     },
     "model_tier": {
      "description": "Model quality/speed tier for hosted-mode agents. 'turbo' = fastest/cheapest, 'balanced' = general use, 'max' = highest quality.",
      "enum": [
       "turbo",
       "balanced",
       "max"
      ],
      "type": "string"
     },
     "name": {
      "description": "New name for the agent",
      "type": "string"
     },
     "stt_mode": {
      "description": "Speech-to-text mode: 'fast' lowest latency, 'accurate' for exact names/numbers (~200ms slower).",
      "enum": [
       "fast",
       "accurate"
      ],
      "type": "string"
     },
     "system_prompt": {
      "description": "The AI's personality and instructions. Required when voice_mode is 'hosted'.",
      "type": "string"
     },
     "transfer_number": {
      "description": "Phone number to transfer calls to (E.164 format), or empty string to remove.",
      "type": "string"
     },
     "voice": {
      "description": "Voice ID for the agent.",
      "type": "string"
     },
     "voice_mode": {
      "description": "'webhook' forwards transcripts to your webhook. 'hosted' uses built-in AI with system_prompt.",
      "enum": [
       "webhook",
       "hosted"
      ],
      "type": "string"
     },
     "voice_speed": {
      "description": "Speech speed multiplier. 1.0 = normal, 0.5 = half speed, 2.0 = double.",
      "maximum": 2,
      "minimum": 0.5,
      "type": "number"
     },
     "voicemail_message": {
      "description": "Voicemail greeting text, or empty string to disable voicemail.",
      "type": "string"
     }
    },
    "required": [
     "agent_id"
    ],
    "type": "object"
   },
   "name": "update_agent"
  },
  {
   "annotations": {
    "idempotentHint": true
   },
   "description": "Set metadata on a conversation. Use this to store custom state, tags, or context that persists between messages. Pass null to clear metadata.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
     "conversation_id": {
      "description": "The conversation ID",
      "type": "string"
     },
     "metadata": {
      "anyOf": [
       {
        "additionalProperties": {},
        "propertyNames": {
         "type": "string"
        },
        "type": "object"
       },
       {
        "type": "null"
       }
      ],
      "description": "JSON metadata object to store on the conversation, or null to clear"
     }
    },
    "required": [
     "conversation_id",
     "metadata"
    ],
    "type": "object"
   },
   "name": "update_conversation"
  }
 ],
 "version": "0.7.0"
}