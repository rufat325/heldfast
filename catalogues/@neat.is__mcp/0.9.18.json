{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "@neat.is/mcp",
 "protocol": "2025-06-18",
 "published": "2026-09-18T18:49:53.335Z",
 "tools": [
  {
   "description": "Ask the graph a question in plain language \u2014 the front door to NEAT. Reach for this FIRST, before Read/Grep/Bash, for any question about this system's behaviour, dependencies, failures, root cause, or blast radius. You do NOT need to know which tool or the exact node id: `ask` resolves the entities in your question to graph nodes and routes it to the right traversal (root cause, dependencies, observed runtime calls, incidents, divergences, blast radius), returning one compact answer with every fact provenance-tagged (EXTRACTED/OBSERVED/INFERRED/STALE) and confidence-scored. Use the structured tools (get_root_cause, get_dependencies, \u2026) when you already have a node id and want just that one traversal.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     },
     "question": {
      "description": "A natural-language question, e.g. \"why is checkout failing?\" or \"what breaks if I change the orders table?\"",
      "type": "string"
     }
    },
    "required": [
     "question"
    ],
    "type": "object"
   },
   "name": "ask"
  },
  {
   "description": "Inspect, dry-run, or get the soft guardrail for the project's policy.json. With applicableTo, returns the policies that apply where you are working \u2014 surfaced as context so you stay inside the lines (informs, never blocks). Without hypotheticalAction or applicableTo, returns currently-recorded violations. With hypotheticalAction, returns violations that would result if the action were applied. Architectural assertions in five shapes (structural / compatibility / provenance / ownership / blast-radius).",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "applicableTo": {
      "description": "Soft guardrail (ADR-108): pass the node id you are about to edit and check_policies returns the policies that govern it, as a context block \u2014 so you stay inside the lines. Advisory only; never blocks.",
      "type": "string"
     },
     "hypotheticalAction": {
      "anyOf": [
       {
        "additionalProperties": false,
        "properties": {
         "frontierId": {
          "minLength": 1,
          "type": "string"
         },
         "kind": {
          "const": "promote-frontier",
          "type": "string"
         }
        },
        "required": [
         "kind",
         "frontierId"
        ],
        "type": "object"
       },
       {
        "additionalProperties": false,
        "properties": {
         "edgeType": {
          "enum": [
           "CALLS",
           "DEPENDS_ON",
           "CONNECTS_TO",
           "CONFIGURED_BY",
           "PUBLISHES_TO",
           "CONSUMES_FROM",
           "RUNS_ON",
           "CONTAINS",
           "IMPORTS",
           "INHERITS",
           "IMPLEMENTS",
           "REFERENCES"
          ],
          "type": "string"
         },
         "kind": {
          "const": "add-edge",
          "type": "string"
         },
         "provenance": {
          "enum": [
           "EXTRACTED",
           "INFERRED",
           "OBSERVED",
           "STALE",
           "FRONTIER"
          ],
          "type": "string"
         },
         "source": {
          "minLength": 1,
          "type": "string"
         },
         "target": {
          "minLength": 1,
          "type": "string"
         }
        },
        "required": [
         "kind",
         "source",
         "target",
         "edgeType",
         "provenance"
        ],
        "type": "object"
       }
      ],
      "description": "Dry-run mode: simulate the action and return resulting violations. Omit for current state."
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     },
     "scope": {
      "anyOf": [
       {
        "enum": [
         "all",
         "unresolved"
        ],
        "type": "string"
       },
       {
        "additionalProperties": false,
        "properties": {
         "policyId": {
          "minLength": 1,
          "type": "string"
         }
        },
        "required": [
         "policyId"
        ],
        "type": "object"
       }
      ],
      "description": "Narrow to a subset. Default \"all\"."
     }
    },
    "type": "object"
   },
   "name": "check_policies"
  },
  {
   "description": "Take one navigation step from a node and classify the neighbourhood (ADR-189). direction \"up\" walks to callers/dependents (who calls this), \"down\" walks to callees/dependencies (what this calls). Each neighbour comes back classified primary-failure / symptom-only / unrelated. Use this to navigate a failure one hop at a time instead of trusting a single verdict \u2014 a symptom-only node is a downstream victim, so walk \"up\" from it toward the real cause.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "direction": {
      "description": "up = callers/dependents (toward the cause), down = callees/dependencies",
      "enum": [
       "up",
       "down"
      ],
      "type": "string"
     },
     "nodeId": {
      "description": "Graph node id to step from",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "nodeId",
     "direction"
    ],
    "type": "object"
   },
   "name": "expand"
  },
  {
   "description": "List every node that depends on the given node \u2014 what would break if this node failed or was redeployed.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "depth": {
      "description": "Max BFS depth (default 10)",
      "maximum": 20,
      "minimum": 0,
      "type": "integer"
     },
     "nodeId": {
      "description": "Graph node id to compute blast radius from",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "nodeId"
    ],
    "type": "object"
   },
   "name": "get_blast_radius"
  },
  {
   "description": "List the transitive outgoing dependencies of a node, BFS to depth N (default 3, max 10). Each result carries distance, edge type, and provenance \u2014 both static (EXTRACTED) and runtime (OBSERVED). Pass depth=1 for direct-only.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "depth": {
      "description": "BFS depth (default 3, max 10). depth=1 returns direct dependencies only.",
      "maximum": 10,
      "minimum": 1,
      "type": "integer"
     },
     "nodeId": {
      "description": "Graph node id to inspect",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "nodeId"
    ],
    "type": "object"
   },
   "name": "get_dependencies"
  },
  {
   "description": "Returns places where what the code declares (EXTRACTED) doesn't match what production observed (OBSERVED). The single most NEAT-shaped query \u2014 the one that justifies the whole graph. Use when the user asks 'is anything weird?' or 'what does production do that the code doesn't?' or 'find me a bug' on an unfamiliar codebase. Returns divergences ranked by confidence \u00d7 severity. Prefer this over `get_root_cause` when no specific node is failing.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "minConfidence": {
      "description": "Drop divergences below this confidence threshold (0.0 - 1.0).",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "node": {
      "description": "Scope to divergences involving this node id (as source or target).",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     },
     "type": {
      "description": "Filter by divergence type. One or more of: missing-observed, missing-extracted, version-mismatch, host-mismatch, compat-violation, observed-symbol-mismatch, observed-failing. Omit for all.",
      "items": {
       "enum": [
        "missing-observed",
        "missing-extracted",
        "version-mismatch",
        "host-mismatch",
        "compat-violation",
        "observed-symbol-mismatch",
        "observed-failing",
        "deploy-mismatch"
       ],
       "type": "string"
      },
      "type": "array"
     }
    },
    "type": "object"
   },
   "name": "get_divergences"
  },
  {
   "description": "Diff a saved graph snapshot against the current live graph. Useful for change reviews and post-incidents \u2014 answers \"what changed in the architecture between then and now.\" Returns added/removed/changed nodes and edges with both snapshot timestamps.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "againstSnapshot": {
      "description": "Path or http(s) URL of the snapshot to diff against (the \"before\" state). The current graph is the \"after\".",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "againstSnapshot"
    ],
    "type": "object"
   },
   "name": "get_graph_diff"
  },
  {
   "description": "One self-sufficient work order for an incident on a node (ADR-221): the incident fused with its root-cause chain, blast radius, governing policies, and node divergence \u2014 each claim provenance-stamped, so you can act without grepping. Give a node id (a service, file, or symbol); omit errorId for the node's most-recent incident, or pass errorId to pin one.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "errorId": {
      "description": "Pin a specific incident by its id; omit for the most recent",
      "type": "string"
     },
     "nodeId": {
      "description": "Graph node id the incident is on (service/file/symbol)",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "nodeId"
    ],
    "type": "object"
   },
   "name": "get_incident_card"
  },
  {
   "description": "Return recent OTel error events recorded against a node, most recent first.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "limit": {
      "description": "Max events to return (default 20)",
      "exclusiveMinimum": 0,
      "maximum": 100,
      "type": "integer"
     },
     "nodeId": {
      "description": "Graph node id to query",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "nodeId"
    ],
    "type": "object"
   },
   "name": "get_incident_history"
  },
  {
   "description": "List only the runtime (OBSERVED via OTel) outgoing dependencies of a node. Use this to compare what code SAYS the service depends on vs what production actually does.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "nodeId": {
      "description": "Graph node id to inspect",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "nodeId"
    ],
    "type": "object"
   },
   "name": "get_observed_dependencies"
  },
  {
   "description": "List the most recent OBSERVED \u2192 STALE edge transitions. Use this to spot integrations that have gone quiet \u2014 a CALLS edge that just went stale typically means an upstream stopped calling, not that the link is healthy.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "edgeType": {
      "description": "Filter by edge type \u2014 e.g. \"CALLS\" or \"CONNECTS_TO\"",
      "type": "string"
     },
     "limit": {
      "description": "Max events to return (default 50)",
      "exclusiveMinimum": 0,
      "maximum": 200,
      "type": "integer"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "get_recent_stale_edges"
  },
  {
   "description": "Trace a failing node up its dependency graph to find the underlying cause. Use this when something is breaking and you want to know which upstream component is the actual culprit.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "errorId": {
      "description": "Specific error event id from incident history; if set, the result is coloured with that error message",
      "type": "string"
     },
     "errorNode": {
      "description": "Graph node id where the error surfaced, e.g. \"database:payments-db\"",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "errorNode"
    ],
    "type": "object"
   },
   "name": "get_root_cause"
  },
  {
   "description": "Install an instrumentation package and splice its registration into the existing OTel hook file. Idempotent \u2014 calling twice with the same args is a no-op. Only modifies instrumentation files, package.json, and the lockfile (via the project package manager).",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "instrumentation_package": {
      "description": "The instrumentation npm package, e.g. \"@prisma/instrumentation\"",
      "type": "string"
     },
     "library": {
      "description": "The library being instrumented, e.g. \"@prisma/client\"",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     },
     "registration_snippet": {
      "description": "The JS/TS snippet to splice into the instrumentations array, e.g. \"instrumentations.push(new PrismaInstrumentation())\"",
      "type": "string"
     },
     "version": {
      "description": "Semver range for the instrumentation package, e.g. \"^6.0.0\"",
      "type": "string"
     }
    },
    "required": [
     "library",
     "instrumentation_package",
     "version",
     "registration_snippet"
    ],
    "type": "object"
   },
   "name": "neat_apply_extension"
  },
  {
   "description": "Connect a provider to this hosted project by pasting its API token \u2014 the headless path, no browser needed. NEAT verifies the token against the provider, seals it, and pulls the provider into the project graph as OBSERVED. Hosted only.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "credential": {
      "description": "The provider's own API token \u2014 e.g. a Supabase Management token (sbp_...) or a Railway account/team token. Sealed at rest; never stored in the graph or returned.",
      "type": "string"
     },
     "provider": {
      "description": "Provider id, e.g. \"supabase\" or \"railway\" (see neat_list_connectable)",
      "type": "string"
     }
    },
    "required": [
     "provider",
     "credential"
    ],
    "type": "object"
   },
   "name": "neat_connect"
  },
  {
   "description": "List the providers connected to this hosted project and each connection's status (connecting / healthy / error / needs reconnect). Hosted only.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {},
    "type": "object"
   },
   "name": "neat_connection_status"
  },
  {
   "description": "Describe the current state of OTel instrumentation in the project: which hook files exist, whether .env.neat is present, which OTel deps are installed.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "neat_describe_project_instrumentation"
  },
  {
   "description": "Disconnect a provider from this hosted project \u2014 drops its stored connection(s). Hosted only.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "provider": {
      "description": "Provider id to disconnect, e.g. \"supabase\"",
      "type": "string"
     }
    },
    "required": [
     "provider"
    ],
    "type": "object"
   },
   "name": "neat_disconnect"
  },
  {
   "description": "Preview what neat_apply_extension would do without making any changes. Returns the exact file diff, deps to add, and install command.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "instrumentation_package": {
      "description": "The instrumentation npm package, e.g. \"@prisma/instrumentation\"",
      "type": "string"
     },
     "library": {
      "description": "The library being instrumented, e.g. \"@prisma/client\"",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     },
     "registration_snippet": {
      "description": "The JS/TS snippet to splice into the instrumentations array",
      "type": "string"
     },
     "version": {
      "description": "Semver range for the instrumentation package, e.g. \"^6.0.0\"",
      "type": "string"
     }
    },
    "required": [
     "library",
     "instrumentation_package",
     "version",
     "registration_snippet"
    ],
    "type": "object"
   },
   "name": "neat_dry_run_extension"
  },
  {
   "description": "List the providers you can connect to this hosted project (Supabase, Railway, \u2026). Hosted only \u2014 needs NEAT_CP_URL and a NEAT_API_KEY (neat_pat_); returns a \"not configured\" note otherwise.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {},
    "type": "object"
   },
   "name": "neat_list_connectable"
  },
  {
   "description": "List libraries in the project that need instrumentation beyond the auto-instrumentations bundle. Returns first-party, third-party, and gap libraries that require an explicit instrumentation package.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "neat_list_uninstrumented"
  },
  {
   "description": "Look up the registry entry for a specific library. Returns the canonical instrumentation package, version, and registration snippet if one exists.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "installedVersion": {
      "description": "Installed version for range matching",
      "type": "string"
     },
     "library": {
      "description": "npm package name, e.g. \"@prisma/client\"",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "library"
    ],
    "type": "object"
   },
   "name": "neat_lookup_instrumentation"
  },
  {
   "description": "Undo the last neat_apply_extension for a given library. Removes the dep from package.json and the registration from the hook file. Does not re-run the package manager \u2014 run install manually to sync the lockfile.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "library": {
      "description": "The library whose instrumentation should be rolled back",
      "type": "string"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "library"
    ],
    "type": "object"
   },
   "name": "neat_rollback_extension"
  },
  {
   "description": "Confirm whether two nodes are connected, which way, and whether the connecting path carries the failure (ADR-189). Returns the direction (a\u2192b or b\u2192a), the path with per-hop provenance, and carriesSignal \u2014 whether errors/latency run end to end, which turns \"a path exists\" into \"a is actually causing b\". No path within the depth bound returns \"no path within N hops\", never a false \"unrelated\".",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "a": {
      "description": "First node id (the hypothesised cause)",
      "type": "string"
     },
     "b": {
      "description": "Second node id (the hypothesised symptom)",
      "type": "string"
     },
     "maxDepth": {
      "description": "Max path length to search (default 5)",
      "maximum": 10,
      "minimum": 1,
      "type": "integer"
     },
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     }
    },
    "required": [
     "a",
     "b"
    ],
    "type": "object"
   },
   "name": "relate"
  },
  {
   "description": "Search nodes by natural-language query. Uses embedding vectors when an embedder is available (Ollama nomic-embed-text \u2192 in-process MiniLM \u2192 substring fallback) \u2014 phrase the query the way you would describe what you want.",
   "execution": {
    "taskSupport": "forbidden"
   },
   "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "additionalProperties": false,
    "properties": {
     "project": {
      "description": "Project name when the core hosts more than one (set NEAT_PROJECTS=...). Omit to use the default project.",
      "type": "string"
     },
     "query": {
      "description": "Free-text query, e.g. \"service handling checkout payments\"",
      "type": "string"
     }
    },
    "required": [
     "query"
    ],
    "type": "object"
   },
   "name": "semantic_search"
  }
 ],
 "version": "0.9.18"
}