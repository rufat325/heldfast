{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "@qase/mcp-server",
 "protocol": "2025-06-18",
 "published": "2026-09-02T12:49:21.678Z",
 "tools": [
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Direct Qase REST API call for endpoints not covered by other tools. Pass the HTTP method, path (starting with /v1/), and optional body/query. See https://developers.qase.io for API reference. Use this as an escape hatch when the dedicated tools do not cover your use case. Sends JSON only: it cannot upload files, which need multipart/form-data \u2014 use qase_attachment_upload for that.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "body": {
      "additionalProperties": {},
      "description": "Request body for POST/PUT/PATCH",
      "type": "object"
     },
     "method": {
      "default": "GET",
      "description": "HTTP method",
      "enum": [
       "GET",
       "POST",
       "PUT",
       "PATCH",
       "DELETE"
      ],
      "type": "string"
     },
     "path": {
      "description": "API path starting with /v1/ (e.g., \"/v1/project/DEMO/run\")",
      "minLength": 1,
      "type": "string"
     },
     "query": {
      "additionalProperties": {
       "type": "string"
      },
      "description": "Query parameters",
      "type": "object"
     }
    },
    "required": [
     "path"
    ],
    "type": "object"
   },
   "name": "qase_api"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Upload a file attachment and get back its hash, which the `attachments` field of qase_case_upsert, qase_result_record, qase_ci_report, qase_triage_defect, and qase_shared_step_upsert accepts. This is the only way to obtain such a hash \u2014 the upload endpoint needs multipart/form-data, which qase_api cannot send. Pass the file as base64 (`file_base64`), which is the only option when the server runs remotely, such as the hosted connector; `file_path` works only when the server runs on the same machine as the file.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "code": {
      "description": "Project code (2-10 uppercase letters, numbers, or underscores)",
      "maxLength": 10,
      "minLength": 2,
      "pattern": "^[A-Z0-9_]+$",
      "type": "string"
     },
     "file": {
      "description": "Deprecated: prefer file_base64 or file_path, which say which one you mean. Accepts either an absolute path to an existing file or base64 content.",
      "type": "string"
     },
     "file_base64": {
      "description": "File content, base64 encoded. Use this whenever the server is not on the same machine as the file \u2014 including the hosted connector, where it is the only option.",
      "type": "string"
     },
     "file_path": {
      "description": "Absolute path to a file on the machine running THIS server. Only usable for a local stdio server; a remote server cannot see your filesystem \u2014 send file_base64 instead.",
      "type": "string"
     },
     "filename": {
      "description": "Original filename with extension",
      "type": "string"
     }
    },
    "required": [
     "code",
     "filename"
    ],
    "type": "object"
   },
   "name": "qase_attachment_upload"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Create or update a test case. If `id` is provided, updates the existing case; if omitted, creates a new one. Enum fields (priority, severity, type, etc.) accept both labels (\"high\", \"blocker\") and numeric IDs \u2014 the server normalizes automatically. If the project has \"Test case review\" enabled, changes may need to go through a review instead of being written directly \u2014 run qase_discover_tools with \"review\" for those tools.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "attachments": {
      "description": "Attachment hashes from qase_attachment_upload",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "automation": {
      "description": "Automation status (label, slug, or numeric ID: 0=Manual / is-not-automated, 1=To be automated, 2=Automated)",
      "type": "string"
     },
     "behavior": {
      "description": "Behavior label or numeric ID",
      "type": "string"
     },
     "code": {
      "description": "Project code (2-10 uppercase letters, numbers, or underscores)",
      "maxLength": 10,
      "minLength": 2,
      "pattern": "^[A-Z0-9_]+$",
      "type": "string"
     },
     "custom_field": {
      "additionalProperties": {},
      "type": "object"
     },
     "description": {
      "type": "string"
     },
     "id": {
      "description": "Case ID \u2014 if provided, updates the case; if omitted, creates a new one",
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "is_flaky": {
      "type": "boolean"
     },
     "layer": {
      "description": "Layer label or numeric ID",
      "type": "string"
     },
     "milestone_id": {
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "postconditions": {
      "type": "string"
     },
     "preconditions": {
      "type": "string"
     },
     "priority": {
      "description": "Priority label or numeric ID (0=not set, 1=high, 2=medium, 3=low)",
      "type": "string"
     },
     "severity": {
      "description": "Severity label or numeric ID",
      "type": "string"
     },
     "status": {
      "description": "Status label or numeric ID",
      "type": "string"
     },
     "steps": {
      "items": {
       "additionalProperties": false,
       "properties": {
        "action": {
         "description": "Step action (classic steps). Not needed when `shared` is set.",
         "type": "string"
        },
        "attachments": {
         "description": "Attachment hashes from qase_attachment_upload",
         "items": {
          "type": "string"
         },
         "type": "array"
        },
        "data": {
         "description": "Test data",
         "type": "string"
        },
        "expected_result": {
         "description": "Expected result",
         "type": "string"
        },
        "shared": {
         "description": "Hash of an existing shared step to insert at this position, from `qase_shared_step_upsert`. The step then reuses that shared step instead of defining its own content, so `action` can be omitted. Reading the case back reports the link as `shared_step_hash`.",
         "type": "string"
        },
        "shared_step_hash": {
         "description": "Alias for `shared` \u2014 the name used when reading a case. Sent to the API as `shared`.",
         "type": "string"
        },
        "steps": {
         "description": "Nested substeps. Same structure as parent steps, supports further nesting.",
         "items": {
          "additionalProperties": true,
          "properties": {
           "action": {
            "description": "Step action (classic steps). Not needed when `shared` is set.",
            "type": "string"
           },
           "attachments": {
            "description": "Attachment hashes from qase_attachment_upload",
            "items": {
             "type": "string"
            },
            "type": "array"
           },
           "data": {
            "description": "Test data",
            "type": "string"
           },
           "expected_result": {
            "description": "Expected result",
            "type": "string"
           },
           "shared": {
            "description": "Hash of an existing shared step to insert at this position, from `qase_shared_step_upsert`. The step then reuses that shared step instead of defining its own content, so `action` can be omitted. Reading the case back reports the link as `shared_step_hash`.",
            "type": "string"
           },
           "shared_step_hash": {
            "description": "Alias for `shared` \u2014 the name used when reading a case. Sent to the API as `shared`.",
            "type": "string"
           },
           "value": {
            "description": "Gherkin scenario text (when steps_type is \"gherkin\")",
            "type": "string"
           }
          },
          "type": "object"
         },
         "type": "array"
        },
        "value": {
         "description": "Gherkin scenario text (when steps_type is \"gherkin\")",
         "type": "string"
        }
       },
       "type": "object"
      },
      "type": "array"
     },
     "steps_type": {
      "enum": [
       "classic",
       "gherkin"
      ],
      "type": "string"
     },
     "suite_id": {
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "tags": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "title": {
      "description": "Test case title",
      "maxLength": 255,
      "minLength": 1,
      "type": "string"
     },
     "type": {
      "description": "Type label or numeric ID",
      "type": "string"
     }
    },
    "required": [
     "code",
     "title"
    ],
    "type": "object"
   },
   "name": "qase_case_upsert"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Report CI/CD test results in one call: creates a run, records all results, and optionally completes the run. Replaces the 3-4 step manual workflow of create_run \u2192 bulk_create_results \u2192 complete_run. Designed for CI pipeline integration.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "code": {
      "description": "Project code (2-10 uppercase letters, numbers, or underscores)",
      "maxLength": 10,
      "minLength": 2,
      "pattern": "^[A-Z0-9_]+$",
      "type": "string"
     },
     "complete": {
      "default": true,
      "description": "Complete the run after recording results (default: true)",
      "type": "boolean"
     },
     "environment_id": {
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "is_autotest": {
      "default": true,
      "description": "Mark as automated run (default: true)",
      "type": "boolean"
     },
     "results": {
      "description": "Test results to record",
      "items": {
       "additionalProperties": false,
       "properties": {
        "attachments": {
         "description": "Attachment hashes from qase_attachment_upload",
         "items": {
          "type": "string"
         },
         "type": "array"
        },
        "case_id": {
         "exclusiveMinimum": 0,
         "type": "integer"
        },
        "comment": {
         "type": "string"
        },
        "defect": {
         "type": "boolean"
        },
        "stacktrace": {
         "type": "string"
        },
        "status": {
         "enum": [
          "passed",
          "failed",
          "blocked",
          "skipped",
          "invalid"
         ],
         "type": "string"
        },
        "time_ms": {
         "minimum": 0,
         "type": "integer"
        }
       },
       "required": [
        "case_id",
        "status"
       ],
       "type": "object"
      },
      "minItems": 1,
      "type": "array"
     },
     "title": {
      "description": "Run title (e.g., \"CI Build #1234\")",
      "maxLength": 255,
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "code",
     "title",
     "results"
    ],
    "type": "object"
   },
   "name": "qase_ci_report",
   "outputSchema": {
    "properties": {
     "results_recorded": {
      "description": "Number of results recorded",
      "type": "integer"
     },
     "run_id": {
      "description": "Created run ID",
      "type": "integer"
     },
     "run_status": {
      "enum": [
       "active",
       "complete",
       "complete_failed"
      ],
      "type": "string"
     }
    },
    "required": [
     "run_id",
     "run_status",
     "results_recorded"
    ],
    "type": "object"
   }
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Create or update a defect. If `id` is provided, updates (including status changes and resolve). If omitted, creates a new defect. Set `status: \"resolved\"` to resolve an existing defect.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "actual_result": {
      "type": "string"
     },
     "attachments": {
      "description": "Attachment hashes from qase_attachment_upload",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "code": {
      "description": "Project code (2-10 uppercase letters, numbers, or underscores)",
      "maxLength": 10,
      "minLength": 2,
      "pattern": "^[A-Z0-9_]+$",
      "type": "string"
     },
     "custom_field": {
      "additionalProperties": {},
      "type": "object"
     },
     "id": {
      "description": "Defect ID \u2014 if provided, updates; if omitted, creates",
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "severity": {
      "enum": [
       "undefined",
       "blocker",
       "critical",
       "major",
       "normal",
       "minor",
       "trivial"
      ],
      "type": "string"
     },
     "status": {
      "description": "Set to \"resolved\" to resolve the defect",
      "enum": [
       "open",
       "in_progress",
       "resolved",
       "invalid"
      ],
      "type": "string"
     },
     "tags": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "title": {
      "description": "Defect title",
      "maxLength": 255,
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "code",
     "title"
    ],
    "type": "object"
   },
   "name": "qase_defect_upsert"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true,
    "readOnlyHint": true
   },
   "description": "Search for and activate additional Qase tools. By default, only core tools are visible. Use this to find tools for specific needs: deletions, milestone management, attachments, etc. Found tools are automatically activated and become available for use.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "activate": {
      "default": true,
      "description": "If true (default), found tools are activated and become available for use",
      "type": "boolean"
     },
     "category": {
      "description": "Filter by tool category",
      "enum": [
       "read",
       "write",
       "delete",
       "composite",
       "all"
      ],
      "type": "string"
     },
     "query": {
      "description": "Search query to find tools by name or description. Examples: \"delete\", \"milestone\", \"attachment\", \"suite\"",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "qase_discover_tools",
   "outputSchema": {
    "properties": {
     "activated": {
      "description": "Number of newly activated tools",
      "type": "integer"
     },
     "found": {
      "description": "Number of matching tools",
      "type": "integer"
     },
     "tools": {
      "items": {
       "properties": {
        "description": {
         "type": "string"
        },
        "destructive": {
         "type": "boolean"
        },
        "name": {
         "type": "string"
        }
       },
       "type": "object"
      },
      "type": "array"
     }
    },
    "required": [
     "found",
     "activated",
     "tools"
    ],
    "type": "object"
   }
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true,
    "readOnlyHint": true
   },
   "description": "Get any Qase entity by type and ID. Supports field projection via the `fields` parameter. For project-scoped entities (case, suite, run, result, plan, defect, milestone, environment, shared_step, shared_parameter, configuration), `code` is required. For global entities (user, author, attachment, custom_field), `code` can be omitted.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "code": {
      "description": "Project code (required for most entities)",
      "maxLength": 10,
      "minLength": 2,
      "pattern": "^[A-Z0-9_]+$",
      "type": "string"
     },
     "entity": {
      "description": "Entity type to fetch",
      "enum": [
       "case",
       "suite",
       "run",
       "result",
       "plan",
       "defect",
       "milestone",
       "environment",
       "shared_step",
       "shared_parameter",
       "configuration",
       "attachment",
       "author",
       "user",
       "review",
       "custom_field"
      ],
      "type": "string"
     },
     "fields": {
      "description": "Optional field projection \u2014 only return these top-level fields. Pass [\"*\"] for all fields.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "id": {
      "anyOf": [
       {
        "description": "Entity ID (positive integer)",
        "exclusiveMinimum": 0,
        "type": "integer"
       },
       {
        "description": "Hash identifier",
        "maxLength": 255,
        "minLength": 1,
        "type": "string"
       }
      ],
      "description": "Entity ID (number) or hash (string)"
     },
     "include": {
      "description": "Comma-separated list of related entities to include in the response. Cases and runs already request their external issue links by default (\"external_issues\" / \"external_issue\"); pass this only to override that.",
      "type": "string"
     }
    },
    "required": [
     "entity",
     "id"
    ],
    "type": "object"
   },
   "name": "qase_get"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true,
    "readOnlyHint": true
   },
   "description": "Get full project context in one call: project details, suites tree, milestones, environments, custom fields, and users. Cached for 5 minutes. Use this as the first call when starting work with a project \u2014 it seeds all the metadata the LLM needs without making 6 separate list calls. Each collection returns its first 100 entities by default; the `coverage` field reports `{ total, loaded, truncated }` per collection, so check it before assuming a list is complete. Pass `full: true` to page through everything.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "code": {
      "description": "Project code (2-10 uppercase letters, numbers, or underscores)",
      "maxLength": 10,
      "minLength": 2,
      "pattern": "^[A-Z0-9_]+$",
      "type": "string"
     },
     "full": {
      "description": "Page through every suite, milestone, environment, custom field, and user instead of fetching only the first 100 of each (default: false). Use this when a collection is reported as truncated and you need the complete set \u2014 it costs one API call per 100 entities and can return thousands of items, so prefer the targeted list tools or qql_search when you only need a subset.",
      "type": "boolean"
     }
    },
    "required": [
     "code"
    ],
    "type": "object"
   },
   "name": "qase_project_context",
   "outputSchema": {
    "properties": {
     "coverage": {
      "description": "Per-collection completeness: each of suites, milestones, environments, custom_fields, and users maps to { total, loaded, truncated }. When truncated is true the list holds only the first `loaded` of `total` entities \u2014 re-call with full: true for the rest.",
      "type": "object"
     },
     "custom_fields": {
      "description": "Custom fields list",
      "type": "object"
     },
     "environments": {
      "description": "Environments list",
      "type": "object"
     },
     "milestones": {
      "description": "Milestones list",
      "type": "object"
     },
     "project": {
      "description": "Project details",
      "type": "object"
     },
     "suites": {
      "description": "Suites list with entities array",
      "type": "object"
     },
     "users": {
      "description": "Team members list",
      "type": "object"
     }
    },
    "required": [
     "project",
     "suites",
     "milestones",
     "environments",
     "coverage"
    ],
    "type": "object"
   }
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Set up a regression test run in one call. Accepts case selection by suite IDs, explicit case IDs, or plan ID. Creates the run and adds all matching cases. Replaces the multi-step workflow of find cases \u2192 create run \u2192 add cases.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "code": {
      "description": "Project code (2-10 uppercase letters, numbers, or underscores)",
      "maxLength": 10,
      "minLength": 2,
      "pattern": "^[A-Z0-9_]+$",
      "type": "string"
     },
     "description": {
      "type": "string"
     },
     "environment_id": {
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "include_cases": {
      "description": "Explicit case IDs to include",
      "items": {
       "exclusiveMinimum": 0,
       "type": "integer"
      },
      "type": "array"
     },
     "milestone_id": {
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "plan_id": {
      "description": "Create run from an existing test plan",
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "suite_ids": {
      "description": "Include cases from these suites",
      "items": {
       "exclusiveMinimum": 0,
       "type": "integer"
      },
      "type": "array"
     },
     "title": {
      "description": "Run title",
      "maxLength": 255,
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "code",
     "title"
    ],
    "type": "object"
   },
   "name": "qase_regression_run",
   "outputSchema": {
    "properties": {
     "cases_added": {
      "description": "Number of cases added to the run",
      "type": "integer"
     },
     "run": {
      "description": "Full run entity",
      "type": "object"
     },
     "run_id": {
      "description": "Created run ID",
      "type": "integer"
     }
    },
    "required": [
     "run_id",
     "cases_added"
    ],
    "type": "object"
   }
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Record one or more test results into a run. Accepts an array of results \u2014 a single entry uses the single-result API, multiple entries use bulk. Each result must include a status; case_id is recommended.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "code": {
      "description": "Project code (2-10 uppercase letters, numbers, or underscores)",
      "maxLength": 10,
      "minLength": 2,
      "pattern": "^[A-Z0-9_]+$",
      "type": "string"
     },
     "results": {
      "description": "One or more results to record",
      "items": {
       "additionalProperties": false,
       "properties": {
        "attachments": {
         "description": "Attachment hashes from qase_attachment_upload",
         "items": {
          "type": "string"
         },
         "type": "array"
        },
        "case_id": {
         "exclusiveMinimum": 0,
         "type": "integer"
        },
        "comment": {
         "type": "string"
        },
        "custom_field": {
         "additionalProperties": {},
         "type": "object"
        },
        "defect": {
         "type": "boolean"
        },
        "stacktrace": {
         "type": "string"
        },
        "status": {
         "enum": [
          "passed",
          "failed",
          "blocked",
          "skipped",
          "invalid"
         ],
         "type": "string"
        },
        "steps": {
         "items": {
          "additionalProperties": false,
          "properties": {
           "attachments": {
            "description": "Attachment hashes from qase_attachment_upload",
            "items": {
             "type": "string"
            },
            "type": "array"
           },
           "comment": {
            "type": "string"
           },
           "position": {
            "minimum": 0,
            "type": "integer"
           },
           "status": {
            "enum": [
             "passed",
             "failed",
             "blocked",
             "skipped"
            ],
            "type": "string"
           }
          },
          "required": [
           "position",
           "status"
          ],
          "type": "object"
         },
         "type": "array"
        },
        "time_ms": {
         "minimum": 0,
         "type": "integer"
        }
       },
       "required": [
        "status"
       ],
       "type": "object"
      },
      "minItems": 1,
      "type": "array"
     },
     "run_id": {
      "description": "Run ID to record results into",
      "exclusiveMinimum": 0,
      "type": "integer"
     }
    },
    "required": [
     "code",
     "run_id",
     "results"
    ],
    "type": "object"
   },
   "name": "qase_result_record"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Create or update a test run. If `id` is provided, updates; if omitted, creates.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "cases": {
      "description": "Case IDs to include",
      "items": {
       "exclusiveMinimum": 0,
       "type": "integer"
      },
      "type": "array"
     },
     "code": {
      "description": "Project code (2-10 uppercase letters, numbers, or underscores)",
      "maxLength": 10,
      "minLength": 2,
      "pattern": "^[A-Z0-9_]+$",
      "type": "string"
     },
     "custom_field": {
      "additionalProperties": {},
      "type": "object"
     },
     "description": {
      "type": "string"
     },
     "end_time": {
      "description": "RFC3339 end time",
      "type": "string"
     },
     "environment_id": {
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "id": {
      "description": "Run ID \u2014 if provided, this is an update (note: Qase API has limited run update support)",
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "is_autotest": {
      "type": "boolean"
     },
     "milestone_id": {
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "plan_id": {
      "description": "Test plan to base run on",
      "exclusiveMinimum": 0,
      "type": "integer"
     },
     "start_time": {
      "description": "RFC3339 start time",
      "type": "string"
     },
     "tags": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "title": {
      "description": "Run title",
      "maxLength": 255,
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "code",
     "title"
    ],
    "type": "object"
   },
   "name": "qase_run_upsert"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
   },
   "description": "Create a defect from a test failure. Requires title, actual_result, and severity. Note: the API offers no way to attach existing runs or results to a defect \u2014 the runs/results seen on a defect are populated by the test runner when a result is reported as a defect. Reference failing results in actual_result instead.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "actual_result": {
      "description": "Observed behavior. Required by the API",
      "type": "string"
     },
     "attachments": {
      "description": "Attachment hashes from qase_attachment_upload",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "code": {
      "description": "Project code (2-10 uppercase letters, numbers, or underscores)",
      "maxLength": 10,
      "minLength": 2,
      "pattern": "^[A-Z0-9_]+$",
      "type": "string"
     },
     "custom_field": {
      "additionalProperties": {},
      "type": "object"
     },
     "description": {
      "type": "string"
     },
     "severity": {
      "description": "Required by the API",
      "enum": [
       "undefined",
       "blocker",
       "critical",
       "major",
       "normal",
       "minor",
       "trivial"
      ],
      "type": "string"
     },
     "tags": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "title": {
      "description": "Defect title",
      "maxLength": 255,
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "code",
     "title",
     "severity",
     "actual_result"
    ],
    "type": "object"
   },
   "name": "qase_triage_defect",
   "outputSchema": {
    "properties": {
     "defect": {
      "description": "Full defect entity",
      "type": "object"
     },
     "defect_id": {
      "description": "Created defect ID",
      "type": "integer"
     }
    },
    "required": [
     "defect_id"
    ],
    "type": "object"
   }
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true,
    "readOnlyHint": true
   },
   "description": "Get one section of the Qase Query Language (QQL) reference. `topic` is required \u2014 ask for the section you need rather than the whole reference. Start with \"entities\" when you are unsure which fields an entity has (they differ per entity), \"aggregation\" to count or summarise without paging, and \"enumValues\" for valid field values.",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "topic": {
      "description": "Which section to return (required \u2014 one section per call):\n- overview: what QQL is, overall query structure, subscription requirement\n- syntax: structure, ordering, custom fields, case-sensitivity, boolean and date fields\n- entities: the fields available on each entity \u2014 field names are NOT uniform across entities, so read this before writing a query against an unfamiliar one\n- operators: comparison, matching, set, null, and logical operators\n- functions: currentUser, activeUsers, and the now/startOf*/endOf* date functions\n- examples: ready-made queries for common questions\n- aggregation: SELECT (COUNT/MIN/MAX/AVG/SUM/FIRST/LAST), GROUP BY, HAVING \u2014 use this to count or summarise instead of paging through rows\n- enumValues: the valid values for priority, severity, and the per-entity status fields",
      "enum": [
       "overview",
       "syntax",
       "entities",
       "operators",
       "functions",
       "examples",
       "aggregation",
       "enumValues"
      ],
      "type": "string"
     }
    },
    "required": [
     "topic"
    ],
    "type": "object"
   },
   "name": "qql_help"
  },
  {
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true,
    "readOnlyHint": true
   },
   "description": "Search entities using Qase Query Language (QQL) with powerful filtering and cross-project queries",
   "inputSchema": {
    "additionalProperties": false,
    "properties": {
     "limit": {
      "description": "Maximum number of results to return (default: 10, max: 100)",
      "exclusiveMinimum": 0,
      "maximum": 100,
      "type": "integer"
     },
     "offset": {
      "description": "Number of results to skip for pagination",
      "minimum": 0,
      "type": "integer"
     },
     "query": {
      "description": "QQL query expression. Examples:\n- entity = \"case\" and project = \"DEMO\" and status = \"Actual\"\n- entity = \"defect\" and severity = \"blocker\" and status = \"open\"\n- entity = \"result\" and status = \"failed\" and ended >= now(\"-7d\")\n- entity = \"run\" and milestone ~ \"Sprint 12\"\nSee QQL documentation for full syntax and examples.",
      "maxLength": 2000,
      "minLength": 1,
      "type": "string"
     }
    },
    "required": [
     "query"
    ],
    "type": "object"
   },
   "name": "qql_search",
   "outputSchema": {
    "properties": {
     "entities": {
      "description": "Matching entities",
      "items": {
       "type": "object"
      },
      "type": "array"
     },
     "total": {
      "description": "Total matching entities",
      "type": "integer"
     }
    },
    "required": [
     "total",
     "entities"
    ],
    "type": "object"
   }
  }
 ],
 "version": "2.3.1"
}