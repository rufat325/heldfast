{
 "args": [
  "mcp"
 ],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "firebase-tools",
 "protocol": "2025-06-18",
 "published": "2026-09-09T22:15:58.227Z",
 "tools": [
  {
   "_meta": {
    "feature": "developerknowledge",
    "requiresAuth": true,
    "requiresProject": true
   },
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Use answer_query to get a grounded answer to a query about Google developer products. This tool has limited quota. This tool will synthesize information from the corpus to generate an answer to the query. answer_query grounds answers using the same corpus as search_documents. \nThis tool returns the generated answer_text and a list of document names (references) used to generate the answer. Use get_documents with the document names to fetch the entire document content if needed.\n\nIf you get a 429 out of quota error, use search_documents instead.\n",
   "inputSchema": {
    "description": "Request message for AnswerQuery.",
    "properties": {
     "query": {
      "description": "Required. The query to answer.",
      "type": "string"
     }
    },
    "required": [
     "query"
    ],
    "type": "object"
   },
   "name": "developerknowledge_answer_query",
   "outputSchema": {
    "description": "Response message for AnswerQuery.",
    "properties": {
     "answerText": {
      "description": "The answer to the query.",
      "type": "string"
     },
     "references": {
      "description": "Output only. The resource names of the documents used to generate the answer.",
      "items": {
       "type": "string"
      },
      "readOnly": true,
      "type": "array"
     }
    },
    "type": "object"
   }
  },
  {
   "_meta": {
    "feature": "developerknowledge",
    "requiresAuth": true,
    "requiresProject": false
   },
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Use this tool to retrieve the full content of a single document or up to 20 documents in a single call. The document names should be obtained from the `parent` field of results from a call to the `search_documents` tool. Set the `names` parameter to a list of document names.",
   "inputSchema": {
    "description": "Request schema for `get_documents`.",
    "properties": {
     "names": {
      "description": "Required. The names of the documents to retrieve, as returned by `search_documents`. A maximum of 20 documents can be retrieved in one call. The documents are returned in the same order as the `names` in the request. Format: `documents/{uri_without_scheme}` Example: `documents/docs.cloud.google.com/storage/docs/creating-buckets`",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "names"
    ],
    "type": "object"
   },
   "name": "developerknowledge_get_documents",
   "outputSchema": {
    "$defs": {
     "Document": {
      "description": "A Document represents a piece of content from the Developer Knowledge corpus.",
      "properties": {
       "content": {
        "description": "Output only. The content of the document in Markdown format.",
        "readOnly": true,
        "type": "string"
       },
       "description": {
        "description": "Output only. A description of the document.",
        "readOnly": true,
        "type": "string"
       },
       "name": {
        "description": "Identifier. The resource name of the document. Format: `documents/{uri_without_scheme}` Example: `documents/docs.cloud.google.com/storage/docs/creating-buckets`",
        "type": "string",
        "x-google-identifier": true
       },
       "title": {
        "description": "Output only. The title of the document.",
        "readOnly": true,
        "type": "string"
       },
       "uri": {
        "description": "Output only. The URI of the content, such as `https://cloud.google.com/storage/docs/creating-buckets`.",
        "readOnly": true,
        "type": "string"
       }
      },
      "type": "object"
     }
    },
    "description": "Response schema for `get_documents`.",
    "properties": {
     "documents": {
      "description": "Documents requested.",
      "items": {
       "$ref": "#/$defs/Document"
      },
      "type": "array"
     }
    },
    "type": "object"
   }
  },
  {
   "_meta": {
    "feature": "developerknowledge",
    "requiresAuth": true,
    "requiresProject": false
   },
   "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true
   },
   "description": "Use this tool to find documentation about Google developer products. The documents contain official APIs, code snippets, release notes, best practices, guides, debugging info, and more. It covers the following products and domains:\n\n\n* ADK: adk.dev\n\n* Android: developer.android.com\n\n* Apigee: docs.apigee.com\n\n* Chrome: developer.chrome.com\n\n* Dart: dart.dev\n\n* Firebase: firebase.google.com\n\n* Flutter: docs.flutter.dev\n\n* Fuchsia: fuchsia.dev\n\n* Gemini CLI: geminicli.com\n\n* Genkit: genkit.dev\n\n* Go: go.dev\n\n* Google AI: ai.google.dev\n\n* Google Antigravity: antigravity.google\n\n* Google Cloud: cloud.google.com & docs.cloud.google.com\n\n* Google Developers, Ads, Search, Google Maps, Youtube: developers.google.com\n\n* Google Home: developers.home.google.com\n\n* Google Maps Platform: mapsplatform.google.com\n\n* TensorFlow: www.tensorflow.org\n\n* Web: web.dev\n\n\nThis tool returns chunks of text, names, and URLs for matching documents. If the returned chunks are not detailed enough to answer the user's question, use `get_documents` with the `parent` from this tool's output to retrieve the full document content.\n",
   "inputSchema": {
    "description": "Request schema for `search_documents`. Use the `query` field to search for related Google developer documentation.",
    "properties": {
     "query": {
      "description": "Required. The raw query string provided by the user, such as \"How to create a Cloud Storage bucket?\".",
      "type": "string"
     }
    },
    "required": [
     "query"
    ],
    "type": "object"
   },
   "name": "developerknowledge_search_documents",
   "outputSchema": {
    "$defs": {
     "DocumentChunk": {
      "description": "A DocumentChunk represents a piece of content from a Document in the DeveloperKnowledge corpus. To fetch the entire document content, pass the `parent` to `get_documents`.",
      "properties": {
       "content": {
        "description": "Output only. The content of the document chunk.",
        "readOnly": true,
        "type": "string"
       },
       "id": {
        "description": "Output only. The ID of this chunk within the document. The chunk ID is unique within a document, but not globally unique across documents. The chunk ID is not stable and may change over time.",
        "readOnly": true,
        "type": "string"
       },
       "parent": {
        "description": "Output only. The resource name of the document this chunk is from. Format: `documents/{uri_without_scheme}` Example: `documents/docs.cloud.google.com/storage/docs/creating-buckets`",
        "readOnly": true,
        "type": "string"
       }
      },
      "type": "object"
     }
    },
    "description": "Response schema for `search_documents`.",
    "properties": {
     "results": {
      "description": "The search results for the given query. Each DocumentChunk in this list contains a snippet of content relevant to the search query. Use the `DocumentChunk.parent` field of each result with `get_documents` to retrieve the full document content.",
      "items": {
       "$ref": "#/$defs/DocumentChunk"
      },
      "type": "array"
     }
    },
    "type": "object"
   }
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": true,
    "requiresProject": true
   },
   "annotations": {
    "destructiveHint": false,
    "readOnlyHint": false,
    "title": "Add SHA Certificate to Android App"
   },
   "description": "Use this to add the specified SHA certificate hash to the specified Firebase Android App.",
   "humanReadableDescription": "Add a SHA-1 or SHA-256 certificate hash to an Android app in the active Firebase project.",
   "inputSchema": {
    "properties": {
     "app_id": {
      "description": "The Android app ID to add the SHA certificate to.",
      "type": "string"
     },
     "sha_hash": {
      "description": "The SHA certificate hash to add (SHA-1 or SHA-256).",
      "type": "string"
     }
    },
    "required": [
     "app_id",
     "sha_hash"
    ],
    "type": "object"
   },
   "name": "firebase_create_android_sha"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": true,
    "requiresProject": true
   },
   "annotations": {
    "destructiveHint": false,
    "readOnlyHint": false,
    "title": "Create Firebase App"
   },
   "description": "Use this to create a new Firebase App in the currently active Firebase Project. Firebase Apps can be iOS, Android, or Web.",
   "humanReadableDescription": "Create a new iOS, Android, or Web app in the active Firebase project.",
   "inputSchema": {
    "properties": {
     "android_config": {
      "description": "Configuration for Android apps. Required if platform is 'android'.",
      "properties": {
       "package_name": {
        "description": "The package name for your Android app (e.g., com.example.myapp).",
        "type": "string"
       }
      },
      "required": [
       "package_name"
      ],
      "type": "object"
     },
     "display_name": {
      "description": "The user-friendly display name for your app. If not provided, a default name may be generated.",
      "type": "string"
     },
     "ios_config": {
      "description": "Configuration for iOS apps. Required if platform is 'ios'.",
      "properties": {
       "app_store_id": {
        "description": "The App Store ID for your iOS app.",
        "type": "string"
       },
       "bundle_id": {
        "description": "The bundle ID for your iOS app (e.g., com.example.myapp).",
        "type": "string"
       }
      },
      "required": [
       "bundle_id"
      ],
      "type": "object"
     },
     "platform": {
      "description": "The platform for which to create an app.",
      "enum": [
       "web",
       "ios",
       "android"
      ],
      "type": "string"
     }
    },
    "required": [
     "platform"
    ],
    "type": "object"
   },
   "name": "firebase_create_app"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": true,
    "requiresProject": false
   },
   "annotations": {
    "destructiveHint": false,
    "readOnlyHint": false,
    "title": "Create Firebase Project"
   },
   "description": "Use this to create a new Firebase Project.",
   "humanReadableDescription": "Create a new Firebase project or enable Firebase services on an existing Google Cloud project.",
   "inputSchema": {
    "properties": {
     "display_name": {
      "description": "The user-friendly display name for the project.",
      "type": "string"
     },
     "project_id": {
      "description": "The project ID to create or use.",
      "type": "string"
     }
    },
    "required": [
     "project_id"
    ],
    "type": "object"
   },
   "name": "firebase_create_project"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": true,
    "requiresProject": true
   },
   "annotations": {
    "readOnlyHint": false,
    "title": "Deploy Firebase Services"
   },
   "description": "Deploy resources to your Firebase project, based on the contents of firebase.json.",
   "humanReadableDescription": "Deploy Firebase services and resources configured in firebase.json to your project.",
   "inputSchema": {
    "properties": {
     "only": {
      "description": "Comma-separated list of services to deploy. Valid targets are: database, storage, firestore, functions, hosting, remoteconfig, extensions, dataconnect, apphosting, auth.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "firebase_deploy"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": true,
    "requiresProject": true
   },
   "annotations": {
    "readOnlyHint": true,
    "title": "Check Deployment Status"
   },
   "description": "Check the status of a background deployment job using its Job ID.",
   "humanReadableDescription": "Check the status and progress of a background deployment job.",
   "inputSchema": {
    "properties": {
     "jobId": {
      "description": "The Job ID returned by the deploy tool",
      "type": "string"
     }
    },
    "required": [
     "jobId"
    ],
    "type": "object"
   },
   "name": "firebase_deploy_status"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": false,
    "requiresProject": false
   },
   "annotations": {
    "readOnlyHint": true,
    "title": "Get Firebase Environment Info"
   },
   "description": "Use this to retrieve the current Firebase **environment** configuration for the Firebase CLI and Firebase MCP server, including current authenticated user, project directory, active Firebase Project, and more. All tools require the user to be authenticated, but not all information is required for all tools. Pay attention to the tool requirements for which pieces of information are required.",
   "humanReadableDescription": "Retrieve the current Firebase CLI and MCP environment configuration, active project, and authenticated user.",
   "inputSchema": {
    "type": "object"
   },
   "name": "firebase_get_environment"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": true,
    "requiresProject": true
   },
   "annotations": {
    "readOnlyHint": true,
    "title": "Get Current Firebase Project"
   },
   "description": "Use this to retrieve information about the currently active Firebase Project.",
   "humanReadableDescription": "Retrieve metadata and configuration details for the currently active Firebase project.",
   "inputSchema": {
    "type": "object"
   },
   "name": "firebase_get_project"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": true,
    "requiresProject": true
   },
   "annotations": {
    "readOnlyHint": true,
    "title": "Get Firebase SDK Config"
   },
   "description": "Use this to retrieve the Firebase configuration information for a Firebase App. You must specify EITHER a platform OR the Firebase App ID for a Firebase App registered in the currently active Firebase Project.",
   "humanReadableDescription": "Retrieve SDK configuration details or config file contents for a registered Firebase app.",
   "inputSchema": {
    "properties": {
     "app_id": {
      "description": "The specific app ID to fetch. One of 'platform' or 'app_id' must be provided.",
      "type": "string"
     },
     "platform": {
      "description": "The platform for which you want config. One of 'platform' or 'app_id' must be provided.",
      "enum": [
       "ios",
       "android",
       "web"
      ],
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "firebase_get_sdk_config"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": true,
    "requiresProject": true
   },
   "annotations": {
    "readOnlyHint": true,
    "title": "Get Firebase Rules"
   },
   "description": "Use this to retrieve the security rules for a specified Firebase service. If there are multiple instances of that service in the product, the rules for the default instance are returned.",
   "humanReadableDescription": "Retrieve the active security rules for Firestore, Storage, or Realtime Database.",
   "inputSchema": {
    "properties": {
     "type": {
      "description": "The service to get rules for.",
      "enum": [
       "firestore",
       "rtdb",
       "storage"
      ],
      "type": "string"
     }
    },
    "required": [
     "type"
    ],
    "type": "object"
   },
   "name": "firebase_get_security_rules"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": false,
    "requiresProject": false
   },
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": false,
    "title": "Initialize Firebase Products"
   },
   "description": "Use this to initialize selected Firebase services in the workspace (Cloud Firestore database, Firebase SQL Connect, Firebase Realtime Database, Firebase AI Logic). All services are optional; specify only the products you want to set up. You can initialize new features into an existing project directory, but re-initializing an existing feature may overwrite configuration. To deploy the initialized features, run the `firebase deploy` command after `firebase_init` tool.",
   "humanReadableDescription": "Initialize and configure Firebase services in your local project workspace.",
   "inputSchema": {
    "properties": {
     "features": {
      "properties": {
       "ailogic": {
        "description": "Enable Firebase AI Logic feature for existing app",
        "properties": {
         "app_id": {
          "description": "Firebase app ID (format: 1:PROJECT_NUMBER:PLATFORM:APP_ID). Must be an existing app in your Firebase project.",
          "type": "string"
         }
        },
        "required": [
         "app_id"
        ],
        "type": "object"
       },
       "auth": {
        "description": "Provide this object to initialize Firebase Authentication.",
        "properties": {
         "providers": {
          "properties": {
           "anonymous": {
            "description": "Enable Anonymous authentication.",
            "type": "boolean"
           },
           "emailPassword": {
            "description": "Enable Email/Password authentication.",
            "type": "boolean"
           },
           "googleSignIn": {
            "description": "Configure Google Sign-In.",
            "properties": {
             "oAuthBrandDisplayName": {
              "description": "The display name for the OAuth brand.",
              "type": "string"
             },
             "supportEmail": {
              "description": "The support email for the OAuth brand.",
              "type": "string"
             }
            },
            "required": [
             "oAuthBrandDisplayName",
             "supportEmail"
            ],
            "type": "object"
           }
          },
          "type": "object"
         }
        },
        "required": [
         "providers"
        ],
        "type": "object"
       },
       "database": {
        "description": "Provide this object to initialize Firebase Realtime Database in this project directory.",
        "properties": {
         "rules": {
          "default": "{\n  \"rules\": {\n    \".read\": \"auth != null\",\n    \".write\": \"auth != null\"\n  }\n}",
          "description": "The security rules to use for Realtime Database Security Rules.",
          "type": "string"
         },
         "rules_filename": {
          "default": "database.rules.json",
          "description": "The file to use for Realtime Database Security Rules.",
          "type": "string"
         }
        },
        "type": "object"
       },
       "dataconnect": {
        "description": "Provide this object to initialize Firebase SQL Connect with Cloud SQL Postgres in this project directory.\nIt installs SQL Connect Generated SDKs in all detected apps in the folder.",
        "properties": {
         "cloudsql_database": {
          "default": "fdcdb",
          "description": "The Postgres database ID to use in the Firebase SQL Connect service.",
          "type": "string"
         },
         "cloudsql_instance_id": {
          "description": "The GCP Cloud SQL instance ID to use in the Firebase SQL Connect service. By default, use <serviceId>-fdc. \nSet `provision_cloudsql` to true to start Cloud SQL provisioning.",
          "type": "string"
         },
         "location_id": {
          "default": "us-east4",
          "description": "The GCP region ID to set up the Firebase SQL Connect service.",
          "type": "string"
         },
         "provision_cloudsql": {
          "default": false,
          "description": "If true, provision the Cloud SQL instance if `cloudsql_instance_id` does not exist already. \nThe first Cloud SQL instance in the project will use the SQL Connect no-cost trial. See its terms of service: https://firebase.google.com/pricing.",
          "type": "boolean"
         },
         "service_id": {
          "description": "The Firebase SQL Connect service ID to initialize. Default to match the current folder name.",
          "type": "string"
         }
        },
        "type": "object"
       },
       "firestore": {
        "description": "Provide this object to initialize Cloud Firestore in this project directory.",
        "properties": {
         "database_id": {
          "default": "(default)",
          "description": "The database ID to use for Firestore.",
          "type": "string"
         },
         "location_id": {
          "default": "nam5",
          "description": "The GCP region ID to set up the Firestore database.",
          "type": "string"
         },
         "rules": {
          "description": "The security rules to use for Firestore Security Rules. Default to open rules that expire in 30 days.",
          "type": "string"
         },
         "rules_filename": {
          "default": "firestore.rules",
          "description": "The file to use for Firestore Security Rules.",
          "type": "string"
         }
        },
        "type": "object"
       },
       "hosting": {
        "description": "Provide this object to initialize Firebase Hosting in this project directory.",
        "properties": {
         "public_directory": {
          "default": "public",
          "description": "The directory containing public files that will be served. If using a build tool, this likely should be the output directory of that tool.",
          "type": "string"
         },
         "single_page_app": {
          "default": false,
          "description": "Configure as a single-page app.",
          "type": "boolean"
         },
         "site_id": {
          "description": "The ID of the hosting site to configure. If omitted and there is a default hosting site, that will be used.",
          "type": "string"
         }
        },
        "type": "object"
       },
       "storage": {
        "description": "Provide this object to initialize Firebase Storage in this project directory.",
        "properties": {
         "rules": {
          "description": "The security rules to use for Firebase Storage Security Rules. Default to closed rules that deny all access.",
          "type": "string"
         },
         "rules_filename": {
          "default": "storage.rules",
          "description": "The file to use for Firebase Storage Security Rules.",
          "type": "string"
         }
        },
        "type": "object"
       }
      },
      "type": "object"
     }
    },
    "required": [
     "features"
    ],
    "type": "object"
   },
   "name": "firebase_init"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": true,
    "requiresProject": true
   },
   "annotations": {
    "readOnlyHint": true,
    "title": "List Firebase Apps"
   },
   "description": "Use this to retrieve a list of the Firebase Apps registered in the currently active Firebase project. Firebase Apps can be iOS, Android, or Web.",
   "humanReadableDescription": "List all Firebase apps registered in the active project.",
   "inputSchema": {
    "properties": {
     "platform": {
      "description": "the specific platform to list (omit to list all platforms)",
      "enum": [
       "ios",
       "android",
       "web",
       "all"
      ],
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "firebase_list_apps"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": true
   },
   "annotations": {
    "idempotentHint": true,
    "readOnlyHint": true,
    "title": "List Firebase Projects"
   },
   "description": "Use this to retrieve a list of Firebase Projects that the signed-in user has access to.",
   "humanReadableDescription": "List Firebase projects accessible by the authenticated user.",
   "inputSchema": {
    "properties": {
     "page_size": {
      "default": 20,
      "description": "the number of projects to list per page (defaults to 1000)",
      "minimum": 1,
      "type": "number"
     },
     "page_token": {
      "description": "the page token to start listing from",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "firebase_list_projects"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": false
   },
   "description": "Signs the user into the Firebase CLI and Firebase MCP server.\n \n**Prerequisites:**\n* A Google Account is required.\n* Sign in is required to work with Firebase Projects and call most other Firebase tools.\n\n**When to use it:**\n* Use this tool when you need to authenticate the Firebase CLI/MCP server.\n* Use this tool to check the current login status (which account is currently authenticated).\n* Use this tool to switch accounts or force reauthentication when credentials expire or change.\n\n**How to use it:**\n* To check current login status, call the tool without any parameters. If a user is already logged in, it will return the logged-in email.\n* To log in (initial or after logging out), call the tool without any parameters:\n  1. The tool will return a login URL (`uri`) and a `sessionId`.\n  2. **CRITICAL SECURITY REQUIREMENT**: The agent MUST display BOTH the login URL and the Session ID to the user. Explicitly instruct the user to verify that the Session ID shown on the browser matches the Session ID provided by the agent. This prevents phishing attacks.\n  3. Once the user signs in on the browser and copies the authorization code, call this tool again with that code passed to the `authCode` parameter.\n* To force reauthentication or log in as a different account, call the tool with `reauth: true`.\n\n**JSON Examples:**\n\n*Scenario 1: Check login status (or initiate login flow if not authenticated)*\n```json\n{}\n```\n\n*Scenario 2: Complete the login flow with the retrieved authorization code*\n```json\n{\n  \"authCode\": \"4/0ATsMZqD...\"\n}\n```\n\n*Scenario 3: Force reauthentication*\n```json\n{\n  \"reauth\": true\n}\n```\n",
   "humanReadableDescription": "Sign the user into the Firebase CLI and MCP server or check current authentication status.",
   "inputSchema": {
    "properties": {
     "authCode": {
      "description": "The authorization code generated after the user signs in via the login URL. Leave this field empty on the first call to initiate the login flow.",
      "type": "string"
     },
     "reauth": {
      "description": "Force reauthentication even if already logged in.",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "firebase_login"
  },
  {
   "_meta": {
    "feature": "firebase",
    "requiresAuth": false,
    "requiresProject": false
   },
   "description": "Use this to sign the user out of the Firebase CLI and Firebase MCP server.",
   "humanReadableDescription": "Sign the user out of the Firebase CLI and MCP server.",
   "inputSchema": {
    "properties": {
     "email": {
      "description": "The email of the account to log out. If not provided, all accounts will be logged out.",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "firebase_logout"
  },
  {
   "_meta": {
    "feature": "firebase"
   },
   "annotations": {
    "destructiveHint": false,
    "readOnlyHint": true,
    "title": "Read Firebase Resources"
   },
   "description": "Use this to read the contents of `firebase://` resources or list available resources",
   "humanReadableDescription": "Read the contents of internal firebase:// documentation resources or list all available resources.",
   "inputSchema": {
    "properties": {
     "uris": {
      "description": "list of resource uris to read. each must start with `firebase://` prefix. omit to list all available resources",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "type": "object"
   },
   "name": "firebase_read_resources"
  },
  {
   "_meta": {
    "feature": "firebase",
    "optionalProjectDir": true,
    "requiresAuth": false,
    "requiresProject": false
   },
   "annotations": {
    "readOnlyHint": false,
    "title": "Update Firebase Environment"
   },
   "description": "Use this to update environment config for the Firebase CLI and Firebase MCP server, such as project directory, active project, active user account, accept terms of service, and more. Use `firebase_get_environment` to see the currently configured environment.",
   "humanReadableDescription": "Update environment settings such as project directory, active project, or active user account.",
   "inputSchema": {
    "properties": {
     "active_project": {
      "description": "Change the active project for the current project directory. Should be a Firebase project ID or configured project alias.",
      "type": "string"
     },
     "active_user_account": {
      "description": "The email address of the signed-in user to authenticate as when interacting with the current project directory.",
      "type": "string"
     },
     "project_dir": {
      "description": "Change the current project directory - this should be a directory that has a `firebase.json` file (or will have one).",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "firebase_update_environment"
  }
 ],
 "version": "15.30.0"
}