{
 "args": [
  "mcp"
 ],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "@izagood/avcs",
 "protocol": "2025-06-18",
 "published": "2026-09-08T05:49:00.891Z",
 "tools": [
  {
   "description": "Record a reviewer verdict on a checkpoint, signed with the reviewer's local key. Requires role >= reviewer.",
   "inputSchema": {
    "properties": {
     "by": {
      "description": "reviewer actor id",
      "type": "string"
     },
     "checkpointOid": {
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "reason": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "verdict": {
      "enum": [
       "approve",
       "request_changes"
      ],
      "type": "string"
     }
    },
    "required": [
     "checkpointOid",
     "by"
    ],
    "type": "object"
   },
   "name": "avcs.approval.record"
  },
  {
   "description": "Who currently owns an entity (file:<path> or symbol:<path>#<name>) and WHY \u2014 the accepted head op with actor, intent title, and purpose. Stronger than git blame.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "entityKey": {
      "type": "string"
     },
     "line": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "entityKey"
    ],
    "type": "object"
   },
   "name": "avcs.blame"
  },
  {
   "description": "Per-line provenance: the op that last wrote each line, with actor, intent and purpose. Why THIS line is here.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "entityKey": {
      "type": "string"
     },
     "line": {
      "type": "string"
     },
     "path": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "entityKey",
     "path"
    ],
    "type": "object"
   },
   "name": "avcs.blame.lines"
  },
  {
   "description": "Freeze a verified (ops + policy + materializer + evidence) state vector for a view.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "summary": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "avcs.checkpoint.create"
  },
  {
   "description": "List the conflicts that require a human/owner decision in a view.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "avcs.conflict.list"
  },
  {
   "description": "Other actors' live concurrent ops and overlapping lease holders for given keys. Run while editing, so overlap surfaces before finalize.",
   "inputSchema": {
    "properties": {
     "acrossLines": {
      "description": "also check other lines/branches; each warning names the competing line",
      "type": "boolean"
     },
     "actor": {
      "description": "actor id for the perspective (with no keys: every key they authored on)",
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "keys": {
      "description": "entity keys to check, e.g. [\"file:src/a.ts\"]",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "line": {
      "description": "lineage to check on; default 'main'",
      "type": "string"
     },
     "sessionOid": {
      "description": "use this session's actor + authored keys as the perspective",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "avcs.contention.check"
  },
  {
   "description": "Assemble working context for a scope under a byte budget: provenance, decisions, risks, history. Deterministic truncation.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "entityKeys": {
      "description": "explicit keys, e.g. ['file:src/a.ts']",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "intentOid": {
      "description": "scope = the intent's allowed scopes plus what its sessions actually touched",
      "type": "string"
     },
     "maxBytes": {
      "description": "budget for the compact response; default 8192. Dropped sections are listed in budget.truncated.",
      "type": "number"
     },
     "paths": {
      "description": "file paths, resolved to file: keys",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "description": "default 'main'",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "avcs.context.build"
  },
  {
   "description": "Prior human decisions for a conflict key plus the policies they taught. Read precedent before re-deciding.",
   "inputSchema": {
    "properties": {
     "conflictKey": {
      "description": "entity key, e.g. 'file:src/a.ts'",
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "avcs.decision.recall"
  },
  {
   "description": "Record a human owner's conflict resolution. Owner-confirmed and signed with their local key; an agent cannot forge it.",
   "inputSchema": {
    "properties": {
     "actor": {
      "properties": {
       "id": {
        "type": "string"
       },
       "kind": {
        "enum": [
         "human",
         "ai_agent",
         "ci_bot"
        ],
        "type": "string"
       },
       "model": {
        "type": "string"
       }
      },
      "required": [
       "id"
      ],
      "type": "object"
     },
     "chosenOps": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "conflictId": {
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "futurePolicy": {
      "type": "string"
     },
     "reason": {
      "type": "string"
     },
     "rejectedOps": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "conflictId",
     "reason"
    ],
    "type": "object"
   },
   "name": "avcs.decision.record"
  },
  {
   "description": "Diff two views/lines. format 'paths' (default) or 'patch' (unified diff).",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "format": {
      "description": "'paths' (default, unchanged shape) or 'patch' for unified diffs",
      "enum": [
       "paths",
       "patch"
      ],
      "type": "string"
     },
     "path": {
      "description": "restrict a patch to this single path",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "viewA": {
      "type": "string"
     },
     "viewB": {
      "type": "string"
     }
    },
    "required": [
     "viewA",
     "viewB"
    ],
    "type": "object"
   },
   "name": "avcs.diff"
  },
  {
   "description": "Attach machine-checkable evidence (test/typecheck/lint/...) to operations. Behavior changes need a passing test to be accepted.",
   "inputSchema": {
    "properties": {
     "actor": {
      "properties": {
       "id": {
        "type": "string"
       },
       "kind": {
        "enum": [
         "human",
         "ai_agent",
         "ci_bot"
        ],
        "type": "string"
       },
       "model": {
        "type": "string"
       }
      },
      "required": [
       "id"
      ],
      "type": "object"
     },
     "command": {
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "detail": {
      "type": "string"
     },
     "forOps": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "kind": {
      "enum": [
       "parse",
       "typecheck",
       "lint",
       "unit_test",
       "integration_test",
       "benchmark",
       "security_scan",
       "api_compat"
      ],
      "type": "string"
     },
     "result": {
      "enum": [
       "pass",
       "fail",
       "partial",
       "not_run"
      ],
      "type": "string"
     },
     "treeHash": {
      "description": "the materialized treeHash this evidence was produced against (docs/16); binds the result to a specific tree",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "forOps",
     "kind",
     "result"
    ],
    "type": "object"
   },
   "name": "avcs.evidence.attach"
  },
  {
   "description": "Read-only review state for a view: protection rules, head, your role, and the effective approvals.",
   "inputSchema": {
    "properties": {
     "as": {
      "description": "actor id whose role to report",
      "type": "string"
     },
     "checkpointOid": {
      "description": "report approvals for this checkpoint instead of the head",
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "description": "default 'main'",
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "avcs.governance.status"
  },
  {
   "description": "How to use AVCS: the canonical loop, agent rules, tool index, error recovery. Call this first.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "topic": {
      "description": "omit for the canonical loop",
      "enum": [
       "workflow",
       "tools",
       "sync",
       "rules",
       "errors"
      ],
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "avcs.guide"
  },
  {
   "description": "History of one entity in causal order. Paged: limit (default 20) + cursor.",
   "inputSchema": {
    "properties": {
     "cursor": {
      "description": "opaque: the last op oid of the previous page; returns what follows it.",
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "entityKey": {
      "type": "string"
     },
     "limit": {
      "description": "max ops to return; default 20. A full page means there may be more.",
      "type": "number"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "entityKey"
    ],
    "type": "object"
   },
   "name": "avcs.history"
  },
  {
   "description": "Idempotent lookup of an integration ticket's recorded verdict on a remote hub (polling companion to avcs.integration.submit).",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "remote": {
      "description": "remote name or hub url; default 'origin'",
      "type": "string"
     },
     "ticketId": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "description": "default 'main'",
      "type": "string"
     }
    },
    "required": [
     "ticketId"
    ],
    "type": "object"
   },
   "name": "avcs.integration.status"
  },
  {
   "description": "Submit a checkpoint to the integration queue. Verdict is advanced, conflict, needs_evidence or queued \u2014 never 'pull and redo'.",
   "inputSchema": {
    "properties": {
     "actor": {
      "properties": {
       "id": {
        "type": "string"
       },
       "kind": {
        "enum": [
         "human",
         "ai_agent",
         "ci_bot"
        ],
        "type": "string"
       },
       "model": {
        "type": "string"
       }
      },
      "required": [
       "id"
      ],
      "type": "object"
     },
     "checkpoint": {
      "description": "draft checkpoint oid; created from the view when omitted",
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "message": {
      "description": "checkpoint summary when auto-creating",
      "type": "string"
     },
     "remote": {
      "description": "remote name or hub url; default 'origin'",
      "type": "string"
     },
     "ticketId": {
      "description": "resubmission: reuse the ticketId from the needs_evidence verdict",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "description": "default 'main'",
      "type": "string"
     }
    },
    "required": [
     "actor"
    ],
    "type": "object"
   },
   "name": "avcs.integration.submit"
  },
  {
   "description": "Open an intent: the goal + constraints + allowed scopes for a unit of work. Agents must work within an intent.",
   "inputSchema": {
    "properties": {
     "allowedScopes": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "constraints": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "kind": {
      "enum": [
       "feature",
       "bugfix",
       "refactor",
       "formatting",
       "generated"
      ],
      "type": "string"
     },
     "owner": {
      "description": "actor id, usually a human",
      "type": "string"
     },
     "priority": {
      "enum": [
       "low",
       "normal",
       "high",
       "critical"
      ],
      "type": "string"
     },
     "successCriteria": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "title": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "title",
     "owner"
    ],
    "type": "object"
   },
   "name": "avcs.intent.create"
  },
  {
   "description": "List intents in the repo. Bounded: limit (default 50).",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "limit": {
      "description": "max intents to return; default 50",
      "type": "number"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "avcs.intent.list"
  },
  {
   "description": "Read an intent (goal, constraints, allowed scopes). An agent should read this BEFORE proposing operations so it works within the constraints.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "intentOid": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "intentOid"
    ],
    "type": "object"
   },
   "name": "avcs.intent.read"
  },
  {
   "description": "Which actors this machine can sign as, and which public keys the repo trusts. Ids only, never key material.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "avcs.key.list"
  },
  {
   "description": "Mint a local signing key for an actor so its decisions and hub writes can be signed. Idempotent.",
   "inputSchema": {
    "properties": {
     "actor": {
      "properties": {
       "id": {
        "type": "string"
       },
       "kind": {
        "enum": [
         "human",
         "ai_agent",
         "ci_bot"
        ],
        "type": "string"
       },
       "model": {
        "type": "string"
       }
      },
      "required": [
       "id"
      ],
      "type": "object"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "actor"
    ],
    "type": "object"
   },
   "name": "avcs.key.provision"
  },
  {
   "description": "Request a soft write-lease over entity scopes BEFORE editing, to avoid duplicating another agent's in-flight work. Returns the lease oid, or the conflicting holders.",
   "inputSchema": {
    "properties": {
     "actor": {
      "properties": {
       "id": {
        "type": "string"
       },
       "kind": {
        "enum": [
         "human",
         "ai_agent",
         "ci_bot"
        ],
        "type": "string"
       },
       "model": {
        "type": "string"
       }
      },
      "required": [
       "id"
      ],
      "type": "object"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "intentOid": {
      "type": "string"
     },
     "mode": {
      "enum": [
       "exclusive",
       "shared"
      ],
      "type": "string"
     },
     "sessionOid": {
      "type": "string"
     },
     "ttlMs": {
      "type": "number"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "writeScopes": {
      "description": "e.g. ['symbol:mod.ts#alpha','file:a.ts']",
      "items": {
       "type": "string"
      },
      "type": "array"
     }
    },
    "required": [
     "intentOid",
     "sessionOid",
     "actor",
     "writeScopes"
    ],
    "type": "object"
   },
   "name": "avcs.lease.request"
  },
  {
   "description": "Fork a long-lived line (e.g. 'v1.x') from another at its current state. It inherits history to the fork, then diverges.",
   "inputSchema": {
    "properties": {
     "atCheckpointOid": {
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "fromLine": {
      "type": "string"
     },
     "name": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "name"
    ],
    "type": "object"
   },
   "name": "avcs.line.create"
  },
  {
   "description": "List the lineage lines in the repo (besides the implicit 'main').",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "avcs.line.list"
  },
  {
   "description": "In-process metrics snapshot for this server (reduce cache hit/miss, reduce.ms timing, materialize.calls).",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "avcs.metrics"
  },
  {
   "description": "Read an object by oid. Blobs return decoded content (utf8 text or base64); other types return the structured object.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "lines": {
      "description": "1-based inclusive line range of a text blob to return instead of the whole thing",
      "properties": {
       "end": {
        "type": "number"
       },
       "start": {
        "type": "number"
       }
      },
      "type": "object"
     },
     "maxBytes": {
      "description": "cap on returned blob content; default 65536. `bytes` always reports the FULL size.",
      "type": "number"
     },
     "oid": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "oid"
    ],
    "type": "object"
   },
   "name": "avcs.object.show"
  },
  {
   "description": "Cherry-pick an operation onto another line: mints a new op carrying the change, with derivedFrom provenance. Source line untouched.",
   "inputSchema": {
    "properties": {
     "actor": {
      "properties": {
       "id": {
        "type": "string"
       },
       "kind": {
        "enum": [
         "human",
         "ai_agent",
         "ci_bot"
        ],
        "type": "string"
       },
       "model": {
        "type": "string"
       }
      },
      "required": [
       "id"
      ],
      "type": "object"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "sourceOpOid": {
      "type": "string"
     },
     "targetLine": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "sourceOpOid",
     "targetLine"
    ],
    "type": "object"
   },
   "name": "avcs.operation.backport"
  },
  {
   "description": "Propose a semantic change (MVP: file writes). Declare effects honestly \u2014 policy gates on them. baseText/baseBlobOid authors a 3-way-mergeable edit_file.",
   "inputSchema": {
    "properties": {
     "actor": {
      "properties": {
       "id": {
        "type": "string"
       },
       "kind": {
        "enum": [
         "human",
         "ai_agent",
         "ci_bot"
        ],
        "type": "string"
       },
       "model": {
        "type": "string"
       }
      },
      "required": [
       "id"
      ],
      "type": "object"
     },
     "baseBlobOid": {
      "description": "oid of the base blob (alternative to baseText); fetch it via avcs.object.show",
      "type": "string"
     },
     "baseText": {
      "description": "the base content this edit was derived from; its presence routes to a 3-way-mergeable edit_file",
      "type": "string"
     },
     "causalDeps": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "content": {
      "description": "the FULL new file content (used for both put_file and edit_file)",
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "declaredPurpose": {
      "type": "string"
     },
     "effects": {
      "properties": {
       "breaksPublicApi": {
        "type": "boolean"
       },
       "changesBehavior": {
        "type": "boolean"
       },
       "reads": {
        "items": {
         "type": "string"
        },
        "type": "array"
       }
      },
      "type": "object"
     },
     "intentOid": {
      "type": "string"
     },
     "line": {
      "description": "lineage to author on; default 'main' (Phase 8)",
      "type": "string"
     },
     "path": {
      "type": "string"
     },
     "sessionOid": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "warnContention": {
      "description": "also run an early conflict check on the op's keys (Phase 15.3): response becomes { oid, contentionWarnings } listing other actors' live concurrent ops + overlapping lease holders. Omit for the plain oid response.",
      "type": "boolean"
     },
     "workspace": {
      "description": "isolate this op to a build/verify workspace (docs/16); a base view excludes it until the workspace is landed via avcs.workspace.land",
      "type": "string"
     }
    },
    "required": [
     "sessionOid",
     "intentOid",
     "actor",
     "path",
     "content",
     "declaredPurpose"
    ],
    "type": "object"
   },
   "name": "avcs.operation.propose"
  },
  {
   "description": "Cut a release: a conflict-free checkpoint plus evidence, SBOM and artifact references. Refuses if the view has open conflicts.",
   "inputSchema": {
    "properties": {
     "artifacts": {
      "items": {
       "properties": {
        "digest": {
         "type": "string"
        },
        "ref": {
         "type": "string"
        },
        "type": {
         "type": "string"
        }
       },
       "required": [
        "type",
        "ref"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "signedBy": {
      "description": "actor ids signing off",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "summary": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "avcs.release.cut"
  },
  {
   "description": "Minimal repair packet for ops whose validation failed: failing output, related decisions, a fix instruction. Cheaper than re-reading the repo.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "ops": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "ops"
    ],
    "type": "object"
   },
   "name": "avcs.repair.context"
  },
  {
   "description": "Begin a work session for an agent/human against an intent. Returns a session id used on every operation.",
   "inputSchema": {
    "properties": {
     "actor": {
      "properties": {
       "id": {
        "type": "string"
       },
       "kind": {
        "enum": [
         "human",
         "ai_agent",
         "ci_bot"
        ],
        "type": "string"
       },
       "model": {
        "type": "string"
       }
      },
      "required": [
       "id"
      ],
      "type": "object"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "intentOid": {
      "type": "string"
     },
     "openedEntities": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "summary": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "intentOid",
     "actor"
    ],
    "type": "object"
   },
   "name": "avcs.session.start"
  },
  {
   "description": "Land work on a protected head in one call: push, merge-check, checkpoint, integrate. Result is landed or a conflict packet.",
   "inputSchema": {
    "properties": {
     "by": {
      "description": "actor id landing the work",
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "hub": {
      "description": "remote name or hub URL; omit for the persisted 'origin', or a local-only repo",
      "type": "string"
     },
     "maxAttempts": {
      "description": "bounded contention retries; default 5. Conflicts are never retried.",
      "type": "number"
     },
     "summary": {
      "description": "checkpoint summary",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "description": "default 'main'",
      "type": "string"
     },
     "workspace": {
      "description": "land this workspace onto the base line first",
      "type": "string"
     }
    },
    "required": [
     "by"
    ],
    "type": "object"
   },
   "name": "avcs.sync.land"
  },
  {
   "description": "Pull objects from the hub (conflict-free gossip). dryRun reports what would arrive without importing.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "dryRun": {
      "description": "report the would-pull count and head comparison, import nothing",
      "type": "boolean"
     },
     "hub": {
      "description": "remote name or hub URL; default the persisted 'origin'",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "avcs.sync.pull"
  },
  {
   "description": "Push local objects to the hub. Returns how many were accepted and how many the gate rejected.",
   "inputSchema": {
    "properties": {
     "as": {
      "description": "actor id whose key signs the writes",
      "type": "string"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "hub": {
      "description": "remote name or hub URL; default the persisted 'origin'",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "avcs.sync.push"
  },
  {
   "description": "Run validation commands against a view and attach treeHash-bound evidence. Pass dir to reuse an existing build environment.",
   "inputSchema": {
    "properties": {
     "checks": {
      "items": {
       "properties": {
        "command": {
         "type": "string"
        },
        "kind": {
         "type": "string"
        }
       },
       "required": [
        "kind",
        "command"
       ],
       "type": "object"
      },
      "type": "array"
     },
     "ciActor": {
      "properties": {
       "id": {
        "type": "string"
       },
       "kind": {
        "enum": [
         "human",
         "ai_agent",
         "ci_bot"
        ],
        "type": "string"
       },
       "model": {
        "type": "string"
       }
      },
      "required": [
       "id"
      ],
      "type": "object"
     },
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "dir": {
      "description": "directory to run checks in; defaults to a fresh isolated temp dir. Pass a dir that already holds the build env (e.g. the working tree) to avoid reinstalling (issue #11)",
      "type": "string"
     },
     "ops": {
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "project": {
      "description": "materialize the view into `dir` before running; defaults true for a temp dir, false when `dir` is given (run in place)",
      "type": "boolean"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "type": "string"
     },
     "workspace": {
      "description": "validate a workspace view (docs/16): base + that workspace's isolated ops",
      "type": "string"
     }
    },
    "required": [
     "ops",
     "ciActor",
     "checks"
    ],
    "type": "object"
   },
   "name": "avcs.validate.run"
  },
  {
   "description": "Reduce a view's operation graph into a tree, per-op status and open conflicts \u2014 how an agent checks that its work merges.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "filesLimit": {
      "description": "max paths to list; default 500. treeHash/status/conflicts/dropped are never bounded.",
      "type": "number"
     },
     "includeStatuses": {
      "description": "op statuses to project into the tree; default ['accepted']",
      "items": {
       "enum": [
        "proposed",
        "validating",
        "accepted",
        "rejected",
        "superseded",
        "needs_decision",
        "quarantined"
       ],
       "type": "string"
      },
      "type": "array"
     },
     "pathsOnlyUnder": {
      "description": "list only paths under this prefix (e.g. 'src/')",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "type": "string"
     }
    },
    "type": "object"
   },
   "name": "avcs.view.materialize"
  },
  {
   "description": "Land a workspace onto its base line: its isolated ops join the base view and merge there. Idempotent.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "name": {
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "required": [
     "name"
    ],
    "type": "object"
   },
   "name": "avcs.workspace.land"
  },
  {
   "description": "List this repo's workspaces: `workspaces` names them all, `landed` the subset joined onto the base view. The rest stay isolated.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     }
    },
    "type": "object"
   },
   "name": "avcs.workspace.list"
  },
  {
   "description": "Write a view to a directory on disk, so build/test loops outside validate.run need no CLI.",
   "inputSchema": {
    "properties": {
     "cwd": {
      "description": "Absolute path of the working directory to act on; the server resolves it upward to the owning AVCS repo (.avcs). Optional \u2014 defaults to the client's workspace root, then the server's own cwd. Ignored when the server is pinned via AVCS_REPO.",
      "type": "string"
     },
     "name": {
      "description": "project this workspace's isolated ops too",
      "type": "string"
     },
     "out": {
      "description": "absolute target directory",
      "type": "string"
     },
     "verbose": {
      "description": "Pretty-print the response for human reading. Default false (compact, fewer tokens).",
      "type": "boolean"
     },
     "view": {
      "description": "default 'main'",
      "type": "string"
     }
    },
    "required": [
     "out"
    ],
    "type": "object"
   },
   "name": "avcs.workspace.project"
  }
 ],
 "version": "0.51.0"
}