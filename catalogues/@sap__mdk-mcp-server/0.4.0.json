{
 "args": [],
 "measured_at": "2026-09-22T19:16:13+00:00",
 "package": "@sap/mdk-mcp-server",
 "protocol": "2025-06-18",
 "published": "2026-07-29T03:08:20.383Z",
 "tools": [
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": false,
    "title": "Create MDK Project"
   },
   "description": "Creates MDK projects or entity metadata using templates (CRUD, List Detail, Base). Use this for initializing new projects or adding entity metadata to existing projects. Supports CAP projects - automatically creates MDK apps in the app/ folder with proper naming and configuration.",
   "inputSchema": {
    "properties": {
     "cfOrg": {
      "description": "Optional: Cloud Foundry organization name. If provided along with cfSpace, will be used to configure the deployment target.",
      "type": "string"
     },
     "cfSpace": {
      "description": "Optional: Cloud Foundry space name. If provided along with cfOrg, will be used to configure the deployment target.",
      "type": "string"
     },
     "folderRootPath": {
      "description": "The path of the current project root folder. For CAP projects, provide the CAP project root - the MDK app will be created in app/<projectname>_mdk/.",
      "type": "string"
     },
     "oDataEntitySets": {
      "description": "The OData entity sets relevant to the user prompt, separated by commas.",
      "type": "string"
     },
     "offline": {
      "default": false,
      "description": "Whether to generate the project in offline mode (only applicable for project scope). Set to false unless offline is explicitly specified.",
      "type": "boolean"
     },
     "scope": {
      "description": "The scope of creation:\n\u2022 project: Initialize a new MDK project with full structure\n\u2022 entity: Add entity metadata to an existing project",
      "enum": [
       "project",
       "entity"
      ],
      "type": "string"
     },
     "templateType": {
      "description": "The type of template to use. Note: 'base' template is only valid for project scope.",
      "enum": [
       "crud",
       "list detail",
       "base"
      ],
      "type": "string"
     }
    },
    "required": [
     "folderRootPath",
     "scope",
     "templateType",
     "oDataEntitySets"
    ],
    "type": "object"
   },
   "name": "mdk-create"
  },
  {
   "annotations": {
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
    "title": "Search MDK Documentation"
   },
   "description": "Unified tool for accessing MDK documentation including search, component schemas, property details, and examples.",
   "inputSchema": {
    "properties": {
     "N": {
      "default": 5,
      "description": "Number of results to return for search operation.",
      "type": "number"
     },
     "component_name": {
      "description": "Name of the component (required for 'component', 'property', and 'example' operations).",
      "type": "string"
     },
     "folderRootPath": {
      "description": "The path of the current project root folder. Used to determine the appropriate MDK schema version.",
      "type": "string"
     },
     "operation": {
      "description": "The type of documentation operation to perform:\n\u2022 search: Returns the top N results from MDK documentation by semantic search, sorted by relevance\n\u2022 component: Returns the schema of an MDK component based on the name of the component\n\u2022 property: Returns the documentation of a specific property of an MDK component\n\u2022 example: Returns an example usage of an MDK component\n\u2022 search-samples: Search through MDK tutorial samples and code examples from GitHub",
      "enum": [
       "search",
       "component",
       "property",
       "example",
       "search-samples"
      ],
      "type": "string"
     },
     "property_name": {
      "description": "Name of the property (required for 'property' operation).",
      "type": "string"
     },
     "query": {
      "description": "Search query string (required for 'search' and 'search-samples' operations).",
      "type": "string"
     }
    },
    "required": [
     "operation",
     "folderRootPath"
    ],
    "type": "object"
   },
   "name": "mdk-docs"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": true,
    "title": "Fetch and Save Mobile Services Metadata"
   },
   "description": "Fetches OData metadata from a Mobile Services destination and automatically saves it to .service.metadata file in the project. This retrieves the $metadata document from a configured destination in a Mobile Services application and creates the service metadata configuration file. Requires CF CLI authentication (cf login).",
   "inputSchema": {
    "properties": {
     "appId": {
      "description": "The Mobile Services application ID (e.g., 'myapp.mdk.demo').",
      "type": "string"
     },
     "destination": {
      "description": "The destination name configured in Mobile Services (e.g., 'IncidentManagement').",
      "type": "string"
     },
     "folderRootPath": {
      "description": "The path of the MDK project root folder where .service.metadata will be saved.",
      "type": "string"
     },
     "landscapeType": {
      "default": "Standard",
      "description": "The Mobile Services landscape type. Use 'Standard' for production environments (default) or 'Preview' for preview/test environments.",
      "enum": [
       "Standard",
       "Preview"
      ],
      "type": "string"
     },
     "pathSuffix": {
      "default": "",
      "description": "Optional path suffix to append before /$metadata (e.g., '/api/v1'). Leave empty if not needed.",
      "type": "string"
     }
    },
    "required": [
     "folderRootPath",
     "appId",
     "destination"
    ],
    "type": "object"
   },
   "name": "mdk-fetch-mobile-metadata"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": false,
    "title": "Generate MDK Artifacts"
   },
   "description": "Generates MDK artifacts including pages, actions, i18n files, and rule references. Returns prompts for LLM processing (pages, actions, i18n) or searches for rule examples.",
   "inputSchema": {
    "properties": {
     "actionType": {
      "description": "The type of action (required when artifactType is 'action').",
      "enum": [
       "CreateODataEntity",
       "UpdateODataEntity",
       "DeleteODataEntity",
       "CreateODataMedia",
       "InitializeOfflineOData",
       "DownloadOfflineOData",
       "UploadOfflineOData",
       "CancelDownloadOfflineOData",
       "CancelUploadOfflineOData",
       "ClearOfflineOData",
       "CloseOfflineOData",
       "CreateODataRelatedEntity",
       "CreateODataRelatedMedia",
       "CreateODataService",
       "DeleteODataMedia",
       "DownloadMediaOData",
       "LogMessage",
       "Message",
       "Navigation",
       "OpenODataService",
       "ProgressBanner",
       "PushNotificationRegister",
       "PushNotificationUnregister",
       "ReadODataService",
       "RemoveDefiningRequest",
       "SendRequest",
       "SetLevel",
       "SetState",
       "ToastMessage",
       "UndoPendingChanges",
       "UploadLog",
       "UploadODataMedia",
       "UploadStreamOData",
       "ChatCompletion",
       "PopoverMenu",
       "CheckRequiredFields",
       "ChangeSet",
       "OpenDocument",
       "Banner",
       "Filter"
      ],
      "type": "string"
     },
     "artifactType": {
      "description": "The type of artifact to generate:\n\u2022 page: Generate MDK page files (.page) with databinding or layout\n\u2022 action: Generate MDK action files (.action)\n\u2022 i18n: Generate internationalization files (.properties)\n\u2022 rule: Search for and return relevant JavaScript rule examples",
      "enum": [
       "page",
       "action",
       "i18n",
       "rule"
      ],
      "type": "string"
     },
     "controlType": {
      "description": "The control type for databinding pages (required when pageType is 'databinding').",
      "enum": [
       "ObjectTable",
       "FormCell",
       "KeyValue",
       "ObjectHeader",
       "ContactTable",
       "SimplePropertyCollection",
       "ObjectCard",
       "DataTable",
       "KPIHeader",
       "ProfileHeader",
       "ObjectCollection",
       "Timeline",
       "TimelinePreview",
       "Calendar"
      ],
      "type": "string"
     },
     "folderRootPath": {
      "description": "The path of the current project root folder (not required for rule artifact type).",
      "type": "string"
     },
     "layoutType": {
      "description": "The layout type for layout pages (required when pageType is 'layout').",
      "enum": [
       "Section",
       "BottomNavigation",
       "FlexibleColumnLayout",
       "SideDrawerNavigation",
       "Tabs",
       "Extension"
      ],
      "type": "string"
     },
     "oDataEntitySets": {
      "description": "Optional: The OData entity sets to use for page/action generation, separated by commas (required only when artifactType is 'action' or artifactType is 'page' and pageType is 'databinding').",
      "type": "string"
     },
     "pageType": {
      "description": "The type of page (required when artifactType is 'page'):\n\u2022 databinding: Data-driven pages with controls bound to OData\n\u2022 layout: Structure-focused pages with specific layouts",
      "enum": [
       "databinding",
       "layout"
      ],
      "type": "string"
     },
     "query": {
      "description": "Search query for rule reference (required only when artifactType is 'rule'). Examples: 'get app name', 'handle form validation', 'navigate to page', etc.",
      "type": "string"
     }
    },
    "required": [
     "artifactType"
    ],
    "type": "object"
   },
   "name": "mdk-gen"
  },
  {
   "annotations": {
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": true,
    "title": "Manage MDK Project"
   },
   "description": "Comprehensive MDK project management tool that handles build, deploy, validate, migrate, show QR code, and mobile app editor operations.",
   "inputSchema": {
    "properties": {
     "externals": {
      "default": [],
      "description": "Optional: Array of external package names to include in the deployment (e.g., ['@nativescript/geolocation']). Defaults to empty array if not specified.",
      "items": {
       "type": "string"
      },
      "type": "array"
     },
     "folderRootPath": {
      "description": "The path of the current project root folder.",
      "type": "string"
     },
     "operation": {
      "description": "The operation to perform on the MDK project. Available operations:\n\u2022 build: Build an MDK project\n\u2022 deploy: Deploy an MDK project to the Mobile Services\n\u2022 validate: Validate an MDK project\n\u2022 migrate: Migrate an MDK project to the latest MDK version\n\u2022 show-qrcode: Show QR code for an MDK project\n\u2022 open-mobile-app-editor: Instruct how to open the Mobile App Editor to create .service.metadata file",
      "enum": [
       "build",
       "deploy",
       "validate",
       "migrate",
       "show-qrcode",
       "open-mobile-app-editor"
      ],
      "type": "string"
     }
    },
    "required": [
     "folderRootPath",
     "operation"
    ],
    "type": "object"
   },
   "name": "mdk-manage"
  }
 ],
 "version": "0.4.0"
}