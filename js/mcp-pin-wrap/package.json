{
  "name": "@rufat325/mcp-pin",
  "version": "0.1.5",
  "description": "npx shim for mcp-pin wrap. The Python wheel is the kernel; this finds it and runs it. Not the npm package named mcp-pin.",
  "license": "Apache-2.0",
  "bin": {
    "mcp-pin": "./bin.js"
  },
  "files": ["bin.js"],
  "engines": { "node": ">=18" },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rufat325/mcp-pin.git",
    "directory": "js/mcp-pin-wrap"
  },
  "keywords": ["mcp", "lockfile", "security"]
}
