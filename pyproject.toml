[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "heldfast"
version = "0.2.0"
description = "Zero-dependency tool-integrity pin for MCP servers and agent skills: approve once, refuse what changed"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
authors = [{ name = "Rufat Mahmudov" }]
keywords = ["mcp", "security", "ai-agents", "supply-chain", "sarif"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Intended Audience :: System Administrators",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Topic :: Security",
]
# No runtime dependencies. This is deliberate: a supply-chain scanner that
# pulls in a dependency tree is asking you to trust the thing it is auditing.
dependencies = []

[project.optional-dependencies]
# The MCPA018 semantic tier only. Keeping it an extra means the scanner every
# user installs stays dependency-free, and only those who opt into sending
# text to an API take on the SDK.
llm = ["anthropic>=1.0"]

[project.urls]
Homepage = "https://github.com/rufat325/heldfast"
Repository = "https://github.com/rufat325/heldfast"
Issues = "https://github.com/rufat325/heldfast/issues"

[project.scripts]
heldfast = "heldfast.cli:main"
# The name this was published under until 0.1.8. Kept so every command,
# script and hook written for it keeps working.
mcp-pin = "heldfast.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/heldfast"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
